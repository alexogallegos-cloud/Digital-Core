# GemCog — no-entregables. Regla: los entregables son .md, nunca .txt.

# Respaldos automáticos
*.bak
*.bak2
*.bak-*
*.html.bak-*

# Índices/dumps de trabajo del tooling de vocab (regenerables) — NO entregables
vocab-index-all.txt

# Snapshots empaquetados (se regeneran con fecha)
*.zip

# Nota: source/ contiene el código COBOL/ALGOL/DASDL raw en .txt.
# Es insumo Capa 0 (no entregable) y no se versiona; permanece local.
