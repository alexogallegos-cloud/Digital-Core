# SP Specs — D01 · `bdicnweb` · Canal Digital Web

> Generado por `build-sp-specs.py` · Grounding Pass v1.0
> Objetivo: verificar el conocimiento del Gemelo Cognitivo contra el código fuente real
> **Convención:** solo se analiza el PRIMER `CREATE PROCEDURE` de cada archivo
> (los siguientes son dependencias concatenadas — ver memoria del proyecto)

## Resumen de validación del dominio

| Métrica | Valor |
|---------|-------|
| SPs analizados | **2184** |
| Presentes en callgraph | 2121 |
| SPs aislados (⚠️ no estaban en el análisis previo) | 63 |
| Propósito **VERIFICADO** | 971 |
| Propósito **PARCIAL** | 1203 |
| Propósito **NO_VERIFICABLE** | 10 |
| SPs con tokens **SINTÉTICOS** detectados | 1480 |

> Los **63 SPs aislados** no aparecen en el callgraph y por tanto
> **no fueron incluidos en el análisis de journeys, reglas ni vocabulario previo**.
> Este grounding pass los analiza por primera vez directamente desde el código.

---

## `eliminasolicusuariomc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_eliminasolicusuariomc.sql` |
| **LOC (1er CREATE)** | 47 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 111 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina solicitud, usuario y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE eliminasolicusuariomc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecutivo                   CHAR(10)
) RETURNING CHAR(5) AS COD_RET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecutivo` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `cCodRet` | `CHAR(6)` | L10 |
| `cMensajeRet` | `CHAR(80)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | SELECT | L35 |
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | DELETE | L35 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet       = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `inserta_img_previo_soc2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_inserta_img_previo_soc2.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta Sistema Operativo Central" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE inserta_img_previo_soc2(
  pempresa                     char(3)
  pcliente                     char(20)
  pcod_docto                   char(4)
  pimg_formato                 char(3)
  pusuario                     char(8)
) RETURNING char(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pempresa` | `char(3)` | — | — |
| `pcliente` | `char(20)` | — | — |
| `pcod_docto` | `char(4)` | — | — |
| `pimg_formato` | `char(3)` | — | — |
| `pusuario` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_codret` | `char(5)` | L11 |
| `v_secuencia` | `smallint` | L12 |
| `v_fecha` | `date` | L13 |
| `v_hora` | `char(10)` | L14 |
| `siSecuenciaHist` | `smallint` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `dg_expediente_img1` | `bdidigital` | ⚠️ sí | SELECT | L57 |
| `dg_expediente_img1` | `bdidigital` | ⚠️ sí | INSERT | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | VALIDACIÓN_NULL | `IF pempresa is null or` |  |
| L77 | VALIDACIÓN_NULL | `IF v_secuencia is null then` |  |
| L80 | FÓRMULA | `LET v_secuencia = v_secuencia +1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?_img_p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?io_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_img_p`, `?io_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_abm_canal_cobro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_abm_canal_cobro.sql` |
| **LOC (1er CREATE)** | 183 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cobro canal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bitacora`, `sp_generararchivo_rst` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_abm_canal_cobro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pid_canal_final              VARCHAR(2)
  pcve_canal_final             VARCHAR(30)
  pnombre_canal                VARCHAR(30)
  pcobrar_otp                  VARCHAR(1)
  pBandera                     integer
  pIp                          VARCHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pid_canal_final` | `VARCHAR(2)` | `canal`=canal (de distribución) | ✅ CÓDIGO |
| `pcve_canal_final` | `VARCHAR(30)` | `canal`=canal (de distribución) | ✅ CÓDIGO |
| `pnombre_canal` | `VARCHAR(30)` | `canal`=canal (de distribución) | ✅ CÓDIGO |
| `pcobrar_otp` | `VARCHAR(1)` | — | — |
| `pBandera` | `integer` | — | — |
| `pIp` | `VARCHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `pcodigoStatus` | `CHAR(2)` | L9 |
| `pdescripcionstatus` | `CHAR(20)` | L10 |
| `calta_fecha` | `DATE` | L11 |
| `cultima_mod_fecha` | `DATE` | L12 |
| `cCodigoStatusExistente` | `CHAR(2)` | L13 |
| `iExiste` | `INTEGER` | L14 |
| `lastIdCanalFinal` | `CHAR(2)` | L15 |
| `cAccion` | `CHAR(50)` | L16 |
| `cEntidad` | `CHAR(100)` | L17 |
| `cCmd1` | `CHAR(2000)` | L18 |
| `cArchivoRespaldo` | `CHAR(50)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `par_canal_final` | `bdirst` | ⚠️ sí | SELECT | L57 |
| `par_canal_final` | `bdirst` | ⚠️ sí | INSERT | L81 |
| `systables` | `bdicnweb` | no | SELECT | L96 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_bitacora` | `bdicnweb` | no | L69 |
| `sp_generararchivo_rst` | `bdicnweb` | no | L100 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pcve_canal_final = '' OR pnombre_canal= ''  OR pBandera IS NU` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | FÓRMULA | `LET lastIdCanalFinal = lastIdCanalFinal + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L96 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L141 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00862';` |  |
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_abm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `canal` | ENTIDAD | canal (de distribución) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cobro` | ACCION | cobro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_abm_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_abono_ref`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_abono_ref.sql` |
| **LOC (1er CREATE)** | 139 |
| **Callgraph** | ✅ fan_in=3 / fan_out=3 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "abono" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `abono_ref` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_abono_ref(
  pUsuario                     char(10)
  pIdFuncion                   char(10)
  pTransacc                    CHAR(4)
  pCuenta                      CHAR(20)
  pDocto                       INTEGER
  pMto_tot                     MONEY(14,2)
  pReferencia                  CHAR(40)
) RETURNING CHAR(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(10)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pTransacc` | `CHAR(4)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pDocto` | `INTEGER` | — | — |
| `pMto_tot` | `MONEY(14,2)` | — | — |
| `pReferencia` | `CHAR(40)` | `ref`=referencia | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L13 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cSucursal` | `CHAR(4)` | L16 |
| `cUsuario` | `CHAR(8)` | L17 |
| `cTransuc` | `CHAR(4)` | L18 |
| `cFolio_suc` | `CHAR(16)` | L19 |
| `mMto_firme` | `MONEY(14,2)` | L20 |
| `mMto_sbc` | `MONEY(14,2)` | L21 |
| `mMto_rem` | `MONEY(14,2)` | L22 |
| `sDias_ret` | `SMALLINT` | L23 |
| `cDivisa` | `CHAR(2)` | L24 |
| `cNum_tarjeta` | `CHAR(16)` | L25 |
| `cUsuautoriza` | `CHAR(8)` | L26 |
| `dHora` | `DATETIME HOUR TO SECOND` | L28 |
| `cHora` | `CHAR(6)` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L89 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L94 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `abono_ref` | `bdicheq` | ⚠️ sí | L100 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `abono` | ENTIDAD | abono / crédito | 🔵 CONVENCIÓN | nombre_sp |
| `ref` | AMBIGUO | referencia | 🟡 INFERIDO | nombre_sp |

---

## `sp_abono_ref_masivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_abono_ref_masivo.sql` |
| **LOC (1er CREATE)** | 231 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "abono (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_abono_ref`, `sp_totalesdepositocap`, `sp_registra_evento` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_abono_ref_masivo(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             char(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `char(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iExiste` | `INT` | L7 |
| `cTransaccion` | `CHAR(4)` | L8 |
| `iDoctoCheque` | `INT` | L9 |
| `mImporte` | `money(14,2)` | L10 |
| `cReferencia` | `CHAR(40)` | L11 |
| `iRegistro` | `INT` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |
| `cFolio` | `CHAR(16)` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `cResultado` | `CHAR(15)` | L16 |
| `iRegistrosExito` | `INT` | L17 |
| `iRegistrosError` | `INT` | L18 |
| `dFechaProceso` | `DATETIME YEAR TO FRACTION(3)` | L19 |
| `cMotivoRechazo` | `char(100)` | L20 |
| `iRegProc` | `int` | L21 |
| `iTransaccionPrev` | `int` | L22 |
| `iTransaccion` | `int` | L23 |
| `cFechaCargaLote` | `date` | L24 |
| `iTotalRegsLote` | `int` | L25 |
| `mMontoLote` | `money(14,2)` | L26 |
| `iRegsAceptadosLote` | `int` | L27 |
| `iRegsRechazoLote` | `int` | L28 |
| `cArchivo` | `char(150)` | L29 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_deposito` | `bdicnweb` | no | SELECT | L88 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L134 |
| `sw_tr_cargamasiva_deposito_hist` | `bdicnweb` | no | INSERT | L149 |
| `sw_tr_cargamasiva_deposito` | `bdicnweb` | no | UPDATE | L177 |
| `sw_tr_cargamasiva_deposito` | `bdicnweb` | no | DELETE | L184 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_abono_ref` | `bdicnweb` | no | L115 |
| `sp_totalesdepositocap` | `bdicnweb` | no | L159 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L190 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | FÓRMULA | `LET iRegistrosError = iRegistrosError - 1;` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00002';` |  |
| L121 | FÓRMULA | `LET iRegistrosError = iRegistrosError + 1;` |  |
| L127 | FÓRMULA | `LET iRegistrosExito = iRegistrosExito + 1;` |  |
| L153 | FÓRMULA | `LET iRegProc = iRegProc + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `abono` | ENTIDAD | abono / crédito | 🔵 CONVENCIÓN | nombre_sp |
| `ref` | AMBIGUO | referencia | 🟡 INFERIDO | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_abonotransfersoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_abonotransfersoc.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ✅ fan_in=3 / fan_out=1 |
| **Propósito inferido** | "abono, transferencia y Sistema Operativo Central" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `abono_ref` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_abonotransfersoc(
  pSucursal                    CHAR(4)
  pUsuario                     CHAR(8)
  pTransacc                    CHAR(4)
  pCuenta                      CHAR(20)
  pCheque                      INTEGER
  pMonto                       DECIMAL(14,2)
  pReferencia                  CHAR(40)
  pTarjeta                     CHAR(16)
) RETURNING CHAR(5), CHAR(40)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSucursal` | `CHAR(4)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pTransacc` | `CHAR(4)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pCheque` | `INTEGER` | — | — |
| `pMonto` | `DECIMAL(14,2)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pTarjeta` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L11 |
| `cCodRet2` | `CHAR(5)` | L12 |
| `cCodRet3` | `CHAR(40)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iSamErr` | `INTEGER` | L15 |
| `cDesErr` | `CHAR(50)` | L16 |
| `cProceso` | `CHAR(10)` | L17 |
| `cStatusTar` | `CHAR(1)` | L18 |
| `cHora` | `CHAR(15)` | L19 |
| `cFolio` | `CHAR(16)` | L20 |
| `cCodRet4` | `CHAR(5)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_trxtrfabonosoc` | `bdicheq` | ⚠️ sí | SELECT | L69 |
| `sc_tarjeta` | `bdicheq` | ⚠️ sí | SELECT | L82 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `abono_ref` | `bdicheq` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | VALIDACIÓN_NULL | `IF ( pMonto     is null OR pMonto <= 0.00 ) OR` |  |
| L72 | VALIDACIÓN_NULL | `IF cProceso is null OR cProceso = '' THEN` |  |
| L79 | VALIDACIÓN_NULL | `IF pCuenta is null OR pCuenta = '' THEN` |  |
| L86 | VALIDACIÓN_NULL | `IF pCuenta is null OR cStatusTar is null OR cStatusTar <> 'A' THEN` |  |
| L94 | VALIDACIÓN_NULL | `IF pTarjeta is null OR pTarjeta = '' THEN` |  |
| L106 | VALIDACIÓN_NULL | `IF pTarjeta is null THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | 🔵 CONVENCIÓN | nombre_sp |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ac_actualizactas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ac_actualizactas.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cuentas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ac_actualizactas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pId                          INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pId` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cNombre` | `CHAR(45)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cuentas_retiro` | `bdicheq` | ⚠️ sí | UPDATE | L35 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L27 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ac_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ac_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ac_busquedacuentas_total`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ac_busquedacuentas_total.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda cuentas (total)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ac_busquedacuentas_total(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cuentas_retiro` | `bdicheq` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet ='01279';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ac_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ac_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ac_desbloquoctas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ac_desbloquoctas.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloqueo cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bloqueo_cta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ac_desbloquoctas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pFechaDesb                   DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaDesb` | `DATE` | `desb`=desbloqueo | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `cNombre` | `CHAR(45)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cClave` | `CHAR(5)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `bloqueo_cta` | `bdicheq` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ac_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desb` | ACCION | desbloqueo | 🟡 INFERIDO | nombre_sp |
| `?loquo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ac_`, `?loquo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actinfosolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actinfosolicitudmc.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza información, solicitud y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_mc_actinfosol` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actinfosolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pNumCliente                  CHAR(20)
  pNumClienteReferencia        CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pNumClienteReferencia` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cMensaje` | `CHAR(80)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_mc_actinfosol` | `bdisolic` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisolic:sp_mc_actinfosol';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_activalidaciontelefono`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_activalidaciontelefono.sql` |
| **LOC (1er CREATE)** | 49 |
| **Callgraph** | ✅ fan_in=0 / fan_out=45 |
| **Deps concatenadas** | 78 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "activa identificador y teléfono" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_actvalidacioncofetel` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_activalidaciontelefono(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pFlagTelefonoCasa            CHAR(1)
  pFlagTelefonoCelular         CHAR(1)
  pflagTelefonoOficina         CHAR(1)
  pTipoDireccion               CHAR(1)
  pTipo                        CHAR(1)
) RETURNING CHAR(5) AS cCodRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumCte` | `CHAR(9)` | — | — |
| `pFlagTelefonoCasa` | `CHAR(1)` | `telefono`=teléfono | 🔵 CONVENCIÓN |
| `pFlagTelefonoCelular` | `CHAR(1)` | `telefono`=teléfono | 🔵 CONVENCIÓN |
| `pflagTelefonoOficina` | `CHAR(1)` | `telefono`=teléfono | 🔵 CONVENCIÓN |
| `pTipoDireccion` | `CHAR(1)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_actvalidacioncofetel` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `activa` | ACCION | activa | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?acion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `telefono` | ENTIDAD | teléfono | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l`, `?acion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_activardesactivarproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_activardesactivarproductos.sql` |
| **LOC (1er CREATE)** | 291 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 216 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "activar Servicios de Atención al Cliente — subsistema de atención en sucursal y productos · IVA" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_activardesactivarproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProdActivaDesactiva         CHAR(250)
  pTipo                        CHAR(1)
  pPlaza                       CHAR(3)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS cRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProdActivaDesactiva` | `CHAR(250)` | `sac`=Servicios de Atención al Cliente — subsistema de atención en sucursal (ventanilla, domiciliación, abonos ATM, remesas WU); base de datos propia bdisac: con tabla sac_movimientoshistorial · `iva`=IVA (impuesto — SAT) | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pTipo` | `CHAR(1)` | — | — |
| `pPlaza` | `CHAR(3)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cProducto` | `CHAR(4)` | L12 |
| `cTieneProducto` | `CHAR(4)` | L13 |
| `cDescripcionProducto` | `CHAR(60)` | L14 |
| `cSistema` | `CHAR(2)` | L15 |
| `cTipoSistema` | `CHAR(2)` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |
| `iTotSuc` | `INTEGER` | L18 |
| `iTotProdSuc` | `INTEGER` | L19 |
| `bInTransaction` | `BOOLEAN` | L20 |
| `cCadenaValores` | `CHAR(10)` | L21 |
| `cCadena` | `CHAR(5)` | L22 |
| `iParams` | `SMALLINT` | L23 |
| `cActivarDesactivar` | `CHAR(1)` | L24 |
| `cSucursal` | `CHAR(4)` | L25 |
| `iNoRegsProcesados` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L104 |
| `si_prod_sucursal` | `bdinteg` | ⚠️ sí | SELECT | L108 |
| `si_prod_sucursal` | `bdinteg` | ⚠️ sí | DELETE | L108 |
| `si_prod_sucursal` | `bdinteg` | ⚠️ sí | INSERT | L110 |
| `ss_control_parametricos` | `bdisolic` | ⚠️ sí | SELECT | L117 |
| `ss_control_parametricos` | `bdisolic` | ⚠️ sí | DELETE | L117 |
| `si_control_tarj_suc` | `bdinteg` | ⚠️ sí | SELECT | L119 |
| `si_control_tarj_suc` | `bdinteg` | ⚠️ sí | DELETE | L119 |
| `ss_control_parametricos` | `bdisolic` | ⚠️ sí | INSERT | L121 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |
| `sp_split_cadena` | `bdicnweb` | no | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | FÓRMULA | `LET cTipoSistema            = "06"; -- CREDITO` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L92 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L113 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |
| L138 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |
| L157 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L160 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L177 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |
| L199 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |
| L216 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L219 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L240 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |
| L262 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `activar` | ACCION | activar | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?de`, `?t`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualiza_admintransaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualiza_admintransaciones.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza Administrador — rol de usuario con privilegios administrativos y [polisemia] Transferencia" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_admintransaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTransaccion                 CHAR(4)
  pSistemaCuenta               CHAR(2)
  pRegulatorio                 CHAR (1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(4)` | `trans`=[polisemia] Transferencia (bditransfer, bditrans: transferencias y remesas con campos pbco_dest/ppais_dest) | Transacción (sufijo genérico en SPs de reversión y procesamiento) | 🔵 CONVENCIÓN |
| `pSistemaCuenta` | `CHAR(2)` | — | — |
| `pRegulatorio` | `CHAR (1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iRegistros_afectados` | `INTEGER` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00037';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00458';` |  |
| L53 | FÓRMULA | `LET iRegistros_afectados = iRegistros_afectados + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `?aciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizacalificaestatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacalificaestatus.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 94 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza estatus" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacalificaestatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecucion                   CHAR(1)
  pNumCte                      CHAR(20)
  pCalificacion                SMALLINT
  pStatus                      SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecucion` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCalificacion` | `SMALLINT` | `califica`=califica / evalúa (scoring) | 🔵 CONVENCIÓN |
| `pStatus` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_calificacion_cliente` | `bdinteg` | ⚠️ sí | INSERT | L78 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCte = '' OR pNumCte IS NULL THEN` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `califica` | ACCION | califica / evalúa (scoring) | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_actualizacambiobilletescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacambiobilletescaja.sql` |
| **LOC (1er CREATE)** | 146 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cambio y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacambiobilletescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodProveedor                CHAR(4)
  pProcedencia                 CHAR(4)
  pMonto                       DECIMAL(14,2)
  pCantidadIng1                DECIMAL(14,2)
  pCantidadIng2                DECIMAL(14,2)
  pCantidadIng3                DECIMAL(14,2)
  pCantidadIng4                DECIMAL(14,2)
  pCantidadIng5                DECIMAL(14,2)
  pCantidadIng6                DECIMAL(14,2)
  pCantidadIng7                DECIMAL(14,2)
  pCantidadIng8                DECIMAL(14,2)
  pCantidadIng9                DECIMAL(14,2)
  pCantidadIng10               DECIMAL(14,2)
  pCantidadIng11               DECIMAL(14,2)
  pCantidadIng12               DECIMAL(14,2)
  pCantidadIng13               DECIMAL(14,2)
  pCantidadIng14               DECIMAL(14,2)
  pCantidadIng15               DECIMAL(14,2)
  pCantidadEgr1                DECIMAL(14,2)
  pCantidadEgr2                DECIMAL(14,2)
  pCantidadEgr3                DECIMAL(14,2)
  pCantidadEgr4                DECIMAL(14,2)
  pCantidadEgr5                DECIMAL(14,2)
  pCantidadEgr6                DECIMAL(14,2)
  pCantidadEgr7                DECIMAL(14,2)
  pCantidadEgr8                DECIMAL(14,2)
  pCantidadEgr9                DECIMAL(14,2)
  pCantidadEgr10               DECIMAL(14,2)
  pCantidadEgr11               DECIMAL(14,2)
  pCantidadEgr12               DECIMAL(14,2)
  pCantidadEgr13               DECIMAL(14,2)
  pCantidadEgr14               DECIMAL(14,2)
  pCantidadEgr15               DECIMAL(14,2)
  pDenominacion1               CHAR(18)
  pDenominacion2               CHAR(18)
  pDenominacion3               CHAR(18)
  pDenominacion4               CHAR(18)
  pDenominacion5               CHAR(18)
  pDenominacion6               CHAR(18)
  pDenominacion7               CHAR(18)
  pDenominacion8               CHAR(18)
  pDenominacion9               CHAR(18)
  pDenominacion10              CHAR(18)
  pDenominacion11              CHAR(18)
  pDenominacion12              CHAR(18)
  pDenominacion13              CHAR(18)
  pDenominacion14              CHAR(18)
  pDenominacion15              CHAR(18)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodProveedor` | `CHAR(4)` | — | — |
| `pProcedencia` | `CHAR(4)` | — | — |
| `pMonto` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng1` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng2` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng3` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng4` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng5` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng6` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng7` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng8` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng9` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng10` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng11` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng12` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng13` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng14` | `DECIMAL(14,2)` | — | — |
| `pCantidadIng15` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr1` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr2` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr3` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr4` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr5` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr6` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr7` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr8` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr9` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr10` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr11` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr12` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr13` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr14` | `DECIMAL(14,2)` | — | — |
| `pCantidadEgr15` | `DECIMAL(14,2)` | — | — |
| `pDenominacion1` | `CHAR(18)` | — | — |
| `pDenominacion2` | `CHAR(18)` | — | — |
| `pDenominacion3` | `CHAR(18)` | — | — |
| `pDenominacion4` | `CHAR(18)` | — | — |
| `pDenominacion5` | `CHAR(18)` | — | — |
| `pDenominacion6` | `CHAR(18)` | — | — |
| `pDenominacion7` | `CHAR(18)` | — | — |
| `pDenominacion8` | `CHAR(18)` | — | — |
| `pDenominacion9` | `CHAR(18)` | — | — |
| `pDenominacion10` | `CHAR(18)` | — | — |
| `pDenominacion11` | `CHAR(18)` | — | — |
| `pDenominacion12` | `CHAR(18)` | — | — |
| `pDenominacion13` | `CHAR(18)` | — | — |
| `pDenominacion14` | `CHAR(18)` | — | — |
| `pDenominacion15` | `CHAR(18)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iFolio` | `INTEGER` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRet` | `CHAR(5)` | L15 |
| `cTrans` | `CHAR(30)` | L16 |
| `cFolio` | `CHAR(8)` | L17 |
| `cSucursal` | `CHAR(4)` | L18 |
| `cFolioSucursal` | `CHAR(16)` | L19 |
| `iRegistrosAfectados` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdisuc` | `bdicnweb` | no | INSERT | L123 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCodProveedor = '' OR pProcedencia = '' OR pMonto IS NULL THE` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | VALIDACIÓN_NULL | `IF iFolio IS NULL THEN` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00190';` |  |
| L112 | VALIDACIÓN_NULL | `IF cTrans IS NULL THEN` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00190';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `?billetes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?billetes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizacatczb_rh`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacatczb_rh.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 160 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacatczb_rh(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCzb_clave_a                 CHAR(4)
  pCzb_nombre_a                CHAR(40)
  pNoempleado_czb_a            CHAR(20)
  pNombre_czb_a                CHAR(80)
  pNocelular_czb_a             CHAR(13)
  pEmail_czb_a                 CHAR(80)
  pCentrocosto_czb_a           CHAR(8)
  pCzb_clave                   CHAR(4)
  pCzb_nombre                  CHAR(40)
  pNoempleado_czb              CHAR(20)
  pNombre_czb                  CHAR(80)
  pNocelular_czb               CHAR(13)
  pEmail_czb                   CHAR(80)
  pCentrocosto_czb             CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCzb_clave_a` | `CHAR(4)` | — | — |
| `pCzb_nombre_a` | `CHAR(40)` | — | — |
| `pNoempleado_czb_a` | `CHAR(20)` | — | — |
| `pNombre_czb_a` | `CHAR(80)` | — | — |
| `pNocelular_czb_a` | `CHAR(13)` | — | — |
| `pEmail_czb_a` | `CHAR(80)` | — | — |
| `pCentrocosto_czb_a` | `CHAR(8)` | — | — |
| `pCzb_clave` | `CHAR(4)` | — | — |
| `pCzb_nombre` | `CHAR(40)` | — | — |
| `pNoempleado_czb` | `CHAR(20)` | — | — |
| `pNombre_czb` | `CHAR(80)` | — | — |
| `pNocelular_czb` | `CHAR(13)` | — | — |
| `pEmail_czb` | `CHAR(80)` | — | — |
| `pCentrocosto_czb` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCzb_clave` | `CHAR(4)` | L8 |
| `cCzb_nombre` | `CHAR(40)` | L9 |
| `cNo_empleado_czb` | `CHAR(20)` | L10 |
| `cNombre_czb` | `CHAR(80)` | L11 |
| `cNo_celular_czb` | `CHAR(13)` | L12 |
| `cE_mail_czb` | `CHAR(80)` | L13 |
| `cCentro_costo_czb` | `CHAR(8)` | L14 |
| `iTotal` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catgcbczb_rh` | `bdinteg` | ⚠️ sí | SELECT | L54 |
| `si_catgcbczb_rh` | `bdinteg` | ⚠️ sí | UPDATE | L67 |
| `si_catgcbczb_rh` | `bdinteg` | ⚠️ sí | INSERT | L111 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?czb_rh` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?czb_rh` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizacatgcb_rh`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacatgcb_rh.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 159 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacatgcb_rh(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCvegerencia_a               CHAR(4)
  pNombregerencia_a            CHAR(40)
  pNoempleado_a                CHAR(20)
  pNombreemp_a                 CHAR(80)
  pNocelular_a                 CHAR(13)
  pEmail_a                     CHAR(80)
  pCentro_costo_a              CHAR(8)
  pCvegerencia                 CHAR(4)
  pNombregerencia              CHAR(40)
  pNoempleado                  CHAR(20)
  pNombreemp                   CHAR(80)
  pNocelular                   CHAR(13)
  pEmail                       CHAR(80)
  pCentro_costo                CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCvegerencia_a` | `CHAR(4)` | — | — |
| `pNombregerencia_a` | `CHAR(40)` | — | — |
| `pNoempleado_a` | `CHAR(20)` | — | — |
| `pNombreemp_a` | `CHAR(80)` | — | — |
| `pNocelular_a` | `CHAR(13)` | — | — |
| `pEmail_a` | `CHAR(80)` | — | — |
| `pCentro_costo_a` | `CHAR(8)` | — | — |
| `pCvegerencia` | `CHAR(4)` | — | — |
| `pNombregerencia` | `CHAR(40)` | — | — |
| `pNoempleado` | `CHAR(20)` | — | — |
| `pNombreemp` | `CHAR(80)` | — | — |
| `pNocelular` | `CHAR(13)` | — | — |
| `pEmail` | `CHAR(80)` | — | — |
| `pCentro_costo` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCve_gerencia` | `CHAR(4)` | L8 |
| `cNombre_gerencia` | `CHAR(40)` | L9 |
| `cNo_empleado` | `CHAR(20)` | L10 |
| `cNombre_emp` | `CHAR(80)` | L11 |
| `cNo_celular` | `CHAR(13)` | L12 |
| `cEmail` | `CHAR(80)` | L13 |
| `cCentro_costo` | `CHAR(8)` | L14 |
| `iTotal` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catgcb_rh` | `bdinteg` | ⚠️ sí | SELECT | L54 |
| `si_catgcb_rh` | `bdinteg` | ⚠️ sí | UPDATE | L67 |
| `si_catgcb_rh` | `bdinteg` | ⚠️ sí | INSERT | L113 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?gcb_rh` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gcb_rh` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizacedulas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacedulas.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza cédulas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacedulas(
  pFechaConcil                 DATE
  pCtaContable                 CHAR(14)
  pObservaciones               CHAR(255)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaConcil` | `DATE` | — | — |
| `pCtaContable` | `CHAR(14)` | — | — |
| `pObservaciones` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontable` | `bdicheq` | ⚠️ sí | SELECT | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | VALIDACIÓN_NULL | `IF ( ( pFechaConcil is null OR pFechaConcil = '' ) OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `cedulas` | ENTIDAD | cédulas | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_actualizacedulasccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacedulasccl.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cédulas y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacedulasccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCcl                    DATE
  pCtaContable                 CHAR(14)
  pObservaciones               CHAR(255)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCcl` | `DATE` | `ccl`=módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (bdicnweb:sp_ccl_*) | 🟡 INFERIDO |
| `pCtaContable` | `CHAR(14)` | — | — |
| `pObservaciones` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `bdicnweb` | `bdicnweb` | no | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_actualizacedulas ";` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cedulas` | ENTIDAD | cédulas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_actualizacentrallincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacentrallincred.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza línea de crédito (central)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacentrallincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pMonto                       DECIMAL(18,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pMonto` | `DECIMAL(18,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cMensajeRetorno` | `CHAR(80)` | L10 |
| `cTipo` | `CHAR(1)` | L11 |
| `cStatus` | `CHAR(1)` | L12 |
| `bInTransaction` | `BOOLEAN` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_maesdos` | `bdicred` | ⚠️ sí | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L47 |
| `bdicred` | `bdicnweb` | no | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCredito = '' OR pMonto IS NULL THEN` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_actualiza_lincred_central';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00278';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00279';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00280';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00281';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `central` | MODIF | central | 🔵 CONVENCIÓN | nombre_sp |
| `lincred` | ENTIDAD | línea de crédito | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_actualizacion_cheques_presentar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacion_cheques_presentar.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cheques" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `sp_cce_consultarchqsxpresentar2`, `sp_cce_consultarchqsxpresentar2_totales`, `sp_ope_actualizarfechacheques` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacion_cheques_presentar(
  pBandera                     CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaPresentacion           CHAR(10)
  pCtaDelCheque                CHAR(20)
  pNumCheque                   CHAR(7)
  pMonto                       DECIMAL(14,2)
) RETURNING CHAR(5)         AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaPresentacion` | `CHAR(10)` | `presenta`=presenta | 🔵 CONVENCIÓN |
| `pCtaDelCheque` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | — | — |
| `pMonto` | `DECIMAL(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L31 |
| `cDescRet` | `CHAR(80)` | L32 |
| `cCodRet` | `CHAR(5)` | L33 |
| `pEmpresa` | `CHAR(3)` | L34 |
| `cSucursal` | `CHAR(4)` | L35 |
| `cDescSucursal` | `CHAR(40)` | L36 |
| `cFechaPres` | `CHAR(10)` | L37 |
| `cCtaDeposito` | `CHAR(20)` | L38 |
| `cBanco` | `CHAR(3)` | L39 |
| `cDescBanco` | `CHAR(40)` | L40 |
| `cCtaDelCheque` | `CHAR(20)` | L41 |
| `iNumCheque` | `INTEGER` | L42 |
| `mImporte` | `MONEY(14,2)` | L43 |
| `cFechaHoy` | `CHAR(10)` | L44 |
| `cDescDiaFeriado` | `CHAR(30)` | L45 |
| `cLaborable` | `CHAR(1)` | L46 |
| `iNoRegistros` | `INTEGER` | L47 |
| `dDiaFeriado` | `CHAR(30)` | L48 |
| `icontador` | `INTEGER` | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cce_consultarchqsxpresentar2` | `bdicheq` | ⚠️ sí | L113 |
| `sp_cce_consultarchqsxpresentar2_totales` | `bdicheq` | ⚠️ sí | L123 |
| `sp_ope_actualizarfechacheques` | `bdicnweb` | no | L128 |
| `sp_ope_consultacheqsxpresentar` | `bdicnweb` | no | L134 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L116 | FÓRMULA | `LET icontador = icontador + 1;` |  |
| L137 | FÓRMULA | `LET icontador = icontador + 1;` |  |
| L155 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?cion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `presenta` | ACCION | presenta | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion_`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizacionctepmsnom`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacionctepmsnom.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 100 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cliente y nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_snom_altaservicioctepmnomina` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacionctepmsnom(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOperacion                   CHAR(1)
  pNumCliente                  CHAR(20)
  pEstatus                     CHAR(1)
  pCuentaCliente               CHAR(20)
  pTipoEmpresa                 CHAR(1)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOperacion` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pEstatus` | `CHAR(1)` | — | — |
| `pCuentaCliente` | `CHAR(20)` | — | — |
| `pTipoEmpresa` | `CHAR(1)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `cCodigo` | `CHAR(3)` | L15 |
| `cNombreCte` | `CHAR(50)` | L16 |
| `cNumeroCte` | `CHAR(20)` | L17 |
| `iTipoEmpresa` | `SMALLINT` | L18 |
| `cAceptaProducto` | `CHAR(50)` | L19 |
| `cCuentaCte` | `CHAR(20)` | L20 |
| `cStatusAlta` | `CHAR(1)` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_snom_altaservicioctepmnomina` | `bdicheq` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:sp_snom_altaservicioct` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00440';` |  |
| L88 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?pms` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion`, `?pms` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizacionctepmsnom2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacionctepmsnom2.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cliente y nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_snom_altaservicioctepmnomina` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacionctepmsnom2(
  pOperacion                   CHAR(1)
  pNumCliente                  CHAR(20)
  pEstatus                     CHAR(1)
  pCuentaCliente               CHAR(20)
  pTipoEmpresa                 CHAR(1)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pOperacion` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pEstatus` | `CHAR(1)` | — | — |
| `pCuentaCliente` | `CHAR(20)` | — | — |
| `pTipoEmpresa` | `CHAR(1)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(6)` | L15 |
| `cCodigo` | `CHAR(3)` | L16 |
| `cNombreCte` | `CHAR(50)` | L17 |
| `cNumeroCte` | `CHAR(20)` | L18 |
| `iTipoEmpresa` | `SMALLINT` | L19 |
| `cAceptaProducto` | `CHAR(50)` | L20 |
| `cCuentaCte` | `CHAR(20)` | L21 |
| `cStatusAlta` | `CHAR(1)` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_snom_altaservicioctepmnomina` | `bdicheq` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_snom_altaserviciocte` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00440';` |  |
| L83 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?pms` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion`, `?pms`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizaclasificacion_gcb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizaclasificacion_gcb.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 158 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza SIF — canal de estado de cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizaclasificacion_gcb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCve_clasificacion_a         CHAR(3)
  pClasificacion_a             CHAR(40)
  pCve_clasificacion           CHAR(3)
  pClasificacion               CHAR(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCve_clasificacion_a` | `CHAR(3)` | `sif`=SIF — canal de estado de cuenta (aclaraciones_edocta_sif, detalle_edocta_sif); procesa aclaraciones de TDC por tarjeta+fecha de emisión · `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pClasificacion_a` | `CHAR(40)` | `sif`=SIF — canal de estado de cuenta (aclaraciones_edocta_sif, detalle_edocta_sif); procesa aclaraciones de TDC por tarjeta+fecha de emisión · `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pCve_clasificacion` | `CHAR(3)` | `sif`=SIF — canal de estado de cuenta (aclaraciones_edocta_sif, detalle_edocta_sif); procesa aclaraciones de TDC por tarjeta+fecha de emisión · `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pClasificacion` | `CHAR(40)` | `sif`=SIF — canal de estado de cuenta (aclaraciones_edocta_sif, detalle_edocta_sif); procesa aclaraciones de TDC por tarjeta+fecha de emisión · `cac`=familia crédito (CAC) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCve_clasificacion` | `CHAR(3)` | L6 |
| `cClasificacion` | `CHAR(40)` | L7 |
| `iTotal` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_clasificacion_gcb` | `bdinteg` | ⚠️ sí | SELECT | L42 |
| `si_clasificacion_gcb` | `bdinteg` | ⚠️ sí | UPDATE | L54 |
| `si_clasificacion_gcb` | `bdinteg` | ⚠️ sí | INSERT | L75 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?cla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sif` | ENTIDAD | SIF — canal de estado de cuenta (aclaraciones_edocta_sif, de | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `?ion_gcb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cla`, `?i`, `?ion_gcb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizacomprasdepositoscaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacomprasdepositoscaja.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza sd — saldo disponible, punto de venta, OS — Originación de Solicitudes y caja (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdisuc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacomprasdepositoscaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pIdOperacion                 CHAR(4)
  pDivisa                      CHAR(2)
  pIdProcedencia               CHAR(4)
  pMontoTotalSolCap            MONEY(14,2)
  pMonto                       MONEY(14,2)
  pDenominacion1               CHAR(18)
  pDenominacion2               CHAR(18)
  pDenominacion3               CHAR(18)
  pDenominacion4               CHAR(18)
  pDenominacion5               CHAR(18)
  pDenominacion6               CHAR(18)
  pDenominacion7               CHAR(18)
  pDenominacion8               CHAR(18)
  pDenominacion9               CHAR(18)
  pDenominacion10              CHAR(18)
  pDenominacion11              CHAR(18)
  pDenominacion12              CHAR(18)
  pDenominacion13              CHAR(18)
  pDenominacion14              CHAR(18)
  pDenominacion15              CHAR(18)
  pCantidad1                   FLOAT(8)
  pCantidad2                   FLOAT(8)
  pCantidad3                   FLOAT(8)
  pCantidad4                   FLOAT(8)
  pCantidad5                   FLOAT(8)
  pCantidad6                   FLOAT(8)
  pCantidad7                   FLOAT(8)
  pCantidad8                   FLOAT(8)
  pCantidad9                   FLOAT(8)
  pCantidad10                  FLOAT(8)
  pCantidad11                  FLOAT(8)
  pCantidad12                  FLOAT(8)
  pCantidad13                  FLOAT(8)
  pCantidad14                  FLOAT(8)
  pCantidad15                  FLOAT(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pIdOperacion` | `CHAR(4)` | — | — |
| `pDivisa` | `CHAR(2)` | — | — |
| `pIdProcedencia` | `CHAR(4)` | — | — |
| `pMontoTotalSolCap` | `MONEY(14,2)` | — | — |
| `pMonto` | `MONEY(14,2)` | — | — |
| `pDenominacion1` | `CHAR(18)` | — | — |
| `pDenominacion2` | `CHAR(18)` | — | — |
| `pDenominacion3` | `CHAR(18)` | — | — |
| `pDenominacion4` | `CHAR(18)` | — | — |
| `pDenominacion5` | `CHAR(18)` | — | — |
| `pDenominacion6` | `CHAR(18)` | — | — |
| `pDenominacion7` | `CHAR(18)` | — | — |
| `pDenominacion8` | `CHAR(18)` | — | — |
| `pDenominacion9` | `CHAR(18)` | — | — |
| `pDenominacion10` | `CHAR(18)` | — | — |
| `pDenominacion11` | `CHAR(18)` | — | — |
| `pDenominacion12` | `CHAR(18)` | — | — |
| `pDenominacion13` | `CHAR(18)` | — | — |
| `pDenominacion14` | `CHAR(18)` | — | — |
| `pDenominacion15` | `CHAR(18)` | — | — |
| `pCantidad1` | `FLOAT(8)` | — | — |
| `pCantidad2` | `FLOAT(8)` | — | — |
| `pCantidad3` | `FLOAT(8)` | — | — |
| `pCantidad4` | `FLOAT(8)` | — | — |
| `pCantidad5` | `FLOAT(8)` | — | — |
| `pCantidad6` | `FLOAT(8)` | — | — |
| `pCantidad7` | `FLOAT(8)` | — | — |
| `pCantidad8` | `FLOAT(8)` | — | — |
| `pCantidad9` | `FLOAT(8)` | — | — |
| `pCantidad10` | `FLOAT(8)` | — | — |
| `pCantidad11` | `FLOAT(8)` | — | — |
| `pCantidad12` | `FLOAT(8)` | — | — |
| `pCantidad13` | `FLOAT(8)` | — | — |
| `pCantidad14` | `FLOAT(8)` | — | — |
| `pCantidad15` | `FLOAT(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodTrans` | `CHAR(4)` | L17 |
| `cSigno` | `CHAR(1)` | L18 |
| `iNoFolio` | `INTEGER` | L19 |
| `cNoFolio` | `CHAR(8)` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cFolioSucursal` | `CHAR(16)` | L22 |
| `cFolio` | `CHAR(8)` | L23 |
| `iNoRegistros` | `INTEGER` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L56 |
| `bdisuc` | `bdicnweb` | no | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdProvCaja = '' OR pIdOperacion = '' OR pDivisa = '' OR pIdP` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_solic_con_terc';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?ra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pos` | ENTIDAD | punto de venta (POS) | 🔵 CONVENCIÓN | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ra`, `?e`, `?it` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizacuentasdormidas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizacuentasdormidas.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cuentas (cuentas dormidas)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizacuentasdormidas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMontoDep                    MONEY(10,2)
  pDiasInac                    SMALLINT
  pDiasBloq                    SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMontoDep` | `MONEY(10,2)` | — | — |
| `pDiasInac` | `SMALLINT` | — | — |
| `pDiasBloq` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `dFechaAlta` | `DATE` | L6 |
| `cHoraAlta` | `CHAR(10)` | L7 |
| `mMontoDeposito` | `MONEY(10,2)` | L8 |
| `iNumDiasInac` | `SMALLINT` | L9 |
| `iNumDiasBloq` | `SMALLINT` | L10 |
| `iTotal` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param_ctasdormidas` | `bdicheq` | ⚠️ sí | SELECT | L48 |
| `sc_param_ctasdormidas` | `bdicheq` | ⚠️ sí | UPDATE | L59 |
| `sc_param_ctasdormidas` | `bdicheq` | ⚠️ sí | INSERT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pMontoDep IS NULL OR pDiasInac IS NULL OR pDiasBloq IS NULL T` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dormidas` | MODIF | cuentas dormidas (inactivas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_actualizadatoscheque`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizadatoscheque.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ✅ fan_in=14 / fan_out=8 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza datos y cheque" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizadatoscheque(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
  pMotivoDev                   CHAR(2)
  pIdConsCheque                INTEGER
  pIdEjecucion                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pMotivoDev` | `CHAR(2)` | — | — |
| `pIdConsCheque` | `INTEGER` | `cheque`=cheque | ✅ CÓDIGO |
| `pIdEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cFechaHoy` | `CHAR(10)` | L8 |
| `cFormatFechaHoy` | `CHAR(10)` | L9 |
| `cCuenta` | `CHAR(20)` | L10 |
| `iCheque` | `INTEGER` | L11 |
| `mImporte` | `MONEY(16,2)` | L12 |
| `cNombreArchivo` | `CHAR(22)` | L13 |
| `iSecuencia` | `INTEGER` | L14 |
| `cCodMotivo` | `CHAR(2)` | L15 |
| `cDescMotivo` | `CHAR(35)` | L16 |
| `cMotivoDevolucion` | `CHAR(38)` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | SELECT | L58 |
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | UPDATE | L63 |
| `cce_propios_det` | `bditef` | ⚠️ sí | UPDATE | L67 |
| `si_coddevcam` | `bdinteg` | ⚠️ sí | SELECT | L84 |
| `sc_contch` | `bdicheq` | ⚠️ sí | UPDATE | L102 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | UPDATE | L109 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha = '' OR pIdConsCheque IS NULL OR pIdEjecucion = '' THE` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L100 | FÓRMULA | `LET cFormatFechaHoy = SUBSTR(pFecha,5,2) \|\|'/'\|\|SUBSTR(pFecha,7,2)\|\|'/'\|\|SUBSTR(pFecha,1,4);` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_actualizadatosclientecli`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizadatosclientecli.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Propósito inferido** | "actualiza datos y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizadatosclientecli(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNombre1                     CHAR(26)
  pNombre2                     CHAR(26)
  pApPaterno                   CHAR(26)
  pApMaterno                   CHAR(26)
  pRfc                         CHAR(13)
  pRfcAlterno                  CHAR(13)
  pTipoCliente                 INTEGER
  pFecNacimiento               DATE
  pNombre1Ant                  CHAR(26)
  pNombre2Ant                  CHAR(26)
  pApPaternoAnt                CHAR(26)
  pApMaternoAnt                CHAR(26)
  pRfcAnt                      CHAR(13)
  pTipoClienteAnt              INTEGER
  pFecNacimientoAnt            DATE
) RETURNING CHAR(5) AS codret, SMALLINT AS etapa
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | `cliente`=cliente | ✅ CÓDIGO |
| `pNombre1` | `CHAR(26)` | — | — |
| `pNombre2` | `CHAR(26)` | — | — |
| `pApPaterno` | `CHAR(26)` | — | — |
| `pApMaterno` | `CHAR(26)` | — | — |
| `pRfc` | `CHAR(13)` | — | — |
| `pRfcAlterno` | `CHAR(13)` | — | — |
| `pTipoCliente` | `INTEGER` | `cliente`=cliente | ✅ CÓDIGO |
| `pFecNacimiento` | `DATE` | — | — |
| `pNombre1Ant` | `CHAR(26)` | — | — |
| `pNombre2Ant` | `CHAR(26)` | — | — |
| `pApPaternoAnt` | `CHAR(26)` | — | — |
| `pApMaternoAnt` | `CHAR(26)` | — | — |
| `pRfcAnt` | `CHAR(13)` | — | — |
| `pTipoClienteAnt` | `INTEGER` | `cliente`=cliente | ✅ CÓDIGO |
| `pFecNacimientoAnt` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegsAfectados` | `INTEGER` | L7 |
| `iMaxSecuencia` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iEtapa` | `SMALLINT` | L10 |
| `iInTrans` | `SMALLINT` | L11 |
| `cAnioMesInicio` | `CHAR(6)` | L12 |
| `cAnioMesActual` | `CHAR(6)` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `cTipoPersona` | `CHAR(2)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L57 |
| `si_cte_bitacora` | `bdinteg` | ⚠️ sí | SELECT | L100 |
| `si_cte_bitacora` | `bdinteg` | ⚠️ sí | INSERT | L104 |
| `si_cliente` | `bdinteg` | ⚠️ sí | UPDATE | L110 |
| `systables` | `bdicnweb` | no | SELECT | L116 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarecaudacioneslide` | `bdilide` | ⚠️ sí | L119 |
| `sp_actualizarfclide` | `bdilide` | ⚠️ sí | L123 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00020';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00020';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cliente` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?cli` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cli` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizadomiciliocte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizadomiciliocte.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza domicilio y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_guardadomiciliocliente` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizadomiciliocte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pPromotor                    CHAR(8)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pEstado                      CHAR(30)
  pCiudad                      CHAR(60)
  pCiudadCoppel                INTEGER
  pZona                        CHAR(30)
  pCalle                       CHAR(30)
  pCodPostal                   CHAR(5)
  pComplemento                 CHAR(80)
  pNumeroExtCalle              CHAR(10)
  pNumeroIntCalle              CHAR(10)
  pEdificio                    INTEGER
  pDepartamento                CHAR(6)
) RETURNING CHAR(5) AS cCodRetorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pPromotor` | `CHAR(8)` | — | — |
| `pNumCte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pEstado` | `CHAR(30)` | — | — |
| `pCiudad` | `CHAR(60)` | — | — |
| `pCiudadCoppel` | `INTEGER` | — | — |
| `pZona` | `CHAR(30)` | — | — |
| `pCalle` | `CHAR(30)` | — | — |
| `pCodPostal` | `CHAR(5)` | — | — |
| `pComplemento` | `CHAR(80)` | — | — |
| `pNumeroExtCalle` | `CHAR(10)` | — | — |
| `pNumeroIntCalle` | `CHAR(10)` | — | — |
| `pEdificio` | `INTEGER` | — | — |
| `pDepartamento` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_guardadomiciliocliente` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00235';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN EJECUCIÃN DE SP bdinteg:sp_guardadomiciliocliente'` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | CÓDIGO_RETORNO | `Let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `domicilio` | ENTIDAD | domicilio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_actualizadomiciliocte2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizadomiciliocte2.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=51 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza domicilio y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_guardadomiciliocliente2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizadomiciliocte2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pPromotor                    CHAR(8)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pEstado                      CHAR(30)
  pCiudad                      CHAR(60)
  pCiudadCoppel                INTEGER
  pZona                        CHAR(30)
  pCalle                       CHAR(30)
  pCodPostal                   CHAR(5)
  pComplemento                 CHAR(80)
  pNumeroExtCalle              CHAR(10)
  pNumeroIntCalle              CHAR(10)
  pEdificio                    INTEGER
  pDepartamento                CHAR(6)
  pEntreCalles                 CHAR(40)
) RETURNING CHAR(5) AS cCodRetorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pPromotor` | `CHAR(8)` | — | — |
| `pNumCte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pEstado` | `CHAR(30)` | — | — |
| `pCiudad` | `CHAR(60)` | — | — |
| `pCiudadCoppel` | `INTEGER` | — | — |
| `pZona` | `CHAR(30)` | — | — |
| `pCalle` | `CHAR(30)` | — | — |
| `pCodPostal` | `CHAR(5)` | — | — |
| `pComplemento` | `CHAR(80)` | — | — |
| `pNumeroExtCalle` | `CHAR(10)` | — | — |
| `pNumeroIntCalle` | `CHAR(10)` | — | — |
| `pEdificio` | `INTEGER` | — | — |
| `pDepartamento` | `CHAR(6)` | — | — |
| `pEntreCalles` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `cCodRetSp` | `CHAR(5)` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `cEmpresa` | `CHAR(3)` | L26 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_guardadomiciliocliente2` | `bdinteg` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00235';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN EJECUCIÃN DE SP bdinteg:sp_guardadomiciliocliente2` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | CÓDIGO_RETORNO | `Let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `domicilio` | ENTIDAD | domicilio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizaformato_gcb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizaformato_gcb.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 157 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizaformato_gcb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCve_formato_a               CHAR(3)
  pFormato_a                   CHAR(40)
  pCve_formato                 CHAR(3)
  pFormato                     CHAR(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCve_formato_a` | `CHAR(3)` | `forma`=construye / arma | 🟡 INFERIDO |
| `pFormato_a` | `CHAR(40)` | `forma`=construye / arma | 🟡 INFERIDO |
| `pCve_formato` | `CHAR(3)` | `forma`=construye / arma | 🟡 INFERIDO |
| `pFormato` | `CHAR(40)` | `forma`=construye / arma | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCve_formato` | `CHAR(3)` | L6 |
| `cFormato` | `CHAR(40)` | L7 |
| `iTotal` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_formato_gcb` | `bdinteg` | ⚠️ sí | SELECT | L42 |
| `si_formato_gcb` | `bdinteg` | ⚠️ sí | UPDATE | L55 |
| `si_formato_gcb` | `bdinteg` | ⚠️ sí | INSERT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `forma` | ACCION | construye / arma | 🟡 INFERIDO | nombre_sp |
| `?to_gcb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?to_gcb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizamonitorprocesos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizamonitorprocesos.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=48 / fan_out=14 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza monitor y proceso" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizamonitorprocesos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      CHAR(1)
  pEstatusProceso              CHAR(1)
  pCodError                    CHAR(5)
  pGenerico                    VARCHAR(255)
  pMac                         CHAR(18)
  pIp                          VARCHAR(16)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pEstatusProceso` | `CHAR(1)` | `sp`=stored procedure · `proceso`=proceso | ✅ CÓDIGO / 🔵 CONVENCIÓN |
| `pCodError` | `CHAR(5)` | — | — |
| `pGenerico` | `VARCHAR(255)` | — | — |
| `pMac` | `CHAR(18)` | — | — |
| `pIp` | `VARCHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_monitorprocesos` | `bdicnweb` | no | INSERT | L23 |
| `sw_monitorprocesos` | `bdicnweb` | no | SELECT | L59 |
| `sw_monitorprocesos` | `bdicnweb` | no | DELETE | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monitor` | ENTIDAD | monitor | 🔵 CONVENCIÓN | nombre_sp |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizapagocheque`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizapagocheque.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=8 / fan_out=12 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza pago y cheque" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizapagocheque(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pDireccionMac                CHAR(12)
  pIdCheque                    INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pIdCheque` | `INTEGER` | `cheque`=cheque | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `dFechaMasUno` | `DATE` | L9 |
| `cCuenta` | `CHAR(20)` | L10 |
| `iCheque` | `INTEGER` | L11 |
| `mImporte` | `MONEY(16,2)` | L12 |
| `cFechaPresini` | `CHAR(8)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_consultadetallechequesdev` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet 	     = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pDireccionMac = '' OR pIdCheque IS NULL THE` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00862';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `pago` | ENTIDAD | pago | 🔵 CONVENCIÓN | nombre_sp |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_actualizaparametrosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizaparametrosccl.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza parámetros y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizaparametrosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR (1)
  pArchivoOrigen               CHAR(3)
  pRepWin                      CHAR(50)
  pRepAix                      CHAR(50)
  pHistorico                   CHAR(90)
  pDiasBitacora                CHAR(90)
  pDiasMovimientos             CHAR(90)
  pDiasArchivosAIX             CHAR(90)
  pFicha                       INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR (1)` | — | — |
| `pArchivoOrigen` | `CHAR(3)` | — | — |
| `pRepWin` | `CHAR(50)` | — | — |
| `pRepAix` | `CHAR(50)` | — | — |
| `pHistorico` | `CHAR(90)` | — | — |
| `pDiasBitacora` | `CHAR(90)` | — | — |
| `pDiasMovimientos` | `CHAR(90)` | — | — |
| `pDiasArchivosAIX` | `CHAR(90)` | — | — |
| `pFicha` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `bditarjeta` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pBandera = '' OR pFicha IS NULL THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_actualizareposit` |  |
| L47 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_actualizareposit` |  |
| L62 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_actualizaprocesoconau`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizaprocesoconau.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza proceso" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizaprocesoconau(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(30)
  pProceso                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(30)` | — | — |
| `pProceso` | `CHAR(1)` | `proceso`=proceso | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |
| `bditarjeta` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_actualizaproceso` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?au` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?au` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizaproghorariosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizaproghorariosccl.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza ro — Rol Operativo, hora, OS — Originación de Solicitudes y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizaproghorariosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchOrigen                  CHAR(3)
  pOrdenProceso                INTEGER
  pHrEjecucionHoy              CHAR(1)
  pHrEjecucionExt              CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchOrigen` | `CHAR(3)` | — | — |
| `pOrdenProceso` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pHrEjecucionHoy` | `CHAR(1)` | — | — |
| `pHrEjecucionExt` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `cDescCodRet` | `CHAR(150)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L32 |
| `bditarjeta` | `bdicnweb` | no | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_concreing_actual` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `hora` | ENTIDAD | hora | 🔵 CONVENCIÓN | nombre_sp |
| `?ri` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g`, `?ri` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizaregistrocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizaregistrocaja.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza registro y caja" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizaregistrocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pCodigo                      CHAR(4)
  pDescripcion                 CHAR(30)
  pIdDivisa                    CHAR(2)
  pIdPlaza                     CHAR(3)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pDescripcion` | `CHAR(30)` | — | — |
| `pIdDivisa` | `CHAR(2)` | — | — |
| `pIdPlaza` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `dCantidad_1` | `MONEY(14,2)` | L10 |
| `dCantidad_2` | `MONEY(14,2)` | L11 |
| `dCantidad_3` | `MONEY(14,2)` | L12 |
| `dCantidad_4` | `MONEY(14,2)` | L13 |
| `dCantidad_5` | `MONEY(14,2)` | L14 |
| `dCantidad_6` | `MONEY(14,2)` | L15 |
| `dCantidad_7` | `MONEY(14,2)` | L16 |
| `dCantidad_8` | `MONEY(14,2)` | L17 |
| `dCantidad_9` | `MONEY(14,2)` | L18 |
| `dCantidad_10` | `MONEY(14,2)` | L19 |
| `dCantidad_11` | `MONEY(14,2)` | L20 |
| `dCantidad_12` | `MONEY(14,2)` | L21 |
| `dCantidad_13` | `MONEY(14,2)` | L22 |
| `dCantidad_14` | `MONEY(14,2)` | L23 |
| `dCantidad_15` | `MONEY(14,2)` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L67 |
| `bdisuc` | `bdicnweb` | no | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:manejacajageneral';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `registro` | ENTIDAD | registro | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_actualizaregistrodevolverext_tef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizaregistrodevolverext_tef.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza registro y TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizaregistrodevolverext_tef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDatosMotDevolucion          CHAR(250)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDatosMotDevolucion` | `CHAR(250)` | `dev`=devolución | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCadenaValores` | `CHAR(15)` | L7 |
| `cCadena` | `CHAR(15)` | L8 |
| `iIdxDevolver` | `INTEGER` | L9 |
| `cMotivoDevolucion` | `CHAR(2)` | L10 |
| `iNoRegsProcesados` | `INTEGER` | L11 |
| `iParams` | `SMALLINT` | L12 |
| `bInTransaction` | `BOOLEAN` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |
| `sp_split_cadena` | `bdicnweb` | no | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L78 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `registro` | ENTIDAD | registro | 🔵 CONVENCIÓN | nombre_sp |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?olvere` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?t_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?olvere`, `?t_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizareportespendientesarqueosuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizareportespendientesarqueosuc.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza reportes y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizareportespendientesarqueosuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreReporte               CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreReporte` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?pendientesarque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?uc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientesarque`, `?uc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizareportespendientesentradasalida`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizareportespendientesentradasalida.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza reportes y salida" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizareportespendientesentradasalida(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreReporte               CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreReporte` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?pendientesentrada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `salida` | ENTIDAD | salida | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientesentrada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizasdosucursalcaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizasdosucursalcaja.sql` |
| **LOC (1er CREATE)** | 199 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza saldos y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizasdosucursalcaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `dFechaAnterior` | `DATE` | L9 |
| `sEmpresa` | `CHAR(3)` | L10 |
| `sSucursal` | `CHAR(4)` | L11 |
| `sDivisa` | `CHAR(2)` | L12 |
| `sSaldoTotal` | `MONEY(14,2)` | L13 |
| `sFecha` | `DATE` | L14 |
| `sCajeroPrincipal` | `CHAR(8)` | L15 |
| `sDeno1` | `CHAR(18)` | L16 |
| `sDeno2` | `CHAR(18)` | L17 |
| `sDeno3` | `CHAR(18)` | L18 |
| `sDeno4` | `CHAR(18)` | L19 |
| `sDeno5` | `CHAR(18)` | L20 |
| `sDeno6` | `CHAR(18)` | L21 |
| `sDeno7` | `CHAR(18)` | L22 |
| `sDeno8` | `CHAR(18)` | L23 |
| `sDeno9` | `CHAR(18)` | L24 |
| `sDeno10` | `CHAR(18)` | L25 |
| `sDeno11` | `CHAR(18)` | L26 |
| `sDeno12` | `CHAR(18)` | L27 |
| `sDeno13` | `CHAR(18)` | L28 |
| `sDeno14` | `CHAR(18)` | L29 |
| *…21 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_pase_sucursal` | `bdisuc` | ⚠️ sí | SELECT | L140 |
| `bdisuc` | `bdicnweb` | no | INSERT | L148 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L116 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L110 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta = '' OR pFechaConsulta IS NULL THEN` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | FÓRMULA | `LET dFechaAnterior = DATE(pFechaConsulta) -1;` |  |
| L162 | FÓRMULA | `LET iRegistrosAfectados = iRegistrosAfectados + 1;` |  |
| L174 | FÓRMULA | `LET iRegistrosAfectados = iRegistrosAfectados + 1;` |  |
| L183 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `sdos` | ENTIDAD | saldos (abreviación) | 🟡 INFERIDO | nombre_sp |
| `?ucursal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ucursal` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizasolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizasolicitudmc.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza solicitud y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizasolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pSucursal                    CHAR(4)
  pTipoOperacion               SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pSucursal` | `CHAR(4)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cNombreEjecutivo` | `CHAR(45)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumSolicitud = '' OR pSucursal = '' OR pTipoOperacion IS NUL` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L59 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::SMALLINT;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_actualizastatusmonitorproceso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizastatusmonitorproceso.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=31 / fan_out=23 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza estatus, monitor y proceso" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizastatusmonitorproceso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      CHAR(1)
  pEstatusProceso              CHAR(1)
  pCodError                    CHAR(5)
  pMac                         CHAR(18)
  pIp                          VARCHAR(16)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pEstatusProceso` | `CHAR(1)` | `sp`=stored procedure · `status`=estatus | ✅ CÓDIGO / 🔵 CONVENCIÓN |
| `pCodError` | `CHAR(5)` | — | — |
| `pMac` | `CHAR(18)` | — | — |
| `pIp` | `VARCHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_monitoreostatusproceso` | `bdicnweb` | no | INSERT | L23 |
| `sw_monitoreostatusproceso` | `bdicnweb` | no | SELECT | L59 |
| `sw_monitoreostatusproceso` | `bdicnweb` | no | DELETE | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monitor` | ENTIDAD | monitor | 🔵 CONVENCIÓN | nombre_sp |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_actualizasucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizasucursal.sql` |
| **LOC (1er CREATE)** | 327 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 156 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza sucursal" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizasucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal_a                CHAR(4)
  pIdSucursal                  CHAR(4)
  pNomSucur                    CHAR(40)
  pFormato                     CHAR(40)
  pTipo                        CHAR(3)
  pTipologia                   CHAR(40)
  pFechaAp                     DATE
  pCalle                       CHAR(100)
  pNumExt                      CHAR(6)
  pNumInt                      CHAR(5)
  pEntreCalle                  CHAR(100)
  pColonia                     CHAR(100)
  pCveMun                      CHAR(3)
  pMun                         CHAR(60)
  pCveEst                      CHAR(2)
  pEst                         CHAR(30)
  pCP                          CHAR(5)
  pEstDom                      CHAR(1)
  pTelSuc                      CHAR(14)
  pLatitud                     CHAR(10)
  pLongitud                    CHAR(11)
  pZonaHoraria                 CHAR(20)
  pSerLun                      CHAR(1)
  pSerMar                      CHAR(1)
  pSerMie                      CHAR(1)
  pSerJue                      CHAR(1)
  pSerVie                      CHAR(1)
  pSerSab                      CHAR(1)
  pSerDom                      CHAR(1)
  pLunAp                       DATETIME YEAR to SECOND
  pLunCi                       DATETIME YEAR to SECOND
  pMarAp                       DATETIME YEAR to SECOND
  pMarCi                       DATETIME YEAR to SECOND
  pMieAp                       DATETIME YEAR to SECOND
  pMieCi                       DATETIME YEAR to SECOND
  pJueAp                       DATETIME YEAR to SECOND
  pJueCi                       DATETIME YEAR to SECOND
  pVieAp                       DATETIME YEAR to SECOND
  pVieCi                       DATETIME YEAR to SECOND
  pSabAp                       DATETIME YEAR to SECOND
  pSabCi                       DATETIME YEAR to SECOND
  pDomAp                       DATETIME YEAR to SECOND
  pDomCi                       DATETIME YEAR to SECOND
  pClasificacion               CHAR(40)
  pCveSucEje                   CHAR(40)
  pM2                          FLOAT
  pNumVent                     INTEGER
  pNumEsc                      INTEGER
  pNumATM                      INTEGER
  pZonaEco                     CHAR(30)
  pCveGer                      CHAR(4)
  pZbcve                       CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal_a` | `CHAR(4)` | `sucursal`=sucursal | 🔵 CONVENCIÓN |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | 🔵 CONVENCIÓN |
| `pNomSucur` | `CHAR(40)` | — | — |
| `pFormato` | `CHAR(40)` | — | — |
| `pTipo` | `CHAR(3)` | — | — |
| `pTipologia` | `CHAR(40)` | — | — |
| `pFechaAp` | `DATE` | — | — |
| `pCalle` | `CHAR(100)` | — | — |
| `pNumExt` | `CHAR(6)` | — | — |
| `pNumInt` | `CHAR(5)` | — | — |
| `pEntreCalle` | `CHAR(100)` | — | — |
| `pColonia` | `CHAR(100)` | — | — |
| `pCveMun` | `CHAR(3)` | — | — |
| `pMun` | `CHAR(60)` | — | — |
| `pCveEst` | `CHAR(2)` | — | — |
| `pEst` | `CHAR(30)` | — | — |
| `pCP` | `CHAR(5)` | — | — |
| `pEstDom` | `CHAR(1)` | — | — |
| `pTelSuc` | `CHAR(14)` | — | — |
| `pLatitud` | `CHAR(10)` | — | — |
| `pLongitud` | `CHAR(11)` | — | — |
| `pZonaHoraria` | `CHAR(20)` | — | — |
| `pSerLun` | `CHAR(1)` | — | — |
| `pSerMar` | `CHAR(1)` | — | — |
| `pSerMie` | `CHAR(1)` | — | — |
| `pSerJue` | `CHAR(1)` | — | — |
| `pSerVie` | `CHAR(1)` | — | — |
| `pSerSab` | `CHAR(1)` | — | — |
| `pSerDom` | `CHAR(1)` | — | — |
| `pLunAp` | `DATETIME YEAR to SECOND` | — | — |
| `pLunCi` | `DATETIME YEAR to SECOND` | — | — |
| `pMarAp` | `DATETIME YEAR to SECOND` | — | — |
| `pMarCi` | `DATETIME YEAR to SECOND` | — | — |
| `pMieAp` | `DATETIME YEAR to SECOND` | — | — |
| `pMieCi` | `DATETIME YEAR to SECOND` | — | — |
| `pJueAp` | `DATETIME YEAR to SECOND` | — | — |
| `pJueCi` | `DATETIME YEAR to SECOND` | — | — |
| `pVieAp` | `DATETIME YEAR to SECOND` | — | — |
| `pVieCi` | `DATETIME YEAR to SECOND` | — | — |
| `pSabAp` | `DATETIME YEAR to SECOND` | — | — |
| `pSabCi` | `DATETIME YEAR to SECOND` | — | — |
| `pDomAp` | `DATETIME YEAR to SECOND` | — | — |
| `pDomCi` | `DATETIME YEAR to SECOND` | — | — |
| `pClasificacion` | `CHAR(40)` | — | — |
| `pCveSucEje` | `CHAR(40)` | — | — |
| `pM2` | `FLOAT` | — | — |
| `pNumVent` | `INTEGER` | — | — |
| `pNumEsc` | `INTEGER` | — | — |
| `pNumATM` | `INTEGER` | — | — |
| `pZonaEco` | `CHAR(30)` | — | — |
| `pCveGer` | `CHAR(4)` | — | — |
| `pZbcve` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cIdSucursal` | `CHAR(4)` | L13 |
| `iTotal` | `INTEGER` | L14 |
| `bEstDom` | `BOOLEAN` | L15 |
| `bSerLun` | `BOOLEAN` | L16 |
| `bSerMar` | `BOOLEAN` | L17 |
| `bSerMie` | `BOOLEAN` | L18 |
| `bSerJue` | `BOOLEAN` | L19 |
| `bSerVie` | `BOOLEAN` | L20 |
| `bSerSab` | `BOOLEAN` | L21 |
| `bSerDom` | `BOOLEAN` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_info_sucur_gcb` | `bdinteg` | ⚠️ sí | SELECT | L227 |
| `si_info_sucur_gcb` | `bdinteg` | ⚠️ sí | UPDATE | L243 |
| `si_info_sucur_gcb` | `bdinteg` | ⚠️ sí | INSERT | L311 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L217 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L238 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |
| L309 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_actualizasufijospm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizasufijospm.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizasufijospm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdOperacion                 CHAR(1)
  pCodSufijo                   CHAR(2)
  pDescSufijo                  CHAR(60)
  pStatus                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdOperacion` | `CHAR(1)` | — | — |
| `pCodSufijo` | `CHAR(2)` | — | — |
| `pDescSufijo` | `CHAR(60)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `codSufijo` | `CHAR(2)` | L8 |
| `descSufijo` | `CHAR(60)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `iRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sufijos` | `bdinteg` | ⚠️ sí | SELECT | L45 |
| `si_sufijos` | `bdinteg` | ⚠️ sí | INSERT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00520';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?sufij` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sufij`, `?pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizatipo_gcb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizatipo_gcb.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 155 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizatipo_gcb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCve_tipo_a                  CHAR(3)
  pCve_tipo                    CHAR(3)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCve_tipo_a` | `CHAR(3)` | `tipo`=tipo de | ✅ CÓDIGO |
| `pCve_tipo` | `CHAR(3)` | `tipo`=tipo de | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCve_tipo` | `CHAR(3)` | L6 |
| `cTipo` | `CHAR(40)` | L7 |
| `iTotal` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tipo_gcb` | `bdinteg` | ⚠️ sí | SELECT | L42 |
| `si_tipo_gcb` | `bdinteg` | ⚠️ sí | UPDATE | L55 |
| `si_tipo_gcb` | `bdinteg` | ⚠️ sí | INSERT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_gcb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gcb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizazona_gcb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_actualizazona_gcb.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 154 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza zona" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizazona_gcb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCve_zona_a                  CHAR(3)
  pZona_economica_a            CHAR(40)
  pCve_zona                    CHAR(3)
  pZona_economica              CHAR(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCve_zona_a` | `CHAR(3)` | `zona`=zona | ✅ CÓDIGO |
| `pZona_economica_a` | `CHAR(40)` | `zona`=zona | ✅ CÓDIGO |
| `pCve_zona` | `CHAR(3)` | `zona`=zona | ✅ CÓDIGO |
| `pZona_economica` | `CHAR(40)` | `zona`=zona | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCve_zona` | `CHAR(3)` | L6 |
| `cZona_economica` | `CHAR(40)` | L7 |
| `iTotal` | `INTEGER` | L8 |
| `iId_zona` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_zona_gcb` | `bdinteg` | ⚠️ sí | SELECT | L44 |
| `si_zona_gcb` | `bdinteg` | ⚠️ sí | UPDATE | L57 |
| `si_zona_gcb` | `bdinteg` | ⚠️ sí | INSERT | L78 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `zona` | ENTIDAD | zona | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_gcb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gcb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_adm_consultabitacora_usuarios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_adm_consultabitacora_usuarios.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 79 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "administración/administrar bitácora y usuarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_adm_consultabitacora_usuarios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdUsuario                   CHAR(8)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pStatus                      CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iTotales` | `INTEGER` | L15 |
| `vIdUsuario` | `CHAR(8)` | L16 |
| `cNombreUusuario` | `CHAR(150)` | L17 |
| `cIdStatus` | `CHAR(1)` | L18 |
| `cDescStatus` | `CHAR(10)` | L19 |
| `cMovimiento` | `CHAR(200)` | L20 |
| `dFechaMovimiento` | `DATE` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_usuarios` | `bdinteg` | ⚠️ sí | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L90 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `adm` | ACCION | administración/administrar (abreviación de admin) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_adm_consultabitacora_usuarios_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_adm_consultabitacora_usuarios_totales.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 78 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "administración/administrar bitácora y usuarios (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_adm_consultabitacora_usuarios_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdUsuario                   CHAR(8)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pStatus                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pStatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iTotales` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_usuarios` | `bdinteg` | ⚠️ sí | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `adm` | ACCION | administración/administrar (abreviación de admin) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_adm_validacampos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_adm_validacampos.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 77 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "administración/administrar Campaña — campaña de cobranza o crédito y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_adm_validacampos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pCampo                       CHAR(100)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pCampo` | `CHAR(100)` | `camp`=Campaña — campaña de cobranza o crédito (sp_envio_camp_ctes, sp_actvig_camp — bdicobranza + bdicred) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotales` | `INTEGER` | L7 |
| `cNomCliente` | `CHAR(150)` | L8 |
| `cExiste` | `CHAR(20)` | L9 |
| `cCampo` | `CHAR(100)` | L10 |
| `cNombre1` | `CHAR(26)` | L11 |
| `cNombre2` | `CHAR(26)` | L12 |
| `cApell_paterno` | `CHAR(26)` | L13 |
| `cApell_materno` | `CHAR(26)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L54 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00088';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `adm` | ACCION | administración/administrar (abreviación de admin) | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `camp` | ENTIDAD | Campaña — campaña de cobranza o crédito (sp_envio_camp_ctes, | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

---

## `sp_administradorespm_complementoinfo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_administradorespm_complementoinfo.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Administrador — rol de usuario con privilegios administrativos y información (especial, complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 10/10/2024 |

### Firma

```sql
CREATE PROCEDURE sp_administradorespm_complementoinfo(
  pNumCteEmp                   CHAR(20)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCteEmp` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INTEGER` | L24 |
| `cCodRet` | `CHAR(6)` | L25 |
| `cMensaje` | `CHAR(50)` | L26 |
| `iContAdmin` | `INTEGER` | L27 |
| `pAdminTipo` | `CHAR(3)` | L29 |
| `pIdAdmin` | `CHAR(30)` | L30 |
| `pApellidoPater` | `CHAR(30)` | L31 |
| `pApellidoMater` | `CHAR(30)` | L32 |
| `pNombre1` | `CHAR(30)` | L33 |
| `pNombre2` | `CHAR(30)` | L34 |
| `pTotal_admin` | `SMALLINT` | L35 |
| `pTotal_oper` | `SMALLINT` | L36 |
| `pTotal_Tokens` | `SMALLINT` | L37 |
| `pFolio_activa` | `CHAR(12)` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | ⚠️ sí | SELECT | L78 |
| `bei_contratacion` | `bdibei` | ⚠️ sí | SELECT | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet             = '00000';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L87 | FÓRMULA | `LET pTotal_Tokens = pTotal_admin + pTotal_oper;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?istrador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?m_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?lemento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?istrador`, `?m_`, `?lemento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_adminitasas_cargarchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_adminitasas_cargarchivo.sql` |
| **LOC (1er CREATE)** | 1635 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga Administrador — rol de usuario con privilegios administrativos, tasa y CHI — formato/protocolo de consulta al Buró de Crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_adminitasas_cargarchivo(
  pUsuario                     CHAR(9)
  pIdFuncion                   CHAR(8)
  pRutaArchivo                 CHAR(120)
  pNombreArchivo               CHAR(35)
  pBandera                     CHAR(1)
  pProducto                    CHAR(4)
) RETURNING CHAR(5)   AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(9)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pRutaArchivo` | `CHAR(120)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |
| `pNombreArchivo` | `CHAR(35)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |
| `pBandera` | `CHAR(1)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cSQL` | `CHAR(500)` | L8 |
| `cRuta` | `CHAR(200)` | L9 |
| `cMensaje` | `CHAR(200)` | L10 |
| `cCanal` | `CHAR(20)` | L11 |
| `dTasa` | `DECIMAL(14,6)` | L12 |
| `iContador` | `INTEGER` | L13 |
| `dCapitalmax` | `CHAR(10)` | L14 |
| `dCapitalmin` | `CHAR(10)` | L15 |
| `iplazoInicio` | `INTEGER` | L16 |
| `iplazoVencimiento` | `INTEGER` | L17 |
| `cErrores` | `CHAR(1)` | L18 |
| `iPromocion` | `INTEGER` | L19 |
| `iPromocion2` | `INTEGER` | L20 |
| `cSucursal` | `CHAR(5)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `iBanCuenta` | `SMALLINT` | L23 |
| `iRenovacion` | `SMALLINT` | L24 |
| `cNumcte` | `CHAR(20)` | L25 |
| `cNombre` | `CHAR(150)` | L26 |
| `iBanPromo` | `SMALLINT` | L27 |
| `iNumFila` | `INTEGER` | L28 |
| `iValido` | `INTEGER` | L29 |
| `cTasaReno` | `CHAR(10)` | L30 |
| *…25 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_admintasas_bitacoraerror` | `bdinvers` | ⚠️ sí | SELECT | L117 |
| `sw_verificastatus_validarchivotxt` | `bdicnweb` | no | UPDATE | L120 |
| `sw_verificastatus_validarchivotxt` | `bdicnweb` | no | SELECT | L146 |
| `sw_verificastatus_validarchivotxt` | `bdicnweb` | no | DELETE | L146 |
| `sw_verificastatus_validarchivotxt` | `bdicnweb` | no | INSERT | L148 |
| `sw_admintasascarga_promociones` | `bdicnweb` | no | SELECT | L173 |
| `sw_admintasascarga_promociones` | `bdicnweb` | no | DELETE | L173 |
| `sw_admintasascarga_tasas` | `bdicnweb` | no | SELECT | L174 |
| `sw_admintasascarga_tasas` | `bdicnweb` | no | DELETE | L174 |
| `sw_admintasascarga_sucursales` | `bdicnweb` | no | SELECT | L175 |
| `sw_admintasascarga_sucursales` | `bdicnweb` | no | DELETE | L175 |
| `sw_admintasascarga_clientes` | `bdicnweb` | no | SELECT | L176 |
| `sw_admintasascarga_clientes` | `bdicnweb` | no | DELETE | L176 |
| `sw_admintasascarga_cuentas` | `bdicnweb` | no | SELECT | L177 |
| `sw_admintasascarga_cuentas` | `bdicnweb` | no | DELETE | L177 |
| `sw_admintasascarga_renovaciones` | `bdicnweb` | no | SELECT | L178 |
| `sw_admintasascarga_renovaciones` | `bdicnweb` | no | DELETE | L178 |
| `informix` | `bdicnweb` | no | INSERT | L201 |
| `statistics` | `bdicnweb` | no | UPDATE | L217 |
| `sw_admintasascarga_promociones` | `bdicnweb` | no | INSERT | L253 |
| `sw_admintasascarga_temp` | `bdicnweb` | no | SELECT | L256 |
| `sw_admintasascarga_tasas` | `bdicnweb` | no | INSERT | L259 |
| `sw_admintasascarga_sucursales` | `bdicnweb` | no | INSERT | L264 |
| `sw_admintasascarga_clientes` | `bdicnweb` | no | INSERT | L269 |
| `sw_admintasascarga_cuentas` | `bdicnweb` | no | INSERT | L274 |
| `sw_admintasascarga_renovaciones` | `bdicnweb` | no | INSERT | L279 |
| `sv_admintasas_bitacoraerror` | `bdinvers` | ⚠️ sí | INSERT | L307 |
| `si_tasavlor` | `bdinteg` | ⚠️ sí | SELECT | L490 |
| `sv_camp_bitacora` | `bdinvers` | ⚠️ sí | INSERT | L1587 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L162 |
| `sp_validadecimales` | `bdicnweb` | no | L354 |
| `sp_validafechacarga` | `bdicnweb` | no | L443 |
| `sp_validaenteros` | `bdicnweb` | no | L601 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '01282';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L298 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L300 | VALIDACIÓN_NULL | `IF (iPromocion IS NULL OR iPromocion = '')  THEN` |  |
| L313 | VALIDACIÓN_NULL | `IF (cNombre IS NULL OR cNombre = '')  THEN` |  |
| L473 | VALIDACIÓN_NULL | `IF cCanal IS NULL OR UPPER(TRIM(cCanal)) NOT IN  ('SUCURSAL', 'APP', 'PORTAL', 'ATM')THEN` |  |
| L633 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L695 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L719 | VALIDACIÓN_NULL | `IF (cNombre IS NULL OR cNombre = '')  THEN` |  |
| L833 | VALIDACIÓN_NULL | `IF cCanal IS NULL OR UPPER(TRIM(cCanal)) NOT IN  ("SUCURSAL", 'APP', 'PORTAL', 'ATM')THEN` |  |
| L881 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L951 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L988 | FÓRMULA | `LET iTotalCeros = 4 - LENGTH(TRIM(cSucursal));` |  |
| L1059 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L1164 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L1374 | FÓRMULA | `LET iTotalCeros = 9 - LENGTH(TRIM(cNumcte));` |  |
| L1388 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L1472 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?vo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?i`, `?s_`, `?r`, `?vo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_adminitasas_ope_guardainfo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_adminitasas_ope_guardainfo.sql` |
| **LOC (1er CREATE)** | 580 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación Administrador — rol de usuario con privilegios administrativos, tasa y información" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_calculagat_promocion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_adminitasas_ope_guardainfo(
  pUsuario                     CHAR(9)
  pIdFuncion                   CHAR(8)
  pBandera                     CHAR(1)
  pErrores                     CHAR(1)
  pProducto                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)       AS codret,-- 0
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(9)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pErrores` | `CHAR(1)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L31 |
| `iSqlErr` | `INTEGER` | L32 |
| `iRegistro` | `INTEGER` | L33 |
| `iIdpromocion` | `INTEGER` | L34 |
| `iPromocionfinal` | `INTEGER` | L35 |
| `cNombreEstrategia` | `CHAR(100)` | L36 |
| `cCanal` | `CHAR(10)` | L37 |
| `dCapitalmin` | `MONEY(14,2)` | L38 |
| `dCapitalmax` | `MONEY(14,2)` | L39 |
| `iplazoInicio` | `INTEGER` | L40 |
| `iplazoVencimiento` | `INTEGER` | L41 |
| `dFechaInicio` | `DATE` | L42 |
| `dFechaVencimiento` | `DATE` | L43 |
| `dTasa` | `DECIMAL(9,6)` | L44 |
| `dGatNominal` | `DECIMAL(9,6)` | L45 |
| `dGatReal` | `DECIMAL(9,6)` | L46 |
| `iCodEstatus` | `SMALLINT` | L47 |
| `cSucursal` | `CHAR(4)` | L48 |
| `cNumCuenta` | `CHAR(20)` | L49 |
| `cNumcte` | `CHAR(20)` | L50 |
| `iSecuencia` | `SMALLINT` | L51 |
| `dTasaRenovacion` | `DECIMAL(9,6)` | L52 |
| `iContador` | `INTEGER` | L53 |
| `cCodRet2` | `CHAR(5)` | L54 |
| `iCodRetSp` | `INTEGER` | L55 |
| *…8 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_admintasascarga_promociones` | `bdicnweb` | no | SELECT | L144 |
| `sw_admintasascarga_sucursales` | `bdicnweb` | no | SELECT | L195 |
| `sw_admintasascarga_cuentas` | `bdicnweb` | no | SELECT | L210 |
| `sw_admintasascarga_clientes` | `bdicnweb` | no | SELECT | L225 |
| `sw_admintasascarga_renovaciones` | `bdicnweb` | no | SELECT | L240 |
| `sw_admintasascarga_tasas` | `bdicnweb` | no | SELECT | L253 |
| `sv_admintasas_pagare` | `bdinvers` | ⚠️ sí | INSERT | L321 |
| `sv_admintasas_estatus` | `bdinvers` | ⚠️ sí | INSERT | L330 |
| `sv_sucursales_promocion` | `bdinvers` | ⚠️ sí | INSERT | L335 |
| `sv_cuentas_promocion` | `bdinvers` | ⚠️ sí | INSERT | L344 |
| `sv_clientes_promocion` | `bdinvers` | ⚠️ sí | INSERT | L353 |
| `sv_admintasas_renovacion` | `bdinvers` | ⚠️ sí | INSERT | L368 |
| `si_admintasas_inv_tasames` | `bdinvers` | ⚠️ sí | INSERT | L385 |
| `sv_camp_bitacora` | `bdinvers` | ⚠️ sí | INSERT | L391 |
| `sc_admintasas_invcreciente` | `bdicheq` | ⚠️ sí | INSERT | L447 |
| `sc_admintasas_inv_estatus` | `bdicheq` | ⚠️ sí | INSERT | L454 |
| `sc_admintasas_inv_sucursales` | `bdicheq` | ⚠️ sí | INSERT | L460 |
| `sc_admintasas_inv_clientes` | `bdicheq` | ⚠️ sí | INSERT | L469 |
| `si_admintasas_inv_tasames` | `bdinteg` | ⚠️ sí | INSERT | L488 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L128 |
| `sp_calculagat_promocion` | `bdinvers` | ⚠️ sí | L148 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L66 | CÓDIGO_RETORNO | `LET cCodRet          = '00000';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L153 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinvers:sp_calculagat_promocion";` |  |
| L160 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L175 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinvers:sp_calculagat_promocion";` |  |
| L182 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L198 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L213 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L229 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L243 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L260 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L282 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinvers:sp_calculagat_promocion";` |  |
| L289 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L427 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinvers:sp_calculagat_promocion";` |  |
| L434 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L527 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L533 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L543 | CÓDIGO_RETORNO | `LET cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?i`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_admintasas_actualizastatuspagare`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_admintasas_actualizastatuspagare.sql` |
| **LOC (1er CREATE)** | 362 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza Administrador — rol de usuario con privilegios administrativos, tasa, estatus y pagaré" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_admintasas_actualizastatuspagare(
  pUsuario                     CHAR(9)
  pIdFuncion                   CHAR(8)
  pIdPromocion                 INTEGER
  pFechaVencimiento            DATE
  pGatNominal                  DECIMAL(9,6)
  pGatReal                     DECIMAL(9,6)
  pEstatus                     SMALLINT
  pProducto                    CHAR(4)
  pMontoSdoCte                 DECIMAL(14,2)
) RETURNING CHAR(5)       AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(9)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pIdPromocion` | `INTEGER` | — | — |
| `pFechaVencimiento` | `DATE` | — | — |
| `pGatNominal` | `DECIMAL(9,6)` | — | — |
| `pGatReal` | `DECIMAL(9,6)` | — | — |
| `pEstatus` | `SMALLINT` | `status`=estatus | 🔵 CONVENCIÓN |
| `pProducto` | `CHAR(4)` | — | — |
| `pMontoSdoCte` | `DECIMAL(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `iIdpromocion` | `INTEGER` | L16 |
| `dFechaVencimiento` | `date` | L17 |
| `dGatNominal` | `DECIMAL(9,6)` | L18 |
| `dGatReal` | `DECIMAL(9,6)` | L19 |
| `cNombreEstrategia` | `CHAR(100)` | L20 |
| `iEstatus` | `INTEGER` | L21 |
| `iBanMod` | `SMALLINT` | L22 |
| `iCodEstatusOrg` | `SMALLINT` | L23 |
| `iTotalPro1100` | `INTEGER` | L25 |
| `dMontoSdoCte` | `DECIMAL(14,2)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_admintasas_pagare` | `bdinvers` | ⚠️ sí | SELECT | L73 |
| `sv_admintasas_estatus` | `bdinvers` | ⚠️ sí | SELECT | L78 |
| `sv_admintasas_pagare` | `bdinvers` | ⚠️ sí | UPDATE | L91 |
| `sv_camp_bitacora` | `bdinvers` | ⚠️ sí | INSERT | L97 |
| `sv_admintasas_estatus` | `bdinvers` | ⚠️ sí | INSERT | L120 |
| `sc_admintasas_invcreciente` | `bdicheq` | ⚠️ sí | SELECT | L212 |
| `sc_admintasas_inv_estatus` | `bdicheq` | ⚠️ sí | SELECT | L223 |
| `sc_admintasas_inv_estatus` | `bdicheq` | ⚠️ sí | INSERT | L248 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet          = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdPromocion = '' OR pIdPromocion IS NULL OR pFechaVencimient` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | FÓRMULA | `LET iBanMod = 1; -- fue ditado` | 🔴 MONEY/aritmética financiera |
| L101 | FÓRMULA | `LET iBanMod = 1; -- fue ditado` | 🔴 MONEY/aritmética financiera |
| L106 | FÓRMULA | `LET iBanMod = 1; -- fue ditado` | 🔴 MONEY/aritmética financiera |
| L112 | FÓRMULA | `LET iBanMod = 1; -- fue ditado` | 🔴 MONEY/aritmética financiera |
| L207 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L335 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `pagare` | ENTIDAD | pagaré | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_admintasas_bitacoraerror`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_admintasas_bitacoraerror.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Administrador — rol de usuario con privilegios administrativos, tasa, bitácora y error" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_admintasas_bitacoraerror(
  pUsuario                     CHAR(9)
  pIdFuncion                   CHAR(8)
  pBandera                     SMALLINT
  pNombreHoja                  CHAR(120)
  pNombreCampo                 CHAR(50)
  pNumeroFila                  INTEGER
  pDescripcion                 CHAR(250)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)       AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(9)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pBandera` | `SMALLINT` | — | — |
| `pNombreHoja` | `CHAR(120)` | — | — |
| `pNombreCampo` | `CHAR(50)` | — | — |
| `pNumeroFila` | `INTEGER` | — | — |
| `pDescripcion` | `CHAR(250)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `iRegistro` | `INTEGER` | L22 |
| `cNombreHoja` | `CHAR(120)` | L23 |
| `cNombreCampo` | `CHAR(50)` | L24 |
| `iNumerofila` | `INTEGER` | L25 |
| `cDescripcion` | `CHAR(250)` | L26 |
| `cUsuario` | `CHAR(9)` | L27 |
| `cFecha_insert` | `DATE` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_admintasas_bitacoraerror` | `bdinvers` | ⚠️ sí | INSERT | L73 |
| `sv_admintasas_bitacoraerror` | `bdinvers` | ⚠️ sí | SELECT | L82 |
| `sv_admintasas_bitacoraerror` | `bdinvers` | ⚠️ sí | DELETE | L102 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet          = '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pBandera = '' OR pBandera IS NULL THEN` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | VALIDACIÓN_NULL | `IF pBandera = '1' AND  (pNombreHoja = ''  OR pNombreCampo = '' OR pNumeroFila IS NULL OR pNumeroFila` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_admintasas_consultabitacora`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_admintasas_consultabitacora.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos, tasa y bitácora" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_admintasas_consultabitacora(
  pUsuario                     CHAR(9)
  pIdFuncion                   CHAR(8)
  pBandera                     INTEGER
  pFuncionalidad               CHAR(1)
  pFechaDel                    DATE
  pFechaAl                     DATE
  pUsuario_insert              CHAR(9)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)                      AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(9)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pBandera` | `INTEGER` | — | — |
| `pFuncionalidad` | `CHAR(1)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |
| `pUsuario_insert` | `CHAR(9)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `dFecha` | `DATE` | L19 |
| `dHora` | `CHAR(12)` | L20 |
| `cUsuario` | `CHAR(9)` | L21 |
| `cFuncionalidad` | `CHAR(20)` | L22 |
| `cCampAnterior` | `CHAR(20)` | L23 |
| `cCampNuevo` | `CHAR(20)` | L24 |
| `cDescripcion` | `CHAR(200)` | L25 |
| `cProducto` | `CHAR(4)` | L26 |
| `cUsuarioMod` | `CHAR(9)` | L27 |
| `iRegistro` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_camp_bitacora` | `bdinvers` | ⚠️ sí | SELECT | L74 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet          = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRecuperacion IS NULL OR pRegistros IS NULL OR pFechaAl = '' ` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_admintasas_consultapagare`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_admintasas_consultapagare.sql` |
| **LOC (1er CREATE)** | 1848 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos, tasa y pagaré" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_admintasas_consultapagare(
  pUsuario                     CHAR(9)
  pIdFuncion                   CHAR(8)
  pProducto                    CHAR(4)
  pEstatus                     SMALLINT
  pIdPromocion                 INTEGER
  pNombrePromocion             CHAR(100)
  pCanal                       SMALLINT
  pCapitalMin                  DECIMAL(14,2)
  pCapitalMax                  DECIMAL(14,2)
  pFechaInicio                 DATE
  pFechaVencimiento            DATE
  pPlazo_inicio                INTEGER
  pPlazo_vencimiento           INTEGER
  pCampoOrden                  SMALLINT
  pOrderBy                     SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)       AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(9)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pEstatus` | `SMALLINT` | — | — |
| `pIdPromocion` | `INTEGER` | — | — |
| `pNombrePromocion` | `CHAR(100)` | — | — |
| `pCanal` | `SMALLINT` | — | — |
| `pCapitalMin` | `DECIMAL(14,2)` | — | — |
| `pCapitalMax` | `DECIMAL(14,2)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaVencimiento` | `DATE` | — | — |
| `pPlazo_inicio` | `INTEGER` | — | — |
| `pPlazo_vencimiento` | `INTEGER` | — | — |
| `pCampoOrden` | `SMALLINT` | — | — |
| `pOrderBy` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCampoOrden` | `INTEGER` | L40 |
| `cCodRet` | `CHAR(5)` | L41 |
| `iSqlErr` | `INTEGER` | L42 |
| `iRegistro` | `INTEGER` | L43 |
| `iIdpromocion` | `INTEGER` | L44 |
| `cNombreEstrategia` | `CHAR(100)` | L45 |
| `cCanal` | `SMALLINT` | L46 |
| `dCapitalmin` | `DECIMAL(14,2)` | L47 |
| `dCapitalmax` | `DECIMAL(14,2)` | L48 |
| `iplazoInicio` | `INTEGER` | L49 |
| `iplazoVencimiento` | `INTEGER` | L50 |
| `dFechaInicio` | `CHAR(10)` | L51 |
| `dFechaVencimiento` | `CHAR(10)` | L52 |
| `dTasa` | `DECIMAL(9,6)` | L53 |
| `dGatNominal` | `DECIMAL(9,6)` | L54 |
| `dGatReal` | `DECIMAL(9,6)` | L55 |
| `iCodEstatus` | `SMALLINT` | L56 |
| `iCodEstatusOrg` | `SMALLINT` | L57 |
| `cCmd1` | `CHAR(6500)` | L58 |
| `vOrdenamiento` | `VARCHAR(50)` | L59 |
| `cCmd2` | `CHAR(6500)` | L60 |
| `cRequiereSdoNuevo` | `CHAR(2)` | L62 |
| `dMontoSdoNuevo` | `DECIMAL(14,2)` | L63 |
| `cRequiereInstrApertura` | `CHAR(2)` | L64 |
| `cInstrumentoApertura` | `CHAR(50)` | L65 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_admintasas_pagare_estatus_temp` | `bdicnweb` | no | SELECT | L146 |
| `sw_admintasas_pagare_estatus_temp` | `bdicnweb` | no | DELETE | L146 |
| `sv_admintasas_pagare` | `bdinvers` | ⚠️ sí | SELECT | L152 |
| `sv_admintasas_estatus` | `bdinvers` | ⚠️ sí | SELECT | L152 |
| `sc_admintasas_invcreciente` | `bdicheq` | ⚠️ sí | SELECT | L181 |
| `sc_admintasas_inv_estatus` | `bdicheq` | ⚠️ sí | SELECT | L181 |
| `sw_admintasas_pagare_estatus_temp` | `bdicnweb` | no | INSERT | L257 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L132 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | CÓDIGO_RETORNO | `LET cCodRet          = '00000';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L206 | VALIDACIÓN_NULL | `IF pCampoOrden IS NULL AND (pOrderBy = 1 OR pOrderBy = '') THEN` |  |
| L210 | VALIDACIÓN_NULL | `IF pCampoOrden IS NULL AND pOrderBy = 2 THEN` |  |
| L247 | VALIDACIÓN_NULL | `IF iCodEstatusOrg IS NULL OR iCodEstatus = '' THEN` |  |
| L255 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L261 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L276 | VALIDACIÓN_NULL | `IF iCodEstatusOrg IS NULL OR iCodEstatus = '' THEN` |  |
| L284 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L290 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L324 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L365 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L412 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L425 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L465 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L507 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L547 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L590 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L631 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L674 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L714 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L757 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L801 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L843 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L884 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L925 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L966 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| L1011 | FÓRMULA | `LET iRegistro = iRegistro + 1;` |  |
| | *…19 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pagare` | ENTIDAD | pagaré | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_admintasas_detallegat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_admintasas_detallegat.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Administrador — rol de usuario con privilegios administrativos, tasa y detalle" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_admintasas_detallegat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pPromocion                   INTEGER
  pMes                         SMALLINT
  pGatNominal                  DECIMAL (9,6)
  pGatReal                     DECIMAL(9,6)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pPromocion` | `INTEGER` | — | — |
| `pMes` | `SMALLINT` | — | — |
| `pGatNominal` | `DECIMAL (9,6)` | — | — |
| `pGatReal` | `DECIMAL(9,6)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `iRecuperacion` | `INTEGER` | L15 |
| `sMesDetalle` | `SMALLINT` | L16 |
| `dTasaDetalle` | `DECIMAL(4,2)` | L17 |
| `dGatNominalDetalle` | `DECIMAL(9,6)` | L18 |
| `dGatRealDetalle` | `DECIMAL(9,6)` | L19 |
| `iTotalRegistros` | `INTEGER` | L20 |
| `sCodEstatus` | `SMALLINT` | L21 |
| `iRegSeleccionado` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_admintasas_inv_tasames` | `bdinteg` | ⚠️ sí | SELECT | L71 |
| `sv_camp_bitacora` | `bdinvers` | ⚠️ sí | INSERT | L109 |
| `si_admintasas_inv_tasames` | `bdinteg` | ⚠️ sí | UPDATE | L118 |
| `sv_camp_bitacora` | `bdinvers` | ⚠️ sí | UPDATE | L123 |
| `sc_admintasas_inv_estatus` | `bdicheq` | ⚠️ sí | SELECT | L136 |
| `sc_admintasas_inv_estatus` | `bdicheq` | ⚠️ sí | UPDATE | L141 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pBandera = '' OR pPromocion IS NULL OR pRegistros IS NULL OR ` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L90 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?gat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_`, `?gat` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_altadministradorespmempnet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_altadministradorespmempnet.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 75 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta Empresa — empleadora del cliente; vinculada a crédito de nómina (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_administradorespm`, `sp_mod_administradorespm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_altadministradorespmempnet(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(9)
  pCodIden                     CHAR(3)
  pNumIden                     CHAR(30)
  pApellPat                    CHAR(30)
  pApellMat                    CHAR(30)
  pNombre1                     CHAR(30)
  pNombre2                     CHAR(30)
  pRepLegal                    CHAR(1)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pCodIden` | `CHAR(3)` | — | — |
| `pNumIden` | `CHAR(30)` | — | — |
| `pApellPat` | `CHAR(30)` | — | — |
| `pApellMat` | `CHAR(30)` | — | — |
| `pNombre1` | `CHAR(30)` | — | — |
| `pNombre2` | `CHAR(30)` | — | — |
| `pRepLegal` | `CHAR(1)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cNumCliente` | `CHAR(20)` | L13 |
| `cFolioToken` | `CHAR(12)` | L14 |
| `MensajeRet` | `CHAR(50)` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_regbitacora_empresanet` | `bdicnweb` | no | INSERT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_administradorespm` | `bdibei` | ⚠️ sí | L53 |
| `sp_mod_administradorespm` | `bdibei` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdibei:sp_administradorespm';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdibei:sp_administradorespm';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?dministrador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dministrador`, `?m`, `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_altadministradorespmempnet2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_altadministradorespmempnet2.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=34 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta Empresa — empleadora del cliente; vinculada a crédito de nómina (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_administradorespm`, `sp_mod_administradorespm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_altadministradorespmempnet2(
  pUsuario                     CHAR(8)
  pNumCliente                  CHAR(9)
  pCodIden                     CHAR(3)
  pNumIden                     CHAR(30)
  pApellPat                    CHAR(30)
  pApellMat                    CHAR(30)
  pNombre1                     CHAR(30)
  pNombre2                     CHAR(30)
  pRepLegal                    CHAR(1)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pCodIden` | `CHAR(3)` | — | — |
| `pNumIden` | `CHAR(30)` | — | — |
| `pApellPat` | `CHAR(30)` | — | — |
| `pApellMat` | `CHAR(30)` | — | — |
| `pNombre1` | `CHAR(30)` | — | — |
| `pNombre2` | `CHAR(30)` | — | — |
| `pRepLegal` | `CHAR(1)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cNumCliente` | `CHAR(20)` | L12 |
| `cFolioToken` | `CHAR(12)` | L13 |
| `MensajeRet` | `CHAR(50)` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_regbitacora_empresanet` | `bdicnweb` | no | INSERT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_administradorespm` | `bdibei` | ⚠️ sí | L46 |
| `sp_mod_administradorespm` | `bdibei` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdibei:sp_administradorespm';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L68 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdibei:sp_administradorespm';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?dministrador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `?net2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dministrador`, `?m`, `?net2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_altaservicioempnet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_altaservicioempnet.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 73 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta servicio y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_senet_altaservicioempresanet`, `sp_mod_senet_altaservicioempresanet` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_altaservicioempnet(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pCantTokens                  SMALLINT
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pCantTokens` | `SMALLINT` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `MensajeRet` | `CHAR(100)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_regbitacora_empresanet` | `bdicnweb` | no | INSERT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_senet_altaservicioempresanet` | `bdibei` | ⚠️ sí | L45 |
| `sp_mod_senet_altaservicioempresanet` | `bdibei` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdibei:sp_senet_altaservicioem` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdibei:sp_senet_altaservicioem` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_altaservicioempnet2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_altaservicioempnet2.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta servicio y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_senet_altaservicioempresanet`, `sp_mod_senet_altaservicioempresanet` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_altaservicioempnet2(
  pUsuario                     CHAR(8)
  pNumCliente                  CHAR(20)
  pCantTokens                  SMALLINT
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pCantTokens` | `SMALLINT` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `MensajeRet` | `CHAR(100)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_regbitacora_empresanet` | `bdicnweb` | no | INSERT | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_senet_altaservicioempresanet` | `bdibei` | ⚠️ sí | L39 |
| `sp_mod_senet_altaservicioempresanet` | `bdibei` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdibei:sp_senet_altaservicioemp` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdibei:sp_senet_altaservicioemp` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?net2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?net2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_aplicabonocuenta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_aplicabonocuenta.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=14 / fan_out=9 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "aplica cuenta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena`, `abono_cta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_aplicabonocuenta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
  pTramaConsecutivo            CHAR(250)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pTramaConsecutivo` | `CHAR(250)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRetSp` | `CHAR(35)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iIdConsCheque` | `INTEGER` | L9 |
| `cFechaHoy` | `CHAR(10)` | L10 |
| `cFormatFechaHoy` | `CHAR(10)` | L11 |
| `cCuenta` | `CHAR(20)` | L12 |
| `iCheque` | `INTEGER` | L13 |
| `mImporte` | `MONEY(16,2)` | L14 |
| `cMotivoDevolucion` | `CHAR(38)` | L15 |
| `cRevisado` | `CHAR(1)` | L16 |
| `cAbonoAplicado` | `CHAR(1)` | L17 |
| `cNombreArchivo` | `CHAR(22)` | L18 |
| `iSecuencia` | `INTEGER` | L19 |
| `cCodMotivo` | `CHAR(2)` | L20 |
| `cDescMotivo` | `CHAR(35)` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | SELECT | L72 |
| `cce_propios_det` | `bditef` | ⚠️ sí | UPDATE | L79 |
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | UPDATE | L89 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |
| `sp_split_cadena` | `bdicnweb` | no | L67 |
| `abono_cta` | `bditef` | ⚠️ sí | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?bono` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cuenta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bono` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_aplicacargaarchcecoban`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_aplicacargaarchcecoban.sql` |
| **LOC (1er CREATE)** | 475 |
| **Callgraph** | ✅ fan_in=9 / fan_out=14 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "aplica archivo y cob — cobranza" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `abono_cta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_aplicacargaarchcecoban(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pDireccionMac                CHAR(12)
  pCodOperacion                CHAR(5)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `arch`=archivo | 🟡 INFERIDO |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pCodOperacion` | `CHAR(5)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `cDescCodRetSp` | `CHAR(35)` | L8 |
| `cEncTipoRegistro` | `CHAR(2)` | L9 |
| `cEncNumSecuencia` | `CHAR(7)` | L10 |
| `cEncNumBanco` | `CHAR(3)` | L11 |
| `cEncSentidoTransfer` | `CHAR(1)` | L12 |
| `cEncPlazaCecoban` | `CHAR(2)` | L13 |
| `cEncServicioTei` | `CHAR(1)` | L14 |
| `cEncDiaMesTransfer` | `CHAR(2)` | L15 |
| `cEncNumBloque` | `CHAR(5)` | L16 |
| `cEncFechaPresenta` | `CHAR(8)` | L17 |
| `cEncTipoArchivo` | `CHAR(1)` | L18 |
| `cDetDatosCodOperacion` | `CHAR(2)` | L19 |
| `cDetDatosNumCuenta` | `CHAR(22)` | L20 |
| `cDetDatosNumCheque` | `INTEGER` | L21 |
| `cDetDatosMotivoDevol` | `CHAR(50)` | L22 |
| `cDetTipoRegistro` | `CHAR(2)` | L23 |
| `cDetNumSecuencia` | `CHAR(7)` | L24 |
| `cDetCodOperacion` | `CHAR(2)` | L25 |
| `cDetFechaTrasnfer` | `CHAR(8)` | L26 |
| `cDetBancoCedente` | `CHAR(3)` | L27 |
| `cDetBancoLibrado` | `CHAR(3)` | L28 |
| *…50 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_procesaencabezado_tmp` | `bdicnweb` | no | SELECT | L185 |
| `cce_propios_enc` | `bditef` | ⚠️ sí | SELECT | L195 |
| `cce_propios_enc` | `bditef` | ⚠️ sí | INSERT | L201 |
| `sw_cr_procesadetalle_tmp` | `bdicnweb` | no | SELECT | L237 |
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L246 |
| `sw_cr_procesadetalle_tmp` | `bdicnweb` | no | UPDATE | L257 |
| `cce_propios_det` | `bditef` | ⚠️ sí | UPDATE | L265 |
| `sc_contch` | `bdicheq` | ⚠️ sí | UPDATE | L275 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L319 |
| `cce_cheques_img` | `bditef` | ⚠️ sí | SELECT | L321 |
| `cce_propios_det` | `bditef` | ⚠️ sí | INSERT | L362 |
| `sw_cr_procesasumario_tmp` | `bdicnweb` | no | SELECT | L390 |
| `cce_propios_sum` | `bditef` | ⚠️ sí | SELECT | L399 |
| `cce_propios_sum` | `bditef` | ⚠️ sí | INSERT | L406 |
| `sw_cr_procesagransumario_tmp` | `bdicnweb` | no | SELECT | L426 |
| `cce_propios_gsum` | `bditef` | ⚠️ sí | SELECT | L431 |
| `cce_propios_gsum` | `bditef` | ⚠️ sí | INSERT | L436 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | INSERT | L452 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | SELECT | L455 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L172 |
| `abono_cta` | `bditef` | ⚠️ sí | L252 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L166 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pDireccionMac = '' OR pCodOperacion = ` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L216 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L240 | FÓRMULA | `LET mImporte = SUBSTR(cDetImporte,1,(LENGTH(cDetImporte)-2))\|\|"."\|\|SUBSTR(cDetImporte,(LENGTH(cD` | 🔴 MONEY/aritmética financiera |
| L271 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L279 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L304 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L375 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L393 | VALIDACIÓN_NULL | `IF cSumImporte IS NULL OR NVL(cSumImporte,0) = 0 THEN` |  |
| L416 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L446 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `?ce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cob` | ENTIDAD | cob — cobranza (abreviación de dominio — sp_repcob_*, sp_obt | 🔵 CONVENCIÓN | nombre_sp |
| `?an` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ce`, `?an` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_aplicacargaarchimgcecoban`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_aplicacargaarchimgcecoban.sql` |
| **LOC (1er CREATE)** | 202 |
| **Callgraph** | ✅ fan_in=9 / fan_out=2 |
| **Propósito inferido** | "aplica archivo y cob — cobranza" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `ins_img_det` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_aplicacargaarchimgcecoban(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pDireccionMac                CHAR(12)
  pFecha                       DATE
  pStatusImgF                  CHAR(1)
  pStatusImgT                  CHAR(1)
  pIdDetalle                   INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `arch`=archivo | 🟡 INFERIDO |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pStatusImgF` | `CHAR(1)` | — | — |
| `pStatusImgT` | `CHAR(1)` | — | — |
| `pIdDetalle` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(35)` | L9 |
| `cFormatFecha` | `CHAR(10)` | L11 |
| `cDia` | `CHAR(2)` | L12 |
| `cMiBanco` | `CHAR(3)` | L13 |
| `iHayDatos` | `INTEGER` | L14 |
| `cDetDatosNombreArch` | `CHAR(22)` | L15 |
| `cDetDatosNumCuenta` | `CHAR(22)` | L16 |
| `iDetDatosNumCheque` | `INTEGER` | L17 |
| `cDetDatosImporte` | `MONEY (14,2)` | L18 |
| `cDatosArchImgF` | `CHAR(100)` | L19 |
| `cDatosArchImgT` | `CHAR(100)` | L20 |
| `cDatosTamImgF` | `CHAR(15)` | L21 |
| `cDatosTamImgT` | `CHAR(15)` | L22 |
| `cDatosCargaImgF` | `CHAR(1)` | L23 |
| `cDatosCargaImgT` | `CHAR(1)` | L24 |
| `iIdDetalle` | `INTEGER` | L25 |
| `cHuboError` | `CHAR(1)` | L26 |
| `iContDetalle` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L85 |
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | SELECT | L88 |
| `cce_propios_det` | `bditef` | ⚠️ sí | UPDATE | L130 |
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | UPDATE | L139 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L72 |
| `ins_img_det` | `bditef` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pDireccionMac = '' OR pFecha IS NULL O` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | FÓRMULA | `LET cFormatFecha = SUBSTR(pFecha,7,4)\|\|'/'\|\|SUBSTR(pFecha,1,2)\|\|'/'\|\|SUBSTR(pFecha,4,2);` |  |
| L122 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:ins_img_det';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L160 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:ins_img_det';` |  |
| L162 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L172 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L182 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `?imgce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cob` | ENTIDAD | cob — cobranza (abreviación de dominio — sp_repcob_*, sp_obt | 🔵 CONVENCIÓN | nombre_sp |
| `?an` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imgce`, `?an` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_aplicadevol_cod41_ccep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_aplicadevol_cod41_ccep.sql` |
| **LOC (1er CREATE)** | 175 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "aplica código y CCE — Cámara de Compensación Electrónica" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `bdinteg`, `ins_reg_devo`, `ins_reg_devo2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_aplicadevol_cod41_ccep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pImporteTotal                DECIMAL(14,2)
  pDireccionMac                CHAR(15)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pImporteTotal` | `DECIMAL(14,2)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cFechaHoy` | `CHAR(10)` | L10 |
| `idRowDetalle` | `INTEGER` | L11 |
| `cBancoLibrado` | `CHAR(3)` | L12 |
| `cDescbancoLibrado` | `CHAR(50)` | L13 |
| `mImporte` | `DECIMAL(14,2)` | L14 |
| `cCuentaReferencia` | `CHAR(13)` | L15 |
| `cNumCheque` | `CHAR(10)` | L16 |
| `cCuentaDeposito` | `CHAR(20)` | L17 |
| `cObservaciones` | `CHAR(70)` | L18 |
| `cMotivoDevolucion` | `CHAR(100)` | L19 |
| `cprocesar` | `CHAR(2)` | L20 |
| `cDescripSp` | `CHAR(50)` | L21 |
| `cMensaje_RET` | `CHAR(80)` | L22 |
| `contRegDevol` | `INTEGER` | L23 |
| `cTransacc` | `CHAR(1)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L77 |
| `ccep_procesacod41detalle_tmp` | `bdicnweb` | no | SELECT | L83 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L72 |
| `ins_reg_devo` | `bditef` | ⚠️ sí | L94 |
| `ins_reg_devo2` | `bditef` | ⚠️ sí | L97 |
| `bditef` | `bdicnweb` | no | L153 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L66 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pImporteTotal IS NULL OR pDireccionMac = '' THEN` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ins_reg_devo';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00761';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00762';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00763';` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00764';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00765';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00766';` |  |
| L139 | FÓRMULA | `LET contRegDevol = contRegDevol + 1;` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00781';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L158 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_tef_grab_arch_cam';` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00753';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?ol_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?41_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ol_`, `?41_`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_aplicadevolucioncheque`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_aplicadevolucioncheque.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ✅ fan_in=15 / fan_out=7 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "aplica cheque" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `abono_cta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_aplicadevolucioncheque(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMotivoDev                   CHAR(2)
  pFecha                       CHAR(8)
  pIdConsCheque                INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMotivoDev` | `CHAR(2)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pIdConsCheque` | `INTEGER` | `cheque`=cheque | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRetSp` | `CHAR(35)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iIdConsCheque` | `INTEGER` | L9 |
| `cFechaHoy` | `CHAR(10)` | L10 |
| `cFormatFechaHoy` | `CHAR(10)` | L11 |
| `cCuenta` | `CHAR(20)` | L12 |
| `iCheque` | `INTEGER` | L13 |
| `mImporte2` | `MONEY(16,2)` | L14 |
| `cMotivoDevolucion` | `CHAR(38)` | L15 |
| `cRevisado` | `CHAR(1)` | L16 |
| `cAbonoAplicado` | `CHAR(1)` | L17 |
| `cNombreArchivo` | `CHAR(22)` | L18 |
| `iSecuencia` | `INTEGER` | L19 |
| `cCodMotivo` | `CHAR(2)` | L21 |
| `cDescMotivo` | `CHAR(35)` | L22 |
| `iRecuperacion` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ccer_aplicaabonocheques` | `bdicnweb` | no | SELECT | L75 |
| `cce_propios_det` | `bditef` | ⚠️ sí | UPDATE | L88 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L97 |
| `sc_contch` | `bdicheq` | ⚠️ sí | UPDATE | L99 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | UPDATE | L107 |
| `si_coddevcam` | `bdinteg` | ⚠️ sí | SELECT | L115 |
| `sw_ccer_aplicaabonocheques` | `bdicnweb` | no | UPDATE | L117 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |
| `abono_cta` | `bditef` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L127 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:abono_cta';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_asigna_aut_solicitud_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_asigna_aut_solicitud_mc.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 110 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "asigna solicitud y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdisolic` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_asigna_aut_solicitud_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNumSolicitud` | `CHAR(21)` | L11 |
| `cNumCliente` | `CHAR(21)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L37 |
| `bdisolic` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00717';		END IF;` |  |
| L52 | FÓRMULA | `LET cCodRet = "00017"; --NO EXISTEN REGISTROS` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_asignacion_usuarios_autorizados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_asignacion_usuarios_autorizados.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "asigna usuarios y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cce_controlusuariosaut` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_asignacion_usuarios_autorizados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(2)
  pOperacion                   INTEGER
  pNumEjecut                   INTEGER
  pNombreEjecut                CHAR(100)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(2)` | — | — |
| `pOperacion` | `INTEGER` | — | — |
| `pNumEjecut` | `INTEGER` | — | — |
| `pNombreEjecut` | `CHAR(100)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cce_controlusuariosaut` | `bditef` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pOperacion IS NULL OR pNumEjecut IS NULL OR pNombreEjecut =  ` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_controlusuariosaut';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?cion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `autoriza` | ACCION | autoriza | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion_`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_asignacionctesusuariofusion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_asignacionctesusuariofusion.sql` |
| **LOC (1er CREATE)** | 288 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "asigna clientes y usuario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_asignacionctesusuariofusion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTamanioBloque               INTEGER
  pTipoOperacion               SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTamanioBloque` | `INTEGER` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L31 |
| `iSqlErr` | `INTEGER` | L32 |
| `iId_registro` | `INTEGER` | L33 |
| `cNumcte_1` | `CHAR(20)` | L34 |
| `cNumcte_2` | `CHAR(20)` | L35 |
| `cRfc_1` | `CHAR(13)` | L36 |
| `cRfc_2` | `CHAR(13)` | L37 |
| `cCte_correcto` | `CHAR(1)` | L38 |
| `cFlag_fusion` | `CHAR(1)` | L39 |
| `cCausa_no_fus` | `CHAR(2)` | L40 |
| `cEstatus_asig` | `CHAR(1)` | L41 |
| `cUser_asig` | `CHAR(8)` | L42 |
| `dFecha_dict` | `DATETIME YEAR TO FRACTION(3)` | L43 |
| `iRegistros` | `INTEGER` | L44 |
| `iRecuperacion` | `INTEGER` | L45 |
| `iNoRegistrosTotales` | `INTEGER` | L46 |
| `iNoRegistrosRecuperados` | `INTEGER` | L47 |
| `iNoRegsRecuperados` | `INTEGER` | L48 |
| `iRowId` | `INTEGER` | L49 |
| `cNombre1Cte1` | `CHAR(26)` | L51 |
| `cNombre2Cte1` | `CHAR(26)` | L52 |
| `cApPaterCte1` | `CHAR(26)` | L53 |
| `cApMaterCte1` | `CHAR(26)` | L54 |
| `cRfcCte1` | `CHAR(26)` | L55 |
| `dFechaNacCte1` | `DATE` | L56 |
| *…11 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_clientesasignados_soc` | `bdicnweb` | no | SELECT | L175 |
| `sw_tr_clientesasignados_soc` | `bdicnweb` | no | INSERT | L177 |
| `sw_tr_clientesduplicados` | `bdicnweb` | no | SELECT | L179 |
| `sw_tr_clientesduplicados` | `bdicnweb` | no | UPDATE | L185 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L236 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L142 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L122 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion IS NULL OR pRegistros IS NULL OR pRecuperacion` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L164 | VALIDACIÓN_NULL | `IF pTamanioBloque IS NULL THEN` |  |
| L165 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L191 | CÓDIGO_RETORNO | `LET cCodRet = '00362';` |  |
| L250 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L257 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L259 | CÓDIGO_RETORNO | `LET cCodRet = '00363';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fusion` | ACCION | fusiona cuentas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_asignasolanalistaaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_asignasolanalistaaumlincred.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 104 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "asigna solicitud y analista" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_asignasolanalistaaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pMonto                       DECIMAL(18,2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pMonto` | `DECIMAL(18,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRet` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(107)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iGuardoHistorica` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCredito = '' OR pMonto IS NULL THEN` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_asignasolanalista';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00253';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '90000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00254';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00255';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00256';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00257';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `analista` | ENTIDAD | analista | 🔵 CONVENCIÓN | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_atms_actualizaconcentracionatm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_actualizaconcentracionatm.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=106 |
| **Deps concatenadas** | 318 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cajeros automáticos y cajero automático" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_concen_atm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_actualizaconcentracionatm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pDivisa                      CHAR(2)
  pMonto                       MONEY(14,2)
  pPapeleta                    CHAR(16)
  pDenominacion1               CHAR(18)
  pDenominacion2               CHAR(18)
  pDenominacion3               CHAR(18)
  pDenominacion4               CHAR(18)
  pDenominacion5               CHAR(18)
  pDenominacion6               CHAR(18)
  pDenominacion7               CHAR(18)
  pDenominacion8               CHAR(18)
  pDenominacion9               CHAR(18)
  pDenominacion10              CHAR(18)
  pDenominacion11              CHAR(18)
  pDenominacion12              CHAR(18)
  pDenominacion13              CHAR(18)
  pDenominacion14              CHAR(18)
  pDenominacion15              CHAR(18)
  pCantidad1                   FLOAT(8)
  pCantidad2                   FLOAT(8)
  pCantidad3                   FLOAT(8)
  pCantidad4                   FLOAT(8)
  pCantidad5                   FLOAT(8)
  pCantidad6                   FLOAT(8)
  pCantidad7                   FLOAT(8)
  pCantidad8                   FLOAT(8)
  pCantidad9                   FLOAT(8)
  pCantidad10                  FLOAT(8)
  pCantidad11                  FLOAT(8)
  pCantidad12                  FLOAT(8)
  pCantidad13                  FLOAT(8)
  pCantidad14                  FLOAT(8)
  pCantidad15                  FLOAT(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |
| `pDivisa` | `CHAR(2)` | — | — |
| `pMonto` | `MONEY(14,2)` | — | — |
| `pPapeleta` | `CHAR(16)` | — | — |
| `pDenominacion1` | `CHAR(18)` | — | — |
| `pDenominacion2` | `CHAR(18)` | — | — |
| `pDenominacion3` | `CHAR(18)` | — | — |
| `pDenominacion4` | `CHAR(18)` | — | — |
| `pDenominacion5` | `CHAR(18)` | — | — |
| `pDenominacion6` | `CHAR(18)` | — | — |
| `pDenominacion7` | `CHAR(18)` | — | — |
| `pDenominacion8` | `CHAR(18)` | — | — |
| `pDenominacion9` | `CHAR(18)` | — | — |
| `pDenominacion10` | `CHAR(18)` | — | — |
| `pDenominacion11` | `CHAR(18)` | — | — |
| `pDenominacion12` | `CHAR(18)` | — | — |
| `pDenominacion13` | `CHAR(18)` | — | — |
| `pDenominacion14` | `CHAR(18)` | — | — |
| `pDenominacion15` | `CHAR(18)` | — | — |
| `pCantidad1` | `FLOAT(8)` | — | — |
| `pCantidad2` | `FLOAT(8)` | — | — |
| `pCantidad3` | `FLOAT(8)` | — | — |
| `pCantidad4` | `FLOAT(8)` | — | — |
| `pCantidad5` | `FLOAT(8)` | — | — |
| `pCantidad6` | `FLOAT(8)` | — | — |
| `pCantidad7` | `FLOAT(8)` | — | — |
| `pCantidad8` | `FLOAT(8)` | — | — |
| `pCantidad9` | `FLOAT(8)` | — | — |
| `pCantidad10` | `FLOAT(8)` | — | — |
| `pCantidad11` | `FLOAT(8)` | — | — |
| `pCantidad12` | `FLOAT(8)` | — | — |
| `pCantidad13` | `FLOAT(8)` | — | — |
| `pCantidad14` | `FLOAT(8)` | — | — |
| `pCantidad15` | `FLOAT(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cCodTrans` | `CHAR(4)` | L17 |
| `cSecuencia` | `CHAR(8)` | L18 |
| `cFechaEnvio` | `DATE` | L19 |
| `cCentecimas` | `CHAR(1)` | L20 |
| `cHora` | `CHAR(6)` | L21 |
| `cFolio` | `CHAR(8)` | L23 |
| `iNoRegistros` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L64 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_concen_atm` | `bdisuc` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdProvCaja = '' OR pDivisa = '' OR pMonto IS NULL OR pPapele` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_concen_atm';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `atm` | ENTIDAD | cajero automático (ATM) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_atms_actualizacorteadmin`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_actualizacorteadmin.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=105 |
| **Deps concatenadas** | 317 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cajeros automáticos, corte y Administrador — rol de usuario con privilegios administrativos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_corte_admin` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_actualizacorteadmin(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pMonto                       MONEY(14,2)
  pDenominacion1               CHAR(18)
  pDenominacion2               CHAR(18)
  pDenominacion3               CHAR(18)
  pDenominacion4               CHAR(18)
  pDenominacion5               CHAR(18)
  pDenominacion6               CHAR(18)
  pDenominacion7               CHAR(18)
  pDenominacion8               CHAR(18)
  pDenominacion9               CHAR(18)
  pDenominacion10              CHAR(18)
  pDenominacion11              CHAR(18)
  pDenominacion12              CHAR(18)
  pDenominacion13              CHAR(18)
  pDenominacion14              CHAR(18)
  pDenominacion15              CHAR(18)
  pCantidad1                   FLOAT(8)
  pCantidad2                   FLOAT(8)
  pCantidad3                   FLOAT(8)
  pCantidad4                   FLOAT(8)
  pCantidad5                   FLOAT(8)
  pCantidad6                   FLOAT(8)
  pCantidad7                   FLOAT(8)
  pCantidad8                   FLOAT(8)
  pCantidad9                   FLOAT(8)
  pCantidad10                  FLOAT(8)
  pCantidad11                  FLOAT(8)
  pCantidad12                  FLOAT(8)
  pCantidad13                  FLOAT(8)
  pCantidad14                  FLOAT(8)
  pCantidad15                  FLOAT(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |
| `pMonto` | `MONEY(14,2)` | — | — |
| `pDenominacion1` | `CHAR(18)` | — | — |
| `pDenominacion2` | `CHAR(18)` | — | — |
| `pDenominacion3` | `CHAR(18)` | — | — |
| `pDenominacion4` | `CHAR(18)` | — | — |
| `pDenominacion5` | `CHAR(18)` | — | — |
| `pDenominacion6` | `CHAR(18)` | — | — |
| `pDenominacion7` | `CHAR(18)` | — | — |
| `pDenominacion8` | `CHAR(18)` | — | — |
| `pDenominacion9` | `CHAR(18)` | — | — |
| `pDenominacion10` | `CHAR(18)` | — | — |
| `pDenominacion11` | `CHAR(18)` | — | — |
| `pDenominacion12` | `CHAR(18)` | — | — |
| `pDenominacion13` | `CHAR(18)` | — | — |
| `pDenominacion14` | `CHAR(18)` | — | — |
| `pDenominacion15` | `CHAR(18)` | — | — |
| `pCantidad1` | `FLOAT(8)` | — | — |
| `pCantidad2` | `FLOAT(8)` | — | — |
| `pCantidad3` | `FLOAT(8)` | — | — |
| `pCantidad4` | `FLOAT(8)` | — | — |
| `pCantidad5` | `FLOAT(8)` | — | — |
| `pCantidad6` | `FLOAT(8)` | — | — |
| `pCantidad7` | `FLOAT(8)` | — | — |
| `pCantidad8` | `FLOAT(8)` | — | — |
| `pCantidad9` | `FLOAT(8)` | — | — |
| `pCantidad10` | `FLOAT(8)` | — | — |
| `pCantidad11` | `FLOAT(8)` | — | — |
| `pCantidad12` | `FLOAT(8)` | — | — |
| `pCantidad13` | `FLOAT(8)` | — | — |
| `pCantidad14` | `FLOAT(8)` | — | — |
| `pCantidad15` | `FLOAT(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cCodTrans` | `CHAR(4)` | L16 |
| `cTipo` | `CHAR(1)` | L17 |
| `cTipoMovto` | `CHAR(1)` | L18 |
| `cDivisa` | `CHAR(1)` | L19 |
| `cSecuencia` | `CHAR(8)` | L20 |
| `cFechaEnvio` | `DATE` | L21 |
| `cCentecimas` | `CHAR(1)` | L22 |
| `cHora` | `CHAR(6)` | L23 |
| `cFolio` | `CHAR(8)` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L69 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L74 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L63 |
| `sp_corte_admin` | `bdisuc` | ⚠️ sí | L79 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdProvCaja = '' OR pMonto IS NULL THEN` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_corte_admin';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `corte` | ENTIDAD | corte (fecha de corte / período) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_atms_actualizadotacionesfaltsobr`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_actualizadotacionesfaltsobr.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=104 |
| **Deps concatenadas** | 316 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cajeros automáticos, dotaciones de efectivo y fal — fallo/disputa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_faltsob_atm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_actualizadotacionesfaltsobr(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pTipoOperacion               CHAR(4)
  pOperacion                   CHAR(2)
  pMonto                       MONEY(14,2)
  pDenominacion1               CHAR(18)
  pDenominacion2               CHAR(18)
  pDenominacion3               CHAR(18)
  pDenominacion4               CHAR(18)
  pDenominacion5               CHAR(18)
  pDenominacion6               CHAR(18)
  pDenominacion7               CHAR(18)
  pDenominacion8               CHAR(18)
  pDenominacion9               CHAR(18)
  pDenominacion10              CHAR(18)
  pDenominacion11              CHAR(18)
  pDenominacion12              CHAR(18)
  pDenominacion13              CHAR(18)
  pDenominacion14              CHAR(18)
  pDenominacion15              CHAR(18)
  pCantidad1                   FLOAT(8)
  pCantidad2                   FLOAT(8)
  pCantidad3                   FLOAT(8)
  pCantidad4                   FLOAT(8)
  pCantidad5                   FLOAT(8)
  pCantidad6                   FLOAT(8)
  pCantidad7                   FLOAT(8)
  pCantidad8                   FLOAT(8)
  pCantidad9                   FLOAT(8)
  pCantidad10                  FLOAT(8)
  pCantidad11                  FLOAT(8)
  pCantidad12                  FLOAT(8)
  pCantidad13                  FLOAT(8)
  pCantidad14                  FLOAT(8)
  pCantidad15                  FLOAT(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |
| `pTipoOperacion` | `CHAR(4)` | — | — |
| `pOperacion` | `CHAR(2)` | — | — |
| `pMonto` | `MONEY(14,2)` | — | — |
| `pDenominacion1` | `CHAR(18)` | — | — |
| `pDenominacion2` | `CHAR(18)` | — | — |
| `pDenominacion3` | `CHAR(18)` | — | — |
| `pDenominacion4` | `CHAR(18)` | — | — |
| `pDenominacion5` | `CHAR(18)` | — | — |
| `pDenominacion6` | `CHAR(18)` | — | — |
| `pDenominacion7` | `CHAR(18)` | — | — |
| `pDenominacion8` | `CHAR(18)` | — | — |
| `pDenominacion9` | `CHAR(18)` | — | — |
| `pDenominacion10` | `CHAR(18)` | — | — |
| `pDenominacion11` | `CHAR(18)` | — | — |
| `pDenominacion12` | `CHAR(18)` | — | — |
| `pDenominacion13` | `CHAR(18)` | — | — |
| `pDenominacion14` | `CHAR(18)` | — | — |
| `pDenominacion15` | `CHAR(18)` | — | — |
| `pCantidad1` | `FLOAT(8)` | — | — |
| `pCantidad2` | `FLOAT(8)` | — | — |
| `pCantidad3` | `FLOAT(8)` | — | — |
| `pCantidad4` | `FLOAT(8)` | — | — |
| `pCantidad5` | `FLOAT(8)` | — | — |
| `pCantidad6` | `FLOAT(8)` | — | — |
| `pCantidad7` | `FLOAT(8)` | — | — |
| `pCantidad8` | `FLOAT(8)` | — | — |
| `pCantidad9` | `FLOAT(8)` | — | — |
| `pCantidad10` | `FLOAT(8)` | — | — |
| `pCantidad11` | `FLOAT(8)` | — | — |
| `pCantidad12` | `FLOAT(8)` | — | — |
| `pCantidad13` | `FLOAT(8)` | — | — |
| `pCantidad14` | `FLOAT(8)` | — | — |
| `pCantidad15` | `FLOAT(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cCodTrans` | `CHAR(4)` | L17 |
| `cTipo` | `CHAR(1)` | L18 |
| `cTipoMovto` | `CHAR(1)` | L19 |
| `cDivisa` | `CHAR(1)` | L20 |
| `cSecuencia` | `CHAR(8)` | L21 |
| `cFechaEnvio` | `DATE` | L22 |
| `cCentecimas` | `CHAR(1)` | L23 |
| `cHora` | `CHAR(6)` | L24 |
| `cFolio` | `CHAR(8)` | L26 |
| `iNoRegistros` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L72 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L90 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |
| `sp_faltsob_atm` | `bdisuc` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdProvCaja = '' OR pTipoOperacion = '' OR pOperacion = '' OR` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L102 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_faltsob_atm';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | 🔵 CONVENCIÓN | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?tsobr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tsobr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_actualizaeliminacionfaltsobr`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_actualizaeliminacionfaltsobr.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=104 |
| **Deps concatenadas** | 315 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cajeros automáticos y fal — fallo/disputa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_faltsob_atm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_actualizaeliminacionfaltsobr(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pFolio                       CHAR(8)
  pTipoAfectacion              CHAR(2)
  pIdMotivo                    CHAR(2)
  pMonto                       MONEY(14,2)
  pDenominacion1               CHAR(18)
  pDenominacion2               CHAR(18)
  pDenominacion3               CHAR(18)
  pDenominacion4               CHAR(18)
  pDenominacion5               CHAR(18)
  pDenominacion6               CHAR(18)
  pDenominacion7               CHAR(18)
  pDenominacion8               CHAR(18)
  pDenominacion9               CHAR(18)
  pDenominacion10              CHAR(18)
  pDenominacion11              CHAR(18)
  pDenominacion12              CHAR(18)
  pDenominacion13              CHAR(18)
  pDenominacion14              CHAR(18)
  pDenominacion15              CHAR(18)
  pCantidad1                   FLOAT(8)
  pCantidad2                   FLOAT(8)
  pCantidad3                   FLOAT(8)
  pCantidad4                   FLOAT(8)
  pCantidad5                   FLOAT(8)
  pCantidad6                   FLOAT(8)
  pCantidad7                   FLOAT(8)
  pCantidad8                   FLOAT(8)
  pCantidad9                   FLOAT(8)
  pCantidad10                  FLOAT(8)
  pCantidad11                  FLOAT(8)
  pCantidad12                  FLOAT(8)
  pCantidad13                  FLOAT(8)
  pCantidad14                  FLOAT(8)
  pCantidad15                  FLOAT(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |
| `pFolio` | `CHAR(8)` | — | — |
| `pTipoAfectacion` | `CHAR(2)` | — | — |
| `pIdMotivo` | `CHAR(2)` | — | — |
| `pMonto` | `MONEY(14,2)` | — | — |
| `pDenominacion1` | `CHAR(18)` | — | — |
| `pDenominacion2` | `CHAR(18)` | — | — |
| `pDenominacion3` | `CHAR(18)` | — | — |
| `pDenominacion4` | `CHAR(18)` | — | — |
| `pDenominacion5` | `CHAR(18)` | — | — |
| `pDenominacion6` | `CHAR(18)` | — | — |
| `pDenominacion7` | `CHAR(18)` | — | — |
| `pDenominacion8` | `CHAR(18)` | — | — |
| `pDenominacion9` | `CHAR(18)` | — | — |
| `pDenominacion10` | `CHAR(18)` | — | — |
| `pDenominacion11` | `CHAR(18)` | — | — |
| `pDenominacion12` | `CHAR(18)` | — | — |
| `pDenominacion13` | `CHAR(18)` | — | — |
| `pDenominacion14` | `CHAR(18)` | — | — |
| `pDenominacion15` | `CHAR(18)` | — | — |
| `pCantidad1` | `FLOAT(8)` | — | — |
| `pCantidad2` | `FLOAT(8)` | — | — |
| `pCantidad3` | `FLOAT(8)` | — | — |
| `pCantidad4` | `FLOAT(8)` | — | — |
| `pCantidad5` | `FLOAT(8)` | — | — |
| `pCantidad6` | `FLOAT(8)` | — | — |
| `pCantidad7` | `FLOAT(8)` | — | — |
| `pCantidad8` | `FLOAT(8)` | — | — |
| `pCantidad9` | `FLOAT(8)` | — | — |
| `pCantidad10` | `FLOAT(8)` | — | — |
| `pCantidad11` | `FLOAT(8)` | — | — |
| `pCantidad12` | `FLOAT(8)` | — | — |
| `pCantidad13` | `FLOAT(8)` | — | — |
| `pCantidad14` | `FLOAT(8)` | — | — |
| `pCantidad15` | `FLOAT(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cCodTrans` | `CHAR(4)` | L17 |
| `cTipo` | `CHAR(1)` | L18 |
| `cTipoMovto` | `SMALLINT` | L19 |
| `cDivisa` | `CHAR(1)` | L20 |
| `cSecuencia` | `CHAR(8)` | L21 |
| `cFechaEnvio` | `DATE` | L22 |
| `cCentecimas` | `CHAR(1)` | L23 |
| `cHora` | `CHAR(6)` | L24 |
| `cFolio` | `CHAR(8)` | L26 |
| `iNoRegistros` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L72 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L88 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |
| `sp_faltsob_atm` | `bdisuc` | ⚠️ sí | L93 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdProvCaja = '' OR pFolio = '' OR pTipoAfectacion = '' OR pI` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_faltsob_atm';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?tsobr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion`, `?tsobr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_actualizarecepdota`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_actualizarecepdota.sql` |
| **LOC (1er CREATE)** | 116 |
| **Callgraph** | ✅ fan_in=0 / fan_out=103 |
| **Deps concatenadas** | 314 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_recepdota_atm`, `sp_inserta_atm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_actualizarecepdota(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pFolio                       CHAR(8)
  pDivisa                      CHAR(2)
  pMonto                       MONEY(14,2)
  pCantidad1                   FLOAT(8)
  pCantidad2                   FLOAT(8)
  pCantidad3                   FLOAT(8)
  pCantidad4                   FLOAT(8)
  pCantidad5                   FLOAT(8)
  pCantidad6                   FLOAT(8)
  pCantidad7                   FLOAT(8)
  pCantidad8                   FLOAT(8)
  pCantidad9                   FLOAT(8)
  pCantidad10                  FLOAT(8)
  pCantidad11                  FLOAT(8)
  pCantidad12                  FLOAT(8)
  pCantidad13                  FLOAT(8)
  pCantidad14                  FLOAT(8)
  pCantidad15                  FLOAT(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |
| `pFolio` | `CHAR(8)` | — | — |
| `pDivisa` | `CHAR(2)` | — | — |
| `pMonto` | `MONEY(14,2)` | — | — |
| `pCantidad1` | `FLOAT(8)` | — | — |
| `pCantidad2` | `FLOAT(8)` | — | — |
| `pCantidad3` | `FLOAT(8)` | — | — |
| `pCantidad4` | `FLOAT(8)` | — | — |
| `pCantidad5` | `FLOAT(8)` | — | — |
| `pCantidad6` | `FLOAT(8)` | — | — |
| `pCantidad7` | `FLOAT(8)` | — | — |
| `pCantidad8` | `FLOAT(8)` | — | — |
| `pCantidad9` | `FLOAT(8)` | — | — |
| `pCantidad10` | `FLOAT(8)` | — | — |
| `pCantidad11` | `FLOAT(8)` | — | — |
| `pCantidad12` | `FLOAT(8)` | — | — |
| `pCantidad13` | `FLOAT(8)` | — | — |
| `pCantidad14` | `FLOAT(8)` | — | — |
| `pCantidad15` | `FLOAT(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cCodTrans` | `CHAR(4)` | L13 |
| `cFechaEnvio` | `DATE` | L14 |
| `cSecuencia` | `CHAR(8)` | L15 |
| `cCentecimas` | `CHAR(1)` | L16 |
| `cHora` | `CHAR(6)` | L17 |
| `cFolioSucursal` | `CHAR(16)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L58 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_recepdota_atm` | `bdisuc` | ⚠️ sí | L68 |
| `sp_inserta_atm` | `bdisuc` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdProvCaja = '' OR pFolio = '' OR pDivisa = '' OR pMonto IS ` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_recepdota_atm';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_inserta_atm';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?epdota` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?epdota` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_actualizasoldotacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_actualizasoldotacion.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=102 |
| **Deps concatenadas** | 313 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cajeros automáticos, solicitud y dotación de efectivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soldot_atm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_actualizasoldotacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pDivisa                      CHAR(2)
  pMonto                       MONEY(14,2)
  pDenominacion1               CHAR(18)
  pDenominacion2               CHAR(18)
  pDenominacion3               CHAR(18)
  pDenominacion4               CHAR(18)
  pDenominacion5               CHAR(18)
  pDenominacion6               CHAR(18)
  pDenominacion7               CHAR(18)
  pDenominacion8               CHAR(18)
  pDenominacion9               CHAR(18)
  pDenominacion10              CHAR(18)
  pDenominacion11              CHAR(18)
  pDenominacion12              CHAR(18)
  pDenominacion13              CHAR(18)
  pDenominacion14              CHAR(18)
  pDenominacion15              CHAR(18)
  pCantidad1                   FLOAT(8)
  pCantidad2                   FLOAT(8)
  pCantidad3                   FLOAT(8)
  pCantidad4                   FLOAT(8)
  pCantidad5                   FLOAT(8)
  pCantidad6                   FLOAT(8)
  pCantidad7                   FLOAT(8)
  pCantidad8                   FLOAT(8)
  pCantidad9                   FLOAT(8)
  pCantidad10                  FLOAT(8)
  pCantidad11                  FLOAT(8)
  pCantidad12                  FLOAT(8)
  pCantidad13                  FLOAT(8)
  pCantidad14                  FLOAT(8)
  pCantidad15                  FLOAT(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |
| `pDivisa` | `CHAR(2)` | — | — |
| `pMonto` | `MONEY(14,2)` | — | — |
| `pDenominacion1` | `CHAR(18)` | — | — |
| `pDenominacion2` | `CHAR(18)` | — | — |
| `pDenominacion3` | `CHAR(18)` | — | — |
| `pDenominacion4` | `CHAR(18)` | — | — |
| `pDenominacion5` | `CHAR(18)` | — | — |
| `pDenominacion6` | `CHAR(18)` | — | — |
| `pDenominacion7` | `CHAR(18)` | — | — |
| `pDenominacion8` | `CHAR(18)` | — | — |
| `pDenominacion9` | `CHAR(18)` | — | — |
| `pDenominacion10` | `CHAR(18)` | — | — |
| `pDenominacion11` | `CHAR(18)` | — | — |
| `pDenominacion12` | `CHAR(18)` | — | — |
| `pDenominacion13` | `CHAR(18)` | — | — |
| `pDenominacion14` | `CHAR(18)` | — | — |
| `pDenominacion15` | `CHAR(18)` | — | — |
| `pCantidad1` | `FLOAT(8)` | — | — |
| `pCantidad2` | `FLOAT(8)` | — | — |
| `pCantidad3` | `FLOAT(8)` | — | — |
| `pCantidad4` | `FLOAT(8)` | — | — |
| `pCantidad5` | `FLOAT(8)` | — | — |
| `pCantidad6` | `FLOAT(8)` | — | — |
| `pCantidad7` | `FLOAT(8)` | — | — |
| `pCantidad8` | `FLOAT(8)` | — | — |
| `pCantidad9` | `FLOAT(8)` | — | — |
| `pCantidad10` | `FLOAT(8)` | — | — |
| `pCantidad11` | `FLOAT(8)` | — | — |
| `pCantidad12` | `FLOAT(8)` | — | — |
| `pCantidad13` | `FLOAT(8)` | — | — |
| `pCantidad14` | `FLOAT(8)` | — | — |
| `pCantidad15` | `FLOAT(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cCodTrans` | `CHAR(4)` | L16 |
| `cSecuencia` | `CHAR(8)` | L17 |
| `cFechaEnvio` | `DATE` | L18 |
| `cHora` | `CHAR(6)` | L19 |
| `cFolio` | `CHAR(8)` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L61 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L66 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |
| `sp_soldot_atm` | `bdisuc` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdProvCaja = '' OR pDivisa = '' OR pMonto IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_soldot_atm';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_atms_cargaarchivoconcentracionmasiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_cargaarchivoconcentracionmasiva.sql` |
| **LOC (1er CREATE)** | 152 |
| **Callgraph** | ✅ fan_in=0 / fan_out=112 |
| **Deps concatenadas** | 339 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga cajeros automáticos y archivo (masiva)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_cargaarchivoconcentracionmasiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBloqueInf                   CHAR(2000)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBloqueInf` | `CHAR(2000)` | — | — |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cSqlerr` | `INTEGER` | L5 |
| `cIsamErr` | `INTEGER` | L6 |
| `cDescErr` | `CHAR(50)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cRegistro` | `LVARCHAR` | L9 |
| `cBloqueInf2` | `LVARCHAR` | L10 |
| `iContador` | `INTEGER` | L11 |
| `iNumReg` | `INTEGER` | L12 |
| `cCc` | `CHAR(4)` | L13 |
| `cId_atm` | `CHAR(6)` | L14 |
| `cNombre` | `VARCHAR(200)` | L15 |
| `cPlaza` | `VARCHAR(100)` | L16 |
| `cTipo_ope` | `VARCHAR(25)` | L17 |
| `iB_1000` | `INTEGER` | L18 |
| `iB_500` | `INTEGER` | L19 |
| `iB_200` | `INTEGER` | L20 |
| `iB_100` | `INTEGER` | L21 |
| `iB_50` | `INTEGER` | L22 |
| `iB_20` | `INTEGER` | L23 |
| `mTotal_b` | `MONEY(18,2)` | L24 |
| `cPapeleta` | `CHAR(15)` | L25 |
| `cStatus` | `VARCHAR(50)` | L26 |
| `cFolio` | `CHAR(8)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_concentracionmasiva` | `bdicnweb` | no | SELECT | L82 |
| `sw_atm_concentracionmasiva` | `bdicnweb` | no | DELETE | L82 |
| `sw_atm_concentracionmasiva` | `bdicnweb` | no | INSERT | L131 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L72 |
| `sp_split_cadena` | `bdicnweb` | no | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | FÓRMULA | `LET cBloqueInf2 = TRIM(cBloqueInf2)\|\|'+';` |  |
| L99 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_atms_cargaarchivodotacionesmasivas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_cargaarchivodotacionesmasivas.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ✅ fan_in=0 / fan_out=102 |
| **Deps concatenadas** | 312 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga cajeros automáticos, archivo y dotaciones de efectivo (masiva)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_cargaarchivodotacionesmasivas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBloqueInf                   CHAR(2000)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBloqueInf` | `CHAR(2000)` | — | — |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cSqlerr` | `INTEGER` | L5 |
| `cIsamErr` | `INTEGER` | L6 |
| `cDescErr` | `CHAR(50)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cRegistro` | `LVARCHAR` | L9 |
| `cBloqueInf2` | `LVARCHAR` | L10 |
| `iContador` | `INTEGER` | L11 |
| `cCc` | `CHAR(4)` | L12 |
| `cId_atm` | `CHAR(6)` | L13 |
| `cNombre` | `CHAR(200)` | L14 |
| `cPlaza` | `CHAR(100)` | L15 |
| `cTipo_ope` | `CHAR(25)` | L16 |
| `iB_1000` | `INTEGER` | L17 |
| `iB_500` | `INTEGER` | L18 |
| `iB_200` | `INTEGER` | L19 |
| `iB_100` | `INTEGER` | L20 |
| `iB_50` | `INTEGER` | L21 |
| `iB_20` | `INTEGER` | L22 |
| `mTotal_b` | `MONEY(18,2)` | L23 |
| `cFolio` | `CHAR(8)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_dotacionmasiva` | `bdicnweb` | no | SELECT | L76 |
| `sw_atm_dotacionmasiva` | `bdicnweb` | no | INSERT | L121 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |
| `sp_split_cadena` | `bdicnweb` | no | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | FÓRMULA | `LET cBloqueInf2 = TRIM(cBloqueInf2)\|\|'+';` |  |
| L93 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | 🔵 CONVENCIÓN | nombre_sp |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_cargaarchivorecepcionmasiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_cargaarchivorecepcionmasiva.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ✅ fan_in=0 / fan_out=112 |
| **Deps concatenadas** | 338 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga cajeros automáticos y archivo (masiva)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_cargaarchivorecepcionmasiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBloqueInf                   CHAR(2000)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBloqueInf` | `CHAR(2000)` | — | — |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cSqlerr` | `INTEGER` | L5 |
| `cIsamErr` | `INTEGER` | L6 |
| `cDescErr` | `CHAR(50)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cRegistro` | `LVARCHAR` | L9 |
| `cBloqueInf2` | `LVARCHAR` | L10 |
| `iContador` | `INTEGER` | L11 |
| `iNumReg` | `INTEGER` | L12 |
| `cCc` | `CHAR(4)` | L13 |
| `cId_atm` | `CHAR(6)` | L14 |
| `cNombre` | `VARCHAR(200)` | L15 |
| `cPlaza` | `VARCHAR(100)` | L16 |
| `cTipo_ope` | `VARCHAR(25)` | L17 |
| `iB_1000` | `INTEGER` | L18 |
| `iB_500` | `INTEGER` | L19 |
| `iB_200` | `INTEGER` | L20 |
| `iB_100` | `INTEGER` | L21 |
| `iB_50` | `INTEGER` | L22 |
| `iB_20` | `INTEGER` | L23 |
| `mTotal_b` | `MONEY(18,2)` | L24 |
| `cFolio` | `CHAR(8)` | L25 |
| `cStatus` | `VARCHAR(100)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_recepcionmasiva` | `bdicnweb` | no | SELECT | L80 |
| `sw_atm_recepcionmasiva` | `bdicnweb` | no | DELETE | L80 |
| `sw_atm_recepcionmasiva` | `bdicnweb` | no | INSERT | L129 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L70 |
| `sp_split_cadena` | `bdicnweb` | no | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | FÓRMULA | `LET cBloqueInf2 = TRIM(cBloqueInf2)\|\|'+';` |  |
| L97 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?epcion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?epcion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_catmovimiento`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_catmovimiento.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=102 |
| **Deps concatenadas** | 311 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cajeros automáticos, catálogo y movimiento" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: DELETE, INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_catmovimiento(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cValor` | `CHAR(4)` | L11 |
| `cDescripcion` | `CHAR(35)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atms_catalogos_tmp` | `bdicnweb` | no | SELECT | L51 |
| `sw_atms_catalogos_tmp` | `bdicnweb` | no | DELETE | L51 |
| `sw_atms_catalogos_tmp` | `bdicnweb` | no | INSERT | L53 |
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L77 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `movimiento` | ENTIDAD | movimiento | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_atms_catoperaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_catoperaciones.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=102 |
| **Deps concatenadas** | 310 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cajeros automáticos, catálogo y operaciones" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_catoperaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCatalogo                  CHAR(1)
  pTipoAfectacion              CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCatalogo` | `CHAR(1)` | `cat`=catálogo | 🔵 CONVENCIÓN |
| `pTipoAfectacion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cCodigo` | `CHAR(4)` | L9 |
| `cDescripcion` | `CHAR(35)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L48 |
| `ss_motiv_afecta` | `bdisuc` | ⚠️ sí | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_atms_catplazacajero`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_catplazacajero.sql` |
| **LOC (1er CREATE)** | 225 |
| **Callgraph** | ✅ fan_in=0 / fan_out=102 |
| **Deps concatenadas** | 309 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cajeros automáticos, catálogo, plaza y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_atms` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_catplazacajero(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pPlaza                       CHAR(3)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pPlaza` | `CHAR(3)` | `plaza`=plaza (regional) | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNombre` | `CHAR(45)` | L11 |
| `cSucursal` | `CHAR(4)` | L12 |
| `cNumAtm` | `CHAR(4)` | L13 |
| `cNombreAtm` | `CHAR(100)` | L14 |
| `cCadenaNumAtm` | `CHAR(5000)` | L15 |
| `cPlaza` | `CHAR(3)` | L16 |
| `cDescripcion` | `CHAR(100)` | L17 |
| `cSiSucursal` | `CHAR(4)` | L18 |
| `cPlazaCajero` | `CHAR(4)` | L19 |
| `dFechaHoy` | `DATE` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L77 |
| `sw_atm_statuscargacat` | `bdicnweb` | no | SELECT | L87 |
| `sw_atm_statuscargacat` | `bdicnweb` | no | INSERT | L90 |
| `sw_atm_catalogoplaza` | `bdicnweb` | no | SELECT | L100 |
| `sw_atm_catalogoplaza` | `bdicnweb` | no | DELETE | L100 |
| `sw_atm_catalogoplaza` | `bdicnweb` | no | INSERT | L115 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L117 |
| `sw_atm_catalogocajero` | `bdicnweb` | no | SELECT | L133 |
| `sw_atm_catalogocajero` | `bdicnweb` | no | DELETE | L133 |
| `sw_atm_catalogocajero` | `bdicnweb` | no | INSERT | L148 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_atms` | `bdisuc` | ⚠️ sí | L103 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THE` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00833';` |  |
| L107 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_atms';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L140 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_atms';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L168 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L195 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `plaza` | ENTIDAD | plaza (regional) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?caje` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?caje`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_catpzaatmoperaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_catpzaatmoperaciones.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=101 |
| **Deps concatenadas** | 308 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cajeros automáticos, catálogo, cajero automático y operaciones" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_atms2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_catpzaatmoperaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNumAtm` | `CHAR(4)` | L11 |
| `cNombreAtm` | `CHAR(100)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_atms2` | `bdisuc` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_atms2';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?pza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `atm` | ENTIDAD | cajero automático (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pza` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_consdotacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_consdotacion.sql` |
| **LOC (1er CREATE)** | 342 |
| **Callgraph** | ✅ fan_in=0 / fan_out=100 |
| **Deps concatenadas** | 307 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cajeros automáticos y dotación de efectivo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_consdotacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pFolio                       CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |
| `pFolio` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L53 |
| `cCodRetSp` | `CHAR(6)` | L54 |
| `iCodRetSp` | `INTEGER` | L55 |
| `iSqlErr` | `INTEGER` | L56 |
| `cEmpresa` | `CHAR(3)` | L57 |
| `cSucursal` | `CHAR(4)` | L58 |
| `mMonto` | `MONEY(14,2)` | L59 |
| `cCodTrans` | `CHAR(4)` | L60 |
| `cSecuencia` | `CHAR(8)` | L61 |
| `cCodAtm` | `CHAR(4)` | L63 |
| `cDivisa` | `CHAR(2)` | L64 |
| `mSaldoAnterior` | `MONEY(14,2)` | L65 |
| `mSaldoAsignado` | `MONEY(14,2)` | L66 |
| `mSaldoTotal` | `MONEY(14,2)` | L67 |
| `dDisponible1` | `DECIMAL(10,2)` | L68 |
| `cDenominacion1` | `CHAR(18)` | L69 |
| `mTotal1` | `MONEY(14,2)` | L70 |
| `dDisponible2` | `DECIMAL(10,2)` | L71 |
| `cDenominacion2` | `CHAR(18)` | L72 |
| `mTotal2` | `MONEY(14,2)` | L73 |
| `dDisponible3` | `DECIMAL(10,2)` | L74 |
| `cDenominacion3` | `CHAR(18)` | L75 |
| `mTotal3` | `MONEY(14,2)` | L76 |
| `dDisponible4` | `DECIMAL(10,2)` | L77 |
| `cDenominacion4` | `CHAR(18)` | L78 |
| *…36 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_atm` | `bdisuc` | ⚠️ sí | SELECT | L261 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L207 |
| `sp_dotatm` | `bdisuc` | ⚠️ sí | L221 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L196 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L225 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_dotatm';` |  |
| L235 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L318 | FÓRMULA | `LET mTotalActual = NVL(mTotal1,0) + NVL(mTotal2,0) + NVL(mTotal3,0) + NVL(mTotal4,0) + NVL(mTotal5,0` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_atms_conseliminacionfaltsobr`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_conseliminacionfaltsobr.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=99 |
| **Deps concatenadas** | 306 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cajeros automáticos y fal — fallo/disputa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_faltsob` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_conseliminacionfaltsobr(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolio                       CHAR(8)
  pIdProvCaja                  CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolio` | `CHAR(8)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cDescOperacion` | `CHAR(30)` | L13 |
| `cDescMotivo` | `CHAR(30)` | L14 |
| `cSucursal` | `CHAR(4)` | L15 |
| `mMonto` | `MONEY(14,2)` | L16 |
| `cTipoAfectacion` | `CHAR(2)` | L17 |
| `cAtm` | `CHAR(4)` | L18 |
| `iNumRegistros` | `INTEGER` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |
| `sp_cons_faltsob` | `bdisuc` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_cons_faltsob';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?tsobr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion`, `?tsobr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_consultadenosdoactual`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_consultadenosdoactual.sql` |
| **LOC (1er CREATE)** | 373 |
| **Callgraph** | ✅ fan_in=0 / fan_out=98 |
| **Deps concatenadas** | 305 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cajeros automáticos y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_consultadenosdoactual(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
  pIdEjecucion                 CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L51 |
| `cCodRetSp` | `CHAR(6)` | L52 |
| `iCodRetSp` | `INTEGER` | L53 |
| `iSqlErr` | `INTEGER` | L54 |
| `cEmpresa` | `CHAR(3)` | L55 |
| `cCodAtm` | `CHAR(4)` | L56 |
| `cDivisa` | `CHAR(2)` | L57 |
| `mSaldoAnterior` | `MONEY(14,2)` | L58 |
| `mSaldoAsignado` | `MONEY(14,2)` | L59 |
| `mSaldoTotal` | `MONEY(14,2)` | L60 |
| `dDisponible1` | `DECIMAL(10,2)` | L61 |
| `cDenominacion1` | `CHAR(18)` | L62 |
| `mTotal1` | `MONEY(14,2)` | L63 |
| `dDisponible2` | `DECIMAL(10,2)` | L64 |
| `cDenominacion2` | `CHAR(18)` | L65 |
| `mTotal2` | `MONEY(14,2)` | L66 |
| `dDisponible3` | `DECIMAL(10,2)` | L67 |
| `cDenominacion3` | `CHAR(18)` | L68 |
| `mTotal3` | `MONEY(14,2)` | L69 |
| `dDisponible4` | `DECIMAL(10,2)` | L70 |
| `cDenominacion4` | `CHAR(18)` | L71 |
| `mTotal4` | `MONEY(14,2)` | L72 |
| `dDisponible5` | `DECIMAL(10,2)` | L73 |
| `cDenominacion5` | `CHAR(18)` | L74 |
| `mTotal5` | `MONEY(14,2)` | L75 |
| *…35 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_atm` | `bdisuc` | ⚠️ sí | SELECT | L216 |
| `ss_atm` | `bdisuc` | ⚠️ sí | INSERT | L219 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L201 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L191 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L230 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L281 | FÓRMULA | `LET mTotalActual = NVL(mTotal1,0) + NVL(mTotal2,0) + NVL(mTotal3,0) + NVL(mTotal4,0) + NVL(mTotal5,0` |  |
| L346 | FÓRMULA | `LET mTotalActual = NVL(mTotal1,0) + NVL(mTotal2,0) + NVL(mTotal3,0) + NVL(mTotal4,0) + NVL(mTotal5,0` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?den` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?do` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?ual` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?den`, `?do`, `?ual` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_detalleconcentracionefectivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_detalleconcentracionefectivo.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=112 |
| **Deps concatenadas** | 337 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "concentración de fondos cajeros automáticos, detalle y efectivo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_detalleconcentracionefectivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `cSqlerr` | `INTEGER` | L17 |
| `cIsamErr` | `INTEGER` | L18 |
| `cDescErr` | `CHAR(50)` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `iNumero` | `INTEGER` | L21 |
| `mMonto` | `MONEY(18,2)` | L22 |
| `iCant_1000` | `INTEGER` | L23 |
| `iCant_500` | `INTEGER` | L24 |
| `iCant_200` | `INTEGER` | L25 |
| `iCant_100` | `INTEGER` | L26 |
| `iCant_50` | `INTEGER` | L27 |
| `iCant_20` | `INTEGER` | L28 |
| `mMonto_1000` | `MONEY(14,2)` | L29 |
| `mMonto_500` | `MONEY(14,2)` | L30 |
| `mMonto_200` | `MONEY(14,2)` | L31 |
| `mMonto_100` | `MONEY(14,2)` | L32 |
| `mMonto_50` | `MONEY(14,2)` | L33 |
| `mMonto_20` | `MONEY(14,2)` | L34 |
| `iExiste` | `INTEGER` | L35 |
| `iRecuperacion` | `INTEGER` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_concentracionmasiva` | `bdicnweb` | no | SELECT | L93 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | FÓRMULA | `LET mMonto_500 = 500 * NVL(iCant_500,0);` | 🔴 MONEY/aritmética financiera |
| L97 | FÓRMULA | `LET mMonto_200 = 200 * NVL(iCant_200,0);` | 🔴 MONEY/aritmética financiera |
| L98 | FÓRMULA | `LET mMonto_100 = 100 * NVL(iCant_100,0);` | 🔴 MONEY/aritmética financiera |
| L99 | FÓRMULA | `LET mMonto_50 = 50 * NVL(iCant_50,0);` | 🔴 MONEY/aritmética financiera |
| L100 | FÓRMULA | `LET mMonto_20 = 20 * NVL(iCant_20,0);` | 🔴 MONEY/aritmética financiera |
| L102 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `efectivo` | ENTIDAD | efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_atms_detalledotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_detalledotaciones.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ✅ fan_in=0 / fan_out=98 |
| **Deps concatenadas** | 304 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cajeros automáticos, detalle y dotaciones de efectivo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_detalledotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `cSqlerr` | `INTEGER` | L19 |
| `cIsamErr` | `INTEGER` | L20 |
| `cDescErr` | `CHAR(50)` | L21 |
| `cEmpresa` | `CHAR(3)` | L22 |
| `iNumero` | `INTEGER` | L23 |
| `mMonto` | `MONEY(18,2)` | L24 |
| `iCant_1000` | `INTEGER` | L25 |
| `iCant_500` | `INTEGER` | L26 |
| `iCant_200` | `INTEGER` | L27 |
| `iCant_100` | `INTEGER` | L28 |
| `iCant_50` | `INTEGER` | L29 |
| `iCant_20` | `INTEGER` | L30 |
| `mMonto_1000` | `MONEY(14,2)` | L31 |
| `mMonto_500` | `MONEY(14,2)` | L32 |
| `mMonto_200` | `MONEY(14,2)` | L33 |
| `mMonto_100` | `MONEY(14,2)` | L34 |
| `mMonto_50` | `MONEY(14,2)` | L35 |
| `mMonto_20` | `MONEY(14,2)` | L36 |
| `iExiste` | `INTEGER` | L37 |
| `iRecuperacion` | `INTEGER` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_dotacionmasiva` | `bdicnweb` | no | SELECT | L91 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L116 | FÓRMULA | `LET mMonto_1000 = 1000 * NVL(iCant_1000,0);` | 🔴 MONEY/aritmética financiera |
| L117 | FÓRMULA | `LET mMonto_500 = 500 * NVL(iCant_500,0);` | 🔴 MONEY/aritmética financiera |
| L118 | FÓRMULA | `LET mMonto_200 = 200 * NVL(iCant_200,0);` | 🔴 MONEY/aritmética financiera |
| L119 | FÓRMULA | `LET mMonto_100 = 100 * NVL(iCant_100,0);` | 🔴 MONEY/aritmética financiera |
| L120 | FÓRMULA | `LET mMonto_50 = 50 * NVL(iCant_50,0);` | 🔴 MONEY/aritmética financiera |
| L121 | FÓRMULA | `LET mMonto_20 = 20 * NVL(iCant_20,0);` | 🔴 MONEY/aritmética financiera |
| L123 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_atms_detalleoperaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_detalleoperaciones.sql` |
| **LOC (1er CREATE)** | 201 |
| **Callgraph** | ✅ fan_in=0 / fan_out=98 |
| **Deps concatenadas** | 303 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cajeros automáticos, detalle y operaciones" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consul_atm2` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_detalleoperaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pCodTransacc                 CHAR(4)
  pTipo                        CHAR(1)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodTransacc` | `CHAR(4)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `iSqlErr` | `INT` | L36 |
| `cCodRetSp` | `CHAR(5)` | L37 |
| `iCodRetSp` | `INTEGER` | L38 |
| `cEmpresa` | `CHAR(3)` | L39 |
| `cSucursal` | `CHAR(50)` | L40 |
| `dFechaOperacion` | `DATE` | L41 |
| `cCodTrans` | `CHAR(4)` | L42 |
| `cAplicaReversado` | `CHAR(1)` | L43 |
| `cReversado` | `CHAR(2)` | L44 |
| `cNomUsuario` | `CHAR(40)` | L45 |
| `cDescDivisa` | `CHAR(40)` | L46 |
| `mMonto` | `MONEY(14,2)` | L47 |
| `fCantidad1` | `FLOAT(8)` | L48 |
| `fCantidad2` | `FLOAT(8)` | L49 |
| `fCantidad3` | `FLOAT(8)` | L50 |
| `fCantidad4` | `FLOAT(8)` | L51 |
| `fCantidad5` | `FLOAT(8)` | L52 |
| `fCantidad6` | `FLOAT(8)` | L53 |
| `fCantidad7` | `FLOAT(8)` | L54 |
| `fCantidad8` | `FLOAT(8)` | L55 |
| `fCantidad9` | `FLOAT(8)` | L56 |
| `fCantidad10` | `FLOAT(8)` | L57 |
| `fCantidad11` | `FLOAT(8)` | L58 |
| `fCantidad12` | `FLOAT(8)` | L59 |
| *…12 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consul_atm2` | `bdisuc` | ⚠️ sí | L155 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L125 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L162 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_consul_atm2';` |  |
| L171 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L179 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_atms_detalleoperaciones_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_detalleoperaciones_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=97 |
| **Deps concatenadas** | 302 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cajeros automáticos, detalle y operaciones (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consul_atm2_totales` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_detalleoperaciones_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pCodTransacc                 CHAR(4)
  pTipo                        CHAR(1)
  pFechaIni                    DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodTransacc` | `CHAR(4)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consul_atm2_totales` | `bdisuc` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_consul_atm2_totales';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_atms_detallerecepciondotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_detallerecepciondotaciones.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=112 |
| **Deps concatenadas** | 336 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recepción cajeros automáticos, detalle y dotaciones de efectivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_detallerecepciondotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `cSqlerr` | `INTEGER` | L17 |
| `cIsamErr` | `INTEGER` | L18 |
| `cDescErr` | `CHAR(50)` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `iNumero` | `INTEGER` | L21 |
| `mMonto` | `MONEY(18,2)` | L22 |
| `iCant_1000` | `INTEGER` | L23 |
| `iCant_500` | `INTEGER` | L24 |
| `iCant_200` | `INTEGER` | L25 |
| `iCant_100` | `INTEGER` | L26 |
| `iCant_50` | `INTEGER` | L27 |
| `iCant_20` | `INTEGER` | L28 |
| `mMonto_1000` | `MONEY(14,2)` | L29 |
| `mMonto_500` | `MONEY(14,2)` | L30 |
| `mMonto_200` | `MONEY(14,2)` | L31 |
| `mMonto_100` | `MONEY(14,2)` | L32 |
| `mMonto_50` | `MONEY(14,2)` | L33 |
| `mMonto_20` | `MONEY(14,2)` | L34 |
| `iExiste` | `INTEGER` | L35 |
| `iRecuperacion` | `INTEGER` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_recepcionmasiva` | `bdicnweb` | no | SELECT | L93 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | FÓRMULA | `LET mMonto_500 = 500 * NVL(iCant_500,0);` | 🔴 MONEY/aritmética financiera |
| L97 | FÓRMULA | `LET mMonto_200 = 200 * NVL(iCant_200,0);` | 🔴 MONEY/aritmética financiera |
| L98 | FÓRMULA | `LET mMonto_100 = 100 * NVL(iCant_100,0);` | 🔴 MONEY/aritmética financiera |
| L99 | FÓRMULA | `LET mMonto_50 = 50 * NVL(iCant_50,0);` | 🔴 MONEY/aritmética financiera |
| L100 | FÓRMULA | `LET mMonto_20 = 20 * NVL(iCant_20,0);` | 🔴 MONEY/aritmética financiera |
| L102 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?epcion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?epcion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_generarepconcentracionefectivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_generarepconcentracionefectivo.sql` |
| **LOC (1er CREATE)** | 248 |
| **Callgraph** | ✅ fan_in=0 / fan_out=106 |
| **Deps concatenadas** | 319 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar cajeros automáticos y efectivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_generarepconcentracionefectivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iIdRegistro` | `INTEGER` | L11 |
| `iSerial` | `INTEGER` | L13 |
| `cRespMensaje` | `CHAR(45)` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `cQuery` | `CHAR(255)` | L16 |
| `cCmd1` | `CHAR(2000)` | L17 |
| `cSql` | `CHAR(2500)` | L18 |
| `cRutaGral` | `CHAR(150)` | L19 |
| `cRutaInformix` | `CHAR(100)` | L20 |
| `cUsrBin` | `CHAR(100)` | L21 |
| `iCountRep` | `INTEGER` | L22 |
| `iProcesaRep` | `INT` | L23 |
| `iArmaReporte` | `INT` | L24 |
| `cArchivoCP` | `CHAR(45)` | L25 |
| `cCmdQuery` | `CHAR(2500)` | L26 |
| `bInTransaction` | `BOOLEAN` | L27 |
| `ven_transacc` | `SMALLINT` | L28 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(5)` | L29 |
| `cReporte` | `CHAR(100)` | L30 |
| *…17 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_concentracionmasiva` | `bdicnweb` | no | SELECT | L140 |
| `systables` | `bdicnweb` | no | SELECT | L160 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L143 | FÓRMULA | `LET mMonto_500 = 500 * NVL(iCant_500,0);` | 🔴 MONEY/aritmética financiera |
| L144 | FÓRMULA | `LET mMonto_200 = 200 * NVL(iCant_200,0);` | 🔴 MONEY/aritmética financiera |
| L145 | FÓRMULA | `LET mMonto_100 = 100 * NVL(iCant_100,0);` | 🔴 MONEY/aritmética financiera |
| L146 | FÓRMULA | `LET mMonto_50 = 50 * NVL(iCant_50,0);` | 🔴 MONEY/aritmética financiera |
| L147 | FÓRMULA | `LET mMonto_20 = 20 * NVL(iCant_20,0);` | 🔴 MONEY/aritmética financiera |
| L148 | FÓRMULA | `LET mTotalMonto = mMonto_500 + mMonto_200 + mMonto_100 + mMonto_50 + mMonto_20;` | 🔴 MONEY/aritmética financiera |
| L151 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L161 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT num_reg::CHAR(11),''''\|\|TRIM(cc)` |  |
| L180 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRutaDescarga)\|\|'query.sql';` |  |
| L189 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L194 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L203 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L212 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L220 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `efectivo` | ENTIDAD | efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_generarepdetalledotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_generarepdetalledotaciones.sql` |
| **LOC (1er CREATE)** | 188 |
| **Callgraph** | ✅ fan_in=0 / fan_out=96 |
| **Deps concatenadas** | 301 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar cajeros automáticos, detalle y dotaciones de efectivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_generarepdetalledotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRuta                        CHAR(100)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRuta` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iIdRegistro` | `INTEGER` | L11 |
| `iSerial` | `INTEGER` | L13 |
| `cRespMensaje` | `CHAR(45)` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `cQuery` | `CHAR(255)` | L16 |
| `cCmd1` | `CHAR(2000)` | L17 |
| `cSql` | `CHAR(2500)` | L18 |
| `cRutaGral` | `CHAR(150)` | L19 |
| `cRutaInformix` | `CHAR(100)` | L20 |
| `cUsrBin` | `CHAR(100)` | L21 |
| `iCountRep` | `INTEGER` | L22 |
| `iProcesaRep` | `INT` | L23 |
| `iArmaReporte` | `INT` | L24 |
| `cArchivoCP` | `CHAR(45)` | L25 |
| `cCmdQuery` | `CHAR(2500)` | L26 |
| `bInTransaction` | `BOOLEAN` | L27 |
| `ven_transacc` | `SMALLINT` | L28 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(5)` | L29 |
| `cReporte` | `CHAR(100)` | L30 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | FÓRMULA | `LET pRuta = TRIM(pRuta) \|\| '/';` |  |
| L111 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT cc,id_atm,nombre,plaza,tipo_ope,b_` |  |
| L127 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRuta)\|\|'query.sql';` |  |
| L135 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L139 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L147 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L155 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L163 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_generareprecepciondotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_generareprecepciondotaciones.sql` |
| **LOC (1er CREATE)** | 245 |
| **Callgraph** | ✅ fan_in=0 / fan_out=106 |
| **Deps concatenadas** | 320 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar cajeros automáticos y dotaciones de efectivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_generareprecepciondotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iIdRegistro` | `INTEGER` | L11 |
| `iSerial` | `INTEGER` | L13 |
| `cRespMensaje` | `CHAR(45)` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `cQuery` | `CHAR(255)` | L16 |
| `cCmd1` | `CHAR(2000)` | L17 |
| `cSql` | `CHAR(2500)` | L18 |
| `cRutaGral` | `CHAR(150)` | L19 |
| `cRutaInformix` | `CHAR(100)` | L20 |
| `cUsrBin` | `CHAR(100)` | L21 |
| `iCountRep` | `INTEGER` | L22 |
| `iProcesaRep` | `INT` | L23 |
| `iArmaReporte` | `INT` | L24 |
| `cArchivoCP` | `CHAR(45)` | L25 |
| `cCmdQuery` | `CHAR(2500)` | L26 |
| `bInTransaction` | `BOOLEAN` | L27 |
| `ven_transacc` | `SMALLINT` | L28 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(5)` | L29 |
| `cReporte` | `CHAR(100)` | L30 |
| *…17 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_recepcionmasiva` | `bdicnweb` | no | SELECT | L140 |
| `systables` | `bdicnweb` | no | SELECT | L160 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L143 | FÓRMULA | `LET mMonto_500 = 500 * NVL(iCant_500,0);` | 🔴 MONEY/aritmética financiera |
| L144 | FÓRMULA | `LET mMonto_200 = 200 * NVL(iCant_200,0);` | 🔴 MONEY/aritmética financiera |
| L145 | FÓRMULA | `LET mMonto_100 = 100 * NVL(iCant_100,0);` | 🔴 MONEY/aritmética financiera |
| L146 | FÓRMULA | `LET mMonto_50 = 50 * NVL(iCant_50,0);` | 🔴 MONEY/aritmética financiera |
| L147 | FÓRMULA | `LET mMonto_20 = 20 * NVL(iCant_20,0);` | 🔴 MONEY/aritmética financiera |
| L148 | FÓRMULA | `LET mTotalMonto = mMonto_500 + mMonto_200 + mMonto_100 + mMonto_50 + mMonto_20;` | 🔴 MONEY/aritmética financiera |
| L151 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L161 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT num_reg::CHAR(11),''''\|\|TRIM(cc)` |  |
| L180 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRutaDescarga)\|\|'query.sql';` |  |
| L189 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L194 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L203 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L212 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L220 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?epcion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep`, `?epcion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_genreportepasecontable`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_genreportepasecontable.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=96 |
| **Deps concatenadas** | 300 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cajeros automáticos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_genreportepasecontable(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INT` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `iLlave` | `INTEGER` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cCuenta` | `CHAR(60)` | L20 |
| `cCargoAbono` | `CHAR(1)` | L21 |
| `cNroAuxiliar` | `CHAR(12)` | L22 |
| `mMonto` | `MONEY(14,2)` | L23 |
| `cCodProveedor` | `CHAR(4)` | L24 |
| `cDescripcion` | `CHAR(50)` | L25 |
| `iNumRegistros` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_poliza_atm` | `bdisuc` | ⚠️ sí | SELECT | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L88 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pasecont` | ACCION | realiza el pase contable (registro a póliza/mayor) | 🔵 CONVENCIÓN | nombre_sp |
| `?able` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?able` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_procesadotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_procesadotaciones.sql` |
| **LOC (1er CREATE)** | 146 |
| **Callgraph** | ✅ fan_in=0 / fan_out=95 |
| **Deps concatenadas** | 298 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa cajeros automáticos y dotaciones de efectivo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soldot_atm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_procesadotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `iCodRetSp` | `INTEGER` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cCodTrans` | `CHAR(4)` | L9 |
| `cSecuencia` | `CHAR(8)` | L10 |
| `cDivisa` | `CHAR(2)` | L11 |
| `cFechaEnvio` | `DATE` | L12 |
| `cHora` | `CHAR(6)` | L13 |
| `cCc` | `CHAR(4)` | L14 |
| `cId_atm` | `CHAR(6)` | L15 |
| `iB_1000` | `INTEGER` | L16 |
| `iB_500` | `INTEGER` | L17 |
| `iB_200` | `INTEGER` | L18 |
| `iB_100` | `INTEGER` | L19 |
| `iB_50` | `INTEGER` | L20 |
| `iB_20` | `INTEGER` | L21 |
| `mTotal_b` | `MONEY(18,2)` | L22 |
| `cFolio` | `CHAR(8)` | L23 |
| `iId_registro` | `INTEGER` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L74 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L77 |
| `sw_atm_dotacionmasiva` | `bdicnweb` | no | SELECT | L89 |
| `sw_atm_dotacionmasiva` | `bdicnweb` | no | UPDATE | L104 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |
| `sp_soldot_atm` | `bdisuc` | ⚠️ sí | L93 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_soldot_atm';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_atms_procesarecepciondotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_procesarecepciondotaciones.sql` |
| **LOC (1er CREATE)** | 315 |
| **Callgraph** | ✅ fan_in=0 / fan_out=93 |
| **Deps concatenadas** | 291 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa cajeros automáticos y dotaciones de efectivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dotatm_2`, `sp_inserta_atm_2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_procesarecepciondotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `iCodRetSp` | `INTEGER` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cDivisa` | `CHAR(2)` | L9 |
| `cHora` | `CHAR(6)` | L10 |
| `cSecuencia` | `CHAR(8)` | L11 |
| `cFolioSuc` | `CHAR(16)` | L12 |
| `iId_registro` | `INTEGER` | L14 |
| `iNumReg` | `INTEGER` | L15 |
| `cCc` | `CHAR(4)` | L16 |
| `cId_atm` | `CHAR(6)` | L17 |
| `cNombre` | `VARCHAR(200)` | L18 |
| `cPlaza` | `VARCHAR(100)` | L19 |
| `cTipo_ope` | `VARCHAR(25)` | L20 |
| `iB_500` | `INTEGER` | L21 |
| `iB_200` | `INTEGER` | L22 |
| `iB_100` | `INTEGER` | L23 |
| `iB_50` | `INTEGER` | L24 |
| `iB_20` | `INTEGER` | L25 |
| `mTotal_b` | `MONEY(18,2)` | L26 |
| `cFolio` | `CHAR(8)` | L27 |
| `cStatus` | `VARCHAR(50)` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| *…11 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_regduplicados` | `bdicnweb` | no | SELECT | L111 |
| `sw_atm_regduplicados` | `bdicnweb` | no | DELETE | L111 |
| `sw_atm_recepcionmasiva` | `bdicnweb` | no | UPDATE | L114 |
| `sw_atm_recepcionmasiva` | `bdicnweb` | no | SELECT | L116 |
| `ss_relacionccid` | `bdisuc` | ⚠️ sí | SELECT | L135 |
| `sw_atm_regduplicados` | `bdicnweb` | no | INSERT | L171 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L102 |
| `sp_dotatm_2` | `bdisuc` | ⚠️ sí | L222 |
| `sp_inserta_atm_2` | `bdisuc` | ⚠️ sí | L251 |
| `sp_recepdota_atm` | `bdisuc` | ⚠️ sí | L260 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L142 | FÓRMULA | `LET mMonto_500 = 500 * NVL(iB_500,0);` | 🔴 MONEY/aritmética financiera |
| L143 | FÓRMULA | `LET mMonto_200 = 200 * NVL(iB_200,0);` | 🔴 MONEY/aritmética financiera |
| L144 | FÓRMULA | `LET mMonto_100 = 100 * NVL(iB_100,0);` | 🔴 MONEY/aritmética financiera |
| L145 | FÓRMULA | `LET mMonto_50 = 50 * NVL(iB_50,0);` | 🔴 MONEY/aritmética financiera |
| L146 | FÓRMULA | `LET mMonto_20 = 20 * NVL(iB_20,0);` | 🔴 MONEY/aritmética financiera |
| L147 | FÓRMULA | `LET mTotalMonto = mMonto_500 + mMonto_200 + mMonto_100 + mMonto_50 + mMonto_20;` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?epcion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?epcion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_reparqueoatm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_reparqueoatm.sql` |
| **LOC (1er CREATE)** | 143 |
| **Callgraph** | ✅ fan_in=0 / fan_out=94 |
| **Deps concatenadas** | 297 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte cajeros automáticos y cajero automático" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_arqueoatms` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_reparqueoatm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodAtm                      CHAR(4)
  pFecha                       DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodAtm` | `CHAR(4)` | `atm`=cajero automático (ATM) | ✅ CÓDIGO |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INT` | L23 |
| `cCodRetSp` | `CHAR(5)` | L24 |
| `iCodRetSp` | `INTEGER` | L25 |
| `cEmpresa` | `CHAR(3)` | L26 |
| `cMes` | `CHAR(2)` | L27 |
| `cDia` | `CHAR(2)` | L28 |
| `cAnio` | `CHAR(4)` | L29 |
| `cSucursal` | `CHAR(4)` | L30 |
| `cAtmNombre` | `CHAR(100)` | L31 |
| `cCajaGeneral` | `CHAR(100)` | L32 |
| `mSaldAnterior` | `MONEY(14,2)` | L33 |
| `mEntradaDia` | `MONEY(14,2)` | L34 |
| `mSalidaDia` | `MONEY(14,2)` | L35 |
| `mSaldoFinDia` | `MONEY(14,2)` | L36 |
| `iDen1000` | `INTEGER` | L37 |
| `iDen500` | `INTEGER` | L38 |
| `iDen200` | `INTEGER` | L39 |
| `iDen100` | `INTEGER` | L40 |
| `iDen50` | `INTEGER` | L41 |
| `iMont1000` | `INTEGER` | L42 |
| `iMont500` | `INTEGER` | L43 |
| `iMont200` | `INTEGER` | L44 |
| `iMont100` | `INTEGER` | L45 |
| `iMont50` | `INTEGER` | L46 |
| *…2 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_arqueoatms` | `bdisuc` | ⚠️ sí | L112 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L90 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCodAtm = '' OR pFecha IS NULL THEN` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L117 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_arqueoatms';` |  |
| L120 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?arqueo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `atm` | ENTIDAD | cajero automático (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?arqueo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_repfaltantesobranteatm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_repfaltantesobranteatm.sql` |
| **LOC (1er CREATE)** | 218 |
| **Callgraph** | ✅ fan_in=0 / fan_out=93 |
| **Deps concatenadas** | 296 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte cajeros automáticos y cajero automático (faltantes, anterior)" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_repfaltantesobranteatm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodTransacc                 CHAR(4)
  pCodAtm                      CHAR(4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodTransacc` | `CHAR(4)` | — | — |
| `pCodAtm` | `CHAR(4)` | `atm`=cajero automático (ATM) | ✅ CÓDIGO |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L33 |
| `iSqlErr` | `INT` | L34 |
| `cCodRetSp` | `CHAR(5)` | L35 |
| `iCodRetSp` | `INTEGER` | L36 |
| `cEmpresa` | `CHAR(3)` | L37 |
| `cDescTransaccion` | `CHAR(35)` | L38 |
| `cCodTransaccion` | `CHAR(4)` | L39 |
| `dFechaOperacion` | `DATE` | L40 |
| `cFolioOper` | `CHAR(8)` | L41 |
| `cAplicaReversado` | `CHAR(1)` | L42 |
| `cReversado` | `CHAR(2)` | L43 |
| `mMonto` | `MONEY(14,2)` | L44 |
| `cDenominacion1` | `CHAR(18)` | L45 |
| `cDenominacion2` | `CHAR(18)` | L46 |
| `cDenominacion3` | `CHAR(18)` | L47 |
| `cDenominacion4` | `CHAR(18)` | L48 |
| `cDenominacion5` | `CHAR(18)` | L49 |
| `cDenominacion6` | `CHAR(18)` | L50 |
| `cDenominacion8` | `CHAR(18)` | L51 |
| `cDenominacion9` | `CHAR(18)` | L52 |
| `cDenominacion10` | `CHAR(18)` | L53 |
| `cDenominacion11` | `CHAR(18)` | L54 |
| `cDenominacion12` | `CHAR(18)` | L55 |
| `cDenominacion13` | `CHAR(18)` | L56 |
| `fCantidad1` | `FLOAT(8)` | L57 |
| *…11 más…* | | |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L122 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCodTransacc = '' OR pCodAtm = '' OR pFechaInicio IS NULL OR ` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L183 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L192 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `faltantes` | MODIF | faltantes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?obr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `atm` | ENTIDAD | cajero automático (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?obr`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_validafechas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_validafechas.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=93 |
| **Deps concatenadas** | 294 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida cajeros automáticos y fechas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_validafechas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `dFechaHoy` | `DATE` | L7 |
| `dFechaIni` | `DATE` | L8 |
| `dFechaFin` | `DATE` | L9 |
| `sHoraActual` | `CHAR(12)` | L10 |
| `dFechaServer` | `DATE` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00864';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `fechas` | ENTIDAD | fechas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_atms_validapasecontable`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_validapasecontable.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=93 |
| **Deps concatenadas** | 293 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida cajeros automáticos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_validapasecontable(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaPase                   DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaPase` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iPaseContable` | `INTEGER` | L10 |
| `cHayPaseContable` | `CHAR(1)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `co_poldet` | `bdicont` | ⚠️ sí | SELECT | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaPase IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `pasecont` | ACCION | realiza el pase contable (registro a póliza/mayor) | 🔵 CONVENCIÓN | nombre_sp |
| `?able` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?able` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_atms_verificastatuscat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_atms_verificastatuscat.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=93 |
| **Deps concatenadas** | 292 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica cajeros automáticos, estatus y catálogo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_atms_verificastatuscat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdStatus` | `CHAR(1)` | L8 |
| `cDescStatus` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_atm_statuscargacat` | `bdicnweb` | no | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_aud_cambioperfilsuc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_aud_cambioperfilsuc_totales.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 96 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "auditoría, cambio y perfil de usuario (sucursal, totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_aud_cambioperfilsuc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pFechaIni                    DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cambio_perf` | `bdinteg` | ⚠️ sí | SELECT | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''OR pSucursal = '' OR pFechaIni IS NULL OR pFechaFin IS NULL THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00091';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_aumcred_validarinfocrediticiaofi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_aumcred_validarinfocrediticiaofi.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida crédito, información y oficio (aumento)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_aumcred_validarinfocrediticiaofi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumTarjeta                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cMensajeRetorno` | `CHAR(80)` | L12 |
| `cDescStatus` | `CHAR(60)` | L13 |
| `cNumCredito` | `CHAR(20)` | L15 |
| `cStatusCred` | `CHAR(2)` | L16 |
| `cMtoVen` | `DECIMAL(14,2)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |
| `sp_validarinfocrediticia_ofi` | `bdicred` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_validarinfocrediticia_ofi";` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00868';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00885';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00886';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00887';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00888';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00869';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00870';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00871';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00872';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00873';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00874';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00875';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `aum` | MODIF | aumento | 🟡 INFERIDO | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `?iticia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ofi` | ENTIDAD | oficio | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?iticia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bccc_catcomentario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_catcomentario.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "BCCC — formato o protocolo de consulta al Buró de Crédito, catálogo, Comisión bancaria — cobro de comisión sobre cuenta y Tarjeta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_mon_buro_conscaterrores` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_catcomentario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(40)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cIdComentario` | `CHAR(2)` | L12 |
| `cDescComentario` | `CHAR(100)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_mon_buro_conscaterrores` | `bdicred` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_mon_buro_conscaterro` |  |
| L59 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?io` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?en`, `?io` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bccc_catproducto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_catproducto.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "BCCC — formato o protocolo de consulta al Buró de Crédito, catálogo y producto" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cac_consulta_productos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_catproducto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cIdProducto` | `CHAR(4)` | L12 |
| `cDescProducto` | `CHAR(40)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cac_consulta_productos` | `bdicred` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_cac_consulta_product` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L62 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `producto` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_bccc_conserroresreenviosol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_conserroresreenviosol.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta BCCC — formato o protocolo de consulta al Buró de Crédito, error y envíos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_conserroresreenviosol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cTipoSolicitud` | `CHAR(30)` | L10 |
| `cNumSolicitud` | `CHAR(20)` | L11 |
| `cDescMensaje` | `CHAR(100)` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_buro_bitacoraerror` | `bdicnweb` | no | SELECT | L58 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?esre` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envios` | ENTIDAD | envíos | 🔵 CONVENCIÓN | nombre_sp |
| `?ol` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?esre`, `?ol` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bccc_consinforeenvio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_consinforeenvio.sql` |
| **LOC (1er CREATE)** | 240 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta BCCC — formato o protocolo de consulta al Buró de Crédito y información" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_mon_buro_coninfoctestatus`, `sp_consdirec` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_consinforeenvio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSolicitud               CHAR(2)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSolicitud` | `CHAR(2)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INT` | L23 |
| `cCodRetSp` | `CHAR(5)` | L24 |
| `iCodRetSp` | `INTEGER` | L25 |
| `cEmpresa` | `CHAR(3)` | L26 |
| `cNomCliente` | `CHAR(104)` | L27 |
| `cRfc` | `CHAR(13)` | L28 |
| `cNumCliente` | `CHAR(20)` | L29 |
| `cSucursal` | `CHAR(4)` | L30 |
| `cNumCredito` | `CHAR(20)` | L31 |
| `cNumProducto` | `CHAR(45)` | L32 |
| `cEstatusSolicitud` | `CHAR(40)` | L33 |
| `iSecuencia` | `INT` | L35 |
| `cTipoDir` | `CHAR(1)` | L36 |
| `cCalle` | `CHAR(40)` | L37 |
| `cColonia` | `CHAR(60)` | L38 |
| `cEntreCalles` | `CHAR(40)` | L39 |
| `cPais` | `CHAR(3)` | L40 |
| `cEstado` | `CHAR(2)` | L41 |
| `cCiudad` | `CHAR(3)` | L42 |
| `cMunicipio` | `CHAR(5)` | L43 |
| `cCodPostal` | `CHAR(5)` | L44 |
| `cApartPostal` | `CHAR(11)` | L45 |
| `cTipoTelef1` | `CHAR(1)` | L46 |
| `cTelefono1` | `CHAR(13)` | L47 |
| *…37 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_mon_buro_coninfoctestatus` | `bdicred` | ⚠️ sí | L183 |
| `sp_consdirec` | `bdinteg` | ⚠️ sí | L202 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L86 | CÓDIGO_RETORNO | `LET cCodRet              = '00000';` |  |
| L164 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L187 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_mon_buro_coninfoctes` |  |
| L189 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L211 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdinteg:sp_consdirec';` |  |
| L216 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?re` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bccc_consultasolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_consultasolicitud.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta BCCC — formato o protocolo de consulta al Buró de Crédito y solicitud" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_consultasolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cOperador` | `CHAR(8)` | L11 |
| `cNombreEjecutivo` | `CHAR(45)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_buro_cteprocesando` | `bdicnweb` | no | SELECT | L51 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '90000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_bccc_detsolicitudeslincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_detsolicitudeslincred.sql` |
| **LOC (1er CREATE)** | 181 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 80 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "BCCC — formato o protocolo de consulta al Buró de Crédito, detalle, solicitudes y línea de crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_detsolicitudeslincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecucion                   SMALLINT
  pTipoSolicitud               CHAR(2)
  pNumSolicitud                CHAR(20)
  pNumCliente                  CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pEstatus                     CHAR(2)
  pProducto                    CHAR(4)
  pCveGrupo                    CHAR(2)
  pSegmento                    CHAR(2)
  pEtiqueta                    CHAR(2)
  pAnalista                    CHAR(8)
  pComentario                  CHAR(100)
  pTramaEjecucion              CHAR(250)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecucion` | `SMALLINT` | — | — |
| `pTipoSolicitud` | `CHAR(2)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pEstatus` | `CHAR(2)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pCveGrupo` | `CHAR(2)` | — | — |
| `pSegmento` | `CHAR(2)` | — | — |
| `pEtiqueta` | `CHAR(2)` | — | — |
| `pAnalista` | `CHAR(8)` | — | — |
| `pComentario` | `CHAR(100)` | — | — |
| `pTramaEjecucion` | `CHAR(250)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INT` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `iCodRetSp` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `cTipoSolicitud` | `CHAR(30)` | L26 |
| `cNumsolicitud` | `CHAR(20)` | L27 |
| `dFecha` | `DATE` | L28 |
| `dHora` | `DATETIME HOUR TO FRACTION` | L29 |
| `cCliente` | `CHAR(20)` | L30 |
| `cEstatus` | `CHAR(2)` | L31 |
| `cComentario` | `CHAR(100)` | L32 |
| `iTotalRegistros` | `INTEGER` | L33 |
| `cClaveGrupo` | `CHAR(2)` | L34 |
| `cSegmento` | `CHAR(2)` | L35 |
| `cEtiqueta` | `CHAR(2)` | L36 |
| `iIdRegistro` | `INTEGER` | L37 |
| `dFechaHoy` | `DATE` | L38 |
| `cPuesto` | `CHAR(2)` | L39 |
| `cEnProceso` | `CHAR(1)` | L40 |
| `cProducto` | `CHAR(4)` | L41 |
| `cTipoMov` | `CHAR(1)` | L42 |
| `iRecuperacion` | `INTEGER` | L43 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_perfiles_cac_aumlincred` | `bdicred` | ⚠️ sí | SELECT | L102 |
| `sw_buro_conslineacred` | `bdicnweb` | no | SELECT | L114 |
| `sw_buro_cteprocesando` | `bdicnweb` | no | SELECT | L119 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEjecucion IS NULL OR pTipoSolicitud = '' OR pRegistros IS NU` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L126 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L150 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `lincred` | ENTIDAD | línea de crédito | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_bccc_detsolicitudeslincred_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_detsolicitudeslincred_totales.sql` |
| **LOC (1er CREATE)** | 511 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "BCCC — formato o protocolo de consulta al Buró de Crédito, detalle, solicitudes y línea de crédito (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_detsolicitudeslincred_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecucion                   SMALLINT
  pTipoSolicitud               CHAR(2)
  pNumSolicitud                CHAR(20)
  pNumCliente                  CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pEstatus                     CHAR(2)
  pProducto                    CHAR(4)
  pCveGrupo                    CHAR(2)
  pSegmento                    CHAR(2)
  pEtiqueta                    CHAR(2)
  pAnalista                    CHAR(8)
  pComentario                  CHAR(100)
  pTramaEjecucion              CHAR(250)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecucion` | `SMALLINT` | — | — |
| `pTipoSolicitud` | `CHAR(2)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pEstatus` | `CHAR(2)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pCveGrupo` | `CHAR(2)` | — | — |
| `pSegmento` | `CHAR(2)` | — | — |
| `pEtiqueta` | `CHAR(2)` | — | — |
| `pAnalista` | `CHAR(8)` | — | — |
| `pComentario` | `CHAR(100)` | — | — |
| `pTramaEjecucion` | `CHAR(250)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INT` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cTipoSolicitud` | `CHAR(30)` | L15 |
| `cNumsolicitud` | `CHAR(20)` | L16 |
| `vNumsolicitud` | `CHAR(20)` | L17 |
| `dFecha` | `DATE` | L18 |
| `dHora` | `DATETIME HOUR TO FRACTION` | L19 |
| `cCliente` | `CHAR(20)` | L20 |
| `cEstatus` | `CHAR(2)` | L21 |
| `cComentario` | `CHAR(100)` | L22 |
| `iTotalRegistros` | `INTEGER` | L23 |
| `cClaveGrupo` | `CHAR(2)` | L24 |
| `cSegmento` | `CHAR(2)` | L25 |
| `cEtiqueta` | `CHAR(2)` | L26 |
| `dFechaHoy` | `DATE` | L27 |
| `cPuesto` | `CHAR(2)` | L28 |
| `iNumRegistros` | `INTEGER` | L29 |
| `cIdRegistro` | `CHAR(11)` | L31 |
| `iIdRegistro` | `INTEGER` | L32 |
| `iRegMixtos` | `INTEGER` | L33 |
| `iTotalEnviadas` | `INTEGER` | L34 |
| `iTotalExitosas` | `INTEGER` | L35 |
| *…5 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_buro_statusmonitorbccc` | `bdicnweb` | no | SELECT | L123 |
| `sw_buro_statusmonitorbccc` | `bdicnweb` | no | DELETE | L123 |
| `sw_buro_statusmonitorbccc` | `bdicnweb` | no | INSERT | L127 |
| `sw_buro_conslineacred` | `bdicnweb` | no | SELECT | L131 |
| `sw_buro_conslineacred` | `bdicnweb` | no | DELETE | L131 |
| `sd_numsolici_datos_tmp2` | `bdicred` | ⚠️ sí | SELECT | L135 |
| `sd_numsolici_datos_tmp2` | `bdicred` | ⚠️ sí | DELETE | L135 |
| `ss_solicitudes` | `bdisolic` | ⚠️ sí | SELECT | L183 |
| `sw_buro_conslineacred` | `bdicnweb` | no | INSERT | L186 |
| `ss_resum_scor_fin` | `bdisolic` | ⚠️ sí | SELECT | L196 |
| `sd_perfiles_cac_aumlincred` | `bdicred` | ⚠️ sí | SELECT | L206 |
| `sw_buro_bitacoraerror` | `bdicnweb` | no | SELECT | L247 |
| `sw_buro_bitacoraerror` | `bdicnweb` | no | DELETE | L247 |
| `sw_buro_bitacoraerror` | `bdicnweb` | no | INSERT | L275 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_mon_buro_conssolcredlincred2` | `bdicred` | ⚠️ sí | L138 |
| `sp_split_cadena` | `bdicnweb` | no | L251 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEjecucion IS NULL OR pTipoSolicitud = '' THEN` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L145 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_mon_buro_conssolcre` |  |
| L226 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L261 | FÓRMULA | `LET iTotalEnviadas = iTotalEnviadas + 1;` |  |
| L271 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_mon_buro_conssolcre` |  |
| L330 | FÓRMULA | `LET iTotalExitosas = iTotalExitosas + 1;` |  |
| L360 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_mon_buro_conssolcre` |  |
| L401 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_mon_buro_conssolcre` |  |
| L445 | FÓRMULA | `LET iTotalEnviadas = iTotalEnviadas + 1;` |  |
| L455 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_mon_buro_conssolcre` |  |
| L481 | FÓRMULA | `LET iTotalExitosas = iTotalExitosas + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `lincred` | ENTIDAD | línea de crédito | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_bccc_procesasolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_procesasolicitud.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa BCCC — formato o protocolo de consulta al Buró de Crédito y solicitud" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_procesasolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdOperacion                 SMALLINT
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdOperacion` | `SMALLINT` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRet` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_buro_cteprocesando` | `bdicnweb` | no | INSERT | L51 |
| `sw_buro_cteprocesando` | `bdicnweb` | no | SELECT | L61 |
| `sw_buro_cteprocesando` | `bdicnweb` | no | DELETE | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdOperacion IS NULL THEN` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00862';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00862';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_bccc_reenviosrep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_reenviosrep.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte BCCC — formato o protocolo de consulta al Buró de Crédito y envíos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_reenviosrep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pModo                        SMALLINT
  pTipoSolicitud               CHAR(1)
  pNumSolicitud                CHAR(20)
  pNumCte                      CHAR(20)
  pEstatus                     CHAR(2)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)		AS codigo_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pModo` | `SMALLINT` | — | — |
| `pTipoSolicitud` | `CHAR(1)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pEstatus` | `CHAR(2)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iFolio` | `INTEGER` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRet` | `CHAR(5)` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |
| `cTipoSolNumAnalista` | `CHAR(20)` | L24 |
| `cProductoNomAnalista` | `CHAR(104)` | L25 |
| `cNumSolPerfilUs` | `CHAR(25)` | L26 |
| `cNumCteErrCve01` | `CHAR(20)` | L27 |
| `cNumSucErrCve02` | `CHAR(4)` | L28 |
| `cNomCteErrCve03` | `CHAR(104)` | L29 |
| `cFechaErrCve04` | `CHAR(10)` | L30 |
| `cHrErrCve05` | `CHAR(12)` | L31 |
| `cEstatusErrCve06` | `CHAR(4)` | L32 |
| `cReenvioErrCve07` | `CHAR(4)` | L33 |
| `cFechaReenvioErrCve08` | `CHAR(10)` | L34 |
| `cEstatusFinErrCve09` | `CHAR(4)` | L35 |
| `cMotReenvioTotalBc` | `CHAR(80)` | L36 |
| `cNomAnalistaTotalCc` | `CHAR(104)` | L37 |
| `cTotalGlobal` | `CHAR(10)` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mon_buro_reenviosrep` | `bdicnweb` | no | SELECT | L88 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L92 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `?_re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envios` | ENTIDAD | envíos | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_re` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bccc_verificastatusmonitor`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bccc_verificastatusmonitor.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 90 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica BCCC — formato o protocolo de consulta al Buró de Crédito, estatus y monitor" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_bccc_verificastatusmonitor(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cStatus` | `CHAR(1)` | L13 |
| `iTotalRegistros` | `INTEGER` | L14 |
| `itotalExitosas` | `INTEGER` | L15 |
| `cExisteError` | `CHAR(1)` | L16 |
| `cEstatus` | `CHAR(2)` | L17 |
| `cErrorProceso` | `CHAR(1)` | L18 |
| `cError` | `CHAR(5)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_buro_statusmonitorbccc` | `bdicnweb` | no | SELECT | L59 |
| `sw_buro_statusmonitorbccc` | `bdicnweb` | no | INSERT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bccc` | ENTIDAD | BCCC — formato o protocolo de consulta al Buró de Crédito (b | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_bitacora`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bitacora.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=345 / fan_out=18 |
| **Principales callers** | `sp_bitacora`, `sp_consultareportepagoscre`, `sp_generararchivo_rst` |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bitácora" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 2 términos |

### Firma

```sql
CREATE PROCEDURE sp_bitacora(
  pAccion                      CHAR(50)
  pEntidad                     CHAR(100)
  pIp                          CHAR(20)
  pArchivoRespaldo             CHAR(100)
  pUsuario                     CHAR(10)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pAccion` | `CHAR(50)` | — | — |
| `pEntidad` | `CHAR(100)` | — | — |
| `pIp` | `CHAR(20)` | — | — |
| `pArchivoRespaldo` | `CHAR(100)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsuario` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `dAlta_Fecha` | `DATE` | L8 |
| `cOrigen` | `CHAR(150)` | L9 |
| `last_bit_id` | `INTEGER` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `vBitEntidad` | `CHAR(1)` | L13 |
| `cRutaArchivo` | `CHAR(150)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bitacora` | `bdirst` | ⚠️ sí | SELECT | L33 |
| `bitacora` | `bdirst` | ⚠️ sí | INSERT | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF last_bit_id IS NULL THEN` |  |
| L37 | FÓRMULA | `LET last_bit_id = last_bit_id + 1;` |  |
| L46 | VALIDACIÓN_NULL | `IF last_bit_id IS NULL THEN` |  |
| L49 | FÓRMULA | `LET last_bit_id = last_bit_id + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_bloquealotemasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bloquealotemasivo.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloquea cuenta lote (masivo)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_bloquealotemasivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              CHAR(10)
  pLote                        INTEGER
  pOpcBloqueo                  INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `CHAR(10)` | — | — |
| `pLote` | `INTEGER` | `lote`=lote (proceso batch) | ✅ CÓDIGO |
| `pOpcBloqueo` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iExiste` | `INTEGER` | L6 |
| `cStatusLote` | `CHAR(2)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00108';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `lote` | ENTIDAD | lote (proceso batch) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_bloquemasivocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bloquemasivocap.sql` |
| **LOC (1er CREATE)** | 249 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloqueo Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bloqueo_cta`, `sp_consaldodisp` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_bloquemasivocap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pPlantilla                   CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cEmpresa` | `CHAR(3)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `cCodRetSpSaldo` | `CHAR(5)` | L7 |
| `dFechaProc` | `DATETIME YEAR TO SECOND` | L8 |
| `cResSp` | `CHAR(5)` | L9 |
| `dFechaBloqueo` | `DATETIME YEAR TO SECOND` | L10 |
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iNoRegs` | `INTEGER` | L13 |
| `iIdRegistro` | `INTEGER` | L14 |
| `cCuenta` | `CHAR(20)` | L15 |
| `mImporte` | `MONEY(14,2)` | L16 |
| `cClaveBloq` | `CHAR(2)` | L17 |
| `cOpcBloque` | `CHAR(2)` | L18 |
| `cAreaSolic` | `CHAR(2)` | L19 |
| `cMotivoBloq` | `CHAR(2)` | L20 |
| `cClaveAreaSolic` | `CHAR(1)` | L21 |
| `cTipoBloqueo` | `CHAR(1)` | L22 |
| `cResultado` | `CHAR(15)` | L23 |
| `cStatus` | `CHAR(1)` | L24 |
| `iInTrans` | `INTEGER` | L25 |
| `iRegRechazados` | `INTEGER` | L26 |
| `iTotalRegs` | `INTEGER` | L27 |
| `mTotalMonto` | `MONEY(14,2)` | L28 |
| `dFechaMail` | `DATETIME YEAR TO SECOND` | L29 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_bloqueocap` | `bdicnweb` | no | SELECT | L90 |
| `sc_areabloqueo` | `bdicheq` | ⚠️ sí | SELECT | L118 |
| `sc_tipobloqueo` | `bdicheq` | ⚠️ sí | SELECT | L124 |
| `sc_histbloq` | `bdicheq` | ⚠️ sí | SELECT | L148 |
| `sw_tr_cargamasiva_bloqueocap_hist` | `bdicnweb` | no | INSERT | L182 |
| `sw_tr_cargamasiva_bloqueocap` | `bdicnweb` | no | DELETE | L185 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L205 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bloqueo_cta` | `bdicheq` | ⚠️ sí | L129 |
| `sp_consaldodisp` | `bdicnweb` | no | L155 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L167 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `?ue` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ue` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bloqueoctacap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_bloqueoctacap.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloquea cuenta cuenta y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bloqueo_cta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_bloqueoctacap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pImporte                     MONEY(14,2)
  pFechaProceso                DATE
  pClaveBloq                   CHAR(2)
  pOpcBloque                   INTEGER
  pAreaSolic                   CHAR(2)
  pMotivoBloq                  CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pFechaProceso` | `DATE` | — | — |
| `pClaveBloq` | `CHAR(2)` | — | — |
| `pOpcBloque` | `INTEGER` | — | — |
| `pAreaSolic` | `CHAR(2)` | — | — |
| `pMotivoBloq` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cEmpresa` | `CHAR(3)` | L5 |
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cClaveAreaSolic` | `CHAR(1)` | L9 |
| `cTipoBloqueo` | `CHAR(1)` | L10 |
| `cClave` | `CHAR(5)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_ctabloqueo` | `bdicheq` | ⚠️ sí | SELECT | L46 |
| `sc_areabloqueo` | `bdicheq` | ⚠️ sí | SELECT | L51 |
| `sc_tipobloqueo` | `bdicheq` | ⚠️ sí | SELECT | L59 |
| `sc_cuentas_retiro` | `bdicheq` | ⚠️ sí | UPDATE | L85 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bloqueo_cta` | `bdicheq` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | VALIDACIÓN_NULL | `IF pAreaSolic = '' or pAreaSolic  is null THEN` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00101';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00103';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00104';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00105';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_blqcancelactaexcluidacap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_blqcancelactaexcluidacap.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ✅ fan_in=27 / fan_out=1 |
| **Propósito inferido** | "cancela cuenta, identificador y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=4 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_blqcancelactaexcluidacap(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `cID_FUNCIONC` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSql_err` | `INT` | L5 |
| `iExiste` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cuentas_concentradas_excluidas` | `bdicnweb` | no | SELECT | L42 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_blq` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?clu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_blq`, `?e`, `?clu`, `?a` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_blqconcentractainactivascap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_blqconcentractainactivascap.sql` |
| **LOC (1er CREATE)** | 174 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (inactivas)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_blqconcentractainactivascap(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCuenta` | `CHAR(20)` | L6 |
| `cSpCodRet` | `CHAR(5)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cFolio` | `CHAR(16)` | L9 |
| `iNumArchivo` | `INTEGER` | L10 |
| `dFechaHoy` | `DATE` | L11 |
| `iEnTransaccion` | `SMALLINT` | L12 |
| `iTotalCuentasTraspasar` | `INT` | L13 |
| `mTotalMontoTraspasar` | `DECIMAL(18,2)` | L14 |
| `iTotalCuentasExlcuir` | `INT` | L15 |
| `mTotalMontoExcluir` | `DECIMAL(18,2)` | L16 |
| `dHoy` | `DATETIME YEAR TO SECOND` | L17 |
| `bInTransaccion` | `BOOLEAN` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_deb_statusregistraevento` | `bdicnweb` | no | SELECT | L62 |
| `sw_deb_statusregistraevento` | `bdicnweb` | no | DELETE | L62 |
| `sw_deb_statusregistraevento` | `bdicnweb` | no | INSERT | L66 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L71 |
| `sc_ctasinactinfor3anios3meses` | `bdicheq` | ⚠️ sí | SELECT | L86 |
| `sc_cuentas_concentradas_procesadas` | `bdicnweb` | no | INSERT | L106 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_blqconcentractainactivas` | `bdicheq` | ⚠️ sí | L102 |
| `sp_blqcancelactaexcluidacap` | `bdicnweb` | no | L115 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L120 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_blq` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?centr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inactivas` | MODIF | inactivas (art.61) | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_blq`, `?centr`, `?a` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_blqconcentractasinactivasexcluidas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_blqconcentractasinactivasexcluidas.sql` |
| **LOC (1er CREATE)** | 180 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta identificador (inactivas)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=6 / 12 términos |

### Firma

```sql
CREATE PROCEDURE sp_blqconcentractasinactivasexcluidas(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMCUENTA                   CHAR(20)
  cOPERACION                   CHAR(1)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `cID_FUNCIONC` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `cNUMCUENTA` | `CHAR(20)` | — | — |
| `cOPERACION` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSql_err` | `INT` | L9 |
| `dFecha` | `DATE` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `vStatusCta` | `CHAR(1)` | L13 |
| `cCuenta` | `CHAR(20)` | L14 |
| `cResultado` | `CHAR(1)` | L15 |
| `iExiste` | `INTEGER` | L16 |
| `vSdoActual` | `DECIMAL(18,2)` | L18 |
| `vSdoRetenido` | `DECIMAL(18,2)` | L19 |
| `vSdoCongelado` | `DECIMAL(18,2)` | L20 |
| `vSdoSobregirado` | `DECIMAL(18,2)` | L21 |
| `vSdoConcentrado` | `DECIMAL(18,2)` | L23 |
| `vIntSdoConcentrado` | `DECIMAL(18,2)` | L24 |
| `vSdoDispCuenta` | `DECIMAL(18,2)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L87 |
| `sc_ctasinactinfor3anios3meses` | `bdicheq` | ⚠️ sí | SELECT | L95 |
| `sc_cuentas_concentradas_excluidas` | `bdicnweb` | no | SELECT | L122 |
| `sc_cuentas_concentradas_excluidas` | `bdicnweb` | no | INSERT | L131 |
| `sc_cuentas_concentradas_excluidas` | `bdicnweb` | no | DELETE | L150 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L112 | FÓRMULA | `LET vSdoDispCuenta = vSdoConcentrado;   -- + vIntSdoConcentrado;` |  |
| L173 | FÓRMULA | `LET vSdoDispCuenta = vSdoConcentrado;   -- + vIntSdoConcentrado;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_blq` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?centr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?as` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inactivas` | MODIF | inactivas (art.61) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?clu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?as` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_blq`, `?centr`, `?as`, `?e`, `?clu`, `?as` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_blqconsultabloqueocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_blqconsultabloqueocap.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_blqvalbloqueocta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_blqconsultabloqueocap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pTipoMov                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTipoMov` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `cDesBloqueo` | `CHAR(50)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_blqvalbloqueocta` | `bdicheq` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00005';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00173';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00172';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_blq` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_blq` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_blqdesconcentractasinactivascap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_blqdesconcentractasinactivascap.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "actualiza [polisemia] Descripción y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (inactivas)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_blqdesconcentractasinactivas` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_blqdesconcentractasinactivascap(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cCodMsg` | `CHAR(50)` | L9 |
| `cCuenta` | `CHAR(20)` | L10 |
| `iTransaccionPrev` | `int` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_blqdesconcentractasinactivas` | `bdicheq` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00137';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00136';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_blq` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `?oncentr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?as` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inactivas` | MODIF | inactivas (art.61) | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_blq`, `?oncentr`, `?as` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_blqvalidacuentasconcentrarcap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_blqvalidacuentasconcentrarcap.sql` |
| **LOC (1er CREATE)** | 173 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Propósito inferido** | "valida cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_blqvalidacuentasconcentrarcap(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L13 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `pTipoEjecx` | `SMALLINT` | L17 |
| `pTarjeta` | `CHAR(20)` | L18 |
| `cCodRet6` | `CHAR(6)` | L20 |
| `cMensajeRet` | `CHAR(80)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cNumCliente` | `CHAR(20)` | L23 |
| `cNomCliente` | `CHAR(104)` | L24 |
| `cProducto` | `CHAR(40)` | L25 |
| `cNumTarjeta` | `CHAR(20)` | L26 |
| `cEstatus` | `CHAR(60)` | L27 |
| `cFechaConcent` | `CHAR(10)` | L28 |
| `dImpteConcent` | `DECIMAL(14,2)` | L29 |
| `vCodRet1` | `CHAR(5)` | L31 |
| `vSaldoConcentrado` | `DECIMAL(14,2)` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L89 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_calcsdoctainactiva` | `bdicheq` | ⚠️ sí | L115 |
| `sp_blqvalidacuentasconcentrar` | `bdicheq` | ⚠️ sí | L131 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | FÓRMULA | `LET pTipoEjecx			= 2; -- 1=POR TARJETA 2=POR CUENTA.` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00134';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00029';` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00134';` |  |
| L165 | CÓDIGO_RETORNO | `LET cCodRet = '00135';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_blq` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?centrar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_blq`, `?centrar` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_buscaestatuslotemasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_buscaestatuslotemasivo.sql` |
| **LOC (1er CREATE)** | 49 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "busca estatus y lote (masivo)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_buscaestatuslotemasivo(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdFuncionPadre              CHAR(10)
  pLote                        int
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdFuncionPadre` | `CHAR(10)` | — | — |
| `pLote` | `int` | `lote`=lote (proceso batch) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cStatusLote` | `CHAR(1)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L36 | VALIDACIÓN_NULL | `IF cStatusLote IS NULL THEN` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `lote` | ENTIDAD | lote (proceso batch) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_buscarchivosprocesarafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_buscarchivosprocesarafore.sql` |
| **LOC (1er CREATE)** | 238 |
| **Callgraph** | ✅ fan_in=0 / fan_out=41 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda/buscar CHI — formato/protocolo de consulta al Buró de Crédito, OS — Originación de Solicitudes y AFORE" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_aforebuscararchivosprocesar2`, `sp_afore_mensajeretorno` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_buscarchivosprocesarafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoBusqueda                CHAR(1)
  pTipoArchivo                 CHAR(1)
  pNombreArchivo               CHAR(30)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pEstatus                     CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoBusqueda` | `CHAR(1)` | — | — |
| `pTipoArchivo` | `CHAR(1)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |
| `pNombreArchivo` | `CHAR(30)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pEstatus` | `CHAR(2)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cMensajeRet` | `CHAR(200)` | L17 |
| `cTipoArchivo` | `CHAR(30)` | L18 |
| `cNombreArchivo` | `CHAR(30)` | L19 |
| `dFechaGeneracion` | `DATE` | L20 |
| `cEstatus` | `CHAR(2)` | L21 |
| `iNoMovimientos` | `INTEGER` | L22 |
| `cEstadoDescripcion` | `CHAR(30)` | L23 |
| `cEstadoNumDes` | `CHAR(32)` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |
| `sp_aforebuscararchivosprocesar2` | `bdiprog` | ⚠️ sí | L88 |
| `sp_afore_mensajeretorno` | `bdiprog` | ⚠️ sí | L94 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoBusqueda = '' OR pRegistros IS NULL OR pRecuperacion IS ` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L92 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_aforebuscararchivos` |  |
| L98 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_afore_mensajeretorn` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00482';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00483';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00484';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00485';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00486';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00487';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00488';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00489';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00490';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00491';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00492';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00493';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00494';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00438';` |  |
| L145 | CÓDIGO_RETORNO | `LET cCodRet = '00495';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00496';` |  |
| L151 | CÓDIGO_RETORNO | `LET cCodRet = '00497';` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '00498';` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00499';` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00500';` |  |
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00501';` |  |
| L166 | CÓDIGO_RETORNO | `LET cCodRet = '00496';` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet = '00502';` |  |
| | *…15 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `buscar` | ACCION | búsqueda/buscar | 🔵 CONVENCIÓN | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?v`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_buscasdosretenidocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_buscasdosretenidocap.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "busca saldos y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (retenido)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_buscasdosretenido` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_buscasdosretenidocap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `dFechaCompra` | `DATE` | L13 |
| `cFolioOperacion` | `CHAR(16)` | L14 |
| `mImporte` | `MONEY(17,2)` | L15 |
| `cReferencia` | `CHAR(40)` | L16 |
| `cDiasRestantes` | `CHAR(12)` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `iRegistros` | `INTEGER` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_buscasdosretenido` | `bdicheq` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCuenta = '' OR pRegistros IS NULL OR pRecuperacion IS NULL T` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L73 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:sp_buscasdosretenido';` |  |
| L78 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L84 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `sdos` | ENTIDAD | saldos (abreviación) | 🟡 INFERIDO | nombre_sp |
| `retenido` | MODIF | retenido (fondos en retención) | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_buscatotalsdosretenidocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_buscatotalsdosretenidocap.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "busca saldos y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (total, retenido)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_buscasdosretenido` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_buscatotalsdosretenidocap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `dFechaCompra` | `DATE` | L9 |
| `cFolioOperacion` | `CHAR(16)` | L10 |
| `mImporte` | `MONEY(17,2)` | L11 |
| `cReferencia` | `CHAR(40)` | L12 |
| `cDiasRestantes` | `CHAR(12)` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_buscasdosretenido` | `bdicheq` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L59 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:sp_buscasdosretenido';` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sdos` | ENTIDAD | saldos (abreviación) | 🟡 INFERIDO | nombre_sp |
| `retenido` | MODIF | retenido (fondos en retención) | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_busqueda_usuario_movil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_busqueda_usuario_movil.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda usuario (canal móvil)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_busqueda_usuario_movil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoEmpleado                  CHAR(8)
  pNombre                      CHAR(60)
  pStatus                      CHAR(1)
  pNo_telefono                 CHAR(10)
  pNumRegistro                 INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoEmpleado` | `CHAR(8)` | — | — |
| `pNombre` | `CHAR(60)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pNo_telefono` | `CHAR(10)` | — | — |
| `pNumRegistro` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `cNoEmpleado` | `CHAR(8)` | L17 |
| `cStatus` | `CHAR(1)` | L18 |
| `cNombre` | `CHAR(60)` | L19 |
| `cCentro_costos` | `CHAR(8)` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cNo_telefono` | `CHAR(10)` | L22 |
| `cNombreSuc` | `CHAR(40)` | L23 |
| `iCont` | `INTEGER` | L24 |
| `cImei` | `CHAR(20)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_usuario_movil` | `bdinteg` | ⚠️ sí | SELECT | L66 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `busqueda` | ACCION | búsqueda | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `movil` | MODIF | canal móvil | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_busquedabancos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_busquedabancos.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda banco" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_busquedabancos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveBanco                  INTEGER
  pNombreCorto                 CHAR(20)
  pNombreBanco                 CHAR(40)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveBanco` | `INTEGER` | `banco`=banco | 🔵 CONVENCIÓN |
| `pNombreCorto` | `CHAR(20)` | — | — |
| `pNombreBanco` | `CHAR(40)` | `banco`=banco | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iCveBanco` | `INTEGER` | L10 |
| `cNombreCorto` | `CHAR(20)` | L11 |
| `cNombreLargo` | `CHAR(40)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR (pClaveBanco IS NULL AND pNombreCorto = '' AND pNombreBanco =` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `busqueda` | ACCION | búsqueda | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `banco` | ENTIDAD | banco | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_busquedausuarioscedulasccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_busquedausuarioscedulasccl.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda usuarios, cédulas y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_busquedausuarioscedulasccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRol                         SMALLINT
  pCedula                      SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRol` | `SMALLINT` | — | — |
| `pCedula` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cAdm_Usuario` | `CHAR (8)` | L12 |
| `cNombre` | `CHAR (104)` | L13 |
| `cFuncion` | `SMALLINT` | L14 |
| `cCedula` | `SMALLINT` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `bdicnweb` | `bdicnweb` | no | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRol IS NULL OR pCedula IS NULL  THEN` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_usuariocedulacons";` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `busqueda` | ACCION | búsqueda | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cedulas` | ENTIDAD | cédulas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_ca_actualizastatuscaxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_actualizastatuscaxml.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 98 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza estatus y XML" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_actualizastatuscaxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveOficio                 CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveOficio` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iNoRegistros` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_statuscargaxml` | `bdicnweb` | no | UPDATE | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ca` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?ca` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_cargaarchivoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_cargaarchivoxml.sql` |
| **LOC (1er CREATE)** | 118 |
| **Callgraph** | ✅ fan_in=107 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga archivo y XML" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: UPDATE, DELETE, INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_cargaarchivoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaCarga                   CHAR(100)
  pArchivoProcesar             CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaCarga` | `CHAR(100)` | `carga`=carga / ingresa | ✅ CÓDIGO |
| `pArchivoProcesar` | `CHAR(100)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCmd` | `CHAR(2000)` | L6 |
| `cArchivoTmp` | `CHAR(250)` | L7 |
| `cScriptCarga` | `CHAR(250)` | L8 |
| `cRutaInformix` | `CHAR(100)` | L9 |
| `cNombreArchivoTmp` | `CHAR(50)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | INSERT | L83 |
| `sw_ca_archivoxml_tmp` | `bdicnweb` | no | SELECT | L86 |
| `sw_ca_archivoxml_tmp` | `bdicnweb` | no | DELETE | L86 |
| `statistics` | `bdicnweb` | no | UPDATE | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET cCmd = "iconv -s -f UTF-8 -t IBM-1252 "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|" > "\|` |  |
| L55 | FÓRMULA | `LET cCmd = "/usr/bin/mv "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|'.iconv '\|\|TRIM(pRutaCa` |  |
| L58 | FÓRMULA | `LET cCmd = "/usr/bin/rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|'.iconv';` |  |
| L62 | FÓRMULA | `LET cCmd = "sed '/<?xml/d' "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|" \| awk '{if($1 ~ /<E` |  |
| L73 | FÓRMULA | `LET cCmd = "/usr/bin/rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|'; /usr/bin/mv '\|\|T` |  |
| L80 | FÓRMULA | `LET cCmd = "/usr/bin/rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|'; /usr/bin/mv '\|\|T` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_catalogoautoridad`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_catalogoautoridad.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 112 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y identificador (automático)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_catalogoautoridad(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdAutoridad` | `SMALLINT` | L8 |
| `cDescAutoridad` | `CHAR(45)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_autoridad` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `auto` | MODIF | automático (proceso automático / batch — sp_*_auto) | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?r`, `?ad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_catalogoestatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_catalogoestatus.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 111 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_catalogoestatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdEstatus` | `CHAR(1)` | L8 |
| `cDescEstatus` | `CHAR(35)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_estatus` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_consultaarchivocaxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_consultaarchivocaxml.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 110 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivo y XML" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_consultaarchivocaxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveOficio                 CHAR(100)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveOficio` | `CHAR(100)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cNombreOficio` | `CHAR(100)` | L9 |
| `cIdEstatus` | `CHAR(1)` | L10 |
| `cDescEstatus` | `CHAR(35)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pClaveOficio = '' OR pRegistros IS NULL OR pRecuperacion IS N` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L60 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `?ca` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?ca` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_detallearchivosxmlnoprocesados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_detallearchivosxmlnoprocesados.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 109 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa detalle, archivos, XML y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_detallearchivosxmlnoprocesados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cNombreOficio` | `CHAR(100)` | L12 |
| `dFechaPublicacion` | `CHAR(25)` | L13 |
| `cFechaHrProcesamiento` | `DATETIME YEAR TO FRACTION(5)` | L14 |
| `cCodError` | `CHAR(6)` | L15 |
| `cDescError` | `CHAR(250)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '01030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?no`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_detallearchivosxmlnoprocesados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_detallearchivosxmlnoprocesados_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 108 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa detalle, archivos, XML y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_detallearchivosxmlnoprocesados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '01030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?no`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_detallearchivosxmlprocesados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_detallearchivosxmlprocesados.sql` |
| **LOC (1er CREATE)** | 211 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 344 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa detalle, archivos, XML y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_detallearchivosxmlprocesados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pIdEstatus                   CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pNumOficio                   CHAR(60)
  pNumExpediente               CHAR(60)
  pIdAutoridad                 CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pIdEstatus` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pNumOficio` | `CHAR(60)` | — | — |
| `pNumExpediente` | `CHAR(60)` | — | — |
| `pIdAutoridad` | `CHAR(2)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L30 |
| `iSqlErr` | `INTEGER` | L31 |
| `cNombre_oficio` | `CHAR(100)` | L32 |
| `cNum_oficio` | `CHAR(60)` | L33 |
| `cNum_expediente` | `CHAR(60)` | L34 |
| `cSolicitud_siara` | `CHAR(60)` | L35 |
| `iFolio` | `INTEGER` | L36 |
| `cAnio_oficio` | `CHAR(4)` | L37 |
| `cId_area` | `CHAR(2)` | L38 |
| `cDesc_area` | `CHAR(30)` | L39 |
| `cFecha_publicacion` | `CHAR(25)` | L40 |
| `cDias_plazo` | `CHAR(2)` | L41 |
| `cNombre_autoridad` | `CHAR(60)` | L42 |
| `cReferencia` | `CHAR(60)` | L43 |
| `iId_persona_ps` | `INTEGER` | L44 |
| `cCaracter_ps` | `CHAR(30)` | L45 |
| `cDes_tipo_persona_ps` | `CHAR(10)` | L46 |
| `cNombre_ps` | `CHAR(150)` | L47 |
| `cAp_paterno_ps` | `CHAR(26)` | L48 |
| `cAp_materno_ps` | `CHAR(26)` | L49 |
| `cRfc_ps` | `CHAR(15)` | L50 |
| `cEntidad` | `CHAR(50)` | L51 |
| `cCuenta` | `CHAR(20)` | L52 |
| `cNum_cliente` | `CHAR(20)` | L53 |
| `cId_estatus` | `CHAR(1)` | L54 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_archivosxml` | `bdicnweb` | no | SELECT | L129 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L112 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L98 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L148 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L180 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L189 | CÓDIGO_RETORNO | `LET cCodRet = '01030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_detallearchivosxmlprocesados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_detallearchivosxmlprocesados_totales.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 107 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa detalle, archivos, XML y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_detallearchivosxmlprocesados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pIdEstatus                   CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pNumOficio                   CHAR(60)
  pNumExpediente               CHAR(60)
  pIdAutoridad                 CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pIdEstatus` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pNumOficio` | `CHAR(60)` | — | — |
| `pNumExpediente` | `CHAR(60)` | — | — |
| `pIdAutoridad` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_archivosxml` | `bdicnweb` | no | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '01030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_ejecutacargaautomaticaxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_ejecutacargaautomaticaxml.sql` |
| **LOC (1er CREATE)** | 1437 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ejecuta XML (automático)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ca_cargaarchivoxml`, `sp_ca_procesaarchivoxml` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_ejecutacargaautomaticaxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaCarga                   CHAR(100)
  pNumIntentos                 SMALLINT
  pUsuarioSrvOrigen            CHAR(25)
  pIpSrvOrigen                 CHAR(15)
  pRutaOrigenFiles             CHAR(100)
  pCarpetaProc                 CHAR(50)
  pCarpetaNoProc               CHAR(50)
) RETURNING CHAR(5) AS codret, CHAR(1) AS bandera_error
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaCarga` | `CHAR(100)` | `carga`=carga / ingresa | ✅ CÓDIGO |
| `pNumIntentos` | `SMALLINT` | — | — |
| `pUsuarioSrvOrigen` | `CHAR(25)` | — | — |
| `pIpSrvOrigen` | `CHAR(15)` | — | — |
| `pRutaOrigenFiles` | `CHAR(100)` | — | — |
| `pCarpetaProc` | `CHAR(50)` | — | — |
| `pCarpetaNoProc` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cIdCodRet` | `CHAR(6)` | L5 |
| `cDesCodRet` | `CHAR(250)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cDescErr` | `CHAR(250)` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cBanDetError` | `CHAR(1)` | L12 |
| `bInTransaction` | `BOOLEAN` | L13 |
| `ven_transacc` | `SMALLINT` | L14 |
| `cCmd` | `CHAR(2000)` | L15 |
| `cCmd2` | `CHAR(500)` | L16 |
| `cPathdbaccess` | `CHAR(35)` | L17 |
| `cUsrbin` | `CHAR(15)` | L18 |
| `cUsrbinServerRemoto` | `CHAR(15)` | L19 |
| `dFormatoFechaPeriodo` | `DATE` | L21 |
| `dFechaPeriodo` | `DATE` | L22 |
| `cPeriodo` | `CHAR(8)` | L23 |
| `cNombreOficio` | `CHAR(100)` | L24 |
| `cGenClaveOficio` | `CHAR(45)` | L25 |
| `dFechaHoraInicio` | `DATETIME YEAR TO FRACTION(5)` | L26 |
| `dFechaInicio` | `DATE` | L27 |
| `dFechaHoraFin` | `DATETIME YEAR TO FRACTION(5)` | L28 |
| `iCtrlIntentos` | `SMALLINT` | L29 |
| *…68 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | INSERT | L219 |
| `sw_ca_buscaarchivosxml` | `bdicnweb` | no | SELECT | L242 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | SELECT | L322 |
| `sw_ca_archivosxml` | `bdicnweb` | no | SELECT | L333 |
| `sw_ca_archivosxml` | `bdicnweb` | no | DELETE | L333 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | SELECT | L340 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | DELETE | L340 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | INSERT | L342 |
| `systables` | `bdicnweb` | no | SELECT | L356 |
| `sw_ca_buscaarchivosxml` | `bdicnweb` | no | INSERT | L372 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | INSERT | L431 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | DELETE | L447 |
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | SELECT | L448 |
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | DELETE | L448 |
| `sw_ca_encabezado` | `bdicnweb` | no | SELECT | L619 |
| `sw_ca_solicitudespecifica` | `bdicnweb` | no | SELECT | L648 |
| `sw_ca_personassolicitud` | `bdicnweb` | no | SELECT | L655 |
| `sw_ca_cuentasconocidas` | `bdicnweb` | no | SELECT | L661 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L700 |
| `sw_ca_archivosxml` | `bdicnweb` | no | INSERT | L721 |
| `sw_ca_archivosxml` | `bdicnweb` | no | UPDATE | L1228 |
| `si_seg_usuarios_funciones` | `bdinteg` | ⚠️ sí | SELECT | L1343 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L303 |
| `sp_ca_cargaarchivoxml` | `bdicnweb` | no | L550 |
| `sp_ca_procesaarchivoxml` | `bdicnweb` | no | L594 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L1346 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L106 | CÓDIGO_RETORNO | `LET cIdCodRet = '00000';` |  |
| L224 | CÓDIGO_RETORNO | `LET cIdCodRet = '01028';` |  |
| L232 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L290 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRutaCarga = '' OR pNumIntentos IS NULL THEN` |  |
| L291 | CÓDIGO_RETORNO | `LET cIdCodRet = '00003';` |  |
| L305 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L306 | CÓDIGO_RETORNO | `LET cIdCodRet = '00028';` |  |
| L317 | FÓRMULA | `LET pRutaCarga = TRIM(pRutaCarga) \|\| '/';` |  |
| L382 | FÓRMULA | `LET cCmd = TRIM(cUsrbin)\|\|"rm -rf "\|\|TRIM(pRutaCarga)\|\|'carpeta.car'\|\|" "\|\|TRIM(pRutaCarga` |  |
| L392 | CÓDIGO_RETORNO | `LET cIdCodRet = '01021';` |  |
| L429 | FÓRMULA | `LET iCtrlIntentos = NVL(pNumIntentos,0) + 1;` |  |
| L434 | FÓRMULA | `LET vCuenta = vCuenta + 1;` |  |
| L437 | CÓDIGO_RETORNO | `LET cIdCodRet = '01023';` |  |
| L456 | FÓRMULA | `LET iContArch = iContArch + 1;` |  |
| L477 | FÓRMULA | `LET iCtrlIntentos = NVL(iCtrlIntentos,0) + 1;` |  |
| L483 | FÓRMULA | `LET vCuenta = vCuenta + 1;` |  |
| L486 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L497 | CÓDIGO_RETORNO | `LET cIdCodRet = '01028';` |  |
| L507 | FÓRMULA | `LET vCuenta = vCuenta + 1;` |  |
| L510 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L531 | FÓRMULA | `LET vCuenta = vCuenta + 1;` |  |
| L534 | CÓDIGO_RETORNO | `LET cIdCodRet = '01023';` |  |
| L566 | FÓRMULA | `LET vCuenta = vCuenta + 1;` |  |
| L580 | FÓRMULA | `LET cCmd = TRIM(cUsrbin)\|\|'rm -rf '\|\|TRIM(pRutaCarga)\|\|'scriptofixml'\|\|TO_CHAR(CURRENT, '%Y%` |  |
| L586 | CÓDIGO_RETORNO | `LET cIdCodRet = '01024';` |  |
| L609 | CÓDIGO_RETORNO | `LET cIdCodRet = '01025';` |  |
| L624 | CÓDIGO_RETORNO | `LET cIdCodRet = '01026';` |  |
| L679 | FÓRMULA | `LET cNombre2 = NVL(TRIM(UPPER(SUBSTR(cNombre, CHARINDEX(' ', cNombre) + 1))), '');` |  |
| L728 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| | *…22 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ejecuta` | ACCION | ejecuta (verbo — proceso / operación) | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `auto` | MODIF | automático (proceso automático / batch — sp_*_auto) | 🟡 INFERIDO | nombre_sp |
| `?matica` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?matica` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_ejecutacargaautomaticaxmlpba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_ejecutacargaautomaticaxmlpba.sql` |
| **LOC (1er CREATE)** | 1027 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ejecuta XML (automático, PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_ejecutacargaautomaticaxmlpba(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaCarga                   CHAR(100)
  pNumIntentos                 SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaCarga` | `CHAR(100)` | `carga`=carga / ingresa | ✅ CÓDIGO |
| `pNumIntentos` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cIdCodRet` | `CHAR(6)` | L6 |
| `cDesCodRet` | `CHAR(250)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIsamErr` | `INTEGER` | L9 |
| `cDescErr` | `CHAR(250)` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cBanDetError` | `CHAR(1)` | L13 |
| `bInTransaction` | `BOOLEAN` | L14 |
| `ven_transacc` | `SMALLINT` | L15 |
| `cCmd` | `CHAR(2000)` | L16 |
| `cPathdbaccess` | `CHAR(35)` | L17 |
| `cUsrbin` | `CHAR(15)` | L18 |
| `dFormatoFechaPeriodo` | `DATE` | L20 |
| `dFechaPeriodo` | `DATE` | L21 |
| `cPeriodo` | `CHAR(8)` | L22 |
| `cNombreOficio` | `CHAR(100)` | L23 |
| `cGenClaveOficio` | `CHAR(45)` | L24 |
| `dFechaHoraInicio` | `DATETIME YEAR TO FRACTION(5)` | L25 |
| `dFechaInicio` | `DATE` | L26 |
| `dFechaHoraFin` | `DATETIME YEAR TO FRACTION(5)` | L27 |
| `iCtrlIntentos` | `SMALLINT` | L28 |
| `iTotalArchivos` | `INTEGER` | L29 |
| `iContArch` | `INTEGER` | L30 |
| *…58 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | INSERT | L199 |
| `sw_ca_buscaarchivosxml` | `bdicnweb` | no | SELECT | L221 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | SELECT | L279 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | INSERT | L289 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | DELETE | L297 |
| `sw_ca_buscaarchivosxml` | `bdicnweb` | no | DELETE | L311 |
| `systables` | `bdicnweb` | no | SELECT | L314 |
| `sw_ca_buscaarchivosxml` | `bdicnweb` | no | INSERT | L330 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | SELECT | L369 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | DELETE | L369 |
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | SELECT | L370 |
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | DELETE | L370 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | INSERT | L441 |
| `sw_ca_encabezado` | `bdicnweb` | no | SELECT | L507 |
| `sw_ca_solicitudespecifica` | `bdicnweb` | no | SELECT | L538 |
| `sw_ca_personassolicitud` | `bdicnweb` | no | SELECT | L545 |
| `sw_ca_cuentasconocidas` | `bdicnweb` | no | SELECT | L551 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L575 |
| `sw_ca_archivosxml` | `bdicnweb` | no | INSERT | L600 |
| `sw_ca_archivosxml` | `bdicnweb` | no | SELECT | L864 |
| `sw_ca_archivosxml` | `bdicnweb` | no | UPDATE | L875 |
| `si_seg_usuarios_funciones` | `bdinteg` | ⚠️ sí | SELECT | L956 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L263 |
| `sp_ca_cargaarchivoxml` | `bdicnweb` | no | L459 |
| `sp_ca_procesaarchivoxml` | `bdicnweb` | no | L482 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L959 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L96 | CÓDIGO_RETORNO | `LET cIdCodRet = '00000';` |  |
| L204 | CÓDIGO_RETORNO | `LET cIdCodRet = '01028';` |  |
| L212 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L226 | FÓRMULA | `LET cCmd = TRIM(cUsrbin)\|\|"rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(cNombreOficio);` |  |
| L249 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRutaCarga = '' OR pNumIntentos IS NULL THEN` |  |
| L251 | CÓDIGO_RETORNO | `LET cIdCodRet = '00003';` |  |
| L265 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L266 | CÓDIGO_RETORNO | `LET cIdCodRet = '00028';` |  |
| L285 | CÓDIGO_RETORNO | `LET cIdCodRet = '01029';` |  |
| L322 | FÓRMULA | `LET pRutaCarga = TRIM(pRutaCarga) \|\| '/';` |  |
| L340 | FÓRMULA | `LET cCmd = TRIM(cUsrbin)\|\|"rm -rf "\|\|TRIM(pRutaCarga)\|\|'carpeta.car'\|\|" "\|\|TRIM(pRutaCarga` |  |
| L350 | CÓDIGO_RETORNO | `LET cIdCodRet = '01021';` |  |
| L378 | FÓRMULA | `LET iContArch = iContArch + 1;` |  |
| L397 | FÓRMULA | `LET iCtrlIntentos = NVL(iCtrlIntentos,0) + 1;` |  |
| L404 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L415 | CÓDIGO_RETORNO | `LET cIdCodRet = '01028';` |  |
| L423 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L432 | FÓRMULA | `LET cCmd = TRIM(cUsrbin)\|\|"rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(cNombreOficio);` |  |
| L445 | CÓDIGO_RETORNO | `LET cIdCodRet = '01023';` |  |
| L474 | CÓDIGO_RETORNO | `LET cIdCodRet = '01024';` |  |
| L497 | CÓDIGO_RETORNO | `LET cIdCodRet = '01025';` |  |
| L512 | CÓDIGO_RETORNO | `LET cIdCodRet = '01026';` |  |
| L569 | FÓRMULA | `LET cNombre2 = NVL(TRIM(UPPER(SUBSTR(cNombre, CHARINDEX(' ', cNombre) + 1))), '');` |  |
| L607 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| L634 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| L660 | FÓRMULA | `LET cSegundaPalabra = SUBSTR(cNombre, CHARINDEX(' ', cNombre) + 1);` |  |
| L662 | FÓRMULA | `LET cSegundaPalabra = SUBSTR(cNombre, CHARINDEX(' ', cNombre)+1, INSTR(cNombre, ' ',1,2)- CHARINDEX(` |  |
| L691 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| L718 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| | *…8 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ejecuta` | ACCION | ejecuta (verbo — proceso / operación) | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `auto` | MODIF | automático (proceso automático / batch — sp_*_auto) | 🟡 INFERIDO | nombre_sp |
| `?matica` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?matica` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_ejecutacargaautomaticaxmlpbanew`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_ejecutacargaautomaticaxmlpbanew.sql` |
| **LOC (1er CREATE)** | 1027 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ejecuta XML (automático, PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_ejecutacargaautomaticaxmlpbanew(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaCarga                   CHAR(100)
  pNumIntentos                 SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaCarga` | `CHAR(100)` | `carga`=carga / ingresa | ✅ CÓDIGO |
| `pNumIntentos` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cIdCodRet` | `CHAR(6)` | L6 |
| `cDesCodRet` | `CHAR(250)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIsamErr` | `INTEGER` | L9 |
| `cDescErr` | `CHAR(250)` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cBanDetError` | `CHAR(1)` | L13 |
| `bInTransaction` | `BOOLEAN` | L14 |
| `ven_transacc` | `SMALLINT` | L15 |
| `cCmd` | `CHAR(2000)` | L16 |
| `cPathdbaccess` | `CHAR(35)` | L17 |
| `cUsrbin` | `CHAR(15)` | L18 |
| `dFormatoFechaPeriodo` | `DATE` | L20 |
| `dFechaPeriodo` | `DATE` | L21 |
| `cPeriodo` | `CHAR(8)` | L22 |
| `cNombreOficio` | `CHAR(100)` | L23 |
| `cGenClaveOficio` | `CHAR(45)` | L24 |
| `dFechaHoraInicio` | `DATETIME YEAR TO FRACTION(5)` | L25 |
| `dFechaInicio` | `DATE` | L26 |
| `dFechaHoraFin` | `DATETIME YEAR TO FRACTION(5)` | L27 |
| `iCtrlIntentos` | `SMALLINT` | L28 |
| `iTotalArchivos` | `INTEGER` | L29 |
| `iContArch` | `INTEGER` | L30 |
| *…58 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | INSERT | L199 |
| `sw_ca_buscaarchivosxml` | `bdicnweb` | no | SELECT | L221 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | SELECT | L279 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | INSERT | L289 |
| `sw_ca_statuscargaxml` | `bdicnweb` | no | DELETE | L297 |
| `sw_ca_buscaarchivosxml` | `bdicnweb` | no | DELETE | L311 |
| `systables` | `bdicnweb` | no | SELECT | L314 |
| `sw_ca_buscaarchivosxml` | `bdicnweb` | no | INSERT | L330 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | SELECT | L369 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | DELETE | L369 |
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | SELECT | L370 |
| `sw_ca_bitacoraerroresxml` | `bdicnweb` | no | DELETE | L370 |
| `sw_ca_bitacoraprocesoxml` | `bdicnweb` | no | INSERT | L441 |
| `sw_ca_encabezado` | `bdicnweb` | no | SELECT | L507 |
| `sw_ca_solicitudespecifica` | `bdicnweb` | no | SELECT | L538 |
| `sw_ca_personassolicitud` | `bdicnweb` | no | SELECT | L545 |
| `sw_ca_cuentasconocidas` | `bdicnweb` | no | SELECT | L551 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L575 |
| `sw_ca_archivosxml` | `bdicnweb` | no | INSERT | L600 |
| `sw_ca_archivosxml` | `bdicnweb` | no | SELECT | L864 |
| `sw_ca_archivosxml` | `bdicnweb` | no | UPDATE | L875 |
| `si_seg_usuarios_funciones` | `bdinteg` | ⚠️ sí | SELECT | L956 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L263 |
| `sp_ca_cargaarchivoxml` | `bdicnweb` | no | L459 |
| `sp_ca_procesaarchivoxml` | `bdicnweb` | no | L482 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L959 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L96 | CÓDIGO_RETORNO | `LET cIdCodRet = '00000';` |  |
| L204 | CÓDIGO_RETORNO | `LET cIdCodRet = '01028';` |  |
| L212 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L226 | FÓRMULA | `LET cCmd = TRIM(cUsrbin)\|\|"rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(cNombreOficio);` |  |
| L249 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRutaCarga = '' OR pNumIntentos IS NULL THEN` |  |
| L251 | CÓDIGO_RETORNO | `LET cIdCodRet = '00003';` |  |
| L265 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L266 | CÓDIGO_RETORNO | `LET cIdCodRet = '00028';` |  |
| L285 | CÓDIGO_RETORNO | `LET cIdCodRet = '01029';` |  |
| L322 | FÓRMULA | `LET pRutaCarga = TRIM(pRutaCarga) \|\| '/';` |  |
| L340 | FÓRMULA | `LET cCmd = TRIM(cUsrbin)\|\|"rm -rf "\|\|TRIM(pRutaCarga)\|\|'carpeta.car'\|\|" "\|\|TRIM(pRutaCarga` |  |
| L350 | CÓDIGO_RETORNO | `LET cIdCodRet = '01021';` |  |
| L378 | FÓRMULA | `LET iContArch = iContArch + 1;` |  |
| L397 | FÓRMULA | `LET iCtrlIntentos = NVL(iCtrlIntentos,0) + 1;` |  |
| L404 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L415 | CÓDIGO_RETORNO | `LET cIdCodRet = '01028';` |  |
| L423 | CÓDIGO_RETORNO | `LET cIdCodRet = '01022';` |  |
| L432 | FÓRMULA | `LET cCmd = TRIM(cUsrbin)\|\|"rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(cNombreOficio);` |  |
| L445 | CÓDIGO_RETORNO | `LET cIdCodRet = '01023';` |  |
| L474 | CÓDIGO_RETORNO | `LET cIdCodRet = '01024';` |  |
| L497 | CÓDIGO_RETORNO | `LET cIdCodRet = '01025';` |  |
| L512 | CÓDIGO_RETORNO | `LET cIdCodRet = '01026';` |  |
| L569 | FÓRMULA | `LET cNombre2 = NVL(TRIM(UPPER(SUBSTR(cNombre, CHARINDEX(' ', cNombre) + 1))), '');` |  |
| L607 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| L634 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| L660 | FÓRMULA | `LET cSegundaPalabra = SUBSTR(cNombre, CHARINDEX(' ', cNombre) + 1);` |  |
| L662 | FÓRMULA | `LET cSegundaPalabra = SUBSTR(cNombre, CHARINDEX(' ', cNombre)+1, INSTR(cNombre, ' ',1,2)- CHARINDEX(` |  |
| L691 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| L718 | CÓDIGO_RETORNO | `LET cIdCodRet = '01027';` |  |
| | *…8 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ejecuta` | ACCION | ejecuta (verbo — proceso / operación) | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `auto` | MODIF | automático (proceso automático / batch — sp_*_auto) | 🟡 INFERIDO | nombre_sp |
| `?matica` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |
| `?new` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?matica`, `?new` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_extraevalortagxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_extraevalortagxml.sql` |
| **LOC (1er CREATE)** | 43 |
| **Callgraph** | ✅ fan_in=17 / fan_out=25 |
| **Deps concatenadas** | 106 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "trae valor y XML" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_extraevalortagxml(
  pIdRow                       INTEGER
  pEtiqueta                    VARCHAR(50)
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdRow` | `INTEGER` | — | — |
| `pEtiqueta` | `VARCHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cValor` | `LVARCHAR(32739)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_archivoxml_tmp` | `bdicnweb` | no | SELECT | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pIdRow IS NULL OR pEtiqueta = '' THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `trae` | ACCION | trae / recupera (verbo — sp_*_trae — bdisuc) | 🟡 INFERIDO | nombre_sp |
| `valor` | ENTIDAD | valor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_e`, `?tag` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_procesaarchivoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_procesaarchivoxml.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=107 / fan_out=24 |
| **Deps concatenadas** | 100 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa archivo y XML" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_ca_procesaencabezadoxml`, `sp_ca_procesasolicpartesxml`, `sp_ca_procesasolicespecificaxml` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_procesaarchivoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cNumOficio` | `CHAR(60)` | L10 |
| `iIdOficio` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_archivoxml_tmp` | `bdicnweb` | no | SELECT | L71 |
| `sw_ca_archivoxml_tmp` | `bdicnweb` | no | DELETE | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ca_procesaencabezadoxml` | `bdicnweb` | no | L42 |
| `sp_ca_procesasolicpartesxml` | `bdicnweb` | no | L52 |
| `sp_ca_procesasolicespecificaxml` | `bdicnweb` | no | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP "informix".sp_ca_procesaencabezadoxml';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP "informix".sp_ca_procesasolicpartesxml';` |  |
| L65 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP "informix".sp_ca_procesasolicespecificax` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_procesaencabezadoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_procesaencabezadoxml.sql` |
| **LOC (1er CREATE)** | 226 |
| **Callgraph** | ✅ fan_in=20 / fan_out=25 |
| **Deps concatenadas** | 105 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa encabezado y XML" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_ca_extraevalortagxml` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_procesaencabezadoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNumIndiceEnc` | `INTEGER` | L11 |
| `iTopeEnc` | `INTEGER` | L12 |
| `cNumOficio` | `CHAR(60)` | L13 |
| `cNumExpediente` | `CHAR(60)` | L14 |
| `cSolSiara` | `CHAR(60)` | L15 |
| `iNumFolio` | `INTEGER` | L16 |
| `cAnioOficio` | `CHAR(4)` | L17 |
| `cIdArea` | `CHAR(2)` | L18 |
| `cDescArea` | `CHAR(30)` | L19 |
| `dFechaPublicacion` | `CHAR(10)` | L20 |
| `cDiazPlazo` | `CHAR(2)` | L21 |
| `cNomAutoridad` | `CHAR(60)` | L22 |
| `cNomAutoridadEsp` | `CHAR(60)` | L23 |
| `cNomSolicitante` | `CHAR(60)` | L24 |
| `cReferencia` | `CHAR(60)` | L25 |
| `cReferencia1` | `CHAR(60)` | L26 |
| `cReferencia2` | `CHAR(60)` | L27 |
| `cAseguramiento` | `CHAR(5)` | L28 |
| `iTotalRegistros` | `INTEGER` | L29 |
| `iIdOficio` | `INTEGER` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_archivoxml_tmp` | `bdicnweb` | no | SELECT | L78 |
| `sw_ca_encabezado` | `bdicnweb` | no | INSERT | L203 |
| `sw_ca_encabezado` | `bdicnweb` | no | SELECT | L212 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ca_extraevalortagxml` | `bdicnweb` | no | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_procesasolicpartesxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_procesasolicpartesxml.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ✅ fan_in=20 / fan_out=25 |
| **Deps concatenadas** | 103 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa solicitud y XML" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_ca_extraevalortagxml` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_procesasolicpartesxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iIdSolicitudEspecifica` | `INTEGER` | L8 |
| `iIdRowMin` | `INTEGER` | L9 |
| `iIdRowMax` | `INTEGER` | L10 |
| `iIdRow` | `INTEGER` | L11 |
| `iParteId` | `INTEGER` | L14 |
| `cCaracter` | `CHAR(30)` | L15 |
| `cDescTipoPersona` | `CHAR(10)` | L16 |
| `cApellidoPaterno` | `CHAR(26)` | L17 |
| `cApellidoMaterno` | `CHAR(26)` | L18 |
| `cNombre` | `CHAR(150)` | L19 |
| `cRfc` | `CHAR(15)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_archivoxml_tmp` | `bdicnweb` | no | SELECT | L61 |
| `sw_ca_solicitudpartes` | `bdicnweb` | no | INSERT | L134 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ca_extraevalortagxml` | `bdicnweb` | no | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL THEN` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `?partes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?partes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ca_verificastatuscaxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ca_verificastatuscaxml.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 102 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y XML" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ca_verificastatuscaxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cBanDetError` | `CHAR(1)` | L12 |
| `cCodError` | `CHAR(6)` | L13 |
| `cDescError` | `CHAR(250)` | L14 |
| `cClaveOficio` | `CHAR(100)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ca_statuscargaxml` | `bdicnweb` | no | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ca_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ca` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ca_`, `?ca` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caj_constaccesotrasaccioncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caj_constaccesotrasaccioncaja.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta acceso, Servicios de Atención al Cliente — subsistema de atención en sucursal y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_accesodot` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_caj_constaccesotrasaccioncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `vmensaje` | `CHAR(50)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_accesodot` | `bdisuc` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet    = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP  bdisuc:sp_accesodot";` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_caj_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `acceso` | ENTIDAD | acceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_caj_`, `?t`, `?tra`, `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caj_genreportefaltsobrantepanacaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caj_genreportefaltsobrantepanacaja.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte TEF — transferencia electrónica de fondos y caja (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rep_faltsob_pana2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_caj_genreportefaltsobrantepanacaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pTransaccion                 CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTransaccion` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cSucursal` | `CHAR(4)` | L14 |
| `cNumPapeleta` | `CHAR(16)` | L15 |
| `mMonto` | `MONEY (16,2)` | L16 |
| `mMontoCSuc` | `MONEY (16,2)` | L17 |
| `mMontoDiferencia` | `MONEY (16,2)` | L18 |
| `dFechaDeferencia` | `DATE` | L19 |
| `cMensaje` | `CHAR(50)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_rep_faltsob_pana2` | `bdisuc` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pTransaccion = '` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L70 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc:sp_rep_faltsob_pana2";` |  |
| L73 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_caj_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?or` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `?altsobr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?epana` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_caj_`, `?or`, `?altsobr`, `?epana` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caj_genreportefaltsobrantepanacaja_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caj_genreportefaltsobrantepanacaja_totales.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte TEF — transferencia electrónica de fondos y caja (anterior, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=4 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_caj_genreportefaltsobrantepanacaja_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pTransaccion                 CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTransaccion` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cSucursal` | `CHAR(4)` | L9 |
| `cNumPapeleta` | `CHAR(16)` | L10 |
| `mMonto` | `MONEY (16,2)` | L11 |
| `mMontoCSuc` | `MONEY (16,2)` | L12 |
| `mMontoDiferencia` | `MONEY (16,2)` | L13 |
| `dFechaDeferencia` | `DATE` | L14 |
| `cMensaje` | `CHAR(50)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_diferenciadot_suc` | `bdisuc` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L41 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pTransaccion = '` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_caj_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?or` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `?altsobr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?epana` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_caj_`, `?or`, `?altsobr`, `?epana` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caj_validasucursalcaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caj_validasucursalcaja.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida sucursal y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_caj_validasucursalcaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `vmensaje` | `CHAR(50)` | L9 |
| `cNombreSucursal` | `CHAR(30)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_valida_suc2` | `bdisuc` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet 	= '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc:sp_valida_suc2";` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00833';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_caj_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_caj_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caja_actualizasaldoporcentaje`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_actualizasaldoporcentaje.sql` |
| **LOC (1er CREATE)** | 192 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 83 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza caja y saldo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_actualizasaldoporcentaje(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pcodproveedor                CHAR(4)
  pSaldoAutorizado             MONEY(19,2)
  pPorcentaje                  INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pcodproveedor` | `CHAR(4)` | — | — |
| `pSaldoAutorizado` | `MONEY(19,2)` | `saldo`=saldo | ✅ CÓDIGO |
| `pPorcentaje` | `INTEGER` | `por`=por (criterio) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `vsqlerr` | `INTEGER` | L7 |
| `vempresa` | `CHAR (3)` | L8 |
| `vcod_proveedor` | `CHAR (4)` | L9 |
| `vdivisa` | `CHAR (2)` | L10 |
| `vsaldo_anterior` | `MONEY (14)` | L11 |
| `vsaldo_asignado` | `MONEY (14)` | L12 |
| `vsaldo_total` | `MONEY (14)` | L13 |
| `vdenominacion_1` | `CHAR (18)` | L14 |
| `vdenominacion_2` | `CHAR (18)` | L15 |
| `vdenominacion_3` | `CHAR (18)` | L16 |
| `vdenominacion_4` | `CHAR (18)` | L17 |
| `vdenominacion_5` | `CHAR (18)` | L18 |
| `vdenominacion_6` | `CHAR (18)` | L19 |
| `vdenominacion_7` | `CHAR (18)` | L20 |
| `vdenominacion_8` | `CHAR (18)` | L21 |
| `vdenominacion_9` | `CHAR (18)` | L22 |
| `vdenominacion_10` | `CHAR (18)` | L23 |
| `vdenominacion_11` | `CHAR (18)` | L24 |
| `vdenominacion_12` | `CHAR (18)` | L25 |
| `vdenominacion_13` | `CHAR (18)` | L26 |
| `vdenominacion_14` | `CHAR (18)` | L27 |
| `vdenominacion_15` | `CHAR (18)` | L28 |
| `vcantidad_1` | `FLOAT (8)` | L29 |
| *…30 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L139 |
| `ss_cajageneral` | `bdisuc` | ⚠️ sí | SELECT | L154 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L164 |
| `ss_bitacora_monitor_saldosautorizados` | `bdisuc` | ⚠️ sí | INSERT | L166 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L131 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L125 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' or pcodproveedor = '' OR pSaldoAutorizado IS NULL OR pPorcentaje` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | FÓRMULA | `LET vbill_det = ((vdenominacion_1 * vcant_1d) + (vdenominacion_2 * vcant_2d) + (vdenominacion_3 * vc` |  |
| L160 | FÓRMULA | `LET vsaldo_total =vsaldo_total + vsaldo_asignado;` |  |
| L161 | FÓRMULA | `LET vsaldo_asignado = vsaldo_asignado - vbill_det;` |  |
| L162 | FÓRMULA | `LET vsaldo_disponible = vsaldo_total - (vsaldo_asignado + vbill_det);` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldo` | ENTIDAD | saldo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?centaje` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?centaje` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caja_cons_denominaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_cons_denominaciones.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 278 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta caja y denominaciones" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_cons_denominaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucAtm                      CHAR(10)
  pFolioOper                   CHAR(8)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucAtm` | `CHAR(10)` | — | — |
| `pFolioOper` | `CHAR(8)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iCodRetSp` | `INTEGER` | L20 |
| `cEmpresa` | `CHAR(3)` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |
| `cDenominacion1` | `CHAR(18)` | L24 |
| `cDenominacion2` | `CHAR(18)` | L25 |
| `cDenominacion3` | `CHAR(18)` | L26 |
| `cDenominacion4` | `CHAR(18)` | L27 |
| `cDenominacion5` | `CHAR(18)` | L28 |
| `cDenominacion6` | `CHAR(18)` | L29 |
| `dCantidad1` | `DECIMAL(12,2)` | L30 |
| `dCantidad2` | `DECIMAL(12,2)` | L31 |
| `dCantidad3` | `DECIMAL(12,2)` | L32 |
| `dCantidad4` | `DECIMAL(12,2)` | L33 |
| `dCantidad5` | `DECIMAL(12,2)` | L34 |
| `dCantidad6` | `DECIMAL(12,2)` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L84 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSucAtm = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL ` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `denominaciones` | ENTIDAD | denominaciones | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_caja_cons_detalle_operacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_cons_detalle_operacion.sql` |
| **LOC (1er CREATE)** | 148 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 277 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta caja y detalle" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_cons_detalle_operacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCajaGral                    CHAR(4)
  pSucAtm                      CHAR(10)
  pFolioOper                   CHAR(8)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCajaGral` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pSucAtm` | `CHAR(10)` | — | — |
| `pFolioOper` | `CHAR(8)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cCodRetSp` | `CHAR(5)` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |
| `cFolioServicio` | `CHAR(16)` | L24 |
| `cDivisa` | `CHAR(2)` | L25 |
| `cStatus` | `CHAR(2)` | L26 |
| `dFechaSolicitud` | `DATE` | L27 |
| `cHoraSolicitud` | `CHAR(5)` | L28 |
| `cUsuarioSolicitud` | `CHAR(8)` | L29 |
| `dFechaEnvio` | `DATE` | L30 |
| `dFechaRecepcion` | `DATE` | L31 |
| `iPzas` | `INTEGER` | L32 |
| `mMontos` | `MONEY(16,2)` | L33 |
| `cDescDivisa` | `CHAR(30)` | L34 |
| `cDescStatus` | `CHAR(30)` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L84 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L88 |
| `si_divisas` | `bdinteg` | ⚠️ sí | SELECT | L114 |
| `ss_catstatus` | `bdisuc` | ⚠️ sí | SELECT | L122 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCajaGral = '' OR pSucAtm = '' OR pFolioOper = '' OR pFechaIn` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L127 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operacion` | ACCION | operación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_caja_cons_detalle_reporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_cons_detalle_reporte.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 276 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta caja, detalle y reporte" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_cons_detalle_reporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCajaGral                    CHAR(4)
  pSucAtm                      CHAR(10)
  pFolioOper                   CHAR(8)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCajaGral` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pSucAtm` | `CHAR(10)` | — | — |
| `pFolioOper` | `CHAR(8)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |
| `dFecha` | `DATE` | L21 |
| `mMonto` | `MONEY(16,2)` | L22 |
| `cSucursal` | `CHAR(4)` | L23 |
| `cNoPapeleta` | `CHAR(16)` | L24 |
| `cCajaGral` | `CHAR(30)` | L25 |
| `cCodTrans` | `CHAR(4)` | L26 |
| `mSaldoAsignado` | `MONEY(16,2)` | L27 |
| `mSaldoTotal` | `MONEY(16,2)` | L28 |
| `cDescSuc` | `CHAR(40)` | L29 |
| `cDescCodTrans` | `CHAR(35)` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L77 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L90 |
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L99 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L60 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pCajaGral = '` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L104 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_caja_cons_informacion_reporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_cons_informacion_reporte.sql` |
| **LOC (1er CREATE)** | 131 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 275 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta caja, información, dirección MAC y reporte" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_cons_informacion_reporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCajaGral                    CHAR(4)
  pSucAtm                      CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCajaGral` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pSucAtm` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cCodRetSp` | `CHAR(5)` | L23 |
| `iCodRetSp` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |
| `dFecha` | `DATE` | L28 |
| `mMonto` | `MONEY(16,2)` | L29 |
| `cSucursal` | `CHAR(4)` | L30 |
| `cFolio` | `CHAR(8)` | L31 |
| `cCodTrans` | `CHAR(4)` | L32 |
| `mSaldoAsignado` | `MONEY(16,2)` | L33 |
| `mSaldoTotal` | `MONEY(16,2)` | L34 |
| `iPzas` | `INTEGER` | L35 |
| `mMontos` | `MONEY(16,2)` | L36 |
| `cCajaGral` | `char(30)` | L37 |
| `cDescripcion` | `char(30)` | L38 |
| `iTotalPzas` | `INTEGER` | L40 |
| `mTotalMontos` | `MONEY(16,2)` | L41 |
| `mTotalSaldoAsignado` | `MONEY(16,2)` | L42 |
| `mTotalSaldoTotal` | `MONEY(16,2)` | L43 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_repdetalle_mensual_cajagral` | `bdicnweb` | no | SELECT | L103 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pCajaGral = '' OR pSucAtm = '' OR pFechaInicial  IS NULL OR ` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L109 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mac` | ENTIDAD | dirección MAC | 🔵 CONVENCIÓN | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?ion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caja_consulta_caja_general`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_consulta_caja_general.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 274 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta caja (general)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_consulta_caja_general(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `cCodProveedor` | `CHAR(4)` | L11 |
| `cDescripcion` | `CHAR(30)` | L12 |
| `cPlaza` | `CHAR(3)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `general` | MODIF | general | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_caja_consulta_sucursal_atm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_consulta_sucursal_atm.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 273 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta caja, sucursal y cajero automático" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_consulta_sucursal_atm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPlaza                       CHAR(3)
  pTipo                        CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPlaza` | `CHAR(3)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `cNoSucursalAtm` | `CHAR(4)` | L10 |
| `cDescripcion` | `CHAR(40)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `atm` | ENTIDAD | cajero automático (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_caja_consultasaldosporcentaje`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_consultasaldosporcentaje.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 82 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta caja y saldos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_consultasaldosporcentaje(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pcodproveedor                CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pcodproveedor` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `msaldo_autorizado` | `MONEY(19,2)` | L8 |
| `iporcentaje_variacion` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L40 |
| `ss_cajageneral` | `bdisuc` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | 🔵 CONVENCIÓN | nombre_sp |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?centaje` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?centaje` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caja_totrepmenscajagral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_totrepmenscajagral.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 272 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte caja (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_totrepmenscajagral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCajaGral                    CHAR(4)
  pSucAtm                      CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCajaGral` | `CHAR(4)` | `caja`=caja / ventanilla · `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pSucAtm` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNumRegistros` | `INTEGER` | L7 |
| `iTotalPzas` | `INTEGER` | L8 |
| `mTotalMontos` | `MONEY(16,2)` | L9 |
| `mTotalSaldoAsignado` | `MONEY(16,2)` | L10 |
| `mTotalSaldoTotal` | `MONEY(16,2)` | L11 |
| `cDescripcion` | `CHAR(30)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_caja_inf_reporte` | `bdicnweb` | no | SELECT | L38 |
| `status_caja_inf_reporte` | `bdicnweb` | no | DELETE | L38 |
| `status_caja_inf_reporte` | `bdicnweb` | no | INSERT | L39 |
| `sw_repdetalle_mensual_cajagral` | `bdicnweb` | no | SELECT | L57 |
| `sw_repdetalle_mensual_cajagral` | `bdicnweb` | no | DELETE | L57 |
| `sw_repdetalle_mensual_cajagral` | `bdicnweb` | no | INSERT | L62 |
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L64 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L69 |
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L104 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCajaGral = '' OR pSucAtm = '' OR pFechaInicial IS NULL OR pF` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?_tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?mens` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tot`, `?mens` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_caja_verificastatusrepmenscajagral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_caja_verificastatusrepmenscajagral.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 271 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica caja y estatus (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_caja_verificastatusrepmenscajagral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L11 |
| `iTotal` | `INTEGER` | L12 |
| `cErrorProceso` | `CHAR(1)` | L13 |
| `cError` | `CHAR(5)` | L14 |
| `iNumRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_caja_inf_reporte` | `bdicnweb` | no | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?mens` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mens` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_calculadigitointercambio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_calculadigitointercambio.sql` |
| **LOC (1er CREATE)** | 217 |
| **Callgraph** | ✅ fan_in=7 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "calcula dígito verificador y intercambio" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_calculadigitointercambio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pModulo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pModulo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iDigitoIntercambio` | `INTEGER` | L9 |
| `iNumModulo` | `INTEGER` | L10 |
| `xCuenta` | `CHAR(20)` | L11 |
| `iMaximo` | `INTEGER` | L12 |
| `iPos` | `INTEGER` | L13 |
| `iNumero` | `INTEGER` | L14 |
| `iAux` | `INTEGER` | L15 |
| `iSuma` | `INTEGER` | L16 |
| `iRes` | `INTEGER` | L17 |
| `iDigito` | `INTEGER` | L18 |
| `cAux` | `CHAR(11)` | L19 |
| `cSuma` | `CHAR(11)` | L20 |
| `cNumModulo` | `CHAR(11)` | L21 |
| `iTamAux` | `INTEGER` | L22 |
| `iCorteAux` | `INTEGER` | L23 |
| `iTamSum` | `INTEGER` | L24 |
| `iCorteSum` | `INTEGER` | L25 |
| `iTamNumMod` | `INTEGER` | L26 |
| `iCorteNumMod` | `INTEGER` | L27 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | FÓRMULA | `LET iDigitoIntercambio = -1;` |  |
| L95 | FÓRMULA | `LET iDigitoIntercambio = -1;` |  |
| L111 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 3;` |  |
| L118 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 7;` |  |
| L125 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 1;` |  |
| L133 | FÓRMULA | `LET iSuma = NVL(iSuma,0) + NVL(iAux,0);` |  |
| L134 | FÓRMULA | `LET iPos = NVL(iPos,0) + 1;` |  |
| L142 | FÓRMULA | `LET cNumModulo = iNumModulo - iRes;` | 🔴 MONEY/aritmética financiera |
| L152 | FÓRMULA | `LET iNumero = SUBSTR(xCuenta,(iMaximo - iPos),1);` |  |
| L155 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 2;` |  |
| L157 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 3;` |  |
| L159 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 4;` |  |
| L161 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 5;` |  |
| L163 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 6;` |  |
| L165 | FÓRMULA | `LET iAux = NVL(iNumero,0) * 7;` |  |
| L168 | FÓRMULA | `LET iSuma = NVL(iSuma,0) + NVL(iAux,0);` |  |
| L169 | FÓRMULA | `LET iPos = NVL(iPos,0) + 1;` |  |
| L180 | FÓRMULA | `LET iDigito = 11 - NVL(iRes,0);` |  |
| L190 | FÓRMULA | `LET iDigitoIntercambio = -1;` |  |
| L194 | FÓRMULA | `LET iDigitoIntercambio = -1;` |  |
| L201 | FÓRMULA | `LET iDigito = NVL(iNumModulo,0) - NVL(iRes,0);` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `calcula` | ACCION | calcula (verbo activo — spei_calculointeres) | 🔵 CONVENCIÓN | nombre_sp |
| `digito` | ENTIDAD | dígito verificador | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `intercambio` | ENTIDAD | intercambio (interbancario) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_calculalinsugcteaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_calculalinsugcteaumlincred.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 103 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "calcula línea y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_calculalinsugcteaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pCompIngreso                 CHAR(1)
  pIngresoMens                 DECIMAL(18,2)
  pOtrosComp                   DECIMAL(18,2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pCompIngreso` | `CHAR(1)` | — | — |
| `pIngresoMens` | `DECIMAL(18,2)` | — | — |
| `pOtrosComp` | `DECIMAL(18,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cMensajeRetorno` | `CHAR(80)` | L13 |
| `dLineaSugerida` | `DECIMAL(18,2)` | L14 |
| `dMontoIncremento` | `DECIMAL(18,2)` | L15 |
| `dRazonIncremento` | `DECIMAL(4,2)` | L16 |
| `dTasaInteresAnualIncremento` | `DECIMAL(5,2)` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L46 |
| `bdicred` | `bdicnweb` | no | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumSolicitud = '' OR pCompIngreso = '' OR pIngresoMens IS NU` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_calculalinsugcte';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00266';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00267';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00268';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00269';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00270';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00271';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00272';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00273';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `calcula` | ACCION | calcula (verbo activo — spei_calculointeres) | 🔵 CONVENCIÓN | nombre_sp |
| `lin` | ENTIDAD | línea (de crédito) | 🟡 INFERIDO | nombre_sp |
| `?sug` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sug` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_calcularrfc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_calcularrfc.sql` |
| **LOC (1er CREATE)** | 266 |
| **Callgraph** | ✅ fan_in=35 / fan_out=10 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "calcula RFC" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Tokens confirmados en el vocab pero DML no correlaciona con el propósito |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_calcularrfc(
  pApellidoPaterno             CHAR(26)
  pApellidoMaterno             CHAR(26)
  pNombre                      CHAR(55)
  pFechaNacimiento             DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pApellidoPaterno` | `CHAR(26)` | — | — |
| `pApellidoMaterno` | `CHAR(26)` | — | — |
| `pNombre` | `CHAR(55)` | — | — |
| `pFechaNacimiento` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cRfc` | `CHAR(13)` | L7 |
| `cCaracter` | `CHAR(1)` | L8 |
| `i` | `SMALLINT` | L9 |
| `bBoolValue` | `BOOLEAN` | L10 |
| `lPalabra` | `LVARCHAR` | L11 |
| `bSalirBucle` | `BOOLEAN` | L12 |
| `cPrimerLetraApellidoPaterno` | `CHAR(1)` | L14 |
| `cVocalApellidoPaterno` | `CHAR(1)` | L15 |
| `cPrimerLetraApellidoMaterno` | `CHAR(1)` | L16 |
| `cPrimerLetraNombre` | `CHAR(1)` | L17 |
| `cFechaNacimientos` | `CHAR(6)` | L18 |
| `cHomoclave` | `CHAR(2)` | L19 |
| `cDigitoVerificador` | `CHAR(2)` | L20 |
| `cApellidoMaterno` | `CHAR(26)` | L21 |
| `iCont` | `INTEGER` | L23 |
| `lPalabra2` | `LVARCHAR` | L24 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pNombre = '' OR pFechaNacimiento IS NULL THEN` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | FÓRMULA | `Let iCont=iCont+1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `calcular` | ACCION | calcula (infinitivo) | 🔵 CONVENCIÓN | nombre_sp |
| `rfc` | ENTIDAD | RFC (registro fiscal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_calificacion_scoring`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_calificacion_scoring.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=117 / fan_out=42 |
| **Principales callers** | `sps_grabaremail`, `sp_cnsif_soliccred`, `sp_consultaorigenpoliza_clubfam` |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "califica scoring crediticio" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_calificacion_scoring(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pSeccion                     SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pSeccion` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `dEvaluacion` | `DECIMAL(5, 2)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumSolicitud = '' OR pSeccion IS NULL THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | VALIDACIÓN_NULL | `IF dEvaluacion IS NULL THEN` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `califica` | ACCION | califica / evalúa (scoring) | 🔵 CONVENCIÓN | nombre_sp |
| `?cion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `scoring` | ENTIDAD | scoring crediticio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cambioinstruccionespagare`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cambioinstruccionespagare.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Propósito inferido** | "insertar cambio y cc — cuenta corriente (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_obtienevalorinstrucc`, `cambinstrucc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cambioinstruccionespagare(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pCuentaReferencia            CHAR(20)
  pInstruccion                 SMALLINT
  pFechaVencimiento            DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pCuentaReferencia` | `CHAR(20)` | — | — |
| `pInstruccion` | `SMALLINT` | `ins`=insertar · `cc`=cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO |
| `pFechaVencimiento` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cCapital` | `CHAR(2)` | L8 |
| `cInteres` | `CHAR(2)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_obtienevalorinstrucc` | `bdinvers` | ⚠️ sí | L40 |
| `cambinstrucc` | `bdinvers` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00172';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00173';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00174';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00175';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00176';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00177';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00178';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?tru` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?agare` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tru`, `?ion`, `?agare` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_canal_cobro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_canal_cobro.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cobro canal" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bitacora` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_canal_cobro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIp                          CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIp` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cIdCanalFinal` | `CHAR(2)` | L13 |
| `cCveCanalFinal` | `CHAR(30)` | L14 |
| `cNombreCanal` | `CHAR(30)` | L15 |
| `cCobrarOtp` | `CHAR(1)` | L16 |
| `cAccion` | `CHAR(50)` | L17 |
| `cEntidad` | `CHAR(100)` | L18 |
| `crespaldo` | `CHAR(50)` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_bitacora` | `bdicnweb` | no | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `canal` | ENTIDAD | canal (de distribución) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cobro` | ACCION | cobro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_canal_cobro_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_canal_cobro_totales.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cobro canal (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_canal_cobro_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iTotales` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `canal` | ENTIDAD | canal (de distribución) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cobro` | ACCION | cobro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_canal_generacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_canal_generacion.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera canal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_canal_generacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIp                          CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIp` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cIdCanalInicial` | `CHAR(2)` | L14 |
| `cCveCanalInicial` | `CHAR(30)` | L15 |
| `cNombreCanal` | `CHAR(30)` | L16 |
| `cAccion` | `CHAR(50)` | L18 |
| `cEntidad` | `CHAR(100)` | L19 |
| `cIp` | `CHAR(20)` | L20 |
| `crespaldo` | `CHAR(50)` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `sp_bitacora` | `bdicnweb` | no | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `canal` | ENTIDAD | canal (de distribución) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_canal_generacion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_canal_generacion_totales.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera canal (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_canal_generacion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iTotales` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `canal` | ENTIDAD | canal (de distribución) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?cion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cancela_detalle_retiro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cancela_detalle_retiro.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 76 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela detalle y retiro" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_generararchivo_rst`, `sp_bitacora` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cancela_detalle_retiro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFoliooperacion              CHAR(100)
  pIp                          CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFoliooperacion` | `CHAR(100)` | — | — |
| `pIp` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cAccion` | `CHAR(50)` | L9 |
| `cEntidad` | `CHAR(100)` | L10 |
| `crespaldo` | `CHAR(50)` | L11 |
| `iExiste` | `INTEGER` | L12 |
| `cCmd1` | `CHAR(2000)` | L13 |
| `cArchivoRespaldo` | `CHAR(50)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_generararchivo_rst` | `bdicnweb` | no | L53 |
| `sp_bitacora` | `bdicnweb` | no | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"'cr_reverso_fecha', 'cr_rechazo_fecha' FROM systables WHERE tabid =` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cancelacion_claves_retiro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cancelacion_claves_retiro.sql` |
| **LOC (1er CREATE)** | 121 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela clave y retiro" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bitacora` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cancelacion_claves_retiro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFoliooperacion              CHAR(100)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pTarjeta                     CHAR(19)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pIp                          CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFoliooperacion` | `CHAR(100)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTarjeta` | `CHAR(19)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pIp` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cCodRetSp` | `CHAR(6)` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `condition` | `CHAR(200)` | L20 |
| `cAccion` | `CHAR(50)` | L21 |
| `cEntidad` | `CHAR(100)` | L22 |
| `vFolio` | `CHAR(100)` | L23 |
| `dFecha` | `DATETIME YEAR TO FRACTION(3)` | L24 |
| `vNumCliente` | `CHAR(20)` | L25 |
| `vNumCuenta` | `CHAR(20)` | L26 |
| `vNumTarjeta` | `CHAR(20)` | L27 |
| `mMonto` | `MONEY(16,2)` | L28 |
| `vCanalCobro` | `CHAR(10)` | L29 |
| `cStatus` | `CHAR(1)` | L30 |
| `vDescStatus` | `CHAR(20)` | L31 |
| `iRecuperacion` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L90 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |
| `sp_bitacora` | `bdicnweb` | no | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancelacion` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cancelacion_claves_retiro_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cancelacion_claves_retiro_totales.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela clave y retiro (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cancelacion_claves_retiro_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFoliooperacion              CHAR(100)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pTarjeta                     CHAR(20)
  pFecha_inicial               DATE
  pFecha_final                 DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFoliooperacion` | `CHAR(100)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTarjeta` | `CHAR(20)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iTotales` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancelacion` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cancelaperturacteprospecto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cancelaperturacteprospecto.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=43 |
| **Deps concatenadas** | 76 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `canc_aper` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cancelaperturacteprospecto(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS cCodigoRetorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L4 |
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `canc_aper` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pertur` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?ecto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pertur`, `?ep`, `ro`, `?ecto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cancelarcreditocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cancelarcreditocre.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cancelarcredito` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cancelarcreditocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pMotivoCancelacion           CHAR(3)
  pSupervisor                  CHAR(8)
  pTipoCancelacion             CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pMotivoCancelacion` | `CHAR(3)` | `cancela`=cancela | 🔵 CONVENCIÓN |
| `pSupervisor` | `CHAR(8)` | — | — |
| `pTipoCancelacion` | `CHAR(1)` | `cancela`=cancela | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cFolioSuc` | `CHAR(16)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cSucursal` | `CHAR(4)` | L10 |
| `iInTrans` | `SMALLINT` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cancelarcredito` | `bdicred` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cancelarcreditomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cancelarcreditomasivocre.sql` |
| **LOC (1er CREATE)** | 229 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_catalogomotivoscancelacioncre`, `sp_cancelarcredito`, `sp_consaldodisp` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cancelarcreditomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pPlantilla                   CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cEmpresa` | `CHAR(3)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `cCodRetSpSaldo` | `CHAR(5)` | L7 |
| `dFechaProc` | `DATETIME YEAR TO SECOND` | L8 |
| `dFechaCancelacion` | `DATETIME YEAR TO SECOND` | L9 |
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iNoRegs` | `INTEGER` | L12 |
| `iIdRegistro` | `INTEGER` | L13 |
| `cCuenta` | `CHAR(20)` | L14 |
| `cResultado` | `CHAR(15)` | L15 |
| `cStatus` | `CHAR(1)` | L16 |
| `iInTrans` | `INTEGER` | L17 |
| `iRegRechazados` | `INTEGER` | L18 |
| `iTotalRegs` | `INTEGER` | L19 |
| `mTotalMonto` | `MONEY(14,2)` | L20 |
| `dFechaMail` | `DATETIME YEAR TO SECOND` | L21 |
| `mSaldo` | `MONEY(14,2)` | L22 |
| `cCodigoCancelacion` | `CHAR(3)` | L23 |
| `cTipoCancelacion` | `CHAR(1)` | L24 |
| `cSucursal` | `CHAR(4)` | L25 |
| `cFolioSuc` | `CHAR(16)` | L26 |
| `iClaveCancelacion` | `SMALLINT` | L27 |
| `cMotivoCancelacion` | `CHAR(45)` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_cancelacioncre` | `bdicnweb` | no | SELECT | L85 |
| `sw_tr_cargamasiva_cancelacioncre_hist` | `bdicnweb` | no | INSERT | L161 |
| `sw_tr_cargamasiva_cancelacioncre` | `bdicnweb` | no | DELETE | L164 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L184 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_catalogomotivoscancelacioncre` | `bdicnweb` | no | L110 |
| `sp_cancelarcredito` | `bdicred` | ⚠️ sí | L119 |
| `sp_consaldodisp` | `bdicnweb` | no | L134 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L146 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `credito` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cancelarprocejecpagosafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cancelarprocejecpagosafore.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=39 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela proceso, código postal, OS — Originación de Solicitudes y AFORE" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_validahoraejec`, `sp_aforecancelarprocejecpagos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cancelarprocejecpagosafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(30)
  pTipoCancelacion             SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(30)` | — | — |
| `pTipoCancelacion` | `SMALLINT` | `cancela`=cancela | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cHoraProceso` | `CHAR(21)` | L8 |
| `cHoraServidor` | `CHAR(21)` | L9 |
| `pTipoArchivo` | `CHAR(1)` | L10 |
| `cMensajeRet` | `CHAR(200)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L46 |
| `15` | `bdicnweb` | no | SELECT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_validahoraejec` | `bdiprog` | ⚠️ sí | L53 |
| `sp_aforecancelarprocejecpagos` | `bdiprog` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | VALIDACIÓN_NULL | `IF cHoraProceso = '' OR cHoraProceso IS NULL THEN` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_validahoraejec';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00434';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_aforecancelarprocej` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00503';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00504';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00505';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00506';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `proc` | ENTIDAD | proceso | 🟡 INFERIDO | nombre_sp |
| `?eje` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `?ag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?eje`, `?ag` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_actualizanombrexmlfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_actualizanombrexmlfatca.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, nombre y XML · FATCA" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_actualizanombrexmlfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNomXml                      CHAR(40)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNomXml` | `CHAR(40)` | `xml`=XML | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNomXml` | `CHAR(40)` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_bitacoreogenreportesfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_bitacoreogenreportesfatca.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 · FATCA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_bitacoreogenreportesfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdilide` | `bdicnweb` | no | INSERT | L32 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L26 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `?_bitacoreo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?ortes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_bitacoreo`, `?ortes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_capturaclientesfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_capturaclientesfatca.sql` |
| **LOC (1er CREATE)** | 345 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "captura Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y clientes · FATCA" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_capturaclientesfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjercicio                   CHAR(4)
  pNumCliente                  CHAR(20)
  pCuenta                      CHAR(20)
  pTipoReporte                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjercicio` | `CHAR(4)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cRazonSocial` | `CHAR(60)` | L6 |
| `cApellPaterno` | `CHAR(26)` | L7 |
| `cApellMaterno` | `CHAR(26)` | L8 |
| `cNombre1` | `CHAR(26)` | L9 |
| `cNombre2` | `CHAR(26)` | L10 |
| `cCalle` | `CHAR(40)` | L11 |
| `cNumIntCalle` | `CHAR(10)` | L12 |
| `cNumExtCalle` | `CHAR(10)` | L13 |
| `cColonia` | `CHAR(32)` | L14 |
| `cDelegacion` | `CHAR(27)` | L15 |
| `cCodPostal` | `CHAR(5)` | L16 |
| `cPais` | `CHAR(3)` | L17 |
| `cCiudad` | `CHAR(30)` | L18 |
| `cRfc` | `CHAR(13)` | L19 |
| `dFechaNac` | `CHAR(10)` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `mMonto` | `MONEY(18,2)` | L22 |
| `cSumaProm` | `MONEY (18,2)` | L23 |
| `cMesesActivo` | `INTEGER` | L24 |
| `mInteresPagado` | `MONEY(16,2)` | L25 |
| `cEjercicio` | `CHAR(4)` | L26 |
| `cTipoPersona` | `CHAR(2)` | L27 |
| `cCRazonSocial` | `CHAR(60)` | L28 |
| *…20 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L125 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L130 |
| `si_paises` | `bdinteg` | ⚠️ sí | SELECT | L159 |
| `sc_retenisr` | `bdicheq` | ⚠️ sí | SELECT | L188 |
| `sl_ftc_cns` | `bdilide` | ⚠️ sí | SELECT | L278 |
| `sl_ftc_cte` | `bdilide` | ⚠️ sí | SELECT | L285 |
| `sl_ftc_det` | `bdilide` | ⚠️ sí | SELECT | L289 |
| `sl_ftc_cte` | `bdilide` | ⚠️ sí | INSERT | L315 |
| `sl_ftc_det` | `bdilide` | ⚠️ sí | INSERT | L321 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L116 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L267 | FÓRMULA | `LET mMonto = cSumaProm / cMesesActivo;` | 🔴 MONEY/aritmética financiera |
| L281 | VALIDACIÓN_NULL | `IF sConsecutivo IS NULL THEN` |  |
| L308 | CÓDIGO_RETORNO | `LET cCodRet = '00293';` |  |
| L326 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `captura` | ACCION | captura | 🔵 CONVENCIÓN | nombre_sp |
| `clientes` | ENTIDAD | clientes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_capturactparametrosfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_capturactparametrosfatca.sql` |
| **LOC (1er CREATE)** | 192 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "captura Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y ro — Rol Operativo (tipo) · FATCA" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cap_clasificadorparametrosfatca` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_capturactparametrosfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(2)
  pTipoParam                   INTEGER
  pValorAsociado               INTEGER
  pValorParam                  CHAR(5)
  pValor                       CHAR(200)
  pDesValor                    CHAR(200)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(2)` | — | — |
| `pTipoParam` | `INTEGER` | — | — |
| `pValorAsociado` | `INTEGER` | — | — |
| `pValorParam` | `CHAR(5)` | — | — |
| `pValor` | `CHAR(200)` | — | — |
| `pDesValor` | `CHAR(200)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cUltimoValor` | `INTEGER` | L8 |
| `cNewValor` | `CHAR(20)` | L9 |
| `sCveParam` | `SMALLINT` | L10 |
| `cValorParam` | `CHAR(5)` | L11 |
| `cValor` | `CHAR(200)` | L12 |
| `cDescValor` | `CHAR(200)` | L13 |
| `cUsuarioAct` | `CHAR(8)` | L14 |
| `dFechaAct` | `DATETIME YEAR TO SECOND` | L15 |
| `cValorAsociadoPadre` | `CHAR(5)` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_prm` | `bdilide` | ⚠️ sí | SELECT | L70 |
| `sl_ftc_prm` | `bdilide` | ⚠️ sí | INSERT | L81 |
| `sl_ftc_log` | `bdilide` | ⚠️ sí | INSERT | L83 |
| `sl_ftc_prm` | `bdilide` | ⚠️ sí | UPDATE | L120 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |
| `sp_cap_clasificadorparametrosfatca` | `bdicnweb` | no | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pBandera = ''  OR pTipoParam IS NULL OR pValor = '' OR pDesVa` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cap_clasificadorparametrosfa` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00520';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00520';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00520';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00520';` |  |
| L174 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `captura` | ACCION | captura | 🔵 CONVENCIÓN | nombre_sp |
| `?c` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?aramet` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?c`, `?aramet`, `ro`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_capturaeactulizagat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_capturaeactulizagat.sql` |
| **LOC (1er CREATE)** | 172 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "captura Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_capturaeactulizagat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pFecha                       DATE
  pProducto                    CHAR(4)
  pTasa                        DECIMAL(9,6)
  pGatNominal                  DECIMAL(9,6)
  pGatReal                     DECIMAL(9,6)
  pPlazaInicio                 INTEGER
  pPlazaFin                    INTEGER
  pPeriodo                     INTEGER
  pRowId                       INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pTasa` | `DECIMAL(9,6)` | — | — |
| `pGatNominal` | `DECIMAL(9,6)` | — | — |
| `pGatReal` | `DECIMAL(9,6)` | — | — |
| `pPlazaInicio` | `INTEGER` | — | — |
| `pPlazaFin` | `INTEGER` | — | — |
| `pPeriodo` | `INTEGER` | — | — |
| `pRowId` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `dTasa` | `DECIMAL(9,6)` | L8 |
| `dFechaMax` | `DATE` | L9 |
| `iRowID` | `INTEGER` | L10 |
| `dGatReal` | `DECIMAL(9,6)` | L12 |
| `dGatNominal` | `DECIMAL(9,6)` | L13 |
| `iPeriodo` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_gat` | `bdicheq` | ⚠️ sí | SELECT | L55 |
| `si_bitacoraprod1100` | `bdinteg` | ⚠️ sí | INSERT | L84 |
| `sc_gat` | `bdicheq` | ⚠️ sí | INSERT | L115 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00133';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `captura` | ACCION | captura | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?ulizagat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?ulizagat` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_catalogocanalfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_catalogocanalfatca.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, catálogo y canal · FATCA" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_catalogocanalfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `sValorParam` | `SMALLINT` | L8 |
| `cValor` | `CHAR(200)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_prm` | `bdilide` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `canal` | ENTIDAD | canal (de distribución) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_catalogoclasificacionfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_catalogoclasificacionfatca.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, catálogo y SIF — canal de estado de cuenta · FATCA" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_catalogoclasificacionfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCvParam` | `CHAR(5)` | L8 |
| `cDescVParam` | `CHAR(200)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_clas_cat` | `bdilide` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?cla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sif` | ENTIDAD | SIF — canal de estado de cuenta (aclaraciones_edocta_sif, de | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cla`, `?i`, `?ion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_catalogotipoparamfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_catalogotipoparamfatca.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, catálogo y parámetro (tipo de) · FATCA" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_catalogotipoparamfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCveParam` | `INTEGER` | L8 |
| `cDescParam` | `CHAR(100)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_cat` | `bdilide` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_clasificadorparametrosfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_clasificadorparametrosfatca.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=78 / fan_out=3 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, SIF — canal de estado de cuenta y parámetros · FATCA" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_clasificadorparametrosfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoParam                   INTEGER
  pValorAsociado               INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoParam` | `INTEGER` | — | — |
| `pValorAsociado` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cUltimoValor` | `INTEGER` | L8 |
| `cNewValor` | `CHAR(20)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | VALIDACIÓN_NULL | `IF cUltimoValor IS NULL THEN` |  |
| L48 | FÓRMULA | `LET cNewValor = cUltimoValor + .1;` |  |
| L62 | VALIDACIÓN_NULL | `IF cUltimoValor IS NULL  THEN` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `?_cla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sif` | ENTIDAD | SIF — canal de estado de cuenta (aclaraciones_edocta_sif, de | 🟡 INFERIDO | nombre_sp |
| `?icador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cla`, `?icador` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consconsecutivonormalfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consconsecutivonormalfatca.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y consecutivo · FATCA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consconsecutivonormalfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjercicio                   SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjercicio` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `sConsecutivo` | `SMALLINT` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `consecutivo` | ENTIDAD | consecutivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?normal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?normal` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consdepositoctatransfer`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consdepositoctatransfer.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, depósito, OS — Originación de Solicitudes, cuenta y transferencia" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consdepositoctatransfer(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cTransaccTrf` | `CHAR(5)` | L11 |
| `iSerial` | `INTEGER` | L12 |
| `cFolioSuc` | `CHAR(16)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `bdicnweb` | `bdicnweb` | no | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidomi:sp_domi_genrep102';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ito` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ito` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consdoctatransfersoccomp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consdoctatransfersoccomp.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, cuenta, transferencia y Sistema Operativo Central (complemento)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consdoctatransfersoccomp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pTransaccTrf                 CHAR(5)
  pSerial                      INTEGER
  pFolioSuc                    CHAR(16)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTransaccTrf` | `CHAR(5)` | — | — |
| `pSerial` | `INTEGER` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `mSdoCtaTrf` | `MONEY(14,2)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L37 |
| `bdicnweb` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicnweb:sp_consdoctatransfers` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00710';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?do` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?do` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consdtalleclientefatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consdtalleclientefatca.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y cliente · FATCA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consdtalleclientefatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjercicio                   CHAR(4)
  pTipoReporte                 CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjercicio` | `CHAR(4)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cNumCte` | `CHAR(20)` | L11 |
| `cNombre` | `CHAR(150)` | L12 |
| `cNumCuenta` | `CHAR(20)` | L13 |
| `dSaldo` | `DECIMAL(16,2)` | L14 |
| `cTipoReporte` | `CHAR(1)` | L15 |
| `sConsecutivoRpt` | `SMALLINT` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |
| `iRegistros` | `INTEGER` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |
| `cActividad` | `CHAR(50)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_cte` | `bdilide` | ⚠️ sí | SELECT | L66 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pEjercicio = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L81 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L99 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?dtalle` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cliente` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dtalle` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consordenxsdfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consordenxsdfatca.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, orden y sd — saldo disponible · FATCA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consordenxsdfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `sCveParam` | `SMALLINT` | L12 |
| `cValorParam` | `CHAR(5)` | L13 |
| `cValor` | `CHAR(200)` | L14 |
| `cDescValor` | `CHAR(200)` | L15 |
| `cUsuarioAct` | `CHAR(8)` | L16 |
| `dFechaAct` | `DATETIME YEAR TO SECOND` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `orden` | ENTIDAD | orden | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_constipoproductogat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_constipoproductogat.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y producto (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_constipoproductogat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `sIdProducto` | `SMALLINT` | L9 |
| `cNumProducto` | `CHAR(4)` | L10 |
| `cDescProducto` | `CHAR(30)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cap_tipoproductogat` | `bdicnweb` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `producto` | ENTIDAD | producto | 🔵 CONVENCIÓN | nombre_sp |
| `?gat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gat` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consultabonotransfer`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultabonotransfer.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y transferencia" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_abonotransfersoc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultabonotransfer(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pTransacc                    CHAR(4)
  pCuenta                      CHAR(20)
  pCheque                      INTEGER
  pMonto                       DECIMAL(14,2)
  pReferencia                  CHAR(40)
  pTarjeta                     CHAR(16)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pTransacc` | `CHAR(4)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pCheque` | `INTEGER` | — | — |
| `pMonto` | `DECIMAL(14,2)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pTarjeta` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(80)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_abonotransfersoc` | `bdicnweb` | no | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pMonto is null OR pSucursal = '' OR pTransacc = ''  THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_abonotransfersoc';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00711';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00667';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00456';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00712';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?bono` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bono` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consultacargotransfersoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultacargotransfersoc.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, cargo, transferencia y Sistema Operativo Central" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cargotransfersoc` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultacargotransfersoc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pTransacc                    CHAR(4)
  pCuenta                      CHAR(20)
  pCheque                      INTEGER
  pMonto                       DECIMAL(14,2)
  pReferencia                  CHAR(40)
  pTarjeta                     CHAR(16)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pTransacc` | `CHAR(4)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pCheque` | `INTEGER` | — | — |
| `pMonto` | `DECIMAL(14,2)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pTarjeta` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(40)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_cargotransfersoc` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pMonto is null OR pSucursal = '' OR pTransacc = ''  THEN` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cargotransfersoc';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00667';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00456';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00714';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_consultaclientefatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultaclientefatca.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y cliente · FATCA" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultaclientefatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjercicio                   CHAR(4)
  pTipoReporte                 CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjercicio` | `CHAR(4)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cNumCte` | `CHAR(20)` | L11 |
| `cNombre` | `CHAR(150)` | L12 |
| `cNumCuenta` | `CHAR(20)` | L13 |
| `dSaldo` | `DECIMAL(16,2)` | L14 |
| `cTipoReporte` | `CHAR(1)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |
| `cActividad` | `CHAR(50)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_log` | `bdilide` | ⚠️ sí | INSERT | L90 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pEjercicio = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L70 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L86 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cliente` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_consultaclientefatca_total`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultaclientefatca_total.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y cliente (total) · FATCA" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultaclientefatca_total(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjercicio                   CHAR(4)
  pTipoReporte                 CHAR (1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjercicio` | `CHAR(4)` | — | — |
| `pTipoReporte` | `CHAR (1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cliente` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_consultaconsecutivofatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultaconsecutivofatca.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y consecutivo · FATCA" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultaconsecutivofatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjercicio                   SMALLINT
  pTipoRpt                     CHAR(1)
  pFolio                       CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjercicio` | `SMALLINT` | — | — |
| `pTipoRpt` | `CHAR(1)` | — | — |
| `pFolio` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `sConsecutivo` | `SMALLINT` | L7 |
| `cFolio` | `CHAR(20)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdilide` | `bdicnweb` | no | INSERT | L41 |
| `sl_ftc_log` | `bdilide` | ⚠️ sí | INSERT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | VALIDACIÓN_NULL | `IF (sConsecutivo IS NULL OR sConsecutivo = 0 ) THEN` |  |
| L80 | FÓRMULA | `LET sConsecutivo = sConsecutivo + 1;` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consecutivo` | ENTIDAD | consecutivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_consultacredencialesfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultacredencialesfatca.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y crédito · FATCA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultacredencialesfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `cCredenciales` | `CHAR(200)` | L12 |
| `cUsuario` | `CHAR(200)` | L13 |
| `cPassword` | `CHAR(200)` | L14 |
| `cPuerto` | `CHAR(200)` | L15 |
| `cIp` | `CHAR(200)` | L16 |
| `cPscp` | `CHAR(200)` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `?enciales` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?enciales` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consultadocumentos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultadocumentos.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y documentos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultadocumentos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdDocumento` | `CHAR(3)` | L8 |
| `cDescDocuemnto` | `CHAR(35)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cap_tipodocumento` | `bdicnweb` | no | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `documentos` | ENTIDAD | documentos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_consultadtallectesfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultadtallectesfatca.sql` |
| **LOC (1er CREATE)** | 194 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y clientes · FATCA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultadtallectesfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjercicio                   CHAR(4)
  pTipoReporte                 CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjercicio` | `CHAR(4)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `cNumCliente` | `CHAR(20)` | L28 |
| `cRazonSoc` | `CHAR(100)` | L29 |
| `cNombre1` | `CHAR(30)` | L30 |
| `cNombre2` | `CHAR(30)` | L31 |
| `cApellPaterno` | `CHAR(30)` | L32 |
| `cApellMaterno` | `CHAR(30)` | L33 |
| `cNomCalle` | `CHAR(30)` | L34 |
| `cDireccion` | `CHAR(130)` | L35 |
| `cTin` | `CHAR(20)` | L36 |
| `cRfc` | `CHAR(13)` | L37 |
| `cFechaNac` | `CHAR(10)` | L38 |
| `cCuenta` | `CHAR(20)` | L39 |
| `dMontoCta` | `DECIMAL (16,2)` | L40 |
| `mInteresPagado` | `MONEY (16,2)` | L41 |
| `cEjercicio` | `CHAR(4)` | L42 |
| `cTipo_persona` | `CHAR(2)` | L43 |
| `cNumCte` | `CHAR(20)` | L44 |
| `cApellPat` | `CHAR(26)` | L45 |
| `cApellMat` | `CHAR(26)` | L46 |
| `cNombres1` | `CHAR(26)` | L47 |
| `cNombres2` | `CHAR(26)` | L48 |
| `cRfc1` | `CHAR(13)` | L49 |
| `cDirecciones` | `CHAR(120)` | L50 |
| *…5 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_cte` | `bdilide` | ⚠️ sí | SELECT | L118 |
| `si_apoderado` | `bdinteg` | ⚠️ sí | SELECT | L157 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L109 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L97 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEjercicio = '' OR pRegistros IS NULL OR pRecuperacion IS NUL` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L170 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L175 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?dtalle` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dtalle` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consultafolioanteriorfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultafolioanteriorfatca.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y folio (anterior) · FATCA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultafolioanteriorfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjercicio                   CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjercicio` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cFolio` | `CHAR(20)` | L6 |
| `cConsecutivo` | `CHAR(4)` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00767';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `folio` | ENTIDAD | folio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?erior` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?erior` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consultageneroisrfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultageneroisrfatca.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y ro — Rol Operativo · ISR — Impuesto Sobre la Renta · FATCA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultageneroisrfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumCuenta                   CHAR(20)
  pEjercicio                   CHAR(4)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pEjercicio` | `CHAR(4)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cNumCliente` | `CHAR (20)` | L10 |
| `cNumCuenta` | `CHAR(20)` | L11 |
| `cEjercicio` | `CHAR(4)` | L12 |
| `cNomCompleto` | `CHAR(104)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |
| `cNoCliente` | `CHAR(20)` | L17 |
| `cTipoPer` | `CHAR(2)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L58 |
| `sc_retenisr` | `bdicheq` | ⚠️ sí | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L79 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consultamanttogat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultamanttogat.sql` |
| **LOC (1er CREATE)** | 153 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (anterior)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultamanttogat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `iPlazoInicio` | `INTEGER` | L20 |
| `iPlazoFin` | `INTEGER` | L21 |
| `dTasa` | `DECIMAL(9,6)` | L22 |
| `dGatNomina` | `DECIMAL(9,6)` | L23 |
| `dGatReal` | `DECIMAL(9,6)` | L24 |
| `dFechaPublicacion` | `DATE` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |
| `iRegistros` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |
| `iRowID` | `INTEGER` | L29 |
| `iPeriodo` | `CHAR(2)` | L31 |
| `iRangoMin` | `MONEY (14,2)` | L32 |
| `iRangoMax` | `MONEY (14,2)` | L33 |
| `cNumProducto` | `CHAR(4)` | L34 |
| `cProductoDesc` | `CHAR(30)` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_gat` | `bdinvers` | ⚠️ sí | SELECT | L84 |
| `sc_gat` | `bdicheq` | ⚠️ sí | SELECT | L97 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L103 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L117 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L131 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?togat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?togat` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consultaparamarchivofatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultaparamarchivofatca.sql` |
| **LOC (1er CREATE)** | 230 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, parámetro y archivo · FATCA" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultaparamarchivofatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `sCveParam` | `SMALLINT` | L12 |
| `cValorParam` | `CHAR(5)` | L13 |
| `cValor` | `CHAR(200)` | L14 |
| `cIdIdentificador` | `CHAR(3)` | L15 |
| `cDesIdentificador` | `CHAR(50)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_prm` | `bdilide` | ⚠️ sí | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L212 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L218 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_consultaparametrosfatca`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultaparametrosfatca.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y parámetros · FATCA" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultaparametrosfatca(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoParametro               INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoParametro` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCanal` | `CHAR(200)` | L11 |
| `cValorParam` | `CHAR(5)` | L12 |
| `cValor` | `CHAR(200)` | L13 |
| `cClasificacion` | `CHAR(200)` | L14 |
| `cIdClasificacion` | `INTEGER` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_prm` | `bdilide` | ⚠️ sí | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoParametro IS NULL OR pRegistros IS NULL OR pRecuperacion` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L77 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_consultaparametrosfatca_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultaparametrosfatca_totales.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y parámetros (totales) · FATCA" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultaparametrosfatca_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoParametro               INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoParametro` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_ftc_prm` | `bdilide` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoParametro IS NULL THEN` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fatca` | REG | FATCA (reporte fiscal cuentas EE.UU. — SAT/IRS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cap_consultareimpresiondoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultareimpresiondoc.sql` |
| **LOC (1er CREATE)** | 257 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y Impago — pago vencido o fallido; confirmado: n_impagos_consec" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultareimpresiondoc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pNumCte                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cNumCuenta` | `CHAR(20)` | L12 |
| `cNumCuentaExtras` | `CHAR(20)` | L13 |
| `cFechaApertura` | `DATE` | L14 |
| `cFechaAperturaExtra` | `DATE` | L15 |
| `cNumCte` | `CHAR(20)` | L16 |
| `cRazonSocial` | `CHAR(100)` | L17 |
| `iNoRegistros` | `INTEGER` | L18 |
| `iRegistros` | `INTEGER` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `cProducto` | `CHAR(1)` | L22 |
| `cSistemaCuenta` | `CHAR(2)` | L23 |
| `cNumeroCuenta` | `CHAR(20)` | L24 |
| `cCveProducto` | `CHAR(4)` | L25 |
| `cNombreProducto` | `CHAR(40)` | L26 |
| `dFechaApertura` | `DATE` | L27 |
| `cStatusCuenta` | `CHAR(60)` | L28 |
| `dFechaStatus` | `DATE` | L29 |
| `cClaveSucursal` | `CHAR(4)` | L30 |
| `cEjecutivo` | `CHAR(8)` | L31 |
| `mSaldoActual` | `MONEY(14,2)` | L32 |
| `cNumeroTarjeta` | `CHAR(20)` | L33 |
| *…41 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L186 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L168 |
| `sp_cnsif_consnumcte` | `bdinteg` | ⚠️ sí | L178 |
| `sp_cnsif_consprodcte` | `bdinteg` | ⚠️ sí | L189 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L77 | CÓDIGO_RETORNO | `LET cCodRet             = '00000';` |  |
| L156 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L182 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consnumcte";` |  |
| L193 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consprodcte";` |  |
| L197 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L210 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consnumcte";` |  |
| L226 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consprodcte";` |  |
| L230 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L238 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?resiondoc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?resiondoc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_consultareimpresiondoc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_consultareimpresiondoc_totales.sql` |
| **LOC (1er CREATE)** | 235 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y Impago — pago vencido o fallido; confirmado: n_impagos_consec (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cnsif_consnumcte`, `sp_cnsif_consprodcte` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_consultareimpresiondoc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cNumCuenta` | `CHAR(20)` | L9 |
| `cNumCuentaExtras` | `CHAR(20)` | L10 |
| `cFechaApertura` | `DATE` | L11 |
| `cFechaAperturaExtra` | `DATE` | L12 |
| `cNumCte` | `CHAR(20)` | L13 |
| `cRazonSocial` | `CHAR(100)` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `iTotalRegitros` | `INTEGER` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |
| `cProducto` | `CHAR(1)` | L20 |
| `cSistemaCuenta` | `CHAR(2)` | L21 |
| `cNumeroCuenta` | `CHAR(20)` | L22 |
| `cCveProducto` | `CHAR(4)` | L23 |
| `cNombreProducto` | `CHAR(40)` | L24 |
| `dFechaApertura` | `DATE` | L25 |
| `cStatusCuenta` | `CHAR(60)` | L26 |
| `dFechaStatus` | `DATE` | L27 |
| `cClaveSucursal` | `CHAR(4)` | L28 |
| `cEjecutivo` | `CHAR(8)` | L29 |
| `mSaldoActual` | `MONEY(14,2)` | L30 |
| *…41 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L161 |
| `sp_cnsif_consnumcte` | `bdinteg` | ⚠️ sí | L171 |
| `sp_cnsif_consprodcte` | `bdinteg` | ⚠️ sí | L180 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L75 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L156 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L175 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consnumcte";` |  |
| L184 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consprodcte";` |  |
| L187 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L197 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consnumcte";` |  |
| L211 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consprodcte";` |  |
| L214 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L220 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?resiondoc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?resiondoc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_cuentascanceladas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_cuentascanceladas.sql` |
| **LOC (1er CREATE)** | 246 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_cuentascanceladas(
  id_usuarioc                  CHAR(8)
  id_funcionc                  CHAR(10)
  pBandera                     CHAR(2)
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pFechaCancelacion            CHAR(20)
  pUsuarioCancela              CHAR(8)
  pRegistro                    INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `id_usuarioc` | `CHAR(8)` | — | — |
| `id_funcionc` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(2)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaCancelacion` | `CHAR(20)` | `cancela`=cancela | 🔵 CONVENCIÓN |
| `pUsuarioCancela` | `CHAR(8)` | `cancela`=cancela | 🔵 CONVENCIÓN |
| `pRegistro` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `v_TotalRegistros` | `INTEGER` | L18 |
| `v_NoCliente` | `CHAR(20)` | L20 |
| `v_NoCuenta` | `CHAR(20)` | L21 |
| `v_RazonSocial` | `CHAR(120)` | L22 |
| `v_FechaUltimoMov` | `DATE` | L23 |
| `v_Saldo` | `DECIMAL(9,2)` | L24 |
| `v_ClienteNotificado` | `BOOLEAN` | L25 |
| `v_Contador` | `INTEGER` | L26 |
| `v_FechaCancelacion` | `CHAR(20)` | L27 |
| `v_FolioCancelacion` | `CHAR(40)` | L28 |
| `v_UsuarioCancelacion` | `CHAR(8)` | L29 |
| `status_ant` | `CHAR(15)` | L30 |
| `cIdStatusAnt` | `CHAR(1)` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | FÓRMULA | `LET v_Contador = v_Contador + 1;` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L135 | FÓRMULA | `LET v_Contador = v_Contador + 1;` |  |
| L164 | FÓRMULA | `LET v_Contador = v_Contador + 1;` |  |
| L193 | FÓRMULA | `LET v_Contador = v_Contador + 1;` |  |
| L222 | FÓRMULA | `LET v_Contador = v_Contador + 1;` |  |
| L228 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?das` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?das` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_cuentascanceladas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_cuentascanceladas_totales.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y cuentas (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_cuentascanceladas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(2)
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pFechaCancelacion            DATE
  pUsuarioCancela              CHAR(8)
) RETURNING CHAR(5) AS codret, INTEGER as total_registros
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(2)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaCancelacion` | `DATE` | `cancela`=cancela | ✅ CÓDIGO |
| `pUsuarioCancela` | `CHAR(8)` | `cancela`=cancela | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `v_TotalRegistros` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente_cancela_notifica` | `bdicheq` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L27 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cancela` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?das_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?das_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_generaportadactamec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_generaportadactamec.sql` |
| **LOC (1er CREATE)** | 161 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_generaportadactamec(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCta                      CHAR(20)
) RETURNING CHAR(5) 	AS      codret,                 --Codigo de retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCodRetSp` | `CHAR(3)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `cCodProducto` | `CHAR(4)` | L29 |
| `cNomProducto` | `CHAR(40)` | L30 |
| `cRazonSoc` | `CHAR(254)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cNumCuenta` | `CHAR(20)` | L33 |
| `cClabe` | `CHAR(18)` | L34 |
| `cClaveRegimen` | `CHAR(1)` | L35 |
| `cRegimenFirmas` | `CHAR(20)` | L36 |
| `cEspeciManejo` | `CHAR(20)` | L37 |
| `cRfc` | `CHAR(13)` | L38 |
| `dFechaOperacion` | `DATE` | L39 |
| `cNombreFirmante` | `CHAR(104)` | L40 |
| `cTipoFirma` | `CHAR(1)` | L41 |
| `cSucursal` | `CHAR(4)` | L42 |
| `cNomsuc` | `CHAR(40)` | L43 |
| `cReca` | `CHAR(60)` | L44 |
| `cHoraOperacion` | `CHAR(10)` | L45 |
| `cFolioOperacion` | `CHAR(20)` | L46 |
| `cCodigoEmpresa` | `CHAR(3)` | L47 |
| `cCuentaLigada` | `CHAR(20)` | L48 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L142 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L97 |
| `sp_ctamec_generarptportada2` | `bdicheq` | ⚠️ sí | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00335';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00303';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00332';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?tad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?amec` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tad`, `?amec` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_genrep_ctascanceladas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_genrep_ctascanceladas.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_genrep_ctascanceladas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(150)
  pNumCte                      CHAR(20)
  pNumCta                      CHAR(20)
  pFechaCancelacion            CHAR(20)
  pUsuarioCancelacion          CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |
| `pFechaCancelacion` | `CHAR(20)` | `cancela`=cancela | 🔵 CONVENCIÓN |
| `pUsuarioCancelacion` | `CHAR(20)` | `cancela`=cancela | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCmd1` | `CHAR(5000)` | L8 |
| `cSql` | `CHAR(5000)` | L9 |
| `cRutaInformix` | `CHAR(100)` | L10 |
| `cRutaGral` | `CHAR(150)` | L11 |
| `cNombreReporte` | `CHAR(150)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L54 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" WHEN cliente_notifica.status_ant = 6 THEN 'CONCENTRADA' WHEN clien` |  |
| L58 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" CASE WHEN cliente_notificado = 'f' THEN 'NO' ELSE 'SI' END, TO_CHA` |  |
| L79 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?das` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?das` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cap_genrep_ctasnocanceladas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cap_genrep_ctasnocanceladas.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cap_genrep_ctasnocanceladas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(150)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(5000)` | L7 |
| `cSql` | `CHAR(5000)` | L8 |
| `cRutaInformix` | `CHAR(100)` | L9 |
| `cRutaGral` | `CHAR(150)` | L10 |
| `cNombreReporte` | `CHAR(150)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L53 |
| `sw_ctasnocanceladas` | `bdicnweb` | no | SELECT | L83 |
| `sw_ctasnocanceladas` | `bdicnweb` | no | DELETE | L83 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT no_cliente,nombre_cliente,TO_CHAR(no_cuenta),TO_CHAR(fec_ul` |  |
| L59 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?das` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?no`, `?das` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_carga_archivo_dotacion_masiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_carga_archivo_dotacion_masiva.sql` |
| **LOC (1er CREATE)** | 709 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga archivo y dotación de efectivo (masiva)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_carga_archivo_dotacion_masiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaArchivo                 CHAR(40)
  pNombrearchivo               CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaArchivo` | `CHAR(40)` | `archivo`=archivo | ✅ CÓDIGO |
| `pNombrearchivo` | `CHAR(50)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsqlerr` | `INTEGER` | L10 |
| `cCodRet` | `CHAR(5)` | L11 |
| `cSQL` | `CHAR(4000)` | L12 |
| `cSQL2` | `CHAR(4000)` | L13 |
| `iTamReg` | `INTEGER` | L14 |
| `banderaRegOK` | `SMALLINT` | L15 |
| `bandera` | `SMALLINT` | L16 |
| `cEstatusValidaciones` | `CHAR(3)` | L17 |
| `cTotRegistrosOk` | `INTEGER` | L18 |
| `cTotRegistrosNok` | `INTEGER` | L19 |
| `cIdRegistro` | `INTEGER` | L21 |
| `cMinReg` | `INTEGER` | L22 |
| `cCadena` | `CHAR(8000)` | L23 |
| `cCampo` | `CHAR(50)` | L24 |
| `iCampo` | `INTEGER` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |
| `cErrores` | `CHAR(8000)` | L28 |
| `cCamposOk` | `CHAR(8000)` | L29 |
| `fechaSolicitud` | `DATE` | L31 |
| `fechaAplicacion` | `DATE` | L32 |
| `c_ccSucursal` | `CHAR(4)` | L33 |
| `nombreSucursal` | `CHAR(40)` | L34 |
| `plaza` | `CHAR(50)` | L35 |
| `tipoOperacion` | `CHAR(20)` | L36 |
| `billete1000` | `SMALLINT` | L37 |
| *…48 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_arch_dot_suc` | `bdicnweb` | no | SELECT | L216 |
| `tmp_arch_dot_suc` | `bdicnweb` | no | DELETE | L216 |
| `tmp_arch_dot_suc_err` | `bdicnweb` | no | SELECT | L217 |
| `tmp_arch_dot_suc_err` | `bdicnweb` | no | DELETE | L217 |
| `tmp_arch_dot_suc_det` | `bdicnweb` | no | SELECT | L218 |
| `tmp_arch_dot_suc_det` | `bdicnweb` | no | DELETE | L218 |
| `tmp_arch_dot_suc` | `bdicnweb` | no | INSERT | L227 |
| `tmp_arch_dot_suc_err` | `bdicnweb` | no | INSERT | L256 |
| `tmp_arch_dot_suc_det` | `bdicnweb` | no | INSERT | L504 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L210 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L195 | CÓDIGO_RETORNO | `LET cCodRet='00002';` |  |
| L205 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L222 | FÓRMULA | `LET cSQL = "sed 's/^/"\|\|TRIM(pUsuario)\|\|","\|\|dtcurr\|\|","\|\|TRIM(pNombrearchivo)\|\|",/' "\|` |  |
| L227 | FÓRMULA | `LET cSQL = "/usr/bin/echo 'SET ISOLATION TO DIRTY READ; LOAD FROM "\|\|TRIM(pRutaArchivo)\|\|'N'\|\|` |  |
| L228 | FÓRMULA | `LET cSQL = TRIM(cSQL)\|\|"usuario_carga,fecha_carga,nombre_archivo,cadena)' \| /ifxsif01/bin/dbacces` |  |
| L253 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L258 | FÓRMULA | `LET cContErrores=cContErrores+1;` |  |
| L259 | FÓRMULA | `LET cErr_enc = cErr_enc+1;` |  |
| L263 | FÓRMULA | `LET cCadena= SUBSTR(TRIM(cCadena), 0, (iTamReg - 1));` |  |
| L277 | FÓRMULA | `LET cErrores = TRIM(cErrores)\|\|'; Registro ['\|\|cIdRegistro+1\|\|','\|\|iCampo\|\|'] '\|\|TRIM(no` |  |
| L280 | FÓRMULA | `LET cContErrores=cContErrores+1;` |  |
| L284 | FÓRMULA | `LET cErrores = TRIM(cErrores)\|\|'; Registro ['\|\|cIdRegistro+1\|\|','\|\|iCampo\|\|'] '\|\|TRIM(no` |  |
| L287 | FÓRMULA | `LET cContErrores=cContErrores+1;` |  |
| L291 | FÓRMULA | `LET cErrores = TRIM(cErrores)\|\|'; Registro ['\|\|cIdRegistro+1\|\|','\|\|iCampo\|\|'] '\|\|TRIM(no` |  |
| L294 | FÓRMULA | `LET cContErrores=cContErrores+1;` |  |
| L297 | FÓRMULA | `LET iCampo=iCampo+1;` |  |
| L302 | FÓRMULA | `LET fechaSolicitud=TO_DATE(TRIM(cCampo), '%Y-%m-%d');` |  |
| L305 | FÓRMULA | `LET fechaAplicacion=TO_DATE(TRIM(cCampo), '%Y-%m-%d');` |  |
| L311 | EXCEPCIÓN | `RAISE EXCEPTION -746,0;` |  |
| L316 | EXCEPCIÓN | `RAISE EXCEPTION -746,0;` |  |
| L322 | EXCEPCIÓN | `RAISE EXCEPTION -746,0;` |  |
| L328 | EXCEPCIÓN | `RAISE EXCEPTION -746,0;` |  |
| L335 | EXCEPCIÓN | `RAISE EXCEPTION -1213,0;` |  |
| L342 | EXCEPCIÓN | `RAISE EXCEPTION -1213,0;` |  |
| L349 | EXCEPCIÓN | `RAISE EXCEPTION -1213,0;` |  |
| L356 | EXCEPCIÓN | `RAISE EXCEPTION -1213,0;` |  |
| L363 | EXCEPCIÓN | `RAISE EXCEPTION -1213,0;` |  |
| L370 | EXCEPCIÓN | `RAISE EXCEPTION -1213,0;` |  |
| L377 | EXCEPCIÓN | `RAISE EXCEPTION -1213,0;` |  |
| | *…37 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_carga_masiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_carga_masiva.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga (masiva)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtienesiguientelote` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_carga_masiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivo                     CHAR(150)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivo` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L5 |
| `pArchivoTemp` | `CHAR(150)` | L6 |
| `cCmd1` | `CHAR(500)` | L7 |
| `cCmd2` | `CHAR(500)` | L8 |
| `cCmd3` | `CHAR(500)` | L9 |
| `cCmd4` | `CHAR(500)` | L10 |
| `cCodRet` | `CHAR(5)` | L11 |
| `iLote` | `INT` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `cCampos` | `CHAR(1024)` | L14 |
| `cTablaDst` | `CHAR(150)` | L15 |
| `cBaseDatos` | `CHAR(50)` | L16 |
| `iSqlErr` | `INT` | L17 |
| `cUsrBin` | `CHAR(15)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L51 |
| `sw_tr_info_tablas` | `bdicnweb` | no | SELECT | L81 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtienesiguientelote` | `bdicnweb` | no | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | FÓRMULA | `LET cCmd3 = TRIM(cUsrBin)\|\|"rm -rf "\|\|TRIM(pArchivo);` |  |
| L85 | FÓRMULA | `LET cCmd2 = TRIM(cCmd1)\|\|TRIM(cCampos)\|\|")' \| /ifxsif01/bin/dbaccess bdicnweb > /dev/null 2>&1"` |  |
| L89 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf "\|\|TRIM(pArchivoTemp);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masiva` | MODIF | masiva | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cargacod41_ccep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargacod41_ccep.sql` |
| **LOC (1er CREATE)** | 365 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga código y CCE — Cámara de Compensación Electrónica" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `cal_habil_ant` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargacod41_ccep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pnombrearchivo               CHAR(30)
  pRutaArchivo                 CHAR(60)
  pDireccionMac                CHAR(15)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pnombrearchivo` | `CHAR(30)` | — | — |
| `pRutaArchivo` | `CHAR(60)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(3)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cRenglon` | `CHAR(400)` | L12 |
| `cNumSecuencia` | `CHAR(7)` | L13 |
| `cCodOperacion` | `CHAR(2)` | L14 |
| `cFechatrasnfer` | `CHAR(8)` | L15 |
| `cBancoCedente` | `CHAR(3)` | L16 |
| `cBancoLibrado` | `CHAR(3)` | L17 |
| `cImporte` | `CHAR(15)` | L18 |
| `cLoteEntrada` | `CHAR(7)` | L19 |
| `cSecEntrada` | `CHAR(4)` | L20 |
| `cLoteSAlida` | `CHAR(7)` | L21 |
| `cSecSalida` | `CHAR(4)` | L22 |
| `cTransaccion` | `CHAR(2)` | L23 |
| `cChqCompensacion` | `CHAR(3)` | L24 |
| `cCuentaReferencia` | `CHAR(13)` | L25 |
| `cNumCheque` | `CHAR(10)` | L26 |
| `cChqDigVerInter` | `CHAR(1)` | L27 |
| `cChqDigVerPre` | `CHAR(1)` | L28 |
| `cChqCodSeguridad` | `CHAR(3)` | L29 |
| `cUbicFis` | `CHAR(8)` | L30 |
| `cTruncado` | `CHAR(1)` | L31 |
| *…39 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ccep_generacioncod41_tmp` | `bdicnweb` | no | SELECT | L176 |
| `ccep_generacioncod41_tmp` | `bdicnweb` | no | DELETE | L176 |
| `ccep_procesacod41detalle_tmp` | `bdicnweb` | no | SELECT | L178 |
| `ccep_procesacod41detalle_tmp` | `bdicnweb` | no | DELETE | L178 |
| `ccep_generacioncod41_tmp` | `bdicnweb` | no | INSERT | L182 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L193 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L210 |
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L252 |
| `si_coddevcam` | `bdinteg` | ⚠️ sí | SELECT | L258 |
| `cce_detalle` | `bditef` | ⚠️ sí | SELECT | L292 |
| `cce_cheques_dev` | `bditef` | ⚠️ sí | SELECT | L313 |
| `ccep_procesacod41detalle_tmp` | `bdicnweb` | no | INSERT | L331 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L164 |
| `cal_habil_ant` | `bditef` | ⚠️ sí | L196 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L187 | FÓRMULA | `Let cSQL = TRIM(cPathdbaccess)\|\|'dbaccess bdicnweb ' \|\|trim(pRutaArchivo)\|\| 'Temporal.sql'; --` |  |
| L248 | FÓRMULA | `LET dImporte = dImporte + dImporte2;` | 🔴 MONEY/aritmética financiera |
| L249 | FÓRMULA | `LET importeTotal = importeTotal + dImporte;` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?41_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?41_`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cargacuentasbc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargacuentasbc.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga cuentas" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargacuentasbc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivo                     CHAR(150)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivo` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L4 |
| `pArchivoTemp` | `CHAR(150)` | L5 |
| `pArchivoTemp2` | `CHAR(150)` | L6 |
| `cCmd1` | `CHAR(2000)` | L7 |
| `cCmd2` | `CHAR(2000)` | L8 |
| `cCmd3` | `CHAR(2000)` | L9 |
| `cCmd4` | `CHAR(2000)` | L10 |
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `cCampos` | `CHAR(1024)` | L13 |
| `cTablaDst` | `CHAR(150)` | L14 |
| `cBaseDatos` | `CHAR(50)` | L15 |
| `iSqlErr` | `INT` | L16 |
| `cUsrBin` | `CHAR(15)` | L17 |
| `bEncontrado` | `BOOLEAN` | L18 |
| `iPosicion` | `INT` | L19 |
| `cAux` | `CHAR(150)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L59 |
| `iposicion` | `bdicnweb` | no | SELECT | L67 |
| `sw_tr_info_tablas` | `bdicnweb` | no | SELECT | L101 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | FÓRMULA | `LET iPosicion = CHARINDEX('/',TRIM(cAux));` |  |
| L67 | FÓRMULA | `LET cAux = REPLACE(SUBSTRING(TRIM(cAux) FROM iPosicion FOR 1),'/','_') \|\| SUBSTRING(TRIM(cAux) FRO` |  |
| L71 | FÓRMULA | `LET cAux = SUBSTRING(TRIM(cAux) FROM 2 FOR LENGTH(TRIM(cAux))-1);` |  |
| L75 | FÓRMULA | `LET cAux = SUBSTRING(TRIM(cAux) FROM LENGTH(TRIM(cAux))-7 FOR 4);` |  |
| L105 | FÓRMULA | `LET cCmd2 = TRIM(cCmd1)\|\|TRIM(cCampos)\|\|")' \| /ifxsif01/bin/dbaccess bdicnweb > /dev/null 2>&1"` |  |
| L109 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf "\|\|TRIM(pArchivoTemp)\|\|" "\|\|TRIM(pArchivoTemp2)\|\|" "\|\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?bc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cargamanualarchivosdmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargamanualarchivosdmi.sql` |
| **LOC (1er CREATE)** | 225 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 78 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga manual archivos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `sp_domi_cargaarchivomanualproveedor` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargamanualarchivosdmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArch                  CHAR(20)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArch` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cDescCodRet` | `CHAR(95)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCtaAbono` | `CHAR(20)` | L14 |
| `iTotalAltas` | `INTEGER` | L15 |
| `mTotalImporteAltas` | `MONEY(18,2)` | L16 |
| `iTotalBajas` | `INTEGER` | L17 |
| `mTotalImporteBajas` | `MONEY(18,2)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |
| `bInTransaction` | `BOOLEAN` | L20 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L56 |
| `sp_domi_cargaarchivomanualproveedor` | `bdidomi` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdidomi:sp_domi_cargaarchivoma` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00615';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00616';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00617';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00618';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00619';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00620';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00482';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00482';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00621';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00622';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00482';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00482';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00623';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00624';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00625';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00626';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00627';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00628';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00629';` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00630';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00631';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00632';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00633';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00634';` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00635';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00636';` |  |
| | *…41 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cargamanual` | ACCION | carga manual | 🔵 CONVENCIÓN | nombre_sp |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?dmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cargarchextemporaneosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargarchextemporaneosccl.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga OS — Originación de Solicitudes y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (temporal)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargarchextemporaneosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOpcion                  INTEGER
  pNombreArch                  CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOpcion` | `INTEGER` | — | — |
| `pNombreArch` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cArchivoOrigen` | `CHAR(3)` | L13 |
| `cPrefijoArchivo` | `CHAR(30)` | L14 |
| `cDiasDesfase` | `CHAR(2)` | L15 |
| `cRepAIX` | `CHAR(50)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `bditarjeta` | `bdicnweb` | no | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOpcion IS NULL  THEN` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_consultaextempor` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L74 | VALIDACIÓN_NULL | `IF pTipoOpcion IN (2,3) AND (pNombreArch IS NULL OR pNombreArch = '')  AND iCodRetSp = 1  THEN` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00537';` |  |
| L84 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?rche` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `temp` | MODIF | temporal | 🔵 CONVENCIÓN | nombre_sp |
| `?orane` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rche`, `?orane` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cargarchivobinesemisor`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargarchivobinesemisor.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga CHI — formato/protocolo de consulta al Buró de Crédito y emisor · INE — Instituto Nacional Electoral" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargarchivobinesemisor(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaArchivo                 CHAR(100)
  pNombreArchivo               CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaArchivo` | `CHAR(100)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |
| `pNombreArchivo` | `CHAR(100)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cIsamErr` | `INTEGER` | L7 |
| `cDescErr` | `CHAR(50)` | L8 |
| `bInTransaction` | `BOOLEAN` | L9 |
| `cSQL` | `CHAR(500)` | L10 |
| `ven_transacc` | `SMALLINT` | L11 |
| `cCampos` | `CHAR(1024)` | L13 |
| `cTablaDst` | `CHAR(150)` | L14 |
| `cBaseDatos` | `CHAR(50)` | L15 |
| `cCmd1` | `CHAR(2000)` | L16 |
| `cCmd2` | `CHAR(2000)` | L17 |
| `cUsrBin` | `CHAR(15)` | L18 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L67 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | FÓRMULA | `LET cCmd2 = TRIM(cCmd1)\|\|TRIM(cCampos)\|\|")"\|\|'"'\|\|" \| /informix/bin/dbaccess bdicnweb > /de` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?vob` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ine` | REG | INE — Instituto Nacional Electoral (validación de identidad  | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emisor` | ENTIDAD | emisor | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?vob`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cargenmanualarchivostef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargenmanualarchivostef.sql` |
| **LOC (1er CREATE)** | 561 |
| **Callgraph** | ✅ fan_in=0 / fan_out=38 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera archivos y TEF — transferencia electrónica de fondos (manual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 5 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_tef_presentador_r`, `sp_tef_receptor_r` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargenmanualarchivostef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(20)
  pIndicador                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | — | — |
| `pIndicador` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `cNomArchivo` | `CHAR(20)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `bTransacInteract` | `BOOLEAN` | L10 |
| `codigoDocto` | `CHAR(10)` | L11 |
| `bError` | `BOOLEAN` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `11` | `bdicnweb` | no | SELECT | L61 |
| `14` | `bdicnweb` | no | SELECT | L355 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_tef_presentador_r` | `bditef` | ⚠️ sí | L66 |
| `sp_tef_receptor_r` | `bditef` | ⚠️ sí | L197 |
| `sp_tef_presentador_g` | `bditef` | ⚠️ sí | L368 |
| `sp_tef_receptor_g` | `bditef` | ⚠️ sí | L453 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L175 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L201 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃÂN DEL SP bditef:sp_tef_receptor_r';` |  |
| L255 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L331 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L372 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃÂN DEL SP bditef:sp_tef_presentador_g'` |  |
| L400 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L430 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L439 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L457 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃÂN DEL SP bditef:sp_tef_receptor_g';` |  |
| L485 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L517 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L526 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L545 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_car` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `manual` | MODIF | manual | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_car` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cargo_ref`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargo_ref.sql` |
| **LOC (1er CREATE)** | 154 |
| **Callgraph** | ✅ fan_in=38 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cargo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `cargo_ref` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargo_ref(
  pUsuario                     char(10)
  pIdFuncion                   char(10)
  pTransacc                    CHAR(4)
  pCuenta                      CHAR(20)
  pCheque                      INTEGER
  pMto_tot                     MONEY(14,2)
  pReferencia                  CHAR(40)
) RETURNING CHAR(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(10)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pTransacc` | `CHAR(4)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pCheque` | `INTEGER` | — | — |
| `pMto_tot` | `MONEY(14,2)` | — | — |
| `pReferencia` | `CHAR(40)` | `ref`=referencia | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L13 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cSucursal` | `CHAR(4)` | L16 |
| `cUsuario` | `CHAR(8)` | L17 |
| `cTransuc` | `CHAR(4)` | L18 |
| `cFolio_suc` | `CHAR(16)` | L19 |
| `mMto_firme` | `MONEY(14,2)` | L20 |
| `mMto_sbc` | `MONEY(14,2)` | L21 |
| `mMto_rem` | `MONEY(14,2)` | L22 |
| `sDias_ret` | `SMALLINT` | L23 |
| `cDivisa` | `CHAR(2)` | L24 |
| `cNum_tarjeta` | `CHAR(16)` | L25 |
| `cUsuautoriza` | `CHAR(8)` | L26 |
| `dHora` | `DATETIME HOUR TO SECOND` | L28 |
| `cHora` | `CHAR(6)` | L29 |
| `vTranret` | `CHAR(4)` | L31 |
| `vFechoy` | `DATE` | L32 |
| `vSdodisp` | `MONEY(14,2)` | L33 |
| `vMontoret` | `MONEY(14,2)` | L34 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L98 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L103 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `cargo_ref` | `bdicheq` | ⚠️ sí | L109 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `ref` | AMBIGUO | referencia | 🟡 INFERIDO | nombre_sp |

---

## `sp_cargo_ref_masivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargo_ref_masivo.sql` |
| **LOC (1er CREATE)** | 227 |
| **Callgraph** | ✅ fan_in=0 / fan_out=53 |
| **Deps concatenadas** | 145 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cargo (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cargo_ref`, `sp_totalesretirocap`, `sp_registra_evento` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargo_ref_masivo(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             char(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `char(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iExiste` | `INT` | L7 |
| `cTransaccion` | `CHAR(4)` | L8 |
| `iDoctoCheque` | `INT` | L9 |
| `mImporte` | `money(14,2)` | L10 |
| `cReferencia` | `CHAR(40)` | L11 |
| `iRegistro` | `INT` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |
| `cFolio` | `CHAR(16)` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `cResultado` | `CHAR(15)` | L16 |
| `iRegistrosExito` | `INT` | L17 |
| `iRegistrosError` | `INT` | L18 |
| `dFechaProceso` | `DATETIME YEAR TO FRACTION(3)` | L19 |
| `cMotivoRechazo` | `char(100)` | L20 |
| `iRegProc` | `int` | L21 |
| `iTransaccionPrev` | `int` | L22 |
| `iTransaccion` | `int` | L23 |
| `cFechaCargaLote` | `date` | L24 |
| `iTotalRegsLote` | `int` | L25 |
| `mMontoLote` | `money(14,2)` | L26 |
| `iRegsAceptadosLote` | `int` | L27 |
| `iRegsRechazoLote` | `int` | L28 |
| `cArchivo` | `char(150)` | L29 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_retiro` | `bdicnweb` | no | SELECT | L84 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L130 |
| `sw_tr_cargamasiva_retiro_hist` | `bdicnweb` | no | INSERT | L145 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cargo_ref` | `bdicnweb` | no | L111 |
| `sp_totalesretirocap` | `bdicnweb` | no | L155 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L186 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00002';` |  |
| L117 | FÓRMULA | `LET iRegistrosError = iRegistrosError + 1;` |  |
| L123 | FÓRMULA | `LET iRegistrosExito = iRegistrosExito + 1;` |  |
| L149 | FÓRMULA | `LET iRegProc = iRegProc + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `ref` | AMBIGUO | referencia | 🟡 INFERIDO | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cargomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargomasivocre.sql` |
| **LOC (1er CREATE)** | 236 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cargo y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_obtenerconceptocargomanuales`, `sp_grabarcargosmasivos`, `sp_consaldodisp` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pPlantilla                   CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vUsuario` | `CHAR(10)` | L5 |
| `cFolioGrupo` | `CHAR(16)` | L6 |
| `dFechaHoy` | `DATE` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cCodRetSpSal` | `CHAR(6)` | L11 |
| `cCodPago` | `CHAR(2)` | L12 |
| `cDescPago` | `CHAR(50)` | L13 |
| `cCodFuncion` | `CHAR(3)` | L14 |
| `cConcepto` | `CHAR(50)` | L15 |
| `cFolioGen` | `CHAR(16)` | L16 |
| `dFechaProc` | `DATETIME YEAR TO FRACTION` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `iNoRegs` | `INTEGER` | L19 |
| `cCredito` | `CHAR(16)` | L21 |
| `mImportePago` | `MONEY(14,2)` | L22 |
| `cTransaccion` | `CHAR(4)` | L23 |
| `cResultado` | `CHAR(15)` | L24 |
| `iIdRegistro` | `INTEGER` | L25 |
| `cStatus` | `CHAR(1)` | L26 |
| `iInTrans` | `INTEGER` | L27 |
| `iRegRechazados` | `INTEGER` | L28 |
| `mSaldoCuenta` | `MONEY(14,2)` | L29 |
| `iTotalRegs` | `INTEGER` | L30 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_cargo` | `bdicnweb` | no | SELECT | L87 |
| `sw_tr_cargamasiva_cargo_hist` | `bdicnweb` | no | INSERT | L163 |
| `sw_tr_cargamasiva_cargo` | `bdicnweb` | no | DELETE | L171 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L192 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtenerconceptocargomanuales` | `bdicred` | ⚠️ sí | L118 |
| `sp_grabarcargosmasivos` | `bdicred` | ⚠️ sí | L122 |
| `sp_consaldodisp` | `bdicnweb` | no | L136 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L148 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_cargotransfersoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cargotransfersoc.sql` |
| **LOC (1er CREATE)** | 174 |
| **Callgraph** | ✅ fan_in=4 / fan_out=2 |
| **Propósito inferido** | "cargo, transferencia y Sistema Operativo Central" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `cargo_ref`, `cargo_ref_cel` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cargotransfersoc(
  pSucursal                    CHAR(4)
  pUsuario                     CHAR(8)
  pTransacc                    CHAR(4)
  pCuenta                      CHAR(20)
  pCheque                      INTEGER
  pMonto                       DECIMAL(14,2)
  pReferencia                  CHAR(40)
  pTarjeta                     CHAR(16)
) RETURNING CHAR(5), CHAR(40)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSucursal` | `CHAR(4)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pTransacc` | `CHAR(4)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pCheque` | `INTEGER` | — | — |
| `pMonto` | `DECIMAL(14,2)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pTarjeta` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L11 |
| `cCodRet2` | `CHAR(5)` | L12 |
| `cCodRet3` | `CHAR(40)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iSamErr` | `INTEGER` | L15 |
| `cDesErr` | `CHAR(50)` | L16 |
| `cProceso` | `CHAR(13)` | L17 |
| `cStatusTar` | `CHAR(1)` | L18 |
| `cHora` | `CHAR(15)` | L19 |
| `cFolio` | `CHAR(16)` | L20 |
| `cCodRet4` | `CHAR(5)` | L21 |
| `cTrxApli` | `CHAR(4)` | L22 |
| `cFechaApli` | `DATE` | L23 |
| `mMtoDisp` | `DECIMAL(14,2)` | L24 |
| `mMtoApli` | `DECIMAL(14,2)` | L25 |
| `cCodRet5` | `CHAR(5)` | L26 |
| `cTrxCom` | `CHAR(4)` | L27 |
| `cFechaCom` | `DATE` | L28 |
| `mSdoCom` | `DECIMAL(14,2)` | L29 |
| `mMtoCom` | `DECIMAL(14,2)` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_trxtrfcargosoc` | `bdicheq` | ⚠️ sí | SELECT | L87 |
| `sc_tarjeta` | `bdicheq` | ⚠️ sí | SELECT | L100 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `cargo_ref` | `bdicheq` | ⚠️ sí | L135 |
| `cargo_ref_cel` | `bdicheq` | ⚠️ sí | L149 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | VALIDACIÓN_NULL | `IF ( pMonto     is null OR pMonto <= 0.00 ) OR` |  |
| L90 | VALIDACIÓN_NULL | `IF cProceso is null OR cProceso = '' THEN` |  |
| L97 | VALIDACIÓN_NULL | `IF pCuenta is null OR pCuenta = '' THEN` |  |
| L104 | VALIDACIÓN_NULL | `IF pCuenta is null OR cStatusTar is null OR cStatusTar <> 'A' THEN` |  |
| L112 | VALIDACIÓN_NULL | `IF pTarjeta is null OR pTarjeta = '' THEN` |  |
| L124 | VALIDACIÓN_NULL | `IF pTarjeta is null THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | 🔵 CONVENCIÓN | nombre_sp |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cat_constransacc_auditar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cat_constransacc_auditar.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, código de transacción, auditoría y Tarjeta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_constransacc_auditar` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cat_constransacc_auditar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pCodigo                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cCodigo` | `CHAR(10)` | L13 |
| `cDescripcion` | `CHAR(40)` | L14 |
| `cNumTran` | `CHAR(300)` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_constransacc_auditar` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL THEN` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_constransacc_auditar";` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `transacc` | ENTIDAD | código de transacción | 🔵 CONVENCIÓN | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cat_puntoscompromisosrespuesta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cat_puntoscompromisosrespuesta.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, puntos, compromiso de pago — promesa formal de liquidación y respuesta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cat_puntoscompromisosrespuesta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCveRespuesta` | `CHAR(5)` | L8 |
| `cDescRespuesta` | `CHAR(100)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cat_pcompromisos_respuesta` | `bdicnweb` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `compromiso` | ENTIDAD | compromiso de pago — promesa formal de liquidación (sp_consu | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `respuesta` | ENTIDAD | respuesta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cat_status_combo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cat_status_combo.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 75 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, estatus y combo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cat_status_combo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS cdret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |
| `cId_status` | `CHAR(2)` | L11 |
| `cDesc_status` | `CHAR(30)` | L12 |
| `cCve_Status` | `CHAR(2)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cat_status_combo_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cat_status_combo_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 74 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, estatus y combo (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cat_status_combo_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS cdret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotsles` | `INTEGER` | L7 |
| `cId_status` | `CHAR(2)` | L9 |
| `cDesc_status` | `CHAR(30)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cat_statususuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cat_statususuario.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 73 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, estatus y usuario" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cat_statususuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS cdret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iRecuperacion` | `INTEGER` | L8 |
| `cId_status` | `CHAR(2)` | L10 |
| `cDesc_status` | `CHAR(30)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_usuarios` | `bdinteg` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogoactividadsocialctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoactividadsocialctemoral.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 75 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza catálogo, identificador, Sistema Operativo Central y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaactividadsocial` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoactividadsocialctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(3)
  pTipoOrden                   SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pCodigo` | `CHAR(3)` | — | — |
| `pTipoOrden` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cMensajeErrSp` | `CHAR(80)` | L11 |
| `cCodActividadGiro` | `CHAR(3)` | L12 |
| `cDescActividadGiro` | `CHAR(40)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `sp_consultaactividadsocial` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOrden IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultaactividadsocial';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?iv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?ial` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iv`, `?ad`, `?ial` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoaniocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoaniocaja.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, año y caja" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoaniocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSaldo                   CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSaldo` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `dAnioMinMax` | `DATE` | L7 |
| `dAnioMin` | `DATE` | L8 |
| `dFechaMin` | `DATE` | L9 |
| `dFechaMax` | `DATE` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iBandera` | `INTEGER` | L12 |
| `iTotalAnios` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | FÓRMULA | `LET dAnioMin = dFechaMin - 1 UNITS YEAR;` |  |
| L67 | FÓRMULA | `LET iTotalAnios = YEAR(dFechaMax) - YEAR(dFechaMin);` |  |
| L72 | FÓRMULA | `LET iBandera = iBandera + 1;` |  |
| L76 | FÓRMULA | `LET iBandera = iBandera + 1;` |  |
| L78 | FÓRMULA | `LET dAnioMin = TO_DATE(1\|\|'/'\|\|1\|\|'/'\|\|YEAR(dAnioMin + 1 UNITS YEAR),'%d/%m/%Y');` |  |
| L79 | FÓRMULA | `LET iBandera = iBandera + 1;` |  |
| L84 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `anio` | ENTIDAD | año | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogoarchivotef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoarchivotef.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, archivo y TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditef` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoarchivotef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRuta                        CHAR(100)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRuta` | `CHAR(100)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cNomArchivo` | `CHAR(50)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `bInTrans` | `BOOLEAN` | L10 |
| `cFechaArchivoOUT` | `CHAR(10)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tef_busca_archivos` | `bditef` | ⚠️ sí | SELECT | L90 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |
| `bditef` | `bdicnweb` | no | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRuta = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THE` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L68 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃ?N DEL SP bditef:sp_buscararchivos_tef';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L92 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00560';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_catalogocajagen`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogocajagen.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 80 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera catálogo y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_cajagen` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogocajagen(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cClave` | `CHAR(4)` | L11 |
| `cDescripcion` | `CHAR(40)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_consulta_cajagen` | `bdisuc` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_consulta_cajagen';` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |

---

## `sp_catalogocajageneral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogocajageneral.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y caja (general)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogocajageneral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdProvCaja` | `CHAR(4)` | L8 |
| `cDescCaja` | `CHAR(30)` | L9 |
| `cPlazaCaja` | `CHAR(3)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L67 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `general` | MODIF | general | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogocalle`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogocalle.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y calle" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogocalle(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConsulta                    CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConsulta` | `CHAR(30)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `iRegistros` | `INTEGER` | L9 |
| `iNumeroCalle` | `INTEGER` | L10 |
| `cNombreCalle` | `CHAR(30)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pConsulta = '' OR pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L58 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L71 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `calle` | ENTIDAD | calle (domicilio) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogocalle_2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogocalle_2.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y calle" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogocalle_2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConsulta                    CHAR(30)
  pCliente                     CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConsulta` | `CHAR(30)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `iRegistros` | `INTEGER` | L9 |
| `iNumeroCalle` | `INTEGER` | L10 |
| `cNombreCalle` | `CHAR(30)` | L11 |
| `iTotalReg` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L56 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pConsulta = '' OR pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L44 | FÓRMULA | `LET iTotalReg = 100 - pRecuperacion;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L73 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L98 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `calle` | ENTIDAD | calle (domicilio) | 🔵 CONVENCIÓN | nombre_sp |
| `?_2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogociudad`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogociudad.sql` |
| **LOC (1er CREATE)** | 131 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y ciudad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogociudad(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pEstado                      CHAR(2)
  pTipoConsulta                SMALLINT
  pConsulta                    CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEstado` | `CHAR(2)` | — | — |
| `pTipoConsulta` | `SMALLINT` | — | — |
| `pConsulta` | `CHAR(30)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdCiudad` | `CHAR(5)` | L9 |
| `cNombreCiudad` | `CHAR(60)` | L10 |
| `iIdCiudadCoppel` | `SMALLINT` | L11 |
| `iExiste` | `INTEGER` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ciudades` | `bdinteg` | ⚠️ sí | SELECT | L58 |
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L93 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pIdUsuario = '' OR pIdFuncion = '' OR pEstado = '' OR pTipoConsulta IS NULL OR pRegistros IS NULL` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L75 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L105 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ciudad` | ENTIDAD | ciudad | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogocodbancotef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogocodbancotef.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=33 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, código, banco y TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_tef_obtcodbanco` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogocodbancotef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoCuenta                  CHAR(2)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoCuenta` | `CHAR(2)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cCodBanco` | `CHAR(3)` | L10 |
| `cDescBanco` | `CHAR(40)` | L11 |
| `iTipo` | `INTEGER` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_tef_obtcodbanco` | `bditef` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditef:sp_tef_obtcodbanco';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00450';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L70 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `banco` | ENTIDAD | banco | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogocoloniacp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogocoloniacp.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, colonia — colonia postal para validación de domicilio y código postal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogocoloniacp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEstado                      CHAR(2)
  pNumCiudad                   CHAR(5)
  pNumColonia                  INTEGER
  pNomZona                     CHAR(32)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEstado` | `CHAR(2)` | — | — |
| `pNumCiudad` | `CHAR(5)` | — | — |
| `pNumColonia` | `INTEGER` | `colonia`=colonia — colonia postal para validación de domicilio (sp_consultacoloniascp fan_in=281) | ✅ CÓDIGO |
| `pNomZona` | `CHAR(32)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `iRegistros` | `INTEGER` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |
| `cMensajeRet` | `CHAR(80)` | L15 |
| `iColonia` | `INTEGER` | L16 |
| `cNombre` | `CHAR(32)` | L17 |
| `iCodigoPostal` | `INTEGER` | L18 |
| `cUnidadHabitacional` | `CHAR(1)` | L19 |
| `cTmpTable` | `CHAR(500)` | L20 |
| `iPid` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cb_param` | `bdicobranza` | ⚠️ sí | SELECT | L71 |
| `tmpcatalog_` | `bdicnweb` | no | INSERT | L102 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |
| `sp_consultacoloniascp` | `bdinteg` | ⚠️ sí | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultacoloniascp';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L97 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultacoloniascp';` |  |
| L105 | FÓRMULA | `LET iNoRegistros = iNoRegistros + DBINFO('sqlca.sqlerrd2');` |  |
| L112 | FÓRMULA | `LET iRecuperacion = iRecuperacion + iRegistros;` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L133 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `colonia` | ENTIDAD | colonia — colonia postal para validación de domicilio (sp_co | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |

---

## `sp_catalogoconsultarpor`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoconsultarpor.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 79 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar catálogo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoconsultarpor(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cClave` | `CHAR(4)` | L9 |
| `cDescripcion` | `CHAR(40)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cat_busqueda` | `bdicnweb` | no | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogoctascontablecaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoctascontablecaja.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, cuentas y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoctascontablecaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cIdCuenta` | `CHAR(20)` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `?able` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?able` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoctasempnet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoctasempnet.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 99 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, cuentas y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienedircta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoctasempnet(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cCuenta` | `CHAR(20)` | L17 |
| `cCliente` | `CHAR(20)` | L18 |
| `cProducto` | `CHAR(4)` | L19 |
| `cStatus` | `CHAR(2)` | L20 |
| `iUltChq` | `INTEGER` | L21 |
| `iChqExpMes` | `SMALLINT` | L22 |
| `iDireccEnvio` | `SMALLINT` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |
| `sp_obtienedircta` | `bdinteg` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdinteg:sp_obtienedircta';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00024';` |  |
| L78 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00024';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoctasxproductomoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoctasxproductomoral.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 98 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, cuentas y producto (persona moral)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarctasxproductomoral` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoctasxproductomoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pProducto` | `CHAR(4)` | `producto`=producto | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cIdCuentaMoral` | `CHAR(20)` | L9 |
| `cProducto` | `CHAR(4)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_consultarctasxproductomoral` | `bdicheq` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:sp_consultarctasxprodu` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `producto` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `moral` | MODIF | persona moral | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogodivisa`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogodivisa.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y divisa" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogodivisa(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cClave` | `CHAR(3)` | L10 |
| `cDescripcion` | `CHAR(30)` | L11 |
| `cDivisa` | `CHAR(2)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_divisas` | `bdinteg` | ⚠️ sí | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `divisa` | ENTIDAD | divisa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogodomiciliocte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogodomiciliocte.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, domicilio y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogodomiciliocte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescripcion` | `CHAR(20)` | L10 |
| `cIdDomicilio` | `CHAR(2)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_domicilio_cliente` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `domicilio` | ENTIDAD | domicilio | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogoedificio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoedificio.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoedificio(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCiudadCoppel              SMALLINT
  pIdZona                      SMALLINT
  pTipoConsulta                SMALLINT
  pConsulta                    CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCiudadCoppel` | `SMALLINT` | — | — |
| `pIdZona` | `SMALLINT` | — | — |
| `pTipoConsulta` | `SMALLINT` | — | — |
| `pConsulta` | `CHAR(30)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `cNombreDomicilio` | `CHAR(30)` | L10 |
| `iClaveComplemento` | `SMALLINT` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catdomicilios` | `bdinteg` | ⚠️ sí | SELECT | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pIdUsuario = '' OR pIdFuncion = '' OR pIdCiudadCoppel IS NULL OR pIdZona IS NULL OR pTipoConsulta` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L71 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L95 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?edificio` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?edificio` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoedoxdigitos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoedoxdigitos.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=41 |
| **Deps concatenadas** | 74 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, estado y dígito verificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consedoxdigitos` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoedoxdigitos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPais                        CHAR(3)
  pCodEstado                   CHAR(2)
  pNombreEstado                CHAR(30)
) RETURNING CHAR(5)  AS  cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPais` | `CHAR(3)` | — | — |
| `pCodEstado` | `CHAR(2)` | — | — |
| `pNombreEstado` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `vMensajeRet` | `VARCHAR(40)` | L12 |
| `cEdo` | `CHAR(2)` | L13 |
| `cNomEdo` | `CHAR(30)` | L14 |
| `cSiglas` | `CHAR(4)` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_consedoxdigitos` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consedoxdigitos';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `edo` | ENTIDAD | estado | 🟡 INFERIDO | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `digito` | ENTIDAD | dígito verificador | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoestado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoestado.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y estado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoestado(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                SMALLINT
  pConsulta                    CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `SMALLINT` | — | — |
| `pConsulta` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdEstado` | `CHAR(2)` | L8 |
| `cNombreEstado` | `CHAR(30)` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_estados` | `bdinteg` | ⚠️ sí | SELECT | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pIdUsuario = '' OR pIdFuncion = '' OR pTipoConsulta IS NULL THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L74 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estado` | ENTIDAD | estado (entidad federativa / estatus) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogoexcepcionesaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoexcepcionesaumlincred.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 112 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Aumento de Línea de Crédito — proceso de incremento del límite crediticio; 26+ SPs en bdicred catálogo y Comprobante Electrónico de Pago" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoexcepcionesaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeRetorno` | `CHAR(80)` | L10 |
| `cClaveExcepcion` | `CHAR(4)` | L11 |
| `cDescripcionExcepcion` | `CHAR(80)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L38 |
| `bdicred` | `bdicnweb` | no | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultar_excepciones_aumlincred';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cep` | ENTIDAD | Comprobante Electrónico de Pago (SPEI · Banxico) | 🔵 CONVENCIÓN | nombre_sp |
| `?ciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?ciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogofechaaptef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogofechaaptef.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, fecha y TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `cal_fecha_pre_fh` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogofechaaptef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cFechaHoy` | `CHAR(10)` | L8 |
| `dFechaAp` | `DATE` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `bFechas` | `BOOLEAN` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `cal_fecha_pre_fh` | `bditef` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃÂN DEL SP bditef:cal_fecha_pre_fh';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ap` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ap` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogogiromercantilctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogogiromercantilctemoral.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=40 |
| **Deps concatenadas** | 73 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, ro — Rol Operativo, cantidad y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultagiromercantil` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogogiromercantilctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigoGiro                  CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigoGiro` | `CHAR(3)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeRet` | `CHAR(80)` | L10 |
| `cCodActividadGiro` | `CHAR(3)` | L11 |
| `cDescActividadGiro` | `CHAR(40)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_consultagiromercantil` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultagiromercantil';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?gi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?mer` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cant` | ENTIDAD | cantidad | 🔵 CONVENCIÓN | nombre_sp |
| `?il` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gi`, `ro`, `?mer`, `?il` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoidentificacionsempnet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoidentificacionsempnet.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 97 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, identificación y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_considentificacion` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoidentificacionsempnet(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCodIdent` | `CHAR(2)` | L12 |
| `cDescripcion` | `CHAR(50)` | L13 |
| `cMensajeRet` | `CHAR(80)` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_considentificacion` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdinteg:sp_considentificacion'` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L62 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `identificacion` | ENTIDAD | identificación | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoinfouhcte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoinfouhcte.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ✅ fan_in=0 / fan_out=39 |
| **Deps concatenadas** | 72 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, información y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtieneinfouh` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoinfouhcte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCiudad                      SMALLINT
  pColonia                     SMALLINT
  pCveDom                      SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCiudad` | `SMALLINT` | — | — |
| `pColonia` | `SMALLINT` | — | — |
| `pCveDom` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iRegistros` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `sCveDomicilio` | `SMALLINT` | L13 |
| `cNombreDomicilio` | `CHAR(30)` | L14 |
| `cTmpTable` | `CHAR(500)` | L15 |
| `iPid` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cb_param` | `bdicobranza` | ⚠️ sí | SELECT | L60 |
| `tmpinfouh_` | `bdicnweb` | no | INSERT | L84 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |
| `sp_obtieneinfouh` | `bdinteg` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCiudad IS NULL OR pColonia IS NULL OR  pCveDom IS NULL OR pR` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L64 | VALIDACIÓN_NULL | `IF iRegistros IS NULL OR  iRegistros = "" THEN` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_obtieneinfouh';` |  |
| L87 | FÓRMULA | `LET iNoRegistros = iNoRegistros + DBINFO('sqlca.sqlerrd2');` |  |
| L94 | FÓRMULA | `LET iRecuperacion = iRecuperacion + iRegistros;` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L114 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?uh` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uh` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogomercado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogomercado.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogomercado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cClave` | `CHAR(1)` | L9 |
| `cDescripcion` | `CHAR(40)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_clase_tc` | `bdinteg` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?mercado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mercado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogomesaniocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogomesaniocaja.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, Mesa de Control — equipo de revisión y autorización de solicitudes de crédito; status codes MC/CM; valida comprobantes de ingreso; comentario explícito en código y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogomesaniocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIndicador                   CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIndicador` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `dMesRecepcion` | `CHAR(2)` | L8 |
| `dAnioRecepcion` | `CHAR(4)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L64 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `mesa` | ENTIDAD | Mesa de Control — equipo de revisión y autorización de solic | 🔵 CONVENCIÓN | nombre_sp |
| `?nio` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nio` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogomotivoscancelacioncre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogomotivoscancelacioncre.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=5 / fan_out=4 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela catálogo, motivo y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consultarmotivoscancelacioncredito` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogomotivoscancelacioncre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave                       SMALLINT
  pCodigo                      CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave` | `SMALLINT` | — | — |
| `pCodigo` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iClave` | `SMALLINT` | L10 |
| `cCodigo` | `CHAR(3)` | L11 |
| `cMotivoCancelacion` | `CHAR(45)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cMesajeEjecucionSp` | `CHAR(65)` | L14 |
| `iNoRegs` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarmotivoscancelacioncredito` | `bdicred` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00191';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `motivo` | ENTIDAD | motivo / causa | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cancelacion` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogonacionalidadcte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogonacionalidadcte.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=38 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, nacionalidad y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienenaccte` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogonacionalidadcte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodNacionalidad             CHAR(3)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodNacionalidad` | `CHAR(3)` | `nacionalidad`=nacionalidad | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cMens` | `CHAR(60)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cCodNac` | `CHAR(3)` | L12 |
| `cDescripcion` | `CHAR(15)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_obtienenaccte` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_obtienenaccte';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00285';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `nacionalidad` | ENTIDAD | nacionalidad | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogopagosdmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogopagosdmi.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 77 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y pagos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogopagosdmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIdPago` | `INTEGER` | L8 |
| `cDescPago` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `pagos` | ENTIDAD | pagos (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?dmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoparammc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoparammc.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, parámetro y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoparammc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iIdParam` | `INTEGER` | L11 |
| `cTipo` | `CHAR(1)` | L12 |
| `sValor` | `SMALLINT` | L13 |
| `cMarca` | `CHAR(2)` | L14 |
| `cDescParam` | `CHAR(100)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogoperacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoperacionescaja.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoperacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdMostrar` | `CHAR(4)` | L8 |
| `cDescMostrar` | `CHAR(35)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L64 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?peraciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?peraciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoperiodoreportesic`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoperiodoreportesic.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, periodo y reportes" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoperiodoreportesic(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(6)
  pTipoReporte                 CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(6)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `dFecha` | `DATE` | L9 |
| `iMes` | `INTEGER` | L10 |
| `cDescipFecha` | `CHAR(20)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `br_fechas_concil` | `bdiburo` | ⚠️ sí | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00297';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `periodo` | ENTIDAD | periodo | 🔵 CONVENCIÓN | nombre_sp |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?ic` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ic` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoplazacaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoplazacaja.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=51 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, plaza y caja" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoplazacaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdPlaza` | `CHAR(3)` | L8 |
| `cDescPlaza` | `CHAR(40)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `plaza` | ENTIDAD | plaza (regional) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogoprocedenciacaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoprocedenciacaja.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procede catálogo y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoprocedenciacaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdProcedencia` | `CHAR(4)` | L8 |
| `cDescProcedencia` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `procede` | ACCION | procede | 🔵 CONVENCIÓN | nombre_sp |
| `?ncia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ncia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoprocrecursosctamec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoprocrecursosctamec.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 70 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recepción catálogo, proceso, OS — Originación de Solicitudes y Cuenta Mecánica — tipo de cuenta de cheques empresarial para nómina y pagos automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ctamec_obtieneprocrecursos` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoprocrecursosctamec(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave                       CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cProcedencia` | `CHAR(2)` | L11 |
| `cDescripcion` | `CHAR(60)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_ctamec_obtieneprocrecursos` | `bdicheq` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_ctamec_obtieneprocrecursos';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `proc` | ENTIDAD | proceso | 🟡 INFERIDO | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?urs` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ctamec` | ENTIDAD | Cuenta Mecánica — tipo de cuenta de cheques empresarial para | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?urs` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoproductosreportesic`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoproductosreportesic.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, productos y reportes" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoproductosreportesic(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdProducto` | `CHAR(4)` | L8 |
| `cDescripcionProducto` | `CHAR(80)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?ic` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ic` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoproductoxtipopersona`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoproductoxtipopersona.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=36 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y producto (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ctamec_obtieneproductosxtipper` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoproductoxtipopersona(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFisica                      CHAR(1)
  pTipoPersona                 CHAR(2)
  pCuenta                      CHAR(20)
  pCodProd                     CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFisica` | `CHAR(1)` | — | — |
| `pTipoPersona` | `CHAR(2)` | `tipo`=tipo de | ✅ CÓDIGO |
| `pCuenta` | `CHAR(20)` | — | — |
| `pCodProd` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `cCodProducto` | `CHAR(4)` | L15 |
| `cDescProducto` | `CHAR(40)` | L16 |
| `cEsPersonaFisica` | `CHAR(1)` | L17 |
| `cTipoPersona` | `CHAR(2)` | L18 |
| `cDescTipoPersona` | `CHAR(20)` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |
| `sp_ctamec_obtieneproductosxtipper` | `bdicheq` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_ctamec_obtieneproductosxtipper';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00021';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `producto` | ENTIDAD | producto | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?persona` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?persona` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogorigenaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogorigenaumlincred.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 144 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogorigenaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeRetorno` | `CHAR(80)` | L10 |
| `cOrigen` | `CHAR(2)` | L11 |
| `cDescripcionOrigen` | `CHAR(40)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L38 |
| `bdicred` | `bdicnweb` | no | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultar_origen_aumlincred';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?ri` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ri` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoscc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoscc.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=0 / fan_out=108 |
| **Deps concatenadas** | 328 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y cc — cuenta corriente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoscc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cClave` | `CHAR(11)` | L11 |
| `cDescripcion` | `CHAR(60)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plazas_cajagen` | `bdinteg` | ⚠️ sí | SELECT | L61 |
| `si_plaza_clabe` | `bdinteg` | ⚠️ sí | SELECT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L64 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogoscteprospecto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogoscteprospecto.sql` |
| **LOC (1er CREATE)** | 131 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 6 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogoscteprospecto(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pConsulta                    CHAR(1)
) RETURNING CHAR(5) AS cCodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pConsulta` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRet` | `CHAR(5)` | L7 |
| `cNacion` | `CHAR(3)` | L8 |
| `cDescNacion` | `CHAR(60)` | L9 |
| `iNumRows` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_nacion` | `bdinteg` | ⚠️ sí | SELECT | L40 |
| `si_cantpersonas` | `bdinteg` | ⚠️ sí | SELECT | L53 |
| `si_habitaen` | `bdinteg` | ⚠️ sí | SELECT | L66 |
| `si_tipoidentif` | `bdinteg` | ⚠️ sí | SELECT | L79 |
| `si_profesion` | `bdinteg` | ⚠️ sí | SELECT | L92 |
| `si_tipocte` | `bdinteg` | ⚠️ sí | SELECT | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?pecto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogostatusaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogostatusaumlincred.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 113 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Aumento de Línea de Crédito — proceso de incremento del límite crediticio; 26+ SPs en bdicred catálogo y estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogostatusaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `mensajeRetorno` | `CHAR(80)` | L10 |
| `status` | `CHAR(2)` | L11 |
| `cDescripcionStatus` | `CHAR(40)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L37 |
| `bdicred` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultar_status_aumlincred';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogostatuscaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogostatuscaja.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, estatus y caja" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogostatuscaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdStatus` | `CHAR(2)` | L8 |
| `cDescStatus` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L63 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L74 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogosuctrancaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogosuctrancaja.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 162 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y caja (sucursal)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogosuctrancaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                CHAR(1)
  pTipoSucursal                CHAR(1)
  pIdProveedor                 CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | — | — |
| `pTipoSucursal` | `CHAR(1)` | `suc`=sucursal | 🟡 INFERIDO |
| `pIdProveedor` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cPlaza` | `CHAR(3)` | L11 |
| `cIdSucursal` | `CHAR(4)` | L12 |
| `cDescSucursal` | `CHAR(40)` | L13 |
| `cIdTransaccion` | `CHAR(30)` | L14 |
| `cDescTransaccion` | `CHAR(35)` | L15 |
| `cCodTransaccion` | `CHAR(4)` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L70 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L74 |
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L91 |
| `ss_cajageneral` | `bdisuc` | ⚠️ sí | SELECT | L121 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoConsulta = '' OR pRegistros IS NULL OR pRecuperacion IS ` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L95 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L107 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L130 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `?tran` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tran` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogosucxcg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogosucxcg.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 78 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y cg — Canal/Cuenta General (sucursal)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_sucxcg2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogosucxcg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCaja                        CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCaja` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cClave` | `CHAR(4)` | L11 |
| `cDescripcion` | `CHAR(40)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `sp_consulta_sucxcg2` | `bdisuc` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCaja = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THE` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_consulta_sucxcg2';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogosufijosctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogosufijosctemoral.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 67 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, OS — Originación de Solicitudes y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultasufijos` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogosufijosctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cMensajeErr` | `CHAR(80)` | L12 |
| `cCodSufijo` | `CHAR(2)` | L13 |
| `cDescripcion` | `CHAR(60)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_consultasufijos` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultasufijos';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?sufij` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sufij` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogosufijospm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogosufijospm.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogosufijospm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodSufijo` | `CHAR(2)` | L10 |
| `cDescSufijo` | `CHAR(60)` | L11 |
| `cStatus` | `CHAR(1)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sufijos` | `bdinteg` | ⚠️ sí | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?sufij` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sufij`, `?pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotasasaltas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotasasaltas.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 97 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta catálogo y tasa" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotasasaltas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pTasa                        CHAR(8)
  pFecha                       DATE
  pValor                       CHAR(15)
  pPorcentaje                  CHAR(15)
  pPuntos                      CHAR(15)
  pPromedio                    CHAR(15)
  pFechaRecalculo              DATE
  pValorBaseRef                CHAR(15)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pTasa` | `CHAR(8)` | `tasa`=tasa (de interés) | ✅ CÓDIGO |
| `pFecha` | `DATE` | — | — |
| `pValor` | `CHAR(15)` | — | — |
| `pPorcentaje` | `CHAR(15)` | — | — |
| `pPuntos` | `CHAR(15)` | — | — |
| `pPromedio` | `CHAR(15)` | — | — |
| `pFechaRecalculo` | `DATE` | — | — |
| `pValorBaseRef` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cTipotasa` | `CHAR(8)` | L6 |
| `cDescripcion` | `CHAR(30)` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `DATE` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechavalor` | `bdinteg` | ⚠️ sí | SELECT | L48 |
| `si_fechavalor` | `bdinteg` | ⚠️ sí | INSERT | L57 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L64 |
| `si_fechavalor` | `bdinteg` | ⚠️ sí | UPDATE | L83 |
| `si_fechavalor` | `bdinteg` | ⚠️ sí | DELETE | L91 |
| `de` | `bdicnweb` | no | UPDATE | L104 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion = '' OR pTasa = '' OR pFecha IS NULL or pValor` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotasasfecha`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotasasfecha.sql` |
| **LOC (1er CREATE)** | 131 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 96 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, tasa y fecha" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotasasfecha(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTasa                        CHAR(8)
  pFecha                       DATE
  pValor                       CHAR(15)
  pPorcentaje                  CHAR(15)
  pPuntos                      CHAR(15)
  pPromedio                    CHAR(15)
  pFechaRecalculo              DATE
  pValorBaseRef                CHAR(15)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTasa` | `CHAR(8)` | `tasa`=tasa (de interés) | ✅ CÓDIGO |
| `pFecha` | `DATE` | `fecha`=fecha | ✅ CÓDIGO |
| `pValor` | `CHAR(15)` | — | — |
| `pPorcentaje` | `CHAR(15)` | — | — |
| `pPuntos` | `CHAR(15)` | — | — |
| `pPromedio` | `CHAR(15)` | — | — |
| `pFechaRecalculo` | `DATE` | `fecha`=fecha | ✅ CÓDIGO |
| `pValorBaseRef` | `CHAR(15)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cTasa` | `CHAR(8)` | L16 |
| `cDescripcion` | `CHAR(30)` | L17 |
| `dFecha` | `DATE` | L18 |
| `dValor` | `DECIMAL(9,6)` | L19 |
| `dPromedio` | `DECIMAL(9,6)` | L20 |
| `dValorBaseRef` | `DECIMAL(9,6)` | L21 |
| `dPorcentaje` | `DECIMAL(9,6)` | L22 |
| `dPuntos` | `DECIMAL(9,6)` | L23 |
| `dFechaRecalculo` | `DATE` | L24 |
| `iNumRegistros` | `INTEGER` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |
| `dValorChr` | `DECIMAL(9,6)` | L27 |
| `dPromedioChr` | `DECIMAL(9,6)` | L28 |
| `dVBaseRefChr` | `DECIMAL(9,6)` | L29 |
| `dPorcentajeChr` | `DECIMAL(9,6)` | L30 |
| `dPuntosChr` | `DECIMAL(9,6)` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechavalor` | `bdinteg` | ⚠️ sí | SELECT | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L108 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotasasrecalculo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotasasrecalculo.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 95 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recepción catálogo y tasa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotasasrecalculo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cClaveTasa` | `CHAR(8)` | L14 |
| `cTasaReferen` | `CHAR(8)` | L15 |
| `dValorBase` | `DECIMAL(9,6)` | L16 |
| `dPorcentaje` | `DECIMAL(9,6)` | L17 |
| `dPuntosAdic` | `DECIMAL(9,6)` | L18 |
| `dValorCalc` | `DECIMAL(9,6)` | L19 |
| `fechaMax` | `DATE` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechavalor` | `bdinteg` | ⚠️ sí | SELECT | L61 |
| `si_fechavalor` | `bdinteg` | ⚠️ sí | UPDATE | L80 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | FÓRMULA | `LET dValorCalc = (dValorBase * dPorcentaje) / (100 + dPuntosAdic);` |  |
| L83 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?alculo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?alculo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipmontomovtosctamec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipmontomovtosctamec.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=34 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, monto, movimiento y Cuenta Mecánica — tipo de cuenta de cheques empresarial para nómina y pagos automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ctamec_obtienetipmontomovtos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipmontomovtosctamec(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cCodNumMov` | `CHAR(2)` | L13 |
| `cDescripcion` | `CHAR(60)` | L14 |
| `cDe` | `CHAR(16)` | L15 |
| `cA` | `CHAR(16)` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_ctamec_obtienetipmontomovtos` | `bdicheq` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?tip` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `monto` | ENTIDAD | monto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctamec` | ENTIDAD | Cuenta Mecánica — tipo de cuenta de cheques empresarial para | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tip`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipmontomovtosmensctamec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipmontomovtosmensctamec.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=33 |
| **Deps concatenadas** | 65 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, monto, movimiento y Cuenta Mecánica — tipo de cuenta de cheques empresarial para nómina y pagos automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ctamec_obtienetipmontomovtosmens` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipmontomovtosmensctamec(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cCodNumMovMes` | `CHAR(2)` | L13 |
| `cDescripcion` | `CHAR(60)` | L14 |
| `cDe` | `CHAR(16)` | L15 |
| `cA` | `CHAR(16)` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_ctamec_obtienetipmontomovtosmens` | `bdicheq` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?tip` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `monto` | ENTIDAD | monto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?smens` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctamec` | ENTIDAD | Cuenta Mecánica — tipo de cuenta de cheques empresarial para | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tip`, `?smens` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipnummovtosctamec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipnummovtosctamec.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, número, movimiento y Cuenta Mecánica — tipo de cuenta de cheques empresarial para nómina y pagos automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ctamec_obtienetipnummovtos` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipnummovtosctamec(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cCodNumMovimiento` | `CHAR(2)` | L14 |
| `cDescripcion` | `CHAR(60)` | L15 |
| `cDe` | `CHAR(16)` | L16 |
| `cA` | `CHAR(16)` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_ctamec_obtienetipnummovtos` | `bdicheq` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?tip` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctamec` | ENTIDAD | Cuenta Mecánica — tipo de cuenta de cheques empresarial para | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tip`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipoadminpmgobierno`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipoadminpmgobierno.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y Administrador — rol de usuario con privilegios administrativos (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_tipo_admin_pm_gobierno` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipoadminpmgobierno(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(3)
  pTipoOrden                   SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(3)` | — | — |
| `pTipoOrden` | `SMALLINT` | `tipo`=tipo de | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCodigo` | `CHAR(3)` | L11 |
| `cDescripcion` | `CHAR(20)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_tipo_admin_pm_gobierno` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOrden IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_tipo_admin_pm_gobierno';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?pmgobierno` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pmgobierno` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipoclientescli`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipoclientescli.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y clientes (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipoclientescli(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)			AS COD_RET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_cod_ret` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iSamErr` | `INTEGER` | L10 |
| `clave_tipo` | `SMALLINT` | L11 |
| `descripcion_corta` | `CHAR(100)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catcterelacionado` | `bdinteg` | ⚠️ sí | SELECT | L42 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `clientes` | ENTIDAD | clientes (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?cli` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cli` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipoctatef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipoctatef.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, cuenta y TEF — transferencia electrónica de fondos (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_tef_obttipocta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipoctatef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cTipoCuenta` | `CHAR(2)` | L10 |
| `cDescCuenta` | `CHAR(50)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_tef_obttipocta` | `bditef` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditef:sp_tef_obttipocta';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogotipoorgpmgobierno`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipoorgpmgobierno.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_tipo_org_pm_gobierno` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipoorgpmgobierno(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(3)
  pTipoOrden                   SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(3)` | — | — |
| `pTipoOrden` | `SMALLINT` | `tipo`=tipo de | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCodigo` | `CHAR(3)` | L11 |
| `cDescripcion` | `CHAR(20)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_tipo_org_pm_gobierno` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOrden IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_tipo_org_pm_gobierno';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?orgpmgobierno` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orgpmgobierno` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipoperacioncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipoperacioncaja.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y caja (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipoperacioncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | `tipo`=tipo de | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdMostrar` | `CHAR(2)` | L8 |
| `cDescMostrar` | `CHAR(25)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `?peracion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?peracion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipopoderpmgobierno`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipopoderpmgobierno.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_tipo_poder_pm_gobierno` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipopoderpmgobierno(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(3)
  pTipoOrden                   SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(3)` | — | — |
| `pTipoOrden` | `SMALLINT` | `tipo`=tipo de | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCodigo` | `CHAR(3)` | L11 |
| `cDescripcion` | `CHAR(20)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_tipo_poder_pm_gobierno` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOrden IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_tipo_poder_pm_gobierno';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?poderpmgobierno` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?poderpmgobierno` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotiporegimenfirmas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotiporegimenfirmas.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registro catálogo y firmas mancomunadas (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ctamec_obtienetiporegimenfirmas` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotiporegimenfirmas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave                       CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cClaveRegimen` | `CHAR(2)` | L11 |
| `cDescripcion` | `CHAR(20)` | L12 |
| `cCombinacion` | `CHAR(20)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_ctamec_obtienetiporegimenfirmas` | `bdicntchq` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_ctamec_obtienetiporegimenfirmas';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?imen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `firmas` | ENTIDAD | firmas mancomunadas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imen` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalogotipotasa`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotipotasa.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 94 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y tasa (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotipotasa(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cClaveTasa` | `CHAR(8)` | L9 |
| `cDescripcion` | `CHAR(30)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tiptasa` | `bdinteg` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catalogotransaccionabonocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotransaccionabonocap.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=84 |
| **Deps concatenadas** | 196 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, transacción, abono y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotransaccionabonocap(
  pUsuario                     CHAR(10)
  pIdFuncion                   CHAR(10)
  pNaturaleza                  CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(10)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNaturaleza` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cClave_transaccion` | `CHAR(4)` | L7 |
| `cDesc_transaccion` | `CHAR(50)` | L8 |
| `iSqlErr` | `INT` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_tranval` | `bdicheq` | ⚠️ sí | SELECT | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `abono` | ENTIDAD | abono / crédito | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogotransaccioncargocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogotransaccioncargocap.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 217 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, transacción, cargo y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogotransaccioncargocap(
  pUsuario                     CHAR(10)
  pIdFuncion                   CHAR(10)
  pNaturaleza                  CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(10)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNaturaleza` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cClave_transaccion` | `CHAR(4)` | L7 |
| `cDesc_transaccion` | `CHAR(50)` | L8 |
| `iSqlErr` | `INT` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_tranval` | `bdicheq` | ⚠️ sí | SELECT | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L68 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogozona`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogozona.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y zona" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogozona(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCiudadCoppel              SMALLINT
  pTipoConsulta                SMALLINT
  pConsulta                    CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCiudadCoppel` | `SMALLINT` | — | — |
| `pTipoConsulta` | `SMALLINT` | — | — |
| `pConsulta` | `CHAR(30)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iExiste` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iIdNumeroColonia` | `SMALLINT` | L11 |
| `cNombreZona` | `CHAR(32)` | L12 |
| `cNombreMunicipio` | `CHAR(27)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pIdUsuario = '' OR pIdFuncion = '' OR pIdCiudadCoppel IS NULL OR pTipoConsulta IS NULL OR pRegist` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L77 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L104 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `zona` | ENTIDAD | zona | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catalogozona_2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalogozona_2.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y zona" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalogozona_2(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCiudadCoppel              SMALLINT
  pTipoConsulta                SMALLINT
  pConsulta                    CHAR(30)
  pCliente                     CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCiudadCoppel` | `SMALLINT` | — | — |
| `pTipoConsulta` | `SMALLINT` | — | — |
| `pConsulta` | `CHAR(30)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iExiste` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iIdNumeroColonia` | `SMALLINT` | L11 |
| `cNombreZona` | `CHAR(32)` | L12 |
| `cNombreMunicipio` | `CHAR(27)` | L13 |
| `iTotalReg` | `INTEGER` | L14 |
| `cCiudad` | `INTEGER` | L15 |
| `cNumColonia` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L77 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pIdUsuario = '' OR pIdFuncion = '' OR pIdCiudadCoppel IS NULL OR pTipoConsulta IS NULL OR pRegist` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iTotalReg = 100 - pRecuperacion;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L112 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L139 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `zona` | ENTIDAD | zona | 🔵 CONVENCIÓN | nombre_sp |
| `?_2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalotipoconciliacionccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalotipoconciliacionccl.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación catálogo y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalotipoconciliacionccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdConciliacion` | `INTEGER` | L8 |
| `cDescConciliacion` | `CHAR(12)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ccl_tipoconciliacion` | `bdicnweb` | no | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?alo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?alo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catalotipoproductoccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catalotipoproductoccl.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, producto y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catalotipoproductoccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdProducto` | `INTEGER` | L8 |
| `cDescProducto` | `CHAR(25)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ccl_tipoproducto` | `bdicnweb` | no | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?alo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `producto` | ENTIDAD | producto | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?alo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catarchivoimportar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catarchivoimportar.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=9 / fan_out=3 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, archivo, Impago — pago vencido o fallido; confirmado: n_impagos_consec y Tarjeta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catarchivoimportar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCodOperacion` | `CHAR(5)` | L11 |
| `cDescArchivo` | `CHAR(100)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_tipoarchivo` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?or` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?or` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catarchivorigendmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catarchivorigendmi.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 76 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera catálogo y archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catarchivorigendmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR (20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR (20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cNombreArchivo` | `CHAR(20)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `bdidomi` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdidomi:sp_domi_consultaarchivoorigenpro` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ri` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `?dmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ri`, `?dmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catbancos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catbancos.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 116 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y banco" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catbancos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCveBanco` | `CHAR(3)` | L12 |
| `cDescBanco` | `CHAR(40)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L63 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `banco` | ENTIDAD | banco | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catbitacoraconciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catbitacoraconciliacion.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación catálogo y bitácora" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catbitacoraconciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iElemente` | `INTEGER` | L8 |
| `cNombreElemento` | `CHAR (35)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `td_elemento` | `bditarjeta` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catcedulacontableccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catcedulacontableccl.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, cédula contable y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catcedulacontableccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cNombre` | `CHAR(10)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `iRegistros` | `INTEGER` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `bdicnweb` | `bdicnweb` | no | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cedulacontablestatus ";` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `cedulacontable` | ENTIDAD | cédula contable | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_catcedulacontablerolesccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catcedulacontablerolesccl.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, cédula contable, rol y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catcedulacontablerolesccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `sTipoRol` | `SMALLINT` | L10 |
| `cNombre` | `CHAR(10)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `iRegistros` | `INTEGER` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `bdicnweb` | `bdicnweb` | no | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cedulacontableroles ";` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00582';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `cedulacontable` | ENTIDAD | cédula contable | 🟡 INFERIDO | nombre_sp |
| `rol` | ENTIDAD | rol / perfil | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catchequespendientes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catchequespendientes.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=12 / fan_out=14 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y cheques" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catchequespendientes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cNombreArchivo` | `CHAR(22)` | L11 |
| `cSecuencia` | `INTEGER` | L12 |
| `cUsuarioValida` | `CHAR(8)` | L13 |
| `iTotalChequesPendientes` | `INTEGER` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha = '' OR pRegistros IS NULL OR pRecuperacion IS NULL TH` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L74 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pendientes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catdevolucionesposccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catdevolucionesposccl.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "devuelve catálogo, OS — Originación de Solicitudes y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catdevolucionesposccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR (50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR (50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cCodigo` | `CHAR(3)` | L11 |
| `cDescripcion` | `CHAR(50)` | L12 |
| `cValor` | `CHAR(90)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `bditarjeta` | `bdicnweb` | no | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditarjeta:sp_concreing_param_consulta';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_catestatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catestatus.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=56 |
| **Deps concatenadas** | 151 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catestatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEstatusSol` | `CHAR(2)` | L8 |
| `cDescripcion` | `CHAR(40)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_status_sol` | `bdisolic` | ⚠️ sí | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_catestmunicipio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catestmunicipio.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 159 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catestmunicipio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCp                          CHAR(5)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCp` | `CHAR(5)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCp` | `CHAR(5)` | L12 |
| `cCve_estado` | `CHAR(2)` | L13 |
| `cCve_mun` | `CHAR(3)` | L14 |
| `cNombre_estado` | `CHAR(30)` | L15 |
| `cDesc_mun` | `CHAR(60)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catsepomex` | `bdinteg` | ⚠️ sí | SELECT | L59 |
| `si_municipios` | `bdinteg` | ⚠️ sí | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCp = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L71 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?estmunicipio` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?estmunicipio` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catguardasignaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catguardasignaciones.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=12 / fan_out=14 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catguardasignaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCheques                     CHAR(250)
  pEjecutivo                   CHAR(8)
  pFecha                       CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCheques` | `CHAR(250)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cNombreCheque` | `CHAR(22)` | L6 |
| `cNombreChequeSecuencia` | `CHAR(80)` | L7 |
| `cSecuencia` | `CHAR(22)` | L8 |
| `iTotal` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_split_cadena` | `bdicnweb` | no | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | FÓRMULA | `LET cNombreCheque = SUBSTR(TRIM(cNombreChequeSecuencia),LENGTH(cSecuencia)+2,LENGTH(cNombreChequeSec` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?signaciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?signaciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catproductoscredito`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catproductoscredito.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=77 |
| **Deps concatenadas** | 183 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, productos y crédito" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cac_consulta_productos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catproductoscredito(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNombreProducto` | `CHAR(40)` | L11 |
| `cNumProducto` | `CHAR(4)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_cac_consulta_productos` | `bdicred` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_cac_consulta_product` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00786';` |  |
| L59 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00786';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `credito` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_catproductosistinversiones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catproductosistinversiones.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, productos y inversión" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_catproductosistinversiones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdInstrum` | `CHAR (4)` | L8 |
| `cDesInstrum` | `CHAR (40)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_instrum` | `bdinvers` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ist` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inversion` | ENTIDAD | inversión (pagaré / plazo) | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ist`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catrevisoresfirmas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catrevisoresfirmas.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=12 / fan_out=14 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reversión catálogo y firmas mancomunadas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_catrevisoresfirmas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iPerfil` | `INTEGER` | L11 |
| `cEjecutivo` | `CHAR(8)` | L12 |
| `cNombreEjecutivo` | `CHAR(45)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L51 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?isores` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `firmas` | ENTIDAD | firmas mancomunadas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?isores` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catseleccionarchivodotacaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catseleccionarchivodotacaja.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, cc — cuenta corriente, archivo y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_catseleccionarchivodotacaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdValor` | `CHAR(4)` | L8 |
| `cDescripcion` | `CHAR(35)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?sele` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `?dota` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sele`, `?ion`, `?dota` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_catstatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catstatus.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 82 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_catstatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cStatus` | `CHAR(1)` | L12 |
| `cDescStatus` | `CHAR(35)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cat_status` | `bdicnweb` | no | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_catstatusupervision`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_catstatusupervision.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 182 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienestatus_superv` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_catstatusupervision(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cStatus` | `CHAR(2)` | L11 |
| `cDescStatus` | `CHAR(50)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_obtienestatus_superv` | `bdisolic` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_obtienestatus_super` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L59 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?upervision` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?upervision` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cattipofechas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cattipofechas.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 88 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y fechas (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cattipofechas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iIdTipo` | `SMALLINT` | L12 |
| `cDescripcion` | `CHAR(35)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tipos_fechas` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fechas` | ENTIDAD | fechas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_cattiporeporteart61`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cattiporeporteart61.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y reporte (tipo de) · Art. 61 LIC" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cattiporeporteart61(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdReporte` | `CHAR(2)` | L8 |
| `cDescReporte` | `CHAR(45)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tiporeporte` | `bdicnweb` | no | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `art61` | REG | Art. 61 LIC (cuentas inactivas cuyos saldos, tras años sin m | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cattotalchequesdetalle`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cattotalchequesdetalle.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=12 / fan_out=14 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, cheques y detalle (total)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cattotalchequesdetalle(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iTotalCheques` | `INTEGER` | L10 |
| `iTotalChequesPorRevisar` | `INTEGER` | L11 |
| `iTotalChequesRevisados` | `INTEGER` | L12 |
| `cCuenta` | `CHAR(20)` | L14 |
| `iCheque` | `INTEGER` | L15 |
| `dImporte` | `DECIMAL(16,2)` | L16 |
| `cRevisadoFirmas` | `CHAR(1)` | L17 |
| `cUsuarioValida` | `CHAR(8)` | L18 |
| `cMotivoDevol` | `CHAR(2)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | FÓRMULA | `LET iTotalCheques = iTotalCheques + 1;` |  |
| L68 | FÓRMULA | `LET iTotalChequesRevisados = iTotalChequesRevisados + 1;` |  |
| L70 | FÓRMULA | `LET iTotalChequesPorRevisar = iTotalChequesPorRevisar + 1;` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cb_consultareportes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cb_consultareportes.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 122 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cb_consultareportes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cNombre` | `CHAR(100)` | L10 |
| `dFecha` | `DATE` | L11 |
| `cHora` | `CHAR(20)` | L12 |
| `cUsuario` | `CHAR(8)` | L13 |
| `iTotal` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesart` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET iTotal = iTotal+1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cb_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cb_`, `?e`, `?tes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cb_genrepcuentasatraspasar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cb_genrepcuentasatraspasar.sql` |
| **LOC (1er CREATE)** | 402 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cb_genrepcuentasatraspasar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumDiasSMVDF                SMALLINT
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumDiasSMVDF` | `SMALLINT` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(15)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `dValorSM` | `DECIMAL(14,2)` | L18 |
| `iNoAnios` | `SMALLINT` | L19 |
| `cnum_cte` | `CHAR(20)` | L20 |
| `ccuenta` | `CHAR(20)` | L21 |
| `ccliente` | `CHAR(104)` | L22 |
| `dsdocon` | `DECIMAL(18,2)` | L23 |
| `dsdofin` | `DECIMAL(14,2)` | L24 |
| `dfechapago` | `DATE` | L25 |
| `iTotal` | `INTEGER` | L26 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L27 |
| `cNombre` | `CHAR(30)` | L28 |
| `dTotal` | `DECIMAL(16,2)` | L29 |
| `pIdMensaje` | `CHAR(10)` | L30 |
| *…17 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L146 |
| `sw_cb_reportecuentasatraspasartmp` | `bdicnweb` | no | SELECT | L150 |
| `sw_cb_reportecuentasatraspasartmp` | `bdicnweb` | no | DELETE | L150 |
| `sw_verificastatusrepcuentasatraspasar` | `bdicnweb` | no | SELECT | L152 |
| `sw_verificastatusrepcuentasatraspasar` | `bdicnweb` | no | DELETE | L152 |
| `sw_verificastatusrepcuentasatraspasar` | `bdicnweb` | no | INSERT | L156 |
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L162 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L175 |
| `sc_cuentas_concentradas` | `bdicheq` | ⚠️ sí | SELECT | L185 |
| `si_fechavalor` | `bdinteg` | ⚠️ sí | SELECT | L214 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L220 |
| `sw_cb_reportecuentasatraspasartmp` | `bdicnweb` | no | INSERT | L273 |
| `systables` | `bdicnweb` | no | SELECT | L294 |
| `sw_ctrlgenreportesart` | `bdicnweb` | no | SELECT | L381 |
| `sw_ctrlgenreportesart` | `bdicnweb` | no | DELETE | L381 |
| `sw_ctrlgenreportesart` | `bdicnweb` | no | INSERT | L382 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L108 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L134 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L245 | VALIDACIÓN_NULL | `IF vbase_exenta is null THEN` |  |
| L249 | FÓRMULA | `LET iDias = vFechaHoy - dfechapago;` |  |
| L250 | FÓRMULA | `LET vBase_gravable = dsdocon - vbase_exenta;` |  |
| L255 | FÓRMULA | `LET vIsrCalc = (vBase_gravable * (dPorRetSuj/100)) * iDias / iAniobase;` |  |
| L260 | FÓRMULA | `LET vIsrCalc = (dsdocon * (dPorRetSuj/100)) * iDias / iAniobase;` |  |
| L270 | FÓRMULA | `LET vValSaldo = NVL(dsdocon,0.00) + NVL(vAcum_sdo_int,0.00) + NVL(vInts_prov_acum,0.00) - NVL(vIsrCa` |  |
| L282 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L296 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT num_cte::CHAR(20), num_cta::CHAR(20),nom_cte::CHAR(104),sal` | 🔴 MONEY/aritmética financiera |
| L304 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L335 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L340 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L349 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L358 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cb_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `traspas` | ACCION | traspaso | 🟡 INFERIDO | nombre_sp |
| `?ar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cb_`, `?a`, `?ar` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cb_genrepcuentastraspasadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cb_genrepcuentastraspasadas.sql` |
| **LOC (1er CREATE)** | 265 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cb_genrepcuentastraspasadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(15)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `dValorSM` | `DECIMAL(14,2)` | L18 |
| `iNoAnios` | `SMALLINT` | L19 |
| `cnum_cte` | `CHAR(20)` | L20 |
| `ccuenta` | `CHAR(20)` | L21 |
| `ccliente` | `CHAR(104)` | L22 |
| `dsdocon` | `DECIMAL(18,2)` | L23 |
| `dsdofin` | `DECIMAL(14,2)` | L24 |
| `dfechapago` | `DATE` | L25 |
| `dfechatran` | `DATE` | L26 |
| `iTotal` | `INTEGER` | L27 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L28 |
| `cNombre` | `CHAR(30)` | L29 |
| `dTotal` | `DECIMAL(16,2)` | L30 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L110 |
| `sw_cb_reportecuentastraspasadastmp` | `bdicnweb` | no | SELECT | L114 |
| `sw_cb_reportecuentastraspasadastmp` | `bdicnweb` | no | DELETE | L114 |
| `sw_verificastatusrepcuentastraspasadas` | `bdicnweb` | no | SELECT | L116 |
| `sw_verificastatusrepcuentastraspasadas` | `bdicnweb` | no | DELETE | L116 |
| `sw_verificastatusrepcuentastraspasadas` | `bdicnweb` | no | INSERT | L120 |
| `sc_cuentas_concentradas` | `bdicheq` | ⚠️ sí | SELECT | L127 |
| `sw_cb_reportecuentastraspasadastmp` | `bdicnweb` | no | INSERT | L134 |
| `systables` | `bdicnweb` | no | SELECT | L154 |
| `sw_ctrlgenreportesart` | `bdicnweb` | no | SELECT | L241 |
| `sw_ctrlgenreportesart` | `bdicnweb` | no | DELETE | L241 |
| `sw_ctrlgenreportesart` | `bdicnweb` | no | INSERT | L242 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L72 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L156 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT num_cte::CHAR(20), num_cta::CHAR(20),nom_cte::CHAR(104),sal` |  |
| L164 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L195 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L200 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L209 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L218 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cb_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `traspas` | ACCION | traspaso | 🟡 INFERIDO | nombre_sp |
| `?adas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cb_`, `?adas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cb_verificapermisodescarga`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cb_verificapermisodescarga.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 121 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica descarga" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cb_verificapermisodescarga(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bPermiso` | `BOOLEAN` | L7 |
| `iTotal` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `seg_permisos_usuarios` | `bdicnweb` | no | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cb_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `?permiso` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `descarga` | ENTIDAD | descarga | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cb_`, `?permiso` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cb_verificastatusrepcuentasatrasp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cb_verificastatusrepcuentasatrasp.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 120 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cb_verificastatusrepcuentasatrasp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cProceso` | `CHAR(15)` | L14 |
| `cNombre_archivo` | `CHAR(50)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepcuentasatraspasar` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cb_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?atra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cb_`, `?atra` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cb_verificastatusrepcuentastrasp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cb_verificastatusrepcuentastrasp.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 119 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cb_verificastatusrepcuentastrasp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cProceso` | `CHAR(15)` | L14 |
| `cNombre_archivo` | `CHAR(50)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepcuentastraspasadas` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cb_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cb_`, `?tra` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_actualizacccajagen`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_actualizacccajagen.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cc — cuenta corriente y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_actualizacccajagen` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_actualizacccajagen(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCentroC                     Char(4)
  pMontoMin                    FLOAT
  pMontoMax                    FLOAT
  pPlazaCG                     CHAR(3)
  pPlazaCB                     INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCentroC` | `Char(4)` | — | — |
| `pMontoMin` | `FLOAT` | — | — |
| `pMontoMax` | `FLOAT` | — | — |
| `pPlazaCG` | `CHAR(3)` | — | — |
| `pPlazaCB` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cStatus` | `CHAR(30)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |
| `sp_actualizacccajagen` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |

---

## `sp_cc_actualizasucursal1`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_actualizasucursal1.sql` |
| **LOC (1er CREATE)** | 225 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cc — cuenta corriente y sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_actualizasucursal1(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal_a                CHAR(4)
  pIdSucursal                  CHAR(4)
  pNomSucur                    CHAR(40)
  pTipo                        CHAR(1)
  pTpoSuc                      CHAR(2)
  pGerente                     CHAR(40)
  pPlaza                       CHAR(3)
  pSubGer                      CHAR(40)
  pTipoAcc                     CHAR(1)
  pTelex                       CHAR(20)
  pCorreo                      CHAR(120)
  pTel1                        CHAR(14)
  pTel2                        CHAR(14)
  pUserIns                     CHAR(30)
  pFechaIns                    DATE
  pServCanj                    CHAR(1)
  pDispBaja                    CHAR(1)
  pComRet                      DECIMAL(5,2)
  pComConsul                   DECIMAL(5,2)
  pTipoBovSuc                  CHAR(3)
  pHorario                     CHAR(60)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal_a` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pNomSucur` | `CHAR(40)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pTpoSuc` | `CHAR(2)` | — | — |
| `pGerente` | `CHAR(40)` | — | — |
| `pPlaza` | `CHAR(3)` | — | — |
| `pSubGer` | `CHAR(40)` | — | — |
| `pTipoAcc` | `CHAR(1)` | `cc`=cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO |
| `pTelex` | `CHAR(20)` | — | — |
| `pCorreo` | `CHAR(120)` | — | — |
| `pTel1` | `CHAR(14)` | — | — |
| `pTel2` | `CHAR(14)` | — | — |
| `pUserIns` | `CHAR(30)` | — | — |
| `pFechaIns` | `DATE` | — | — |
| `pServCanj` | `CHAR(1)` | — | — |
| `pDispBaja` | `CHAR(1)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pComRet` | `DECIMAL(5,2)` | — | — |
| `pComConsul` | `DECIMAL(5,2)` | — | — |
| `pTipoBovSuc` | `CHAR(3)` | — | — |
| `pHorario` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdSucursal` | `CHAR(4)` | L9 |
| `iTotal` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L45 |
| `si_ptf` | `bdinteg` | ⚠️ sí | SELECT | L66 |
| `si_ptf` | `bdinteg` | ⚠️ sí | INSERT | L70 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | UPDATE | L74 |
| `si_ptf` | `bdinteg` | ⚠️ sí | UPDATE | L89 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | INSERT | L166 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '01272';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '01272';` |  |
| L162 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?1` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?1` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_actualizasucursal2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_actualizasucursal2.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cc — cuenta corriente y sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_actualizasucursal2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal_a                CHAR(4)
  pIdSucursal                  CHAR(4)
  pCritCom                     CHAR(5)
  pCvePais                     CHAR(3)
  pCveEst                      CHAR(2)
  pCveCiu                      CHAR(3)
  pCp                          CHAR(5)
  pCveMun                      CHAR(3)
  pCveLocal                    CHAR(14)
  pCveCol                      CHAR(8)
  pLatitud                     CHAR(10)
  pLongitud                    CHAR(11)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal_a` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pCritCom` | `CHAR(5)` | — | — |
| `pCvePais` | `CHAR(3)` | — | — |
| `pCveEst` | `CHAR(2)` | — | — |
| `pCveCiu` | `CHAR(3)` | — | — |
| `pCp` | `CHAR(5)` | — | — |
| `pCveMun` | `CHAR(3)` | — | — |
| `pCveLocal` | `CHAR(14)` | — | — |
| `pCveCol` | `CHAR(8)` | — | — |
| `pLatitud` | `CHAR(10)` | — | — |
| `pLongitud` | `CHAR(11)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdSucursal` | `CHAR(4)` | L8 |
| `iTotal` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L49 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | UPDATE | L66 |
| `si_ptf` | `bdinteg` | ⚠️ sí | UPDATE | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '01272';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '01272';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_actualizasucursal3`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_actualizasucursal3.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cc — cuenta corriente y sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_actualizasucursal3(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal_a                CHAR(4)
  pIdSucursal                  CHAR(4)
  pCalle                       CHAR (100)
  pNumExt                      CHAR(6)
  pNumInt                      CHAR(5)
  pReferencia                  CHAR(120)
  pMonMin                      DECIMAL(10,2)
  pMonMinEfe                   MONEY(16,2)
  pMonMaxEfe                   MONEY(16,2)
  pMonMinSBC                   DECIMAL(14,2)
  pSalMinPlz                   DECIMAL(14,2)
  pDiasLab                     INTEGER
  pFactorRem                   DECIMAL(9,6)
  pFactorRemSBC                DECIMAL(10,5)
  pIva                         DECIMAL(5,3)
  pIdPlazaCG                   CHAR(3)
  pIdPlazaClabe                INTEGER
  pClaveSit                    CHAR(3)
  pFechaSit                    DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal_a` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pCalle` | `CHAR (100)` | — | — |
| `pNumExt` | `CHAR(6)` | — | — |
| `pNumInt` | `CHAR(5)` | — | — |
| `pReferencia` | `CHAR(120)` | — | — |
| `pMonMin` | `DECIMAL(10,2)` | — | — |
| `pMonMinEfe` | `MONEY(16,2)` | — | — |
| `pMonMaxEfe` | `MONEY(16,2)` | — | — |
| `pMonMinSBC` | `DECIMAL(14,2)` | — | — |
| `pSalMinPlz` | `DECIMAL(14,2)` | — | — |
| `pDiasLab` | `INTEGER` | — | — |
| `pFactorRem` | `DECIMAL(9,6)` | — | — |
| `pFactorRemSBC` | `DECIMAL(10,5)` | — | — |
| `pIva` | `DECIMAL(5,3)` | — | — |
| `pIdPlazaCG` | `CHAR(3)` | — | — |
| `pIdPlazaClabe` | `INTEGER` | — | — |
| `pClaveSit` | `CHAR(3)` | — | — |
| `pFechaSit` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdSucursal` | `CHAR(4)` | L9 |
| `iTotal` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L51 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | UPDATE | L68 |
| `si_ptf` | `bdinteg` | ⚠️ sí | UPDATE | L82 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '01272';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '01272';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?3` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?3` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_archdiariodepositoscoppel_repb2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_archdiariodepositoscoppel_repb2.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=13 / fan_out=43 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte cc — cuenta corriente, archivo, depósito, OS — Originación de Solicitudes y Coppel (diario)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=5 · SINTÉTICO=2 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_archdiariodepositoscoppel_repb2(
  pBandera                     CHAR(2)
  pFecha                       DATE
  pCuenta                      CHAR(20)
  pSucursal                    CHAR(4)
  pRutaDescarga                CHAR(100)
  pEmpresa                     CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNombreRep` | `CHAR(45)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `dFechaHoy` | `DATE` | L10 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L11 |
| `dHoy` | `DATE` | L12 |
| `cNombreReporteHist` | `CHAR(100)` | L13 |
| `ven_transacc` | `SMALLINT` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `referencia` | `CHAR(10)` | L16 |
| `cEmpresa` | `CHAR(5)` | L18 |
| `cCuenta` | `CHAR(20)` | L19 |
| `cFecha_alta` | `DATE` | L20 |
| `cForma` | `CHAR(4)` | L21 |
| `cFolio_suc` | `CHAR(16)` | L22 |
| `cMonto_ori` | `DECIMAL(16,2)` | L23 |
| `cSucursal` | `CHAR(4)` | L24 |
| `cFech_hor` | `DATETIME HOUR TO MINUTE` | L25 |
| `cDescripcion` | `CHAR(40)` | L26 |
| `cNum_chq` | `INTEGER` | L27 |
| `cMonto_tot` | `MONEY` | L28 |
| `iContador` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L96 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L103 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L85 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L109 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `diario` | MODIF | diario | 🔵 CONVENCIÓN | nombre_sp |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?b2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?it`, `?b2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_archdiariodepositoscoppelb2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_archdiariodepositoscoppelb2.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cc — cuenta corriente, archivo, depósito, OS — Originación de Solicitudes y Coppel (diario)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cc_archdiariodepositoscoppel_repb2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=5 · SINTÉTICO=2 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_archdiariodepositoscoppelb2(
  pBandera                     CHAR(2)
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pCuenta                      CHAR(15)
  pSucursal                    CHAR(4)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pCuenta` | `CHAR(15)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(5)` | L17 |
| `cCuenta` | `CHAR(20)` | L18 |
| `cFecha_alta` | `DATE` | L19 |
| `cForma` | `CHAR(4)` | L20 |
| `cFolio_suc` | `CHAR(16)` | L21 |
| `cMonto_ori` | `MONEY` | L22 |
| `cSucursal` | `CHAR(4)` | L23 |
| `cFech_hor` | `DATETIME HOUR TO MINUTE` | L24 |
| `cDescripcion` | `CHAR(40)` | L25 |
| `cNum_chq` | `INTEGER` | L26 |
| `cMonto_tot` | `MONEY` | L27 |
| `cNombreReporte` | `CHAR(100)` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L76 |
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L87 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L94 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |
| `sp_cc_archdiariodepositoscoppel_repb2` | `bdicnweb` | no | L104 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `diario` | MODIF | diario | 🔵 CONVENCIÓN | nombre_sp |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | 🔵 CONVENCIÓN | nombre_sp |
| `?b2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?it`, `?b2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consulta_gral_direccion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consulta_gral_direccion.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente y dirección (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consulta_gral_direccion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPais                        CHAR(3)
  pEstado                      CHAR (3)
  pCiudad                      CHAR (3)
  pNomCiudad                   CHAR(40)
  pCodigo                      CHAR(5)
  pMunicipio                   CHAR(5)
  pLocalidad                   CHAR(14)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPais` | `CHAR(3)` | — | — |
| `pEstado` | `CHAR (3)` | — | — |
| `pCiudad` | `CHAR (3)` | — | — |
| `pNomCiudad` | `CHAR(40)` | — | — |
| `pCodigo` | `CHAR(5)` | — | — |
| `pMunicipio` | `CHAR(5)` | — | — |
| `pLocalidad` | `CHAR(14)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cClave` | `CHAR(20)` | L8 |
| `cDescripcion` | `CHAR(100)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_gral_direcciones` | `bdicnweb` | no | SELECT | L48 |
| `sw_cons_gral_direcciones` | `bdicnweb` | no | DELETE | L48 |
| `sw_cons_gral_direcciones` | `bdicnweb` | no | INSERT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_consulta_gral_direccion` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L64 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `gral` | MODIF | general | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `direccion` | ENTIDAD | dirección | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cc_consultacajagen`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultacajagen.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultacajagen(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodPlaza` | `CHAR(3)` | L9 |
| `cNombre` | `CHAR(40)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plazas_cajagen` | `bdinteg` | ⚠️ sí | SELECT | L43 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |

---

## `sp_cc_consultacajagencombo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultacajagencombo.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente, caja y combo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultacajagencombo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodPlaza` | `CHAR(3)` | L9 |
| `cNombre` | `CHAR(40)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_gral_cajagen` | `bdicnweb` | no | SELECT | L49 |
| `sw_cons_gral_cajagen` | `bdicnweb` | no | DELETE | L49 |
| `sw_cons_gral_cajagen` | `bdicnweb` | no | INSERT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_consultacajagen` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cc_consultaimpre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultaimpre.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente y Impago — pago vencido o fallido; confirmado: n_impagos_consec" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultaimpre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodImp` | `CHAR(4)` | L9 |
| `cNombre` | `CHAR(60)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_gral_impresora` | `bdicnweb` | no | SELECT | L49 |
| `sw_cons_gral_impresora` | `bdicnweb` | no | DELETE | L49 |
| `si_impresoras` | `bdinteg` | ⚠️ sí | SELECT | L54 |
| `sw_cons_gral_impresora` | `bdicnweb` | no | INSERT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?re` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultanumvbparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultanumvbparam.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente, número y parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultanumvbparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bExitoso` | `BOOLEAN` | L7 |
| `cDescrip` | `CHAR(20)` | L8 |
| `cValor` | `CHAR(50)` | L9 |
| `iTotal` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_vbparam` | `bdinteg` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L53 | FÓRMULA | `LET iTotal = iTotal+1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?vb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?vb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultaperfil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultaperfil.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente y perfil de usuario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaperfil` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultaperfil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `ivPerfil` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_consultaperfil` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `perfil` | ENTIDAD | perfil de usuario | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cc_consultaplazaclabecc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultaplazaclabecc.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente, plaza y CLABE interbancaria" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaplazaclabecc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultaplazaclabecc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIdplazaClabe` | `INTEGER` | L9 |
| `cNomPlaza` | `CHAR(60)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `sp_consultaplazaclabecc` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet ='00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `plaza` | ENTIDAD | plaza (regional) | 🔵 CONVENCIÓN | nombre_sp |
| `clabe` | ENTIDAD | CLABE interbancaria | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |

---

## `sp_cc_consultaplazaclavecombo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultaplazaclavecombo.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente, plaza, clave y combo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultaplazaclavecombo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodPlaza` | `CHAR(3)` | L9 |
| `cNombre` | `CHAR(40)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_gral_plazas` | `bdicnweb` | no | SELECT | L49 |
| `sw_cons_gral_plazas` | `bdicnweb` | no | DELETE | L49 |
| `sw_cons_gral_plazas` | `bdicnweb` | no | INSERT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_consultaplazaclabe` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `plaza` | ENTIDAD | plaza (regional) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cc_consultaplazacmb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultaplazacmb.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente y plaza" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultaplazacmb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cClave` | `CHAR(3)` | L8 |
| `cDescripcion` | `CHAR(40)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `plaza` | ENTIDAD | plaza (regional) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?cmb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cmb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultarazsoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultarazsoc.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cc — cuenta corriente y Sistema Operativo Central" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultarazsoc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(30)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cRazon` | `CHAR(30)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?az` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?az` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultaregional`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultaregional.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cc — cuenta corriente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultaregional(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPlaza                       CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPlaza` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cRegional` | `CHAR(3)` | L9 |
| `cNombre` | `CHAR(40)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_regional` | `bdinteg` | ⚠️ sí | SELECT | L43 |
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?egional` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?egional` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultarepsucursales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultarepsucursales.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cc — cuenta corriente y sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultarepsucursales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cSucursal` | `CHAR(4)` | L20 |
| `cNombre` | `CHAR(40)` | L21 |
| `cCalle` | `CHAR(100)` | L22 |
| `cNumExt` | `CHAR(6)` | L23 |
| `cColonia` | `CHAR(100)` | L24 |
| `cCiudad` | `CHAR(60)` | L25 |
| `cCp` | `CHAR(5)` | L26 |
| `cEstado` | `CHAR(30)` | L27 |
| `cTel` | `CHAR(14)` | L28 |
| `cTipo` | `CHAR(20)` | L29 |
| `cTipoS` | `CHAR(30)` | L30 |
| `cResponsable` | `CHAR(80)` | L31 |
| `cActEst` | `CHAR(20)` | L32 |
| `iTotal` | `INTEGER` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L89 | FÓRMULA | `LET iTotal = iTotal+1;` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultarepsucursales_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultarepsucursales_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cc — cuenta corriente y sucursal (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultarepsucursales_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iTotal` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep`, `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultarutareporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultarutareporte.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cc — cuenta corriente y Tarjeta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultarutareporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cValor` | `CHAR(100)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?u` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?u`, `?e`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultasuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultasuc.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultasuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSuc                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSuc` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cEmpresa` | `CHAR(3)` | L29 |
| `cSucursal` | `CHAR(4)` | L30 |
| `cNombre` | `CHAR(40)` | L31 |
| `cTipo` | `CHAR(1)` | L32 |
| `cTipo_acceso` | `CHAR(1)` | L33 |
| `cTelex` | `CHAR(20)` | L34 |
| `cCorreo` | `CHAR(120)` | L35 |
| `cTel1` | `CHAR(14)` | L36 |
| `cTel2` | `CHAR(14)` | L37 |
| `cTipoS` | `CHAR(1)` | L38 |
| `cServCanj` | `CHAR(1)` | L39 |
| `cTpoSuc` | `CHAR(2)` | L40 |
| `cHorario` | `CHAR(12)` | L41 |
| `cDispensabaja` | `CHAR(1)` | L42 |
| `dComRetiro` | `DECIMAL(5,2)` | L43 |
| `dComConsulta` | `DECIMAL(5,2)` | L44 |
| `cComCrit` | `CHAR(5)` | L45 |
| `cTipoBovsuc` | `CHAR(3)` | L46 |
| `cUserInsert` | `CHAR(30)` | L47 |
| `dFechaInsert` | `DATE` | L48 |
| `cDir` | `CHAR(40)` | L49 |
| `cRazon` | `CHAR(30)` | L50 |
| `ivPerfil` | `INTEGER` | L51 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L110 |
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L120 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L97 |
| `sp_consultaperfil` | `bdinteg` | ⚠️ sí | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |

---

## `sp_cc_consultasucactest`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultasucactest.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultasucactest(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSuc                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSuc` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cClaveSit` | `CHAR(3)` | L8 |
| `dFechaSit` | `DATE` | L9 |
| `ivPerfil` | `INTEGER` | L10 |
| `iExiste` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_consultaperfil` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?est` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?est` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultasucelim`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultasucelim.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultasucelim(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_feriadsuc` | `bdinteg` | ⚠️ sí | SELECT | L41 |
| `si_sucgpo` | `bdinteg` | ⚠️ sí | SELECT | L47 |
| `si_tcsuc` | `bdinteg` | ⚠️ sí | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET iTotal = iTotal+iExiste;` |  |
| L50 | FÓRMULA | `LET iTotal = iTotal+iExiste;` |  |
| L56 | FÓRMULA | `LET iTotal = iTotal+iExiste;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `?elim` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?elim` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultasucfunc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultasucfunc.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultasucfunc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSuc                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSuc` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cGerente` | `CHAR(40)` | L8 |
| `cSubGer` | `CHAR(40)` | L9 |
| `ivPerfil` | `INTEGER` | L10 |
| `iExiste` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_consultaperfil` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `?func` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?func` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultasucparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultasucparam.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente y parámetro (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultasucparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSuc                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSuc` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `dmontomin` | `DECIMAL(10,2)` | L18 |
| `mmontominefe` | `MONEY(16,2)` | L19 |
| `mmontomaxefe` | `MONEY(16,2)` | L20 |
| `dmonminsbc` | `DECIMAL(14,2)` | L21 |
| `dsalminplaza` | `DECIMAL(14,2)` | L22 |
| `idiaslab` | `INTEGER` | L23 |
| `dfactorrem` | `DECIMAL(9,2)` | L24 |
| `dfactorremsbc` | `DECIMAL(10,2)` | L25 |
| `diva` | `DECIMAL(5,2)` | L26 |
| `cplazacg` | `CHAR(3)` | L27 |
| `iplazaclabe` | `INTEGER` | L28 |
| `ivPerfil` | `INTEGER` | L29 |
| `iExiste` | `INTEGER` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |
| `sp_consultaperfil` | `bdinteg` | ⚠️ sí | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cc_consultasucubica`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultasucubica.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultasucubica(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSuc                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSuc` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `ccvepais` | `CHAR(3)` | L21 |
| `ccveestado` | `CHAR(2)` | L22 |
| `ccveciudad` | `CHAR(3)` | L23 |
| `ccp` | `CHAR(5)` | L24 |
| `cplaza` | `CHAR(3)` | L25 |
| `ccvemunicipio` | `CHAR(3)` | L26 |
| `ccvelocalidad` | `CHAR(14)` | L27 |
| `ccvecolonia` | `CHAR(8)` | L28 |
| `clatitud` | `CHAR(10)` | L29 |
| `clongitud` | `CHAR(11)` | L30 |
| `ccalle` | `CHAR(100)` | L31 |
| `cnumext` | `CHAR(6)` | L32 |
| `cnumint` | `CHAR(5)` | L33 |
| `creferencia` | `CHAR(120)` | L34 |
| `ivPerfil` | `INTEGER` | L35 |
| `iExiste` | `INTEGER` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L86 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |
| `sp_consultaperfil` | `bdinteg` | ⚠️ sí | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `?ubica` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ubica` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_consultatiposuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultatiposuc.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente (tipo de, sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultatiposuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cTipo` | `CHAR(1)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |

---

## `sp_cc_consultavbparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_consultavbparam.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente y parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_consultavbparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cDescrip` | `CHAR(20)` | L8 |
| `cValor` | `CHAR(50)` | L9 |
| `iTotal` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_vbparam` | `bdinteg` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iTotal = iTotal +1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?vb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?vb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_eliminasuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_eliminasuc.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina cc — cuenta corriente (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_eliminasuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `suc`=sucursal | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L34 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | DELETE | L34 |
| `si_ptf` | `bdinteg` | ⚠️ sí | SELECT | L37 |
| `si_ptf` | `bdinteg` | ⚠️ sí | DELETE | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L26 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |

---

## `sp_cc_libracionsalvobuencobro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_libracionsalvobuencobro.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cobro cc — cuenta corriente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 7 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ope_diasret`, `sp_ope_liberasalret` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_libracionsalvobuencobro(
  pBandera                     CHAR(2)
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDiaslib                     CHAR(6)
  pFechaReporte                CHAR(10)
  pPassword                    CHAR(40)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(6)       AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDiaslib` | `CHAR(6)` | — | — |
| `pFechaReporte` | `CHAR(10)` | — | — |
| `pPassword` | `CHAR(40)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L17 |
| `cCodRet` | `CHAR(6)` | L18 |
| `iTotalRegistros` | `INTEGER` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `cSucursal` | `CHAR(4)` | L22 |
| `dFechaAlta` | `DATE` | L23 |
| `cTransaccion` | `CHAR(4)` | L24 |
| `cReferencia` | `CHAR(40)` | L25 |
| `iNumCheque` | `INTEGER` | L26 |
| `sDiasOri` | `SMALLINT` | L27 |
| `mMontoOri` | `MONEY(14,2)` | L28 |
| `cSiglas` | `CHAR (2)` | L29 |
| `cBanco` | `CHAR(40)` | L30 |
| `cFecharep` | `CHAR(10)` | L31 |
| `cStatus` | `CHAR(1)` | L32 |
| `cErrorProceso` | `CHAR(1)` | L33 |
| `cError` | `CHAR(5)` | L34 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |
| `sp_ope_diasret` | `bdicnweb` | no | L81 |
| `sp_ope_liberasalret` | `bdicnweb` | no | L87 |
| `sp_ope_reportesbc_totales` | `bdicnweb` | no | L93 |
| `sp_ope_reportesbc` | `bdicnweb` | no | L100 |
| `sp_ope_validapassword` | `bdicnweb` | no | L108 |
| `sp_verificastatusliberaret` | `bdicnweb` | no | L114 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?_libracionsalvobuen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cobro` | ACCION | cobro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_libracionsalvobuen` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_recsiparamptf`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_recsiparamptf.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recepción cc — cuenta corriente y parámetro" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_recsi_paramptf` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_recsiparamptf(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pParametro                   CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pParametro` | `CHAR(1)` | `param`=parámetro | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cDesparam` | `CHAR(100)` | L9 |
| `cValorparam` | `CHAR(4)` | L10 |
| `iTotal` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_recsi_paramptf` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | FÓRMULA | `LET iTotal =iTotal+1;` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?si` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `?ptf` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?si`, `?ptf` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cc_repsucursales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cc_repsucursales.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte cc — cuenta corriente y sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cc_repsucursales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(2000)` | L7 |
| `cSql` | `CHAR(2500)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(15)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET cCmd1 ="SELECT * FROM (SELECT 'NUMERO CENTRO','NOMBRE CENTRO','CALLE','NO. EXTERIOR','COLONIA','` |  |
| L66 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM (select a.sucursal,TRIM(a.nombre),  UPPER( NVL(TRIM ` |  |
| L78 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L109 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L114 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L123 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L132 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cce_consultausuariosaut`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cce_consultausuariosaut.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=25 / fan_out=23 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CCE — Cámara de Compensación Electrónica y usuarios" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cce_consultausuariosaut(
  pNumEjecut                   INTEGER
) RETURNING CHAR(5)	AS CodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumEjecut` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSql_Err` | `INTEGER` | L7 |
| `iSam_Err` | `INTEGER` | L8 |
| `cDesc_Err` | `CHAR(60)` | L9 |
| `iEjecutivo` | `INTEGER` | L10 |
| `vNombreEjecut` | `VARCHAR(100)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_usuarios_revision` | `bditef` | ⚠️ sí | SELECT | L41 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |

---

## `sp_ccep_eliminacheques_cod46`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccep_eliminacheques_cod46.sql` |
| **LOC (1er CREATE)** | 200 |
| **Callgraph** | ✅ fan_in=78 / fan_out=37 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina CCE — Cámara de Compensación Electrónica, cheques y código" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `bdinteg`, `sp_split_cadena`, `bditef` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccep_eliminacheques_cod46(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
  pIdsEliminar                 CHAR(500)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pIdsEliminar` | `CHAR(500)` | `elimina`=elimina | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iIddetallechq` | `INTEGER` | L10 |
| `cBanco` | `CHAR(3)` | L11 |
| `cDesc_banco` | `CHAR(40)` | L12 |
| `cCtareferencia` | `CHAR(14)` | L13 |
| `iNum_cheque` | `INTEGER` | L14 |
| `dMonto_orig` | `DECIMAL(14,2)` | L15 |
| `cNum_cuentadep` | `CHAR(20)` | L16 |
| `cSi_transacc` | `CHAR(4)` | L17 |
| `cAplica` | `CHAR(2)` | L18 |
| `cMotivo_dev` | `CHAR(37)` | L19 |
| `cDigitalizado` | `CHAR(1)` | L20 |
| `cCompensacion` | `CHAR(3)` | L21 |
| `cTransacc` | `CHAR(2)` | L22 |
| `cCodseguridad` | `CHAR(3)` | L23 |
| `cDigverpre` | `CHAR(1)` | L24 |
| `cDigverinter` | `CHAR(1)` | L25 |
| `cTipo_cta_dep` | `CHAR(2)` | L26 |
| `cNombreBen` | `CHAR(60)` | L27 |
| `cRfcCte` | `CHAR(13)` | L28 |
| `cCurpCte` | `CHAR(20)` | L29 |
| *…15 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L118 |
| `sp_split_cadena` | `bdicnweb` | no | L136 |
| `bditef` | `bdicnweb` | no | L174 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L180 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cce_chequesrevisados';` |  |
| L182 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | 🔵 CONVENCIÓN | nombre_sp |
| `?p_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cod` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?46` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p_`, `?46` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ccl_actintisrxprodcedula`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_actintisrxprodcedula.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, interés, producto y cédula de identificación · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_actintisrxprodcedula` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=3 · SINTÉTICO=0 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_actintisrxprodcedula(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pProducto                    CHAR(4)
  pObservaciones               CHAR(255)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pProducto` | `CHAR(4)` | `prod`=producto | 🟡 INFERIDO |
| `pObservaciones` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_actintisrxprodcedula` | `bdicheq` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pProducto = '' THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_actintisrxprodcedula";` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `cedula` | ENTIDAD | cédula de identificación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ccl_conciliadorautoinvcreciente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_conciliadorautoinvcreciente.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 244 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, conciliadora, inv — inversión y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=4 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_conciliadorautoinvcreciente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pTotalRegistros              INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTotalRegistros` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |
| `dFecha` | `DATE` | L24 |
| `cNumCliente` | `CHAR(20)` | L25 |
| `cCuentaEje` | `CHAR(20)` | L26 |
| `cTransCtaEje` | `CHAR(4)` | L27 |
| `cDescCtaEje` | `CHAR(50)` | L28 |
| `cCtaCargoCtaEje` | `CHAR(14)` | L29 |
| `cCtaAbonoCtaEje` | `CHAR(14)` | L30 |
| `mImporteCtaEje` | `MONEY(18,2)` | L31 |
| `cInversCrecte` | `CHAR(20)` | L32 |
| `cTransInversCrecte` | `CHAR(4)` | L33 |
| `cDescInversCrecte` | `CHAR(50)` | L34 |
| `cCtaCargoInversCrecte` | `CHAR(14)` | L35 |
| `mMontoCargo` | `MONEY(18,2)` | L36 |
| `cCtaAbonoInversCrecte` | `CHAR(14)` | L37 |
| `mMontoAbono` | `MONEY(18,2)` | L38 |
| `mImporteInversCrecte` | `MONEY(18,2)` | L39 |
| `mDiferencia` | `MONEY(18,2)` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_concilia_transacc` | `bdicheq` | ⚠️ sí | SELECT | L91 |
| `sc_diferencia_concilia` | `bdicheq` | ⚠️ sí | SELECT | L107 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL  THEN` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L112 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `conciliadora` | ENTIDAD | conciliadora | 🟡 INFERIDO | nombre_sp |
| `?uto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inv` | ENTIDAD | inv — inversión (abreviación — calsdoinvcrec, cierrechqinvcr | 🟡 INFERIDO | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `?ciente` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uto`, `?ciente` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ccl_conciliadorautoinvcreciente_genrep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_conciliadorautoinvcreciente_genrep.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 243 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, conciliadora, inv — inversión y crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=5 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_conciliadorautoinvcreciente_genrep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pFechaReporte                CHAR(8)
  pRutaDescarga                CHAR(150)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pFechaReporte` | `CHAR(8)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(3000)` | L7 |
| `cArchSinDif` | `CHAR(150)` | L8 |
| `cArchConDif` | `CHAR(150)` | L9 |
| `cArchDescarga` | `CHAR(150)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `mMontoCargo` | `CHAR(22)` | L12 |
| `mMontoAbono` | `CHAR(22)` | L13 |
| `mImporteInversCrecte` | `CHAR(20)` | L14 |
| `mImporteDiferencia` | `CHAR(20)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_concilia_transacc` | `bdicheq` | ⚠️ sí | SELECT | L54 |
| `systables` | `bdicnweb` | no | SELECT | L65 |
| `sc_diferencia_concilia` | `bdicheq` | ⚠️ sí | SELECT | L83 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pFechaReporte = ` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `conciliadora` | ENTIDAD | conciliadora | 🟡 INFERIDO | nombre_sp |
| `?uto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inv` | ENTIDAD | inv — inversión (abreviación — calsdoinvcrec, cierrechqinvcr | 🟡 INFERIDO | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `?ciente_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uto`, `?ciente_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ccl_conciliadorautoinvcreciente_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_conciliadorautoinvcreciente_totales.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 242 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, conciliadora, inv — inversión y crédito (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=4 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_conciliadorautoinvcreciente_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `mMontoCargo` | `MONEY(20,2)` | L12 |
| `mMontoAbono` | `MONEY(20,2)` | L13 |
| `mImporteInversCrecte` | `MONEY(18,2)` | L14 |
| `mImporteDiferencia` | `MONEY(18,2)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_concilia_transacc` | `bdicheq` | ⚠️ sí | SELECT | L51 |
| `sc_diferencia_concilia` | `bdicheq` | ⚠️ sí | SELECT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL  THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `conciliadora` | ENTIDAD | conciliadora | 🟡 INFERIDO | nombre_sp |
| `?uto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inv` | ENTIDAD | inv — inversión (abreviación — calsdoinvcrec, cierrechqinvcr | 🟡 INFERIDO | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `?ciente_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uto`, `?ciente_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ccl_consaldosdiariospagare`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_consaldosdiariospagare.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, OS — Originación de Solicitudes y pagaré (diarios)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_consaldosdiariospagare(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |
| `dFecha` | `DATE` | L17 |
| `cSucursal` | `CHAR (4)` | L18 |
| `cCuenta` | `CHAR (20)` | L19 |
| `cNumCte` | `CHAR (20)` | L20 |
| `dFechCap` | `DATE` | L21 |
| `dCapital` | `DECIMAL (18,2)` | L22 |
| `dInteres` | `DECIMAL (18,2)` | L23 |
| `sSecuencia` | `SMALLINT` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_sdosdiarios` | `bdinvers` | ⚠️ sí | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS N` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ald` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `diarios` | MODIF | diarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pagare` | ENTIDAD | pagaré | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ald` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ccl_consaldosdiariospagare_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_consaldosdiariospagare_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, OS — Originación de Solicitudes y pagaré (diarios, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_consaldosdiariospagare_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_sdosdiarios` | `bdinvers` | ⚠️ sí | SELECT | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL  THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ald` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `diarios` | MODIF | diarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pagare` | ENTIDAD | pagaré | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ald` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ccl_consintisrxprodcedula`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_consintisrxprodcedula.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, interés, producto y cédula de identificación · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consintisrxprodcedula2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=3 · SINTÉTICO=0 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_consintisrxprodcedula(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfechaCedula                 DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfechaCedula` | `DATE` | `cedula`=cédula de identificación | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `dFecha` | `DATE` | L19 |
| `cProducto` | `CHAR(4)` | L20 |
| `cNombre` | `CHAR(40)` | L21 |
| `mInteresCalc` | `DECIMAL(18,2)` | L22 |
| `mInteresPag` | `DECIMAL(18,2)` | L23 |
| `mDifInteres` | `DECIMAL(18,2)` | L24 |
| `mIsrCalc` | `DECIMAL(18,2)` | L25 |
| `mIsrPagado` | `DECIMAL(18,2)` | L26 |
| `mDifIsr` | `DECIMAL(18,2)` | L27 |
| `cObservaciones` | `CHAR(255)` | L28 |
| `cEditable` | `CHAR(1)` | L29 |
| `iNoRegistros` | `INTEGER` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |
| `sp_consintisrxprodcedula2` | `bdicheq` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L59 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pfechaCedula IS NULL OR pRegistros IS NULL OR pRecuperacion I` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_consintisrxprodcedula2";` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L88 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `cedula` | ENTIDAD | cédula de identificación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ccl_consintisrxprodcedula_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_consintisrxprodcedula_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, interés, producto y cédula de identificación (totales) · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consintisrxprodcedula2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=3 · SINTÉTICO=0 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_consintisrxprodcedula_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConciliacion           DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConciliacion` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_consintisrxprodcedula2_totales` | `bdicheq` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConciliacion IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_consintisrxprodcedula2_totale` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `cedula` | ENTIDAD | cédula de identificación | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ccl_consintisrxproddetalle`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_consintisrxproddetalle.sql` |
| **LOC (1er CREATE)** | 119 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, interés, producto y detalle · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consintisrxproddetalle2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=3 · SINTÉTICO=0 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_consintisrxproddetalle(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfechaCedula                 DATE
  pProducto                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfechaCedula` | `DATE` | — | — |
| `pProducto` | `CHAR(4)` | `prod`=producto | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(5)` | L20 |
| `iCodRetSp` | `INTEGER` | L21 |
| `dFecha` | `DATE` | L22 |
| `cProducto` | `CHAR(4)` | L23 |
| `cNombre` | `CHAR(40)` | L24 |
| `cCuenta` | `CHAR(20)` | L25 |
| `cCliente` | `CHAR(20)` | L26 |
| `mSdoPromedio` | `DECIMAL(18,2)` | L27 |
| `iDias` | `SMALLINT` | L28 |
| `dTasa` | `DECIMAL(9,6)` | L29 |
| `mInteresCalc` | `DECIMAL(18,2)` | L30 |
| `mInteresPag` | `DECIMAL(18,2)` | L31 |
| `mDifInteres` | `DECIMAL(18,2)` | L32 |
| `mIsrCalc` | `DECIMAL(18,2)` | L33 |
| `mIsrPagado` | `DECIMAL(18,2)` | L34 |
| `mDifIsr` | `DECIMAL(18,2)` | L35 |
| `iNoRegistros` | `INTEGER` | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L80 |
| `sp_consintisrxproddetalle2` | `bdicheq` | ⚠️ sí | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L68 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pfechaCedula IS NULL OR pRegistros IS NULL OR pRecuperacion I` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L91 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_consintisrxproddetalle2";` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L97 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ccl_consintisrxproddetalle_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_consintisrxproddetalle_totales.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, interés, producto y detalle (totales) · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consintisrxproddetalle2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=3 · SINTÉTICO=0 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_consintisrxproddetalle_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConciliacion           DATE
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConciliacion` | `DATE` | — | — |
| `pProducto` | `CHAR(4)` | `prod`=producto | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_consintisrxproddetalle2_totales` | `bdicheq` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConciliacion IS NULL OR pProducto = '' THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_consintisrxproddetalle2_total` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ccl_consultainteresisr`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_consultainteresisr.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente y interés · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consintisrxprod2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_consultainteresisr(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfechaCedula                 DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfechaCedula` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `dFecha` | `DATE` | L17 |
| `cProducto` | `CHAR(4)` | L18 |
| `cNombre` | `CHAR(40)` | L19 |
| `mInteresCalc` | `DECIMAL(18,2)` | L20 |
| `mInteresPag` | `DECIMAL(18,2)` | L21 |
| `mDifInteres` | `DECIMAL(18,2)` | L22 |
| `mIsrCalc` | `DECIMAL(18,2)` | L23 |
| `mIsrPagado` | `DECIMAL(18,2)` | L24 |
| `mDifIsr` | `DECIMAL(18,2)` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |
| `sp_consintisrxprod2` | `bdicheq` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pfechaCedula IS NULL OR pRegistros IS NULL OR pRecuperacion I` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L76 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_consintisrxprod2";` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L82 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `interes` | ENTIDAD | interés | 🔵 CONVENCIÓN | nombre_sp |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ccl_consultainteresisr_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_consultainteresisr_totales.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente y interés (totales) · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consintisrxprod2_totales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_consultainteresisr_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConciliacion           DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConciliacion` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_consintisrxprod2_totales` | `bdicheq` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConciliacion IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_consintisrxprod2_totales";` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `interes` | ENTIDAD | interés | 🔵 CONVENCIÓN | nombre_sp |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ccl_finalizacedulainterescap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ccl_finalizacedulainterescap.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente, cédula de identificación, interés y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (final)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_finintisrxprodcedula` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ccl_finalizacedulainterescap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |
| `sp_finintisrxprodcedula` | `bdicheq` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_finintisrxprodcedula";` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `final` | MODIF | final | 🔵 CONVENCIÓN | nombre_sp |
| `?iza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cedula` | ENTIDAD | cédula de identificación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `interes` | ENTIDAD | interés | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iza` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cedulacontablenombre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cedulacontablenombre.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 403 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cédula contable y nombre" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_cedulacontablenombre(
  pEmpresa                     CHAR(3)
) RETURNING CHAR(5), SMALLINT, CHAR(40)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `iTipoCedula` | `SMALLINT` | L11 |
| `cNombre` | `CHAR(40)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontablenombre2` | `bdicheq` | ⚠️ sí | SELECT | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | VALIDACIÓN_NULL | `IF ( pEmpresa is null OR pEmpresa <> '001' ) THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cedulacontable` | ENTIDAD | cédula contable | 🟡 INFERIDO | nombre_sp |
| `nombre` | ENTIDAD | nombre | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cedulacontableroles`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cedulacontableroles.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "cédula contable y rol" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cedulacontableroles(
  pEmpresa                     CHAR(3)
) RETURNING CHAR(5), SMALLINT, CHAR(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `iTipoRol` | `SMALLINT` | L11 |
| `cNombre` | `CHAR(10)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontableroles` | `bdicheq` | ⚠️ sí | SELECT | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | VALIDACIÓN_NULL | `IF ( pEmpresa is null OR pEmpresa <> '001' ) THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cedulacontable` | ENTIDAD | cédula contable | 🟡 INFERIDO | nombre_sp |
| `rol` | ENTIDAD | rol / perfil | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_actbancont`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_actbancont.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 89 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cg — Canal/Cuenta General y banco" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_actbancont` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_actbancont(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pBandera                     INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pBandera` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iRecuperacion` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `sp_actbancont` | `bdispei` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pBandera IS NULL THEN` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdispei:sp_actbancont';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `banco` | ENTIDAD | banco | 🔵 CONVENCIÓN | nombre_sp |
| `?nt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?nt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_activaetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_activaetv.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "activa cg — Canal/Cuenta General" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_activaetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripcionEtv              CHAR(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripcionEtv` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iRecuperacion` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_mant_etv` | `bdisuc` | ⚠️ sí | INSERT | L47 |
| `ss_catalago_etv` | `bdisuc` | ⚠️ sí | SELECT | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `activa` | ACCION | activa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_actsolicitudes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_actsolicitudes.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cg — Canal/Cuenta General y solicitudes" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_actsolicitudes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pFolio                       CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFolio` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `cTipoOperacion` | `CHAR(25)` | L7 |
| `cDescActualiza` | `CHAR(20)` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |
| `cStatus` | `CHAR(2)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | UPDATE | L45 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | UPDATE | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `solicitudes` | ENTIDAD | solicitudes (plural) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_actualizastatusmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_actualizastatusmasivo.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 213 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cg — Canal/Cuenta General y estatus (masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_actualizastatusmasivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pIdRegistros                 VARCHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pIdRegistros` | `VARCHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `iExitosos` | `INT` | L9 |
| `iFracasos` | `INT` | L10 |
| `iCount` | `INT` | L11 |
| `iStart` | `INT` | L12 |
| `iLength` | `INT` | L13 |
| `iRow` | `INT` | L14 |
| `cBaseDatos` | `CHAR(50)` | L15 |
| `cTablaDst` | `CHAR(50)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_info_tablas` | `bdicnweb` | no | SELECT | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | VALIDACIÓN_NULL | `IF cBaseDatos IS NULL OR cBaseDatos = '' THEN` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |
| L75 | FÓRMULA | `LET iRow = SUBSTR(pIdRegistros, iStart, iCount - iStart);` |  |
| L80 | FÓRMULA | `LET iExitosos = iExitosos + 1;` |  |
| L82 | FÓRMULA | `LET iFracasos = iFracasos + 1;` |  |
| L84 | FÓRMULA | `LET iStart = iCount + 1;` |  |
| L93 | FÓRMULA | `LET iExitosos = iExitosos + 1;` |  |
| L95 | FÓRMULA | `LET iFracasos = iFracasos + 1;` |  |
| L97 | FÓRMULA | `LET iStart = iCount + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_actualizatipoconcentracion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_actualizatipoconcentracion.sql` |
| **LOC (1er CREATE)** | 172 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cg — Canal/Cuenta General (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_actualizatipoconcentracion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecucion                   CHAR(1)
  pConcentracion               CHAR(10)
  pTrama                       CHAR(250)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecucion` | `CHAR(1)` | — | — |
| `pConcentracion` | `CHAR(10)` | `concentracion`=concentración de fondos | ✅ CÓDIGO |
| `pTrama` | `CHAR(250)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRet` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cEstatus` | `CHAR(1)` | L10 |
| `cConcentracion` | `CHAR(10)` | L11 |
| `iRowId` | `INTEGER` | L12 |
| `cNo_sucursal_atm` | `CHAR(4)` | L13 |
| `cNombre_sucursal_atm` | `CHAR(40)` | L14 |
| `cNoSucursal` | `CHAR(4)` | L15 |
| `cNoAtm` | `CHAR(6)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_tipo_concentracion_etv` | `bdisuc` | ⚠️ sí | SELECT | L81 |
| `ss_bitacora_tipo_concentracion` | `bdisuc` | ⚠️ sí | INSERT | L106 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_split_cadena` | `bdicnweb` | no | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L139 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L145 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_actualizatransacciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_actualizatransacciones.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza cg — Canal/Cuenta General y transacción" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_actualizatransacciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdActualiza                 CHAR(1)
  pSucursal                    CHAR(4)
  pFecha                       DATE
  pFolio                       CHAR(8)
  pOperacion                   CHAR(4)
  pMonto                       MONEY(16,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdActualiza` | `CHAR(1)` | `actualiza`=actualiza | 🔵 CONVENCIÓN |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pFolio` | `CHAR(8)` | — | — |
| `pOperacion` | `CHAR(4)` | — | — |
| `pMonto` | `MONEY(16,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cTipoOperacion` | `CHAR(25)` | L8 |
| `cDescActualiza` | `CHAR(20)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cStatus` | `CHAR(2)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | UPDATE | L48 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | UPDATE | L56 |
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L73 |
| `ss_bitacora_reversoscg` | `bdisuc` | ⚠️ sí | INSERT | L115 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdActualiza = '' OR pSucursal = '' OR pFecha IS NULL OR pFol` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_altaccetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_altaccetv.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta cg — Canal/Cuenta General y CCE — Cámara de Compensación Electrónica" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_altaccetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripcionEtv              CHAR(40)
  pCodProveedor                CHAR(4)
  pCentroCostosEtv             CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripcionEtv` | `CHAR(40)` | — | — |
| `pCodProveedor` | `CHAR(4)` | — | — |
| `pCentroCostosEtv` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iRecuperacion` | `INTEGER` | L7 |
| `cDescripcionCC` | `CHAR(30)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L44 |
| `ss_relacion_cc_etvs` | `bdisuc` | ⚠️ sí | SELECT | L47 |
| `ss_relacion_cc_etvs` | `bdisuc` | ⚠️ sí | INSERT | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | 🔵 CONVENCIÓN | nombre_sp |
| `?tv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?tv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_altamodificacion_piezas_bym`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_altamodificacion_piezas_bym.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta cg — Canal/Cuenta General, piezas de efectivo y Billetes y Monedas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_altamodificacion_piezas_bym(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      CHAR(1)
  pIdDenominacion              INTEGER
  pNumRecibo                   CHAR(10)
  pTipoPieza                   CHAR(1)
  pSerie                       CHAR(40)
  pFolio                       CHAR(40)
  pFechaEmision                DATE
  pNumPiezas                   INTEGER
  pNota                        CHAR(200)
  pNumGuia                     CHAR(12)
  pFolioBanxico                CHAR(40)
  pDictamenBanxico             INTEGER
  pNumLoteBanxico              CHAR(40)
  pEstatus                     INTEGER
  pIdPieza                     INTEGER
  pTrama                       CHAR(500)
) RETURNING CHAR(5) AS CodRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pIdDenominacion` | `INTEGER` | — | — |
| `pNumRecibo` | `CHAR(10)` | — | — |
| `pTipoPieza` | `CHAR(1)` | — | — |
| `pSerie` | `CHAR(40)` | — | — |
| `pFolio` | `CHAR(40)` | — | — |
| `pFechaEmision` | `DATE` | — | — |
| `pNumPiezas` | `INTEGER` | `piezas`=piezas de efectivo (billetes y monedas) | 🔵 CONVENCIÓN |
| `pNota` | `CHAR(200)` | — | — |
| `pNumGuia` | `CHAR(12)` | — | — |
| `pFolioBanxico` | `CHAR(40)` | — | — |
| `pDictamenBanxico` | `INTEGER` | — | — |
| `pNumLoteBanxico` | `CHAR(40)` | — | — |
| `pEstatus` | `INTEGER` | — | — |
| `pIdPieza` | `INTEGER` | — | — |
| `pTrama` | `CHAR(500)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L4 |
| `iSamErr` | `INTEGER` | L5 |
| `cDesErr` | `CHAR(80)` | L6 |
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cMensaje` | `CHAR(80)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `cFolio` | `INTEGER` | L13 |
| `cNumRecibo` | `CHAR(10)` | L14 |
| `dFechaEmision` | `DATE` | L15 |
| `iNumPiezas` | `INTEGER` | L16 |
| `iCvePieza` | `INTEGER` | L17 |
| `iDictamen` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_billetesfalsos` | `bdicnweb` | no | SELECT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_split_cadena` | `bdicnweb` | no | L63 |
| `sp_altamodificacion_piezas_bym` | `bdisuc` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet                 = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_altamodificacion_piez` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00492';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L107 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_altamodificacion_piez` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00492';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `modificacion` | ACCION | modificación | 🔵 CONVENCIÓN | nombre_sp |
| `piezas` | ENTIDAD | piezas de efectivo (billetes y monedas) | 🔵 CONVENCIÓN | nombre_sp |
| `bym` | ENTIDAD | Billetes y Monedas (efectivo en sucursal — evidencia: 'pieza | 🟡 INFERIDO | nombre_sp |

---

## `sp_cg_bajaccetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_bajaccetv.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General y CCE — Cámara de Compensación Electrónica (de baja)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_bajaccetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBajaCcEtv                   CHAR(250)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBajaCcEtv` | `CHAR(250)` | `baja`=de baja · `cce`=CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — sistema de compensación interbancaria de cheques; SPs: sp_cce_consultar_cheques40/46, chequespresentados (bdicheq) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCadenaValor` | `CHAR(40)` | L7 |
| `iNoRegsProcesados` | `INTEGER` | L8 |
| `bInTransaction` | `BOOLEAN` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_split_cadena` | `bdicnweb` | no | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `baja` | MODIF | de baja | 🔵 CONVENCIÓN | nombre_sp |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | 🔵 CONVENCIÓN | nombre_sp |
| `?tv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?tv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_bajaetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_bajaetv.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 65 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General (de baja)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_bajaetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEtvBaja                     CHAR(250)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEtvBaja` | `CHAR(250)` | `baja`=de baja | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCadenaValor` | `CHAR(40)` | L7 |
| `iNoRegsProcesados` | `INTEGER` | L8 |
| `bInTransaction` | `BOOLEAN` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_mant_etv` | `bdisuc` | ⚠️ sí | INSERT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_split_cadena` | `bdicnweb` | no | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `baja` | MODIF | de baja | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_bitacoraerroreslibro1`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_bitacoraerroreslibro1.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, bitácora, error y libro mayor" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_bitacoraerroreslibro1(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTramaErrores                CHAR(250)
  pSucursal                    CHAR(4)
  pTramaEnviada                CHAR(600)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTramaErrores` | `CHAR(250)` | `error`=error | 🔵 CONVENCIÓN |
| `pSucursal` | `CHAR(4)` | — | — |
| `pTramaEnviada` | `CHAR(600)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodError` | `CHAR(3)` | L6 |
| `cDescError` | `CHAR(100)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cat_errores_panamericano` | `bdicnweb` | no | SELECT | L45 |
| `ss_bitacora_envio_libro` | `bdisuc` | ⚠️ sí | INSERT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_split_cadena` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `libro` | ENTIDAD | libro mayor / libro contable — general ledger (libromayor_di | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?1` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?es`, `?1` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_bitacoraerrorespanamericano`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_bitacoraerrorespanamericano.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, bitácora y error (especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_bitacoraerrorespanamericano(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTramaErrores                CHAR(250)
  pMetodoServicio              CHAR(40)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTramaErrores` | `CHAR(250)` | `error`=error | 🔵 CONVENCIÓN |
| `pMetodoServicio` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodError` | `CHAR(3)` | L6 |
| `cDescError` | `CHAR(100)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cat_errores_panamericano` | `bdicnweb` | no | SELECT | L43 |
| `sw_cg_bitacoraerrorwspanamericano` | `bdicnweb` | no | INSERT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_split_cadena` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?anamericano` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?anamericano` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_cat_atmsuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_cat_atmsuc.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, catálogo y cajeros automáticos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_cat_atmsuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cNoAtmSuc` | `CHAR(5)` | L9 |
| `cDescAtmSuc` | `CHAR(100)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cat_atm_sucursal` | `bdicnweb` | no | SELECT | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cat` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `?uc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?uc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_catatmsuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_catatmsuc.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, catálogo y cajeros automáticos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_catatmsuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecucion                   CHAR(1)
  pCg                          CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecucion` | `CHAR(1)` | — | — |
| `pCg` | `CHAR(4)` | `cg`=cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNoAtmSuc` | `CHAR(10)` | L12 |
| `cDescAtmSuc` | `CHAR(40)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_tipo_concentracion_etv` | `bdisuc` | ⚠️ sí | SELECT | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEjecucion = '' OR pRegistros IS NULL OR pRecuperacion IS NUL` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L70 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L82 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L97 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L109 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `?uc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?uc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_cattipoconcentracion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_cattipoconcentracion.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "concentración de fondos cg — Canal/Cuenta General y catálogo (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_cattipoconcentracion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cTipo` | `CHAR(10)` | L12 |
| `cDescripcion` | `CHAR(40)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_tipoconcentracion_etv` | `bdicnweb` | no | SELECT | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L64 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cg_consbancont`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consbancont.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 88 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y banco" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consbancont` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consbancont(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cActivar` | `CHAR(1)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `sp_consbancont` | `bdispei` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdispei:sp_consbancont';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `banco` | ENTIDAD | banco | 🔵 CONVENCIÓN | nombre_sp |
| `?nt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?nt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consbitoperacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consbitoperacion.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 204 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consbitoperacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cDesc_operacion` | `CHAR(20)` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_operacion` | `bdicnweb` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?bit` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `operacion` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bit` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consbitusuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consbitusuario.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 205 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y usuario" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consbitusuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cUsuario` | `CHAR(8)` | L9 |
| `cNombreUsuario` | `CHAR(45)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_reversoscg` | `bdisuc` | ⚠️ sí | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L58 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?bit` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?bit` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consfoliooperacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consfoliooperacion.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y folio" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consfoliooperacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cFolioOper` | `CHAR(8)` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSucursal = '' OR pFecha IS NULL OR pRegistros IS NULL OR pRe` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L54 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '01275';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `folio` | ENTIDAD | folio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operacion` | ACCION | operación | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consfuncionalidadesmttoetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consfuncionalidadesmttoetv.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y identificador" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consfuncionalidadesmttoetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIdCatalogo` | `INTEGER` | L9 |
| `cDescFuncion` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_catalogomantenimientoetv` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?funcional` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?adesmttoetv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?funcional`, `?adesmttoetv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consfuncionalidadesoperacionesmttocat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consfuncionalidadesoperacionesmttocat.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, identificador, operaciones y catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consfuncionalidadesoperacionesmttocat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCatalogo                  INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pIdCatalogo` | `INTEGER` | `id`=identificador (de) · `cat`=catálogo | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iIdCatalogo` | `INTEGER` | L10 |
| `cIdFuncion` | `CHAR(10)` | L11 |
| `cDescFuncion` | `CHAR(100)` | L12 |
| `iIdSubmodulo` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_operacionesmantenimientoetv` | `bdicnweb` | no | SELECT | L51 |
| `si_seg_usuarios_funciones` | `bdinteg` | ⚠️ sí | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?funcional` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ades` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mtto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?funcional`, `?ades`, `?mtto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_conslistasolicitudesconcentracion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_conslistasolicitudesconcentracion.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_conslistasolicitudesconcentracion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdBanco                     CHAR(2)
  pFechaDel                    DATE
  pFechaAl                     DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdBanco` | `CHAR(2)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `cIdbanco` | `CHAR(5)` | L17 |
| `cIdSolicitud` | `CHAR(25)` | L18 |
| `cFechaSolicitud` | `CHAR(10)` | L19 |
| `cFolioComprobante` | `CHAR(16)` | L20 |
| `cSucursalBanco` | `CHAR(8)` | L21 |
| `cNombreSucursal` | `CHAR(40)` | L22 |
| `cSucursalPanam` | `CHAR(30)` | L23 |
| `dImporte` | `DECIMAL(10,2)` | L24 |
| `cCodPanam` | `CHAR(4)` | L25 |
| `cHoraSol` | `CHAR(5)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdBanco = '' OR pFechaDel IS NULL OR pFechaAl IS NULL THEN` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?lista` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?lista` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_conslistasolicitudesconcentracion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_conslistasolicitudesconcentracion_totales.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_conslistasolicitudesconcentracion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdBanco                     CHAR(2)
  pFechaDel                    DATE
  pFechaAl                     DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdBanco` | `CHAR(2)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdBanco = '' OR pFechaDel IS NULL OR pFechaAl IS NULL THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '01058';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?lista` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitudes` | ENTIDAD | solicitudes (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?lista` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consulta_catdenominacion_bym`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consulta_catdenominacion_bym.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, catálogo de denominaciones y Billetes y Monedas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_catdenominacion_bym` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consulta_catdenominacion_bym(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      CHAR(1)
  pDato                        CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pDato` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iIdDenominacion` | `INTEGER` | L13 |
| `cCvePieza` | `CHAR(1)` | L14 |
| `cTipoPieza` | `CHAR(7)` | L15 |
| `cDenominacion` | `CHAR(10)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_consulta_catdenominacion_bym` | `bdisuc` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_consulta_catdenominacion_bym';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L67 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catdenominacion` | ENTIDAD | catálogo de denominaciones | 🔵 CONVENCIÓN | nombre_sp |
| `bym` | ENTIDAD | Billetes y Monedas (efectivo en sucursal — evidencia: 'pieza | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultabitacorasumarestaimp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultabitacorasumarestaimp.sql` |
| **LOC (1er CREATE)** | 161 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, bitácora y Impago — pago vencido o fallido; confirmado: n_impagos_consec" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultabitacorasumarestaimp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pAnio                        INTEGER
  pMes                         INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)   AS  codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pAnio` | `INTEGER` | — | — |
| `pMes` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cCodRetSp` | `CHAR(6)` | L30 |
| `iCodRetSp` | `INTEGER` | L31 |
| `iNoRegistros` | `INTEGER` | L32 |
| `cEmpresa` | `CHAR(03)` | L34 |
| `cCaja_General` | `CHAR(40)` | L35 |
| `cUsuario` | `CHAR(8)` | L36 |
| `cNombre` | `CHAR(40)` | L37 |
| `dFechaModificacion` | `DATE` | L38 |
| `cDescripcion` | `CHAR(1)` | L39 |
| `dSaldo` | `MONEY(19,2)` | L40 |
| `iCantidad_1000` | `INTEGER` | L41 |
| `iCantidad_500` | `INTEGER` | L42 |
| `iCantidad_200` | `INTEGER` | L43 |
| `iCantidad_100` | `INTEGER` | L44 |
| `iCantidad_50` | `INTEGER` | L45 |
| `iCantidad_20` | `INTEGER` | L46 |
| `iCant_m_100` | `INTEGER` | L47 |
| `iCant_m_50` | `INTEGER` | L48 |
| `iCant_m_20` | `INTEGER` | L49 |
| `iCant_m_10` | `INTEGER` | L50 |
| `iCant_m_5` | `INTEGER` | L51 |
| `iCant_m_2` | `INTEGER` | L52 |
| `iCant_m_1` | `INTEGER` | L53 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_importe` | `bdisuc` | ⚠️ sí | SELECT | L128 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L115 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L101 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pAnio IS NULL OR pMes IS NULL OR pRegistros IS NULL OR pRecup` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L133 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '01113';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?sumaresta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?sumaresta` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultacajagenetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultacajagenetv.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consulta_cajagen_etv2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultacajagenetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cCodProvedor` | `CHAR(4)` | L10 |
| `cDescripcion` | `CHAR(60)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consulta_cajagen_etv2` | `bdisuc` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_consulta_cajagen_etv` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `?etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultacat_estatus_bym`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultacat_estatus_bym.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, catálogo, estatus y Billetes y Monedas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultacat_estatus_bym` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultacat_estatus_bym(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      CHAR(1)
  pDato                        INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pDato` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensaje` | `CHAR(80)` | L10 |
| `iCveEstatus` | `INTEGER` | L11 |
| `cDescripcion` | `CHAR(20)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_consultacat_estatus_bym` | `bdisuc` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_consultacat_estatus_bym';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bym` | ENTIDAD | Billetes y Monedas (efectivo en sucursal — evidencia: 'pieza | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultacatalogoetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultacatalogoetv.sql` |
| **LOC (1er CREATE)** | 130 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultacatalogoetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEstatus                     CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEstatus` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cNombreEtv` | `CHAR(40)` | L11 |
| `cActiva` | `CHAR(1)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `iIdRow` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_catalago_etv` | `bdisuc` | ⚠️ sí | SELECT | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEstatus = ''  OR pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L69 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L89 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L108 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultaccetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultaccetv.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y CCE — Cámara de Compensación Electrónica" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultaccetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripcionEtv              CHAR(40)
  pCodProveedor                CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripcionEtv` | `CHAR(40)` | — | — |
| `pCodProveedor` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `iRowId` | `INTEGER` | L13 |
| `cCajaGeneral` | `CHAR(30)` | L14 |
| `cCcEtv` | `CHAR(4)` | L15 |
| `cEtv` | `CHAR(30)` | L16 |
| `cEstatus` | `CHAR(8)` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_relacion_cc_etvs` | `bdisuc` | ⚠️ sí | SELECT | L68 |
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDescripcionEtv = '' OR pRegistros IS NULL OR pRecuperacion I` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L76 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L91 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | 🔵 CONVENCIÓN | nombre_sp |
| `?tv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?tv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultaccetv_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultaccetv_totales.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y CCE — Cámara de Compensación Electrónica (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultaccetv_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripcionEtv              CHAR(40)
  pCodProveedor                CHAR(4)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripcionEtv` | `CHAR(40)` | — | — |
| `pCodProveedor` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iTotalRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_relacion_cc_etvs` | `bdisuc` | ⚠️ sí | SELECT | L47 |
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | 🔵 CONVENCIÓN | nombre_sp |
| `?tv_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?tv_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultacentrocostos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultacentrocostos.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, ro — Rol Operativo y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=5 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultacentrocostos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pPlaza                       CHAR(3)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pPlaza` | `CHAR(3)` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cClave` | `CHAR(4)` | L8 |
| `cDescripcion` | `CHAR(45)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?cent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?c` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?cent`, `ro`, `?c`, `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultacentrocostos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultacentrocostos_totales.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, ro — Rol Operativo y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=5 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultacentrocostos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pPlaza                       CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pPlaza` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?cent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?c` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?cent`, `ro`, `?c`, `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultadatospiezas_bym`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultadatospiezas_bym.sql` |
| **LOC (1er CREATE)** | 366 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, datos, piezas de efectivo y Billetes y Monedas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultadatospiezas_bym(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCaptura                DATE
  pFechaIni                    DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pNumRecibo                   CHAR(10)
  pNumGuia                     CHAR(12)
  pEstatus                     INTEGER
  pDictamen                    INTEGER
  pTipoConsulta                INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCaptura` | `DATE` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumRecibo` | `CHAR(10)` | — | — |
| `pNumGuia` | `CHAR(12)` | — | — |
| `pEstatus` | `INTEGER` | — | — |
| `pDictamen` | `INTEGER` | — | — |
| `pTipoConsulta` | `INTEGER` | `consulta`=consulta / lee | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L43 |
| `iSamErr` | `INTEGER` | L44 |
| `cDesErr` | `CHAR(80)` | L45 |
| `cCodRet` | `CHAR(5)` | L46 |
| `cCodRetSp` | `CHAR(6)` | L47 |
| `cMensaje` | `CHAR(80)` | L48 |
| `iRecuperacion` | `INTEGER` | L49 |
| `iCvePieza` | `INTEGER` | L50 |
| `dFechaCaptura` | `DATE` | L51 |
| `cNumRecibo` | `CHAR(10)` | L52 |
| `iNumPiezas` | `INTEGER` | L53 |
| `cTipoPieza` | `CHAR(1)` | L54 |
| `cDenominacion` | `CHAR(10)` | L55 |
| `iCveDenominacion` | `INTEGER` | L56 |
| `cSerie` | `CHAR(40)` | L57 |
| `cFolio` | `CHAR(40)` | L58 |
| `dFechaEmision` | `DATE` | L59 |
| `cNota` | `CHAR(200)` | L60 |
| `cEstatus` | `CHAR(20)` | L61 |
| `cDictamenBanxico` | `CHAR(20)` | L62 |
| `cNumLoteBanxico` | `CHAR(40)` | L63 |
| `cFolioBanxico` | `CHAR(40)` | L64 |
| `dFechaPago` | `DATE` | L65 |
| `cFormaPago` | `CHAR(20)` | L66 |
| `cNumCta` | `CHAR(11)` | L67 |
| *…86 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_billetesfalsos` | `bdicnweb` | no | SELECT | L310 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L291 |
| `sp_consultadatospiezas_bym3` | `bdisuc` | ⚠️ sí | L323 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L159 | CÓDIGO_RETORNO | `LET cCodRet                 = '00000';` |  |
| L279 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL OR pTipoConsulta ` |  |
| L280 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L286 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L314 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L327 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_consultadatospiezas_b` |  |
| L329 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L332 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L339 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L347 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `piezas` | ENTIDAD | piezas de efectivo (billetes y monedas) | 🔵 CONVENCIÓN | nombre_sp |
| `bym` | ENTIDAD | Billetes y Monedas (efectivo en sucursal — evidencia: 'pieza | 🟡 INFERIDO | nombre_sp |

---

## `sp_cg_consultadatospiezas_bym_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultadatospiezas_bym_totales.sql` |
| **LOC (1er CREATE)** | 276 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, datos, piezas de efectivo y Billetes y Monedas (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultadatospiezas_bym_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCaptura                DATE
  pFechaIni                    DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pNumRecibo                   CHAR(10)
  pNumGuia                     CHAR(12)
  pEstatus                     INTEGER
  pDictamen                    INTEGER
  pTipoConsulta                INTEGER
) RETURNING CHAR(5) 	AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCaptura` | `DATE` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumRecibo` | `CHAR(10)` | — | — |
| `pNumGuia` | `CHAR(12)` | — | — |
| `pEstatus` | `INTEGER` | — | — |
| `pDictamen` | `INTEGER` | — | — |
| `pTipoConsulta` | `INTEGER` | `consulta`=consulta / lee | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L5 |
| `iSamErr` | `INTEGER` | L6 |
| `cDesErr` | `CHAR(80)` | L7 |
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cMensaje` | `CHAR(80)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `iCvePieza` | `INTEGER` | L12 |
| `dFechaCaptura` | `DATE` | L13 |
| `cNumRecibo` | `CHAR(10)` | L14 |
| `iNumPiezas` | `INTEGER` | L15 |
| `cTipoPieza` | `CHAR(1)` | L16 |
| `cDenominacion` | `CHAR(10)` | L17 |
| `iCveDenominacion` | `INTEGER` | L18 |
| `cSerie` | `CHAR(40)` | L19 |
| `cFolio` | `CHAR(40)` | L20 |
| `dFechaEmision` | `DATE` | L21 |
| `cNota` | `CHAR(200)` | L22 |
| `cEstatus` | `CHAR(20)` | L23 |
| `cDictamenBanxico` | `CHAR(20)` | L24 |
| `cNumLoteBanxico` | `CHAR(40)` | L25 |
| `cFolioBanxico` | `CHAR(40)` | L26 |
| `dFechaPago` | `DATE` | L27 |
| `cFormaPago` | `CHAR(20)` | L28 |
| `cNumCta` | `CHAR(11)` | L29 |
| *…82 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_billetesfalsos` | `bdicnweb` | no | SELECT | L193 |
| `sw_cg_validaestatusbf` | `bdicnweb` | no | SELECT | L195 |
| `sw_cg_validaestatusbf` | `bdicnweb` | no | INSERT | L197 |
| `sw_cg_billetesfalsos` | `bdicnweb` | no | INSERT | L222 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L183 |
| `sp_consultadatospiezas_bym2` | `bdisuc` | ⚠️ sí | L201 |
| `sp_consultadatospiezas_bym3_totales` | `bdisuc` | ⚠️ sí | L248 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L120 | CÓDIGO_RETORNO | `LET cCodRet                 = '00000';` |  |
| L177 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pTipoConsulta IS NULL THEN` |  |
| L178 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L205 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_consultadatospiezas_b` |  |
| L211 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L218 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L221 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L252 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_consultadatospiezas_b` |  |
| L254 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L256 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L262 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `piezas` | ENTIDAD | piezas de efectivo (billetes y monedas) | 🔵 CONVENCIÓN | nombre_sp |
| `bym` | ENTIDAD | Billetes y Monedas (efectivo en sucursal — evidencia: 'pieza | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cg_consultadotacionesrecoleccionesetv_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultadotacionesrecoleccionesetv_totales.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, dotaciones de efectivo y cc — cuenta corriente (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultadotacionesrecoleccionesetv_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoServicio                CHAR(30)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoServicio` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iTotalRegistros` | `INTEGER` | L9 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00981';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?ole` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ionesetv_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?ole`, `?ionesetv_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultamelbconcentraciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultamelbconcentraciones.sql` |
| **LOC (1er CREATE)** | 164 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 290 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultamelbconcentraciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               CHAR(10)
  pTipOperacion                CHAR(5)
  pSucursal                    CHAR(8)
  pDireccionMac                CHAR(12)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `CHAR(10)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pTipOperacion` | `CHAR(5)` | — | — |
| `pSucursal` | `CHAR(8)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `iNoRegistros` | `INTEGER` | L28 |
| `cIdbanco` | `CHAR(5)` | L29 |
| `cIdSolicitud` | `CHAR(25)` | L30 |
| `cFechaAplicacion` | `CHAR(10)` | L31 |
| `cMotivoVisita` | `CHAR(120)` | L32 |
| `cFolioComprobante` | `CHAR(16)` | L33 |
| `dDiceContener` | `DECIMAL(17,2)` | L34 |
| `dRealVerificado` | `DECIMAL(17,2)` | L35 |
| `cTipoDomicilio` | `CHAR(3)` | L36 |
| `cTipoMovimiento` | `CHAR(3)` | L37 |
| `cUrbFor` | `CHAR(1)` | L38 |
| `cEstadoTransito` | `CHAR(16)` | L39 |
| `cEstadoVisita` | `CHAR(16)` | L40 |
| `cEstadoBoveda` | `CHAR(10)` | L41 |
| `cEstadoVerificada` | `CHAR(10)` | L42 |
| `cEstadoOtro` | `CHAR(10)` | L43 |
| `cSucursalBanco` | `CHAR(8)` | L44 |
| `cMisc1` | `CHAR(40)` | L45 |
| `cMisc2` | `CHAR(40)` | L46 |
| `cMisc3` | `CHAR(40)` | L47 |
| `cMisc4` | `CHAR(40)` | L48 |
| `cMisc5` | `CHAR(40)` | L49 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_recibe_datosconcentracionws` | `bdisuc` | ⚠️ sí | SELECT | L105 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L94 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L134 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L143 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?melb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?melb`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultamelbconcentraciones_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultamelbconcentraciones_totales.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultamelbconcentraciones_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               CHAR(10)
  pTipOperacion                CHAR(5)
  pSucursal                    CHAR(8)
  pDireccionMac                CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `CHAR(10)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pTipOperacion` | `CHAR(5)` | — | — |
| `pSucursal` | `CHAR(8)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_recibe_datosconcentracionws` | `bdisuc` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?melb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?melb`, `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultamelbdetalleope`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultamelbdetalleope.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y detalle" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultamelbdetalleope(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSolicitud                 CHAR(25)
  pFechaSolicitud              DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSolicitud` | `CHAR(25)` | — | — |
| `pFechaSolicitud` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L36 |
| `iSqlErr` | `INTEGER` | L37 |
| `iNoRegistros` | `INTEGER` | L38 |
| `cFechaOperacion` | `CHAR(10)` | L39 |
| `mMontoEntradaSalida` | `MONEY(18,2)` | L40 |
| `cDescProveedor` | `CHAR(30)` | L41 |
| `cSucursal` | `CHAR(4)` | L42 |
| `cFolioServicio` | `CHAR(16)` | L43 |
| `cDescStatus` | `CHAR(30)` | L44 |
| `cUsuarioOpe` | `CHAR(8)` | L45 |
| `dCantidad1` | `INTEGER` | L46 |
| `dCantidad2` | `INTEGER` | L47 |
| `dCantidad3` | `INTEGER` | L48 |
| `dCantidad4` | `INTEGER` | L49 |
| `dCantidad5` | `INTEGER` | L50 |
| `dCantidad6` | `INTEGER` | L51 |
| `dCantidad7` | `INTEGER` | L52 |
| `mMontoOper` | `MONEY(18,2)` | L53 |
| `cFolioOper` | `CHAR(8)` | L54 |
| `cDescDivisa` | `CHAR(30)` | L55 |
| `cFechaSolicitud` | `CHAR(10)` | L56 |
| `cHoraSolicitud` | `CHAR(5)` | L57 |
| `cUsuarioSolicitud` | `CHAR(8)` | L58 |
| `cFechaEnvio` | `CHAR(10)` | L59 |
| `cHoraEnvio` | `CHAR(5)` | L60 |
| *…10 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L149 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L132 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L123 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdSolicitud = '' OR pFechaSolicitud IS NULL THEN` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L159 | VALIDACIÓN_NULL | `IF cFechaOperacion = '' OR cFechaOperacion IS NULL THEN` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?melb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?melb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultamelbdotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultamelbdotaciones.sql` |
| **LOC (1er CREATE)** | 261 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 289 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y dotaciones de efectivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultamelbdotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               CHAR(10)
  pTipOperacion                CHAR(5)
  pSucursal                    CHAR(8)
  pDireccionMac                CHAR(12)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `CHAR(10)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pTipOperacion` | `CHAR(5)` | — | — |
| `pSucursal` | `CHAR(8)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L54 |
| `iSqlErr` | `INTEGER` | L55 |
| `iNoRegistros` | `INTEGER` | L56 |
| `cIdBanco` | `CHAR(5)` | L57 |
| `cIdSol` | `CHAR(25)` | L58 |
| `cFechAplica` | `CHAR(10)` | L59 |
| `cMtvoCambio` | `CHAR(120)` | L60 |
| `cMtvoDev` | `CHAR(120)` | L61 |
| `cFolioComp` | `CHAR(16)` | L62 |
| `dMntoComp` | `MONEY(17,2)` | L63 |
| `cTipoDomi` | `CHAR(3)` | L64 |
| `cUrbFor` | `CHAR(1)` | L65 |
| `cEdoRechazada` | `CHAR(10)` | L66 |
| `cEdoAprobada` | `CHAR(10)` | L67 |
| `cEdoPreparada` | `CHAR(10)` | L68 |
| `cEdoBoveda` | `CHAR(10)` | L69 |
| `cEdoTransito` | `CHAR(10)` | L70 |
| `cEdoEntreBanco` | `CHAR(16)` | L71 |
| `cEdoDevolucion` | `CHAR(10)` | L72 |
| `cEdoCerrada` | `CHAR(10)` | L73 |
| `cEdoCerradaDev` | `CHAR(10)` | L74 |
| `cMtvoRechazo` | `CHAR(120)` | L75 |
| `cSucBanco` | `CHAR(8)` | L76 |
| `dBill1` | `INTEGER` | L77 |
| `dBill2` | `INTEGER` | L78 |
| *…26 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_recibe_datosdotacionws` | `bdisuc` | ⚠️ sí | SELECT | L193 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L178 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L171 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L198 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L208 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L228 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L238 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?melb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?melb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultamelbdotaciones_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultamelbdotaciones_totales.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y dotaciones de efectivo (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultamelbdotaciones_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               CHAR(10)
  pTipOperacion                CHAR(5)
  pSucursal                    CHAR(8)
  pDireccionMac                CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `CHAR(10)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pTipOperacion` | `CHAR(5)` | — | — |
| `pSucursal` | `CHAR(8)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_recibe_datosdotacionws` | `bdisuc` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?melb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?melb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultamelbtotalesacusesetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultamelbtotalesacusesetv.sql` |
| **LOC (1er CREATE)** | 185 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultamelbtotalesacusesetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pTipoServicio                CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTipoServicio` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `iTotalDot` | `INTEGER` | L13 |
| `iDotAceptadas` | `INTEGER` | L14 |
| `iDotRechazadas` | `INTEGER` | L15 |
| `iTotalConcentra` | `INTEGER` | L16 |
| `iConcentraAceptadas` | `INTEGER` | L17 |
| `iConcentraRechazadas` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pTipoServicio = ` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?melb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `?acusesetv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?melb`, `?acusesetv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultapanamericanoetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultapanamericanoetv.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consulta_ccpanamericano_etv2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultapanamericanoetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cCentroCostos` | `CHAR(4)` | L10 |
| `cCajaGeneral` | `CHAR(30)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consulta_ccpanamericano_etv2` | `bdisuc` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_consulta_ccpanameric` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?panamericanoetv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?panamericanoetv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultaplaza`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultaplaza.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y plaza" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultaplaza(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cClave` | `CHAR(3)` | L8 |
| `cDescripcion` | `CHAR(40)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `plaza` | ENTIDAD | plaza (regional) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultaplaza_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultaplaza_totales.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y plaza (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultaplaza_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `plaza` | ENTIDAD | plaza (regional) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultaranios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultaranios.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cg — Canal/Cuenta General y año" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultaranios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pAnioInicial                 INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pAnioInicial` | `INTEGER` | `anio`=año | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCve` | `CHAR(4)` | L9 |
| `cAnio` | `CHAR(4)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `iAnioActual` | `INTEGER` | L12 |
| `iAnioCount` | `INTEGER` | L13 |
| `valida` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L48 |
| `criterio_busqueda_anio_tmp` | `bdicnweb` | no | INSERT | L64 |
| `criterio_busqueda_anio_tmp` | `bdicnweb` | no | SELECT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''   OR pAnioInicial IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | FÓRMULA | `LET iAnioCount = iAnioCount + 1;` |  |
| L81 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `anio` | ENTIDAD | año | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultarmesesanio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultarmesesanio.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cg — Canal/Cuenta General, mes y año" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultarmesesanio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pAnio                        INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pAnio` | `INTEGER` | `anio`=año | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIdMes` | `INTEGER` | L10 |
| `cDescripcionMes` | `CHAR(20)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `iAnioActual` | `INTEGER` | L13 |
| `iMesActual` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cat_mesesanio` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pAnio IS NULL THEN` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L69 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `mes` | ENTIDAD | mes | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `anio` | ENTIDAD | año | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultaroperacionsumarestaimp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultaroperacionsumarestaimp.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cg — Canal/Cuenta General y Impago — pago vencido o fallido; confirmado: n_impagos_consec" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultaroperacionsumarestaimp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cClave` | `CHAR(1)` | L10 |
| `cDescripcion` | `CHAR(30)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_operacionsumarestaimportes` | `bdicnweb` | no | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `operacion` | ACCION | operación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?sumaresta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sumaresta` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultasaldoactualsumarestaimp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultasaldoactualsumarestaimp.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, saldo y Impago — pago vencido o fallido; confirmado: n_impagos_consec" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultasaldoactualsumarestaimp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCajaGeneral                 CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCajaGeneral` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iCodRetSp` | `INTEGER` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `iNoRegistros` | `INTEGER` | L27 |
| `cEmpresa` | `CHAR(3)` | L28 |
| `valida` | `INTEGER` | L29 |
| `fCantidad_1000` | `FLOAT(8)` | L31 |
| `fCantidad_500` | `FLOAT(8)` | L32 |
| `fCantidad_200` | `FLOAT(8)` | L33 |
| `fCantidad_100` | `FLOAT(8)` | L34 |
| `fCantidad_50` | `FLOAT(8)` | L35 |
| `fCantidad_20` | `FLOAT(8)` | L36 |
| `dTotal_billete` | `DECIMAL(18,2)` | L37 |
| `fCantidad_mon_100` | `FLOAT(8)` | L38 |
| `fCantidad_mon_50` | `FLOAT(8)` | L39 |
| `fCantidad_mon_20` | `FLOAT(8)` | L40 |
| `fCantidad_mon_10` | `FLOAT(8)` | L41 |
| `fCantidad_mon_5` | `FLOAT(8)` | L42 |
| `fCantidad_mon_2` | `FLOAT(8)` | L43 |
| `fCantidad_mon_1` | `FLOAT(8)` | L44 |
| `fCantidad_mon_0_50` | `FLOAT(8)` | L45 |
| `fCantidad_mon_0_20` | `FLOAT(8)` | L46 |
| `fCantidad_mon_0_10` | `FLOAT(8)` | L47 |
| `fCantidad_mon_0_05` | `FLOAT(8)` | L48 |
| `dTotal_morralla` | `DECIMAL(18,2)` | L49 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_cajageneral` | `bdisuc` | ⚠️ sí | SELECT | L108 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L99 |
| `sp_saldoactual_cajageneral` | `bdisuc` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc:'informix'.sp_saldoactual_cajagen` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?ualsumaresta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?ualsumaresta` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultastatusprocesobf`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultastatusprocesobf.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, estatus y proceso" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultastatusprocesobf(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdStatus` | `CHAR(1)` | L8 |
| `cDescStatus` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_validaestatusbf` | `bdicnweb` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?bf` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bf` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultastatusprocesomonitor`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultastatusprocesomonitor.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, estatus, proceso y monitor" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultastatusprocesomonitor(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdStatus` | `CHAR(1)` | L8 |
| `cDescStatus` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_validaestatus` | `bdicnweb` | no | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monitor` | ENTIDAD | monitor | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cg_consultasucursalatmetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultasucursalatmetv.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General, sucursal y cajero automático" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultasucursalatmetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipOperacion                CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipOperacion` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cCentroCostos` | `CHAR(4)` | L10 |
| `cNombreCc` | `CHAR(40)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consulta_sucursal_atm_etv2` | `bdisuc` | ⚠️ sí | L55 |
| `sp_consulta_sucursal_atm_etv2_2` | `bdisuc` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L59 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP sp_consulta_sucursal_atm_etv2';` |  |
| L67 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP sp_consulta_sucursal_atm_etv2_2` |  |
| L86 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `atm` | ENTIDAD | cajero automático (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `?etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_consultatipocc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consultatipocc.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cg — Canal/Cuenta General y cc — cuenta corriente (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consultatipocc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cDescrip` | `CHAR(40)` | L8 |
| `cTipo` | `CHAR(2)` | L9 |
| `iTotal` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_catalogotipocc` | `bdicnweb` | no | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iTotal = iTotal+1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |

---

## `sp_cg_consutacat_dictamen_bym`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_consutacat_dictamen_bym.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo [typo] cg — Canal/Cuenta General, dictamen y Billetes y Monedas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consutacat_dictamen_bym` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_consutacat_dictamen_bym(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      CHAR(1)
  pDato                        INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pDato` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensaje` | `CHAR(80)` | L10 |
| `iCveDictamen` | `INTEGER` | L11 |
| `cDescripcion` | `CHAR(20)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_consutacat_dictamen_bym` | `bdisuc` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_consutacat_dictamen_bym';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consutacat` | ACCION | consulta catálogo [typo] | 🔵 CONVENCIÓN | nombre_sp |
| `dictamen` | ENTIDAD | dictamen | 🔵 CONVENCIÓN | nombre_sp |
| `bym` | ENTIDAD | Billetes y Monedas (efectivo en sucursal — evidencia: 'pieza | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallealertasspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallealertasspei.sql` |
| **LOC (1er CREATE)** | 121 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 87 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, detalle y alertas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallealertasspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INT` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cDescCodRet` | `CHAR(80)` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `dFechaAct` | `DATE` | L19 |
| `cHrAct` | `CHAR(12)` | L20 |
| `cUsAct` | `CHAR(8)` | L21 |
| `cNombreUsAct` | `CHAR(45)` | L22 |
| `dFechaDes` | `DATE` | L23 |
| `cHrDes` | `CHAR(12)` | L24 |
| `cUsDes` | `CHAR(8)` | L25 |
| `cNombreUsDes` | `CHAR(45)` | L26 |
| `iNumserial` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tblbitalertaspei` | `bdispei` | ⚠️ sí | SELECT | L82 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L100 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `alertas` | ENTIDAD | alertas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `spei` | PREFIJO | familia SPEI (pagos interbancarios) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallealtatipoconcentracion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallealtatipoconcentracion.sql` |
| **LOC (1er CREATE)** | 291 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta cg — Canal/Cuenta General y detalle (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallealtatipoconcentracion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEtv                         CHAR(40)
  pCg                          CHAR(4)
  pAtmSuc                      CHAR(4)
  pCajaGral                    CHAR(40)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEtv` | `CHAR(40)` | — | — |
| `pCg` | `CHAR(4)` | `cg`=cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO |
| `pAtmSuc` | `CHAR(4)` | — | — |
| `pCajaGral` | `CHAR(40)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INT` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cDescCodRet` | `CHAR(80)` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `cEtv` | `CHAR(30)` | L19 |
| `cCaja_general` | `CHAR(40)` | L20 |
| `cCentro_costos` | `CHAR(4)` | L21 |
| `cNo_sucursal_atm` | `CHAR(10)` | L22 |
| `cNombre_sucursal_atm` | `CHAR(40)` | L23 |
| `cTipo_concentracion` | `CHAR(10)` | L24 |
| `cEstatus` | `CHAR(1)` | L25 |
| `iRowId` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_tipo_concentracion_etv` | `bdisuc` | ⚠️ sí | SELECT | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEtv = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L107 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L121 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L135 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L153 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L168 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L183 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L198 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L216 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L231 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L246 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L261 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L269 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallealtatipoconcentracion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallealtatipoconcentracion_totales.sql` |
| **LOC (1er CREATE)** | 173 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta cg — Canal/Cuenta General y detalle (tipo de, totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallealtatipoconcentracion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEtv                         CHAR(40)
  pCg                          CHAR(4)
  pAtmSuc                      CHAR(4)
  pCajaGral                    CHAR(40)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEtv` | `CHAR(40)` | — | — |
| `pCg` | `CHAR(4)` | `cg`=cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO |
| `pAtmSuc` | `CHAR(4)` | — | — |
| `pCajaGral` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cEtv` | `CHAR(30)` | L11 |
| `cCaja_general` | `CHAR(40)` | L12 |
| `cCentro_costos` | `CHAR(4)` | L13 |
| `cNo_sucursal_atm` | `CHAR(4)` | L14 |
| `cNombre_sucursal_atm` | `CHAR(40)` | L15 |
| `cTipo_concentracion` | `CHAR(10)` | L16 |
| `cEstatus` | `CHAR(1)` | L17 |
| `iNoRegistros` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_tipo_concentracion_etv` | `bdisuc` | ⚠️ sí | SELECT | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L156 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallebajatipoconcentracion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallebajatipoconcentracion.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "concentración de fondos cg — Canal/Cuenta General y detalle (de baja, tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallebajatipoconcentracion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEtv                         CHAR(40)
  pCg                          CHAR(4)
  pAtmSuc                      CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEtv` | `CHAR(40)` | — | — |
| `pCg` | `CHAR(4)` | `cg`=cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO |
| `pAtmSuc` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INT` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cDescCodRet` | `CHAR(80)` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `cEtv` | `CHAR(30)` | L19 |
| `cCaja_general` | `CHAR(40)` | L20 |
| `cCentro_costos` | `CHAR(4)` | L21 |
| `cNo_sucursal_atm` | `CHAR(10)` | L22 |
| `cNombre_sucursal_atm` | `CHAR(40)` | L23 |
| `cTipo_concentracion` | `CHAR(10)` | L24 |
| `cEstatus` | `CHAR(1)` | L25 |
| `iRowId` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_tipo_concentracion_etv` | `bdisuc` | ⚠️ sí | SELECT | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEtv = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L92 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L105 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L119 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L133 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `baja` | MODIF | de baja | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallebajatipoconcentracion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallebajatipoconcentracion_totales.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "concentración de fondos cg — Canal/Cuenta General y detalle (de baja, tipo de, totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallebajatipoconcentracion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEtv                         CHAR(40)
  pCg                          CHAR(4)
  pAtmSuc                      CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEtv` | `CHAR(40)` | — | — |
| `pCg` | `CHAR(4)` | `cg`=cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO |
| `pAtmSuc` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cEtv` | `CHAR(30)` | L11 |
| `cCaja_general` | `CHAR(40)` | L12 |
| `cCentro_costos` | `CHAR(4)` | L13 |
| `cNo_sucursal_atm` | `CHAR(4)` | L14 |
| `cNombre_sucursal_atm` | `CHAR(40)` | L15 |
| `cTipo_concentracion` | `CHAR(10)` | L16 |
| `cEstatus` | `CHAR(1)` | L17 |
| `iNoRegistros` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_tipo_concentracion_etv` | `bdisuc` | ⚠️ sí | SELECT | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `baja` | MODIF | de baja | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallebitacora`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallebitacora.sql` |
| **LOC (1er CREATE)** | 153 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, detalle y bitácora" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallebitacora(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaDel                    DATE
  pFechaAl                     DATE
  pNumUsuario                  CHAR(8)
  pOperacion                   CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |
| `pNumUsuario` | `CHAR(8)` | — | — |
| `pOperacion` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `dFecha_modificacion` | `DATE` | L15 |
| `cSucursal` | `CHAR(4)` | L16 |
| `cFolio_operacion` | `CHAR(8)` | L17 |
| `cTipo_operacion` | `CHAR(25)` | L18 |
| `mMonto` | `MONEY(16,2)` | L19 |
| `cUsuario` | `CHAR(8)` | L20 |
| `cReverso_cambio` | `CHAR(20)` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_reversoscg` | `bdisuc` | ⚠️ sí | SELECT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaDel IS NULL OR pFechaAl IS NULL OR` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L97 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L112 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L127 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallebitacora_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallebitacora_totales.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, detalle y bitácora (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallebitacora_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaDel                    DATE
  pFechaAl                     DATE
  pNumUsuario                  CHAR(8)
  pOperacion                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |
| `pNumUsuario` | `CHAR(8)` | — | — |
| `pOperacion` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_reversoscg` | `bdisuc` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaDel IS NULL OR pFechaAl IS NULL THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallebitacoramodificaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallebitacoramodificaciones.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "modificación cg — Canal/Cuenta General, detalle y bitácora" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallebitacoramodificaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INT` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cDescCodRet` | `CHAR(80)` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `dFecha` | `DATE` | L16 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L17 |
| `cTipoMantenimiento` | `CHAR(10)` | L18 |
| `cNoEmpleado` | `CHAR(8)` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `cNombreETV` | `CHAR(40)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_mant_etv` | `bdisuc` | ⚠️ sí | SELECT | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `modificacion` | ACCION | modificación | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallebitacoramodificaciones_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallebitacoramodificaciones_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "modificación cg — Canal/Cuenta General, detalle y bitácora (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallebitacoramodificaciones_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_mant_etv` | `bdisuc` | ⚠️ sí | SELECT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `modificacion` | ACCION | modificación | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallebitacoratipoconcentracion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallebitacoratipoconcentracion.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "concentración de fondos cg — Canal/Cuenta General, detalle y bitácora (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallebitacoratipoconcentracion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INT` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cDescCodRet` | `CHAR(80)` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `dFecha` | `DATE` | L18 |
| `dHora` | `CHAR(25)` | L19 |
| `cTipo_mantenimiento` | `CHAR(10)` | L20 |
| `cUsuario` | `CHAR(8)` | L21 |
| `cNo_sucursal` | `CHAR(4)` | L22 |
| `cNo_atm` | `CHAR(6)` | L23 |
| `cNombre_sucursal_atm` | `CHAR(60)` | L24 |
| `cTipo_concentracion_asignado` | `CHAR(10)` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_tipo_concentracion` | `bdisuc` | ⚠️ sí | SELECT | L79 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L83 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detallebitacoratipoconcentracion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detallebitacoratipoconcentracion_totales.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "concentración de fondos cg — Canal/Cuenta General, detalle y bitácora (tipo de, totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detallebitacoratipoconcentracion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `dFecha` | `DATE` | L11 |
| `dHora` | `CHAR(25)` | L12 |
| `cTipo_mantenimiento` | `CHAR(10)` | L13 |
| `cUsuario` | `CHAR(8)` | L14 |
| `cNo_sucursal` | `CHAR(4)` | L15 |
| `cNo_atm` | `CHAR(6)` | L16 |
| `cNombre_sucursal_atm` | `CHAR(60)` | L17 |
| `cTipo_concentracion_asignado` | `CHAR(10)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_bitacora_tipo_concentracion` | `bdisuc` | ⚠️ sí | SELECT | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `concentracion` | ACCION | concentración de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detalletransacciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detalletransacciones.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, detalle y transacción" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detalletransacciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pSucursal                    CHAR(4)
  pFecha                       DATE
  pFolio                       CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pFolio` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `dFecha_solicitud` | `DATE` | L17 |
| `cFolio_oper` | `CHAR(8)` | L18 |
| `mMonto` | `MONEY(16,2)` | L19 |
| `cSucursal` | `CHAR(4)` | L20 |
| `cCod_proveedor` | `CHAR(4)` | L21 |
| `cFolio_servicio` | `CHAR(16)` | L22 |
| `cStatus` | `CHAR(2)` | L23 |
| `cOperacion` | `CHAR(4)` | L24 |
| `cDescOperacion` | `CHAR(35)` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L78 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pSucursal = '' OR pFecha IS NULL OR pFoli` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L88 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L95 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_detalletransacciones_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_detalletransacciones_totales.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, detalle y transacción (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_detalletransacciones_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pSucursal                    CHAR(4)
  pFecha                       DATE
  pFolio                       CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pFolio` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cFolio_oper` | `CHAR(8)` | L9 |
| `cOperacion` | `CHAR(4)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pSucursal = '' OR pFecha IS NULL OR pFoli` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L63 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_enviodotacionmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_enviodotacionmasivo.sql` |
| **LOC (1er CREATE)** | 581 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 212 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía cg — Canal/Cuenta General y dotación de efectivo (masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo · `envía` → `INSERT` encontrado en el cuerpo · `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_enviodotacionmasivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegsProcesados` | `INTEGER` | L7 |
| `cDenominacion1` | `CHAR(18)` | L8 |
| `cDenominacion2` | `CHAR(18)` | L9 |
| `cDenominacion3` | `CHAR(18)` | L10 |
| `cDenominacion4` | `CHAR(18)` | L11 |
| `cDenominacion5` | `CHAR(18)` | L12 |
| `cDenominacion6` | `CHAR(18)` | L13 |
| `cDenominacion7` | `CHAR(18)` | L14 |
| `cDenominacion8` | `CHAR(18)` | L15 |
| `cDenominacion9` | `CHAR(18)` | L16 |
| `cDenominacion10` | `CHAR(18)` | L17 |
| `cDenominacion11` | `CHAR(18)` | L18 |
| `cDenominacion12` | `CHAR(18)` | L19 |
| `cDenominacion13` | `CHAR(18)` | L20 |
| `cDenominacion14` | `CHAR(18)` | L21 |
| `cDenominacion15` | `CHAR(18)` | L22 |
| `dCantidadIng1` | `FLOAT(8)` | L23 |
| `dCantidadIng2` | `FLOAT(8)` | L24 |
| `dCantidadIng3` | `FLOAT(8)` | L25 |
| `dCantidadIng4` | `FLOAT(8)` | L26 |
| `dCantidadIng5` | `FLOAT(8)` | L27 |
| `dCantidadIng6` | `FLOAT(8)` | L28 |
| `dCantidadIng7` | `FLOAT(8)` | L29 |
| *…61 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_cg_dotaciones_masivo` | `bdicnweb` | no | SELECT | L210 |
| `status_cg_dotaciones_masivo` | `bdicnweb` | no | DELETE | L210 |
| `status_cg_dotaciones_masivo` | `bdicnweb` | no | INSERT | L212 |
| `sw_cg_enviodotaciones` | `bdicnweb` | no | SELECT | L239 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L245 |
| `sw_cg_enviodotaciones_hist` | `bdicnweb` | no | INSERT | L530 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L222 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L214 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL THEN` |  |
| L215 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L295 | FÓRMULA | `LET dTotalBilletes = dTotalBilletes + (dCantidadIng1 * cDenominacion1);` |  |
| L301 | FÓRMULA | `LET dTotalBilletes = dTotalBilletes + (dCantidadIng2 * cDenominacion2);` |  |
| L307 | FÓRMULA | `LET dTotalBilletes = dTotalBilletes + (dCantidadIng3 * cDenominacion3);` |  |
| L313 | FÓRMULA | `LET dTotalBilletes = dTotalBilletes + (dCantidadIng4 * cDenominacion4);` |  |
| L319 | FÓRMULA | `LET dTotalBilletes = dTotalBilletes + (dCantidadIng5 * cDenominacion5);` |  |
| L325 | FÓRMULA | `LET dTotalBilletes = dTotalBilletes + (dCantidadIng6 * cDenominacion6);` |  |
| L331 | FÓRMULA | `LET dTotalBilletes = dTotalBilletes + (dCantidadIng7 * cDenominacion7);` |  |
| L337 | FÓRMULA | `LET dTotalMorralla = dTotalMorralla + (dCantidadIng8 * cDenominacion8);` |  |
| L343 | FÓRMULA | `LET dTotalMorralla = dTotalMorralla + (dCantidadIng9 * cDenominacion9);` |  |
| L349 | FÓRMULA | `LET dTotalMorralla = dTotalMorralla + (dCantidadIng10 * cDenominacion10);` |  |
| L355 | FÓRMULA | `LET dTotalMorralla = dTotalMorralla + (dCantidadIng11 * cDenominacion11);` |  |
| L361 | FÓRMULA | `LET dTotalMorralla = dTotalMorralla + (dCantidadIng12 * cDenominacion12);` |  |
| L367 | FÓRMULA | `LET dTotalMorralla = dTotalMorralla + (dCantidadIng13 * cDenominacion13);` |  |
| L373 | FÓRMULA | `LET dTotalMorralla = dTotalMorralla + (dCantidadIng14 * cDenominacion14);` |  |
| L379 | FÓRMULA | `LET dTotalMorralla = dTotalMorralla + (dCantidadIng15 * cDenominacion15);` |  |
| L384 | FÓRMULA | `LET dSumaIngreso = dTotalBilletes + dTotalMorralla;` |  |
| L388 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| L393 | FÓRMULA | `LET dSumaEgreso = dTotalBilletesEgr + dTotalMorrallaEgr;` |  |
| L403 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| L407 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| L411 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| L415 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| L419 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| L423 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| L427 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| L451 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + 1;` |  |
| L462 | FÓRMULA | `LET iNumRegsNoProcesados = iNumRegsNoProcesados + 1;` |  |
| | *…7 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `envio` | ACCION | envía | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_cg_faltsob`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_faltsob.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 99 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General y fal — fallo/disputa" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_faltsob(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pcodigo_proveedor            CHAR(4)
  pcajeroprincipal             CHAR(8)
  pfolio_suc                   CHAR(16)
  ptransaccion                 CHAR(4)
  pdivisa                      CHAR(2)
  pmonto                       MONEY(14,2)
  pfecha                       DATE
  pdeno1                       CHAR(18)
  pdeno2                       CHAR(18)
  pdeno3                       CHAR(18)
  pdeno4                       CHAR(18)
  pdeno5                       CHAR(18)
  pdeno6                       CHAR(18)
  pdeno7                       CHAR(18)
  pdeno8                       CHAR(18)
  pdeno9                       CHAR(18)
  pdeno10                      CHAR(18)
  pdeno11                      CHAR(18)
  pdeno12                      CHAR(18)
  pdeno13                      CHAR(18)
  pdeno14                      CHAR(18)
  pdeno15                      CHAR(18)
  pcant1                       FLOAT(8)
  pcant2                       FLOAT(8)
  pcant3                       FLOAT(8)
  pcant4                       FLOAT(8)
  pcant5                       FLOAT(8)
  pcant6                       FLOAT(8)
  pcant7                       FLOAT(8)
  pcant8                       FLOAT(8)
  pcant9                       FLOAT(8)
  pcant10                      FLOAT(8)
  pcant11                      FLOAT(8)
  pcant12                      FLOAT(8)
  pcant13                      FLOAT(8)
  pcant14                      FLOAT(8)
  pcant15                      FLOAT(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pcodigo_proveedor` | `CHAR(4)` | — | — |
| `pcajeroprincipal` | `CHAR(8)` | — | — |
| `pfolio_suc` | `CHAR(16)` | — | — |
| `ptransaccion` | `CHAR(4)` | — | — |
| `pdivisa` | `CHAR(2)` | — | — |
| `pmonto` | `MONEY(14,2)` | — | — |
| `pfecha` | `DATE` | — | — |
| `pdeno1` | `CHAR(18)` | — | — |
| `pdeno2` | `CHAR(18)` | — | — |
| `pdeno3` | `CHAR(18)` | — | — |
| `pdeno4` | `CHAR(18)` | — | — |
| `pdeno5` | `CHAR(18)` | — | — |
| `pdeno6` | `CHAR(18)` | — | — |
| `pdeno7` | `CHAR(18)` | — | — |
| `pdeno8` | `CHAR(18)` | — | — |
| `pdeno9` | `CHAR(18)` | — | — |
| `pdeno10` | `CHAR(18)` | — | — |
| `pdeno11` | `CHAR(18)` | — | — |
| `pdeno12` | `CHAR(18)` | — | — |
| `pdeno13` | `CHAR(18)` | — | — |
| `pdeno14` | `CHAR(18)` | — | — |
| `pdeno15` | `CHAR(18)` | — | — |
| `pcant1` | `FLOAT(8)` | — | — |
| `pcant2` | `FLOAT(8)` | — | — |
| `pcant3` | `FLOAT(8)` | — | — |
| `pcant4` | `FLOAT(8)` | — | — |
| `pcant5` | `FLOAT(8)` | — | — |
| `pcant6` | `FLOAT(8)` | — | — |
| `pcant7` | `FLOAT(8)` | — | — |
| `pcant8` | `FLOAT(8)` | — | — |
| `pcant9` | `FLOAT(8)` | — | — |
| `pcant10` | `FLOAT(8)` | — | — |
| `pcant11` | `FLOAT(8)` | — | — |
| `pcant12` | `FLOAT(8)` | — | — |
| `pcant13` | `FLOAT(8)` | — | — |
| `pcant14` | `FLOAT(8)` | — | — |
| `pcant15` | `FLOAT(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(20)` | L11 |
| `cfolio` | `CHAR(8)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_faltsob_cg` | `bdisuc` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_faltsob_cg";` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?tsob` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?tsob` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_genera_archivo_bym`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_genera_archivo_bym.sql` |
| **LOC (1er CREATE)** | 152 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera cg — Canal/Cuenta General, archivo y Billetes y Monedas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_genera_archivo_bym(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      CHAR(1)
  pRuta                        CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pRuta` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |
| `cNombreArchivo` | `CHAR(30)` | L10 |
| `cCmd1` | `CHAR(2500)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `pRutaGra` | `CHAR(100)` | L13 |
| `cDelFile` | `CHAR(200)` | L14 |
| `cFolio` | `INTEGER` | L15 |
| `bInTransaction` | `BOOLEAN` | L17 |
| `ven_transacc` | `SMALLINT` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_billetesfalsos` | `bdicnweb` | no | SELECT | L93 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | FÓRMULA | `LET ven_transacc = 0; --` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L92 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"RPAD(NVL(folio,''),20)\|\|RPAD(NVL(nota,''),278)\|\|'*'\|\|LPAD(NVL` |  |
| L111 | FÓRMULA | `LET cSql= "sed 's/\|$//g;/^$/d' " \|\|  TRIM(pRuta) \|\|  cNombreArchivo \|\| " > " \|\| TRIM(pRuta)` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `genera` | ACCION | genera / produce | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bym` | ENTIDAD | Billetes y Monedas (efectivo en sucursal — evidencia: 'pieza | 🟡 INFERIDO | nombre_sp |

---

## `sp_cg_genreportedepositoscoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_genreportedepositoscoppel.sql` |
| **LOC (1er CREATE)** | 156 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 98 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cg — Canal/Cuenta General, depósito, OS — Originación de Solicitudes y Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=4 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_genreportedepositoscoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoEjecucion               CHAR(1)
  pInicio                      DATE
  pFin                         DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |
| `pInicio` | `DATE` | — | — |
| `pFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `cCodRetSp` | `CHAR(5)` | L25 |
| `iCodRetSp` | `INTEGER` | L26 |
| `cEmpresa` | `CHAR(3)` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |
| `cSucursal` | `CHAR(70)` | L29 |
| `cUsuario` | `CHAR(8)` | L30 |
| `cNombre` | `CHAR(50)` | L31 |
| `dFechaOperacion` | `DATE` | L32 |
| `cFolioOper` | `CHAR(8)` | L33 |
| `cDescripcion` | `CHAR(50)` | L34 |
| `mMonto_Tot` | `MONEY(14,2)` | L35 |
| `mDenominacion1` | `INTEGER` | L36 |
| `mDenominacion2` | `INTEGER` | L37 |
| `mDenominacion3` | `INTEGER` | L38 |
| `mDenominacion4` | `INTEGER` | L39 |
| `mDenominacion5` | `INTEGER` | L40 |
| `mDenominacion6` | `INTEGER` | L41 |
| `mDenominacion7` | `INTEGER` | L42 |
| `cCajagen` | `CHAR(70)` | L43 |
| `cComprobante` | `CHAR(10)` | L44 |
| `mImporteComp` | `MONEY(14,2)` | L45 |
| `mFaltante` | `MONEY(14,2)` | L46 |
| `mSobrante` | `MONEY(14,2)` | L47 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L110 |
| `ss_bitacora_deposito_coppel` | `bdisuc` | ⚠️ sí | SELECT | L126 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L97 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L85 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoEjecucion = '' OR pInicio IS NULL OR pFin IS NULL OR pRe` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L116 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L131 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?orte`, `?it` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_grabadepositoscoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_grabadepositoscoppel.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 97 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba cg — Canal/Cuenta General, depósito, OS — Originación de Solicitudes y Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_grabadepositoscoppel(
  pUsuario                     CHAR(10)
  pIdFuncion                   CHAR(10)
  pHora                        CHAR(5)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(10)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pHora` | `CHAR(5)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cClave_transaccion` | `CHAR(4)` | L5 |
| `cDesc_transaccion` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |
| `dFecha` | `DATE` | L10 |
| `cComprobante` | `CHAR(10)` | L11 |
| `cSucCoppel` | `CHAR(10)` | L12 |
| `cNombreEmp` | `CHAR(80)` | L13 |
| `cNombreSuc` | `CHAR(40)` | L14 |
| `cCajaGeneral` | `CHAR(4)` | L15 |
| `cPlaza` | `CHAR(20)` | L16 |
| `mImpComprobante` | `MONEY(14,2)` | L17 |
| `mImpFicha` | `MONEY(14,2)` | L18 |
| `iCantidad1` | `INTEGER` | L19 |
| `iCantidad2` | `INTEGER` | L20 |
| `iCantidad3` | `INTEGER` | L21 |
| `iCantidad4` | `INTEGER` | L22 |
| `iCantidad5` | `INTEGER` | L23 |
| `iCantidad6` | `INTEGER` | L24 |
| `iCantidad7` | `MONEY(14,2)` | L25 |
| `mFaltante` | `MONEY(14,2)` | L26 |
| `mSobrante` | `MONEY(14,2)` | L27 |
| `iTotReg` | `INTEGER` | L28 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_temp_deposito_coppel` | `bdisuc` | ⚠️ sí | SELECT | L92 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | INSERT | L94 |
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | INSERT | L97 |
| `ss_cajageneral` | `bdisuc` | ⚠️ sí | UPDATE | L100 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L114 |
| `ss_bitacora_deposito_coppel` | `bdisuc` | ⚠️ sí | INSERT | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?it` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_insertainfocomprobantetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_insertainfocomprobantetv.sql` |
| **LOC (1er CREATE)** | 50 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta cg — Canal/Cuenta General, información y ro — Rol Operativo (complemento, anterior)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_insertainfocomprobantetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolioComprobante            CHAR(16)
  pIdSolicitud                 CHAR(25)
  pFechaHora                   CHAR(20)
  pEstado                      CHAR(20)
  pMisc1                       CHAR(40)
  pMisc2                       CHAR(40)
  pMisc3                       CHAR(40)
  pMisc4                       CHAR(40)
  pMisc5                       CHAR(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolioComprobante` | `CHAR(16)` | `comp`=complemento · `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔵 CONVENCIÓN / 🔴 SINTÉTICO |
| `pIdSolicitud` | `CHAR(25)` | — | — |
| `pFechaHora` | `CHAR(20)` | — | — |
| `pEstado` | `CHAR(20)` | — | — |
| `pMisc1` | `CHAR(40)` | — | — |
| `pMisc2` | `CHAR(40)` | — | — |
| `pMisc3` | `CHAR(40)` | — | — |
| `pMisc4` | `CHAR(40)` | — | — |
| `pMisc5` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_informacion_comprobante_etv` | `bdisuc` | ⚠️ sí | INSERT | L32 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L27 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `ro`, `?b`, `?etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_lecturarchivosmelb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_lecturarchivosmelb.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General y archivos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_lecturarchivosmelb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               INTEGER
  pDireccionMac                CHAR(12)
  pRutaArchivo                 CHAR(100)
  pNombreArchivo               CHAR(40)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `INTEGER` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pRutaArchivo` | `CHAR(100)` | — | — |
| `pNombreArchivo` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cIsamErr` | `INTEGER` | L6 |
| `cDescErr` | `CHAR(50)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cSQL` | `CHAR(500)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `cCampos` | `CHAR(1024)` | L13 |
| `cTablaDst` | `CHAR(150)` | L14 |
| `cBaseDatos` | `CHAR(50)` | L15 |
| `cCmd1` | `CHAR(2000)` | L16 |
| `cCmd2` | `CHAR(2000)` | L17 |
| `cUsrBin` | `CHAR(15)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_recibe_datosconcentracionws` | `bdisuc` | ⚠️ sí | SELECT | L74 |
| `ss_recibe_datosconcentracionws` | `bdisuc` | ⚠️ sí | DELETE | L74 |
| `ss_recibe_datosdotacionws` | `bdisuc` | ⚠️ sí | SELECT | L92 |
| `ss_recibe_datosdotacionws` | `bdisuc` | ⚠️ sí | DELETE | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pDireccionMac = '' OR pRutaArchivo = '' OR pNombreArchivo = ` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | FÓRMULA | `LET cCmd2 = TRIM(cCmd1)\|\|TRIM(cCampos)\|\|")"\|\|'"'\|\|" \| /ifxsif01/bin/dbaccess bdicnweb > /de` |  |
| L105 | FÓRMULA | `LET cCmd2 = TRIM(cCmd1)\|\|TRIM(cCampos)\|\|")"\|\|'"'\|\|" \| /ifxsif01/bin/dbaccess bdicnweb > /de` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?_lectur` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivos` | ENTIDAD | archivos | 🔵 CONVENCIÓN | nombre_sp |
| `?melb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?_lectur`, `?melb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_polizadepcoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_polizadepcoppel.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, póliza contable, depósito y Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_polizadepcoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `wtesoreria` | `CHAR(4)` | L9 |
| `pempresa` | `CHAR(3)` | L10 |
| `vtranenvio` | `CHAR(4)` | L11 |
| `wsucursal` | `CHAR(4)` | L12 |
| `wdivisa` | `CHAR(2)` | L13 |
| `wprocedencia` | `CHAR(4)` | L14 |
| `vnaturaleza` | `CHAR(1)` | L15 |
| `vtipo_tran` | `CHAR(2)` | L16 |
| `wmonto` | `MONEY(14,2)` | L17 |
| `wfecha_hoy` | `DATE` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L51 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L56 |
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L65 |
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_contacg` | `bdisuc` | ⚠️ sí | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_poliza_faltsob";` |  |
| L87 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_poliza_depcoppel";` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_polizafaltsob`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_polizafaltsob.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 96 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General, póliza contable y fal — fallo/disputa" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_polizafaltsob(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  vfolio_oper                  CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `vfolio_oper` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `wfecha_hoy` | `DATE` | L9 |
| `wtesoreria` | `CHAR(4)` | L10 |
| `pempresa` | `CHAR(3)` | L11 |
| `vtranenvio` | `CHAR(4)` | L12 |
| `wsucursal` | `CHAR(4)` | L13 |
| `wdivisa` | `CHAR(2)` | L14 |
| `wprocedencia` | `CHAR(4)` | L15 |
| `vnaturaleza` | `CHAR(1)` | L16 |
| `vtipo_tran` | `CHAR(2)` | L17 |
| `wmonto` | `MONEY(14,2)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L55 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L59 |
| `ss_param_cajagen` | `bdicnweb` | no | SELECT | L67 |
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `bdisuc` | `bdicnweb` | no | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_poliza_faltsob";` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | 🔵 CONVENCIÓN | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?tsob` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?tsob` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_reporteenviodotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_reporteenviodotaciones.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía cg — Canal/Cuenta General, reporte y dotaciones de efectivo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo · `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_reporteenviodotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pCodProveedor                CHAR(4)
  pSucursal                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pArchDescarga                CHAR(150)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pCodProveedor` | `CHAR(4)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `cCmdSelect` | `CHAR(2500)` | L14 |
| `cCmdFrom` | `CHAR(2500)` | L15 |
| `cCmdWhere` | `CHAR(2500)` | L16 |
| `cCmdCount` | `CHAR(2500)` | L17 |
| `cStatus` | `CHAR(2)` | L18 |
| `iRegistros` | `INTEGER` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L55 |
| `bdicnweb` | `bdicnweb` | no | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | VALIDACIÓN_NULL | `IF pFechaInicio IS NULL THEN` |  |
| L50 | VALIDACIÓN_NULL | `IF pFechaFin IS NULL THEN` |  |
| L67 | FÓRMULA | `LET cCmdSelect = " SELECT TRIM(sucursal) AS sucursal, TRIM(caja_general) AS caja_general, TRIM(TO_CH` | 🔴 MONEY/aritmética financiera |
| L79 | FÓRMULA | `LET cCmdCount = "SELECT COUNT(*)"\|\|" "\|\|TRIM(cCmdFrom)\|\|" "\|\|TRIM(cCmdWhere);` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `envio` | ACCION | envía | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_reversodotacionmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_reversodotacionmasivo.sql` |
| **LOC (1er CREATE)** | 255 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 211 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reverso cg — Canal/Cuenta General y dotación de efectivo (masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_reversodotacionmasivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iRegsProcesados` | `INTEGER` | L7 |
| `iNoRegsAfectados` | `INTEGER` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iNoRegsTotales` | `INTEGER` | L11 |
| `iIdRegistro` | `INTEGER` | L13 |
| `cIdUsuarioSoc` | `CHAR(8)` | L14 |
| `cSucursal` | `CHAR(50)` | L15 |
| `cCajaGen` | `CHAR(50)` | L16 |
| `dFecha` | `DATE` | L17 |
| `cDescStatus` | `CHAR(50)` | L18 |
| `cFolioOperacion` | `CHAR(8)` | L19 |
| `dMonto` | `DECIMAL(14,2)` | L20 |
| `cUsuario` | `CHAR(50)` | L21 |
| `cIdUsuario` | `CHAR(8)` | L22 |
| `cIdStatus` | `CHAR(2)` | L23 |
| `cCodTrans` | `CHAR(4)` | L24 |
| `cTpoSucursal` | `CHAR(2)` | L25 |
| `cCodProveedor` | `CHAR(4)` | L26 |
| `cIdSucursal` | `CHAR(4)` | L27 |
| `cStatus` | `CHAR(2)` | L28 |
| `iIdLote` | `INTEGER` | L29 |
| `cCodRetorno` | `CHAR(5)` | L30 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_cg_dotaciones_masivo` | `bdicnweb` | no | INSERT | L100 |
| `sw_cg_enviodotaciones` | `bdicnweb` | no | SELECT | L127 |
| `sw_cg_enviodotaciones_hist` | `bdicnweb` | no | INSERT | L214 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L102 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL THEN` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L162 | FÓRMULA | `LET iRegsProcesados = iRegsProcesados + 1;` |  |
| L173 | FÓRMULA | `LET iNoRegsTotales = iNoRegsTotales + 1;` |  |
| L174 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L213 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L219 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cg_tipooperacionetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_tipooperacionetv.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cg — Canal/Cuenta General (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_tipo_operacion_etv` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_tipooperacionetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cTipoOperacion` | `CHAR(20)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_tipo_operacion_etv` | `bdisuc` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_tipo_operacion_etv';` |  |
| L52 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operacion` | ACCION | operación | 🔵 CONVENCIÓN | nombre_sp |
| `?etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_tiporeportetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_tiporeportetv.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General y reporte (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_tipo_reporte_etv2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_tiporeportetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cTipoOperacion` | `CHAR(35)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_tipo_reporte_etv2` | `bdisuc` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_tipo_reporte_etv';` |  |
| L52 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?tv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_tiposervicioacusesetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_tiposervicioacusesetv.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General y servicio (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_tipo_servicio_acuses_etv` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_tiposervicioacusesetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cTipoServicio` | `CHAR(15)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_tipo_servicio_acuses_etv` | `bdisuc` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_tipo_servicio_acuses_` |  |
| L52 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?acusesetv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?acusesetv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_tiposervicioetv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_tiposervicioetv.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cg — Canal/Cuenta General y servicio (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_tipo_servicio_etv2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_tiposervicioetv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cTipoServicio` | `CHAR(30)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_tipo_servicio_etv2` | `bdisuc` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_tipo_servicio_etv';` |  |
| L52 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cg_validasaldoarqueosucucaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cg_validasaldoarqueosucucaja.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida cg — Canal/Cuenta General, saldo, OS — Originación de Solicitudes y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cg_validasaldoarqueosucucaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdPlaza                     CHAR(3)
  pIdSucursal                  CHAR(4)
  pIdCajeroPrincArq            CHAR(8)
  pFechaArq                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdPlaza` | `CHAR(3)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pIdCajeroPrincArq` | `CHAR(8)` | — | — |
| `pFechaArq` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `mSaldoTot` | `MONEY(14,2)` | L12 |
| `mMontoMin` | `MONEY(14,2)` | L13 |
| `mMontoMax` | `MONEY(14,2)` | L14 |
| `mTotalDiferenciaArq` | `MONEY(14,2)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | VALIDACIÓN_NULL | `IF mSaldoTot IS NULL OR mMontoMin IS NULL OR mMontoMax IS NULL THEN` |  |
| L64 | FÓRMULA | `LET mTotalDiferenciaArq = mMontoMin - mSaldoTot;` | 🔴 MONEY/aritmética financiera |
| L67 | FÓRMULA | `LET mTotalDiferenciaArq = mMontoMax - mSaldoTot;` | 🔴 MONEY/aritmética financiera |
| L73 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldo` | ENTIDAD | saldo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?arque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ucu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cg`, `?arque`, `?ucu` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cheques_devueltos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cheques_devueltos.sql` |
| **LOC (1er CREATE)** | 204 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "devolución cheques y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 6 llamada(s): `sp_ope_consultacheqsimg`, `sp_ope_consultachequesdevueltos`, `sp_ope_consultachequesdevueltos_totales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cheques_devueltos(
  pBandera                     CHAR(2)
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pNumCheque                   CHAR(7)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pSistema                     CHAR(20)
  pConsecutivo                 CHAR(3)
  pCodBanco                    CHAR(3)
  pFechaPresenta               DATE
  pArchivo                     CHAR(100)
  pLado                        CHAR(1)
  pStatusImg                   SMALLINT
  pLimpia                      CHAR(1)
  pNumCte                      CHAR(9)
  pFecha                       DATE
) RETURNING CHAR(5) 	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |
| `pSistema` | `CHAR(20)` | — | — |
| `pConsecutivo` | `CHAR(3)` | — | — |
| `pCodBanco` | `CHAR(3)` | — | — |
| `pFechaPresenta` | `DATE` | — | — |
| `pArchivo` | `CHAR(100)` | — | — |
| `pLado` | `CHAR(1)` | — | — |
| `pStatusImg` | `SMALLINT` | — | — |
| `pLimpia` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L44 |
| `iSqlErr` | `INTEGER` | L45 |
| `iNoRegistros` | `INTEGER` | L46 |
| `cMotivoDevolucion` | `CHAR(2)` | L47 |
| `cDecripcion` | `CHAR(35)` | L48 |
| `cEmpresa` | `CHAR(3)` | L49 |
| `cClaveBanco` | `CHAR(3)` | L50 |
| `cDescripcionBanco` | `CHAR(40)` | L51 |
| `cCuenta` | `CHAR(20)` | L52 |
| `NumCheque` | `CHAR(7)` | L53 |
| `fechaPresenta` | `DATE` | L54 |
| `mMonto` | `MONEY(18,2)` | L55 |
| `mMontoAplica` | `MONEY(18,2)` | L56 |
| `mSumaComision` | `MONEY(18,2)` | L57 |
| `cFormatoImagen` | `CHAR(3)` | L58 |
| `iTamanoImagenA` | `INTEGER` | L59 |
| `iTamanoImagenB` | `INTEGER` | L60 |
| `iConsecutivo` | `INTEGER` | L61 |
| `dFechaHoy` | `DATE` | L62 |
| `cImgFormato` | `CHAR(3)` | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ope_consultacheqsimg` | `bdicnweb` | no | L157 |
| `sp_ope_consultachequesdevueltos` | `bdicnweb` | no | L163 |
| `sp_ope_consultachequesdevueltos_totales` | `bdicnweb` | no | L169 |
| `sp_ope_consultaconsecutivoarch` | `bdicnweb` | no | L174 |
| `sp_ope_grabaimagenchqsdevueltos` | `bdicnweb` | no | L184 |
| `sp_ope_validaimagenchequedev` | `bdicnweb` | no | L190 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?uelt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uelt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cla_consdetallelistasnegativas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cla_consdetallelistasnegativas.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle · IVA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cla_consdetallelistasnegativas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecutivo                   CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecutivo` | `CHAR(20)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `dFecha` | `DATE` | L16 |
| `cNum_cliente` | `CHAR(20)` | L17 |
| `cNom_cliente` | `CHAR(110)` | L18 |
| `cTipo_persona` | `CHAR(10)` | L19 |
| `cRazon_social` | `CHAR(60)` | L20 |
| `dFecha_constitucion` | `DATE` | L21 |
| `cRfc` | `CHAR(15)` | L22 |
| `cComentarios` | `CHAR(250)` | L23 |
| `dFecha_inclusion` | `DATE` | L24 |
| `cUsuario` | `CHAR(15)` | L25 |
| `cStatus` | `CHAR(8)` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_mant_listainterna` | `bdinteg` | ⚠️ sí | SELECT | L87 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L55 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | VALIDACIÓN_NULL | `IF pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L92 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cla_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?listasnegat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cla_`, `?listasnegat`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cla_consdetallelistasnegativas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cla_consdetallelistasnegativas_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle (totales) · IVA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cla_consdetallelistasnegativas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecutivo                   CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecutivo` | `CHAR(20)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_mant_listainterna` | `bdinteg` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L31 | VALIDACIÓN_NULL | `IF pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cla_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `?listasnegat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cla_`, `?listasnegat`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cla_eliminactelistasnegativas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cla_eliminactelistasnegativas.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina cliente · IVA" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cla_eliminactelistasnegativas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecutivo                   CHAR(20)
  pStatus                      CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecutivo` | `CHAR(20)` | — | — |
| `pStatus` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `iNumRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_mant_listainterna` | `bdinteg` | ⚠️ sí | UPDATE | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cla_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?listasnegat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cla_`, `?listasnegat`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cla_genrepdetallelistasnegativas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cla_genrepdetallelistasnegativas.sql` |
| **LOC (1er CREATE)** | 192 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte detalle · IVA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cla_genrepdetallelistasnegativas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRuta                        CHAR(100)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRuta` | `CHAR(100)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iIdRegistro` | `INTEGER` | L11 |
| `iSerial` | `INTEGER` | L13 |
| `cRespMensaje` | `CHAR(45)` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `cQuery` | `CHAR(255)` | L16 |
| `cCmd1` | `CHAR(2000)` | L17 |
| `cSql` | `CHAR(2500)` | L18 |
| `cRutaGral` | `CHAR(150)` | L19 |
| `cRutaInformix` | `CHAR(100)` | L20 |
| `cUsrBin` | `CHAR(100)` | L21 |
| `iCountRep` | `INTEGER` | L22 |
| `iProcesaRep` | `INT` | L23 |
| `iArmaReporte` | `INT` | L24 |
| `cArchivoCP` | `CHAR(45)` | L25 |
| `cCmdQuery` | `CHAR(2500)` | L26 |
| `bInTransaction` | `BOOLEAN` | L27 |
| `ven_transacc` | `SMALLINT` | L28 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(5)` | L29 |
| `cReporte` | `CHAR(100)` | L30 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L109 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L85 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRuta = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | FÓRMULA | `LET pRuta = TRIM(pRuta) \|\| '/';` |  |
| L110 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT ''''\|\|num_cliente,razon_social,T` |  |
| L127 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRuta)\|\|'query.sql';` |  |
| L136 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L141 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L150 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L159 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L167 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cla_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `?listasnegat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cla_`, `?listasnegat`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cla_insertactelistasnegativas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cla_insertactelistasnegativas.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta cliente · IVA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cla_insertactelistasnegativas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecutivo                   CHAR(20)
  pNombreCte                   CHAR(110)
  pTipoPersona                 CHAR(10)
  pRazonSocial                 CHAR(60)
  pFechaCon                    DATE
  pRfc                         CHAR(13)
  pComentarios                 CHAR(250)
  pFechaInc                    DATE
  pStatus                      CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecutivo` | `CHAR(20)` | — | — |
| `pNombreCte` | `CHAR(110)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pTipoPersona` | `CHAR(10)` | — | — |
| `pRazonSocial` | `CHAR(60)` | — | — |
| `pFechaCon` | `DATE` | — | — |
| `pRfc` | `CHAR(13)` | — | — |
| `pComentarios` | `CHAR(250)` | — | — |
| `pFechaInc` | `DATE` | — | — |
| `pStatus` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iExiste` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_mant_listainterna` | `bdinteg` | ⚠️ sí | SELECT | L40 |
| `si_mant_listainterna` | `bdinteg` | ⚠️ sí | INSERT | L44 |
| `si_mant_listainterna` | `bdinteg` | ⚠️ sí | UPDATE | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEjecutivo = '' OR pComentarios = '' OR pFechaInc IS NULL OR ` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cla_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?listasnegat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cla_`, `?listasnegat`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cla_validactelistasnegativas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cla_validactelistasnegativas.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida cliente · IVA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cla_validactelistasnegativas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecutivo                   CHAR(20)
  pEjecucion                   CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecutivo` | `CHAR(20)` | — | — |
| `pEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cStatus` | `CHAR(8)` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `iValidaExiste` | `CHAR(1)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_mant_listainterna` | `bdinteg` | ⚠️ sí | SELECT | L48 |
| `si_mant_listainterna` | `bdinteg` | ⚠️ sí | UPDATE | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cla_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?listasnegat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cla_`, `?listasnegat`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cli_busquedacalle`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cli_busquedacalle.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda calle" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cli_busquedacalle(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripcion                 CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripcion` | `CHAR(30)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `iRegistros` | `INTEGER` | L9 |
| `iNumeroCalle` | `INTEGER` | L10 |
| `cNombreCalle` | `CHAR(30)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDescripcion = '' OR pRegistros IS NULL OR pRecuperacion IS N` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L54 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cli_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `calle` | ENTIDAD | calle (domicilio) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cli_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cli_busquedacalle_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cli_busquedacalle_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda calle (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cli_busquedacalle_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripcion                 CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripcion` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cli_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `calle` | ENTIDAD | calle (domicilio) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cli_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cli_busquedazona`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cli_busquedazona.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda zona" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cli_busquedazona(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCiudadCoppel              SMALLINT
  pDescripcion                 CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCiudadCoppel` | `SMALLINT` | — | — |
| `pDescripcion` | `CHAR(30)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `iIdNumeroColonia` | `SMALLINT` | L10 |
| `cNombreZona` | `CHAR(32)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pIdUsuario = '' OR pIdFuncion = '' OR pDescripcion = '' OR pRegistros IS NULL OR pRecuperacion IS` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L60 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cli_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `zona` | ENTIDAD | zona | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cli_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cli_busquedazona_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cli_busquedazona_totales.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda zona (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cli_busquedazona_totales(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCiudadCoppel              SMALLINT
  pDescripcion                 CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCiudadCoppel` | `SMALLINT` | — | — |
| `pDescripcion` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cli_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `zona` | ENTIDAD | zona | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cli_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cli_consultadocumentos_especificos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cli_consultadocumentos_especificos.sql` |
| **LOC (1er CREATE)** | 183 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta documentos y OS — Originación de Solicitudes (especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cli_consultadocumentos_especificos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               SMALLINT
  pTiposDocumentos             CHAR(255)
  pDireccionMAC                CHAR(15)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pTiposDocumentos` | `CHAR(255)` | `documentos`=documentos · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / ✅ CÓDIGO |
| `pDireccionMAC` | `CHAR(15)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `iCodRetSp` | `INTEGER` | L21 |
| `cEmpresa` | `CHAR(3)` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |
| `cCodigoDocto` | `CHAR(4)` | L24 |
| `cCodDocto` | `CHAR(4)` | L25 |
| `cDescDocto` | `CHAR(35)` | L26 |
| `cCodGrupo` | `CHAR(3)` | L27 |
| `cDescGrupo` | `CHAR(30)` | L28 |
| `cMultiImg` | `CHAR(1)` | L29 |
| `iImgTamMax` | `INTEGER` | L30 |
| `cImgFormato` | `CHAR(3)` | L31 |
| `iImgCompresion` | `SMALLINT` | L32 |
| `dImgLargo` | `DECIMAL` | L33 |
| `dImgAncho` | `DECIMAL` | L34 |
| `iImgDPI` | `SMALLINT` | L35 |
| `cImgColores` | `CHAR(1)` | L36 |
| `cGenerico` | `CHAR(1)` | L37 |
| `cLigar` | `CHAR(1)` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cli_tmp_docespecificos` | `bdicnweb` | no | SELECT | L119 |
| `sw_cli_tmp_docespecificos` | `bdicnweb` | no | INSERT | L126 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L111 |
| `sp_split_cadena` | `bdicnweb` | no | L121 |
| `sp_dgconsultadocumentoespecifico` | `bdidigital` | ⚠️ sí | L122 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion IS NULL OR pDireccionMAC = '' THEN` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00440';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | VALIDACIÓN_NULL | `IF pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L151 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cli_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `documentos` | ENTIDAD | documentos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?ecific` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cli_`, `?ecific` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_cons_expediente2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_cons_expediente2.sql` |
| **LOC (1er CREATE)** | 151 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo y expediente" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_cons_expediente2(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMCLIENTE                  CHAR(20)
  pNumRegistro                 INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNUMCLIENTE` | `CHAR(20)` | — | — |
| `pNumRegistro` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_codret` | `CHAR(5)` | L16 |
| `v_cuenta` | `CHAR(20)` | L17 |
| `v_cve_prod` | `CHAR(4)` | L18 |
| `v_prod_nombre` | `CHAR(40)` | L19 |
| `v_cod_docto` | `CHAR(4)` | L20 |
| `v_fecha_alta` | `DATE` | L21 |
| `v_cod_grupo` | `CHAR(3)` | L22 |
| `v_descrip_gpo` | `CHAR(30)` | L23 |
| `v_descrip_docto` | `CHAR(35)` | L24 |
| `v_descrip2` | `CHAR(30)` | L25 |
| `v_multi_img` | `CHAR(1)` | L26 |
| `v_secuencia` | `SMALLINT` | L27 |
| `iCont` | `SMALLINT` | L28 |
| `v_SistemaC` | `CHAR(2)` | L30 |
| `cNumCtePrincipal` | `CHAR(20)` | L31 |
| `iTpo_cliente` | `INT` | L32 |
| `iTotalReg` | `INTEGER` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_expedientetotales_tmp` | `bdicnweb` | no | SELECT | L117 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L84 | CÓDIGO_RETORNO | `LET v_codret='00098';` |  |
| L89 | CÓDIGO_RETORNO | `LET v_codret='00098';` |  |
| L121 | FÓRMULA | `LET iTotalReg = iTotalReg +1;` |  |
| L124 | CÓDIGO_RETORNO | `LET v_codret = '00000';` |  |
| L133 | CÓDIGO_RETORNO | `LET v_codret = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `expediente` | ENTIDAD | expediente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_consarchivosgenerados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consarchivosgenerados.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, archivos y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consarchivosgenerados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistemaCuenta               CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistemaCuenta` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cNombreArchivo` | `CHAR(45)` | L13 |
| `dFecha` | `DATE` | L14 |
| `cFecha` | `CHAR(10)` | L15 |
| `cHora` | `CHAR(5)` | L16 |
| `dFechaHoy` | `DATE` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_archivosgenerados` | `bdicnweb` | no | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSistemaCuenta = '' OR pRegistros IS NULL OR pRecuperacion IS` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L69 | FÓRMULA | `LET cFecha = LPAD(DAY(dFecha),2,0)\|\|'/'\|\|LPAD(MONTH(dFecha),2,0)\|\|'/'\|\|YEAR(dFecha);` |  |
| L71 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_consarchivosgenerados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consarchivosgenerados_totales.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, archivos y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consarchivosgenerados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistemaCuenta               CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistemaCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cNombreArchivo` | `CHAR(45)` | L10 |
| `dFecha` | `DATE` | L11 |
| `cFecha` | `CHAR(10)` | L12 |
| `cHora` | `CHAR(5)` | L13 |
| `dFechaHoy` | `DATE` | L14 |
| `iNumRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_archivosgenerados` | `bdicnweb` | no | SELECT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_consctedetalle_3`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consctedetalle_3.sql` |
| **LOC (1er CREATE)** | 469 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, cliente y detalle" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consctedetalle_3(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 char(10)
  cnumcte                      char(20)
  cTPERSONA                    CHAR(6)
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `char(10)` | — | — |
| `cnumcte` | `char(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `cTPERSONA` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iexiste` | `INT` | L51 |
| `cCodRet` | `CHAR(5)` | L52 |
| `iSql_err` | `INT` | L53 |
| `cNumeroCliente` | `CHAR(20)` | L54 |
| `cNacionalidad` | `CHAR(15)` | L55 |
| `cCdomiciliacion` | `CHAR(1)` | L56 |
| `cDDomiliciacion` | `CHAR(30)` | L57 |
| `cCBPI` | `CHAR(1)` | L58 |
| `cDBPI` | `CHAR(30)` | L59 |
| `cCpagos` | `CHAR(1)` | L60 |
| `cDpagos` | `CHAR(30)` | L61 |
| `cTpo_persona` | `CHAR(2)` | L62 |
| `cActividadEc` | `CHAR(50)` | L63 |
| `cSubActividadEc` | `CHAR(50)` | L64 |
| `vcodret1` | `CHAR(3)` | L67 |
| `vtipocorreo` | `SMALLINT` | L68 |
| `vstatuscorreo` | `CHAR(1)` | L69 |
| `cEstado_civil` | `CHAR(20)` | L72 |
| `clugar_nac` | `CHAR(2)` | L73 |
| `cNo_fm3` | `CHAR(18)` | L74 |
| `cCurp` | `CHAR(20)` | L75 |
| `cEscolaridad` | `CHAR(30)` | L76 |
| `cProfesion` | `CHAR(60)` | L77 |
| `cEmail` | `CHAR(100)` | L78 |
| `cClienteCop` | `CHAR(20)` | L79 |
| *…32 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L239 |
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L241 |
| `dom_autorizaciones` | `bdidomi` | ⚠️ sí | SELECT | L253 |
| `bpi_usuario` | `bdibpi` | ⚠️ sí | SELECT | L263 |
| `pp_pagoprog` | `bdiprog` | ⚠️ sí | SELECT | L273 |
| `si_cteppes` | `bdinteg` | ⚠️ sí | SELECT | L284 |
| `si_puestosppes` | `bdinteg` | ⚠️ sí | SELECT | L294 |
| `si_ingresos` | `bdinteg` | ⚠️ sí | SELECT | L355 |
| `si_nacion` | `bdinteg` | ⚠️ sí | SELECT | L418 |
| `si_apoderado` | `bdinteg` | ⚠️ sí | SELECT | L423 |
| `si_correos` | `bdinteg` | ⚠️ sí | SELECT | L430 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L221 |
| `sp_validacte_transfer` | `bdicnweb` | no | L233 |
| `sp_consulta_correos` | `bdinteg` | ⚠️ sí | L346 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L450 | VALIDACIÓN_NULL | `IF 	cTpo_persona IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `?_3` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_3` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_consdetallemovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consdetallemovimientos.sql` |
| **LOC (1er CREATE)** | 192 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 65 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, detalle y movimientos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consdetallemovimientos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistemaCuenta               CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pNumCuenta                   CHAR(20)
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pImporte                     MONEY(14,2)
  pClaveMov                    CHAR(50)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistemaCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pClaveMov` | `CHAR(50)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `cCodRetSp` | `CHAR(5)` | L28 |
| `iCodRetSp` | `INTEGER` | L29 |
| `cEmpresa` | `CHAR(3)` | L30 |
| `cCmd1` | `CHAR(2000)` | L31 |
| `cSql` | `CHAR(2500)` | L32 |
| `cRutaGral` | `CHAR(150)` | L33 |
| `cNombreArchivo` | `CHAR(45)` | L34 |
| `iRecuperacion` | `INTEGER` | L35 |
| `bInTransaction` | `BOOLEAN` | L36 |
| `ven_transacc` | `SMALLINT` | L37 |
| `dFecha` | `DATE` | L39 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L40 |
| `cCveTransacc` | `CHAR(4)` | L41 |
| `cDescTransacc` | `CHAR(50)` | L42 |
| `cFolio` | `CHAR(16)` | L43 |
| `dPeriodoInicial` | `DATE` | L44 |
| `mMonto` | `MONEY(14,2)` | L45 |
| `dPeriodoFinal` | `DATE` | L46 |
| `cSisCuenta` | `CHAR(20)` | L47 |
| `cNaturaleza` | `CHAR(1)` | L48 |
| `cReferencia` | `CHAR(40)` | L49 |
| `cReversos` | `CHAR(1)` | L50 |
| `cSucursal` | `CHAR(4)` | L51 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_movimientos` | `bdicnweb` | no | SELECT | L146 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L130 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L115 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSistemaCuenta = '' OR pFechaInicial IS NULL OR pFechaFinal I` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L139 | FÓRMULA | `LET dFechaInicial=LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|YE` |  |
| L140 | FÓRMULA | `LET dFechaFinal  =LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|YEAR(p` |  |
| L153 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_cnsif_consdetallemovimientos2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consdetallemovimientos2.sql` |
| **LOC (1er CREATE)** | 238 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, detalle y movimientos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consdetallemovimientos2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistemaCuenta               CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pNumCuenta                   CHAR(20)
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pImporte                     MONEY(14,2)
  pClaveMov                    CHAR(50)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) 		AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistemaCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pClaveMov` | `CHAR(50)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L30 |
| `iSqlErr` | `INTEGER` | L31 |
| `cCodRetSp` | `CHAR(5)` | L32 |
| `iCodRetSp` | `INTEGER` | L33 |
| `cEmpresa` | `CHAR(3)` | L34 |
| `cCmd1` | `CHAR(2000)` | L35 |
| `cSql` | `CHAR(2500)` | L36 |
| `cRutaGral` | `CHAR(150)` | L37 |
| `cNombreArchivo` | `CHAR(45)` | L38 |
| `iRecuperacion` | `INTEGER` | L39 |
| `bInTransaction` | `BOOLEAN` | L40 |
| `ven_transacc` | `SMALLINT` | L41 |
| `dFecha` | `DATE` | L43 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L44 |
| `cCveTransacc` | `CHAR(4)` | L45 |
| `cDescTransacc` | `CHAR(50)` | L46 |
| `cFolio` | `CHAR(16)` | L47 |
| `dPeriodoInicial` | `DATE` | L48 |
| `mMonto` | `MONEY(14,2)` | L49 |
| `dPeriodoFinal` | `DATE` | L50 |
| `cSisCuenta` | `CHAR(20)` | L51 |
| `cNaturaleza` | `CHAR(1)` | L52 |
| `cReferencia` | `CHAR(40)` | L53 |
| `cReversos` | `CHAR(1)` | L54 |
| `cSucursal` | `CHAR(4)` | L55 |
| *…17 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_movimientos` | `bdicnweb` | no | SELECT | L163 |
| `sd_ce_ctas_nostro` | `bdicred` | ⚠️ sí | SELECT | L173 |
| `tblhistpago` | `bdispei` | ⚠️ sí | SELECT | L178 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L145 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L130 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSistemaCuenta = '' OR pFechaInicial IS NULL OR pFechaFinal I` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L154 | FÓRMULA | `LET dFechaInicial=LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|YE` |  |
| L155 | FÓRMULA | `LET dFechaFinal  =LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|YEAR(p` |  |
| L188 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L195 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_consdetallemovimientos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consdetallemovimientos_totales.sql` |
| **LOC (1er CREATE)** | 296 |
| **Callgraph** | ✅ fan_in=25 / fan_out=10 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, detalle y movimientos (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consdetallemovimientos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistemaCuenta               CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pNumCuenta                   CHAR(20)
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pImporte                     MONEY(14,2)
  pEjecucion                   CHAR(1)
  pClaveMov                    CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistemaCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pEjecucion` | `CHAR(1)` | — | — |
| `pClaveMov` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `cRutaGral` | `CHAR(150)` | L13 |
| `cNombreArchivo` | `CHAR(45)` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |
| `bInTransaction` | `BOOLEAN` | L16 |
| `ven_transacc` | `SMALLINT` | L17 |
| `dFecha` | `DATE` | L19 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L20 |
| `cCveTransacc` | `CHAR(4)` | L21 |
| `cDescTransacc` | `CHAR(50)` | L22 |
| `cFolio` | `CHAR(16)` | L23 |
| `dPeriodoInicial` | `DATE` | L24 |
| `mMonto` | `MONEY(14,2)` | L25 |
| `dPeriodoFinal` | `DATE` | L26 |
| `cSisCuenta` | `CHAR(20)` | L27 |
| `cNaturaleza` | `CHAR(1)` | L28 |
| `cReferencia` | `CHAR(40)` | L29 |
| `cReversos` | `CHAR(1)` | L30 |
| `cSucursal` | `CHAR(4)` | L31 |
| *…17 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_statusproceso` | `bdicnweb` | no | INSERT | L139 |
| `sw_cons_movimientos` | `bdicnweb` | no | SELECT | L203 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L151 |
| `sp_cnsif_consultatotalmovtosdiarioscta_2` | `bdicnweb` | no | L161 |
| `sp_cnsif_consultamovtosdiarioscta3_2` | `bdinteg` | ⚠️ sí | L187 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L143 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSistemaCuenta = '' OR pFechaInicial IS NULL OR pFechaFinal I` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L166 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consultatotalmovtosdiar` |  |
| L182 | FÓRMULA | `LET dFechaInicial= TO_DATE((LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\` |  |
| L183 | FÓRMULA | `LET dFechaFinal  = TO_DATE((LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'` |  |
| L193 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consultamovtosdiariosct` |  |
| L207 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L216 | FÓRMULA | `LET dFechaInicial= TO_DATE((LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\` |  |
| L217 | FÓRMULA | `LET dFechaFinal  = TO_DATE((LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'` |  |
| L227 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_consultamovtosdiariosct` |  |
| L243 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cnsif_consultatotalmovtosdiarioscta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consultatotalmovtosdiarioscta.sql` |
| **LOC (1er CREATE)** | 499 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 98 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, movimiento, sd — saldo disponible, OS — Originación de Solicitudes y cuenta (total)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consultatotalmovtosdiarioscta(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMCUENTA                   CHAR(20)
  dPERIODOI                    DATE
  dPERIODOF                    DATE
  cSISTEMACUENTA               CHAR(20)
  pUsuario                     CHAR(8)
  cSuc                         CHAR(4)
  mImporte                     MONEY(14,2)
  pReversado                   CHAR(1)
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNUMCUENTA` | `CHAR(20)` | — | — |
| `dPERIODOI` | `DATE` | — | — |
| `dPERIODOF` | `DATE` | — | — |
| `cSISTEMACUENTA` | `CHAR(20)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `cSuc` | `CHAR(4)` | — | — |
| `mImporte` | `MONEY(14,2)` | — | — |
| `pReversado` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iexiste` | `INT` | L5 |
| `cCodRet` | `CHAR(5)` | L6 |
| `iSql_err` | `INT` | L7 |
| `dFecha` | `DATE` | L9 |
| `dHora` | `DATETIME HOUR to FRACTION(3)` | L10 |
| `cTransaccion` | `CHAR(4)` | L11 |
| `cD_Transaccion` | `CHAR(50)` | L12 |
| `mMonto` | `MONEY(14,2)` | L13 |
| `cNaturaleza` | `CHAR(1)` | L14 |
| `mSaldo` | `MONEY(14,2)` | L15 |
| `cReferencia` | `CHAR(40)` | L16 |
| `cReversos` | `CHAR(1)` | L17 |
| `cReversados` | `CHAR(1)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cFolio` | `CHAR(16)` | L20 |
| `cProcedencia` | `CHAR(20)` | L21 |
| `cD_Procedencia` | `CHAR(50)` | L22 |
| `dPeriodoI_1` | `DATE` | L23 |
| `dPeriodoF_1` | `DATE` | L24 |
| `sNUMSERIAL` | `INT8` | L25 |
| `sNumSecuencia` | `INT8` | L26 |
| `cUsuario` | `CHAR(8)` | L27 |
| `cReferencia23` | `CHAR(23)` | L28 |
| `cNumtarjeta` | `CHAR(20)` | L30 |
| `cconsmovhis` | `CHAR(10)` | L32 |
| *…10 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L106 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L137 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L153 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L169 |
| `sc_movhis_old2` | `bdicheq` | ⚠️ sí | SELECT | L184 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L199 |
| `sc_movhis_old3` | `bdicheq` | ⚠️ sí | SELECT | L206 |
| `sc_movhis_old4` | `bdicheq` | ⚠️ sí | SELECT | L223 |
| `tf_success_transac` | `bditransfer` | ⚠️ sí | SELECT | L240 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L262 |
| `sd_movdia` | `bdicred` | ⚠️ sí | SELECT | L268 |
| `sd_movhis` | `bdicred` | ⚠️ sí | SELECT | L282 |
| `sd_movhis_new` | `bdicred` | ⚠️ sí | SELECT | L296 |
| `sd_movdiacrd` | `bdicred` | ⚠️ sí | SELECT | L312 |
| `sd_movhiscrd` | `bdicred` | ⚠️ sí | SELECT | L320 |
| `sv_movdia` | `bdinvers` | ⚠️ sí | SELECT | L459 |
| `sv_movhis` | `bdinvers` | ⚠️ sí | SELECT | L469 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L163 | FÓRMULA | `LET iCont = iCont + iexiste;` |  |
| L179 | FÓRMULA | `LET iCont = iCont + iexiste;` |  |
| L194 | FÓRMULA | `LET iCont = iCont + iexiste;` |  |
| L216 | FÓRMULA | `LET iCont = iCont + iexiste;` |  |
| L233 | FÓRMULA | `LET iCont = iCont + iexiste;` |  |
| L245 | FÓRMULA | `LET iCont = iCont + iexiste;` |  |
| L384 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L388 | FÓRMULA | `LET iCont=iCont - 1;` |  |
| L392 | FÓRMULA | `LET iCont=iCont - 1;` |  |
| L434 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L438 | FÓRMULA | `LET iCont=iCont - 1;` |  |
| L442 | FÓRMULA | `LET iCont=iCont - 1;` |  |
| L475 | FÓRMULA | `LET iCont = iCont + iexiste;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?iari` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iari` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_consultatotalmovtosdiarioscta_2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consultatotalmovtosdiarioscta_2.sql` |
| **LOC (1er CREATE)** | 526 |
| **Callgraph** | ✅ fan_in=38 / fan_out=3 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, movimiento, sd — saldo disponible, OS — Originación de Solicitudes y cuenta (total)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consultatotalmovtosdiarioscta_2(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMCUENTA                   CHAR(20)
  dPERIODOI                    DATE
  dPERIODOF                    DATE
  cSISTEMACUENTA               CHAR(20)
  pUsuario                     CHAR(8)
  cSuc                         CHAR(4)
  mImporte                     MONEY(14,2)
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNUMCUENTA` | `CHAR(20)` | — | — |
| `dPERIODOI` | `DATE` | — | — |
| `dPERIODOF` | `DATE` | — | — |
| `cSISTEMACUENTA` | `CHAR(20)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `cSuc` | `CHAR(4)` | — | — |
| `mImporte` | `MONEY(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iexiste` | `INT` | L5 |
| `cCodRet` | `CHAR(5)` | L6 |
| `iSql_err` | `INT` | L7 |
| `dFecha` | `DATE` | L9 |
| `dHora` | `DATETIME HOUR to FRACTION(3)` | L10 |
| `cTransaccion` | `CHAR(4)` | L11 |
| `cD_Transaccion` | `CHAR(50)` | L12 |
| `mMonto` | `MONEY(14,2)` | L13 |
| `cNaturaleza` | `CHAR(1)` | L14 |
| `mSaldo` | `MONEY(14,2)` | L15 |
| `cReferencia` | `CHAR(40)` | L16 |
| `cReversos` | `CHAR(1)` | L17 |
| `cReversados` | `CHAR(1)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cFolio` | `CHAR(16)` | L20 |
| `cProcedencia` | `CHAR(20)` | L21 |
| `cD_Procedencia` | `CHAR(50)` | L22 |
| `dPeriodoI_1` | `DATE` | L23 |
| `dPeriodoF_1` | `DATE` | L24 |
| `sNUMSERIAL` | `INT8` | L25 |
| `sNumSecuencia` | `INT8` | L26 |
| `cUsuario` | `CHAR(8)` | L27 |
| `cReferencia23` | `CHAR(23)` | L28 |
| `cNumtarjeta` | `CHAR(20)` | L30 |
| `cconsmovhis` | `CHAR(10)` | L32 |
| *…15 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L118 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L149 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L165 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L182 |
| `sc_movhis_old2` | `bdicheq` | ⚠️ sí | SELECT | L199 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L216 |
| `sc_movhis_old3` | `bdicheq` | ⚠️ sí | SELECT | L223 |
| `sc_movhis_old4` | `bdicheq` | ⚠️ sí | SELECT | L242 |
| `tf_success_transac` | `bditransfer` | ⚠️ sí | SELECT | L261 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L283 |
| `sd_movdia` | `bdicred` | ⚠️ sí | SELECT | L289 |
| `sd_movhis` | `bdicred` | ⚠️ sí | SELECT | L303 |
| `sd_movhis_new` | `bdicred` | ⚠️ sí | SELECT | L317 |
| `sd_movdiacrd` | `bdicred` | ⚠️ sí | SELECT | L333 |
| `sd_movhiscrd` | `bdicred` | ⚠️ sí | SELECT | L341 |
| `sv_maeinv` | `bdinvers` | ⚠️ sí | SELECT | L472 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L176 | FÓRMULA | `LET iCont = iCont + NVL(iexiste,0);` |  |
| L194 | FÓRMULA | `LET iCont = iCont + NVL(iexiste,0);` |  |
| L211 | FÓRMULA | `LET iCont = iCont + NVL(iexiste,0);` |  |
| L235 | FÓRMULA | `LET iCont = iCont + NVL(iexiste,0);` |  |
| L254 | FÓRMULA | `LET iCont = iCont + NVL(iexiste,0);` |  |
| L266 | FÓRMULA | `LET iCont = iCont + NVL(iexiste,0);` |  |
| L378 | FÓRMULA | `LET iRegTotal = iRegTotal + NVL(iTotal,0);` |  |
| L379 | FÓRMULA | `LET iRegResta = iRegResta + NVL(iResta1,0) + NVL(iResta2,0);` |  |
| L398 | FÓRMULA | `LET iRegTotal = iRegTotal + NVL(iTotal,0);` |  |
| L399 | FÓRMULA | `LET iRegResta = iRegResta + NVL(iResta1,0) + NVL(iResta2,0);` |  |
| L417 | FÓRMULA | `LET iRegTotal = iRegTotal + NVL(iTotal,0);` |  |
| L418 | FÓRMULA | `LET iRegResta = iRegResta + NVL(iResta1,0) + NVL(iResta2,0);` |  |
| L422 | FÓRMULA | `LET iCont = iRegTotal - iRegResta;` |  |
| L441 | FÓRMULA | `LET iRegTotal = iRegTotal + NVL(iTotal,0);` |  |
| L442 | FÓRMULA | `LET iRegResta = iRegResta + NVL(iResta1,0) + NVL(iResta2,0);` |  |
| L456 | FÓRMULA | `LET iRegTotal = iRegTotal + NVL(iTotal,0);` |  |
| L457 | FÓRMULA | `LET iRegResta = iRegResta + NVL(iResta1,0) + NVL(iResta2,0);` |  |
| L461 | FÓRMULA | `LET iCont = iRegTotal - iRegResta;` |  |
| L497 | FÓRMULA | `LET iCont = NVL(iCont,0) + NVL(iexiste,0);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?iari` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?_2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iari`, `?_2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_consultatotalregtransacciontarjeta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_consultatotalregtransacciontarjeta.sql` |
| **LOC (1er CREATE)** | 206 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNSIF — sistema de confirmación de ejecutivo, transacción y tarjeta (total)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_consultatotalregtransacciontarjeta(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMEROTARJETA               CHAR(16)
  cSECUENCIA                   CHAR(7)
  cREFERENCIA                  CHAR(12)
  cPOS_ATM                     CHAR(2)
  dPERIODOI                    DATE
  dPERIODOF                    DATE
) RETURNING CHAR(5)         AS  Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNUMEROTARJETA` | `CHAR(16)` | `tarjeta`=tarjeta | ✅ CÓDIGO |
| `cSECUENCIA` | `CHAR(7)` | — | — |
| `cREFERENCIA` | `CHAR(12)` | — | — |
| `cPOS_ATM` | `CHAR(2)` | — | — |
| `dPERIODOI` | `DATE` | — | — |
| `dPERIODOF` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iexiste` | `INT` | L8 |
| `cCodRet` | `CHAR(5)` | L9 |
| `iSql_err` | `INT` | L10 |
| `iCont` | `INT` | L14 |
| `iMes` | `int` | L15 |
| `iDia` | `int` | L16 |
| `iAnio` | `int` | L17 |
| `iMesF` | `int` | L18 |
| `iDiaF` | `int` | L19 |
| `iAnioF` | `int` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `movimiento` | `intercard` | ⚠️ sí | SELECT | L72 |
| `movimientohistorico` | `intercard` | ⚠️ sí | SELECT | L74 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L64 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `tarjeta` | ENTIDAD | tarjeta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cnsif_depuramovimientostemp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_depuramovimientostemp.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "depura CNSIF — sistema de confirmación de ejecutivo y movimientos (temporal)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `depura` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_depuramovimientostemp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `bInTransaction` | `BOOLEAN` | L6 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |
| `sp_cnsif_depuramovimientostempo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00114';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `depura` | ACCION | depura / limpia | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `temp` | MODIF | temporal | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cnsif_genarch_aumlimcred_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_genarch_aumlimcred_mc.sql` |
| **LOC (1er CREATE)** | 259 |
| **Callgraph** | ✅ fan_in=14 / fan_out=11 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera CNSIF — sistema de confirmación de ejecutivo, archivo y mc — Mesa de Control (aumento)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_genarch_aumlimcred_mc(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pNombreReporte               CHAR(50)
  pRutaDescarga                CHAR(150)
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pNombreReporte` | `CHAR(50)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCmd1` | `CHAR(8000)` | L6 |
| `cCmd2` | `CHAR(8000)` | L7 |
| `cArchDescarga` | `CHAR(200)` | L8 |
| `iNoRegistros` | `INT` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `cRutaInformix` | `CHAR(100)` | L11 |
| `cUsrBin` | `CHAR(100)` | L12 |
| `cNameReport` | `CHAR(60)` | L13 |
| `ven_transacc` | `SMALLINT` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `cSql` | `CHAR(8000)` | L16 |
| `cNombreReporteHist` | `CHAR(100)` | L17 |
| `dFechaHoy` | `DATE` | L18 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L74 |
| `sw_ctrlgenreportesaumlimcred_mc` | `bdicnweb` | no | SELECT | L183 |
| `sw_ctrlgenreportesaumlimcred_mc` | `bdicnweb` | no | DELETE | L191 |
| `sw_ctrlgenreportesaumlimcred_mc` | `bdicnweb` | no | INSERT | L203 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L212 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | FÓRMULA | `LET cCmd1 ="SELECT 'FECHA ORIGEN','NO. SOLICITUD','NO. CLIENTE','ORIGEN','MANUAL / AUTOMATICO','FECH` |  |
| L75 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L77 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" TO_CHAR(fecha_origen,'%d/%m/%Y'), ''''\|\|numero_solicitud, numero` |  |
| L87 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L94 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L104 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L111 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L121 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L128 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L138 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L145 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L165 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf "\|\|TRIM(pRutaDescarga)\|\|'queryAumLimMC.sql';` |  |
| L242 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdimnsj:sp_registra_evento';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `aum` | MODIF | aumento | 🟡 INFERIDO | nombre_sp |
| `?li` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?red_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?li`, `?red_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_genarchmovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_genarchmovimientos.sql` |
| **LOC (1er CREATE)** | 369 |
| **Callgraph** | ✅ fan_in=18 / fan_out=21 |
| **Deps concatenadas** | 95 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera CNSIF — sistema de confirmación de ejecutivo, archivo y movimientos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_genarchmovimientos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
  pSistemaCuenta               CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pNumCuenta                   CHAR(20)
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pImporte                     MONEY(14,2)
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
  pClaveMov                    CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pSistemaCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |
| `pClaveMov` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cCmd1` | `CHAR(2000)` | L13 |
| `cSql` | `CHAR(2500)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cNombreArchivo` | `CHAR(45)` | L16 |
| `cNombreArchivoHist` | `CHAR(45)` | L17 |
| `cFechaHoraArchivo` | `CHAR(15)` | L18 |
| `dFechaHoy` | `DATE` | L19 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |
| `bInTransaction` | `BOOLEAN` | L22 |
| `ven_transacc` | `SMALLINT` | L23 |
| `dHoy` | `DATETIME YEAR TO FRACTION(3)` | L24 |
| `cStr7` | `CHAR(60)` | L25 |
| `cStr8` | `CHAR(60)` | L26 |
| `dFecha` | `DATE` | L28 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L29 |
| `cCveTransacc` | `CHAR(4)` | L30 |
| `cDescTransacc` | `CHAR(50)` | L31 |
| `cFolio` | `CHAR(16)` | L32 |
| `dPeriodoInicial` | `DATE` | L33 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L143 |
| `sw_cons_archivosgenerados` | `bdicnweb` | no | SELECT | L269 |
| `sw_cons_archivosgenerados` | `bdicnweb` | no | DELETE | L277 |
| `sw_cons_archivosgenerados` | `bdicnweb` | no | INSERT | L290 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L122 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L295 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L130 | FÓRMULA | `LET dFechaInicial=LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|YE` |  |
| L131 | FÓRMULA | `LET dFechaFinal  =LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|YEAR(p` |  |
| L144 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM (SELECT NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''),NVL(TO_C` | 🔴 MONEY/aritmética financiera |
| L164 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM (SELECT NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''),NVL(TO_C` | 🔴 MONEY/aritmética financiera |
| L183 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM (SELECT NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''),NVL(TO_C` | 🔴 MONEY/aritmética financiera |
| L197 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L226 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L231 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L240 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L249 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L332 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdimnsj:sp_registra_evento';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cnsif_genarchmovimientos2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_genarchmovimientos2.sql` |
| **LOC (1er CREATE)** | 352 |
| **Callgraph** | ✅ fan_in=7 / fan_out=45 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera CNSIF — sistema de confirmación de ejecutivo, archivo y movimientos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_genarchmovimientos2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
  pSistemaCuenta               CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pNumCuenta                   CHAR(20)
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pImporte                     MONEY(14,2)
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
  pClaveMov                    CHAR(50)
) RETURNING CHAR(5) 	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pSistemaCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |
| `pClaveMov` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `cRutaGral` | `CHAR(150)` | L13 |
| `cNombreArchivo` | `CHAR(45)` | L14 |
| `cNombreArchivoHist` | `CHAR(45)` | L15 |
| `cFechaHoraArchivo` | `CHAR(15)` | L16 |
| `dFechaHoy` | `DATE` | L17 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |
| `bInTransaction` | `BOOLEAN` | L20 |
| `ven_transacc` | `SMALLINT` | L21 |
| `dHoy` | `DATETIME YEAR TO FRACTION(3)` | L22 |
| `cStr7` | `CHAR(60)` | L23 |
| `cStr8` | `CHAR(60)` | L24 |
| `dFecha` | `DATE` | L26 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L27 |
| `cCveTransacc` | `CHAR(4)` | L28 |
| `cDescTransacc` | `CHAR(50)` | L29 |
| `cFolio` | `CHAR(16)` | L30 |
| `dPeriodoInicial` | `DATE` | L31 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L155 |
| `sw_cons_archivosgenerados` | `bdicnweb` | no | SELECT | L242 |
| `sw_cons_archivosgenerados` | `bdicnweb` | no | INSERT | L266 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L120 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L271 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | FÓRMULA | `LET dFechaInicial=LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|YE` |  |
| L130 | FÓRMULA | `LET dFechaFinal  =LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|YEAR(p` |  |
| L156 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM (SELECT NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''),NVL(TO_C` | 🔴 MONEY/aritmética financiera |
| L166 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L199 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L204 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L213 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L222 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L308 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdimnsj:sp_registra_evento';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_genarchmovimientos_masivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_genarchmovimientos_masivo.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 67 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera CNSIF — sistema de confirmación de ejecutivo, archivo y movimientos (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cnsif_consdetallemovimientos_totales`, `sp_cnsif_genarchmovimientos2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_genarchmovimientos_masivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
  pSistemaCuenta               CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pNumCuenta                   CHAR(20)
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pImporte                     MONEY(14,2)
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
  pClaveMov                    CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pSistemaCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |
| `pClaveMov` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cNombreArchivo` | `CHAR(45)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `cEjecucion` | `CHAR(1)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_cnsif_consdetallemovimientos_totales` | `bdicnweb` | no | L45 |
| `sp_cnsif_genarchmovimientos2` | `bdicnweb` | no | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet             = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cnsif_consdetallemovimientos` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cnsif_genarchmovimientos';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cnsif_genarchprocesosucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_genarchprocesosucursal.sql` |
| **LOC (1er CREATE)** | 1322 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera CNSIF — sistema de confirmación de ejecutivo, archivo, proceso y sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_genarchprocesosucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
  pReporte                     CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pReporte` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cCmd1` | `CHAR(2000)` | L9 |
| `cSql` | `CHAR(2500)` | L10 |
| `cRutaGral` | `CHAR(150)` | L11 |
| `cNombreArchivo` | `CHAR(45)` | L12 |
| `cNombreArchivoHist` | `CHAR(45)` | L13 |
| `cFechaHoraArchivo` | `CHAR(15)` | L14 |
| `dFechaHoy` | `DATE` | L15 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |
| `bInTransaction` | `BOOLEAN` | L18 |
| `ven_transacc` | `SMALLINT` | L19 |
| `dHoy` | `DATETIME YEAR TO FRACTION(3)` | L20 |
| `cStr7` | `CHAR(60)` | L21 |
| `cStr8` | `CHAR(60)` | L22 |
| `dFecha` | `DATE` | L24 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L25 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(3)` | L26 |
| `dFech` | `DATE` | L27 |
| `cCveTransacc` | `CHAR(4)` | L28 |
| `cDescTransacc` | `CHAR(50)` | L29 |
| *…76 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L270 |
| `sw_tmp_alta_ctes_prod_mae` | `bdicnweb` | no | INSERT | L278 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L336 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L368 |
| `sw_tmp_alta_ctes_prod_mae` | `bdicnweb` | no | SELECT | L400 |
| `sw_mantenimiento_clientes_productos` | `bdicnweb` | no | INSERT | L412 |
| `systables` | `bdicnweb` | no | SELECT | L444 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L483 |
| `sw_mantenimiento_clientes` | `bdicnweb` | no | INSERT | L492 |
| `si_solicitud_movil` | `bdinteg` | ⚠️ sí | SELECT | L583 |
| `si_usuario_movil` | `bdinteg` | ⚠️ sí | SELECT | L589 |
| `sw_mantenimiento_moviles` | `bdicnweb` | no | INSERT | L595 |
| `sw_mantenimiento_clientes` | `bdicnweb` | no | SELECT | L712 |
| `si_biometria_rechazo` | `bdinteg` | ⚠️ sí | SELECT | L720 |
| `sw_mantenimiento_biometria` | `bdicnweb` | no | INSERT | L742 |
| `se_ctessitespcte` | `bdisitesp` | ⚠️ sí | SELECT | L801 |
| `si_bitacora_ife` | `bdinteg` | ⚠️ sí | SELECT | L809 |
| `sw_mantenimiento_p109` | `bdicnweb` | no | INSERT | L870 |
| `sw_mantenimiento_u3` | `bdicnweb` | no | INSERT | L944 |
| `si_cte_huella` | `bdinteg` | ⚠️ sí | SELECT | L999 |
| `sw_mantenimiento_huellas` | `bdicnweb` | no | INSERT | L1016 |
| `si_telefonos_actual` | `bdinteg` | ⚠️ sí | SELECT | L1069 |
| `si_telefonos` | `bdinteg` | ⚠️ sí | SELECT | L1084 |
| `sw_validacion_tel` | `bdicnweb` | no | INSERT | L1125 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L1241 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L238 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L244 | FÓRMULA | `LET pFechaInicial = TODAY-1;		LET pFechaFinal = TODAY-1;` |  |
| L245 | FÓRMULA | `LET dFechaInicial=LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|YE` |  |
| L246 | FÓRMULA | `LET dFechaFinal  =LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|YEAR(p` |  |
| L254 | FÓRMULA | `LET dFechaHoy = TODAY-1;			LET dHoraHoy = TODAY-1;			LET cFechaHoraArchivo = LPAD(DAY(dFechaHoy),2,0` |  |
| L276 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L309 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L341 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L373 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L417 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L443 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'FECHA','SUCURSAL','PROMOTOR','CLIENTE NUEVO/EXISTENTE','PR` |  |
| L444 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L445 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''), ''''\|\|sucursal, prom` |  |
| L453 | FÓRMULA | `LET dFechaHoy = TODAY-1;			LET dHoraHoy = TODAY-1;			LET cFechaHoraArchivo = LPAD(DAY(dFechaHoy),2,0` |  |
| L490 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L528 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L560 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L561 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''), ''''\|\|sucursal, prom` |  |
| L568 | FÓRMULA | `LET dFechaHoy = TODAY-1;			LET dHoraHoy = TODAY-1;			LET cFechaHoraArchivo = LPAD(DAY(dFechaHoy),2,0` |  |
| L598 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L655 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L691 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L692 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''), ''''\|\|sucursal, prom` |  |
| L699 | FÓRMULA | `LET dFechaHoy = TODAY-1;			LET dHoraHoy = TODAY-1;			LET cFechaHoraArchivo = LPAD(DAY(dFechaHoy),2,0` |  |
| L747 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L778 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'FECHA','SUCURSAL','PROMOTOR','CLIENTE NUEVO/EXISTENTE','NU` |  |
| L779 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L780 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''), ''''\|\|sucursal, prom` |  |
| L787 | FÓRMULA | `LET dFechaHoy = TODAY-1;			LET dHoraHoy = TODAY-1;			LET cFechaHoraArchivo = LPAD(DAY(dFechaHoy),2,0` |  |
| | *…26 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cnsif_genrepaltactesacum`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_genrepaltactesacum.sql` |
| **LOC (1er CREATE)** | 711 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte CNSIF — sistema de confirmación de ejecutivo y clientes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_genrepaltactesacum(
) RETURNING CHAR(5) AS codret
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `cCmd1` | `CHAR(2000)` | L7 |
| `cSql` | `CHAR(2500)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(45)` | L10 |
| `cFechaHoraArchivo` | `CHAR(15)` | L11 |
| `dFechaHoy` | `DATE` | L12 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L13 |
| `bInTransaction` | `BOOLEAN` | L14 |
| `ven_transacc` | `SMALLINT` | L15 |
| `cStr8` | `CHAR(60)` | L16 |
| `dFecha` | `DATE` | L17 |
| `dFecha_insert` | `DATE` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cPromotor` | `CHAR(8)` | L20 |
| `cNumcte` | `CHAR(20)` | L21 |
| `cNumcte_coppel` | `CHAR(20)` | L22 |
| `cTipoCte` | `CHAR(20)` | L23 |
| `cTpo_biometria` | `CHAR(1)` | L24 |
| `cTpo_biometria_desc` | `CHAR(10)` | L25 |
| `iCont` | `INTEGER` | L26 |
| `iRegCommit` | `INTEGER` | L27 |
| `cSituacion` | `CHAR(1)` | L28 |
| *…13 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_telefonos` | `bdinteg` | ⚠️ sí | SELECT | L284 |
| `sw_alta_clientes_acumulado` | `bdicnweb` | no | INSERT | L309 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L371 |
| `systables` | `bdicnweb` | no | SELECT | L612 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L116 | FÓRMULA | `LET dFechaHoy = TODAY-1;		LET dHoraHoy = TODAY-1;		LET cFechaHoraArchivo = LPAD(DAY(dFechaHoy),2,0)\` |  |
| L286 | VALIDACIÓN_NULL | `IF (cEst_tel_cel IS NULL OR (NVL(cEst_tel_cel,'') = '') OR cEst_tel_cel = 'F') THEN` |  |
| L299 | VALIDACIÓN_NULL | `IF (cEst_tel_cas IS NULL OR (NVL(cEst_tel_cas,'') = '') OR cEst_tel_cas = 'F') THEN` |  |
| L307 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L478 | VALIDACIÓN_NULL | `IF (cEst_tel_cel IS NULL OR (NVL(cEst_tel_cel,'') = '') OR cEst_tel_cel = 'F') THEN` |  |
| L491 | VALIDACIÓN_NULL | `IF (cEst_tel_cas IS NULL OR (NVL(cEst_tel_cas,'') = '') OR cEst_tel_cas = 'F') THEN` |  |
| L498 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L571 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L617 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L646 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L651 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L660 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L669 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `?acum` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?acum` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_genreportes_aumlimcred_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_genreportes_aumlimcred_mc.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte CNSIF — sistema de confirmación de ejecutivo y mc — Mesa de Control (aumento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 7 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultagralautaumlincred_rep`, `sp_consultagralstatusaumlincred_rep` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_genreportes_aumlimcred_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pRutaDescarga                CHAR(100)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pStatus                      CHAR(2)
  pOrigen                      CHAR(1)
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cNombreArchivo` | `CHAR(50)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |
| `sp_consultagralautaumlincred_rep` | `bdicnweb` | no | L58 |
| `sp_consultagralstatusaumlincred_rep` | `bdicnweb` | no | L69 |
| `sp_consultarevisioncentralaumlincred_rep` | `bdicnweb` | no | L80 |
| `sp_consultarevisioncacaumlincred_rep` | `bdicnweb` | no | L91 |
| `sp_consultaperfilusuarioaumlincred_rep` | `bdicnweb` | no | L102 |
| `sp_cnsif_genarch_aumlimcred_mc` | `bdicnweb` | no | L115 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoReporte = '' OR pRutaDescarga = '' OR pFechaInicial IS N` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consultagralautaumlincred_re` |  |
| L72 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consultagralstatusaumlincred` |  |
| L83 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consultarevisioncentralaumli` |  |
| L94 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consultarevisioncacaumlincre` |  |
| L105 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consultaperfilusuarioaumlinc` |  |
| L118 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cnsif_genarch_aumlimcred_mc'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?ortes_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aum` | MODIF | aumento | 🟡 INFERIDO | nombre_sp |
| `?li` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?red_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ortes_`, `?li`, `?red_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_monitorsucursales2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_monitorsucursales2.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "CNSIF — sistema de confirmación de ejecutivo, monitor y sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_monitorsucursales2(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  Tp_Busqueda                  CHAR(1)
  Id_Plaza                     CHAR(3)
  pNumRegistro                 INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS Cod_Retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `Tp_Busqueda` | `CHAR(1)` | — | — |
| `Id_Plaza` | `CHAR(3)` | — | — |
| `pNumRegistro` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSql_err` | `INT` | L6 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | VALIDACIÓN_NULL | `IF cID_USUARIOC = '' OR cID_FUNCIONC = '' OR Tp_Busqueda  = '' OR pNumRegistro IS NULL OR pRecuperac` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | 🔵 CONVENCIÓN | nombre_sp |
| `?es2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_pasesucursal2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_pasesucursal2.sql` |
| **LOC (1er CREATE)** | 41 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "pase contable CNSIF — sistema de confirmación de ejecutivo y sucursal" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_pasesucursal2(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  Num_Sucursal                 CHAR(4)
) RETURNING CHAR(5) AS Cod_Retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `Num_Sucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSql_err` | `INT` | L5 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L26 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `pase` | ACCION | pase contable (registra/traslada a póliza o mayor) | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_validaplanapoyo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_validaplanapoyo.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 191 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida CNSIF — sistema de confirmación de ejecutivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_validaplanapoyo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `cPlanApoyo` | `CHAR(1)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_diferir` | `bdicred` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?planapoyo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?planapoyo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnsif_verificastatusmovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_verificastatusmovimientos.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica CNSIF — sistema de confirmación de ejecutivo, estatus y movimientos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_verificastatusmovimientos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveMov                    CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveMov` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_statusproceso` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cnsif_verificastatusremconsgralremesascte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnsif_verificastatusremconsgralremesascte.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica CNSIF — sistema de confirmación de ejecutivo, estatus, remesa, remesas internacionales y cliente (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnsif_verificastatusremconsgralremesascte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_statusprocesorem` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cnt_catagrupa`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_catagrupa.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "CNT — módulo de convenios y control de descuentos de nómina de empleados y catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_catagrupa(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSt                          CHAR(2)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSt` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cIdAgrupa` | `CHAR(1)` | L11 |
| `cDescAgrupa` | `CHAR(35)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cnt_agrupacion` | `bdicnweb` | no | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L67 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?agrupa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?agrupa` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_catconsulta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_catconsulta.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 117 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNT — módulo de convenios y control de descuentos de nómina de empleados y catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_catconsulta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cIdCatalogo` | `CHAR(1)` | L11 |
| `cDescCatalogo` | `CHAR(35)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cnt_tipoconsulta` | `bdicnweb` | no | SELECT | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cnt_catconvenio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_catconvenio.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "CNT — módulo de convenios y control de descuentos de nómina de empleados, catálogo y convenio" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_catconvenio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNumCategoria` | `CHAR(2)` | L12 |
| `cNumConvenio` | `CHAR(3)` | L13 |
| `cNomConvenio` | `CHAR(40)` | L14 |
| `iNumRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cnt_consdetalleempleado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_consdetalleempleado.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 116 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNT — módulo de convenios y control de descuentos de nómina de empleados, detalle y empleado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarempleado` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_consdetalleempleado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | `empleado`=empleado | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cDesCodRetSp` | `CHAR(80)` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |
| `iNumRegistros` | `INTEGER` | L17 |
| `cEmpresaSp` | `CHAR(3)` | L19 |
| `cNomEmpleado` | `CHAR(45)` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cPuesto` | `CHAR(3)` | L22 |
| `cNombramiento` | `CHAR(20)` | L23 |
| `cAsistente` | `CHAR(10)` | L24 |
| `cPassword` | `CHAR(40)` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |
| `sp_consultarempleado` | `bdinteg` | ⚠️ sí | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `empleado` | ENTIDAD | empleado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cnt_consultadetallefaltdescemp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_consultadetallefaltdescemp.sql` |
| **LOC (1er CREATE)** | 198 |
| **Callgraph** | ✅ fan_in=0 / fan_out=75 |
| **Deps concatenadas** | 113 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNT — módulo de convenios y control de descuentos de nómina de empleados, detalle, fal — fallo/disputa, [polisemia] Descripción y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_consultadetallefaltdescemp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pZona                        CHAR(3)
  pRegional                    CHAR(3)
  pIdAsignado                  SMALLINT
  pEstatus                     SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,      --
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pZona` | `CHAR(3)` | — | — |
| `pRegional` | `CHAR(3)` | — | — |
| `pIdAsignado` | `SMALLINT` | — | — |
| `pEstatus` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `cCodRetSp` | `CHAR(6)` | L36 |
| `iCodRet` | `INTEGER` | L37 |
| `iSqlErr` | `INTEGER` | L38 |
| `cEmpresa` | `CHAR(3)` | L39 |
| `iRegistros` | `INTEGER` | L40 |
| `iRecuperacion` | `INTEGER` | L41 |
| `cNumEmpleado` | `CHAR(8)` | L43 |
| `cNomEmpleado` | `CHAR(45)` | L44 |
| `cNumSucursal` | `CHAR(4)` | L45 |
| `cNomSucursal` | `CHAR(40)` | L46 |
| `sIdFaltante` | `SMALLINT` | L47 |
| `cAuxiliar` | `CHAR(12)` | L48 |
| `cNumZona` | `CHAR(3)` | L49 |
| `cNumRegional` | `CHAR(3)` | L50 |
| `sIdConcepto` | `SMALLINT` | L51 |
| `cDesConcepto` | `CHAR(80)` | L52 |
| `sIdRecupera` | `SMALLINT` | L53 |
| `cDesRecupera` | `CHAR(80)` | L54 |
| `sIdAsignado` | `SMALLINT` | L55 |
| `cDesAsignado` | `CHAR(80)` | L56 |
| `sIdEstatus` | `SMALLINT` | L57 |
| `cDesEstatus` | `CHAR(80)` | L58 |
| `mSaldoInicial` | `MONEY(10,0)` | L59 |
| `mDescAcumulado` | `MONEY(10,0)` | L60 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cnt_detallefaltdescemp` | `bdicnweb` | no | SELECT | L164 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L129 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L167 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L176 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_consultadetallefaltdescemp_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_consultadetallefaltdescemp_totales.sql` |
| **LOC (1er CREATE)** | 178 |
| **Callgraph** | ✅ fan_in=0 / fan_out=75 |
| **Deps concatenadas** | 112 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta CNT — módulo de convenios y control de descuentos de nómina de empleados, detalle, fal — fallo/disputa, [polisemia] Descripción y Empresa — empleadora del cliente; vinculada a crédito de nómina (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_consultadetallefaltdescemp_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pZona                        CHAR(3)
  pRegional                    CHAR(3)
  pIdAsignado                  SMALLINT
  pEstatus                     SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pZona` | `CHAR(3)` | — | — |
| `pRegional` | `CHAR(3)` | — | — |
| `pIdAsignado` | `SMALLINT` | — | — |
| `pEstatus` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRet` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iRegistros` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cNumEmpleado` | `CHAR(8)` | L14 |
| `cNomEmpleado` | `CHAR(45)` | L15 |
| `cNumSucursal` | `CHAR(4)` | L16 |
| `cNomSucursal` | `CHAR(40)` | L17 |
| `sIdFaltante` | `SMALLINT` | L18 |
| `cAuxiliar` | `CHAR(12)` | L19 |
| `cNumZona` | `CHAR(3)` | L20 |
| `cNumRegional` | `CHAR(3)` | L21 |
| `sIdConcepto` | `SMALLINT` | L22 |
| `cDesConcepto` | `CHAR(80)` | L23 |
| `sIdRecupera` | `SMALLINT` | L24 |
| `cDesRecupera` | `CHAR(80)` | L25 |
| `sIdAsignado` | `SMALLINT` | L26 |
| `cDesAsignado` | `CHAR(80)` | L27 |
| `sIdEstatus` | `SMALLINT` | L28 |
| `cDesEstatus` | `CHAR(80)` | L29 |
| `mSaldoInicial` | `MONEY(10,0)` | L30 |
| `mDescAcumulado` | `MONEY(10,0)` | L31 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_cnt_detallefaltdescemp` | `bdicnweb` | no | SELECT | L97 |
| `status_cnt_detallefaltdescemp` | `bdicnweb` | no | DELETE | L97 |
| `status_cnt_detallefaltdescemp` | `bdicnweb` | no | INSERT | L98 |
| `sw_cnt_detallefaltdescemp` | `bdicnweb` | no | SELECT | L118 |
| `sw_cnt_detallefaltdescemp` | `bdicnweb` | no | DELETE | L118 |
| `sw_cnt_detallefaltdescemp` | `bdicnweb` | no | INSERT | L140 |
| `sw_cnt_detallefaltdescemp` | `bdicnweb` | no | UPDATE | L151 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `spconsultarconfaltante` | `bdirech` | ⚠️ sí | L122 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L100 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L130 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP spconsultarconfaltante';` |  |
| L134 | FÓRMULA | `LET mTotalFaltante = mTotalFaltante + mSaldoInicial;` |  |
| L135 | FÓRMULA | `LET mTotalSaldo = mTotalSaldo + mSaldoActual;` |  |
| L137 | FÓRMULA | `LET mTotalQueb = mTotalQueb + mSaldoQueb;` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_detallecatalogos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_detallecatalogos.sql` |
| **LOC (1er CREATE)** | 271 |
| **Callgraph** | ✅ fan_in=0 / fan_out=74 |
| **Deps concatenadas** | 111 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "CNT — módulo de convenios y control de descuentos de nómina de empleados, detalle y catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 5 llamada(s): `sp_consultarcatsucursales2`, `sp_consultarcatplazas`, `spconsultarcatregiones` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_detallecatalogos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCatalogo                  CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCatalogo` | `CHAR(1)` | `catalogo`=catálogo | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRet` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cIdCampo` | `CHAR(10)` | L11 |
| `cDescCampo` | `CHAR(100)` | L12 |
| `cSucursal` | `CHAR(4)` | L13 |
| `cNombre` | `CHAR(80)` | L14 |
| `cDireccion1` | `CHAR(40)` | L15 |
| `cDireccion2` | `CHAR(40)` | L16 |
| `cTelefono1` | `CHAR(14)` | L17 |
| `cGerente` | `CHAR(40)` | L18 |
| `cSubgerente` | `CHAR(40)` | L19 |
| `cTipoSucursal` | `CHAR(2)` | L20 |
| `cPlaza` | `CHAR(3)` | L21 |
| `cRegional` | `CHAR(3)` | L22 |
| `dFecha` | `DATE` | L23 |
| `iIdAsignado` | `SMALLINT` | L24 |
| `iIdEstatus` | `SMALLINT` | L25 |
| `cDivisa` | `CHAR(2)` | L26 |
| `cStatus` | `CHAR(2)` | L27 |
| `cSolicitud` | `CHAR(1)` | L28 |
| `iRegistros` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L117 |
| `si_regional` | `bdinteg` | ⚠️ sí | SELECT | L158 |
| `si_divisas` | `bdinteg` | ⚠️ sí | SELECT | L223 |
| `ss_status_sol` | `bdisolic` | ⚠️ sí | SELECT | L236 |
| `ss_tp_solicitud` | `bdisolic` | ⚠️ sí | SELECT | L249 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarcatsucursales2` | `bdinteg` | ⚠️ sí | L97 |
| `sp_consultarcatplazas` | `bdinteg` | ⚠️ sí | L138 |
| `spconsultarcatregiones` | `bdinteg` | ⚠️ sí | L168 |
| `spconsultarcatasignado` | `bdirech` | ⚠️ sí | L189 |
| `spconsultarcatestatus` | `bdirech` | ⚠️ sí | L205 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | VALIDACIÓN_NULL | `IF pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L102 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultarcatsucursales2';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L108 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L120 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L143 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultarcatplazas';` |  |
| L146 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L161 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L173 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP spconsultarcatregiones';` |  |
| L175 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L179 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L194 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP spconsultarcatasignado';` |  |
| L197 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L210 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP spconsultarcatestatus';` |  |
| L213 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L226 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L239 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L251 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L258 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_detalleconvenio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_detalleconvenio.sql` |
| **LOC (1er CREATE)** | 303 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "CNT — módulo de convenios y control de descuentos de nómina de empleados, detalle y convenio" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_sacconsultascentral` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_detalleconvenio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(5)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | `convenio`=convenio (nómina/empresarial) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L66 |
| `iSqlErr` | `INT` | L67 |
| `cCodRetSp` | `CHAR(5)` | L68 |
| `iCodRetSp` | `INTEGER` | L69 |
| `cEmpresa` | `CHAR(3)` | L70 |
| `iNumRegistros` | `INTEGER` | L71 |
| `cNumcategoria` | `CHAR(2)` | L73 |
| `cNumconvenio` | `CHAR(3)` | L74 |
| `cNomconvenio` | `CHAR(40)` | L75 |
| `dFechaapertura` | `DATE` | L76 |
| `dFechaclausura` | `DATE` | L77 |
| `dFecha_alta` | `DATE` | L78 |
| `cStatusconvenio` | `CHAR(1)` | L79 |
| `cTipo_Referencia` | `CHAR(1)` | L80 |
| `cNomlegalempresa` | `CHAR(40)` | L81 |
| `cRfcempresa` | `CHAR(13)` | L82 |
| `cNomcomercialempresa` | `CHAR(40)` | L83 |
| `cDireccionempresa` | `CHAR(80)` | L84 |
| `cEstado` | `CHAR(30)` | L85 |
| `cCiudad` | `CHAR(30)` | L86 |
| `cCodpostal` | `CHAR(5)` | L87 |
| `cNumtelcorporativo` | `CHAR(10)` | L88 |
| `cNumfaxcorporativo` | `CHAR(10)` | L89 |
| `cNomcontacto1` | `CHAR(40)` | L90 |
| `cNumtelcontacto1` | `CHAR(10)` | L91 |
| *…43 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_tiporeferencia` | `bdisac` | ⚠️ sí | SELECT | L271 |
| `sac_statusconvenio` | `bdisac` | ⚠️ sí | SELECT | L272 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sacconsultascentral` | `bdisac` | ⚠️ sí | L249 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L224 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L259 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdisac:"informix".sp_sacconsultascentral` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_cnt_genreportesfaltdescemp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_genreportesfaltdescemp.sql` |
| **LOC (1er CREATE)** | 233 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte CNT — módulo de convenios y control de descuentos de nómina de empleados, fal — fallo/disputa, [polisemia] Descripción y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_genreportesfaltdescemp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pEjecutivo                   CHAR(8)
  pSucursal                    CHAR(4)
  pZona                        CHAR(3)
  pRegional                    CHAR(3)
  pIdAsignado                  SMALLINT
  pEstatus                     SMALLINT
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pZona` | `CHAR(3)` | — | — |
| `pRegional` | `CHAR(3)` | — | — |
| `pIdAsignado` | `SMALLINT` | — | — |
| `pEstatus` | `SMALLINT` | — | — |
| `pRutaDescarga` | `CHAR(100)` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDesCodRetSp` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCmd1` | `CHAR(2000)` | L12 |
| `cSql` | `CHAR(2500)` | L13 |
| `cRutaInformix` | `CHAR(100)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cNombreReporte` | `CHAR(100)` | L16 |
| `cNombreReporteHist` | `CHAR(100)` | L17 |
| `cFechaHoraArchivo` | `CHAR(15)` | L18 |
| `dFechaHoy` | `DATE` | L19 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L20 |
| `bInTransaction` | `BOOLEAN` | L21 |
| `ven_transacc` | `SMALLINT` | L22 |
| `dHoy` | `DATETIME YEAR TO FRACTION(3)` | L23 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L26 |
| `cStr7` | `CHAR(60)` | L27 |
| `cStr8` | `CHAR(60)` | L28 |
| `iRegistros` | `INTEGER` | L29 |
| `iCountRep` | `INTEGER` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |
| `iNumRegistros` | `INTEGER` | L32 |
| *…8 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cnt_detallerepfaltdescemp` | `bdicnweb` | no | SELECT | L122 |
| `sw_cnt_detallerepfaltdescemp` | `bdicnweb` | no | DELETE | L122 |
| `sw_cnt_detallerepfaltdescemp` | `bdicnweb` | no | INSERT | L134 |
| `systables` | `bdicnweb` | no | SELECT | L143 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L108 |
| `spconsultarfaltante_reporte` | `bdirech` | ⚠️ sí | L126 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L102 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRutaDescarga = ` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L130 | EXCEPCIÓN | `RAISE EXCEPTION cNumEmpleado::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdirech:"informix".spconsult` |  |
| L133 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L144 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT num_empleado,num_sucursal,id_falta` |  |
| L149 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L177 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L182 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L191 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L200 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?ortes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ortes`, `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_genreportesolcred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_genreportesolcred.sql` |
| **LOC (1er CREATE)** | 345 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 356 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte CNT — módulo de convenios y control de descuentos de nómina de empleados, solicitud y crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_genreportesolcred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
  pRegion                      CHAR(3)
  pNumCte                      CHAR(20)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pRegion` | `CHAR(3)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cDescCodRetSp` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCmd1` | `CHAR(2000)` | L12 |
| `cSql` | `CHAR(2500)` | L13 |
| `cRutaInformix` | `CHAR(100)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cNombreReporte` | `CHAR(100)` | L16 |
| `cNombreReporteHist` | `CHAR(100)` | L17 |
| `cFechaHoraArchivo` | `CHAR(15)` | L18 |
| `dFechaHoy` | `DATE` | L19 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L20 |
| `bInTransaction` | `BOOLEAN` | L21 |
| `ven_transacc` | `SMALLINT` | L22 |
| `dHoy` | `DATETIME YEAR TO FRACTION(3)` | L23 |
| `iRow` | `INTEGER` | L24 |
| `iContBloque` | `INTEGER` | L25 |
| `iCodRet` | `INTEGER` | L26 |
| `cNum_solicitud` | `CHAR(20)` | L28 |
| `cNum_cliente` | `CHAR(20)` | L29 |
| `dFecha_alta` | `DATE` | L30 |
| `cNom_cliente` | `CHAR(104)` | L31 |
| *…29 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenrepdetallesolcred` | `bdicnweb` | no | SELECT | L144 |
| `sw_ctrlgenrepdetallesolcred` | `bdicnweb` | no | DELETE | L144 |
| `sw_ctrlgenrepdetallesolcred` | `bdicnweb` | no | INSERT | L146 |
| `sw_cnt_detallesolcred` | `bdicnweb` | no | SELECT | L181 |
| `sw_cnt_detallesolcred` | `bdicnweb` | no | DELETE | L183 |
| `systables` | `bdicnweb` | no | SELECT | L224 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L158 |
| `sp_status_sol_audexcel3` | `bdicred` | ⚠️ sí | L199 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L149 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRutaDescarga = ` |  |
| L150 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L187 | FÓRMULA | `LET iContBloque = iContBloque + 1;` |  |
| L204 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP bdisolic:"informix".sp_status_sol_audexcel` |  |
| L207 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L225 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT solicitud,cliente,fecha_sol::CHAR(` |  |
| L233 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L261 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L266 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L275 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L284 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_reportefaltantedescuentoemp_xls`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_reportefaltantedescuentoemp_xls.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 110 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "CNT — módulo de convenios y control de descuentos de nómina de empleados, reporte, fal — fallo/disputa, [polisemia] Descripción y Empresa — empleadora del cliente; vinculada a crédito de nómina (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=7 · INFERIDO=1 · SINTÉTICO=4 / 12 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_reportefaltantedescuentoemp_xls(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(150)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cCmd2` | `CHAR(4000)` | L8 |
| `cArchDescarga` | `CHAR(200)` | L9 |
| `iNoRegistros` | `INT` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `cRutaInformix` | `CHAR(100)` | L12 |
| `cUsrBin` | `CHAR(100)` | L13 |
| `cNameReport` | `CHAR(60)` | L14 |
| `ven_transacc` | `SMALLINT` | L15 |
| `bInTransaction` | `BOOLEAN` | L16 |
| `cSql` | `CHAR(8000)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT ''''\|\|sucursal,nom_sucursal,num_` |  |
| L84 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf "\|\|TRIM(pRutaDescarga)\|\|'queryRFDE.sql';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | 🟡 INFERIDO | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `?uento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ls` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t`, `?e`, `?uento`, `?ls` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_verificastatusdetfaltantedescaemp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_verificastatusdetfaltantedescaemp.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 109 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica CNT — módulo de convenios y control de descuentos de nómina de empleados, estatus, detalle, fal — fallo/disputa, [polisemia] Descripción y Empresa — empleadora del cliente; vinculada a crédito de nómina (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=6 · INFERIDO=1 · SINTÉTICO=3 / 12 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_verificastatusdetfaltantedescaemp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_cnt_detallefaltantedescaemp` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t`, `?e`, `?a` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_verificastatusdetfaltdescemp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_verificastatusdetfaltdescemp.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 108 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica CNT — módulo de convenios y control de descuentos de nómina de empleados, estatus, detalle, fal — fallo/disputa, [polisemia] Descripción y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_verificastatusdetfaltdescemp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_cnt_detallefaltdescemp` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cnt_verificastatusgenreportesolcred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cnt_verificastatusgenreportesolcred.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica CNT — módulo de convenios y control de descuentos de nómina de empleados, estatus, solicitud y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cnt_verificastatusgenreportesolcred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `cNombreReporte` | `CHAR(100)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenrepdetallesolcred` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cnt` | ENTIDAD | CNT — módulo de convenios y control de descuentos de nómina  | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_comparasdofisconcaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_comparasdofisconcaja.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta saldo y caja (complemento)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_comparasdofisconcaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSaldo                   CHAR(1)
  pFechaInicio                 DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSaldo` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cIdSucursal` | `CHAR(4)` | L17 |
| `cNomSucursal` | `CHAR(40)` | L18 |
| `cIdCajaGen` | `CHAR(4)` | L19 |
| `cNomCajaGen` | `CHAR(40)` | L20 |
| `mSdoFisico` | `MONEY(14,2)` | L21 |
| `mSdoContable` | `MONEY(14,2)` | L22 |
| `mSdoDiferencia` | `MONEY(14,2)` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_compara_sdo_fis_con` | `bdicnweb` | no | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSaldo = '' OR pFechaInicio IS NULL OR pRegistros IS NULL` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L74 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?ara` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sdo` | ENTIDAD | saldo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?fis` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ara`, `?fis` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_actgenreportesconciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_actgenreportesconciliacion.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_actgenreportesconciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(100)
  pEstatus                     CHAR(1)
) RETURNING CHAR(5)     AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(100)` | — | — |
| `pEstatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotalRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | SELECT | L40 |
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | UPDATE | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?ortes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ortes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_altamoddiasinhabiles`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_altamoddiasinhabiles.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 286 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta día hábil — día bancario operativo (del día)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_altamoddiasinhabiles(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pFechaDiaInhOrigen           DATE
  pDescripcion                 CHAR(30)
  pFechaDiaInh                 DATE
  pFechaProxDiaLab             DATE
  pDiaLab                      CHAR(1)
  pDiaLabBanca                 CHAR(1)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pFechaDiaInhOrigen` | `DATE` | `dia`=del día | 🔵 CONVENCIÓN |
| `pDescripcion` | `CHAR(30)` | — | — |
| `pFechaDiaInh` | `DATE` | `dia`=del día | 🔵 CONVENCIÓN |
| `pFechaProxDiaLab` | `DATE` | `dia`=del día | 🔵 CONVENCIÓN |
| `pDiaLab` | `CHAR(1)` | `dia`=del día | 🔵 CONVENCIÓN |
| `pDiaLabBanca` | `CHAR(1)` | `dia`=del día | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cPais` | `CHAR(3)` | L9 |
| `cDescripcion` | `CHAR(30)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_feriado` | `bdinteg` | ⚠️ sí | INSERT | L48 |
| `si_feriado_banca` | `bdinteg` | ⚠️ sí | INSERT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L66 | VALIDACIÓN_NULL | `IF pFechaDiaInhOrigen IS NULL THEN` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mod` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `?sin` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `habil` | ENTIDAD | día hábil — día bancario operativo (spei_validafecha, sp_cam | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mod`, `?sin`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_carga_dotacion_masiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_carga_dotacion_masiva.sql` |
| **LOC (1er CREATE)** | 207 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta dotación de efectivo (masiva)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_carga_dotacion_masiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATETIME YEAR TO SECOND
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATETIME YEAR TO SECOND` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cfechaSolicitud` | `DATE` | L30 |
| `cfechaAplicacion` | `DATE` | L31 |
| `cccSucursal` | `CHAR(4)` | L32 |
| `cnombreSucursal` | `CHAR(40)` | L33 |
| `cplaza` | `CHAR(50)` | L34 |
| `ctipoOperacion` | `CHAR(20)` | L35 |
| `cbillete1000` | `SMALLINT` | L36 |
| `cbillete500` | `SMALLINT` | L37 |
| `cbillete200` | `SMALLINT` | L38 |
| `cbillete100` | `SMALLINT` | L39 |
| `cbillete50` | `SMALLINT` | L40 |
| `cbillete20` | `SMALLINT` | L41 |
| `ctotalBilletes` | `INTEGER` | L42 |
| `cmoneda100` | `SMALLINT` | L43 |
| `cmoneda50` | `SMALLINT` | L44 |
| `cmoneda20` | `SMALLINT` | L45 |
| `cmoneda10` | `SMALLINT` | L46 |
| `cmoneda5` | `SMALLINT` | L47 |
| `cmoneda2` | `SMALLINT` | L48 |
| `cmoneda1` | `SMALLINT` | L49 |
| `cmoneda50c` | `SMALLINT` | L50 |
| `ctotalMonedas` | `INTEGER` | L51 |
| `ctotalImporte` | `MONEY` | L52 |
| *…17 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_arch_dot_suc_det` | `bdicnweb` | no | SELECT | L156 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L144 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L160 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L162 | FÓRMULA | `LET cImporte1000 = cbillete1000 * 1000;` | 🔴 MONEY/aritmética financiera |
| L163 | FÓRMULA | `LET cImporte500 = cbillete500 * 500;` | 🔴 MONEY/aritmética financiera |
| L164 | FÓRMULA | `LET cImporte200 = cbillete200 * 200;` | 🔴 MONEY/aritmética financiera |
| L165 | FÓRMULA | `LET cImporte100 = cbillete100 * 100;` | 🔴 MONEY/aritmética financiera |
| L166 | FÓRMULA | `LET cImporte50 = cbillete50 * 50;` | 🔴 MONEY/aritmética financiera |
| L167 | FÓRMULA | `LET cImporte20 = cbillete20 * 20;` | 🔴 MONEY/aritmética financiera |
| L168 | FÓRMULA | `LET ctotalBilletes = cImporte1000 + cImporte500 + cImporte200 + cImporte100 + cImporte50 + cImporte2` | 🔴 MONEY/aritmética financiera |
| L171 | FÓRMULA | `LET cImporteM100 = cmoneda100 * 100;` | 🔴 MONEY/aritmética financiera |
| L172 | FÓRMULA | `LET cImporteM50 = cmoneda50 * 50;` | 🔴 MONEY/aritmética financiera |
| L173 | FÓRMULA | `LET cImporteM20 = cmoneda20 * 20;` | 🔴 MONEY/aritmética financiera |
| L174 | FÓRMULA | `LET cImporteM10 = cmoneda10 * 10;` | 🔴 MONEY/aritmética financiera |
| L175 | FÓRMULA | `LET cImporteM5 = cmoneda5 * 5;` | 🔴 MONEY/aritmética financiera |
| L176 | FÓRMULA | `LET cImporteM2 = cmoneda2 * 2;` | 🔴 MONEY/aritmética financiera |
| L177 | FÓRMULA | `LET cImporteM1 = cmoneda1 * 1;` | 🔴 MONEY/aritmética financiera |
| L178 | FÓRMULA | `LET cImporteM50c = cmoneda50c * 0.5;` | 🔴 MONEY/aritmética financiera |
| L180 | FÓRMULA | `LET ctotalMonedas = cImporteM100 + cImporteM50 + cImporteM20 + cImporteM10 + cImporteM5 + cImporteM2` | 🔴 MONEY/aritmética financiera |
| L182 | FÓRMULA | `LET ctotalImporte = ctotalBilletes + ctotalMonedas;` | 🔴 MONEY/aritmética financiera |
| L190 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_con_carga_dotacion_masiva_cifras`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_carga_dotacion_masiva_cifras.sql` |
| **LOC (1er CREATE)** | 200 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta dotación de efectivo (masiva)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_carga_dotacion_masiva_cifras(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATETIME YEAR TO SECOND
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATETIME YEAR TO SECOND` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L39 |
| `iSqlErr` | `INTEGER` | L40 |
| `cbillete1000` | `INTEGER` | L42 |
| `cbillete500` | `INTEGER` | L43 |
| `cbillete200` | `INTEGER` | L44 |
| `cbillete100` | `INTEGER` | L45 |
| `cbillete50` | `INTEGER` | L46 |
| `cbillete20` | `INTEGER` | L47 |
| `ctotalBilletes` | `INTEGER` | L48 |
| `cmoneda100` | `INTEGER` | L49 |
| `cmoneda50` | `INTEGER` | L50 |
| `cmoneda20` | `INTEGER` | L51 |
| `cmoneda10` | `INTEGER` | L52 |
| `cmoneda5` | `INTEGER` | L53 |
| `cmoneda2` | `INTEGER` | L54 |
| `cmoneda1` | `INTEGER` | L55 |
| `cmoneda50c` | `INTEGER` | L56 |
| `ctotalMonedas` | `INTEGER` | L57 |
| `ctotalImporte` | `MONEY` | L58 |
| `sum_billete1000` | `MONEY` | L60 |
| `sum_billete500` | `MONEY` | L61 |
| `sum_billete200` | `MONEY` | L62 |
| `sum_billete100` | `MONEY` | L63 |
| `sum_billete50` | `MONEY` | L64 |
| `sum_billete20` | `MONEY` | L65 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_arch_dot_suc_det` | `bdicnweb` | no | SELECT | L161 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L139 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L178 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L181 | FÓRMULA | `LET ctotalBilletes = cbillete1000 + cbillete500 + cbillete200 + cbillete100 + cbillete50 + cbillete2` |  |
| L182 | FÓRMULA | `LET ctotalMonedas = cmoneda100 + cmoneda50 + cmoneda20 + cmoneda10 + cmoneda5 + cmoneda2 + cmoneda1 ` |  |
| L183 | FÓRMULA | `LET ctotalImporte = ctotalBilletes + ctotalMonedas;` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_cifras` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cifras` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_cattipocliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_cattipocliente.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 92 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo y cliente (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_cattipocliente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `sCalificacion` | `SMALLINT` | L8 |
| `cDescripcion` | `CHAR(50)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iRegistros` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tipocliente` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cliente` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_con_constiporeporteprocesosuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_constiporeporteprocesosuc.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 110 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte y proceso (tipo de, sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_constiporeporteprocesosuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iReporte` | `INTEGER` | L8 |
| `cDescReporte` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_con_tiporeporteprocesosuc` | `bdicnweb` | no | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |

---

## `sp_con_consultactesfusionados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_consultactesfusionados.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clientes (fusionados)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_consultactesfusionados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechIni                     DATE
  pFechFin                     DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechIni` | `DATE` | — | — |
| `pFechFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cFecha` | `CHAR(10)` | L15 |
| `iFusionados` | `INTEGER` | L16 |
| `iNoFusionados` | `INTEGER` | L17 |
| `iTotal` | `INTEGER` | L18 |
| `iTotalFusionados` | `INTEGER` | L19 |
| `iTotalNoFusionados` | `INTEGER` | L20 |
| `iResultadoTotal` | `INTEGER` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |
| `dFechaActual` | `DATE` | L23 |
| `iAnioActual` | `INTEGER` | L24 |
| `iAnioIngreso` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |
| `sp_ctes_fusionados` | `bdinteg` | ⚠️ sí | L81 |
| `sp_ctes_fusionados2` | `bdinteg` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L55 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechIni IS NULL OR pFechFin IS NULL OR pRegistros IS NULL O` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet= '00996';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L86 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_ctes_fusionados';` |  |
| L89 | FÓRMULA | `LET iTotalFusionados = iTotalFusionados + iFusionados;` |  |
| L90 | FÓRMULA | `LET iTotalNoFusionados = iTotalNoFusionados + iNoFusionados;` |  |
| L91 | FÓRMULA | `LET iResultadoTotal = iResultadoTotal + iTotal;` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_ctes_fusionados2';` |  |
| L103 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `fusionados` | MODIF | fusionados | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_con_consultactesfusionados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_consultactesfusionados_totales.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clientes (fusionados, totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ctes_fusionados2_totales` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_consultactesfusionados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechIni                     DATE
  pFechFin                     DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechIni` | `DATE` | — | — |
| `pFechFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_ctes_fusionados2_totales` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechIni IS NULL OR pFechFin IS NULL THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_ctes_fusionados2_totales';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `fusionados` | MODIF | fusionados | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_con_consultaltas_idbox`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_consultaltas_idbox.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta identificador" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_consultaltas_idbox(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecIni                      DATE
  pFecFin                      DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pFecIni` | `DATE` | — | — |
| `pFecFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cSucursal` | `CHAR(5)` | L11 |
| `iAltasTotal` | `INTEGER` | L12 |
| `iTotIdb` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iResultadoAltasTotal` | `INTEGER` | L15 |
| `iResultadoTotIdb` | `INTEGER` | L16 |
| `dFechaActual` | `DATE` | L17 |
| `iAnioActual` | `INTEGER` | L18 |
| `iAnioIngreso` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L45 |
| `si_indicadores_idbox` | `bdinteg` | ⚠️ sí | SELECT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet= '00996';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecIni IS NULL OR pFecFin IS NULL OR pRegistros IS NULL OR p` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L84 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ltas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?bo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ltas_`, `?bo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_consultaltas_idbox_reporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_consultaltas_idbox_reporte.sql` |
| **LOC (1er CREATE)** | 156 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 283 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta identificador y reporte" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_consultaltas_idbox_reporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cSucursal` | `CHAR(5)` | L7 |
| `iAltasTotal` | `INTEGER` | L8 |
| `iTotIdb` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `porciento_sin_idb` | `NUMERIC` | L11 |
| `porciento_idb` | `NUMERIC` | L12 |
| `iResultadoTotSnIdb` | `INTEGER` | L13 |
| `dFechaActual` | `DATE` | L14 |
| `iAnioActual` | `INTEGER` | L15 |
| `iAnioIngreso` | `INTEGER` | L16 |
| `pFecIni` | `DATE` | L17 |
| `pFecFin` | `DATE` | L18 |
| `vcodret` | `char(5)` | L19 |
| `vsqlerr` | `integer` | L20 |
| `vsql` | `char(3000)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L55 |
| `si_indicadores_idbox_reporte` | `bdinteg` | ⚠️ sí | SELECT | L64 |
| `si_indicadores_idbox_reporte` | `bdinteg` | ⚠️ sí | DELETE | L64 |
| `si_indicadores_idbox` | `bdinteg` | ⚠️ sí | SELECT | L83 |
| `si_indicadores_idbox_reporte` | `bdinteg` | ⚠️ sí | INSERT | L113 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet= '00996';` |  |
| L66 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecIni IS NULL OR pFecFin IS NULL OR pRegistros IS NULL OR p` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L87 | FÓRMULA | `LET iResultadoTotSnIdb = (iAltasTotal - iTotIdb );` |  |
| L88 | FÓRMULA | `LET porciento_sin_idb = (iResultadoTotSnIdb / iAltasTotal ) * 100;` |  |
| L89 | FÓRMULA | `LET porciento_idb = (iTotIdb / iAltasTotal ) * 100;` |  |
| L99 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L101 | FÓRMULA | `LET iResultadoTotSnIdb = (iAltasTotal - iTotIdb );` |  |
| L106 | FÓRMULA | `LET porciento_sin_idb = (iResultadoTotSnIdb / iAltasTotal ) * 100;` |  |
| L111 | FÓRMULA | `LET porciento_idb = (iTotIdb / iAltasTotal ) * 100;` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L128 | FÓRMULA | `let vsql = ' echo "cCodRet\|cSucursal\|iResultadoTotSnIdb\|iTotIdb\|iAltasTotal\|porciento_sin_idb\|` |  |
| L130 | FÓRMULA | `let vsql=  'echo "UNLOAD TO /RESPALDOSNEW/TmpIDBox.xls '\|\|` |  |
| L134 | FÓRMULA | `let vsql= 'dbaccess bdinteg /RESPALDOSNEW/rpt_idbox.sql';` |  |
| L136 | FÓRMULA | `let vsql ='rm  /RESPALDOSNEW/rpt_idbox.sql';` |  |
| L138 | FÓRMULA | `let vsql = "sed 's/\|$//g' /RESPALDOSNEW/TmpIDBox.xls >>/RESPALDOSNEW/rep_idbox_mensual.xls";` |  |
| L140 | FÓRMULA | `let vsql ='rm  /RESPALDOSNEW/TmpIDBox.xls';` |  |
| L143 | CÓDIGO_RETORNO | `let vcodret = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?ltas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?bo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ltas_`, `?bo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_consultausokiosko`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_consultausokiosko.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_consultausokiosko(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cSucursal` | `CHAR(4)` | L14 |
| `dConsSaldos` | `INTEGER` | L15 |
| `dConsMov` | `INTEGER` | L16 |
| `dConsEstadoCta` | `INTEGER` | L17 |
| `iNoRegistros` | `INTEGER` | L18 |
| `iTotalSaldos` | `INTEGER` | L19 |
| `iTotalMovimiento` | `INTEGER` | L20 |
| `iTotalEstadoCuenta` | `INTEGER` | L21 |
| `iIdConsecutivo` | `INTEGER` | L22 |
| `dFechaActual` | `DATE` | L23 |
| `iAnioActual` | `INTEGER` | L24 |
| `iAnioIngreso` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L75 |
| `si_indicadores_kiosko` | `bdinteg` | ⚠️ sí | SELECT | L87 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet			 		= '00000';` |  |
| L57 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet= '00996';` |  |
| L98 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L117 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?usoki` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ko` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?usoki`, `?ko` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_consultausokiosko_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_consultausokiosko_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_consultausokiosko_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_indicadores_kiosko` | `bdinteg` | ⚠️ sí | SELECT | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?usoki` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ko_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?usoki`, `?ko_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_consultaverificasms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_consultaverificasms.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SMS" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_verifica_sms`, `sp_verifica_sms2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_consultaverificasms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechIni                     DATE
  pFechFin                     DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechIni` | `DATE` | — | — |
| `pFechFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cFecha` | `CHAR(10)` | L15 |
| `iValidos` | `INTEGER` | L16 |
| `iNoValidos` | `INTEGER` | L17 |
| `iTotal` | `INTEGER` | L18 |
| `iTotalValidos` | `INTEGER` | L19 |
| `iTotalNoValidos` | `INTEGER` | L20 |
| `iResultadoTotal` | `INTEGER` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_verifica_sms` | `bdinteg` | ⚠️ sí | L64 |
| `sp_verifica_sms2` | `bdinteg` | ⚠️ sí | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechIni IS NULL OR pFechFin IS NULL OR pRegistros IS NULL O` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_verifica_sms';` |  |
| L72 | FÓRMULA | `LET iTotalValidos = iTotalValidos + iValidos;` |  |
| L73 | FÓRMULA | `LET iTotalNoValidos = iTotalNoValidos + iNoValidos;` |  |
| L74 | FÓRMULA | `LET iResultadoTotal = iResultadoTotal + iTotal;` |  |
| L83 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_verifica_sms2';` |  |
| L86 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `sms` | ENTIDAD | SMS | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_con_consultaverificasms_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_consultaverificasms_totales.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SMS (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_consultaverificasms_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechIni                     DATE
  pFechFin                     DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechIni` | `DATE` | — | — |
| `pFechFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `dFechaActual` | `DATE` | L11 |
| `iAnioActual` | `INTEGER` | L12 |
| `iAnioIngreso` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_verifica_sms2_totales` | `bdinteg` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechIni IS NULL OR pFechFin IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet= '00996';` |  |
| L66 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_verifica_sms2_totales';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `sms` | ENTIDAD | SMS | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_con_diasinhabiles`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_diasinhabiles.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 285 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta día hábil — día bancario operativo (del día)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_diasinhabiles(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INT` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cDescripcion` | `CHAR(30)` | L13 |
| `dFechaDiaInhabil` | `DATE` | L14 |
| `dFechaProxLaborable` | `DATE` | L15 |
| `cLaborable` | `CHAR(1)` | L16 |
| `cLaborableBanca` | `CHAR(1)` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_feriado` | `bdinteg` | ⚠️ sí | SELECT | L58 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `?sin` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `habil` | ENTIDAD | día hábil — día bancario operativo (spei_validafecha, sp_cam | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sin`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_enviar_dotacion_masiva_etv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_enviar_dotacion_masiva_etv.sql` |
| **LOC (1er CREATE)** | 177 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta dotación de efectivo (masiva)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_enviar_dotacion_masiva_etv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATETIME YEAR TO SECOND
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATETIME YEAR TO SECOND` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L30 |
| `iSqlErr` | `INTEGER` | L31 |
| `cfechaSolicitud` | `DATE` | L32 |
| `cfechaAplicacion` | `DATE` | L33 |
| `cccSucursal` | `CHAR(4)` | L34 |
| `cnombreSucursal` | `CHAR(40)` | L35 |
| `cplaza` | `CHAR(50)` | L36 |
| `ctipoOperacion` | `CHAR(20)` | L37 |
| `cbillete1000` | `SMALLINT` | L38 |
| `cbillete500` | `SMALLINT` | L39 |
| `cbillete200` | `SMALLINT` | L40 |
| `cbillete100` | `SMALLINT` | L41 |
| `cbillete50` | `SMALLINT` | L42 |
| `cbillete20` | `SMALLINT` | L43 |
| `ctotalBilletes` | `SMALLINT` | L44 |
| `cmoneda100` | `SMALLINT` | L45 |
| `cmoneda50` | `SMALLINT` | L46 |
| `cmoneda20` | `SMALLINT` | L47 |
| `cmoneda10` | `SMALLINT` | L48 |
| `cmoneda5` | `SMALLINT` | L49 |
| `cmoneda2` | `SMALLINT` | L50 |
| `cmoneda1` | `SMALLINT` | L51 |
| `cmoneda50c` | `SMALLINT` | L52 |
| `ctotalMonedas` | `SMALLINT` | L53 |
| `ctotalImporte` | `MONEY` | L54 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `arch_dotacion_sucursal` | `bdicnweb` | no | SELECT | L137 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L119 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L144 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `envia` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_etv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_`, `?_etv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_genrepoconrresponsales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_genrepoconrresponsales.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta (especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_genrepoconrresponsales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  psArchivoOrigen              CHAR(3)
  pFecha                       DATE
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `psArchivoOrigen` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `cNombreArchivo2` | `CHAR(100)` | L11 |
| `bInTransaction` | `BOOLEAN` | L12 |
| `ven_transacc` | `SMALLINT` | L13 |
| `cBanDetError` | `CHAR(1)` | L14 |
| `iTotal` | `INTEGER` | L15 |
| `dFechaHoy` | `DATE` | L16 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L17 |
| `cFechaHoraArchivo` | `CHAR(35)` | L18 |
| `nomFecha` | `CHAR(19)` | L19 |
| `cFecha` | `CHAR(10)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | SELECT | L70 |
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | DELETE | L78 |
| `systables` | `bdicnweb` | no | SELECT | L88 |
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | INSERT | L168 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pRutaDescarga='' OR psArchivoOrigen = '' OR psArchivoOrigen ` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | FÓRMULA | `LET pFecha = MDY(MONTH(pFecha), DAY(pFecha), YEAR(pFecha)) -1 UNITS DAY;` |  |
| L89 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L90 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT ''''\|\|idterminal\|\|'',TO_CHAR(fechamov, '%d-%m-%Y %H:%M:` | 🔴 MONEY/aritmética financiera |
| L100 | FÓRMULA | `LET cRutaGral = TRIM(pRutaDescarga)\|\|'/'\|\|TRIM(cNombreArchivo);` |  |
| L129 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L134 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L143 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L152 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?rr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?onsales` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o`, `?rr`, `?onsales` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_genreportesconciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_genreportesconciliacion.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_genreportesconciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)     AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iRegistro` | `INTEGER` | L14 |
| `cNombreArchivo` | `CHAR(100)` | L15 |
| `dFechaReporte` | `DATE` | L16 |
| `dHrReporte` | `DATETIME HOUR TO SECOND` | L17 |
| `cUsuario` | `CHAR(8)` | L18 |
| `cEstatus` | `CHAR(1)` | L19 |
| `iTotalRegistros` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | FÓRMULA | `LET iTotalRegistros = iTotalRegistros + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?ortes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ortes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_genreportesconciliacion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_genreportesconciliacion_totales.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_genreportesconciliacion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)     AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iTotalRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?ortes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ortes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_repoconsultaconadmin2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_repoconsultaconadmin2.sql` |
| **LOC (1er CREATE)** | 201 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_repoconsultaconadmin2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  psArchivoOrigen              CHAR(3)
  pFecha                       DATE
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `psArchivoOrigen` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `cNombreArchivo2` | `CHAR(100)` | L11 |
| `bInTransaction` | `BOOLEAN` | L12 |
| `ven_transacc` | `SMALLINT` | L13 |
| `cBanDetError` | `CHAR(1)` | L14 |
| `iTotal` | `INTEGER` | L15 |
| `dFechaHoy` | `DATE` | L16 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L17 |
| `cFechaHoraArchivo` | `CHAR(35)` | L18 |
| `nomFecha` | `CHAR(19)` | L19 |
| `cFecha` | `CHAR(10)` | L20 |
| `vsnomarchivo` | `CHAR(23)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | SELECT | L73 |
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | DELETE | L81 |
| `systables` | `bdicnweb` | no | SELECT | L92 |
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | INSERT | L187 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pRutaDescarga='' OR psArchivoOrigen = '' OR psArchivoOrigen ` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | FÓRMULA | `LET pFecha = MDY(MONTH(pFecha), DAY(pFecha), YEAR(pFecha)) -1 UNITS DAY;` |  |
| L93 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L94 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT ''''\|\|idterminal\|\|'',TO_CHAR(fechamov, '%d-%m-%Y %H:%M:` | 🔴 MONEY/aritmética financiera |
| L107 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L108 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT {+index (intercard:conadmin idx_conadmin4)} nomarchivo325\|` | 🔴 MONEY/aritmética financiera |
| L120 | FÓRMULA | `LET cRutaGral = TRIM(pRutaDescarga)\|\|'/'\|\|TRIM(cNombreArchivo);` |  |
| L148 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L153 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L162 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L171 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_repoconsultaconadmincorr2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_repoconsultaconadmincorr2.sql` |
| **LOC (1er CREATE)** | 185 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_repoconsultaconadmincorr2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  psArchivoOrigen              CHAR(3)
  pFecha                       DATE
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `psArchivoOrigen` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `cNombreArchivo2` | `CHAR(100)` | L11 |
| `bInTransaction` | `BOOLEAN` | L12 |
| `ven_transacc` | `SMALLINT` | L13 |
| `cBanDetError` | `CHAR(1)` | L14 |
| `iTotal` | `INTEGER` | L15 |
| `dFechaHoy` | `DATE` | L16 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L17 |
| `cFechaHoraArchivo` | `CHAR(35)` | L18 |
| `nomFecha` | `CHAR(19)` | L19 |
| `cFecha` | `CHAR(10)` | L20 |
| `vsnomarchivo` | `CHAR(23)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | SELECT | L73 |
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | DELETE | L81 |
| `systables` | `bdicnweb` | no | SELECT | L88 |
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | INSERT | L172 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pRutaDescarga='' OR psArchivoOrigen = '' OR psArchivoOrigen ` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L90 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT  nomarchivo325\|\|'',TO_CHAR(fecha,'%d/%m/%Y'),''''\|\|tarj` | 🔴 MONEY/aritmética financiera |
| L101 | FÓRMULA | `LET cRutaGral = TRIM(pRutaDescarga)\|\|'/'\|\|TRIM(cNombreArchivo);` |  |
| L133 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L138 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L147 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L156 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?corr2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o`, `?corr2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_con_validadiainhabil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_con_validadiainhabil.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 288 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta día hábil — día bancario operativo (del día)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_con_validadiainhabil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDiaInhabil                  DATE
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDiaInhabil` | `DATE` | `dia`=del día · `habil`=día hábil — día bancario operativo (spei_validafecha, sp_cambio_fecha) | 🔵 CONVENCIÓN / ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_feriado` | `bdinteg` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDiaInhabil IS NULL THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `dia` | MODIF | del día | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?in` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `habil` | ENTIDAD | día hábil — día bancario operativo (spei_validafecha, sp_cam | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?in` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_concentracionrecibida_cg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_concentracionrecibida_cg.sql` |
| **LOC (1er CREATE)** | 206 |
| **Callgraph** | ✅ fan_in=25 / fan_out=9 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "concentración de fondos identificador y cg — Canal/Cuenta General" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_concentracionrecibida_cg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolioOperacion              CHAR(8)
  pOrigen                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pFolioOperacion` | `CHAR(8)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cStatus` | `CHAR(2)` | L7 |
| `cCodTrans` | `CHAR(4)` | L8 |
| `cCodProveedor` | `CHAR(4)` | L9 |
| `fDenominacionOp1` | `FLOAT` | L10 |
| `fDenominacionOp2` | `FLOAT` | L11 |
| `fDenominacionOp3` | `FLOAT` | L12 |
| `fDenominacionOp4` | `FLOAT` | L13 |
| `fDenominacionOp5` | `FLOAT` | L14 |
| `fDenominacionOp6` | `FLOAT` | L15 |
| `fDenominacionOp7` | `FLOAT` | L16 |
| `fDenominacionOp8` | `FLOAT` | L17 |
| `fDenominacionOp9` | `FLOAT` | L18 |
| `fDenominacionOp10` | `FLOAT` | L19 |
| `fDenominacionOp11` | `FLOAT` | L20 |
| `fDenominacionOp12` | `FLOAT` | L21 |
| `fDenominacionOp13` | `FLOAT` | L22 |
| `fDenominacionOp14` | `FLOAT` | L23 |
| `fDenominacionOp15` | `FLOAT` | L24 |
| `fCantidadOp1` | `FLOAT` | L25 |
| `fCantidadOp2` | `FLOAT` | L26 |
| `fCantidadOp3` | `FLOAT` | L27 |
| `fCantidadOp4` | `FLOAT` | L28 |
| `fCantidadOp5` | `FLOAT` | L29 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L112 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L139 |
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | UPDATE | L179 |
| `ss_catstatus` | `bdisuc` | ⚠️ sí | SELECT | L189 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L102 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L144 | FÓRMULA | `LET fMonto = fCantidadOp1 * fDenominacionOp1 +` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?ib` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?a_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ib`, `?a_`, `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_concentracionrecibida_cg_masivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_concentracionrecibida_cg_masivo.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "concentración de fondos identificador y cg — Canal/Cuenta General (masivo)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena`, `sp_concentracionrecibida_cg` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_concentracionrecibida_cg_masivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTramaFolios                 CHAR(500)
  pOrigen                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pTramaFolios` | `CHAR(500)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cDescripcion` | `CHAR(30)` | L8 |
| `cFolioOperacion` | `CHAR(8)` | L9 |
| `iRegActualizados` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_split_cadena` | `bdicnweb` | no | L48 |
| `sp_concentracionrecibida_cg` | `bdicnweb` | no | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L55 | FÓRMULA | `LET iRegActualizados =	iRegActualizados + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `concentracion` | ACCION | concentración de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?ib` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?a_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cg` | ENTIDAD | cg — Canal/Cuenta General (subsistema sp_cg_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ib`, `?a_`, `cg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conciliaciontotalporconveniosac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conciliaciontotalporconveniosac.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación convenio y Servicios de Atención al Cliente — subsistema de atención en sucursal (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_conciliaciontotalporconvenio` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_conciliaciontotalporconveniosac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  cConvenio                    CHAR(5)
  dFechaIni                    DATE
  dFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)         AS retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `cConvenio` | `CHAR(5)` | `convenio`=convenio (nómina/empresarial) | ✅ CÓDIGO |
| `dFechaIni` | `DATE` | — | — |
| `dFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cNomConvenio` | `CHAR(40)` | L15 |
| `dFechapago` | `DATE` | L16 |
| `deImporteArchivo` | `DECIMAL(16,2)` | L17 |
| `cCuentaCheques` | `CHAR(30)` | L18 |
| `deImporteCheq` | `DECIMAL(16,2)` | L19 |
| `cCuentaContable` | `CHAR(30)` | L20 |
| `deImporteConta` | `DECIMAL(16,2)` | L21 |
| `iRegistros` | `INTEGER` | L22 |
| `iRecuperacion` | `INTEGER` | L23 |
| `iNoRegs` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusconveniosac` | `bdicnweb` | no | SELECT | L70 |
| `sw_verificastatusconveniosac` | `bdicnweb` | no | DELETE | L70 |
| `sw_conciliaciontotalporconveniosacinfo` | `bdicnweb` | no | SELECT | L71 |
| `sw_conciliaciontotalporconveniosacinfo` | `bdicnweb` | no | DELETE | L71 |
| `sw_verificastatusconveniosac` | `bdicnweb` | no | INSERT | L72 |
| `sw_conciliaciontotalporconveniosacinfo` | `bdicnweb` | no | INSERT | L103 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_conciliaciontotalporconvenio` | `bdisac` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L86 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL bdisac:sp_conciliaciontotalporcon` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L100 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L101 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_conciliaciontotalporconveniosacinfo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conciliaciontotalporconveniosacinfo.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación convenio, Servicios de Atención al Cliente — subsistema de atención en sucursal y información (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_conciliaciontotalporconveniosacinfo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)         AS retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `cCodRetSp` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `iNumPagos` | `INTEGER` | L23 |
| `cNomConvenio` | `CHAR(40)` | L24 |
| `mImportePago` | `MONEY(16,2)` | L25 |
| `mImporteComisionConvenio` | `MONEY(16,2)` | L26 |
| `mIvaComisionConvenio` | `MONEY(16,2)` | L27 |
| `mImporteComisionCte` | `MONEY(16,2)` | L28 |
| `mIvaComisionCte` | `MONEY(16,2)` | L29 |
| `iFlagConfirmacionCentral` | `INTEGER` | L30 |
| `iFlagConfirmacionSucursal` | `INTEGER` | L31 |
| `dFechapago` | `DATE` | L33 |
| `deImporteArchivo` | `DECIMAL(16,2)` | L34 |
| `cCuentaCheques` | `CHAR(30)` | L35 |
| `deImporteCheq` | `DECIMAL(16,2)` | L36 |
| `cCuentaContable` | `CHAR(30)` | L37 |
| `deImporteConta` | `DECIMAL(16,2)` | L38 |
| `iRegistros` | `INTEGER` | L39 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_conciliaciontotalporconveniosacinfo` | `bdicnweb` | no | SELECT | L96 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_concurrencia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_concurrencia.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_concurrencia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBloqueo                     CHAR(1)
  pIdSession                   char(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBloqueo` | `CHAR(1)` | — | — |
| `pIdSession` | `char(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cStatusFuncionalidad` | `CHAR(1)` | L5 |
| `cSesionActiva` | `CHAR(40)` | L6 |
| `iSqlErr` | `INT` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_concurrencia` | `bdicnweb` | no | SELECT | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | VALIDACIÓN_NULL | `IF cStatusFuncionalidad IS NULL THEN` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00150';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?currencia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?currencia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_confechaultmovimiento`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_confechaultmovimiento.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta fecha y movimiento" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_confechaultmovimiento(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMCUENTA                   CHAR(20)
  cSISTEMACUENTA               CHAR(2)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNUMCUENTA` | `CHAR(20)` | — | — |
| `cSISTEMACUENTA` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSql_err` | `INT` | L9 |
| `pEmpresa` | `CHAR(3)` | L11 |
| `dFecha` | `DATE` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L61 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fecha` | ENTIDAD | fecha | 🔵 CONVENCIÓN | nombre_sp |
| `?ult` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movimiento` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ult` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conreversosmvtos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conreversosmvtos.sql` |
| **LOC (1er CREATE)** | 131 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_conreversosmvtos(
  pUsuario                     char(10)
  pIdFuncion                   char(10)
  pFolioMovimiento             char(16)
  pRegistros                   int
  pRecuperacion                int
) RETURNING char(5) as codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(10)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pFolioMovimiento` | `char(16)` | — | — |
| `pRegistros` | `int` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L15 |
| `cFolio` | `char(16)` | L16 |
| `cDescripcion` | `char(60)` | L17 |
| `cCuenta` | `char(20)` | L18 |
| `iNoCheque` | `integer` | L19 |
| `mMonto` | `money(14,2)` | L20 |
| `cCancelado` | `char(1)` | L21 |
| `cNoSucursal` | `char(4)` | L22 |
| `iSqlErr` | `int` | L23 |
| `cEmpresa` | `char(3)` | L24 |
| `iNoRegistros` | `int` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L102 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L116 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?smvt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?smvt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_teltransfer`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cons_teltransfer.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta teléfono y transferencia" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cons_teltransfer(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cTelefono` | `CHAR(10)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | VALIDACIÓN_NULL | `IF cTelefono = '' OR cTelefono IS NULL THEN` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP";` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tel` | ENTIDAD | teléfono | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consadmincorresponsalccl_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consadmincorresponsalccl_totales.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos, corresponsal y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `intercard` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consadmincorresponsalccl_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivoOrigen               CHAR(3)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivoOrigen` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `intercard` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pArchivoOrigen = '' OR pFecha IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP intercard:sp_consultaconadminco` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `corresponsal` | ENTIDAD | corresponsal | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consadmincorresponsalesccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consadmincorresponsalesccl.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos, corresponsal y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `intercard` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consadmincorresponsalesccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivoOrigen               CHAR(3)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivoOrigen` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cIdTerminal` | `CHAR(4)` | L14 |
| `dFechaMov` | `DATETIME YEAR TO FRACTION(5)` | L15 |
| `mMonto` | `MONEY(18,2)` | L16 |
| `mComision` | `MONEY(18,2)` | L17 |
| `mComisionIva` | `MONEY(18,2)` | L18 |
| `mId_Tipo_Operacion` | `MONEY(18,2)` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |
| `iRegistros` | `INTEGER` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L60 |
| `intercard` | `bdicnweb` | no | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pArchivoOrigen = '' OR pFecha IS NULL OR pRegistros IS NULL O` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L71 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP intercard:sp_concorresponsales';` |  |
| L73 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `corresponsal` | ENTIDAD | corresponsal | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consadmincorresponsalesccl_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consadmincorresponsalesccl_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos, corresponsal y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `intercard` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consadmincorresponsalesccl_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivoOrigen               CHAR(3)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivoOrigen` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `intercard` | `bdicnweb` | no | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pArchivoOrigen = '' OR pFecha IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP intercard:sp_concorresponsales2` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `corresponsal` | ENTIDAD | corresponsal | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consadminusuarioccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consadminusuarioccl.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos, usuario y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consadminusuarioccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveUsuario                CHAR (10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveUsuario` | `CHAR (10)` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR (50)` | L9 |
| `cNombre` | `CHAR (90)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `bditarjeta` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_consultausuario'` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00693';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consaldodisp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consaldodisp.sql` |
| **LOC (1er CREATE)** | 116 |
| **Callgraph** | ✅ fan_in=20 / fan_out=3 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consaldodisp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pSistemaCuenta               CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `dSaldoDisponible` | `DECIMAL(14,2)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cNumProductoCred` | `CHAR(4)` | L9 |
| `cCodTipoCred` | `CHAR(2)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L54 |
| `sv_maeinv` | `bdinvers` | ⚠️ sí | SELECT | L65 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L76 |
| `sd_maesdos` | `bdicred` | ⚠️ sí | SELECT | L89 |
| `sd_maesdoscrd` | `bdicred` | ⚠️ sí | SELECT | L96 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00037';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?aldodi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aldodi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consaltapagaresintransaccheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consaltapagaresintransaccheques.sql` |
| **LOC (1er CREATE)** | 196 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta pagarés, interés, Servicios de Atención al Cliente — subsistema de atención en sucursal y cheques" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consaltapagaresintransaccheques(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L46 |
| `iSqlErr` | `INTEGER` | L47 |
| `iNoRegistros` | `INTEGER` | L48 |
| `dFecha` | `DATE` | L49 |
| `cEmpresa` | `CHAR` | L50 |
| `cCuenta` | `CHAR` | L51 |
| `sSecuencia` | `SMALLINT` | L52 |
| `cCodInstrum` | `CHAR (4)` | L53 |
| `cNumCte` | `CHAR (20)` | L54 |
| `cStatus` | `CHAR (1)` | L55 |
| `cMotivo` | `CHAR (2)` | L56 |
| `dFecUltMov` | `DATE` | L57 |
| `dFecCancelac` | `DATE` | L58 |
| `dFecReinversion` | `DATE` | L59 |
| `mCapital` | `MONEY (18,2)` | L60 |
| `mSdoRetenido` | `MONEY (18,2)` | L61 |
| `mSdoCong` | `MONEY (18,2)` | L62 |
| `sPlazo` | `SMaLLINT` | L63 |
| `dFechaVenc` | `DATE` | L64 |
| `cOpcionRetiro` | `CHAR (2)` | L65 |
| `mInteres` | `MONEY (18,2)` | L66 |
| `mIsr` | `MONEY (18,2)` | L67 |
| `dTasa` | `DECIMAL (9,6)` | L68 |
| `dSobreTasa` | `DECIMAL (9,6)` | L69 |
| `sDiaSdoPos` | `SMALLINT` | L70 |
| *…22 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_altainvsintranchq` | `bdinvers` | ⚠️ sí | SELECT | L171 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L164 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L152 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L174 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L180 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `pagares` | ENTIDAD | pagarés | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?ran` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ran` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consaltapagaresintransaccheques_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consaltapagaresintransaccheques_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta pagarés, interés, Servicios de Atención al Cliente — subsistema de atención en sucursal y cheques (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consaltapagaresintransaccheques_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_altainvsintranchq` | `bdinvers` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pFechaInicio IS NULL OR pFechaFin IS NULL   THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `pagares` | ENTIDAD | pagarés | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?ran` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ran` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consarchivoresumenccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consarchivoresumenccl.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivo y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consarchivoresumenccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNomArchivo                  CHAR(23)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNomArchivo` | `CHAR(23)` | `archivo`=archivo | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `cCodRetSp` | `CHAR(6)` | L29 |
| `cDescCodRet` | `CHAR(80)` | L30 |
| `iCodRetSp` | `INTEGER` | L31 |
| `iSqlErr` | `INTEGER` | L32 |
| `cNombreArchivo` | `CHAR(23)` | L33 |
| `cArchivoOrigen` | `CHAR(3)` | L34 |
| `iRegistrosArch` | `INTEGER` | L35 |
| `mMontoArch` | `MONEY(16,2)` | L36 |
| `iRegistrosAplicadosArch` | `INTEGER` | L37 |
| `iNumCargosArch` | `INTEGER` | L38 |
| `iNumAbonosArch` | `INTEGER` | L39 |
| `iCargosAplicados` | `INTEGER` | L40 |
| `mMontoCargosApli` | `MONEY(16,2)` | L41 |
| `iNumAbonosApli` | `INTEGER` | L42 |
| `mMontoAbonosApli` | `MONEY(16,2)` | L43 |
| `iMovErrAplica` | `INTEGER` | L44 |
| `iConciliadoIntercard` | `INTEGER` | L45 |
| `iPagoInter` | `INTEGER` | L46 |
| `iConcMontoMenor` | `INTEGER` | L47 |
| `iConcMontoMayor` | `INTEGER` | L48 |
| `iMovPreConc` | `INTEGER` | L49 |
| `iNoConc` | `INTEGER` | L50 |
| `iForzado` | `INTEGER` | L51 |
| `iNoConcErr` | `INTEGER` | L52 |
| *…5 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L111 |
| `bditarjeta` | `bdicnweb` | no | L120 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L126 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_conarchivoresume` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `?resumen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?resumen` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consarchivosdetcashbackccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consarchivosdetcashbackccl.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivos, detalle y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consarchivosdetcashbackccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pTipoConciliacion            CHAR(3)
  pNombreArchivo               CHAR(23)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pTipoConciliacion` | `CHAR(3)` | — | — |
| `pNombreArchivo` | `CHAR(23)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iCodRetSp` | `INTEGER` | L20 |
| `cNombreArchivo` | `CHAR(23)` | L21 |
| `cArchivoOrigen` | `CHAR(3)` | L22 |
| `cFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L23 |
| `cIntegridad` | `CHAR(1)` | L24 |
| `cNumTarjeta` | `CHAR(16)` | L25 |
| `cSecuencia325` | `CHAR(6)` | L26 |
| `cMonto325` | `CHAR(13)` | L27 |
| `cMontoCashBack325` | `CHAR(13)` | L28 |
| `cNumCuenta` | `CHAR(20)` | L29 |
| `cIdComercio325` | `CHAR(9)` | L30 |
| `cNomComercio325` | `CHAR(30)` | L31 |
| `cTipoTransaccion325` | `CHAR(15)` | L32 |
| `cP_MENSAJE` | `CHAR(80)` | L33 |
| `iNoRegistros` | `INTEGER` | L34 |
| `iRegistros` | `INTEGER` | L35 |
| `iRecuperacion` | `INTEGER` | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L80 |
| `bditarjeta` | `bdicnweb` | no | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L68 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pTipo = '' OR pNombreArchivo = '' OR pRegistros IS NULL OR p` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_conarchivodetalle_con';` |  |
| L95 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `?cashback` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cashback` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consarchivosdetcashbackccl_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consarchivosdetcashbackccl_totales.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivos, detalle y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consarchivosdetcashbackccl_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pTipoConciliacion            CHAR(3)
  pNombreArchivo               CHAR(23)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pTipoConciliacion` | `CHAR(3)` | — | — |
| `pNombreArchivo` | `CHAR(23)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRet` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L36 |
| `bditarjeta` | `bdicnweb` | no | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditarjeta:sp_conarchivodetalle` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `?cashback` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cashback` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consarchivosdmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consarchivosdmi.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 75 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `sp_domi_consarch2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consarchivosdmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cDescCodRet` | `CHAR(80)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cNombreArch` | `CHAR(20)` | L14 |
| `iTotRegistros` | `INTEGER` | L15 |
| `iImporte` | `MONEY(16,2)` | L16 |
| `dFechaPresentacion` | `DATE` | L17 |
| `cDescEstatus` | `CHAR(30)` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L55 |
| `sp_domi_consarch2` | `bdidomi` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdidomi:sp_domi_consarch2';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L70 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?dmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consarchivosdmi_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consarchivosdmi_totales.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 74 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivos (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `sp_domi_consarch2_totales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consarchivosdmi_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(80)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `sp_domi_consarch2_totales` | `bdidomi` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdidomi:sp_domi_consarch2_tota` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?dmi_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dmi_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consarchrecibidos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consarchrecibidos.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=10 / fan_out=14 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivo, identificador y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consarchrecibidos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pFechaConsulta` | `DATE` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | `rec`=recepción / recibe | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `dFechaHoy` | `DATE` | L18 |
| `cNombreArch` | `CHAR(22)` | L19 |
| `cCodOperacion` | `CHAR(3)` | L20 |
| `cHoraEntrada` | `CHAR(25)` | L21 |
| `cTipoIngreso` | `CHAR(1)` | L22 |
| `iTotalRegistros` | `INTEGER` | L23 |
| `cComentario` | `CHAR(100)` | L24 |
| `cFechaAplic` | `CHAR(10)` | L25 |
| `cHoraAplic` | `CHAR(25)` | L26 |
| `cCveStatus` | `CHAR(2)` | L27 |
| `cProcesado` | `CHAR(1)` | L28 |
| `cLectura` | `CHAR(10)` | L29 |
| `cStatus` | `CHAR(16)` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | SELECT | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta IS NULL OR pRegistros IS NULL OR pRecuperacion` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L101 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00782';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?ib` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ib` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consarchrecibidos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consarchrecibidos_totales.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=10 / fan_out=14 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivo, identificador y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consarchrecibidos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pFechaConsulta` | `DATE` | `cons`=consulta | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `dFechaHoy` | `DATE` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?ib` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ib` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consasignacionesfusioncte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consasignacionesfusioncte.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consasignacionesfusioncte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `cNumEjecutivo` | `CHAR(20)` | L16 |
| `cNombreEjecutivo` | `CHAR(100)` | L17 |
| `cNumCliente` | `CHAR(20)` | L18 |
| `cNombreCliente` | `CHAR(100)` | L19 |
| `cCveStatusAsig` | `CHAR(1)` | L20 |
| `cDescripcionStatusAsig` | `CHAR(20)` | L21 |
| `iNoRegistro` | `INTEGER` | L22 |
| `iIdRegistro` | `INTEGER` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | FÓRMULA | `LET iNoRegistro = iNoRegistro + 1;` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?ciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fusion` | ACCION | fusiona cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consbloqctacre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consbloqctacre.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuenta y crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consbloqctacre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEstatusBloqueoCta` | `CHAR(1)` | L9 |
| `iTipoBloqueo` | `SMALLINT` | L10 |
| `cClaveBloqueo` | `CHAR(2)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_consbloqueomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consbloqueomasivocre.sql` |
| **LOC (1er CREATE)** | 197 |
| **Callgraph** | ✅ fan_in=0 / fan_out=45 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito (masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consbloqueomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `iIdRegistro` | `INTEGER` | L23 |
| `cNoCuenta` | `CHAR(20)` | L24 |
| `cNoCliente` | `CHAR(20)` | L25 |
| `cResultado` | `CHAR(15)` | L26 |
| `cCodRetSp` | `CHAR(6)` | L27 |
| `cMotivoRechazo` | `CHAR(80)` | L28 |
| `mSaldo` | `MONEY(14,2)` | L29 |
| `cNombreCliente` | `CHAR(107)` | L30 |
| `iTipoBloqueo` | `INTEGER` | L31 |
| `cCausaBloqueo` | `CHAR(2)` | L32 |
| `dFechaBloqueo` | `DATE` | L33 |
| `cEmpleado` | `CHAR(8)` | L34 |
| `cNombreEmpleado` | `CHAR(45)` | L35 |
| `cStatusRegistro` | `CHAR(1)` | L36 |
| `iExiste` | `INTEGER` | L37 |
| `iNoRegistros` | `INTEGER` | L38 |
| `cAreaSolicita` | `CHAR(150)` | L39 |
| `cJustificacion` | `CHAR(150)` | L40 |
| `dSaldoCapital` | `DECIMAL(18,2)` | L41 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_bloqueocre` | `bdicnweb` | no | SELECT | L100 |
| `sw_tr_cargamasiva_bloqueocre_hist` | `bdicnweb` | no | SELECT | L104 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L145 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L161 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L166 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L76 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL ` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L142 | VALIDACIÓN_NULL | `IF cNoCliente IS NULL OR cNoCliente = '' THEN` |  |
| L172 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_conscargomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscargomasivocre.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cargo y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscargomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INT` | L18 |
| `iExiste` | `INT` | L19 |
| `iIdRegistro` | `INT` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `cResultado` | `CHAR(15)` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `cMotivoRechazo` | `CHAR(100)` | L24 |
| `cFolio` | `CHAR(20)` | L25 |
| `mSaldo` | `money(14,2)` | L26 |
| `cTransacc` | `CHAR(4)` | L27 |
| `cDescTransaccion` | `CHAR(50)` | L28 |
| `mImportePago` | `money(14,2)` | L29 |
| `cComentario` | `CHAR(255)` | L30 |
| `cStatus` | `CHAR(1)` | L31 |
| `iRegistros` | `int` | L32 |
| `cNumCliente` | `char(20)` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_cargo` | `bdicnweb` | no | SELECT | L76 |
| `sw_tr_cargamasiva_cargo_hist` | `bdicnweb` | no | SELECT | L80 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L107 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L125 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_conscargomasivocre_hist`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscargomasivocre_hist.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cargo y crédito (masivo, histórico/historial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscargomasivocre_hist(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INT` | L18 |
| `iExiste` | `INT` | L19 |
| `iIdRegistro` | `INT` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `cResultado` | `CHAR(15)` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `cMotivoRechazo` | `CHAR(100)` | L24 |
| `cFolio` | `CHAR(20)` | L25 |
| `mSaldo` | `money(14,2)` | L26 |
| `cTransacc` | `CHAR(4)` | L27 |
| `cDescTransaccion` | `CHAR(50)` | L28 |
| `mImportePago` | `money(14,2)` | L29 |
| `cComentario` | `CHAR(255)` | L30 |
| `cStatus` | `CHAR(1)` | L31 |
| `iRegistros` | `int` | L32 |
| `cNumCliente` | `char(20)` | L33 |
| `cCodRetReverso` | `CHAR(6)` | L34 |
| `cResultadoReverso` | `CHAR(15)` | L35 |
| `cComentarioReverso` | `CHAR(150)` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_cargo_hist` | `bdicnweb` | no | SELECT | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L108 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `hist` | MODIF | histórico/historial | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_conscargosaperturacheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscargosaperturacheques.sql` |
| **LOC (1er CREATE)** | 162 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cargo, apertura y cheques" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscargosaperturacheques(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L34 |
| `iSqlErr` | `INTEGER` | L35 |
| `dFecha` | `DATE` | L36 |
| `cAniomes` | `CHAR (6)` | L37 |
| `iNumSerial` | `INTEGER` | L38 |
| `cFolioSuc` | `CHAR (16)` | L39 |
| `cSucursal` | `CHAR (4)` | L40 |
| `cUsuario` | `CHAR (8)` | L41 |
| `dFechAlt` | `DATE` | L42 |
| `dFechVal` | `DATE` | L43 |
| `dtFechHor` | `DATETIME HOUR to FRACTION(3)` | L44 |
| `cTransacc` | `CHAR (4)` | L45 |
| `cSucCuen` | `CHAR (4)` | L46 |
| `cProducto` | `CHAR (4)` | L47 |
| `cEmpresa` | `CHAR (3)` | L48 |
| `cCuenta` | `CHAR (20)` | L49 |
| `cCausaDev` | `CHAR (2)` | L50 |
| `iNumCheq` | `INTEGER` | L51 |
| `mMontoTot` | `MONEY (18,2)` | L52 |
| `mFirme` | `MONEY (18,2)` | L53 |
| `mEnSbc` | `MONEY (18,2)` | L54 |
| `mRemes` | `MONEY (18,2)` | L55 |
| `sDiasRet` | `SMALLINT` | L56 |
| `cCancelad` | `CHAR (1)` | L57 |
| `cEdoCta` | `CHAR (1)` | L58 |
| *…10 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_cargochqsinaltainv` | `bdinvers` | ⚠️ sí | SELECT | L135 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L128 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L116 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechaInicio IS NULL  OR pFechaFin IS NULL OR pRegistros IS ` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L138 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L145 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `apertura` | ENTIDAD | apertura (de cuenta/crédito) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conscargosaperturacheques_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscargosaperturacheques_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cargo, apertura y cheques (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscargosaperturacheques_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_cargochqsinaltainv` | `bdinvers` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pFechaInicio IS NULL OR pFechaFin IS NULL   THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `apertura` | ENTIDAD | apertura (de cuenta/crédito) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conscargosreversadoscheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscargosreversadoscheques.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cargo, OS — Originación de Solicitudes y cheques" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscargosreversadoscheques(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `dFecha` | `DATE` | L10 |
| `cCuenta` | `CHAR (20)` | L11 |
| `cSucursal` | `CHAR (4)` | L12 |
| `mMontoTot` | `MONEY (18,2)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_cargoreverinv` | `bdinvers` | ⚠️ sí | SELECT | L58 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conscargosreversadoscheques_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscargosreversadoscheques_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cargo, OS — Originación de Solicitudes y cheques (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscargosreversadoscheques_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_cargoreverinv` | `bdinvers` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conscatalogostatuslote`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscatalogostatuslote.sql` |
| **LOC (1er CREATE)** | 49 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, estatus y lote" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscatalogostatuslote(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cStatus` | `CHAR(1)` | L8 |
| `cDescripcion` | `CHAR(15)` | L9 |
| `iSqlErr` | `INT` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_statuslotes` | `bdicnweb` | no | SELECT | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `lote` | ENTIDAD | lote (proceso batch) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_conscattiporeporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscattiporeporte.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 352 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo y reporte (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscattiporeporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cDescCodRetSp` | `CHAR(80)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iExiste` | `INTEGER` | L12 |
| `cIdRegistro` | `CHAR(1)` | L13 |
| `cDescripcion` | `CHAR(100)` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cattiporeporteconciliacion` | `bdicnweb` | no | SELECT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_conscattiporeporteconciliacionapertura`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscattiporeporteconciliacionapertura.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 348 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, reporte y apertura (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscattiporeporteconciliacionapertura(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cDescCodRetSp` | `CHAR(80)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iExiste` | `INTEGER` | L12 |
| `cIdReporte` | `CHAR(1)` | L13 |
| `cDescripcion` | `CHAR(100)` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cattiporeporteconciliacionapertura` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `apertura` | ENTIDAD | apertura (de cuenta/crédito) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_conscausaimpresionedocta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscausaimpresionedocta.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta causa, Impago — pago vencido o fallido; confirmado: n_impagos_consec y Estado de Cuenta — documento periódico de movimientos y saldos; generado como PDF" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscausaimpresionedocta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistemaCuenta               CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIdMotivo` | `INTEGER` | L8 |
| `cDescMotivo` | `CHAR(150)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `kw_cat_motivos_impresion_cfdi` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00077';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `causa` | ENTIDAD | causa / motivo | 🔵 CONVENCIÓN | nombre_sp |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?resion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edocta` | ENTIDAD | Estado de Cuenta — documento periódico de movimientos y sald | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?resion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conscedulasccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscedulasccl.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cédulas módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultacedulas2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscedulasccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCcl                    DATE
  pTipo                        SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCcl` | `DATE` | `ccl`=módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (bdicnweb:sp_ccl_*) | 🟡 INFERIDO |
| `pTipo` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cNombre` | `CHAR(40)` | L15 |
| `cCtaContable` | `CHAR(14)` | L16 |
| `dSdoCheques` | `DECIMAL(16,2)` | L17 |
| `dSdoContab` | `DECIMAL(16,2)` | L18 |
| `dDifSaldos` | `DECIMAL(16,2)` | L19 |
| `cObservaciones` | `CHAR(255)` | L20 |
| `cEditable` | `CHAR(1)` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |
| `iRegistros` | `INTEGER` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L63 |
| `sp_consultacedulas2` | `bdicheq` | ⚠️ sí | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaCcl = '' OR pTipo IS NULL OR pRegistros IS NULL OR pRec` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consultacedulas2 ";` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L81 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `conscedulas` | ACCION | consulta cédulas | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_conscedulasccl_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscedulasccl_totales.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cédulas módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultacedulas2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscedulasccl_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCcl                    DATE
  pTipo                        SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCcl` | `DATE` | `ccl`=módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (bdicnweb:sp_ccl_*) | 🟡 INFERIDO |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_consultacedulas2_totales` | `bdicheq` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaCcl = '' OR pTipo IS NULL  THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditarjeta:sp_consultacedulas2_` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `conscedulas` | ACCION | consulta cédulas | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_conscedulasusuariosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscedulasusuariosccl.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 402 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cédulas usuarios y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscedulasusuariosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cUsuario` | `CHAR (8)` | L15 |
| `cNombre` | `CHAR (104)` | L16 |
| `cFuncion` | `CHAR (8)` | L17 |
| `cConcepto` | `CHAR (10)` | L18 |
| `cEstatus` | `CHAR (10)` | L19 |
| `cNombreCedula` | `CHAR(40)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontablenombre2` | `bdicheq` | ⚠️ sí | SELECT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `bdicnweb` | `bdicnweb` | no | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_usuarioscedulascons ";` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00582';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L74 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `conscedulas` | ACCION | consulta cédulas | 🔵 CONVENCIÓN | nombre_sp |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consclientescoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consclientescoppel.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 222 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clientes y Coppel" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consclientescoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteBanco                    CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteBanco` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRet` | `INTEGER` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iNoRegsEncontrados` | `INTEGER` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |
| `cNumCteCoppel` | `CHAR(20)` | L16 |
| `cNomCteCoppel` | `CHAR(107)` | L17 |
| `dFecha_nac` | `DATE` | L18 |
| `dFecha_consulta` | `DATE` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_det_ctescoppel` | `bdicnweb` | no | SELECT | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L71 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `clientes` | ENTIDAD | clientes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consclientescoppel_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consclientescoppel_totales.sql` |
| **LOC (1er CREATE)** | 197 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 223 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clientes y Coppel (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consclientescoppel_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteBanco                    CHAR(20)
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteBanco` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRet` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegsEncontrados` | `INTEGER` | L9 |
| `iRegistros` | `INTEGER` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `cTicket` | `CHAR(20)` | L13 |
| `cCteCoppel` | `CHAR(20)` | L14 |
| `cCteBanco` | `CHAR(20)` | L15 |
| `cNombre1` | `CHAR(26)` | L16 |
| `cNombre2` | `CHAR(26)` | L17 |
| `cApell_paterno` | `CHAR(26)` | L18 |
| `cApell_materno` | `CHAR(26)` | L19 |
| `dFecha_nac` | `DATE` | L20 |
| `dFecha` | `DATE` | L21 |
| `cNomCteCoppel` | `CHAR(107)` | L22 |
| `iIdRegistro` | `INTEGER` | L24 |
| `bEnTransaccion` | `BOOLEAN` | L25 |
| `iContador` | `INTEGER` | L26 |
| `iMaxCommit` | `INTEGER` | L27 |
| `iNoRegistros` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusctescoppel` | `bdicnweb` | no | SELECT | L80 |
| `statusctescoppel` | `bdicnweb` | no | DELETE | L80 |
| `statusctescoppel` | `bdicnweb` | no | INSERT | L81 |
| `sw_det_ctescoppel` | `bdicnweb` | no | SELECT | L104 |
| `sw_det_ctescoppel` | `bdicnweb` | no | DELETE | L106 |
| `si_huella_linea` | `bdinteg` | ⚠️ sí | SELECT | L127 |
| `si_huella_linea_resultado` | `bdinteg` | ⚠️ sí | SELECT | L133 |
| `sw_det_ctescoppel` | `bdicnweb` | no | INSERT | L156 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L108 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L155 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L159 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `clientes` | ENTIDAD | clientes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_conscodigosufijopm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conscodigosufijopm.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta códigos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_conscodigosufijopm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `codSufijo` | `CHAR(2)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sufijos` | `bdinteg` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `codigos` | ENTIDAD | códigos | 🔵 CONVENCIÓN | nombre_sp |
| `?ufijopm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ufijopm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consconciliacionadminccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consconciliacionadminccl.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `intercard` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consconciliacionadminccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivoOrigen               CHAR(3)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivoOrigen` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L33 |
| `iSqlErr` | `INTEGER` | L34 |
| `cCodRetSp` | `CHAR(5)` | L35 |
| `iCodRetSp` | `INTEGER` | L36 |
| `cArchivoOrigen` | `CHAR(3)` | L37 |
| `cNomArchivo325` | `CHAR(23)` | L38 |
| `cNomArchivoCom` | `CHAR(23)` | L39 |
| `dFechaRegistro` | `DATE` | L40 |
| `dFecha` | `DATE` | L41 |
| `cProdTarjeta` | `CHAR(4)` | L42 |
| `cTarjeta` | `CHAR(16)` | L43 |
| `cCuenta` | `CHAR(12)` | L44 |
| `cTipoMov` | `CHAR(1)` | L45 |
| `cTranCentral` | `CHAR(4)` | L46 |
| `cFolio325` | `CHAR(15)` | L47 |
| `mMonto325` | `MONEY(18,2)` | L48 |
| `cEstatus` | `CHAR(1)` | L49 |
| `cTxnLiberacion` | `CHAR(4)` | L50 |
| `cCuentac` | `CHAR(19)` | L51 |
| `cCuentaa` | `CHAR(19)` | L52 |
| `cFolioSif` | `CHAR(15)` | L53 |
| `mMontoSif` | `MONEY(18,2)` | L54 |
| `cSecIntercard` | `CHAR(7)` | L55 |
| `mMontoIntcrd` | `MONEY(18,2)` | L56 |
| `dtFechaHoraInauth` | `DATETIME YEAR TO FRACTION(5)` | L57 |
| *…10 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L127 |
| `intercard` | `bdicnweb` | no | L135 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L69 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L115 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pArchivoOrigen = '' OR pFecha IS NULL OR pRegistros IS NULL O` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L140 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP intercard:sp_consultaconadmin2 ';` |  |
| L143 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consconciliacionadminccl_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consconciliacionadminccl_totales.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `intercard` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consconciliacionadminccl_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivoOrigen               CHAR(3)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivoOrigen` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `intercard` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pArchivoOrigen = '' OR pFecha IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP intercard:sp_consultaconadmin2_` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consctasbloqmasivocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasbloqmasivocap.sql` |
| **LOC (1er CREATE)** | 212 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasbloqmasivocap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INT` | L23 |
| `iExiste` | `INT` | L24 |
| `iIdRegistro` | `INT` | L25 |
| `cStatus` | `CHAR(1)` | L26 |
| `cCuenta` | `CHAR(20)` | L27 |
| `cResultado` | `CHAR(15)` | L28 |
| `cCodRetProc` | `CHAR(5)` | L29 |
| `cMotivoRechazo` | `CHAR(100)` | L30 |
| `cFolioBloqueo` | `CHAR(20)` | L31 |
| `mSaldo` | `MONEY(14,2)` | L32 |
| `cClaveBloqueo` | `CHAR(2)` | L33 |
| `cOpcBloqueo` | `CHAR(2)` | L34 |
| `mImporte` | `MONEY(14,2)` | L35 |
| `cAreaSolic` | `CHAR(2)` | L36 |
| `cMotivoBloq` | `CHAR(2)` | L37 |
| `dFechaBloqueo` | `DATE` | L38 |
| `cUsuario` | `CHAR(8)` | L39 |
| `cStatusCta` | `CHAR(5)` | L40 |
| `cDescStatusCta` | `CHAR(50)` | L41 |
| `cCodRetSp` | `CHAR(5)` | L42 |
| `cNumCliente` | `CHAR(20)` | L43 |
| `iRegistros` | `INT` | L44 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_bloqueocap` | `bdicnweb` | no | SELECT | L98 |
| `sw_tr_cargamasiva_bloqueocap_hist` | `bdicnweb` | no | SELECT | L102 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L138 |
| `sc_histbloq` | `bdicheq` | ⚠️ sí | SELECT | L166 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_blqvalbloqueocta` | `bdicheq` | ⚠️ sí | L154 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L135 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L153 | VALIDACIÓN_NULL | `IF cStatusCta IS NULL THEN` |  |
| L193 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consctascancelacionmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctascancelacionmasivocre.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctascancelacionmasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INT` | L16 |
| `iExiste` | `INT` | L17 |
| `iIdRegistro` | `INT` | L18 |
| `cStatus` | `CHAR(1)` | L19 |
| `cCuenta` | `CHAR(20)` | L20 |
| `cResultado` | `CHAR(15)` | L21 |
| `cCodRetProc` | `CHAR(5)` | L22 |
| `cMotivoRechazo` | `CHAR(100)` | L23 |
| `cFolioCancelacion` | `CHAR(20)` | L24 |
| `mSaldo` | `MONEY(14,2)` | L25 |
| `cClaveCancelacion` | `CHAR(3)` | L26 |
| `dFechaCancelacion` | `DATE` | L27 |
| `cNumCliente` | `CHAR(20)` | L28 |
| `iRegistros` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_cancelacioncre` | `bdicnweb` | no | SELECT | L75 |
| `sw_tr_cargamasiva_cancelacioncre_hist` | `bdicnweb` | no | SELECT | L79 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L108 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L125 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cancelacion` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_consctasdesbloqmasivocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasdesbloqmasivocap.sql` |
| **LOC (1er CREATE)** | 207 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasdesbloqmasivocap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INT` | L20 |
| `iExiste` | `INT` | L21 |
| `iIdRegistro` | `INT` | L22 |
| `cStatus` | `CHAR(1)` | L23 |
| `cCuenta` | `CHAR(20)` | L24 |
| `cResultado` | `CHAR(15)` | L25 |
| `cCodRetProc` | `CHAR(5)` | L26 |
| `cMotivoRechazo` | `CHAR(100)` | L27 |
| `cFolioBloqueo` | `CHAR(20)` | L28 |
| `mSaldo` | `MONEY(14,2)` | L29 |
| `mImporte` | `MONEY(14,2)` | L30 |
| `cAreaSolic` | `CHAR(2)` | L31 |
| `cMotivoBloq` | `CHAR(2)` | L32 |
| `dFechaBloqueo` | `DATE` | L33 |
| `cUsuario` | `CHAR(8)` | L34 |
| `cStatusCta` | `CHAR(5)` | L35 |
| `cDescStatusCta` | `CHAR(50)` | L36 |
| `cCodRetSp` | `CHAR(5)` | L37 |
| `cNumCliente` | `CHAR(20)` | L38 |
| `iRegistros` | `INT` | L39 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_desbloqueocap` | `bdicnweb` | no | SELECT | L91 |
| `sw_tr_cargamasiva_desbloqueocap_hist` | `bdicnweb` | no | SELECT | L95 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L131 |
| `sc_histbloq` | `bdicheq` | ⚠️ sí | SELECT | L152 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_blqvalbloqueocta` | `bdicheq` | ⚠️ sí | L147 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L128 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L146 | VALIDACIÓN_NULL | `IF cStatusCta IS NULL THEN` |  |
| L188 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `desb` | ACCION | desbloqueo | 🟡 INFERIDO | nombre_sp |
| `?loq` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?loq` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consctasdiferenciasaldo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasdiferenciasaldo.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas y saldo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasdiferenciasaldo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `dFecha` | `date` | L25 |
| `cCuenta` | `CHAR (20)` | L26 |
| `sSecuencia` | `SMALLINT` | L27 |
| `cProducto` | `CHAR (4)` | L28 |
| `cNumCte` | `CHAR (20)` | L29 |
| `cSucursal` | `CHAR (4)` | L30 |
| `cEjecutivo` | `CHAR (8)` | L31 |
| `mCapAnterior` | `MONEY (18,2)` | L32 |
| `mMovscargocap` | `MONEY (18,2)` | L33 |
| `mMovsabonocap` | `MONEY (18,2)` | L34 |
| `mCapCalculado` | `MONEY (18,2)` | L35 |
| `mCapActual` | `MONEY (18,2)` | L36 |
| `mDifSdos` | `MONEY (18,2)` | L37 |
| `mIntAnterior` | `MONEY (18,2)` | L38 |
| `mMovscargoint` | `MONEY (18,2)` | L39 |
| `mMovsabonoint` | `MONEY (18,2)` | L40 |
| `mIntCalculado` | `MONEY (18,2)` | L41 |
| `mIntActual` | `MONEY (18,2)` | L42 |
| `mDifInts` | `MONEY (18,2)` | L43 |
| `iNoRegistros` | `INTEGER` | L44 |
| `iRegistros` | `INTEGER` | L45 |
| `iRecuperacion` | `INTEGER` | L46 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_conciliainvdif` | `bdinvers` | ⚠️ sí | SELECT | L102 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L83 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L105 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?diferencia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `saldo` | ENTIDAD | saldo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?diferencia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consctasdiferenciasaldo_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasdiferenciasaldo_totales.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas y saldo (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasdiferenciasaldo_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_conciliainvdif` | `bdinvers` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?diferencia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `saldo` | ENTIDAD | saldo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?diferencia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consctasmayorsdocheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasmayorsdocheques.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas, mayor contable, saldo y cheques" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasmayorsdocheques(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `dFecha` | `DATE` | L13 |
| `cNumcte` | `CHAR (20)` | L14 |
| `cNombre` | `CHAR (104)` | L15 |
| `cCuenta` | `CHAR (20)` | L16 |
| `dSaldo` | `DECIMAL (16,2)` | L17 |
| `dProv` | `DECIMAL (16,2)` | L18 |
| `dSdoprov` | `DECIMAL (16,2)` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |
| `iRegistros` | `INTEGER` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_ctassdosmayores` | `bdicheq` | ⚠️ sí | SELECT | L66 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L69 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `mayor` | ENTIDAD | mayor contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consctasmayorsdocheques_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasmayorsdocheques_totales.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas, mayor contable, saldo y cheques (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasmayorsdocheques_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_ctassdosmayores` | `bdicheq` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pFechaInicio IS NULL OR pFechaFin IS NULL  THEN` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `mayor` | ENTIDAD | mayor contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consctasmayorsdoinversiones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasmayorsdoinversiones.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas, mayor contable, saldo y inversión" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasmayorsdoinversiones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `iRegistros` | `INTEGER` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |
| `dFecha` | `DATE` | L18 |
| `cNumcte` | `CHAR (20)` | L19 |
| `cNombre` | `CHAR (104)` | L20 |
| `dFechaAlta` | `DATE` | L21 |
| `dFechaVenc` | `DATE` | L22 |
| `cCuenta` | `CHAR (20)` | L23 |
| `dSaldo` | `DECIMAL (18,2)` | L24 |
| `dProv` | `DECIMAL (14,2)` | L25 |
| `dSdoprov` | `DECIMAL (18,2)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_pagaresdosmayores` | `bdinvers` | ⚠️ sí | SELECT | L73 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS N` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L76 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `mayor` | ENTIDAD | mayor contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `inversion` | ENTIDAD | inversión (pagaré / plazo) | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consctasmayorsdoinversiones_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasmayorsdoinversiones_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas, mayor contable, saldo y inversión (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasmayorsdoinversiones_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_pagaresdosmayores` | `bdinvers` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `mayor` | ENTIDAD | mayor contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `inversion` | ENTIDAD | inversión (pagaré / plazo) | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consctasretirosmayores`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasretirosmayores.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas, retiros y mayor contable" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasretirosmayores(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `dFecha` | `DATE` | L11 |
| `cCuenta` | `CHAR (20)` | L12 |
| `cSucursal` | `CHAR (4)` | L13 |
| `dHora` | `DATETIME HOUR to FRACTION(3)` | L14 |
| `dMonto` | `DECIMAL ( 14,2)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_retirosmayores` | `bdicheq` | ⚠️ sí | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L64 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `retiros` | ENTIDAD | retiros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mayor` | ENTIDAD | mayor contable | 🟡 INFERIDO | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consctasretirosmayores_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctasretirosmayores_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas, retiros y mayor contable (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctasretirosmayores_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_retirosmayores` | `bdicheq` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL   THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `retiros` | ENTIDAD | retiros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mayor` | ENTIDAD | mayor contable | 🟡 INFERIDO | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consctastransfersoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctastransfersoc.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuentas, transferencia y Sistema Operativo Central" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctastransfersoc(
  pNumCte                      CHAR(20)
) RETURNING CHAR(5), CHAR(20), CHAR(16), CHAR(40), CHAR(18), CHAR(10), CHAR(12), CHAR(10), CHAR(4), CHAR(8), CHAR(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cCuenta` | `CHAR(104)` | L11 |
| `cTarjeta` | `CHAR(16)` | L12 |
| `cProducto` | `CHAR(40)` | L13 |
| `cCtaClabe` | `CHAR(18)` | L14 |
| `cFechaAlta` | `CHAR(10)` | L15 |
| `cStatusTar` | `CHAR(10)` | L16 |
| `cStatusCta` | `CHAR(12)` | L17 |
| `cSucursal` | `CHAR(40)` | L18 |
| `cEjecutivo` | `CHAR(8)` | L19 |
| `cFechaCanc` | `CHAR(10)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L78 | VALIDACIÓN_NULL | `IF ( cCuenta is null OR cCuenta = '' ) OR` |  |
| L97 | VALIDACIÓN_NULL | `IF cTarjeta   is null THEN LET cTarjeta   = ''; END IF;` |  |
| L98 | VALIDACIÓN_NULL | `IF cProducto  is null THEN LET cProducto  = ''; END IF;` |  |
| L99 | VALIDACIÓN_NULL | `IF cCtaClabe  is null THEN LET cCtaClabe  = ''; END IF;` |  |
| L100 | VALIDACIÓN_NULL | `IF cFechaAlta is null THEN LET cFechaAlta = ''; END IF;` |  |
| L101 | VALIDACIÓN_NULL | `IF cStatusTar is null THEN LET cStatusTar = ''; END IF;` |  |
| L102 | VALIDACIÓN_NULL | `IF cEjecutivo is null THEN LET cEjecutivo = ''; END IF;` |  |
| L103 | VALIDACIÓN_NULL | `IF cFechaCanc is null THEN LET cFechaCanc = ''; END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | 🔵 CONVENCIÓN | nombre_sp |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consctecuenta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctecuenta.sql` |
| **LOC (1er CREATE)** | 161 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cliente y cuenta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_cnsif_consprodcte` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctecuenta(
  pIdUsuario                   char(8)
  pIdFuncion                   char(10)
  pNumCliente                  char(20)
  pNumCuenta                   char(20)
  pSistemaCuenta               char(2)
) RETURNING CHAR(5)     AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pNumCuenta` | `char(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pSistemaCuenta` | `char(2)` | `cuenta`=cuenta | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iexiste` | `INT` | L21 |
| `cCodRet` | `CHAR(5)` | L22 |
| `cCodRetSp` | `CHAR(5)` | L23 |
| `iSql_err` | `INT` | L24 |
| `cIProducto_chequera` | `CHAR(1)` | L26 |
| `cScuenta` | `CHAR(2)` | L27 |
| `cNo_cuenta` | `CHAR(20)` | L28 |
| `cNo_tarjeta` | `CHAR(20)` | L29 |
| `cClave_producto` | `CHAR(4)` | L30 |
| `cNombre_producto` | `CHAR(40)` | L31 |
| `cCuenta_clabe` | `CHAR(18)` | L32 |
| `dFecha_apertura` | `DATE` | L33 |
| `cStatus_tarjeta` | `CHAR(15)` | L34 |
| `cStatus_cuenta` | `CHAR(60)` | L35 |
| `dFecha_status` | `DATE` | L36 |
| `cClave_sucursal` | `CHAR(4)` | L37 |
| `cEjecutivo` | `CHAR(8)` | L38 |
| `mSaldo_actual` | `MONEY(14,2)` | L39 |
| `dFecha_aperturaO_inv` | `DATE` | L40 |
| `dFecha_max` | `DATE` | L41 |
| `dFecha_min` | `DATE` | L42 |
| `cNumero_cuenta` | `CHAR(20)` | L43 |
| `dFecha` | `DATE` | L44 |
| `iCont` | `INTEGER` | L45 |
| `iMaxSec` | `INTEGER` | L46 |
| *…7 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L110 |
| `sp_cnsif_consprodcte` | `bdinteg` | ⚠️ sí | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L150 | FÓRMULA | `LET iRegistros = iRegistros + iRecuperacion;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_consctetransfersoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consctetransfersoc.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cliente, transferencia y Sistema Operativo Central" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consctetransfersoc(
  pNumCte                      CHAR(20)
) RETURNING CHAR(5), CHAR(104), CHAR(13), CHAR(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | `cte`=cliente | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cNombreCte` | `CHAR(104)` | L11 |
| `cRFC` | `CHAR(13)` | L12 |
| `cTpoCte` | `CHAR(10)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L47 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L64 | VALIDACIÓN_NULL | `IF cNombreCte is null OR cNombreCte = '' THEN` |  |
| L68 | VALIDACIÓN_NULL | `IF cRFC is null OR cRFC = '' THEN` |  |
| L72 | VALIDACIÓN_NULL | `IF cTpoCte is null OR cTpoCte = '' THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | 🔵 CONVENCIÓN | nombre_sp |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consdatosrepositorioccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdatosrepositorioccl.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta datos, OS — Originación de Solicitudes y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdatosrepositorioccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cArchivoOrigen` | `CHAR(3)` | L12 |
| `cDescripcion` | `CHAR(50)` | L13 |
| `cRepWin` | `CHAR(80)` | L14 |
| `cRepAix` | `CHAR(80)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `bditarjeta` | `bdicnweb` | no | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_consultareposito` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?itorio` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?itorio` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdepositomasivocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdepositomasivocap.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta depósito, OS — Originación de Solicitudes y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdepositomasivocap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INT` | L19 |
| `iExiste` | `INT` | L20 |
| `iIdRegistro` | `INT` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cResultado` | `CHAR(15)` | L23 |
| `cCodRetSp` | `CHAR(5)` | L24 |
| `cMotivoRechazo` | `CHAR(100)` | L25 |
| `cFolio` | `CHAR(20)` | L26 |
| `mSaldo` | `money(14,2)` | L27 |
| `cTransacc` | `CHAR(4)` | L28 |
| `cDoctoChq` | `INT` | L29 |
| `mDeposito` | `money(14,2)` | L30 |
| `cReferencia` | `CHAR(255)` | L31 |
| `cStatus` | `CHAR(1)` | L32 |
| `iRegistros` | `int` | L33 |
| `cNumCliente` | `char(20)` | L34 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_deposito` | `bdicnweb` | no | SELECT | L76 |
| `sw_tr_cargamasiva_deposito_hist` | `bdicnweb` | no | SELECT | L80 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L107 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L127 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ito` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ito` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdesbloqueomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdesbloqueomasivocre.sql` |
| **LOC (1er CREATE)** | 189 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito (masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdesbloqueomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `iIdRegistro` | `INTEGER` | L21 |
| `cNoCuenta` | `CHAR(20)` | L22 |
| `cNoCliente` | `CHAR(20)` | L23 |
| `cResultado` | `CHAR(15)` | L24 |
| `cCodRetSp` | `CHAR(6)` | L25 |
| `cMotivoRechazo` | `CHAR(80)` | L26 |
| `mSaldo` | `MONEY(14,2)` | L27 |
| `cNombreCliente` | `CHAR(107)` | L28 |
| `dFechaDesbloqueo` | `DATE` | L29 |
| `cEmpleado` | `CHAR(8)` | L30 |
| `cNombreEmpleado` | `CHAR(45)` | L31 |
| `cStatusRegistro` | `CHAR(1)` | L32 |
| `iExiste` | `INTEGER` | L33 |
| `iNoRegistros` | `INTEGER` | L34 |
| `cAreaSolicitante` | `CHAR(150)` | L35 |
| `cJustificacion` | `CHAR(150)` | L36 |
| `dSaldoCapital` | `DECIMAL(18,2)` | L37 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_desbloqueocre` | `bdicnweb` | no | SELECT | L95 |
| `sw_tr_cargamasiva_desbloqueocre_hist` | `bdicnweb` | no | SELECT | L99 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L137 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L153 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L158 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL ` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L134 | VALIDACIÓN_NULL | `IF cNoCliente IS NULL OR cNoCliente = '' THEN` |  |
| L164 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `desbloqueo` | ACCION | desbloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_consdesbloqueomasivocre_exp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdesbloqueomasivocre_exp.sql` |
| **LOC (1er CREATE)** | 190 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 77 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito (masivo, sufijo Exportar — SP genera/exporta archivo de salida)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdesbloqueomasivocre_exp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `iIdRegistro` | `INTEGER` | L21 |
| `cNoCuenta` | `CHAR(20)` | L22 |
| `cNoCliente` | `CHAR(20)` | L23 |
| `cResultado` | `CHAR(15)` | L24 |
| `cCodRetSp` | `CHAR(6)` | L25 |
| `cMotivoRechazo` | `CHAR(80)` | L26 |
| `mSaldo` | `MONEY(14,2)` | L27 |
| `cNombreCliente` | `CHAR(107)` | L28 |
| `dFechaDesbloqueo` | `DATE` | L29 |
| `cEmpleado` | `CHAR(8)` | L30 |
| `cNombreEmpleado` | `CHAR(45)` | L31 |
| `cStatusRegistro` | `CHAR(1)` | L32 |
| `iExiste` | `INTEGER` | L33 |
| `iNoRegistros` | `INTEGER` | L34 |
| `cAreaSolicitante` | `CHAR(150)` | L35 |
| `cJustificacion` | `CHAR(150)` | L36 |
| `dSaldoCapital` | `DECIMAL(18,2)` | L37 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_desbloqueocre` | `bdicnweb` | no | SELECT | L94 |
| `sw_tr_cargamasiva_desbloqueocre_hist` | `bdicnweb` | no | SELECT | L98 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L138 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L154 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L159 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L70 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL ` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L135 | VALIDACIÓN_NULL | `IF cNoCliente IS NULL OR cNoCliente = '' THEN` |  |
| L165 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `desbloqueo` | ACCION | desbloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `exp` | MODIF | sufijo Exportar — SP genera/exporta archivo de salida (sp_*_ | 🟡 INFERIDO | nombre_sp |

---

## `sp_consdetalleaplicacioncargoscta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetalleaplicacioncargoscta.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=21 / fan_out=5 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, cargo y cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetalleaplicacioncargoscta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cProceso` | `CHAR(20)` | L13 |
| `cStatusProc` | `CHAR(1)` | L14 |
| `cEjecutivo` | `CHAR(8)` | L15 |
| `dHoraIni` | `DATETIME HOUR TO SECOND` | L16 |
| `dHoraFin` | `DATETIME HOUR TO SECOND` | L17 |
| `cCodretSp` | `CHAR(5)` | L18 |
| `cStatus` | `CHAR(10)` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_contproc` | `bditef` | ⚠️ sí | SELECT | L58 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL THEN` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallearchcecoban`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallearchcecoban.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=9 / fan_out=3 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, archivo y cob — cobranza" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallearchcecoban(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pDireccionMac                CHAR(12)
  pFechaHoy                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `arch`=archivo | 🟡 INFERIDO |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pFechaHoy` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cNombreArchivo` | `CHAR(22)` | L14 |
| `cCodOperacion` | `CHAR(2)` | L15 |
| `cNumCuenta` | `CHAR(22)` | L16 |
| `iNumCheque` | `INTEGER` | L17 |
| `cMotivoDev` | `CHAR(50)` | L18 |
| `cImporte` | `CHAR(15)` | L19 |
| `mImporte` | `MONEY(14,2)` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_procesadetalle_tmp` | `bdicnweb` | no | SELECT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pDireccionMac = '' OR	pFechaHoy IS NUL` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | FÓRMULA | `LET mImporte = SUBSTR(cImporte,1,(LENGTH(cImporte)-2))\|\|"."\|\|SUBSTR(cImporte,(LENGTH(cImporte)-1` | 🔴 MONEY/aritmética financiera |
| L77 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '10001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `?ce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cob` | ENTIDAD | cob — cobranza (abreviación de dominio — sp_repcob_*, sp_obt | 🔵 CONVENCIÓN | nombre_sp |
| `?an` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ce`, `?an` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallearchimgcecoban`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallearchimgcecoban.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=9 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, archivo y cob — cobranza" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallearchimgcecoban(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pDireccionMac                CHAR(12)
  pFechaHoy                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `arch`=archivo | 🟡 INFERIDO |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pFechaHoy` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cNombreArchivo` | `CHAR(22)` | L15 |
| `cNumCuenta` | `CHAR(22)` | L16 |
| `iNumCheque` | `INTEGER` | L17 |
| `cImporte` | `CHAR(15)` | L18 |
| `mImporte` | `MONEY(14,2)` | L19 |
| `cStatusCargaImgF` | `CHAR(1)` | L20 |
| `cStatusCargaImgT` | `CHAR(1)` | L21 |
| `iIdDetalle` | `INTEGER` | L22 |
| `iRecuperacion` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | SELECT | L74 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pDireccionMac = '' OR	pFechaHoy IS NUL` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L78 | FÓRMULA | `LET mImporte = SUBSTR(cImporte,1,(LENGTH(cImporte)-2))\|\|"."\|\|SUBSTR(cImporte,(LENGTH(cImporte)-1` | 🔴 MONEY/aritmética financiera |
| L80 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '10001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `?imgce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cob` | ENTIDAD | cob — cobranza (abreviación de dominio — sp_repcob_*, sp_obt | 🔵 CONVENCIÓN | nombre_sp |
| `?an` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imgce`, `?an` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallearchimgcecoban_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallearchimgcecoban_totales.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=9 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, archivo y cob — cobranza (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallearchimgcecoban_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pDireccionMac                CHAR(12)
  pFechaHoy                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `arch`=archivo | 🟡 INFERIDO |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pFechaHoy` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pDireccionMac = '' OR	pFechaHoy IS NUL` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `?imgce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cob` | ENTIDAD | cob — cobranza (abreviación de dominio — sp_repcob_*, sp_obt | 🔵 CONVENCIÓN | nombre_sp |
| `?an_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imgce`, `?an_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallechequespagados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallechequespagados.sql` |
| **LOC (1er CREATE)** | 152 |
| **Callgraph** | ✅ fan_in=15 / fan_out=6 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, cheques y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallechequespagados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumero                      CHAR(20)
  pFecha                       CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNumero` | `CHAR(20)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `cEmpresa` | `CHAR(3)` | L21 |
| `cNumCte` | `CHAR(20)` | L22 |
| `cNombreCte` | `CHAR(107)` | L23 |
| `cRfc` | `CHAR(13)` | L24 |
| `dFechaNac` | `DATE` | L25 |
| `cBanco` | `CHAR(44)` | L26 |
| `cNoCuenta` | `CHAR(20)` | L27 |
| `iNoCheque` | `INTEGER` | L28 |
| `mImporte` | `MONEY(16,2)` | L29 |
| `cTruncamiento` | `CHAR(1)` | L30 |
| `iSecuencia` | `INTEGER` | L31 |
| `cNombreArchivo` | `CHAR(22)` | L32 |
| `cTruncado` | `CHAR(2)` | L33 |
| `cEstatus` | `CHAR(19)` | L34 |
| `cCausaDev` | `CHAR(38)` | L35 |
| `cImporte2` | `CHAR(18)` | L36 |
| `iIdConsCheque` | `INTEGER` | L37 |
| `iRecuperacion` | `INTEGER` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ccer_aplicaabonocheques` | `bdicnweb` | no | SELECT | L98 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L70 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pNumero = '' OR pFecha = '' OR pRegistros` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L102 | FÓRMULA | `LET cImporte2 = SUBSTR(cImporte2, CHARINDEX('$', cImporte2) + 1);` | 🔴 MONEY/aritmética financiera |
| L104 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L122 | FÓRMULA | `LET cImporte2 = SUBSTR(cImporte2, CHARINDEX('$', cImporte2) + 1);` | 🔴 MONEY/aritmética financiera |
| L124 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pagad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pagad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallechequespagados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallechequespagados_totales.sql` |
| **LOC (1er CREATE)** | 195 |
| **Callgraph** | ✅ fan_in=15 / fan_out=6 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, cheques y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallechequespagados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumero                      CHAR(20)
  pFecha                       CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNumero` | `CHAR(20)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cNumCte` | `CHAR(20)` | L8 |
| `cNombreCte` | `CHAR(107)` | L9 |
| `cRfc` | `CHAR(13)` | L10 |
| `dFechaNac` | `DATE` | L11 |
| `cBancoPresenta` | `CHAR(3)` | L12 |
| `cDescBanco` | `CHAR(40)` | L13 |
| `cNoCuenta` | `CHAR(20)` | L14 |
| `iNoCheque` | `INTEGER` | L15 |
| `mImporte` | `MONEY(16,2)` | L16 |
| `cTruncamiento` | `CHAR(1)` | L17 |
| `iSecuencia` | `INTEGER` | L18 |
| `cNombreArchivo` | `CHAR(22)` | L19 |
| `cTruncado` | `CHAR(2)` | L20 |
| `cEstatus` | `CHAR(19)` | L21 |
| `cCausaDev` | `CHAR(38)` | L22 |
| `iIdConsCheque` | `INTEGER` | L23 |
| `iNumRegistros` | `INTEGER` | L24 |
| `cIdMotivoDev` | `CHAR(2)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L80 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L89 |
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L99 |
| `si_coddevcam` | `bdinteg` | ⚠️ sí | SELECT | L112 |
| `sw_ccer_aplicaabonocheques` | `bdicnweb` | no | INSERT | L120 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L67 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L117 | FÓRMULA | `LET iIdConsCheque = iIdConsCheque + 1;` |  |
| L168 | FÓRMULA | `LET iIdConsCheque = iIdConsCheque + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pagad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pagad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallechequespagadossuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallechequespagadossuc.sql` |
| **LOC (1er CREATE)** | 178 |
| **Callgraph** | ✅ fan_in=13 / fan_out=12 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, cheques y OS — Originación de Solicitudes (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallechequespagadossuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cCodRetSp` | `CHAR(6)` | L18 |
| `cDesCodRetSp` | `CHAR(50)` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `cCveBanco` | `CHAR(3)` | L21 |
| `cSucursal` | `CHAR(4)` | L22 |
| `cNombreSuc` | `CHAR(40)` | L23 |
| `cCuenta` | `CHAR(20)` | L24 |
| `cNumCheque` | `CHAR(5)` | L25 |
| `mMonto` | `MONEY(16,2)` | L26 |
| `cFechaHora` | `CHAR(60)` | L27 |
| `cFolioSuc` | `CHAR(16)` | L28 |
| `cTransacc` | `CHAR(4)` | L29 |
| `cCliente` | `CHAR(20)` | L30 |
| `sSecuencia` | `SMALLINT` | L31 |
| `dFechaHora` | `CHAR(20)` | L32 |
| `cFormatoImg` | `CHAR(3)` | L33 |
| `cBanco` | `CHAR(3)` | L34 |
| `cDescripcion` | `CHAR(40)` | L35 |
| `cNumcta` | `CHAR(20)` | L36 |
| `cNumchq` | `CHAR(7)` | L37 |
| `cLado` | `CHAR(1)` | L38 |
| `dFechaAlta` | `DATE` | L39 |
| `dFechaPresenta` | `DATE` | L40 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L107 |
| `cce_cheques_img` | `bditef` | ⚠️ sí | SELECT | L140 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L102 |
| `sp_obtienechqpag2` | `bdicntchq` | ⚠️ sí | L113 |
| `sp_validaimagencheque` | `bditef` | ⚠️ sí | L129 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L90 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio = '' OR pFechaFin = '' OR pRegistros IS NULL OR ` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L117 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bdicntchq:sp_obtienechqpag2';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L126 | FÓRMULA | `LET dFechaHora = SUBSTR(cFechaHora,4,2)\|\|'/'\|\|SUBSTR(cFechaHora,1,2)\|\|'/'\|\|SUBSTR(cFechaHora` |  |
| L154 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?pagad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pagad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallechequespagadossuc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallechequespagadossuc_totales.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=13 / fan_out=13 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, cheques y OS — Originación de Solicitudes (sucursal, totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienechqpag2_totales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallechequespagadossuc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(50)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_obtienechqpag2_totales` | `bdicntchq` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicntchq:sp_obtienechqpag2_tot` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pagad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pagad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallechequesprocnocturno`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallechequesprocnocturno.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=21 / fan_out=5 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, cheques y proceso" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallechequesprocnocturno(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCuenta` | `CHAR(20)` | L12 |
| `iCheque` | `INTEGER` | L13 |
| `cProceso` | `CHAR(20)` | L14 |
| `dHora` | `DATETIME HOUR TO SECOND` | L15 |
| `cCodretSp` | `CHAR(5)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_bitacora` | `bditef` | ⚠️ sí | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L63 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proc` | ENTIDAD | proceso | 🟡 INFERIDO | nombre_sp |
| `?nocturno` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nocturno` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallechequesprocnocturno_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallechequesprocnocturno_totales.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=21 / fan_out=5 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, cheques y proceso (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallechequesprocnocturno_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_bitacora` | `bditef` | ⚠️ sí | SELECT | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL THEN` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proc` | ENTIDAD | proceso | 🟡 INFERIDO | nombre_sp |
| `?nocturno_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nocturno_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallefacturacionos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallefacturacionos.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 87 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, facturación y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallefacturacionos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pNumCte                      CHAR(9)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pTipoFecha                   SMALLINT
  pTipoConsulta                SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pTipoFecha` | `SMALLINT` | — | — |
| `pTipoConsulta` | `SMALLINT` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | ✅ CÓDIGO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `cCodRetSp` | `CHAR(6)` | L25 |
| `cDesCodRetSp` | `CHAR(50)` | L26 |
| `cEmpresa` | `CHAR(3)` | L27 |
| `cSucursal` | `CHAR (4)` | L28 |
| `iTotalEnviadas` | `INTEGER` | L29 |
| `iImpresas` | `INTEGER` | L30 |
| `dImpresasPor` | `DECIMAL(5,2)` | L31 |
| `iNoImpresas` | `INTEGER` | L32 |
| `dNoImpresasPor` | `DECIMAL(5,2)` | L33 |
| `iStatusA` | `INTEGER` | L34 |
| `dStatusAPor` | `DECIMAL(5,2)` | L35 |
| `iStatusR` | `INTEGER` | L36 |
| `dStatusRPor` | `DECIMAL(5,2)` | L37 |
| `iStatusD` | `INTEGER` | L38 |
| `dStatusDPor` | `DECIMAL(5,2)` | L39 |
| `iStatusS` | `INTEGER` | L40 |
| `dStatusSPor` | `DECIMAL(5,2)` | L41 |
| `iBancoppel` | `INTEGER` | L42 |
| `iCoppel` | `INTEGER` | L43 |
| `iMixta` | `INTEGER` | L44 |
| `iTotal` | `INTEGER` | L45 |
| `iRecuperacion` | `INTEGER` | L46 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_facturacion_os` | `bdicnweb` | no | SELECT | L113 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet 		  = '00000';` |  |
| L84 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio = '' OR pFechaFin = '' OR pTipoFecha IS NULL OR ` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L116 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `facturacion` | ENTIDAD | facturación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consdetallefacturacionos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallefacturacionos_totales.sql` |
| **LOC (1er CREATE)** | 162 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, facturación y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallefacturacionos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pNumCte                      CHAR(9)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pTipoFecha                   SMALLINT
  pTipoConsulta                SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pTipoFecha` | `SMALLINT` | — | — |
| `pTipoConsulta` | `SMALLINT` | `cons`=consulta | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDesCodRetSp` | `CHAR(50)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cSucursal` | `CHAR (4)` | L11 |
| `iTotalEnviadas` | `INTEGER` | L12 |
| `iImpresas` | `INTEGER` | L13 |
| `dImpresasPor` | `DECIMAL(5,2)` | L14 |
| `iNoImpresas` | `INTEGER` | L15 |
| `dNoImpresasPor` | `DECIMAL(5,2)` | L16 |
| `iStatusA` | `INTEGER` | L17 |
| `dStatusAPor` | `DECIMAL(5,2)` | L18 |
| `iStatusR` | `INTEGER` | L19 |
| `dStatusRPor` | `DECIMAL(5,2)` | L20 |
| `iStatusD` | `INTEGER` | L21 |
| `dStatusDPor` | `DECIMAL(5,2)` | L22 |
| `iStatusS` | `INTEGER` | L23 |
| `dStatusSPor` | `DECIMAL(5,2)` | L24 |
| `iBancoppel` | `INTEGER` | L25 |
| `iCoppel` | `INTEGER` | L26 |
| `iMixta` | `INTEGER` | L27 |
| `iTotal` | `INTEGER` | L28 |
| `iNumRegistros` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusproceso_os` | `bdicnweb` | no | SELECT | L72 |
| `sw_statusproceso_os` | `bdicnweb` | no | DELETE | L72 |
| `sw_statusproceso_os` | `bdicnweb` | no | INSERT | L76 |
| `sw_facturacion_os` | `bdicnweb` | no | SELECT | L96 |
| `sw_facturacion_os` | `bdicnweb` | no | DELETE | L96 |
| `sw_facturacion_os` | `bdicnweb` | no | INSERT | L131 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L87 |
| `sp_consultarfacturacionos2` | `bdisolic` | ⚠️ sí | L102 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet 		  = '00000';` |  |
| L79 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio = '' OR pFechaFin = '' OR pTipoFecha IS NULL OR ` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_consultarfacturacio` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L130 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L143 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `facturacion` | ENTIDAD | facturación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consdetallemovchequespropios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallemovchequespropios.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=11 / fan_out=14 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, movimiento, cheques, ro — Rol Operativo y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallemovchequespropios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(6)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `dFechaHoy` | `DATE` | L17 |
| `cSucursal` | `CHAR(4)` | L18 |
| `cNombreSuc` | `CHAR(40)` | L19 |
| `cCuenta` | `CHAR(20)` | L20 |
| `iNumCheque` | `INTEGER` | L21 |
| `mMonto` | `MONEY(16,2)` | L22 |
| `cFechaAlta` | `CHAR(10)` | L23 |
| `cHoraAlta` | `CHAR(10)` | L24 |
| `cFolioSuc` | `CHAR(16)` | L25 |
| `cTransaccion` | `CHAR(4)` | L26 |
| `cFechaHora` | `CHAR(21)` | L27 |
| `cHora` | `CHAR(8)` | L28 |
| `dFechaPresenta` | `DATE` | L29 |
| `cDigitalizado` | `CHAR(1)` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L81 |
| `sc_contch_hist` | `bdicheq` | ⚠️ sí | SELECT | L90 |
| `cce_cheques_img` | `bditef` | ⚠️ sí | SELECT | L100 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L96 | FÓRMULA | `LET cFechaHora = NVL(TRIM(TO_CHAR(DATE(cFechaAlta), '%d/%m/%Y')),'')\|\|' '\|\|NVL(TRIM(cHoraAlta),'` |  |
| L108 | FÓRMULA | `LET cFechaHora = TO_CHAR(dFechaPresenta,'%d/%m/%Y');` |  |
| L110 | FÓRMULA | `LET cFechaHora = TO_CHAR(dFechaPresenta,'%d/%m/%Y')\|\|' '\|\|cHora;` |  |
| L115 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '10001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?pi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?pi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdetallesolsupervision`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallesolsupervision.sql` |
| **LOC (1er CREATE)** | 146 |
| **Callgraph** | ✅ fan_in=0 / fan_out=75 |
| **Deps concatenadas** | 181 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, solicitud y supervisión" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_busca_sol_supervision`, `sp_consulta_supervision_mc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallesolsupervision(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pStatus                      CHAR(2)
  pProducto                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud | 🟡 INFERIDO |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `cDesCodRetSp` | `CHAR(80)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cNumSolicitud` | `CHAR(20)` | L17 |
| `cNumCliente` | `CHAR(20)` | L18 |
| `cNombreCliente` | `CHAR(130)` | L19 |
| `dFechaSolicitud` | `DATE` | L20 |
| `dFechaCambioStatus` | `DATE` | L21 |
| `cStatus` | `CHAR(2)` | L22 |
| `cRespuestaOs` | `CHAR(8)` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |
| `sp_busca_sol_supervision` | `bdisolic` | ⚠️ sí | L73 |
| `sp_consulta_supervision_mc` | `bdicred` | ⚠️ sí | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_busca_sol_supervisi` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00787';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00788';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00789';` |  |
| L92 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00021';` |  |
| L112 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_consulta_supervision` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00021';` |  |
| L121 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00021';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `supervision` | ENTIDAD | supervisión | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consdetallesolsupervision_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallesolsupervision_totales.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, solicitud y supervisión (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_busca_sol_supervision_totales`, `sp_consulta_supervision_mc_totales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallesolsupervision_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pStatus                      CHAR(2)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud | 🟡 INFERIDO |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_busca_sol_supervision_totales` | `bdisolic` | ⚠️ sí | L46 |
| `sp_consulta_supervision_mc_totales` | `bdicred` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_busca_sol_supervisi` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00787';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00788';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_consulta_supervision` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `supervision` | ENTIDAD | supervisión | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consdetallesumarioarch`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdetallesumarioarch.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=10 / fan_out=14 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle y archivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdetallesumarioarch(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pTipo                        CHAR(3)
  pStatus                      CHAR(16)
  pFechaConsulta               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `arch`=archivo | 🟡 INFERIDO |
| `pTipo` | `CHAR(3)` | — | — |
| `pStatus` | `CHAR(16)` | — | — |
| `pFechaConsulta` | `DATE` | `cons`=consulta | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cCuenta` | `CHAR(20)` | L14 |
| `iNumCheque` | `INTEGER` | L15 |
| `mImporte` | `MONEY(16,2)` | L16 |
| `cCodOperacion` | `CHAR(2)` | L17 |
| `cImgStat1` | `CHAR(1)` | L18 |
| `cImgStat2` | `CHAR(1)` | L19 |
| `cStatus` | `CHAR(2)` | L20 |
| `cCodigoRet` | `CHAR(5)` | L21 |
| `dFechaProceso` | `DATE` | L22 |
| `cMotivoDev` | `CHAR(2)` | L23 |
| `cTruncamiento` | `CHAR(1)` | L24 |
| `cImgF` | `CHAR(3)` | L25 |
| `cImgT` | `CHAR(3)` | L26 |
| `cDescStatus` | `CHAR(16)` | L27 |
| `iTotImagenRec` | `INTEGER` | L28 |
| `iTotalImgRec` | `INTEGER` | L29 |
| `iNumOperaciones` | `INTEGER` | L30 |
| `mImporteTotal` | `MONEY(16,2)` | L31 |
| `iTotalRegistrosTi` | `INTEGER` | L32 |
| `iTotalImgGsum` | `INTEGER` | L33 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L100 |
| `cce_propios_gsum` | `bditef` | ⚠️ sí | SELECT | L115 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pFechaConsulta IS NULL THEN` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00783';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00784';` |  |
| L103 | FÓRMULA | `LET iTotImagenRec = iTotImagenRec + 1;` |  |
| L107 | FÓRMULA | `LET iTotImagenRec = iTotImagenRec + 1;` |  |
| L117 | FÓRMULA | `LET iTotalImgGsum = NVL(iTotalRegistrosTi,0) * 2;` |  |
| L118 | FÓRMULA | `LET iTotalImgFalt = NVL(iTotalImgGsum,0) - NVL(iTotalImgRec,0);` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00785';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?sumario` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sumario` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdevolucionesposccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdevolucionesposccl.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdevolucionesposccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoArchivo                 INTEGER
  pFechaConsulta               CHAR(10)
  pNomArchivo                  CHAR(21)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoArchivo` | `INTEGER` | — | — |
| `pFechaConsulta` | `CHAR(10)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNomArchivo` | `CHAR(21)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `cCodRetSp` | `CHAR(6)` | L22 |
| `cDescCodRet` | `CHAR(100)` | L23 |
| `iCodRetSp` | `INTEGER` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `cTipoArchivo` | `CHAR(48)` | L26 |
| `cNombreArchivo` | `CHAR(23)` | L27 |
| `dFechaCarga` | `DATE` | L28 |
| `iDevRecibidas` | `INTEGER` | L29 |
| `iDevAplicadas` | `INTEGER` | L30 |
| `iDevAplicadasForzadas` | `INTEGER` | L31 |
| `iDevConciliadasSA` | `INTEGER` | L32 |
| `iDevErrorIntegridad` | `INTEGER` | L33 |
| `iDevFaltantes` | `INTEGER` | L34 |
| `cNumTarjeta` | `CHAR(16)` | L35 |
| `cTipoOperacion` | `CHAR(5)` | L36 |
| `cMotivo` | `CHAR(61)` | L37 |
| `cNomComercio` | `CHAR(30)` | L38 |
| `cReferencia` | `CHAR(40)` | L39 |
| `mMonto` | `MONEY(18,2)` | L40 |
| `mMontoCashbackArchivo` | `MONEY(18,2)` | L41 |
| `iRecuperacion` | `INTEGER` | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L92 |
| `bditarjeta` | `bdicnweb` | no | L99 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoArchivo = '' OR pFechaConsulta = '' OR pRegistros IS NUL` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L104 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_concreing_consde` |  |
| L107 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consdevolucionesposccl_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdevolucionesposccl_totales.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (especial, totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=2 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdevolucionesposccl_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoArchivo                 INTEGER
  pFechaConsulta               CHAR(10)
  pNomArchivo                  CHAR(21)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoArchivo` | `INTEGER` | — | — |
| `pFechaConsulta` | `CHAR(10)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNomArchivo` | `CHAR(21)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRet` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L36 |
| `bditarjeta` | `bdicnweb` | no | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_concreing_consde` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consdirec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdirec.sql` |
| **LOC (1er CREATE)** | 297 |
| **Callgraph** | ✅ fan_in=30 / fan_out=27 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdirec(
  eEmpresa                     CHAR(3)
  eNumCte                      CHAR(20)
  eTpDirec                     INTEGER
) RETURNING char(5),int,char(1),char(40),char(60),char(40),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `eEmpresa` | `CHAR(3)` | — | — |
| `eNumCte` | `CHAR(20)` | — | — |
| `eTpDirec` | `INTEGER` | `rec`=recepción / recibe | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsqlerr` | `integer` | L13 |
| `vsecuencia` | `int` | L14 |
| `vnumerocalle` | `int` | L15 |
| `vnumerocolonia` | `int` | L16 |
| `vcodret` | `char(5)` | L17 |
| `vtipo_dir` | `char(1)` | L18 |
| `vcalle` | `char(40)` | L19 |
| `vcolonia` | `char(60)` | L20 |
| `ventre_calles` | `char(40)` | L21 |
| `vpais` | `char(3)` | L22 |
| `vestado` | `char(2)` | L23 |
| `vciudad` | `char(3)` | L24 |
| `vmunicipio` | `char(5)` | L25 |
| `vcod_postal` | `char(5)` | L26 |
| `vapart_postal` | `char(11)` | L27 |
| `vtipo_telef1` | `char(1)` | L28 |
| `vtelefono1` | `char(13)` | L29 |
| `vtipo_telef2` | `char(1)` | L30 |
| `vtelefono2` | `char(13)` | L31 |
| `vtipo_telef3` | `char(1)` | L32 |
| `vtelefono3` | `char(13)` | L33 |
| `vextension` | `char(5)` | L34 |
| `vestado_inegi` | `char(2)` | L35 |
| `vmunicipio_inegi` | `char(3)` | L36 |
| `vlocalidad_inegi` | `char(4)` | L37 |
| *…29 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L146 |
| `si_estados` | `bdinteg` | ⚠️ sí | SELECT | L176 |
| `si_catciudades` | `bdinteg` | ⚠️ sí | SELECT | L182 |
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L187 |
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L193 |
| `si_catdomicilios` | `bdinteg` | ⚠️ sí | SELECT | L211 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L150 | VALIDACIÓN_NULL | `IF vSec IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?di` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?di` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdireccionctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdireccionctemoral.sql` |
| **LOC (1er CREATE)** | 259 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta dirección y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consul_direc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdireccionctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pTipoDirec                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pTipoDirec` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L54 |
| `iSqlErr` | `INTEGER` | L55 |
| `cCodRetSp` | `CHAR(5)` | L56 |
| `iCodRetSp` | `INTEGER` | L57 |
| `cEmpresa` | `CHAR(3)` | L58 |
| `vsecuencia` | `INT` | L59 |
| `vnumerocalle` | `INT` | L60 |
| `vnumerocolonia` | `INT` | L61 |
| `vcodret` | `CHAR(5)` | L62 |
| `vtipo_dir` | `CHAR(1)` | L63 |
| `vcalle` | `CHAR(40)` | L64 |
| `vcolonia` | `CHAR(60)` | L65 |
| `ventre_calles` | `CHAR(40)` | L66 |
| `vpais` | `CHAR(3)` | L67 |
| `vestado` | `CHAR(2)` | L68 |
| `vciudad` | `CHAR(3)` | L69 |
| `vmunicipio` | `CHAR(5)` | L70 |
| `vcod_postal` | `CHAR(5)` | L71 |
| `vapart_postal` | `CHAR(11)` | L72 |
| `vtipo_telef1` | `CHAR(1)` | L73 |
| `vtelefono1` | `CHAR(13)` | L74 |
| `vtipo_telef2` | `CHAR(1)` | L75 |
| `vtelefono2` | `CHAR(13)` | L76 |
| `vtipo_telef3` | `CHAR(1)` | L77 |
| `vtelefono3` | `CHAR(13)` | L78 |
| *…32 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L201 |
| `sp_consul_direc` | `bdicnweb` | no | L214 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L188 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pNumCliente = '' OR pTipoDirec IS NULL THEN` |  |
| L189 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L226 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consul_direc';` |  |
| L240 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `direccion` | ENTIDAD | dirección | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

---

## `sp_consdireccionctemoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdireccionctemoral2.sql` |
| **LOC (1er CREATE)** | 245 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta dirección y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consul_direc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdireccionctemoral2(
  pNumCliente                  CHAR(20)
  pTipoDirec                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(20)` | — | — |
| `pTipoDirec` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L54 |
| `iSqlErr` | `INTEGER` | L55 |
| `cCodRetSp` | `CHAR(5)` | L56 |
| `iCodRetSp` | `INTEGER` | L57 |
| `cEmpresa` | `CHAR(3)` | L58 |
| `vsecuencia` | `INT` | L59 |
| `vnumerocalle` | `INT` | L60 |
| `vnumerocolonia` | `INT` | L61 |
| `vcodret` | `CHAR(5)` | L62 |
| `vtipo_dir` | `CHAR(1)` | L63 |
| `vcalle` | `CHAR(40)` | L64 |
| `vcolonia` | `CHAR(60)` | L65 |
| `ventre_calles` | `CHAR(40)` | L66 |
| `vpais` | `CHAR(3)` | L67 |
| `vestado` | `CHAR(2)` | L68 |
| `vciudad` | `CHAR(3)` | L69 |
| `vmunicipio` | `CHAR(5)` | L70 |
| `vcod_postal` | `CHAR(5)` | L71 |
| `vapart_postal` | `CHAR(11)` | L72 |
| `vtipo_telef1` | `CHAR(1)` | L73 |
| `vtelefono1` | `CHAR(13)` | L74 |
| `vtipo_telef2` | `CHAR(1)` | L75 |
| `vtelefono2` | `CHAR(13)` | L76 |
| `vtipo_telef3` | `CHAR(1)` | L77 |
| `vtelefono3` | `CHAR(13)` | L78 |
| *…32 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consul_direc` | `bdicnweb` | no | L200 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L188 | VALIDACIÓN_NULL | `IF pNumCliente = '' OR pTipoDirec IS NULL THEN` |  |
| L189 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L212 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consul_direc';` |  |
| L226 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `direccion` | ENTIDAD | dirección | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdireccionescteprospecto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdireccionescteprospecto.sql` |
| **LOC (1er CREATE)** | 243 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta direcciones, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consdirec` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdireccionescteprospecto(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pCliente                     CHAR(20)
  pTipoDireccion               INTEGER
) RETURNING CHAR(5)	AS	cCodret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pTipoDireccion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodret` | `CHAR(5)` | L55 |
| `cCodretSP` | `CHAR(3)` | L56 |
| `iSqlErr` | `INTEGER` | L57 |
| `vsqlerr` | `INTEGER` | L58 |
| `vsecuencia` | `INT` | L59 |
| `vnumerocalle` | `INT` | L60 |
| `vnumerocolonia` | `INT` | L61 |
| `vtipo_dir` | `CHAR(1)` | L62 |
| `vcalle` | `CHAR(40)` | L63 |
| `vcolonia` | `CHAR(60)` | L64 |
| `ventre_calles` | `CHAR(40)` | L65 |
| `vpais` | `CHAR(3)` | L66 |
| `vestado` | `CHAR(2)` | L67 |
| `vciudad` | `CHAR(3)` | L68 |
| `vmunicipio` | `CHAR(5)` | L69 |
| `vcod_postal` | `CHAR(5)` | L70 |
| `vapart_postal` | `CHAR(11)` | L71 |
| `vtipo_telef1` | `CHAR(1)` | L72 |
| `vtelefono1` | `CHAR(13)` | L73 |
| `vtipo_telef2` | `CHAR(1)` | L74 |
| `vtelefono2` | `CHAR(13)` | L75 |
| `vtipo_telef3` | `CHAR(1)` | L76 |
| `vtelefono3` | `CHAR(13)` | L77 |
| `vextension` | `CHAR(5)` | L78 |
| `vestado_inegi` | `CHAR(2)` | L79 |
| *…31 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consdirec` | `bdicnweb` | no | L205 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L113 | CÓDIGO_RETORNO | `LET cCodret = '00000';` |  |
| L187 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `direcciones` | ENTIDAD | direcciones | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdireccionescteprospecto2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdireccionescteprospecto2.sql` |
| **LOC (1er CREATE)** | 233 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta direcciones, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consdirec` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdireccionescteprospecto2(
  pCliente                     CHAR(20)
  pTipoDireccion               INTEGER
) RETURNING CHAR(5)	AS	cCodret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCliente` | `CHAR(20)` | — | — |
| `pTipoDireccion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodret` | `CHAR(5)` | L55 |
| `cCodretSP` | `CHAR(3)` | L56 |
| `iSqlErr` | `INTEGER` | L57 |
| `vsqlerr` | `INTEGER` | L58 |
| `vsecuencia` | `INT` | L59 |
| `vnumerocalle` | `INT` | L60 |
| `vnumerocolonia` | `INT` | L61 |
| `vtipo_dir` | `CHAR(1)` | L62 |
| `vcalle` | `CHAR(40)` | L63 |
| `vcolonia` | `CHAR(60)` | L64 |
| `ventre_calles` | `CHAR(40)` | L65 |
| `vpais` | `CHAR(3)` | L66 |
| `vestado` | `CHAR(2)` | L67 |
| `vciudad` | `CHAR(3)` | L68 |
| `vmunicipio` | `CHAR(5)` | L69 |
| `vcod_postal` | `CHAR(5)` | L70 |
| `vapart_postal` | `CHAR(11)` | L71 |
| `vtipo_telef1` | `CHAR(1)` | L72 |
| `vtelefono1` | `CHAR(13)` | L73 |
| `vtipo_telef2` | `CHAR(1)` | L74 |
| `vtelefono2` | `CHAR(13)` | L75 |
| `vtipo_telef3` | `CHAR(1)` | L76 |
| `vtelefono3` | `CHAR(13)` | L77 |
| `vextension` | `CHAR(5)` | L78 |
| `vestado_inegi` | `CHAR(2)` | L79 |
| *…31 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consdirec` | `bdicnweb` | no | L195 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L113 | CÓDIGO_RETORNO | `LET cCodret = '00000';` |  |
| L187 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `direcciones` | ENTIDAD | direcciones | 🔵 CONVENCIÓN | nombre_sp |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdoctatransfersoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdoctatransfersoc.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuenta, transferencia y Sistema Operativo Central" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdoctatransfersoc(
  pCuenta                      CHAR(20)
) RETURNING CHAR(5), CHAR(5), INTEGER, CHAR(16)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `cHora` | `CHAR(15)` | L10 |
| `cFolioSuc` | `CHAR(16)` | L11 |
| `cTransaccTrf` | `CHAR(5)` | L12 |
| `cUsuario` | `CHAR(8)` | L13 |
| `mMonto` | `DECIMAL(14,2)` | L14 |
| `cCodRetTrf` | `CHAR(5)` | L15 |
| `iSerial` | `INTEGER` | L16 |
| `iVueltas` | `SMALLINT` | L17 |
| `cStatus` | `CHAR(1)` | L18 |
| `cSQL` | `CHAR(10)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L62 |
| `sc_transfer_online` | `bdicheq` | ⚠️ sí | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_transfer_online_consdo` | `bdicheq` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L88 | FÓRMULA | `LET iVueltas = iVueltas + 1;` |  |
| L95 | VALIDACIÓN_NULL | `IF ( cStatus is null OR cStatus = '' OR cStatus IN('N','E') ) THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?do` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?do` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consdoctatransfersoccomp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consdoctatransfersoccomp.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuenta, transferencia y Sistema Operativo Central (complemento)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consdoctatransfersoccomp(
  pCuenta                      CHAR(20)
  pTransaccTrf                 CHAR(5)
  pSerial                      INTEGER
  pFolioSuc                    CHAR(16)
) RETURNING CHAR(5), DECIMAL(14,2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCuenta` | `CHAR(20)` | — | — |
| `pTransaccTrf` | `CHAR(5)` | — | — |
| `pSerial` | `INTEGER` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `mSdoCtaTrf` | `DECIMAL(14,2)` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cTramaResp` | `CHAR(500)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_transfer_online` | `bdicheq` | ⚠️ sí | SELECT | L45 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?do` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?do` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consencabezadoslotes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consencabezadoslotes.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta encabezado y lote" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consencabezadoslotes(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              CHAR(10)
  pArchDescarga                char(150)
  pLote                        int
  pFechaInicial                date
  pFechaFinal                  date
  pUsuario                     CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `CHAR(10)` | — | — |
| `pArchDescarga` | `char(150)` | — | — |
| `pLote` | `int` | `lote`=lote (proceso batch) | ✅ CÓDIGO |
| `pFechaInicial` | `date` | — | — |
| `pFechaFinal` | `date` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cSelectQry` | `CHAR(1500)` | L6 |
| `cCmd1` | `CHAR(1500)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L33 |
| `systables` | `bdicnweb` | no | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneencabezadototalesmasivo` | `bdicnweb` | no | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | FÓRMULA | `LET cSelectQry = "select id_lote, to_char(fecha_carga, "\|\|'"%d/%m/%Y"'\|\|") as fecha_carga, total` | 🔴 MONEY/aritmética financiera |
| L35 | VALIDACIÓN_NULL | `IF pFechaInicial IS NULL THEN` |  |
| L54 | FÓRMULA | `LET cCmd1 = "/usr/bin/echo 'SET ISOLATION TO DIRTY READ; UNLOAD TO "\|\|TRIM(pArchDescarga)\|\|" "\|` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `lote` | ENTIDAD | lote (proceso batch) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consencabezadoslotesreversos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consencabezadoslotesreversos.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta encabezado y lote" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consencabezadoslotesreversos(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              CHAR(10)
  pArchDescarga                char(150)
  pLote                        int
  pFechaInicial                date
  pFechaFinal                  date
  pUsuario                     CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `CHAR(10)` | — | — |
| `pArchDescarga` | `char(150)` | — | — |
| `pLote` | `int` | `lote`=lote (proceso batch) | 🔵 CONVENCIÓN |
| `pFechaInicial` | `date` | — | — |
| `pFechaFinal` | `date` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cSelectQry` | `CHAR(1500)` | L6 |
| `cCmd1` | `CHAR(1500)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_reversos` | `bdicnweb` | no | SELECT | L34 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneencabezadototalesmasivo` | `bdicnweb` | no | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L34 | FÓRMULA | `LET cSelectQry = "select id_lote, to_char(fecha_carga, "\|\|'"%d/%m/%Y"'\|\|') as fecha_carga, total` | 🔴 MONEY/aritmética financiera |
| L36 | VALIDACIÓN_NULL | `IF pFechaInicial IS NULL THEN` |  |
| L54 | FÓRMULA | `LET cCmd1 = "/usr/bin/echo 'SET ISOLATION TO DIRTY READ; UNLOAD TO "\|\|TRIM(pArchDescarga)\|\|" "\|` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `lote` | ENTIDAD | lote (proceso batch) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?s`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conserroresarchcecoban`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conserroresarchcecoban.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=9 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta error, archivo y cob — cobranza" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_conserroresarchcecoban(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNombreArchivo               CHAR(22)
  pDireccionMac                CHAR(12)
  pFechaHoy                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNombreArchivo` | `CHAR(22)` | `arch`=archivo | 🟡 INFERIDO |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pFechaHoy` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cSeccion` | `CHAR(12)` | L12 |
| `cCampo` | `CHAR(2)` | L13 |
| `cDescMensaje` | `CHAR(100)` | L14 |
| `iLinea` | `INTEGER` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_bitacoraerror` | `bdicnweb` | no | SELECT | L64 |
| `sw_cr_bitacoraerrorimg` | `bdicnweb` | no | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pNombreArchivo = '' OR pDireccionMac = ''` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L66 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `?ce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cob` | ENTIDAD | cob — cobranza (abreviación de dominio — sp_repcob_*, sp_obt | 🔵 CONVENCIÓN | nombre_sp |
| `?an` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es`, `?ce`, `?an` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conserroresarchcecoban_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conserroresarchcecoban_totales.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=9 / fan_out=3 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta error, archivo y cob — cobranza (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_conserroresarchcecoban_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNombreArchivo               CHAR(22)
  pDireccionMac                CHAR(12)
  pFechaHoy                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNombreArchivo` | `CHAR(22)` | `arch`=archivo | 🟡 INFERIDO |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pFechaHoy` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_bitacoraerror` | `bdicnweb` | no | SELECT | L42 |
| `sw_cr_bitacoraerrorimg` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pNombreArchivo = '' OR pDireccionMac = ''` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `?ce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cob` | ENTIDAD | cob — cobranza (abreviación de dominio — sp_repcob_*, sp_obt | 🔵 CONVENCIÓN | nombre_sp |
| `?an_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es`, `?ce`, `?an_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consfechadianteriorsac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consfechadianteriorsac.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fecha y Servicios de Atención al Cliente — subsistema de atención en sucursal (del día)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consfechadianteriorsac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `dFechaAnterior` | `DATE` | L8 |
| `dFechaAcutal` | `DATE` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `?nterior` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nterior` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consfinalizacedulasccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consfinalizacedulasccl.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cédulas y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (final)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consfinalizacedulasccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCcl                    DATE
  pTipo                        SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCcl` | `DATE` | `ccl`=módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (bdicnweb:sp_ccl_*) | 🟡 INFERIDO |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `bdicnweb` | `bdicnweb` | no | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaCcl = '' OR pTipo IS NULL  THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_finalizacedulas ";` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `final` | MODIF | final | 🔵 CONVENCIÓN | nombre_sp |
| `?iza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cedulas` | ENTIDAD | cédulas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iza` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consgeneralfechas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consgeneralfechas.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=22 / fan_out=5 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fechas (general)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consgeneralfechas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFechaHoy` | `DATE` | L10 |
| `dFechaAnt` | `DATE` | L11 |
| `dProxFecha` | `DATE` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L51 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L57 |
| `co_fechas` | `bdicont` | ⚠️ sí | SELECT | L63 |
| `sd_fechas` | `bdicred` | ⚠️ sí | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | VALIDACIÓN_NULL | `IF dFechaHoy IS NULL AND dFechaAnt IS NULL AND dProxFecha IS NULL THEN` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `fechas` | ENTIDAD | fechas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consgralproximafechahabil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consgralproximafechahabil.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=8 / fan_out=12 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta ro — Rol Operativo, fecha y día hábil — día bancario operativo (general)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `cal_fecha_pre_fh` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consgralproximafechahabil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pFecha` | `DATE` | `fecha`=fecha | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `CHAR(10)` | L10 |
| `dFechaHabilProx` | `DATE` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `cal_fecha_pre_fh` | `bditef` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pFecha IS NULL THEN` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:cal_fecha_pre_fh';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | VALIDACIÓN_NULL | `IF dFechaHabilProx IS NULL THEN` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ima` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `habil` | ENTIDAD | día hábil — día bancario operativo (spei_validafecha, sp_cam | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?ima` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consguardabitacoraccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consguardabitacoraccl.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta bitácora y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consguardabitacoraccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pElemento                    INTEGER
  pActividad                   CHAR(150)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pElemento` | `INTEGER` | — | — |
| `pActividad` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `bditarjeta` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta.sp_concreing_guardabitacora';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_conshistcambiosautorizadosctapm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conshistcambiosautorizadosctapm.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cambio, OS — Originación de Solicitudes y cuenta (histórico/historial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=1 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_conshistcambiosautorizadosctapm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(2)
  pNumCliente                  CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(2)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `dFechaMod` | `CHAR(10)` | L16 |
| `cNumCuenta` | `CHAR(20)` | L17 |
| `cTipoFirma` | `CHAR(22)` | L18 |
| `cNumFirmas` | `CHAR(2)` | L19 |
| `cCombinacion` | `CHAR(120)` | L20 |
| `cUsMod` | `CHAR(10)` | L21 |
| `cIdRegFirmas` | `CHAR(2)` | L22 |
| `cDescRegFirmas` | `CHAR(20)` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sc_firmantes` | `bdicnweb` | no | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L85 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `autoriza` | ACCION | autoriza | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?d`, `?pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conshistcambiosautorizadosctapm_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conshistcambiosautorizadosctapm_totales.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 96 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cambio, OS — Originación de Solicitudes y cuenta (histórico/historial, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=7 · INFERIDO=1 · SINTÉTICO=3 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_conshistcambiosautorizadosctapm_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(2)
  pNumCliente                  CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(2)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sc_firmantes` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `autoriza` | ACCION | autoriza | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?d`, `?pm_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consinfodetallesolsupervision`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consinfodetallesolsupervision.sql` |
| **LOC (1er CREATE)** | 178 |
| **Callgraph** | ✅ fan_in=0 / fan_out=73 |
| **Deps concatenadas** | 180 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, detalle, solicitud y supervisión" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_catinfogrid_sol_superv` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consinfodetallesolsupervision(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L31 |
| `cCodRetSp` | `CHAR(6)` | L32 |
| `cDesCodRetSp` | `CHAR(80)` | L33 |
| `iSqlErr` | `INTEGER` | L34 |
| `cEmpresa` | `CHAR(3)` | L35 |
| `cFolioOs` | `CHAR(20)` | L36 |
| `dFechaGeneracionOs` | `DATE` | L37 |
| `cNumSolicitud` | `CHAR(20)` | L38 |
| `cNumCliente` | `CHAR(20)` | L39 |
| `cNombreCliente` | `CHAR(110)` | L40 |
| `cStatus` | `CHAR(2)` | L41 |
| `iNumCiudad` | `INTEGER` | L42 |
| `iNumZona` | `INTEGER` | L43 |
| `cNombreZona` | `CHAR(32)` | L44 |
| `cNombreCiudad` | `CHAR(60)` | L45 |
| `cMunicipio` | `CHAR(27)` | L46 |
| `cCodPostal` | `CHAR(6)` | L47 |
| `cEstado` | `CHAR(30)` | L48 |
| `cFolioOsCoppel` | `CHAR(20)` | L49 |
| `dFechaGeneracionOsCoppel` | `DATE` | L50 |
| `cNumSolicitudCoppel` | `CHAR(20)` | L51 |
| `cNumCteCoppel` | `CHAR(20)` | L52 |
| `iNumCiudadCoppel` | `INTEGER` | L53 |
| `iNumZonaCoppel` | `INTEGER` | L54 |
| `cNombreZonaCoppel` | `CHAR(32)` | L55 |
| *…7 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L119 |
| `sp_catinfogrid_sol_superv` | `bdisolic` | ⚠️ sí | L131 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L138 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_catinfogrid_sol_sup` |  |
| L140 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L153 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L162 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `supervision` | ENTIDAD | supervisión | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consinfosolsupervision`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consinfosolsupervision.sql` |
| **LOC (1er CREATE)** | 145 |
| **Callgraph** | ✅ fan_in=0 / fan_out=72 |
| **Deps concatenadas** | 179 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, solicitud y supervisión" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cargainfo_sol_superv` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consinfosolsupervision(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `cCodRetSp` | `CHAR(6)` | L27 |
| `cDesCodRetSp` | `CHAR(80)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cEmpresa` | `CHAR(3)` | L30 |
| `cNumCliente` | `CHAR(20)` | L31 |
| `cNumSolicitud` | `CHAR(20)` | L32 |
| `cDescProducto` | `CHAR(80)` | L33 |
| `cDescStatus` | `CHAR(80)` | L34 |
| `cSucursal` | `CHAR(4)` | L35 |
| `cNombreCliente` | `CHAR(150)` | L36 |
| `cRfc` | `CHAR(13)` | L37 |
| `cCalle` | `CHAR(30)` | L38 |
| `cEstado` | `CHAR(30)` | L39 |
| `cDepartamento` | `CHAR(6)` | L40 |
| `cComplemento` | `CHAR(80)` | L41 |
| `cCiudad` | `CHAR(30)` | L42 |
| `cMunicipio` | `CHAR(27)` | L43 |
| `sEdificio` | `SMALLINT` | L44 |
| `cNumInt` | `CHAR(10)` | L45 |
| `cNumExt` | `CHAR(10)` | L46 |
| `cCodPostal` | `CHAR(5)` | L47 |
| `cZonaCol` | `CHAR(32)` | L48 |
| `cTelCasa` | `CHAR(13)` | L49 |
| `cTelCelular` | `CHAR(13)` | L50 |
| *…3 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L104 |
| `sp_cargainfo_sol_superv` | `bdisolic` | ⚠️ sí | L114 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L120 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_cargainfo_sol_super` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `supervision` | ENTIDAD | supervisión | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consinfprospectos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consinfprospectos.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 221 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información y prospectos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consinfprospectos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteBanco                    CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteBanco` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRet` | `INTEGER` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iNoRegsEncontrados` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |
| `cTipo_prospecto` | `CHAR(20)` | L17 |
| `cRel_cliente` | `CHAR(2)` | L18 |
| `cRel_a_cliente` | `CHAR(20)` | L19 |
| `cNom_cliente_rel` | `CHAR(107)` | L20 |
| `cRel_registrada` | `CHAR(20)` | L21 |
| `cNombre1` | `CHAR(26)` | L22 |
| `cNombre2` | `CHAR(26)` | L23 |
| `cApell_paterno` | `CHAR(26)` | L24 |
| `cApell_materno` | `CHAR(26)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_det_infprospectos` | `bdicnweb` | no | SELECT | L79 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `prospectos` | ENTIDAD | prospectos (nuevos clientes potenciales) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consinfprospectos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consinfprospectos_totales.sql` |
| **LOC (1er CREATE)** | 456 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 220 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información y prospectos (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consinfprospectos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteBanco                    CHAR(20)
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteBanco` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRet` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegsEncontrados` | `INTEGER` | L9 |
| `iRegistros` | `INTEGER` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `cTipo_prospecto` | `CHAR(20)` | L13 |
| `cRel_cliente` | `CHAR(2)` | L14 |
| `cRel_a_cliente` | `CHAR(20)` | L15 |
| `cNom_cliente_rel` | `CHAR(107)` | L16 |
| `cRel_registrada` | `CHAR(20)` | L17 |
| `cNombre1` | `CHAR(26)` | L18 |
| `cNombre2` | `CHAR(26)` | L19 |
| `cApell_paterno` | `CHAR(26)` | L20 |
| `cApell_materno` | `CHAR(26)` | L21 |
| `cNumcteBanco` | `CHAR(20)` | L22 |
| `cNumcteRef` | `CHAR(20)` | L23 |
| `cParentesco` | `CHAR(2)` | L24 |
| `iIdRegistro` | `INTEGER` | L26 |
| `bEnTransaccion` | `BOOLEAN` | L27 |
| `iContador` | `INTEGER` | L28 |
| `iMaxCommit` | `INTEGER` | L29 |
| `iNoRegistros` | `INTEGER` | L30 |
| `iExiste` | `INTEGER` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusinfprospectos` | `bdicnweb` | no | SELECT | L84 |
| `statusinfprospectos` | `bdicnweb` | no | DELETE | L84 |
| `statusinfprospectos` | `bdicnweb` | no | INSERT | L86 |
| `sw_det_infprospectos` | `bdicnweb` | no | SELECT | L109 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L143 |
| `sw_det_infprospectos` | `bdicnweb` | no | INSERT | L161 |
| `si_refclientes` | `bdinteg` | ⚠️ sí | SELECT | L179 |
| `si_parentesco` | `bdinteg` | ⚠️ sí | SELECT | L187 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L113 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L160 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L164 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L208 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L223 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L274 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L289 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L332 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L347 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L390 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L405 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L436 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `prospectos` | ENTIDAD | prospectos (nuevos clientes potenciales) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consinstventocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consinstventocap.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `cons_instvento` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consinstventocap(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pCuenta                      CHAR(20)
  pNumMotivo                   SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pNumMotivo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cCodRet` | `CHAR(5)` | L15 |
| `cCuenta` | `CHAR(20)` | L16 |
| `cNomCapInt` | `CHAR(7)` | L17 |
| `iInstVento` | `SMALLINT` | L18 |
| `cDescInstVento` | `CHAR(30)` | L19 |
| `mImporte` | `MONEY(14,2)` | L20 |
| `cNombreSistema` | `CHAR(35)` | L21 |
| `cCuentaTraspaso` | `CHAR(20)` | L22 |
| `cAplicado` | `CHAR(1)` | L23 |
| `dFechaVencimiento` | `DATE` | L24 |
| `iRegs` | `INTEGER` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L57 |
| `cons_instvento` | `bdinvers` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L71 | FÓRMULA | `LET iRegs = iRegs + 1;` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?tvento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tvento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conslotesmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conslotesmasivo.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta lote (masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_conslotesmasivo(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCarga                  DATE
  pSistemaCuenta               char(2)
  pNombreArchivo               CHAR(150)
  pStatusLote                  CHAR(1)
  pPagina                      int
  pRegistros                   int
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCarga` | `DATE` | — | — |
| `pSistemaCuenta` | `char(2)` | — | — |
| `pNombreArchivo` | `CHAR(150)` | — | — |
| `pStatusLote` | `CHAR(1)` | `lote`=lote (proceso batch) | ✅ CÓDIGO |
| `pPagina` | `int` | — | — |
| `pRegistros` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `dFechaCarga` | `DATE` | L11 |
| `cArchivoCarga` | `CHAR(150)` | L12 |
| `cStatusLote` | `CHAR(15)` | L13 |
| `iLote` | `INT` | L14 |
| `cDynamicQuery` | `char(1500)` | L15 |
| `iRows` | `int` | L16 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | FÓRMULA | `LET iRows = iRows + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `lote` | ENTIDAD | lote (proceso batch) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consmantolineasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consmantolineasmasivocre.sql` |
| **LOC (1er CREATE)** | 189 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta línea y crédito (anterior, masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consmantolineasmasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `iIdRegistro` | `INTEGER` | L17 |
| `cNoCuenta` | `CHAR(20)` | L18 |
| `cResultado` | `CHAR(15)` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `cMotivoRechazo` | `CHAR(80)` | L21 |
| `cNoCliente` | `CHAR(20)` | L22 |
| `cNombreCliente` | `CHAR(107)` | L23 |
| `mMontoLineaActual` | `MONEY(18,2)` | L24 |
| `mMontoNuevaLinea` | `MONEY(18,2)` | L25 |
| `dFechaMovimiento` | `DATE` | L26 |
| `cStatusRegistro` | `CHAR(1)` | L27 |
| `iExiste` | `INTEGER` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_mantolineascredito` | `bdicnweb` | no | SELECT | L82 |
| `sw_tr_cargamasiva_mantolineascredito_hist` | `bdicnweb` | no | SELECT | L86 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L120 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L126 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL ` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L117 | VALIDACIÓN_NULL | `IF cNoCliente IS NULL OR cNoCliente = '' THEN` |  |
| L167 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L172 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `linea` | ENTIDAD | línea (de crédito) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?o`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consmonarchconciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consmonarchconciliacion.sql` |
| **LOC (1er CREATE)** | 172 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consmonarchconciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoProceso                 CHAR (1)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoProceso` | `CHAR (1)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cCodRetSp` | `CHAR(5)` | L30 |
| `iCodRetSp` | `INTEGER` | L31 |
| `cNombreArchivo` | `CHAR(23)` | L32 |
| `cArchivoOrigen` | `CHAR(3)` | L33 |
| `cNumRegistros325` | `CHAR(20)` | L34 |
| `cFechaArchivo` | `CHAR(10)` | L35 |
| `cMonto325` | `CHAR(16)` | L36 |
| `cFechaProceso` | `CHAR(10)` | L37 |
| `cFechaHoraTransferencia` | `CHAR(25)` | L38 |
| `cFechaHoraIniProceso` | `CHAR(25)` | L39 |
| `cFechaHoraCargaArchivo` | `CHAR(25)` | L40 |
| `cFechaHoraCargaTabla` | `CHAR(25)` | L41 |
| `cFechaHoraIniConciliaReg` | `CHAR(25)` | L42 |
| `cFechaHoraFinConciliaReg` | `CHAR(25)` | L43 |
| `cFechaHoraFinProceso` | `CHAR(25)` | L44 |
| `cFechaHoraGenConadmin` | `CHAR(25)` | L45 |
| `cTransferencia` | `CHAR(1)` | L46 |
| `cCarga` | `CHAR(1)` | L47 |
| `cConadmin` | `CHAR(1)` | L48 |
| `cNumCargo` | `CHAR(20)` | L49 |
| `cMontoCargo` | `CHAR(16)` | L50 |
| `cNumAbono` | `CHAR(20)` | L51 |
| `cMontoAbono` | `CHAR(16)` | L52 |
| *…6 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L114 |
| `bditarjeta` | `bdicnweb` | no | L120 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L102 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoProceso = '' OR  pRegistros IS NULL OR pRecuperacion IS ` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L125 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_monarchcr ';` |  |
| L128 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mon` | PREFIJO | monitor / módulo | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consmonarchconciliacion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consmonarchconciliacion_totales.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivo (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consmonarchconciliacion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoProceso                 CHAR (1)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoProceso` | `CHAR (1)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L36 |
| `bditarjeta` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditarjeta:sp_concreing_monarch` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mon` | PREFIJO | monitor / módulo | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consmotdevolucion_tef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consmotdevolucion_tef.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consmotdevolucion_tef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdDevolucion` | `CHAR(2)` | L9 |
| `cDescipcionMotivo` | `CHAR(70)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tf_motivos_devolucion` | `bdicnweb` | no | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?mot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `devolucion` | ACCION | devuelve | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consmovpagaresnoaplicados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consmovpagaresnoaplicados.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento, pagarés y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consmovpagaresnoaplicados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `dFecha` | `DATE` | L21 |
| `cEmpresa` | `CHAR (3)` | L22 |
| `cTipoMov` | `CHAR (1)` | L23 |
| `cSucursal` | `CHAR (4)` | L24 |
| `cCuenta` | `CHAR (20)` | L25 |
| `mMonto` | `MONEY (18,2)` | L26 |
| `cDivisa` | `CHAR (3)` | L27 |
| `cProcesado` | `CHAR (1)` | L28 |
| `dFechaAlta` | `DATE` | L29 |
| `cTransacc` | `CHAR (4)` | L30 |
| `cReferencia` | `CHAR (40)` | L31 |
| `cUsuario` | `CHAR (8)` | L32 |
| `cCodigoRetorno` | `CHAR (5)` | L33 |
| `dFechaApli` | `DATE` | L34 |
| `dFechaProceso` | `DATE` | L35 |
| `iNoRegistros` | `INTEGER` | L36 |
| `iRegistros` | `INTEGER` | L37 |
| `iRecuperacion` | `INTEGER` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_movinvernoaplichq` | `bdinvers` | ⚠️ sí | SELECT | L90 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `pagares` | ENTIDAD | pagarés | 🔵 CONVENCIÓN | nombre_sp |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?no`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consmovretenidosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consmovretenidosccl.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (retenido)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consmovretenidosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `codret` | `CHAR(5)` | L14 |
| `cCuentaCredito` | `CHAR(15)` | L15 |
| `dFechaRetencion` | `DATETIME YEAR TO FRACTION(5)` | L16 |
| `cFolioRetencion` | `CHAR(20)` | L17 |
| `mMontoRetenido` | `MONEY(18,2)` | L18 |
| `iDiasRestantesLib` | `INTEGER` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |
| `iRegistros` | `INTEGER` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L60 |
| `bditarjeta` | `bdicnweb` | no | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pTipo = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR p` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L73 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP intercard:sp_consultaconadmincorr';` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `retenido` | MODIF | retenido (fondos en retención) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consmovretenidosccl_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consmovretenidosccl_totales.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (retenido, totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `sp_concreing_movimientosretenidos2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consmovretenidosccl_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `sp_concreing_movimientosretenidos2_totales` | `bditarjeta` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pTipo = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditarjeta:sp_concreing_movimie` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `retenido` | MODIF | retenido (fondos en retención) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consnomcliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consnomcliente.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 86 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta nómina y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consnomcliente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCliente                     CHAR(20)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | `cliente`=cliente | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNombreCte` | `CHAR(107)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00088';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |
| `cliente` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_conspagaresvencidosvigentes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conspagaresvencidosvigentes.sql` |
| **LOC (1er CREATE)** | 196 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta pagarés, vencimiento, identificador y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=3 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_conspagaresvencidosvigentes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L46 |
| `iSqlErr` | `INTEGER` | L47 |
| `dFecha` | `date` | L48 |
| `cEmpresa` | `CHAR (3)` | L49 |
| `cCuenta` | `CHAR (20)` | L50 |
| `sSecuencia` | `SMALLINT` | L51 |
| `cCodInstrum` | `CHAR (4)` | L52 |
| `cNumCte` | `CHAR (20)` | L53 |
| `cStatusCta` | `CHAR (1)` | L54 |
| `cMotivo` | `CHAR (2)` | L55 |
| `cFecUltMov` | `DATE` | L56 |
| `dFecCancelac` | `DATE` | L57 |
| `dFecReinversion` | `DATE` | L58 |
| `mCapital` | `MONEY (18,2)` | L59 |
| `mSdoRetenido` | `MONEY (18,2)` | L60 |
| `mSdoCong` | `MONEY (18,2)` | L61 |
| `sPlazo` | `SMALLINT` | L62 |
| `dFechaVenc` | `DATE` | L63 |
| `cOpcionRetiro` | `CHAR (2)` | L64 |
| `mIntereses` | `MONEY (18,2)` | L65 |
| `Isr` | `MONEY (18,2)` | L66 |
| `dTasa` | `DECIMAL (9,2)` | L67 |
| `dSobretasa` | `DECIMAL (9,2)` | L68 |
| `sDiaSdoPos` | `SMALLINT` | L69 |
| `dAcumSdoPos` | `MONEY (18,2)` | L70 |
| *…22 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_invvencvig` | `bdinvers` | ⚠️ sí | SELECT | L171 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L164 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L152 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L174 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L180 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `pagares` | ENTIDAD | pagarés | 🔵 CONVENCIÓN | nombre_sp |
| `venc` | ENTIDAD | vencimiento | 🟡 INFERIDO | nombre_sp |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?vi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `?tes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?vi`, `?tes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conspagaresvencidosvigentes_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conspagaresvencidosvigentes_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta pagarés, vencimiento, identificador y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=3 · SINTÉTICO=2 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_conspagaresvencidosvigentes_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_invvencvig` | `bdinvers` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `pagares` | ENTIDAD | pagarés | 🔵 CONVENCIÓN | nombre_sp |
| `venc` | ENTIDAD | vencimiento | 🟡 INFERIDO | nombre_sp |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?vi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `?tes_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?vi`, `?tes_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conspagomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conspagomasivocre.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta pago y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_conspagomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INT` | L18 |
| `iExiste` | `INT` | L19 |
| `iIdRegistro` | `INT` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `cResultado` | `CHAR(15)` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `cMotivoRechazo` | `CHAR(100)` | L24 |
| `cFolio` | `CHAR(20)` | L25 |
| `mSaldo` | `money(14,2)` | L26 |
| `cTransacc` | `CHAR(4)` | L27 |
| `cDescTransaccion` | `CHAR(50)` | L28 |
| `mImportePago` | `money(14,2)` | L29 |
| `cComentario` | `CHAR(255)` | L30 |
| `cStatus` | `CHAR(1)` | L31 |
| `iRegistros` | `int` | L32 |
| `cNumCliente` | `char(20)` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_pago` | `bdicnweb` | no | SELECT | L78 |
| `sw_tr_cargamasiva_pago_hist` | `bdicnweb` | no | SELECT | L82 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L113 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L109 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L141 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_conspagomasivocre_hist`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conspagomasivocre_hist.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta pago y crédito (masivo, histórico/historial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_conspagomasivocre_hist(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INT` | L18 |
| `iExiste` | `INT` | L19 |
| `iIdRegistro` | `INT` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `cResultado` | `CHAR(15)` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `cMotivoRechazo` | `CHAR(100)` | L24 |
| `cFolio` | `CHAR(20)` | L25 |
| `mSaldo` | `money(14,2)` | L26 |
| `cTransacc` | `CHAR(4)` | L27 |
| `cDescTransaccion` | `CHAR(50)` | L28 |
| `mImportePago` | `money(14,2)` | L29 |
| `cComentario` | `CHAR(255)` | L30 |
| `cStatus` | `CHAR(1)` | L31 |
| `iRegistros` | `int` | L32 |
| `cNumCliente` | `char(20)` | L33 |
| `cCodRetReverso` | `CHAR(6)` | L34 |
| `cResultadoReverso` | `CHAR(15)` | L35 |
| `cComentarioReverso` | `CHAR(150)` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_pago_hist` | `bdicnweb` | no | SELECT | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L108 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `hist` | MODIF | histórico/historial | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consproductoscap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consproductoscap.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta productos y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consproductoscap(
  pIdProducto                  INT
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdProducto` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iIdProducto` | `INT` | L15 |
| `cTasaintvariable` | `CHAR(200)` | L16 |
| `cGatNominal` | `CHAR(10)` | L17 |
| `cGatReal` | `CHAR(10)` | L18 |
| `cComisionRel1` | `CHAR(200)` | L19 |
| `cComisionRel2` | `CHAR(200)` | L20 |
| `cMediosDisposicion` | `CHAR(200)` | L21 |
| `cLugEfectRetiros` | `CHAR(200)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_productoscaptacion` | `bdicnweb` | no | SELECT | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consproductosistemapagare`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consproductosistemapagare.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta productos y pagaré" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consproductosistemapagare(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveProducto               CHAR (4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveProducto` | `CHAR (4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `dFecha` | `DATE` | L16 |
| `cProducto` | `CHAR (4)` | L17 |
| `iNoCuentas` | `INTEGER` | L18 |
| `mCapAnterior` | `MONEY (18, 2)` | L19 |
| `mCapCalculado` | `MONEY (18, 2)` | L20 |
| `mCapActual` | `MONEY (18, 2)` | L21 |
| `mIntAnterior` | `MONEY (18, 2)` | L22 |
| `mIntCalculado` | `MONEY (18, 2)` | L23 |
| `mIntActual` | `MONEY (18, 2)` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_conciliainvglob` | `bdinvers` | ⚠️ sí | SELECT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pClaveProducto = '' OR pRegistros IS NULL OR pRecuperacion IS` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L83 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L93 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L100 | VALIDACIÓN_NULL | `IF pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?istema` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pagare` | ENTIDAD | pagaré | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?istema` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consproductosistemapagare_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consproductosistemapagare_totales.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta productos y pagaré (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consproductosistemapagare_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveProducto               CHAR (4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveProducto` | `CHAR (4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_conciliainvglob` | `bdinvers` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | VALIDACIÓN_NULL | `IF pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?istema` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pagare` | ENTIDAD | pagaré | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?istema` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consregresasaldosmovccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consregresasaldosmovccl.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta saldos, movimiento y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_conciliar_saldos_hist` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consregresasaldosmovccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pTipoProd                    INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pTipoProd` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cDescripcion` | `CHAR(60)` | L15 |
| `cNumCcl` | `CHAR(60)` | L16 |
| `cCargosDia` | `MONEY(18,2)` | L17 |
| `mAbonosDia` | `MONEY(18,2)` | L18 |
| `mSaldoInicioDia` | `MONEY(18,2)` | L19 |
| `mSaldoFinDia` | `MONEY(18,2)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_conciliar_saldos_hist` | `bdicred` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFecha IS NULL OR pTipoProd IS NULL THEN` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicred:sp_conciliar_saldos_hist";` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?resa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?resa` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consrempagbts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consrempagbts.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y beneficiarios" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consrempagbts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumConfirmacion             CHAR(11)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumConfirmacion` | `CHAR(11)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `cCodRetSp` | `CHAR(5)` | L27 |
| `cSucursal` | `CHAR(15)` | L28 |
| `cTerminal` | `CHAR(15)` | L29 |
| `cRTypeCd` | `CHAR(3)` | L30 |
| `cRIdentificacionNm` | `CHAR(20)` | L31 |
| `cRNombreCalle` | `CHAR(50)` | L32 |
| `cRNumExterior` | `CHAR(5)` | L33 |
| `cRNumInterior` | `CHAR(5)` | L34 |
| `cRDepartamento` | `CHAR(10)` | L35 |
| `cRColonia` | `CHAR(80)` | L36 |
| `cRCodigoPostal` | `CHAR(5)` | L37 |
| `cRMunicipioDelegacion` | `CHAR(50)` | L38 |
| `cRCiudad` | `CHAR(50)` | L39 |
| `cREstado` | `CHAR(50)` | L40 |
| `cRIssuerCountryCd` | `CHAR(3)` | L41 |
| `cRTelefono` | `CHAR(15)` | L42 |
| `cTipoPago` | `CHAR(1)` | L43 |
| `cRFechaNacimiento` | `CHAR(8)` | L44 |
| `dRFechaNacimiento` | `DATE` | L45 |
| `cRNacionalidad` | `CHAR(20)` | L46 |
| `cRPaisNacimiento` | `CHAR(50)` | L47 |
| `cFolioSucPayi` | `CHAR(20)` | L48 |
| `iCodRetorno` | `INTEGER` | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consrempag` | `bdisac` | ⚠️ sí | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L91 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumConfirmacion = '' OR pFecha IS NULL THEN` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L114 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetorno, 0, 'ERROR EN LA EJECUCION DEL SP bdisac:sp_consrempag';` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00244';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00244';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00244';` |  |
| L125 | FÓRMULA | `LET dRFechaNacimiento = TRIM(SUBSTRING(cRFechaNacimiento FROM 5 FOR 2)\|\|'/'\|\|SUBSTRING(cRFechaNa` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `?pag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pag` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportearqueosuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportearqueosuc.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportearqueosuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `dFecha` | `DATE` | L11 |
| `cNombre_reporte` | `CHAR(100)` | L12 |
| `dFecha_reporte` | `DATE` | L13 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |
| `cStatus` | `CHAR(1)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesarqueos` | `bdicnweb` | no | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L68 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreporte` | ACCION | consulta reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?arque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?uc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?arque`, `?uc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportearqueosuc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportearqueosuc_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportearqueosuc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cNombre_reporte` | `CHAR(100)` | L9 |
| `dFecha_reporte` | `DATE` | L10 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesarqueos` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreporte` | ACCION | consulta reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?arque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?uc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?arque`, `?uc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesaumlim_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesaumlim_mc.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes mc — Mesa de Control (aumento)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesaumlim_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `DATE` | L10 |
| `cNombre_reporte` | `CHAR(100)` | L11 |
| `dFecha_reporte` | `DATE` | L12 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesaumlimcred_mc` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `aum` | MODIF | aumento | 🟡 INFERIDO | nombre_sp |
| `?lim_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?lim_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesaumlim_mc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesaumlim_mc_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes mc — Mesa de Control (aumento, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesaumlim_mc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cNombre_reporte` | `CHAR(100)` | L9 |
| `dFecha_reporte` | `DATE` | L10 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesaumlimcred_mc` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `aum` | MODIF | aumento | 🟡 INFERIDO | nombre_sp |
| `?lim_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?lim_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesctasinactivasart61`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesctasinactivasart61.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 399 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes cuentas inactivas · Art. 61 LIC" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesctasinactivasart61(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `DATE` | L10 |
| `cNombre_reporte` | `CHAR(100)` | L11 |
| `dFecha_reporte` | `DATE` | L12 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesart61` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `ctasinactivas` | ENTIDAD | cuentas inactivas | 🔵 CONVENCIÓN | nombre_sp |
| `art61` | REG | Art. 61 LIC (cuentas inactivas cuyos saldos, tras años sin m | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consreportesctasinactivasart61_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesctasinactivasart61_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 398 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes cuentas inactivas (totales) · Art. 61 LIC" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesctasinactivasart61_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cNombre_reporte` | `CHAR(100)` | L9 |
| `dFecha_reporte` | `DATE` | L10 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesart61` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `ctasinactivas` | ENTIDAD | cuentas inactivas | 🔵 CONVENCIÓN | nombre_sp |
| `art61` | REG | Art. 61 LIC (cuentas inactivas cuyos saldos, tras años sin m | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consreportesentradasalida`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesentradasalida.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes salida" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesentradasalida(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `dFecha` | `DATE` | L11 |
| `cNombre_reporte` | `CHAR(100)` | L12 |
| `dFecha_reporte` | `DATE` | L13 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |
| `cStatus` | `CHAR(1)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesentradasalida` | `bdicnweb` | no | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L68 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?entrada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `salida` | ENTIDAD | salida | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?entrada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesentradasalida_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesentradasalida_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes salida (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesentradasalida_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cNombre_reporte` | `CHAR(100)` | L9 |
| `dFecha_reporte` | `DATE` | L10 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesentradasalida` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?entrada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `salida` | ENTIDAD | salida | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?entrada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesgeneradosmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesgeneradosmc.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes OS — Originación de Solicitudes y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesgeneradosmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `DATE` | L10 |
| `cNombre_reporte` | `CHAR(100)` | L11 |
| `dFecha_reporte` | `DATE` | L12 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesmc` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesgeneradosmc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesgeneradosmc_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes OS — Originación de Solicitudes y mc — Mesa de Control (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesgeneradosmc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cNombre_reporte` | `CHAR(100)` | L9 |
| `dFecha_reporte` | `DATE` | L10 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesmc` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesgeneradosmsi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesgeneradosmsi.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes OS — Originación de Solicitudes y meses sin intereses" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesgeneradosmsi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `DATE` | L10 |
| `cNombre_reporte` | `CHAR(100)` | L11 |
| `dFecha_reporte` | `DATE` | L12 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlreportesmsi` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesgeneradosmsi_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesgeneradosmsi_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes OS — Originación de Solicitudes y meses sin intereses (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesgeneradosmsi_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cNombre_reporte` | `CHAR(100)` | L9 |
| `dFecha_reporte` | `DATE` | L10 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlreportesmsi` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesgeneradospuntoscomp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesgeneradospuntoscomp.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 280 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes OS — Originación de Solicitudes y puntos (complemento)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesgeneradospuntoscomp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `DATE` | L10 |
| `cNombre_reporte` | `CHAR(100)` | L11 |
| `dFecha_reporte` | `DATE` | L12 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportespuntoscomp` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportesgeneradospuntoscomp_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportesgeneradospuntoscomp_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 279 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes OS — Originación de Solicitudes y puntos (complemento, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportesgeneradospuntoscomp_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cNombre_reporte` | `CHAR(100)` | L9 |
| `dFecha_reporte` | `DATE` | L10 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportespuntoscomp` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportessolicitudessupmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportessolicitudessupmc.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes solicitudes y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportessolicitudessupmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `DATE` | L10 |
| `cNombre_reporte` | `CHAR(100)` | L11 |
| `dFecha_reporte` | `DATE` | L12 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesolsupmc` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?sup` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sup` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consreportessolicitudessupmc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consreportessolicitudessupmc_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes solicitudes y mc — Mesa de Control (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consreportessolicitudessupmc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cNombre_reporte` | `CHAR(100)` | L9 |
| `dFecha_reporte` | `DATE` | L10 |
| `dHr_reporte` | `DATETIME HOUR TO SECOND` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesolsupmc` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?sup` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sup` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consresumenmovcheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consresumenmovcheques.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=11 / fan_out=14 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento y cheques" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consresumenmovcheques(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `dFechaHoy` | `DATE` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |
| `iNumCheque` | `INTEGER` | L14 |
| `dFechaPresenta` | `DATE` | L15 |
| `iRegDigitalizados` | `INTEGER` | L16 |
| `iOperados` | `INTEGER` | L17 |
| `iDigitalizados` | `INTEGER` | L18 |
| `iPorRecibir` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L58 |
| `sc_contch_hist` | `bdicheq` | ⚠️ sí | SELECT | L61 |
| `cce_cheques_img` | `bditef` | ⚠️ sí | SELECT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | FÓRMULA | `LET iDigitalizados = iDigitalizados + iRegDigitalizados;` |  |
| L88 | FÓRMULA | `LET iPorRecibir = iOperados - iDigitalizados;` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?resumen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mov` | ENTIDAD | movimiento | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?resumen` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consretiromasivocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consretiromasivocap.sql` |
| **LOC (1er CREATE)** | 154 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta retiro y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consretiromasivocap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INT` | L19 |
| `iExiste` | `INT` | L20 |
| `iIdRegistro` | `INT` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cResultado` | `CHAR(15)` | L23 |
| `cCodRetSp` | `CHAR(5)` | L24 |
| `cMotivoRechazo` | `CHAR(100)` | L25 |
| `cFolio` | `CHAR(20)` | L26 |
| `mSaldo` | `money(14,2)` | L27 |
| `cTransacc` | `CHAR(4)` | L28 |
| `cDoctoChq` | `INT` | L29 |
| `mDeposito` | `money(14,2)` | L30 |
| `cReferencia` | `CHAR(255)` | L31 |
| `cStatus` | `CHAR(1)` | L32 |
| `iRegistros` | `int` | L33 |
| `cNumCliente` | `char(20)` | L34 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_retiro` | `bdicnweb` | no | SELECT | L76 |
| `sw_tr_cargamasiva_retiro_hist` | `bdicnweb` | no | SELECT | L80 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L119 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L115 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L136 | FÓRMULA | `LET iRegistros =  iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_conssolsupconstatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conssolsupconstatus.sql` |
| **LOC (1er CREATE)** | 186 |
| **Callgraph** | ✅ fan_in=0 / fan_out=122 |
| **Deps concatenadas** | 380 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rptsolsup_conest` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_conssolsupconstatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `cCodRetSp` | `CHAR(6)` | L24 |
| `cDescCodRetSp` | `CHAR(80)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `iExiste` | `INTEGER` | L27 |
| `cEjecutivo` | `CHAR(8)` | L28 |
| `cEmpresa` | `CHAR(3)` | L29 |
| `cNumSolicitud` | `CHAR(20)` | L30 |
| `cNumCte` | `CHAR(20)` | L31 |
| `cNombreCte` | `CHAR(180)` | L32 |
| `cSucursal` | `CHAR(4)` | L33 |
| `cNumProducto` | `CHAR(4)` | L34 |
| `dLineaAsignada` | `DECIMAL(18,2)` | L35 |
| `cStatus` | `CHAR(3)` | L36 |
| `dFechaCambioStatus` | `DATE` | L37 |
| `dHoraCambioStatus` | `DATETIME YEAR TO SECOND` | L38 |
| `cNumCteRef` | `CHAR(20)` | L39 |
| `dEficiencia` | `DECIMAL(5,2)` | L40 |
| `iMesesHistoria` | `INTEGER` | L41 |
| `dVdoCoppel` | `DECIMAL(18,2)` | L42 |
| `cPuntualidad` | `CHAR(2)` | L43 |
| `dBcScore` | `DECIMAL(5,2)` | L44 |
| `dScorePropietario` | `DECIMAL(5,2)` | L45 |
| `cTpoMovto` | `CHAR(20)` | L46 |
| `dFechaMinSel` | `DATE` | L47 |
| *…2 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L108 |
| `sp_rptsolsup_conest` | `bdisolic` | ⚠️ sí | L129 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L91 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pStatus = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL ` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L117 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L135 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_rptsolsup_conest';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L153 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?sup` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?tatus` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sup`, `?tatus` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conssolsupconstatus_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conssolsupconstatus_totales.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=0 / fan_out=121 |
| **Deps concatenadas** | 379 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_conssolsupconstatus_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `cEjecutivo` | `CHAR(8)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `dFechaMinSel` | `DATE` | L13 |
| `dFechaMaxSel` | `DATE` | L14 |
| `iNumRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultasolsupstatus` | `bdicnweb` | no | SELECT | L47 |
| `sw_statusconsultasolsupstatus` | `bdicnweb` | no | DELETE | L47 |
| `sw_statusconsultasolsupstatus` | `bdicnweb` | no | INSERT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L69 |
| `sp_rptsolsup_conest_total` | `bdisolic` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L55 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pStatus = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL ` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L96 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_rptsolsup_conest_to` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?sup` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?tatus_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sup`, `?tatus_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conssolsupdetallesolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conssolsupdetallesolicitud.sql` |
| **LOC (1er CREATE)** | 163 |
| **Callgraph** | ✅ fan_in=0 / fan_out=120 |
| **Deps concatenadas** | 378 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rptsolsup_detsol` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_conssolsupdetallesolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud · `solicitud`=solicitud | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `cDescCodRetSp` | `CHAR(80)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `iExiste` | `INTEGER` | L23 |
| `cEjecutivo` | `CHAR(8)` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `cNumSolicitud` | `CHAR(20)` | L26 |
| `cNumCte` | `CHAR(20)` | L27 |
| `cNombreCte` | `CHAR(180)` | L28 |
| `cSucursal` | `CHAR(4)` | L29 |
| `dFechaSolicitud` | `DATE` | L30 |
| `cStatus` | `CHAR(3)` | L31 |
| `dFechaIngresoStatus` | `DATE` | L32 |
| `dHoraIngresoStatus` | `DATETIME YEAR TO SECOND` | L33 |
| `dFechaReenvio` | `DATE` | L34 |
| `dHoraReenvio` | `DATETIME YEAR TO SECOND` | L35 |
| `cStatusActual` | `CHAR(3)` | L36 |
| `cTipoMovimiento` | `CHAR(5)` | L37 |
| `cNombreAnalista` | `CHAR(180)` | L38 |
| `dFechaMinSel` | `DATE` | L39 |
| `dFechaMaxSel` | `DATE` | L40 |
| `iRecuperacion` | `INTEGER` | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L92 |
| `sp_rptsolsup_detsol` | `bdisolic` | ⚠️ sí | L113 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L101 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L118 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_rptsolsup_detsol';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L133 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `upd` | ACCION | actualiza (update) | 🟡 INFERIDO | nombre_sp |
| `?etalle` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?etalle` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conssolsupdetallesolicitud_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conssolsupdetallesolicitud_totales.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ✅ fan_in=0 / fan_out=119 |
| **Deps concatenadas** | 377 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_conssolsupdetallesolicitud_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud · `solicitud`=solicitud | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iExiste` | `INTEGER` | L11 |
| `cEjecutivo` | `CHAR(8)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `dFechaMinSel` | `DATE` | L14 |
| `dFechaMaxSel` | `DATE` | L15 |
| `iNumRegistros` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultasolsupdetsol` | `bdicnweb` | no | SELECT | L48 |
| `sw_statusconsultasolsupdetsol` | `bdicnweb` | no | DELETE | L48 |
| `sw_statusconsultasolsupdetsol` | `bdicnweb` | no | INSERT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L69 |
| `sp_rptsolsup_detsol_total` | `bdisolic` | ⚠️ sí | L101 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L105 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_rptsolsup_detsol_to` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `upd` | ACCION | actualiza (update) | 🟡 INFERIDO | nombre_sp |
| `?etalle` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?etalle` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conssolsupoperacionmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conssolsupoperacionmc.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=118 |
| **Deps concatenadas** | 376 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rptsolsup_opermc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_conssolsupoperacionmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `cDescCodRetSp` | `CHAR(80)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iExiste` | `INTEGER` | L15 |
| `cEjecutivo` | `CHAR(8)` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cFecha` | `CHAR(10)` | L18 |
| `iGeneradas` | `INTEGER` | L19 |
| `iNoAtendidas` | `INTEGER` | L20 |
| `iPorcNoAtendidas` | `INTEGER` | L21 |
| `iAtendidas` | `INTEGER` | L22 |
| `iPorcAtendidas` | `INTEGER` | L23 |
| `dFechaMinSel` | `DATE` | L24 |
| `dFechaMaxSel` | `DATE` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |
| `sp_rptsolsup_opermc` | `bdisolic` | ⚠️ sí | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L55 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL THEN` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L82 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_rptsolsup_opermc';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L91 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?sup` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `operacion` | ACCION | operación | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sup` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conssolsupoperacionmc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conssolsupoperacionmc_totales.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=117 |
| **Deps concatenadas** | 375 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud y mc — Mesa de Control (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_conssolsupoperacionmc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `cEjecutivo` | `CHAR(8)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cFecha` | `CHAR(10)` | L13 |
| `iGeneradas` | `INTEGER` | L14 |
| `iNoAtendidas` | `INTEGER` | L15 |
| `iPorcNoAtendidas` | `INTEGER` | L16 |
| `iAtendidas` | `INTEGER` | L17 |
| `iPorcAtendidas` | `INTEGER` | L18 |
| `dFechaMinSel` | `DATE` | L19 |
| `dFechaMaxSel` | `DATE` | L20 |
| `iNumRegistros` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultasolsupoperacionmc` | `bdicnweb` | no | SELECT | L59 |
| `sw_statusconsultasolsupoperacionmc` | `bdicnweb` | no | DELETE | L59 |
| `sw_statusconsultasolsupoperacionmc` | `bdicnweb` | no | INSERT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L80 |
| `sp_rptsolsup_opermc_totales` | `bdisolic` | ⚠️ sí | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L67 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL THEN` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L114 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_rptsolsup_opermc_to` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?sup` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `operacion` | ACCION | operación | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sup` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conssolsupreenvioanalista`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conssolsupreenvioanalista.sql` |
| **LOC (1er CREATE)** | 148 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 381 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud y analista" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rptsolsup_reean` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_conssolsupreenvioanalista(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `cCodRetSp` | `CHAR(6)` | L17 |
| `cDescCodRetSp` | `CHAR(80)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `iExiste` | `INTEGER` | L20 |
| `cEjecutivo` | `CHAR(8)` | L21 |
| `cEmpresa` | `CHAR(3)` | L22 |
| `cNumEmpleado` | `CHAR(10)` | L23 |
| `cNomEmpleado` | `CHAR(180)` | L24 |
| `iTotalAtendidas` | `INTEGER` | L25 |
| `iPorcAtendidas` | `INTEGER` | L26 |
| `iTotalTDC` | `INTEGER` | L27 |
| `iPorcTDC` | `INTEGER` | L28 |
| `iTotalCoppel` | `INTEGER` | L29 |
| `iPorcCoppel` | `INTEGER` | L30 |
| `iTotalPP` | `INTEGER` | L31 |
| `iPorcPP` | `INTEGER` | L32 |
| `dFechaMinSel` | `DATE` | L33 |
| `dFechaMaxSel` | `DATE` | L34 |
| `iRecuperacion` | `INTEGER` | L35 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L83 |
| `sp_rptsolsup_reean` | `bdisolic` | ⚠️ sí | L102 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pRegistros IS` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L91 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L107 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_rptsolsup_reean';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L118 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?supre` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `analista` | ENTIDAD | analista | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?supre` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_conssolsupreenvioanalista_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_conssolsupreenvioanalista_totales.sql` |
| **LOC (1er CREATE)** | 162 |
| **Callgraph** | ✅ fan_in=0 / fan_out=116 |
| **Deps concatenadas** | 374 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud y analista (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_conssolsupreenvioanalista_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `cEjecutivo` | `CHAR(8)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cNumEmpleado` | `CHAR(10)` | L13 |
| `cNomEmpleado` | `CHAR(180)` | L14 |
| `iTotalAtendidas` | `INTEGER` | L15 |
| `iPorcAtendidas` | `INTEGER` | L16 |
| `iTotalTDC` | `INTEGER` | L17 |
| `iPorcTDC` | `INTEGER` | L18 |
| `iTotalCoppel` | `INTEGER` | L19 |
| `iPorcCoppel` | `INTEGER` | L20 |
| `iTotalPP` | `INTEGER` | L21 |
| `iPorcPP` | `INTEGER` | L22 |
| `dFechaMinSel` | `DATE` | L23 |
| `dFechaMaxSel` | `DATE` | L24 |
| `iNumRegistros` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultasolsupreenvioanalista` | `bdicnweb` | no | SELECT | L67 |
| `sw_statusconsultasolsupreenvioanalista` | `bdicnweb` | no | DELETE | L67 |
| `sw_statusconsultasolsupreenvioanalista` | `bdicnweb` | no | INSERT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |
| `sp_rptsolsup_reean_totales` | `bdisolic` | ⚠️ sí | L118 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet 			   = '00000';` |  |
| L75 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL THEN` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L102 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L122 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_rptsolsup_reean_tot` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?supre` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `analista` | ENTIDAD | analista | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?supre` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_constabonotransfercap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constabonotransfercap.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta abono, transferencia y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_trxsabonotransfersoc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_constabonotransfercap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cTransacc` | `CHAR(4)` | L10 |
| `cDescripcion` | `CHAR(80)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_trxsabonotransfersoc` | `bdicnweb` | no | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_trxsabonotransfersoc';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00710';` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_constasareferencia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constasareferencia.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 93 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta tasa y referencia" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_constasareferencia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTasa                        CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTasa` | `CHAR(8)` | `tasa`=tasa (de interés) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cTasa` | `CHAR(8)` | L13 |
| `cDescripcion` | `CHAR(30)` | L14 |
| `cRangoFecha` | `CHAR(1)` | L15 |
| `cAplicaPromedio` | `CHAR(1)` | L16 |
| `cTasaReferen` | `CHAR(8)` | L17 |
| `cManejaTasaReferen` | `CHAR(1)` | L18 |
| `iNumRegistros` | `INTEGER` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tiptasa` | `bdinteg` | ⚠️ sí | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `referencia` | ENTIDAD | referencia | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_constasatabular`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constasatabular.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 92 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta tasa" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_constasatabular(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripcion                 CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripcion` | `CHAR(30)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cTasa` | `CHAR(8)` | L12 |
| `cDescripcion` | `CHAR(30)` | L13 |
| `cFecha` | `DATE` | L14 |
| `cValor` | `DECIMAL(9,6)` | L15 |
| `cPromedio` | `DECIMAL(9,6)` | L16 |
| `iNumRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |
| `cDesConsulta` | `CHAR(30)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tiptasa` | `bdinteg` | ⚠️ sí | SELECT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tabular` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tabular` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_constatuscodebts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constatuscodebts.sql` |
| **LOC (1er CREATE)** | 40 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código y beneficiarios" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_constatuscodebts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDansStatusCode              CHAR(3)
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDansStatusCode` | `CHAR(3)` | `cod`=código | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRet` | `CHAR(5)` | L6 |
| `cMensaje` | `CHAR(80)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_bts_catstatusremesas` | `bdisac` | ⚠️ sí | SELECT | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET  cCodRet = '00000';` |  |
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?tatus` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tatus`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_constdepositoctstransfercap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constdepositoctstransfercap.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta depósito, OS — Originación de Solicitudes, transferencia y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consdoctatransfersoc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_constdepositoctstransfercap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `dSaldoCuenta` | `DECIMAL(16,2)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_consdoctatransfersoc` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consdoctatransfersoc';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00710';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?itocts` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t`, `?itocts` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_constipooperacionesinternet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constipooperacionesinternet.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta operaciones y interés (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_constipooperacionesinternet(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodret` | `CHAR(6)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdOperacion` | `CHAR(4)` | L8 |
| `cConcepto` | `CHAR(50)` | L9 |
| `iExiste` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bpioperaciones` | `bdinteg` | ⚠️ sí | SELECT | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodret = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodret = '00003';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodret = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?ernet` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ernet` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_constotalbloqueomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constotalbloqueomasivocre.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito (total, masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_constotalbloqueomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iIdRegistro` | `INTEGER` | L7 |
| `cNoCuenta` | `CHAR(20)` | L8 |
| `cNoCliente` | `CHAR(20)` | L9 |
| `cResultado` | `CHAR(15)` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `cMotivoRechazo` | `CHAR(80)` | L12 |
| `mSaldo` | `MONEY(14,2)` | L13 |
| `cNombreCliente` | `CHAR(107)` | L14 |
| `iTipoBloqueo` | `INTEGER` | L15 |
| `cCausaBloqueo` | `CHAR(2)` | L16 |
| `dFechaBloqueo` | `DATE` | L17 |
| `cEmpleado` | `CHAR(8)` | L18 |
| `cNombreEmpleado` | `CHAR(45)` | L19 |
| `cStatusRegistro` | `CHAR(1)` | L20 |
| `iExiste` | `INTEGER` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_bloqueocre` | `bdicnweb` | no | SELECT | L69 |
| `sw_tr_cargamasiva_bloqueocre_hist` | `bdicnweb` | no | SELECT | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL THEN` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L102 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_constotaldesbloqueomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constotaldesbloqueomasivocre.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito (total, masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_constotaldesbloqueomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iIdRegistro` | `INTEGER` | L7 |
| `cNoCuenta` | `CHAR(20)` | L8 |
| `cNoCliente` | `CHAR(20)` | L9 |
| `cResultado` | `CHAR(15)` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `cMotivoRechazo` | `CHAR(80)` | L12 |
| `mSaldo` | `MONEY(14,2)` | L13 |
| `cNombreCliente` | `CHAR(107)` | L14 |
| `dFechaDesbloqueo` | `DATE` | L15 |
| `cEmpleado` | `CHAR(8)` | L16 |
| `cNombreEmpleado` | `CHAR(45)` | L17 |
| `cStatusRegistro` | `CHAR(1)` | L18 |
| `iExiste` | `INTEGER` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_desbloqueocre` | `bdicnweb` | no | SELECT | L65 |
| `sw_tr_cargamasiva_desbloqueocre_hist` | `bdicnweb` | no | SELECT | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L94 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `desbloqueo` | ACCION | desbloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_constotalesreversocargocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constotalesreversocargocre.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=4 / fan_out=3 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cargo y crédito (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_constotalesreversocargocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodret` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |
| `iRegReversados` | `INTEGER` | L11 |
| `iRegNoReversados` | `INTEGER` | L12 |
| `iExiste` | `INTEGER` | L13 |
| `mImporte` | `MONEY(14,2)` | L14 |
| `iOpcion` | `SMALLINT` | L15 |
| `dFechaCarga` | `DATETIME YEAR TO SECOND` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_cargo_hist` | `bdicnweb` | no | SELECT | L47 |
| `sw_tr_totales_reversos` | `bdicnweb` | no | SELECT | L77 |
| `sw_tr_totales_reversos` | `bdicnweb` | no | INSERT | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | FÓRMULA | `LET iOpcion = 0;  -- Es un insert de los datos del reverso` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_constotalesreversopagocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constotalesreversopagocre.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=7 / fan_out=3 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta pago y crédito (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_constotalesreversopagocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodret` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |
| `iRegReversados` | `INTEGER` | L11 |
| `iRegNoReversados` | `INTEGER` | L12 |
| `iExiste` | `INTEGER` | L13 |
| `mImporte` | `MONEY(14,2)` | L14 |
| `iOpcion` | `SMALLINT` | L15 |
| `dFechaCarga` | `DATETIME YEAR TO SECOND` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_pago_hist` | `bdicnweb` | no | SELECT | L46 |
| `sw_tr_totales_reversos` | `bdicnweb` | no | SELECT | L77 |
| `sw_tr_totales_reversos` | `bdicnweb` | no | INSERT | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | FÓRMULA | `LET iOpcion = 0;  -- Es un insert de los datos del reverso` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_constotalmantolineasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constotalmantolineasmasivocre.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta línea y crédito (total, anterior, masivo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_constotalmantolineasmasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_mantolineascredito` | `bdicnweb` | no | SELECT | L41 |
| `sw_tr_cargamasiva_mantolineascredito_hist` | `bdicnweb` | no | SELECT | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `linea` | ENTIDAD | línea (de crédito) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?o`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_constparametrosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_constparametrosccl.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta ro — Rol Operativo y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (tipo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_constparametrosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cHistoricoCcl` | `CHAR(90)` | L12 |
| `cBitacoraCcl` | `CHAR(90)` | L13 |
| `cMovimientosCcl` | `CHAR(90)` | L14 |
| `cArchivosAix` | `CHAR(90)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `bditarjeta` | `bdicnweb` | no | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_consultamttopara` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00100';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?aramet` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aramet`, `ro`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consul_direc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consul_direc.sql` |
| **LOC (1er CREATE)** | 326 |
| **Callgraph** | ✅ fan_in=28 / fan_out=25 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consul_direc(
  eEmpresa                     CHAR(3)
  eNumCte                      CHAR(20)
  eTpDirec                     INTEGER
) RETURNING char(5),int,char(1),char(40),char(60),char(40),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `eEmpresa` | `CHAR(3)` | — | — |
| `eNumCte` | `CHAR(20)` | — | — |
| `eTpDirec` | `INTEGER` | `rec`=recepción / recibe | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsqlerr` | `integer` | L13 |
| `vsecuencia` | `int` | L14 |
| `vnumerocalle` | `int` | L15 |
| `vnumerocolonia` | `int` | L16 |
| `vcodret` | `char(5)` | L17 |
| `vtipo_dir` | `char(1)` | L18 |
| `vcalle` | `char(40)` | L19 |
| `vcolonia` | `char(60)` | L20 |
| `ventre_calles` | `char(40)` | L21 |
| `vpais` | `char(3)` | L22 |
| `vestado` | `char(2)` | L23 |
| `vciudad` | `char(3)` | L24 |
| `vmunicipio` | `char(5)` | L25 |
| `vcod_postal` | `char(5)` | L26 |
| `vapart_postal` | `char(11)` | L27 |
| `vtipo_telef1` | `char(1)` | L28 |
| `vtelefono1` | `char(13)` | L29 |
| `vtipo_telef2` | `char(1)` | L30 |
| `vtelefono2` | `char(13)` | L31 |
| `vtipo_telef3` | `char(1)` | L32 |
| `vtelefono3` | `char(13)` | L33 |
| `vextension` | `char(5)` | L34 |
| `vestado_inegi` | `char(2)` | L35 |
| `vmunicipio_inegi` | `char(3)` | L36 |
| `vlocalidad_inegi` | `char(4)` | L37 |
| *…28 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_direcciones` | `bdicnweb` | no | SELECT | L142 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L159 |
| `si_estados` | `bdinteg` | ⚠️ sí | SELECT | L170 |
| `si_catciudades` | `bdinteg` | ⚠️ sí | SELECT | L175 |
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L180 |
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L186 |
| `si_catdomicilios` | `bdinteg` | ⚠️ sí | SELECT | L192 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L252 | VALIDACIÓN_NULL | `IF vsecuencia is null THEN LET vsecuencia = 0; END IF;` |  |
| L253 | VALIDACIÓN_NULL | `IF vtipo_dir is null THEN LET vtipo_dir = ' '; END IF;` |  |
| L254 | VALIDACIÓN_NULL | `IF vcalle is null THEN LET vcalle = ' '; END IF;` |  |
| L255 | VALIDACIÓN_NULL | `IF vcolonia is null THEN LET vcolonia = ' '; END IF;` |  |
| L256 | VALIDACIÓN_NULL | `IF ventre_calles is null THEN LET ventre_calles = ' '; END IF;` |  |
| L257 | VALIDACIÓN_NULL | `IF vpais is null THEN LET vpais = ' '; END IF;` |  |
| L258 | VALIDACIÓN_NULL | `IF vestado is null THEN LET vestado = ' '; END IF;` |  |
| L259 | VALIDACIÓN_NULL | `IF vciudad is null THEN LET vciudad = ' '; END IF;` |  |
| L260 | VALIDACIÓN_NULL | `IF vmunicipio is null THEN LET vmunicipio = ' '; END IF;` |  |
| L261 | VALIDACIÓN_NULL | `IF vcod_postal is null THEN LET vcod_postal = ' '; END IF;` |  |
| L262 | VALIDACIÓN_NULL | `IF vapart_postal is null THEN LET vapart_postal = ' '; END IF;` |  |
| L263 | VALIDACIÓN_NULL | `IF vtipo_telef1 is null THEN LET vtipo_telef1 = ' '; END IF;` |  |
| L264 | VALIDACIÓN_NULL | `IF vtelefono1 is null THEN LET vtelefono1 = ' '; END IF;` |  |
| L265 | VALIDACIÓN_NULL | `IF vtipo_telef2 is null THEN LET vtipo_telef2 = ' '; END IF;` |  |
| L266 | VALIDACIÓN_NULL | `IF vtelefono2 is null THEN LET vtelefono2 = ' '; END IF;` |  |
| L267 | VALIDACIÓN_NULL | `IF vtipo_telef3 is null THEN LET vtipo_telef3 = ' '; END IF;` |  |
| L268 | VALIDACIÓN_NULL | `IF vtelefono3 is null THEN LET vtelefono3 = ' '; END IF;` |  |
| L269 | VALIDACIÓN_NULL | `IF vextension is null THEN LET vextension = ' '; END IF;` |  |
| L270 | VALIDACIÓN_NULL | `IF vestado_inegi is null THEN LET vestado_inegi = ' '; END IF;` |  |
| L271 | VALIDACIÓN_NULL | `IF vmunicipio_inegi is null THEN LET vmunicipio_inegi = ' '; END IF;` |  |
| L272 | VALIDACIÓN_NULL | `IF vlocalidad_inegi is null THEN LET vlocalidad_inegi = ' '; END IF;` |  |
| L273 | VALIDACIÓN_NULL | `IF vnumerociudad is null THEN LET vnumerociudad = 0; END IF;` |  |
| L274 | VALIDACIÓN_NULL | `IF vnumeroextcalle is null THEN LET vnumeroextcalle = ' '; END IF;` |  |
| L275 | VALIDACIÓN_NULL | `IF vnumerointcalle is null THEN LET vnumerointcalle = ' '; END IF;` |  |
| L276 | VALIDACIÓN_NULL | `IF vdepartamento is null THEN LET vdepartamento = ' '; END IF;` |  |
| L277 | VALIDACIÓN_NULL | `IF vnumerocalle is null THEN LET vnumerocalle = 0; END IF;` |  |
| L278 | VALIDACIÓN_NULL | `IF vnumerocolonia is null THEN LET vnumerocolonia = 0; END IF;` |  |
| L279 | VALIDACIÓN_NULL | `IF vpuntocardinal is null THEN LET vpuntocardinal = ' '; END IF;` |  |
| L280 | VALIDACIÓN_NULL | `IF vunidadhabitac is null THEN LET vunidadhabitac = ' '; END IF;` |  |
| L281 | VALIDACIÓN_NULL | `IF vmanzana is null THEN LET vmanzana = 0; END IF;` |  |
| | *…18 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul_di` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul_di` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_admintransaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_admintransaciones.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos y [polisemia] Transferencia" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_admintransaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pSistemaCuenta               CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cTransaccion` | `CHAR (4)` | L9 |
| `cDescripcion` | `CHAR (50)` | L10 |
| `bRegulatorios` | `BOOLEAN` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L67 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pTipoOperacion = '' OR pSistemaCuenta = '' OR pRegistros IS ` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00037';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L72 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L84 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `?aciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_claves_retiro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_claves_retiro.sql` |
| **LOC (1er CREATE)** | 156 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 72 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clave y retiro" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_claves_retiro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFoliooperacion              CHAR(100)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pTarjeta                     CHAR(19)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pStatus                      CHAR(1)
  pIp                          CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFoliooperacion` | `CHAR(100)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTarjeta` | `CHAR(19)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pIp` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `iCodRetSp` | `INTEGER` | L21 |
| `iNoRegistros` | `INTEGER` | L23 |
| `iRegistros` | `INTEGER` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |
| `cFoliooperacion` | `CHAR(100)` | L27 |
| `dAltaFecha` | `DATE` | L28 |
| `cCliente` | `CHAR(20)` | L29 |
| `cCuenta` | `CHAR(20)` | L30 |
| `cTarjeta` | `CHAR(20)` | L31 |
| `cMonto` | `INTEGER` | L32 |
| `cStatus` | `CHAR(2)` | L33 |
| `cDescStatus` | `CHAR(40)` | L34 |
| `cAccion` | `CHAR(50)` | L35 |
| `cEntidad` | `CHAR(100)` | L36 |
| `vCanalGeneracion` | `CHAR(10)` | L37 |
| `vCanalCobro` | `CHAR(10)` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L100 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L81 |
| `sp_bitacora` | `bdicnweb` | no | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L70 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L108 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L128 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_claves_retiro_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_claves_retiro_totales.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clave y retiro (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_claves_retiro_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFoliooperacion              CHAR(100)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pTarjeta                     CHAR(19)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pStatus                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFoliooperacion` | `CHAR(100)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTarjeta` | `CHAR(19)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pStatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_desactualizadas_buro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_desactualizadas_buro.sql` |
| **LOC (1er CREATE)** | 262 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal y Buró de Crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_desactualizadas_buro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(4)
  pStatus                      CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `iSqlErr` | `INTEGER` | L36 |
| `iNoRegistros` | `INTEGER` | L37 |
| `cDuplicada` | `CHAR(2)` | L38 |
| `cNivelDesactualizada` | `CHAR(3)` | L39 |
| `cMemberCode` | `CHAR(10)` | L40 |
| `cMemberKob` | `CHAR(2)` | L41 |
| `cNumCredito` | `CHAR(20)` | L42 |
| `cFechaReporte` | `CHAR(8)` | L43 |
| `cIdExpediente` | `CHAR(10)` | L44 |
| `cRfc` | `CHAR(20)` | L45 |
| `cApellidoPaterno` | `CHAR(30)` | L46 |
| `cApellidoMaterno` | `CHAR(30)` | L47 |
| `cApellidoAdicional` | `CHAR(30)` | L48 |
| `cPrimerNombre` | `CHAR(30)` | L49 |
| `cSegundoNombre` | `CHAR(30)` | L50 |
| `cFechaApertura` | `CHAR(8)` | L51 |
| `cTipoContrato` | `CHAR(2)` | L52 |
| `cTipoCuenta` | `CHAR(1)` | L53 |
| `cLimiteCredito` | `CHAR(20)` | L54 |
| `cHistoricoPago` | `CHAR(128)` | L55 |
| `cIdInterno` | `CHAR(20)` | L56 |
| `cClaveObservacion` | `CHAR(10)` | L57 |
| `cFormaPago` | `CHAR(2)` | L58 |
| `cSaldoActual` | `CHAR(20)` | L59 |
| *…12 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L158 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L128 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha = '' OR pStatus = '' OR pRegistros IS NULL OR pRecuper` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L178 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L189 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L219 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L230 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `?tualizadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `buro` | ENTIDAD | Buró de Crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_de`, `?tualizadas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_documentos_presentados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_documentos_presentados.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Propósito inferido** | "consulta documentos (presentado)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_presenta2`, `sp_cons_presenta2_totales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_documentos_presentados(
  pBandera                     CHAR(2)
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfechapre                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret ,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfechapre` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_codret` | `char(5)` | L13 |
| `v_banco` | `char(100)` | L14 |
| `v_cuenta` | `char(11)` | L15 |
| `v_numcheque` | `char(7)` | L16 |
| `v_monto` | `decimal(16,2)` | L17 |
| `v_sucursal` | `char(45)` | L18 |
| `v_ctadeposito` | `char(20)` | L19 |
| `v_nombrecte` | `char(100)` | L20 |
| `v_presentado` | `char(1)` | L21 |
| `v_noregistros` | `INTEGER` | L22 |
| `sql_err` | `integer` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_cons_presenta2` | `bditef` | ⚠️ sí | L54 |
| `sp_cons_presenta2_totales` | `bditef` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET v_codret = '00000';` |  |
| L58 | CÓDIGO_RETORNO | `LET v_codret = '00000';` |  |
| L68 | CÓDIGO_RETORNO | `LET v_codret = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `documentos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `presentado` | MODIF | presentado (a cobro) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_imagenes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_imagenes.sql` |
| **LOC (1er CREATE)** | 365 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta imágenes" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 7 llamada(s): `sp_ope_obtienecheques`, `sp_ope_obtienecheques_totales`, `sp_cce_consultausuariosaut` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_imagenes(
  pBandera                     CHAR(2)
  pClaveBanco                  CHAR(3)
  pCuentaDeposito              CHAR(20)
  pNumeroCheque                CHAR(7)
  pFechaPresenta               DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pNumEjecut                   INTEGER
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pTipoSubOperacion            CHAR(1)
  pNumeroCuenta                CHAR(20)
  pNumeroCliente               CHAR(20)
  pMonto                       DECIMAL(16,2)
  pSucursal                    CHAR(4)
  pMotivo                      CHAR(2)
  pEjecutivoReviso             CHAR(8)
  pFechaRevision               DATE
  pTiempoIniRev                CHAR(10)
  pTiempoFinRev                CHAR(10)
  pDevuelto                    CHAR(1)
  pRevisado                    CHAR(1)
  pMacAdress                   CHAR(18)
  pDireccionIp                 CHAR(16)
) RETURNING CHAR(5)   	AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pClaveBanco` | `CHAR(3)` | — | — |
| `pCuentaDeposito` | `CHAR(20)` | — | — |
| `pNumeroCheque` | `CHAR(7)` | — | — |
| `pFechaPresenta` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |
| `pNumEjecut` | `INTEGER` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pTipoSubOperacion` | `CHAR(1)` | — | — |
| `pNumeroCuenta` | `CHAR(20)` | — | — |
| `pNumeroCliente` | `CHAR(20)` | — | — |
| `pMonto` | `DECIMAL(16,2)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pMotivo` | `CHAR(2)` | — | — |
| `pEjecutivoReviso` | `CHAR(8)` | — | — |
| `pFechaRevision` | `DATE` | — | — |
| `pTiempoIniRev` | `CHAR(10)` | — | — |
| `pTiempoFinRev` | `CHAR(10)` | — | — |
| `pDevuelto` | `CHAR(1)` | — | — |
| `pRevisado` | `CHAR(1)` | — | — |
| `pMacAdress` | `CHAR(18)` | — | — |
| `pDireccionIp` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L71 |
| `iSql_Err` | `INTEGER` | L72 |
| `cCveBanco` | `CHAR(3)` | L73 |
| `cDescripcion` | `CHAR(150)` | L74 |
| `iCuenta` | `INT8` | L75 |
| `iNumCheque` | `INT8` | L76 |
| `dFechaAlta` | `DATE` | L77 |
| `dFechaPresenta` | `DATE` | L78 |
| `cUsuarioAlta` | `CHAR(8)` | L79 |
| `cProducto` | `CHAR(4)` | L80 |
| `cCliente` | `CHAR(20)` | L81 |
| `cNombreCte` | `CHAR(104)` | L82 |
| `cNomProducto` | `CHAR(40)` | L83 |
| `cCuentaDep` | `CHAR(20)` | L84 |
| `dMonto` | `DECIMAL(18,2)` | L85 |
| `cRevisado` | `CHAR(1)` | L86 |
| `cEjecutivoReviso` | `CHAR(8)` | L87 |
| `dMontoRet` | `DECIMAL(16,2)` | L88 |
| `cSucursal` | `CHAR(4)` | L89 |
| `pEmpresa` | `CHAR(3)` | L90 |
| `iTotalRegistrosRevisados` | `INTEGER` | L91 |
| `iTotalRegistros` | `INTEGER` | L92 |
| `iEjecutivo` | `INTEGER` | L93 |
| `vNombreEjecut` | `VARCHAR(100)` | L94 |
| `cCodigo` | `CHAR(2)` | L95 |
| *…15 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ope_obtienecheques` | `bdicnweb` | no | L244 |
| `sp_ope_obtienecheques_totales` | `bdicnweb` | no | L255 |
| `sp_cce_consultausuariosaut` | `bdicnweb` | no | L267 |
| `sp_consultadevcam` | `bdicnweb` | no | L277 |
| `sp_obtenerparametroscce` | `bdicnweb` | no | L286 |
| `sp_ope_chequesrevisados` | `bdicnweb` | no | L294 |
| `sp_ope_catalogobanco` | `bdicnweb` | no | L323 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L115 | CÓDIGO_RETORNO | `LET cCodRet         = '00000';` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L185 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L193 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L202 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L211 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L219 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L227 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L235 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L348 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `imagenes` | ENTIDAD | imágenes / documentos digitales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consulta_imei_app`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_imei_app.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta (canal app)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_imei_app(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pImei                        CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pImei` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `cUsuario` | `CHAR(8)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_usuario_movil` | `bdinteg` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_imei_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_imei_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_reporte_cantidades_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_reporte_cantidades_totales.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 70 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte y cantidad (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_reporte_cantidades_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcionCanal                 CHAR(1)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pStatus                      CHAR(1)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pTarjeta                     CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcionCanal` | `CHAR(1)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTarjeta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iTotales` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cantidad` | ENTIDAD | cantidad | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_reportebpioperacionesinternet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_reportebpioperacionesinternet.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte, operaciones y interés (Banca Por Internet)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consulta_bpioperaciones` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_reportebpioperacionesinternet(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pIdOperacion                 CHAR(5)
  pDtFechaIni                  DATE
  pDtFechaFinal                DATE
  pCtaOrigen                   CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pIdOperacion` | `CHAR(5)` | — | — |
| `pDtFechaIni` | `DATE` | — | — |
| `pDtFechaFinal` | `DATE` | — | — |
| `pCtaOrigen` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `cMensaje` | `CHAR(80)` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cDescrip` | `CHAR(30)` | L18 |
| `dFechaAplicacion` | `DATE` | L19 |
| `cCtaOrigen` | `CHAR(20)` | L20 |
| `cCtaDestino` | `CHAR(20)` | L21 |
| `mMonto` | `MONEY(14,2)` | L22 |
| `cFolioSuc` | `CHAR(25)` | L23 |
| `dFechaOperacion` | `DATETIME YEAR TO FRACTION` | L24 |
| `cRefPago` | `CHAR(25)` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iNoRegs` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consulta_bpioperaciones` | `bdinteg` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L85 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L87 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L90 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bpi` | MODIF | Banca Por Internet (canal web BPI) | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?ernet` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ernet` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_sac_reportediario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_sac_reportediario.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal y reporte (diario)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_sac_reportediario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,DATE AS FechaProceso, INTEGER AS num_mesesvent, MONEY(16,2) AS importe_vent,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iTotales` | `INTEGER` | L8 |
| `dFechaProceso` | `DATE` | L9 |
| `iNum_mesesvent` | `INTEGER` | L10 |
| `mImporte_vent` | `MONEY(16,2)` | L11 |
| `iNum_mesesdomi` | `INTEGER` | L12 |
| `mImporte_domi` | `MONEY(16,2)` | L13 |
| `iNum_meses` | `INTEGER` | L14 |
| `mImporte_total` | `MONEY(16,2)` | L15 |
| `mComision` | `MONEY(16,2)` | L16 |
| `mIva` | `MONEY(16,2)` | L17 |
| `mImporte_pago_coppel` | `MONEY(16,2)` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_reportediario_seg` | `bdisac` | ⚠️ sí | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `diario` | MODIF | diario | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consulta_sac_reportedomiciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_sac_reportedomiciliacion.sql` |
| **LOC (1er CREATE)** | 192 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, reporte y domiciliación" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_sac_reportedomiciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pSucursal                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,DATE AS fecha_pago, CHAR(20) AS num_cliente, MONEY(16,2) AS importe_pago,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `dFecha_pago` | `DATE` | L9 |
| `cNum_cliente` | `CHAR(20)` | L10 |
| `mImporte_pago` | `MONEY(16,2)` | L11 |
| `mImporte_comision_convenio` | `MONEY(16,2)` | L12 |
| `mIva_comision_convenio` | `MONEY(16,2)` | L13 |
| `iMeses` | `INTEGER` | L14 |
| `cNumcte_coppel` | `CHAR(20)` | L15 |
| `cNumcte` | `CHAR(20)` | L16 |
| `cNum_poliza` | `CHAR(20)` | L17 |
| `mMonto_mes` | `MONEY(16,2)` | L18 |
| `cSucursal_alta` | `CHAR(4)` | L19 |
| `cPromotor` | `CHAR(8)` | L20 |
| `cNom_promotor` | `CHAR(45)` | L21 |
| `cTipo_plan` | `CHAR(1)` | L22 |
| `dFecha_alta` | `DATE` | L23 |
| `dFecha_cambio` | `DATE` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_reportediariodomi_seg` | `bdisac` | ⚠️ sí | SELECT | L98 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L101 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L152 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L161 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `domiciliacion` | ENTIDAD | domiciliación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consulta_sac_reportedomiciliacion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_sac_reportedomiciliacion_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, reporte y domiciliación (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_sac_reportedomiciliacion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,INTEGER AS totRegistros
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iTotales` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_reportediariodomi_seg` | `bdisac` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `domiciliacion` | ENTIDAD | domiciliación | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consulta_sac_reporteventanilla`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_sac_reporteventanilla.sql` |
| **LOC (1er CREATE)** | 201 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal y reporte" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_sac_reporteventanilla(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pSucursal                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,DATE AS fecha_pago, CHAR(40) AS referencia1, MONEY(16,2) AS importe_pago,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `dFecha_pago` | `DATE` | L10 |
| `cReferencia1` | `CHAR(40)` | L11 |
| `mImporte_pago` | `MONEY(16,2)` | L12 |
| `mImporte_comision_convenio` | `MONEY(16,2)` | L13 |
| `mIva_comision_convenio` | `MONEY(16,2)` | L14 |
| `iMeses` | `INTEGER` | L15 |
| `cSucursal_pago_ventanilla` | `CHAR(4)` | L16 |
| `cForma_pago` | `CHAR(1)` | L17 |
| `cCajero` | `CHAR(8)` | L18 |
| `cNom_cajero` | `CHAR(45)` | L19 |
| `cNumcte_coppel` | `CHAR(20)` | L20 |
| `cNumcte` | `CHAR(20)` | L21 |
| `cNum_poliza` | `CHAR(20)` | L22 |
| `mMonto_mes` | `MONEY(16,2)` | L23 |
| `cSucursal_alta` | `CHAR(4)` | L24 |
| `cPromotor` | `CHAR(8)` | L25 |
| `cNom_promotor` | `CHAR(45)` | L26 |
| `cTipo_plan` | `CHAR(1)` | L27 |
| `dFecha_alta` | `DATE` | L28 |
| `dFecha_cambio` | `DATE` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_reportediariovent_seg` | `bdisac` | ⚠️ sí | SELECT | L107 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L161 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?ventanilla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ventanilla` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_sac_reporteventanilla_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_sac_reporteventanilla_totales.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal y reporte (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_sac_reporteventanilla_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,INTEGER AS totRegistros
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iTotales` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_reportediariovent_seg` | `bdisac` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?ventanilla_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ventanilla_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_saldos_general_cre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_saldos_general_cre.sql` |
| **LOC (1er CREATE)** | 224 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta saldos y crédito (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_saldos_general_cre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumeroCredito               CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumeroCredito` | `CHAR(20)` | `cre`=crédito | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L51 |
| `cCodRetSp` | `CHAR(6)` | L52 |
| `iSqlErr` | `INTEGER` | L53 |
| `cEmpresa` | `CHAR(3)` | L54 |
| `mensajeRetorno` | `CHAR(80)` | L55 |
| `numeroCredito` | `CHAR(20)` | L56 |
| `codigoTipcred` | `CHAR(2)` | L57 |
| `fechaOrigen` | `DATE` | L58 |
| `fechaProxPago` | `DATE` | L59 |
| `pagoMinimo` | `DECIMAL(18,2)` | L60 |
| `fechaUltPago` | `DATE` | L61 |
| `plazo` | `INTEGER` | L62 |
| `pagosRealizados` | `INTEGER` | L63 |
| `lineaOtorgada` | `DECIMAL(18,2)` | L64 |
| `tasaInteres` | `DECIMAL(9,6)` | L65 |
| `tasaMoratorios` | `DECIMAL(9,6)` | L66 |
| `montoSbc` | `DECIMAL(14,2)` | L67 |
| `capVig` | `DECIMAL(18,2)` | L68 |
| `capTrans` | `DECIMAL(18,2)` | L69 |
| `capVdoExig` | `DECIMAL(18,2)` | L70 |
| `capVdoNoExig` | `DECIMAL(18,2)` | L71 |
| `sdoActTotalCap` | `DECIMAL(18,2)` | L72 |
| `intVig` | `DECIMAL(18,2)` | L73 |
| `intVdo` | `DECIMAL(18,2)` | L74 |
| `intMoratorios` | `DECIMAL(18,2)` | L75 |
| *…27 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_maecred_vendida` | `bdicred` | ⚠️ sí | SELECT | L210 |
| `sd_maecredcrd_vendida` | `bdicred` | ⚠️ sí | SELECT | L212 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consulta_saldos_general` | `bdicred` | ⚠️ sí | L189 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L172 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L199 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L203 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L207 | CÓDIGO_RETORNO | `LET cCodRet = '00209';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `saldos` | ENTIDAD | saldos | 🔵 CONVENCIÓN | nombre_sp |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_consulta_saldos_generalcre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_saldos_generalcre.sql` |
| **LOC (1er CREATE)** | 351 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta saldos y crédito (general)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consulta_saldos_general_mora` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_saldos_generalcre(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  pNumCredito                  CHAR(20)
) RETURNING CHAR(5)       AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cre`=crédito | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L55 |
| `cCodRet` | `CHAR(5)` | L56 |
| `vCodRet` | `CHAR(6)` | L57 |
| `cMensajeRet` | `CHAR(80)` | L58 |
| `pEmpresa` | `CHAR(3)` | L59 |
| `cEmpresa` | `CHAR(3)` | L60 |
| `cNumCte` | `CHAR(20)` | L61 |
| `cNumCredito` | `CHAR(20)` | L62 |
| `cCodTipCred` | `CHAR(2)` | L63 |
| `cNumTarjeta` | `CHAR(20)` | L64 |
| `cDescStatusCred` | `CHAR(60)` | L65 |
| `cSucursal` | `CHAR(4)` | L66 |
| `iIdUnidadProd` | `INTEGER` | L67 |
| `cCodCaract2` | `CHAR(3)` | L68 |
| `dMontoFinanciado` | `DECIMAL(18,2)` | L69 |
| `dIvaSuc` | `DECIMAL(5,3)` | L70 |
| `dtFechaOrigen` | `DATE` | L71 |
| `dtFechaProxPago` | `DATE` | L72 |
| `dPagoMinimo` | `DECIMAL(18,2)` | L73 |
| `dtFechaUltPago` | `DATE` | L74 |
| `iPlazo` | `INTEGER` | L75 |
| `iPagosRealizados` | `INTEGER` | L76 |
| `dLineaOtorgada` | `DECIMAL(18,2)` | L77 |
| `dTasaInteres` | `DECIMAL(9,6)` | L78 |
| `dTasaMoratorios` | `DECIMAL(9,6)` | L79 |
| *…46 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consulta_saldos_general_mora` | `bdicred` | ⚠️ sí | L250 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L221 | VALIDACIÓN_NULL | `IF cID_USUARIOC = '' OR cID_FUNCIONC = '' OR (pEmpresa IS NULL AND pNumCredito IS NULL) THEN` |  |
| L222 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L317 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L331 | CÓDIGO_RETORNO | `LET cCodRet = '00373';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_consulta_usuario_movil_cc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_usuario_movil_cc.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuario y cc — cuenta corriente (canal móvil)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_usuario_movil_cc(
  pCentroCostos                CHAR(8)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCentroCostos` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `cNoEmpleado` | `CHAR(8)` | L15 |
| `cStatus` | `CHAR(1)` | L16 |
| `cNombre` | `CHAR(60)` | L17 |
| `cCentro_costos` | `CHAR(8)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cNo_telefono` | `CHAR(10)` | L20 |
| `cNombreSuc` | `CHAR(40)` | L21 |
| `iCont` | `INTEGER` | L22 |
| `cImei` | `CHAR(20)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_usuario_movil` | `bdinteg` | ⚠️ sí | SELECT | L55 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L60 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `movil` | MODIF | canal móvil | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |

---

## `sp_consulta_usuario_movil_ws`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consulta_usuario_movil_ws.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuario (canal móvil)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_usuario_movil_ws(
  pEjecutivo                   CHAR(8)
  pImei                        CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pImei` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |
| `cPassword` | `CHAR(20)` | L20 |
| `cImei` | `CHAR(20)` | L21 |
| `cActivo` | `CHAR(1)` | L22 |
| `cNombre` | `CHAR(60)` | L23 |
| `cCentro_costos` | `CHAR(8)` | L24 |
| `cNo_telefono` | `CHAR(10)` | L25 |
| `cGenerico1` | `CHAR(20)` | L26 |
| `cGenerico2` | `CHAR(30)` | L27 |
| `cGenerico3` | `CHAR(40)` | L28 |
| `cSucursal` | `CHAR(4)` | L29 |
| `cNombreSuc` | `CHAR(30)` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_usuario_movil` | `bdinteg` | ⚠️ sí | SELECT | L69 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00478';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `movil` | MODIF | canal móvil | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_ws` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ws` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaarchivosconau`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaarchivosconau.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaarchivosconau(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cNombreArchivo` | `CHAR(30)` | L12 |
| `cArchivoOrigen` | `CHAR(5)` | L14 |
| `cProceso` | `CHAR(1)` | L15 |
| `dFechaArchivo` | `DATE` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |
| `bditarjeta` | `bdicnweb` | no | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_consultaarchivos` |  |
| L64 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivos` | ENTIDAD | archivos | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?au` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?au` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaarchivosconau_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaarchivosconau_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta archivos (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaarchivosconau_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `bditarjeta` | `bdicnweb` | no | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_consultaarchivos` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?au_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?au_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultabajausuariosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultabajausuariosccl.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuarios y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (de baja)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultabajausuariosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave                       CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |
| `bditarjeta` | `bdicnweb` | no | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_bajausuarios';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `baja` | MODIF | de baja | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultabitacoraccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultabitacoraccl.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta bitácora y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `sp_conbitacora_con2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultabitacoraccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pElemento                    INTEGER
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pElemento` | `INTEGER` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cDescCodRet` | `CHAR(80)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iElemento` | `INTEGER` | L14 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(5)` | L15 |
| `cActividad` | `CHAR(250)` | L16 |
| `cClaveUsuario` | `CHAR(10)` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L53 |
| `sp_conbitacora_con2` | `bditarjeta` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pR` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_conbitacora_con2` |  |
| L73 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultabitacoratasascomipmtc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultabitacoratasascomipmtc.sql` |
| **LOC (1er CREATE)** | 146 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 95 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta bitácora, tasa, Comisión bancaria — cobro de comisión sobre cuenta y tc — Tarjeta de Crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultabitacoratasascomipmtc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumCuenta                   CHAR(20)
  pComision                    CHAR(4)
  pFechaInicial                CHAR(10)
  pFechaFinal                  CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pComision` | `CHAR(4)` | `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) | 🔵 CONVENCIÓN |
| `pFechaInicial` | `CHAR(10)` | — | — |
| `pFechaFinal` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `dFecha` | `CHAR(10)` | L15 |
| `cNombreComision` | `CHAR(50)` | L16 |
| `cCuenta` | `CHAR(20)` | L17 |
| `cValorAnterior` | `CHAR(30)` | L18 |
| `cValorFinal` | `CHAR(30)` | L19 |
| `cNombreUsuario` | `CHAR(45)` | L20 |
| `cDescripcionParam` | `CHAR(60)` | L21 |
| `cValorParam` | `CHAR(60)` | L22 |
| `iTotalDias` | `INTEGER` | L23 |
| `iTotalDiasConsultar` | `INTEGER` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tc_consultabittascomimp_tmp` | `bdicnweb` | no | SELECT | L91 |
| `sw_tc_consultabittascomimp_tmp` | `bdicnweb` | no | DELETE | L91 |
| `sw_tc_consultabittascomimp_tmp` | `bdicnweb` | no | INSERT | L110 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |
| `sp_consultavalorparametro` | `bdicheq` | ⚠️ sí | L74 |
| `sp_consultabittascomi_pm` | `bdicheq` | ⚠️ sí | L97 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pFechaInicial = '' OR pFechaFinal = '' OR` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:sp_consultavalorparame` |  |
| L81 | FÓRMULA | `LET iTotalDiasConsultar = MDY((SUBSTRING(pFechaFinal FROM 6 FOR 2)) , (SUBSTRING(pFechaFinal FROM 9 ` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00232';` |  |
| L101 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:sp_consultabittascomi_` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L122 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?ipm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?ipm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacargotransfercap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacargotransfercap.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cargo, transferencia y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_trxscargotransfersoc` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacargotransfercap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(3)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cTransacc` | `CHAR(4)` | L10 |
| `cDescripcion` | `CHAR(80)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_trxscargotransfersoc` | `bdicnweb` | no | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_trxscargotransfersoc';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00710';` |  |
| L54 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultacatalogo_productosmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogo_productosmc.sql` |
| **LOC (1er CREATE)** | 44 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta catálogo, productos y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogo_productosmc(
  pIdFuncionDummy              CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdFuncionDummy` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(80)` | L9 |
| `cNombreProducto` | `CHAR(40)` | L10 |
| `cNumProducto` | `CHAR(4)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultacatalogocausastatusaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogocausastatusaumlincred.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 109 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, causa y estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogocausastatusaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeError` | `CHAR(80)` | L10 |
| `cCausaStatus` | `CHAR(3)` | L11 |
| `cDescCausaStatus` | `CHAR(100)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_obtencausastatusaumlincred';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00258';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00259';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `causa` | ENTIDAD | causa / motivo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultacatalogocausastatusmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogocausastatusmc.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, causa, estatus y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogocausastatusmc(
  pStatus                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `cStatus` | `CHAR(2)` | L12 |
| `cDescripcionStatus` | `CHAR(40)` | L13 |
| `cCausa` | `CHAR(3)` | L14 |
| `cDescripcionCausa` | `CHAR(100)` | L15 |
| `cJustificacion` | `CHAR(100)` | L16 |
| `iNoRegitros` | `INTEGER` | L17 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | VALIDACIÓN_NULL | `IF pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L64 | FÓRMULA | `LET iNoRegitros = iNoRegitros + 1;` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `causa` | ENTIDAD | causa / motivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultacatalogocausastatusmc_oro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogocausastatusmc_oro.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 78 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, causa, estatus, mc — Mesa de Control y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogocausastatusmc_oro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `cStatus` | `CHAR(2)` | L12 |
| `cDescripcionStatus` | `CHAR(40)` | L13 |
| `cCausa` | `CHAR(3)` | L14 |
| `cDescripcionCausa` | `CHAR(100)` | L15 |
| `cJustificacion` | `CHAR(100)` | L16 |
| `iNoRegitros` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L62 | FÓRMULA | `LET iNoRegitros = iNoRegitros + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `causa` | ENTIDAD | causa / motivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultacatalogoproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogoproductos.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo y productos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogoproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `cClaveProducto` | `CHAR(4)` | L10 |
| `cNombre` | `CHAR (40)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_producto` | `bdicheq` | ⚠️ sí | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultacatalogorigenaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogorigenaumlincred.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 114 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogorigenaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeRetorno` | `CHAR(80)` | L10 |
| `cOrigen` | `CHAR(2)` | L11 |
| `cDescOrigen` | `CHAR(40)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultar_origen_aumlincred';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ri` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ri` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacatalogosepomex`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogosepomex.sql` |
| **LOC (1er CREATE)** | 118 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_sepomexconscatalogos` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogosepomex(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoBusqueda                SMALLINT
  pDatoConsulta                CHAR(30)
  pIdEstado                    SMALLINT
  pCiudad                      CHAR(35)
  pIdCiudad                    SMALLINT
  pModificacion                SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoBusqueda` | `SMALLINT` | — | — |
| `pDatoConsulta` | `CHAR(30)` | `consulta`=consulta / lee | 🔵 CONVENCIÓN |
| `pIdEstado` | `SMALLINT` | — | — |
| `pCiudad` | `CHAR(35)` | — | — |
| `pIdCiudad` | `SMALLINT` | — | — |
| `pModificacion` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |
| `cCveEstado` | `CHAR(2)` | L21 |
| `cNombreEstado` | `CHAR(30)` | L22 |
| `cClaveCiudad` | `CHAR(3)` | L23 |
| `cNombreCiudad` | `CHAR(60)` | L24 |
| `cCodPostal` | `CHAR(5)` | L25 |
| `cAsenta` | `CHAR(60)` | L26 |
| `iNumColonia` | `SMALLINT` | L27 |
| `cNombreZona` | `CHAR(32)` | L28 |
| `cNombreMunicipio` | `CHAR(40)` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sepomexconscatalogos` | `bdinteg` | ⚠️ sí | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoBusqueda IS NULL OR pRegistros IS NULL OR pRecuperacion ` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L90 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L94 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER;` |  |
| L97 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?sepome` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sepome` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacatalogostatusaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogostatusaumlincred.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 143 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo y estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cac_obtenstatusaumlincred` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogostatusaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pDescripcion                 CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pDescripcion` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeSp` | `CHAR(100)` | L10 |
| `cStatus` | `CHAR(2)` | L11 |
| `cDescripcion` | `CHAR(50)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cac_obtenstatusaumlincred` | `bdicred` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_obtenstatusaumlincred';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultacatalogotiporeportesmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatalogotiporeportesmc.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, reportes y mc — Mesa de Control (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicobranza` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatalogotiporeportesmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoCampania                SMALLINT
  pNumParametro                INTEGER
  pGrupoParametro              CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoCampania` | `SMALLINT` | `tipo`=tipo de | 🔵 CONVENCIÓN |
| `pNumParametro` | `INTEGER` | — | — |
| `pGrupoParametro` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cMensajeSp` | `CHAR(100)` | L10 |
| `cValorTexto` | `CHAR(100)` | L11 |
| `dValorNumero` | `DECIMAL(18,2)` | L12 |
| `iCodRetorno` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicobranza` | `bdicnweb` | no | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoCampania IS NULL OR pGrupoParametro = '' THEN` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetorno, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultacampaniasctrl';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultacatcomisionespmtc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatcomisionespmtc.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 94 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo y tc — Tarjeta de Crédito (especial) · comisión" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultacatcomisiones_pm` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatcomisionespmtc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdComision                  CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdComision` | `CHAR(4)` | `comision`=comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cIdComision` | `CHAR(4)` | L12 |
| `cDescComision` | `CHAR(50)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_consultacatcomisiones_pm` | `bdicheq` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:sp_consultacatcomision` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `comision` | REG | comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacatczb_rh`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatczb_rh.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 153 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatczb_rh(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,CHAR(4) AS czb_clave,CHAR(40) AS czb_nombre,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCzb_clave` | `CHAR(4)` | L8 |
| `cCzb_nombre` | `CHAR(40)` | L9 |
| `cNo_empleado_czb` | `CHAR(20)` | L10 |
| `cNombre_czb` | `CHAR(80)` | L11 |
| `cNo_celular_czb` | `CHAR(13)` | L12 |
| `cE_mail_czb` | `CHAR(80)` | L13 |
| `cCentro_costo_czb` | `CHAR(8)` | L14 |
| `iFilas` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catgcbczb_rh` | `bdinteg` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | FÓRMULA | `LET iFilas = iFilas+1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet ='01267';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?czb_rh` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?czb_rh` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacatgcb_rh`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatgcb_rh.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 152 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatgcb_rh(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,CHAR(4) AS cve_gerencia,CHAR(40) AS nombre_gerencia,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCve_gerencia` | `CHAR(4)` | L8 |
| `cNombre_gerencia` | `CHAR(40)` | L9 |
| `cNo_empleado` | `CHAR(20)` | L10 |
| `cNombre_emp` | `CHAR(80)` | L11 |
| `cNo_celular` | `CHAR(13)` | L12 |
| `cEmail` | `CHAR(80)` | L13 |
| `cCentro_costo` | `CHAR(8)` | L14 |
| `iFilas` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catgcb_rh` | `bdinteg` | ⚠️ sí | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iFilas = iFilas+1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet ='01266';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?gcb_rh` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gcb_rh` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacatnumnomconveniosac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatnumnomconveniosac.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, número, nómina, convenio y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatnumnomconveniosac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `isqlerr` | `INTEGER` | L8 |
| `cNumCategoria` | `CHAR(2)` | L9 |
| `cNumConvenio` | `CHAR(3)` | L10 |
| `cNomconvenio` | `CHAR(40)` | L11 |
| `iNumRows` | `INTEGER` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo = '' OR pRegistros IS NULL OR pRecuperacion IS NULL  TH` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L67 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L88 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_consultacatsecausa`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacatsecausa.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=56 |
| **Deps concatenadas** | 150 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo y causa" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarsecausaconalcance_clon` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacatsecausa(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSituacion                   CHAR(1)
  pCausa                       INTEGER
  pFlagSoloSituaciones         CHAR(1)
  pAlcance                     SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSituacion` | `CHAR(1)` | — | — |
| `pCausa` | `INTEGER` | `causa`=causa / motivo | ✅ CÓDIGO |
| `pFlagSoloSituaciones` | `CHAR(1)` | — | — |
| `pAlcance` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `cDescCodRetSp` | `CHAR(100)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cSituacion` | `CHAR(1)` | L18 |
| `iCausa` | `INTEGER` | L19 |
| `cDescripcion` | `CHAR(75)` | L20 |
| `cAlcance` | `CHAR(1)` | L21 |
| `cDespliegue` | `CHAR(1)` | L22 |
| `cVigente` | `CHAR(1)` | L23 |
| `cCvecc` | `CHAR(5)` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |
| `sp_consultarsecausaconalcance_clon` | `bdisitesp` | ⚠️ sí | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisitesp:sp_consultarsecausaco` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `causa` | ENTIDAD | causa / motivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?se` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacedulas2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacedulas2.sql` |
| **LOC (1er CREATE)** | 201 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cédulas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacedulas2(
  pFechaConcil                 DATE
  pTipo                        SMALLINT
  pregistros                   INTEGER
  precuperacion                INTEGER
) RETURNING CHAR(5), CHAR(40), CHAR(14), DECIMAL(18,2), DECIMAL(18,2), DECIMAL(18,2), CHAR(255), CHAR(1)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaConcil` | `DATE` | — | — |
| `pTipo` | `SMALLINT` | — | — |
| `pregistros` | `INTEGER` | — | — |
| `precuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cNombre` | `CHAR(40)` | L11 |
| `cCtaContable` | `CHAR(14)` | L12 |
| `mSdoCheques` | `DECIMAL(18,2)` | L13 |
| `mSdoContab` | `DECIMAL(18,2)` | L14 |
| `mDifSaldos` | `DECIMAL(18,2)` | L15 |
| `cObservaciones` | `CHAR(255)` | L16 |
| `cEditable` | `CHAR(1)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontable` | `bdicheq` | ⚠️ sí | SELECT | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | VALIDACIÓN_NULL | `IF ( ( pFechaConcil is null OR pFechaConcil = '' ) OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cedulas` | ENTIDAD | cédulas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacedulas2_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacedulas2_totales.sql` |
| **LOC (1er CREATE)** | 204 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cédulas (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacedulas2_totales(
  pFechaConcil                 DATE
  pTipo                        SMALLINT
) RETURNING CHAR(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaConcil` | `DATE` | — | — |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cNombre` | `CHAR(40)` | L11 |
| `cCtaContable` | `CHAR(14)` | L12 |
| `mSdoCheques` | `DECIMAL(18,2)` | L13 |
| `mSdoContab` | `DECIMAL(18,2)` | L14 |
| `mDifSaldos` | `DECIMAL(18,2)` | L15 |
| `cObservaciones` | `CHAR(255)` | L16 |
| `cEditable` | `CHAR(1)` | L17 |
| `vNoRegistros` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontable` | `bdicheq` | ⚠️ sí | SELECT | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | VALIDACIÓN_NULL | `IF ( ( pFechaConcil is null OR pFechaConcil = '' ) OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cedulas` | ENTIDAD | cédulas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultachequescod47totales_ccep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultachequescod47totales_ccep.sql` |
| **LOC (1er CREATE)** | 245 |
| **Callgraph** | ✅ fan_in=78 / fan_out=37 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cheques, código y CCE — Cámara de Compensación Electrónica (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultachequescod47totales_ccep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaHoy                    DATE
  pFechadevol                  DATE
  pDireccionMac                CHAR(15)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaHoy` | `DATE` | — | — |
| `pFechadevol` | `DATE` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNombreArchivoPRE` | `CHAR(40)` | L12 |
| `cNombreArchivo` | `CHAR(40)` | L13 |
| `iNombrePro` | `INTEGER` | L14 |
| `codret` | `INTEGER` | L15 |
| `cCveBanco` | `CHAR(3)` | L16 |
| `cDescBanco` | `CHAR(40)` | L17 |
| `cCtaReferencia` | `CHAR(40)` | L18 |
| `iNumCheque` | `INTEGER` | L19 |
| `mImporte` | `DECIMAL(14,2)` | L20 |
| `cCuentaDeposito` | `CHAR(20)` | L21 |
| `cSucursal` | `CHAR(44)` | L22 |
| `cSiTransaccion` | `CHAR(4)` | L23 |
| `contReg` | `INTEGER` | L24 |
| `dMontoTotalValido` | `DECIMAL(18,2)` | L25 |
| `cDigitalizado` | `CHAR(1)` | L26 |
| `iTotalValidos` | `INTEGER` | L27 |
| `cTipoCuentaDep` | `CHAR(2)` | L28 |
| `cCampoCliente` | `CHAR(20)` | L29 |
| `cCampoCuenta` | `CHAR(20)` | L30 |
| `cTablaClientes` | `CHAR(30)` | L31 |
| *…13 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_encabezado` | `bditef` | ⚠️ sí | SELECT | L115 |
| `ccep_procesacod47detalle_tmp` | `bdicnweb` | no | SELECT | L123 |
| `ccep_procesacod47detalle_tmp` | `bdicnweb` | no | DELETE | L123 |
| `cce_detalle` | `bditef` | ⚠️ sí | SELECT | L201 |
| `ccep_procesacod47detalle_tmp` | `bdicnweb` | no | INSERT | L211 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L100 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechaHoy IS NULL OR pFechadevol IS NULL OR pDireccionMac = ` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00778';` |  |
| L135 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_cce_consultar_detallecheques'` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L152 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_cce_consultar_detallecheques'` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L159 | FÓRMULA | `LET iTotalValidos = iTotalValidos + 1;` |  |
| L160 | FÓRMULA | `LET dMontoTotalValido = dMontoTotalValido + mImporte;` | 🔴 MONEY/aritmética financiera |
| L173 | VALIDACIÓN_NULL | `IF cTipoCuentaDep IS NULL THEN -- ERROR DE QUE NO SE ENCONTRO EL MAPEO CECOBAN` |  |
| L192 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:consnomcte';` |  |
| L230 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?47` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?47`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultachequesdevueltoscap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultachequesdevueltoscap.sql` |
| **LOC (1er CREATE)** | 145 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cheques, OS — Originación de Solicitudes y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultachequesdevueltoscap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pCausalesDevolucion          CHAR(100)
  pCodigosAlertamiento         CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pCausalesDevolucion` | `CHAR(100)` | `dev`=devolución | 🔵 CONVENCIÓN |
| `pCodigosAlertamiento` | `CHAR(30)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `dFecha` | `DATE` | L16 |
| `cCuentaDeposito` | `CHAR(20)` | L17 |
| `cCodigoAlertamiento` | `CHAR(2)` | L18 |
| `cDescCodAlertamiento` | `CHAR(35)` | L19 |
| `cCuentaCheque` | `CHAR(20)` | L20 |
| `cBancoEmisor` | `CHAR(45)` | L21 |
| `cNombreBeneficiario` | `CHAR(40)` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |
| `cQuery` | `CHAR(250)` | L24 |
| `cFromQuery` | `CHAR(450)` | L25 |
| `cWhereQuery` | `CHAR(500)` | L26 |
| `i` | `INTEGER` | L27 |
| `cCaracter` | `CHAR(1)` | L28 |
| `iNoCheque` | `INTEGER` | L29 |
| `cveBanco` | `CHAR(3)` | L31 |
| `mMontoCheque` | `MONEY(16, 2)` | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCuenta = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL ` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L121 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?uelt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uelt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultachqsaldonegativo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultachqsaldonegativo.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cheque y saldo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultachqsaldonegativo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `dFecha` | `DATE` | L13 |
| `cCuenta` | `CHAR (20)` | L14 |
| `cStatusCta` | `CHAR (1)` | L15 |
| `cSucursal` | `CHAR (4)` | L16 |
| `dSdoActual` | `DECIMAL (14, 2)` | L17 |
| `dImpChqSbg` | `DECIMAL (14, 2)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cuentassdoneg` | `bdicheq` | ⚠️ sí | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFecha_Inicio = '' OR pFecha_Fin = '' OR pRegistros IS NULL ` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `saldo` | ENTIDAD | saldo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?negativo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?negativo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultachqsaldonegativo_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultachqsaldonegativo_totales.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cheque y saldo (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultachqsaldonegativo_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cuentassdoneg` | `bdicheq` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `saldo` | ENTIDAD | saldo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?negativo_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?negativo_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultachqsldsobregirado_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultachqsldsobregirado_totales.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cheque (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultachqsldsobregirado_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cuentassbg` | `bdicheq` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `?sldsob` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?irado_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sldsob`, `?irado_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaciudadessepomex`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaciudadessepomex.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta ciudades" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaciudadessepomex(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEstado                    CHAR(2)
  pIdCiudad                    CHAR(3)
  pNombreCiudad                CHAR(60)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEstado` | `CHAR(2)` | — | — |
| `pIdCiudad` | `CHAR(3)` | — | — |
| `pNombreCiudad` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIdCiudadCoppel` | `INTEGER` | L9 |
| `cDescCiudad` | `CHAR(60)` | L10 |
| `cMunicipio` | `CHAR(100)` | L11 |
| `iExiste` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ciudades` | `bdinteg` | ⚠️ sí | SELECT | L44 |
| `si_catsepomex` | `bdinteg` | ⚠️ sí | SELECT | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00218';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `ciudades` | ENTIDAD | ciudades (catálogo) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?sepome` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sepome` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaclasificacion_gcb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaclasificacion_gcb.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 151 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SIF — canal de estado de cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaclasificacion_gcb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,CHAR(3) AS cve_clasificacion,CHAR(40) AS clasificacion
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCve_clasificacion` | `CHAR(3)` | L6 |
| `cClasificacion` | `CHAR(40)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_clasificacion_gcb` | `bdinteg` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet ='01269';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?cla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sif` | ENTIDAD | SIF — canal de estado de cuenta (aclaraciones_edocta_sif, de | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `?ion_gcb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cla`, `?i`, `?ion_gcb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacomisionestasatc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacomisionestasatc.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 93 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta tasa y tc — Tarjeta de Crédito · comisión" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultadettasacomi_pm` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacomisionestasatc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
  pCuenta                      CHAR(20)
  pProducto                    CHAR(4)
  pTpoconsulta                 SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pTpoconsulta` | `SMALLINT` | `consulta`=consulta / lee | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `cCodRetSp` | `CHAR(6)` | L26 |
| `iCodRetSp` | `INTEGER` | L27 |
| `cNumCte` | `CHAR(20)` | L28 |
| `cCuenta` | `CHAR(20)` | L29 |
| `dcTasaRend` | `DECIMAL(9,2)` | L30 |
| `mSdoPromMm` | `MONEY(18,2)` | L31 |
| `mMonMinAper` | `MONEY(18,2)` | L32 |
| `mComCgoNoSmm` | `MONEY(18,2)` | L33 |
| `mComCboAclaNp` | `MONEY(18,2)` | L34 |
| `mComChqGirCob` | `MONEY(18,2)` | L35 |
| `mComInaCta` | `MONEY(18,2)` | L36 |
| `mServTranSpei` | `MONEY(18,2)` | L37 |
| `mServTranTef` | `MONEY(18,2)` | L38 |
| `mServAnualidad` | `MONEY(18,2)` | L39 |
| `mServReenvToken` | `MONEY(18,2)` | L40 |
| `mServReepToken` | `MONEY(18,2)` | L41 |
| `mDispCtaBcoppel` | `MONEY(18,2)` | L42 |
| `mDispCtaOtrobco` | `MONEY(18,2)` | L43 |
| `mDispLinea` | `MONEY(18,2)` | L44 |
| `dFechAltaCmspm` | `CHAR(10)` | L45 |
| `dFecUltMovCmspm` | `CHAR(10)` | L46 |
| `iNoRegistros` | `INTEGER` | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L91 |
| `sp_consultadettasacomi_pm` | `bdicheq` | ⚠️ sí | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L85 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumcte = '' OR   pTpoconsulta IS NULL  THEN` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L102 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_consultadettasacomi_pm";` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L108 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comision` | REG | comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tasa` | ENTIDAD | tasa (de interés) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacompaplicacionccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacompaplicacionccl.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_concreing_compaplicacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacompaplicacionccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchOrigen                  CHAR (3)
  pIntegridad                  CHAR(1)
  pConcecutivo                 INTEGER
  pAplica                      CHAR (1)
  pSecuencia325                CHAR(6)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchOrigen` | `CHAR (3)` | — | — |
| `pIntegridad` | `CHAR(1)` | — | — |
| `pConcecutivo` | `INTEGER` | — | — |
| `pAplica` | `CHAR (1)` | `aplica`=aplica / ejecuta | 🔵 CONVENCIÓN |
| `pSecuencia325` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `cAplicacion` | `CHAR (1)` | L14 |
| `cErrorActividad` | `CHAR(250)` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_concreing_compaplicacion` | `bditarjeta` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pArchOrigen ='' OR pIntegridad ='' OR pConcecutivo IS NULL O` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_compaplicacion';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00613';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacomplementoccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacomplementoccl.sql` |
| **LOC (1er CREATE)** | 130 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacomplementoccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchOrigen                  CHAR(3)
  pConciliacion                CHAR(1)
  pConsecutivo                 INTEGER
  pNumTarjeta                  CHAR(16)
  pSecuencia325                CHAR(6)
  pMonto325                    CHAR(13)
  pTipoTransaccion325          CHAR(15)
  pIntegridad                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchOrigen` | `CHAR(3)` | — | — |
| `pConciliacion` | `CHAR(1)` | — | — |
| `pConsecutivo` | `INTEGER` | — | — |
| `pNumTarjeta` | `CHAR(16)` | — | — |
| `pSecuencia325` | `CHAR(6)` | — | — |
| `pMonto325` | `CHAR(13)` | — | — |
| `pTipoTransaccion325` | `CHAR(15)` | — | — |
| `pIntegridad` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `cCodRetSp` | `CHAR(6)` | L25 |
| `cDescCodRet` | `CHAR(100)` | L26 |
| `iCodRetSp` | `INTEGER` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cConciliacion` | `CHAR(1)` | L29 |
| `cSecuencia` | `CHAR(7)` | L30 |
| `cSecExtendida` | `CHAR(15)` | L31 |
| `mMontoIntercard` | `MONEY(16,2)` | L32 |
| `dFechaTransaccion` | `DATE` | L33 |
| `cInfReceptor` | `CHAR(40)` | L34 |
| `cIdTerminal` | `CHAR(16)` | L35 |
| `cMetodoCaptura` | `CHAR(2)` | L36 |
| `cMovConciliado` | `CHAR(1)` | L37 |
| `cMovReversado` | `CHAR(1)` | L38 |
| `cTipoMov` | `CHAR(1)` | L39 |
| `cFolioMov` | `CHAR(16)` | L40 |
| `dFechaConcilia` | `DATE` | L41 |
| `iTipoConciliacion` | `INTEGER` | L42 |
| `cDescConciliacion` | `CHAR(60)` | L43 |
| `cErrorActividad` | `CHAR(250)` | L44 |
| `iElemento` | `INTEGER` | L45 |
| `iNoRegistros` | `INTEGER` | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L91 |
| `bditarjeta` | `bdicnweb` | no | L99 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_concreing_consul` |  |
| L107 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?lemento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?lemento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaconciliaaperturapagarescargo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaconciliaaperturapagarescargo.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 347 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta apertura, pagarés y cargo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaconciliaaperturapagarescargo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INT` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |
| `dFechaAlt` | `DATE` | L22 |
| `cNumeroSerial` | `CHAR(20)` | L23 |
| `cFolioSuc` | `CHAR(30)` | L24 |
| `cUsuario` | `CHAR(20)` | L25 |
| `cCuentaApert` | `CHAR(20)` | L26 |
| `cSucCuenta` | `CHAR(50)` | L27 |
| `cSucursal` | `CHAR(20)` | L28 |
| `cTransaccApert` | `CHAR(4)` | L29 |
| `cDescripcionApert` | `CHAR(50)` | L30 |
| `cTransaccCargo` | `CHAR(4)` | L31 |
| `cDescripcionCargo` | `CHAR(50)` | L32 |
| `cProductoCargo` | `CHAR(40)` | L33 |
| `mMontoApert` | `MONEY(18,2)` | L34 |
| `mMontoCargo` | `MONEY(18,2)` | L35 |
| `mMontoDiferencia` | `MONEY(18,2)` | L36 |
| `dFechaMinSel` | `DATE` | L37 |
| `dFechaMaxSel` | `DATE` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_concilia_cargos_apertura` | `bdinvers` | ⚠️ sí | SELECT | L121 |
| `sv_difer_concilia_cargos_apertura` | `bdinvers` | ⚠️ sí | SELECT | L145 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pTipoReporte = '' OR pFechaInicial IS NULL OR pFechaFinal IS` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L101 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L129 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L152 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L162 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `concilia` | ACCION | conciliación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `apertura` | ENTIDAD | apertura (de cuenta/crédito) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `pagares` | ENTIDAD | pagarés | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultaconciliaaperturapagarescargos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaconciliaaperturapagarescargos_totales.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 346 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta apertura, pagarés y cargo (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaconciliaaperturapagarescargos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `mTotalApertura` | `MONEY(18,2)` | L11 |
| `mTotalCargo` | `MONEY(18,2)` | L12 |
| `mTotalDiferencia` | `MONEY(18,2)` | L13 |
| `dFechaMinSel` | `DATE` | L14 |
| `dFechaMaxSel` | `DATE` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_concilia_cargos_apertura` | `bdinvers` | ⚠️ sí | SELECT | L56 |
| `sv_difer_concilia_cargos_apertura` | `bdinvers` | ⚠️ sí | SELECT | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet             	= '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pTipoReporte = '' OR pFechaInicial IS NULL OR pFechaFinal IS` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `concilia` | ACCION | conciliación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `apertura` | ENTIDAD | apertura (de cuenta/crédito) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `pagares` | ENTIDAD | pagarés | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaconciliatraspcuentas_archivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaconciliatraspcuentas_archivo.sql` |
| **LOC (1er CREATE)** | 304 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 350 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas y archivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaconciliatraspcuentas_archivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
  pTipoReporte                 CHAR(1)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `cDescCodRetSp` | `CHAR(80)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iExiste` | `INTEGER` | L12 |
| `cEjecutivo` | `CHAR(8)` | L13 |
| `dfechaAlt` | `DATE` | L14 |
| `cFolioSuc` | `CHAR(30)` | L15 |
| `cNumCteCargo` | `CHAR(20)` | L16 |
| `cCuentaCargo` | `CHAR(20)` | L17 |
| `cTransaccCargo` | `CHAR(4)` | L18 |
| `cDescripTransCargo` | `CHAR(50)` | L19 |
| `cProductoCargo` | `CHAR(50)` | L20 |
| `cNumCteAbono` | `CHAR(10)` | L21 |
| `cCuentaAbono` | `CHAR(20)` | L22 |
| `cTransaccAbono` | `CHAR(4)` | L23 |
| `cDescripTransAbono` | `CHAR(50)` | L24 |
| `cProductoAbono` | `CHAR(50)` | L25 |
| `mMontoCargo` | `MONEY` | L26 |
| `mMontoAbono` | `MONEY` | L27 |
| `mDiferencia` | `MONEY` | L28 |
| `dFechaMinSel` | `DATE` | L29 |
| `dFechaMaxSel` | `DATE` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |
| *…10 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L133 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet 			    = '00000';` |  |
| L92 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pTipoReporte = '' OR pFechaInicial IS NULL OR pFechaFinal IS` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L133 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L134 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT NVL(TO_CHAR(fecha_alt, '%d/%m/%Y'), ''), ''''\|\|folio_suc,` | 🔴 MONEY/aritmética financiera |
| L146 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L147 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT NVL(TO_CHAR(fecha_alt, '%d/%m/%Y'), ''), ''''\|\|folio_suc,` | 🔴 MONEY/aritmética financiera |
| L155 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L183 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L188 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L197 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L206 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L246 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |
| L251 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralTxt);` |  |
| L260 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralTxt)\|\|".tmp > "\|\|TRIM(cRutaGralTxt);` |  |
| L269 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |
| L288 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_consultaconciliatraspcuentas_` |  |
| L290 | CÓDIGO_RETORNO | `LET cCodRet = '01018';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `concilia` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `?tra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tra` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaconciliatraspcuentas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaconciliatraspcuentas_totales.sql` |
| **LOC (1er CREATE)** | 119 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 349 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaconciliatraspcuentas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5)  		AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cMensajeRet` | `VARCHAR(80,1)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iIsamErr` | `INTEGER` | L12 |
| `cErrorInfo` | `VARCHAR(80,1)` | L13 |
| `dFechaMinSel` | `DATE` | L15 |
| `dFechaMaxSel` | `DATE` | L16 |
| `mMontoCargo` | `MONEY(18,2)` | L17 |
| `mMontoAbono` | `MONEY(18,2)` | L18 |
| `mDiferencia` | `MONEY(18,2)` | L19 |
| `iNumReg` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_concilia_trasp_cuentas` | `bdicheq` | ⚠️ sí | SELECT | L79 |
| `sc_difer_concilia_trasp_cuentas` | `bdicheq` | ⚠️ sí | SELECT | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pTipoReporte = '' OR pFechaInicial IS NULL OR pFechaFinal IS` |  |
| L65 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `concilia` | ACCION | conciliación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?tra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tra` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaconssubproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaconssubproductos.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 100 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sub-producto" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaconssubproductos(
  pUsuario                     CHAR(10)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(10)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iConsecutivo` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_subproducto` | `bdicred` | ⚠️ sí | SELECT | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iConsecutivo = (nvl(iConsecutivo, 0)::INTEGER) + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `subproducto` | ENTIDAD | sub-producto | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacorreo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacorreo.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta correo electrónico" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacorreo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  cNumApoderado                CHAR(20)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `cNumApoderado` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cCorreoE` | `CHAR(100)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_correos` | `bdinteg` | ⚠️ sí | SELECT | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `correo` | ENTIDAD | correo electrónico | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultactabloqueodesbloqueocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultactabloqueodesbloqueocre.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuenta y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_consultacuenta` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultactabloqueodesbloqueocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pTipoConsulta                CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cMensajeError` | `CHAR(80)` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cNumCuenta` | `CHAR(20)` | L18 |
| `cNumCliente` | `CHAR(20)` | L19 |
| `cNombreCliente` | `CHAR(50)` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cBloqueo` | `CHAR(30)` | L22 |
| `cCausa` | `CHAR(50)` | L23 |
| `cStatus` | `CHAR(2)` | L24 |
| `dFechaApertura` | `DATE` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L63 |
| `sp_consultacuenta` | `bdicred` | ⚠️ sí | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultacuenta';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00070';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00033';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00018';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00019';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00032';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `desbloqueo` | ACCION | desbloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultactadebitocredito`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultactadebitocredito.sql` |
| **LOC (1er CREATE)** | 285 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuenta, débito y crédito" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultactadebitocredito(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuentaClienteTarjeta        CHAR(20)
  pTipoBusqueda                SMALLINT
  pSistemaCuenta               CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuentaClienteTarjeta` | `CHAR(20)` | — | — |
| `pTipoBusqueda` | `SMALLINT` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cNumCuenta` | `CHAR(13)` | L18 |
| `cSistemaCuenta` | `CHAR(2)` | L19 |
| `cNumCliente` | `CHAR(20)` | L20 |
| `cNumTarjeta` | `CHAR(16)` | L21 |
| `cNumProducto` | `CHAR(4)` | L22 |
| `cDescProducto` | `CHAR(40)` | L23 |
| `cNombre1` | `CHAR(26)` | L24 |
| `cNombre2` | `CHAR(26)` | L25 |
| `cApellidoPaterno` | `CHAR(26)` | L26 |
| `cApellidoMaterno` | `CHAR(26)` | L27 |
| `cStatus` | `CHAR(30)` | L28 |
| `cCuenta` | `CHAR(20)` | L29 |
| `cTarjeta` | `CHAR(20)` | L30 |
| `cCliente` | `CHAR(20)` | L31 |
| `cEmpresa` | `CHAR(3)` | L32 |
| `iNoRegistros` | `INTEGER` | L33 |
| `iRecuperacion` | `INTEGER` | L34 |
| `iRegistros` | `INTEGER` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_producto` | `bdicheq` | ⚠️ sí | SELECT | L130 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L174 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L96 |
| `sp_consultacuentadebito` | `bdinteg` | ⚠️ sí | L114 |
| `sp_consultacuentacredito` | `bdinteg` | ⚠️ sí | L159 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L70 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCuentaClienteTarjeta = '' OR pTipoBusqueda IS NULL OR pSiste` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00049';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00048';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00024';` |  |
| L123 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN EJECUCION DE SP bdinteg:sp_consultactadebitocredito` |  |
| L137 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L142 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L164 | CÓDIGO_RETORNO | `LET cCodRet = '00024';` |  |
| L168 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN EJECUCION DE SP bdinteg:sp_consultactadebitocredito` |  |
| L181 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L186 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L191 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L209 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN EJECUCION DE SP bdinteg:sp_consultactadebitocredito` |  |
| L223 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L224 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L229 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L241 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN EJECUCION DE SP bdinteg:sp_consultactadebitocredito` |  |
| L254 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L255 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L260 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L265 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `debito` | ENTIDAD | débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultactastransfercap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultactastransfercap.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas, transferencia y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consctastransfersoc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultactastransfercap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(3)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cCuenta` | `CHAR(20)` | L18 |
| `cTarjeta` | `CHAR(16)` | L19 |
| `cProducto` | `CHAR(40)` | L20 |
| `cClabe` | `CHAR(18)` | L21 |
| `cFechaAlta` | `CHAR(10)` | L22 |
| `cStatusTarj` | `CHAR(10)` | L23 |
| `cStatusCta` | `CHAR(10)` | L24 |
| `cSucursal` | `CHAR(4)` | L25 |
| `cEjecutivo` | `CHAR(8)` | L26 |
| `cFechaCanc` | `CHAR(10)` | L27 |
| `iNoRegistros` | `INTEGER` | L28 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |
| `sp_consctastransfersoc` | `bdicnweb` | no | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consctastransfersoc';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L79 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultactesfusionados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultactesfusionados.sql` |
| **LOC (1er CREATE)** | 170 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 83 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clientes (fusionados)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultactesfusionados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cNoCliente1` | `CHAR(20)` | L22 |
| `cNoCliente2` | `CHAR(20)` | L23 |
| `cStatusFusion` | `CHAR(1)` | L24 |
| `cDescStatusFusion` | `CHAR(30)` | L25 |
| `cNombre1_1` | `CHAR(26)` | L26 |
| `cNombre2_1` | `CHAR(26)` | L27 |
| `cApPaterno_1` | `CHAR(26)` | L28 |
| `cApMaterno_1` | `CHAR(26)` | L29 |
| `cRfc_1` | `CHAR(13)` | L30 |
| `dFechaNacimiento_1` | `DATE` | L31 |
| `cNombre1_2` | `CHAR(26)` | L32 |
| `cNombre2_2` | `CHAR(26)` | L33 |
| `cApPaterno_2` | `CHAR(26)` | L34 |
| `cApMaterno_2` | `CHAR(26)` | L35 |
| `cRfc_2` | `CHAR(13)` | L36 |
| `dFechaNacimiento_2` | `DATE` | L37 |
| `iNoRegistrosCliente` | `INTEGER` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L90 |
| `sw_tr_clientesduplicados` | `bdicnweb` | no | SELECT | L104 |
| `si_fuscliente` | `bdinteg` | ⚠️ sí | SELECT | L134 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L156 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `fusionados` | MODIF | fusionados | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultactetransfercap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultactetransfercap.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cliente, transferencia y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consctetransfersoc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultactetransfercap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(3)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cNombreCte` | `CHAR(104)` | L11 |
| `cRFC` | `CHAR(13)` | L12 |
| `cTipoCte` | `CHAR(10)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_consctetransfersoc` | `bdicnweb` | no | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_consctetransfersoc';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultactualizaparamlimctas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultactualizaparamlimctas.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 81 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultactualizaparamlimctas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pCodParam                    CHAR(20)
  pValor                       CHAR(60)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pCodParam` | `CHAR(20)` | `param`=parámetro | ✅ CÓDIGO |
| `pValor` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cValorParam` | `CHAR(60)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L52 |
| `sc_param` | `bdicheq` | ⚠️ sí | UPDATE | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ctualiza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `param` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?li` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `?tas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ctualiza`, `?li`, `?tas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultactualizastatuscliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultactualizastatuscliente.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 80 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta estatus y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultactualizastatuscliente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pCliente                     CHAR(20)
  pStatus                      CHAR(1)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pCliente` | `CHAR(20)` | `cliente`=cliente | ✅ CÓDIGO |
| `pStatus` | `CHAR(1)` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_limite_cuentas` | `bdicheq` | ⚠️ sí | SELECT | L52 |
| `sc_limite_cuentas` | `bdicheq` | ⚠️ sí | INSERT | L61 |
| `sc_limite_cuentas` | `bdicheq` | ⚠️ sí | UPDATE | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ctualiza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cliente` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ctualiza` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacuentabloqueadacap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacuentabloqueadacap.sql` |
| **LOC (1er CREATE)** | 170 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuenta y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacuentabloqueadacap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L24 |
| `cCodRet` | `CHAR(5)` | L25 |
| `cCodRetSp` | `CHAR(5)` | L26 |
| `cCodRetBloq` | `CHAR(5)` | L27 |
| `cDescBloqueo` | `CHAR(50)` | L28 |
| `cMensaje` | `CHAR(60)` | L29 |
| `cMovimiento` | `CHAR(1)` | L30 |
| `cClave` | `CHAR(5)` | L31 |
| `cStatus` | `CHAR(1)` | L32 |
| `mImporteBloq` | `MONEY(14,2)` | L33 |
| `dFechaBloq` | `DATE` | L34 |
| `cUsuarioBloq` | `CHAR(8)` | L35 |
| `cMotivoBloq` | `CHAR(2)` | L36 |
| `cDescMotivoBloq` | `CHAR(50)` | L37 |
| `cOpcionBloq` | `CHAR(1)` | L38 |
| `cDescOpcionBloq` | `CHAR(50)` | L39 |
| `cAreaSolic` | `CHAR(20)` | L40 |
| `cTipoBloq` | `CHAR(20)` | L41 |
| `cCliente` | `CHAR(20)` | L42 |
| `cClaveArea` | `CHAR(2)` | L43 |
| `cCodigoArea` | `CHAR(1)` | L44 |
| `cClaveTipoBloq` | `CHAR(2)` | L45 |
| `cCodTipoBloq` | `CHAR(1)` | L46 |
| `iNoRegs` | `INTEGER` | L47 |
| `cBs` | `CHAR(1)` | L48 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_opcionbloqueo` | `bdicheq` | ⚠️ sí | SELECT | L156 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_blqvalbloqueocta` | `bdicheq` | ⚠️ sí | L101 |
| `sp_blqconsultabloqueo` | `bdicheq` | ⚠️ sí | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L115 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00174';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00175';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?da` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?da` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacuentasdiferencia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacuentasdiferencia.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacuentasdiferencia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `dFecha` | `DATE` | L26 |
| `cCuenta` | `CHAR (20)` | L27 |
| `cProducto` | `CHAR (4)` | L28 |
| `cNumCte` | `CHAR (20)` | L29 |
| `cSucursal` | `CHAR (4)` | L30 |
| `cEjecutivo` | `CHAR (8)` | L31 |
| `mCapitalAnterior` | `MONEY (18,2)` | L32 |
| `mMovsCargo` | `MONEY (18,2)` | L33 |
| `mMovsAbono` | `MONEY (18,2)` | L34 |
| `mCapitalCalculado` | `MONEY (18,2)` | L35 |
| `mCapitalActual` | `MONEY (18,2)` | L36 |
| `mDiferenciaCapital` | `MONEY (18,2)` | L37 |
| `mInteresAnterior` | `MONEY (18,2)` | L38 |
| `mMovsCargoInteres` | `MONEY (18,2)` | L39 |
| `mMovsAbonoInteres` | `MONEY (18,2)` | L40 |
| `mInteresCalculado` | `MONEY (18,2)` | L41 |
| `mInteresActual` | `MONEY (18,2)` | L42 |
| `mDiferenciaInteres` | `MONEY (18,2)` | L43 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_conciliachqdif` | `bdicheq` | ⚠️ sí | SELECT | L97 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L90 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha_Inicio = '' OR pFecha_Fin= '' OR pRegistros IS NULL  O` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L100 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?diferencia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?diferencia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacuentasdiferencia_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacuentasdiferencia_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacuentasdiferencia_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_conciliachqdif` | `bdicheq` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?diferencia_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?diferencia_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultacuentasdormidas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultacuentasdormidas.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuentas (cuentas dormidas)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultacuentasdormidas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret, MONEY(10,2) AS Monto_deposito, SMALLINT AS NumDiasInac, SMALLINT AS iNumDiasBloq
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `mMontoDeposito` | `MONEY(10,2)` | L6 |
| `iNumDiasInac` | `SMALLINT` | L7 |
| `iNumDiasBloq` | `SMALLINT` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param_ctasdormidas` | `bdicheq` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dormidas` | MODIF | cuentas dormidas (inactivas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadatosbanco`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadatosbanco.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta datos y banco" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadatosbanco(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveBanco                  INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveBanco` | `INTEGER` | `banco`=banco | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `iClaveBanco` | `INTEGER` | L19 |
| `cNombreLargo` | `CHAR(40)` | L20 |
| `cNombreCorto` | `CHAR(20)` | L21 |
| `dFecha` | `DATE` | L22 |
| `iIndSpei` | `CHAR(1)` | L23 |
| `iIndCheques` | `CHAR(1)` | L24 |
| `iIndNomina` | `CHAR(1)` | L25 |
| `iIndTefR` | `CHAR(1)` | L26 |
| `iIndTefP` | `CHAR(1)` | L27 |
| `iIndDomiR` | `CHAR(1)` | L28 |
| `iIndDomiP` | `CHAR(1)` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pClaveBanco IS NULL THEN` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L76 | VALIDACIÓN_NULL | `IF cNombreLargo IS NULL AND cNombreCorto IS NULL AND dFecha IS NULL THEN` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `banco` | ENTIDAD | banco | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadefporpoductodigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadefporpoductodigitalizacion.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta digitalización de documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dgconsultadefinicionesporproducto_soc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadefporpoductodigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodSistema                  CHAR(2)
  pCodProducto                 CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodSistema` | `CHAR(2)` | — | — |
| `pCodProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cCodDefinicion` | `CHAR(3)` | L13 |
| `cProdNombre` | `CHAR(50)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_dgconsultadefinicionesporproducto_soc` | `bdidigital` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00370';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00364';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?def` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?poducto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?def`, `?poducto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadelvorevcod46total_ccep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadelvorevcod46total_ccep.sql` |
| **LOC (1er CREATE)** | 284 |
| **Callgraph** | ✅ fan_in=78 / fan_out=37 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código y CCE — Cámara de Compensación Electrónica (total)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadelvorevcod46total_ccep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechadevol                  DATE
  pFechaHoy                    DATE
  pDireccionMac                CHAR(15)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechadevol` | `DATE` | — | — |
| `pFechaHoy` | `DATE` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `codRet3` | `CHAR(3)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cBanco` | `CHAR(3)` | L13 |
| `cDesc_banco` | `CHAR(40)` | L14 |
| `cCtaReferencia` | `CHAR(40)` | L15 |
| `iNumCheque` | `INTEGER` | L16 |
| `dMonto_orig` | `DECIMAL(14,2)` | L17 |
| `cNumCuentaDep` | `CHAR(20)` | L18 |
| `cSucursal` | `CHAR(44)` | L19 |
| `cSiTransacc` | `CHAR(4)` | L20 |
| `cCodDescDevo` | `CHAR(37)` | L21 |
| `cDigitalizado` | `CHAR(1)` | L22 |
| `cCompensacion` | `CHAR(3)` | L23 |
| `cTransacc` | `CHAR(2)` | L24 |
| `cCodseguridad` | `CHAR(3)` | L25 |
| `cDigverpre` | `CHAR(1)` | L26 |
| `cDigverinter` | `CHAR(1)` | L27 |
| `iTotalValidos` | `INTEGER` | L28 |
| `dMontoTotalValido` | `DECIMAL(20,2)` | L29 |
| `cTipoCuentaDep` | `CHAR(2)` | L30 |
| `cCampoCliente` | `CHAR(20)` | L31 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_encabezado` | `bditef` | ⚠️ sí | SELECT | L117 |
| `ccep_procesacod46detalle_tmp` | `bdicnweb` | no | SELECT | L131 |
| `ccep_procesacod46detalle_tmp` | `bdicnweb` | no | DELETE | L131 |
| `cce_cheques_dev` | `bditef` | ⚠️ sí | SELECT | L190 |
| `cce_detalle` | `bditef` | ⚠️ sí | SELECT | L214 |
| `ccep_procesacod46detalle_tmp` | `bdicnweb` | no | INSERT | L255 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L102 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDireccionMac = '' OR pFechadevol IS NULL OR pFechaHoy IS NUL` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L140 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_cce_consultar_cheques46';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L157 | FÓRMULA | `LET iTotalValidos = iTotalValidos + 1;` |  |
| L169 | VALIDACIÓN_NULL | `IF cTipoCuentaDep IS NULL THEN -- ERROR DE QUE NO SE ENCONTRO EL MAPEO CECOBAN` |  |
| L253 | FÓRMULA | `LET dMontoTotalValido = dMontoTotalValido + dMonto_orig;` | 🔴 MONEY/aritmética financiera |
| L264 | FÓRMULA | `LET iTotalValidos = iTotalValidos - 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?delvo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?46` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?delvo`, `?46`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadenominacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadenominacionescaja.sql` |
| **LOC (1er CREATE)** | 240 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta denominaciones y caja" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadenominacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProvCaja                  CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L38 |
| `cCodRetSp` | `CHAR(6)` | L39 |
| `iCodRetSp` | `INTEGER` | L40 |
| `iSqlErr` | `INTEGER` | L41 |
| `mSaldoAnterior` | `MONEY(14,2)` | L42 |
| `mSaldoTotal` | `MONEY(14,2)` | L43 |
| `dDisponible1` | `INTEGER` | L44 |
| `cDenominacion1` | `CHAR(18)` | L45 |
| `dDisponible2` | `INTEGER` | L46 |
| `cDenominacion2` | `CHAR(18)` | L47 |
| `dDisponible3` | `INTEGER` | L48 |
| `cDenominacion3` | `CHAR(18)` | L49 |
| `dDisponible4` | `INTEGER` | L50 |
| `cDenominacion4` | `CHAR(18)` | L51 |
| `dDisponible5` | `INTEGER` | L52 |
| `cDenominacion5` | `CHAR(18)` | L53 |
| `dDisponible6` | `INTEGER` | L54 |
| `cDenominacion6` | `CHAR(18)` | L55 |
| `dDisponible7` | `INTEGER` | L56 |
| `cDenominacion7` | `CHAR(18)` | L57 |
| `dDisponible8` | `INTEGER` | L58 |
| `cDenominacion8` | `CHAR(18)` | L59 |
| `dDisponible9` | `INTEGER` | L60 |
| `cDenominacion9` | `CHAR(18)` | L61 |
| `dDisponible10` | `INTEGER` | L62 |
| *…16 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L143 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L165 | VALIDACIÓN_NULL | `IF mSaldoTotal IS NULL THEN` |  |
| L168 | VALIDACIÓN_NULL | `IF mSaldoAnterior IS NULL THEN` |  |
| L222 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L226 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `denominaciones` | ENTIDAD | denominaciones | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadepositoscoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadepositoscoppel.sql` |
| **LOC (1er CREATE)** | 138 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 95 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta depósito, OS — Originación de Solicitudes y Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadepositoscoppel(
  pUsuario                     CHAR(10)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(10)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `cClave_transaccion` | `CHAR(4)` | L22 |
| `cDesc_transaccion` | `CHAR(50)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |
| `dFecha` | `DATE` | L27 |
| `cComprobante` | `CHAR(10)` | L28 |
| `cSucCoppel` | `CHAR(10)` | L29 |
| `cNombreSuc` | `CHAR(40)` | L30 |
| `cCajaGeneral` | `CHAR(4)` | L31 |
| `cPlaza` | `CHAR(20)` | L32 |
| `mImpComprobante` | `MONEY(14,2)` | L33 |
| `mImpFicha` | `MONEY(14,2)` | L34 |
| `iCantidad1` | `INTEGER` | L35 |
| `iCantidad2` | `INTEGER` | L36 |
| `iCantidad3` | `INTEGER` | L37 |
| `iCantidad4` | `INTEGER` | L38 |
| `iCantidad5` | `INTEGER` | L39 |
| `iCantidad6` | `INTEGER` | L40 |
| `iCantidad7` | `MONEY(14,2)` | L41 |
| `mFaltante` | `MONEY(14,2)` | L42 |
| `mSobrante` | `MONEY(14,2)` | L43 |
| `iTotReg` | `INTEGER` | L44 |
| `cMensaje` | `CHAR(60)` | L45 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_temp_deposito_coppel` | `bdisuc` | ⚠️ sí | SELECT | L113 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L91 | VALIDACIÓN_NULL | `IF  pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L115 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?it` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadesdocumentosctemc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadesdocumentosctemc.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta saldo, OS — Originación de Solicitudes, cliente y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadesdocumentosctemc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClienteCoppel               CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClienteCoppel` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cDescripcion` | `CHAR(35)` | L7 |
| `iNoRegs` | `INTEGER` | L8 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `?cument` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?de`, `?cument` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadetallearchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetallearchivo.sql` |
| **LOC (1er CREATE)** | 183 |
| **Callgraph** | ✅ fan_in=10 / fan_out=14 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle y archivo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetallearchivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pTipo                        CHAR(3)
  pStatus                      CHAR(16)
  pFechaConsulta               DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `archivo`=archivo | ✅ CÓDIGO |
| `pTipo` | `CHAR(3)` | — | — |
| `pStatus` | `CHAR(16)` | — | — |
| `pFechaConsulta` | `DATE` | `consulta`=consulta / lee | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cCodRetSp` | `CHAR(6)` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `dFechaHoy` | `DATE` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `iNumCheque` | `INTEGER` | L22 |
| `mImporte` | `MONEY(16,2)` | L23 |
| `cCodOperacion` | `CHAR(2)` | L24 |
| `cImgStat1` | `CHAR(1)` | L25 |
| `cImgStat2` | `CHAR(1)` | L26 |
| `cStatus` | `CHAR(2)` | L27 |
| `cCodigoRet` | `CHAR(5)` | L28 |
| `dFechaProceso` | `DATE` | L29 |
| `cMotivoDev` | `CHAR(2)` | L30 |
| `cTruncamiento` | `CHAR(1)` | L31 |
| `cImgF` | `CHAR(3)` | L32 |
| `cImgT` | `CHAR(3)` | L33 |
| `cDescStatus` | `CHAR(16)` | L34 |
| `iTotImagenRec` | `INTEGER` | L35 |
| `cDescMotivoDev` | `CHAR(35)` | L36 |
| `iRecuperacion` | `INTEGER` | L37 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L107 |
| `si_coddevcam` | `bdinteg` | ⚠️ sí | SELECT | L155 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pFechaConsulta IS NULL OR pRegistros I` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00783';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00784';` |  |
| L114 | FÓRMULA | `LET iTotImagenRec = iTotImagenRec + 1;` |  |
| L121 | FÓRMULA | `LET iTotImagenRec = iTotImagenRec + 1;` |  |
| L158 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L164 | CÓDIGO_RETORNO | `LET cCodRet = '00785';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadetallearchivo_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetallearchivo_totales.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=10 / fan_out=14 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle y archivo (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetallearchivo_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pTipo                        CHAR(3)
  pStatus                      CHAR(16)
  pFechaConsulta               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `archivo`=archivo | ✅ CÓDIGO |
| `pTipo` | `CHAR(3)` | — | — |
| `pStatus` | `CHAR(16)` | — | — |
| `pFechaConsulta` | `DATE` | `consulta`=consulta / lee | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `dFechaHoy` | `DATE` | L10 |
| `cCuenta` | `CHAR(20)` | L11 |
| `iNumCheque` | `INTEGER` | L12 |
| `mImporte` | `MONEY(16,2)` | L13 |
| `cCodOperacion` | `CHAR(2)` | L14 |
| `cImgStat1` | `CHAR(1)` | L15 |
| `cImgStat2` | `CHAR(1)` | L16 |
| `cStatus` | `CHAR(2)` | L17 |
| `cCodigoRet` | `CHAR(5)` | L18 |
| `dFechaProceso` | `DATE` | L19 |
| `cMotivoDev` | `CHAR(2)` | L20 |
| `cTruncamiento` | `CHAR(1)` | L21 |
| `cImgF` | `CHAR(3)` | L22 |
| `cImgT` | `CHAR(3)` | L23 |
| `cDescStatus` | `CHAR(16)` | L24 |
| `iTotImagenRec` | `INTEGER` | L25 |
| `iNumRegistros` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L85 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pFechaConsulta IS NULL THEN` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00783';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00784';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00785';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultadetallecatalogocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetallecatalogocaja.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, catálogo y caja" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetallecatalogocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodProveedor` | `CHAR(4)` | L15 |
| `cDescProveedor` | `CHAR(30)` | L16 |
| `cIdDivisa` | `CHAR(2)` | L17 |
| `cDescDivisa` | `CHAR(30)` | L18 |
| `cIdPlaza` | `CHAR(3)` | L19 |
| `cDescPlaza` | `CHAR(40)` | L20 |
| `cEmpresa` | `CHAR(3)` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadetallechequesdev`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetallechequesdev.sql` |
| **LOC (1er CREATE)** | 319 |
| **Callgraph** | ✅ fan_in=8 / fan_out=12 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle y cheques" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetallechequesdev(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pBandera                     CHAR(1)
  pTipo                        CHAR(1)
  pDireccionMac                CHAR(12)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L41 |
| `iSqlErr` | `INTEGER` | L42 |
| `cCodRetSp` | `CHAR(5)` | L43 |
| `iCodRetSp` | `INTEGER` | L44 |
| `dFechaMasUno` | `DATE` | L45 |
| `dFechaHabilProx` | `DATE` | L46 |
| `cFormatFecha` | `CHAR(8)` | L47 |
| `cFormatFechaMasUno` | `CHAR(8)` | L48 |
| `cFormatFechaProx` | `CHAR(8)` | L49 |
| `iNumBloque` | `INT` | L50 |
| `cNumBloque` | `CHAR(2)` | L51 |
| `cNombreArchivo` | `CHAR(22)` | L52 |
| `cStatusParam` | `CHAR(2)` | L53 |
| `cBcoPresenta` | `CHAR(3)` | L54 |
| `cNombreBanco` | `CHAR(40)` | L55 |
| `cCuenta` | `CHAR(20)` | L56 |
| `iCheque` | `INTEGER` | L57 |
| `mImporte` | `MONEY(16,2)` | L58 |
| `cMotDevol` | `CHAR(2)` | L59 |
| `cGeneradoDev` | `CHAR(1)` | L60 |
| `cFechaTransfer` | `CHAR(8)` | L61 |
| `cImporte` | `CHAR(15)` | L62 |
| `cLoteEntrada` | `CHAR(7)` | L63 |
| `cSecEntrada` | `CHAR(4)` | L64 |
| `cLoteSalida` | `CHAR(7)` | L65 |
| *…31 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_encabezado` | `bditef` | ⚠️ sí | SELECT | L209 |
| `sw_cr_consultadetallechequesdev` | `bdicnweb` | no | SELECT | L231 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L190 |
| `cal_fecha_pre_fh` | `bditef` | ⚠️ sí | L200 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L98 | CÓDIGO_RETORNO | `LET cCodRet                     = '00000';` |  |
| L169 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pBandera = '' OR pTipo = '' OR pDireccionMa` |  |
| L171 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L181 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L248 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L279 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | 🔵 CONVENCIÓN | nombre_sp |
| `dev` | ACCION | devolución | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadetallechequesdev_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetallechequesdev_totales.sql` |
| **LOC (1er CREATE)** | 339 |
| **Callgraph** | ✅ fan_in=8 / fan_out=12 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle y cheques (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetallechequesdev_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pBandera                     CHAR(1)
  pTipo                        CHAR(1)
  pDireccionMac                CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `dFechaMasUno` | `DATE` | L11 |
| `dFechaHabilProx` | `DATE` | L12 |
| `cFormatFecha` | `CHAR(8)` | L13 |
| `cFormatFechaMasUno` | `CHAR(8)` | L14 |
| `cFormatFechaProx` | `CHAR(8)` | L15 |
| `iNumBloque` | `INT` | L16 |
| `cNumBloque` | `CHAR(2)` | L17 |
| `cNombreArchivo` | `CHAR(22)` | L18 |
| `cStatusParam` | `CHAR(2)` | L19 |
| `cBcoPresenta` | `CHAR(3)` | L20 |
| `cNombreBanco` | `CHAR(40)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `iCheque` | `INTEGER` | L23 |
| `mImporte` | `MONEY(16,2)` | L24 |
| `cMotDevol` | `CHAR(2)` | L25 |
| `cGeneradoDev` | `CHAR(1)` | L26 |
| `cFechaTransfer` | `CHAR(8)` | L27 |
| `cImporte` | `CHAR(15)` | L28 |
| `cLoteEntrada` | `CHAR(7)` | L29 |
| `cSecEntrada` | `CHAR(4)` | L30 |
| `cLoteSalida` | `CHAR(7)` | L31 |
| *…31 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_encabezado` | `bditef` | ⚠️ sí | SELECT | L152 |
| `sw_cr_consultadetallechequesdev` | `bdicnweb` | no | SELECT | L163 |
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L184 |
| `cce_detalle` | `bditef` | ⚠️ sí | SELECT | L190 |
| `cce_propios_det` | `bditef` | ⚠️ sí | UPDATE | L199 |
| `si_coddevcam` | `bdinteg` | ⚠️ sí | SELECT | L215 |
| `sw_cr_consultadetallechequesdev` | `bdicnweb` | no | INSERT | L218 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L137 |
| `cal_fecha_pre_fh` | `bditef` | ⚠️ sí | L143 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L64 | CÓDIGO_RETORNO | `LET cCodRet                     = '00000';` |  |
| L131 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pBandera = '' OR pTipo = '' OR pDireccionMa` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L204 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L211 | FÓRMULA | `LET iCountReg = iCountReg + 1;` |  |
| L212 | FÓRMULA | `LET mMontoTotalValido = mMontoTotalValido + NVL(mImporte,0);` | 🔴 MONEY/aritmética financiera |
| L264 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L271 | FÓRMULA | `LET iCountReg = iCountReg + 1;` |  |
| L272 | FÓRMULA | `LET mMontoTotalValido = mMontoTotalValido + NVL(mImporte,0);` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cheques` | ENTIDAD | cheques | 🔵 CONVENCIÓN | nombre_sp |
| `dev` | ACCION | devolución | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultadetalledotacioncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetalledotacioncaja.sql` |
| **LOC (1er CREATE)** | 327 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, dotación de efectivo y caja" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetalledotacioncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolioOperacion              CHAR(8)
  pCodTrans                    CHAR(4)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolioOperacion` | `CHAR(8)` | — | — |
| `pCodTrans` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L41 |
| `iSqlErr` | `INTEGER` | L42 |
| `fCant1` | `DECIMAL(10,2)` | L43 |
| `cDenominacion1` | `CHAR(18)` | L44 |
| `mTotal1` | `MONEY(14,2)` | L45 |
| `fCant2` | `DECIMAL(10,2)` | L46 |
| `cDenominacion2` | `CHAR(18)` | L47 |
| `mTotal2` | `MONEY(14,2)` | L48 |
| `fCant3` | `DECIMAL(10,2)` | L49 |
| `cDenominacion3` | `CHAR(18)` | L50 |
| `mTotal3` | `MONEY(14,2)` | L51 |
| `fCant4` | `DECIMAL(10,2)` | L52 |
| `cDenominacion4` | `CHAR(18)` | L53 |
| `mTotal4` | `MONEY(14,2)` | L54 |
| `fCant5` | `DECIMAL(10,2)` | L55 |
| `cDenominacion5` | `CHAR(18)` | L56 |
| `mTotal5` | `MONEY(14,2)` | L57 |
| `fCant6` | `DECIMAL(10,2)` | L58 |
| `cDenominacion6` | `CHAR(18)` | L59 |
| `mTotal6` | `MONEY(14,2)` | L60 |
| `fCant7` | `DECIMAL(10,2)` | L61 |
| `cDenominacion7` | `CHAR(18)` | L62 |
| `mTotal7` | `MONEY(14,2)` | L63 |
| `fCant8` | `DECIMAL(10,2)` | L64 |
| `cDenominacion8` | `CHAR(18)` | L65 |
| *…49 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_cajageneral` | `bdisuc` | ⚠️ sí | SELECT | L293 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L215 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L207 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L306 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L312 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadetalleenviodinya`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetalleenviodinya.sql` |
| **LOC (1er CREATE)** | 143 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle y DINYA — sistema/plataforma de remesas domésticas en sucursal; retorna nombre_remitente, sucursal_origen, importe_eviado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_dinya_obtenerdetalleenvio` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetalleenviodinya(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumeroControl               CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumeroControl` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cCodRetSp` | `CHAR(5)` | L30 |
| `cNumControl` | `CHAR(12)` | L31 |
| `dFechaEnvio` | `DATE` | L32 |
| `cHoraEnvio` | `CHAR(8)` | L33 |
| `cSucursalOrigen` | `CHAR(4)` | L34 |
| `cNombre1Remitente` | `CHAR(26)` | L35 |
| `cNombre2Remitente` | `CHAR(26)` | L36 |
| `cApellidoPaternoRemitente` | `CHAR(26)` | L37 |
| `cApellidoMaternoRemitente` | `CHAR(26)` | L38 |
| `cTelefonoRemitente` | `CHAR(20)` | L39 |
| `mImporteEnviado` | `MONEY(16,2)` | L40 |
| `cFolioSucursalEnvio` | `CHAR(16)` | L41 |
| `dFechaPago` | `DATE` | L42 |
| `cHoraPago` | `CHAR(8)` | L43 |
| `cSucursalCobroPago` | `CHAR(8)` | L44 |
| `cNombre1Beneficiario` | `CHAR(26)` | L45 |
| `cNombre2Beneficiario` | `CHAR(26)` | L46 |
| `cApellidoPaternoBeneficiario` | `CHAR(26)` | L47 |
| `cApellidoMaternoBeneficiario` | `CHAR(26)` | L48 |
| `cTelefonoBeneficiario` | `CHAR(20)` | L49 |
| `cEstatus` | `CHAR(20)` | L50 |
| `cIdentificacion` | `CHAR(2)` | L51 |
| `cNumIdentificacion` | `CHAR(25)` | L52 |
| *…2 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_dinya_obtenerdetalleenvio` | `bdisac` | ⚠️ sí | L118 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L125 | VALIDACIÓN_NULL | `IF cNumControl = '' AND dFechaEnvio IS NULL THEN` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `dinya` | ENTIDAD | DINYA — sistema/plataforma de remesas domésticas en sucursal | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultadetallemonitorefectivocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetallemonitorefectivocaja.sql` |
| **LOC (1er CREATE)** | 223 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, monitor, efectivo y caja" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdisuc` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetallemonitorefectivocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodProveedor                CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodProveedor` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `cCodRetSp` | `CHAR(6)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cEmpresa` | `CHAR(3)` | L30 |
| `cCodProveedor` | `CHAR(4)` | L31 |
| `cDescProveedor` | `CHAR(30)` | L32 |
| `cPlaza` | `CHAR(3)` | L33 |
| `cDescPlaza` | `CHAR(40)` | L34 |
| `mSaldoAnterior` | `MONEY(14)` | L35 |
| `mSaldoTotal` | `MONEY(14)` | L36 |
| `mSaldoAsignado` | `MONEY(14)` | L37 |
| `mTotalBillDet` | `FLOAT(8)` | L38 |
| `mSaldoDisponible` | `FLOAT(8)` | L39 |
| `cDivisa` | `CHAR(2)` | L40 |
| `cDescDivisa` | `CHAR(30)` | L41 |
| `cDenominacion_1` | `CHAR(18)` | L42 |
| `cDenominacion_2` | `CHAR(18)` | L43 |
| `cDenominacion_3` | `CHAR(18)` | L44 |
| `cDenominacion_4` | `CHAR(18)` | L45 |
| `cDenominacion_5` | `CHAR(18)` | L46 |
| `cDenominacion_6` | `CHAR(18)` | L47 |
| `cDenominacion_7` | `CHAR(18)` | L48 |
| `cDenominacion_8` | `CHAR(18)` | L49 |
| `cDenominacion_9` | `CHAR(18)` | L50 |
| *…31 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L161 |
| `bdisuc` | `bdicnweb` | no | L172 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L182 | FÓRMULA | `LET mMontoTotal1 = NVL(cDenominacion_1::INTEGER * dCantidad_1,0);` | 🔴 MONEY/aritmética financiera |
| L183 | FÓRMULA | `LET mMontoTotal2 = NVL(cDenominacion_2::INTEGER * dCantidad_2,0);` | 🔴 MONEY/aritmética financiera |
| L184 | FÓRMULA | `LET mMontoTotal3 = NVL(cDenominacion_3::INTEGER * dCantidad_3,0);` | 🔴 MONEY/aritmética financiera |
| L185 | FÓRMULA | `LET mMontoTotal4 = NVL(cDenominacion_4::INTEGER * dCantidad_4,0);` | 🔴 MONEY/aritmética financiera |
| L186 | FÓRMULA | `LET mMontoTotal5 = NVL(cDenominacion_5::INTEGER * dCantidad_5,0);` | 🔴 MONEY/aritmética financiera |
| L187 | FÓRMULA | `LET mMontoTotal6 = NVL(cDenominacion_6::INTEGER * dCantidad_6,0);` | 🔴 MONEY/aritmética financiera |
| L189 | FÓRMULA | `LET mTotal = mMontoTotal1 + mMontoTotal2 + mMontoTotal3 + mMontoTotal4 + mMontoTotal5 + mMontoTotal6` | 🔴 MONEY/aritmética financiera |
| L192 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:consultacajageneral';` |  |
| L194 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L199 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L207 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `efectivo` | ENTIDAD | efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadetalleoperacionmonitorcaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetalleoperacionmonitorcaja.sql` |
| **LOC (1er CREATE)** | 303 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, monitor y caja" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetalleoperacionmonitorcaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoFolio                     CHAR(8)
  pCodStatus                   CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoFolio` | `CHAR(8)` | — | — |
| `pCodStatus` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L46 |
| `iSqlErr` | `INTEGER` | L47 |
| `fCant1` | `DECIMAL(10,2)` | L48 |
| `cDenominacion1` | `CHAR(18)` | L49 |
| `mTotal1` | `MONEY(14,2)` | L50 |
| `fCant2` | `DECIMAL(10,2)` | L51 |
| `cDenominacion2` | `CHAR(18)` | L52 |
| `mTotal2` | `MONEY(14,2)` | L53 |
| `fCant3` | `DECIMAL(10,2)` | L54 |
| `cDenominacion3` | `CHAR(18)` | L55 |
| `mTotal3` | `MONEY(14,2)` | L56 |
| `fCant4` | `DECIMAL(10,2)` | L57 |
| `cDenominacion4` | `CHAR(18)` | L58 |
| `mTotal4` | `MONEY(14,2)` | L59 |
| `fCant5` | `DECIMAL(10,2)` | L60 |
| `cDenominacion5` | `CHAR(18)` | L61 |
| `mTotal5` | `MONEY(14,2)` | L62 |
| `fCant6` | `DECIMAL(10,2)` | L63 |
| `cDenominacion6` | `CHAR(18)` | L64 |
| `mTotal6` | `MONEY(14,2)` | L65 |
| `fCant7` | `DECIMAL(10,2)` | L66 |
| `cDenominacion7` | `CHAR(18)` | L67 |
| `mTotal7` | `MONEY(14,2)` | L68 |
| `fCant8` | `DECIMAL(10,2)` | L69 |
| `cDenominacion8` | `CHAR(18)` | L70 |
| *…46 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L258 |
| `si_divisas` | `bdinteg` | ⚠️ sí | SELECT | L261 |
| `ss_catstatus` | `bdisuc` | ⚠️ sí | SELECT | L263 |
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L267 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L280 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L212 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L205 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L270 | VALIDACIÓN_NULL | `IF dFechRecepcion IS NULL AND cHoRecepcion IS NULL AND cUsRecepcion IS NULL THEN` |  |
| L271 | VALIDACIÓN_NULL | `IF dFechReversion IS NULL AND cHoReversion IS NULL AND cUsReversion IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operacion` | ACCION | operación | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultadetallesaldosctascap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetallesaldosctascap.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 172 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta detalle, saldos, cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetallesaldosctascap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `mImp_chq_sbc` | `MONEY(14,2)` | L13 |
| `mImp_chq_sbg` | `MONEY(14,2)` | L14 |
| `mSdo_retenido` | `MONEY(14,2)` | L15 |
| `mSdo_cong` | `MONEY(14,2)` | L16 |
| `mCom_pendiente` | `MONEY(14,2)` | L17 |
| `dFecha_proceso` | `DATE` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultadetlide`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadetlide.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta detalle y identificador" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadetlide(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRfc                         CHAR(13)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pRfc` | `CHAR(13)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cAnioMes` | `CHAR(6)` | L10 |
| `cNumCliente` | `CHAR(20)` | L11 |
| `cReferenciaRetencion` | `CHAR(20)` | L12 |
| `dFechaRet` | `DATE` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_detlide` | `bdilide` | ⚠️ sí | SELECT | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadevcam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadevcam.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=25 / fan_out=23 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadevcam(
) RETURNING CHAR(5)    AS CodigoRetorno,
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSql_Err` | `INTEGER` | L7 |
| `iSam_Err` | `INTEGER` | L8 |
| `cDesc_Err` | `CHAR(60)` | L9 |
| `cCodigo` | `CHAR(2)` | L10 |
| `cDescripcion` | `CHAR(35)` | L11 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `cam` | PREFIJO | cámara / captura contable | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultadevexttef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadevexttef.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadevexttef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pFechaArch                   CHAR(8)
  pRegistros                   INT
  pRecuperacion                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pFechaArch` | `CHAR(8)` | — | — |
| `pRegistros` | `INT` | — | — |
| `pRecuperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |
| `iNoRegistrosC` | `INTEGER` | L22 |
| `iIdx` | `INTEGER` | L23 |
| `vsFechaAbono` | `CHAR(8)` | L24 |
| `vsCuenta` | `CHAR(20)` | L25 |
| `vsReferencia` | `CHAR(18)` | L26 |
| `vsBanco` | `CHAR(40)` | L27 |
| `vsImporte` | `CHAR(30)` | L28 |
| `vsEstatus` | `CHAR(10)` | L29 |
| `vsConfirmado` | `CHAR(1)` | L30 |
| `vsNumSecuencia` | `CHAR(7)` | L31 |
| `vsClaveRastreo` | `CHAR(30)` | L32 |
| `vsCveStatus` | `CHAR(2)` | L33 |
| `vsMotDev` | `CHAR(2)` | L34 |
| `vsEstatusReg` | `CHAR(1)` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tf_consdevext_tef` | `bdicnweb` | no | SELECT | L86 |
| `sw_tf_consdevext_tef` | `bdicnweb` | no | DELETE | L86 |
| `sw_tf_consdevext_tef` | `bdicnweb` | no | INSERT | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L77 |
| `sp_consdevext_tef` | `bditef` | ⚠️ sí | L93 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L97 | FÓRMULA | `LET iNoRegistros = iNoRegistros + DBINFO('sqlca.sqlerrd2');` |  |
| L112 | FÓRMULA | `LET iNoRegistrosC = iNoRegistrosC +	1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadocdijotraerctedigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadocdijotraerctedigitalizacion.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cliente y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dgconsultadocumentosdijotraercliente` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadocdijotraerctedigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pProducto                    CHAR(4)
  pAccion                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pAccion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `vParametros` | `CHAR(100)` | L13 |
| `cCodigoUsuario` | `CHAR(8)` | L14 |
| `dFechaAlta` | `DATE` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_dgconsultadocumentosdijotraercliente` | `bdidigital` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00364';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00366';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?docdijo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `trae` | ACCION | trae / recupera (verbo — sp_*_trae — bdisuc) | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?docdijo`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadocsegundadefdigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadocsegundadefdigitalizacion.sql` |
| **LOC (1er CREATE)** | 201 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 84 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Seguridad y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dgconsultadocumentossegundefinicion`, `sp_dgconsultadocumentossegundefinicion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadocsegundadefdigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodDefinicion               CHAR(3)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodDefinicion` | `CHAR(3)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(6)` | L22 |
| `iCodRetSp` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |
| `iNoRegs` | `INTEGER` | L28 |
| `sSecuencia` | `SMALLINT` | L30 |
| `cCodigoDocumento` | `CHAR(4)` | L31 |
| `cDescripcionDocumento` | `CHAR(50)` | L32 |
| `cCodigoGrupo` | `CHAR(3)` | L33 |
| `cDescripcionGrupo` | `CHAR(50)` | L34 |
| `cMultiImagen` | `CHAR(1)` | L35 |
| `iImgTamanioMax` | `INTEGER` | L36 |
| `cImgFormato` | `CHAR(3)` | L37 |
| `sImgCompresion` | `SMALLINT` | L38 |
| `bImgLargo` | `DECIMAL` | L39 |
| `bImgAncho` | `DECIMAL` | L40 |
| `sImgDPI` | `SMALLINT` | L41 |
| `cImgColores` | `CHAR(1)` | L42 |
| `cGenerico` | `CHAR(1)` | L43 |
| `cLigar` | `CHAR(1)` | L44 |
| `cFuncion` | `CHAR(10)` | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L92 |
| `sp_dgconsultadocumentossegundefinicion` | `bdicnweb` | no | L100 |
| `sp_dgconsultadocumentossegundefinicion` | `bdidigital` | ⚠️ sí | L147 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00371';` |  |
| L115 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L118 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L121 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L153 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L155 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00371';` |  |
| L162 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L165 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L168 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L178 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L185 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?doc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | 🔵 CONVENCIÓN | nombre_sp |
| `?undadef` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?doc`, `?undadef` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadocsegundadefdigitalizacion2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadocsegundadefdigitalizacion2.sql` |
| **LOC (1er CREATE)** | 206 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 119 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Seguridad y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dgconsultadocumentossegundefinicion`, `sp_dgconsultadocumentossegundefinicion2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadocsegundadefdigitalizacion2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodDefinicion               CHAR(3)
  pProducto                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodDefinicion` | `CHAR(3)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(6)` | L22 |
| `iCodRetSp` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |
| `iNoRegs` | `INTEGER` | L28 |
| `sSecuencia` | `SMALLINT` | L30 |
| `cCodigoDocumento` | `CHAR(4)` | L31 |
| `cDescripcionDocumento` | `CHAR(50)` | L32 |
| `cCodigoGrupo` | `CHAR(3)` | L33 |
| `cDescripcionGrupo` | `CHAR(50)` | L34 |
| `cMultiImagen` | `CHAR(1)` | L35 |
| `iImgTamanioMax` | `INTEGER` | L36 |
| `cImgFormato` | `CHAR(3)` | L37 |
| `sImgCompresion` | `SMALLINT` | L38 |
| `bImgLargo` | `DECIMAL` | L39 |
| `bImgAncho` | `DECIMAL` | L40 |
| `sImgDPI` | `SMALLINT` | L41 |
| `cImgColores` | `CHAR(1)` | L42 |
| `cGenerico` | `CHAR(1)` | L43 |
| `cLigar` | `CHAR(1)` | L44 |
| `cFuncion` | `CHAR(10)` | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L93 |
| `sp_dgconsultadocumentossegundefinicion` | `bdicnweb` | no | L101 |
| `sp_dgconsultadocumentossegundefinicion2` | `bdidigital` | ⚠️ sí | L148 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00371';` |  |
| L116 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L119 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L122 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L154 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L156 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00371';` |  |
| L163 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L166 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L169 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L179 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L186 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?doc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | 🔵 CONVENCIÓN | nombre_sp |
| `?undadef` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?doc`, `?undadef`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultadomicilioactualcte2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultadomicilioactualcte2.sql` |
| **LOC (1er CREATE)** | 177 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta domicilio y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtenerdomiciliocliente2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultadomicilioactualcte2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pTipoDir                     CHAR(1)
) RETURNING CHAR(5) AS codRetorno, -- Codigo de Retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pTipoDir` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `cCodRetSp` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cNumCliente` | `CHAR(20)` | L29 |
| `cNumCredito` | `CHAR(20)` | L30 |
| `cNomCliente` | `CHAR(104)` | L31 |
| `cEstado` | `CHAR(2)` | L32 |
| `cNomEstado` | `CHAR(30)` | L33 |
| `cCiudad` | `CHAR(3)` | L34 |
| `sNumCiudad` | `SMALLINT` | L35 |
| `cNomCiudad` | `CHAR(60)` | L36 |
| `sCiudadCoppel` | `SMALLINT` | L37 |
| `iNumColonia` | `INTEGER` | L38 |
| `cNomColonia` | `CHAR(32)` | L39 |
| `cMunicipio` | `CHAR(27)` | L40 |
| `iNumCalle` | `INTEGER` | L41 |
| `cNomCalle` | `CHAR(30)` | L42 |
| `sEdificio` | `SMALLINT` | L43 |
| `cDepartamento` | `CHAR(6)` | L44 |
| `cCodPostal` | `CHAR(5)` | L45 |
| `cObservaciones` | `CHAR(80)` | L46 |
| `cNumExterior` | `CHAR(10)` | L47 |
| `cNumInterior` | `CHAR(10)` | L48 |
| `cNomEdificio` | `CHAR(30)` | L49 |
| `cEmpresa` | `CHAR(3)` | L50 |
| *…1 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L105 |
| `sp_obtenerdomiciliocliente2` | `bdinteg` | ⚠️ sí | L113 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET     cCodRet = '00000';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L119 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'Error en la ejeuciï¿½n del SP prodcutivo sp_obtenerdomicilio` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00046';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00066';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00080';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00841';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `domicilio` | ENTIDAD | domicilio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?ual` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ual`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaencabezadoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaencabezadoxml.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta encabezado y XML" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaencabezadoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iFolio` | `INTEGER` | L14 |
| `cAnioOficio` | `CHAR(4)` | L15 |
| `cNumOficio` | `CHAR(60)` | L16 |
| `cNumExpediente` | `CHAR(60)` | L17 |
| `dFechaPublicacion` | `DATE` | L18 |
| `cArea` | `CHAR(100)` | L19 |
| `iDiasPlazo` | `SMALLINT` | L20 |
| `dFechaVencimiento` | `DATE` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL THEN` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `encabezado` | ENTIDAD | encabezado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultaenviospagodinya`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaenviospagodinya.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 79 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta envíos, pago y DINYA — sistema/plataforma de remesas domésticas en sucursal; retorna nombre_remitente, sucursal_origen, importe_eviado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_dinya_obtenerenviospagos` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaenviospagodinya(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(3)
  pImporte1                    CHAR(16)
  pImporte2                    CHAR (16)
  pSucursalOrigen              CHAR(4)
  pNombre1Remitente            CHAR(26)
  pNombre2Remitente            CHAR(26)
  pApellido1Remitente          CHAR(26)
  pApellido2Remitente          CHAR(26)
  pFechaEnvio1                 DATE
  pFechaEnvio2                 DATE
  pNombre1Beneficiario         CHAR(26)
  pNombre2Beneneficiario       CHAR(26)
  pApellido1Beneneficiario     CHAR(26)
  pApellido2Beneneficiario     CHAR(26)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(3)` | — | — |
| `pImporte1` | `CHAR(16)` | — | — |
| `pImporte2` | `CHAR (16)` | — | — |
| `pSucursalOrigen` | `CHAR(4)` | — | — |
| `pNombre1Remitente` | `CHAR(26)` | — | — |
| `pNombre2Remitente` | `CHAR(26)` | — | — |
| `pApellido1Remitente` | `CHAR(26)` | — | — |
| `pApellido2Remitente` | `CHAR(26)` | — | — |
| `pFechaEnvio1` | `DATE` | — | — |
| `pFechaEnvio2` | `DATE` | — | — |
| `pNombre1Beneficiario` | `CHAR(26)` | — | — |
| `pNombre2Beneneficiario` | `CHAR(26)` | — | — |
| `pApellido1Beneneficiario` | `CHAR(26)` | — | — |
| `pApellido2Beneneficiario` | `CHAR(26)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `cCodRetSp` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |
| `iRegistros` | `INTEGER` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |
| `cNoControl` | `CHAR(12)` | L24 |
| `dFechaEnvio` | `DATE` | L25 |
| `cSucursalOrigen` | `CHAR(4)` | L26 |
| `cNombre1Remitente` | `CHAR(26)` | L27 |
| `cNombre2Remitente` | `CHAR(26)` | L28 |
| `cApellido1Remitente` | `CHAR(26)` | L29 |
| `cApellido2Remitente` | `CHAR(26)` | L30 |
| `mImporteEnviado` | `MONEY (16,2)` | L31 |
| `cStatus` | `CHAR(20)` | L32 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_dinya_obtenerenviospagos` | `bdisac` | ⚠️ sí | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L89 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP PRODUCTIVO sp_dinya_obtenerenv` |  |
| L96 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L97 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L100 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `envios` | ENTIDAD | envíos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dinya` | ENTIDAD | DINYA — sistema/plataforma de remesas domésticas en sucursal | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultaestatusdevext_tef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaestatusdevext_tef.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta estatus y TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaestatusdevext_tef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaArch                   CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaArch` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `vsEstatusProceso` | `CHAR(1)` | L7 |
| `vsTotalEnProceso` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tf_consdevext_tef` | `bdicnweb` | no | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estatus` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?t_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?t_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaexcepcionesaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaexcepcionesaumlincred.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 373 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Comprobante Electrónico de Pago" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaexcepcionesaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pExcepcion                   CHAR(3)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pExcepcion` | `CHAR(3)` | `x`=por (criterio) · `cep`=Comprobante Electrónico de Pago (SPEI · Banxico) | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `iRegistros` | `INTEGER` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |
| `iNoRegs` | `INTEGER` | L18 |
| `cMensajeRetorno` | `CHAR(80)` | L20 |
| `iTieneCausa` | `INTEGER` | L21 |
| `cDescripcion` | `CHAR(100)` | L22 |
| `iTotalExcepcion` | `INTEGER` | L23 |
| `dPorcentaje` | `DECIMAL(18,2)` | L24 |
| `iTotalGeneral` | `INTEGER` | L25 |
| `dTotalPorcentaje` | `DECIMAL(18,2)` | L26 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L70 |
| `bdicred` | `bdicnweb` | no | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechainicial = '' OR pFechaFinal = '' OR pRegistros IS NULL ` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L80 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_excepciones2';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cep` | ENTIDAD | Comprobante Electrónico de Pago (SPEI · Banxico) | 🔵 CONVENCIÓN | nombre_sp |
| `?ciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?ciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaexpediente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaexpediente.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta expediente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `cons_expediente` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaexpediente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumRegs                     CHAR(6)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumRegs` | `CHAR(6)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(3)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `cCuenta` | `CHAR(20)` | L19 |
| `cProdNombre` | `CHAR(40)` | L20 |
| `cCodDocto` | `CHAR(4)` | L21 |
| `cFechaAlta` | `DATE` | L22 |
| `cCodGrupo` | `CHAR(3)` | L23 |
| `cDescripGpo` | `CHAR(30)` | L24 |
| `cDescripDocto` | `CHAR(35)` | L25 |
| `cDescrip2` | `CHAR(30)` | L26 |
| `cMultiImg` | `CHAR(1)` | L27 |
| `cSecuencia` | `SMALLINT` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| `iRegistros` | `INTEGER` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |
| `iNoRegs` | `INTEGER` | L32 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L70 |
| `cons_expediente` | `bdidigital` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L95 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L98 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `expediente` | ENTIDAD | expediente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultaexpediente2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaexpediente2.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta expediente" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaexpediente2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumRegs                     CHAR(6)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumRegs` | `CHAR(6)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(3)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `cCuenta` | `CHAR(20)` | L19 |
| `cProdNombre` | `CHAR(40)` | L20 |
| `cCodDocto` | `CHAR(4)` | L21 |
| `cFechaAlta` | `DATE` | L22 |
| `cCodGrupo` | `CHAR(3)` | L23 |
| `cDescripGpo` | `CHAR(30)` | L24 |
| `cDescripDocto` | `CHAR(35)` | L25 |
| `cDescrip2` | `CHAR(30)` | L26 |
| `cMultiImg` | `CHAR(1)` | L27 |
| `cSecuencia` | `SMALLINT` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| `iRegistros` | `INTEGER` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |
| `iNoRegs` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_datos_consultaexpediente` | `bdicnweb` | no | SELECT | L86 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `expediente` | ENTIDAD | expediente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaexpediente_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaexpediente_totales.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta expediente (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaexpediente_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumRegs                     CHAR(6)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumRegs` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(3)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iCodRet` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cCuenta` | `CHAR(20)` | L14 |
| `cProdNombre` | `CHAR(40)` | L15 |
| `cCodDocto` | `CHAR(4)` | L16 |
| `cFechaAlta` | `DATE` | L17 |
| `cCodGrupo` | `CHAR(3)` | L18 |
| `cDescripGpo` | `CHAR(30)` | L19 |
| `cDescripDocto` | `CHAR(35)` | L20 |
| `cDescrip2` | `CHAR(30)` | L21 |
| `cMultiImg` | `CHAR(1)` | L22 |
| `cSecuencia` | `SMALLINT` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_consultaexpediente` | `bdicnweb` | no | SELECT | L58 |
| `status_consultaexpediente` | `bdicnweb` | no | DELETE | L58 |
| `sw_datos_consultaexpediente` | `bdicnweb` | no | SELECT | L59 |
| `sw_datos_consultaexpediente` | `bdicnweb` | no | DELETE | L59 |
| `status_consultaexpediente` | `bdicnweb` | no | INSERT | L60 |
| `sw_datos_consultaexpediente` | `bdicnweb` | no | INSERT | L96 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `cons_expediente` | `bdidigital` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital:"informix".cons_expediente';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `expediente` | ENTIDAD | expediente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultafechasart61`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultafechasart61.sql` |
| **LOC (1er CREATE)** | 193 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 397 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fechas · Art. 61 LIC" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultafechasart61(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `dFechaInf` | `DATE` | L10 |
| `dFechaCon` | `DATE` | L11 |
| `dFechaDes` | `DATE` | L12 |
| `dFechaCan` | `DATE` | L13 |
| `iExiste` | `INTEGER` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `cStatus` | `CHAR(1)` | L16 |
| `cMotivo` | `CHAR(2)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_consultafechasart61` | `bdicnweb` | no | SELECT | L41 |
| `status_consultafechasart61` | `bdicnweb` | no | DELETE | L41 |
| `status_consultafechasart61` | `bdicnweb` | no | INSERT | L42 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L65 |
| `sc_ctasinformadas` | `bdicheq` | ⚠️ sí | SELECT | L81 |
| `sc_cuentas_concentradas` | `bdicheq` | ⚠️ sí | SELECT | L102 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L97 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L105 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L118 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L126 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L134 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L147 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L155 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L163 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `fechas` | ENTIDAD | fechas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `art61` | REG | Art. 61 LIC (cuentas inactivas cuyos saldos, tras años sin m | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultafechasrep_desactualizadasbc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultafechasrep_desactualizadasbc.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fechas, Servicios de Atención al Cliente — subsistema de atención en sucursal y saldo básico de cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultafechasrep_desactualizadasbc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cFecha` | `CHAR(10)` | L7 |
| `iNoRegistros` | `INT` | L8 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fechas` | ENTIDAD | fechas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?_de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `?tualizada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sbc` | ENTIDAD | saldo básico de cuenta (SBC) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_de`, `?tualizada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultafirmantesxcta_pm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultafirmantesxcta_pm.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 81 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuenta (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `consfirmantes` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultafirmantesxcta_pm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumeroCuenta                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumeroCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cNoCliente` | `CHAR(20)` | L17 |
| `cNombre1` | `CHAR(26)` | L18 |
| `cNombre2` | `CHAR(26)` | L19 |
| `cApellidoPaterno` | `CHAR(26)` | L20 |
| `cApellidoMaterno` | `CHAR(26)` | L21 |
| `cSecuencia` | `CHAR(2)` | L22 |
| `cParentesco` | `CHAR(2)` | L23 |
| `cDescripcionParentesco` | `CHAR(20)` | L24 |
| `cTipoFirma` | `CHAR(1)` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |
| `cEmpresa` | `CHAR(3)` | L27 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |
| `consfirmantes` | `bdicheq` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:'informix'.consfirmantes";` |  |
| L74 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?firm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?firm`, `?es`, `?_pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultafirmascomb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultafirmascomb.sql` |
| **LOC (1er CREATE)** | 130 |
| **Callgraph** | ✅ fan_in=13 / fan_out=14 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta firmas mancomunadas y Comisión bancaria — cobro de comisión sobre cuenta" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultafirmascomb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pCheque                      CHAR(7)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pCheque` | `CHAR(7)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(6)` | L17 |
| `cDesCodRetSp` | `CHAR(50)` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `cNumFirmas` | `CHAR(2)` | L20 |
| `cCombFirmas` | `CHAR(120)` | L21 |
| `cTipoFirma` | `CHAR(20)` | L22 |
| `cCuenta` | `CHAR(20)` | L23 |
| `iCheque` | `CHAR(7)` | L24 |
| `cCveBanco` | `CHAR(3)` | L25 |
| `dFechaPresenta` | `DATE` | L26 |
| `cCliente` | `CHAR(20)` | L27 |
| `cCodDocumento` | `CHAR(4)` | L28 |
| `iSecDocto` | `SMALLINT` | L29 |
| `cHayImgFirmas` | `CHAR(1)` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_cheques_img` | `bditef` | ⚠️ sí | SELECT | L94 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |
| `sp_obtienefirmascomb` | `bdicntchq` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bdicntchq:sp_obtienefirmascom` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `firmas` | ENTIDAD | firmas mancomunadas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?b` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultafirmasxcta_pm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultafirmasxcta_pm.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 80 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta firmas mancomunadas y cuenta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienefirmascomb` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultafirmasxcta_pm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumeroCuenta                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumeroCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cNumeroFirmas` | `CHAR(2)` | L11 |
| `cCombinacionFirmas` | `CHAR(120)` | L12 |
| `cTipoFirma` | `CHAR(20)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_obtienefirmascomb` | `bdicntchq` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicntchq:'informix'.sp_obtienefirmascom` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `firmas` | ENTIDAD | firmas mancomunadas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaformato_gcb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaformato_gcb.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 150 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaformato_gcb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,CHAR(3) AS cve_formato,CHAR(40) AS formato
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCve_formato` | `CHAR(3)` | L6 |
| `cFormato` | `CHAR(40)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_formato_gcb` | `bdinteg` | ⚠️ sí | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet ='01271';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `forma` | ACCION | construye / arma | 🟡 INFERIDO | nombre_sp |
| `?to_gcb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?to_gcb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultagralaplicadosaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultagralaplicadosaumlincred.sql` |
| **LOC (1er CREATE)** | 177 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 372 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes (general)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultagralaplicadosaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                CHAR(10)
  pFechaFinal                  CHAR(10)
  pStatus                      CHAR(2)
  pOrigen                      CHAR(1)
  pOpcFecha                    CHAR(1)
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `CHAR(10)` | — | — |
| `pFechaFinal` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |
| `pOpcFecha` | `CHAR(1)` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCodRetSp` | `CHAR(6)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| `iRegistros` | `INTEGER` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |
| `iNoRegs` | `INTEGER` | L32 |
| `cMensajeRetorno` | `CHAR(80)` | L34 |
| `dFechaAtencion` | `DATE` | L35 |
| `cNumeroSolicitud` | `CHAR(20)` | L36 |
| `cOrigen` | `CHAR(8)` | L37 |
| `cNumeroCliente` | `CHAR(20)` | L38 |
| `cApellidoPaterno` | `CHAR(26)` | L39 |
| `cApellidoMaterno` | `CHAR(26)` | L40 |
| `cNombre` | `CHAR(53)` | L41 |
| `dLincredActual` | `DECIMAL(18,2)` | L42 |
| `dLincredSugerida` | `DECIMAL(18,2)` | L43 |
| `dIncremento` | `DECIMAL(18,2)` | L44 |
| `cStatus` | `CHAR(2)` | L45 |
| `cAnalistaCac` | `CHAR(45)` | L46 |
| `cAnalista2nivel` | `CHAR(45)` | L47 |
| `cAnalista3nivel` | `CHAR(45)` | L48 |
| `cAnalista4nivel` | `CHAR(45)` | L49 |
| `cMotivo` | `CHAR(106)` | L50 |
| *…3 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L113 |
| `bdicred` | `bdicnweb` | no | L120 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L97 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial = '' OR pFechaFinal = '' OR pStatus  = '' OR pR` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L127 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consulta_gral_aumlincred_aplicados2';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L144 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultagralautaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultagralautaumlincred.sql` |
| **LOC (1er CREATE)** | 177 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 371 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta (general)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultagralautaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pStatus                      CHAR(2)
  pOrigen                      CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) 						AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `cCodRetSp` | `CHAR(6)` | L28 |
| `iCodRetSp` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |
| `cMensajeRetorno` | `CHAR(80)` | L31 |
| `dFechaOrigen` | `DATE` | L32 |
| `cNumeroSolicitud` | `CHAR(20)` | L33 |
| `cOrigen` | `CHAR(1)` | L34 |
| `cNumeroCliente` | `CHAR(20)` | L35 |
| `cApellidoPaterno` | `CHAR(26)` | L36 |
| `cApellidoMaterno` | `CHAR(26)` | L37 |
| `cNombre` | `CHAR(53)` | L38 |
| `dLincredActual` | `DECIMAL(18,2)` | L39 |
| `dLincredSugerida` | `DECIMAL(18,2)` | L40 |
| `dIncremento` | `DECIMAL(18,2)` | L41 |
| `Status` | `CHAR(2)` | L42 |
| `AnalistaCac` | `CHAR(45)` | L43 |
| `Analista2nivel` | `CHAR(45)` | L44 |
| `Analista3nivel` | `CHAR(45)` | L45 |
| `Analista4nivel` | `CHAR(45)` | L46 |
| `motivo` | `CHAR(106)` | L47 |
| `dFechaIngresoAC` | `DATE` | L48 |
| `dHoraIngresoAC` | `DATETIME HOUR TO FRACTION(3)` | L49 |
| `dFechaAtencion` | `DATE` | L50 |
| *…2 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L110 |
| `bdicred` | `bdicnweb` | no | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L94 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial = '' OR pFechaFinal = '' OR pStatus = '' OR pOr` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L124 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consulta_gral_aumlincred_aut2';` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L141 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultagralautaumlincred_rep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultagralautaumlincred_rep.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=14 / fan_out=5 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultagralautaumlincred_rep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pStatus                      CHAR(2)
  pOrigen                      CHAR(1)
) RETURNING CHAR(5) 	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `cMensajeRetorno` | `CHAR(80)` | L12 |
| `dFechaOrigen` | `DATE` | L13 |
| `cNumeroSolicitud` | `CHAR(20)` | L14 |
| `cOrigen` | `CHAR(1)` | L15 |
| `cNumeroCliente` | `CHAR(20)` | L16 |
| `cApellidoPaterno` | `CHAR(26)` | L17 |
| `cApellidoMaterno` | `CHAR(26)` | L18 |
| `cNombre` | `CHAR(53)` | L19 |
| `dLincredActual` | `DECIMAL(18,2)` | L20 |
| `dLincredSugerida` | `DECIMAL(18,2)` | L21 |
| `dIncremento` | `DECIMAL(18,2)` | L22 |
| `Status` | `CHAR(2)` | L23 |
| `AnalistaCac` | `CHAR(45)` | L24 |
| `Analista2nivel` | `CHAR(45)` | L25 |
| `Analista3nivel` | `CHAR(45)` | L26 |
| `Analista4nivel` | `CHAR(45)` | L27 |
| `motivo` | `CHAR(106)` | L28 |
| `dFechaIngresoAC` | `DATE` | L29 |
| `dHoraIngresoAC` | `DATETIME HOUR TO FRACTION(3)` | L30 |
| `dFechaAtencion` | `DATE` | L31 |
| *…3 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_aumlim_incremento_mc` | `bdicnweb` | no | INSERT | L110 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L81 |
| `bdicred` | `bdicnweb` | no | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consulta_gral_aumlincred_aut3';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L109 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultagralstatusaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultagralstatusaumlincred.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 370 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta estatus (general)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultagralstatusaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pOrigen                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pOrigen` | `CHAR(2)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iRegistros` | `INTEGER` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |
| `iNoRegs` | `INTEGER` | L17 |
| `cMensajeRetorno` | `CHAR(80)` | L18 |
| `iTieneCausa` | `INTEGER` | L19 |
| `cDescripcion` | `CHAR(100)` | L20 |
| `iTotalStatus` | `INTEGER` | L21 |
| `dPorcentaje` | `DECIMAL(18,2)` | L22 |
| `iTotalGeneral` | `INTEGER` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L65 |
| `bdicred` | `bdicnweb` | no | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial = '' OR pFechaFinal = '' OR pOrigen = '' OR pRe` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_rep_gral_status2';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00388';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultagralstatusaumlincred_rep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultagralstatusaumlincred_rep.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=14 / fan_out=5 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta estatus (general)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultagralstatusaumlincred_rep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pOrigen                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pOrigen` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `iNoRegs` | `INTEGER` | L14 |
| `cMensajeRetorno` | `CHAR(80)` | L15 |
| `iTieneCausa` | `INTEGER` | L16 |
| `cDescripcion` | `CHAR(100)` | L17 |
| `iTotalStatus` | `INTEGER` | L18 |
| `dPorcentaje` | `DECIMAL(18,2)` | L19 |
| `iTotalGeneral` | `INTEGER` | L20 |
| `cNombreReporte` | `CHAR(50)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_aumlim_status_mc` | `bdicnweb` | no | INSERT | L84 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L58 |
| `bdicred` | `bdicnweb` | no | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_rep_gral_status2';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00388';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultahistcambiospm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultahistcambiospm.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 92 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cambio (histórico/historial)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultahistcambiospm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(2)
  pNumCliente                  CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(2)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `dFechaMod` | `CHAR(10)` | L20 |
| `cGiro` | `CHAR(20)` | L21 |
| `cActividadSocial` | `CHAR(3)` | L22 |
| `cRepLegal` | `CHAR(60)` | L23 |
| `cFirmaElect` | `CHAR(25)` | L24 |
| `cCorreoElect` | `CHAR(60)` | L25 |
| `cPagInternet` | `CHAR(30)` | L26 |
| `cTelefono` | `CHAR(15)` | L27 |
| `cUsMod` | `CHAR(10)` | L28 |
| `cApPaternoRepLeg` | `CHAR(26)` | L29 |
| `cApMaternoRepLeg` | `CHAR(26)` | L30 |
| `cNom1RepLeg` | `CHAR(26)` | L31 |
| `cNom2RepLeg` | `CHAR(26)` | L32 |
| `iRecuperacion` | `INTEGER` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_si_ctepm` | `bdicnweb` | no | SELECT | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L100 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultahistoincrementosaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultahistoincrementosaumlincred.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito y OS — Originación de Solicitudes (histórico/historial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_cac_histincremento` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultahistoincrementosaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cMensajeRetorno` | `CHAR(107)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cNumSolicitud` | `CHAR(20)` | L15 |
| `cStatus` | `CHAR(2)` | L16 |
| `cOrigen` | `CHAR(10)` | L17 |
| `cFechaStatus` | `CHAR(10)` | L18 |
| `mLineaCreditoActual` | `MONEY(18,2)` | L19 |
| `mLineaCreditoSugerida` | `MONEY(18,2)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |
| `cEmpresa` | `CHAR(3)` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_cac_histincremento` | `bdicred` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `?oin` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `?ment` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?oin`, `?ment` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultahistorialstatusmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultahistorialstatusmc.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta estatus y mc — Mesa de Control (histórico/historial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultahistorialstatusmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSol                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `cMensajeRet` | `CHAR(80)` | L13 |
| `cComentario` | `CHAR(80)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `iIsamErr` | `INTEGER` | L16 |
| `cErrorInfo` | `CHAR(80)` | L17 |
| `cStatusSol` | `CHAR(2)` | L18 |
| `dFechaEntrada` | `DATE` | L19 |
| `dFechaSalida` | `DATE` | L20 |
| `iNoRegs` | `INTEGER` | L21 |
| `iRegistros` | `INTEGER` | L22 |
| `iRecuperacion` | `INTEGER` | L23 |
| `cDescStatus` | `CHAR(40)` | L24 |
| `cEjecutivo` | `CHAR(8)` | L25 |
| `cNombreEjecutivo` | `CHAR(45)` | L26 |
| `cEmpresa` | `CHAR(3)` | L27 |
| `cCanal` | `CHAR(1)` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_status_sol` | `bdisolic` | ⚠️ sí | SELECT | L86 |
| `ss_solicitudes` | `bdisolic` | ⚠️ sí | SELECT | L91 |
| `ss_autorizacion` | `bdisolic` | ⚠️ sí | SELECT | L98 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L116 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L118 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L121 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `?orial` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orial` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultahistoricomttohuellacte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultahistoricomttohuellacte.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta huella biométrica y cliente (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consultahistoricomttohuella` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultahistoricomttohuellacte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `dFechaMantto` | `DATE` | L16 |
| `cFechaMantto` | `CHAR(10)` | L17 |
| `cHora` | `CHAR(10)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cEmpEjecuto` | `CHAR(8)` | L20 |
| `cNomEmpEjecuto` | `CHAR(30)` | L21 |
| `cEmpAutorizo` | `CHAR(8)` | L22 |
| `cNomEmpAutorizo` | `CHAR(30)` | L23 |
| `cEmpCaja` | `CHAR(8)` | L24 |
| `cNomEmpCaja` | `CHAR(30)` | L25 |
| `iNoRegs` | `INTEGER` | L26 |
| `cEmpresa` | `CHAR(3)` | L27 |
| `iRegistros` | `INTEGER` | L28 |
| `iRecuperacion` | `INTEGER` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultahistoricomttohuella` | `bdinteg` | ⚠️ sí | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdinteg:sp_consultahistoricomt` |  |
| L86 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L87 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L91 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mtto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `huella` | ENTIDAD | huella biométrica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mtto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultahistoricotarjetas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultahistoricotarjetas.sql` |
| **LOC (1er CREATE)** | 402 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta tarjetas (histórico)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultahistoricotarjetas(
  psNumCuenta                  CHAR(12)
  psNumTarjeta                 CHAR(16)
  piTipoBusqueda               SMALLINT
  psEmpresa                    CHAR(3)
) RETURNING CHAR(5) AS CodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `psNumCuenta` | `CHAR(12)` | — | — |
| `psNumTarjeta` | `CHAR(16)` | — | — |
| `piTipoBusqueda` | `SMALLINT` | — | — |
| `psEmpresa` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sCodRet` | `CHAR(5)` | L10 |
| `sSqlErr` | `INTEGER` | L11 |
| `sTipo` | `CHAR(1)` | L14 |
| `sFechaStatusUltMod` | `CHAR(10)` | L15 |
| `sHoraStatusUltMod` | `CHAR(10)` | L16 |
| `sEmpleadoModifico` | `CHAR(8)` | L17 |
| `sNombreEmpMod` | `CHAR(100)` | L18 |
| `sFechaStatusAperturaCuenta` | `CHAR(10)` | L21 |
| `sSucursalAperturaCuenta` | `CHAR(4)` | L22 |
| `sEmpleadoAperturo` | `CHAR(8)` | L23 |
| `sNombreEmpAperturo` | `CHAR(100)` | L24 |
| `sFechaStatusEntrega` | `CHAR(10)` | L27 |
| `sHoraStatusEntrega` | `CHAR(10)` | L28 |
| `sSucursalEntrega` | `CHAR(4)` | L29 |
| `sEmpleadoEntrega` | `CHAR(8)` | L30 |
| `sNombreEmpEntrego` | `CHAR(100)` | L31 |
| `sStatusTarjetaCancelada` | `CHAR(25)` | L34 |
| `sFechaStatusCancelada` | `CHAR(10)` | L35 |
| `sHoraStatusCancelada` | `CHAR(10)` | L36 |
| `iSecuencia` | `SMALLINT` | L37 |
| `sEmpleadoModificoCancelacion` | `CHAR(8)` | L38 |
| `sNombreEmpModCancelacion` | `CHAR(100)` | L39 |
| `sNumTarjeta` | `CHAR(16)` | L41 |
| `sNumCuenta` | `CHAR(12)` | L42 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tarjetacuenta` | `intercard` | ⚠️ sí | SELECT | L114 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L121 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L135 |
| `movimientohistorico` | `intercard` | ⚠️ sí | SELECT | L158 |
| `movimiento` | `intercard` | ⚠️ sí | SELECT | L168 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L178 |
| `tarjeta` | `intercard` | ⚠️ sí | SELECT | L199 |
| `sc_tarjeta` | `bdicheq` | ⚠️ sí | SELECT | L304 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET sCodRet = '00000';` |  |
| L221 | CÓDIGO_RETORNO | `LET sCodRet = '00004';` |  |
| L226 | CÓDIGO_RETORNO | `LET sCodRet = '00003';` |  |
| L231 | CÓDIGO_RETORNO | `LET sCodRet = '00002';` |  |
| L345 | CÓDIGO_RETORNO | `LET sCodRet = '00004';` |  |
| L350 | CÓDIGO_RETORNO | `LET sCodRet = '00003';` |  |
| L355 | CÓDIGO_RETORNO | `LET sCodRet = '00002';` |  |
| L361 | CÓDIGO_RETORNO | `LET sCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `tarjetas` | ENTIDAD | tarjetas (plural) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultahistoricotarjetascte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultahistoricotarjetascte.sql` |
| **LOC (1er CREATE)** | 262 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 80 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta tarjetas y cliente (histórico)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultahistoricotarjetascte(
  pUsuario                     CHAR(10)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pNumTarjeta                  CHAR(16)
  pSistemaCuenta               CHAR(2)
  pTipoOperacion               CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(10)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(16)` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `cCodRetSp` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cEmpresa` | `CHAR(3)` | L30 |
| `iTipoBusqueda` | `SMALLINT` | L31 |
| `cTipoCliente` | `CHAR(1)` | L32 |
| `cFechaStatusUltMod` | `CHAR(10)` | L33 |
| `cHoraStatusUltMod` | `CHAR(10)` | L34 |
| `cEmpleadoModifico` | `CHAR(8)` | L35 |
| `cNombreEmpMod` | `CHAR(100)` | L36 |
| `cFechaStatusAperturaCuenta` | `CHAR(10)` | L37 |
| `cSucursalAperturaCuenta` | `CHAR(4)` | L38 |
| `cEmpleadoAperturo` | `CHAR(8)` | L39 |
| `cNombreEmpAperturo` | `CHAR(100)` | L40 |
| `cFechaStatusEntrega` | `CHAR(10)` | L41 |
| `cHoraStatusEntrega` | `CHAR(10)` | L42 |
| `cSucursalEntrega` | `CHAR(4)` | L43 |
| `cEmpleadoEntrega` | `CHAR(8)` | L44 |
| `cNombreEmpEntrego` | `CHAR(100)` | L45 |
| `cStatusTarjetaCancelada` | `CHAR(25)` | L46 |
| `cFechaStatusCancelada` | `CHAR(10)` | L47 |
| `cHoraStatusCancelada` | `CHAR(10)` | L48 |
| `iSecuencia` | `SMALLINT` | L49 |
| `cEmpleadoModificoCancelacion` | `CHAR(8)` | L50 |
| `cNombreEmpModCancelacion` | `CHAR(100)` | L51 |
| *…5 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L202 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L207 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L138 |
| `bdicnweb` | `bdicnweb` | no | L167 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00037';` |  |
| L174 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_consultahistoricotar` |  |
| L225 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_consultahistoricotar` |  |
| L232 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L237 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `tarjetas` | ENTIDAD | tarjetas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultahuellascte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultahuellascte.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta huellas biométricas y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_conhuella` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultahuellascte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cHuellaDerecha` | `CHAR(942)` | L11 |
| `cHuellaIzquierda` | `CHAR(942)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_conhuella` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_conhuella';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00006';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00312';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `huellas` | ENTIDAD | huellas biométricas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultaimgnula`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaimgnula.sql` |
| **LOC (1er CREATE)** | 250 |
| **Callgraph** | ✅ fan_in=9 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaimgnula(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pDireccionMac                CHAR(12)
  pFecha                       DATE
  pStatusImgF                  CHAR(1)
  pStatusImgT                  CHAR(1)
  pIdDetalle                   INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pStatusImgF` | `CHAR(1)` | — | — |
| `pStatusImgT` | `CHAR(1)` | — | — |
| `pIdDetalle` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(35)` | L9 |
| `cFormatFecha` | `CHAR(10)` | L10 |
| `cDia` | `CHAR(2)` | L11 |
| `cMiBanco` | `CHAR(3)` | L12 |
| `iHayDatos` | `INTEGER` | L13 |
| `cDetDatosNombreArch` | `CHAR(22)` | L14 |
| `cDetDatosNumCuenta` | `CHAR(22)` | L15 |
| `iDetDatosNumCheque` | `INTEGER` | L16 |
| `cDetDatosImporte` | `MONEY (14,2)` | L17 |
| `cDatosArchImgF` | `CHAR(100)` | L18 |
| `cDatosArchImgT` | `CHAR(100)` | L19 |
| `cDatosTamImgF` | `CHAR(15)` | L20 |
| `cDatosTamImgT` | `CHAR(15)` | L21 |
| `cDatosCargaImgF` | `CHAR(1)` | L22 |
| `cDatosCargaImgT` | `CHAR(1)` | L23 |
| `iIdDetalle` | `INTEGER` | L24 |
| `cHuboError` | `CHAR(1)` | L25 |
| `iContDetalle` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L83 |
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | SELECT | L86 |
| `cce_propios_det` | `bditef` | ⚠️ sí | UPDATE | L124 |
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | UPDATE | L133 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | INSERT | L218 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | SELECT | L222 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L70 |
| `cons_img_nula` | `bditef` | ⚠️ sí | L109 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pDireccionMac = '' OR pFecha IS NULL O` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | FÓRMULA | `LET cFormatFecha = SUBSTR(pFecha,7,4)\|\|'/'\|\|SUBSTR(pFecha,1,2)\|\|'/'\|\|SUBSTR(pFecha,4,2);` |  |
| L113 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:cons_img_nula';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00720';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L152 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:cons_img_nula';` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00720';` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L199 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L233 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?imgnula` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imgnula` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfarchivodotacaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfarchivodotacaja.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 92 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, archivo y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfarchivodotacaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
  pIdSucursal                  CHAR(4)
  pIdProvCaja                  CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret, CHAR(8) AS folio_operacion, DATE AS fecha_operacion, DATE AS fecha_solicitud, DATE AS fecha_envio, CHAR(4) AS id_sucursal, CHAR(40) AS desc_sucursal,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cTrans1` | `CHAR(30)` | L16 |
| `cTrans2` | `CHAR(30)` | L17 |
| `cTrans3` | `CHAR(30)` | L18 |
| `cFolioOperacion` | `CHAR(8)` | L19 |
| `dFechaOperacion` | `DATE` | L20 |
| `dFechaSolicitud` | `DATE` | L21 |
| `dFechaEnvio` | `DATE` | L22 |
| `cIdSucursal` | `CHAR(4)` | L23 |
| `cDescSucursal` | `CHAR(40)` | L24 |
| `mMonto` | `MONEY(14,2)` | L25 |
| `cCajaGeneral` | `CHAR(30)` | L26 |
| `status` | `CHAR(1)` | L27 |
| `iIdRegistro` | `INTEGER` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |
| `iRegistros` | `INTEGER` | L31 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSucursal = '' OR pIdSucursal = '' OR pIdProvCaja = '' OR` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `?dota` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dota` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfarqueosucucaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfarqueosucucaja.sql` |
| **LOC (1er CREATE)** | 155 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, OS — Originación de Solicitudes y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfarqueosucucaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cSucursal` | `CHAR(4)` | L29 |
| `cNombreSuc` | `CHAR(40)` | L30 |
| `cDescPlazaCajaGeneralArq` | `CHAR(40)` | L31 |
| `fCantidad_1` | `FLOAT` | L32 |
| `fCantidad_2` | `FLOAT` | L33 |
| `fCantidad_3` | `FLOAT` | L34 |
| `fCantidad_4` | `FLOAT` | L35 |
| `fCantidad_5` | `FLOAT` | L36 |
| `fCantidad_6` | `FLOAT` | L37 |
| `fCantidad_7` | `FLOAT` | L38 |
| `mSaldoTotalArq` | `MONEY(14,2)` | L39 |
| `dFechaArq` | `DATE` | L40 |
| `cIdCajeroPrincArq` | `CHAR(8)` | L41 |
| `iTotSucursales` | `INTEGER` | L42 |
| `iSucAbrieron` | `INTEGER` | L43 |
| `iSucNoAbrio` | `INTEGER` | L44 |
| `iSucCerraron` | `INTEGER` | L45 |
| `iSucPenCerrar` | `INTEGER` | L46 |
| `iRecuperacion` | `INTEGER` | L47 |
| `mSaldoTotal` | `MONEY(14,2)` | L48 |
| `mTotalDotaciones` | `MONEY(14,2)` | L49 |
| `cDescDivisaArq` | `CHAR(30)` | L50 |
| `cNombreCajeroArq` | `CHAR(45)` | L51 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_arqueosucajatmp` | `bdicnweb` | no | SELECT | L121 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L91 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L124 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `?arque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ucu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?arque`, `?ucu` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfbts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfbts.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información y beneficiarios" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfbts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cMensaje` | `CHAR(80)` | L11 |
| `dFecha` | `DATE` | L12 |
| `cSucursal` | `CHAR(5)` | L13 |
| `cHora` | `CHAR(10)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consinfobtssif` | `bdisac` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultainfctepmsempnet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfctepmsempnet.sql` |
| **LOC (1er CREATE)** | 204 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 74 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, cliente y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarctepmempresanet_clon` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfctepmsempnet(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `cCodRetSp` | `CHAR(6)` | L29 |
| `iCodRetSp` | `INTEGER` | L30 |
| `iSqlErr` | `INTEGER` | L31 |
| `cRazonSocial` | `CHAR(60)` | L32 |
| `cTipoPersona` | `CHAR(2)` | L33 |
| `cFolioActiva1` | `CHAR(12)` | L34 |
| `cFolioActiva2` | `CHAR(12)` | L35 |
| `iContrato1` | `INTEGER` | L36 |
| `iContrato2` | `INTEGER` | L37 |
| `cCodigoIdent1` | `CHAR(30)` | L38 |
| `cCodigoIdent2` | `CHAR(30)` | L39 |
| `cDescIdent1` | `CHAR(50)` | L40 |
| `cDescIdent2` | `CHAR(50)` | L41 |
| `cNoIdent1` | `CHAR(2)` | L42 |
| `cNoIdent2` | `CHAR(2)` | L43 |
| `iNoTokens` | `SMALLINT` | L44 |
| `iNoAdmins` | `SMALLINT` | L45 |
| `cUsAut1Nombre1` | `CHAR(50)` | L46 |
| `cUsAut1Nombre2` | `CHAR(50)` | L47 |
| `cUsAut1ApPaterno` | `CHAR(50)` | L48 |
| `cUsAut1ApMaterno` | `CHAR(50)` | L49 |
| `cUsAut2Nombre1` | `CHAR(50)` | L50 |
| `cUsAut2Nombre2` | `CHAR(50)` | L51 |
| `cUsAut2ApPaterno` | `CHAR(50)` | L52 |
| *…3 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L111 |
| `sp_consultarctepmempresanet_clon` | `bdibei` | ⚠️ sí | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdibei:sp_consultarctepmempres` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L166 | CÓDIGO_RETORNO | `LET cCodRet = '00275';` |  |
| L173 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L183 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?pms` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pms`, `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfdotacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfdotacionescaja.sql` |
| **LOC (1er CREATE)** | 136 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 210 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, dotaciones de efectivo y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfdotacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
  pIdProvCaja                  CHAR(4)
  pIdSucursal                  CHAR(4)
  pFechaInic                   DATE
  pFechaFin                    DATE
  pIdLote                      INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdLote` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `cCodRetSp` | `CHAR(6)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cSucursal` | `CHAR(50)` | L20 |
| `cCajaGen` | `CHAR(50)` | L21 |
| `dFecha` | `DATE` | L22 |
| `cStatus` | `CHAR(50)` | L23 |
| `cFolioOperacion` | `CHAR(8)` | L24 |
| `dMonto` | `DECIMAL(14,2)` | L25 |
| `cUsuario` | `CHAR(50)` | L26 |
| `cIdUsuario` | `CHAR(8)` | L27 |
| `cIdStatus` | `CHAR(2)` | L28 |
| `cCodTrans` | `CHAR(4)` | L29 |
| `iIdRegistro` | `INTEGER` | L30 |
| `cEmpresa` | `CHAR(3)` | L32 |
| `iNoRegistros` | `INTEGER` | L33 |
| `iRecuperacion` | `INTEGER` | L34 |
| `iRegistros` | `INTEGER` | L35 |
| `iIdLote` | `INTEGER` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_enviodotaciones` | `bdicnweb` | no | SELECT | L107 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSucursal = '' OR pRegistros IS NULL OR pRecuperacion IS ` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L112 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultainfentradasalidacaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfentradasalidacaja.sql` |
| **LOC (1er CREATE)** | 192 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, salida y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfentradasalidacaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(1)
  pIdSucursal                  CHAR(4)
  pIdPlaza                     CHAR(3)
  pFechaInic                   DATE
  pFechaFin                    DATE
  pMes                         CHAR(2)
  pAnio                        CHAR(4)
  pIdStatus                    CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(1)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pIdPlaza` | `CHAR(3)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pMes` | `CHAR(2)` | — | — |
| `pAnio` | `CHAR(4)` | — | — |
| `pIdStatus` | `CHAR(2)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `cCodRetSp` | `CHAR(6)` | L24 |
| `iCodRetSp` | `INTEGER` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCajaGeneral` | `CHAR(50)` | L28 |
| `cFolioOpe` | `CHAR(8)` | L29 |
| `cStatus` | `CHAR(40)` | L30 |
| `cSucursal` | `CHAR(50)` | L31 |
| `cFolioSuc` | `CHAR(16)` | L32 |
| `dFechaSolicitud` | `DATE` | L33 |
| `cUsSolicitud` | `CHAR(8)` | L34 |
| `dFechaEnvio` | `DATE` | L35 |
| `cUsEnvio` | `CHAR(8)` | L36 |
| `dFechaRecepcion` | `DATE` | L37 |
| `cUsRecepcion` | `CHAR(8)` | L38 |
| `cOtroStatus` | `CHAR(40)` | L39 |
| `mMonto` | `MONEY(14,2)` | L40 |
| `dFechaReversion` | `DATE` | L41 |
| `cUsReversion` | `CHAR(8)` | L42 |
| `cPlaza` | `CHAR(40)` | L43 |
| `cTpoSucRSP` | `CHAR(1)` | L44 |
| `cTpoSuc` | `CHAR(10)` | L45 |
| `iExisteSuc` | `INTEGER` | L46 |
| `iExisteATM` | `INTEGER` | L47 |
| `cEmpresa` | `CHAR(3)` | L49 |
| *…3 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_entradasalida` | `bdicnweb` | no | SELECT | L126 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L114 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L100 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSucursal = '' OR pIdPlaza = '' OR pRegistros IS NULL OR ` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L138 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L143 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L169 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L174 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `?entrada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `salida` | ENTIDAD | salida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?entrada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfmonitorefectivocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfmonitorefectivocaja.sql` |
| **LOC (1er CREATE)** | 299 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 84 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, monitor, efectivo y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `consultacajageneral2` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfmonitorefectivocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodProveedor                CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodProveedor` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L36 |
| `cCodRetSp` | `CHAR(6)` | L37 |
| `iCodRetSp` | `INTEGER` | L38 |
| `iSqlErr` | `INTEGER` | L39 |
| `cEmpresa` | `CHAR(3)` | L40 |
| `cCodProveedor` | `CHAR(4)` | L41 |
| `cDescProveedor` | `CHAR(30)` | L42 |
| `cPlaza` | `CHAR(3)` | L43 |
| `cDescPlaza` | `CHAR(40)` | L44 |
| `mSaldoAnterior` | `MONEY(14,2)` | L45 |
| `mSaldoTotal` | `MONEY(14,2)` | L46 |
| `mSaldoAsignado` | `MONEY(14,2)` | L47 |
| `mTotalBillDet` | `MONEY(14,2)` | L48 |
| `mSaldoDisponible` | `MONEY(14,2)` | L49 |
| `cDivisa` | `CHAR(2)` | L50 |
| `cDescDivisa` | `CHAR(30)` | L51 |
| `cDenominacion_1` | `CHAR(18)` | L52 |
| `cDenominacion_2` | `CHAR(18)` | L53 |
| `cDenominacion_3` | `CHAR(18)` | L54 |
| `cDenominacion_4` | `CHAR(18)` | L55 |
| `cDenominacion_5` | `CHAR(18)` | L56 |
| `cDenominacion_6` | `CHAR(18)` | L57 |
| `cDenominacion_7` | `CHAR(18)` | L58 |
| `cDenominacion_8` | `CHAR(18)` | L59 |
| `cDenominacion_9` | `CHAR(18)` | L60 |
| *…37 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L198 |
| `consultacajageneral2` | `bdisuc` | ⚠️ sí | L213 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L176 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCodProveedor = '' OR pRegistros IS NULL OR pRecuperacion IS ` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L188 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L232 | FÓRMULA | `LET mCalc = NVL(mSaldoAut,0) * (NVL(iPorcentaje,0) / 100);` |  |
| L233 | FÓRMULA | `LET mRangoMax = NVL(mSaldoAut,0) + mCalc;` |  |
| L234 | FÓRMULA | `LET mRangoMin = NVL(mSaldoAut,0) - mCalc;` |  |
| L236 | VALIDACIÓN_NULL | `IF mSaldoAut IS NULL AND iPorcentaje IS NULL THEN` |  |
| L247 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:consultacajageneral2';` |  |
| L257 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L268 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `efectivo` | ENTIDAD | efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultainfmonitoroperacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfmonitoroperacionescaja.sql` |
| **LOC (1er CREATE)** | 480 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, monitor, operaciones y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfmonitoroperacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
  pIdSucursal                  CHAR(4)
  pIdMostrar                   CHAR(4)
  pFechaInic                   DATE
  pFechaFin                    DATE
  pOperacion                   CHAR(1)
  pIdProvCaja                  CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pIdMostrar` | `CHAR(4)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pOperacion` | `CHAR(1)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `cCodRetSp` | `CHAR(6)` | L36 |
| `iCodRetSp` | `INTEGER` | L37 |
| `iSqlErr` | `INTEGER` | L38 |
| `cSucursal` | `CHAR(4)` | L39 |
| `cDescSucursal` | `CHAR(45)` | L40 |
| `dFechaOperacion` | `DATE` | L41 |
| `cDescStatus` | `CHAR(50)` | L42 |
| `cFolioOperacion` | `CHAR(8)` | L43 |
| `mImporte` | `MONEY(14,2)` | L44 |
| `cOperacion` | `CHAR(50)` | L45 |
| `cCodProveedor` | `CHAR(4)` | L46 |
| `cTerceros` | `CHAR(50)` | L47 |
| `cPapeleta` | `CHAR(16)` | L48 |
| `cUsuario` | `CHAR(40)` | L49 |
| `cCodStatus` | `CHAR(2)` | L50 |
| `iIdATM` | `CHAR(6)` | L51 |
| `iBillete1000` | `INTEGER` | L52 |
| `iBillete500` | `INTEGER` | L53 |
| `iBillete200` | `INTEGER` | L54 |
| `iBillete100` | `INTEGER` | L55 |
| `iBillete50` | `INTEGER` | L56 |
| `iBillete20` | `INTEGER` | L57 |
| `iBillete10` | `INTEGER` | L58 |
| `iBillete5` | `INTEGER` | L59 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_validaestatus` | `bdicnweb` | no | SELECT | L138 |
| `sw_cg_validaestatus` | `bdicnweb` | no | DELETE | L138 |
| `sw_cg_validaestatus` | `bdicnweb` | no | INSERT | L140 |
| `sw_cg_monitoroperacioness` | `bdicnweb` | no | SELECT | L177 |
| `sw_cg_monitoroperacioness` | `bdicnweb` | no | DELETE | L177 |
| `sw_cg_monitoroperacioness` | `bdicnweb` | no | INSERT | L226 |
| `sw_cg_monitoroperacioness` | `bdicnweb` | no | UPDATE | L272 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L169 |
| `sp_monitor_operaciones2_2` | `bdisuc` | ⚠️ sí | L197 |
| `sp_monitor_operaciones3_2` | `bdisuc` | ⚠️ sí | L333 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L143 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSucursal = '' OR pFechaInic IS NULL OR pFechaFin IS NULL` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L202 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_monitor_operaciones2_` |  |
| L205 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L225 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L291 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L299 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L338 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_monitor_operaciones3_` |  |
| L341 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L361 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L427 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L435 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultainfmonitoroperacionescaja_duplicados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfmonitoroperacionescaja_duplicados.sql` |
| **LOC (1er CREATE)** | 328 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, monitor, operaciones, caja y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfmonitoroperacionescaja_duplicados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDuplicado                   INTEGER
  pStatus                      CHAR(2)
  pOperacion                   CHAR(1)
  pOpcion                      CHAR(1)
  pFechaAct                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDuplicado` | `INTEGER` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pOperacion` | `CHAR(1)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pFechaAct` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L34 |
| `cCodRetSp` | `CHAR(6)` | L35 |
| `iCodRetSp` | `INTEGER` | L36 |
| `iSqlErr` | `INTEGER` | L37 |
| `cSucursal` | `CHAR(4)` | L38 |
| `cDescSucursal` | `CHAR(45)` | L39 |
| `dFechaOperacion` | `DATE` | L40 |
| `cDescStatus` | `CHAR(50)` | L41 |
| `cFolioOperacion` | `CHAR(8)` | L42 |
| `mImporte` | `MONEY(14,2)` | L43 |
| `cOperacion` | `CHAR(50)` | L44 |
| `cCodProveedor` | `CHAR(4)` | L45 |
| `cTerceros` | `CHAR(50)` | L46 |
| `cPapeleta` | `CHAR(16)` | L47 |
| `cUsuario` | `CHAR(40)` | L48 |
| `cCodStatus` | `CHAR(2)` | L49 |
| `iIdATM` | `CHAR(6)` | L50 |
| `iBillete1000` | `INTEGER` | L51 |
| `iBillete500` | `INTEGER` | L52 |
| `iBillete200` | `INTEGER` | L53 |
| `iBillete100` | `INTEGER` | L54 |
| `iBillete50` | `INTEGER` | L55 |
| `iBillete20` | `INTEGER` | L56 |
| `iBillete10` | `INTEGER` | L57 |
| `iBillete5` | `INTEGER` | L58 |
| *…13 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_monitoroperacioness` | `bdicnweb` | no | SELECT | L159 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L140 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L124 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL OR pOperacion =''` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L163 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L188 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L208 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L229 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L259 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L285 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L295 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_duplicad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_duplicad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfoctemc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfoctemc.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, cliente y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_mc_obteninfocliente` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfoctemc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSol                      CHAR(20)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |
| `pNumcte` | `CHAR(20)` | `cte`=cliente · `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L30 |
| `cCodRetSP` | `CHAR(6)` | L31 |
| `cMensajeRet` | `CHAR(80)` | L32 |
| `cComentario` | `CHAR(80)` | L33 |
| `iSqlErr` | `INTEGER` | L34 |
| `iIsamErr` | `INTEGER` | L35 |
| `cErrorInfo` | `CHAR(80)` | L36 |
| `cEstado` | `CHAR(30)` | L37 |
| `cCiudad` | `CHAR(30)` | L38 |
| `cMunicipio` | `CHAR(27)` | L39 |
| `cColonia` | `CHAR(32)` | L40 |
| `cCalle` | `CHAR(30)` | L41 |
| `cNumExt` | `CHAR(10)` | L42 |
| `cNumInt` | `CHAR(10)` | L43 |
| `cCodPostal` | `CHAR(5)` | L44 |
| `cDepto` | `CHAR(6)` | L45 |
| `sEdificio` | `SMALLINT` | L46 |
| `sTipoTel` | `SMALLINT` | L47 |
| `cComplemento` | `CHAR(80)` | L48 |
| `cTelCasa` | `CHAR(13)` | L49 |
| `cTelCel` | `CHAR(13)` | L50 |
| `cTel` | `CHAR(13)` | L51 |
| `cTelTrab` | `CHAR(13)` | L52 |
| `cExt` | `CHAR(5)` | L53 |
| `cLugarTrabajo` | `CHAR(60)` | L54 |
| *…18 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_mc_obteninfocliente` | `bdinteg` | ⚠️ sí | L140 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L76 | CÓDIGO_RETORNO | `LET cCodRet				= '00000';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L150 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultainfoctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfoctemoral.sql` |
| **LOC (1er CREATE)** | 430 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 73 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfoctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) 		AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L82 |
| `iSqlErr` | `INTEGER` | L83 |
| `cCodRetSp` | `CHAR(6)` | L84 |
| `iCodRetSp` | `INTEGER` | L85 |
| `cRFC` | `CHAR(13)` | L86 |
| `cSucursal` | `CHAR(4)` | L87 |
| `cApellPaterContactoRepLeg` | `CHAR(26)` | L88 |
| `cApellMaterContactoRepLeg` | `CHAR(26)` | L89 |
| `cNomb1ContactoRepLeg` | `CHAR(26)` | L90 |
| `cNomb2ContactoRepLeg` | `CHAR(26)` | L91 |
| `cCalleFiscal` | `CHAR(40)` | L92 |
| `cNumExtCalleFiscal` | `CHAR(10)` | L93 |
| `cColFiscal` | `CHAR(60)` | L94 |
| `vNomCiudFiscal` | `CHAR(60)` | L95 |
| `cCodMunFiscal` | `CHAR(3)` | L96 |
| `cNomEstadoFiscal` | `CHAR(30)` | L97 |
| `cNumcte` | `CHAR(20)` | L98 |
| `cNomCorto` | `CHAR(60)` | L99 |
| `cPagInternet` | `CHAR(30)` | L100 |
| `cSatFea` | `CHAR(25)` | L101 |
| `cTelContacto` | `CHAR(15)` | L102 |
| `cGiro` | `CHAR(20)` | L103 |
| `cNomGiro` | `CHAR(40)` | L104 |
| `cActividadSoc` | `CHAR(3)` | L105 |
| `cDesActObj` | `CHAR(30)` | L106 |
| *…67 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_correos` | `bdinteg` | ⚠️ sí | SELECT | L383 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L310 |
| `sp_consultarctemoral_03` | `bdinteg` | ⚠️ sí | L328 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L175 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L292 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L346 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultarctemoral_03';` |  |
| L348 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L364 | CÓDIGO_RETORNO | `LET cCodRet = '00701';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultainfoctemoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfoctemoral2.sql` |
| **LOC (1er CREATE)** | 396 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 70 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfoctemoral2(
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) 		AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L82 |
| `iSqlErr` | `INTEGER` | L83 |
| `cCodRetSp` | `CHAR(6)` | L84 |
| `iCodRetSp` | `INTEGER` | L85 |
| `cRFC` | `CHAR(13)` | L86 |
| `cSucursal` | `CHAR(4)` | L87 |
| `cApellPaterContactoRepLeg` | `CHAR(26)` | L88 |
| `cApellMaterContactoRepLeg` | `CHAR(26)` | L89 |
| `cNomb1ContactoRepLeg` | `CHAR(26)` | L90 |
| `cNomb2ContactoRepLeg` | `CHAR(26)` | L91 |
| `cCalleFiscal` | `CHAR(40)` | L92 |
| `cNumExtCalleFiscal` | `CHAR(10)` | L93 |
| `cColFiscal` | `CHAR(60)` | L94 |
| `vNomCiudFiscal` | `CHAR(60)` | L95 |
| `cCodMunFiscal` | `CHAR(3)` | L96 |
| `cNomEstadoFiscal` | `CHAR(30)` | L97 |
| `cNumcte` | `CHAR(20)` | L98 |
| `cNomCorto` | `CHAR(60)` | L99 |
| `cPagInternet` | `CHAR(30)` | L100 |
| `cSatFea` | `CHAR(25)` | L101 |
| `cTelContacto` | `CHAR(15)` | L102 |
| `cGiro` | `CHAR(20)` | L103 |
| `cNomGiro` | `CHAR(40)` | L104 |
| `cActividadSoc` | `CHAR(3)` | L105 |
| `cDesActObj` | `CHAR(30)` | L106 |
| *…67 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_correos` | `bdinteg` | ⚠️ sí | SELECT | L366 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarctemoral_04` | `bdinteg` | ⚠️ sí | L311 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L175 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L294 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L329 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultarctemoral_04';` |  |
| L331 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L347 | CÓDIGO_RETORNO | `LET cCodRet = '00701';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfoctemoralrfc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfoctemoralrfc.sql` |
| **LOC (1er CREATE)** | 394 |
| **Callgraph** | ✅ fan_in=0 / fan_out=63 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, ctemoral — cuenta temporal y RFC" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfoctemoralrfc(
  pRFC                         CHAR(13)
) RETURNING CHAR(5) 		AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRFC` | `CHAR(13)` | `rfc`=RFC (registro fiscal) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L83 |
| `iSqlErr` | `INTEGER` | L84 |
| `cCodRetSp` | `CHAR(6)` | L85 |
| `iCodRetSp` | `INTEGER` | L86 |
| `cRFC` | `CHAR(13)` | L87 |
| `cSucursal` | `CHAR(4)` | L88 |
| `cApellPaterContactoRepLeg` | `CHAR(26)` | L89 |
| `cApellMaterContactoRepLeg` | `CHAR(26)` | L90 |
| `cNomb1ContactoRepLeg` | `CHAR(26)` | L91 |
| `cNomb2ContactoRepLeg` | `CHAR(26)` | L92 |
| `cCalleFiscal` | `CHAR(40)` | L93 |
| `cNumExtCalleFiscal` | `CHAR(10)` | L94 |
| `cColFiscal` | `CHAR(60)` | L95 |
| `vNomCiudFiscal` | `CHAR(60)` | L96 |
| `cCodMunFiscal` | `CHAR(3)` | L97 |
| `cNomEstadoFiscal` | `CHAR(30)` | L98 |
| `cNumcte` | `CHAR(20)` | L99 |
| `cNomCorto` | `CHAR(60)` | L100 |
| `cPagInternet` | `CHAR(30)` | L101 |
| `cSatFea` | `CHAR(25)` | L102 |
| `cTelContacto` | `CHAR(15)` | L103 |
| `cGiro` | `CHAR(20)` | L104 |
| `cNomGiro` | `CHAR(40)` | L105 |
| `cActividadSoc` | `CHAR(3)` | L106 |
| `cDesActObj` | `CHAR(30)` | L107 |
| *…67 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_correos` | `bdinteg` | ⚠️ sí | SELECT | L367 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarctemoral_rfc` | `bdinteg` | ⚠️ sí | L312 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L176 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L295 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L330 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultarctemoral_04';` |  |
| L332 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L348 | CÓDIGO_RETORNO | `LET cCodRet = '00701';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `rfc` | ENTIDAD | RFC (registro fiscal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultainfocteprospecto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfocteprospecto.sql` |
| **LOC (1er CREATE)** | 333 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfocteprospecto(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L78 |
| `cCodRet` | `CHAR(5)` | L79 |
| `cCodRetSp` | `CHAR(5)` | L80 |
| `vempresa` | `CHAR(3)` | L82 |
| `vnumcte` | `CHAR(20)` | L83 |
| `vstatus_cte` | `CHAR(2)` | L84 |
| `vsucursal` | `CHAR(4)` | L85 |
| `vejecutivo` | `CHAR(8)` | L86 |
| `vtpo_persona` | `CHAR(2)` | L87 |
| `vtipo_cliente` | `CHAR(1)` | L88 |
| `vapell_paterno` | `CHAR(26)` | L89 |
| `vapell_materno` | `CHAR(26)` | L90 |
| `vnombre1` | `CHAR(26)` | L91 |
| `vnombre2` | `CHAR(26)` | L92 |
| `vrazon_social` | `CHAR(60)` | L93 |
| `vrfc` | `CHAR(13)` | L94 |
| `vsector` | `CHAR(2)` | L95 |
| `vsegmento` | `CHAR(3)` | L96 |
| `vactividad_princ` | `CHAR(3)` | L97 |
| `vgrupo` | `CHAR(3)` | L98 |
| `vsubgrupo` | `CHAR(3)` | L99 |
| `vresidencia` | `CHAR(1)` | L100 |
| `vfecha_alta` | `DATE` | L101 |
| `vapell_casada` | `CHAR(26)` | L102 |
| `vdistrito` | `CHAR(2)` | L103 |
| *…51 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L281 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `consnumcte_n` | `bdinteg` | ⚠️ sí | L287 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L255 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfocteprospecto2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfocteprospecto2.sql` |
| **LOC (1er CREATE)** | 311 |
| **Callgraph** | ✅ fan_in=0 / fan_out=62 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfocteprospecto2(
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L78 |
| `cCodRet` | `CHAR(5)` | L79 |
| `cCodRetSp` | `CHAR(5)` | L80 |
| `vempresa` | `CHAR(3)` | L82 |
| `vnumcte` | `CHAR(20)` | L83 |
| `vstatus_cte` | `CHAR(2)` | L84 |
| `vsucursal` | `CHAR(4)` | L85 |
| `vejecutivo` | `CHAR(8)` | L86 |
| `vtpo_persona` | `CHAR(2)` | L87 |
| `vtipo_cliente` | `CHAR(1)` | L88 |
| `vapell_paterno` | `CHAR(26)` | L89 |
| `vapell_materno` | `CHAR(26)` | L90 |
| `vnombre1` | `CHAR(26)` | L91 |
| `vnombre2` | `CHAR(26)` | L92 |
| `vrazon_social` | `CHAR(254)` | L93 |
| `vrfc` | `CHAR(13)` | L94 |
| `vsector` | `CHAR(2)` | L95 |
| `vsegmento` | `CHAR(3)` | L96 |
| `vactividad_princ` | `CHAR(3)` | L97 |
| `vgrupo` | `CHAR(3)` | L98 |
| `vsubgrupo` | `CHAR(3)` | L99 |
| `vresidencia` | `CHAR(1)` | L100 |
| `vfecha_alta` | `DATE` | L101 |
| `vapell_casada` | `CHAR(26)` | L102 |
| `vdistrito` | `CHAR(2)` | L103 |
| *…51 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L259 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `consnumcte_n` | `bdinteg` | ⚠️ sí | L265 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfocteprospectorfc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfocteprospectorfc.sql` |
| **LOC (1er CREATE)** | 320 |
| **Callgraph** | ✅ fan_in=0 / fan_out=61 |
| **Deps concatenadas** | 67 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, Cliente Prospecto — cliente potencial aún sin cuenta abierta, OS — Originación de Solicitudes y RFC" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfocteprospectorfc(
  pRfc                         CHAR(13)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRfc` | `CHAR(13)` | `rfc`=RFC (registro fiscal) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L78 |
| `cCodRet` | `CHAR(5)` | L79 |
| `cCodRetSp` | `CHAR(5)` | L80 |
| `vempresa` | `CHAR(3)` | L82 |
| `vnumcte` | `CHAR(20)` | L83 |
| `vstatus_cte` | `CHAR(2)` | L84 |
| `vsucursal` | `CHAR(4)` | L85 |
| `vejecutivo` | `CHAR(8)` | L86 |
| `vtpo_persona` | `CHAR(2)` | L87 |
| `vtipo_cliente` | `CHAR(1)` | L88 |
| `vapell_paterno` | `CHAR(26)` | L89 |
| `vapell_materno` | `CHAR(26)` | L90 |
| `vnombre1` | `CHAR(26)` | L91 |
| `vnombre2` | `CHAR(26)` | L92 |
| `vrazon_social` | `CHAR(254)` | L93 |
| `vrfc` | `CHAR(13)` | L94 |
| `vsector` | `CHAR(2)` | L95 |
| `vsegmento` | `CHAR(3)` | L96 |
| `vactividad_princ` | `CHAR(3)` | L97 |
| `vgrupo` | `CHAR(3)` | L98 |
| `vsubgrupo` | `CHAR(3)` | L99 |
| `vresidencia` | `CHAR(1)` | L100 |
| `vfecha_alta` | `DATE` | L101 |
| `vapell_casada` | `CHAR(26)` | L102 |
| `vdistrito` | `CHAR(2)` | L103 |
| *…51 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L280 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `consnumcte_prospectorfc` | `bdinteg` | ⚠️ sí | L268 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L256 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rfc` | ENTIDAD | RFC (registro fiscal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfocteretieneisrcap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfocteretieneisrcap.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, cliente y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consultainfocteretieneisr` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfocteretieneisrcap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pAnio                        SMALLINT
  pTipo                        SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pAnio` | `SMALLINT` | — | — |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cMensaje` | `CHAR(60)` | L22 |
| `cTipoPersona` | `CHAR(2)` | L23 |
| `cNombre` | `CHAR(104)` | L24 |
| `cCuenta` | `CHAR(20)` | L25 |
| `cRfc` | `CHAR(13)` | L26 |
| `cCurp` | `CHAR(20)` | L27 |
| `dInteresNomTotal` | `DECIMAL(16, 2)` | L28 |
| `dInteresReal` | `DECIMAL(16, 2)` | L29 |
| `dPerdida` | `DECIMAL(16, 2)` | L30 |
| `dInteresNomExento` | `DECIMAL(16, 2)` | L31 |
| `dRetenInteres` | `DECIMAL(16, 2)` | L32 |
| `cRazonSocialRetenedor` | `CHAR(60)` | L33 |
| `cRfcRetenedor` | `CHAR(13)` | L34 |
| `cNombreRepLegal` | `CHAR(104)` | L35 |
| `cRepLegalIsr` | `CHAR(13)` | L36 |
| `cCurpRepLegal` | `CHAR(20)` | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultainfocteretieneisr` | `bdicheq` | ⚠️ sí | L100 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L70 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR (pAnio IS NULL OR pAnio = 0)  OR pTipo IS NULL OR pCliente = ` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00049';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00233';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L113 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN EL AEJECUCION DEL SP bdicheq:sp_consultainfoctereti` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?retiene` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?retiene` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfoperacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfoperacionescaja.sql` |
| **LOC (1er CREATE)** | 210 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfoperacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoFolio                     CHAR(8)
  pIdSucursal                  CHAR(4)
  pCodTransaccion              CHAR(4)
  pTipoConsulta                CHAR(1)
  pIdCajaGen                   CHAR(4)
  pTipoSucursal                CHAR(1)
  pFechaInic                   DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoFolio` | `CHAR(8)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pCodTransaccion` | `CHAR(4)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pIdCajaGen` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pTipoSucursal` | `CHAR(1)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `cCodRetSp` | `CHAR(6)` | L36 |
| `iCodRetSp` | `INTEGER` | L37 |
| `iSqlErr` | `INTEGER` | L38 |
| `cSucursal` | `CHAR(50)` | L39 |
| `dFecha` | `DATE` | L40 |
| `cCodTrans` | `CHAR(4)` | L41 |
| `cDatoReversado` | `CHAR(2)` | L42 |
| `cReversado` | `CHAR(2)` | L43 |
| `cUsuario` | `CHAR(40)` | L44 |
| `cDivisa` | `CHAR(40)` | L45 |
| `mImporte` | `MONEY(14,2)` | L46 |
| `dCantidad1` | `DECIMAL(10,2)` | L47 |
| `dCantidad2` | `DECIMAL(10,2)` | L48 |
| `dCantidad3` | `DECIMAL(10,2)` | L49 |
| `dCantidad4` | `DECIMAL(10,2)` | L50 |
| `dCantidad5` | `DECIMAL(10,2)` | L51 |
| `dCantidad6` | `DECIMAL(10,2)` | L52 |
| `dCantidad7` | `DECIMAL(10,2)` | L53 |
| `dCantidad8` | `DECIMAL(10,2)` | L54 |
| `dCantidad9` | `DECIMAL(10,2)` | L55 |
| `dCantidad10` | `DECIMAL(10,2)` | L56 |
| `dCantidad11` | `DECIMAL(10,2)` | L57 |
| `dCantidad12` | `DECIMAL(10,2)` | L58 |
| `dCantidad13` | `DECIMAL(10,2)` | L59 |
| *…12 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L140 |
| `bdisuc` | `bdicnweb` | no | L157 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L124 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSucursal = '' OR pTipoConsulta = '' OR pRegistros IS NUL` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L151 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L174 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_consul_operaciones2'` |  |
| L176 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L181 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L190 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?peraciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?peraciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainforelacionctebcpcp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainforelacionctebcpcp.sql` |
| **LOC (1er CREATE)** | 154 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, cliente y código postal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainforelacionctebcpcp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L29 |
| `iSqlErr` | `INTEGER` | L30 |
| `cCodRetSp` | `CHAR(6)` | L31 |
| `cMensajeRetorno` | `CHAR(100)` | L32 |
| `cNumCliente` | `CHAR(20)` | L33 |
| `cNombreCliente` | `CHAR(107)` | L34 |
| `dFechaNacimiento` | `DATE` | L35 |
| `iTipoRelacion` | `SMALLINT` | L36 |
| `cDescripcionTipoRelacion` | `CHAR(100)` | L37 |
| `cNombreAnalista` | `CHAR(107)` | L38 |
| `cNumClienteCoppel` | `CHAR(20)` | L39 |
| `cNombreClienteCoppel` | `CHAR(104)` | L40 |
| `cFechaNacimientoCoppel` | `CHAR(10)` | L41 |
| `cFechaRelacion` | `CHAR(10)` | L42 |
| `dEficiencia` | `DECIMAL(5,2)` | L43 |
| `iMesesHistoria` | `SMALLINT` | L44 |
| `cPuntualidad` | `CHAR(1)` | L45 |
| `dAbonosVencidos` | `DECIMAL(14,6)` | L46 |
| `cSituacionEspecial` | `CHAR(1)` | L47 |
| `iCausaSituacionEspecial` | `SMALLINT` | L48 |
| `cDescripcionCausaSituacionEspecial` | `CHAR(100)` | L49 |
| `cNombre1` | `CHAR(26)` | L50 |
| `cNombre2` | `CHAR(26)` | L51 |
| `cApellidoPaterno` | `CHAR(26)` | L52 |
| `cApellidoMaterno` | `CHAR(26)` | L53 |
| *…4 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L128 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdinteg:sp_relacion_consultain` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '90000';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?relacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?relacion`, `?b` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainforeportebc_alertahawk`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainforeportebc_alertahawk.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, reporte y alerta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainforeportebc_alertahawk(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iCodRet` | `INTEGER` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cInstitucion` | `CHAR(2)` | L19 |
| `cNumCliente` | `CHAR(20)` | L20 |
| `dFechaReportehi` | `DATE` | L21 |
| `cClaveHi` | `CHAR(3)` | L22 |
| `cOtorganteHi` | `CHAR(16)` | L23 |
| `cMensajeHi` | `CHAR(48)` | L24 |
| `dFechaReportehr` | `DATE` | L25 |
| `cClaveHr` | `CHAR(3)` | L26 |
| `cOtorganteHr` | `CHAR(16)` | L27 |
| `cMensajeHr` | `CHAR(48)` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `br_hi` | `bdiburo` | ⚠️ sí | SELECT | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?bc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alerta` | ENTIDAD | alerta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?hawk` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc_`, `?hawk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainforeportebc_datosgenerales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainforeportebc_datosgenerales.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, reporte y datos (general)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainforeportebc_datosgenerales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `cCodRetSp` | `CHAR(6)` | L15 |
| `iCodRet` | `INTEGER` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cInstitucion` | `CHAR(2)` | L18 |
| `cNumCliente` | `CHAR(20)` | L19 |
| `dFechaConsulta` | `DATE` | L20 |
| `cApellido1` | `CHAR(26)` | L21 |
| `cApellido2` | `CHAR(26)` | L22 |
| `cNombre1` | `CHAR(26)` | L23 |
| `cNombre2` | `CHAR(26)` | L24 |
| `dFechaNacimiento` | `DATE` | L25 |
| `cRfc` | `CHAR(13)` | L26 |
| `dRegistroBc` | `DATE` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `br_pn` | `bdiburo` | ⚠️ sí | SELECT | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?bc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc_`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainforeportebc_detalle_creditos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainforeportebc_detalle_creditos.sql` |
| **LOC (1er CREATE)** | 275 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, reporte, detalle y créditos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainforeportebc_detalle_creditos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L38 |
| `iSqlErr` | `INTEGER` | L39 |
| `cInstitucion` | `CHAR(2)` | L40 |
| `cNumCliente` | `CHAR(20)` | L41 |
| `dFechaActualizado` | `DATE` | L42 |
| `cOtorgamientoCuenta` | `CHAR(16)` | L43 |
| `cMoneda` | `CHAR(2)` | L44 |
| `mMontoPagar` | `MONEY(14,2)` | L45 |
| `dFechaApertura` | `DATE` | L46 |
| `dFechaUltimoPago` | `DATE` | L47 |
| `dFechaUltimaCompra` | `DATE` | L48 |
| `dFechaCierre` | `DATE` | L49 |
| `dFechaUltimaVezSaldo` | `DATE` | L50 |
| `mImporteCreditoMaximo` | `MONEY(14,6)` | L51 |
| `mImporteSaldoActual` | `MONEY(14,6)` | L52 |
| `mImporteLimiteCredito` | `MONEY(14,6)` | L53 |
| `mImporteMonto` | `MONEY(14,6)` | L54 |
| `cFormaPagoFechaActualizacion` | `CHAR(2)` | L55 |
| `cValorX` | `CHAR(24)` | L56 |
| `dFecha2` | `DATE` | L57 |
| `cClaveObservacion` | `CHAR(2)` | L58 |
| `mMopImporte` | `MONEY(14,2)` | L59 |
| `dMopFecha` | `DATE` | L60 |
| `cMaxMoratorios` | `CHAR(2)` | L61 |
| `cResponsabilidad` | `CHAR(40)` | L62 |
| *…14 más…* | | |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L134 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L145 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L188 | FÓRMULA | `LET fanios = YEAR(dFecha2) - 1;` |  |
| L190 | FÓRMULA | `LET aini = YEAR(dFecha2) - 2;` |  |
| L197 | FÓRMULA | `LET finter = SUBSTR(cValorX, LENGTH(fini2) + 1, 12);` |  |
| L198 | FÓRMULA | `LET faini = SUBSTR(cValorX, LENGTH(fini2) + LENGTH(finter) + 1, 12);` |  |
| L245 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L250 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?bc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `creditos` | ENTIDAD | créditos (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainforeportebc_detalleconsultas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainforeportebc_detalleconsultas.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 404 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, reporte y detalle" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainforeportebc_detalleconsultas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iCodRet` | `INTEGER` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cInstitucion` | `CHAR(2)` | L17 |
| `cNumCliente` | `CHAR(20)` | L18 |
| `cOtorgante` | `CHAR(16)` | L19 |
| `dFechaConsulta` | `DATE` | L20 |
| `cResponsabilidad` | `CHAR(1)` | L21 |
| `cTipoContrato` | `CHAR(40)` | L22 |
| `mImporte` | `MONEY(9,2)` | L23 |
| `cTipoMoneda` | `CHAR(2)` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `br_iq` | `bdiburo` | ⚠️ sí | SELECT | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumcte = '' OR pRegistros IS NULL OR pRecuperacion IS NULL T` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L83 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?bc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc_`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainforeportebc_domiciliosreportados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainforeportebc_domiciliosreportados.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, reporte, domicilio y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainforeportebc_domiciliosreportados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cInstitucion` | `CHAR(2)` | L17 |
| `cNumCliente` | `CHAR(20)` | L18 |
| `cCalle` | `CHAR(40)` | L19 |
| `cCalle2` | `CHAR(40)` | L20 |
| `cColonia` | `CHAR(40)` | L21 |
| `cDelegacionMunicipio` | `CHAR(40)` | L22 |
| `cCiudad` | `CHAR(40)` | L23 |
| `cEstado` | `CHAR(4)` | L24 |
| `cCp` | `CHAR(5)` | L25 |
| `cTelefono` | `CHAR(11)` | L26 |
| `dFechaRegistroBc` | `DATE` | L27 |
| `iNoRegs` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `br_pa` | `bdiburo` | ⚠️ sí | SELECT | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?bc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `domicilio` | ENTIDAD | domicilio | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?ortad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc_`, `?s`, `?ortad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainforeportebc_ingresos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainforeportebc_ingresos.sql` |
| **LOC (1er CREATE)** | 130 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, reporte y ingreso" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainforeportebc_ingresos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cInstitucion` | `CHAR(2)` | L23 |
| `cNumCliente` | `CHAR(20)` | L24 |
| `cCompania` | `CHAR(40)` | L25 |
| `cCalle1` | `CHAR(40)` | L26 |
| `cCalle2` | `CHAR(40)` | L27 |
| `cColonia` | `CHAR(40)` | L28 |
| `cDelmun` | `CHAR(40)` | L29 |
| `cCiudad` | `CHAR(40)` | L30 |
| `cEstado` | `CHAR(4)` | L31 |
| `cCp` | `CHAR(5)` | L32 |
| `cTelefono` | `CHAR(11)` | L33 |
| `cPuesto` | `CHAR(30)` | L34 |
| `dFechaContratacion` | `DATE` | L35 |
| `mSalario` | `MONEY(14,2)` | L36 |
| `cBase` | `CHAR(1)` | L37 |
| `dUltimoDiaEmpleo` | `DATE` | L38 |
| `dRegistroBc` | `DATE` | L39 |
| `iNoRegs` | `INTEGER` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `br_pe` | `bdiburo` | ⚠️ sí | SELECT | L108 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?bc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ingreso` | ENTIDAD | ingreso (del solicitante) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc_`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainforeportebc_resumencreditos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainforeportebc_resumencreditos.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, reporte y créditos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainforeportebc_resumencreditos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `cCodRetSp` | `CHAR(6)` | L25 |
| `iCodRet` | `INTEGER` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `cInstitucion` | `CHAR(2)` | L28 |
| `cNumCliente` | `CHAR(20)` | L29 |
| `iCtasAbiertas` | `INTEGER` | L30 |
| `iLimAbiertas` | `MONEY(9,2)` | L31 |
| `iMaxAbiertas` | `MONEY(9,2)` | L32 |
| `iSaldoAct` | `MONEY(10,2)` | L33 |
| `iMontoAbiertas` | `MONEY(9,2)` | L34 |
| `iPagoRealizar` | `MONEY(9,2)` | L35 |
| `iCtasCerradas` | `INTEGER` | L36 |
| `iCtasMob00` | `INTEGER` | L37 |
| `iCtasMob01` | `INTEGER` | L38 |
| `iCtasMob02` | `INTEGER` | L39 |
| `iCtasMob03` | `INTEGER` | L40 |
| `iCtasMob04` | `INTEGER` | L41 |
| `mCtasMob05` | `INTEGER` | L42 |
| `mCtasMob06` | `INTEGER` | L43 |
| `mCtasMob07` | `INTEGER` | L44 |
| `mCtasMobur` | `INTEGER` | L45 |
| `mTotalCtas` | `INTEGER` | L46 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `br_rs` | `bdiburo` | ⚠️ sí | SELECT | L125 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?bc_resumen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `creditos` | ENTIDAD | créditos (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc_resumen` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainformacionctamoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainformacionctamoral.sql` |
| **LOC (1er CREATE)** | 273 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, dirección MAC y cuenta (persona moral)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_ctamec_consultarinfoctamoral` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainformacionctamoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `iSqlErr` | `INTEGER` | L36 |
| `cCodRetSp` | `CHAR(6)` | L37 |
| `iCodRetSp` | `INTEGER` | L38 |
| `cEmpresa` | `CHAR(3)` | L39 |
| `cMensaje` | `CHAR(60)` | L40 |
| `cCodigoproducto` | `CHAR(4)` | L41 |
| `cDescproducto` | `CHAR(40)` | L42 |
| `cNumcliente` | `CHAR(20)` | L43 |
| `cNombrecliente` | `CHAR(60)` | L44 |
| `cCodregimen` | `CHAR(1)` | L45 |
| `cRegimendefirmas` | `CHAR(20)` | L46 |
| `sNumerofirmantes` | `SMALLINT` | L47 |
| `cClavefirmante` | `CHAR(20)` | L48 |
| `cNombrefirmante` | `CHAR(30)` | L49 |
| `cApellfirmante` | `CHAR(30)` | L50 |
| `sSecuencia` | `SMALLINT` | L51 |
| `cTipofirma` | `CHAR(1)` | L52 |
| `cCombinacion` | `CHAR(20)` | L53 |
| `cCodigoprocedenciareca` | `CHAR(2)` | L54 |
| `cDescprocedenciareca` | `CHAR(60)` | L55 |
| `cCodigoprocedenciarecm` | `CHAR(2)` | L56 |
| `cDescprocedenciarecm` | `CHAR(60)` | L57 |
| `cCodnumerodeposito` | `CHAR(2)` | L58 |
| `cDescnumerodeposito` | `CHAR(60)` | L59 |
| *…12 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L136 |
| `sp_ctamec_consultarinfoctamoral` | `bdicheq` | ⚠️ sí | L147 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L156 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_ctamec_consultarinfoctamoral: '\|\|cM` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L165 | CÓDIGO_RETORNO | `LET cCodRet = '00070';` |  |
| L172 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L179 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L186 | CÓDIGO_RETORNO | `LET cCodRet = '00303';` |  |
| L193 | CÓDIGO_RETORNO | `LET cCodRet = '00299';` |  |
| L200 | CÓDIGO_RETORNO | `LET cCodRet = '00304';` |  |
| L207 | CÓDIGO_RETORNO | `LET cCodRet = '00305';` |  |
| L214 | CÓDIGO_RETORNO | `LET cCodRet = '00306';` |  |
| L221 | CÓDIGO_RETORNO | `LET cCodRet = '00307';` |  |
| L228 | CÓDIGO_RETORNO | `LET cCodRet = '00308';` |  |
| L235 | CÓDIGO_RETORNO | `LET cCodRet = '00309';` |  |
| L242 | CÓDIGO_RETORNO | `LET cCodRet = '00310';` |  |
| L249 | CÓDIGO_RETORNO | `LET cCodRet = '00311';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mac` | ENTIDAD | dirección MAC | 🔵 CONVENCIÓN | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `moral` | MODIF | persona moral | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?ion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainformacionctamoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainformacionctamoral2.sql` |
| **LOC (1er CREATE)** | 279 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 118 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, dirección MAC y cuenta (persona moral)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_ctamec_consultarinfoctamoral2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainformacionctamoral2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `iSqlErr` | `INTEGER` | L36 |
| `cCodRetSp` | `CHAR(6)` | L37 |
| `iCodRetSp` | `INTEGER` | L38 |
| `cEmpresa` | `CHAR(3)` | L39 |
| `cMensaje` | `CHAR(60)` | L40 |
| `cCodigoproducto` | `CHAR(4)` | L41 |
| `cDescproducto` | `CHAR(40)` | L42 |
| `cNumcliente` | `CHAR(20)` | L43 |
| `cNombrecliente` | `CHAR(60)` | L44 |
| `cCodregimen` | `CHAR(1)` | L45 |
| `cRegimendefirmas` | `CHAR(20)` | L46 |
| `sNumerofirmantes` | `SMALLINT` | L47 |
| `cClavefirmante` | `CHAR(20)` | L48 |
| `cNombrefirmante` | `CHAR(30)` | L49 |
| `cApellfirmante` | `CHAR(30)` | L50 |
| `sSecuencia` | `SMALLINT` | L51 |
| `cTipofirma` | `CHAR(1)` | L52 |
| `cCombinacion` | `CHAR(20)` | L53 |
| `cCodigoprocedenciareca` | `CHAR(2)` | L54 |
| `cDescprocedenciareca` | `CHAR(60)` | L55 |
| `cCodigoprocedenciarecm` | `CHAR(2)` | L56 |
| `cDescprocedenciarecm` | `CHAR(60)` | L57 |
| `cCodnumerodeposito` | `CHAR(2)` | L58 |
| `cDescnumerodeposito` | `CHAR(60)` | L59 |
| *…12 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L138 |
| `sp_ctamec_consultarinfoctamoral2` | `bdicheq` | ⚠️ sí | L149 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_ctamec_consultarinfoctamoral: '\|\|cM` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '00070';` |  |
| L174 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L181 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L188 | CÓDIGO_RETORNO | `LET cCodRet = '00303';` |  |
| L195 | CÓDIGO_RETORNO | `LET cCodRet = '00299';` |  |
| L202 | CÓDIGO_RETORNO | `LET cCodRet = '00304';` |  |
| L209 | CÓDIGO_RETORNO | `LET cCodRet = '00305';` |  |
| L216 | CÓDIGO_RETORNO | `LET cCodRet = '00306';` |  |
| L223 | CÓDIGO_RETORNO | `LET cCodRet = '00307';` |  |
| L230 | CÓDIGO_RETORNO | `LET cCodRet = '00308';` |  |
| L237 | CÓDIGO_RETORNO | `LET cCodRet = '00309';` |  |
| L244 | CÓDIGO_RETORNO | `LET cCodRet = '00310';` |  |
| L251 | CÓDIGO_RETORNO | `LET cCodRet = '00311';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mac` | ENTIDAD | dirección MAC | 🔵 CONVENCIÓN | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `moral` | MODIF | persona moral | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?ion`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfosolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfosolicitudmc.sql` |
| **LOC (1er CREATE)** | 402 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, solicitud y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_mc_obteninfosolicitudsoc`, `sp_valida_solicitud_movil`, `sp_calificacion_scoring` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfosolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L77 |
| `cCodRetSp` | `CHAR(6)` | L78 |
| `iSqlErr` | `INTEGER` | L79 |
| `cEmpresa` | `CHAR(3)` | L80 |
| `iCodRetSp` | `INTEGER` | L81 |
| `cMensajeRetorno` | `CHAR(80)` | L83 |
| `cNumeroCliente` | `CHAR(20)` | L84 |
| `cNombreCliente` | `CHAR(104)` | L85 |
| `cRfc` | `CHAR(13)` | L86 |
| `cSucursal` | `CHAR(4)` | L87 |
| `cNumClienteCoppel` | `CHAR(20)` | L88 |
| `dLineaCoppel` | `DECIMAL(18,2)` | L89 |
| `dEficienciaPago` | `DECIMAL(18,2)` | L90 |
| `iMesesHist` | `SMALLINT` | L91 |
| `cPuntualidad` | `CHAR(2)` | L92 |
| `dAbonosVen` | `DECIMAL(18,2)` | L93 |
| `cSituacionCredito` | `CHAR(1)` | L94 |
| `iCausa` | `SMALLINT` | L95 |
| `cDescripcionSituacionEspecial` | `CHAR(40)` | L96 |
| `cNumeroSolicitud` | `CHAR(20)` | L97 |
| `dFechaSolicitud` | `DATE` | L98 |
| `dFechaCambioEstatus` | `DATE` | L99 |
| `dBcScore` | `DECIMAL(18,2)` | L100 |
| `dScoreProp` | `DECIMAL(18,2)` | L101 |
| `dResultadoTotal` | `DECIMAL(18,2)` | L102 |
| *…56 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_mc_obteninfosolicitudsoc` | `bdisolic` | ⚠️ sí | L289 |
| `sp_valida_solicitud_movil` | `bdisolic` | ⚠️ sí | L331 |
| `sp_calificacion_scoring` | `bdicnweb` | no | L340 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L262 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L303 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN EJECUCION DE SP PRODUCTIVO sp_mc_obteninfosolicitudgen';` |  |
| L305 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L316 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L336 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN EJECUCION DE SP PRODUCTIVO sp_valida_solicitud_movil';` |  |
| L344 | EXCEPCIÓN | `RAISE EXCEPTION cCodRet::INTEGER, 0, 'ERROR EN EJECUCION DE SP bdicnweb:sp_calificacion_scoring';` |  |
| L356 | EXCEPCIÓN | `RAISE EXCEPTION cCodRet::INTEGER, 0, 'ERROR EN EJECUCION DE SP bdicnweb:sp_calificacion_scoring';` |  |
| L363 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultainfsdohistoricocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfsdohistoricocaja.sql` |
| **LOC (1er CREATE)** | 223 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, saldo y caja (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdisuc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfsdohistoricocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSaldo                   CHAR(1)
  pCcMayor                     CHAR(4)
  pCcSub                       CHAR(2)
  pCcsubsub                    CHAR(2)
  pCcssubsub                   CHAR(2)
  pCcsssubsub                  CHAR(2)
  pSector                      CHAR(2)
  pFechaMes                    CHAR(2)
  pFechaAnio                   CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSaldo` | `CHAR(1)` | — | — |
| `pCcMayor` | `CHAR(4)` | — | — |
| `pCcSub` | `CHAR(2)` | — | — |
| `pCcsubsub` | `CHAR(2)` | — | — |
| `pCcssubsub` | `CHAR(2)` | — | — |
| `pCcsssubsub` | `CHAR(2)` | — | — |
| `pSector` | `CHAR(2)` | — | — |
| `pFechaMes` | `CHAR(2)` | — | — |
| `pFechaAnio` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L41 |
| `cCodRetSp` | `CHAR(6)` | L42 |
| `iCodRetSp` | `INTEGER` | L43 |
| `iSqlErr` | `INTEGER` | L44 |
| `cEmpresa` | `CHAR(3)` | L45 |
| `sTipo` | `SMALLINT` | L46 |
| `dFechaMinSel` | `DATE` | L47 |
| `dFechaMaxSel` | `DATE` | L48 |
| `dFechaMaxima` | `DATE` | L49 |
| `cSucursal` | `CHAR(4)` | L50 |
| `cNombre` | `CHAR(40)` | L51 |
| `mDia1` | `MONEY(14,2)` | L52 |
| `mDia2` | `MONEY(14,2)` | L53 |
| `mDia3` | `MONEY(14,2)` | L54 |
| `mDia4` | `MONEY(14,2)` | L55 |
| `mDia5` | `MONEY(14,2)` | L56 |
| `mDia6` | `MONEY(14,2)` | L57 |
| `mDia7` | `MONEY(14,2)` | L58 |
| `mDia8` | `MONEY(14,2)` | L59 |
| `mDia9` | `MONEY(14,2)` | L60 |
| `mDia10` | `MONEY(14,2)` | L61 |
| `mDia11` | `MONEY(14,2)` | L62 |
| `mDia12` | `MONEY(14,2)` | L63 |
| `mDia13` | `MONEY(14,2)` | L64 |
| `mDia14` | `MONEY(14,2)` | L65 |
| *…18 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L161 |
| `bdisuc` | `bdicnweb` | no | L182 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L143 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSaldo = '' OR pFechaMes = '' OR pFechaAnio = '' OR pRegi` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L177 | FÓRMULA | `LET dFechaMinSel = TO_DATE(1\|\|'/'\|\|(pFechaMes::INTEGER)\|\|'/'\|\|(pFechaAnio::INTEGER),'%d/%m/%` |  |
| L178 | FÓRMULA | `LET dFechaMaxima = dFechaMinSel + 1 UNITS MONTH;` |  |
| L179 | FÓRMULA | `LET dFechaMaxSel = dFechaMaxima - 1 UNITS DAY;` |  |
| L189 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_sel_sdohistorico2';` |  |
| L191 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L200 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultainfsdostef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfsdostef.sql` |
| **LOC (1er CREATE)** | 209 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, saldos y TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfsdostef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pNumTarjeta                  CHAR(16)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L29 |
| `cCodRetSp` | `CHAR(6)` | L30 |
| `cCodRetSp2` | `CHAR(5)` | L31 |
| `cDescCodRet` | `CHAR(100)` | L32 |
| `iCodRetSp` | `INTEGER` | L33 |
| `iSqlErr` | `INTEGER` | L34 |
| `cEmpresa` | `CHAR(3)` | L35 |
| `cCuenta` | `CHAR(20)` | L36 |
| `cNoCliente` | `CHAR(20)` | L37 |
| `cApPaterno` | `CHAR(26)` | L38 |
| `cApMaterno` | `CHAR(26)` | L39 |
| `cNombre1` | `CHAR(26)` | L40 |
| `cNombre2` | `CHAR(26)` | L41 |
| `cRazonSocial` | `CHAR(60)` | L42 |
| `cStatusCuenta` | `CHAR(1)` | L43 |
| `mSaldoDisponible` | `MONEY(14,2)` | L44 |
| `mSaldoRetenido` | `MONEY(14,2)` | L45 |
| `mSaldoCCC` | `MONEY(14,2)` | L46 |
| `mSaldoCCCDisp` | `MONEY(14,2)` | L47 |
| `mSaldoCuenta` | `MONEY(14,2)` | L48 |
| `cTipoLinea` | `CHAR(1)` | L49 |
| `cDescripcion1` | `CHAR(40)` | L50 |
| `cDescripcion2` | `CHAR(40)` | L51 |
| `mSaldoT1` | `MONEY(14,2)` | L52 |
| `mSaldoCongelado` | `MONEY(14,2)` | L53 |
| *…10 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L122 |
| `cons_sdos2` | `bdicheq` | ⚠️ sí | L138 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L144 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:cons_sdos2';` |  |
| L185 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L189 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `sdos` | ENTIDAD | saldos (abreviación) | 🟡 INFERIDO | nombre_sp |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultainfsucabrieroncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfsucabrieroncaja.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, ro — Rol Operativo y caja (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfsucabrieroncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cFechaAbrio` | `DATE` | L15 |
| `cIdSuc` | `CHAR(4)` | L16 |
| `cIdSucursalAbrio` | `CHAR(4)` | L17 |
| `cDescSuc` | `CHAR(40)` | L18 |
| `cDescSucursalAbrio` | `CHAR(40)` | L19 |
| `cDescCaja` | `CHAR(30)` | L20 |
| `cNombreCajaAbrio` | `CHAR(30)` | L21 |
| `cSucAbrio` | `CHAR(1)` | L22 |
| `iRecuperacion` | `INTEGER` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta = '' OR pFechaConsulta IS NULL OR pRegistros I` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L74 | VALIDACIÓN_NULL | `IF cIdSuc IS NULL OR cIdSuc = '' THEN` |  |
| L90 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `suc` | MODIF | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?abrie` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?n` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?abrie`, `ro`, `?n` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultainfsucnoabrieroncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultainfsucnoabrieroncaja.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta información, ro — Rol Operativo y caja (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultainfsucnoabrieroncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `cCodRetSp` | `CHAR(6)` | L28 |
| `iCodRetSp` | `INTEGER` | L29 |
| `iSqlErr` | `INTEGER` | L30 |
| `sFecha` | `DATE` | L31 |
| `sSucursal` | `CHAR(4)` | L32 |
| `cDescSucursal` | `CHAR(40)` | L33 |
| `cDescDivisa` | `CHAR(30)` | L34 |
| `sSaldoTotal` | `MONEY(14,2)` | L35 |
| `sCant1` | `DECIMAL(10,2)` | L36 |
| `sCant2` | `DECIMAL(10,2)` | L37 |
| `sCant3` | `DECIMAL(10,2)` | L38 |
| `sCant4` | `DECIMAL(10,2)` | L39 |
| `sCant5` | `DECIMAL(10,2)` | L40 |
| `sCant6` | `DECIMAL(10,2)` | L41 |
| `sCant7` | `DECIMAL(10,2)` | L42 |
| `sCant8` | `DECIMAL(10,2)` | L43 |
| `sCant9` | `DECIMAL(10,2)` | L44 |
| `sCant10` | `DECIMAL(10,2)` | L45 |
| `sCant11` | `DECIMAL(10,2)` | L46 |
| `sCant12` | `DECIMAL(10,2)` | L47 |
| `sCant13` | `DECIMAL(10,2)` | L48 |
| `sCant14` | `DECIMAL(10,2)` | L49 |
| `sCant15` | `DECIMAL(10,2)` | L50 |
| `sCant16` | `DECIMAL(10,2)` | L51 |
| *…7 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_pase_sucursal` | `bdisuc` | ⚠️ sí | SELECT | L143 |
| `si_divisas` | `bdinteg` | ⚠️ sí | SELECT | L151 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L122 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L106 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta = '' OR pFechaConsulta IS NULL OR pRegistros I` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L130 | FÓRMULA | `LET dFechaAnterior = DATE(pFechaConsulta) -1;` |  |
| L156 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L161 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `suc` | MODIF | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?noabrie` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?n` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?noabrie`, `ro`, `?n` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultalconsatlide`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultalconsatlide.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consultaslconsat` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultalconsatlide(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRfc                         CHAR(13)
  pAnio                        CHAR(6)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pRfc` | `CHAR(13)` | — | — |
| `pAnio` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `dFechaCons` | `DATE` | L9 |
| `cExcento` | `CHAR(1)` | L10 |
| `cEstado` | `CHAR(1)` | L11 |
| `cCodRetSp` | `CHAR(3)` | L12 |
| `iNoRegs` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultaslconsat` | `bdilide` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?atl` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l`, `?atl`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultalide_exentos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultalide_exentos.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta identificador y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultalide_exentos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRfc                         CHAR(13)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pRfc` | `CHAR(13)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cNumCliente` | `CHAR(20)` | L9 |
| `dFechaCambio` | `DATE` | L10 |
| `cEstado` | `CHAR(1)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_exentos` | `bdilide` | ⚠️ sí | SELECT | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l`, `?e_e`, `?ent` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultalideconstancias`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultalideconstancias.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta identificador" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultalideconstancias(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRfc                         CHAR(13)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pRfc` | `CHAR(13)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cAnioMes` | `CHAR(6)` | L9 |
| `cNumCliente` | `CHAR(20)` | L10 |
| `cTipoConstancia` | `CHAR(1)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sl_constancias` | `bdilide` | ⚠️ sí | SELECT | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?tancias` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l`, `?e`, `?tancias` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultalistabancos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultalistabancos.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta banco" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultalistabancos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `SMALLINT` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cClaveBanco` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `cNombreBanco` | `CHAR(20)` | L14 |
| `iSpSkip` | `INTEGER` | L15 |
| `iSpSkipInc` | `SMALLINT` | L16 |
| `bConsultaTerminada` | `BOOLEAN` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cat_bancos_tmp` | `bdicnweb` | no | SELECT | L69 |
| `cat_bancos_tmp` | `bdicnweb` | no | DELETE | L69 |
| `cat_bancos_tmp` | `bdicnweb` | no | INSERT | L84 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L61 |
| `sp_listaltabancos` | `bdinteg` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoConsulta IS NULL OR pRegistros IS NULL OR pRecuperacion ` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00440';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'bdinteg:"informix".sp_listaltabancos';` |  |
| L86 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L94 | FÓRMULA | `LET iSpSkip = iSpSkip + iSpSkipInc;` |  |
| L109 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?lista` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `banco` | ENTIDAD | banco | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?lista`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaltausuariosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaltausuariosccl.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuarios y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaltausuariosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pActivo                      CHAR(1)
  pClave                       CHAR(10)
  pNombre                      CHAR(30)
  pOperacion                   CHAR(1)
  pMonitoreo                   CHAR(1)
  pAdmon                       CHAR(1)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pActivo` | `CHAR(1)` | — | — |
| `pClave` | `CHAR(10)` | — | — |
| `pNombre` | `CHAR(30)` | — | — |
| `pOperacion` | `CHAR(1)` | — | — |
| `pMonitoreo` | `CHAR(1)` | — | — |
| `pAdmon` | `CHAR(1)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `bditarjeta` | `bdicnweb` | no | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L85 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_altausuarios';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?lta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?lta` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultamodificausuariosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamodificausuariosccl.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuarios y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamodificausuariosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave                       CHAR(10)
  pNombre                      CHAR(30)
  pOperacion                   CHAR(1)
  pMonitoreo                   CHAR(1)
  pAdmon                       CHAR(1)
  pFecha                       DATE
  pActivo                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave` | `CHAR(10)` | — | — |
| `pNombre` | `CHAR(30)` | — | — |
| `pOperacion` | `CHAR(1)` | — | — |
| `pMonitoreo` | `CHAR(1)` | — | — |
| `pAdmon` | `CHAR(1)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pActivo` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `bditarjeta` | `bdicnweb` | no | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_modificausuarios` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `modifica` | ACCION | modifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultamontocreditocentral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamontocreditocentral.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta monto y crédito (central)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamontocreditocentral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoCredito                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoCredito` | `CHAR(20)` | `credito`=crédito | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cMensajeRetorno` | `CHAR(80)` | L10 |
| `cNoCliente` | `CHAR(20)` | L11 |
| `cNombreCliente` | `CHAR(104)` | L12 |
| `dMontoLinea` | `DECIMAL(18,2)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L41 |
| `bdicred` | `bdicnweb` | no | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultacredito_central';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00046';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monto` | ENTIDAD | monto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `credito` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `central` | MODIF | central | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultamontosautorizados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamontosautorizados.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta monto y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamontosautorizados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEmpleado                  CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEmpleado` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `mMontoMaxDebCargo` | `MONEY(16,2)` | L14 |
| `mMontoMaxDebAbono` | `MONEY(16,2)` | L15 |
| `mMontoMaxDebReverso` | `MONEY(16,2)` | L16 |
| `mMontoMaxCredCargo` | `MONEY(16,2)` | L17 |
| `mMontoMaxCredAbono` | `MONEY(16,2)` | L18 |
| `mMontoMaxCredReverso` | `MONEY(16,2)` | L19 |
| `cUsuarioAutoriza` | `CHAR(8)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_montos_autorizados` | `bdinteg` | ⚠️ sí | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `autoriza` | ACCION | autoriza | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultamovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamovimientos.sql` |
| **LOC (1er CREATE)** | 175 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimientos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamovimientos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L39 |
| `iSqlErr` | `INTEGER` | L40 |
| `dFecha` | `DATE` | L41 |
| `cAniomes` | `CHAR (6)` | L42 |
| `iNumSerial` | `INTEGER` | L43 |
| `cFolioSuc` | `CHAR (16)` | L44 |
| `cSucursal` | `CHAR (4)` | L45 |
| `cUsuario` | `CHAR (8)` | L46 |
| `dfechAlt` | `DATE` | L47 |
| `dFechVal` | `DATE` | L48 |
| `fech_hor` | `DATETIME HOUR to FRACTION(3)` | L49 |
| `cTransacc` | `CHAR (4)` | L50 |
| `cSucCuen` | `CHAR (4)` | L51 |
| `cProducto` | `CHAR (4)` | L52 |
| `cEmpresa` | `CHAR (3)` | L53 |
| `cCuenta` | `CHAR (20)` | L54 |
| `cCausaDev` | `CHAR (2)` | L55 |
| `iNumCheq` | `INTEGER` | L56 |
| `mMontoTot` | `MONEY (18,2)` | L57 |
| `mFirme` | `MONEY (18,2)` | L58 |
| `mEnSbc` | `MONEY (18,2)` | L59 |
| `mRemesas` | `MONEY (18,2)` | L60 |
| `sDiasRet` | `SMALLINT` | L61 |
| `cCancelad` | `CHAR (1)` | L62 |
| `cEdoCta` | `CHAR (1)` | L63 |
| *…15 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_movs2402` | `bdicheq` | ⚠️ sí | SELECT | L150 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L143 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L131 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL  OR pRegistros IS N` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L153 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultamovimientos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamovimientos_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimientos (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamovimientos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_movs2402` | `bdicheq` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pFechaInicio IS NULL OR pFechaFin IS NULL   THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultamovinversion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamovinversion.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento y inversión" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamovinversion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 Date
  pFechafin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `Date` | — | — |
| `pFechafin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cFecha` | `DATE` | L16 |
| `cSucursal` | `CHAR(4)` | L17 |
| `dFechaMov` | `DATE` | L18 |
| `cTransaccion` | `CHAR(4)` | L19 |
| `cNumCuenta` | `CHAR(20)` | L20 |
| `cNumCte` | `CHAR(20)` | L21 |
| `dMontoTotal` | `DECIMAL(18,2)` | L22 |
| `cCtaCargo` | `CHAR(14)` | L23 |
| `cCtaAbono` | `CHAR(14)` | L24 |
| `sSecuencia` | `SMALLINT` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |
| `iRegistros` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_movsinver` | `bdinvers` | ⚠️ sí | SELECT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet 	  = '00000';` |  |
| L56 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechaInicio IS NULL OR pFechafin IS NULL OR pRegistros IS N` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L80 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `inversion` | ENTIDAD | inversión (pagaré / plazo) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultamovinversion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamovinversion_totales.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento y inversión (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamovinversion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 Date
  pFechafin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `Date` | — | — |
| `pFechafin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_movsinver` | `bdinvers` | ⚠️ sí | SELECT | L35 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet 	  = '00000';` |  |
| L22 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechaInicio IS NULL OR pFechafin IS NULL THEN` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `inversion` | ENTIDAD | inversión (pagaré / plazo) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultamovpendientesccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamovpendientesccl.sql` |
| **LOC (1er CREATE)** | 173 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamovpendientesccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L31 |
| `cCodRetSp` | `CHAR(6)` | L32 |
| `cDescCodRet` | `CHAR(100)` | L33 |
| `iCodRetSp` | `INTEGER` | L34 |
| `iSqlErr` | `INTEGER` | L35 |
| `iConsecutivo` | `INTEGER` | L36 |
| `cNomArchivo` | `CHAR(23)` | L37 |
| `cArchivoOrigen` | `CHAR(3)` | L38 |
| `cIntegridad` | `CHAR(1)` | L39 |
| `cIntegridadError` | `CHAR(20)` | L40 |
| `cNumTarjeta` | `CHAR(16)` | L41 |
| `cSecuencia` | `CHAR(6)` | L42 |
| `cIdComercio325` | `CHAR(9)` | L43 |
| `cNomComercio325` | `CHAR(30)` | L44 |
| `cReferencia23_325` | `CHAR(23)` | L45 |
| `cMonto` | `CHAR(13)` | L46 |
| `cRFC325` | `CHAR(15)` | L47 |
| `cDivisa325` | `CHAR(3)` | L48 |
| `cMontoCashBack` | `CHAR(13)` | L49 |
| `cConciliacion` | `CHAR(1)` | L50 |
| `cTipoConciliacion` | `INTEGER` | L51 |
| `cSecuenciaExtendida` | `CHAR(15)` | L52 |
| `mMontoIntercard` | `MONEY(16,2)` | L53 |
| `cMovConciliado` | `CHAR(1)` | L54 |
| `cMovReversado` | `CHAR(1)` | L55 |
| *…7 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L126 |
| `bditarjeta` | `bdicnweb` | no | L136 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L110 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L142 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_concreing_consul` |  |
| L144 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?pendientes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultamovtosdiariosreversos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamovtosdiariosreversos.sql` |
| **LOC (1er CREATE)** | 487 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta movimiento, sd — saldo disponible y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamovtosdiariosreversos(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMCUENTA                   CHAR(20)
  dPERIODOI                    DATE
  dPERIODOF                    DATE
  cSISTEMACUENTA               CHAR(20)
  cUsuario                     CHAR(8)
  cSuc                         CHAR(4)
  mImporte                     MONEY(14,2)
  pFolio                       CHAR(20)
  pNumRegistro                 INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNUMCUENTA` | `CHAR(20)` | — | — |
| `dPERIODOI` | `DATE` | — | — |
| `dPERIODOF` | `DATE` | — | — |
| `cSISTEMACUENTA` | `CHAR(20)` | — | — |
| `cUsuario` | `CHAR(8)` | — | — |
| `cSuc` | `CHAR(4)` | — | — |
| `mImporte` | `MONEY(14,2)` | — | — |
| `pFolio` | `CHAR(20)` | — | — |
| `pNumRegistro` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iexiste` | `INT` | L34 |
| `cCodRet` | `CHAR(5)` | L35 |
| `iSql_err` | `INT` | L36 |
| `dFecha` | `DATE` | L38 |
| `dHora` | `DATETIME HOUR to FRACTION(3)` | L39 |
| `cTransaccion` | `CHAR(4)` | L40 |
| `cD_Transaccion` | `CHAR(50)` | L41 |
| `mMonto` | `MONEY(14,2)` | L42 |
| `cNaturaleza` | `CHAR(1)` | L43 |
| `mSaldo` | `MONEY(14,2)` | L44 |
| `cReferencia` | `CHAR(40)` | L45 |
| `cReversos` | `CHAR(1)` | L46 |
| `cReversados` | `CHAR(1)` | L47 |
| `cSucursal` | `CHAR(4)` | L48 |
| `cTpsuc` | `CHAR(1)` | L49 |
| `cFolio` | `CHAR(16)` | L50 |
| `cProcedencia` | `CHAR(20)` | L51 |
| `cD_Procedencia` | `CHAR(50)` | L52 |
| `dPeriodoI_1` | `DATE` | L53 |
| `dPeriodoF_1` | `DATE` | L54 |
| `sNUMSERIAL` | `INT8` | L55 |
| `cNumtarjeta` | `CHAR(20)` | L58 |
| `iCont` | `INT` | L60 |
| `pEmpresa` | `CHAR(3)` | L62 |
| `cCodfun` | `CHAR(3)` | L63 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L169 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L182 |
| `si_procedencia` | `bdinteg` | ⚠️ sí | SELECT | L225 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L263 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L267 |
| `sd_movdia` | `bdicred` | ⚠️ sí | SELECT | L281 |
| `sd_movdiacrd` | `bdicred` | ⚠️ sí | SELECT | L292 |
| `sv_maeinv` | `bdinvers` | ⚠️ sí | SELECT | L411 |
| `sv_movdia` | `bdinvers` | ⚠️ sí | SELECT | L425 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L120 | CÓDIGO_RETORNO | `LET cCodRet='00098';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet='00098';` |  |
| L244 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L376 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L379 | FÓRMULA | `LET iCont=iCont - 1;` |  |
| L383 | FÓRMULA | `LET iCont=iCont - 1;` |  |
| L395 | CÓDIGO_RETORNO | `LET cCodRet = '00039';` |  |
| L469 | FÓRMULA | `LET iCont=iCont+1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?iari` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iari`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultamovtosnvasfuncre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultamovtosnvasfuncre.sql` |
| **LOC (1er CREATE)** | 193 |
| **Callgraph** | ✅ fan_in=37 / fan_out=6 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_cargamovtosnvasfunc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultamovtosnvasfuncre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cre`=crédito | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L42 |
| `iSqlErr` | `INTEGER` | L43 |
| `cCodRetSp` | `CHAR(6)` | L44 |
| `iCodRetSp` | `INTEGER` | L45 |
| `cMensajeRetorno` | `CHAR(120)` | L46 |
| `cEmpresa` | `CHAR(3)` | L47 |
| `cNumCliente` | `CHAR(20)` | L49 |
| `cSucursal` | `CHAR(4)` | L50 |
| `cStatusCred` | `CHAR(2)` | L51 |
| `iPlazo` | `INTEGER` | L52 |
| `dFechaApertura` | `DATE` | L53 |
| `dFechaVencimiento` | `DATE` | L54 |
| `dTasaInteres` | `DECIMAL(9,6)` | L55 |
| `dTasaMoratorios` | `DECIMAL(9,6)` | L56 |
| `dSdoRetenido` | `DECIMAL(18,2)` | L57 |
| `dSdoNoExig` | `DECIMAL(18,2)` | L58 |
| `dSdoContabMora` | `DECIMAL(18,2)` | L59 |
| `dSdoCapital` | `DECIMAL(18,2)` | L60 |
| `dSdoCapInsoluto` | `DECIMAL(18,2)` | L61 |
| `dSdoMtoVdo` | `DECIMAL(18,2)` | L62 |
| `dMtoVdoTrasp` | `DECIMAL(18,2)` | L63 |
| `dMtoFinanciado` | `DECIMAL(18,2)` | L64 |
| `dMtoOtorgado` | `DECIMAL(18,2)` | L65 |
| `dCapTrasnoVdo` | `DECIMAL(18,2)` | L66 |
| `dMtoVdoInt` | `DECIMAL(18,2)` | L67 |
| *…19 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L156 |
| `sp_cargamovtosnvasfunc` | `bdicred` | ⚠️ sí | L165 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L173 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, cMensajeRetorno;` |  |
| L175 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?snvasfun` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?snvasfun` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultanombrearchivodmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultanombrearchivodmi.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta nombre y archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultanombrearchivodmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cNomArchivo` | `CHAR(20)` | L8 |
| `cFechaHoy` | `CHAR(8)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `bdidomi` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `nombre` | ENTIDAD | nombre | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `?dmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultanombrearchivotef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultanombrearchivotef.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta nombre, archivo y TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtenernomarch_tef` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultanombrearchivotef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `INTEGER` | `consulta`=consulta / lee | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cNomArchivo` | `CHAR(20)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `sp_obtenernomarch_tef` | `bditef` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta IS NULL THEN` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditef:sp_obtenernomarch_tef';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `nombre` | ENTIDAD | nombre | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultanombrecedulasccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultanombrecedulasccl.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta nombre, cédulas y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultanombrecedulasccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `sTipoCedula` | `SMALLINT` | L11 |
| `cNombre` | `CHAR (40)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `bdicnweb` | `bdicnweb` | no | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cedulacontablenombre ";` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00582';` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cedulas` | ENTIDAD | cédulas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultapanel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultapanel.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultapanel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `cCve_producto` | `CHAR(4)` | L12 |
| `cId_panel` | `CHAR(35)` | L13 |
| `cId_funcion` | `CHAR(2)` | L14 |
| `cEstatus` | `CHAR(1)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_idproducto` | `bdicnweb` | no | SELECT | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?panel` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?panel` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaparamccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaparamccl.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaparamccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cDescCodRet` | `CHAR(100)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodigo` | `CHAR(3)` | L13 |
| `cDescripcion` | `CHAR(50)` | L14 |
| `cValor` | `CHAR(90)` | L15 |
| `dFechaMod` | `DATE` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L47 |
| `bditarjeta` | `bdicnweb` | no | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00190';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultaparamdigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaparamdigitalizacion.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=22 / fan_out=21 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro y digitalización de documentos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dgconsultaparametrosdigitalizacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaparamdigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodParam                    CHAR(6)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodParam` | `CHAR(6)` | `param`=parámetro | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cValor` | `CHAR(100)` | L11 |
| `cDescripcion` | `CHAR(50)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_dgconsultaparametrosdigitalizacion` | `bdidigital` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultaparamdigitalizacion2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaparamdigitalizacion2.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro y digitalización de documentos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_dgconsultaparametrosdigitalizacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaparamdigitalizacion2(
  pCodParam                    CHAR(6)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCodParam` | `CHAR(6)` | `param`=parámetro | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cValor` | `CHAR(100)` | L11 |
| `cDescripcion` | `CHAR(50)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_dgconsultaparametrosdigitalizacion` | `bdidigital` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaparamdigitalizacionlike`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaparamdigitalizacionlike.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dgconsultaparametrosdigitalizacionlike` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaparamdigitalizacionlike(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodParamIni                 SMALLINT
  pCodParamFin                 SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodParamIni` | `SMALLINT` | `param`=parámetro | 🔵 CONVENCIÓN |
| `pCodParamFin` | `SMALLINT` | `param`=parámetro | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCodParam` | `SMALLINT` | L12 |
| `cValor` | `CHAR(100)` | L13 |
| `cDescripcion` | `CHAR(50)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_dgconsultaparametrosdigitalizacionlike` | `bdidigital` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00367';				RETURN cCodRet, cCodParam, cValor, cDescripcion;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?like` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?like` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaparametros`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaparametros.sql` |
| **LOC (1er CREATE)** | 119 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetros" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaparametros(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pValorParametro              CHAR(60)
  pTipoBusqueda                SMALLINT
  pSistemaCuenta               CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pValorParametro` | `CHAR(60)` | — | — |
| `pTipoBusqueda` | `SMALLINT` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cValor` | `CHAR(100)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L49 |
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L65 |
| `sv_param` | `bdinvers` | ⚠️ sí | SELECT | L81 |
| `sd_param` | `bdicred` | ⚠️ sí | SELECT | L97 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR TRIM(pValorParametro) = '' OR pTipoBusqueda IS NULL OR pSiste` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00048';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametros` | ENTIDAD | parámetros | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultaparametrosgenerales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaparametrosgenerales.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetros (general)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaparametrosgenerales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pWhere                       CHAR(15)
  pIdParametro                 CHAR(15)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pWhere` | `CHAR(15)` | — | — |
| `pIdParametro` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cValor` | `CHAR(100)` | L7 |
| `cQuery` | `CHAR(500)` | L8 |
| `sIdFuncion` | `CHAR(10)` | L9 |
| `sNombreBase` | `CHAR(12)` | L10 |
| `sNmbreTabla` | `CHAR(40)` | L11 |
| `sCampo` | `CHAR(20)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_parametros_generales` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultapendientesenviodigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultapendientesenviodigitalizacion.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 83 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta digitalización de documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dgconsultapendientesenvio` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultapendientesenviodigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCtlProcesado                CHAR(1)
  pCtlLigado                   CHAR(1)
  pMacaddressLocal             CHAR(17)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCtlProcesado` | `CHAR(1)` | — | — |
| `pCtlLigado` | `CHAR(1)` | — | — |
| `pMacaddressLocal` | `CHAR(17)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(6)` | L22 |
| `iCodRetSp` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |
| `iIdControl` | `INTEGER` | L28 |
| `cCliente` | `CHAR(20)` | L29 |
| `cCuenta` | `CHAR(20)` | L30 |
| `cProducto` | `CHAR(4)` | L31 |
| `cCodDocto` | `CHAR(4)` | L32 |
| `iSecuencia` | `INTEGER` | L33 |
| `cProdNombre` | `CHAR(40)` | L34 |
| `vCtlArchivoLocal` | `CHAR(25)` | L35 |
| `vCtlArchivoZip` | `CHAR(50)` | L36 |
| `cCtlImgEnviada` | `CHAR(1)` | L37 |
| `cCtlProcesado` | `CHAR(1)` | L38 |
| `cCtlLigado` | `CHAR(1)` | L39 |
| `vCtlArchivoLocalRuta` | `CHAR(150)` | L40 |
| `cUsuarioAlta` | `CHAR(8)` | L41 |
| `vDescripcion` | `CHAR(35)` | L42 |
| `cDescrip2` | `CHAR(30)` | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L98 |
| `sp_dgconsultapendientesenvio` | `bdicnweb` | no | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L82 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pCtlProcesado = '' OR pCtlLigado = '' OR pMacaddressLocal = ` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L111 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L129 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_dgconsultapendientesenvio';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00382';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00383';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00384';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00385';` |  |
| L151 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pendientes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaperfilusuarioaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaperfilusuarioaumlincred.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 369 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta perfil de usuario y usuario" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaperfilusuarioaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) 		AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | 🔵 CONVENCIÓN |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `cCodRetSp` | `CHAR(6)` | L26 |
| `iCodRetSp` | `INTEGER` | L27 |
| `iNoRegistros` | `INTEGER` | L28 |
| `iRegistros` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |
| `iNoRegs` | `INTEGER` | L31 |
| `cMensajeRetorno` | `CHAR(80)` | L32 |
| `cNumempleado` | `CHAR(8)` | L33 |
| `cNombre` | `CHAR(45)` | L34 |
| `cPerfilPuesto` | `CHAR (25)` | L35 |
| `iAtendidas` | `INTEGER` | L36 |
| `iPorcAtendidas` | `DECIMAL(18,2)` | L37 |
| `iCanceladas` | `INTEGER` | L38 |
| `iPorcCanceladas` | `DECIMAL(18,2)` | L39 |
| `iRechazadas` | `INTEGER` | L40 |
| `iPorcRechazadas` | `DECIMAL(18,2)` | L41 |
| `iAutorizadas` | `INTEGER` | L42 |
| `iPorcAutorizadas` | `DECIMAL(18,2)` | L43 |
| `iTotalAtendidas` | `INTEGER` | L44 |
| `iTotalPorcAtendidas` | `DECIMAL(18,2)` | L45 |
| `iTotalCanceladas` | `INTEGER` | L46 |
| `iTotalPorcCanceladas` | `DECIMAL(18,2)` | L47 |
| `iTotalRechazadas` | `INTEGER` | L48 |
| *…3 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L113 |
| `bdicred` | `bdicnweb` | no | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET cCodRet 			  = '00000';` |  |
| L95 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial = '' OR pFechaFinal = '' OR pRegistros IS NULL ` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L129 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_perfil_usuario2';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L143 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | 🔵 CONVENCIÓN | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultaperfilusuarioaumlincred_rep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaperfilusuarioaumlincred_rep.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=14 / fan_out=5 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta perfil de usuario y usuario" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaperfilusuarioaumlincred_rep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `iNoRegs` | `INTEGER` | L14 |
| `cMensajeRetorno` | `CHAR(80)` | L15 |
| `cNumempleado` | `CHAR(8)` | L16 |
| `cNombre` | `CHAR(45)` | L17 |
| `cPerfilPuesto` | `CHAR (25)` | L18 |
| `iAtendidas` | `INTEGER` | L19 |
| `iPorcAtendidas` | `DECIMAL(18,2)` | L20 |
| `iCanceladas` | `INTEGER` | L21 |
| `iPorcCanceladas` | `DECIMAL(18,2)` | L22 |
| `iRechazadas` | `INTEGER` | L23 |
| `iPorcRechazadas` | `DECIMAL(18,2)` | L24 |
| `iAutorizadas` | `INTEGER` | L25 |
| `iPorcAutorizadas` | `DECIMAL(18,2)` | L26 |
| `iTotalAtendidas` | `INTEGER` | L27 |
| `iTotalPorcAtendidas` | `DECIMAL(18,2)` | L28 |
| `iTotalCanceladas` | `INTEGER` | L29 |
| `iTotalPorcCanceladas` | `DECIMAL(18,2)` | L30 |
| `iTotalRechazadas` | `INTEGER` | L31 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `informix` | `bdicnweb` | no | INSERT | L111 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L83 |
| `bdicred` | `bdicnweb` | no | L94 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L102 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_perfil_usuario3';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L110 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultaperiodosemanaltelmex`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaperiodosemanaltelmex.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta periodo y teléfono" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaperiodosemanaltelmex(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codigoretorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `ikeyx` | `INTEGER` | L9 |
| `dfechaIniperiodo` | `DATE` | L10 |
| `dFechaFinPeriodo` | `DATE` | L11 |
| `iNumRows` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_liquidacionestelmex` | `bdisac` | ⚠️ sí | SELECT | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `periodo` | ENTIDAD | periodo | 🔵 CONVENCIÓN | nombre_sp |
| `?semanal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tel` | ENTIDAD | teléfono | 🔵 CONVENCIÓN | nombre_sp |
| `?me` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?semanal`, `?me` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaperiodosreportesespecialesac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaperiodosreportesespecialesac.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta periodo, reportes y Servicios de Atención al Cliente — subsistema de atención en sucursal (especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaperiodosreportesespecialesac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(5)
  pTipoPeriodo                 CHAR(1)
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pTipoPeriodo` | `CHAR(1)` | `periodo`=periodo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `isqlerr` | `INTEGER` | L8 |
| `cFecIniperiodo` | `CHAR(10)` | L9 |
| `cFecFinperiodo` | `CHAR(10)` | L10 |
| `iNumRows` | `INTEGER` | L11 |
| `iConsecutivoConvenio` | `INTEGER` | L12 |
| `cQuery` | `CHAR(1500)` | L13 |
| `cTabla` | `CHAR(200)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_liquidacionsemanal` | `bdisac` | ⚠️ sí | SELECT | L45 |
| `sac_liquidacionmensual` | `bdisac` | ⚠️ sí | SELECT | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `periodo` | ENTIDAD | periodo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?eciale` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?eciale` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaprocescod41_ccep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaprocescod41_ccep.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=78 / fan_out=37 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta proceso, código y CCE — Cámara de Compensación Electrónica" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaprocescod41_ccep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `iNoRegistros` | `INTEGER` | L18 |
| `idRowDetalle` | `INTEGER` | L19 |
| `cBancoLibrado` | `CHAR(3)` | L20 |
| `cDescbancoLibrado` | `CHAR(50)` | L21 |
| `mImporte` | `DECIMAL(14,2)` | L22 |
| `cCuentaReferencia` | `CHAR(13)` | L23 |
| `cNumCheque` | `CHAR(10)` | L24 |
| `cCuentaDeposito` | `CHAR(20)` | L25 |
| `cObservaciones` | `CHAR(70)` | L26 |
| `cMotivoDevolucion` | `CHAR(100)` | L27 |
| `cprocesar` | `CHAR(2)` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ccep_procesacod41detalle_tmp` | `bdicnweb` | no | SELECT | L75 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDireccionMac = '' OR pRegistros IS NULL OR pRecuperacion IS ` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proc` | ENTIDAD | proceso | 🟡 INFERIDO | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?41_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es`, `?41_`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaproductosactdesac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaproductosactdesac.sql` |
| **LOC (1er CREATE)** | 245 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 215 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta productos y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaproductosactdesac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pPlaza                       CHAR(3)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5)  AS cRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pPlaza` | `CHAR(3)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cProducto` | `CHAR(4)` | L10 |
| `cTieneProducto` | `CHAR(4)` | L11 |
| `cDescripcionProducto` | `CHAR(60)` | L12 |
| `cSistema` | `CHAR(2)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iTotSuc` | `INTEGER` | L15 |
| `iTotProdSuc` | `INTEGER` | L16 |
| `cSucursal` | `CHAR(4)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_producto` | `bdicheq` | ⚠️ sí | SELECT | L60 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L72 |
| `sv_instrum` | `bdinvers` | ⚠️ sí | SELECT | L84 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L98 |
| `si_prod_sucursal` | `bdinteg` | ⚠️ sí | SELECT | L104 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L114 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L168 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L216 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L222 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L229 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?de` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaproductosaldonegativo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaproductosaldonegativo.sql` |
| **LOC (1er CREATE)** | 136 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta productos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaproductosaldonegativo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave_Producto              CHAR (4)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave_Producto` | `CHAR (4)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `dFecha` | `DATE` | L16 |
| `cProducto` | `CHAR (4)` | L17 |
| `iNoCuentas` | `INTEGER` | L18 |
| `mCapitalAnterior` | `MONEY (18, 2)` | L19 |
| `mCapitalCalculado` | `MONEY (18, 2)` | L20 |
| `mCapitalActual` | `MONEY (18, 2)` | L21 |
| `mInteresAnterior` | `MONEY (18, 2)` | L22 |
| `mInteresCalculado` | `MONEY (18, 2)` | L23 |
| `mInteresActual` | `MONEY (18, 2)` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_conciliachqglobneg` | `bdicheq` | ⚠️ sí | SELECT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L67 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L55 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pClave_Producto = '' OR pRegistros IS NULL OR pRecuperacion I` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L84 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L95 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L102 | VALIDACIÓN_NULL | `IF pFecha_Inicio IS NULL OR pFecha_Fin IS NULL THEN` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?aldonegativo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aldonegativo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaproductosaldonegativo_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaproductosaldonegativo_totales.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta productos (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaproductosaldonegativo_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave_Producto              CHAR (4)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave_Producto` | `CHAR (4)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_conciliachqglobneg` | `bdicheq` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | VALIDACIÓN_NULL | `IF pFecha_Inicio IS NULL OR pFecha_Fin IS NULL THEN` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?aldonegativo_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aldonegativo_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaproductosaldopositivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaproductosaldopositivo.sql` |
| **LOC (1er CREATE)** | 137 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta productos y punto de venta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaproductosaldopositivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave_Producto              CHAR (4)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave_Producto` | `CHAR (4)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `dFecha` | `DATE` | L17 |
| `cProducto` | `CHAR (4)` | L18 |
| `iNoCuentas` | `INTEGER` | L19 |
| `mCapitalAnterior` | `MONEY (18, 2)` | L20 |
| `mCapitalCalculado` | `MONEY (18, 2)` | L21 |
| `mCapitalActual` | `MONEY (18, 2)` | L22 |
| `mInteresAnterior` | `MONEY (18, 2)` | L23 |
| `mInteresCalculado` | `MONEY (18, 2)` | L24 |
| `mInteresActual` | `MONEY (18, 2)` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |
| `iRegistros` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_conciliachqglob` | `bdicheq` | ⚠️ sí | SELECT | L80 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pClave_Producto = '' OR pRegistros IS NULL OR pRecuperacion I` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L85 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L96 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L103 | VALIDACIÓN_NULL | `IF pFecha_Inicio IS NULL OR pFecha_Fin IS NULL THEN` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L113 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?aldo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pos` | ENTIDAD | punto de venta (POS) | 🔵 CONVENCIÓN | nombre_sp |
| `?itivo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aldo`, `?itivo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaproductosldpstvo_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaproductosldpstvo_totales.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta productos (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaproductosldpstvo_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave_Producto              CHAR (4)
  pFecha_Inicio                DATE
  pFecha_Fin                   DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave_Producto` | `CHAR (4)` | — | — |
| `pFecha_Inicio` | `DATE` | — | — |
| `pFecha_Fin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_conciliachqglob` | `bdicheq` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | VALIDACIÓN_NULL | `IF pFecha_Inicio IS NULL OR pFecha_Fin IS NULL THEN` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ldpstvo_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ldpstvo_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarchivochequesdevueltoscap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarchivochequesdevueltoscap.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar CHI — formato/protocolo de consulta al Buró de Crédito, cheques, OS — Originación de Solicitudes y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarchivochequesdevueltoscap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `dFechaUltActualizacion` | `DATE` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?vo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?uelt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?vo`, `?uelt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarchivosafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarchivosafore.sql` |
| **LOC (1er CREATE)** | 361 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar CHI — formato/protocolo de consulta al Buró de Crédito, OS — Originación de Solicitudes y AFORE" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarchivosafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(30)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L41 |
| `cCodRetSp` | `CHAR(6)` | L42 |
| `iSqlErr` | `INTEGER` | L43 |
| `cMensajeRet` | `CHAR(200)` | L44 |
| `cTipoRegistro` | `CHAR(1)` | L45 |
| `cNoContratoEmpresa` | `CHAR(10)` | L46 |
| `dFechaGen` | `DATE` | L47 |
| `dFechaInicialInformacion` | `DATE` | L48 |
| `dFechaFinalInformacion` | `DATE` | L49 |
| `cNoMovimientosContenidos` | `CHAR(9)` | L50 |
| `cFiller` | `CHAR(232)` | L51 |
| `cFinLinea` | `CHAR(2)` | L52 |
| `cNSS` | `CHAR(11)` | L53 |
| `cNombreBeneficiario` | `CHAR(40)` | L54 |
| `cApellidoPaternoBeneficiario` | `CHAR(40)` | L55 |
| `cApellidoMaternoBeneficiario` | `CHAR(40)` | L56 |
| `cFormasPago` | `CHAR(1)` | L57 |
| `cCLABE` | `CHAR(18)` | L58 |
| `dFechaCaptura` | `DATE` | L59 |
| `cImporteDocumentoNetoPagar` | `CHAR(15)` | L60 |
| `cImporteDocumentoAntesImpuesto` | `CHAR(15)` | L61 |
| `cImpuestoRetenido` | `CHAR(11)` | L62 |
| `cNumeroFolioServicio` | `CHAR(8)` | L63 |
| `cNumeroTienda` | `CHAR(4)` | L64 |
| `cTipoRetiro` | `CHAR(3)` | L65 |
| *…31 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `15` | `bdicnweb` | no | SELECT | L217 |
| `sw_af_registros_tmp` | `bdicnweb` | no | SELECT | L232 |
| `sw_af_registros_tmp` | `bdicnweb` | no | DELETE | L232 |
| `sw_af_registros_tmp` | `bdicnweb` | no | INSERT | L260 |
| `pp_encabezado` | `bdiprog` | ⚠️ sí | SELECT | L308 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L313 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L205 |
| `sp_aforeconsultaarchivos` | `bdiprog` | ⚠️ sí | L238 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L100 | CÓDIGO_RETORNO | `LET cCodRet 							   = '00000';` |  |
| L181 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pRegistros IS NULL OR pRecuperacion IS` |  |
| L182 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L194 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L248 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃÂN DEL SP bdiprog:sp_aforeconsultaarch` |  |
| L250 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L268 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L325 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L329 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?v` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarchivosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarchivosccl.sql` |
| **LOC (1er CREATE)** | 170 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar CHI — formato/protocolo de consulta al Buró de Crédito, OS — Originación de Solicitudes y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarchivosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pParam                       CHAR(1)
  pTipoArchOrigen              CHAR(3)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pParam` | `CHAR(1)` | — | — |
| `pTipoArchOrigen` | `CHAR(3)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `cCodRetSp` | `CHAR(6)` | L28 |
| `cDescCodRet` | `CHAR(80)` | L29 |
| `iCodRetSp` | `INTEGER` | L30 |
| `iSqlErr` | `INTEGER` | L31 |
| `cNombreArchivo` | `CHAR(23)` | L32 |
| `cArchivoOrigen` | `CHAR(3)` | L33 |
| `dFechaArchivo` | `DATE` | L34 |
| `iNumRegistros325` | `INTEGER` | L35 |
| `mMonto325` | `MONEY(16,2)` | L36 |
| `dFechaProceso` | `DATE` | L37 |
| `dFechaHoraTransferencia` | `DATETIME YEAR TO FRACTION(5)` | L38 |
| `dFechaHoraIniProceso` | `DATETIME YEAR TO FRACTION(5)` | L39 |
| `dFechaHoraCargaArchivo` | `DATETIME YEAR TO FRACTION(5)` | L40 |
| `dFechaHoraCargaTabla` | `DATETIME YEAR TO FRACTION(5)` | L41 |
| `dFechaHoraIniConciliaReg` | `DATETIME YEAR TO FRACTION(5)` | L42 |
| `dFechaHoraFinConciliaReg` | `DATETIME YEAR TO FRACTION(5)` | L43 |
| `dFechaHoraFinProceso` | `DATETIME YEAR TO FRACTION(5)` | L44 |
| `dFechaHoraGenConadmin` | `DATETIME YEAR TO FRACTION(5)` | L45 |
| `cTransferencia` | `CHAR(1)` | L46 |
| `cCarga` | `CHAR(1)` | L47 |
| `cConadmin` | `CHAR(1)` | L48 |
| `iNumCargo` | `INTEGER` | L49 |
| `mMontoCargo` | `MONEY(16,2)` | L50 |
| `iNumAbono` | `INTEGER` | L51 |
| *…3 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L114 |
| `bditarjeta` | `bdicnweb` | no | L133 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L98 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pParam = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR p` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_conarchivos_con2` |  |
| L141 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?v` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarchivosccl_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarchivosccl_totales.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar CHI — formato/protocolo de consulta al Buró de Crédito, OS — Originación de Solicitudes y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarchivosccl_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pParam                       CHAR(1)
  pTipoArchOrigen              CHAR(3)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pParam` | `CHAR(1)` | — | — |
| `pTipoArchOrigen` | `CHAR(3)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRet` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L36 |
| `bditarjeta` | `bdicnweb` | no | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pParam = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_conarchivos_con2` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?v` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarcorreccionesdatoscliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarcorreccionesdatoscliente.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consultar sd — saldo disponible, OS — Originación de Solicitudes y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consultarcorreccionesdatoscte` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarcorreccionesdatoscliente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipoReporte                 CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | `cliente`=cliente | 🔵 CONVENCIÓN |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | `rec`=recepción / recibe | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cFecha` | `CHAR(20)` | L13 |
| `cRfcAnterior` | `CHAR(13)` | L14 |
| `cNombreAnterior` | `CHAR(100)` | L15 |
| `cRfcActual` | `CHAR(13)` | L16 |
| `cNombreActual` | `CHAR(100)` | L17 |
| `cUsuarioInsert` | `CHAR(8)` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iNoRegs` | `INTEGER` | L20 |
| `iSkip` | `INTEGER` | L21 |
| `iFirst` | `INTEGER` | L22 |
| `pFechaInicialSp` | `CHAR(10)` | L23 |
| `pFechaFinalSp` | `CHAR(10)` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarcorreccionesdatoscte` | `bdinteg` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | FÓRMULA | `LET pFechaInicialSp = TO_CHAR(pFechaInicial, '%m-%d-%Y');` |  |
| L78 | FÓRMULA | `LET pFechaFinalSp = TO_CHAR(pFechaFinal, '%m-%d-%Y');` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L101 | FÓRMULA | `LET iFirst = iFirst + 1;` |  |
| L104 | FÓRMULA | `LET iSkip = iSkip + 1;` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?cor` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?cione` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?at` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cliente` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cor`, `?cione`, `?at` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarctesporasignarcli`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarctesporasignarcli.sql` |
| **LOC (1er CREATE)** | 139 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consultar clientes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consultarctesporasignar` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarctesporasignarcli(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                CHAR(1)
  pNumCliente                  CHAR(20)
  pTipoCuenta                  CHAR(1)
  pTipoCampo                   CHAR(1)
  pTipoDireccion               CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pTipoCuenta` | `CHAR(1)` | — | — |
| `pTipoCampo` | `CHAR(1)` | — | — |
| `pTipoDireccion` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `iRegistros` | `INTEGER` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |
| `cNumCliente` | `CHAR(20)` | L25 |
| `cNombreCliente` | `CHAR(107)` | L26 |
| `dFechaNacimiento` | `DATE` | L27 |
| `cEstado` | `CHAR(30)` | L28 |
| `cCiudad` | `CHAR(60)` | L29 |
| `cCalle` | `CHAR(30)` | L30 |
| `cCodigoPostal` | `CHAR(5)` | L31 |
| `cTipoDomicilio` | `CHAR(10)` | L32 |
| `cSucursal` | `CHAR(4)` | L33 |
| `cMunicipio` | `CHAR(27)` | L34 |
| `iNumColonia` | `INTEGER` | L35 |
| `cZona` | `CHAR(32)` | L36 |
| `cObservaciones` | `CHAR(80)` | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarctesporasignar` | `bdinteg` | ⚠️ sí | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoConsulta = '' OR pRegistros IS NULL OR pRecuperacion IS ` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L115 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L123 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?rcli` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rcli` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareporteautorizasolicitudesmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareporteautorizasolicitudesmc.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar solicitudes y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_generareporteautorizasolicitudes` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareporteautorizasolicitudesmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(8)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `cCodRetSp` | `CHAR(3)` | L18 |
| `iCodRet` | `INTEGER` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `cNoSolicitud` | `CHAR(20)` | L21 |
| `cNumCliente` | `CHAR(20)` | L22 |
| `cSucursal` | `CHAR(4)` | L23 |
| `cNombreSucursal` | `CHAR(40)` | L24 |
| `cEjecutivoAutoriza` | `CHAR(45)` | L25 |
| `dFechaInsercion` | `DATE` | L26 |
| `cJustificacion` | `CHAR(500)` | L27 |
| `cNombrecliente` | `CHAR(52)` | L28 |
| `cApepaterno` | `CHAR(26)` | L29 |
| `cApematerno` | `CHAR(26)` | L30 |
| `cRfc` | `CHAR(13)` | L31 |
| `cEjecutivoElabora` | `CHAR(45)` | L32 |
| `cCambioEstatus` | `CHAR(5)` | L33 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_generareporteautorizasolicitudes` | `bdicred` | ⚠️ sí | L79 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_generareporteautorizasolicitudes';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR cNumCliente = '' THEN` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `autoriza` | ACCION | autoriza | 🔵 CONVENCIÓN | nombre_sp |
| `solicitudes` | ENTIDAD | solicitudes (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareportecomprascaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareportecomprascaja.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar caja (complemento)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareportecomprascaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `cCodRetSp` | `CHAR(6)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `cFolioOperacion` | `CHAR(8)` | L24 |
| `dFechaOperacion` | `DATE` | L25 |
| `cCodTrans` | `CHAR(4)` | L26 |
| `cOperacion` | `CHAR(8)` | L27 |
| `cInstitucion` | `CHAR(12)` | L28 |
| `cCajaGeneral` | `CHAR(30)` | L29 |
| `cProcedencia` | `CHAR(4)` | L30 |
| `cCC` | `CHAR(4)` | L31 |
| `cDenominacion` | `CHAR(8)` | L32 |
| `mTotal20` | `MONEY(14,2)` | L33 |
| `mTotal50` | `MONEY(14,2)` | L34 |
| `mTotal100` | `MONEY(14,2)` | L35 |
| `mTotal200` | `MONEY(14,2)` | L36 |
| `mTotal500` | `MONEY(14,2)` | L37 |
| `mTotal1000` | `MONEY(14,2)` | L38 |
| `mTotalBilletes` | `MONEY(14,2)` | L39 |
| `mTotalMoneda` | `MONEY(14,2)` | L40 |
| `mTotalGeneral` | `MONEY(14,2)` | L41 |
| `iRecuperacion` | `INTEGER` | L42 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L122 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L94 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pRegistros IS` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L137 | FÓRMULA | `LET mTotalBilletes = mTotal20 + mTotal50 + mTotal100 + mTotal200 + mTotal500 + mTotal1000;` |  |
| L138 | FÓRMULA | `LET mTotalGeneral = mTotalBilletes + mTotalMoneda;` |  |
| L142 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?ras` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te`, `?ras` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareporteconsolidadosic`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareporteconsolidadosic.sql` |
| **LOC (1er CREATE)** | 149 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar identificador y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=5 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareporteconsolidadosic(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(6)
  pFechaProceso                DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumProducto` | `CHAR(6)` | — | — |
| `pFechaProceso` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `dSdoActualSicenv` | `DECIMAL(18,2)` | L26 |
| `dSdoVencidoSicenv` | `DECIMAL(18,2)` | L27 |
| `dSdoInsolutoSicenv` | `DECIMAL(18,2)` | L28 |
| `dSdoActualSicexc` | `DECIMAL(18,2)` | L29 |
| `dSdoVencidoSicexc` | `DECIMAL(18,2)` | L30 |
| `dSdoInsolutoSicexc` | `DECIMAL(18,2)` | L31 |
| `dSdoActualApp` | `DECIMAL(18,2)` | L32 |
| `dSdoVencidoApp` | `DECIMAL(18,2)` | L33 |
| `dSdoInsolutoApp` | `DECIMAL(18,2)` | L34 |
| `dSdoActualDif` | `DECIMAL(18,2)` | L35 |
| `dSdoVencidoDif` | `DECIMAL(18,2)` | L36 |
| `dSdoInsolutoDif` | `DECIMAL(18,2)` | L37 |
| `dCredEnviados` | `DECIMAL(18,2)` | L38 |
| `dCredExcluidos` | `DECIMAL(18,2)` | L39 |
| `dCredCentral` | `DECIMAL(18,2)` | L40 |
| `dCredDiferencia` | `DECIMAL(18,2)` | L41 |
| `cTipoCred` | `CHAR(2)` | L42 |
| `cNumProducto` | `CHAR(4)` | L43 |
| `dFechaProceso` | `DATE` | L44 |
| `cNombreProd` | `CHAR(80)` | L45 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L90 |
| `br_concil_consolidado_cnr` | `bdiburo` | ⚠️ sí | SELECT | L112 |
| `br_concil_consolidado` | `bdiburo` | ⚠️ sí | SELECT | L129 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L99 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L83 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pNumProducto = '' OR pFechaProceso IS NULL THEN` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00057';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00298';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00298';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ol` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ic` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te`, `?ol`, `?ad`, `?ic` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareportedetallediferenciasic`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareportedetallediferenciasic.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar detalle" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareportedetallediferenciasic(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(6)
  pFechaProceso                DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(6)` | — | — |
| `pFechaProceso` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |
| `iRegistros` | `INTEGER` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |
| `cNumProducto` | `CHAR(4)` | L23 |
| `dFechaProceso` | `DATE` | L24 |
| `cNombreProd` | `CHAR(80)` | L25 |
| `cTipoCred` | `CHAR(2)` | L26 |
| `cNumCredito` | `CHAR(20)` | L27 |
| `dSaldoActual` | `DECIMAL(18,2)` | L28 |
| `dSaldoVenc` | `DECIMAL(18,2)` | L29 |
| `dSaldoInsoluto` | `DECIMAL(18,2)` | L30 |
| `dSaldoActualApp` | `DECIMAL(18,2)` | L31 |
| `dSaldoVencApp` | `DECIMAL(18,2)` | L32 |
| `dSaldoInsolutoApp` | `DECIMAL(18,2)` | L33 |
| `dDsaldoActual` | `DECIMAL(18,2)` | L34 |
| `dDSaldoVenc` | `DECIMAL(18,2)` | L35 |
| `dDsaldoInsoluto` | `DECIMAL(18,2)` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L76 |
| `br_concil_diferencias_cnr` | `bdiburo` | ⚠️ sí | SELECT | L104 |
| `br_concil_diferencias` | `bdiburo` | ⚠️ sí | SELECT | L121 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet 		 = '00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumProducto = '' OR pFechaProceso IS NULL OR pRegistros IS N` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00057';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00298';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00298';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?diferenciasic` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te`, `?diferenciasic` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareportedetallesolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareportedetallesolicitudmc.sql` |
| **LOC (1er CREATE)** | 168 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar detalle, solicitud y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareportedetallesolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)     AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `cCodRetSp` | `CHAR(6)` | L27 |
| `iCodRet` | `INTEGER` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cNumSolicitud` | `CHAR(20)` | L30 |
| `cSucursal` | `CHAR(4)` | L31 |
| `vNombreCte` | `CHAR(100)` | L32 |
| `dFechaSol` | `DATE` | L33 |
| `dFechaCambio` | `DATE` | L34 |
| `cRevaluada` | `CHAR(2)` | L35 |
| `cReferenciaCoppel` | `CHAR(20)` | L36 |
| `dcEficienciaCoppel` | `DECIMAL(18,2)` | L37 |
| `sMesesCoppel` | `SMALLINT` | L38 |
| `dcVencidoCoppel` | `DECIMAL(18,2)` | L39 |
| `iVencidoCoppeludis` | `INTEGER` | L40 |
| `cPuntualidad` | `CHAR(2)` | L41 |
| `iScoring1` | `INTEGER` | L42 |
| `iScoring2` | `INTEGER` | L43 |
| `cDescStatus` | `CHAR(40)` | L44 |
| `cCausaSolic` | `CHAR(3)` | L45 |
| `vComentario` | `CHAR(100)` | L46 |
| `cAnalista` | `CHAR(45)` | L47 |
| `cTipoMovto` | `CHAR(10)` | L48 |
| `cNombreProducto` | `CHAR(50)` | L49 |
| `iRegistros` | `INTEGER` | L50 |
| *…3 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_reportesolicitudmc` | `bdicnweb` | no | SELECT | L130 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L97 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L133 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareportelincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareportelincred.sql` |
| **LOC (1er CREATE)** | 130 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 73 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar teléfono y crédito" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultafechalincred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareportelincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pTipo                        CHAR(1)
  pStatus1                     CHAR(1)
  pStatus2                     CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pStatus1` | `CHAR(1)` | — | — |
| `pStatus2` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `iNoRegistros` | `INTEGER` | L18 |
| `iRegistros` | `INTEGER` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `cNumCredito` | `CHAR(20)` | L21 |
| `dMontoAnterior` | `DECIMAL(18,2)` | L22 |
| `dMontoNuevo` | `DECIMAL(18,2)` | L23 |
| `dMontoIncremento` | `DECIMAL(18,2)` | L24 |
| `cNumCliente` | `CHAR(20)` | L25 |
| `cNombreCliente` | `CHAR(104)` | L26 |
| `dFechaAlta` | `DATE` | L27 |
| `dHoraAlta` | `DATETIME HOUR TO SECOND` | L28 |
| `cTipoEjecucion` | `CHAR(1)` | L29 |
| `cEstatusCredito` | `CHAR(1)` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L75 |
| `sp_consultafechalincred` | `bdicred` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L61 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio = '' OR pFechaFin = '' OR pTipo = '' OR pRegistr` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L89 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultafechalincred';` |  |
| L97 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L98 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L105 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tel` | ENTIDAD | teléfono | 🔵 CONVENCIÓN | nombre_sp |
| `?in` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?in` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareporteoperacionesmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareporteoperacionesmc.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar operaciones y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareporteoperacionesmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `iCodRetSp` | `INTEGER` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cMensaje` | `CHAR(80)` | L23 |
| `dFecha` | `CHAR(10)` | L24 |
| `cFecha` | `CHAR(10)` | L25 |
| `cNombreProducto` | `CHAR(50)` | L26 |
| `iSolicitudesMc` | `INTEGER` | L27 |
| `iNoAnalizadasATiempo` | `INTEGER` | L28 |
| `iAnalizadasATiempo` | `INTEGER` | L29 |
| `iRevaluadas` | `INTEGER` | L30 |
| `iNoRevaluadas` | `INTEGER` | L31 |
| `iSigueProceso` | `INTEGER` | L32 |
| `dPorcentajeSp` | `DECIMAL(18,2)` | L33 |
| `iRechazadas` | `INTEGER` | L34 |
| `dPorcentajeRt` | `DECIMAL(18,2)` | L35 |
| `iCanceladas` | `INTEGER` | L36 |
| `dPorcentajeCn` | `DECIMAL(18,2)` | L37 |
| `iMixta` | `INTEGER` | L38 |
| `iUnica` | `INTEGER` | L39 |
| `iRegistros` | `INTEGER` | L40 |
| `iRecuperacion` | `INTEGER` | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pRegistros IS` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L112 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultaoperacionesmc';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |
| L129 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L134 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L140 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `operaciones` | ENTIDAD | operaciones (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareportepagoscre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareportepagoscre.sql` |
| **LOC (1er CREATE)** | 217 |
| **Callgraph** | ✅ fan_in=250 / fan_out=84 |
| **Principales callers** | `sp_consultareportepagoscre` |
| **Deps concatenadas** | 197 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar pagos y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareportepagoscre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pStatus                      CHAR(1)
  pFolio                       CHAR(16)
  pAplicacion                  SMALLINT
  pConsulta                    SMALLINT
  pProducto                    CHAR (4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pFolio` | `CHAR(16)` | — | — |
| `pAplicacion` | `SMALLINT` | — | — |
| `pConsulta` | `SMALLINT` | — | — |
| `pProducto` | `CHAR (4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L38 |
| `iSqlErr` | `INTEGER` | L39 |
| `cCodRetSp` | `CHAR(6)` | L40 |
| `iCodRetSp` | `INTEGER` | L41 |
| `iNoRegistros` | `INTEGER` | L42 |
| `iRegistros` | `INTEGER` | L43 |
| `iRecuperacion` | `INTEGER` | L44 |
| `iNoRegs` | `INTEGER` | L45 |
| `cReg` | `CHAR(3)` | L47 |
| `cSecuencia` | `CHAR(1)` | L48 |
| `cConcepto` | `CHAR(30)` | L49 |
| `cNumCredito` | `CHAR(20)` | L50 |
| `cNombreCte` | `CHAR(104)` | L51 |
| `m_ImportePago` | `MONEY(18,2)` | L52 |
| `mCapVigente` | `MONEY(18,2)` | L53 |
| `mCapTransitorio` | `MONEY(18,2)` | L54 |
| `mCapVencido` | `MONEY(18,2)` | L55 |
| `mCapVdoNoExigible` | `MONEY(18,2)` | L56 |
| `mCapitalTotal` | `MONEY(18,2)` | L57 |
| `mIntVigente` | `MONEY(18,2)` | L58 |
| `mIvaIntVigente` | `MONEY(18,2)` | L59 |
| `mInteresVencido` | `MONEY(18,2)` | L60 |
| `mIvaIntVencido` | `MONEY(18,2)` | L61 |
| `mIntMoraCopete` | `MONEY(18,2)` | L62 |
| `mIvaIntMoratorio` | `MONEY(18,2)` | L63 |
| *…15 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L153 |
| `bdicred` | `bdicnweb` | no | L160 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L137 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pStatus = ''  OR pRegistros IS NULL OR pRecuperacion IS NULL ` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L167 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L175 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L182 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L187 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L190 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L202 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pagos` | ENTIDAD | pagos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareporter24d`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareporter24d.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareporter24d(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMes                         CHAR(2)
  pAnio                        CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMes` | `CHAR(2)` | — | — |
| `pAnio` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `cTransaccion` | `CHAR(4)` | L11 |
| `mMonto` | `MONEY` | L12 |
| `iNumTransaccion` | `INTEGER` | L13 |
| `dFechaInicio` | `DATE` | L14 |
| `dFechaFin` | `DATE` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_valr24d` | `bdicheq` | ⚠️ sí | SELECT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pMes = '' OR pAnio= '' OR pRegistros IS NULL OR pRecuperacion` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L66 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ter24d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?ter24d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareporter24d_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareporter24d_totales.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareporter24d_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMes                         CHAR(2)
  pAnio                        CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMes` | `CHAR(2)` | — | — |
| `pAnio` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `dFechaInicio` | `DATE` | L9 |
| `dFechaFin` | `DATE` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_valr24d` | `bdicheq` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ter24d_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?ter24d_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareporterelacionbcpcp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareporterelacionbcpcp.sql` |
| **LOC (1er CREATE)** | 130 |
| **Callgraph** | ✅ fan_in=0 / fan_out=88 |
| **Deps concatenadas** | 250 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar código postal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareporterelacionbcpcp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pDescripcion                 CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) 	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pDescripcion` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iCodRet` | `INTEGER` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cMensajeRetorno` | `CHAR(80)` | L19 |
| `cNumCliente` | `CHAR(20)` | L20 |
| `cNombreCliente` | `CHAR(107)` | L21 |
| `dFechaNacimiento` | `DATE` | L22 |
| `cDescripcionTipoRelacionInicial` | `CHAR(100)` | L23 |
| `cNumClienteCoppel` | `CHAR(20)` | L24 |
| `cNombreClienteCoppel` | `CHAR(104)` | L25 |
| `cFechaNacimientoCoppel` | `CHAR(10)` | L26 |
| `cModificacion` | `CHAR(10)` | L27 |
| `cFechaRelacion` | `CHAR(10)` | L28 |
| `cDescripcionTipoRelacion` | `CHAR(100)` | L29 |
| `cNombreAnalista` | `CHAR(107)` | L30 |
| `iNoRegistros` | `INTEGER` | L31 |
| `iRecuperacion` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_det_relacionbcpcp` | `bdicnweb` | no | SELECT | L97 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pRegistros IS` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L100 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?terelacionb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?terelacionb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareporteventascaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareporteventascaja.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareporteventascaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cFolioOperacion` | `CHAR(8)` | L16 |
| `dFechaOperacion` | `DATE` | L17 |
| `cOperacion` | `CHAR(8)` | L18 |
| `cInstitucion` | `CHAR(12)` | L19 |
| `cCajaGeneral` | `CHAR(30)` | L20 |
| `cProcedencia` | `CHAR(4)` | L21 |
| `cCC` | `CHAR(4)` | L22 |
| `mImporteTotal` | `MONEY(14,2)` | L23 |
| `cCodTrans` | `CHAR(4)` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L80 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pRegistros IS` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L95 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?teventas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?teventas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultareptoincmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultareptoincmc.sql` |
| **LOC (1er CREATE)** | 146 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultareptoincmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `iCodRetSp` | `INTEGER` | L21 |
| `cEmpresa` | `CHAR(3)` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |
| `cMensajeRetorno` | `CHAR(80)` | L28 |
| `dFechaOrigen` | `DATE` | L29 |
| `cNumSolicitud` | `CHAR(20)` | L30 |
| `cOrigen` | `CHAR(1)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cNombre` | `CHAR(110)` | L33 |
| `dLinCredActual` | `DECIMAL(18,2)` | L34 |
| `dLinCredSugerida` | `DECIMAL(18,2)` | L35 |
| `dPorcentaje` | `DECIMAL(18,2)` | L36 |
| `cStatus` | `CHAR(2)` | L37 |
| `cEjecutivoUno` | `CHAR(8)` | L38 |
| `cEjecutivoDos` | `CHAR(8)` | L39 |
| `cEjecutivoTres` | `CHAR(8)` | L40 |
| `cMotivo` | `CHAR(120)` | L41 |
| `iNumRegistros` | `INTEGER` | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L93 |
| `bdicred` | `bdicnweb` | no | L99 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L79 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio = '' OR pFechaFin = '' OR pRegistros IS NULL OR ` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L105 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicred:sp_repto_inc_mc';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00232';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?eptoinc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?eptoinc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarevisioncacaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarevisioncacaumlincred.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 368 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarevisioncacaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |
| `cMensajeRetorno` | `CHAR(80)` | L19 |
| `dTieneCausa` | `INTEGER` | L20 |
| `cDescripcion` | `CHAR(100)` | L21 |
| `iTotalRegCentral` | `INTEGER` | L22 |
| `dPorcentajeCentral` | `DECIMAL(18,2)` | L23 |
| `iTotalCentral` | `INTEGER` | L24 |
| `dTotPorCentral` | `DECIMAL(18,2)` | L25 |
| `iTotalRegSucursal` | `INTEGER` | L26 |
| `dPorcentajeSucursal` | `DECIMAL(18,2)` | L27 |
| `iTotalSucursal` | `INTEGER` | L28 |
| `dTotPorSucursal` | `DECIMAL(18,2)` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L76 |
| `bdicred` | `bdicnweb` | no | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L62 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechainicial = '' OR pFechaFinal = '' OR pRegistros IS NULL ` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L88 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_revisioncac2';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?evision` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?evision` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarevisioncacaumlincred_rep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarevisioncacaumlincred_rep.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=14 / fan_out=5 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarevisioncacaumlincred_rep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `cMensajeRetorno` | `CHAR(80)` | L12 |
| `dTieneCausa` | `INTEGER` | L13 |
| `cDescripcion` | `CHAR(100)` | L14 |
| `iTotalRegCentral` | `INTEGER` | L15 |
| `dPorcentajeCentral` | `DECIMAL(18,2)` | L16 |
| `iTotalCentral` | `INTEGER` | L17 |
| `dTotPorCentral` | `DECIMAL(18,2)` | L18 |
| `iTotalRegSucursal` | `INTEGER` | L19 |
| `dPorcentajeSucursal` | `DECIMAL(18,2)` | L20 |
| `iTotalSucursal` | `INTEGER` | L21 |
| `dTotPorSucursal` | `DECIMAL(18,2)` | L22 |
| `cNombreReporte` | `CHAR(50)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_aumlim_mesac_mc` | `bdicnweb` | no | INSERT | L89 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L62 |
| `bdicred` | `bdicnweb` | no | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_revisioncac3';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?evision` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?evision` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarevisioncentralaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarevisioncentralaumlincred.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 367 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar (central)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarevisioncentralaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(6)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |
| `iRegistros` | `INTEGER` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |
| `iNoRegs` | `INTEGER` | L22 |
| `cMensajeRetorno` | `CHAR(80)` | L23 |
| `iTieneCausa` | `INTEGER` | L24 |
| `cDescripcion` | `CHAR(100)` | L25 |
| `iTotalRegCasosCac` | `INTEGER` | L26 |
| `dPorcentajeCasosCac` | `DECIMAL(18,2)` | L27 |
| `iTotCAC` | `INTEGER` | L28 |
| `dPorCAC` | `DECIMAL(18,2)` | L29 |
| `iTotalRegCasosAuto` | `INTEGER` | L30 |
| `dDorcentajeCasosAuto` | `DECIMAL(18,2)` | L31 |
| `iTotAUTO` | `INTEGER` | L32 |
| `dPorAUTO` | `DECIMAL(18,2)` | L33 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L83 |
| `bdicred` | `bdicnweb` | no | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet 			 = '00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial = '' OR pFechaFinal = '' OR pRegistros IS NULL ` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L95 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_revisioncentral';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?evision` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `central` | MODIF | central | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?evision` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarevisioncentralaumlincred_rep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarevisioncentralaumlincred_rep.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=14 / fan_out=5 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar (central)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarevisioncentralaumlincred_rep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `iNoRegs` | `INTEGER` | L14 |
| `cMensajeRetorno` | `CHAR(80)` | L15 |
| `iTieneCausa` | `INTEGER` | L16 |
| `cDescripcion` | `CHAR(100)` | L17 |
| `iTotalRegCasosCac` | `INTEGER` | L18 |
| `dPorcentajeCasosCac` | `DECIMAL(18,2)` | L19 |
| `iTotCAC` | `INTEGER` | L20 |
| `dPorCAC` | `DECIMAL(18,2)` | L21 |
| `iTotalRegCasosAuto` | `INTEGER` | L22 |
| `dDorcentajeCasosAuto` | `DECIMAL(18,2)` | L23 |
| `iTotAUTO` | `INTEGER` | L24 |
| `dPorAUTO` | `DECIMAL(18,2)` | L25 |
| `cNombreReporte` | `CHAR(50)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_aumlim_statuscentral_mc` | `bdicnweb` | no | INSERT | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L68 |
| `bdicred` | `bdicnweb` | no | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_revisioncentral';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?evision` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `central` | MODIF | central | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?evision` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarfctpocterelacionadocli`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarfctpocterelacionadocli.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consultar fc — Fuentes Combinadas y cliente (tipo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarfctpocterelacionadocli(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cRfc` | `CHAR(13)` | L9 |
| `cRfcAlterno` | `CHAR(13)` | L10 |
| `iTipoRelacionCliente` | `SMALLINT` | L11 |
| `iExiste` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00053';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?relacionadocli` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?o`, `?relacionadocli` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarhistestatussolmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarhistestatussolmc.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar estatus, solicitud y mc — Mesa de Control (histórico/historial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarhistestatussolmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumeroSolicitud             CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumeroSolicitud` | `CHAR(20)` | `sol`=solicitud | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `cMensajeError` | `CHAR(100)` | L14 |
| `cEstatusAnterior` | `CHAR(2)` | L15 |
| `cEstatusActual` | `CHAR(2)` | L16 |
| `cJustificacion` | `CHAR(200)` | L17 |
| `cNombreUsuario` | `CHAR(104)` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00231';` |  |
| L67 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisolic:sp_consultarhistestat` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultarinforeporteanalistamc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarinforeporteanalistamc.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar información, reporte, analista y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarinforeporteanalistamc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)     AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `cCodRetSp` | `CHAR(6)` | L24 |
| `iCodRet` | `INTEGER` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cEjecutivo` | `CHAR(8)` | L27 |
| `cNombre` | `CHAR(80)` | L28 |
| `iNoAnalizadas` | `INTEGER` | L29 |
| `dPorcentajeAnalizadas` | `DECIMAL(14,2)` | L30 |
| `iNumReevaluadas` | `INTEGER` | L31 |
| `dPorcentajeRevaluadas` | `DECIMAL(14,2)` | L32 |
| `iNumRevaluadas` | `INTEGER` | L33 |
| `dPorcentajeNumRevaluadas` | `DECIMAL(14,2)` | L34 |
| `iNumSiguenProceso` | `INTEGER` | L35 |
| `dPorcentajeSigueProc` | `DECIMAL(14,2)` | L36 |
| `iNumRechazadas` | `INTEGER` | L37 |
| `dPorcentajeRechazadas` | `DECIMAL(14,2)` | L38 |
| `iCanceladas` | `INTEGER` | L39 |
| `dPorcentajeCanceladas` | `DECIMAL(14,2)` | L40 |
| `iNumMixta` | `INTEGER` | L41 |
| `dPorcentajeMixta` | `DECIMAL(14,2)` | L42 |
| `iNumUnica` | `INTEGER` | L43 |
| `dPorcentajeUnica` | `DECIMAL(14,2)` | L44 |
| `cNombreProducto` | `CHAR(80)` | L45 |
| `iRegistros` | `INTEGER` | L46 |
| `iRecuperacion` | `INTEGER` | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L118 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L89 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L127 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultaporanalistamc';` |  |
| L136 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L141 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `analista` | ENTIDAD | analista | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultarinforeportecacmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarinforeportecacmc.sql` |
| **LOC (1er CREATE)** | 203 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar información, reporte y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarinforeportecacmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArea                        CHAR(2)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pStatusInicial               CHAR(2)
  pStatusFinal                 CHAR(2)
  pCveProducto                 CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArea` | `CHAR(2)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pStatusInicial` | `CHAR(2)` | — | — |
| `pStatusFinal` | `CHAR(2)` | — | — |
| `pCveProducto` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L29 |
| `iSqlErr` | `INTEGER` | L30 |
| `cCodRetSp` | `CHAR(5)` | L31 |
| `cEmpresa` | `CHAR(3)` | L32 |
| `iCodRet` | `INTEGER` | L33 |
| `iSolAnalizadas` | `INTEGER` | L34 |
| `iSolRechazadas` | `INTEGER` | L35 |
| `dPorcRechazadas` | `DECIMAL(10,2)` | L36 |
| `iSolEE` | `INTEGER` | L37 |
| `dPorcEE` | `DECIMAL(10,2)` | L38 |
| `iSolAutorizadas` | `INTEGER` | L39 |
| `dPorcAT` | `DECIMAL(10,2)` | L40 |
| `iSolCE` | `INTEGER` | L41 |
| `dProcCE` | `DECIMAL(10,2)` | L42 |
| `iSolProceso` | `INTEGER` | L43 |
| `dPorcSolProceso` | `DECIMAL(10,2)` | L44 |
| `cNumSolicitud` | `CHAR(20)` | L45 |
| `cNumCliente` | `CHAR(20)` | L46 |
| `cNombreCliente` | `CHAR(104)` | L47 |
| `cSucursal` | `CHAR(4)` | L48 |
| `cStatusInicial` | `CHAR(2)` | L49 |
| `cStatusFinal` | `CHAR(2)` | L50 |
| `dFechaSolicitud` | `DATE` | L51 |
| `dFechaCambio` | `DATE` | L52 |
| `cMotivo` | `CHAR(3)` | L53 |
| *…7 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_reportecambioestatusmc` | `bdicnweb` | no | SELECT | L164 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L108 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pArea = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00005';` |  |
| L142 | VALIDACIÓN_NULL | `IF pFechaInicial IS NULL OR pFechaFinal IS NULL OR pStatusFinal = '' THEN` |  |
| L143 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L167 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L176 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultarmvtosnipcte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarmvtosnipcte.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar OS — Originación de Solicitudes, NIP — Número de Identificación Personal y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `spconsultarmvtosnip` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarmvtosnipcte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumTarjeta                  CHAR(16)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumTarjeta` | `CHAR(16)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `dFecha` | `DATE` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `iCodRetorno` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `cFecha` | `CHAR(10)` | L18 |
| `cHora` | `CHAR(9)` | L19 |
| `cSucursal` | `CHAR(4)` | L20 |
| `cEmpleado` | `CHAR(8)` | L21 |
| `cNombreEmpleado` | `CHAR(45)` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `spconsultarmvtosnip` | `bdinteg` | ⚠️ sí | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumTarjeta = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetorno, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:spconsultarmvtosnip';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L89 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?mvt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `nip` | ENTIDAD | NIP — Número de Identificación Personal (PIN bancario) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mvt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultarproductosmoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarproductosmoral.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar productos (persona moral)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarproductosmoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cProducto` | `CHAR(4)` | L8 |
| `cProductoNombre` | `CHAR(40)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00024';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `moral` | MODIF | persona moral | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultarreportepagossplcre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultarreportepagossplcre.sql` |
| **LOC (1er CREATE)** | 241 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar reporte, pagos y crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarreportepagossplcre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  p_FechaIni                   CHAR(10)
  p_FechaFin                   CHAR(10)
  p_Tipo                       CHAR(1)
  p_Folio                      CHAR(16)
  pRegistros                   int
  pRecuperacion                int
) RETURNING CHAR(5)                 AS COD_RET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `p_FechaIni` | `CHAR(10)` | — | — |
| `p_FechaFin` | `CHAR(10)` | — | — |
| `p_Tipo` | `CHAR(1)` | — | — |
| `p_Folio` | `CHAR(16)` | — | — |
| `pRegistros` | `int` | — | — |
| `pRecuperacion` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_cod_ret` | `CHAR(5)` | L32 |
| `iSqlErr` | `INTEGER` | L33 |
| `iSamErr` | `INTEGER` | L34 |
| `s_num_credito` | `CHAR(20)` | L35 |
| `d_fecha_mov` | `DATE` | L36 |
| `s_numcte` | `CHAR(20)` | L37 |
| `s_sucursal` | `CHAR(4)` | L38 |
| `s_num_producto` | `CHAR(4)` | L39 |
| `s_folio` | `CHAR(16)` | L40 |
| `s_concepto_mov` | `CHAR(50)` | L41 |
| `s_desc_pago` | `CHAR(50)` | L42 |
| `s_desc_rev` | `CHAR(50)` | L43 |
| `m_importe_pago` | `MONEY(18,2)` | L44 |
| `s_transaccion` | `CHAR(4)` | L45 |
| `m_cap_vigente` | `MONEY(18,2)` | L46 |
| `m_cap_transitorio` | `MONEY(18,2)` | L47 |
| `m_cap_vencido` | `MONEY(18,2)` | L48 |
| `m_cap_vdo_noexigible` | `MONEY(18,2)` | L49 |
| `m_int_vigente` | `MONEY(18,2)` | L50 |
| `m_iva_intvigente` | `MONEY(18,2)` | L51 |
| `m_interes_vencido` | `MONEY(18,2)` | L52 |
| `m_iva_interesvencido` | `MONEY(18,2)` | L53 |
| `m_int_moratorio` | `MONEY(18,2)` | L54 |
| `m_iva_intmoratorio` | `MONEY(18,2)` | L55 |
| `s_NomCte` | `CHAR(80)` | L56 |
| *…9 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L203 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L124 | VALIDACIÓN_NULL | `IF p_Folio IS NULL OR p_Folio = '' THEN` |  |
| L126 | VALIDACIÓN_NULL | `IF (p_FechaIni IS NULL OR p_FechaIni = '' OR p_FechaFin IS NULL OR p_FechaFin ='' OR p_Tipo IS NULL ` |  |
| L156 | FÓRMULA | `LET mSaldoAntRev = m_cap_vigente - m_importe_pago;` | 🔴 MONEY/aritmética financiera |
| L158 | FÓRMULA | `LET mSaldoAntRev = m_cap_vigente + m_importe_pago;` | 🔴 MONEY/aritmética financiera |
| L162 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L207 | FÓRMULA | `LET mSaldoAntRev = m_cap_vigente - m_importe_pago;` | 🔴 MONEY/aritmética financiera |
| L209 | FÓRMULA | `LET mSaldoAntRev = m_cap_vigente + m_importe_pago;` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pagos` | ENTIDAD | pagos (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultartotalinforeporteanalistamc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultartotalinforeporteanalistamc.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar información, reporte, analista y mc — Mesa de Control (total)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultartotalinforeporteanalistamc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5)     AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRet` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEjecutivo` | `CHAR(8)` | L9 |
| `cNombre` | `CHAR(80)` | L10 |
| `iNoAnalizadas` | `INTEGER` | L11 |
| `dPorcentajeAnalizadas` | `DECIMAL(14,2)` | L12 |
| `iNumReevaluadas` | `INTEGER` | L13 |
| `dPorcentajeRevaluadas` | `DECIMAL(14,2)` | L14 |
| `iNumRevaluadas` | `INTEGER` | L15 |
| `dPorcentajeNumRevaluadas` | `DECIMAL(14,2)` | L16 |
| `iNumSiguenProceso` | `INTEGER` | L17 |
| `dPorcentajeSigueProc` | `DECIMAL(14,2)` | L18 |
| `iNumRechazadas` | `INTEGER` | L19 |
| `dPorcentajeRechazadas` | `DECIMAL(14,2)` | L20 |
| `iCanceladas` | `INTEGER` | L21 |
| `dPorcentajeCanceladas` | `DECIMAL(14,2)` | L22 |
| `iNumMixta` | `INTEGER` | L23 |
| `dPorcentajeMixta` | `DECIMAL(14,2)` | L24 |
| `iNumUnica` | `INTEGER` | L25 |
| `dPorcentajeUnica` | `DECIMAL(14,2)` | L26 |
| `cNombreProducto` | `CHAR(80)` | L27 |
| `iRegistros` | `INTEGER` | L28 |
| `iRecuperacion` | `INTEGER` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultaporanalistamc';` |  |
| L89 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `analista` | ENTIDAD | analista | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultartotalmvtosnipcte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultartotalmvtosnipcte.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar OS — Originación de Solicitudes, NIP — Número de Identificación Personal y cliente (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `spconsultarmvtosnip` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultartotalmvtosnipcte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumTarjeta                  CHAR(16)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumTarjeta` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iRegistros` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `cFecha` | `CHAR(10)` | L11 |
| `cHora` | `CHAR(9)` | L12 |
| `cSucursal` | `CHAR(4)` | L13 |
| `cEmpleado` | `CHAR(8)` | L14 |
| `cNombreEmpleado` | `CHAR(45)` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `spconsultarmvtosnip` | `bdinteg` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mvt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `nip` | ENTIDAD | NIP — Número de Identificación Personal (PIN bancario) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mvt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultartotctesporasignarcli`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultartotctesporasignarcli.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consultar tc — Tarjeta de Crédito (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consultarctesporasignar` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultartotctesporasignarcli(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                CHAR(1)
  pNumCliente                  CHAR(20)
  pTipoCuenta                  CHAR(1)
  pTipoCampo                   CHAR(1)
  pTipoDireccion               CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pTipoCuenta` | `CHAR(1)` | — | — |
| `pTipoCampo` | `CHAR(1)` | — | — |
| `pTipoDireccion` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iRegistros` | `INTEGER` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `cNumCliente` | `CHAR(20)` | L13 |
| `cNombreCliente` | `CHAR(107)` | L14 |
| `dFechaNacimiento` | `DATE` | L15 |
| `cEstado` | `CHAR(30)` | L16 |
| `cCiudad` | `CHAR(60)` | L17 |
| `cCalle` | `CHAR(30)` | L18 |
| `cCodigoPostal` | `CHAR(5)` | L19 |
| `cTipoDomicilio` | `CHAR(10)` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cMunicipio` | `CHAR(27)` | L22 |
| `iNumColonia` | `INTEGER` | L23 |
| `cZona` | `CHAR(32)` | L24 |
| `cObservaciones` | `CHAR(80)` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarctesporasignar` | `bdinteg` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?or` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?rcli` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?to`, `?t`, `?or`, `?rcli` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultartotinforeportecacmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultartotinforeportecacmc.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar información, reporte y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultartotinforeportecacmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArea                        CHAR(2)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pStatusInicial               CHAR(2)
  pStatusFinal                 CHAR(2)
  pCveProducto                 CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArea` | `CHAR(2)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pStatusInicial` | `CHAR(2)` | — | — |
| `pStatusFinal` | `CHAR(2)` | — | — |
| `pCveProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iCodRet` | `INTEGER` | L9 |
| `iSolAnalizadas` | `INTEGER` | L10 |
| `iSolRechazadas` | `INTEGER` | L11 |
| `dPorcRechazadas` | `DECIMAL(10,2)` | L12 |
| `iSolEE` | `INTEGER` | L13 |
| `dPorcEE` | `DECIMAL(10,2)` | L14 |
| `iSolAutorizadas` | `INTEGER` | L15 |
| `dPorcAT` | `DECIMAL(10,2)` | L16 |
| `iSolCE` | `INTEGER` | L17 |
| `dProcCE` | `DECIMAL(10,2)` | L18 |
| `iSolProceso` | `INTEGER` | L19 |
| `dPorcSolProceso` | `DECIMAL(10,2)` | L20 |
| `cNumSolicitud` | `CHAR(20)` | L21 |
| `cNumCliente` | `CHAR(20)` | L22 |
| `cNombreCliente` | `CHAR(104)` | L23 |
| `cSucursal` | `CHAR(4)` | L24 |
| `cStatusInicial` | `CHAR(2)` | L25 |
| `cStatusFinal` | `CHAR(2)` | L26 |
| `dFechaSolicitud` | `DATE` | L27 |
| `dFechaCambio` | `DATE` | L28 |
| `cMotivo` | `CHAR(3)` | L29 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_repcambioestatusmc` | `bdicnweb` | no | SELECT | L82 |
| `status_repcambioestatusmc` | `bdicnweb` | no | DELETE | L82 |
| `status_repcambioestatusmc` | `bdicnweb` | no | INSERT | L83 |
| `sw_reportecambioestatusmc` | `bdicnweb` | no | SELECT | L113 |
| `sw_reportecambioestatusmc` | `bdicnweb` | no | DELETE | L113 |
| `sw_reportecambioestatusmc` | `bdicnweb` | no | INSERT | L134 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultarinforeportecac` | `bdicred` | ⚠️ sí | L115 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L85 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pArea = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL TH` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00005';` |  |
| L104 | VALIDACIÓN_NULL | `IF pFechaInicial IS NULL OR pFechaFinal IS NULL OR pStatusFinal = '' THEN` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L123 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultarinforeportecac';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |
| L130 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultas_cac_central_ctemc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultas_cac_central_ctemc.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta cliente y mc — Mesa de Control (central)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultas_cac_central_ctemc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombre1                     CHAR(30)
  pNombre2                     CHAR(30)
  pApellidoPaterno             CHAR(30)
  pApellidoMaterno             CHAR(30)
  pNumCliente                  CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombre1` | `CHAR(30)` | — | — |
| `pNombre2` | `CHAR(30)` | — | — |
| `pApellidoPaterno` | `CHAR(30)` | — | — |
| `pApellidoMaterno` | `CHAR(30)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `iRegistros` | `INTEGER` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |
| `cEmpresa` | `CHAR(3)` | L27 |
| `cMensajeError` | `CHAR(80)` | L30 |
| `cNumSolicitud` | `CHAR(20)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cNombreCliente` | `CHAR(104)` | L33 |
| `cRfc` | `CHAR(13)` | L34 |
| `cSucursal` | `CHAR(4)` | L35 |
| `dFechaSolicitud` | `DATE` | L36 |
| `dFechaCambio` | `DATE` | L37 |
| `dImporteLinea` | `DECIMAL(18,2)` | L38 |
| `dEficiencia` | `DECIMAL(5,2)` | L39 |
| `iHistorial` | `INTEGER` | L40 |
| `dPuntos1Seccion` | `DECIMAL(5,2)` | L41 |
| `dPuntos2Seccion` | `DECIMAL(5,2)` | L42 |
| `cStatus` | `CHAR(2)` | L43 |
| `cObservaciones` | `CHAR(511)` | L44 |
| `dSumaSecciones` | `DECIMAL(8,2)` | L45 |
| `cCausaSolicitud` | `CHAR(3)` | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L108 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L86 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L119 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L122 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L125 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER;` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `central` | MODIF | central | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultas_cac_central_total`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultas_cac_central_total.sql` |
| **LOC (1er CREATE)** | 549 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta (central, total)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultas_cac_central_total(
  pEmpresa                     CHAR(3)
  pSucursal                    CHAR(4)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pNumSol                      CHAR(20)
  pBanCac                      CHAR(1)
  pCac_Opt1_1                  DECIMAL(5,2)
  pCac_Opt3_1                  INTEGER
  pArea                        CHAR(2)
  pStatus                      CHAR(2)
  pCausa                       CHAR(3)
  pProducto                    CHAR(4)
) RETURNING CHAR(6),          -- Codigo de Retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |
| `pBanCac` | `CHAR(1)` | `cac`=familia crédito (CAC) | ✅ CÓDIGO |
| `pCac_Opt1_1` | `DECIMAL(5,2)` | `cac`=familia crédito (CAC) | ✅ CÓDIGO |
| `pCac_Opt3_1` | `INTEGER` | `cac`=familia crédito (CAC) | ✅ CÓDIGO |
| `pArea` | `CHAR(2)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pCausa` | `CHAR(3)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cNumSolicitud` | `CHAR(20)` | L17 |
| `cNumCte` | `CHAR(20)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `dtFechaInsert` | `DATE` | L20 |
| `dtFechaModificacion` | `DATE` | L21 |
| `dMontoSolicitado` | `DECIMAL(18,2)` | L22 |
| `cStatusSol` | `CHAR(2)` | L23 |
| `cTipoSolicitud` | `CHAR(1)` | L24 |
| `iInfoBuro` | `INTEGER` | L25 |
| `cComentarioAut` | `CHAR(511)` | L26 |
| `iRevisionCac` | `INTEGER` | L27 |
| `cNombreCte` | `CHAR(104)` | L28 |
| `cRFC` | `CHAR(13)` | L29 |
| `dSituacionPago` | `DECIMAL(5,2)` | L30 |
| `iMesesHistoria` | `INTEGER` | L31 |
| `dSeccion1` | `DECIMAL(18,2)` | L32 |
| `dSeccion2` | `DECIMAL(18,2)` | L33 |
| `dSeccionAux` | `DECIMAL(18,2)` | L34 |
| `dSumaSecciones` | `DECIMAL(18,2)` | L35 |
| `iCantidad` | `INTEGER` | L36 |
| `icuantos` | `INTEGER` | L37 |
| `iSecAux` | `INTEGER` | L38 |
| `cEmpAux` | `CHAR(3)` | L39 |
| `iSqlErr` | `INTEGER` | L40 |
| `iIsamErr` | `INTEGER` | L41 |
| *…26 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_param` | `bdicred` | ⚠️ sí | SELECT | L182 |
| `ss_solicitudes` | `bdisolic` | ⚠️ sí | SELECT | L203 |
| `paso1` | `bdicnweb` | no | SELECT | L221 |
| `paso1` | `bdicnweb` | no | DELETE | L225 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L179 | VALIDACIÓN_NULL | `IF pFechaInicial IS NOT NULL AND pFechaFinal IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `central` | MODIF | central | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultas_cac_central_total2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultas_cac_central_total2.sql` |
| **LOC (1er CREATE)** | 653 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta (central, total)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultas_cac_central_total2(
  pEmpresa                     CHAR(3)
  pSucursal                    CHAR(4)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pNumSol                      CHAR(20)
  pBanCac                      CHAR(1)
  pCac_Opt1_1                  DECIMAL(5,2)
  pCac_Opt3_1                  INTEGER
  pArea                        CHAR(2)
  pStatus                      CHAR(2)
  pCausa                       CHAR(3)
  pProducto                    CHAR(4)
  pUsuario                     CHAR(10)
) RETURNING CHAR(6),          -- Codigo de Retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |
| `pBanCac` | `CHAR(1)` | `cac`=familia crédito (CAC) | ✅ CÓDIGO |
| `pCac_Opt1_1` | `DECIMAL(5,2)` | `cac`=familia crédito (CAC) | ✅ CÓDIGO |
| `pCac_Opt3_1` | `INTEGER` | `cac`=familia crédito (CAC) | ✅ CÓDIGO |
| `pArea` | `CHAR(2)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pCausa` | `CHAR(3)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pUsuario` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cNumSolicitud` | `CHAR(20)` | L6 |
| `cNumCte` | `CHAR(20)` | L7 |
| `cSucursal` | `CHAR(4)` | L8 |
| `dtFechaInsert` | `DATE` | L9 |
| `dtFechaModificacion` | `DATE` | L10 |
| `dMontoSolicitado` | `DECIMAL(18,2)` | L11 |
| `cStatusSol` | `CHAR(2)` | L12 |
| `cTipoSolicitud` | `CHAR(1)` | L13 |
| `iInfoBuro` | `INTEGER` | L14 |
| `cComentarioAut` | `CHAR(511)` | L15 |
| `iRevisionCac` | `INTEGER` | L16 |
| `cNombreCte` | `CHAR(104)` | L17 |
| `cRFC` | `CHAR(13)` | L18 |
| `dSituacionPago` | `DECIMAL(5,2)` | L19 |
| `iMesesHistoria` | `INTEGER` | L20 |
| `dSeccion1` | `DECIMAL(18,2)` | L21 |
| `dSeccion2` | `DECIMAL(18,2)` | L22 |
| `dSeccionAux` | `DECIMAL(18,2)` | L23 |
| `dSumaSecciones` | `DECIMAL(18,2)` | L24 |
| `iCantidad` | `INTEGER` | L25 |
| `icuantos` | `INTEGER` | L26 |
| `iSecAux` | `INTEGER` | L27 |
| `cEmpAux` | `CHAR(3)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `iIsamErr` | `INTEGER` | L30 |
| *…14 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_param` | `bdicred` | ⚠️ sí | SELECT | L154 |
| `paso1` | `bdicnweb` | no | SELECT | L190 |
| `paso1` | `bdicnweb` | no | DELETE | L190 |
| `ss_solicitudes` | `bdisolic` | ⚠️ sí | SELECT | L227 |
| `ss_autorizacion` | `bdisolic` | ⚠️ sí | SELECT | L232 |
| `ss_autorizacion_especial` | `bdisolic` | ⚠️ sí | SELECT | L244 |
| `br_traslado` | `bdiburo` | ⚠️ sí | SELECT | L271 |
| `ss_resum_scor_fin` | `bdisolic` | ⚠️ sí | SELECT | L318 |
| `ss_resumen_scoring` | `bdisolic` | ⚠️ sí | SELECT | L345 |
| `ss_scoring_financ` | `bdisolic` | ⚠️ sí | SELECT | L359 |
| `ss_detalle_scoring` | `bdisolic` | ⚠️ sí | SELECT | L374 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L403 |
| `ss_solautorizadasgte` | `bdisolic` | ⚠️ sí | SELECT | L409 |
| `paso1` | `bdicnweb` | no | INSERT | L413 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L151 | VALIDACIÓN_NULL | `IF pFechaInicial IS NOT NULL AND pFechaFinal IS NULL THEN` |  |
| L383 | FÓRMULA | `LET dSeccion2= dSeccion2 + dSeccionAux;` |  |
| L384 | FÓRMULA | `LET dSumaSecciones= dSeccion1 + dSeccion2;` |  |
| L410 | FÓRMULA | `LET cComentarioAut = "Solicitud Autorizada GTE"\|\|"-"\|\|TRIM(cComentarioAut);` |  |
| L607 | FÓRMULA | `LET dSeccion2= dSeccion2 + dSeccionAux;` |  |
| L608 | FÓRMULA | `LET dSumaSecciones= dSeccion1 + dSeccion2;` |  |
| L634 | FÓRMULA | `LET cComentarioAut = "Solicitud Autorizada GTE"\|\|"-"\|\|TRIM(cComentarioAut);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `central` | MODIF | central | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultas_cac_central_totctemc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultas_cac_central_totctemc.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta tc — Tarjeta de Crédito y mc — Mesa de Control (central)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultas_cac_central_totctemc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombre1                     CHAR(30)
  pNombre2                     CHAR(30)
  pApellidoPaterno             CHAR(30)
  pApellidoMaterno             CHAR(30)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombre1` | `CHAR(30)` | — | — |
| `pNombre2` | `CHAR(30)` | — | — |
| `pApellidoPaterno` | `CHAR(30)` | — | — |
| `pApellidoMaterno` | `CHAR(30)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iRegistros` | `INTEGER` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cMensajeError` | `CHAR(80)` | L14 |
| `cNumSolicitud` | `CHAR(20)` | L15 |
| `cNumCliente` | `CHAR(20)` | L16 |
| `cNombreCliente` | `CHAR(104)` | L17 |
| `cRfc` | `CHAR(13)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `dFechaSolicitud` | `DATE` | L20 |
| `dFechaCambio` | `DATE` | L21 |
| `dImporteLinea` | `DECIMAL(18,2)` | L22 |
| `dEficiencia` | `DECIMAL(5,2)` | L23 |
| `iHistorial` | `INTEGER` | L24 |
| `dPuntos1Seccion` | `DECIMAL(5,2)` | L25 |
| `dPuntos2Seccion` | `DECIMAL(5,2)` | L26 |
| `cStatus` | `CHAR(2)` | L27 |
| `cObservaciones` | `CHAR(511)` | L28 |
| `dSumaSecciones` | `DECIMAL(8,2)` | L29 |
| `cCausaSolicitud` | `CHAR(3)` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L79 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER;` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `central` | MODIF | central | 🔵 CONVENCIÓN | nombre_sp |
| `?_to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_`, `?_to`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultas_cac_centralcred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultas_cac_centralcred.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito (central)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultas_cac_centralcred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFinal                  DATE
  pNumSolicitud                CHAR(20)
  pBanCac                      CHAR(1)
  pCac_Opt1_1                  DECIMAL(5,2)
  pCac_Opt3_1                  INTEGER
  pArea                        CHAR(2)
  pStatus                      CHAR(2)
  pCausa                       CHAR(3)
  pProducto                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pBanCac` | `CHAR(1)` | `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pCac_Opt1_1` | `DECIMAL(5,2)` | `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pCac_Opt3_1` | `INTEGER` | `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pArea` | `CHAR(2)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pCausa` | `CHAR(3)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `cCodRetSp` | `CHAR(6)` | L24 |
| `pEmpresa` | `CHAR(3)` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |
| `cMensajeRetorno` | `CHAR(80)` | L30 |
| `cNumSolicitud` | `CHAR(20)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cNombreCliente` | `CHAR(104)` | L33 |
| `cRfc` | `CHAR(13)` | L34 |
| `cSucursal` | `CHAR(4)` | L35 |
| `dFechaSolicitud` | `DATE` | L36 |
| `dFechaCambioStatus` | `DATE` | L37 |
| `dImporteLinea` | `DECIMAL(18,2)` | L38 |
| `dEficiencia` | `DECIMAL(5,2)` | L39 |
| `iHistorial` | `INTEGER` | L40 |
| `dPuntos1Secc` | `DECIMAL(5,2)` | L41 |
| `dPuntos2Secc` | `DECIMAL(5,2)` | L42 |
| `cStatus` | `CHAR(2)` | L43 |
| `cObservacionesAnteriores` | `CHAR(511)` | L44 |
| `dSumaSecciones` | `DECIMAL(8,2)` | L45 |
| `cCausaStatus` | `CHAR(3)` | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L100 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN SP bdicred:sp_consultas_CAC_central';` |  |
| L111 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `central` | MODIF | central | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultas_cac_centraltotcred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultas_cac_centraltotcred.sql` |
| **LOC (1er CREATE)** | 152 |
| **Callgraph** | ✅ fan_in=0 / fan_out=114 |
| **Deps concatenadas** | 355 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta tc — Tarjeta de Crédito (central)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultas_cac_centraltotcred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFinal                  DATE
  pNumSolicitud                CHAR(20)
  pBanCac                      CHAR(1)
  pCac_Opt1_1                  DECIMAL(5,2)
  pCac_Opt3_1                  INTEGER
  pArea                        CHAR(2)
  pStatus                      CHAR(2)
  pCausa                       CHAR(3)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pBanCac` | `CHAR(1)` | `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pCac_Opt1_1` | `DECIMAL(5,2)` | `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pCac_Opt3_1` | `INTEGER` | `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pArea` | `CHAR(2)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pCausa` | `CHAR(3)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `pEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cMensajeRetorno` | `CHAR(80)` | L13 |
| `cNumSolicitud` | `CHAR(20)` | L14 |
| `cNumCliente` | `CHAR(20)` | L15 |
| `cNombreCliente` | `CHAR(104)` | L16 |
| `cRfc` | `CHAR(13)` | L17 |
| `cSucursal` | `CHAR(4)` | L18 |
| `dFechaSolicitud` | `DATE` | L19 |
| `dFechaCambioStatus` | `DATE` | L20 |
| `dImporteLinea` | `DECIMAL(18,2)` | L21 |
| `dEficiencia` | `DECIMAL(5,2)` | L22 |
| `iHistorial` | `INTEGER` | L23 |
| `dPuntos1Secc` | `DECIMAL(5,2)` | L24 |
| `dPuntos2Secc` | `DECIMAL(5,2)` | L25 |
| `cStatus` | `CHAR(2)` | L26 |
| `cObservacionesAnteriores` | `CHAR(511)` | L27 |
| `dSumaSecciones` | `DECIMAL(8,2)` | L28 |
| `cCausaStatus` | `CHAR(3)` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultacaccentral` | `bdicnweb` | no | SELECT | L79 |
| `sw_statusconsultacaccentral` | `bdicnweb` | no | DELETE | L79 |
| `sw_statusconsultacaccentral` | `bdicnweb` | no | INSERT | L84 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultas_cac_central_total2` | `bdicnweb` | no | L113 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L117 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN SP bdicred:sp_consultas_CAC_central';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `central` | MODIF | central | 🔵 CONVENCIÓN | nombre_sp |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `?red` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_`, `?to`, `?red` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultas_movcaptacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultas_movcaptacion.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta movimiento y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultas_movcaptacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iExiste` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_consultas_movcaptacion` | `bdicnweb` | no | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `?tacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_`, `?tacion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasaldopagare`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasaldopagare.sql` |
| **LOC (1er CREATE)** | 197 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta saldo y pagaré" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `cons_sdo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasaldopagare(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L44 |
| `cEmpresa` | `CHAR(3)` | L45 |
| `cCodRet` | `CHAR(5)` | L46 |
| `cCliente` | `CHAR(20)` | L47 |
| `cSecuencia` | `CHAR(6)` | L48 |
| `cNombreCliente` | `CHAR(104)` | L49 |
| `cTpPersona` | `CHAR(2)` | L50 |
| `cDescTpPersona` | `CHAR(20)` | L51 |
| `cCalle` | `CHAR(35)` | L52 |
| `cColonia` | `CHAR(35)` | L53 |
| `cDelegacion` | `CHAR(35)` | L54 |
| `cPoblacion` | `CHAR(20)` | L55 |
| `cCodPostal` | `CHAR(5)` | L56 |
| `cTelefono` | `CHAR(14)` | L57 |
| `cInstrumento` | `CHAR(4)` | L58 |
| `cDescInstrumento` | `CHAR(40)` | L59 |
| `cMoneda` | `CHAR(12)` | L60 |
| `cDescMoneda` | `CHAR(30)` | L61 |
| `cPlaza` | `CHAR(3)` | L62 |
| `cSucursal` | `CHAR(4)` | L63 |
| `cNombreSucursal` | `CHAR(40)` | L64 |
| `dTasaBruta` | `DECIMAL(9,6)` | L65 |
| `dTasaISR` | `DECIMAL(9,6)` | L66 |
| `dTasaNeta` | `DECIMAL(9,6)` | L67 |
| `mIntereses` | `MONEY(14,2)` | L68 |
| *…17 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L152 |
| `cons_sdo` | `bdinvers` | ⚠️ sí | L161 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L178 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L181 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L184 | CÓDIGO_RETORNO | `LET cCodRet = '00099';` |  |
| L187 | CÓDIGO_RETORNO | `LET cCodRet = '00020';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `saldo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `pagare` | ENTIDAD | pagaré | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultasaldoscredito`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasaldoscredito.sql` |
| **LOC (1er CREATE)** | 191 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 161 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta saldos y crédito" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasaldoscredito(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `dSaldoUltimoCorte` | `DECIMAL(14,2)` | L26 |
| `dSusCompras` | `DECIMAL(14,2)` | L27 |
| `dDisposiciones` | `DECIMAL(14,2)` | L28 |
| `dSusComisiones` | `DECIMAL(14,2)` | L29 |
| `dIva` | `DECIMAL(14,2)` | L30 |
| `dSusPagos` | `DECIMAL(14,2)` | L31 |
| `dIntMoratorio1` | `DECIMAL(14,2)` | L32 |
| `dIvaIntMoratorio1` | `DECIMAL(14,2)` | L33 |
| `dSaldoRetenido1` | `DECIMAL(18,2)` | L36 |
| `dIntMoratorio2` | `DECIMAL(14,2)` | L37 |
| `dIvaIntMoratorio2` | `DECIMAL(14,2)` | L38 |
| `dPagoMinimoHoy` | `DECIMAL(18,2)` | L39 |
| `dSaldoDisponible` | `DECIMAL(18,2)` | L40 |
| `dSaldoVencido` | `DECIMAL(18,2)` | L41 |
| `dSaldoDiferido` | `DECIMAL(18,2)` | L44 |
| `dSdoRetenido` | `DECIMAL(18,2)` | L47 |
| `dIntMoratorio` | `DECIMAL(18,2)` | L48 |
| `dIvaIntMoratorio` | `DECIMAL(18,2)` | L49 |
| `dSdoActCap` | `DECIMAL(18,2)` | L50 |
| `dFechaLimitePago` | `DATE` | L51 |
| `dPagoNoIntereses` | `DECIMAL(18,2)` | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicred:sp_conssdoticket";` |  |
| L141 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicred:sp_consulta_saldos_general";` |  |
| L143 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L154 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "bdicred:sp_consdos_dif_promo";` |  |
| L158 | FÓRMULA | `LET dSdoRetenido = dSaldoRetenido1 - dSaldoDiferido;` |  |
| L159 | FÓRMULA | `LET dIntMoratorio = dIntMoratorio1 + dIntMoratorio2;` |  |
| L160 | FÓRMULA | `LET dIvaIntMoratorio = dIvaIntMoratorio1 + dIvaIntMoratorio2;` |  |
| L169 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicred:sp_consultasaldocortemin";` |  |
| L171 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L175 | CÓDIGO_RETORNO | `LET cCodRet = '00359';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultaservicioactivotc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultaservicioactivotc.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 90 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta servicio y tc — Tarjeta de Crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaserviciocte_pm` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaservicioactivotc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pTipoServ                    SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTipoServ` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `cServicioActivo` | `CHAR(1)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_consultaserviciocte_pm` | `bdicheq` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pNumCte = '' OR pTipoServ IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_consultaserviciocte_pm";` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?ivo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ivo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasesustituibles`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasesustituibles.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=55 |
| **Deps concatenadas** | 149 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienesesustituibles_clon` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasesustituibles(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSituacion                   CHAR(1)
  pCausa                       SMALLINT
  pAlcance                     SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSituacion` | `CHAR(1)` | — | — |
| `pCausa` | `SMALLINT` | — | — |
| `pAlcance` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cDescCodRetSp` | `CHAR(100)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cSituacion` | `CHAR(5)` | L14 |
| `iCausa` | `CHAR(5)` | L15 |
| `cDescripcion` | `CHAR(75)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_obtienesesustituibles_clon` | `bdisitesp` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisitesp:sp_obtienesesustituib` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?sesustituibles` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sesustituibles` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasolicitudprocesomc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasolicitudprocesomc.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud, proceso y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasolicitudprocesomc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEjecutivo` | `CHAR(8)` | L7 |
| `cNombreEjecutivo` | `CHAR(45)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | SELECT | L39 |
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | DELETE | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '90000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultasucursal_datos1`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasucursal_datos1.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 149 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sucursal y datos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasucursal_datos1(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal                  CHAR(4)
) RETURNING CHAR(5) 		 AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cNombreSuc` | `CHAR(40)` | L17 |
| `cCveestado` | `CHAR(2)` | L18 |
| `cNombreEst` | `CHAR(30)` | L19 |
| `cCveMun` | `CHAR(3)` | L20 |
| `cNombreMun` | `CHAR(60)` | L21 |
| `cColonia` | `CHAR(100)` | L22 |
| `cNumInt` | `CHAR(5)` | L23 |
| `cCP` | `CHAR(5)` | L24 |
| `cTel` | `CHAR(14)` | L25 |
| `cLatitud` | `CHAR(11)` | L26 |
| `cLongitud` | `CHAR(10)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet ='01265';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `?1` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?1` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasucursal_datos2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasucursal_datos2.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 148 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sucursal y datos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasucursal_datos2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal                  CHAR(4)
) RETURNING CHAR(5) 		 AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iNumATM` | `INTEGER` | L14 |
| `fM2` | `FLOAT` | L15 |
| `iNumVentanilla` | `INTEGER` | L16 |
| `iNumEscri` | `INTEGER` | L17 |
| `cTipologia` | `CHAR(40)` | L18 |
| `cTipo` | `CHAR(3)` | L19 |
| `cZonaEco` | `CHAR(30)` | L20 |
| `cFormato` | `CHAR(40)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet ='01265';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasucursal_datosgen`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasucursal_datosgen.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 147 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sucursal y datos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasucursal_datosgen(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal                  CHAR(4)
) RETURNING CHAR(5) 		 AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `dFechaAp` | `DATE` | L12 |
| `cZonaHoraria` | `CHAR(20)` | L13 |
| `cClasificacion` | `CHAR(40)` | L14 |
| `cSucEje` | `CHAR(40)` | L15 |
| `cCveGer` | `CHAR(4)` | L16 |
| `cZbcve` | `CHAR(4)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet ='01265';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultasucursal_diasap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasucursal_diasap.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 146 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sucursal (del día)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasucursal_diasap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal                  CHAR(4)
) RETURNING CHAR(5) 		 AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `dLunAp` | `DATETIME YEAR to SECOND` | L13 |
| `dMarAp` | `DATETIME YEAR to SECOND` | L14 |
| `dMieAp` | `DATETIME YEAR to SECOND` | L15 |
| `dJueAp` | `DATETIME YEAR to SECOND` | L16 |
| `dVieAp` | `DATETIME YEAR to SECOND` | L17 |
| `dSabAp` | `DATETIME YEAR to SECOND` | L18 |
| `dDomAp` | `DATETIME YEAR to SECOND` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet ='01265';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `?sap` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sap` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasucursal_diasserv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasucursal_diasserv.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 145 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sucursal y ss — subsistema (del día)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasucursal_diasserv(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal                  CHAR(4)
) RETURNING CHAR(5) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `bServLun` | `BOOLEAN` | L14 |
| `bServMar` | `BOOLEAN` | L15 |
| `bServMie` | `BOOLEAN` | L16 |
| `bServJue` | `BOOLEAN` | L17 |
| `bServVie` | `BOOLEAN` | L18 |
| `bServSab` | `BOOLEAN` | L19 |
| `bServDom` | `BOOLEAN` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet ='01265';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?erv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ss`, `?erv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasucursal_ub`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasucursal_ub.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 144 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sucursal" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasucursal_ub(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSucursal                  CHAR(4)
) RETURNING CHAR(5) 		 AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cECalles` | `CHAR(100)` | L10 |
| `bEstDom` | `BOOLEAN` | L11 |
| `cCalle` | `CHAR(100)` | L12 |
| `cNumExt` | `CHAR(6)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='01265';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_ub` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ub` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasucursalesfiltro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultasucursalesfiltro.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 118 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sucursal y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultasucursalesfiltro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFiltro                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFiltro` | `CHAR(2)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cNumSucursal` | `CHAR(4)` | L8 |
| `cNombreSucursal` | `CHAR(40)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L79 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?esfilt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?esfilt`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatabulartiposcambio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatabulartiposcambio.sql` |
| **LOC (1er CREATE)** | 116 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cambio (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatabulartiposcambio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveTpCambio               CHAR(3)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveTpCambio` | `CHAR(3)` | `cambio`=cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `iRowid` | `INTEGER` | L18 |
| `cDivisa` | `CHAR(2)` | L19 |
| `cCveIntl` | `CHAR(3)` | L20 |
| `dFecha_tpcambio` | `DATE` | L21 |
| `cClase_tpcambio` | `CHAR(1)` | L22 |
| `dPrecio_compra` | `DECIMAL(14,6)` | L23 |
| `dPrecio_venta` | `DECIMAL(14,6)` | L24 |
| `dTipo_cpa_div_dll` | `DECIMAL(14,6)` | L25 |
| `dTipo_cpa_mn_div` | `DECIMAL(14,6)` | L26 |
| `dTipo_vta_div_dll` | `DECIMAL(14,6)` | L27 |
| `dTipo_vta_mn_div` | `DECIMAL(14,6)` | L28 |
| `iRecuperacion` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tpcambio` | `bdinteg` | ⚠️ sí | SELECT | L80 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tabular` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tabular`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatabulartiposcambio_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatabulartiposcambio_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cambio (tipo de, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatabulartiposcambio_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveTpCambio               CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveTpCambio` | `CHAR(3)` | `cambio`=cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotalRegistros` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tpcambio` | `bdinteg` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tabular` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tabular`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatipo_gcb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatipo_gcb.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 143 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatipo_gcb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,CHAR(3) AS cve_tipo
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCve_tipo` | `CHAR(3)` | L6 |
| `cTipo` | `CHAR(40)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tipo_gcb` | `bdinteg` | ⚠️ sí | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet ='01270';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_gcb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gcb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatiposcambio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatiposcambio.sql` |
| **LOC (1er CREATE)** | 215 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cambio (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatiposcambio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pRowId                       INTEGER
  pCveMercado                  CHAR(1)
  pDivisa                      CHAR(2)
  pFechaTpC                    DATE
  pPrecioCpa                   DECIMAL(14,6)
  pPrecioVta                   DECIMAL(14,6)
  pPrecioVtaA                  DECIMAL(14,6)
  pTipoCpaMnDiv                DECIMAL(14,6)
  pTipoVtaMnDiv                DECIMAL(14,6)
  pVariacionVta                DECIMAL(9,7)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pRowId` | `INTEGER` | — | — |
| `pCveMercado` | `CHAR(1)` | — | — |
| `pDivisa` | `CHAR(2)` | — | — |
| `pFechaTpC` | `DATE` | — | — |
| `pPrecioCpa` | `DECIMAL(14,6)` | — | — |
| `pPrecioVta` | `DECIMAL(14,6)` | — | — |
| `pPrecioVtaA` | `DECIMAL(14,6)` | — | — |
| `pTipoCpaMnDiv` | `DECIMAL(14,6)` | `tipo`=tipo de | 🔵 CONVENCIÓN |
| `pTipoVtaMnDiv` | `DECIMAL(14,6)` | `tipo`=tipo de | 🔵 CONVENCIÓN |
| `pVariacionVta` | `DECIMAL(9,7)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cEmpresa` | `CHAR(3)` | L29 |
| `iRowid` | `INTEGER` | L30 |
| `cDivisa` | `char(2)` | L31 |
| `dFecha_tpcambio` | `date` | L32 |
| `cClase_tpcambio` | `char(1)` | L33 |
| `dPrecio_compra` | `decimal(14,6)` | L34 |
| `dPc_arriba` | `decimal(14,6)` | L35 |
| `dPc_abajo` | `decimal(14,6)` | L36 |
| `dPrecio_venta` | `decimal(14,6)` | L37 |
| `dPv_abajo` | `decimal(14,6)` | L38 |
| `dPv_arriba` | `decimal(14,6)` | L39 |
| `dVariacion_cpa` | `decimal(9,7)` | L40 |
| `dVariacion_vta` | `decimal(9,7)` | L41 |
| `dTipo_cpa_div_dll` | `decimal(14,6)` | L42 |
| `dTipo_cpa_mn_dll` | `decimal(14,6)` | L43 |
| `dTipo_cpa_mn_div` | `decimal(14,6)` | L44 |
| `dTipo_vta_div_dll` | `decimal(14,6)` | L45 |
| `dTipo_vta_mn_dll` | `decimal(14,6)` | L46 |
| `dTipo_vta_mn_div` | `decimal(14,6)` | L47 |
| `iRecuperacion` | `INTEGER` | L48 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tpcambio` | `bdinteg` | ⚠️ sí | SELECT | L135 |
| `si_tpcambio` | `bdinteg` | ⚠️ sí | UPDATE | L169 |
| `si_tpcambio` | `bdinteg` | ⚠️ sí | DELETE | L196 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L92 | VALIDACIÓN_NULL | `IF pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | VALIDACIÓN_NULL | `IF pRowId IS NULL OR  pCveMercado = '' OR pDivisa = '' OR pFechaTpC IS NULL OR pPrecioCpa IS NULL OR` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L108 | VALIDACIÓN_NULL | `IF pRowId IS NULL THEN` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L144 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotalenviospagodinya`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotalenviospagodinya.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta envíos, pago y DINYA — sistema/plataforma de remesas domésticas en sucursal; retorna nombre_remitente, sucursal_origen, importe_eviado (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_dinya_obtenerenviospagos` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotalenviospagodinya(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(3)
  pImporte1                    CHAR(16)
  pImporte2                    CHAR (16)
  pSucursalOrigen              CHAR(4)
  pNombre1Remitente            CHAR(26)
  pNombre2Remitente            CHAR(26)
  pApellido1Remitente          CHAR(26)
  pApellido2Remitente          CHAR(26)
  pFechaEnvio1                 DATE
  pFechaEnvio2                 DATE
  pNombre1Beneficiario         CHAR(26)
  pNombre2Beneneficiario       CHAR(26)
  pApellido1Beneneficiario     CHAR(26)
  pApellido2Beneneficiario     CHAR(26)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(3)` | — | — |
| `pImporte1` | `CHAR(16)` | — | — |
| `pImporte2` | `CHAR (16)` | — | — |
| `pSucursalOrigen` | `CHAR(4)` | — | — |
| `pNombre1Remitente` | `CHAR(26)` | — | — |
| `pNombre2Remitente` | `CHAR(26)` | — | — |
| `pApellido1Remitente` | `CHAR(26)` | — | — |
| `pApellido2Remitente` | `CHAR(26)` | — | — |
| `pFechaEnvio1` | `DATE` | — | — |
| `pFechaEnvio2` | `DATE` | — | — |
| `pNombre1Beneficiario` | `CHAR(26)` | — | — |
| `pNombre2Beneneficiario` | `CHAR(26)` | — | — |
| `pApellido1Beneneficiario` | `CHAR(26)` | — | — |
| `pApellido2Beneneficiario` | `CHAR(26)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `cNoControl` | `CHAR(12)` | L14 |
| `dFechaEnvio` | `DATE` | L15 |
| `cSucursalOrigen` | `CHAR(4)` | L16 |
| `cNombre1Remitente` | `CHAR(26)` | L17 |
| `cNombre2Remitente` | `CHAR(26)` | L18 |
| `cApellido1Remitente` | `CHAR(26)` | L19 |
| `cApellido2Remitente` | `CHAR(26)` | L20 |
| `mImporteEnviado` | `MONEY (16,2)` | L21 |
| `cStatus` | `CHAR(20)` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_dinya_obtenerenviospagos` | `bdisac` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP PRODUCTIVO sp_dinya_obtenerenv` |  |
| L71 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `envios` | ENTIDAD | envíos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dinya` | ENTIDAD | DINYA — sistema/plataforma de remesas domésticas en sucursal | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultatotalesdepositoscoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotalesdepositoscoppel.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 94 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta depósito, OS — Originación de Solicitudes y Coppel (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotalesdepositoscoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `mImpFicha` | `MONEY(14,2)` | L9 |
| `iTotReg` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_temp_deposito_coppel` | `bdisuc` | ⚠️ sí | SELECT | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet 	= '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?it` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotalhistorialstatusmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotalhistorialstatusmc.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta estatus y mc — Mesa de Control (total, histórico/historial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotalhistorialstatusmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(3)
  pNumSol                      CHAR(20)
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cMensajeRet` | `CHAR(80)` | L7 |
| `cComentario` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iIsamErr` | `INTEGER` | L10 |
| `cErrorInfo` | `CHAR(80)` | L11 |
| `cStatusSol` | `CHAR(2)` | L12 |
| `dFechaEntrada` | `DATE` | L13 |
| `dFechaSalida` | `DATE` | L14 |
| `iNoRegs` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L60 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `?orial` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orial` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotalreportedetallesolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotalreportedetallesolicitudmc.sql` |
| **LOC (1er CREATE)** | 216 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte, detalle, solicitud y mc — Mesa de Control (total)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotalreportedetallesolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRet` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cNumSolicitud` | `CHAR(20)` | L9 |
| `cSucursal` | `CHAR(4)` | L10 |
| `vNombreCte` | `VARCHAR(100)` | L11 |
| `dFechaSol` | `DATE` | L12 |
| `dFechaCambio` | `DATE` | L13 |
| `cRevaluada` | `CHAR(2)` | L14 |
| `cReferenciaCoppel` | `CHAR(20)` | L15 |
| `dcEficienciaCoppel` | `DECIMAL(18,2)` | L16 |
| `sMesesCoppel` | `SMALLINT` | L17 |
| `dcVencidoCoppel` | `DECIMAL(18,2)` | L18 |
| `iVencidoCoppeludis` | `INTEGER` | L19 |
| `cPuntualidad` | `CHAR(2)` | L20 |
| `iScoring1` | `INTEGER` | L21 |
| `iScoring2` | `INTEGER` | L22 |
| `cDescStatus` | `CHAR(40)` | L23 |
| `cCausaSolic` | `CHAR(3)` | L24 |
| `vComentario` | `VARCHAR(100)` | L25 |
| `cAnalista` | `CHAR(45)` | L26 |
| `cTipoMovto` | `CHAR(10)` | L27 |
| `cNombreProducto` | `CHAR(50)` | L28 |
| `iRegistros` | `INTEGER` | L29 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_repsolicitudmc` | `bdicnweb` | no | SELECT | L101 |
| `status_repsolicitudmc` | `bdicnweb` | no | DELETE | L101 |
| `status_repsolicitudmc` | `bdicnweb` | no | INSERT | L102 |
| `sw_reportesolicitudmc` | `bdicnweb` | no | SELECT | L133 |
| `sw_reportesolicitudmc` | `bdicnweb` | no | DELETE | L133 |
| `sw_reportesolicitudmc` | `bdicnweb` | no | INSERT | L160 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultadetallesolicitudmc` | `bdicred` | ⚠️ sí | L138 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L105 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L146 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultadetallesolicitudmc';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L174 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultatotalsaldosdiarioscap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotalsaldosdiarioscap.sql` |
| **LOC (1er CREATE)** | 49 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta saldos y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (total, diarios)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_permisosejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotalsaldosdiarioscap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iRegistros` | `INTEGER` | L7 |
| `dFechaMovimiento` | `DATE` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCuenta = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL ` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | FÓRMULA | `LET iRegistros = (pFechaFinal - pFechaInicial) + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `diarios` | MODIF | diarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultatotchequesdevueltoscap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotchequesdevueltoscap.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta tc — Tarjeta de Crédito, sd — saldo disponible, OS — Originación de Solicitudes y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotchequesdevueltoscap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pCausalesDevolucion          CHAR(100)
  pCodigosAlertamiento         CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pCausalesDevolucion` | `CHAR(100)` | `sd`=sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO |
| `pCodigosAlertamiento` | `CHAR(30)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `dFecha` | `DATE` | L7 |
| `cCuentaDeposito` | `CHAR(20)` | L8 |
| `cCodigoAlertamiento` | `CHAR(2)` | L9 |
| `cDescCodAlertamiento` | `CHAR(35)` | L10 |
| `cCuentaCheque` | `CHAR(20)` | L11 |
| `cBancoEmisor` | `CHAR(45)` | L12 |
| `cNombreBeneficiario` | `CHAR(40)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `cQuery` | `CHAR(200)` | L15 |
| `cFromQuery` | `CHAR(450)` | L16 |
| `cWhereQuery` | `CHAR(500)` | L17 |
| `cCaracter` | `CHAR(1)` | L18 |
| `i` | `INTEGER` | L19 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL THEN` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET cQuery = "SELECT count(*)";` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `?heque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?evuelt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?to`, `?heque`, `?evuelt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotexcepcionesaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotexcepcionesaumlincred.sql` |
| **LOC (1er CREATE)** | 119 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 366 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Comprobante Electrónico de Pago" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotexcepcionesaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pExcepcion                   CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pExcepcion` | `CHAR(3)` | `x`=por (criterio) · `cep`=Comprobante Electrónico de Pago (SPEI · Banxico) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(80)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusconsultaincrementosexcepciones` | `bdicnweb` | no | SELECT | L40 |
| `statusconsultaincrementosexcepciones` | `bdicnweb` | no | DELETE | L40 |
| `statusconsultaincrementosexcepciones` | `bdicnweb` | no | INSERT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L61 |
| `bdicred` | `bdicnweb` | no | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_excepciones_tot';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tote` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cep` | ENTIDAD | Comprobante Electrónico de Pago (SPEI · Banxico) | 🔵 CONVENCIÓN | nombre_sp |
| `?ciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tote`, `?ciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotgralaplicadosaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotgralaplicadosaumlincred.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 365 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotgralaplicadosaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                CHAR(10)
  pFechaFinal                  CHAR(10)
  pStatus                      CHAR(2)
  pOrigen                      CHAR(1)
  pOpcFecha                    CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `CHAR(10)` | — | — |
| `pFechaFinal` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |
| `pOpcFecha` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeRetorno` | `CHAR(80)` | L10 |
| `iTotalRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosgralaplicados` | `bdicnweb` | no | SELECT | L39 |
| `sw_statusconsultaincrementosgralaplicados` | `bdicnweb` | no | DELETE | L39 |
| `sw_statusconsultaincrementosgralaplicados` | `bdicnweb` | no | INSERT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L60 |
| `bdicred` | `bdicnweb` | no | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consulta_gral_aumlincred_aplicados';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotgralautaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotgralautaumlincred.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 364 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotgralautaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pStatus                      CHAR(2)
  pOrigen                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(80)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosgralaut` | `bdicnweb` | no | SELECT | L38 |
| `sw_statusconsultaincrementosgralaut` | `bdicnweb` | no | DELETE | L38 |
| `sw_statusconsultaincrementosgralaut` | `bdicnweb` | no | INSERT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L60 |
| `bdicred` | `bdicnweb` | no | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consulta_gral_aumlincred_aut_tot';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotgralstatusaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotgralstatusaumlincred.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 363 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta estatus (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotgralstatusaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
  pOrigen                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pOrigen` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(80)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosgralstatus` | `bdicnweb` | no | SELECT | L40 |
| `sw_statusconsultaincrementosgralstatus` | `bdicnweb` | no | DELETE | L40 |
| `sw_statusconsultaincrementosgralstatus` | `bdicnweb` | no | INSERT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L62 |
| `bdicred` | `bdicnweb` | no | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_rep_gral_status_total';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatothistoricomttohuellacte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatothistoricomttohuellacte.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta huella biométrica y cliente (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consultahistoricomttohuella` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatothistoricomttohuellacte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `dFechaMantto` | `DATE` | L8 |
| `cFechaMantto` | `CHAR(10)` | L9 |
| `cHora` | `CHAR(10)` | L10 |
| `cSucursal` | `CHAR(4)` | L11 |
| `cEmpEjecuto` | `CHAR(8)` | L12 |
| `cNomEmpEjecuto` | `CHAR(30)` | L13 |
| `cEmpAutorizo` | `CHAR(8)` | L14 |
| `cNomEmpAutorizo` | `CHAR(30)` | L15 |
| `cEmpCaja` | `CHAR(8)` | L16 |
| `cNomEmpCaja` | `CHAR(30)` | L17 |
| `iNoRegs` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultahistoricomttohuella` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mtto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `huella` | ENTIDAD | huella biométrica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot`, `?mtto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotperfilusuarioaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotperfilusuarioaumlincred.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 362 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuario (tipo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotperfilusuarioaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(80)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosperfilusuario` | `bdicnweb` | no | SELECT | L38 |
| `sw_statusconsultaincrementosperfilusuario` | `bdicnweb` | no | DELETE | L38 |
| `sw_statusconsultaincrementosperfilusuario` | `bdicnweb` | no | INSERT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L59 |
| `bdicred` | `bdicnweb` | no | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_perfil_usuario_tot';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?erfil` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?to`, `?erfil` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotreporteoperacionesmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotreporteoperacionesmc.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte, operaciones y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotreporteoperacionesmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(80)` | L9 |
| `dFecha` | `DATE` | L10 |
| `cFecha` | `CHAR(10)` | L11 |
| `cNombreProducto` | `CHAR(50)` | L12 |
| `iSolicitudesMc` | `INTEGER` | L13 |
| `iNoAnalizadasATiempo` | `INTEGER` | L14 |
| `iAnalizadasATiempo` | `INTEGER` | L15 |
| `iRevaluadas` | `INTEGER` | L16 |
| `iNoRevaluadas` | `INTEGER` | L17 |
| `iSigueProceso` | `INTEGER` | L18 |
| `dPorcentajeSp` | `DECIMAL(18,2)` | L19 |
| `iRechazadas` | `INTEGER` | L20 |
| `dPorcentajeRt` | `DECIMAL(18,2)` | L21 |
| `iCanceladas` | `INTEGER` | L22 |
| `dPorcentajeCn` | `DECIMAL(18,2)` | L23 |
| `iMixta` | `INTEGER` | L24 |
| `iUnica` | `INTEGER` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL THEN` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultaoperacionesmc';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |
| L92 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotreporterelacionbcpcp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotreporterelacionbcpcp.sql` |
| **LOC (1er CREATE)** | 190 |
| **Callgraph** | ✅ fan_in=0 / fan_out=88 |
| **Deps concatenadas** | 248 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte y código postal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotreporterelacionbcpcp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pDescripcion                 CHAR(20)
) RETURNING CHAR(5) 	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pDescripcion` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRet` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(80)` | L9 |
| `cNumCliente` | `CHAR(20)` | L10 |
| `cNombreCliente` | `CHAR(107)` | L11 |
| `dFechaNacimiento` | `DATE` | L12 |
| `cDescripcionTipoRelacionInicial` | `CHAR(100)` | L13 |
| `cNumClienteCoppel` | `CHAR(20)` | L14 |
| `cNombreClienteCoppel` | `CHAR(104)` | L15 |
| `cFechaNacimientoCoppel` | `CHAR(10)` | L16 |
| `cModificacion` | `CHAR(10)` | L17 |
| `cFechaRelacion` | `CHAR(10)` | L18 |
| `cDescripcionTipoRelacion` | `CHAR(100)` | L19 |
| `cNombreAnalista` | `CHAR(107)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |
| `iIdRegistro` | `INTEGER` | L24 |
| `bEnTransaccion` | `BOOLEAN` | L25 |
| `iContador` | `INTEGER` | L26 |
| `iMaxCommit` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_relacionbcpcp` | `bdicnweb` | no | SELECT | L76 |
| `status_relacionbcpcp` | `bdicnweb` | no | DELETE | L76 |
| `status_relacionbcpcp` | `bdicnweb` | no | INSERT | L77 |
| `sw_det_relacionbcpcp` | `bdicnweb` | no | SELECT | L100 |
| `sw_det_relacionbcpcp` | `bdicnweb` | no | DELETE | L102 |
| `sw_det_relacionbcpcp` | `bdicnweb` | no | INSERT | L144 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_relacion_consultadatosrpt` | `bdinteg` | ⚠️ sí | L124 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L79 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL THEN` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L104 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L130 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN EJECUCIÃN DEL SP sp_relacion_consultadatosrpt';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L143 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L149 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?relacionb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot`, `?relacionb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotrevisioncacaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotrevisioncacaumlincred.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 361 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta revisión" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotrevisioncacaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(80)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosrevisioncac` | `bdicnweb` | no | SELECT | L41 |
| `sw_statusconsultaincrementosrevisioncac` | `bdicnweb` | no | DELETE | L41 |
| `sw_statusconsultaincrementosrevisioncac` | `bdicnweb` | no | INSERT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L62 |
| `bdicred` | `bdicnweb` | no | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_revisioncac_total';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `revision` | ENTIDAD | revisión | 🔵 CONVENCIÓN | nombre_sp |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotrevisioncentralaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotrevisioncentralaumlincred.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 360 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta revisión (central)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotrevisioncentralaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechainicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechainicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(80)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosrevisioncentral` | `bdicnweb` | no | SELECT | L41 |
| `sw_statusconsultaincrementosrevisioncentral` | `bdicnweb` | no | DELETE | L41 |
| `sw_statusconsultaincrementosrevisioncentral` | `bdicnweb` | no | INSERT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L62 |
| `bdicred` | `bdicnweb` | no | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_rep_revisioncentral_total';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `revision` | ENTIDAD | revisión | 🔵 CONVENCIÓN | nombre_sp |
| `central` | MODIF | central | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatotsolicitudesaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultatotsolicitudesaumlincred.sql` |
| **LOC (1er CREATE)** | 271 |
| **Callgraph** | ✅ fan_in=0 / fan_out=87 |
| **Deps concatenadas** | 247 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultatotsolicitudesaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pOrigen                      CHAR(1)
  pNumCte                      CHAR(20)
  pNumCred                     CHAR(20)
  pNumTarjeta                  CHAR(20)
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCred` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRet` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegsEncontrados` | `INTEGER` | L10 |
| `iRegistros` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cMensajeRetorno` | `CHAR(100)` | L14 |
| `cNumCredito` | `CHAR(20)` | L15 |
| `cNumCliente` | `CHAR(20)` | L16 |
| `cNombreCliente` | `CHAR(107)` | L17 |
| `cStatus` | `CHAR(2)` | L18 |
| `cOrigen` | `CHAR(10)` | L19 |
| `cSucursal` | `CHAR(4)` | L20 |
| `dFechaSolicitud` | `DATE` | L21 |
| `mLineaActual` | `MONEY(18,2)` | L22 |
| `mLineaSugerida` | `MONEY(18,2)` | L23 |
| `iNivelAutorizacion` | `SMALLINT` | L24 |
| `cUsuarioTrabajando` | `CHAR(107)` | L25 |
| `iContinua` | `SMALLINT` | L26 |
| `iPagina` | `INTEGER` | L27 |
| `iTotRegsSp` | `INTEGER` | L28 |
| `dFechaCambioEstatus` | `DATE` | L29 |
| `cCveRtCm` | `CHAR(3)` | L30 |
| `cEmpresa` | `CHAR(3)` | L32 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_aumlincred` | `bdicnweb` | no | SELECT | L120 |
| `status_aumlincred` | `bdicnweb` | no | DELETE | L120 |
| `status_aumlincred` | `bdicnweb` | no | INSERT | L121 |
| `sw_det_aumlincred` | `bdicnweb` | no | SELECT | L144 |
| `sw_det_aumlincred` | `bdicnweb` | no | DELETE | L146 |
| `sw_det_aumlincred` | `bdicnweb` | no | INSERT | L227 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cac_consultasolincrelincred` | `bdicred` | ⚠️ sí | L168 |
| `sp_cac_asignasolanalista` | `bdicred` | ⚠️ sí | L192 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L84 | FÓRMULA | `LET iNumeroPagina = 0; -- Desde la pagina 0` |  |
| L85 | FÓRMULA | `LET iRecuperarRegistros = 0; -- Consulta general sin paginaciÃ³n` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L148 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L176 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP bdicred:sp_cac_consultasolincrelincred';` |  |
| L179 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L181 | CÓDIGO_RETORNO | `LET cCodRet = '00252';` |  |
| L196 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetAsig, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_asignasolanalista';` |  |
| L226 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L232 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L249 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitudes` | ENTIDAD | solicitudes (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultausuariocteprocesandomc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultausuariocteprocesandomc.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuario, Cliente Prospecto — cliente potencial aún sin cuenta abierta y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultausuariocteprocesandomc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cNumUsuario` | `CHAR(8)` | L8 |
| `cNombreUsuario` | `CHAR(45)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | SELECT | L39 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '90000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `?ocesando` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ocesando` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultausuariosccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultausuariosccl.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuarios y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultausuariosccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cActivo` | `CHAR(1)` | L16 |
| `cClave` | `CHAR(10)` | L17 |
| `cNombre` | `CHAR(30)` | L18 |
| `cOperacion` | `CHAR(1)` | L19 |
| `cMonitoreo` | `CHAR(1)` | L20 |
| `cAdmon` | `CHAR(1)` | L21 |
| `dFecha` | `DATETIME YEAR TO FRACTION(5)` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `bditarjeta` | `bdicnweb` | no | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_consultausuarios` |  |
| L68 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultausuarioscedulasccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultausuarioscedulasccl.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuarios, cédulas y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultausuarioscedulasccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCcl                    DATE
  pTipo                        SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCcl` | `DATE` | `ccl`=módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (bdicnweb:sp_ccl_*) | 🟡 INFERIDO |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cNombre` | `CHAR(104)` | L10 |
| `sfuncion` | `SMALLINT` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `bdicnweb` | `bdicnweb` | no | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaCcl = '' OR pTipo IS NULL  THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_usuarioscedulas ";` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cedulas` | ENTIDAD | cédulas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_consultazona_gcb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultazona_gcb.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 142 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta zona" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultazona_gcb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,CHAR(3) AS cve_zona,CHAR(40) AS zona_economica
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCve_zona` | `CHAR(3)` | L6 |
| `cZona_economica` | `CHAR(40)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_zona_gcb` | `bdinteg` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet ='01268';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `zona` | ENTIDAD | zona | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_gcb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gcb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultazonasimilar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_consultazonasimilar.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta zona" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultazonasimilar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               SMALLINT
  pTipoAlta                    SMALLINT
  pIdCiudad                    SMALLINT
  pNombreZona                  CHAR(32)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pTipoAlta` | `SMALLINT` | — | — |
| `pIdCiudad` | `SMALLINT` | — | — |
| `pNombreZona` | `CHAR(32)` | `zona`=zona | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `cNombreZona` | `CHAR(32)` | L9 |
| `cDynamicQuery` | `char(500)` | L10 |
| `iIdZonaCalle` | `INTEGER` | L11 |
| `iRows` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L67 |
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion IS NULL OR pTipoAlta IS NULL OR pRegistros IS ` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L52 | VALIDACIÓN_NULL | `IF pTipoAlta = 1 AND pIdCiudad IS NULL THEN` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L102 | FÓRMULA | `LET iRows = iRows + 1;` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `zona` | ENTIDAD | zona | 🔵 CONVENCIÓN | nombre_sp |
| `?similar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?similar` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_coppelmax_consultarpuntos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_coppelmax_consultarpuntos.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=21 / fan_out=53 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar Coppel y puntos (máximo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_coppelmax_consultarpuntos(
  pNumcte                      CHAR(20)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pNumProducto                 CHAR(4)
) RETURNING VARCHAR(5,0)  AS codRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumcte` | `CHAR(20)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cRow_id` | `INTEGER` | L14 |
| `cFechaMovimiento` | `CHAR(30)` | L15 |
| `cDescripcion` | `CHAR(40)` | L16 |
| `cNaturaleza` | `CHAR(1)` | L17 |
| `cMonto` | `DECIMAL(16,2)` | L18 |
| `cBeneficio_calculado` | `DECIMAL(16,2)` | L19 |
| `cPuntos_totales` | `DECIMAL(16,2)` | L20 |
| `cSaldo_actual` | `DECIMAL(16,2)` | L21 |
| `iRegistros` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_movs_monedero_plan_lealtad` | `bdicred` | ⚠️ sí | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `max` | MODIF | máximo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_coppelmax_modificarpuntos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_coppelmax_modificarpuntos.sql` |
| **LOC (1er CREATE)** | 48 |
| **Callgraph** | ✅ fan_in=21 / fan_out=53 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "modifica Coppel y puntos (máximo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `modifica` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_coppelmax_modificarpuntos(
  pRow_id                      INTEGER
  pPuntos_beneficio            DECIMAL(16,2)
) RETURNING VARCHAR(5,0)  AS codRetorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRow_id` | `INTEGER` | — | — |
| `pPuntos_beneficio` | `DECIMAL(16,2)` | `puntos`=puntos (recompensas) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_movs_monedero_plan_lealtad` | `bdicred` | ⚠️ sí | UPDATE | L27 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `max` | MODIF | máximo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `modifica` | ACCION | modifica | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_bitacoraerrormanualtdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_bitacoraerrormanualtdc.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "código postal, bitácora, error y tarjeta de crédito (manual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_grabadetallearchivotdc` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_bitacoraerrormanualtdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pNomProceso                  CHAR(35)
  pNumCredito                  CHAR(20)
  pNumCte                      CHAR(20)
  pNumTarjeta                  CHAR(17)
  pTipoTarjeta                 CHAR(4)
  pMensajeError                CHAR(100)
  pDirMac                      CHAR(12)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pNomProceso` | `CHAR(35)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(17)` | — | — |
| `pTipoTarjeta` | `CHAR(4)` | — | — |
| `pMensajeError` | `CHAR(100)` | `error`=error | ✅ CÓDIGO |
| `pDirMac` | `CHAR(12)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cNombreProceso` | `CHAR(35)` | L16 |
| `cNumeroCredito` | `CHAR(20)` | L17 |
| `cNumeroCliente` | `CHAR(20)` | L18 |
| `cNumeroTarjeta` | `CHAR(17)` | L19 |
| `cTipoTarjeta` | `CHAR(4)` | L20 |
| `cMensajeError` | `CHAR(100)` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |
| `iRegistros` | `INTEGER` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |
| `iErrorInf` | `INTEGER;	DEFINE cMensajeRet CHAR(100)` | L25 |
| `cNomArchivo` | `CHAR(35)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_credito_upgrade` | `bdicred` | ⚠️ sí | SELECT | L77 |
| `sw_cp_bitacoraerrormanualtdc` | `bdicnweb` | no | INSERT | L95 |
| `sw_cp_bitacoraerrormanualtdc` | `bdicnweb` | no | SELECT | L116 |
| `sw_cp_bitacoraerrormanualtdc` | `bdicnweb` | no | DELETE | L143 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L67 |
| `sp_grabadetallearchivotdc` | `bdicred` | ⚠️ sí | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | FÓRMULA | `LET iErrorInf = 0; --- AAME 20190218 RQM 10682-4 iErrorInf, cMensajeRet, cNomArchivo` |  |
| L55 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L81 | FÓRMULA | `LET iErrorInf= CHARINDEX('-',pMensajeError);` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L129 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00862';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `error` | ENTIDAD | error | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `manual` | MODIF | manual | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cp_consultadetcuentas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadetcuentas.sql` |
| **LOC (1er CREATE)** | 188 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal, detalle y cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadetcuentas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `cNumTarjeta` | `CHAR(20)` | L24 |
| `cStatusCred` | `CHAR(2)` | L25 |
| `cNumCredito` | `CHAR(20)` | L26 |
| `cTipoTarjeta` | `CHAR(3)` | L27 |
| `cNombreCliente` | `CHAR(30)` | L28 |
| `cNombreEmbozado` | `CHAR(21)` | L29 |
| `cMaster` | `CHAR(2)` | L30 |
| `cDomicilioEnvio` | `CHAR(1)` | L31 |
| `cSucursal` | `CHAR(4)` | L32 |
| `cNumCliente` | `CHAR(20)` | L33 |
| `cProdDestino` | `CHAR(4)` | L34 |
| `cDescProdDestino` | `CHAR(40)` | L35 |
| `cOrigenReg` | `CHAR(1)` | L36 |
| `iIdRegistro` | `INTEGER` | L37 |
| `cDescDomEnvio` | `CHAR(20)` | L38 |
| `cDescSucursal` | `CHAR(40)` | L39 |
| `iRecuperacion` | `INTEGER` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_datosctastdc` | `bdicnweb` | no | SELECT | L106 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | SELECT | L110 |
| `sw_cp_datosctastdc` | `bdicnweb` | no | UPDATE | L133 |
| `si_tipo_dir_upg` | `bdinteg` | ⚠️ sí | SELECT | L142 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L147 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDireccionMac = '' OR pNombreArchivo = '' OR pRegistros IS NU` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L157 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L164 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_consultadetcuentas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadetcuentas_totales.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal, detalle y cuentas (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadetcuentas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_datosctastdc` | `bdicnweb` | no | SELECT | L42 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cp_consultadetcuentastdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadetcuentastdcoro.sql` |
| **LOC (1er CREATE)** | 191 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal, detalle, cuentas, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadetcuentastdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `cNumTarjeta` | `CHAR(20)` | L24 |
| `cStatusCred` | `CHAR(2)` | L25 |
| `cNumCredito` | `CHAR(20)` | L26 |
| `cTipoTarjeta` | `CHAR(3)` | L27 |
| `cNombreCliente` | `CHAR(30)` | L28 |
| `cNombreEmbozado` | `CHAR(21)` | L29 |
| `cMaster` | `CHAR(2)` | L30 |
| `cDomicilioEnvio` | `CHAR(1)` | L31 |
| `cSucursal` | `CHAR(4)` | L32 |
| `cNumCliente` | `CHAR(20)` | L33 |
| `cProdDestino` | `CHAR(4)` | L34 |
| `cDescProdDestino` | `CHAR(40)` | L35 |
| `cOrigenReg` | `CHAR(1)` | L36 |
| `iIdRegistro` | `INTEGER` | L37 |
| `cDescDomEnvio` | `CHAR(20)` | L38 |
| `cDescSucursal` | `CHAR(40)` | L39 |
| `iRecuperacion` | `INTEGER` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_datosctastdc` | `bdicnweb` | no | SELECT | L106 |
| `sw_cp_procesadetallearchivotdctdcoro` | `bdicnweb` | no | SELECT | L110 |
| `sw_cp_datosctastdc` | `bdicnweb` | no | UPDATE | L133 |
| `si_tipo_dir_upg` | `bdinteg` | ⚠️ sí | SELECT | L142 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L147 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDireccionMac = '' OR pNombreArchivo = '' OR pRegistros IS NU` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L157 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L164 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_consultadetcuentastdcoro_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadetcuentastdcoro_totales.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal, detalle, cuentas, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadetcuentastdcoro_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_datosctastdc` | `bdicnweb` | no | SELECT | L42 |
| `sw_cp_procesadetallearchivotdctdcoro` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cp_consultadeterrores`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadeterrores.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=43 |
| **Deps concatenadas** | 73 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal, detalle y error" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadeterrores(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iLinea` | `INTEGER` | L11 |
| `cCampo` | `CHAR(35)` | L12 |
| `cDescMensaje` | `CHAR(120)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDireccionMac = '' OR pNombreArchivo = '' OR pRegistros IS NU` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_consultadeterrores_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadeterrores_totales.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=43 |
| **Deps concatenadas** | 74 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal, detalle y error (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadeterrores_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_consultadeterrores_totalestdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadeterrores_totalestdcoro.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal, detalle, error, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadeterrores_totalestdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iNumRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_consultadeterrorestdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadeterrorestdcoro.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal, detalle, error, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadeterrorestdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iLinea` | `INTEGER` | L11 |
| `cCampo` | `CHAR(35)` | L12 |
| `cDescMensaje` | `CHAR(120)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDireccionMac = '' OR pNombreArchivo = '' OR pRegistros IS NU` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_consultadomicilios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultadomicilios.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 75 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal y domicilio" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_dom_upgrade` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultadomicilios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTpoEjec                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTpoEjec` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cTipoDir` | `CHAR(4)` | L10 |
| `cDescTipoDir` | `CHAR(40)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_consulta_dom_upgrade` | `bdicred` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_consulta_prod_upgrade";` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `domicilio` | ENTIDAD | domicilio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_consultaproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_consultaproductos.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=45 |
| **Deps concatenadas** | 76 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código postal y productos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_prod_upgrade` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_consultaproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSiglaProd                   CHAR(2)
  pSiglaProdUp                 CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSiglaProd` | `CHAR(2)` | — | — |
| `pSiglaProdUp` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cNumProducto` | `CHAR(4)` | L11 |
| `vNomProducto` | `CHAR(100)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_consulta_prod_upgrade` | `bdicred` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_consulta_prod_upgrade";` |  |
| L56 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cp_graba_prod_upgrade`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_graba_prod_upgrade.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 78 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba código postal y producto" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_graba_prod_upgrade` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=3 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_graba_prod_upgrade(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCredito                     CHAR(20)
  pNumCte                      CHAR(20)
  pNumTarjeta                  CHAR(20)
  pTit                         CHAR(3)
  pNombre                      CHAR(107)
  pEmbozado                    CHAR(21)
  pMaster                      CHAR(1)
  pTipoDomicilio               CHAR(1)
  pTipoProceso                 CHAR(1)
  pNombreArchivo               CHAR(100)
  pProdUpgrade                 CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCredito` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pTit` | `CHAR(3)` | — | — |
| `pNombre` | `CHAR(107)` | — | — |
| `pEmbozado` | `CHAR(21)` | — | — |
| `pMaster` | `CHAR(1)` | — | — |
| `pTipoDomicilio` | `CHAR(1)` | — | — |
| `pTipoProceso` | `CHAR(1)` | — | — |
| `pNombreArchivo` | `CHAR(100)` | — | — |
| `pProdUpgrade` | `CHAR(4)` | `prod`=producto · `upgrade`=actualiza producto (upgrade) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cDescripcion` | `CHAR(100)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_graba_prod_upgrade` | `bdicred` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP: bdicred:sp_graba_prod_upgrade";` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00972';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `graba` | ACCION | graba / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `upgrade` | ACCION | actualiza producto (upgrade) | 🟡 INFERIDO | nombre_sp |

---

## `sp_cp_graba_prod_upgradetdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_graba_prod_upgradetdcoro.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba código postal, producto, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_graba_prod_upgradetdcoro` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=3 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_graba_prod_upgradetdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCredito                     CHAR(20)
  pNumCte                      CHAR(20)
  pNumTarjeta                  CHAR(20)
  pTit                         CHAR(3)
  pNombre                      CHAR(107)
  pEmbozado                    CHAR(21)
  pMaster                      CHAR(1)
  pTipoDomicilio               CHAR(1)
  pTipoProceso                 CHAR(1)
  pNombreArchivo               CHAR(100)
  pProdUpgrade                 CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCredito` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pTit` | `CHAR(3)` | — | — |
| `pNombre` | `CHAR(107)` | — | — |
| `pEmbozado` | `CHAR(21)` | — | — |
| `pMaster` | `CHAR(1)` | — | — |
| `pTipoDomicilio` | `CHAR(1)` | — | — |
| `pTipoProceso` | `CHAR(1)` | — | — |
| `pNombreArchivo` | `CHAR(100)` | — | — |
| `pProdUpgrade` | `CHAR(4)` | `prod`=producto · `upgrade`=actualiza producto (upgrade) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cDescripcion` | `CHAR(100)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_graba_prod_upgradetdcoro` | `bdicred` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP: bdicred:sp_graba_prod_upgrade";` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00972';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `graba` | ACCION | graba / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `upgrade` | ACCION | actualiza producto (upgrade) | 🟡 INFERIDO | nombre_sp |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_grabadetallearchivotdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_grabadetallearchivotdc.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba código postal, detalle, archivo y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_grabadetallearchivotdc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_grabadetallearchivotdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pCredito                     CHAR(20)
  pNumCte                      CHAR(20)
  pNumTarjeta                  CHAR(20)
  pTit                         CHAR(3)
  pNombre                      CHAR(107)
  pEmbozado                    CHAR(21)
  pMaster                      CHAR(1)
  pTipoDomicilio               CHAR(1)
  pTipoProceso                 CHAR(1)
  pNombreArchivo               CHAR(100)
  pProdUpgrade                 CHAR(4)
  pError                       CHAR(1)
  pMensajeError                CHAR(120)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pCredito` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pTit` | `CHAR(3)` | — | — |
| `pNombre` | `CHAR(107)` | — | — |
| `pEmbozado` | `CHAR(21)` | — | — |
| `pMaster` | `CHAR(1)` | — | — |
| `pTipoDomicilio` | `CHAR(1)` | — | — |
| `pTipoProceso` | `CHAR(1)` | — | — |
| `pNombreArchivo` | `CHAR(100)` | `archivo`=archivo | ✅ CÓDIGO |
| `pProdUpgrade` | `CHAR(4)` | — | — |
| `pError` | `CHAR(1)` | — | — |
| `pMensajeError` | `CHAR(120)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(100)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iExiste` | `SMALLINT` | L11 |
| `cMensajeError` | `CHAR(120)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cred_cambioproducto` | `bdicnweb` | no | INSERT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_grabadetallearchivotdc` | `bdicred` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_grabadetallearchivot` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_grabadetallearchivotdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_grabadetallearchivotdcoro.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba código postal, detalle, archivo, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_grabadetallearchivotdcoro` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_grabadetallearchivotdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pCredito                     CHAR(20)
  pNumCte                      CHAR(20)
  pNumTarjeta                  CHAR(20)
  pTit                         CHAR(3)
  pNombre                      CHAR(107)
  pEmbozado                    CHAR(21)
  pMaster                      CHAR(1)
  pTipoDomicilio               CHAR(1)
  pTipoProceso                 CHAR(1)
  pNombreArchivo               CHAR(100)
  pProdUpgrade                 CHAR(4)
  pError                       CHAR(1)
  pMensajeError                CHAR(120)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pCredito` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pTit` | `CHAR(3)` | — | — |
| `pNombre` | `CHAR(107)` | — | — |
| `pEmbozado` | `CHAR(21)` | — | — |
| `pMaster` | `CHAR(1)` | — | — |
| `pTipoDomicilio` | `CHAR(1)` | — | — |
| `pTipoProceso` | `CHAR(1)` | — | — |
| `pNombreArchivo` | `CHAR(100)` | `archivo`=archivo | ✅ CÓDIGO |
| `pProdUpgrade` | `CHAR(4)` | — | — |
| `pError` | `CHAR(1)` | — | — |
| `pMensajeError` | `CHAR(120)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(100)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iExiste` | `SMALLINT` | L11 |
| `cMensajeError` | `CHAR(120)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cred_cambioproducto` | `bdicnweb` | no | INSERT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_grabadetallearchivotdcoro` | `bdicred` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_grabadetallearchivot` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_lecturarchivodatostdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_lecturarchivodatostdc.sql` |
| **LOC (1er CREATE)** | 1062 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "código postal, archivo, datos y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cp_validacaractertdc`, `sp_grabadetallearchivotdc` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_lecturarchivodatostdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pRutaArchivo                 CHAR(100)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pRutaArchivo` | `CHAR(100)` | `archivo`=archivo | ✅ CÓDIGO |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDesCodRetSp` | `CHAR(100)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cRenglon` | `CHAR(400)` | L12 |
| `iLinea` | `INTEGER` | L13 |
| `cCampo` | `CHAR(35)` | L14 |
| `cDesMensajeError` | `CHAR(120)` | L15 |
| `iContador` | `INTEGER` | L16 |
| `cObservaciones` | `CHAR(50)` | L18 |
| `bBanderaError` | `CHAR(1)` | L19 |
| `sEnTransacc` | `SMALLINT` | L20 |
| `cSqlerr` | `INTEGER` | L21 |
| `cIsamErr` | `INTEGER` | L22 |
| `cDescErr` | `CHAR(50)` | L23 |
| `cSQL` | `CHAR(500)` | L24 |
| `iNoProcesado` | `INTEGER` | L25 |
| `bInTransaction` | `BOOLEAN` | L26 |
| `ven_transacc` | `SMALLINT` | L27 |
| `cPathdbaccess` | `CHAR(20)` | L28 |
| `iNroSecuencia` | `INTEGER` | L29 |
| `iNroLinea` | `INTEGER` | L30 |
| *…37 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statuslecturaarchivotdc` | `bdicnweb` | no | SELECT | L187 |
| `sw_cp_statuslecturaarchivotdc` | `bdicnweb` | no | DELETE | L187 |
| `sw_cp_statuslecturaarchivotdc` | `bdicnweb` | no | INSERT | L191 |
| `systables` | `bdicnweb` | no | SELECT | L204 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L236 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | DELETE | L236 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | SELECT | L237 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | DELETE | L237 |
| `sw_cp_cargaarchivotdc_tmp` | `bdicnweb` | no | INSERT | L252 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | INSERT | L270 |
| `sw_cp_cargaarchivotdc_tmp` | `bdicnweb` | no | SELECT | L275 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | INSERT | L330 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L354 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L363 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | UPDATE | L371 |
| `si_tipo_dir_upg` | `bdinteg` | ⚠️ sí | SELECT | L853 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L938 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L176 |
| `sp_cp_validacaractertdc` | `bdicnweb` | no | L335 |
| `sp_grabadetallearchivotdc` | `bdicred` | ⚠️ sí | L375 |
| `sp_consulta_prod_upgrade` | `bdicred` | ⚠️ sí | L756 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L166 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L247 | FÓRMULA | `LET cSQL = "/usr/bin/rm -rf "\|\|TRIM(pRutaArchivo)\|\|TRIM(pNombrearchivo)\|\|'; /usr/bin/mv '\|\|T` |  |
| L252 | FÓRMULA | `LET cSQL = "/usr/bin/echo 'SET ISOLATION TO DIRTY READ; LOAD FROM "\|\|TRIM(pRutaArchivo)\|\|TRIM(pN` |  |
| L253 | FÓRMULA | `LET cSQL = TRIM(cSQL)\|\|"num_credito,num_tarjeta,prod_destino,domicilio_envio,sucursal,aceptacion)'` |  |
| L323 | FÓRMULA | `LET iLinea = iLinea + 1;` |  |
| L380 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L383 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L394 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L411 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L414 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L425 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L455 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L458 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L469 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L524 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L527 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L538 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L558 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L561 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L572 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L592 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L595 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L606 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L626 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L629 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L640 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L663 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L666 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L677 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| | *…18 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_lectur` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_lectur` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_lecturarchivodatostdcrep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_lecturarchivodatostdcrep.sql` |
| **LOC (1er CREATE)** | 922 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte código postal, archivo, datos y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cp_validacaractertdc`, `sp_grabadetallearchivotdc` |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_lecturarchivodatostdcrep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pRutaArchivo                 CHAR(100)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pRutaArchivo` | `CHAR(100)` | `archivo`=archivo | ✅ CÓDIGO |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDesCodRetSp` | `CHAR(100)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cRenglon` | `CHAR(400)` | L12 |
| `iLinea` | `INTEGER` | L13 |
| `cCampo` | `CHAR(35)` | L14 |
| `cDesMensajeError` | `CHAR(120)` | L15 |
| `iContador` | `INTEGER` | L16 |
| `cObservaciones` | `CHAR(50)` | L18 |
| `bBanderaError` | `CHAR(1)` | L19 |
| `sEnTransacc` | `SMALLINT` | L20 |
| `cSqlerr` | `INTEGER` | L21 |
| `cIsamErr` | `INTEGER` | L22 |
| `cDescErr` | `CHAR(50)` | L23 |
| `cSQL` | `CHAR(500)` | L24 |
| `iNoProcesado` | `INTEGER` | L25 |
| `bInTransaction` | `BOOLEAN` | L26 |
| `ven_transacc` | `SMALLINT` | L27 |
| `cPathdbaccess` | `CHAR(20)` | L28 |
| `iNroSecuencia` | `INTEGER` | L29 |
| `iNroLinea` | `INTEGER` | L30 |
| *…38 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statuslecturaarchivotdc` | `bdicnweb` | no | SELECT | L183 |
| `sw_cp_statuslecturaarchivotdc` | `bdicnweb` | no | DELETE | L183 |
| `sw_cp_statuslecturaarchivotdc` | `bdicnweb` | no | INSERT | L187 |
| `systables` | `bdicnweb` | no | SELECT | L198 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L216 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | DELETE | L216 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | SELECT | L217 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | DELETE | L217 |
| `sw_cp_cargaarchivotdcrep_tmp` | `bdicnweb` | no | INSERT | L232 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | INSERT | L253 |
| `sw_cp_cargaarchivotdcrep_tmp` | `bdicnweb` | no | SELECT | L258 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | INSERT | L311 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L335 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L345 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | UPDATE | L353 |
| `solicitudtarjeta` | `intercard` | ⚠️ sí | SELECT | L415 |
| `si_tipo_dir_upg` | `bdinteg` | ⚠️ sí | SELECT | L717 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L800 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L172 |
| `sp_cp_validacaractertdc` | `bdicnweb` | no | L316 |
| `sp_grabadetallearchivotdc` | `bdicred` | ⚠️ sí | L357 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L162 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L227 | FÓRMULA | `LET cSQL = "/usr/bin/rm -rf "\|\|TRIM(pRutaArchivo)\|\|TRIM(pNombrearchivo)\|\|'; /usr/bin/mv '\|\|T` |  |
| L232 | FÓRMULA | `LET cSQL = "/usr/bin/echo 'SET ISOLATION TO DIRTY READ; LOAD FROM "\|\|TRIM(pRutaArchivo)\|\|TRIM(pN` |  |
| L233 | FÓRMULA | `LET cSQL = TRIM(cSQL)\|\|"num_credito,num_tarjeta,domicilio_envio,sucursal,aceptacion)' \| /ifxsif01` |  |
| L305 | FÓRMULA | `LET iLinea = iLinea + 1;` |  |
| L362 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L365 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L376 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L393 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L396 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L407 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L431 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L434 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L445 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L474 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L477 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L488 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L543 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L546 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L557 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L577 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L580 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L591 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L611 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L614 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L625 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L645 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L648 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L659 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| | *…11 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_lectur` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_lectur` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_lecturarchivodatostdctdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_lecturarchivodatostdctdcoro.sql` |
| **LOC (1er CREATE)** | 974 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "código postal, archivo, datos, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cp_validacaractertdc`, `sp_grabadetallearchivotdcoro` |
| **Evidencia vocab** | CÓDIGO=6 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_lecturarchivodatostdctdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pRutaArchivo                 CHAR(100)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pRutaArchivo` | `CHAR(100)` | `archivo`=archivo | ✅ CÓDIGO |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDesCodRetSp` | `CHAR(100)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cRenglon` | `CHAR(400)` | L12 |
| `iLinea` | `INTEGER` | L13 |
| `cCampo` | `CHAR(35)` | L14 |
| `cDesMensajeError` | `CHAR(120)` | L15 |
| `iContador` | `INTEGER` | L16 |
| `cObservaciones` | `CHAR(50)` | L18 |
| `bBanderaError` | `CHAR(1)` | L19 |
| `sEnTransacc` | `SMALLINT` | L20 |
| `cSqlerr` | `INTEGER` | L21 |
| `cIsamErr` | `INTEGER` | L22 |
| `cDescErr` | `CHAR(50)` | L23 |
| `cSQL` | `CHAR(500)` | L24 |
| `iNoProcesado` | `INTEGER` | L25 |
| `bInTransaction` | `BOOLEAN` | L26 |
| `ven_transacc` | `SMALLINT` | L27 |
| `cPathdbaccess` | `CHAR(20)` | L28 |
| `iNroSecuencia` | `INTEGER` | L29 |
| `iNroLinea` | `INTEGER` | L30 |
| *…37 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statuslecturaarchivotdctdcoro` | `bdicnweb` | no | UPDATE | L170 |
| `sw_cp_statuslecturaarchivotdctdcoro` | `bdicnweb` | no | SELECT | L189 |
| `sw_cp_statuslecturaarchivotdctdcoro` | `bdicnweb` | no | DELETE | L189 |
| `sw_cp_statuslecturaarchivotdctdcoro` | `bdicnweb` | no | INSERT | L193 |
| `systables` | `bdicnweb` | no | SELECT | L206 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L227 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | DELETE | L227 |
| `sw_cp_procesadetallearchivotdctdcoro` | `bdicnweb` | no | SELECT | L228 |
| `sw_cp_procesadetallearchivotdctdcoro` | `bdicnweb` | no | DELETE | L228 |
| `sw_cp_cargaarchivotdctdcoro_tmp` | `bdicnweb` | no | INSERT | L243 |
| `sw_cp_cargaarchivotdctdcoro_tmp` | `bdicnweb` | no | SELECT | L262 |
| `sw_cp_procesadetallearchivotdctdcoro` | `bdicnweb` | no | INSERT | L270 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | INSERT | L301 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L325 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L334 |
| `sw_cp_procesadetallearchivotdctdcoro` | `bdicnweb` | no | UPDATE | L342 |
| `si_tipo_dir_upg` | `bdinteg` | ⚠️ sí | SELECT | L847 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L178 |
| `sp_cp_validacaractertdc` | `bdicnweb` | no | L306 |
| `sp_grabadetallearchivotdcoro` | `bdicred` | ⚠️ sí | L346 |
| `sp_consulta_prod_upgrade` | `bdicred` | ⚠️ sí | L728 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L238 | FÓRMULA | `LET cSQL = "/usr/bin/rm -rf "\|\|TRIM(pRutaArchivo)\|\|TRIM(pNombrearchivo)\|\|'; /usr/bin/mv '\|\|T` |  |
| L243 | FÓRMULA | `LET cSQL = "/usr/bin/echo 'SET ISOLATION TO DIRTY READ; LOAD FROM "\|\|TRIM(pRutaArchivo)\|\|TRIM(pN` |  |
| L244 | FÓRMULA | `LET cSQL = TRIM(cSQL)\|\|"num_credito,num_tarjeta,prod_destino,domicilio_envio,marca)' \| /ifxsif01/` |  |
| L294 | FÓRMULA | `LET iLinea = iLinea + 1;` |  |
| L351 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L354 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L365 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L382 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L385 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L396 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L426 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L429 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L440 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L496 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L499 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L510 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L530 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L533 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L544 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L564 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L567 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L578 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L598 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L601 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L612 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| L635 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_grabadetallearchivo` |  |
| L638 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L649 | FÓRMULA | `LET iLineaError_Rep = iLineaError_Rep + 1;` |  |
| | *…15 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_lectur` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_lectur` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_limpiatablas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_limpiatablas.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 79 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "código postal y Impago — pago vencido o fallido; confirmado: n_impagos_consec" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_limpiatablas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
  pIdEjecucion                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | SELECT | L40 |
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | DELETE | L41 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L51 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | DELETE | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?iatablas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_l`, `?iatablas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_limpiatablastdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_limpiatablastdcoro.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "código postal, Impago — pago vencido o fallido; confirmado: n_impagos_consec, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_limpiatablastdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
  pIdEjecucion                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | SELECT | L40 |
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | DELETE | L41 |
| `sd_rep_detallearchivotdc` | `bdicred` | ⚠️ sí | SELECT | L48 |
| `sd_rep_detallearchivotdc` | `bdicred` | ⚠️ sí | DELETE | L49 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | SELECT | L59 |
| `sw_cp_bitacoraerrortdc` | `bdicnweb` | no | DELETE | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?iatablas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_l`, `?iatablas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_mostrar_grid_upgrade2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_mostrar_grid_upgrade2.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=38 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza producto código postal, OS — Originación de Solicitudes y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_mostrar_grid_upgrade2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_mostrar_grid_upgrade2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolCred                  CHAR(20)
  pFlagAdicional               CHAR(1)
  pNumcte                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumSolCred` | `CHAR(20)` | — | — |
| `pFlagAdicional` | `CHAR(1)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |
| `cNumcredito` | `CHAR(20)` | L19 |
| `cstatus` | `CHAR(2)` | L20 |
| `cTipotarjeta` | `CHAR(3)` | L21 |
| `cNombre` | `CHAR(30)` | L22 |
| `cNomEmbozado` | `CHAR(21)` | L23 |
| `cTarjeta` | `CHAR(20)` | L24 |
| `cNumcte` | `CHAR(20)` | L25 |
| `iNumRegistros` | `INTEGER` | L26 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L67 |
| `sp_mostrar_grid_upgrade2` | `bdicred` | ⚠️ sí | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_mostrar_grid_upgrade2";` |  |
| L81 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?trar_gr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `upgrade` | ACCION | actualiza producto (upgrade) | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?trar_gr`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_mostrar_grid_upgrade2_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_mostrar_grid_upgrade2_totales.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=39 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza producto código postal, OS — Originación de Solicitudes y identificador (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_mostrar_grid_upgrade2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_mostrar_grid_upgrade2_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolCred                  CHAR(20)
  pFlagAdicional               CHAR(1)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumSolCred` | `CHAR(20)` | — | — |
| `pFlagAdicional` | `CHAR(1)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_mostrar_grid_upgrade2_totales` | `bdicred` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_mostrar_grid_upgrade2_totales` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?trar_gr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `upgrade` | ACCION | actualiza producto (upgrade) | 🟡 INFERIDO | nombre_sp |
| `?2_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?trar_gr`, `?2_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_nombreembozado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_nombreembozado.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=40 |
| **Deps concatenadas** | 65 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "código postal y nombre" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_nom_embozado_upgrade` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_nombreembozado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cNombre1` | `CHAR(26)` | L18 |
| `cNombre2` | `CHAR(26)` | L19 |
| `capell_paterno` | `CHAR(26)` | L20 |
| `capell_materno` | `CHAR(26)` | L21 |
| `cNom1_ini` | `CHAR(1)` | L22 |
| `cNom2_ini` | `CHAR(1)` | L23 |
| `cApellPat_ini` | `CHAR(1)` | L24 |
| `cApellMat_ini` | `CHAR(1)` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |
| `sp_nom_embozado_upgrade` | `bdicred` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP: bdicred:sp_nom_embozado_upgrade";` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `nombre` | ENTIDAD | nombre | 🔵 CONVENCIÓN | nombre_sp |
| `?embozado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?embozado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_obtensolicitudmaquilatdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_obtensolicitudmaquilatdc.sql` |
| **LOC (1er CREATE)** | 188 |
| **Callgraph** | ✅ fan_in=0 / fan_out=43 |
| **Deps concatenadas** | 72 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene código postal, solicitud, maquila — proceso de externalización de solicitudes TDC y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtensolicitudmaquilatdc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_obtensolicitudmaquilatdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pNumTarjeta                  CHAR(20)
  pTipoEnvio                   CHAR (1)
  pSucursal                    CHAR(5)
  pTpoMaquila                  CHAR(1)
  pTpoEjecucion                CHAR(1)
  pEjecutivo                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pTipoEnvio` | `CHAR (1)` | — | — |
| `pSucursal` | `CHAR(5)` | — | — |
| `pTpoMaquila` | `CHAR(1)` | `maquila`=maquila — proceso de externalización de solicitudes TDC | 🟡 INFERIDO |
| `pTpoEjecucion` | `CHAR(1)` | — | — |
| `pEjecutivo` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L41 |
| `iSqlErr` | `INTEGER` | L42 |
| `cCodRetSp` | `CHAR(6)` | L43 |
| `iCodRetSp` | `INTEGER` | L44 |
| `cEmpresa` | `CHAR(3)` | L45 |
| `ctpoenvio` | `CHAR(1)` | L46 |
| `csucursal` | `CHAR(5)` | L47 |
| `ctpomaquila` | `CHAR(1)` | L48 |
| `cnumcte` | `CHAR(20)` | L49 |
| `cnum_credito` | `CHAR(20)` | L50 |
| `cnomcliente1` | `CHAR(20)` | L51 |
| `cnomcliente2` | `CHAR(20)` | L52 |
| `capellpatcliente` | `CHAR(20)` | L53 |
| `capellmatcliente` | `CHAR(20)` | L54 |
| `cnombre_embosado` | `CHAR(21)` | L55 |
| `cdir_calle1` | `CHAR(30)` | L56 |
| `cdir_calle2` | `CHAR(30)` | L57 |
| `cdir_colonia` | `CHAR(50)` | L58 |
| `cdir_municipio` | `CHAR(15)` | L59 |
| `cdir_estado` | `CHAR(13)` | L60 |
| `cdir_cp` | `CHAR(5)` | L61 |
| `ctipotarjeta` | `CHAR(2)` | L62 |
| `cbintarjeta` | `CHAR(6)` | L63 |
| `ccodproducto` | `CHAR(3)` | L64 |
| `cfimagen` | `CHAR(1)` | L65 |
| *…19 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L154 |
| `sp_obtensolicitudmaquilatdc` | `bdisolic` | ⚠️ sí | L162 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L171 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP: bdisolic:sp_obtensolicitudmaquilatdc";` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `maquila` | ENTIDAD | maquila — proceso de externalización de solicitudes TDC | 🟡 INFERIDO | nombre_sp |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cp_rep_prod_upgrade2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_rep_prod_upgrade2.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte código postal y producto" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rep_prod_upgrade2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_rep_prod_upgrade2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pTipo                        CHAR(1)
  pStatus                      CHAR(1)
  pArchivo                     CHAR(50)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pArchivo` | `CHAR(50)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(6)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |
| `cNombreEmbozado` | `CHAR(100)` | L19 |
| `cNumCredito` | `CHAR(20)` | L20 |
| `cTipoTarjeta` | `CHAR(10)` | L21 |
| `cMiembro` | `CHAR(2)` | L22 |
| `dFecha` | `DATE` | L23 |
| `cResultado` | `CHAR(15)` | L24 |
| `cDescripcion` | `CHAR(100)` | L25 |
| `cNumTarjeta` | `CHAR(20)` | L26 |
| `cMarcaje` | `CHAR(3)` | L27 |
| `cSolPlastico` | `CHAR(2)` | L28 |
| `cMensajeError` | `CHAR(100)` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |
| `sp_rep_prod_upgrade2` | `bdicred` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L83 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_rep_prod_upgrade2";` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00973';` |  |
| L89 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `upgrade` | ACCION | actualiza producto (upgrade) | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_rep_prod_upgrade2_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_rep_prod_upgrade2_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=41 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte código postal y producto (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rep_prod_upgrade2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_rep_prod_upgrade2_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pTipo                        CHAR(1)
  pStatus                      CHAR(1)
  pArchivo                     CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pArchivo` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cDescMensaje` | `CHAR(100)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_rep_prod_upgrade2_totales` | `bdicred` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_rep_prod_upgrade2_totales";` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `upgrade` | ACCION | actualiza producto (upgrade) | 🟡 INFERIDO | nombre_sp |
| `?2_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_rep_status_upgrade`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_rep_status_upgrade.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 67 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte código postal y estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rep_status_upgrade` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_rep_status_upgrade(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNumParametro` | `INTEGER` | L10 |
| `ccStatus` | `CHAR(100)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_rep_status_upgrade` | `bdicred` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_rep_status_upgrade";` |  |
| L52 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `upgrade` | ACCION | actualiza producto (upgrade) | 🟡 INFERIDO | nombre_sp |

---

## `sp_cp_statuscredito`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_statuscredito.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "código postal, estatus y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_statuscredito(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoCredito                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoCredito` | `CHAR(20)` | `credito`=crédito | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cStatus` | `CHAR(2)` | L11 |
| `cSiglas` | `CHAR(2)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_updatedatoscuentastdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_updatedatoscuentastdc.sql` |
| **LOC (1er CREATE)** | 215 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza código postal, datos, cuentas y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_updatedatoscuentastdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(12)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDesCodRetSp` | `CHAR(100)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cRenglon` | `CHAR(400)` | L12 |
| `iLinea` | `INTEGER` | L13 |
| `cCampo` | `CHAR(35)` | L14 |
| `cDesMensajeError` | `CHAR(120)` | L15 |
| `iContador` | `INTEGER` | L16 |
| `cBanDetError` | `CHAR(1)` | L17 |
| `cNumCredito_Sol` | `CHAR(20)` | L19 |
| `cNumCredito_Up` | `CHAR(20)` | L21 |
| `cNumTarjeta_Up` | `CHAR(20)` | L22 |
| `cMaster_Up` | `CHAR(1)` | L23 |
| `cDomicilio_Up` | `CHAR(1)` | L24 |
| `cSucursal_Up` | `CHAR(4)` | L25 |
| `cProdDestino_Up` | `CHAR(4)` | L26 |
| `cDescProdDestino_Up` | `CHAR(40)` | L27 |
| `cNumCredito_sp` | `CHAR(20)` | L28 |
| `cStatusCred_sp` | `CHAR(2)` | L29 |
| `cTipoTarjeta_sp` | `CHAR(3)` | L30 |
| `cNomCliente_sp` | `CHAR(30)` | L31 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statuslecturadatosctastdc` | `bdicnweb` | no | SELECT | L112 |
| `sw_cp_statuslecturadatosctastdc` | `bdicnweb` | no | DELETE | L112 |
| `sw_cp_statuslecturadatosctastdc` | `bdicnweb` | no | INSERT | L116 |
| `sw_cp_datosctastdc` | `bdicnweb` | no | SELECT | L120 |
| `sw_cp_datosctastdc` | `bdicnweb` | no | DELETE | L120 |
| `sw_cred_cambioproducto` | `bdicnweb` | no | SELECT | L122 |
| `sw_cred_cambioproducto` | `bdicnweb` | no | DELETE | L122 |
| `sw_cp_procesadetallearchivotdc` | `bdicnweb` | no | SELECT | L128 |
| `sw_cp_datosctastdc` | `bdicnweb` | no | INSERT | L133 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L171 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L101 |
| `sp_mostrar_grid_upgrade` | `bdicred` | ⚠️ sí | L142 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L181 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `upd` | ACCION | actualiza (update) | 🟡 INFERIDO | nombre_sp |
| `?ate` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ate` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_validacaractertdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_validacaractertdc.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=120 / fan_out=42 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida código postal y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_validacaractertdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCadena                      CHAR(500)
  pTipoCompara                 CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCadena` | `CHAR(500)` | — | — |
| `pTipoCompara` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cTamCadena` | `CHAR(500)` | L7 |
| `iPos` | `INTEGER` | L8 |
| `cComparaCadena` | `CHAR(65)` | L9 |
| `cCaracter` | `CHAR(1)` | L10 |
| `cCaracterInvalido` | `CHAR(1)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET iPos = iPos + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?car` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?er` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?car`, `?er` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_validacargaarchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_validacargaarchivo.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida código postal y archivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_validacargaarchivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `cDesCodRetSp` | `CHAR(80)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iExiste` | `SMALLINT` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_rep_detallearchivotdc` | `bdicred` | ⚠️ sí | SELECT | L43 |
| `sd_credito_upgrade` | `bdicred` | ⚠️ sí | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_validacargaarchivotdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_validacargaarchivotdcoro.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida código postal, archivo, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_validacargaarchivotdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `cDesCodRetSp` | `CHAR(80)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iExiste` | `SMALLINT` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_rep_detallearchivotdc` | `bdicred` | ⚠️ sí | SELECT | L45 |
| `sd_credito_upgrade` | `bdicred` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_validaconsecutivonomarchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_validaconsecutivonomarchivo.sql` |
| **LOC (1er CREATE)** | 143 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida código postal, consecutivo, nómina y archivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_validaconsecutivonomarchivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
  pConsecutivo                 CHAR(5)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pConsecutivo` | `CHAR(5)` | `consecutivo`=consecutivo | ✅ CÓDIGO |
| `pNombreArchivo` | `CHAR(35)` | `nom`=nómina · `archivo`=archivo | 🟡 INFERIDO / ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNomArchivo` | `CHAR(35)` | L11 |
| `cNomArcValido` | `CHAR(35)` | L12 |
| `dFecha` | `DATE` | L13 |
| `iConsDisponible` | `INTEGER` | L14 |
| `cConsInvalido` | `CHAR(1)` | L15 |
| `iExiste` | `INTEGER` | L16 |
| `dtFecha` | `DATETIME YEAR TO SECOND` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_credito_upgrade` | `bdicred` | ⚠️ sí | SELECT | L63 |
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | SELECT | L68 |
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | DELETE | L68 |
| `sd_rep_detallearchivotdc` | `bdicred` | ⚠️ sí | SELECT | L70 |
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | INSERT | L108 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | FÓRMULA | `LET dFecha = SUBSTRING(TRIM(pFecha) FROM 5 FOR 2)\|\|'/'\|\|SUBSTRING(TRIM(pFecha) FROM 7 FOR 2) \|\` |  |
| L84 | FÓRMULA | `LET dFecha = SUBSTRING(TRIM(pFecha) FROM 5 FOR 2)\|\|'/'\|\|SUBSTRING(TRIM(pFecha) FROM 7 FOR 2) \|\` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `consecutivo` | ENTIDAD | consecutivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_validaconsecutivonomarchivotdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_validaconsecutivonomarchivotdcoro.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida código postal, consecutivo, nómina, archivo, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_validaconsecutivonomarchivotdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
  pConsecutivo                 CHAR(5)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pConsecutivo` | `CHAR(5)` | `consecutivo`=consecutivo | ✅ CÓDIGO |
| `pNombreArchivo` | `CHAR(35)` | `nom`=nómina · `archivo`=archivo | 🟡 INFERIDO / ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDesCodRetSp` | `CHAR(80)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNomArchivo` | `CHAR(35)` | L11 |
| `cNomArcValido` | `CHAR(35)` | L12 |
| `dFecha` | `DATE` | L13 |
| `iConsDisponible` | `INTEGER` | L14 |
| `cConsInvalido` | `CHAR(1)` | L15 |
| `iExiste` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | SELECT | L63 |
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | DELETE | L63 |
| `sd_rep_detallearchivotdc` | `bdicred` | ⚠️ sí | SELECT | L65 |
| `sd_credito_upgrade` | `bdicred` | ⚠️ sí | SELECT | L66 |
| `sw_cp_nomarchivotdc` | `bdicnweb` | no | INSERT | L81 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | FÓRMULA | `LET dFecha = SUBSTRING(TRIM(pFecha) FROM 5 FOR 2)\|\|'/'\|\|SUBSTRING(TRIM(pFecha) FROM 7 FOR 2) \|\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `consecutivo` | ENTIDAD | consecutivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_verificastatusarchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_verificastatusarchivo.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica código postal, estatus y archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_verificastatusarchivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cStatus` | `CHAR(1)` | L13 |
| `cBanDetError` | `CHAR(1)` | L14 |
| `cErrorProceso` | `CHAR(1)` | L15 |
| `cError` | `CHAR(5)` | L16 |
| `iTotal` | `INTEGER` | L17 |
| `iProcesados` | `INTEGER` | L18 |
| `iNoProcesados` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statuslecturaarchivotdc` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cp_verificastatusarchivotdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_verificastatusarchivotdcoro.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica código postal, estatus, archivo, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_verificastatusarchivotdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cStatus` | `CHAR(1)` | L13 |
| `cBanDetError` | `CHAR(1)` | L14 |
| `cErrorProceso` | `CHAR(1)` | L15 |
| `cError` | `CHAR(5)` | L16 |
| `iTotal` | `INTEGER` | L17 |
| `iProcesados` | `INTEGER` | L18 |
| `iNoProcesados` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statuslecturaarchivotdctdcoro` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_verificastatusreptdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_verificastatusreptdcoro.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica código postal, estatus, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_verificastatusreptdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(40)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cStatus` | `CHAR(1)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cErrorProceso` | `CHAR(1)` | L16 |
| `cError` | `CHAR(5)` | L17 |
| `iTotal` | `INTEGER` | L18 |
| `iProcesados` | `INTEGER` | L19 |
| `iNoProcesados` | `INTEGER` | L20 |
| `cNombre_Arch` | `CHAR(40)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statusgenrepstdcoro2` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cp_verificastatusupdate`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_verificastatusupdate.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica código postal y estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_verificastatusupdate(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cBanDetError` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statuslecturadatosctastdc` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `upd` | ACCION | actualiza (update) | 🟡 INFERIDO | nombre_sp |
| `?ate` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ate` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cp_verificastatusupdatetdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cp_verificastatusupdatetdcoro.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica código postal, estatus, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cp_verificastatusupdatetdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cBanDetError` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cp_statuslecturaarchivotdctdcoro` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `upd` | ACCION | actualiza (update) | 🟡 INFERIDO | nombre_sp |
| `?ate` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ate` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cr_consultasemaforo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cr_consultasemaforo.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=73 |
| **Deps concatenadas** | 84 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cr_consultasemaforo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCred                     CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCred` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cSemaforo` | `CHAR(2)` | L7 |
| `cProd` | `CHAR(4)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L31 |
| `sd_ctes_retencion` | `bdicred` | ⚠️ sí | SELECT | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?semaf` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cr_`, `?semaf` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_capturactecreditocoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_capturactecreditocoppel.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "captura crédito, cliente y Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_capturactecreditocoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pNumCte                      CHAR(20)
  pNombre                      CHAR(104)
  pSucursal                    CHAR(4)
  pFechaSolicitud              CHAR(20)
  pNumAut1                     CHAR(8)
  pNumAut2                     CHAR(8)
  pStatus                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pNombre` | `CHAR(104)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaSolicitud` | `CHAR(20)` | — | — |
| `pNumAut1` | `CHAR(8)` | — | — |
| `pNumAut2` | `CHAR(8)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `vNomAut1` | `CHAR(80)` | L6 |
| `vNomAut2` | `CHAR(80)` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `DfechaSol` | `DATE` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_solicitudes` | `bdisolic` | ⚠️ sí | SELECT | L53 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L61 |
| `ss_clientes_exentos_rgc` | `bdisolic` | ⚠️ sí | INSERT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `captura` | ACCION | captura | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cre_catalogoproductoscat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_catalogoproductoscat.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "crédito, catálogo y productos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_catalogoproductoscat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cIdProducto` | `CHAR(4)` | L10 |
| `cDescProducto` | `CHAR(40)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cre_catalogostatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_catalogostatus.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "crédito, catálogo y estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_catalogostatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cStatus` | `CHAR(2)` | L12 |
| `cDescripcion` | `CHAR(40)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_status_sol` | `bdisolic` | ⚠️ sí | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_cre_consultabuscproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultabuscproductos.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 106 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, código postal, ro — Rol Operativo y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultabuscproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
  pNomProducto                 CHAR(40)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pNomProducto` | `CHAR(40)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cNumProducto` | `CHAR(4)` | L13 |
| `cNomProducto` | `CHAR(40)` | L14 |
| `cNumSubProducto` | `INTEGER` | L15 |
| `cNomSubproducto` | `CHAR(40)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?bus` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?duct` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bus`, `ro`, `?duct` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_consultadivisionescat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultadivisionescat.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, división y catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultardivisiones` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultadivisionescat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cMensajeRet` | `CHAR(80)` | L12 |
| `cIdDivision` | `INTEGER` | L13 |
| `cDescDivision` | `CHAR(10)` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_consultardivisiones` | `bdicobranza` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicobranza:sp_consultardivisiones';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L62 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `division` | ENTIDAD | división | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_consultaestatuscoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultaestatuscoppel.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=35 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, estatus y Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultaestatuscoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cStatus` | `CHAR(1)` | L7 |
| `cNumCte` | `CHAR(20)` | L8 |
| `cFecha` | `CHAR(20)` | L9 |
| `cSecuencia` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_clientes_exentos_rgc` | `bdisolic` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cre_consultafechacorteproducto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultafechacorteproducto.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, fecha, corte y producto" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultafechacorteproducto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | `producto`=producto | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iDiaCorte` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_productodiacorte` | `bdicnweb` | no | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `corte` | ENTIDAD | corte (fecha de corte / período) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `producto` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cre_consultanumsolicitudcoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultanumsolicitudcoppel.sql` |
| **LOC (1er CREATE)** | 223 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, número de solicitud y Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultanumsolicitudcoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `numsolicitud`=número de solicitud | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cNumCte` | `CHAR(20)` | L18 |
| `cNoCte` | `CHAR(20)` | L19 |
| `cNumSolicitud` | `CHAR(20)` | L20 |
| `cNoSolicitud` | `CHAR(20)` | L21 |
| `cNombre` | `CHAR(90)` | L22 |
| `cSucursal` | `CHAR(4)` | L23 |
| `dFechaSolicitud` | `DATE` | L24 |
| `dFechaCambioSolicitud` | `DATE` | L25 |
| `cStatusSolicitud` | `CHAR(2)` | L26 |
| `cCausaSolicitud` | `CHAR(3)` | L27 |
| `iNoRegistros` | `INTEGER` | L28 |
| `vNumProducto` | `CHAR(4)` | L29 |
| `vFechaSolictudUltima` | `CHAR(25)` | L30 |
| `vNumSolicitud` | `CHAR(20)` | L31 |
| `cBandera` | `CHAR(1)` | L32 |
| `cStatus` | `CHAR(1)` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_solicitudes` | `bdisolic` | ⚠️ sí | SELECT | L83 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L156 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L72 |
| `sp_cre_consultaestatuscoppel` | `bdicnweb` | no | L120 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L124 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_cre_consultaestatuscoppel';` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L146 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L159 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L177 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `numsolicitud` | ENTIDAD | número de solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cre_consultaobtcanalorigencat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultaobtcanalorigencat.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, canal, origen y catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtcanalorigen` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultaobtcanalorigencat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumParam                    INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumParam` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(3)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeRet` | `CHAR(85)` | L10 |
| `dIdValorAlfabetico` | `DECIMAL(18,2)` | L11 |
| `cDesValorAlfabetico` | `CHAR(100)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_obtcanalorigen` | `bdicobranza` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pNumParam IS NULL THEN` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicobranza:sp_obtcanalorigen ";` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `canal` | ENTIDAD | canal (de distribución) | 🔵 CONVENCIÓN | nombre_sp |
| `origen` | ENTIDAD | origen | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cre_consultaperfilusuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultaperfilusuario.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, perfil de usuario y usuario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_valida_perfil_usuario` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultaperfilusuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iTipoPerfil` | `SMALLINT` | L12 |
| `cDescPerfil` | `CHAR(10)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_valida_perfil_usuario` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdinteg:sp_valida_perfil_usuari` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cre_consultaplantillamnsj`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultaplantillamnsj.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 105 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito y plantilla" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultaplantillamnsj(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     SMALLINT
  pIdEvento                    SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `SMALLINT` | — | — |
| `pIdEvento` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `iCodPlantilla` | `SMALLINT` | L13 |
| `cCodMsmMail` | `CHAR(50)` | L14 |
| `cTipoPlantilla` | `CHAR(20)` | L15 |
| `cMsjPlantilla` | `CHAR(10000)` | L16 |
| `v_ya_existe` | `SMALLINT` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_tipo_plantilla` | `bdicred` | ⚠️ sí | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pBandera IS NULL AND pIdEvento IS NULL THEN` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `plantilla` | ENTIDAD | plantilla | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `mnsj` | PREFIJO | mensajería / notificaciones (dominio bdimnsj) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cre_consultaregionescat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultaregionescat.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar crédito y catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarregiones` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultaregionescat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDivicion                    SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDivicion` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cIdRegion` | `SMALLINT` | L11 |
| `cDescREgistros` | `CHAR(30)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_consultarregiones` | `bdicobranza` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pDivicion IS NULL  THEN` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicobranza:sp_consultarregiones';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L59 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?egiones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?egiones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_consultastatusproceso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultastatusproceso.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, estatus y proceso" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultastatusproceso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdStatus` | `CHAR(1)` | L8 |
| `cDescStatus` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_tipostatus` | `bdicnweb` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cre_consultasucursalescat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultasucursalescat.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, sucursal y catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarsucursales2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultasucursalescat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegion                      SMALLINT
  pSucursal                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegion` | `SMALLINT` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cIdSucursal` | `CHAR(4)` | L11 |
| `cDescSucursal` | `CHAR(40)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_consultarsucursales2` | `bdicobranza` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegion IS NULL OR pRegistros IS NULL OR pRecuperacion IS NUL` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicobranza:sp_consultarsucursales2';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00105';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_consultatiporeporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultatiporeporte.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito y reporte (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultatiporeporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensajeRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `cDescArchivo` | `CHAR(80)` | L13 |
| `iIdArchivo` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_compac_tipo_reporte_tmp` | `bdicnweb` | no | SELECT | L52 |
| `sw_compac_tipo_reporte_tmp` | `bdicnweb` | no | DELETE | L52 |
| `sw_compac_tipo_reporte_tmp` | `bdicnweb` | no | INSERT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_compac_tipo_reporte` | `bdicobranza` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicobranza:sp_compac_tipo_reporte';` |  |
| L76 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cre_consultavalidapermisoscoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_consultavalidapermisoscoppel.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, OS — Originación de Solicitudes y Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_validarpermisousuariocac2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_consultavalidapermisoscoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecutivo                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iRecuperacion` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |
| `sp_validarpermisousuariocac2` | `bdicred` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP baicred:sp_validarpermisousuariocac2';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00796';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00797';` |  |
| L49 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?permis` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?permis` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_detallestatussol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_detallestatussol.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "crédito, detalle, estatus y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `status_sol2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_detallestatussol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(6)` | L22 |
| `cDescCodRetSp` | `CHAR(80)` | L23 |
| `iCodRetSp` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `cNum_solicitud` | `CHAR(20)` | L26 |
| `cSucursal` | `CHAR(4)` | L27 |
| `cNom_sucursal` | `CHAR(40)` | L28 |
| `cNom_cliente` | `CHAR(104)` | L29 |
| `cStatus_solicitud` | `CHAR(2)` | L30 |
| `mMonto_solicitud` | `MONEY(14,2)` | L31 |
| `mMonto_otorgado` | `MONEY(14,2)` | L32 |
| `dFecha_alta` | `DATE` | L33 |
| `dFecha_cambio_status` | `DATE` | L34 |
| `dEficiencia_pago` | `DECIMAL(10,2)` | L35 |
| `iMeses_historial` | `SMALLINT` | L36 |
| `iScoring_1` | `SMALLINT` | L37 |
| `iScoring_2` | `SMALLINT` | L38 |
| `iTotal_scoring` | `SMALLINT` | L39 |
| `cCausa_rechazo` | `CHAR(10)` | L40 |
| `iRecuperacion` | `INTEGER` | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L95 |
| `status_sol2` | `bdicred` | ⚠️ sí | L106 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L112 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:status_sol2';` |  |
| L115 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |

---

## `sp_cre_detallestatussolaud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_detallestatussolaud.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "crédito, detalle, estatus, solicitud y auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_status_sol_aud2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=3 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_detallestatussolaud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `cDescCodRetSp` | `CHAR(80)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `cNum_solicitud` | `CHAR(20)` | L24 |
| `cNum_cliente` | `CHAR(20)` | L25 |
| `dFecha_alta` | `DATE` | L26 |
| `cNom_cliente` | `CHAR(104)` | L27 |
| `cStatus_solicitud` | `CHAR(2)` | L28 |
| `mMonto_solicitud` | `MONEY(14,2)` | L29 |
| `mMonto_otorgado` | `MONEY(14,2)` | L30 |
| `dFecha_cambio_status` | `DATE` | L31 |
| `cNom_promotor` | `CHAR(45)` | L32 |
| `cTelefono_particular` | `CHAR(13)` | L33 |
| `cTelefono_celular` | `CHAR(13)` | L34 |
| `cTelefono_oficina` | `CHAR(13)` | L35 |
| `cSucursal` | `CHAR(4)` | L36 |
| `iRecuperacion` | `INTEGER` | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |
| `sp_status_sol_aud2` | `bdicred` | ⚠️ sí | L96 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L101 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_status_sol_aud2';` |  |
| L104 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_cre_detallestatussolaudexcel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_detallestatussolaudexcel.sql` |
| **LOC (1er CREATE)** | 386 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 281 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "crédito, detalle, estatus, solicitud, auditoría y celular" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_status_sol_audexcel2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=1 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_detallestatussolaudexcel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L38 |
| `iSqlErr` | `INTEGER` | L39 |
| `cCodRetSp` | `CHAR(6)` | L40 |
| `cDescCodRetSp` | `CHAR(80)` | L41 |
| `iCodRetSp` | `INTEGER` | L42 |
| `cEmpresa` | `CHAR(3)` | L43 |
| `cNum_solicitud` | `CHAR(20)` | L44 |
| `cNum_cliente` | `CHAR(20)` | L45 |
| `dFecha_alta` | `DATE` | L46 |
| `cNom_cliente` | `CHAR(104)` | L47 |
| `cStatus_solicitud` | `CHAR(2)` | L48 |
| `mMonto_solicitud` | `MONEY(14,2)` | L49 |
| `mMonto_otorgado` | `MONEY(14,2)` | L50 |
| `dFecha_cambio_status` | `DATE` | L51 |
| `cNom_promotor_realizo` | `CHAR(45)` | L52 |
| `cNom_promotor_autorizo` | `CHAR(45)` | L53 |
| `cNom_promotor_entrego` | `CHAR(45)` | L54 |
| `cNom_promotor_asigno` | `CHAR(45)` | L55 |
| `cTelefono_particular` | `CHAR(13)` | L56 |
| `cTelefono_celular` | `CHAR(13)` | L57 |
| `cTelefono_oficina` | `CHAR(13)` | L58 |
| `cSucursal` | `CHAR(4)` | L59 |
| `cNom_referencia1` | `CHAR(104)` | L60 |
| `cTel_ref1_particular` | `CHAR(13)` | L61 |
| `cTel_ref1_celular` | `CHAR(13)` | L62 |
| *…18 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ctepf` | `bdinteg` | ⚠️ sí | SELECT | L238 |
| `si_bitacora_ife` | `bdinteg` | ⚠️ sí | SELECT | L248 |
| `si_cte_huella` | `bdinteg` | ⚠️ sí | SELECT | L258 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L286 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L298 |
| `sd_maecredanexo` | `bdicred` | ⚠️ sí | SELECT | L308 |
| `sd_maecredanexocrd` | `bdicred` | ⚠️ sí | SELECT | L317 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L170 |
| `sp_status_sol_audexcel2` | `bdicred` | ⚠️ sí | L187 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L143 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L145 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L196 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃ?N DEL SP bdicred:sp_status_sol_audexcel` |  |
| L199 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L205 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L326 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L343 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_genreporteconveniosifcat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_genreporteconveniosifcat.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte crédito, convenio, SIF — canal de estado de cuenta y catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rep_convenios_sif2_tmp` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_genreporteconveniosifcat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `cNombreArchivo` | `CHAR(80)` | L12 |
| `cMensajeRet` | `CHAR(80)` | L13 |
| `cRuta` | `CHAR(80)` | L14 |
| `bTransaccion` | `BOOLEAN` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |
| `sp_rep_convenios_sif2_tmp` | `bdicred` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechaInicio IS NULL OR pFechaFin IS NULL OR pProducto = '' ` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicobranza:sp_consultardivisiones';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sif` | ENTIDAD | SIF — canal de estado de cuenta (aclaraciones_edocta_sif, de | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_genreportevalidacionhuellacoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_genreportevalidacionhuellacoppel.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte crédito, huella biométrica y Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_genreportevalidacionhuellacoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIncio                  DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIncio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `dFecha` | `DATE` | L14 |
| `cNoSolicitud` | `CHAR(20)` | L15 |
| `cNoCte` | `CHAR(20)` | L16 |
| `cNombreCte` | `CHAR(104)` | L17 |
| `cSucursal` | `CHAR(4)` | L18 |
| `dFechaSol` | `DATE` | L19 |
| `cNombreAut1` | `CHAR(80)` | L20 |
| `cNombreAut2` | `CHAR(80)` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |
| `iRegistros` | `INTEGER` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_clientes_exentos_rgc` | `bdisolic` | ⚠️ sí | SELECT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaIncio IS NULL OR pFechaFin IS NULL OR pRegistros IS NUL` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L76 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `validacion` | ACCION | validación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `huella` | ENTIDAD | huella biométrica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_genreportevalidacionhuellacoppel_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_genreportevalidacionhuellacoppel_totales.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte crédito, huella biométrica y Coppel (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_genreportevalidacionhuellacoppel_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIncio                  DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIncio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_clientes_exentos_rgc` | `bdisolic` | ⚠️ sí | SELECT | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaIncio IS NULL OR pFechaFin IS NULL THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `validacion` | ACCION | validación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `huella` | ENTIDAD | huella biométrica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cre_verificastatus_detalle`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cre_verificastatus_detalle.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=54 |
| **Deps concatenadas** | 148 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica crédito, estatus y detalle" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cre_verificastatus_detalle(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOperacion                   CHAR(1)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOperacion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorSp` | `CHAR(6)` | L11 |
| `cOperacion` | `CHAR(1)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verifica_detallemarcaje` | `bdicnweb` | no | SELECT | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cred_caracteristicasproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_caracteristicasproductos.sql` |
| **LOC (1er CREATE)** | 249 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 84 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza crédito, Riesgo — módulo de gestión de riesgo crediticio, ro — Rol Operativo y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=5 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_caracteristicasproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pNumProducto                 CHAR(4)
  pMontoMinCred                DECIMAL(18,2)
  pMontoMaxCred                DECIMAL(18,2)
  pEdadMin                     INTEGER
  pEdadMax                     INTEGER
  pPlazoMinCred                INTEGER
  pPlazoMaxCred                INTEGER
  pIdFrecPago                  SMALLINT
  pCobroComisionAnual          CHAR(1)
  pComiGastoCobranza           SMALLINT
  pCobroComisApertura          CHAR(1)
  pComiDisposicionEfect        SMALLINT
  pComiAclaracionNo            SMALLINT
  pComiLiquidacionAntic        SMALLINT
  pCodComisionApertura         CHAR(4)
  pCodComiGastoCobranza        CHAR(4)
  pCodComisionAnualidad        CHAR(8)
  pCodComiDisposicionEfect     CHAR(4)
  pCodComiAclaracionNo         CHAR(4)
  pCodComiLiquidacionAntic     CHAR(4)
  pGarantias                   SMALLINT
  pIdGarantia                  SMALLINT
  pPorcentajeAforo             DECIMAL(16)
  pObligadoSolidario           CHAR(1)
  pNumObligados                CHAR(1)
  pCapturaObligatoria          CHAR(1)
  pMontoMinDisp                DECIMAL(18,2)
  pMontoMaxDisp                DECIMAL(18,2)
  pFormApliComiGasCob          CHAR(1)
  pFormApliComiAper            CHAR(1)
  pFormApliComiDispo           CHAR(1)
  pFormApliComiAnual           CHAR(1)
  pFormApliComiAclara          CHAR(1)
  pFormApliComiLiqAnt          CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pNumProducto` | `CHAR(4)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pMontoMinCred` | `DECIMAL(18,2)` | `cred`=crédito | 🔵 CONVENCIÓN |
| `pMontoMaxCred` | `DECIMAL(18,2)` | `cred`=crédito | 🔵 CONVENCIÓN |
| `pEdadMin` | `INTEGER` | — | — |
| `pEdadMax` | `INTEGER` | — | — |
| `pPlazoMinCred` | `INTEGER` | `cred`=crédito | 🔵 CONVENCIÓN |
| `pPlazoMaxCred` | `INTEGER` | `cred`=crédito | 🔵 CONVENCIÓN |
| `pIdFrecPago` | `SMALLINT` | — | — |
| `pCobroComisionAnual` | `CHAR(1)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pComiGastoCobranza` | `SMALLINT` | — | — |
| `pCobroComisApertura` | `CHAR(1)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pComiDisposicionEfect` | `SMALLINT` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pComiAclaracionNo` | `SMALLINT` | — | — |
| `pComiLiquidacionAntic` | `SMALLINT` | — | — |
| `pCodComisionApertura` | `CHAR(4)` | — | — |
| `pCodComiGastoCobranza` | `CHAR(4)` | — | — |
| `pCodComisionAnualidad` | `CHAR(8)` | — | — |
| `pCodComiDisposicionEfect` | `CHAR(4)` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pCodComiAclaracionNo` | `CHAR(4)` | — | — |
| `pCodComiLiquidacionAntic` | `CHAR(4)` | — | — |
| `pGarantias` | `SMALLINT` | — | — |
| `pIdGarantia` | `SMALLINT` | — | — |
| `pPorcentajeAforo` | `DECIMAL(16)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pObligadoSolidario` | `CHAR(1)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pNumObligados` | `CHAR(1)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pCapturaObligatoria` | `CHAR(1)` | — | — |
| `pMontoMinDisp` | `DECIMAL(18,2)` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pMontoMaxDisp` | `DECIMAL(18,2)` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFormApliComiGasCob` | `CHAR(1)` | — | — |
| `pFormApliComiAper` | `CHAR(1)` | — | — |
| `pFormApliComiDispo` | `CHAR(1)` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFormApliComiAnual` | `CHAR(1)` | — | — |
| `pFormApliComiAclara` | `CHAR(1)` | — | — |
| `pFormApliComiLiqAnt` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L42 |
| `iSqlErr` | `INTEGER` | L43 |
| `cCodRetSp` | `CHAR(5)` | L44 |
| `iCodRetSp` | `INTEGER` | L45 |
| `cEmpresa` | `CHAR(3)` | L46 |
| `cNumProducto` | `CHAR(4)` | L47 |
| `dMontoMinCred` | `DECIMAL(18,2)` | L48 |
| `dMontoMaxCred` | `DECIMAL(18,2)` | L49 |
| `iEdadMin` | `INTEGER` | L50 |
| `iEdadMax` | `INTEGER` | L51 |
| `iPlazoMinCred` | `INTEGER` | L52 |
| `iPlazoMaxCred` | `INTEGER` | L53 |
| `iIdFrecPago` | `SMALLINT` | L54 |
| `cCobroComisionAnual` | `CHAR(1)` | L55 |
| `iComiGastoCobranza` | `SMALLINT` | L56 |
| `cCobroComisApertura` | `CHAR(1)` | L57 |
| `iComiDisposicionEfect` | `SMALLINT` | L58 |
| `iComiAclaracionNo` | `SMALLINT` | L59 |
| `iComiLiquidacionAntic` | `SMALLINT` | L60 |
| `cCodComisionApertura` | `CHAR(4)` | L61 |
| `cCodComiGastoCobranza` | `CHAR(4)` | L62 |
| `cCodComisionAnualidad` | `CHAR(8)` | L63 |
| `cCodComiDisposicionEfect` | `CHAR(4)` | L64 |
| `cCodComiAclaracionNo` | `CHAR(4)` | L65 |
| `cCodComiLiquidacionAntic` | `CHAR(4)` | L66 |
| *…14 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L153 |
| `tmp_sd_definicion` | `bdicred` | ⚠️ sí | DELETE | L153 |
| `tmp_sd_definicion` | `bdicred` | ⚠️ sí | INSERT | L155 |
| `tmp_sd_definicion` | `bdicred` | ⚠️ sí | UPDATE | L164 |
| `sd_definicion` | `bdicred` | ⚠️ sí | INSERT | L227 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L142 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L82 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `?_car` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `?tica` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?duct` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_car`, `?e`, `?tica`, `ro`, `?duct` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_conspoliticacreditoprod`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_conspoliticacreditoprod.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 83 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito, política de crédito y producto" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_conspoliticacreditoprod` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_conspoliticacreditoprod(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | `prod`=producto | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cNumProducto` | `CHAR(4)` | L18 |
| `cRespuestaSic` | `CHAR(1)` | L19 |
| `cGrupo` | `CHAR(1)` | L20 |
| `cModelo` | `CHAR(50)` | L21 |
| `cStatusSol` | `CHAR(2)` | L22 |
| `iBcScoreMin1` | `INTEGER` | L23 |
| `iBcScoreMax1` | `INTEGER` | L24 |
| `iProScoreMin1` | `INTEGER` | L25 |
| `iProScoreMax1` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L67 |
| `sp_conspoliticacreditoprod` | `bdicred` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L55 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumProducto = '' OR pRegistros IS NULL OR pRecuperacion IS N` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L80 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `politica` | ENTIDAD | política de crédito | 🟡 INFERIDO | nombre_sp |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |

---

## `sp_cred_consulta_familia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_consulta_familia.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=40 |
| **Deps concatenadas** | 119 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_familia` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_consulta_familia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cId` | `CHAR(3)` | L10 |
| `cFamilia` | `CHAR(40)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_consulta_familia` | `bdicred` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_consulta_familia";` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?_familia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_familia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_consultaproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_consultaproductos.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=39 |
| **Deps concatenadas** | 118 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito y productos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_productos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_consultaproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
  pNombreProd                  CHAR(40)
  p_tpo_ejecucion              CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |
| `pNombreProd` | `CHAR(40)` | — | — |
| `p_tpo_ejecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `vVariable1` | `VARCHAR(100)` | L17 |
| `vVariable2` | `VARCHAR(100)` | L18 |
| `vVariable3` | `VARCHAR(100)` | L19 |
| `vVariable4` | `VARCHAR(100)` | L20 |
| `vVariable5` | `VARCHAR(100)` | L21 |
| `vVariable6` | `VARCHAR(100)` | L22 |
| `vVariable7` | `VARCHAR(100)` | L23 |
| `vVariable8` | `VARCHAR(100)` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_consulta_productos` | `bdicred` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_cred_consultaproductos";` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cred_consultarinfocrediticia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_consultarinfocrediticia.sql` |
| **LOC (1er CREATE)** | 249 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 187 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar crédito y información" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarinfocrediticia` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_consultarinfocrediticia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pTpDirec                     INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pTpDirec` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L34 |
| `iSqlErr` | `INTEGER` | L35 |
| `cCodRetSp` | `CHAR(5)` | L36 |
| `iCodRetSp` | `INTEGER` | L37 |
| `cEmpresa` | `CHAR(3)` | L38 |
| `cNumCte` | `CHAR(20)` | L41 |
| `cApellPat` | `CHAR(26)` | L42 |
| `cApellMat` | `CHAR(26)` | L43 |
| `cNom1` | `CHAR(26)` | L44 |
| `cNom2` | `CHAR(26)` | L45 |
| `cRFC` | `CHAR(13)` | L46 |
| `dtFechaNac` | `DATE` | L47 |
| `cNumCteCoppel` | `CHAR(20)` | L48 |
| `cTipo_dir` | `CHAR(1)` | L51 |
| `cCalle` | `CHAR(40)` | L52 |
| `cColonia` | `CHAR(60)` | L53 |
| `cEstado` | `CHAR(2)` | L54 |
| `cCiudad` | `CHAR(3)` | L55 |
| `cMunicipio` | `CHAR(5)` | L56 |
| `cCod_postal` | `CHAR(5)` | L57 |
| `cTelefono1` | `CHAR(13)` | L58 |
| `cTelefono2` | `CHAR(13)` | L59 |
| `cTelefono3` | `CHAR(13)` | L60 |
| `sNumerociudad` | `SMALLINT` | L61 |
| `iNumerocalle` | `INT` | L62 |
| *…17 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L155 |
| `sp_consultarinfocrediticia` | `bdicred` | ⚠️ sí | L163 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L146 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR NVL(pNumCte,"") = "" OR pTpDirec IS NULL THEN` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L171 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_consultarinfocrediticia";` |  |
| L173 | CÓDIGO_RETORNO | `LET cCodRet = '00050';` |  |
| L179 | CÓDIGO_RETORNO | `LET cCodRet = '00056';` |  |
| L185 | CÓDIGO_RETORNO | `LET cCodRet = '00846';` |  |
| L191 | CÓDIGO_RETORNO | `LET cCodRet = '00847';` |  |
| L197 | CÓDIGO_RETORNO | `LET cCodRet = '00848';` |  |
| L203 | CÓDIGO_RETORNO | `LET cCodRet = '00849';` |  |
| L209 | CÓDIGO_RETORNO | `LET cCodRet = '00850';` |  |
| L215 | CÓDIGO_RETORNO | `LET cCodRet = '00851';` |  |
| L221 | CÓDIGO_RETORNO | `LET cCodRet = '00852';` |  |
| L227 | CÓDIGO_RETORNO | `LET cCodRet = '00853';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `?iticia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iticia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_consultasaldosgeneral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_consultasaldosgeneral.sql` |
| **LOC (1er CREATE)** | 336 |
| **Callgraph** | ✅ fan_in=0 / fan_out=79 |
| **Deps concatenadas** | 186 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito y saldos (general)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_saldos_general` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_consultasaldosgeneral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cred`=crédito | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L52 |
| `iSqlErr` | `INTEGER` | L53 |
| `cCodRetSp` | `CHAR(6)` | L54 |
| `iCodRetSp` | `INTEGER` | L55 |
| `cEmpresa` | `CHAR(3)` | L56 |
| `cMensajeRet` | `CHAR(80)` | L58 |
| `cNumCredito` | `CHAR(20)` | L59 |
| `cCodTipCred` | `CHAR(2)` | L60 |
| `cDescStatusCred` | `CHAR(60)` | L61 |
| `iIdUnidadProd` | `INTEGER` | L63 |
| `cCodCaract2` | `CHAR(3)` | L64 |
| `dtFechaOrigen` | `DATE` | L66 |
| `dtFechaProxPago` | `DATE` | L67 |
| `dPagoMinimo` | `DECIMAL(18,2)` | L68 |
| `dtFechaUltPago` | `DATE` | L69 |
| `iPlazo` | `INTEGER` | L70 |
| `iPagosRealizados` | `INTEGER` | L71 |
| `dLineaOtorgada` | `DECIMAL(18,2)` | L72 |
| `dTasaInteres` | `DECIMAL(9,2)` | L74 |
| `dTasaMoratorios` | `DECIMAL(9,2)` | L75 |
| `dMontoSBC` | `DECIMAL(14,2)` | L76 |
| `dCapVig` | `DECIMAL(18,2)` | L78 |
| `dCapTrans` | `DECIMAL(18,2)` | L79 |
| `dCapVdoExig` | `DECIMAL(18,2)` | L80 |
| `dCapVdoNoExig` | `DECIMAL(18,2)` | L81 |
| *…27 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L210 |
| `sp_consulta_saldos_general` | `bdicred` | ⚠️ sí | L223 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L115 | CÓDIGO_RETORNO | `LET cCodRet 			  = '00000';` |  |
| L197 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L236 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP";` |  |
| L238 | CÓDIGO_RETORNO | `LET cCodRet = '00358';` |  |
| L249 | CÓDIGO_RETORNO | `LET cCodRet = '00046';` |  |
| L260 | CÓDIGO_RETORNO | `LET cCodRet = '00046';` |  |
| L271 | CÓDIGO_RETORNO | `LET cCodRet = '00014';` |  |
| L282 | CÓDIGO_RETORNO | `LET cCodRet = '00209';` |  |
| L293 | CÓDIGO_RETORNO | `LET cCodRet = '00889';` |  |
| L304 | CÓDIGO_RETORNO | `LET cCodRet = '00889';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `general` | MODIF | general | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cred_consultasubproducto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_consultasubproducto.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 89 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito y sub-producto" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_subproducto` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_consultasubproducto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
  pNombreProd                  CHAR(40)
  pIdSubProducto               CHAR(4)
  pTipoEjecucion               CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |
| `pNombreProd` | `CHAR(40)` | — | — |
| `pIdSubProducto` | `CHAR(4)` | `subproducto`=sub-producto | ✅ CÓDIGO |
| `pTipoEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `vVariable1` | `VARCHAR(100)` | L16 |
| `vVariable2` | `VARCHAR(100)` | L17 |
| `vVariable3` | `VARCHAR(100)` | L18 |
| `vVariable4` | `VARCHAR(100)` | L19 |
| `vVariable5` | `VARCHAR(100)` | L20 |
| `vVariable6` | `VARCHAR(100)` | L21 |
| `vVariable7` | `VARCHAR(100)` | L22 |
| `vVariable8` | `VARCHAR(100)` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |
| `sp_consulta_subproducto` | `bdicred` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_consulta_subproducto";` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L78 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `subproducto` | ENTIDAD | sub-producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cred_consultatgarantia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_consultatgarantia.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 108 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta crédito y garantía" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultatgarantia` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_consultatgarantia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iIdGarantia` | `INTEGER` | L12 |
| `cTipoGarantia` | `VARCHAR(20)` | L13 |
| `cDescGarantias` | `VARCHAR(30)` | L14 |
| `dPorcentajeAforo` | `DECIMAL(16)` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_consultatgarantia` | `bdicred` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_consultatgarantia";` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `garantia` | ENTIDAD | garantía | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_elimina_tmp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_elimina_tmp.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 101 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_eliminatemp` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_elimina_tmp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS CodRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L4 |
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |
| `sp_eliminatemp` | `bdicred` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_eliminatemp";` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `?_tmp` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tmp` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_grabacomplementarias`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_grabacomplementarias.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 109 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba crédito y Tarjeta (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_grabacomplementarias(
  pUsuario                     CHAR(10)
  pIdFuncion                   CHAR(10)
  pCuentas_Medios              CHAR(1)
  pCancelacion_Inac            CHAR(1)
  pCancelacion_Vig             CHAR(1)
  pTiempo_Cancelar             CHAR(1)
  pSeguro_Vida                 CHAR(1)
  pCobro_Mensualidad           CHAR(1)
  pEnvio_Mesa_Control          CHAR(1)
  pId_Domiciliacion            CHAR(1)
  pConciliador                 CHAR(1)
  pHistorico_Cred              CHAR(1)
  pPeriodo_Gracia              CHAR(1)
  pDias_Gracia                 INTEGER
  pCapital_Interes             CHAR(1)
  pIntereses                   CHAR(1)
  pCapital                     CHAR(1)
  pEstado_Cuenta               CHAR(1)
  pTipoEdocta                  CHAR(1)
  pTipoFacturacion             CHAR(1)
  pd_estadocuenta              CHAR(20)
  pemision_estado_cuenta       CHAR(2)
  prango_inicial               CHAR(2)
  prango_final                 CHAR(2)
  pid_tipo_facturacion         CHAR(2)
  pn_dias_facturacion          CHAR(3)
  pdia_facturacion             CHAR(2)
  prango_f_fecha_inic          CHAR(2)
  prango_f_fecha_fin           CHAR(2)
  pidcta_concentradora         CHAR(1)
  pcta_concentradora           CHAR(20)
  pRangoini_gracia             CHAR(2)
  pRangofin_gracia             CHAR(2)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(10)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuentas_Medios` | `CHAR(1)` | — | — |
| `pCancelacion_Inac` | `CHAR(1)` | — | — |
| `pCancelacion_Vig` | `CHAR(1)` | — | — |
| `pTiempo_Cancelar` | `CHAR(1)` | — | — |
| `pSeguro_Vida` | `CHAR(1)` | — | — |
| `pCobro_Mensualidad` | `CHAR(1)` | — | — |
| `pEnvio_Mesa_Control` | `CHAR(1)` | — | — |
| `pId_Domiciliacion` | `CHAR(1)` | — | — |
| `pConciliador` | `CHAR(1)` | — | — |
| `pHistorico_Cred` | `CHAR(1)` | `cred`=crédito | ✅ CÓDIGO |
| `pPeriodo_Gracia` | `CHAR(1)` | — | — |
| `pDias_Gracia` | `INTEGER` | — | — |
| `pCapital_Interes` | `CHAR(1)` | — | — |
| `pIntereses` | `CHAR(1)` | — | — |
| `pCapital` | `CHAR(1)` | — | — |
| `pEstado_Cuenta` | `CHAR(1)` | — | — |
| `pTipoEdocta` | `CHAR(1)` | — | — |
| `pTipoFacturacion` | `CHAR(1)` | — | — |
| `pd_estadocuenta` | `CHAR(20)` | — | — |
| `pemision_estado_cuenta` | `CHAR(2)` | — | — |
| `prango_inicial` | `CHAR(2)` | — | — |
| `prango_final` | `CHAR(2)` | — | — |
| `pid_tipo_facturacion` | `CHAR(2)` | — | — |
| `pn_dias_facturacion` | `CHAR(3)` | — | — |
| `pdia_facturacion` | `CHAR(2)` | — | — |
| `prango_f_fecha_inic` | `CHAR(2)` | — | — |
| `prango_f_fecha_fin` | `CHAR(2)` | — | — |
| `pidcta_concentradora` | `CHAR(1)` | — | — |
| `pcta_concentradora` | `CHAR(20)` | — | — |
| `pRangoini_gracia` | `CHAR(2)` | — | — |
| `pRangofin_gracia` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_caracteristicas_complementarias` | `bdicred` | ⚠️ sí | INSERT | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?lemen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?ias` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?lemen`, `?ias` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_grabapoliticacreditoprod`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_grabapoliticacreditoprod.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 103 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba crédito, política de crédito y producto" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_graba_politicacreditoprod` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_grabapoliticacreditoprod(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
  pRespuestaSic                CHAR(1)
  pGrupo                       CHAR(1)
  pBcScoremin                  INTEGER
  pBcScoremax                  INTEGER
  pProScormin                  INTEGER
  pProScormax                  INTEGER
  pStatusSol                   CHAR(2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | `prod`=producto | 🟡 INFERIDO |
| `pRespuestaSic` | `CHAR(1)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pGrupo` | `CHAR(1)` | — | — |
| `pBcScoremin` | `INTEGER` | — | — |
| `pBcScoremax` | `INTEGER` | — | — |
| `pProScormin` | `INTEGER` | — | — |
| `pProScormax` | `INTEGER` | — | — |
| `pStatusSol` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_graba_politicacreditoprod` | `bdicred` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_graba_politicacreditoprod";` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `politica` | ENTIDAD | política de crédito | 🟡 INFERIDO | nombre_sp |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |

---

## `sp_cred_grabatipofacturacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_grabatipofacturacion.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 116 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba crédito y facturación (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_grabatipofacturacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_grabatipofacturacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
  pIdFacturacion               CHAR(3)
  pNoDias                      INTEGER
  pDiaFijo                     CHAR(3)
  pRgoMin                      CHAR(3)
  pRgoMax                      CHAR(3)
  pTipoEjecucion               CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |
| `pIdFacturacion` | `CHAR(3)` | `facturacion`=facturación | ✅ CÓDIGO |
| `pNoDias` | `INTEGER` | — | — |
| `pDiaFijo` | `CHAR(3)` | — | — |
| `pRgoMin` | `CHAR(3)` | — | — |
| `pRgoMax` | `CHAR(3)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | `tipo`=tipo de | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cId_facturacion` | `CHAR(3)` | L13 |
| `cNo_dias` | `SMALLINT` | L14 |
| `cDia_fijo` | `CHAR(3)` | L15 |
| `cRgo_min` | `CHAR(3)` | L16 |
| `cRgo_max` | `CHAR(3)` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_grabatipofacturacion` | `bdicred` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_grabatipofacturacion";` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `facturacion` | ENTIDAD | facturación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cred_insertaconvproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_insertaconvproductos.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=38 |
| **Deps concatenadas** | 117 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta crédito y productos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_insertaconvproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(2)
  pProducto                    CHAR(4)
  pDescripcion                 CHAR(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(2)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pDescripcion` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |
| `sp_inserta_conv_productos` | `bdicred` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_inserta_conv_productos";` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?v` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_insertaproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_insertaproductos.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 81 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta crédito y productos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_insertaproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFamilia                     CHAR(3)
  pNum_Producto                CHAR(4)
  pNomb_Producto               CHAR(40)
  psub_producto                INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFamilia` | `CHAR(3)` | — | — |
| `pNum_Producto` | `CHAR(4)` | — | — |
| `pNomb_Producto` | `CHAR(40)` | — | — |
| `psub_producto` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_inserta_productos` | `bdicred` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_inserta_productos";` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cred_insertasubproducto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_insertasubproducto.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=36 |
| **Deps concatenadas** | 115 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta crédito y sub-producto" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_insertasubproducto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDesc_subproducto            VARCHAR(40)
  pNum_producto                CHAR(4)
  pNomb_Producto               CHAR(40)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDesc_subproducto` | `VARCHAR(40)` | `subproducto`=sub-producto | ✅ CÓDIGO |
| `pNum_producto` | `CHAR(4)` | — | — |
| `pNomb_Producto` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cId_subproducto` | `VARCHAR(5)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `sp_inserta_subproducto` | `bdicred` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_consulta_frecpago";` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `subproducto` | ENTIDAD | sub-producto | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_cred_mensajesactivos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_mensajesactivos.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 87 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "crédito y mensajes (activos)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_mensajes_activos` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_mensajesactivos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
  pCveCanal                    SMALLINT
  pIdEvento                    SMALLINT
  pCodPlantilla                SMALLINT
  pCodMsmMail                  CHAR(50)
  pActivoSms                   CHAR(1)
  pActivoEmail                 CHAR(1)
  pTipoEjecucion               CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |
| `pCveCanal` | `SMALLINT` | — | — |
| `pIdEvento` | `SMALLINT` | — | — |
| `pCodPlantilla` | `SMALLINT` | — | — |
| `pCodMsmMail` | `CHAR(50)` | — | — |
| `pActivoSms` | `CHAR(1)` | `activos`=activos | ✅ CÓDIGO |
| `pActivoEmail` | `CHAR(1)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `sIdEvento` | `SMALLINT` | L16 |
| `cDescripcion` | `CHAR(40)` | L17 |
| `cCveCanal` | `CHAR(50)` | L18 |
| `cActivoSms` | `CHAR(50)` | L19 |
| `cActivoEmail` | `CHAR(50)` | L20 |
| `cCodPlantilla` | `CHAR(50)` | L21 |
| `cCodMsmMail` | `CHAR(50)` | L22 |
| `sCveEvento` | `CHAR(4)` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_mensajes_activos` | `bdicred` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_mensajes_activos";` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `mensajes` | ENTIDAD | mensajes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `activos` | MODIF | activos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cred_obtenctasmedioacceso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obtenctasmedioacceso.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 102 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito, cuentas y medio de acceso" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtenctasmedioacceso` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obtenctasmedioacceso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoEjecucion               CHAR(1)
  pNumProducto                 CHAR(4)
  pNumProdCapt                 CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |
| `pNumProdCapt` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cEmpresaSp` | `CHAR(3)` | L12 |
| `cNum_Producto` | `CHAR(4)` | L13 |
| `cNomb_Producto` | `CHAR(40)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_obtenctasmedioacceso` | `bdicred` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obtenctasmedioacceso";` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `medioacceso` | ENTIDAD | medio de acceso | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cred_obtenercanaloperacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obtenercanaloperacion.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=33 |
| **Deps concatenadas** | 111 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito y canal" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtenercanaloperacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obtenercanaloperacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCanal                     SMALLINT
  pNombreCanal                 CHAR(30)
  pIdOperaciones               SMALLINT
  pDescOperacion               CHAR(100)
  pNumproducto                 CHAR(4)
  pTipoEjecucion               CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCanal` | `SMALLINT` | `canal`=canal (de distribución) | 🔵 CONVENCIÓN |
| `pNombreCanal` | `CHAR(30)` | `canal`=canal (de distribución) | 🔵 CONVENCIÓN |
| `pIdOperaciones` | `SMALLINT` | `operacion`=operación | ✅ CÓDIGO |
| `pDescOperacion` | `CHAR(100)` | `operacion`=operación | ✅ CÓDIGO |
| `pNumproducto` | `CHAR(4)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `var1` | `SMALLINT` | L12 |
| `var2` | `CHAR(30)` | L13 |
| `var3` | `SMALLINT` | L14 |
| `var4` | `CHAR(30)` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_obtenercanaloperacion` | `bdicred` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obtenercanaloperacion";` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `canal` | ENTIDAD | canal (de distribución) | 🔵 CONVENCIÓN | nombre_sp |
| `operacion` | ACCION | operación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cred_obtenerdoctosdigitalizar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obtenerdoctosdigitalizar.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 86 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito y documentos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtenerdoctosdigitalizar` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obtenerdoctosdigitalizar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
  pCodigoGrupo                 CHAR(3)
  pCodigoDocto                 CHAR(4)
  pDescripcion                 CHAR(50)
  pTipoEjecucion               CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pNombreProducto              CHAR(50)
  pligar                       CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |
| `pCodigoGrupo` | `CHAR(3)` | — | — |
| `pCodigoDocto` | `CHAR(4)` | — | — |
| `pDescripcion` | `CHAR(50)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |
| `pNombreProducto` | `CHAR(50)` | — | — |
| `pligar` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(4)` | L14 |
| `cCodigo` | `CHAR(4)` | L15 |
| `cDescripcion` | `CHAR(50)` | L16 |
| `cVariable1` | `CHAR(4)` | L17 |
| `cVariable2` | `CHAR(4)` | L18 |
| `cVariable3` | `CHAR(50)` | L19 |
| `cVariable4` | `CHAR(1)` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_obtenerdoctosdigitalizar` | `bdicred` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoEjecucion = '' OR pRegistros IS NULL OR pRecuperacion IS` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L73 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obtenerdoctosdigitalizar";` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L83 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizar` | ACCION | digitaliza documento | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cred_obtenerdoctosimprimir`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obtenerdoctosimprimir.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=34 |
| **Deps concatenadas** | 113 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito, documentos y Impago — pago vencido o fallido; confirmado: n_impagos_consec" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtenerdoctosimprimir` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obtenerdoctosimprimir(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigoDocumento             CHAR(4)
  pDescripcionDocumento        CHAR(40)
  pCantidad                    INTEGER
  pNumProducto                 CHAR(4)
  pTipoEjecucion               CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigoDocumento` | `CHAR(4)` | — | — |
| `pDescripcionDocumento` | `CHAR(40)` | — | — |
| `pCantidad` | `INTEGER` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cCodigoDocumento` | `CHAR(4)` | L11 |
| `cDescripcionDocumento` | `CHAR(40)` | L12 |
| `iCantidad` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_obtenerdoctosimprimir` | `bdicred` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obtenerdoctosimprimir";` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?rimir` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rimir` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_obtenerprospectosaumlincredofi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obtenerprospectosaumlincredofi.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=79 |
| **Deps concatenadas** | 185 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito, prospectos y oficio" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtener_prospectos_aumlincred_ofi` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obtenerprospectosaumlincredofi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pNumCredito                  CHAR(20)
  pNumProd                     CHAR(4)
  pNumcte                      CHAR(20)
  pNumcte_cop                  CHAR(20)
  pLincredSolicitada           DECIMAL(18,2)
  pComprobanteIngresos         CHAR(2)
  pMensaje                     CHAR(250)
  pFechaHoyAumlincred          DATE
  pIngresoMensual              DECIMAL(18,2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cred`=crédito | 🔵 CONVENCIÓN |
| `pNumProd` | `CHAR(4)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pNumcte_cop` | `CHAR(20)` | — | — |
| `pLincredSolicitada` | `DECIMAL(18,2)` | `cred`=crédito | 🔵 CONVENCIÓN |
| `pComprobanteIngresos` | `CHAR(2)` | — | — |
| `pMensaje` | `CHAR(250)` | — | — |
| `pFechaHoyAumlincred` | `DATE` | `cred`=crédito · `aumlincred`=Aumento de Línea de Crédito — proceso de incremento del límite crediticio; 26+ SPs en bdicred (sp_*_aumlincred) | 🔵 CONVENCIÓN |
| `pIngresoMensual` | `DECIMAL(18,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cMensajeRet` | `CHAR(150)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_obtener_prospectos_aumlincred_ofi` | `bdicred` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obtener_prospectos_aumlincred_ofi";` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00905';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00905';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00903';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00902';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00901';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00901';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00900';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00899';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00898';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00897';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00896';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00896';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00896';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `prospectos` | ENTIDAD | prospectos (nuevos clientes potenciales) | 🔵 CONVENCIÓN | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | 🟡 INFERIDO | nombre_sp |

---

## `sp_cred_obteneventosmsj`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obteneventosmsj.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 114 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito, evento/notificación y SMS" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `notifica` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obteneventosmsj(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `sIdEvento` | `SMALLINT` | L10 |
| `cDescripcion` | `CHAR(30)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_obten_eventos_msj` | `bdicred` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obten_eventos_msj";` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `evento` | ENTIDAD | evento/notificación | 🔵 CONVENCIÓN | nombre_sp |
| `sms` | ENTIDAD | SMS | 🔵 CONVENCIÓN | nombre_sp |
| `?j` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?j` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cred_obteninfosolincredmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obteninfosolincredmc.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=78 |
| **Deps concatenadas** | 184 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito, información, solicitud de crédito y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obteninfosolincred_mc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obteninfosolincredmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) 		AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cred`=crédito · `cred`=crédito | 🔵 CONVENCIÓN |
| `pNumcte` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cMensajeRet` | `CHAR(107)` | L16 |
| `cNumCredito` | `CHAR(20)` | L17 |
| `cNumcte` | `CHAR(20)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cNumProd` | `CHAR(4)` | L20 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |
| `sp_obteninfosolincred_mc` | `bdicred` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_cnsif_confirmaejecutivo";` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00358';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `solin` | ENTIDAD | solicitud de crédito | 🟡 INFERIDO | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cred_obtentipoedocta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obtentipoedocta.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 110 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito y Estado de Cuenta — documento periódico de movimientos y saldos; generado como PDF (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtentipoedocta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obtentipoedocta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cid_estdocta` | `VARCHAR(3)` | L10 |
| `cDesc_Estdocta` | `VARCHAR(40)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_obtentipoedocta` | `bdicred` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obtentipoedocta";` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `edocta` | ENTIDAD | Estado de Cuenta — documento periódico de movimientos y sald | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cred_obtentipofactura`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_obtentipofactura.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 109 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene crédito y factura (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtentipofactura` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_obtentipofactura(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cIdTpofacturacion` | `CHAR(3)` | L10 |
| `cDescFacturacion` | `CHAR(40)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_obtentipofactura` | `bdicred` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obtentipofactura";` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `factura` | ENTIDAD | factura | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_cred_tasasdiferenciadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cred_tasasdiferenciadas.sql` |
| **LOC (1er CREATE)** | 238 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 85 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "crédito, tasa y sd — saldo disponible" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cred_tasasdiferenciadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pFechaTasa                   DATE
  pNumProducto                 CHAR(4)
  pGrupo                       CHAR(1)
  pModeloHitBuenoOrdnario      DECIMAL(11,6)
  pModeloHitMaloOrdnario       DECIMAL(11,6)
  pModeloNoHitOrdinario        DECIMAL(11,6)
  pModeloHitBuenoMoratorio     DECIMAL(11,6)
  pModeloHitMaloMoratorio      DECIMAL(11,6)
  pModeloNoHitMoratorio        DECIMAL(11,6)
  pNumSubProducto              CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pFechaTasa` | `DATE` | `tasa`=tasa (de interés) | 🔵 CONVENCIÓN |
| `pNumProducto` | `CHAR(4)` | — | — |
| `pGrupo` | `CHAR(1)` | — | — |
| `pModeloHitBuenoOrdnario` | `DECIMAL(11,6)` | — | — |
| `pModeloHitMaloOrdnario` | `DECIMAL(11,6)` | — | — |
| `pModeloNoHitOrdinario` | `DECIMAL(11,6)` | — | — |
| `pModeloHitBuenoMoratorio` | `DECIMAL(11,6)` | — | — |
| `pModeloHitMaloMoratorio` | `DECIMAL(11,6)` | — | — |
| `pModeloNoHitMoratorio` | `DECIMAL(11,6)` | — | — |
| `pNumSubProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `dFechaTasa` | `DATE` | L19 |
| `cNumProducto` | `CHAR(4)` | L20 |
| `cGrupo` | `CHAR(1)` | L21 |
| `dModeloHitBuenoOrdnario` | `DECIMAL(11,6)` | L22 |
| `dModeloHitMaloOrdnario` | `DECIMAL(11,6)` | L23 |
| `dModeloNoHitOrdinario` | `DECIMAL(11,6)` | L24 |
| `dModeloHitBuenoMoratorio` | `DECIMAL(11,6)` | L25 |
| `dModeloHitMaloMoratorio` | `DECIMAL(11,6)` | L26 |
| `dModeloNoHitMoratorio` | `DECIMAL(11,6)` | L27 |
| `dFechaInsert` | `DATETIME YEAR to FRACTION(5)` | L28 |
| `iTotReg` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_tasas_diferenciadas` | `bdicred` | ⚠️ sí | INSERT | L75 |
| `tmp_tasas_diferenciadas` | `bdicred` | ⚠️ sí | UPDATE | L82 |
| `tmp_tasas_diferenciadas` | `bdicred` | ⚠️ sí | SELECT | L95 |
| `tmp_tasas_diferenciadas` | `bdicred` | ⚠️ sí | DELETE | L95 |
| `sd_tasas_disposiciones_diferenciadas` | `bdicred` | ⚠️ sí | SELECT | L134 |
| `sd_tasas_disposiciones_diferenciadas` | `bdicred` | ⚠️ sí | INSERT | L138 |
| `sd_tasas_disposiciones_diferenciadas` | `bdicred` | ⚠️ sí | UPDATE | L147 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | FÓRMULA | `LET dModeloHitBuenoMoratorio = dModeloHitBuenoMoratorio - dModeloHitBuenoOrdnario;` | 🔴 MONEY/aritmética financiera |
| L109 | FÓRMULA | `LET dModeloHitMaloMoratorio = dModeloHitMaloMoratorio - dModeloHitMaloOrdnario;` | 🔴 MONEY/aritmética financiera |
| L111 | FÓRMULA | `LET dModeloNoHitMoratorio = dModeloNoHitMoratorio - dModeloNoHitOrdinario;` | 🔴 MONEY/aritmética financiera |
| L124 | FÓRMULA | `LET dModeloHitBuenoMoratorio = dModeloHitBuenoMoratorio + dModeloHitBuenoOrdnario;` | 🔴 MONEY/aritmética financiera |
| L127 | FÓRMULA | `LET dModeloHitMaloMoratorio = dModeloHitMaloMoratorio + dModeloHitMaloOrdnario;` | 🔴 MONEY/aritmética financiera |
| L130 | FÓRMULA | `LET dModeloNoHitMoratorio = dModeloNoHitMoratorio + dModeloNoHitOrdinario;` | 🔴 MONEY/aritmética financiera |
| L210 | FÓRMULA | `LET dModeloHitBuenoMoratorio = dModeloHitBuenoMoratorio - dModeloHitBuenoOrdnario;` | 🔴 MONEY/aritmética financiera |
| L213 | FÓRMULA | `LET dModeloHitMaloMoratorio = dModeloHitMaloMoratorio - dModeloHitMaloOrdnario;` | 🔴 MONEY/aritmética financiera |
| L216 | FÓRMULA | `LET dModeloNoHitMoratorio = dModeloNoHitMoratorio - dModeloNoHitOrdinario;` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `tasa` | ENTIDAD | tasa (de interés) | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?iferenciadas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iferenciadas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_criteriosgeneracionreportepagoscre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_criteriosgeneracionreportepagoscre.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera OS — Originación de Solicitudes, reporte, pagos y crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_criteriosgeneracionreportepagoscre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoCampania                SMALLINT
  pGpoParametro                CHAR(10)
  pNumParametro                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoCampania` | `SMALLINT` | — | — |
| `pGpoParametro` | `CHAR(10)` | — | — |
| `pNumParametro` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `sTipoCapania` | `SMALLINT` | L17 |
| `cGrupoParametro` | `CHAR(10)` | L18 |
| `iNumParametro` | `INTEGER` | L19 |
| `cDescripcion` | `CHAR(100)` | L20 |
| `cValorAlfabetico` | `CHAR(100)` | L21 |
| `dValorNumerico` | `DECIMAL(18,2)` | L22 |
| `dFechaInsert` | `DATE` | L23 |
| `cUserInsert` | `CHAR(8)` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |
| `sp_consulta_criteriosgeneracionreporte` | `bdicred` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consulta_criteriosgeneracionreporte';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_criteri` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `pagos` | ENTIDAD | pagos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_criteri`, `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cs_actporcentajeproveedores`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cs_actporcentajeproveedores.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza ro — Rol Operativo y estado" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=5 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_cs_actporcentajeproveedores(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTrama                       CHAR(2000)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTrama` | `CHAR(2000)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `cDescCodRetSp` | `CHAR(150)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cCadena` | `CHAR(250)` | L10 |
| `cProvedor` | `CHAR(60)` | L11 |
| `cPorcentaje` | `CHAR(5)` | L12 |
| `cValidaPorcentaje` | `CHAR(5)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_porcentaje_repsoc` | `bdisac` | ⚠️ sí | SELECT | L62 |
| `sac_porcentaje_repsoc` | `bdisac` | ⚠️ sí | UPDATE | L66 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `sp_split_cadena` | `bdicnweb` | no | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET cProvedor = SUBSTR(cCadena, 1, CHARINDEX(',', cCadena) - 1);` |  |
| L59 | FÓRMULA | `LET cPorcentaje = SUBSTR(cCadena, CHARINDEX(',', cCadena) + 1);` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?centajep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?ve` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edo` | ENTIDAD | estado | 🟡 INFERIDO | nombre_sp |
| `?res` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cs_`, `?centajep`, `ro`, `?ve`, `?res` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cs_catantad`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cs_catantad.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo (anterior)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cs_catantad(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `cDescCodRetSp` | `CHAR(150)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cNumCategoria` | `CHAR(2)` | L13 |
| `cNumConvenio` | `CHAR(3)` | L14 |
| `cNomConvenio` | `CHAR(40)` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L69 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?ad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cs_`, `?ad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cs_catproceso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cs_catproceso.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y proceso" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cs_catproceso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(150)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iId_proceso` | `SMALLINT` | L12 |
| `cDesc_proceso` | `CHAR(50)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L56 |
| `systables` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cs_catventatiempoaire`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cs_catventatiempoaire.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cs_catventatiempoaire(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `cDescCodRetSp` | `CHAR(150)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cCodParam` | `CHAR(4)` | L13 |
| `cValor` | `CHAR(2)` | L14 |
| `cDescripcion` | `CHAR(50)` | L15 |
| `iRecuperacion` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?ventati` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `?oaire` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cs_`, `?ventati`, `?oaire` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cs_detrepoperaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cs_detrepoperaciones.sql` |
| **LOC (1er CREATE)** | 266 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte detalle y operaciones" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cs_detrepoperaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCategoria                CHAR(2)
  pNumConvenio                 CHAR(3)
  pIdProv                      CHAR(2)
  pDescProv                    CHAR(50)
  pIdConsulta                  CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCategoria` | `CHAR(2)` | — | — |
| `pNumConvenio` | `CHAR(3)` | — | — |
| `pIdProv` | `CHAR(2)` | — | — |
| `pDescProv` | `CHAR(50)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cDescCodRetSp` | `CHAR(150)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `cMes` | `CHAR(2)` | L20 |
| `cDescMes` | `CHAR(10)` | L21 |
| `cAnio` | `CHAR(4)` | L22 |
| `cFechaMes` | `CHAR(20)` | L23 |
| `cProveedor` | `CHAR(40)` | L24 |
| `iNumOperaciones` | `INTEGER` | L25 |
| `dImporteTotal` | `MONEY(16,2)` | L26 |
| `cPorcentaje` | `CHAR(5)` | L27 |
| `dImporteSobre` | `MONEY(16,2)` | L28 |
| `dPagoBcp` | `MONEY(16,2)` | L29 |
| `dPagoCp` | `MONEY(16,2)` | L30 |
| `cNumCategoria` | `CHAR(2)` | L31 |
| `cNumConvenio` | `CHAR(3)` | L32 |
| `cProvedor` | `CHAR(50)` | L33 |
| `iNumRegistros` | `INTEGER` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_repdetalleoperaciones` | `bdicnweb` | no | SELECT | L100 |
| `sac_porcentaje_repsoc` | `bdisac` | ⚠️ sí | SELECT | L147 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L131 | FÓRMULA | `LET cFechaMes = TRIM(cDescMes)\|\|'-'\|\|cAnio;` |  |
| L133 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L193 | FÓRMULA | `LET cFechaMes = TRIM(cDescMes)\|\|'-'\|\|cAnio;` |  |
| L196 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L239 | FÓRMULA | `LET cFechaMes = TRIM(cDescMes)\|\|'-'\|\|cAnio;` |  |
| L241 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cs_totrepoperaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cs_totrepoperaciones.sql` |
| **LOC (1er CREATE)** | 263 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte operaciones" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_cs_totrepoperaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCategoria                CHAR(2)
  pNumConvenio                 CHAR(3)
  pIdProv                      CHAR(2)
  pDescProv                    CHAR(50)
  pIdConsulta                  CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCategoria` | `CHAR(2)` | — | — |
| `pNumConvenio` | `CHAR(3)` | — | — |
| `pIdProv` | `CHAR(2)` | — | — |
| `pDescProv` | `CHAR(50)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `cDescCodRetSp` | `CHAR(150)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cMes` | `CHAR(2)` | L15 |
| `cDescMes` | `CHAR(10)` | L16 |
| `cAnio` | `CHAR(4)` | L17 |
| `cFechaMes` | `CHAR(20)` | L18 |
| `cProveedor` | `CHAR(40)` | L19 |
| `iNumOperaciones` | `INTEGER` | L20 |
| `dImporteTotal` | `MONEY(16,2)` | L21 |
| `cPorcentaje` | `CHAR(5)` | L22 |
| `dImporteSobre` | `MONEY(16,2)` | L23 |
| `dPagoBcp` | `MONEY(16,2)` | L24 |
| `dPagoCp` | `MONEY(16,2)` | L25 |
| `iTotImporteTotal` | `MONEY(18,2)` | L27 |
| `iTotImporteSobre` | `MONEY(18,2)` | L28 |
| `iTotPagoBCp` | `MONEY(18,2)` | L29 |
| `iTotPagoCp` | `MONEY(18,2)` | L30 |
| `iNumRegistros` | `INTEGER` | L31 |
| `cNumCategoria` | `CHAR(2)` | L32 |
| `cNumConvenio` | `CHAR(3)` | L33 |
| `cProvedor` | `CHAR(50)` | L34 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_repdetalleoperaciones` | `bdicnweb` | no | SELECT | L78 |
| `status_repdetalleoperaciones` | `bdicnweb` | no | DELETE | L78 |
| `status_repdetalleoperaciones` | `bdicnweb` | no | INSERT | L79 |
| `sw_repdetalleoperaciones` | `bdicnweb` | no | SELECT | L97 |
| `sw_repdetalleoperaciones` | `bdicnweb` | no | DELETE | L97 |
| `sw_repdetalleoperaciones` | `bdicnweb` | no | INSERT | L106 |
| `sac_porcentaje_repsoc` | `bdisac` | ⚠️ sí | SELECT | L112 |
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L176 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cs_tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cs_tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cs_verificastatusrepoperaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cs_verificastatusrepoperaciones.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y operaciones" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_cs_verificastatusrepoperaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cStatus` | `CHAR(1)` | L13 |
| `cErrorProceso` | `CHAR(1)` | L14 |
| `cError` | `CHAR(5)` | L15 |
| `iTotImporteTotal` | `MONEY(18,2)` | L17 |
| `iTotImporteSobre` | `MONEY(18,2)` | L18 |
| `iTotPagoBCp` | `MONEY(18,2)` | L19 |
| `iTotPagoCp` | `MONEY(18,2)` | L20 |
| `iNumRegistros` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_repdetalleoperaciones` | `bdicnweb` | no | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_cs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ctes_consdoctosdigitalizacion_manto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ctes_consdoctosdigitalizacion_manto.sql` |
| **LOC (1er CREATE)** | 241 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 89 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clientes, documentos y digitalización de documentos (anterior)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ctes_consdoctosdigitalizacion_manto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEsAclaracion                CHAR(1)
  pOpcion                      SMALLINT
  pCodigosGrupo                CHAR(1000)
  pCodigosDoctos               CHAR(1000)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEsAclaracion` | `CHAR(1)` | — | — |
| `pOpcion` | `SMALLINT` | — | — |
| `pCodigosGrupo` | `CHAR(1000)` | — | — |
| `pCodigosDoctos` | `CHAR(1000)` | `doctos`=documentos | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cCodDocumento` | `CHAR(4)` | L13 |
| `cDescripcionDocto` | `CHAR(35)` | L14 |
| `cGenericoDocto` | `CHAR(1)` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cValorParam` | `CHAR(100)` | L17 |
| `cDescripcionParam` | `CHAR(50)` | L18 |
| `iCodFirmas` | `SMALLINT` | L19 |
| `iCodAclaracionesOFI` | `SMALLINT` | L20 |
| `iCodAclaracionesSIF` | `SMALLINT` | L21 |
| `cGpoFirmas` | `CHAR(100)` | L22 |
| `cGpoAclaracionesOFI` | `CHAR(100)` | L23 |
| `cGpoAclaracionesSIF` | `CHAR(100)` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `cCodGrupo` | `CHAR(3)` | L26 |
| `cDescCodGrupo` | `CHAR(30)` | L27 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultaparamdigitalizacion` | `bdicnweb` | no | L93 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEsAclaracion = '' OR pOpcion IS NULL OR pRegistros IS NULL O` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L98 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_consultaparamdigi` |  |
| L114 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_consultaparamdigi` |  |
| L127 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_consultaparamdigi` |  |
| L143 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L150 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L166 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_consultaparamdigi` |  |
| L182 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L189 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L211 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L218 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?o` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ctes_consdoctosdigitalizacion_manto_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ctes_consdoctosdigitalizacion_manto_totales.sql` |
| **LOC (1er CREATE)** | 176 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 88 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clientes, documentos y digitalización de documentos (anterior, totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=7 · INFERIDO=0 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ctes_consdoctosdigitalizacion_manto_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEsAclaracion                CHAR(1)
  pOpcion                      SMALLINT
  pCodigosGrupo                CHAR(255)
  pCodigosDoctos               CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEsAclaracion` | `CHAR(1)` | — | — |
| `pOpcion` | `SMALLINT` | — | — |
| `pCodigosGrupo` | `CHAR(255)` | — | — |
| `pCodigosDoctos` | `CHAR(255)` | `doctos`=documentos | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValorParam` | `CHAR(100)` | L10 |
| `cDescripcionParam` | `CHAR(50)` | L11 |
| `iCodFirmas` | `SMALLINT` | L12 |
| `iCodAclaracionesOFI` | `SMALLINT` | L13 |
| `iCodAclaracionesSIF` | `SMALLINT` | L14 |
| `cGpoFirmas` | `CHAR(100)` | L15 |
| `cGpoAclaracionesOFI` | `CHAR(100)` | L16 |
| `cGpoAclaracionesSIF` | `CHAR(100)` | L17 |
| `iNoRegistros` | `INTEGER` | L18 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultaparamdigitalizacion` | `bdicnweb` | no | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEsAclaracion = '' OR pOpcion IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L65 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_consultaparamdigi` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_consultaparamdigi` |  |
| L94 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_consultaparamdigi` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L123 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_consultaparamdigi` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?o_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ctes_consultahuellaactual`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ctes_consultahuellaactual.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta clientes y huella biométrica" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_huella_actual` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ctes_consultahuellaactual(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `iSqlErr` | `INTEGER` | L36 |
| `cCodRetSp` | `CHAR(5)` | L37 |
| `iCodRetSp` | `INTEGER` | L38 |
| `cTemplate1` | `CHAR(942)` | L39 |
| `cTemplate2` | `CHAR(942)` | L40 |
| `cTemplate3` | `CHAR(942)` | L41 |
| `cTemplate4` | `CHAR(942)` | L42 |
| `cTemplate5` | `CHAR(942)` | L43 |
| `cTemplate6` | `CHAR(942)` | L44 |
| `cTemplate7` | `CHAR(942)` | L45 |
| `cTemplate8` | `CHAR(942)` | L46 |
| `cTemplate9` | `CHAR(942)` | L47 |
| `cTemplate10` | `CHAR(942)` | L48 |
| `sNfiq1` | `SMALLINT` | L49 |
| `sNfiq2` | `SMALLINT` | L50 |
| `sNfiq3` | `SMALLINT` | L51 |
| `sNfiq4` | `SMALLINT` | L52 |
| `sNfiq5` | `SMALLINT` | L53 |
| `sNfiq6` | `SMALLINT` | L54 |
| `sNfiq7` | `SMALLINT` | L55 |
| `sNfiq8` | `SMALLINT` | L56 |
| `sNfiq9` | `SMALLINT` | L57 |
| `sNfiq10` | `SMALLINT` | L58 |
| `sMinucias1` | `SMALLINT` | L59 |
| *…10 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L127 |
| `sp_consulta_huella_actual` | `bdinteg` | ⚠️ sí | L135 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L140 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP";` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `huella` | ENTIDAD | huella biométrica | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?ual` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ual` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cuentadoctosfusioncte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_cuentadoctosfusioncte.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fusiona cuentas cuenta, documentos y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cuentadoctos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cuentadoctosfusioncte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumeroCliente               CHAR(20)
  pTipo_cte                    SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumeroCliente` | `CHAR(20)` | — | — |
| `pTipo_cte` | `SMALLINT` | `cte`=cliente | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `isam_err` | `INT` | L10 |
| `cDescripcion` | `CHAR(100)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_cuentadoctos` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pNumeroCliente = '' OR pTipo_cte IS NULL THEN` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cuentadoctos';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00354';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00355';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `fusion` | ACCION | fusiona cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_datosdiahoy_cod47`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_datosdiahoy_cod47.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=78 / fan_out=37 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "datos del día y código (de hoy)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditef` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_datosdiahoy_cod47(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `dFecha` | `DATE` | L12 |
| `cNoBanco` | `CHAR(3)` | L13 |
| `cNombreArchivo` | `CHAR(30)` | L14 |
| `iNombrePro` | `INTEGER` | L15 |
| `cProcesado` | `CHAR(1)` | L16 |
| `dFechaHabilAnt` | `DATE` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L70 |
| `cce_encabezado` | `bditef` | ⚠️ sí | SELECT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L50 |
| `bditef` | `bdicnweb` | no | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:cal_habil_ant';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `datosdia` | ENTIDAD | datos del día | 🔵 CONVENCIÓN | nombre_sp |
| `hoy` | MODIF | de hoy / fecha actual | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?47` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?47` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_deb_actualizascparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_deb_actualizascparam.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza código postal (débito)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_deb_actualizascparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodParam                    CHAR(100)
  pValor                       CHAR(100)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodParam` | `CHAR(100)` | — | — |
| `pValor` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRet` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | UPDATE | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `deb` | MODIF | débito | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `?aram` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?aram` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_deb_calculagat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_deb_calculagat.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "calcula (débito)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_calculagat` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_deb_calculagat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) 		AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRet` | `CHAR(5)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `sp_calculagat` | `bdicheq` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `deb` | MODIF | débito | 🔵 CONVENCIÓN | nombre_sp |
| `calcula` | ACCION | calcula (verbo activo — spei_calculointeres) | 🔵 CONVENCIÓN | nombre_sp |
| `?gat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gat` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_deb_catestatusexencioncte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_deb_catestatusexencioncte.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo, estatus y cliente (débito)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_deb_catestatusexencioncte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iIdExencion` | `INT` | L12 |
| `cDescExencion` | `CHAR(35)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_exencioncte` | `bdicheq` | ⚠️ sí | SELECT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `deb` | MODIF | débito | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?encion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?encion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_deb_catperiodicidad`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_deb_catperiodicidad.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo (débito, periodicidad)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_deb_catperiodicidad(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iIdPeriodicidad` | `INT` | L12 |
| `cDescPeriodicidad` | `CHAR(35)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_periodicidad` | `bdicheq` | ⚠️ sí | SELECT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `deb` | MODIF | débito | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `periodicidad` | MODIF | periodicidad | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_deb_insertconsexencioncte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_deb_insertconsexencioncte.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "insertar TCO — Tarjetas Coppel y cliente (débito)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_deb_insertconsexencioncte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pCliente                     CHAR(20)
  pStatus                      INT
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pStatus` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iStatus` | `INT` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_retirocliente_exento` | `bdicheq` | ⚠️ sí | INSERT | L55 |
| `sc_retirocliente_exento` | `bdicheq` | ⚠️ sí | UPDATE | L65 |
| `sc_retirocliente_exento` | `bdicheq` | ⚠️ sí | SELECT | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | VALIDACIÓN_NULL | `IF pStatus IS NULL THEN` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `deb` | MODIF | débito | 🔵 CONVENCIÓN | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?er` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tco` | ENTIDAD | TCO — Tarjetas Coppel / TCoppel (producto de crédito Grupo C | 🔵 CONVENCIÓN | nombre_sp |
| `?nse` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?encion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?er`, `?nse`, `?encion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_deb_verificastatusarchivotxt`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_deb_verificastatusarchivotxt.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y archivo (débito)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_deb_verificastatusarchivotxt(
  pUsuario                     CHAR(9)
  pIdFuncion                   CHAR(8)
  pProducto                    CHAR(4)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(9)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cStatus` | `CHAR(1)` | L12 |
| `cErrorProceso` | `CHAR(1)` | L13 |
| `cError` | `CHAR(5)` | L14 |
| `cHayErrores` | `CHAR(1)` | L15 |
| `cProducto` | `CHAR(4)` | L16 |
| `cEmergente` | `CHAR(1)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatus_validarchivotxt` | `bdicnweb` | no | SELECT | L55 |
| `sw_verificastatus_validarchivotxt` | `bdicnweb` | no | DELETE | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `deb` | MODIF | débito | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t`, `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_depurar_genarchmovtos_masivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_depurar_genarchmovtos_masivo.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=45 |
| **Deps concatenadas** | 65 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "depura archivo y movimiento (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_depurar_tempomovs` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_depurar_genarchmovtos_masivo(
) RETURNING CHAR(5) AS CodRet
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L4 |
| `cCodRet` | `CHAR(5)` | L5 |
| `ven_transacc` | `SMALLINT` | L6 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_depurar_tempomovs` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet 							= '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `depura` | ACCION | depura / limpia | 🔵 CONVENCIÓN | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_desactualizadasbc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_desactualizadasbc_totales.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal y saldo básico de cuenta (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_desactualizadasbc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(4)
  pStatus                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INT` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `?tualizada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sbc` | ENTIDAD | saldo básico de cuenta (SBC) | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_de`, `?tualizada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_desbloqueoctacre_masivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_desbloqueoctacre_masivo.sql` |
| **LOC (1er CREATE)** | 232 |
| **Callgraph** | ✅ fan_in=0 / fan_out=45 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloquea cuenta cuenta y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `bdicred`, `sp_consaldodisp`, `sp_totalesdesbloqueocre` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_desbloqueoctacre_masivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INTEGER
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INTEGER` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cMensajeRetorno` | `CHAR(80)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `iRegistrosExitosos` | `INTEGER` | L11 |
| `iRegistrosFallidos` | `INTEGER` | L12 |
| `iCodRet` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cCuenta` | `CHAR(20)` | L15 |
| `cResultado` | `CHAR(15)` | L16 |
| `cStatus` | `CHAR(1)` | L17 |
| `cMotivoRechazo` | `CHAR(80)` | L18 |
| `mSaldoCuenta` | `MONEY(14,2)` | L19 |
| `cCodRetSpSal` | `CHAR(5)` | L20 |
| `dFechaProceso` | `DATETIME YEAR TO FRACTION(3)` | L21 |
| `iIdRegistro` | `INTEGER` | L22 |
| `cFechaCargaLote` | `DATE` | L23 |
| `iTotalRegsLote` | `INTEGER` | L24 |
| `mMontoLote` | `MONEY(14, 2)` | L25 |
| `iRegsAceptadosLote` | `INTEGER` | L26 |
| `iRegsRechazoLote` | `INTEGER` | L27 |
| `cArchivo` | `CHAR(150)` | L28 |
| `cStatusLote` | `CHAR(1)` | L29 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_desbloqueocre` | `bdicnweb` | no | SELECT | L92 |
| `sw_tr_cargamasiva_desbloqueocre_hist` | `bdicnweb` | no | SELECT | L96 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L164 |
| `sw_tr_cargamasiva_desbloqueocre` | `bdicnweb` | no | DELETE | L190 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L119 |
| `sp_consaldodisp` | `bdicnweb` | no | L137 |
| `sp_totalesdesbloqueocre` | `bdicnweb` | no | L174 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L195 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L75 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL OR pIdPlantilla = '' OR pTituloPlantilla = '' T` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L123 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCIÃN DEL SP sp_bloqueocuenta EN PROCESO MASIVO';` |  |
| L127 | FÓRMULA | `LET iRegistrosFallidos = iRegistrosFallidos + 1;` |  |
| L132 | FÓRMULA | `LET iRegistrosExitosos = iRegistrosExitosos + 1;` |  |
| L168 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `desbloqueo` | ACCION | desbloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_desbloqueocuentacre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_desbloqueocuentacre.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloquea cuenta cuenta y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_desbloqueocuentacre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pTipo                        SMALLINT
  pAreaPersonaSolicita         CHAR(150)
  pMotivoBloqueo               CHAR(150)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pTipo` | `SMALLINT` | — | — |
| `pAreaPersonaSolicita` | `CHAR(150)` | — | — |
| `pMotivoBloqueo` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `SMALLINT` | L7 |
| `cMensajeRet` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCuenta = '' OR pTipo IS NULL OR pAreaPersonaSolicita = '' OR` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00249';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_desbloqueocuenta';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00032';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00250';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00033';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00018';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '01128';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00251';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `desbloqueo` | ACCION | desbloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_desccodret`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_desccodret.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=54 |
| **Deps concatenadas** | 147 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Descripción y código" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_desc_ret` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_desccodret(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodRet                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodRet` | `CHAR(4)` | `cod`=código | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(50)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_desc_ret` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdinteg:sp_desc_ret';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?ret` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ret` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detalle_clave_retiro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detalle_clave_retiro.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, clave y retiro" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_detalle_clave_retiro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFoliooperacion              CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFoliooperacion` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cCodRetSp` | `CHAR(6)` | L19 |
| `iCodRetSp` | `INTEGER` | L20 |
| `cCr_status` | `CHAR(20)` | L22 |
| `cDes_status` | `CHAR(20)` | L23 |
| `cFoliooperacion` | `CHAR(6)` | L24 |
| `dAlta_fecha` | `DATE` | L25 |
| `dUltima_mod_fecha` | `DATE` | L26 |
| `cCliente` | `CHAR(20)` | L27 |
| `cCuenta` | `CHAR(20)` | L28 |
| `cTarjeta` | `CHAR(20)` | L29 |
| `iMonto` | `INTEGER` | L30 |
| `cConcepto` | `CHAR(45)` | L31 |
| `cModalidad` | `CHAR(1)` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_detalle_retiro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detalle_retiro.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle y retiro" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_detalle_retiro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFoliooperacion              CHAR(100)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFoliooperacion` | `CHAR(100)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `cCodRetSp` | `CHAR(6)` | L25 |
| `iCodRetSp` | `INTEGER` | L26 |
| `cCr_status` | `CHAR(20)` | L28 |
| `cDes_status` | `CHAR(20)` | L29 |
| `cFoliooperacion` | `CHAR(100)` | L30 |
| `dAlta_fecha` | `DATE` | L31 |
| `dUltima_mod_fecha` | `DATE` | L32 |
| `cCliente` | `CHAR(20)` | L33 |
| `cCuenta` | `CHAR(20)` | L34 |
| `cTarjeta` | `CHAR(20)` | L35 |
| `iMonto` | `INTEGER` | L36 |
| `cConcepto` | `CHAR(45)` | L37 |
| `cModalidad` | `CHAR(1)` | L38 |
| `cCajero` | `CHAR(8)` | L39 |
| `cAut_oper_retiro` | `CHAR(8)` | L40 |
| `dCobrado_fecha` | `DATE` | L41 |
| `dRechazo_fecha` | `DATE` | L42 |
| `cCodigoiso_reverso` | `CHAR(2)` | L43 |
| `dCanc_fecha` | `DATE` | L44 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L99 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L79 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_detallearchivodotacioncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallearchivodotacioncaja.sql` |
| **LOC (1er CREATE)** | 295 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, archivo, dotación de efectivo y caja" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallearchivodotacioncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoFolio                     CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoFolio` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L46 |
| `iSqlErr` | `INTEGER` | L47 |
| `fCant1` | `DECIMAL(10,2)` | L48 |
| `cDenominacion1` | `CHAR(18)` | L49 |
| `mTotal1` | `MONEY(14,2)` | L50 |
| `fCant2` | `DECIMAL(10,2)` | L51 |
| `cDenominacion2` | `CHAR(18)` | L52 |
| `mTotal2` | `MONEY(14,2)` | L53 |
| `fCant3` | `DECIMAL(10,2)` | L54 |
| `cDenominacion3` | `CHAR(18)` | L55 |
| `mTotal3` | `MONEY(14,2)` | L56 |
| `fCant4` | `DECIMAL(10,2)` | L57 |
| `cDenominacion4` | `CHAR(18)` | L58 |
| `mTotal4` | `MONEY(14,2)` | L59 |
| `fCant5` | `DECIMAL(10,2)` | L60 |
| `cDenominacion5` | `CHAR(18)` | L61 |
| `mTotal5` | `MONEY(14,2)` | L62 |
| `fCant6` | `DECIMAL(10,2)` | L63 |
| `cDenominacion6` | `CHAR(18)` | L64 |
| `mTotal6` | `MONEY(14,2)` | L65 |
| `fCant7` | `DECIMAL(10,2)` | L66 |
| `cDenominacion7` | `CHAR(18)` | L67 |
| `mTotal7` | `MONEY(14,2)` | L68 |
| `fCant8` | `DECIMAL(10,2)` | L69 |
| `cDenominacion8` | `CHAR(18)` | L70 |
| *…48 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L214 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L207 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L264 | VALIDACIÓN_NULL | `IF dFechRecepcion IS NULL AND cHoRecepcion IS NULL AND cUsRecepcion IS NULL THEN` |  |
| L265 | VALIDACIÓN_NULL | `IF dFechReversion IS NULL AND cHoReversion IS NULL AND cUsReversion IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_detallecatalogocc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecatalogocc.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=108 |
| **Deps concatenadas** | 327 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, catálogo y cc — cuenta corriente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecatalogocc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `cDescCodRetSp` | `CHAR(100)` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `iDias_laborables` | `INTEGER` | L20 |
| `dFactor_remesas` | `DECIMAL(9,2)` | L21 |
| `dFactor_rem_sbc` | `DECIMAL(10,2)` | L22 |
| `dIva` | `DECIMAL(5,2)` | L23 |
| `dMonto_minimo` | `DECIMAL(10,2)` | L24 |
| `dMonto_min_sbc` | `DECIMAL(14,2)` | L25 |
| `dSal_min_pza` | `DECIMAL(14,2)` | L26 |
| `mMto_min_efect` | `MONEY(16,2)` | L27 |
| `mMto_max_efect` | `MONEY(16,2)` | L28 |
| `cPlaza_cajagen` | `CHAR(3)` | L29 |
| `iId_plazaclabe` | `INTEGER` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L82 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |

---

## `sp_detallecoincidenciasind`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecoincidenciasind.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=53 |
| **Deps concatenadas** | 146 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecoincidenciasind(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pNumTarjeta                  CHAR(20)
  pApePat                      CHAR(26)
  pApeMat                      CHAR(26)
  pNombre1                     CHAR(26)
  pNombre2                     CHAR(26)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pApePat` | `CHAR(26)` | — | — |
| `pApeMat` | `CHAR(26)` | — | — |
| `pNombre1` | `CHAR(26)` | — | — |
| `pNombre2` | `CHAR(26)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `cDescCodRetSp` | `CHAR(100)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cNumCte` | `CHAR(20)` | L16 |
| `cNombre` | `CHAR(104)` | L17 |
| `cRFC` | `CHAR(13)` | L18 |
| `cNumCred` | `CHAR(20)` | L19 |
| `cNumTarjeta` | `CHAR(20)` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_detcoincidenciasind` | `bdicnweb` | no | SELECT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L73 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?coinc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?enciasind` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?coinc`, `?enciasind` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detallecoincidenciasind_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecoincidenciasind_totales.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=0 / fan_out=53 |
| **Deps concatenadas** | 145 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle y identificador (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultacoincidenciasindividual` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecoincidenciasind_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pNumTarjeta                  CHAR(20)
  pApePat                      CHAR(26)
  pApeMat                      CHAR(26)
  pNombre1                     CHAR(26)
  pNombre2                     CHAR(26)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pApePat` | `CHAR(26)` | — | — |
| `pApeMat` | `CHAR(26)` | — | — |
| `pNombre1` | `CHAR(26)` | — | — |
| `pNombre2` | `CHAR(26)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNumCte` | `CHAR(20)` | L12 |
| `cNombre` | `CHAR(104)` | L13 |
| `cRFC` | `CHAR(13)` | L14 |
| `cNumCred` | `CHAR(20)` | L15 |
| `cNumTarjeta` | `CHAR(20)` | L16 |
| `iNumRegistros` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verifica_detallemarcaje` | `bdicnweb` | no | SELECT | L50 |
| `sw_verifica_detallemarcaje` | `bdicnweb` | no | DELETE | L50 |
| `sw_verifica_detallemarcaje` | `bdicnweb` | no | INSERT | L53 |
| `sw_detcoincidenciasind` | `bdicnweb` | no | SELECT | L67 |
| `sw_detcoincidenciasind` | `bdicnweb` | no | DELETE | L67 |
| `sw_detcoincidenciasind` | `bdicnweb` | no | INSERT | L108 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L70 |
| `sp_consultacoincidenciasindividual` | `bdisitesp` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisitesp:sp_consultacoincidenc` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?coinc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?enciasind_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?coinc`, `?enciasind_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detallecolonias`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecolonias.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 158 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle y colonias" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecolonias(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCp                          CHAR(5)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCp` | `CHAR(5)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cCp` | `CHAR(5)` | L17 |
| `cCve_estado` | `CHAR(2)` | L18 |
| `cCve_mun` | `CHAR(3)` | L19 |
| `cCve_localidad` | `CHAR(14)` | L20 |
| `cNombre_estado` | `CHAR(30)` | L21 |
| `cDesc_mun` | `CHAR(60)` | L22 |
| `cDesc_localidad` | `CHAR(60)` | L23 |
| `cCve_col` | `CHAR(8)` | L24 |
| `cDesc_colonia` | `CHAR(100)` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catsepomex` | `bdinteg` | ⚠️ sí | SELECT | L76 |
| `si_localidades` | `bdinteg` | ⚠️ sí | SELECT | L83 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L67 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCp = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L88 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `colonias` | ENTIDAD | colonias (catálogo domicilio) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_detallecp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecp.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 157 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle y código postal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCp                          CHAR(5)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCp` | `CHAR(5)` | `cp`=código postal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cCve_estado` | `CHAR(2)` | L13 |
| `cCve_mun` | `CHAR(3)` | L14 |
| `cCve_localidad` | `CHAR(14)` | L15 |
| `cNombre_estado` | `CHAR(30)` | L16 |
| `cDesc_mun` | `CHAR(60)` | L17 |
| `cDesc_localidad` | `CHAR(60)` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_localidades` | `bdinteg` | ⚠️ sí | SELECT | L58 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_detallecredbloqfall`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecredbloqfall.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ✅ fan_in=0 / fan_out=52 |
| **Deps concatenadas** | 144 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloqueo detalle, crédito y fal — fallo/disputa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecredbloqfall(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cred`=crédito | 🔵 CONVENCIÓN |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `cDescCodRetSp` | `CHAR(80)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `cNumcte` | `CHAR(20)` | L24 |
| `cApell_paterno` | `CHAR(26)` | L25 |
| `cApell_materno` | `CHAR(26)` | L26 |
| `cNombre1` | `CHAR(26)` | L27 |
| `cNombre2` | `CHAR(26)` | L28 |
| `cNum_credito` | `CHAR(20)` | L29 |
| `dFecha_bloqueo` | `DATE` | L30 |
| `cStatus` | `CHAR(2)` | L31 |
| `cDescripcion` | `CHAR(60)` | L32 |
| `dCapital_Vigente` | `DECIMAL(18,2)` | L33 |
| `dCapital_Vencido` | `DECIMAL(18,2)` | L34 |
| `dVencido_No_Exigible` | `DECIMAL(18,2)` | L35 |
| `dTransitorio` | `DECIMAL(18,2)` | L36 |
| `dTotal_Quebrantado` | `DECIMAL(18,2)` | L37 |
| `iRecuperacion` | `INTEGER` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_detcredbloqfall` | `bdicnweb` | no | SELECT | L102 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L73 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L105 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detallecredbloqfall_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecredbloqfall_totales.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=52 |
| **Deps concatenadas** | 143 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloqueo detalle, crédito y fal — fallo/disputa (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultacredbloqfallecimiento` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecredbloqfall_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cred`=crédito | 🔵 CONVENCIÓN |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(80)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNumcte` | `CHAR(20)` | L11 |
| `cApell_paterno` | `CHAR(26)` | L12 |
| `cApell_materno` | `CHAR(26)` | L13 |
| `cNombre1` | `CHAR(26)` | L14 |
| `cNombre2` | `CHAR(26)` | L15 |
| `cNum_credito` | `CHAR(20)` | L16 |
| `dFecha_bloqueo` | `DATE` | L17 |
| `cStatus` | `CHAR(2)` | L18 |
| `cDescripcion` | `CHAR(60)` | L19 |
| `dCapital_Vigente` | `DECIMAL(18,2)` | L20 |
| `dCapital_Vencido` | `DECIMAL(18,2)` | L21 |
| `dVencido_No_Exigible` | `DECIMAL(18,2)` | L22 |
| `dTransitorio` | `DECIMAL(18,2)` | L23 |
| `dTotal_Quebrantado` | `DECIMAL(18,2)` | L24 |
| `iNumRegistros` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepqueb` | `bdicnweb` | no | SELECT | L64 |
| `sw_verificastatusrepqueb` | `bdicnweb` | no | DELETE | L64 |
| `sw_verificastatusrepqueb` | `bdicnweb` | no | INSERT | L65 |
| `sw_detcredbloqfall` | `bdicnweb` | no | SELECT | L67 |
| `sw_detcredbloqfall` | `bdicnweb` | no | DELETE | L67 |
| `sw_detcredbloqfall` | `bdicnweb` | no | INSERT | L108 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L77 |
| `sp_consultacredbloqfallecimiento` | `bdicred` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_consultacredbloqfall` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L107 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?l_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detallecteseind`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecteseind.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=51 |
| **Deps concatenadas** | 142 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle y clientes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecteseind(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pIdBusqueda                  INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pIdBusqueda` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `cDescCodRetSp` | `CHAR(100)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cNumCred` | `CHAR(20)` | L16 |
| `cNumTarjeta` | `CHAR(20)` | L17 |
| `cSitEsp` | `CHAR(1)` | L18 |
| `cCausa` | `SMALLINT` | L19 |
| `cDescripcion` | `CHAR(75)` | L20 |
| `dFechaMod` | `DATETIME YEAR TO SECOND` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_detcteseind` | `bdicnweb` | no | SELECT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `?eind` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?eind` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detallecteseind_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallecteseind_totales.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=51 |
| **Deps concatenadas** | 141 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle y clientes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaclienteseindividual` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallecteseind_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pIdBusqueda                  INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pIdBusqueda` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNumCred` | `CHAR(20)` | L11 |
| `cNumTarjeta` | `CHAR(20)` | L12 |
| `cSitEsp` | `CHAR(1)` | L13 |
| `cCausa` | `SMALLINT` | L14 |
| `cDescripcion` | `CHAR(75)` | L15 |
| `dFechaMod` | `DATETIME YEAR TO SECOND` | L16 |
| `iNumRegistros` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_detcteseind` | `bdicnweb` | no | SELECT | L51 |
| `sw_detcteseind` | `bdicnweb` | no | DELETE | L51 |
| `sw_detcteseind` | `bdicnweb` | no | INSERT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_consultaclienteseindividual` | `bdisitesp` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisitesp:sp_consultaclientesei` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `?eind_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?eind_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detalledenoarqueosucaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detalledenoarqueosucaja.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, OS — Originación de Solicitudes y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_detalledenoarqueosucaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdPlazaArq                  CHAR(3)
  pIdSucursalArq               CHAR(4)
  pFechaArq                    DATE
  pIdCajeroPrincArq            CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdPlazaArq` | `CHAR(3)` | — | — |
| `pIdSucursalArq` | `CHAR(4)` | — | — |
| `pFechaArq` | `DATE` | — | — |
| `pIdCajeroPrincArq` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `cCodRetSp` | `CHAR(6)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `dCantidad1` | `DECIMAL(10,2)` | L25 |
| `dCantidad2` | `DECIMAL(10,2)` | L26 |
| `dCantidad3` | `DECIMAL(10,2)` | L27 |
| `dCantidad4` | `DECIMAL(10,2)` | L28 |
| `dCantidad5` | `DECIMAL(10,2)` | L29 |
| `dCantidad6` | `DECIMAL(10,2)` | L30 |
| `dCantidad7` | `DECIMAL(10,2)` | L31 |
| `mCantTotal1` | `MONEY(14,2)` | L32 |
| `mCantTotal2` | `MONEY(14,2)` | L33 |
| `mCantTotal3` | `MONEY(14,2)` | L34 |
| `mCantTotal4` | `MONEY(14,2)` | L35 |
| `mCantTotal5` | `MONEY(14,2)` | L36 |
| `mCantTotal6` | `MONEY(14,2)` | L37 |
| `mCantTotal7` | `MONEY(14,2)` | L38 |
| `mMontoTotal` | `MONEY(14,2)` | L39 |
| `iNoRegistros` | `INTEGER` | L40 |
| `cCmd1` | `CHAR(1000)` | L41 |
| `cCmd2` | `CHAR(500)` | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdPlazaArq = '' OR pFechaArq IS NULL OR pIdCajeroPrincArq = ` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L119 | FÓRMULA | `LET mCantTotal1 = NVL(dCantidad1,0) * 1000;` |  |
| L120 | FÓRMULA | `LET mCantTotal2 = NVL(dCantidad2,0) *  500;` |  |
| L121 | FÓRMULA | `LET mCantTotal3 = NVL(dCantidad3,0) *  200;` |  |
| L122 | FÓRMULA | `LET mCantTotal4 = NVL(dCantidad4,0) *  100;` |  |
| L123 | FÓRMULA | `LET mCantTotal5 = NVL(dCantidad5,0) *   50;` |  |
| L124 | FÓRMULA | `LET mCantTotal6 = NVL(dCantidad6,0) *   20;` |  |
| L125 | FÓRMULA | `LET mCantTotal7 = NVL(dCantidad7,0) *    1;` |  |
| L127 | FÓRMULA | `LET mMontoTotal = mCantTotal1 + mCantTotal2 + mCantTotal3 + mCantTotal4 + mCantTotal5 + mCantTotal6 ` | 🔴 MONEY/aritmética financiera |
| L132 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?denoarque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?u` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?denoarque`, `?u` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detallesaldoscierrediario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallesaldoscierrediario.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 77 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cierre detalle y saldos (diario)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallesaldoscierrediario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pClaveCg                     CHAR(4)
  pClaveSuc                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pClaveCg` | `CHAR(4)` | — | — |
| `pClaveSuc` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cCodRetSp` | `CHAR(5)` | L29 |
| `cDescCodRetSp` | `CHAR(100)` | L30 |
| `cEmpresa` | `CHAR(3)` | L31 |
| `dFecha` | `DATE` | L32 |
| `cClave` | `CHAR(4)` | L33 |
| `mCantidad_1` | `MONEY(18,2)` | L34 |
| `mCantidad_2` | `MONEY(18,2)` | L35 |
| `mCantidad_3` | `MONEY(18,2)` | L36 |
| `mCantidad_4` | `MONEY(18,2)` | L37 |
| `mCantidad_5` | `MONEY(18,2)` | L38 |
| `mCantidad_6` | `MONEY(18,2)` | L39 |
| `mCantidad_8` | `MONEY(18,2)` | L40 |
| `mCantidad_9` | `MONEY(18,2)` | L41 |
| `mCantidad_10` | `MONEY(18,2)` | L42 |
| `mCantidad_11` | `MONEY(18,2)` | L43 |
| `mCantidad_12` | `MONEY(18,2)` | L44 |
| `mCantidad_13` | `MONEY(18,2)` | L45 |
| `mCantidad_14` | `MONEY(18,2)` | L46 |
| `mCantidad_15` | `MONEY(18,2)` | L47 |
| `mCantidad_16` | `MONEY(18,2)` | L48 |
| `mCantidad_17` | `MONEY(18,2)` | L49 |
| `mCantidad_18` | `MONEY(18,2)` | L50 |
| `mSaldo_total_b` | `MONEY(18,2)` | L51 |
| *…3 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_saldoscierre_sucg` | `bdicnweb` | no | SELECT | L131 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L114 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L133 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L140 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cierre` | ACCION | cierre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `diario` | MODIF | diario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_detallesaldoscierrediario_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallesaldoscierrediario_totales.sql` |
| **LOC (1er CREATE)** | 168 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 76 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cierre detalle y saldos (diario, totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_saldoscierre_sucg` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallesaldoscierrediario_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pClaveCg                     CHAR(4)
  pClaveSuc                    CHAR(4)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pClaveCg` | `CHAR(4)` | — | — |
| `pClaveSuc` | `CHAR(4)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `dFecha` | `DATE` | L11 |
| `cClave` | `CHAR(4)` | L12 |
| `mCantidad_1` | `MONEY(18,2)` | L13 |
| `mCantidad_2` | `MONEY(18,2)` | L14 |
| `mCantidad_3` | `MONEY(18,2)` | L15 |
| `mCantidad_4` | `MONEY(18,2)` | L16 |
| `mCantidad_5` | `MONEY(18,2)` | L17 |
| `mCantidad_6` | `MONEY(18,2)` | L18 |
| `mCantidad_8` | `MONEY(18,2)` | L19 |
| `mCantidad_9` | `MONEY(18,2)` | L20 |
| `mCantidad_10` | `MONEY(18,2)` | L21 |
| `mCantidad_11` | `MONEY(18,2)` | L22 |
| `mCantidad_12` | `MONEY(18,2)` | L23 |
| `mCantidad_13` | `MONEY(18,2)` | L24 |
| `mCantidad_14` | `MONEY(18,2)` | L25 |
| `mCantidad_15` | `MONEY(18,2)` | L26 |
| `mCantidad_16` | `MONEY(18,2)` | L27 |
| `mCantidad_17` | `MONEY(18,2)` | L28 |
| `mCantidad_18` | `MONEY(18,2)` | L29 |
| `mSaldo_total_b` | `MONEY(18,2)` | L30 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificaprocesosaldos_sucg` | `bdicnweb` | no | INSERT | L91 |
| `sw_saldoscierre_sucg` | `bdicnweb` | no | SELECT | L119 |
| `sw_saldoscierre_sucg` | `bdicnweb` | no | DELETE | L119 |
| `sw_saldoscierre_sucg` | `bdicnweb` | no | INSERT | L134 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L106 |
| `sp_consulta_saldoscierre_sucg` | `bdisuc` | ⚠️ sí | L123 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L94 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisuc:sp_consulta_saldoscierre` |  |
| L132 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cierre` | ACCION | cierre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `diario` | MODIF | diario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_detallestatussol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallestatussol.sql` |
| **LOC (1er CREATE)** | 148 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 140 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, estatus y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallestatussol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(6)` | L22 |
| `cDescCodRetSp` | `CHAR(80)` | L23 |
| `iCodRetSp` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `cNum_solicitud` | `CHAR(20)` | L26 |
| `cSucursal` | `CHAR(4)` | L27 |
| `cNom_sucursal` | `CHAR(40)` | L28 |
| `cNom_cliente` | `CHAR(104)` | L29 |
| `cStatus_solicitud` | `CHAR(2)` | L30 |
| `mMonto_solicitud` | `MONEY(14,2)` | L31 |
| `mMonto_otorgado` | `MONEY(14,2)` | L32 |
| `dFecha_alta` | `DATE` | L33 |
| `dFecha_cambio_status` | `DATE` | L34 |
| `dEficiencia_pago` | `DECIMAL(10,2)` | L35 |
| `iMeses_historial` | `SMALLINT` | L36 |
| `iScoring_1` | `SMALLINT` | L37 |
| `iScoring_2` | `SMALLINT` | L38 |
| `iTotal_scoring` | `SMALLINT` | L39 |
| `cCausa_rechazo` | `CHAR(10)` | L40 |
| `iRecuperacion` | `INTEGER` | L41 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_detstatussol` | `bdicnweb` | no | SELECT | L115 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L97 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L118 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |

---

## `sp_detallestatussol_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallestatussol_totales.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, estatus y solicitud (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `status_sol` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallestatussol_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNum_solicitud` | `CHAR(20)` | L12 |
| `cSucursal` | `CHAR(4)` | L13 |
| `cNom_sucursal` | `CHAR(40)` | L14 |
| `cNom_cliente` | `CHAR(104)` | L15 |
| `cStatus_solicitud` | `CHAR(2)` | L16 |
| `mMonto_solicitud` | `MONEY(14,2)` | L17 |
| `mMonto_otorgado` | `MONEY(14,2)` | L18 |
| `dFecha_alta` | `DATE` | L19 |
| `dFecha_cambio_status` | `DATE` | L20 |
| `dEficiencia_pago` | `DECIMAL(10,2)` | L21 |
| `iMeses_historial` | `SMALLINT` | L22 |
| `iScoring_1` | `SMALLINT` | L23 |
| `iScoring_2` | `SMALLINT` | L24 |
| `iTotal_scoring` | `SMALLINT` | L25 |
| `cCausa_rechazo` | `CHAR(10)` | L26 |
| `iNumRegistros` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrep` | `bdicnweb` | no | SELECT | L70 |
| `sw_verificastatusrep` | `bdicnweb` | no | DELETE | L70 |
| `sw_verificastatusrep` | `bdicnweb` | no | INSERT | L71 |
| `sw_detstatussol` | `bdicnweb` | no | SELECT | L73 |
| `sw_detstatussol` | `bdicnweb` | no | DELETE | L73 |
| `sw_detstatussol` | `bdicnweb` | no | INSERT | L109 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L85 |
| `status_sol` | `bdicred` | ⚠️ sí | L99 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:status_sol';` |  |
| L108 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_detallestatussolaud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallestatussolaud.sql` |
| **LOC (1er CREATE)** | 131 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 139 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, estatus, solicitud y auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallestatussolaud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(6)` | L20 |
| `cDescCodRetSp` | `CHAR(80)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `cNum_solicitud` | `CHAR(20)` | L24 |
| `cNum_cliente` | `CHAR(20)` | L25 |
| `dFecha_alta` | `DATE` | L26 |
| `cNom_cliente` | `CHAR(104)` | L27 |
| `cStatus_solicitud` | `CHAR(2)` | L28 |
| `mMonto_solicitud` | `MONEY(14,2)` | L29 |
| `mMonto_otorgado` | `MONEY(14,2)` | L30 |
| `dFecha_cambio_status` | `DATE` | L31 |
| `cNom_promotor` | `CHAR(45)` | L32 |
| `cTelefono_particular` | `CHAR(13)` | L33 |
| `cTelefono_celular` | `CHAR(13)` | L34 |
| `cTelefono_oficina` | `CHAR(13)` | L35 |
| `cSucursal` | `CHAR(4)` | L36 |
| `iRecuperacion` | `INTEGER` | L37 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_detstatussolaud` | `bdicnweb` | no | SELECT | L101 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pStatus  = '' OR` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L104 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_detallestatussolaud_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallestatussolaud_totales.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 138 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, estatus, solicitud y auditoría (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_status_sol_aud` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallestatussolaud_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNum_solicitud` | `CHAR(20)` | L12 |
| `cNum_cliente` | `CHAR(20)` | L13 |
| `dFecha_alta` | `DATE` | L14 |
| `cNom_cliente` | `CHAR(104)` | L15 |
| `cStatus_solicitud` | `CHAR(2)` | L16 |
| `mMonto_solicitud` | `MONEY(14,2)` | L17 |
| `mMonto_otorgado` | `MONEY(14,2)` | L18 |
| `dFecha_cambio_status` | `DATE` | L19 |
| `cNom_promotor` | `CHAR(45)` | L20 |
| `cTelefono_particular` | `CHAR(13)` | L21 |
| `cTelefono_celular` | `CHAR(13)` | L22 |
| `cTelefono_oficina` | `CHAR(13)` | L23 |
| `cSucursal` | `CHAR(4)` | L24 |
| `iNumRegistros` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrep` | `bdicnweb` | no | SELECT | L63 |
| `sw_verificastatusrep` | `bdicnweb` | no | DELETE | L63 |
| `sw_verificastatusrep` | `bdicnweb` | no | INSERT | L64 |
| `sw_detstatussolaud` | `bdicnweb` | no | SELECT | L66 |
| `sw_detstatussolaud` | `bdicnweb` | no | DELETE | L66 |
| `sw_detstatussolaud` | `bdicnweb` | no | INSERT | L96 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L76 |
| `sp_status_sol_aud` | `bdicred` | ⚠️ sí | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L68 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pStatus  = '' TH` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L92 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_status_sol_aud';` |  |
| L95 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_detallestatussolaudexcel_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detallestatussolaudexcel_totales.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 137 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle, estatus, solicitud, auditoría y celular (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_status_sol_audexcel` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=2 · SINTÉTICO=1 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_detallestatussolaudexcel_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pStatus                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNum_solicitud` | `CHAR(20)` | L12 |
| `cNum_cliente` | `CHAR(20)` | L13 |
| `dFecha_alta` | `DATE` | L14 |
| `cNom_cliente` | `CHAR(104)` | L15 |
| `cStatus_solicitud` | `CHAR(2)` | L16 |
| `mMonto_solicitud` | `MONEY(14,2)` | L17 |
| `mMonto_otorgado` | `MONEY(14,2)` | L18 |
| `dFecha_cambio_status` | `DATE` | L19 |
| `cNom_promotor_realizo` | `CHAR(45)` | L20 |
| `cNom_promotor_autorizo` | `CHAR(45)` | L21 |
| `cNom_promotor_entrego` | `CHAR(45)` | L22 |
| `cNom_promotor_asigno` | `CHAR(45)` | L23 |
| `cTelefono_particular` | `CHAR(13)` | L24 |
| `cTelefono_celular` | `CHAR(13)` | L25 |
| `cTelefono_oficina` | `CHAR(13)` | L26 |
| `cSucursal` | `CHAR(4)` | L27 |
| `cNom_referencia1` | `CHAR(104)` | L28 |
| `cTel_ref1_particular` | `CHAR(13)` | L29 |
| `cTel_ref1_celular` | `CHAR(13)` | L30 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrep` | `bdicnweb` | no | SELECT | L85 |
| `sw_verificastatusrep` | `bdicnweb` | no | DELETE | L85 |
| `sw_verificastatusrep` | `bdicnweb` | no | INSERT | L86 |
| `sw_detstatussolaudexcel` | `bdicnweb` | no | SELECT | L88 |
| `sw_detstatussolaudexcel` | `bdicnweb` | no | DELETE | L88 |
| `sw_detstatussolaudexcel` | `bdicnweb` | no | INSERT | L137 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L99 |
| `sp_status_sol_audexcel` | `bdicred` | ⚠️ sí | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L90 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L119 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicred:sp_status_sol_audexcel` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L136 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_detbusquedasucursales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_detbusquedasucursales.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=108 |
| **Deps concatenadas** | 326 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda detalle y sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_detbusquedasucursales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pNomSucursal                 CHAR(45)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pNomSucursal` | `CHAR(45)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNumSucursal` | `CHAR(4)` | L11 |
| `cDescSucursal` | `CHAR(45)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L67 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_determinalincredtccjunk`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_determinalincredtccjunk.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "determina línea de crédito y tc — Tarjeta de Crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `determina_lincred_tc_cjunk` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_determinalincredtccjunk(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSol                      CHAR(20)
  pCteNvo                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |
| `pCteNvo` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cDescCodRetSp` | `CHAR(150)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |
| `mLineaCred` | `MONEY(14,2)` | L15 |
| `mCapacidadPago` | `MONEY(14,2)` | L16 |
| `iPlazo` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `determina_lincred_tc_cjunk` | `bdisolic` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisolic:determina_lincred_tc_c` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `determina` | ACCION | determina | 🔵 CONVENCIÓN | nombre_sp |
| `lincred` | ENTIDAD | línea de crédito | 🔵 CONVENCIÓN | nombre_sp |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `cjunk` | AMBIGUO | variable temporal (ruido de código, se ignora) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `cjunk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_devolucionextemporanea_tef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_devolucionextemporanea_tef.sql` |
| **LOC (1er CREATE)** | 241 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "devuelve TEF — transferencia electrónica de fondos (extemporánea)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_devolucionextemporanea_tef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaArch                   CHAR(8)
  pPlantilla                   CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaArch` | `CHAR(8)` | — | — |
| `pPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegs` | `INTEGER` | L8 |
| `iInTrans` | `INTEGER` | L9 |
| `iTotalRegs` | `INTEGER` | L10 |
| `mTotalMonto` | `MONEY(14,2)` | L11 |
| `dFechaMail` | `DATETIME YEAR TO SECOND` | L12 |
| `cValorSucContTef` | `CHAR(100)` | L14 |
| `cValorTransacCargoTef` | `CHAR(100)` | L15 |
| `cNumeroFolio` | `CHAR(16)` | L16 |
| `iIdx` | `INTEGER` | L17 |
| `vsFechaAbono` | `CHAR(8)` | L18 |
| `vsCuenta` | `CHAR(20)` | L19 |
| `vsReferencia` | `CHAR(18)` | L20 |
| `vsBanco` | `CHAR(40)` | L21 |
| `vsImporte` | `CHAR(30)` | L22 |
| `vsEstatus` | `CHAR(10)` | L23 |
| `vsConfirmado` | `CHAR(1)` | L24 |
| `vsNumSecuencia` | `CHAR(7)` | L25 |
| `vsClaveRastreo` | `CHAR(30)` | L26 |
| `vsCveStatus` | `CHAR(2)` | L27 |
| `vsMotDev` | `CHAR(2)` | L28 |
| `vsEstatusReg` | `CHAR(1)` | L29 |
| `vsAuxCta` | `CHAR(11)` | L30 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tef_parametros` | `bditef` | ⚠️ sí | SELECT | L115 |
| `sw_tf_consdevext_tef` | `bdicnweb` | no | SELECT | L135 |
| `tef_errores` | `bditef` | ⚠️ sí | INSERT | L161 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L143 | FÓRMULA | `LET vsImporteAux = SUBSTRING(TRIM(vsImporte) FROM 1 FOR (LENGTH(vsImporte)-2)) \|\|'.'\|\|SUBSTRING(` | 🔴 MONEY/aritmética financiera |
| L167 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |
| `extemporanea` | MODIF | extemporánea | 🔵 CONVENCIÓN | nombre_sp |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_dgconsultadocumentossegundefinicion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dgconsultadocumentossegundefinicion.sql` |
| **LOC (1er CREATE)** | 148 |
| **Callgraph** | ✅ fan_in=29 / fan_out=22 |
| **Deps concatenadas** | 87 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta documentos y [polisemia] Seguridad (inicio)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_dgconsultadocumentossegundefinicion(
  pEmpresa                     CHAR(3)
  pCodDefinicion               CHAR(3)
) RETURNING CHAR(6) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pCodDefinicion` | `CHAR(3)` | `inicio`=inicio | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cEmpresa` | `CHAR(3)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cCodRet` | `CHAR(6)` | L23 |
| `sSecuencia` | `SMALLINT` | L24 |
| `cCodDocto` | `CHAR(4)` | L25 |
| `cDescripDocto` | `CHAR(50)` | L26 |
| `cCodGrupo` | `CHAR(3)` | L27 |
| `cDescripGrup` | `CHAR(50)` | L28 |
| `cMultiImagen` | `CHAR(1)` | L29 |
| `iImagenTamMax` | `INTEGER` | L30 |
| `cImagenFormato` | `CHAR(3)` | L31 |
| `sImagenCompresion` | `SMALLINT` | L32 |
| `dImagenLargo` | `DECIMAL` | L33 |
| `dImagenAncho` | `DECIMAL` | L34 |
| `sImagenDPI` | `SMALLINT` | L35 |
| `cImagenColores` | `CHAR(1)` | L36 |
| `cGenerico` | `CHAR(1)` | L37 |
| `cLiga` | `CHAR(1)` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L100 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `documentos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | 🔵 CONVENCIÓN | nombre_sp |
| `?undef` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inicio` | MODIF | inicio | 🔵 CONVENCIÓN | nombre_sp |
| `?n` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dg`, `?undef`, `?n` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dgconsultapendientesenvio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dgconsultapendientesenvio.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=27 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dgconsultapendientesenvio(
  pEmpresa                     CHAR(3)
  pCtlProcesado                CHAR(1)
  pCtlLigado                   CHAR(1)
  pMacaddressLocal             CHAR(17)
  pNumRegistro                 INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(6),INTEGER,CHAR(20),CHAR(20),CHAR(4),CHAR(4),INTEGER,CHAR(40),CHAR(25),CHAR(50),CHAR(1),CHAR(1),CHAR(1),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pCtlProcesado` | `CHAR(1)` | — | — |
| `pCtlLigado` | `CHAR(1)` | — | — |
| `pMacaddressLocal` | `CHAR(17)` | — | — |
| `pNumRegistro` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cEmpresa` | `CHAR(3)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRet` | `CHAR(6)` | L8 |
| `iIdControl` | `INTEGER` | L9 |
| `cCliente` | `CHAR(20)` | L10 |
| `cCuenta` | `CHAR(20)` | L11 |
| `cProducto` | `CHAR(4)` | L12 |
| `cCodDocto` | `CHAR(4)` | L13 |
| `iSecuencia` | `INTEGER` | L14 |
| `cProdNombre` | `CHAR(40)` | L15 |
| `vCtlArchivoLocal` | `CHAR(25)` | L16 |
| `vCtlArchivoZip` | `CHAR(50)` | L17 |
| `cCtlImgEnviada` | `CHAR(1)` | L18 |
| `cCtlProcesado` | `CHAR(1)` | L19 |
| `cCtlLigado` | `CHAR(1)` | L20 |
| `vCtlArchivoLocalRuta` | `CHAR(150)` | L21 |
| `cUsuarioAlta` | `CHAR(8)` | L22 |
| `vDescripcion` | `CHAR(35)` | L23 |
| `cDescrip2` | `CHAR(30)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L126 |
| `dg_expediente_envio` | `bdidigital` | ⚠️ sí | SELECT | L143 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L112 | CÓDIGO_RETORNO | `LET cCodRet='00098';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet='00098';` |  |
| L129 | VALIDACIÓN_NULL | `IF cEmpresa IS NULL OR cEmpresa = '' THEN` |  |
| L155 | VALIDACIÓN_NULL | `IF iIdControl='' OR iIdControl IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pendientes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dg`, `?pendientes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_actualizastatusalerta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_actualizastatusalerta.sql` |
| **LOC (1er CREATE)** | 219 |
| **Callgraph** | ✅ fan_in=0 / fan_out=41 |
| **Deps concatenadas** | 121 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza [polisemia] Dictamen, estatus y alerta" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_actualizastatusalerta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTramaEnvios                 CHAR(250)
  pEstatus                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTramaEnvios` | `CHAR(250)` | — | — |
| `pEstatus` | `CHAR(1)` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescMensaje` | `CHAR(30)` | L9 |
| `cNumCte` | `CHAR(9)` | L10 |
| `cNumSucursal` | `CHAR(9)` | L11 |
| `cStatus` | `CHAR (1)` | L12 |
| `cSucursal` | `CHAR (4)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `cFechaInsert` | `DATETIME YEAR TO SECOND` | L15 |
| `iTotalReg` | `INTEGER` | L16 |
| `sMatches` | `SMALLINT` | L17 |
| `sDictaminados` | `SMALLINT` | L18 |
| `sTotal` | `SMALLINT` | L19 |
| `cOrigen` | `CHAR (1)` | L20 |
| `iPagina` | `INTEGER` | L21 |
| `sCtesAfore` | `SMALLINT` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_statusenviobuzon` | `bdicnweb` | no | SELECT | L75 |
| `sw_dic_statusenviobuzon` | `bdicnweb` | no | DELETE | L75 |
| `sw_dic_statusenviobuzon` | `bdicnweb` | no | INSERT | L77 |
| `si_bitacora_alerta_tmp` | `bdinteg` | ⚠️ sí | SELECT | L86 |
| `si_bitacora_comparaciones` | `bdinteg` | ⚠️ sí | SELECT | L115 |
| `si_bitacora_dictamenes` | `bdinteg` | ⚠️ sí | SELECT | L121 |
| `si_huella_linea_resultado` | `bdinteg` | ⚠️ sí | SELECT | L147 |
| `si_huella_linea_resultado_hist` | `bdinteg` | ⚠️ sí | SELECT | L156 |
| `si_bitacora_alerta_tmp` | `bdinteg` | ⚠️ sí | INSERT | L171 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |
| `sp_split_cadena` | `bdicnweb` | no | L81 |
| `sp_dicta_actualizastatusalerta` | `bdinteg` | ⚠️ sí | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_dicta_actualizasta` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L111 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L141 | FÓRMULA | `LET iTotalReg = iTotalReg + 1;` |  |
| L165 | FÓRMULA | `LET sMatches = sMatches - sCtesAfore;` |  |
| L168 | FÓRMULA | `LET sMatches = sMatches - sDictaminados;` |  |
| L181 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_dicta_actualizasta` |  |
| L183 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L188 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L194 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `alerta` | ENTIDAD | alerta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_dic_cilocconsultacausasitespeciales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_cilocconsultacausasitespeciales.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=82 |
| **Deps concatenadas** | 162 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta local [polisemia] Dictamen y causa (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cilocconsultacausasituacionesespeciales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_cilocconsultacausasitespeciales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  cSituacion                   CHAR(1)
  pCausa                       INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `cSituacion` | `CHAR(1)` | — | — |
| `pCausa` | `INTEGER` | `causa`=causa / motivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescripcion` | `CHAR(75)` | L10 |
| `cCausa` | `CHAR(3)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_cilocconsultacausasituacionesespeciales` | `bdicobranza` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicobranza:"c92357113".sp_cilocconsulta` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `cilocconsulta` | ACCION | consulta local (cobranza) | 🔵 CONVENCIÓN | nombre_sp |
| `causa` | ENTIDAD | causa / motivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?sit` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?eciales` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sit`, `?eciales` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consanalistadictamen`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consanalistadictamen.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, analista y dictamen" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dicta_consanalistadictamen` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consanalistadictamen(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `numAnalista` | `CHAR(8)` | L10 |
| `nomAnalista` | `CHAR(45)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_dicta_consanalistadictamen` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `analista` | ENTIDAD | analista | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dictamen` | ENTIDAD | dictamen | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_dic_consexpedientehuella`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consexpedientehuella.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, expediente y huella biométrica" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `cons_expediente_huella` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consexpedientehuella(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(4)
  pCliente                     CHAR(9)
  pNumRegs                     SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR(4)` | — | — |
| `pCliente` | `CHAR(9)` | — | — |
| `pNumRegs` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cCuenta` | `CHAR(20)` | L19 |
| `cProdNombre` | `CHAR(40)` | L20 |
| `cCodDocto` | `CHAR(4)` | L21 |
| `dFechAlta` | `DATE` | L22 |
| `cCodGrupo` | `CHAR(3)` | L23 |
| `cDescripGpo` | `CHAR(30)` | L24 |
| `cDescripDocto` | `CHAR(35)` | L25 |
| `cDescrip2` | `CHAR(30)` | L26 |
| `cMultImg` | `CHAR(1)` | L27 |
| `cSecuencia` | `SMALLINT` | L28 |
| `cImaEsnula` | `CHAR(1)` | L29 |
| `iCont` | `SMALLINT` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |
| `cons_expediente_huella` | `bdidigital` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEmpresa = '' OR pCliente = '' OR pNumRegs IS NULL THEN` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital@coppelimg_tcp:"informix".cons` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `expediente` | ENTIDAD | expediente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `huella` | ENTIDAD | huella biométrica | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_dic_consultacalles`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultacalles.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen y calle" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultacalles` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultacalles(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCalle                       INTEGER
  pNombre                      CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCalle` | `INTEGER` | `calle`=calle (domicilio) | ✅ CÓDIGO |
| `pNombre` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cMensajeRet` | `CHAR(80)` | L11 |
| `iCalle` | `INTEGER` | L12 |
| `cNombre` | `CHAR(30)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_consultacalles` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCalle IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_consultacalles';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `calle` | ENTIDAD | calle (domicilio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultaciudades`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultaciudades.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 126 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen y ciudades" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaciudades` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultaciudades(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEstado                      CHAR(2)
  pNumCiudad                   CHAR(3)
  pNomCiudad                   CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEstado` | `CHAR(2)` | — | — |
| `pNumCiudad` | `CHAR(3)` | — | — |
| `pNomCiudad` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cMensajeRet` | `CHAR(80)` | L12 |
| `cCiudad` | `CHAR(2)` | L13 |
| `cNombre` | `CHAR(30)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_consultaciudades` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEstado IS NULL OR pNumCiudad IS NULL OR pNumCiudad = '' THEN` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_consultaciudades';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ciudades` | ENTIDAD | ciudades (catálogo) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_dic_consultaclienteseindividual`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultaclienteseindividual.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=106 |
| **Deps concatenadas** | 321 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, clientes y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaclienteseindividual` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultaclienteseindividual(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(3)
  pNumCte                      CHAR(20)
  ptipoBusqueda                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `ptipoBusqueda` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cNumCredito` | `CHAR(20)` | L15 |
| `cNumTarjeta` | `CHAR(20)` | L16 |
| `cSitEspecial` | `CHAR(1)` | L17 |
| `cCausa` | `SMALLINT` | L18 |
| `cDescripcion` | `CHAR(75)` | L19 |
| `dFechaMod` | `DATETIME YEAR TO SECOND` | L20 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_consultaclienteseindividual` | `bdisitesp` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEmpresa = '' OR pNumCte = '' OR ptipoBusqueda IS NULL THEN` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdisitesp:"informix".sp_consultaclientes` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clientes` | ENTIDAD | clientes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?eindiv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ual` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?eindiv`, `?ual` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultacoloniascp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultacoloniascp.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ✅ fan_in=0 / fan_out=43 |
| **Deps concatenadas** | 125 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, colonias y código postal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultacoloniascp` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultacoloniascp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEstado                      CHAR(2)
  pNumCiudad                   CHAR(3)
  pNumColonia                  INTEGER
  pNomZona                     CHAR(32)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEstado` | `CHAR(2)` | — | — |
| `pNumCiudad` | `CHAR(3)` | — | — |
| `pNumColonia` | `INTEGER` | — | — |
| `pNomZona` | `CHAR(32)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cMensajeRet` | `CHAR(80)` | L14 |
| `iColonia` | `INTEGER` | L15 |
| `cNombre` | `CHAR(32)` | L16 |
| `iCodigoPostal` | `INTEGER` | L17 |
| `cUnidadHabitacional` | `CHAR(1)` | L18 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |
| `sp_consultacoloniascp` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEstado = '' OR pNumCiudad = '' OR pNumColonia IS NULL THEN` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_consultacoloniascp` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `colonias` | ENTIDAD | colonias (catálogo domicilio) | 🔵 CONVENCIÓN | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |

---

## `sp_dic_consultactecoincidencia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultactecoincidencia.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, cliente y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultactecoincidencia` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultactecoincidencia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pEmpresa                     CHAR(3)
  pTpDireccion                 INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumCte` | `CHAR(9)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pTpDireccion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `cCodRetSp` | `CHAR(6)` | L28 |
| `iCodRetSp` | `INTEGER` | L29 |
| `cNombre1` | `CHAR(40)` | L30 |
| `cNombre2` | `CHAR(40)` | L31 |
| `cApPaterno` | `CHAR(40)` | L32 |
| `cApMaterno` | `CHAR(40)` | L33 |
| `cRFC` | `CHAR(13)` | L34 |
| `cFechNacimiento` | `CHAR(10)` | L35 |
| `cSexo` | `CHAR(1)` | L36 |
| `cDescTpPersona` | `CHAR(20)` | L37 |
| `cFechaAlta` | `CHAR(10)` | L38 |
| `cSucursal` | `CHAR(4)` | L39 |
| `cNomCalle` | `CHAR(30)` | L40 |
| `cNumeroExt` | `CHAR(10)` | L41 |
| `cNumeroInt` | `CHAR(10)` | L42 |
| `cDepto` | `CHAR(6)` | L43 |
| `cNomColonia` | `CHAR(30)` | L44 |
| `cNomMunicipio` | `CHAR(30)` | L45 |
| `cNomCiudad` | `CHAR(30)` | L46 |
| `cNomEstado` | `CHAR(30)` | L47 |
| `cTelParticular` | `CHAR(10)` | L48 |
| `cTelCelular` | `CHAR(10)` | L49 |
| `cTelTrabajo` | `CHAR(10)` | L50 |
| *…1 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L99 |
| `sp_consultactecoincidencia` | `bdinteg` | ⚠️ sí | L104 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_consultactecoincid` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00020';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?coinc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?encia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?coinc`, `?encia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultactesdictamenhawk`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultactesdictamenhawk.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 124 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, clientes y dictamen" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultactesdictamenhawk(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                CHAR(1)
  pSucursal                    CHAR(4)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pNumCte                      CHAR(20)
  pTipoDictamen                CHAR(1)
  pAnalista                    CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pTipoDictamen` | `CHAR(1)` | `dic`=[polisemia] Dictamen (bdicnweb:sp_dic_* — decisión crediticia, analista de dictamen, hawk) | Diciembre (columna dic en tablas de series mensuales ene…dic) · `dictamen`=dictamen | ✅ CÓDIGO |
| `pAnalista` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `iCodRetSp` | `INTEGER` | L27 |
| `dFechaAlerta` | `DATETIME YEAR TO SECOND` | L28 |
| `dFechaAtendida` | `DATETIME YEAR TO SECOND` | L29 |
| `sCoincidencias` | `SMALLINT` | L30 |
| `cSucursal` | `CHAR(4)` | L31 |
| `cNumEmpProm` | `CHAR(8)` | L32 |
| `cNombrePromotor` | `CHAR(45)` | L33 |
| `cNumCte` | `CHAR(20)` | L34 |
| `cNomCte` | `CHAR(100)` | L35 |
| `cEmpAnalista` | `CHAR(8)` | L36 |
| `cNomAnalista` | `CHAR(45)` | L37 |
| `cTiempoResp` | `CHAR(20)` | L38 |
| `cNumCteCoinc` | `CHAR(20)` | L39 |
| `cDescCoinc` | `CHAR(25)` | L40 |
| `cOrigen` | `CHAR(1)` | L41 |
| `cDescripOrigen` | `CHAR(30)` | L42 |
| `cNomSucursal` | `CHAR(40)` | L43 |
| `cEstadoSuc` | `CHAR(30)` | L44 |
| `cCiudadSuc` | `CHAR(60)` | L45 |
| `iRecuperacion` | `INTEGER` | L46 |
| `cDescripDict` | `CHAR(50)` | L47 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_ctesdictamenhawktmp` | `bdicnweb` | no | SELECT | L122 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L114 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L126 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `dictamen` | ENTIDAD | dictamen | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?hawk` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hawk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultactesdictamenhawk_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultactesdictamenhawk_totales.sql` |
| **LOC (1er CREATE)** | 179 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 123 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, clientes y dictamen (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultactesdictamenhawk_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                CHAR(1)
  pSucursal                    CHAR(4)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pNumCte                      CHAR(20)
  pTipoDictamen                CHAR(1)
  pAnalista                    CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pTipoDictamen` | `CHAR(1)` | `dic`=[polisemia] Dictamen (bdicnweb:sp_dic_* — decisión crediticia, analista de dictamen, hawk) | Diciembre (columna dic en tablas de series mensuales ene…dic) · `dictamen`=dictamen | ✅ CÓDIGO |
| `pAnalista` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `dFechaAlerta` | `DATETIME YEAR TO SECOND` | L11 |
| `dFechaAtendida` | `DATETIME YEAR TO SECOND` | L12 |
| `sCoincidencias` | `SMALLINT` | L13 |
| `cSucursal` | `CHAR(4)` | L14 |
| `cNumEmpProm` | `CHAR(8)` | L15 |
| `cNombrePromotor` | `CHAR(45)` | L16 |
| `cNumCte` | `CHAR(20)` | L17 |
| `cNomCte` | `CHAR(100)` | L18 |
| `cEmpAnalista` | `CHAR(8)` | L19 |
| `cNomAnalista` | `CHAR(45)` | L20 |
| `cTiempoResp` | `CHAR(20)` | L21 |
| `cNumCteCoinc` | `CHAR(20)` | L22 |
| `cDescCoinc` | `CHAR(25)` | L23 |
| `cOrigen` | `CHAR(1)` | L24 |
| `cDescripOrigen` | `CHAR(30)` | L25 |
| `cNomSucursal` | `CHAR(40)` | L26 |
| `cEstadoSuc` | `CHAR(30)` | L27 |
| `cCiudadSuc` | `CHAR(60)` | L28 |
| `cDescripDict` | `CHAR(50)` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_statusconsctesdichawk` | `bdicnweb` | no | SELECT | L115 |
| `sw_dic_statusconsctesdichawk` | `bdicnweb` | no | DELETE | L115 |
| `sw_dic_statusconsctesdichawk` | `bdicnweb` | no | INSERT | L117 |
| `sw_dic_ctesdictamenhawktmp` | `bdicnweb` | no | SELECT | L121 |
| `sw_dic_ctesdictamenhawktmp` | `bdicnweb` | no | DELETE | L121 |
| `sw_dic_ctesdictamenhawktmp` | `bdicnweb` | no | INSERT | L143 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L107 |
| `sp_dicta_consultactesdictamen2` | `bdinteg` | ⚠️ sí | L124 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L131 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L155 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `dictamen` | ENTIDAD | dictamen | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?hawk_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hawk_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultaempleadoiccat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultaempleadoiccat.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, empleado y ICCAT — canal de atención al cliente en BPI; gestiona solicitudes de entrega y reposición de token, desbloqueo de acceso" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_empleado_iccat` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultaempleadoiccat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR (3)
  pNumEmp                      CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR (3)` | — | — |
| `pNumEmp` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cNomEmpleado` | `CHAR(45)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_consulta_empleado_iccat` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_consulta_empleado_` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00072';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `empleado` | ENTIDAD | empleado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `iccat` | ENTIDAD | ICCAT — canal de atención al cliente en BPI; gestiona solici | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_dic_consultaestados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultaestados.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen y estado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaestados` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultaestados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEstado                      CHAR(2)
  pNombre                      CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEstado` | `CHAR(2)` | `estado`=estado (entidad federativa / estatus) | ✅ CÓDIGO |
| `pNombre` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cMensajeRet` | `CHAR(80)` | L11 |
| `cEstado` | `CHAR(2)` | L12 |
| `cNombre` | `CHAR(30)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_consultaestados` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEstado IS NULL THEN` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_consultaestados';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estado` | ENTIDAD | estado (entidad federativa / estatus) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultalertacomph`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultalertacomph.sql` |
| **LOC (1er CREATE)** | 118 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dicta_consultalertacomph2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultalertacomph(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pModo                        SMALLINT
  pSkyp                        INTEGER
  pStatus                      CHAR (1)
  pNumctenvo                   CHAR (20)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pModo` | `SMALLINT` | — | — |
| `pSkyp` | `INTEGER` | — | — |
| `pStatus` | `CHAR (1)` | — | — |
| `pNumctenvo` | `CHAR (20)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(6)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cStatusAlerta` | `CHAR(1)` | L19 |
| `cSucursal` | `CHAR (4)` | L20 |
| `cCliente` | `CHAR (20)` | L21 |
| `cMatches` | `SMALLINT` | L22 |
| `dFechaInsert` | `DATE` | L23 |
| `cOrigen` | `CHAR (1)` | L24 |
| `iTotalComparaciones` | `INTEGER` | L25 |
| `cHora` | `CHAR(5)` | L26 |
| `cAnalistaFraudes` | `CHAR(8)` | L27 |
| `cDescripcionOrigen` | `CHAR(30)` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |
| `sp_dicta_consultalertacomph2` | `bdinteg` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L60 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pModo IS NULL OR pModo NOT IN (1,2) THEN` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L88 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_dicta_consultalertacomph2';` |  |
| L93 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?lerta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?h` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?lerta`, `?h` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultalertacomph_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultalertacomph_totales.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=109 |
| **Deps concatenadas** | 329 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen (complemento, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultalertacomph_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pModo                        SMALLINT
  pSkyp                        INTEGER
  pStatus                      CHAR (1)
  pNumctenvo                   CHAR (20)
  pFechaIni                    DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pModo` | `SMALLINT` | — | — |
| `pSkyp` | `INTEGER` | — | — |
| `pStatus` | `CHAR (1)` | — | — |
| `pNumctenvo` | `CHAR (20)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_statusconsultahuellas` | `bdicnweb` | no | SELECT | L63 |
| `sw_dic_statusconsultahuellas` | `bdicnweb` | no | DELETE | L63 |
| `sw_dic_statusconsultahuellas` | `bdicnweb` | no | INSERT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_dicta_consultalertacomph2_totales` | `bdinteg` | ⚠️ sí | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pModo IS NULL OR pModo NOT IN (1,2)  THEN` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_dicta_consultalertacomph2_tot` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?lerta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?h_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?lerta`, `?h_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultamatchhuellacte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultamatchhuellacte.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, tc — Tarjeta de Crédito, huella biométrica y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_matcheshuellacte` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultamatchhuellacte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNvoCteBco                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNvoCteBco` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cClienteMatch` | `CHAR(20)` | L13 |
| `cEmpresa` | `CHAR(4)` | L14 |
| `cDescripcion` | `CHAR(25)` | L15 |
| `sCteExiste` | `SMALLINT` | L16 |
| `cSucursal` | `CHAR(4)` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |
| `sp_consulta_matcheshuellacte` | `bdinteg` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_consulta_matcheshu` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?ma` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `?h` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `huella` | ENTIDAD | huella biométrica | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ma`, `?h` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultaparamdigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultaparamdigitalizacion.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, parámetro y digitalización de documentos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dgconsultaparametrosdigitalizacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultaparamdigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(3)
  pCodParam                    SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pCodParam` | `SMALLINT` | `param`=parámetro | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cValorParam` | `CHAR(100)` | L10 |
| `cDesParam` | `CHAR(50)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_dgconsultaparametrosdigitalizacion` | `bdidigital` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital:"informix".sp_dgconsultaparam` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00367';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '01071';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_dic_consultarcatsucursales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultarcatsucursales.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar [polisemia] Dictamen, catálogo y sucursal" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarcatsucursales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultarcatsucursales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(3)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(6)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cSucursal` | `CHAR(4)` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `cNombre` | `CHAR(40)` | L19 |
| `cDireccion1` | `CHAR(40)` | L20 |
| `cDireccion2` | `CHAR(40)` | L21 |
| `cTelefono1` | `CHAR(14)` | L22 |
| `cGerente` | `CHAR(40)` | L23 |
| `cSubgerente` | `CHAR(40)` | L24 |
| `cTipoSucursal` | `CHAR(2)` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |
| `sp_consultarcatsucursales` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_consultarcatsucurs` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultasucursales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultasucursales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen y sucursal" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultasucursales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNombreSuc` | `CHAR(40)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_consultasucursalesalertas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_consultasucursalesalertas.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=108 |
| **Deps concatenadas** | 325 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Dictamen, sucursal y alertas" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dicta_consultasucursalesalertas` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_consultasucursalesalertas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pNombreSucursal              CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | 🔵 CONVENCIÓN |
| `pNombreSucursal` | `CHAR(10)` | `sucursal`=sucursal | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(6)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cNumSuc` | `CHAR(4)` | L17 |
| `cNombreSuc` | `CHAR(40)` | L18 |
| `cDireccion` | `CHAR(40)` | L19 |
| `cCodCiu` | `CHAR(3)` | L20 |
| `cNombreCiu` | `CHAR(60)` | L21 |
| `cCodEst` | `CHAR(2)` | L22 |
| `cNombreEst` | `CHAR(30)` | L23 |
| `iTotsuc` | `INTEGER` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |
| `sp_dicta_consultasucursalesalertas` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_dicta_consultasucu` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00833';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '01070';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alertas` | ENTIDAD | alertas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_correciondatoscte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_correciondatoscte.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recepción [polisemia] Dictamen, datos y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bit_solicitudessos_sif` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_correciondatoscte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pTipoSol                     CHAR(20)
  pNombreInc                   CHAR (104)
  pFechaNacInc                 DATE
  pNumCteCorr                  CHAR(20)
  pNombreCorr                  CHAR(104)
  pFechaNacCorr                DATE
  pSucursal                    CHAR(4)
  pOrigen                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pTipoSol` | `CHAR(20)` | — | — |
| `pNombreInc` | `CHAR (104)` | — | — |
| `pFechaNacInc` | `DATE` | — | — |
| `pNumCteCorr` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pNombreCorr` | `CHAR(104)` | `rec`=recepción / recibe | 🔵 CONVENCIÓN |
| `pFechaNacCorr` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_bit_solicitudessos_sif` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCte = '' OR pTipoSol = '' OR pNombreInc = '' OR pFechaNac` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_bit_solicitudessos` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `?_cor` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_cor`, `?ion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_detallectesdictamenhawk`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_detallectesdictamenhawk.sql` |
| **LOC (1er CREATE)** | 172 |
| **Callgraph** | ✅ fan_in=0 / fan_out=41 |
| **Deps concatenadas** | 122 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Dictamen, detalle, clientes y dictamen" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_detallectesdictamenhawk(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `dFechaAlerta` | `DATETIME YEAR TO SECOND` | L23 |
| `dFechaAtendida` | `DATETIME YEAR TO SECOND` | L24 |
| `cOrigen` | `CHAR(1)` | L25 |
| `cDescripOrigen` | `CHAR(30)` | L26 |
| `cSucursal` | `CHAR(4)` | L27 |
| `cNomSucursal` | `CHAR(40)` | L28 |
| `cNumCte` | `CHAR(20)` | L29 |
| `cNomCte` | `CHAR(100)` | L30 |
| `cDescCoinc` | `CHAR(25)` | L31 |
| `cNumCteCoinc` | `CHAR(20)` | L32 |
| `cEmpAnalista` | `CHAR(8)` | L33 |
| `cNomAnalista` | `CHAR(45)` | L34 |
| `cTiempoResp` | `CHAR(20)` | L35 |
| `cTipoCoinc` | `CHAR(1)` | L36 |
| `cTipoDictamen` | `CHAR(1)` | L37 |
| `cNombre1` | `VARCHAR(26)` | L38 |
| `cNombre2` | `VARCHAR(26)` | L39 |
| `cApellPaterno` | `VARCHAR(26)` | L40 |
| `cApellMaterno` | `VARCHAR(26)` | L41 |
| `cDescripDict` | `CHAR(50)` | L42 |
| `cNombreCteCoinc` | `CHAR(104)` | L43 |
| `cTicket` | `CHAR(20)` | L44 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bitacora_dictamenes` | `bdinteg` | ⚠️ sí | SELECT | L100 |
| `se_catdictamenes` | `bdisitesp` | ⚠️ sí | SELECT | L107 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L112 |
| `si_empresa_huella` | `bdinteg` | ⚠️ sí | SELECT | L117 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L123 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L131 |
| `si_huella_linea` | `bdinteg` | ⚠️ sí | SELECT | L135 |
| `si_huella_linea_resultado` | `bdinteg` | ⚠️ sí | SELECT | L138 |
| `si_huella_linea_resultado_hist` | `bdinteg` | ⚠️ sí | SELECT | L143 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `dictamen` | ENTIDAD | dictamen | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?hawk` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hawk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_eliminabitadicta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_eliminabitadicta.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 161 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina [polisemia] Dictamen y dicta — dictamen" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_eliminabitadicta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmp                      CHAR(8)
  pNumcte                      CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmp` | `CHAR(8)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `dFecha` | `DATE` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bitacora_dictamenes` | `bdinteg` | ⚠️ sí | SELECT | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `?bita` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dicta` | ENTIDAD | dicta — dictamen / subsistema de dictaminación (sp_dicta_* — | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bita` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_enviarmonitoralertas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_enviarmonitoralertas.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía [polisemia] Dictamen, monitor y alertas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `envía` → `INSERT` encontrado en el cuerpo · `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_enviarmonitoralertas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTramaEnvios                 CHAR(250)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTramaEnvios` | `CHAR(250)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cNumCte` | `CHAR(9)` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_statusbuzonenvmonitor` | `bdicnweb` | no | SELECT | L43 |
| `sw_dic_statusbuzonenvmonitor` | `bdicnweb` | no | DELETE | L43 |
| `sw_dic_statusbuzonenvmonitor` | `bdicnweb` | no | INSERT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_split_cadena` | `bdicnweb` | no | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `envia` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `alertas` | ENTIDAD | alertas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_modactualizastatusalerta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_modactualizastatusalerta.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 201 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza [polisemia] Dictamen, estatus y alerta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dicta_actualizastatusalerta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_modactualizastatusalerta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
  pSucursal                    CHAR(4)
  pStatus                      CHAR(1)
  pFechaInsert                 DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pStatus` | `CHAR(1)` | `status`=estatus | 🔵 CONVENCIÓN |
| `pFechaInsert` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cDescMensaje` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_dicta_actualizastatusalerta` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_dicta_actualizasta` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `?_mod` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `alerta` | ENTIDAD | alerta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_mod` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_moddictamen`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_moddictamen.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 200 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Dictamen y dictamen" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dicta_modificaciondictamen` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_moddictamen(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(20)
  pSituacion                   CHAR(4)
  pCausa                       SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pSituacion` | `CHAR(4)` | — | — |
| `pCausa` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cDescMensaje` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_dicta_modificaciondictamen` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_dicta_modificacion` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `?_mod` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dictamen` | ENTIDAD | dictamen | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_mod` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_reevdparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_reevdparam.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Dictamen y parámetro" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dicta_reevdparam` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_reevdparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cTotalRegEncon` | `CHAR(8)` | L11 |
| `cTotalRegenDep` | `CHAR(8)` | L12 |
| `cTotalRegPen` | `CHAR(8)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_dicta_reevdparam` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_dicta_reevdparam';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `?_reevd` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_reevd` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_registrositespcte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_registrositespcte.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=84 |
| **Deps concatenadas** | 194 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Dictamen, registros y cliente (especial)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dicta_registrositespcte` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_registrositespcte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteNvo                      CHAR(20)
  BANCOPPEL                    pSucursal CHAR(4)
  2                            DISTINTA PERSONA
  3                            FRAUDE.															
																		pFechaDictaINI DATETIME YEAR TO SECOND
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteNvo` | `CHAR(20)` | `cte`=cliente | ✅ CÓDIGO |
| `BANCOPPEL` | `pSucursal CHAR(4)` | — | — |
| `2` | `DISTINTA PERSONA` | — | — |
| `3` | `FRAUDE.															
																		pFechaDictaINI DATETIME YEAR TO SECOND` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `iCodRetSp` | `INTEGER` | L24 |
| `cMensaje` | `CHAR(100)` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_dicta_registrositespcte` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCteNvo = '' OR pSitNvo = '' OR pCausaNvo IS NULL OR pCteMat ` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_dicta_registrosite` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `registros` | ENTIDAD | registros | 🔵 CONVENCIÓN | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?it` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_verificastatusbuzonenvmonitor`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_verificastatusbuzonenvmonitor.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica [polisemia] Dictamen, estatus y monitor" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_verificastatusbuzonenvmonitor(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cPathFile` | `CHAR(100)` | L14 |
| `cNomFile` | `CHAR(50)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_statusbuzonenvmonitor` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?buzonenv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?buzonenv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_verificastatusconsctesdichawk`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_verificastatusconsctesdichawk.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica [polisemia] Dictamen, estatus y clientes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_verificastatusconsctesdichawk(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cPathFile` | `CHAR(100)` | L14 |
| `cNomFile` | `CHAR(50)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_statusconsctesdichawk` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?hawk` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hawk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dic_verificastatusconsultahuellas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_verificastatusconsultahuellas.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica [polisemia] Dictamen, estatus y huellas biométricas" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_verificastatusconsultahuellas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cPathFile` | `CHAR(100)` | L14 |
| `cNomFile` | `CHAR(50)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_statusconsultahuellas` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `huellas` | ENTIDAD | huellas biométricas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_dic_verificastatusenviobuzon`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dic_verificastatusenviobuzon.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica [polisemia] Dictamen y estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dic_verificastatusenviobuzon(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cPathFile` | `CHAR(100)` | L14 |
| `cNomFile` | `CHAR(50)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_dic_statusenviobuzon` | `bdicnweb` | no | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `?buzon` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?buzon` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dinya_obtienetotalesordenpagosac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dinya_obtienetotalesordenpagosac.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene DINYA — sistema/plataforma de remesas domésticas en sucursal; retorna nombre_remitente, sucursal_origen, importe_eviado, orden de pago y Servicios de Atención al Cliente — subsistema de atención en sucursal (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_dinya_obtienetotales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dinya_obtienetotalesordenpagosac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(5)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cFechaEnvio` | `CHAR(10)` | L12 |
| `cEstatus` | `CHAR(20)` | L13 |
| `iTotalEnvios` | `INTEGER` | L14 |
| `mTotalImporteEnviado` | `MONEY(16,2)` | L15 |
| `iRegistros` | `INTEGER` | L16 |
| `iNoRegs` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_dinya_obtienetotales` | `bdisac` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L63 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L66 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dinya` | ENTIDAD | DINYA — sistema/plataforma de remesas domésticas en sucursal | 🟡 INFERIDO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `ordenpago` | ENTIDAD | orden de pago | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |

---

## `sp_dinyaobtienedetdiariosac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dinyaobtienedetdiariosac.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene DINYA — sistema/plataforma de remesas domésticas en sucursal; retorna nombre_remitente, sucursal_origen, importe_eviado y detalle (diarios)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_dinya_obtienedetdiario` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dinyaobtienedetdiariosac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(5)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `cCodRetSp` | `CHAR(5)` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `cFechaEnvio` | `CHAR(10)` | L24 |
| `cHoraEnvio` | `CHAR(8)` | L25 |
| `cNocontrol` | `CHAR(12)` | L26 |
| `cSucursalOrigen` | `CHAR(4)` | L27 |
| `cNombre1Rem` | `CHAR(26)` | L28 |
| `cNombre2Rem` | `CHAR(26)` | L29 |
| `cApellido1Rem` | `CHAR(26)` | L30 |
| `cApellido2Rem` | `CHAR(26)` | L31 |
| `mImporteEnviado` | `MONEY (16,2)` | L32 |
| `cFechaPago` | `CHAR(10)` | L33 |
| `cHoraPago` | `CHAR(8)` | L34 |
| `cSucCobropago` | `CHAR(4)` | L35 |
| `cNombre1ben` | `CHAR(26)` | L36 |
| `cNombre2ben` | `CHAR(26)` | L37 |
| `cApellido1ben` | `CHAR(26)` | L38 |
| `cApellido2ben` | `CHAR(26)` | L39 |
| `cEstatus` | `CHAR(20)` | L40 |
| `iRegistros` | `INTEGER` | L41 |
| `iNoRegs` | `INTEGER` | L42 |
| `iRecuperacion` | `INTEGER` | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_dinya_obtienedetdiario` | `bdisac` | ⚠️ sí | L99 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET	cCodRet = '00000';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L110 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L114 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L117 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dinya` | ENTIDAD | DINYA — sistema/plataforma de remesas domésticas en sucursal | 🟡 INFERIDO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `diarios` | MODIF | diarios | 🔵 CONVENCIÓN | nombre_sp |
| `?ac` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ac` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dinyaobtienemovconciliacionsac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dinyaobtienemovconciliacionsac.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene DINYA — sistema/plataforma de remesas domésticas en sucursal; retorna nombre_remitente, sucursal_origen, importe_eviado, movimiento y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dinyaobtienemovconciliacionsac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS CodigiRetoro,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cFechaHoy` | `DATE` | L13 |
| `mImporteEnviosNoCobrados` | `MONEY(16,2)` | L14 |
| `mImporteCuentaContable` | `MONEY(16,2)` | L15 |
| `cCuentaContable` | `CHAR(20)` | L16 |
| `mImporteCuentaCheques` | `MONEY(16,2)` | L17 |
| `cCuentaCheques` | `CHAR(20)` | L18 |
| `bEsApellidoValido` | `BOOLEAN` | L65 |
| `cApellido` | `CHAR(26)` | L66 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `apellidos_novalidos` | `bdicnweb` | no | SELECT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_dinya_obtienemovconciliacion` | `bdisac` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dinya` | ENTIDAD | DINYA — sistema/plataforma de remesas domésticas en sucursal | 🟡 INFERIDO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |

---

## `sp_dispersionafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dispersionafore.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "dispersión — dispersión de nómina y AFORE" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_validahoraejec`, `sp_afore_dispersion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_dispersionafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(30)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cHoraProceso` | `CHAR(21)` | L8 |
| `cHoraServidor` | `CHAR(21)` | L9 |
| `pTipoArchivo` | `CHAR(1)` | L10 |
| `cMensajeRet` | `CHAR(50)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `bInTrans` | `BOOLEAN` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L52 |
| `15` | `bdicnweb` | no | SELECT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `sp_validahoraejec` | `bdiprog` | ⚠️ sí | L59 |
| `sp_afore_dispersion` | `bdiprog` | ⚠️ sí | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | VALIDACIÓN_NULL | `IF cHoraProceso = '' OR cHoraProceso IS NULL THEN` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_validahoraejec';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00434';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_afore_dispersion';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00482';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00483';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00484';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00485';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00486';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00487';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00488';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00489';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00490';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00491';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00492';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00493';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00494';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00438';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00495';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00496';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00497';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00498';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00499';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00500';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00501';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00496';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00502';` |  |
| | *…13 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `dispersion` | ENTIDAD | dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN | nombre_sp |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_dr_abono_ref`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dr_abono_ref.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=81 |
| **Deps concatenadas** | 141 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "abono" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `abono_ref` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_dr_abono_ref(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(3)
  pSucursal                    CHAR(4)
  pTransacc                    CHAR(4)
  pTransuc                     CHAR(4)
  pFolio_suc                   CHAR(16)
  pCuenta                      CHAR(20)
  pDocto                       INTEGER
  pMto_tot                     MONEY(14,2)
  pMto_firme                   MONEY(14,2)
  pMto_sbc                     MONEY(14,2)
  pMto_rem                     MONEY(14,2)
  pDias_ret                    SMALLINT
  pDivisa                      CHAR(2)
  pReferencia                  CHAR(40)
  pNum_tarjeta                 CHAR(16)
  pUsuautoriza                 CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pTransacc` | `CHAR(4)` | — | — |
| `pTransuc` | `CHAR(4)` | — | — |
| `pFolio_suc` | `CHAR(16)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pDocto` | `INTEGER` | — | — |
| `pMto_tot` | `MONEY(14,2)` | — | — |
| `pMto_firme` | `MONEY(14,2)` | — | — |
| `pMto_sbc` | `MONEY(14,2)` | — | — |
| `pMto_rem` | `MONEY(14,2)` | — | — |
| `pDias_ret` | `SMALLINT` | — | — |
| `pDivisa` | `CHAR(2)` | — | — |
| `pReferencia` | `CHAR(40)` | `ref`=referencia | 🟡 INFERIDO |
| `pNum_tarjeta` | `CHAR(16)` | — | — |
| `pUsuautoriza` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cDescrip` | `CHAR(16)` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `abono_ref` | `bdicheq` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet ='00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `abono` | ENTIDAD | abono / crédito | 🔵 CONVENCIÓN | nombre_sp |
| `ref` | AMBIGUO | referencia | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dr_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dr_consultafecha`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dr_consultafecha.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 140 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fecha" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_dr_consultafecha(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `dFecha` | `DATE` | L7 |
| `iTotal` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | FÓRMULA | `LET iTotal = iTotal+1;` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dr_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dr_consultaparamlong`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dr_consultaparamlong.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 139 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dr_consultaparamlong(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cValor` | `CHAR(100)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?long` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dr_`, `?long` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dr_consultaparammoneda`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dr_consultaparammoneda.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 138 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_dr_consultaparammoneda(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cValor` | `CHAR(100)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `mon` | PREFIJO | monitor / módulo | 🟡 INFERIDO | nombre_sp |
| `?eda` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dr_`, `?eda` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dr_consultaparsistema`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dr_consultaparsistema.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 137 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sistema" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dr_consultaparsistema(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cValor` | `CHAR(2)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sistema` | `bdinteg` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?par` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sistema` | ENTIDAD | sistema | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dr_`, `?par` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dr_consultartdacoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dr_consultartdacoppel.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 136 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultartdacoppel` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_dr_consultartdacoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumTienda                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumTienda` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cTienda` | `CHAR(10)` | L8 |
| `cDescrip` | `CHAR(40)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_consultartdacoppel` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?tda` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `coppel` | ENTIDAD | Coppel (grupo) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dr_`, `?tda` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_dr_desc_ret`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_dr_desc_ret.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=79 |
| **Deps concatenadas** | 135 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Descripción" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_desc_ret` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_dr_desc_ret(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistema                     CHAR(2)
  pCodigoE                     CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(2)` | — | — |
| `pCodigoE` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCod` | `CHAR(3)` | L8 |
| `cDescrip` | `CHAR(50)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_desc_ret` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_dr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `?_ret` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dr_`, `?_ret` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_eliminadesactualizadasbc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_eliminadesactualizadasbc.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=6 / fan_out=11 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina Servicios de Atención al Cliente — subsistema de atención en sucursal y saldo básico de cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_eliminadesactualizadasbc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(4)
  pOpcion                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(4)` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicred` | `bdicnweb` | no | DELETE | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `?tualizada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sbc` | ENTIDAD | saldo básico de cuenta (SBC) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?de`, `?tualizada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_eliminase`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_eliminase.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 136 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_eliminase(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pSE                          CHAR(1)
  pCausa                       SMALLINT
  pTipoMovimiento              INTEGER
  pProcedencia                 INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pSE` | `CHAR(1)` | — | — |
| `pCausa` | `SMALLINT` | — | — |
| `pTipoMovimiento` | `INTEGER` | — | — |
| `pProcedencia` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNombreUsuario` | `CHAR(45)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verifica_detallemarcaje` | `bdicnweb` | no | SELECT | L42 |
| `sw_verifica_detallemarcaje` | `bdicnweb` | no | DELETE | L42 |
| `sw_verifica_detallemarcaje` | `bdicnweb` | no | INSERT | L45 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |
| `sp_eliminarse` | `bdisitesp` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisitesp:sp_eliminarse';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?se` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_eliminasignacionctesfusion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_eliminasignacionctesfusion.sql` |
| **LOC (1er CREATE)** | 49 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina clientes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_eliminasignacionctesfusion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_clientesasignados_soc` | `bdicnweb` | no | SELECT | L33 |
| `sw_tr_clientesasignados_soc` | `bdicnweb` | no | DELETE | L33 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L27 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?signacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `fusion` | ACCION | fusiona cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?signacion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_eliminasinprocesartmpcod40`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_eliminasinprocesartmpcod40.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=78 / fan_out=37 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina código" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_eliminasinprocesartmpcod40(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | DELETE | L34 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?sin` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?rtmp` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?40` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sin`, `?rtmp`, `?40` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_eliminasolsupervision`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_eliminasolsupervision.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=71 |
| **Deps concatenadas** | 178 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina solicitud y supervisión" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_depuratabla_sol_supervision`, `sp_elimina_sol_supervision` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_eliminasolsupervision(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdOperacion                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdOperacion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `cDesCodRetSp` | `CHAR(80)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_depuratabla_sol_supervision` | `bdisolic` | ⚠️ sí | L44 |
| `sp_elimina_sol_supervision` | `bdisolic` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_depuratabla_sol_sup` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_elimina_sol_supervi` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `supervision` | ENTIDAD | supervisión | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_encabezadoreportesemanalsac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_encabezadoreportesemanalsac.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "encabezado, reportes y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_encabezadoreportesemanalsac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(5)
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEncabezado1` | `CHAR(100)` | L10 |
| `cEncabezado2` | `CHAR(100)` | L11 |
| `cEncabezado3` | `CHAR(100)` | L12 |
| `cEncabezado4` | `CHAR(100)` | L13 |
| `cValor1` | `CHAR(5)` | L14 |
| `cValor2` | `CHAR(5)` | L15 |
| `cValor3` | `CHAR(5)` | L16 |
| `cValor4` | `CHAR(5)` | L17 |
| `iNumRows` | `INTEGER` | L18 |
| `cCategoria` | `CHAR(2)` | L19 |
| `cConvenio` | `CHAR(3)` | L20 |
| `cDireccionEmpresa` | `CHAR(100)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L61 |
| `si_catciudades` | `bdinteg` | ⚠️ sí | SELECT | L73 |
| `si_estados` | `bdinteg` | ⚠️ sí | SELECT | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | 🔵 CONVENCIÓN | nombre_sp |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?emanal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?emanal` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_enviardotacioncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_enviardotacioncaja.sql` |
| **LOC (1er CREATE)** | 347 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 209 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía dotación de efectivo y caja" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_enviardotacioncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolioOperacion              CHAR(8)
  pStatus                      CHAR(2)
  pCantidadEnviada1            DECIMAL(10,2)
  pTotalEnviada1               MONEY(14,2)
  pCantidadEnviada2            DECIMAL(10,2)
  pTotalEnviada2               MONEY(14,2)
  pCantidadEnviada3            DECIMAL(10,2)
  pTotalEnviada3               MONEY(14,2)
  pCantidadEnviada4            DECIMAL(10,2)
  pTotalEnviada4               MONEY(14,2)
  pCantidadEnviada5            DECIMAL(10,2)
  pTotalEnviada5               MONEY(14,2)
  pCantidadEnviada6            DECIMAL(10,2)
  pTotalEnviada6               MONEY(14,2)
  pMontoMorrallaEnv            MONEY(14,2)
  pTotalDotEnviada             MONEY(14,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolioOperacion` | `CHAR(8)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pCantidadEnviada1` | `DECIMAL(10,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pTotalEnviada1` | `MONEY(14,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pCantidadEnviada2` | `DECIMAL(10,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pTotalEnviada2` | `MONEY(14,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pCantidadEnviada3` | `DECIMAL(10,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pTotalEnviada3` | `MONEY(14,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pCantidadEnviada4` | `DECIMAL(10,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pTotalEnviada4` | `MONEY(14,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pCantidadEnviada5` | `DECIMAL(10,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pTotalEnviada5` | `MONEY(14,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pCantidadEnviada6` | `DECIMAL(10,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pTotalEnviada6` | `MONEY(14,2)` | `envia`=envía | 🔵 CONVENCIÓN |
| `pMontoMorrallaEnv` | `MONEY(14,2)` | — | — |
| `pTotalDotEnviada` | `MONEY(14,2)` | `envia`=envía | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `fCant1` | `FLOAT(8)` | L12 |
| `cDenominacion1` | `CHAR(18)` | L13 |
| `mTotal1` | `MONEY(14,2)` | L14 |
| `fCant2` | `FLOAT(8)` | L15 |
| `cDenominacion2` | `CHAR(18)` | L16 |
| `mTotal2` | `MONEY(14,2)` | L17 |
| `fCant3` | `FLOAT(8)` | L18 |
| `cDenominacion3` | `CHAR(18)` | L19 |
| `mTotal3` | `MONEY(14,2)` | L20 |
| `fCant4` | `FLOAT(8)` | L21 |
| `cDenominacion4` | `CHAR(18)` | L22 |
| `mTotal4` | `MONEY(14,2)` | L23 |
| `fCant5` | `FLOAT(8)` | L24 |
| `cDenominacion5` | `CHAR(18)` | L25 |
| `mTotal5` | `MONEY(14,2)` | L26 |
| `fCant6` | `FLOAT(8)` | L27 |
| `cDenominacion6` | `CHAR(18)` | L28 |
| `mTotal6` | `MONEY(14,2)` | L29 |
| `cDenMorralla` | `CHAR(2)` | L30 |
| `mTotalMorralla` | `MONEY(14,2)` | L31 |
| `mTotalDotSolicitada` | `MONEY(14,2)` | L32 |
| `fCantEnv_1` | `FLOAT(8)` | L33 |
| `cDenEnv_1` | `CHAR(18)` | L34 |
| *…40 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L189 |
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | SELECT | L197 |
| `ss_cajageneral` | `bdisuc` | ⚠️ sí | SELECT | L236 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L158 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L152 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFolioOperacion = '' OR pStatus = '' OR pStatus IS NULL THEN` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L254 | FÓRMULA | `LET fCantEnv_1 = fCantEnv_1 - NVL(pTotalEnviada1,0);` |  |
| L256 | FÓRMULA | `LET fCantEnv_1 = fCantEnv_1 - NVL(pCantidadEnviada1,0);` |  |
| L259 | FÓRMULA | `LET fCantEnv_2 = fCantEnv_2 - NVL(pTotalEnviada2,0);` |  |
| L261 | FÓRMULA | `LET fCantEnv_2 = fCantEnv_2 - NVL(pCantidadEnviada2,0);` |  |
| L264 | FÓRMULA | `LET fCantEnv_3 = fCantEnv_3 - NVL(pTotalEnviada3,0);` |  |
| L266 | FÓRMULA | `LET fCantEnv_3 = fCantEnv_3 - NVL(pCantidadEnviada3,0);` |  |
| L269 | FÓRMULA | `LET fCantEnv_4 = fCantEnv_4 - NVL(pTotalEnviada4,0);` |  |
| L271 | FÓRMULA | `LET fCantEnv_4 = fCantEnv_4 - NVL(pCantidadEnviada4,0);` |  |
| L274 | FÓRMULA | `LET fCantEnv_5 = fCantEnv_5 - NVL(pTotalEnviada5,0);` |  |
| L276 | FÓRMULA | `LET fCantEnv_5 = fCantEnv_5 - NVL(pCantidadEnviada5,0);` |  |
| L279 | FÓRMULA | `LET fCantEnv_6 = fCantEnv_6 - NVL(pTotalEnviada6,0);` |  |
| L281 | FÓRMULA | `LET fCantEnv_6 = fCantEnv_6 - NVL(pCantidadEnviada6,0);` |  |
| L284 | FÓRMULA | `LET fCantEnv_7 = fCantEnv_7 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L288 | FÓRMULA | `LET fCantEnv_8 = fCantEnv_8 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L291 | FÓRMULA | `LET fCantEnv_9 = fCantEnv_9 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L294 | FÓRMULA | `LET fCantEnv_10 = fCantEnv_10 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L297 | FÓRMULA | `LET fCantEnv_11 = fCantEnv_11 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L300 | FÓRMULA | `LET fCantEnv_12 = fCantEnv_12 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L303 | FÓRMULA | `LET fCantEnv_13 = fCantEnv_13 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L306 | FÓRMULA | `LET fCantEnv_14 = fCantEnv_14 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L309 | FÓRMULA | `LET fCantEnv_15 = fCantEnv_15 - NVL(pMontoMorrallaEnv,0);` | 🔴 MONEY/aritmética financiera |
| L331 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `envia` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_envio_dotacion_masiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_envio_dotacion_masiva.sql` |
| **LOC (1er CREATE)** | 323 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía dotación de efectivo (masiva)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `envía` → `INSERT` encontrado en el cuerpo · `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_envio_dotacion_masiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATETIME YEAR TO SECOND
  pETV                         INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATETIME YEAR TO SECOND` | — | — |
| `pETV` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsqlerr` | `INTEGER` | L4 |
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSpl` | `CHAR(5)` | L6 |
| `cSQL` | `CHAR(4000)` | L7 |
| `cContErrores` | `INTEGER` | L9 |
| `cDivisa` | `CHAR(2)` | L11 |
| `ctransaccion` | `CHAR(4)` | L12 |
| `cIdRegistro` | `INTEGER` | L13 |
| `cfechaSolicitud` | `DATE` | L14 |
| `cfechaAplicacion` | `DATE` | L15 |
| `cccSucursal` | `CHAR(4)` | L16 |
| `cnombreSucursal` | `CHAR(40)` | L17 |
| `cplaza` | `CHAR(50)` | L18 |
| `ctipoOperacion` | `CHAR(20)` | L19 |
| `cbillete1000` | `SMALLINT` | L20 |
| `cbillete500` | `SMALLINT` | L21 |
| `cbillete200` | `SMALLINT` | L22 |
| `cbillete100` | `SMALLINT` | L23 |
| `cbillete50` | `SMALLINT` | L24 |
| `cbillete20` | `SMALLINT` | L25 |
| `ctotalBilletes` | `INTEGER` | L26 |
| `cmoneda100` | `SMALLINT` | L27 |
| `cmoneda50` | `SMALLINT` | L28 |
| `cmoneda20` | `SMALLINT` | L29 |
| `cmoneda10` | `SMALLINT` | L30 |
| *…29 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `arch_dotacion_sucursal` | `bdicnweb` | no | INSERT | L163 |
| `tmp_arch_dot_suc_det` | `bdicnweb` | no | SELECT | L172 |
| `tmp_arch_dot_suc_det` | `bdicnweb` | no | DELETE | L177 |
| `arch_dotacion_sucursal` | `bdicnweb` | no | SELECT | L188 |
| `arch_dotacion_sucursal` | `bdicnweb` | no | UPDATE | L258 |
| `ss_mae_entradasalida` | `bdisuc` | ⚠️ sí | UPDATE | L275 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | UPDATE | L283 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L152 |
| `sp_soldocta_ws` | `bdisuc` | ⚠️ sí | L214 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L192 | FÓRMULA | `LET cImporte1000 = cbillete1000 * 1000;` | 🔴 MONEY/aritmética financiera |
| L193 | FÓRMULA | `LET cImporte500 = cbillete500 * 500;` | 🔴 MONEY/aritmética financiera |
| L194 | FÓRMULA | `LET cImporte200 = cbillete200 * 200;` | 🔴 MONEY/aritmética financiera |
| L195 | FÓRMULA | `LET cImporte100 = cbillete100 * 100;` | 🔴 MONEY/aritmética financiera |
| L196 | FÓRMULA | `LET cImporte50 = cbillete50 * 50;` | 🔴 MONEY/aritmética financiera |
| L197 | FÓRMULA | `LET cImporte20 = cbillete20 * 20;` | 🔴 MONEY/aritmética financiera |
| L198 | FÓRMULA | `LET ctotalBilletes = cImporte1000 + cImporte500 + cImporte200 + cImporte100 + cImporte50 + cImporte2` | 🔴 MONEY/aritmética financiera |
| L201 | FÓRMULA | `LET cImporteM100 = cmoneda100 * 100;` | 🔴 MONEY/aritmética financiera |
| L204 | FÓRMULA | `LET cImporteM10 = cmoneda10 * 10;` | 🔴 MONEY/aritmética financiera |
| L205 | FÓRMULA | `LET cImporteM5 = cmoneda5 * 5;` | 🔴 MONEY/aritmética financiera |
| L206 | FÓRMULA | `LET cImporteM2 = cmoneda2 * 2;` | 🔴 MONEY/aritmética financiera |
| L207 | FÓRMULA | `LET cImporteM1 = cmoneda1 * 1;` | 🔴 MONEY/aritmética financiera |
| L208 | FÓRMULA | `LET cImporteM50c = cmoneda50c * 0.5;` | 🔴 MONEY/aritmética financiera |
| L210 | FÓRMULA | `LET ctotalMonedas = cImporteM100 + cImporteM50 + cImporteM20 + cImporteM10 + cImporteM5 + cImporteM2` | 🔴 MONEY/aritmética financiera |
| L212 | FÓRMULA | `LET ctotalImporte = ctotalBilletes + ctotalMonedas;` | 🔴 MONEY/aritmética financiera |
| L257 | FÓRMULA | `LET cContErrores=cContErrores+1;` |  |
| L279 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L287 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L294 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L297 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L303 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_envios_dotacion_masiva_genrep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_envios_dotacion_masiva_genrep.sql` |
| **LOC (1er CREATE)** | 281 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte envíos y dotación de efectivo (masiva)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_envios_dotacion_masiva_genrep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATETIME YEAR TO SECOND
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATETIME YEAR TO SECOND` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCmd1` | `CHAR(4000)` | L8 |
| `cSql` | `CHAR(4000)` | L9 |
| `cRutaGral` | `CHAR(150)` | L10 |
| `cNombreArchivo` | `CHAR(100)` | L11 |
| `cNombreArchivo2` | `CHAR(100)` | L12 |
| `bInTransaction` | `BOOLEAN` | L13 |
| `ven_transacc` | `SMALLINT` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `iTotal` | `INTEGER` | L16 |
| `dFechaHoy` | `DATE` | L17 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L18 |
| `cFechaHoraArchivo` | `CHAR(35)` | L19 |
| `nomFecha` | `CHAR(19)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L88 | FÓRMULA | `LET cRutaGral = TRIM(pRutaDescarga)\|\|'/'\|\|TRIM(cNombreArchivo);` |  |
| L120 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L125 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L134 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L143 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L166 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L174 | FÓRMULA | `LET cRutaGral = TRIM(pRutaDescarga)\|\| '/'\|\|TRIM(cNombreArchivo2);` |  |
| L206 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L211 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L220 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L229 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `envios` | ENTIDAD | envíos | 🔵 CONVENCIÓN | nombre_sp |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |

---

## `sp_errores_dotacion_masiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_errores_dotacion_masiva.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "error y dotación de efectivo (masiva)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_errores_dotacion_masiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATETIME YEAR TO SECOND
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATETIME YEAR TO SECOND` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cNoRegistro` | `SMALLINT` | L10 |
| `cCampo` | `CHAR(50)` | L11 |
| `cDescripcion` | `CHAR(100)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_arch_dot_suc_err` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabazonacallexasignar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_grabazonacallexasignar.sql` |
| **LOC (1er CREATE)** | 149 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "graba zona y calle" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `grabazonacallexasignar` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_grabazonacallexasignar(
  pIdFuncion                   CHAR(10)
  pTipo                        SMALLINT
  pCiudad                      SMALLINT
  pClave                       INTEGER
  pDesc                        CHAR(30)
  pRumbo                       CHAR(40)
  pCodigoP                     INTEGER
  pMunicipio                   CHAR(27)
  pPoblacionZona               CHAR(30)
  pUsuario                     CHAR(8)
  pConfirma                    CHAR(1)
) RETURNING CHAR(5) AS CodRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `SMALLINT` | — | — |
| `pCiudad` | `SMALLINT` | — | — |
| `pClave` | `INTEGER` | — | — |
| `pDesc` | `CHAR(30)` | — | — |
| `pRumbo` | `CHAR(40)` | — | — |
| `pCodigoP` | `INTEGER` | — | — |
| `pMunicipio` | `CHAR(27)` | — | — |
| `pPoblacionZona` | `CHAR(30)` | `zona`=zona | 🔵 CONVENCIÓN |
| `pUsuario` | `CHAR(8)` | — | — |
| `pConfirma` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L36 |
| `cCodRetSp` | `CHAR(5)` | L37 |
| `iSqlErr` | `INTEGER` | L38 |
| `cExist` | `CHAR(1)` | L39 |
| `cNumeroCalle` | `INTEGER` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L72 |
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L112 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `grabazonacallexasignar` | `bdinteg` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00107';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00195';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L145 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L151 | CÓDIGO_RETORNO | `LET cCodRet = '00107';` |  |
| L155 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `zona` | ENTIDAD | zona | 🔵 CONVENCIÓN | nombre_sp |
| `calle` | ENTIDAD | calle (domicilio) | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_esnombre_apellido_abreviado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_esnombre_apellido_abreviado.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ✅ fan_in=1 / fan_out=1 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reversión nombre y apellido" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_esnombre_apellido_abreviado(
  pPalabra                     CHAR(26)
) RETURNING BOOLEAN AS es_palabra_abreviada
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pPalabra` | `CHAR(26)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `bEsPalabraAbreviada` | `BOOLEAN` | L4 |
| `cPalabra` | `CHAR(26)` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `apellido_nombre_abreviado` | `bdicnweb` | no | SELECT | L18 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `apellido` | ENTIDAD | apellido | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?_ab` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?iado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_es`, `?_ab`, `?iado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_esnombre_valido`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_esnombre_valido.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ✅ fan_in=1 / fan_out=1 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida nombre" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_esnombre_valido(
  pNombre                      CHAR(26)
) RETURNING BOOLEAN AS es_nombre_valido
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNombre` | `CHAR(26)` | `nombre`=nombre | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `bEsNombreValido` | `BOOLEAN` | L4 |
| `cNombre` | `CHAR(26)` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `nombres_novalidos` | `bdicnweb` | no | SELECT | L18 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `nombre` | ENTIDAD | nombre | 🔵 CONVENCIÓN | nombre_sp |
| `valid` | ACCION | valida | 🟡 INFERIDO | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_es`, `?o` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_espalabra_altisonante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_espalabra_altisonante.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ✅ fan_in=1 / fan_out=1 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "(especial, anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_espalabra_altisonante(
  pPalabra                     CHAR(10)
) RETURNING BOOLEAN AS es_palabra_altisonante
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pPalabra` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `bEsPalabraAltisonante` | `BOOLEAN` | L4 |
| `cPalabra` | `CHAR(10)` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `palabra_inconveniente` | `bdicnweb` | no | SELECT | L18 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?alabra_altison` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?alabra_altison`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_esvocal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_esvocal.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ✅ fan_in=1 / fan_out=1 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "sv — supervisión/servicio y [polisemia] Cálculo" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_esvocal(
  pLetra                       CHAR(1)
) RETURNING BOOLEAN as esvocal
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pLetra` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `bEsVocal` | `BOOLEAN` | L4 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sv` | ENTIDAD | sv — supervisión/servicio (abreviación — bdiaclaracion) | 🔴 SINTÉTICO | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cal` | ENTIDAD | [polisemia] Cálculo (cal_fecha, cal_riesgo_cliente, cal_trad | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_e`, `sv`, `?o` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evaldispefeccred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evaldispefeccred.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 251 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cc — cuenta corriente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_evaldispefec_cred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_evaldispefeccred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pMonto                       MONEY(16,2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pMonto` | `MONEY(16,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRet` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `iNumRegistros` | `INTEGER` | L14 |
| `cPorc_disp_efec` | `CHAR(3)` | L15 |
| `dMonto_disp` | `DECIMAL(18,2)` | L17 |
| `cMsj_disp_efec` | `CHAR(100)` | L18 |
| `cEntero` | `CHAR(3)` | L19 |
| `cDecimal` | `CHAR(2)` | L20 |
| `dPorc_disp_efec` | `DECIMAL(5,2)` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_evaldispefec_cred` | `bdicred` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP bdicred:sp_evaldispefec_cred';` |  |
| L85 | FÓRMULA | `LET cEntero = SUBSTR(cPorc_disp_efec, 1, CHARINDEX('.', cPorc_disp_efec) - 1);` |  |
| L86 | FÓRMULA | `LET cDecimal = SUBSTR(cPorc_disp_efec, CHARINDEX('.', cPorc_disp_efec) + 1);` | 🔴 MONEY/aritmética financiera |
| L88 | FÓRMULA | `LET dPorc_disp_efec = dPorc_disp_efec/100;` |  |
| L89 | FÓRMULA | `LET cPorc_disp_efec = dPorc_disp_efec/100;` |  |
| L93 | FÓRMULA | `LET dPorc_disp_efec = cPorc_disp_efec::INTEGER/100;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_evaldi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?efe` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?red` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_evaldi`, `?efe`, `?red` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_borra_repetidosexcluir`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_borra_repetidosexcluir.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "borra EVC — Evaluación/Cartera a Quebrantar, identificador y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_borra_repetidosexcluir(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `cID_FUNCIONC` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vStatusSol` | `CHAR(2)` | L11 |
| `vHoy` | `DATE` | L12 |
| `dFechaEnt` | `DATE` | L13 |
| `vCausaSol` | `CHAR(3)` | L14 |
| `P_COD_RET` | `VARCHAR(5)` | L15 |
| `cNumcte` | `CHAR(20)` | L16 |
| `cCodRet` | `CHAR(6)` | L17 |
| `cMensajeRet` | `CHAR(100)` | L18 |
| `iValido` | `INTEGER` | L19 |
| `cSucursal` | `CHAR(4)` | L20 |
| `cNumProd` | `CHAR(4)` | L21 |
| `vRegistro` | `DECIMAL(18,2)` | L22 |
| `vMensajeStatus` | `CHAR(80)` | L23 |
| `iSqlErr` | `INT` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_evc_excluidos` | `bdicnweb` | no | SELECT | L72 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `borra` | ACCION | borra / elimina registros (borramovs_movhis, borramovscfd*) | 🟡 INFERIDO | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?et` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?cluir` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?et`, `?e`, `?cluir` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_cartera_quebrantar_archivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_cartera_quebrantar_archivo.sql` |
| **LOC (1er CREATE)** | 216 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "quebranto — write-off de cartera vencida EVC — Evaluación/Cartera a Quebrantar, cartera de crédito y archivo (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_evc_excluyecliente`, `sp_rep_cartera_quebrantar_archivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_cartera_quebrantar_archivo(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  sfecha                       date
  ctipoarchivo                 char(1)
) RETURNING char (5) as Resultado, char(80) as Mensaje
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `sfecha` | `date` | — | — |
| `ctipoarchivo` | `char(1)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `pNum_Vencidos` | `Smallint` | L25 |
| `cRfc` | `char(13)` | L28 |
| `cApellidoCasada` | `char(26)` | L29 |
| `cSector` | `char(2)` | L31 |
| `dFechaNac` | `date` | L32 |
| `cCurp` | `char(20)` | L33 |
| `cSexo` | `char(1)` | L34 |
| `cEdoCivil` | `char(2)` | L35 |
| `cNumIdentificacion` | `char(30)` | L36 |
| `cEmail` | `char(60)` | L38 |
| `cTipoIdentificacion` | `char(40)` | L39 |
| `cNacionalidad` | `char(15)` | L40 |
| `cPoblacion` | `char(80)` | L43 |
| `cCodPostal` | `char(5)` | L48 |
| `cPuntoCardinal` | `char(1)` | L49 |
| `cDepartamento` | `char(6)` | L52 |
| `cComplemento` | `char(80)` | L53 |
| `cEntreCalles` | `char(40)` | L54 |
| `sOtros` | `smallint` | L55 |
| `mIngresoMensual` | `money(14,2)` | L57 |
| `cPuesto` | `char(3)` | L58 |
| `cLugarTrabajo` | `char(25)` | L59 |
| `sElementoRes` | `smallint` | L62 |
| `cDescripcion` | `char(80)` | L63 |
| `sElemResTrabajo` | `smallint` | L64 |
| *…54 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_param` | `bdicred` | ⚠️ sí | SELECT | L181 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_evc_excluyecliente` | `bdicnweb` | no | L197 |
| `sp_rep_cartera_quebrantar_archivo` | `bdicred` | ⚠️ sí | L199 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L184 | VALIDACIÓN_NULL | `IF TRIM(vexcluir) <> '0' OR vexcluir IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | 🟡 INFERIDO | nombre_sp |
| `cartera` | ENTIDAD | cartera de crédito | 🔵 CONVENCIÓN | nombre_sp |
| `quebr` | ACCION | quebranto — write-off de cartera vencida (bdicred) | 🟡 INFERIDO | nombre_sp |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?ar_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ar_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_consexclusionlote`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_consexclusionlote.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta EVC — Evaluación/Cartera a Quebrantar y lote" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_consexclusionlote(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  cLote                        INTEGER
  cNumRegistros                integer
  cNumRecuperacion             Integer
) RETURNING char (5)	as codigoRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cLote` | `INTEGER` | `lote`=lote (proceso batch) | ✅ CÓDIGO |
| `cNumRegistros` | `integer` | — | — |
| `cNumRecuperacion` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cMensajeRet` | `CHAR(100)` | L19 |
| `iCodRet` | `INTEGER` | L20 |
| `SCodRet` | `CHAR(5)` | L21 |
| `vidRegistro` | `integer` | L23 |
| `vStatus` | `char(1)` | L24 |
| `vMotivoRechazo` | `char(50)` | L25 |
| `vNumCte` | `char(20)` | L26 |
| `vNumCuenta` | `char(20)` | L27 |
| `vNombre1` | `char(26)` | L28 |
| `vNombre2` | `char(26)` | L29 |
| `vApell_paterno` | `char(26)` | L30 |
| `vApell_materno` | `char(26)` | L31 |
| `vMotivo` | `char(1)` | L32 |
| `vProducto` | `char(4)` | L33 |
| `dtFechaReporte` | `DATE` | L34 |
| `vDescripcion` | `char(50)` | L35 |
| `iContador` | `INTEGER` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cb_rep_cart_quebrantar` | `bdicobranza` | ⚠️ sí | SELECT | L75 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L88 |
| `sw_evc_excluidos` | `bdicnweb` | no | SELECT | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | CÓDIGO_RETORNO | `LET SCodRet     ='00000';` |  |
| L110 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?clusion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `lote` | ENTIDAD | lote (proceso batch) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?clusion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_consexclusionlotetotales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_consexclusionlotetotales.sql` |
| **LOC (1er CREATE)** | 309 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta EVC — Evaluación/Cartera a Quebrantar y lote (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_consexclusionlotetotales(
  pIdUsuario                   char(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | `lote`=lote (proceso batch) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INT` | L21 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L23 |
| `iTotalRegistros` | `INT` | L25 |
| `iTotalAceptados` | `INT` | L27 |
| `iTotalRechazados` | `INT` | L29 |
| `cArchivoCarga` | `char(150)` | L31 |
| `iExiste` | `int` | L33 |
| `mTotalMonto` | `MONEY(14,2)` | L35 |
| `cNombreEjecutivo` | `char(45)` | L37 |
| `cSistemaCuenta` | `char(2)` | L39 |
| `cStatusLote` | `CHAR(1)` | L41 |
| `cTotalRecal` | `int` | L43 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L121 |
| `sw_evc_excluidos` | `bdicnweb` | no | SELECT | L135 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L209 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L219 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | UPDATE | L271 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L143 | CÓDIGO_RETORNO | `let cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?clusion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `lote` | ENTIDAD | lote (proceso batch) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?clusion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_consultacifras`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_consultacifras.sql` |
| **LOC (1er CREATE)** | 190 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta EVC — Evaluación/Cartera a Quebrantar" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_consultacifras(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  pFecha                       CHAR(6)
) RETURNING CHAR(5) 			as resultado,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L19 |
| `iIsamErr` | `INTEGER` | L20 |
| `cErrorInfo` | `CHAR(80)` | L21 |
| `cCodRet` | `CHAR(5)` | L22 |
| `cMensajeRet` | `VARCHAR(80,1)` | L23 |
| `iOrden` | `INTEGER` | L24 |
| `cElemento` | `VARCHAR(4)` | L25 |
| `cProducto` | `CHAR(4)` | L27 |
| `cDescripcion` | `CHAR(50)` | L28 |
| `iClientes` | `INTEGER` | L29 |
| `iContador` | `INTEGER` | L30 |
| `dSdoActual` | `DECIMAL (14,2)` | L31 |
| `dSdoVencido` | `DECIMAL (14,2)` | L32 |
| `dSdoNoExig` | `DECIMAL (14,2)` | L33 |
| `dIntVencido` | `DECIMAL (14,2)` | L34 |
| `dIvaIntVencido` | `DECIMAL (14,2)` | L35 |
| `dIntMoraOrdi` | `DECIMAL (14,2)` | L36 |
| `dIvaIntMoraOrdi` | `DECIMAL (14,2)` | L37 |
| `dIntMoraCope` | `DECIMAL (14,2)` | L38 |
| `dIvaIntMoraCope` | `DECIMAL (14,2)` | L39 |
| `dTotalExcluidos` | `INTEGER` | L40 |
| `dFechaVenta` | `DATE` | L41 |
| `dfechaapertura` | `DATE` | L43 |
| `dstatusTar` | `CHAR(2)` | L44 |
| `dsucursal` | `CHAR(4)` | L45 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_fechas` | `bdicred` | ⚠️ sí | SELECT | L127 |
| `cb_rep_cart_quebrantar` | `bdicobranza` | ⚠️ sí | SELECT | L131 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L144 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | CÓDIGO_RETORNO | `LET cCodRet            = '00000';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet     = '00003';` |  |
| L121 | FÓRMULA | `LET dtFechaReporte = mdy(substr(pfecha,5,2)+1,1,substr(pfecha,1,4)) - 1 units day;` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet	= '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?cifras` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cifras` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_consultacte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_consultacte.sql` |
| **LOC (1er CREATE)** | 213 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta EVC — Evaluación/Cartera a Quebrantar y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_consultacte(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  cnumcte                      char(20)
  cnumcredito                  char(20)
  cnombre1                     char(20)
  cnombre2                     char(20)
  capell_pat                   char(20)
  capell_materno               char(20)
  crfc                         char(13)
) RETURNING CHAR(5) 			as resultado,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cnumcte` | `char(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `cnumcredito` | `char(20)` | — | — |
| `cnombre1` | `char(20)` | — | — |
| `cnombre2` | `char(20)` | — | — |
| `capell_pat` | `char(20)` | — | — |
| `capell_materno` | `char(20)` | — | — |
| `crfc` | `char(13)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L29 |
| `iIsamErr` | `INTEGER` | L30 |
| `cErrorInfo` | `CHAR(80)` | L31 |
| `cCodRet` | `CHAR(5)` | L32 |
| `cMensajeRet` | `VARCHAR(80,1)` | L33 |
| `iOrden` | `INTEGER` | L34 |
| `cElemento` | `VARCHAR(4)` | L35 |
| `cProducto` | `CHAR(4)` | L37 |
| `cDescripcion` | `CHAR(50)` | L38 |
| `iClientes` | `INTEGER` | L39 |
| `dSdoActual` | `DECIMAL (14,2)` | L40 |
| `dSdoVencido` | `DECIMAL (14,2)` | L41 |
| `dSdoNoExig` | `DECIMAL (14,2)` | L42 |
| `dIntVencido` | `DECIMAL (14,2)` | L43 |
| `dIvaIntVencido` | `DECIMAL (14,2)` | L44 |
| `dIntMoraOrdi` | `DECIMAL (14,2)` | L45 |
| `dIvaIntMoraOrdi` | `DECIMAL (14,2)` | L46 |
| `dIntMoraCope` | `DECIMAL (14,2)` | L47 |
| `dIvaIntMoraCope` | `DECIMAL (14,2)` | L48 |
| `dTotalExcluidos` | `INTEGER` | L49 |
| `dFechaVenta` | `DATE` | L50 |
| `dEjecutivo` | `CHAR(10)` | L51 |
| `dFechaCancela` | `DATE` | L52 |
| `dfechaapertura` | `DATE` | L54 |
| `dstatusTar` | `CHAR(50)` | L55 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L130 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L131 |
| `cb_rep_cart_quebrantar` | `bdicobranza` | ⚠️ sí | SELECT | L141 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L145 |
| `sd_tipocartera` | `bdicred` | ⚠️ sí | SELECT | L153 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L173 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L119 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L67 | CÓDIGO_RETORNO | `LET cCodRet            = '00000';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet    = '00201';` |  |
| L197 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_evc_excluyecliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_excluyecliente.sql` |
| **LOC (1er CREATE)** | 145 |
| **Callgraph** | ✅ fan_in=12 / fan_out=8 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "EVC — Evaluación/Cartera a Quebrantar y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `val_num`, `sp_cnsif_permisosejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_excluyecliente(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  pNumCredito                  char(20)
  TipoExcepcion                char(1)
  TipoEjecucion                Smallint
) RETURNING CHAR(5) as resultado,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `pNumCredito` | `char(20)` | — | — |
| `TipoExcepcion` | `char(1)` | `x`=por (criterio) | 🔵 CONVENCIÓN |
| `TipoEjecucion` | `Smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cMensajeRet` | `CHAR(100)` | L7 |
| `iCodRet` | `INTEGER` | L8 |
| `SCodRet` | `CHAR(5)` | L9 |
| `dtFechaHoy` | `DATE` | L10 |
| `cRuta` | `CHAR(100)` | L11 |
| `cNomArchOri` | `CHAR(50)` | L12 |
| `cNomArchRep` | `CHAR(50)` | L13 |
| `cSQL` | `CHAR(4000)` | L14 |
| `vnumcredito` | `CHAR(20)` | L15 |
| `vmotivo` | `CHAR(1)` | L16 |
| `vfecharep` | `DATE` | L17 |
| `vproducto` | `CHAR(4)` | L18 |
| `vexcluir` | `CHAR(100)` | L19 |
| `bEsNumero` | `BOOLEAN` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_evc_excluidos` | `bdicnweb` | no | UPDATE | L75 |
| `cb_rep_cart_quebrantar` | `bdicobranza` | ⚠️ sí | SELECT | L83 |
| `sw_evc_excluidos` | `bdicnweb` | no | SELECT | L96 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `val_num` | `bdinteg` | ⚠️ sí | L47 |
| `sp_cnsif_permisosejecutivo` | `bdicnweb` | no | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET SCodRet     ='00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET SCodRet    = '00002';` |  |
| L49 | CÓDIGO_RETORNO | `LET SCodRet    = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?cluye` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cliente` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_e`, `?cluye` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_excluyecredito`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_excluyecredito.sql` |
| **LOC (1er CREATE)** | 180 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "EVC — Evaluación/Cartera a Quebrantar y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_permisosejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_excluyecredito(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMCREDITO                  char(20)
  cMotivo                      char(1)
) RETURNING CHAR(5) as resultado,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNUMCREDITO` | `char(20)` | `credito`=crédito | 🔵 CONVENCIÓN |
| `cMotivo` | `char(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cMensajeRet` | `CHAR(100)` | L7 |
| `iCodRet` | `INTEGER` | L8 |
| `SCodRet` | `CHAR(5)` | L9 |
| `dtFechaHoy` | `DATE` | L10 |
| `dtFechaAnt` | `DATE` | L11 |
| `dtFechaRep` | `CHAR(8)` | L12 |
| `cRuta` | `CHAR(100)` | L13 |
| `cNomArchOri` | `CHAR(50)` | L14 |
| `cNomArchRep` | `CHAR(50)` | L15 |
| `cSQL` | `CHAR(4000)` | L16 |
| `vnumcredito` | `CHAR(20)` | L17 |
| `vmotivo` | `CHAR(1)` | L18 |
| `vfecharep` | `DATE` | L19 |
| `vproducto` | `CHAR(4)` | L20 |
| `vexcluir` | `CHAR(100)` | L21 |
| `vidRegistro` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cb_rep_cart_quebrantar` | `bdicobranza` | ⚠️ sí | SELECT | L61 |
| `sd_param` | `bdicred` | ⚠️ sí | SELECT | L66 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L89 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L90 |
| `sw_evc_excluidos` | `bdicnweb` | no | SELECT | L99 |
| `sw_evc_excluidos` | `bdicnweb` | no | INSERT | L153 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET SCodRet     ='00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF TRIM(vexcluir) <> '0' OR vexcluir IS NULL THEN` |  |
| L70 | CÓDIGO_RETORNO | `LET SCodRet    = '00212';` |  |
| L85 | CÓDIGO_RETORNO | `LET SCodRet    = '00001';` |  |
| L92 | CÓDIGO_RETORNO | `LET SCodRet    	  = '00201';` |  |
| L114 | CÓDIGO_RETORNO | `LET SCodRet    = '00213';` |  |
| L134 | CÓDIGO_RETORNO | `LET SCodRet    = '00213';` |  |
| L140 | CÓDIGO_RETORNO | `LET SCodRet = '00017';` |  |
| L147 | CÓDIGO_RETORNO | `LET SCodRet = '00214';` |  |
| L163 | VALIDACIÓN_NULL | `IF vIdRegistro IS NULL THEN LET vIdRegistro = 0; END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?cluye` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `credito` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_e`, `?cluye` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_marca_creditosnovalidos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_marca_creditosnovalidos.sql` |
| **LOC (1er CREATE)** | 148 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida EVC — Evaluación/Cartera a Quebrantar, marca, créditos y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_marca_creditosnovalidos(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vStatusSol` | `CHAR(2)` | L11 |
| `vHoy` | `DATE` | L12 |
| `dFechaEnt` | `DATE` | L13 |
| `vCausaSol` | `CHAR(3)` | L14 |
| `P_COD_RET` | `VARCHAR(5)` | L15 |
| `cNumcte` | `CHAR(20)` | L16 |
| `cCodRet` | `CHAR(6)` | L17 |
| `cMensajeRet` | `CHAR(100)` | L18 |
| `iValido` | `INTEGER` | L19 |
| `cSucursal` | `CHAR(4)` | L20 |
| `cNumProd` | `CHAR(4)` | L21 |
| `vRegistro` | `DECIMAL(18,2)` | L22 |
| `vMensajeStatus` | `CHAR(80)` | L23 |
| `iSqlErr` | `INT` | L24 |
| `dFechaRep` | `DATE` | L25 |
| `cNumCredito` | `CHAR(20)` | L26 |
| `cMotivo` | `CHAR(1)` | L27 |
| `iNumLote` | `INTEGER` | L28 |
| `iLote` | `INTEGER` | L29 |
| `iNumReg` | `INTEGER` | L30 |
| `cStatus` | `CHAR(1)` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cb_rep_cart_quebrantar` | `bdicobranza` | ⚠️ sí | SELECT | L67 |
| `sw_evc_excluidos` | `bdicnweb` | no | SELECT | L72 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L102 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L103 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `marca` | ENTIDAD | marca | 🔵 CONVENCIÓN | nombre_sp |
| `creditos` | ENTIDAD | créditos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `valid` | ACCION | valida | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?no` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_evc_obten_parametro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_evc_obten_parametro.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene EVC — Evaluación/Cartera a Quebrantar y parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_evc_obten_parametro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cValor` | `CHAR(1)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_param` | `bdicred` | ⚠️ sí | SELECT | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `evc` | ENTIDAD | EVC — Evaluación/Cartera a Quebrantar (write-off de cartera  | 🟡 INFERIDO | nombre_sp |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `parametro` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_existeimagendigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_existeimagendigitalizacion.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 120 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "imagen digital y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_existeimagendigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCliente                     CHAR(20)
  pCodDocto                    CHAR(4)
  pSecuencia                   CHAR(6)
  pSvrRemoto                   CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCodDocto` | `CHAR(4)` | — | — |
| `pSecuencia` | `CHAR(6)` | — | — |
| `pSvrRemoto` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cEsNula` | `CHAR(1)` | L10 |
| `cQuery` | `CHAR(1500)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?iste` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imagen` | ENTIDAD | imagen digital | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_e`, `?iste` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_existeimagendigitalizacion2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_existeimagendigitalizacion2.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "imagen digital y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_existeimagendigitalizacion2(
  pCliente                     CHAR(20)
  pCodDocto                    CHAR(4)
  pSecuencia                   CHAR(6)
  pSvrRemoto                   CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCliente` | `CHAR(20)` | — | — |
| `pCodDocto` | `CHAR(4)` | — | — |
| `pSecuencia` | `CHAR(6)` | — | — |
| `pSvrRemoto` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cEsNula` | `CHAR(1)` | L10 |
| `cQuery` | `CHAR(1500)` | L11 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?iste` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imagen` | ENTIDAD | imagen digital | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_e`, `?iste`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_bitacorahuellas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_bitacorahuellas.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=77 |
| **Deps concatenadas** | 186 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fc — Fuentes Combinadas, bitácora y huellas biométricas" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bithuellasfusion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_bitacorahuellas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCteCorr                  CHAR(20)
  pNumCteInc                   CHAR(20)
  pMensaje                     CHAR(2)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCteCorr` | `CHAR(20)` | — | — |
| `pNumCteInc` | `CHAR(20)` | — | — |
| `pMensaje` | `CHAR(2)` | — | — |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iIsamErrorSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_bithuellasfusion` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_bithuellasfusion';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `huellas` | ENTIDAD | huellas biométricas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_bloqueactas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_bloqueactas.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 185 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloquea cuenta fc — Fuentes Combinadas y cuentas" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bloqueactas` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_bloqueactas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cCodRetSp2` | `CHAR(6)` | L10 |
| `cCodRetSp3` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cDescCodRetSp` | `CHAR(100)` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cValor` | `CHAR(100)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_bloqueactas` | `bdinteg` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_bloqueactas';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_conscliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_conscliente.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=75 |
| **Deps concatenadas** | 184 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fc — Fuentes Combinadas y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_fuscte_conscte` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_conscliente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cDescCodRetSp` | `CHAR(100)` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `cApellPaterno` | `CHAR(26)` | L20 |
| `cApellMaterno` | `CHAR(26)` | L21 |
| `cNombre` | `CHAR(26)` | L22 |
| `cNombre2` | `CHAR(26)` | L23 |
| `cSucursal` | `CHAR(4)` | L24 |
| `cRFC` | `CHAR(13)` | L25 |
| `cStatusCte` | `CHAR(2)` | L26 |
| `dFechaAlta` | `DATE` | L27 |
| `dFechaNac` | `DATE` | L28 |
| `cSexo` | `CHAR(1)` | L29 |
| `iNoRegistros` | `INTEGER` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |
| `sp_fuscte_conscte` | `bdinteg` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_fuscte_conscte';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cliente` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_consctesdesb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_consctesdesb.sql` |
| **LOC (1er CREATE)** | 147 |
| **Callgraph** | ✅ fan_in=0 / fan_out=74 |
| **Deps concatenadas** | 183 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fc — Fuentes Combinadas y clientes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_desbctasfus_conscte` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_consctesdesb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pAnalista                    CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pAnalista` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCodRetSp` | `CHAR(6)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `cDescCodRetSp` | `CHAR(100)` | L29 |
| `cEmpresa` | `CHAR(3)` | L30 |
| `cApellPaterno` | `CHAR(26)` | L31 |
| `cApellMaterno` | `CHAR(26)` | L32 |
| `cNombre` | `CHAR(26)` | L33 |
| `cNombre2` | `CHAR(26)` | L34 |
| `cSucursal` | `CHAR(4)` | L35 |
| `cRFC` | `CHAR(13)` | L36 |
| `cStatusCte` | `CHAR(2)` | L37 |
| `dFechaAlta` | `DATE` | L38 |
| `dFechaNac` | `DATE` | L39 |
| `cSexo` | `CHAR(1)` | L40 |
| `cApellPaterno_CteI` | `CHAR(26)` | L41 |
| `cApellMaterno_CteI` | `CHAR(26)` | L42 |
| `cNombre_CteI` | `CHAR(26)` | L43 |
| `cNombre2_CteI` | `CHAR(26)` | L44 |
| `cSucursal_CteI` | `CHAR(4)` | L45 |
| `cRFC_CteI` | `CHAR(13)` | L46 |
| `cStatusCte_CteI` | `CHAR(2)` | L47 |
| `dFechaAlta_CteI` | `DATE` | L48 |
| `dFechaNac_CteI` | `DATE` | L49 |
| *…3 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L101 |
| `sp_desbctasfus_conscte` | `bdinteg` | ⚠️ sí | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L116 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_desbctasfus_conscte';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `desb` | ACCION | desbloqueo | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_conslocalctesrs`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_conslocalctesrs.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=73 |
| **Deps concatenadas** | 181 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fc — Fuentes Combinadas y clientes (local)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_conslocalctesrs(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRazonSoc                    CHAR(60)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRazonSoc` | `CHAR(60)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cDescCodRetSp` | `CHAR(100)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cNumCte` | `CHAR(20)` | L14 |
| `cRazon_social` | `CHAR(60)` | L15 |
| `cRfc` | `CHAR(13)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L72 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `local` | MODIF | local | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `?rs` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?rs` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_consnombresupana`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_consnombresupana.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=73 |
| **Deps concatenadas** | 180 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fc — Fuentes Combinadas y nombre" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_desbctasfus_obtnombresupana` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_consnombresupana(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmp                      CHAR(20)
  pTipo                        CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmp` | `CHAR(20)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNomEmpleado` | `CHAR(50)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_desbctasfus_obtnombresupana` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_desbctasfus_obtnombresupana';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?supana` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?supana` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_consparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_consparam.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=72 |
| **Deps concatenadas** | 179 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta fc — Fuentes Combinadas y parámetro" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_consparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pFiltro                      CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pFiltro` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cValor` | `CHAR(100)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_cuentadoctos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_cuentadoctos.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fc — Fuentes Combinadas, cuenta y documentos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cuentadoctos_soc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_cuentadoctos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pTipoCte                     SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pTipoCte` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iIsamErrorSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cValor` | `CHAR(100)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_cuentadoctos_soc` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_cuentadoctos_soc';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_deletectetraspasa`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_deletectetraspasa.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=72 |
| **Deps concatenadas** | 178 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso fc — Fuentes Combinadas y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_deletectetraspasa(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTraspasaCtas             CHAR(20)
  pIdEjecucion                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTraspasaCtas` | `CHAR(20)` | `sp`=stored procedure · `cte`=cliente | 🔵 CONVENCIÓN |
| `pIdEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValor` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fuscliente` | `bdinteg` | ⚠️ sí | SELECT | L48 |
| `si_fuscliente` | `bdinteg` | ⚠️ sí | DELETE | L48 |
| `si_fusctepf` | `bdinteg` | ⚠️ sí | SELECT | L54 |
| `si_fusctepf` | `bdinteg` | ⚠️ sí | DELETE | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00862';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00862';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `?_delete` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `traspas` | ACCION | traspaso | 🟡 INFERIDO | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?_delete`, `?a` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_desbctasfus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_desbctasfus.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=72 |
| **Deps concatenadas** | 177 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloqueo fc — Fuentes Combinadas y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_desbctasfus` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_desbctasfus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pUsEjecuta                   CHAR(8)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pUsEjecuta` | `CHAR(8)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cEmp` | `CHAR(3)` | L10 |
| `cCuenta` | `CHAR(20)` | L11 |
| `iRecuperacion` | `INTEGER` | L13 |
| `iContador` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_desbctasfus` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_desbctasfus';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `desb` | ACCION | desbloqueo | 🟡 INFERIDO | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `fus` | ACCION | fusión de cuentas | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_detallecuentasdesb`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_detallecuentasdesb.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=71 |
| **Deps concatenadas** | 176 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloqueo fc — Fuentes Combinadas, detalle y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_detallecuentasdesb(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pAnalista                    CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pAnalista` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(6)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cDescCodRetSp` | `CHAR(100)` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `cNumCte` | `CHAR(20)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cProducto` | `CHAR(4)` | L23 |
| `cDescProducto` | `CHAR(40)` | L24 |
| `cStatusCta` | `CHAR(2)` | L25 |
| `mSaldo` | `MONEY(10,2)` | L26 |
| `cUsuario_bloqueo` | `CHAR(10)` | L27 |
| `dFecha_bloqueo` | `DATE` | L28 |
| `cSupervisor_desbloqueo` | `CHAR(10)` | L29 |
| `dFecha_desbloqueo` | `DATE` | L30 |
| `cBloqueo` | `CHAR(1)` | L31 |
| `iRecuperacion` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_desbctasfus_cuentas` | `bdinteg` | ⚠️ sí | SELECT | L89 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `desb` | ACCION | desbloqueo | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_detallecuentasdesb_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_detallecuentasdesb_totales.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=71 |
| **Deps concatenadas** | 175 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloqueo fc — Fuentes Combinadas, detalle y cuentas (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_desbctasfus_consctas` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_detallecuentasdesb_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pAnalista                    CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pAnalista` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNumCte` | `CHAR(20)` | L11 |
| `cCuenta` | `CHAR(20)` | L12 |
| `cProducto` | `CHAR(4)` | L13 |
| `cDescProducto` | `CHAR(40)` | L14 |
| `cStatusCta` | `CHAR(2)` | L15 |
| `mSaldo` | `MONEY(10,2)` | L16 |
| `cUsuario_bloqueo` | `CHAR(10)` | L17 |
| `dFecha_bloqueo` | `DATE` | L18 |
| `cSupervisor_desbloqueo` | `CHAR(10)` | L19 |
| `dFecha_desbloqueo` | `DATE` | L20 |
| `cBloqueo` | `CHAR(1)` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_desbctasfus_cuentas` | `bdinteg` | ⚠️ sí | SELECT | L83 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |
| `sp_desbctasfus_consctas` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_desbctasfus_consctas';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `desb` | ACCION | desbloqueo | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_detallecuentastraspasar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_detallecuentastraspasar.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 174 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso fc — Fuentes Combinadas, detalle y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_detallecuentastraspasar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cDescCodRetSp` | `CHAR(100)` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cNumCte` | `CHAR(20)` | L18 |
| `cCuenta` | `CHAR(20)` | L19 |
| `cProducto` | `CHAR(4)` | L20 |
| `cDescProducto` | `CHAR(40)` | L21 |
| `dFechaAlta` | `DATE` | L22 |
| `cStatusCta` | `CHAR(2)` | L23 |
| `dFechaUltMov` | `DATE` | L24 |
| `mSaldo` | `MONEY(16,2)` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_detcuentastraspasar` | `bdicnweb` | no | SELECT | L78 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L89 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `traspas` | ACCION | traspaso | 🟡 INFERIDO | nombre_sp |
| `?ar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ar` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_detallecuentastraspasar_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_detallecuentastraspasar_totales.sql` |
| **LOC (1er CREATE)** | 153 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 173 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso fc — Fuentes Combinadas, detalle y cuentas (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consctastraspasar` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_detallecuentastraspasar_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNumCte` | `CHAR(20)` | L11 |
| `cCuenta` | `CHAR(20)` | L12 |
| `cProducto` | `CHAR(4)` | L13 |
| `cDescProducto` | `CHAR(40)` | L14 |
| `dFechaAlta` | `DATE` | L15 |
| `cStatusCta` | `CHAR(2)` | L16 |
| `dFechaUltMov` | `DATE` | L17 |
| `mSaldo` | `MONEY(16,2)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |
| `iIdRegistro` | `INTEGER` | L21 |
| `bEnTransaccion` | `BOOLEAN` | L22 |
| `iContador` | `INTEGER` | L23 |
| `iMaxCommit` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_detcuentastraspasar` | `bdicnweb` | no | SELECT | L86 |
| `sw_fc_detcuentastraspasar` | `bdicnweb` | no | DELETE | L88 |
| `sw_fc_detcuentastraspasar` | `bdicnweb` | no | INSERT | L131 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L75 |
| `sp_consctastraspasar` | `bdinteg` | ⚠️ sí | L109 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L115 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_consctastraspasar';` |  |
| L128 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `traspas` | ACCION | traspaso | 🟡 INFERIDO | nombre_sp |
| `?ar_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ar_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_detalledocsdigitalizados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_detalledocsdigitalizados.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 172 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fc — Fuentes Combinadas, detalle, sd — saldo disponible y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_detalledocsdigitalizados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoCte                     CHAR(1)
  pNumCte                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoCte` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cDescCodRetSp` | `CHAR(100)` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cCod_docto` | `CHAR(4)` | L18 |
| `cDesc_docto` | `CHAR(35)` | L19 |
| `cCuenta` | `CHAR(20)` | L20 |
| `sSecuencia` | `SMALLINT` | L21 |
| `cFecha` | `CHAR(10)` | L22 |
| `cDescripcion` | `CHAR(50)` | L23 |
| `cNumCte` | `CHAR(20)` | L24 |
| `iIdReg` | `INTEGER` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_detdocsdigitalizados` | `bdicnweb` | no | SELECT | L78 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoCte = '' OR pNumCte = '' OR pRegistros IS NULL OR pRecup` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?doc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?igitalizad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?doc`, `?igitalizad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_detalledocsdigitalizados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_detalledocsdigitalizados_totales.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 170 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fc — Fuentes Combinadas, detalle, sd — saldo disponible y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtenerdoctos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_detalledocsdigitalizados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoCte                     CHAR(1)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoCte` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCod_docto` | `CHAR(4)` | L11 |
| `cDesc_docto` | `CHAR(35)` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |
| `sSecuencia` | `SMALLINT` | L14 |
| `cFecha` | `CHAR(10)` | L15 |
| `cDescripcion` | `CHAR(50)` | L16 |
| `cNumCte` | `CHAR(20)` | L17 |
| `iNoRegistros` | `INTEGER` | L18 |
| `iIdRegistro` | `INTEGER` | L20 |
| `bEnTransaccion` | `BOOLEAN` | L21 |
| `iContador` | `INTEGER` | L22 |
| `iMaxCommit` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_detdocsdigitalizados` | `bdicnweb` | no | SELECT | L81 |
| `sw_fc_detdocsdigitalizados` | `bdicnweb` | no | DELETE | L83 |
| `sw_fc_detdocsdigitalizados` | `bdicnweb` | no | INSERT | L116 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L70 |
| `sp_obtenerdoctos` | `bdinteg` | ⚠️ sí | L104 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L109 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_obtenerdoctos';` |  |
| L115 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `?doc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?igitalizad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?doc`, `?igitalizad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_detalledocsdigitalizadosdesb_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_detalledocsdigitalizadosdesb_totales.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=60 |
| **Deps concatenadas** | 155 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloqueo fc — Fuentes Combinadas, detalle, sd — saldo disponible y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_desbctasfus_obtenerdoctos_soc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_detalledocsdigitalizadosdesb_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoCte                     CHAR(1)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoCte` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCod_docto` | `CHAR(4)` | L11 |
| `cDesc_docto` | `CHAR(35)` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |
| `sSecuencia` | `SMALLINT` | L14 |
| `cFecha` | `CHAR(10)` | L15 |
| `cDescripcion` | `CHAR(50)` | L16 |
| `cCuentaValida` | `CHAR(4)` | L17 |
| `cProducto` | `CHAR(4)` | L18 |
| `cDescProducto` | `CHAR(40)` | L19 |
| `cNumCte` | `CHAR(20)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |
| `iIdRegistro` | `INTEGER` | L23 |
| `bEnTransaccion` | `BOOLEAN` | L24 |
| `iContador` | `INTEGER` | L25 |
| `iMaxCommit` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_detdocsdigitalizadosdesb` | `bdicnweb` | no | SELECT | L90 |
| `sw_fc_detdocsdigitalizadosdesb` | `bdicnweb` | no | DELETE | L92 |
| `sw_fc_detdocsdigitalizadosdesb` | `bdicnweb` | no | INSERT | L125 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L79 |
| `sp_desbctasfus_obtenerdoctos_soc` | `bdinteg` | ⚠️ sí | L113 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L118 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_desbctasfus_obtenerdoctos_soc` |  |
| L124 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `?doc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?igitalizad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `desb` | ACCION | desbloqueo | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?doc`, `?igitalizad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_ejecutalog`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_ejecutalog.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=68 |
| **Deps concatenadas** | 169 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ejecuta fc — Fuentes Combinadas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_logfsnctes` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_ejecutalog(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pUsEjecuta                   CHAR(20)
  pAbreAplic                   CHAR(2)
  pFusion                      CHAR(2)
  pFechaAbreApli               DATETIME YEAR TO SECOND
  pHraAbreApli                 DATETIME HOUR TO FRACTION(3)
  pFechaFus                    DATETIME YEAR TO SECOND
  pHraFus                      DATETIME HOUR TO FRACTION(3)
  pUsNoAut                     CHAR(20)
  pCteTit                      CHAR(20)
  pCteTras                     CHAR(20)
  pStatus                      CHAR(100)
  pTipoRep                     CHAR(1)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pUsEjecuta` | `CHAR(20)` | `ejecuta`=ejecuta (verbo — proceso / operación) | 🟡 INFERIDO |
| `pAbreAplic` | `CHAR(2)` | — | — |
| `pFusion` | `CHAR(2)` | — | — |
| `pFechaAbreApli` | `DATETIME YEAR TO SECOND` | — | — |
| `pHraAbreApli` | `DATETIME HOUR TO FRACTION(3)` | — | — |
| `pFechaFus` | `DATETIME YEAR TO SECOND` | — | — |
| `pHraFus` | `DATETIME HOUR TO FRACTION(3)` | — | — |
| `pUsNoAut` | `CHAR(20)` | — | — |
| `pCteTit` | `CHAR(20)` | — | — |
| `pCteTras` | `CHAR(20)` | — | — |
| `pStatus` | `CHAR(100)` | — | — |
| `pTipoRep` | `CHAR(1)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cValor` | `CHAR(100)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_logfsnctes` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_logfsnctes';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `ejecuta` | ACCION | ejecuta (verbo — proceso / operación) | 🟡 INFERIDO | nombre_sp |
| `?log` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?log` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_ejecutaquery`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_ejecutaquery.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ejecuta fc — Fuentes Combinadas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_ejecutaquery(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasa                 CHAR(20)
  pUsEjecuta                   CHAR(8)
  pIdEjecucion                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasa` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | `ejecuta`=ejecuta (verbo — proceso / operación) | 🟡 INFERIDO |
| `pIdEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValor` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `cTipo_clienteTit` | `CHAR(1)` | L12 |
| `cTipo_clienteTras` | `CHAR(1)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | UPDATE | L51 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L59 |
| `si_fuscliente` | `bdinteg` | ⚠️ sí | SELECT | L74 |
| `si_fusclientes_ide` | `bdinteg` | ⚠️ sí | INSERT | L86 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | VALIDACIÓN_NULL | `IF cTipo_clienteTras IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `ejecuta` | ACCION | ejecuta (verbo — proceso / operación) | 🟡 INFERIDO | nombre_sp |
| `?query` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?query` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_historicodoctosfusionados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_historicodoctosfusionados.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ✅ fan_in=0 / fan_out=67 |
| **Deps concatenadas** | 168 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fc — Fuentes Combinadas y documentos (histórico, fusionados)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_historicodoctosfusionados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCteTrasp                 CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCteTrasp` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `cDescCodRetSp` | `CHAR(150)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `iNumRegistros` | `INTEGER` | L16 |
| `bEnTransaccion` | `BOOLEAN` | L17 |
| `iContador` | `INTEGER` | L18 |
| `iMaxCommit` | `INTEGER` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |
| `iIdRegistro` | `INTEGER` | L22 |
| `cCliente` | `CHAR(20)` | L24 |
| `cCuenta` | `CHAR(20)` | L25 |
| `cCodDocto` | `CHAR(4)` | L26 |
| `iSecuencia` | `INTEGER` | L27 |
| `cFechaAlta` | `CHAR(10)` | L28 |
| `cDescripcion` | `CHAR(35)` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_rephistoricodoctos` | `bdicnweb` | no | SELECT | L86 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L90 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `fusionados` | MODIF | fusionados | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_fc_historicodoctosfusionados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_historicodoctosfusionados_totales.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=0 / fan_out=67 |
| **Deps concatenadas** | 167 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fc — Fuentes Combinadas y documentos (histórico, fusionados, totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_doctosfusionados` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_historicodoctosfusionados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCteTrasp                 CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCteTrasp` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `bEnTransaccion` | `BOOLEAN` | L12 |
| `iContador` | `INTEGER` | L13 |
| `iMaxCommit` | `INTEGER` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `iIdRegistro` | `INTEGER` | L16 |
| `cCliente` | `CHAR(20)` | L18 |
| `cCuenta` | `CHAR(20)` | L19 |
| `cCodDocto` | `CHAR(4)` | L20 |
| `iSecuencia` | `INTEGER` | L21 |
| `cFechaAlta` | `CHAR(10)` | L22 |
| `cDescripcion` | `CHAR(35)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_rephistoricodoctos` | `bdicnweb` | no | SELECT | L81 |
| `sw_fc_rephistoricodoctos` | `bdicnweb` | no | DELETE | L84 |
| `sw_fc_rephistoricodoctos` | `bdicnweb` | no | INSERT | L116 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L70 |
| `sp_doctosfusionados` | `bdinteg` | ⚠️ sí | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L109 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdinteg:sp_doctosfusionados';` |  |
| L115 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `fusionados` | MODIF | fusionados | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_fc_historicofusion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_historicofusion.sql` |
| **LOC (1er CREATE)** | 184 |
| **Callgraph** | ✅ fan_in=0 / fan_out=66 |
| **Deps concatenadas** | 166 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fusiona cuentas fc — Fuentes Combinadas (histórico)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_historicofusion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaDel                    DATE
  pFechaAl                     DATE
  pOpcion                      CHAR(1)
  pUsAnalista                  CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pUsAnalista` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `cCodRetSp` | `CHAR(6)` | L24 |
| `cDescCodRetSp` | `CHAR(150)` | L25 |
| `iCodRetSp` | `INTEGER` | L26 |
| `cEmpresa` | `CHAR(3)` | L27 |
| `iNumRegistros` | `INTEGER` | L28 |
| `bEnTransaccion` | `BOOLEAN` | L29 |
| `iContador` | `INTEGER` | L30 |
| `iMaxCommit` | `INTEGER` | L31 |
| `iNoRegistros` | `INTEGER` | L32 |
| `iRecuperacion` | `INTEGER` | L33 |
| `iIdRegistro` | `INTEGER` | L34 |
| `cDia` | `CHAR(2)` | L35 |
| `cMes` | `CHAR(2)` | L36 |
| `cAnio` | `CHAR(4)` | L37 |
| `cDiaHasta` | `CHAR(2)` | L38 |
| `cMesHasta` | `CHAR(2)` | L39 |
| `cAnioHasta` | `CHAR(4)` | L40 |
| `cNumCteTit` | `CHAR(20)` | L42 |
| `cNumCteTrasp` | `CHAR(20)` | L43 |
| `cApePaterTit` | `CHAR(26)` | L44 |
| `cApeMaterTit` | `CHAR(26)` | L45 |
| `cNom1Tit` | `CHAR(26)` | L46 |
| `cNom2Tit` | `CHAR(26)` | L47 |
| *…20 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_rephistorico` | `bdicnweb` | no | SELECT | L156 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L141 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L127 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaDel IS NULL OR pFechaAl IS NULL OR pOpcion = '' OR pReg` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L160 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `fusion` | ACCION | fusiona cuentas | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_fc_historicofusion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_historicofusion_totales.sql` |
| **LOC (1er CREATE)** | 210 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 154 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "fusiona cuentas fc — Fuentes Combinadas (histórico, totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_historico_fusion_soc` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_historicofusion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaDel                    DATE
  pFechaAl                     DATE
  pOpcion                      CHAR(1)
  pUsAnalista                  CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |
| `pOpcion` | `CHAR(1)` | — | — |
| `pUsAnalista` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `bEnTransaccion` | `BOOLEAN` | L12 |
| `iContador` | `INTEGER` | L13 |
| `iMaxCommit` | `INTEGER` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `iIdRegistro` | `INTEGER` | L16 |
| `cDia` | `CHAR(2)` | L17 |
| `cMes` | `CHAR(2)` | L18 |
| `cAnio` | `CHAR(4)` | L19 |
| `cDiaHasta` | `CHAR(2)` | L20 |
| `cMesHasta` | `CHAR(2)` | L21 |
| `cAnioHasta` | `CHAR(4)` | L22 |
| `cNumCteTit` | `CHAR(20)` | L24 |
| `cNumCteTrasp` | `CHAR(20)` | L25 |
| `cApePaterTit` | `CHAR(26)` | L26 |
| `cApeMaterTit` | `CHAR(26)` | L27 |
| `cNom1Tit` | `CHAR(26)` | L28 |
| `cNom2Tit` | `CHAR(26)` | L29 |
| `cFechaNacTit` | `CHAR(10)` | L30 |
| *…19 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_rephistorico` | `bdicnweb` | no | SELECT | L136 |
| `sw_fc_rephistorico` | `bdicnweb` | no | DELETE | L139 |
| `sw_fc_rephistorico` | `bdicnweb` | no | INSERT | L182 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L125 |
| `sp_historico_fusion_soc` | `bdinteg` | ⚠️ sí | L163 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L119 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaDel IS NULL OR pFechaAl IS NULL OR pOpcion = '' THEN` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L141 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L168 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_historico_fusion_soc` |  |
| L181 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fusion` | ACCION | fusiona cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_fc_obtienehuellas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_obtienehuellas.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=66 |
| **Deps concatenadas** | 165 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene fc — Fuentes Combinadas y huellas biométricas" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obthuellasactes` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_obtienehuellas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCteCorr                  CHAR(20)
  pNumCteInc                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCteCorr` | `CHAR(20)` | — | — |
| `pNumCteInc` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRetSp` | `CHAR(100)` | L10 |
| `iIsamErrorSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cTrama_1` | `CHAR(942)` | L13 |
| `cTrama_2` | `CHAR(942)` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_obthuellasactes` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_obthuellasactes';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `huellas` | ENTIDAD | huellas biométricas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_respaldacargaimg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_respaldacargaimg.sql` |
| **LOC (1er CREATE)** | 185 |
| **Callgraph** | ✅ fan_in=0 / fan_out=65 |
| **Deps concatenadas** | 164 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "respalda fc — Fuentes Combinadas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidigital` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_respaldacargaimg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasa                 CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasa` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iIsamErrorSp` | `INT` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cInsImg` | `CHAR(100)` | L11 |
| `cInsImgHis` | `CHAR(100)` | L12 |
| `cRuta` | `CHAR(50)` | L13 |
| `cArchUnl` | `CHAR(20)` | L14 |
| `cEmp` | `CHAR(3)` | L16 |
| `cCliente` | `CHAR(20)` | L17 |
| `cCuenta` | `CHAR(20)` | L18 |
| `cProducto` | `CHAR(4)` | L19 |
| `cCod_docto` | `CHAR(4)` | L20 |
| `sSecuencia` | `SMALLINT` | L21 |
| `cProd_nombre` | `CHAR(40)` | L22 |
| `cDescrip2` | `CHAR(30)` | L23 |
| `cUsuario_alta` | `CHAR(8)` | L24 |
| `dFecha_alta` | `DATE` | L25 |
| `cUsuario_modif` | `CHAR(8)` | L26 |
| `dFecha_modif` | `DATE` | L27 |
| `cDetalleMov` | `CHAR(200)` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L80 |
| `bdidigital` | `bdicnweb` | no | INSERT | L110 |
| `bdidigital` | `bdicnweb` | no | DELETE | L116 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L124 |
| `si_cliente` | `bdinteg` | ⚠️ sí | DELETE | L124 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | SELECT | L128 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | DELETE | L128 |
| `si_fuscliente` | `bdinteg` | ⚠️ sí | SELECT | L134 |
| `si_fuscliente` | `bdinteg` | ⚠️ sí | DELETE | L134 |
| `si_fusctepf` | `bdinteg` | ⚠️ sí | SELECT | L138 |
| `si_fusctepf` | `bdinteg` | ⚠️ sí | DELETE | L138 |
| `log_fusionclientes` | `bdinteg` | ⚠️ sí | INSERT | L144 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |
| `bdidigital` | `bdicnweb` | no | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L92 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital:sp_respalda_img';` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital:sp_carga_img';` |  |
| L109 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `respalda` | ACCION | respalda / garantiza — aval o garantía de crédito (respalda_ | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?img` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?img` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_respaldacargaimg2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_respaldacargaimg2.sql` |
| **LOC (1er CREATE)** | 171 |
| **Callgraph** | ✅ fan_in=0 / fan_out=65 |
| **Deps concatenadas** | 163 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "respalda fc — Fuentes Combinadas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidigital` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_respaldacargaimg2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasa                 CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasa` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iIsamErrorSp` | `INT` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cInsImg` | `CHAR(100)` | L11 |
| `cInsImgHis` | `CHAR(100)` | L12 |
| `cRuta` | `CHAR(50)` | L13 |
| `cArchUnl` | `CHAR(20)` | L14 |
| `cEmp` | `CHAR(3)` | L16 |
| `cCliente` | `CHAR(20)` | L17 |
| `cCuenta` | `CHAR(20)` | L18 |
| `cProducto` | `CHAR(4)` | L19 |
| `cCod_docto` | `CHAR(4)` | L20 |
| `sSecuencia` | `SMALLINT` | L21 |
| `cProd_nombre` | `CHAR(40)` | L22 |
| `cDescrip2` | `CHAR(30)` | L23 |
| `cUsuario_alta` | `CHAR(8)` | L24 |
| `dFecha_alta` | `DATE` | L25 |
| `cUsuario_modif` | `CHAR(8)` | L26 |
| `dFecha_modif` | `DATE` | L27 |
| `cDetalleMov` | `CHAR(200)` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L80 |
| `bdidigital` | `bdicnweb` | no | INSERT | L111 |
| `bdidigital` | `bdicnweb` | no | DELETE | L118 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L126 |
| `si_cliente` | `bdinteg` | ⚠️ sí | DELETE | L126 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | SELECT | L130 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | DELETE | L130 |
| `log_fusionclientes` | `bdinteg` | ⚠️ sí | INSERT | L138 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |
| `bdidigital` | `bdicnweb` | no | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital:sp_respalda_img2';` |  |
| L101 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital:sp_carga_img2';` |  |
| L110 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `respalda` | ACCION | respalda / garantiza — aval o garantía de crédito (respalda_ | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?img2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?img2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_respaldacargaimg3`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_respaldacargaimg3.sql` |
| **LOC (1er CREATE)** | 187 |
| **Callgraph** | ✅ fan_in=0 / fan_out=65 |
| **Deps concatenadas** | 162 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "respalda fc — Fuentes Combinadas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidigital` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_respaldacargaimg3(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasa                 CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasa` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iIsamErrorSp` | `INT` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cInsImg` | `CHAR(100)` | L11 |
| `cInsImgHis` | `CHAR(100)` | L12 |
| `cRuta` | `CHAR(50)` | L13 |
| `cArchUnl` | `CHAR(20)` | L14 |
| `cEmp` | `CHAR(3)` | L16 |
| `cCliente` | `CHAR(20)` | L17 |
| `cCuenta` | `CHAR(20)` | L18 |
| `cProducto` | `CHAR(4)` | L19 |
| `cCod_docto` | `CHAR(4)` | L20 |
| `sSecuencia` | `SMALLINT` | L21 |
| `cProd_nombre` | `CHAR(40)` | L22 |
| `cDescrip2` | `CHAR(30)` | L23 |
| `cUsuario_alta` | `CHAR(8)` | L24 |
| `dFecha_alta` | `DATE` | L25 |
| `cUsuario_modif` | `CHAR(8)` | L26 |
| `dFecha_modif` | `DATE` | L27 |
| `cDetalleMov` | `CHAR(200)` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L80 |
| `bdidigital` | `bdicnweb` | no | INSERT | L111 |
| `bdidigital` | `bdicnweb` | no | DELETE | L118 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L126 |
| `si_cliente` | `bdinteg` | ⚠️ sí | DELETE | L126 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | SELECT | L130 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | DELETE | L130 |
| `si_fuscliente` | `bdinteg` | ⚠️ sí | SELECT | L136 |
| `si_fuscliente` | `bdinteg` | ⚠️ sí | DELETE | L136 |
| `si_fusctepf` | `bdinteg` | ⚠️ sí | SELECT | L140 |
| `si_fusctepf` | `bdinteg` | ⚠️ sí | DELETE | L140 |
| `log_fusionclientes` | `bdinteg` | ⚠️ sí | INSERT | L146 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |
| `bdidigital` | `bdicnweb` | no | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital:sp_respalda_img3';` |  |
| L101 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidigital:sp_carga_img3';` |  |
| L110 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `respalda` | ACCION | respalda / garantiza — aval o garantía de crédito (respalda_ | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?img3` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?img3` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_traspasoctascap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_traspasoctascap.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso entre cuentas fc — Fuentes Combinadas, cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_traspasocuentas_cap_soc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_traspasoctascap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasaCtas             CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasaCtas` | `CHAR(20)` | `sp`=stored procedure · `ctas`=cuentas | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValor` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_traspasocuentas_cap_soc` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_traspasocuentas_cap_soc';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `traspaso` | ACCION | traspaso entre cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_traspasoctascred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_traspasoctascred.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=58 |
| **Deps concatenadas** | 153 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso entre cuentas fc — Fuentes Combinadas, cuentas y crédito" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_traspasocuentas_cred_soc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_traspasoctascred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasaCtas             CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasaCtas` | `CHAR(20)` | `sp`=stored procedure · `ctas`=cuentas | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValor` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_traspasocuentas_cred_soc` | `bdicred` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicred:sp_traspasocuentas_cred_soc';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `traspaso` | ACCION | traspaso entre cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cred` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_traspasoctaside`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_traspasoctaside.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso entre cuentas fc — Fuentes Combinadas, cuentas y identificador" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_traspasocuentas_ide_soc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_traspasoctaside(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasaCtas             CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasaCtas` | `CHAR(20)` | `sp`=stored procedure · `ctas`=cuentas | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValor` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificactaside` | `bdicnweb` | no | SELECT | L59 |
| `sw_verificactaside` | `bdicnweb` | no | DELETE | L59 |
| `sw_verificactaside` | `bdicnweb` | no | INSERT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |
| `sp_traspasocuentas_ide_soc` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_traspasocuentas_ide_soc';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `traspaso` | ACCION | traspaso entre cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_traspasodoctoscorr`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_traspasodoctoscorr.sql` |
| **LOC (1er CREATE)** | 228 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso entre cuentas fc — Fuentes Combinadas y documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_traspasodoctoscorr(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasa                 CHAR(20)
  pUsEjecuta                   CHAR(8)
  pTipoCte                     CHAR(1)
  pBloqueInf                   CHAR(500)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasa` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |
| `pTipoCte` | `CHAR(1)` | — | — |
| `pBloqueInf` | `CHAR(500)` | — | — |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cInsImg` | `CHAR(100)` | L11 |
| `cInsImgHis` | `CHAR(100)` | L12 |
| `cCod_doctoDig` | `CHAR(4)` | L13 |
| `cDesc_doctoDig` | `CHAR(35)` | L14 |
| `cCuentaDig` | `CHAR(20)` | L15 |
| `sSecuenciaDig` | `SMALLINT` | L16 |
| `cFechaDig` | `CHAR(10)` | L17 |
| `cDescripcionDig` | `CHAR(50)` | L18 |
| `cNumCteDig` | `CHAR(20)` | L19 |
| `cEmp` | `CHAR(3)` | L20 |
| `cCliente` | `CHAR(20)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cProducto` | `CHAR(4)` | L23 |
| `cCod_docto` | `CHAR(4)` | L24 |
| `sSecuencia` | `SMALLINT` | L25 |
| `cProd_nombre` | `CHAR(40)` | L26 |
| `cDescrip2` | `CHAR(30)` | L27 |
| `cUsuario_alta` | `CHAR(8)` | L28 |
| `dFecha_alta` | `DATE` | L29 |
| *…14 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_detdocsdigitalizados` | `bdicnweb` | no | SELECT | L118 |
| `si_fusexpediente` | `bdinteg` | ⚠️ sí | INSERT | L129 |
| `log_fusionclientes` | `bdinteg` | ⚠️ sí | INSERT | L206 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L103 |
| `sp_split_cadena` | `bdicnweb` | no | L113 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L141 | FÓRMULA | `LET iMaxSecAux = iMaxSecAux + 1;` |  |
| L157 | FÓRMULA | `LET iMaxSecAux = iMaxSecAux + 1;` |  |
| L174 | FÓRMULA | `LET iMaxSecAux = iMaxSecAux + 1;` |  |
| L193 | FÓRMULA | `LET iMaxSecAux = iMaxSecAux + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `traspaso` | ACCION | traspaso entre cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?corr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?corr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_traspasodoctosinc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_traspasodoctosinc.sql` |
| **LOC (1er CREATE)** | 200 |
| **Callgraph** | ✅ fan_in=0 / fan_out=65 |
| **Deps concatenadas** | 161 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso entre cuentas fc — Fuentes Combinadas y documentos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_traspasodoctosinc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasa                 CHAR(20)
  pUsEjecuta                   CHAR(8)
  pTipoCte                     CHAR(1)
  pBloqueInf                   CHAR(500)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasa` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |
| `pTipoCte` | `CHAR(1)` | — | — |
| `pBloqueInf` | `CHAR(500)` | — | — |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cInsImg` | `CHAR(100)` | L11 |
| `cInsImgHis` | `CHAR(100)` | L12 |
| `cCod_doctoDig` | `CHAR(4)` | L13 |
| `cDesc_doctoDig` | `CHAR(35)` | L14 |
| `cCuentaDig` | `CHAR(20)` | L15 |
| `sSecuenciaDig` | `SMALLINT` | L16 |
| `cFechaDig` | `CHAR(10)` | L17 |
| `cDescripcionDig` | `CHAR(50)` | L18 |
| `cNumCteDig` | `CHAR(20)` | L19 |
| `cEmp` | `CHAR(3)` | L20 |
| `cCliente` | `CHAR(20)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cProducto` | `CHAR(4)` | L23 |
| `cCod_docto` | `CHAR(4)` | L24 |
| `sSecuencia` | `SMALLINT` | L25 |
| `cProd_nombre` | `CHAR(40)` | L26 |
| `cDescrip2` | `CHAR(30)` | L27 |
| `cUsuario_alta` | `CHAR(8)` | L28 |
| `dFecha_alta` | `DATE` | L29 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_fc_detdocsdigitalizados` | `bdicnweb` | no | SELECT | L124 |
| `log_fusionclientes` | `bdinteg` | ⚠️ sí | INSERT | L137 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L109 |
| `sp_split_cadena` | `bdicnweb` | no | L119 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L127 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `traspaso` | ACCION | traspaso entre cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `doctos` | ENTIDAD | documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?inc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?inc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_traspasotelefonos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_traspasotelefonos.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=57 |
| **Deps concatenadas** | 152 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso entre cuentas fc — Fuentes Combinadas y teléfonos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_fustraspasotelefonos_soc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_traspasotelefonos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasaCtas             CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasaCtas` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValor` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_fustraspasotelefonos_soc` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_fustraspasotelefonos_soc';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `traspaso` | ACCION | traspaso entre cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `telefonos` | ENTIDAD | teléfonos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_validactasbloqueadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_validactasbloqueadas.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=65 |
| **Deps concatenadas** | 160 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida fc — Fuentes Combinadas y cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_validactasbloqueadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iIsamErrorSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cValor` | `CHAR(100)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_vdactasbqdas` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_vdactasbqdas';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?das` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc`, `?das` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_validaejecucion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_validaejecucion.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 159 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida fc — Fuentes Combinadas y ejecución" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_validaejecucion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pAutoridad                   CHAR(8)
  pCveReporte                  CHAR(50)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pAutoridad` | `CHAR(8)` | — | — |
| `pCveReporte` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValor` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_valida_ejecucion_reporte_regulatorio` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_valida_ejecucion_reporte_regu` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `ejecucion` | ENTIDAD | ejecución (de proceso) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_fc_validaprocede`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_fc_validaprocede.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=62 |
| **Deps concatenadas** | 157 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida fc — Fuentes Combinadas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_fc_validaprocede(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCteTitular                  CHAR(20)
  pCteTraspasaCtas             CHAR(20)
  pUsEjecuta                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCteTitular` | `CHAR(20)` | — | — |
| `pCteTraspasaCtas` | `CHAR(20)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsEjecuta` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cValor` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_valida_procede_fusion` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_valida_procede_fusion_clon';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `procede` | ACCION | procede | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `fc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_finalizacedulas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_finalizacedulas.sql` |
| **LOC (1er CREATE)** | 139 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "cédulas (final)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_finalizacedulas(
  pFechaConcil                 DATE
  pTipo                        SMALLINT
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaConcil` | `DATE` | — | — |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontable` | `bdicheq` | ⚠️ sí | SELECT | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | VALIDACIÓN_NULL | `IF ( ( pFechaConcil is null OR pFechaConcil = '' ) OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `final` | MODIF | final | 🔵 CONVENCIÓN | nombre_sp |
| `?iza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cedulas` | ENTIDAD | cédulas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iza` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gc_altamodificacionbines`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gc_altamodificacionbines.sql` |
| **LOC (1er CREATE)** | 168 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 107 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta · INE — Instituto Nacional Electoral" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_gc_altamodificacionbines(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBin                         CHAR(6)
  pTipoOperacion               CHAR(1)
  pIdBanco                     CHAR(2)
  pCveBanco                    CHAR(3)
  pNombreBanco                 CHAR(25)
  pCredDeb                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBin` | `CHAR(6)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pIdBanco` | `CHAR(2)` | — | — |
| `pCveBanco` | `CHAR(3)` | — | — |
| `pNombreBanco` | `CHAR(25)` | — | — |
| `pCredDeb` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `dFechaActual` | `DATETIME YEAR TO SECOND` | L9 |
| `cBin` | `CHAR(6)` | L10 |
| `cIdBco` | `CHAR(2)` | L11 |
| `cCreditoDebito` | `CHAR(1)` | L12 |
| `cBancoProsa` | `CHAR(25)` | L13 |
| `cClaveBanco` | `CHAR(3)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_bines` | `bdicheq` | ⚠️ sí | SELECT | L85 |
| `sc_bines_hist` | `bdicnweb` | no | INSERT | L89 |
| `sc_bines` | `bdicheq` | ⚠️ sí | DELETE | L92 |
| `sc_bines` | `bdicheq` | ⚠️ sí | INSERT | L100 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00755';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00485';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00485';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00454';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `modificacion` | ACCION | modificación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ine` | REG | INE — Instituto Nacional Electoral (validación de identidad  | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gc_`, `?b`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gc_consultabines`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gc_consultabines.sql` |
| **LOC (1er CREATE)** | 194 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 106 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta · INE — Instituto Nacional Electoral" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gc_consultabines(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pBin                         CHAR(6)
  pPeriodoDel                  DATE
  pPeriodoAl                   DATE
  pCreditoDebito               CHAR(1)
  pClaveBanco                  CHAR(3)
  pIdentificador               CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | — | — |
| `pBin` | `CHAR(6)` | — | — |
| `pPeriodoDel` | `DATE` | — | — |
| `pPeriodoAl` | `DATE` | — | — |
| `pCreditoDebito` | `CHAR(1)` | — | — |
| `pClaveBanco` | `CHAR(3)` | — | — |
| `pIdentificador` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `cBinBanco` | `CHAR(6)` | L16 |
| `cIdBanco` | `CHAR(2)` | L17 |
| `cCreditoDebito` | `CHAR(1)` | L18 |
| `cNombreBanco` | `CHAR(25)` | L19 |
| `cClaveBanco` | `CHAR(3)` | L20 |
| `cStatusOperacion` | `CHAR(20)` | L21 |
| `dFechaOperacion` | `DATE` | L22 |
| `cEjecutivo` | `CHAR(8)` | L23 |
| `cNombreEjecutivo` | `CHAR(45)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_bines` | `bdicheq` | ⚠️ sí | SELECT | L91 |
| `sc_bines_hist` | `bdicnweb` | no | SELECT | L122 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion = '' OR pRegistros IS NULL OR pRecuperacion IS` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | VALIDACIÓN_NULL | `IF pRegistros IS NULL OR pRecuperacion IS NULL OR pPeriodoDel IS NULL OR pPeriodoAl IS NULL THEN` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L129 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L160 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L166 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ine` | REG | INE — Instituto Nacional Electoral (validación de identidad  | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gc_`, `?b`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gc_consultabines_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gc_consultabines_totales.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=53 |
| **Deps concatenadas** | 146 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta (totales) · INE — Instituto Nacional Electoral" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_gc_consultabines_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPeriodoDel                  DATE
  pPeriodoAl                   DATE
  pBin                         CHAR(6)
  pCreditoDebito               CHAR(1)
  pClaveBanco                  CHAR(3)
  pIdentificador               CHAR (1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPeriodoDel` | `DATE` | — | — |
| `pPeriodoAl` | `DATE` | — | — |
| `pBin` | `CHAR(6)` | — | — |
| `pCreditoDebito` | `CHAR(1)` | — | — |
| `pClaveBanco` | `CHAR(3)` | — | — |
| `pIdentificador` | `CHAR (1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_bines` | `bdicheq` | ⚠️ sí | SELECT | L52 |
| `sc_bines_hist` | `bdicnweb` | no | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L29 | VALIDACIÓN_NULL | `IF  pPeriodoDel IS NULL OR pPeriodoAl IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ine` | REG | INE — Instituto Nacional Electoral (validación de identidad  | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gc_`, `?b`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gc_obtiene_catalogos_usuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gc_obtiene_catalogos_usuario.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 105 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene catálogo y usuario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gc_obtiene_catalogos_usuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               SMALLINT
  pIdCatalogo                  SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pIdCatalogo` | `SMALLINT` | `catalogo`=catálogo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIdCatalogo` | `INTEGER` | L9 |
| `cDescCatalogo` | `CHAR(50)` | L10 |
| `iIdABC` | `INTEGER` | L11 |
| `cDescTipoOperacionABC` | `CHAR(15)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gc_catalogos_funcionalidades` | `bdicnweb` | no | SELECT | L84 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L66 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00749';` |  |
| L77 | VALIDACIÓN_NULL | `IF pIdCatalogo IS NULL THEN` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gc_`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genarchivocopiadmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genarchivocopiadmi.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 70 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genarchivocopiadmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(20)
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | `archivo`=archivo | ✅ CÓDIGO |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRetSp` | `CHAR(100)` | L7 |
| `cNomArchivo` | `CHAR(20)` | L8 |
| `cFechaHoy` | `CHAR(8)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L36 |
| `bdidomi` | `bdicnweb` | no | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdidomi:sp_domi_archcopia';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00616';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00492';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?copi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `adm` | ACCION | administración/administrar (abreviación de admin) | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?copi`, `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genarchivodevoluciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genarchivodevoluciones.sql` |
| **LOC (1er CREATE)** | 680 |
| **Callgraph** | ✅ fan_in=8 / fan_out=12 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera archivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_genarchivodevoluciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pTipo                        CHAR(1)
  pNombreArchivo               CHAR(22)
  pRutaDescarga                CHAR(100)
  pDireccionMac                CHAR(12)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `archivo`=archivo | ✅ CÓDIGO |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pDireccionMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cMiBanco` | `CHAR(3)` | L9 |
| `dFechaMasUno` | `DATE` | L10 |
| `dFechaHabilProx` | `DATE` | L11 |
| `cFormatFecha` | `CHAR(8)` | L12 |
| `cFormatFechaMasUno` | `CHAR(8)` | L13 |
| `cFormatFechaProx` | `CHAR(8)` | L14 |
| `cFechaProx` | `CHAR(8)` | L15 |
| `iNumBloque` | `INTEGER` | L16 |
| `cStatusParam` | `CHAR(2)` | L17 |
| `iHayDatos` | `INTEGER` | L18 |
| `cBcoPresenta` | `CHAR(3)` | L19 |
| `cNombreBanco` | `CHAR(40)` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `iCheque` | `INTEGER` | L22 |
| `mImporte` | `MONEY(16,2)` | L23 |
| `cMotDevol` | `CHAR(2)` | L24 |
| `cGeneradoDev` | `CHAR(1)` | L25 |
| `cFechaTransfer` | `CHAR(8)` | L26 |
| `cImporte` | `CHAR(15)` | L27 |
| `cLoteEntrada` | `CHAR(7)` | L28 |
| *…85 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L249 |
| `cce_encabezado` | `bditef` | ⚠️ sí | SELECT | L262 |
| `sw_cr_consultadetallechequesdev` | `bdicnweb` | no | SELECT | L276 |
| `cce_propios_det` | `bditef` | ⚠️ sí | UPDATE | L406 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L243 |
| `cal_fecha_pre_fh` | `bditef` | ⚠️ sí | L252 |
| `sp_cce_guardar_encabezado` | `bditef` | ⚠️ sí | L311 |
| `sp_cce_guardar_detalle` | `bditef` | ⚠️ sí | L395 |
| `sp_cce_guardar_sumario` | `bditef` | ⚠️ sí | L452 |
| `sp_cce_guardar_gransumario` | `bditef` | ⚠️ sí | L485 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L237 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pTipo = '' OR pNombreArchivo = '' OR pRutaD` |  |
| L238 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L317 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_encabezado';` |  |
| L344 | FÓRMULA | `LET cSecuencia = NVL(iBloqueInicial,0) + NVL(iTotalChequesDet,0);` |  |
| L367 | FÓRMULA | `LET iTotalRegTruncados = iTotalRegTruncados + 1;` | 🔴 MONEY/aritmética financiera |
| L391 | FÓRMULA | `LET iTotalCheques = iTotalCheques + 1;` |  |
| L392 | FÓRMULA | `LET mTotalImporte = mTotalImporte + mImporte;` | 🔴 MONEY/aritmética financiera |
| L403 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_detalle';` |  |
| L412 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L421 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L425 | FÓRMULA | `LET iTotalChequesDet = iTotalChequesDet + 1;` |  |
| L432 | FÓRMULA | `LET iSecuencia = NVL(iTotalCheques,0) + 2;` |  |
| L440 | FÓRMULA | `LET cTotalImporte = SUBSTR(cTotalImporte, CHARINDEX('$', cTotalImporte) + 1);` | 🔴 MONEY/aritmética financiera |
| L441 | FÓRMULA | `LET cMonto = SUBSTR(cTotalImporte, 1, CHARINDEX('.', cTotalImporte) - 1);` | 🔴 MONEY/aritmética financiera |
| L442 | FÓRMULA | `LET cCents = SUBSTR(cTotalImporte, CHARINDEX('.', cTotalImporte) + 1);` | 🔴 MONEY/aritmética financiera |
| L456 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_sumario';` |  |
| L473 | FÓRMULA | `LET cTotalImporte = SUBSTR(cTotalImporte, CHARINDEX('$', cTotalImporte) + 1);` | 🔴 MONEY/aritmética financiera |
| L474 | FÓRMULA | `LET cMonto = SUBSTR(cTotalImporte, 1, CHARINDEX('.', cTotalImporte) - 1);` | 🔴 MONEY/aritmética financiera |
| L475 | FÓRMULA | `LET cCents = SUBSTR(cTotalImporte, CHARINDEX('.', cTotalImporte) + 1);` | 🔴 MONEY/aritmética financiera |
| L490 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_gransumario';` |  |
| L511 | FÓRMULA | `LET cSecuencia = NVL(iBloqueInicial,0) + NVL(iTotalChequesDet,0);` |  |
| L534 | FÓRMULA | `LET iTotalRegTruncados = iTotalRegTruncados + 1;` | 🔴 MONEY/aritmética financiera |
| L558 | FÓRMULA | `LET iTotalCheques = iTotalCheques + 1;` |  |
| L559 | FÓRMULA | `LET mTotalImporte = mTotalImporte + mImporte;` | 🔴 MONEY/aritmética financiera |
| L570 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_detalle';` |  |
| L579 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L588 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L592 | FÓRMULA | `LET iTotalChequesDet = iTotalChequesDet + 1;` |  |
| L599 | FÓRMULA | `LET iSecuencia = NVL(iTotalCheques,0) + 2;` |  |
| | *…8 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genarchivoresproveedordmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genarchivoresproveedordmi.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera archivo, ro — Rol Operativo y estado (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_genarchivoresproveedordmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoArchivo                 CHAR(1)
  pNombreArch                  CHAR(20)
  pNumCliente                  CHAR(20)
  pFechaInicio                 CHAR(8)
  pFechaFin                    CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoArchivo` | `CHAR(1)` | `archivo`=archivo | ✅ CÓDIGO |
| `pNombreArch` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicio` | `CHAR(8)` | — | — |
| `pFechaFin` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `cDescCodRet` | `CHAR(95)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `bInTransaction` | `BOOLEAN` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `bdidomi` | `bdicnweb` | no | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdidomi:sp_genera_archivo_prove` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00618';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00644';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00667';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00618';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00616';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00388';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00388';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00115';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?ve` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edo` | ENTIDAD | estado | 🟡 INFERIDO | nombre_sp |
| `?rdmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `ro`, `?ve`, `?rdmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genera_archivo_presencod46`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genera_archivo_presencod46.sql` |
| **LOC (1er CREATE)** | 525 |
| **Callgraph** | ✅ fan_in=78 / fan_out=37 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera archivo y código" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genera_archivo_presencod46(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechadevol                  DATE
  pFechaHoy                    DATE
  pNoBloque                    INTEGER
  pNoBanco                     CHAR(3)
  pRutaDescarga                CHAR(50)
  pDireccionMac                CHAR(15)
  pIdsPresentados              CHAR(500)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechadevol` | `DATE` | — | — |
| `pFechaHoy` | `DATE` | — | — |
| `pNoBloque` | `INTEGER` | — | — |
| `pNoBanco` | `CHAR(3)` | — | — |
| `pRutaDescarga` | `CHAR(50)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pIdsPresentados` | `CHAR(500)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cNombreArchivo` | `CHAR(30)` | L13 |
| `cPrueba` | `CHAR(1)` | L14 |
| `iTotCheques` | `INTEGER` | L15 |
| `iTotImporte` | `INTEGER` | L16 |
| `itotRegTruncados` | `INTEGER` | L17 |
| `iBloqueInicial` | `INTEGER` | L18 |
| `cIdPresentar` | `CHAR(8)` | L19 |
| `iNoDigitalizados` | `INTEGER` | L20 |
| `cTipoRegistro` | `CHAR(2)` | L21 |
| `cNumSecuencia` | `CHAR(7)` | L22 |
| `cNumBanco` | `CHAR(3)` | L23 |
| `cNoBanco` | `CHAR(3)` | L24 |
| `cSentidoTransfer` | `CHAR(1)` | L25 |
| `cPlazaCecoban` | `CHAR(2)` | L26 |
| `cServicioTEI` | `CHAR(1)` | L27 |
| `iDiaMesTransfer` | `SMALLINT` | L28 |
| `cFechaPresenta` | `CHAR(8)` | L29 |
| `cUsoFuturo1` | `CHAR(9)` | L30 |
| `cTipoArchivo` | `CHAR(1)` | L31 |
| `cUsoFuturo2` | `CHAR(302)` | L32 |
| *…70 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L318 |
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L393 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L233 |
| `sp_split_cadena` | `bdicnweb` | no | L257 |
| `bditef` | `bdicnweb` | no | L305 |
| `cta_alertamiento` | `bditef` | ⚠️ sí | L398 |
| `sp_cce_guardar_detalle` | `bditef` | ⚠️ sí | L421 |
| `sp_cce_guardar_sumario` | `bditef` | ⚠️ sí | L465 |
| `sp_cce_guardar_gransumario` | `bditef` | ⚠️ sí | L497 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L215 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L225 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechadevol IS NULL OR pFechaHoy IS NULL OR pNoBloque IS NULL` |  |
| L227 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L289 | FÓRMULA | `LET cNumBanco = pNoBanco;      --' banco bancoppel` |  |
| L310 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_encabezado';` |  |
| L332 | FÓRMULA | `LET iSecuencia = iSecuencia + 1;` |  |
| L339 | FÓRMULA | `LET cBancoCedente = pNoBanco;      --' banco bancoppel` |  |
| L345 | FÓRMULA | `LET cMonto = substr(cImported, 1, CHARINDEX('.', cImported) - 1);` | 🔴 MONEY/aritmética financiera |
| L347 | FÓRMULA | `LET cCents = substr(cImported, CHARINDEX('.', cImported) + 1);` | 🔴 MONEY/aritmética financiera |
| L367 | FÓRMULA | `LET iTotalRegTruncados = iTotalRegTruncados + 1;` | 🔴 MONEY/aritmética financiera |
| L377 | VALIDACIÓN_NULL | `IF TRIM(cRfcCte) = '' OR cRfcCte IS NULL  THEN` |  |
| L417 | FÓRMULA | `LET iTotalCheques = iTotalCheques + 1;` |  |
| L418 | FÓRMULA | `LET mTotalImporte = mTotalImporte + dMonto_orig;` | 🔴 MONEY/aritmética financiera |
| L429 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_detalle';` |  |
| L446 | FÓRMULA | `LET iSecuencia = iSecuencia + 2;` |  |
| L453 | FÓRMULA | `LET cMontos = substr(cImportes, 1, CHARINDEX('.', cImportes) - 1);` | 🔴 MONEY/aritmética financiera |
| L455 | FÓRMULA | `LET cCents = substr(cImportes, CHARINDEX('.', cImportes) + 1);` | 🔴 MONEY/aritmética financiera |
| L468 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_sumario';` |  |
| L485 | FÓRMULA | `LET cMontos = substr(cImportes, 1, CHARINDEX('.', cImportes) - 1);` | 🔴 MONEY/aritmética financiera |
| L487 | FÓRMULA | `LET cCents = substr(cImportes, CHARINDEX('.', cImportes) + 1);` | 🔴 MONEY/aritmética financiera |
| L502 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_gransumario';` |  |
| L508 | CÓDIGO_RETORNO | `LET cCodRet = '00768';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_presen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cod` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?46` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_presen`, `?46` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genera_archivo_presencod47`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genera_archivo_presencod47.sql` |
| **LOC (1er CREATE)** | 516 |
| **Callgraph** | ✅ fan_in=78 / fan_out=37 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera archivo y código" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genera_archivo_presencod47(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaHoy                    DATE
  pNoBloque                    INTEGER
  pNoBanco                     CHAR(3)
  pRutaDescarga                CHAR(50)
  pDireccionMac                CHAR(15)
  pIdsEliminar                 CHAR(500)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaHoy` | `DATE` | — | — |
| `pNoBloque` | `INTEGER` | — | — |
| `pNoBanco` | `CHAR(3)` | — | — |
| `pRutaDescarga` | `CHAR(50)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pIdsEliminar` | `CHAR(500)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNombreArchivo` | `CHAR(30)` | L12 |
| `cPrueba` | `CHAR(1)` | L13 |
| `iTotCheques` | `INTEGER` | L14 |
| `iTotImporte` | `INTEGER` | L15 |
| `itotRegTruncados` | `INTEGER` | L16 |
| `iBloqueInicial` | `INTEGER` | L17 |
| `cIdPresentar` | `CHAR(8)` | L18 |
| `cTipoRegistro` | `CHAR(2)` | L19 |
| `cNumSecuencia` | `CHAR(7)` | L20 |
| `cNumBanco` | `CHAR(3)` | L21 |
| `cNoBanco` | `CHAR(3)` | L22 |
| `cSentidoTransfer` | `CHAR(1)` | L23 |
| `cPlazaCecoban` | `CHAR(2)` | L24 |
| `cServicioTEI` | `CHAR(1)` | L25 |
| `iDiaMesTransfer` | `SMALLINT` | L26 |
| `cFechaPresenta` | `CHAR(8)` | L27 |
| `cUsoFuturo1` | `CHAR(9)` | L28 |
| `cTipoArchivo` | `CHAR(1)` | L29 |
| `cUsoFuturo2` | `CHAR(302)` | L30 |
| `iSecuencia` | `INTEGER` | L31 |
| *…67 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L299 |
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L372 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L224 |
| `sp_split_cadena` | `bdicnweb` | no | L250 |
| `bditef` | `bdicnweb` | no | L285 |
| `cta_alertamiento` | `bditef` | ⚠️ sí | L377 |
| `sp_cce_guardar_detalle` | `bditef` | ⚠️ sí | L401 |
| `sp_cce_eliminar_cheques` | `bdicheq` | ⚠️ sí | L413 |
| `sp_cce_guardar_sumario` | `bditef` | ⚠️ sí | L457 |
| `sp_cce_guardar_gransumario` | `bditef` | ⚠️ sí | L492 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L206 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L216 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaHoy IS NULL OR pNoBloque IS NULL  OR pNoBanco ='' OR` |  |
| L218 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L269 | FÓRMULA | `LET cNumBanco = pNoBanco;      --' banco bancoppel` |  |
| L290 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_encabezado';` |  |
| L311 | FÓRMULA | `LET iSecuencia = iSecuencia + 1;` |  |
| L313 | FÓRMULA | `LET cNumSecuencia = TO_CHAR(iBloqueInicial + iSecuencia);` |  |
| L319 | FÓRMULA | `LET cBancoCedente = pNoBanco;      --' banco bancoppel` |  |
| L326 | FÓRMULA | `LET cMonto = substr(cImported, 1, CHARINDEX('.', cImported) - 1);` | 🔴 MONEY/aritmética financiera |
| L328 | FÓRMULA | `LET cCents = substr(cImported, CHARINDEX('.', cImported) + 1);` | 🔴 MONEY/aritmética financiera |
| L347 | FÓRMULA | `LET iTotalRegTruncados = iTotalRegTruncados + 1;` | 🔴 MONEY/aritmética financiera |
| L356 | VALIDACIÓN_NULL | `IF TRIM(cRfcCte) = ''  OR cRfcCte IS NULL THEN` |  |
| L396 | FÓRMULA | `LET iTotalCheques = iTotalCheques + 1;` |  |
| L397 | FÓRMULA | `LET mTotalImporte = mTotalImporte + mImporte;` | 🔴 MONEY/aritmética financiera |
| L409 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_detalle';` |  |
| L416 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_cce_eliminar_cheques';` |  |
| L433 | FÓRMULA | `LET iSecuencia = iSecuencia + 2;` |  |
| L444 | FÓRMULA | `LET cMontos = substr(cImportes, 1, CHARINDEX('.', cImportes) - 1);` | 🔴 MONEY/aritmética financiera |
| L446 | FÓRMULA | `LET cCents = substr(cImportes, CHARINDEX('.', cImportes) + 1);` | 🔴 MONEY/aritmética financiera |
| L460 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_sumario';` |  |
| L479 | FÓRMULA | `LET cMontos = substr(cImportes, 1, CHARINDEX('.', cImportes) - 1);` | 🔴 MONEY/aritmética financiera |
| L481 | FÓRMULA | `LET cCents = substr(cImportes, CHARINDEX('.', cImportes) + 1);` | 🔴 MONEY/aritmética financiera |
| L497 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_gransumario';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_presen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cod` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?47` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_presen`, `?47` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generacedulaintsisr`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generacedulaintsisr.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "genera cédula de identificación y interés · ISR — Impuesto Sobre la Renta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generacedulaintsisr(
  pEmpresa                     CHAR(3)
  pFecha                       DATE
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iTransacc` | `SMALLINT` | L10 |
| `dFecha` | `DATE` | L11 |
| `cProducto` | `CHAR(4)` | L12 |
| `cNombreProd` | `CHAR(40)` | L13 |
| `mIntsCalculados` | `DECIMAL(18,2)` | L14 |
| `mIntsPagados` | `DECIMAL(18,2)` | L15 |
| `mDifIntereses` | `DECIMAL(18,2)` | L16 |
| `mISRCalculado` | `DECIMAL(14,2)` | L17 |
| `mIsrCobrado` | `DECIMAL(18,2)` | L18 |
| `mDiferenciaISR` | `DECIMAL(14,2)` | L19 |
| `cObservaciones` | `CHAR(255)` | L20 |
| `cEditable` | `CHAR(1)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_pagoints_cobroisr` | `bdicheq` | ⚠️ sí | SELECT | L86 |
| `sc_intisrxprodcedula` | `bdicheq` | ⚠️ sí | INSERT | L94 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | VALIDACIÓN_NULL | `IF ( ( pEmpresa is null OR pEmpresa = '' ) OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `cedula` | ENTIDAD | cédula de identificación | 🔵 CONVENCIÓN | nombre_sp |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `isr` | REG | ISR — Impuesto Sobre la Renta (retención · SAT) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generador_archivos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generador_archivos.sql` |
| **LOC (1er CREATE)** | 581 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "genera archivos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_generador_archivos(
  pBandera                     CHAR(2)
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
  pIdRegistro                  INTEGER
  pOpcion                      INTEGER
  pImporteTotal                DECIMAL(14,2)
  pnombrearchivo               CHAR(30)
  pRutaArchivo                 CHAR(60)
  pIdsEliminar                 CHAR(500)
  pFechaHoy                    DATE
  pFechadevol                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pNoBloque                    INTEGER
  pNoBanco                     CHAR(3)
  pRutaDescarga                CHAR(50)
  pIdsPresentados              CHAR(500)
  pIdCheque                    INTEGER
  pFecha                       DATE
  pCodigo                      CHAR(2)
  pIdConsulta                  CHAR(1)
) RETURNING CHAR(5) 		AS r_codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pIdRegistro` | `INTEGER` | — | — |
| `pOpcion` | `INTEGER` | — | — |
| `pImporteTotal` | `DECIMAL(14,2)` | — | — |
| `pnombrearchivo` | `CHAR(30)` | — | — |
| `pRutaArchivo` | `CHAR(60)` | — | — |
| `pIdsEliminar` | `CHAR(500)` | — | — |
| `pFechaHoy` | `DATE` | — | — |
| `pFechadevol` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |
| `pNoBloque` | `INTEGER` | — | — |
| `pNoBanco` | `CHAR(3)` | — | — |
| `pRutaDescarga` | `CHAR(50)` | — | — |
| `pIdsPresentados` | `CHAR(500)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pIdCheque` | `INTEGER` | — | — |
| `pFecha` | `DATE` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L85 |
| `iSqlErr` | `INTEGER` | L86 |
| `importeTotal` | `DECIMAL(20,2)` | L87 |
| `bBanDet` | `CHAR(1)` | L88 |
| `iNoRegistros` | `INTEGER` | L89 |
| `iTotalValidos` | `INTEGER` | L90 |
| `iNoBloque` | `INTEGER` | L91 |
| `idRowDetalle` | `INTEGER` | L92 |
| `cBancoLibrado` | `CHAR(3)` | L93 |
| `cDescbancoLibrado` | `CHAR(50)` | L94 |
| `mImporte` | `DECIMAL(14,2)` | L95 |
| `cCuentaReferencia` | `CHAR(13)` | L96 |
| `cNumCheque` | `CHAR(10)` | L97 |
| `cCuentaDeposito` | `CHAR(20)` | L98 |
| `cObservaciones` | `CHAR(70)` | L99 |
| `cMotivoDevolucion` | `CHAR(100)` | L100 |
| `cprocesar` | `CHAR(2)` | L101 |
| `cEmpresa` | `CHAR(3)` | L102 |
| `dFecha` | `DATE` | L103 |
| `cNoBanco` | `CHAR(3)` | L104 |
| `cProcesado` | `CHAR(1)` | L105 |
| `cNombreArchivo` | `CHAR(30)` | L106 |
| `dFechaHabilAnt` | `DATE` | L107 |
| `bIsChequeDuplicado` | `BOOLEAN` | L108 |
| `dMontoTotalValido` | `DECIMAL(16,2)` | L109 |
| *…30 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_actualiza_chqrevisados_ccep` | `bdicnweb` | no | L225 |
| `sp_aplicadevol_cod41_ccep` | `bdicnweb` | no | L237 |
| `sp_cargacod41_ccep` | `bdicnweb` | no | L249 |
| `sp_ccep_eliminacheques_cod46` | `bdicnweb` | no | L261 |
| `sp_consultachequescod47totales_ccep` | `bdicnweb` | no | L273 |
| `sp_consultadelvorevcod46total_ccep` | `bdicnweb` | no | L284 |
| `sp_consultaprocescod41_ccep` | `bdicnweb` | no | L296 |
| `sp_datosdiahoy_cod47` | `bdicnweb` | no | L311 |
| `sp_genera_archivo_presencod46` | `bdicnweb` | no | L323 |
| `sp_genera_archivo_presencod47` | `bdicnweb` | no | L334 |
| `sp_ope_chequeduplicado` | `bdicnweb` | no | L345 |
| `sp_ope_consbloquearchivopresentado` | `bdicnweb` | no | L356 |
| `sp_ope_consultachequetamdif` | `bdicnweb` | no | L368 |
| `sp_ope_consultadetallechequecodigo40` | `bdicnweb` | no | L384 |
| `sp_ope_consultadetallechequecodigo40_totales` | `bdicnweb` | no | L399 |
| `sp_ope_datoscarga_genarchivo` | `bdicnweb` | no | L413 |
| `sp_ope_datosgral_archivocod46_ccep` | `bdicnweb` | no | L424 |
| `sp_ope_consultaimportececoban` | `bdicnweb` | no | L435 |
| `sp_ope_validachequeduplicado` | `bdicnweb` | no | L446 |
| `sp_ope_generarchivopresentado` | `bdicnweb` | no | L457 |
| `sp_ope_genera_archivo_img_presentado` | `bdicnweb` | no | L468 |
| `sp_tef_grab_arch_cam` | `bditef` | ⚠️ sí | L479 |
| `sp_valida_descargaimg_ccep` | `bdicnweb` | no | L490 |
| `sp_eliminasinprocesartmpcod40` | `bdicnweb` | no | L501 |
| `sp_ope_consultadetallechequecodigo46` | `bdicnweb` | no | L512 |
| `sp_totalesconsultacod41` | `bdicnweb` | no | L527 |
| `sp_ope_reportecodigo46` | `bdicnweb` | no | L539 |
| `sp_ope_consultadetallechequecodigo47` | `bdicnweb` | no | L552 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?dor_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivos` | ENTIDAD | archivos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dor_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generadorpresentadoreceptordmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generadorpresentadoreceptordmi.sql` |
| **LOC (1er CREATE)** | 238 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera receptor (presentado)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generadorpresentadoreceptordmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(20)
  pIdOperacion                 CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | — | — |
| `pIdOperacion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `cNombreArchivo` | `CHAR(20)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `bInTransaccion` | `BOOLEAN` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L42 |
| `bdidomi` | `bdicnweb` | no | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdidomi:sp_domi_generador_pres` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00521';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00522';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00523';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00524';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00525';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00526';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00677';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00574';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00678';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00530';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00679';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00680';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00532';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00533';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00534';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00535';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00536';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00537';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00538';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00695';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00540';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00541';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00616';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00644';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00653';` |  |
| | *…42 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?dor` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `presentado` | MODIF | presentado (a cobro) | 🔵 CONVENCIÓN | nombre_sp |
| `receptor` | ENTIDAD | receptor | 🔵 CONVENCIÓN | nombre_sp |
| `?dmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dor`, `?dmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generafoliocallezona`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generafoliocallezona.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 225 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera folio, calle y zona" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_generafoliocallezona(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                CHAR(1)
  pIdCiudad                    INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | — | — |
| `pIdCiudad` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iFolio` | `INTEGER` | L7 |
| `cValorParam` | `CHAR(100)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L48 |
| `si_param_dom` | `bdinteg` | ⚠️ sí | SELECT | L58 |
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | VALIDACIÓN_NULL | `IF iFolio IS NULL THEN` |  |
| L64 | FÓRMULA | `LET iFolio = iFolio + 1;` |  |
| L78 | VALIDACIÓN_NULL | `IF iFolio IS NULL THEN` |  |
| L83 | FÓRMULA | `LET iFolio = iFolio + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `folio` | ENTIDAD | folio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `calle` | ENTIDAD | calle (domicilio) | 🔵 CONVENCIÓN | nombre_sp |
| `zona` | ENTIDAD | zona | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_generafolionominacre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generafolionominacre.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera folio de nómina crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_generafolionomina` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_generafolionominacre(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  cNombreUsuario               CHAR(8)
) RETURNING CHAR(5) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNombreUsuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L9 |
| `vCodRet` | `Char(3)` | L11 |
| `cNumeroFolio` | `Char(16)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_generafolionomina` | `bdicheq` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generafolionomina` | ACCION | genera folio de nómina | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_generahojafirmasctamec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generahojafirmasctamec.sql` |
| **LOC (1er CREATE)** | 149 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera firmas mancomunadas y Cuenta Mecánica — tipo de cuenta de cheques empresarial para nómina y pagos automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_ctamec_generarrpthojadefirmas2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_generahojafirmasctamec(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `iCodRetSp` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `cMensajeEjec` | `CHAR(60)` | L26 |
| `cDescProducto` | `CHAR(40)` | L27 |
| `cNumCliente` | `CHAR(20)` | L28 |
| `cNombreCte` | `CHAR(104)` | L29 |
| `cCodSucursal` | `CHAR(4)` | L30 |
| `cDescMoneda` | `CHAR(30)` | L31 |
| `dFechaAlta` | `DATE` | L32 |
| `dFechaModific` | `DATE` | L33 |
| `cRegimen` | `CHAR(20)` | L34 |
| `cEspecifManejo` | `CHAR(20)` | L35 |
| `cNombreFirmante1` | `CHAR(104)` | L36 |
| `cFirmante1` | `CHAR(20)` | L37 |
| `cNombreFirmante2` | `CHAR(104)` | L38 |
| `cFirmante2` | `CHAR(20)` | L39 |
| `cNombreFirmante3` | `CHAR(104)` | L40 |
| `cFirmante3` | `CHAR(20)` | L41 |
| `cNombreFirmante4` | `CHAR(104)` | L42 |
| `cFirmante4` | `CHAR(20)` | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L91 |
| `sp_ctamec_generarrpthojadefirmas2` | `bdicheq` | ⚠️ sí | L100 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L108 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_ctamec_generarrpthojadefirmas` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00332';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00127';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00127';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?hoja` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `firmas` | ENTIDAD | firmas mancomunadas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctamec` | ENTIDAD | Cuenta Mecánica — tipo de cuenta de cheques empresarial para | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hoja` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generahojafirmasctamec2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generahojafirmasctamec2.sql` |
| **LOC (1er CREATE)** | 136 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera firmas mancomunadas y Cuenta Mecánica — tipo de cuenta de cheques empresarial para nómina y pagos automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_ctamec_generarrpthojadefirmas2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generahojafirmasctamec2(
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cCodRetSp` | `CHAR(6)` | L23 |
| `iCodRetSp` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `cMensajeEjec` | `CHAR(60)` | L26 |
| `cDescProducto` | `CHAR(40)` | L27 |
| `cNumCliente` | `CHAR(20)` | L28 |
| `cNombreCte` | `CHAR(104)` | L29 |
| `cCodSucursal` | `CHAR(4)` | L30 |
| `cDescMoneda` | `CHAR(30)` | L31 |
| `dFechaAlta` | `DATE` | L32 |
| `dFechaModific` | `DATE` | L33 |
| `cRegimen` | `CHAR(20)` | L34 |
| `cEspecifManejo` | `CHAR(20)` | L35 |
| `cNombreFirmante1` | `CHAR(104)` | L36 |
| `cFirmante1` | `CHAR(20)` | L37 |
| `cNombreFirmante2` | `CHAR(104)` | L38 |
| `cFirmante2` | `CHAR(20)` | L39 |
| `cNombreFirmante3` | `CHAR(104)` | L40 |
| `cFirmante3` | `CHAR(20)` | L41 |
| `cNombreFirmante4` | `CHAR(104)` | L42 |
| `cFirmante4` | `CHAR(20)` | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ctamec_generarrpthojadefirmas2` | `bdicheq` | ⚠️ sí | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_ctamec_generarrpthojadefirmas` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00332';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00127';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00127';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?hoja` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `firmas` | ENTIDAD | firmas mancomunadas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctamec` | ENTIDAD | Cuenta Mecánica — tipo de cuenta de cheques empresarial para | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hoja`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generaportadactamec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generaportadactamec.sql` |
| **LOC (1er CREATE)** | 181 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 82 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generaportadactamec(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCta                      CHAR(20)
) RETURNING CHAR(5)       AS      codret,                 --Codigo de retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCodRetSp` | `CHAR(3)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `cCodProducto` | `CHAR(4)` | L29 |
| `cNomProducto` | `CHAR(40)` | L30 |
| `cRazonSoc` | `CHAR(104)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cNumCuenta` | `CHAR(20)` | L33 |
| `cClabe` | `CHAR(18)` | L34 |
| `cClaveRegimen` | `CHAR(1)` | L35 |
| `cRegimenFirmas` | `CHAR(20)` | L36 |
| `cEspeciManejo` | `CHAR(20)` | L37 |
| `cRfc` | `CHAR(13)` | L38 |
| `dFechaOperacion` | `DATE` | L39 |
| `cNombreFirmante` | `CHAR(104)` | L40 |
| `cTipoFirma` | `CHAR(1)` | L41 |
| `cSucursal` | `CHAR(4)` | L42 |
| `cNomsuc` | `CHAR(40)` | L43 |
| `cReca` | `CHAR(60)` | L44 |
| `cHoraOperacion` | `CHAR(10)` | L45 |
| `cFolioOperacion` | `CHAR(20)` | L46 |
| `cCodigoEmpresa` | `CHAR(3)` | L47 |
| `cCuentaLigada` | `CHAR(20)` | L48 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L160 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L105 |
| `sp_ctamec_generarptportada` | `bdicheq` | ⚠️ sí | L115 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00335';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L145 | CÓDIGO_RETORNO | `LET cCodRet = '00303';` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00332';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?tad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?amec` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tad`, `?amec` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generaportadactamec2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generaportadactamec2.sql` |
| **LOC (1er CREATE)** | 185 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generaportadactamec2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCta                      CHAR(20)
) RETURNING CHAR(5)       AS      codret,                 --Codigo de retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCodRetSp` | `CHAR(3)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `cCodProducto` | `CHAR(4)` | L29 |
| `cNomProducto` | `CHAR(40)` | L30 |
| `cRazonSoc` | `CHAR(254)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cNumCuenta` | `CHAR(20)` | L33 |
| `cClabe` | `CHAR(18)` | L34 |
| `cClaveRegimen` | `CHAR(1)` | L35 |
| `cRegimenFirmas` | `CHAR(20)` | L36 |
| `cEspeciManejo` | `CHAR(20)` | L37 |
| `cRfc` | `CHAR(13)` | L38 |
| `dFechaOperacion` | `DATE` | L39 |
| `cNombreFirmante` | `CHAR(104)` | L40 |
| `cTipoFirma` | `CHAR(1)` | L41 |
| `cSucursal` | `CHAR(4)` | L42 |
| `cNomsuc` | `CHAR(40)` | L43 |
| `cReca` | `CHAR(60)` | L44 |
| `cHoraOperacion` | `CHAR(10)` | L45 |
| `cFolioOperacion` | `CHAR(20)` | L46 |
| `cCodigoEmpresa` | `CHAR(3)` | L47 |
| `cCuentaLigada` | `CHAR(20)` | L48 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L160 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L105 |
| `sp_ctamec_generarptportadaproducto2_1` | `bdicheq` | ⚠️ sí | L115 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00335';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L145 | CÓDIGO_RETORNO | `LET cCodRet = '00303';` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00332';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?tad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?amec2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tad`, `?amec2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generaportadactamec3`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generaportadactamec3.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generaportadactamec3(
  pNumCte                      CHAR(20)
  pNumCta                      CHAR(20)
) RETURNING CHAR(5)       AS      codret,                 --Codigo de retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCodRetSp` | `CHAR(3)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `cCodProducto` | `CHAR(4)` | L29 |
| `cNomProducto` | `CHAR(40)` | L30 |
| `cRazonSoc` | `CHAR(104)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cNumCuenta` | `CHAR(20)` | L33 |
| `cClabe` | `CHAR(18)` | L34 |
| `cClaveRegimen` | `CHAR(1)` | L35 |
| `cRegimenFirmas` | `CHAR(20)` | L36 |
| `cEspeciManejo` | `CHAR(20)` | L37 |
| `cRfc` | `CHAR(13)` | L38 |
| `dFechaOperacion` | `DATE` | L39 |
| `cNombreFirmante` | `CHAR(104)` | L40 |
| `cTipoFirma` | `CHAR(1)` | L41 |
| `cSucursal` | `CHAR(4)` | L42 |
| `cNomsuc` | `CHAR(40)` | L43 |
| `cReca` | `CHAR(60)` | L44 |
| `cHoraOperacion` | `CHAR(10)` | L45 |
| `cFolioOperacion` | `CHAR(20)` | L46 |
| `cCodigoEmpresa` | `CHAR(3)` | L47 |
| `cCuentaLigada` | `CHAR(20)` | L48 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L142 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ctamec_generarptportadaproducto2_1` | `bdicheq` | ⚠️ sí | L97 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00335';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00303';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00332';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?tad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?amec3` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tad`, `?amec3` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generaportadactamec4`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generaportadactamec4.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=0 / fan_out=60 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generaportadactamec4(
  pNumCte                      CHAR(20)
  pNumCta                      CHAR(20)
) RETURNING CHAR(5)       AS      codret,                 --Codigo de retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCodRetSp` | `CHAR(3)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `cCodProducto` | `CHAR(4)` | L29 |
| `cNomProducto` | `CHAR(40)` | L30 |
| `cRazonSoc` | `CHAR(314)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cNumCuenta` | `CHAR(20)` | L33 |
| `cClabe` | `CHAR(18)` | L34 |
| `cClaveRegimen` | `CHAR(1)` | L35 |
| `cRegimenFirmas` | `CHAR(20)` | L36 |
| `cEspeciManejo` | `CHAR(20)` | L37 |
| `cRfc` | `CHAR(13)` | L38 |
| `dFechaOperacion` | `DATE` | L39 |
| `cNombreFirmante` | `CHAR(104)` | L40 |
| `cTipoFirma` | `CHAR(1)` | L41 |
| `cSucursal` | `CHAR(4)` | L42 |
| `cNomsuc` | `CHAR(40)` | L43 |
| `cReca` | `CHAR(60)` | L44 |
| `cHoraOperacion` | `CHAR(10)` | L45 |
| `cFolioOperacion` | `CHAR(20)` | L46 |
| `cCodigoEmpresa` | `CHAR(3)` | L47 |
| `cCuentaLigada` | `CHAR(20)` | L48 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L145 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ctamec_generarptportadaproducto2_2` | `bdicheq` | ⚠️ sí | L100 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00335';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00016';` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00303';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00332';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?tad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?amec4` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tad`, `?amec4` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generararchivo_rst`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generararchivo_rst.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=345 / fan_out=18 |
| **Principales callers** | `sp_bitacora`, `sp_consultareportepagoscre`, `sp_generararchivo_rst` |
| **Deps concatenadas** | 67 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar archivo y rst — formato RST" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_generararchivo_rst(
  pNombreArchivo               CHAR(255)
  pCmdRespaldo                 CHAR(2000)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNombreArchivo` | `CHAR(255)` | `archivo`=archivo | ✅ CÓDIGO |
| `pCmdRespaldo` | `CHAR(2000)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cSql` | `CHAR(2500)` | L7 |
| `cArchivoTemp` | `CHAR(50)` | L8 |
| `cRutaArchivo` | `CHAR(255)` | L9 |
| `ven_transacc` | `SMALLINT` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rst` | ENTIDAD | rst — formato RST (sp_generararchivo_rst fan_in=345 — NO_VER | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `rst` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generarchivodotacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generarchivodotacionescaja.sql` |
| **LOC (1er CREATE)** | 227 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar CHI — formato/protocolo de consulta al Buró de Crédito, dotaciones de efectivo y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generarchivodotacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchDescarga                CHAR(150)
  pSelectAll                   CHAR(1)
  pTipoSuc                     CHAR(1)
  pConsecutivo                 CHAR (30)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pSelectAll` | `CHAR(1)` | — | — |
| `pTipoSuc` | `CHAR(1)` | — | — |
| `pConsecutivo` | `CHAR (30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cFolioOperacion` | `CHAR(8)` | L8 |
| `cTotalRegistros` | `CHAR(8)` | L9 |
| `cSelect` | `CHAR(1500)` | L10 |
| `cFrom` | `CHAR(1500)` | L11 |
| `cWhere` | `CHAR(1500)` | L12 |
| `cCmdSelect` | `CHAR(6000)` | L13 |
| `cCmdFrom` | `CHAR(4000)` | L14 |
| `cCmdWhere` | `CHAR(2000)` | L15 |
| `cCmndSelect` | `CHAR(1500)` | L16 |
| `cCmndFrom` | `CHAR(1500)` | L17 |
| `cCmndWhere` | `CHAR(1500)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |
| `cValor` | `CHAR(30)` | L21 |
| `d_datm` | `CHAR(4)` | L22 |
| `fecha_enc` | `CHAR(8)` | L23 |
| `no_consec` | `CHAR(2)` | L24 |
| `no_registros` | `CHAR(8)` | L25 |
| `cEncabezados` | `CHAR(1500)` | L26 |
| `bInTransaction` | `CHAR(1)` | L27 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L76 |
| `bdicnweb` | `bdicnweb` | no | L210 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | FÓRMULA | `LET cSelect = "SELECT COUNT(*)";` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L149 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" LPAD(a.sucursal::INTEGER, 10, '0') AS sucursal, TO_CHAR(` | 🔴 MONEY/aritmética financiera |
| L152 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_1 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L153 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_1 > 0 THEN TO_CHAR((a.cantidad_1 * ` | 🔴 MONEY/aritmética financiera |
| L155 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_2 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L156 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_2 > 0 THEN TO_CHAR((a.cantidad_2 * ` | 🔴 MONEY/aritmética financiera |
| L158 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_3 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L159 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_3 > 0 THEN TO_CHAR((a.cantidad_3 * ` | 🔴 MONEY/aritmética financiera |
| L161 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_4 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L162 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_4 > 0 THEN TO_CHAR((a.cantidad_4 * ` | 🔴 MONEY/aritmética financiera |
| L164 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_5 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L165 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_5 > 0 THEN TO_CHAR((a.cantidad_5 * ` | 🔴 MONEY/aritmética financiera |
| L167 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_6 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L168 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_6 > 0 THEN TO_CHAR((a.cantidad_6 * ` | 🔴 MONEY/aritmética financiera |
| L170 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_7 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L171 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_7 > 0 THEN TO_CHAR((a.cantidad_7 * ` | 🔴 MONEY/aritmética financiera |
| L173 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_8 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L174 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_8 > 0 THEN TO_CHAR((a.cantidad_8 * ` | 🔴 MONEY/aritmética financiera |
| L176 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_9 > 0 THEN TO_CHAR((a.denominacion_` |  |
| L177 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_9 > 0 THEN TO_CHAR((a.cantidad_9 * ` | 🔴 MONEY/aritmética financiera |
| L179 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_10 > 0 THEN TO_CHAR((a.denominacion` |  |
| L180 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_10 > 0 THEN TO_CHAR((a.cantidad_10 ` | 🔴 MONEY/aritmética financiera |
| L182 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_11 > 0 THEN TO_CHAR((a.denominacion` |  |
| L183 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_11 > 0 THEN TO_CHAR((a.cantidad_11 ` | 🔴 MONEY/aritmética financiera |
| L185 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_12 > 0 THEN TO_CHAR((a.denominacion` |  |
| L186 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_12 > 0 THEN TO_CHAR((a.cantidad_12 ` | 🔴 MONEY/aritmética financiera |
| L188 | FÓRMULA | `LET cCmdSelect =""\|\|TRIM(cCmdSelect)\|\|" CASE WHEN a.cantidad_13 > 0 THEN TO_CHAR((a.denominacion` |  |
| | *…6 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?vo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?vo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generarelacionctebcpcp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generarelacionctebcpcp.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 343 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar cliente y código postal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_relacion_generarelacion` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_generarelacionctebcpcp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumCteReferencia            CHAR(20)
  pTipoRelacion                CHAR(1)
  pOrigen                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumCteReferencia` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pTipoRelacion` | `CHAR(1)` | — | — |
| `pOrigen` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cMensajeRetorno` | `CHAR(80)` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_relacion_generarelacion` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERORR EN LA EJECUCION DEL SP sp_relacion_generarelacion';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00275';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '01136';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `?elacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?elacion`, `?b` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generareporte_desactualizadasbc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generareporte_desactualizadasbc.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar Servicios de Atención al Cliente — subsistema de atención en sucursal y saldo básico de cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_generareporte_desactualizadasbc(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(150)
  pFecha                       CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |
| `pFecha` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iExiste` | `INT` | L7 |
| `cCmd1` | `CHAR(3000)` | L8 |
| `cCmd2` | `CHAR(3000)` | L9 |
| `cNivelDesactualizada` | `CHAR(3)` | L10 |
| `cMemberKob` | `CHAR(2)` | L11 |
| `cNameReport` | `CHAR(60)` | L12 |
| `cArchivoAux` | `CHAR(150)` | L13 |
| `cArchDescarga` | `CHAR(150)` | L14 |
| `iNoRegistros` | `INT` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_desactualizadas_buro` | `bdicred` | ⚠️ sí | SELECT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneencabezadomasivo` | `bdicnweb` | no | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |
| L78 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te_de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `?tualizada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sbc` | ENTIDAD | saldo básico de cuenta (SBC) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te_de`, `?tualizada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generareporteconciliaaperturapagarescargo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generareporteconciliaaperturapagarescargo.sql` |
| **LOC (1er CREATE)** | 228 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 345 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar apertura, pagarés y cargo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_generareporteconciliaaperturapagarescargo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pFormato                     CHAR(1)
  pRutaDescarga                CHAR(100)
  pTotalTrans                  CHAR(11)
  pMontoApe                    CHAR(20)
  pMontoCar                    CHAR(20)
  pMontoDif                    CHAR(20)
) RETURNING CHAR(5)AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | `por`=por (criterio) | 🔵 CONVENCIÓN |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pFormato` | `CHAR(1)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pTotalTrans` | `CHAR(11)` | — | — |
| `pMontoApe` | `CHAR(20)` | — | — |
| `pMontoCar` | `CHAR(20)` | — | — |
| `pMontoDif` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cCmd1` | `CHAR(2000)` | L10 |
| `cSql` | `CHAR(2500)` | L11 |
| `cRutaInformix` | `CHAR(100)` | L12 |
| `cUsrBin` | `CHAR(100)` | L13 |
| `cRutaGral` | `CHAR(150)` | L14 |
| `cNombreReporte` | `CHAR(100)` | L15 |
| `bInTransaction` | `BOOLEAN` | L16 |
| `ven_transacc` | `SMALLINT` | L17 |
| `dFechaHoy` | `DATE` | L18 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L19 |
| `dFechaMinSel` | `DATE` | L20 |
| `dFechaMaxSel` | `DATE` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L94 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pTipoReporte = '' OR pFechaInicial IS NULL OR pFechaFinal IS` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | FÓRMULA | `LET dFechaMaxSel = dFechaMinSel + 1 UNITS MONTH;` |  |
| L95 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT 'FECHA ALTA','NO. SERIAL','FOLIO S` |  |
| L98 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT TO_CHAR(fecha_alt, '%d/%m/%Y') AS ` |  |
| L118 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT 'FECHA ALTA','NO. SERIAL','FOLIO S` |  |
| L121 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT TO_CHAR(fecha_alt, '%d/%m/%Y') AS ` |  |
| L122 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"descripcion_apert,''''\|\|transacc_cargo,descripcion_cargo,producto` | 🔴 MONEY/aritmética financiera |
| L134 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L172 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L186 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L195 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `concilia` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `apertura` | ENTIDAD | apertura (de cuenta/crédito) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pagares` | ENTIDAD | pagarés | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generareportemedianainflacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generareportemedianainflacion.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar información (del día)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_generareportemedianainflacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) 		AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRet` | `CHAR(5)` | L15 |
| `cProducto` | `CHAR(5)` | L17 |
| `cDescProducto` | `CHAR(30)` | L18 |
| `dTasa` | `DECIMAL (9,6)` | L19 |
| `dMedInflacion` | `DECIMAL(9,6)` | L20 |
| `cPeriodo` | `CHAR (2)` | L21 |
| `dGatNominal` | `DECIMAL (9,6)` | L22 |
| `dGatReal` | `DECIMAL (9,6)` | L23 |
| `dFecha` | `DATE` | L24 |
| `iTotalReg` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_reportemediainflacion_tmp` | `bdicnweb` | no | SELECT | L67 |
| `sw_reportemediainflacion_tmp` | `bdicnweb` | no | DELETE | L67 |
| `sc_medianainflacion` | `bdicheq` | ⚠️ sí | SELECT | L71 |
| `sv_gat` | `bdinvers` | ⚠️ sí | SELECT | L78 |
| `sw_reportemediainflacion_tmp` | `bdicnweb` | no | INSERT | L81 |
| `sc_gat` | `bdicheq` | ⚠️ sí | SELECT | L92 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | FÓRMULA | `LET iTotalReg = iTotalReg + 1;` |  |
| L100 | FÓRMULA | `LET iTotalReg = iTotalReg + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?teme` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `?na` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `?lacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?teme`, `?na`, `?lacion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_generarreportes_tef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generarreportes_tef.sql` |
| **LOC (1er CREATE)** | 193 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar reportes y TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_reportearchivos_tef2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_generarreportes_tef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(20)
  pTipoArchivo                 CHAR(4)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipoReporte                 SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | — | — |
| `pTipoArchivo` | `CHAR(4)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipoReporte` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cCodRetSp` | `CHAR(5)` | L29 |
| `iCodRetSp` | `INTEGER` | L30 |
| `cFechaInicio` | `CHAR(8)` | L31 |
| `cFechaFin` | `CHAR(8)` | L32 |
| `iNoRegistros` | `INTEGER` | L33 |
| `cFechaPresentacion` | `CHAR(10)` | L34 |
| `cNombreArch` | `CHAR(20)` | L35 |
| `cCodOperacion` | `CHAR(2)` | L36 |
| `cNoSucursal` | `CHAR(4)` | L37 |
| `cNombreOrd` | `CHAR(40)` | L38 |
| `cNumCtaOrd` | `CHAR(20)` | L39 |
| `cTipoOperacion` | `CHAR(50)` | L40 |
| `cRefNumerica` | `CHAR(7)` | L41 |
| `dImporte` | `DECIMAL(11,2)` | L42 |
| `cNombreRec` | `CHAR(40)` | L43 |
| `cNumCtaRec` | `CHAR(20)` | L44 |
| `cNumSecuencia` | `CHAR(7)` | L45 |
| `cTipoCtaDestino` | `CHAR(40)` | L46 |
| `cBancoDestino` | `CHAR(40)` | L47 |
| `cStatus` | `CHAR(20)` | L48 |
| `dImpOperaciones` | `DECIMAL(18,2)` | L49 |
| `cFechaPresentacion2` | `CHAR(8)` | L50 |
| `cMotivoDev` | `CHAR(2)` | L51 |
| *…5 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L140 |
| `sp_reportearchivos_tef2` | `bditef` | ⚠️ sí | L148 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L105 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoArchivo = '' OR pTipoReporte IS NULL OR pRegistros IS NU` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L117 | VALIDACIÓN_NULL | `IF pFechaInicial IS NULL OR pFechaFinal IS NULL THEN` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L156 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:"informix".sp_reportearchivos_tef` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L161 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_generarreportes_tef_mx`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_generarreportes_tef_mx.sql` |
| **LOC (1er CREATE)** | 190 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar reportes y TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_reportearchivos_tef2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_generarreportes_tef_mx(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(20)
  pTipoArchivo                 CHAR(4)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipoReporte                 SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | — | — |
| `pTipoArchivo` | `CHAR(4)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipoReporte` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cCodRetSp` | `CHAR(5)` | L29 |
| `iCodRetSp` | `INTEGER` | L30 |
| `cFechaInicio` | `CHAR(8)` | L31 |
| `cFechaFin` | `CHAR(8)` | L32 |
| `iNoRegistros` | `INTEGER` | L33 |
| `cFechaPresentacion` | `CHAR(10)` | L34 |
| `cNombreArch` | `CHAR(20)` | L35 |
| `cCodOperacion` | `CHAR(2)` | L36 |
| `cNoSucursal` | `CHAR(4)` | L37 |
| `cNombreOrd` | `CHAR(40)` | L38 |
| `cNumCtaOrd` | `CHAR(20)` | L39 |
| `cTipoOperacion` | `CHAR(50)` | L40 |
| `cRefNumerica` | `CHAR(7)` | L41 |
| `dImporte` | `DECIMAL(11,2)` | L42 |
| `cNombreRec` | `CHAR(40)` | L43 |
| `cNumCtaRec` | `CHAR(20)` | L44 |
| `cNumSecuencia` | `CHAR(7)` | L45 |
| `cTipoCtaDestino` | `CHAR(40)` | L46 |
| `cBancoDestino` | `CHAR(40)` | L47 |
| `cStatus` | `CHAR(20)` | L48 |
| `dImpOperaciones` | `DECIMAL(18,2)` | L49 |
| `cFechaPresentacion2` | `CHAR(8)` | L50 |
| `cMotivoDev` | `CHAR(2)` | L51 |
| *…5 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L137 |
| `sp_reportearchivos_tef2` | `bditef` | ⚠️ sí | L145 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L102 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoArchivo = '' OR pTipoReporte IS NULL OR pRegistros IS NU` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L114 | VALIDACIÓN_NULL | `IF pFechaInicial IS NULL OR pFechaFinal IS NULL THEN` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L153 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:"informix".sp_reportearchivos_tef` |  |
| L155 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrep_cons_bts_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_bts_aud.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=36 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte beneficiarios y auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_bts_aud` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_bts_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(5)` | L22 |
| `iCodRetSp` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |
| `dFecha` | `CHAR(10)` | L26 |
| `cHora` | `CHAR(12)` | L27 |
| `cFolio` | `CHAR(16)` | L28 |
| `cUsuario` | `CHAR(8)` | L29 |
| `cSucursal` | `CHAR(4)` | L30 |
| `cImporte` | `CHAR(17)` | L31 |
| `cTransaccion` | `CHAR(4)` | L32 |
| `cCveConfirm` | `CHAR(20)` | L33 |
| `cBeneficiario` | `CHAR(104)` | L34 |
| `cIdentificacion` | `CHAR(25)` | L35 |
| `cFolioIdentificacion` | `CHAR(25)` | L36 |
| `cFormaPago` | `CHAR(45)` | L37 |
| `cCuenta` | `CHAR(20)` | L38 |
| `cTransacSuc` | `CHAR(4)` | L39 |
| `iTotalRows` | `INTEGER` | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |
| `sp_cons_bts_aud` | `bdinteg` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_bts_aud";` |  |
| L103 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_genrep_cons_chq_dev_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_chq_dev_aud.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cheque y auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_chq_dev_aud` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_chq_dev_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(5)` | L22 |
| `iCodRetSp` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |
| `cCliente` | `CHAR(20)` | L26 |
| `cFolio` | `CHAR(16)` | L27 |
| `dFechaAlt` | `CHAR(10)` | L28 |
| `cCuenta` | `CHAR(20)` | L29 |
| `cMonto` | `CHAR(17)` | L30 |
| `cHora` | `CHAR(12)` | L31 |
| `cUsuario` | `CHAR(8)` | L32 |
| `cTransaccion` | `CHAR(4)` | L33 |
| `cSaldo` | `CHAR(17)` | L34 |
| `cSucursal` | `CHAR(4)` | L35 |
| `cBanco` | `CHAR(4)` | L36 |
| `cCtaBanco` | `CHAR(20)` | L37 |
| `cCheque` | `CHAR(11)` | L38 |
| `cTarjeta` | `CHAR(16)` | L39 |
| `iTotalRows` | `INTEGER` | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |
| `sp_cons_chq_dev_aud` | `bdinteg` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_chq_dev_aud";` |  |
| L103 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_genrep_cons_chq_propios_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_chq_propios_aud.sql` |
| **LOC (1er CREATE)** | 121 |
| **Callgraph** | ✅ fan_in=0 / fan_out=34 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cheque, ro — Rol Operativo, OS — Originación de Solicitudes y auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_chq_propios_aud` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=4 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_chq_propios_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `cCodRetSp` | `CHAR(5)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |
| `cCliente` | `CHAR(20)` | L25 |
| `cFolio` | `CHAR(16)` | L26 |
| `cFecha` | `CHAR(10)` | L27 |
| `cCuenta` | `CHAR(20)` | L28 |
| `cMonto` | `CHAR(17)` | L29 |
| `cHora` | `CHAR(12)` | L30 |
| `cUsuario` | `CHAR(8)` | L31 |
| `cTransaccion` | `CHAR(4)` | L32 |
| `cSaldo` | `CHAR(17)` | L33 |
| `cSucursal` | `CHAR (4)` | L34 |
| `cCheque` | `CHAR(11)` | L35 |
| `cTrans_Suc` | `CHAR(4)` | L36 |
| `cTarjeta` | `CHAR(16)` | L37 |
| `iTotalRows` | `INTEGER` | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L83 |
| `sp_cons_chq_propios_aud` | `bdinteg` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L97 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_chq_propios_aud";` |  |
| L100 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `?_p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?pi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_p`, `ro`, `?pi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrep_cons_chq_sbc_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_chq_sbc_aud.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=33 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cheque, saldo básico de cuenta y auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_chq_sbc_aud` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_chq_sbc_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(5)` | L22 |
| `iCodRetSp` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |
| `cCliente` | `CHAR(20)` | L26 |
| `cFolio` | `CHAR(16)` | L27 |
| `cFecha` | `CHAR(10)` | L28 |
| `cCuenta` | `CHAR(20)` | L29 |
| `cMonto` | `CHAR(17)` | L30 |
| `cHora` | `CHAR(12)` | L31 |
| `cUsuario` | `CHAR(8)` | L32 |
| `cTransaccion` | `CHAR(4)` | L33 |
| `cSaldo` | `CHAR(17)` | L34 |
| `cSucursal` | `CHAR(4)` | L35 |
| `cBanco` | `CHAR(4)` | L36 |
| `cCtaBanco` | `CHAR(20)` | L37 |
| `cCheque` | `CHAR(11)` | L38 |
| `cTarjeta` | `CHAR(16)` | L39 |
| `iTotalRows` | `INTEGER` | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |
| `sp_cons_chq_sbc_aud` | `bdinteg` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_chq_sbc_aud";` |  |
| L103 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `sbc` | ENTIDAD | saldo básico de cuenta (SBC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_genrep_cons_conc_efect_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_conc_efect_aud.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_conc_efect_aud` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_conc_efect_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |
| `cFecha` | `CHAR(10)` | L20 |
| `cHora` | `CHAR(12)` | L21 |
| `cFolio` | `CHAR(16)` | L22 |
| `cUsuario` | `CHAR(8)` | L23 |
| `cSucursal` | `CHAR(4)` | L24 |
| `cImporte` | `CHAR(17)` | L25 |
| `cTransaccion` | `CHAR(4)` | L26 |
| `cFolioPap` | `CHAR(10)` | L27 |
| `iTotalRows` | `INTEGER` | L28 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |
| `sp_cons_conc_efect_aud` | `bdinteg` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_conc_efect_aud";` |  |
| L85 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?c_efect_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?c_efect_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrep_cons_dota_efect_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_dota_efect_aud.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_dota_efect_aud` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_dota_efect_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `cFecha` | `CHAR(10)` | L21 |
| `cHora` | `CHAR(12)` | L22 |
| `cFolio` | `CHAR(16)` | L23 |
| `cUsuario` | `CHAR(8)` | L24 |
| `cSucursal` | `CHAR(4)` | L25 |
| `cImporte` | `CHAR(17)` | L26 |
| `cTransaccion` | `CHAR(4)` | L27 |
| `cFolioPap` | `CHAR(8)` | L28 |
| `cTransacSuc` | `CHAR(4)` | L29 |
| `iTotalRows` | `INTEGER` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |
| `sp_cons_dota_efect_aud` | `bdinteg` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L85 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_dota_efect_aud";` |  |
| L88 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_dota_efect_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dota_efect_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrep_cons_faltantes_caja_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_faltantes_caja_aud.sql` |
| **LOC (1er CREATE)** | 139 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte caja y auditoría (faltantes)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_faltantes_caja_aud` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_faltantes_caja_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `cFecha` | `CHAR(10)` | L21 |
| `cUsuario` | `CHAR(8)` | L22 |
| `cImporte` | `CHAR(21)` | L23 |
| `cNombre` | `CHAR(45)` | L24 |
| `cTransaccion` | `CHAR(4)` | L25 |
| `cSucursal` | `CHAR(4)` | L26 |
| `cFechaElimina` | `CHAR(10)` | L27 |
| `cSaldo` | `CHAR(21)` | L28 |
| `cFechaAsigna` | `CHAR(10)` | L29 |
| `iTotalRows` | `INTEGER` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |
| `sp_cons_faltantes_caja_aud` | `bdinteg` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni = '' OR pFechaFin = '' OR pSucursa` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_faltantes_caja_aud";` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SPL bdinteg:sp_cons_faltantes_caja_aud";` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L118 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `faltantes` | MODIF | faltantes | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_genrep_cons_orden_pago_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_orden_pago_aud.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte orden, pago y auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_orden_pago_aud` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_orden_pago_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(5)` | L22 |
| `iCodRetSp` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |
| `cFecha` | `CHAR(10)` | L26 |
| `cHora` | `CHAR(12)` | L27 |
| `cFolio` | `CHAR(16)` | L28 |
| `cUsuario` | `CHAR(8)` | L29 |
| `cSucursal` | `CHAR(4)` | L30 |
| `cImporte` | `CHAR(14)` | L31 |
| `cTransaccion` | `CHAR(4)` | L32 |
| `cNumOrden` | `CHAR(20)` | L33 |
| `cBeneficiario` | `CHAR(105)` | L34 |
| `cIdentificacion` | `CHAR(2)` | L35 |
| `cFolioIdent` | `CHAR(25)` | L36 |
| `cFormaPago` | `CHAR(15)` | L37 |
| `cCuenta` | `CHAR(12)` | L38 |
| `cTransacSuc` | `CHAR(4)` | L39 |
| `iTotalRows` | `INTEGER` | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |
| `sp_cons_orden_pago_aud` | `bdinteg` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_orden_pago_aud";` |  |
| L103 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `orden` | ENTIDAD | orden | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_genrep_cons_reversos_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_reversos_aud.sql` |
| **LOC (1er CREATE)** | 121 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_reversos_aud` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_reversos_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `cCodRetSp` | `CHAR(5)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |
| `cCliente` | `CHAR(20)` | L25 |
| `cFolio` | `CHAR(16)` | L26 |
| `cUsuario` | `CHAR(8)` | L27 |
| `cFecha` | `CHAR(10)` | L28 |
| `cHora` | `CHAR(12)` | L29 |
| `cCuenta` | `CHAR(20)` | L30 |
| `cMonto` | `CHAR(18)` | L31 |
| `cTransaccion` | `CHAR(4)` | L32 |
| `cSaldo` | `CHAR(18)` | L33 |
| `cSucursal` | `CHAR(4)` | L34 |
| `cTransSuc` | `CHAR(4)` | L35 |
| `cReferencia` | `CHAR(40)` | L36 |
| `cTarjeta` | `CHAR(20)` | L37 |
| `iTotalRows` | `INTEGER` | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L83 |
| `sp_cons_reversos_aud` | `bdinteg` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L97 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_reversos_aud";` |  |
| L100 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrep_cons_sobrantes_caja_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_sobrantes_caja_aud.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte caja y auditoría (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_sobrantes_caja_aud` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_sobrantes_caja_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pFechaRep                    CHAR(10)
  pImporte                     CHAR(21)
  pFechaEliminacion            CHAR(10)
  pNumTransaccion              CHAR(4)
  pOperador                    CHAR(8)
  pLinea                       INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pFechaRep` | `CHAR(10)` | — | — |
| `pImporte` | `CHAR(21)` | — | — |
| `pFechaEliminacion` | `CHAR(10)` | — | — |
| `pNumTransaccion` | `CHAR(4)` | — | — |
| `pOperador` | `CHAR(8)` | — | — |
| `pLinea` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `cFecha` | `CHAR(10)` | L21 |
| `cUsuario` | `CHAR(8)` | L22 |
| `cNombreUsu` | `CHAR(45)` | L23 |
| `cImporte` | `CHAR(21)` | L24 |
| `cImporte2` | `CHAR(21)` | L25 |
| `cFechaElimina` | `CHAR(10)` | L26 |
| `cTransaccion` | `CHAR(4)` | L27 |
| `cSucursal` | `CHAR(4)` | L28 |
| `cTransSuc` | `CHAR(4)` | L29 |
| `iTotalRows` | `INTEGER` | L30 |
| `cVacio` | `CHAR(4)` | L31 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |
| `sp_cons_sobrantes_caja_aud` | `bdinteg` | ⚠️ sí | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L61 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L87 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_sobrantes_caja_aud";` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_sobr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sobr`, `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrep_cons_spei_aud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrep_cons_spei_aud.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte auditoría" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_spei_aud` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrep_cons_spei_aud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        INTEGER
  pFechaIni                    CHAR(10)
  pFechaFin                    CHAR(10)
  pSucursal                    CHAR(4)
  pCodigo                      CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `INTEGER` | — | — |
| `pFechaIni` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iCodRetSp` | `INTEGER` | L20 |
| `cEmpresa` | `CHAR(3)` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |
| `cCliente` | `CHAR(20)` | L23 |
| `cFolio` | `CHAR(16)` | L24 |
| `dFecha` | `CHAR(10)` | L25 |
| `cHora` | `CHAR(12)` | L26 |
| `cCuenta` | `CHAR(20)` | L27 |
| `cMonto` | `CHAR(17)` | L28 |
| `cTransaccion` | `CHAR(4)` | L29 |
| `cSaldo` | `CHAR(17)` | L30 |
| `cSucursal` | `CHAR(4)` | L31 |
| `cTransacSuc` | `CHAR(4)` | L32 |
| `cReferencia` | `CHAR(40)` | L33 |
| `iTotalRows` | `INTEGER` | L34 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L77 |
| `sp_cons_spei_aud` | `bdinteg` | ⚠️ sí | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo IS NULL OR pFechaIni  = '' OR pFechaFin  = '' OR pSucur` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L91 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_cons_spei_aud";` |  |
| L94 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `spei` | PREFIJO | familia SPEI (pagos interbancarios) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_genreparqueosucucaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreparqueosucucaja.sql` |
| **LOC (1er CREATE)** | 190 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte OS — Originación de Solicitudes y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreparqueosucucaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(2000)` | L7 |
| `cSql` | `CHAR(2500)` | L8 |
| `cRutaInformix` | `CHAR(100)` | L9 |
| `cNombreRepXls` | `CHAR(45)` | L10 |
| `cRutaGralXls` | `CHAR(150)` | L11 |
| `dFechaHoy` | `DATE` | L12 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L13 |
| `dHoy` | `DATE` | L14 |
| `cNombreReporteHist` | `CHAR(100)` | L15 |
| `ven_transacc` | `SMALLINT` | L16 |
| `bInTransaction` | `BOOLEAN` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesarqueos` | `bdicnweb` | no | SELECT | L84 |
| `systables` | `bdicnweb` | no | SELECT | L102 |
| `sw_cg_arqueosucajatmp` | `bdicnweb` | no | SELECT | L104 |
| `sw_ctrlgenreportesarqueos` | `bdicnweb` | no | INSERT | L170 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L100 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM (SELECT 'FECHA',";` |  |
| L102 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L103 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(fecha),2,0)\|\|'/'\|\|LPAD(MONTH(fecha),2,0)\|\|'/` |  |
| L130 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L135 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L144 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L153 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?arque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ucu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?arque`, `?ucu` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrepcredbloqfall`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrepcredbloqfall.sql` |
| **LOC (1er CREATE)** | 216 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 135 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte crédito y fal — fallo/disputa" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrepcredbloqfall(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cQuery` | `CHAR(255)` | L12 |
| `cCmd1` | `CHAR(2000)` | L13 |
| `cSql` | `CHAR(2500)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cRutaInformix` | `CHAR(100)` | L16 |
| `cUsrBin` | `CHAR(100)` | L17 |
| `cNombreRepXls` | `CHAR(45)` | L18 |
| `cNombreRepTxt` | `CHAR(45)` | L19 |
| `cRutaGralXls` | `CHAR(150)` | L20 |
| `cRutaGralTxt` | `CHAR(150)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L73 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L73 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L74 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT TO_CHAR(fecha_bloqueo, '%d/%m/%Y'),''''\|\|num_cliente,''''\` |  |
| L102 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L107 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L116 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L125 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L164 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |
| L169 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralTxt);` |  |
| L178 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralTxt)\|\|".tmp > "\|\|TRIM(cRutaGralTxt);` |  |
| L187 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrepcuentasdormidas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrepcuentasdormidas.sql` |
| **LOC (1er CREATE)** | 208 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cuentas (cuentas dormidas)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrepcuentasdormidas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfechaDe                     DATE
  pfechaHasta                  DATE
  pRutaDescarga                CHAR(50)
  pTipo                        CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfechaDe` | `DATE` | — | — |
| `pfechaHasta` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(50)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cNumCte` | `CHAR(20)` | L9 |
| `cSucursal` | `CHAR(4)` | L10 |
| `cCuenta` | `CHAR(20)` | L11 |
| `dFecha` | `DATE` | L12 |
| `cApMat` | `CHAR(26)` | L13 |
| `cApPat` | `CHAR(26)` | L14 |
| `cNombre1` | `CHAR(26)` | L15 |
| `cNombre2` | `CHAR(26)` | L16 |
| `dFechaAlta` | `DATE` | L17 |
| `mImporte` | `MONEY(10,2)` | L18 |
| `cQuery` | `CHAR(255)` | L19 |
| `cCmd1` | `CHAR(2000)` | L20 |
| `cSql` | `CHAR(2500)` | L21 |
| `cRutaGral` | `CHAR(150)` | L22 |
| `cRutaInformix` | `CHAR(100)` | L23 |
| `cUsrBin` | `CHAR(100)` | L24 |
| `cNombreRepXls` | `CHAR(45)` | L25 |
| `cNombreRepTxt` | `CHAR(45)` | L26 |
| `cRutaGralXls` | `CHAR(150)` | L27 |
| `cRutaGralTxt` | `CHAR(150)` | L28 |
| `dFechaHoy` | `DATE` | L29 |
| *…7 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_histbloq` | `bdicheq` | ⚠️ sí | SELECT | L108 |
| `systables` | `bdicnweb` | no | SELECT | L121 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L118 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT * FROM( ";` |  |
| L121 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L122 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT b.num_cte::CHAR(20),a.cuenta::CHAR(20), TRIM (c.apell_patern` | 🔴 MONEY/aritmética financiera |
| L123 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"TO_CHAR(d.fecha_alta, '%d/%m/%Y')";` |  |
| L154 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L159 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L168 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L177 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L193 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dormidas` | MODIF | cuentas dormidas (inactivas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_genreporte30dmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreporte30dmi.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreporte30dmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNomArchivo                  CHAR(20)
  pTipoProceso                 INTEGER
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNomArchivo` | `CHAR(20)` | — | — |
| `pTipoProceso` | `INTEGER` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iCodRetSp` | `INTEGER` | L20 |
| `cDescError` | `CHAR(130)` | L21 |
| `cFechPresentacion` | `CHAR(10)` | L22 |
| `cNomArchivo` | `CHAR(20)` | L23 |
| `cNomOrdenante` | `CHAR(40)` | L24 |
| `cServicio` | `CHAR(60)` | L25 |
| `cReferencia` | `CHAR(7)` | L26 |
| `mImporte` | `MONEY(18,2)` | L27 |
| `cCtaDest` | `CHAR(20)` | L28 |
| `cSec` | `CHAR(7)` | L29 |
| `cTipoCta` | `CHAR(20)` | L30 |
| `cBancDest` | `CHAR(20)` | L31 |
| `cStatus` | `CHAR(20)` | L32 |
| `cCausaRech` | `CHAR(60)` | L33 |
| `cCodResp` | `CHAR(2)` | L34 |
| `iNoRegistros` | `INTEGER` | L35 |
| `iRegistros` | `INTEGER` | L36 |
| `iRecuperacion` | `INTEGER` | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L81 |
| `bdidomi` | `bdicnweb` | no | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L94 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidomi:sp_domi_genrep102';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L113 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte30dmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte30dmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportealtaoptef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportealtaoptef.sql` |
| **LOC (1er CREATE)** | 158 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_tef_obtinforpt` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportealtaoptef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveRastreo                CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveRastreo` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `cCodRetSp` | `CHAR(6)` | L24 |
| `cCodRetSp2` | `CHAR(5)` | L25 |
| `cDescCodRet` | `CHAR(100)` | L26 |
| `iCodRetSp` | `INTEGER` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cEmpresa` | `CHAR(3)` | L29 |
| `dFechaTrans` | `DATE` | L30 |
| `cClaveRastreo` | `CHAR(30)` | L31 |
| `cImporteTef` | `CHAR(10)` | L32 |
| `dFechaProgramacion` | `DATE` | L33 |
| `cNombreUsuario` | `CHAR(45)` | L34 |
| `cFolioSuc` | `CHAR(16)` | L35 |
| `cNombreCteOrd` | `CHAR(30)` | L36 |
| `cNumCteOrd` | `CHAR(20)` | L37 |
| `cNumCtaOrd` | `CHAR(20)` | L38 |
| `cTipoCtaOrdDesc` | `CHAR(30)` | L40 |
| `cComisionTef` | `CHAR(5)` | L41 |
| `cIvaTef` | `CHAR(5)` | L42 |
| `cNombreBen` | `CHAR(30)` | L43 |
| `cTipoCtaBenDes` | `CHAR(30)` | L45 |
| `cNumCtaTarjBen` | `CHAR(20)` | L46 |
| `cRfcBen` | `CHAR(15)` | L47 |
| `cConceptoPago` | `CHAR(50)` | L48 |
| `cRefNum` | `CHAR(7)` | L49 |
| *…2 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L105 |
| `sp_tef_obtinforpt` | `bditef` | ⚠️ sí | L115 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_tef_obtinforpt';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L135 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?op` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?op` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportecargosdmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportecargosdmi.sql` |
| **LOC (1er CREATE)** | 147 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cargo y sd — saldo disponible" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportecargosdmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                CHAR(1)
  pNumCliente                  CHAR(20)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iCodRetSp` | `INTEGER` | L20 |
| `dFechaCargo` | `DATE` | L21 |
| `cNoCuenta` | `CHAR(20)` | L22 |
| `mImporte` | `MONEY(16,2)` | L23 |
| `cReferencia` | `CHAR(40)` | L24 |
| `cBancoRecPres` | `CHAR(41)` | L25 |
| `cEstatus` | `CHAR(20)` | L26 |
| `cDescripcionEstatus` | `CHAR(20)` | L27 |
| `cCausaRechazo` | `CHAR(20)` | L28 |
| `cDescripcionRechazo` | `CHAR(60)` | L29 |
| `dFechaInicio` | `DATE` | L30 |
| `dFechaFin` | `DATE` | L31 |
| `iExiste` | `INTEGER` | L32 |
| `iRecuperacion` | `INTEGER` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | DELETE | L90 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L102 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L78 |
| `bdidomi` | `bdicnweb` | no | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L64 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoConsulta = '' OR pNumCliente = '' OR pFechaInicio  = '' ` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L108 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdidomi:sp_domi_reportecargoscl` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00525';` |  |
| L123 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00698';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?mi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?mi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportecargosdmi_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportecargosdmi_totales.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cargo y sd — saldo disponible (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportecargosdmi_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoConsulta                CHAR(1)
  pNumCliente                  CHAR(20)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `dFechaCargo` | `DATE` | L11 |
| `cNoCuenta` | `CHAR(20)` | L12 |
| `mImporte` | `MONEY(16,2)` | L13 |
| `cReferencia` | `CHAR(40)` | L14 |
| `cBancoRecPres` | `CHAR(41)` | L15 |
| `cEstatus` | `CHAR(20)` | L16 |
| `cDescripcionEstatus` | `CHAR(20)` | L17 |
| `cCausaRechazo` | `CHAR(20)` | L18 |
| `cDescripcionRechazo` | `CHAR(60)` | L19 |
| `dFechaInicio` | `DATE` | L20 |
| `dFechaFin` | `DATE` | L21 |
| `iExiste` | `INTEGER` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | DELETE | L72 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L84 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |
| `bdidomi` | `bdicnweb` | no | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdidomi:sp_domi_reportecargoscl` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00525';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00698';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?mi_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?mi_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportecedulasccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportecedulasccl.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cédulas y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportecedulasccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaCcl                    DATE
  pTipo                        SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaCcl` | `DATE` | `ccl`=módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (bdicnweb:sp_ccl_*) | 🟡 INFERIDO |
| `pTipo` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cNombre` | `CHAR(40)` | L14 |
| `cCtaContable` | `CHAR(14)` | L15 |
| `dSdoCheques` | `DECIMAL(16,2)` | L16 |
| `dSdoContab` | `DECIMAL(16,2)` | L17 |
| `dDifSaldos` | `DECIMAL(16,2)` | L18 |
| `cObservaciones` | `CHAR(255)` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |
| `bdicnweb` | `bdicnweb` | no | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaCcl = '' OR pTipo IS NULL  THEN` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_imprimecedulas ";` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L67 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cedulas` | ENTIDAD | cédulas | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreporteconccreditoccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreporteconccreditoccl.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte cc — cuenta corriente y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rconcilsdoscred2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=4 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreporteconccreditoccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(3)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cIdProducto` | `CHAR(25)` | L15 |
| `cConcepto` | `CHAR(40)` | L16 |
| `cNivelContable` | `CHAR(14)` | L17 |
| `mSaldoOperativo` | `MONEY(18,2)` | L18 |
| `mSaldoContable` | `MONEY(18,2)` | L19 |
| `mSaldoDiferente` | `MONEY(18,2)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |
| `iRegistros` | `INTEGER` | L22 |
| `iRecuperacion` | `INTEGER` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |
| `sp_rconcilsdoscred2` | `bdicred` | ⚠️ sí | L69 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L74 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicred:sp_rconcilsdoscred2';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00105';` |  |
| L80 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?redito` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?redito` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreporteconciliacontabledmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreporteconciliacontabledmi.sql` |
| **LOC (1er CREATE)** | 130 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreporteconciliacontabledmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNomArchivo                  CHAR(20)
  pTipoProceso                 INTEGER
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNomArchivo` | `CHAR(20)` | — | — |
| `pTipoProceso` | `INTEGER` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `cCodRetSp` | `CHAR(5)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `dFechaPresentacion` | `DATE` | L23 |
| `iRegArchOriginal` | `INTEGER` | L24 |
| `mSumArchOriginal` | `MONEY(18,2)` | L25 |
| `iRegArchResp1` | `INTEGER` | L26 |
| `mSumArchResp1` | `MONEY(18,2)` | L27 |
| `iRegArchResp2` | `INTEGER` | L28 |
| `mSumArchResp2` | `MONEY(18,2)` | L29 |
| `cCuentaContableCargo` | `CHAR(14)` | L30 |
| `mSumCuentaContableCargo` | `MONEY(18,2)` | L31 |
| `cCuentaContableDeudora` | `CHAR(14)` | L32 |
| `mSumCuentaContableDeudora` | `MONEY(18,2)` | L33 |
| `cCuentaContableAbono` | `CHAR(14)` | L34 |
| `mSumCuentaContableAbono` | `MONEY(18,2)` | L35 |
| `iRecuperacion` | `INTEGER` | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L83 |
| `bdidomi` | `bdicnweb` | no | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L97 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdidomi:sp_domi_conciliacontabl` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L106 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `concilia` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `?abledmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?abledmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreporteconcreditomovccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreporteconcreditomovccl.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte crédito, movimiento y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rconcilsdoscredmov2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreporteconcreditomovccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `iCodRetSp` | `INTEGER` | L17 |
| `cIdProducto` | `CHAR(25)` | L18 |
| `cDescripcion` | `CHAR(40)` | L19 |
| `cNivelContable` | `CHAR(14)` | L20 |
| `mAbonoOperativo` | `MONEY(18,2)` | L21 |
| `mCargoOperativo` | `MONEY(18,2)` | L22 |
| `mAbonoConta` | `MONEY(18,2)` | L23 |
| `mCargoConta` | `MONEY(18,2)` | L24 |
| `mAbonosDif` | `MONEY(18,2)` | L25 |
| `mCargosDif` | `MONEY(18,2)` | L26 |
| `iRegistros` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |
| `sp_rconcilsdoscredmov2` | `bdicred` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicred:sp_rconcilsdoscredmov2';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00105';` |  |
| L85 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreporteconsultatrimestralmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreporteconsultatrimestralmc.sql` |
| **LOC (1er CREATE)** | 309 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 282 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte mes y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreporteconsultatrimestralmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(2)
  pNumProducto                 SMALLINT
  pTrimestre                   SMALLINT
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pIdPoducto                   CHAR (3)
  pTipoCompras                 CHAR (60)
  pTipoTransaccion             CHAR (60)
  pTipoDevolucion              CHAR(60)
  pTipoCuenta                  CHAR (30)
  pIdTipo                      CHAR (1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(2)` | — | — |
| `pNumProducto` | `SMALLINT` | — | — |
| `pTrimestre` | `SMALLINT` | `mes`=mes | 🔵 CONVENCIÓN |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdPoducto` | `CHAR (3)` | — | — |
| `pTipoCompras` | `CHAR (60)` | — | — |
| `pTipoTransaccion` | `CHAR (60)` | — | — |
| `pTipoDevolucion` | `CHAR(60)` | — | — |
| `pTipoCuenta` | `CHAR (30)` | — | — |
| `pIdTipo` | `CHAR (1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `sIdReporte` | `SMALLINT` | L20 |
| `iTotalCompras` | `INTEGER` | L21 |
| `mMontoCompras` | `MONEY (18,2)` | L22 |
| `iTotalTransacciones` | `INTEGER` | L23 |
| `mMontoTransacciones` | `MONEY (18,2)` | L24 |
| `iTotalDevolucion` | `INTEGER` | L25 |
| `mMontoDevolucion` | `MONEY (18,2)` | L26 |
| `iTotalCuenta` | `INTEGER` | L27 |
| `cTotalCmprs` | `CHAR(16)` | L28 |
| `cTotalTransac` | `CHAR(16)` | L29 |
| `cTotalDev` | `CHAR(16)` | L30 |
| `cTotalCta` | `CHAR(16)` | L31 |
| `cTotalCta_Ab` | `CHAR(16)` | L32 |
| `cTotalCta_Cr` | `CHAR(16)` | L33 |
| `iNoRegistros` | `INTEGER` | L34 |
| `iSuma_cuentas_A` | `INTEGER` | L35 |
| `iSuma_cuentas_B` | `INTEGER` | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L68 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pBandera = '' OR pNumProducto IS NULL OR pTrimestre IS NULL O` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L103 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L117 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L129 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L141 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L146 | VALIDACIÓN_NULL | `IF pFechaInicio IS  NULL OR pFechaFin IS  NULL THEN` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L170 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L182 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L193 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L216 | FÓRMULA | `LET cTotalCta=iSuma_cuentas_A - iSuma_cuentas_B;` |  |
| L217 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L233 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L244 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L256 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L268 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L280 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L291 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L297 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tri` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mes` | ENTIDAD | mes | 🔵 CONVENCIÓN | nombre_sp |
| `?tral` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?tri`, `?tral` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportedatoserviciosdmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportedatoserviciosdmi.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte datos y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportedatoserviciosdmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pServicio                    CHAR(18)
  pSucursal                    CHAR(4)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pServicio` | `CHAR(18)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cErrorInfo` | `CHAR(80)` | L17 |
| `cFechaAplica` | `CHAR(10)` | L18 |
| `cNumCte` | `CHAR(20)` | L19 |
| `cNomCte` | `CHAR(80)` | L20 |
| `cCuenta` | `CHAR(20)` | L21 |
| `cServicio` | `CHAR(60)` | L22 |
| `cSucursal` | `CHAR(4)` | L23 |
| `cEstatus` | `CHAR(20)` | L24 |
| `mMontoMaximo` | `MONEY(18,2)` | L25 |
| `sNumCve` | `SMALLINT` | L26 |
| `iNoRegistros` | `INTEGER` | L27 |
| `iRegistros` | `INTEGER` | L28 |
| `iRecuperacion` | `INTEGER` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L72 |
| `bdidomi` | `bdicnweb` | no | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pServicio = '' OR pSucursal	= '' OR pRegistros IS NULL OR pRe` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L85 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidomi:sp_domi_genrepservicios';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00091';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L92 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ervici` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?dmi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?ervici`, `?dmi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportedetallesucursales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportedetallesucursales.sql` |
| **LOC (1er CREATE)** | 200 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte detalle y sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportedetallesucursales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pFechaInicio                 DATE
  pFechaFinal                  DATE
  pTransaccion1                CHAR(4)
  pTransaccion2                CHAR(4)
  pSucursal                    CHAR(4)
  pCategoria                   CHAR(2)
  pConvenio                    CHAR(5)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTransaccion1` | `CHAR(4)` | — | — |
| `pTransaccion2` | `CHAR(4)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pCategoria` | `CHAR(2)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `cSucursal` | `CHAR(44)` | L14 |
| `cCajaGeneral` | `CHAR(45)` | L15 |
| `cEntidad` | `CHAR(30)` | L16 |
| `iTotalOperacion` | `INTEGER` | L17 |
| `mMonto` | `MONEY(14,2)` | L18 |
| `iTotalOperacion1` | `INTEGER` | L19 |
| `mMonto1` | `MONEY(14,2)` | L20 |
| `cCodRet2` | `CHAR(5)` | L22 |
| `vIdptf` | `VARCHAR(5)` | L23 |
| `vTipos` | `VARCHAR(1)` | L24 |
| `cClavesit` | `CHAR(3)` | L25 |
| `fechasit` | `DATE` | L26 |
| `vCalles` | `VARCHAR(100)` | L27 |
| `vNumext` | `VARCHAR(6)` | L28 |
| `vNumint` | `VARCHAR(5)` | L29 |
| `cCvecol` | `CHAR(8)` | L30 |
| `vColonias` | `VARCHAR(100)` | L31 |
| `cCvemun` | `CHAR(5)` | L32 |
| `VMunicipio` | `VARCHAR(60)` | L33 |
| `cCvelocalidades` | `CHAR(14)` | L34 |
| *…9 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cg_generacionreportedetallessuc` | `bdicnweb` | no | SELECT | L120 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L139 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L146 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L103 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L91 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pBandera = '' OR  pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L111 | VALIDACIÓN_NULL | `IF pFechaInicio IS NULL OR pFechaFinal IS NULL OR  pTransaccion1 = '' OR pTransaccion2 = '' THEN` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L128 | VALIDACIÓN_NULL | `IF pFechaInicio IS NULL OR pFechaFinal IS NULL OR  pTransaccion1 = '' OR pTransaccion2 = '' THEN` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L162 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportepagdiariosafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportepagdiariosafore.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte AFORE (diarios)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_aforegenerarreportedetallepagosdiarios2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportepagdiariosafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `cCodRetSp` | `CHAR(6)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cCLABE` | `CHAR(18)` | L23 |
| `cTipoCuenta` | `CHAR(40)` | L24 |
| `cNombreCliente` | `CHAR(120)` | L25 |
| `mImporte` | `MONEY(17,2)` | L26 |
| `mComision` | `MONEY(17,2)` | L27 |
| `dIVA` | `DECIMAL(18,2)` | L28 |
| `cEstadosPago` | `CHAR(30)` | L29 |
| `cEstado` | `CHAR(2)` | L30 |
| `dFechaHoy` | `DATE` | L31 |
| `iNumTotalPagados` | `INTEGER` | L32 |
| `mImpTotalPagados` | `MONEY(17,2)` | L33 |
| `iNumTotalNoPagados` | `INTEGER` | L34 |
| `mImpTotalNoPagados` | `MONEY(17,2)` | L35 |
| `cTipoArchivo` | `CHAR(1)` | L36 |
| `iRecuperacion` | `INTEGER` | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L77 |
| `sp_aforegenerarreportedetallepagosdiarios2` | `bdiprog` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet 		       = '00000';` |  |
| L70 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta IS NULL THEN` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdiprog:sp_aforegenerarreported` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00491';` |  |
| L96 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?ortepag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `diarios` | MODIF | diarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ortepag` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreporteresumpagafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreporteresumpagafore.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte AFORE" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_aforegenerarreporteresumpagproc2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreporteresumpagafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `dFechaHoy` | `DATE` | L15 |
| `dFechaPago` | `DATE` | L16 |
| `iNumOperacion` | `INTEGER` | L17 |
| `dComision` | `DECIMAL(16, 2)` | L18 |
| `dTotalIva` | `DECIMAL(16, 2)` | L19 |
| `dTotal` | `DECIMAL(16, 2)` | L20 |
| `cTipoArchivo` | `CHAR(1)` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |
| `sp_aforegenerarreporteresumpagproc2` | `bdiprog` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet 		    = '00000';` |  |
| L45 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta IS NULL THEN` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdiprog:sp_aforegenerarreporter` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orteresumpag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orteresumpag` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreporteserviciosdmi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreporteserviciosdmi.sql` |
| **LOC (1er CREATE)** | 131 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte servicio y sd — saldo disponible" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreporteserviciosdmi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `cCodRetSp` | `CHAR(5)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cRfc` | `CHAR (18)` | L23 |
| `cRazonSocial` | `CHAR (60)` | L24 |
| `cNombreCorto` | `CHAR (20)` | L25 |
| `cConvenio` | `CHAR (1)` | L26 |
| `cCveCanal` | `CHAR (2)` | L27 |
| `cPresentador` | `CHAR (1)` | L28 |
| `cNumCte` | `CHAR (20)` | L29 |
| `cNumReintentos` | `CHAR (20)` | L30 |
| `cComision` | `CHAR (20)` | L31 |
| `cComisionDev` | `CHAR (20)` | L32 |
| `cCuentaCargocomision` | `CHAR (20)` | L33 |
| `cLayoutEspecial` | `CHAR (1)` | L34 |
| `cCodGrupoAct` | `CHAR (4)` | L35 |
| `cCodGrupoDes` | `CHAR (4)` | L36 |
| `cCodGrupoReact` | `CHAR (4)` | L37 |
| `iNoRegistros` | `INTEGER` | L38 |
| `iRegistros` | `INTEGER` | L39 |
| `iRecuperacion` | `INTEGER` | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L87 |
| `bdidomi` | `bdicnweb` | no | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L103 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdidomi:sp_domi_consultaservicios2 ';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `servicio` | ENTIDAD | servicio | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?mi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?mi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportesmesacontrol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportesmesacontrol.sql` |
| **LOC (1er CREATE)** | 504 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte Mesa de Control — equipo de revisión y autorización de solicitudes de crédito; status codes MC/CM; valida comprobantes de ingreso; comentario explícito en código y rol" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportesmesacontrol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdReporte                   CHAR(20)
  pProducto                    CHAR(4)
  pDescProducto                CHAR(40)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pStatusInicial               CHAR(2)
  pStatusFinal                 CHAR(2)
  pArea                        CHAR(2)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdReporte` | `CHAR(20)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pDescProducto` | `CHAR(40)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pStatusInicial` | `CHAR(2)` | — | — |
| `pStatusFinal` | `CHAR(2)` | — | — |
| `pArea` | `CHAR(2)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `cRutaInformix` | `CHAR(100)` | L13 |
| `cRutaGral` | `CHAR(150)` | L14 |
| `cNombreReporte` | `CHAR(100)` | L15 |
| `cNombreReporteHist` | `CHAR(100)` | L16 |
| `cFechaHoraArchivo` | `CHAR(15)` | L17 |
| `dFechaHoy` | `DATE` | L18 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L19 |
| `bInTransaction` | `BOOLEAN` | L20 |
| `ven_transacc` | `SMALLINT` | L21 |
| `dHoy` | `DATETIME YEAR TO FRACTION(3)` | L22 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L25 |
| `cStr7` | `CHAR(60)` | L26 |
| `cStr8` | `CHAR(60)` | L27 |
| `iRegistros` | `INTEGER` | L28 |
| `iCountRep` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |
| `cMensaje` | `CHAR(80)` | L32 |
| `cFecha` | `CHAR(10)` | L33 |
| *…72 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_consultaoperacionesmc` | `bdicnweb` | no | SELECT | L290 |
| `sw_consultaoperacionesmc` | `bdicnweb` | no | DELETE | L290 |
| `sw_consultaoperacionesmc` | `bdicnweb` | no | INSERT | L311 |
| `systables` | `bdicnweb` | no | SELECT | L324 |
| `sw_consultaporanalistamc` | `bdicnweb` | no | SELECT | L347 |
| `sw_consultaporanalistamc` | `bdicnweb` | no | DELETE | L347 |
| `sw_consultaporanalistamc` | `bdicnweb` | no | INSERT | L361 |
| `sw_ctrlgenreportesmc` | `bdicnweb` | no | SELECT | L421 |
| `sw_ctrlgenreportesmc` | `bdicnweb` | no | DELETE | L430 |
| `sw_ctrlgenreportesmc` | `bdicnweb` | no | INSERT | L445 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L272 |
| `sp_consultaoperacionesmc` | `bdicred` | ⚠️ sí | L294 |
| `sp_consultaporanalistamc` | `bdicred` | ⚠️ sí | L351 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L454 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L250 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdReporte = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL ` |  |
| L252 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L259 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L265 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L301 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicred:"informix".sp_consultaoperacione` |  |
| L303 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L306 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |
| L310 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L325 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT fecha, solicitudes_mc::CHAR(11), n` |  |
| L335 | FÓRMULA | `LET cCmd1 = "SELECT 'SOLICITUD','CLIENTE','NOMBRE','SUCURSAL','FECHA SOLICITUD','ESTATUS INICIAL','M` |  |
| L337 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT ''''\|\|num_solicitud, ''''\|\|num` |  |
| L338 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" NVL(TO_CHAR(fecha_sol, '%d/%m/%Y'), ''), sta_inicial, motivo, NVL(` |  |
| L357 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicred:"informix".sp_consultaporanalist` |  |
| L360 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L373 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT ejecutivo, nombre, no_analizadas::` |  |
| L384 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"'VENCIDO COPPEL','VENCIDO COPPEL (UDIS)','PUNTUALIDAD COPPEL','BC S` |  |
| L385 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT ''''\|\|num_solicitud, sucursal, n` |  |
| L397 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L442 | FÓRMULA | `LET iCountRep = iCountRep + 1;` |  |
| L484 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdimnsj:sp_registra_evento';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?ortes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mesa` | ENTIDAD | Mesa de Control — equipo de revisión y autorización de solic | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `rol` | ENTIDAD | rol / perfil | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ortes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportessolicitudessupmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportessolicitudessupmc.sql` |
| **LOC (1er CREATE)** | 376 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte ss — subsistema y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportessolicitudessupmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pStatus                      CHAR(2)
  pProducto                    CHAR(4)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDesCodRetSp` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCmd1` | `CHAR(2000)` | L12 |
| `cSql` | `CHAR(2500)` | L13 |
| `cRutaInformix` | `CHAR(100)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cNombreReporte` | `CHAR(100)` | L16 |
| `cNombreReporteHist` | `CHAR(100)` | L17 |
| `cFechaHoraArchivo` | `CHAR(15)` | L18 |
| `dFechaHoy` | `DATE` | L19 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L20 |
| `bInTransaction` | `BOOLEAN` | L21 |
| `ven_transacc` | `SMALLINT` | L22 |
| `dHoy` | `DATETIME YEAR TO FRACTION(3)` | L23 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L26 |
| `cStr7` | `CHAR(60)` | L27 |
| `cStr8` | `CHAR(60)` | L28 |
| `iRegistros` | `INTEGER` | L29 |
| `iCountRep` | `INTEGER` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |
| `iNumRegistros` | `INTEGER` | L32 |
| *…8 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_consdetallereportesolsupmc` | `bdicnweb` | no | SELECT | L124 |
| `sw_consdetallereportesolsupmc` | `bdicnweb` | no | DELETE | L124 |
| `ss_solsuperv_paso` | `bdisolic` | ⚠️ sí | SELECT | L131 |
| `ss_solsuperv_paso` | `bdisolic` | ⚠️ sí | DELETE | L134 |
| `sw_consdetallereportesolsupmc` | `bdicnweb` | no | INSERT | L161 |
| `systables` | `bdicnweb` | no | SELECT | L215 |
| `sw_ctrlgenreportesolsupmc` | `bdicnweb` | no | SELECT | L296 |
| `sw_ctrlgenreportesolsupmc` | `bdicnweb` | no | DELETE | L305 |
| `sw_ctrlgenreportesolsupmc` | `bdicnweb` | no | INSERT | L320 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L108 |
| `sp_busca_sol_supervision` | `bdisolic` | ⚠️ sí | L141 |
| `sp_consulta_supervision_mc_totales` | `bdicred` | ⚠️ sí | L170 |
| `sp_consulta_supervision_mc` | `bdicred` | ⚠️ sí | L184 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L329 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L145 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃ?N DEL SP bdisolic:"informix".sp_busca_s` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L150 | CÓDIGO_RETORNO | `LET cCodRet = '00787';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00788';` |  |
| L156 | CÓDIGO_RETORNO | `LET cCodRet = '00789';` |  |
| L160 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L174 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃ?N DEL SP bdicred:"informix".sp_consulta` |  |
| L176 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L179 | CÓDIGO_RETORNO | `LET cCodRet = '00021';` |  |
| L189 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃ?N DEL SP bdicred:"informix".sp_consulta` |  |
| L191 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L194 | CÓDIGO_RETORNO | `LET cCodRet = '00021';` |  |
| L198 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L216 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT num_solicitud, num_cliente, nombre` |  |
| L224 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L253 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L258 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L267 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L276 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L317 | FÓRMULA | `LET iCountRep = iCountRep + 1;` |  |
| L359 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdimnsj:sp_registra_evento';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?olicitude` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?up` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?olicitude`, `?up` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genreportexlsdepositoscoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genreportexlsdepositoscoppel.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte sd — saldo disponible, punto de venta, OS — Originación de Solicitudes y Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=4 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_genreportexlsdepositoscoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cCmd1` | `CHAR(2000)` | L10 |
| `cSql` | `CHAR(2500)` | L11 |
| `cRutaInformix` | `CHAR(100)` | L12 |
| `cRutaGral` | `CHAR(150)` | L13 |
| `cNombreReporte` | `CHAR(100)` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `ven_transacc` | `SMALLINT` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET cCmd1 = "SELECT 'FOLIO','FECHA','FOLIO COMPROBANTE','NO. SUC / ATM','NO. CAJA GENERAL','PLAZA','` | 🔴 MONEY/aritmética financiera |
| L64 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT folio_oper, NVL(TO_CHAR(fecha, '%d/%m/%Y'), ''), ` |  |
| L68 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pos` | ENTIDAD | punto de venta (POS) | 🔵 CONVENCIÓN | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?l`, `?e`, `?it` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_genrepstatussol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrepstatussol.sql` |
| **LOC (1er CREATE)** | 213 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 134 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte estatus y solicitud" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrepstatussol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cQuery` | `CHAR(255)` | L12 |
| `cCmd1` | `CHAR(2000)` | L13 |
| `cSql` | `CHAR(2500)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cRutaInformix` | `CHAR(100)` | L16 |
| `cUsrBin` | `CHAR(100)` | L17 |
| `cNombreRepXls` | `CHAR(45)` | L18 |
| `cNombreRepTxt` | `CHAR(45)` | L19 |
| `cRutaGralXls` | `CHAR(150)` | L20 |
| `cRutaGralTxt` | `CHAR(150)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L73 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L73 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L75 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"TO_CHAR(fecha_alta, '%d/%m/%Y'),TO_CHAR(fecha_cambio_status, '%d/%m` |  |
| L102 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L107 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L116 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L125 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L164 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |
| L169 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralTxt);` |  |
| L178 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralTxt)\|\|".tmp > "\|\|TRIM(cRutaGralTxt);` |  |
| L187 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_genrepstatussolaud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrepstatussolaud.sql` |
| **LOC (1er CREATE)** | 213 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 133 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte estatus, solicitud y auditoría" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrepstatussolaud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cQuery` | `CHAR(255)` | L12 |
| `cCmd1` | `CHAR(2000)` | L13 |
| `cSql` | `CHAR(2500)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cRutaInformix` | `CHAR(100)` | L16 |
| `cUsrBin` | `CHAR(100)` | L17 |
| `cNombreRepXls` | `CHAR(45)` | L18 |
| `cNombreRepTxt` | `CHAR(45)` | L19 |
| `cRutaGralXls` | `CHAR(150)` | L20 |
| `cRutaGralTxt` | `CHAR(150)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L73 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L73 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L74 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT ''''\|\|num_solicitud,''''\|\|num_cliente,TO_CHAR(fecha_alta` | 🔴 MONEY/aritmética financiera |
| L75 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"TO_CHAR(fecha_cambio_status, '%d/%m/%Y'),nom_promotor,tel_particula` |  |
| L102 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L107 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L116 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L125 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L164 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |
| L169 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralTxt);` |  |
| L178 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralTxt)\|\|".tmp > "\|\|TRIM(cRutaGralTxt);` |  |
| L187 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |

---

## `sp_genrepstatussolaudexcel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_genrepstatussolaudexcel.sql` |
| **LOC (1er CREATE)** | 207 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 132 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte estatus, solicitud, auditoría y celular" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_genrepstatussolaudexcel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(80)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNum_solicitud` | `CHAR(20)` | L11 |
| `cNum_cliente` | `CHAR(20)` | L12 |
| `dFecha_alta` | `DATE` | L13 |
| `cNom_cliente` | `CHAR(104)` | L14 |
| `cStatus_solicitud` | `CHAR(2)` | L15 |
| `mMonto_solicitud` | `MONEY(14,2)` | L16 |
| `mMonto_otorgado` | `MONEY(14,2)` | L17 |
| `dFecha_cambio_status` | `DATE` | L18 |
| `cNom_promotor_realizo` | `CHAR(45)` | L19 |
| `cNom_promotor_autorizo` | `CHAR(45)` | L20 |
| `cNom_promotor_entrego` | `CHAR(45)` | L21 |
| `cNom_promotor_asigno` | `CHAR(45)` | L22 |
| `cTelefono_particular` | `CHAR(13)` | L23 |
| `cTelefono_celular` | `CHAR(13)` | L24 |
| `cTelefono_oficina` | `CHAR(13)` | L25 |
| `cSucursal` | `CHAR(4)` | L26 |
| `cNom_referencia1` | `CHAR(104)` | L27 |
| `cTel_ref1_particular` | `CHAR(13)` | L28 |
| `cTel_ref1_celular` | `CHAR(13)` | L29 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L122 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L114 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L122 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L124 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L128 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L129 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT ''''\|\|num_solicitud,''''\|\|num_cliente,TO_CHAR(fecha_alta` | 🔴 MONEY/aritmética financiera |
| L130 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"TO_CHAR(fecha_cambio_status, '%d/%m/%Y'),nom_promotor_realizo,nom_p` |  |
| L158 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L163 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L172 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L181 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sol` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aud` | ENTIDAD | auditoría | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabaoperaciontef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_grabaoperaciontef.sql` |
| **LOC (1er CREATE)** | 348 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_tef_grabaoperacion`, `reversion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_grabaoperaciontef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pNumeroCtaOrd                CHAR(20)
  pTipoCtaOrd                  CHAR(2)
  pFechaProg                   DATE
  pCveRastreo                  CHAR(30)
  pNombreCteOrd                CHAR(30)
  pRfcCteOrd                   CHAR(15)
  pImpTef                      CHAR(10)
  pComisionTef                 CHAR(5)
  pIvaTef                      CHAR(5)
  pImpTotTef                   CHAR(10)
  pTipoCtaBen                  CHAR(2)
  pNombreBen                   CHAR(30)
  pNumCtaTarjBen               CHAR(20)
  pCveBancoRec                 CHAR(3)
  pRfcBen                      CHAR(15)
  pConceptoPago                CHAR(50)
  pRefNum                      CHAR(7)
  pReferencia                  CHAR(40)
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pNumeroCtaOrd` | `CHAR(20)` | — | — |
| `pTipoCtaOrd` | `CHAR(2)` | — | — |
| `pFechaProg` | `DATE` | — | — |
| `pCveRastreo` | `CHAR(30)` | — | — |
| `pNombreCteOrd` | `CHAR(30)` | — | — |
| `pRfcCteOrd` | `CHAR(15)` | — | — |
| `pImpTef` | `CHAR(10)` | `tef`=TEF — transferencia electrónica de fondos | ✅ CÓDIGO |
| `pComisionTef` | `CHAR(5)` | `tef`=TEF — transferencia electrónica de fondos | ✅ CÓDIGO |
| `pIvaTef` | `CHAR(5)` | `tef`=TEF — transferencia electrónica de fondos | ✅ CÓDIGO |
| `pImpTotTef` | `CHAR(10)` | `tef`=TEF — transferencia electrónica de fondos | ✅ CÓDIGO |
| `pTipoCtaBen` | `CHAR(2)` | — | — |
| `pNombreBen` | `CHAR(30)` | — | — |
| `pNumCtaTarjBen` | `CHAR(20)` | — | — |
| `pCveBancoRec` | `CHAR(3)` | — | — |
| `pRfcBen` | `CHAR(15)` | — | — |
| `pConceptoPago` | `CHAR(50)` | — | — |
| `pRefNum` | `CHAR(7)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetOpTef` | `CHAR(5)` | L10 |
| `cCodRetRev` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `cCodRetSp2` | `CHAR(5)` | L13 |
| `cDescCodRet` | `CHAR(100)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cFechaHoy` | `CHAR(10)` | L17 |
| `cHoraFolio` | `CHAR(10)` | L18 |
| `cFolioSucursal` | `CHAR(16)` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cTipoOperacion` | `CHAR(2)` | L22 |
| `cCveCanal` | `CHAR(2)` | L23 |
| `cMotivoDev` | `CHAR(2)` | L24 |
| `cDivisa` | `CHAR(2)` | L25 |
| `cTransacSuc` | `CHAR(4)` | L26 |
| `cNumeroCtaOrd` | `CHAR(20)` | L27 |
| `cNumTarjeta` | `CHAR(16)` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| `bInTrans` | `BOOLEAN` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_fechas` | `bdinvers` | ⚠️ sí | SELECT | L81 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |
| `sp_tef_grabaoperacion` | `bditef` | ⚠️ sí | L114 |
| `reversion` | `bdicheq` | ⚠️ sí | L196 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipo = '' OR pNumeroCtaOrd = '' OR pTipoCtaOrd = '' OR pFech` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00573';` |  |
| L126 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_tef_grabaoperacion';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00574';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00562';` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00563';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00575';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00576';` |  |
| L140 | CÓDIGO_RETORNO | `LET cCodRet = '00577';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00578';` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00121';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00581';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00582';` |  |
| L150 | CÓDIGO_RETORNO | `LET cCodRet = '00456';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00105';` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '00392';` |  |
| L156 | CÓDIGO_RETORNO | `LET cCodRet = '00460';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00118';` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00583';` |  |
| L162 | CÓDIGO_RETORNO | `LET cCodRet = '00584';` |  |
| L164 | CÓDIGO_RETORNO | `LET cCodRet = '00457';` |  |
| L166 | CÓDIGO_RETORNO | `LET cCodRet = '00458';` |  |
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00585';` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet = '00586';` |  |
| L172 | CÓDIGO_RETORNO | `LET cCodRet = '00587';` |  |
| L174 | CÓDIGO_RETORNO | `LET cCodRet = '00588';` |  |
| L176 | CÓDIGO_RETORNO | `LET cCodRet = '00589';` |  |
| | *…45 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `operacion` | ACCION | operación | 🔵 CONVENCIÓN | nombre_sp |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_grabarcambiostatusolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_grabarcambiostatusolicitudmc.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "graba cambio, estatus y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_grabarcambiostatusolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud1               CHAR(20)
  pNumSolicitud2               CHAR(20)
  pNumCliente                  CHAR(20)
  pEjecutivoAnaliza            CHAR(10)
  pEjecutivoAutoriza           CHAR(10)
  pStatusInicial               CHAR(2)
  pStatusFinal                 CHAR(2)
  pMontoAnterior               DECIMAL(18,2)
  pMontoNuevo                  DECIMAL(18,2)
  pCausa                       CHAR(3)
  pComentario                  CHAR(500)
  pTipoMovto                   CHAR(1)
  pTipoBusqueda                CHAR(1)
  pBanderaMotor                CHAR(1)
) RETURNING CHAR(5) AS codret, CHAR(80) AS DESCRIPCION, CHAR(1) AS BANDERAMOTORMC
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud1` | `CHAR(20)` | — | — |
| `pNumSolicitud2` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pEjecutivoAnaliza` | `CHAR(10)` | — | — |
| `pEjecutivoAutoriza` | `CHAR(10)` | — | — |
| `pStatusInicial` | `CHAR(2)` | `status`=estatus | 🔵 CONVENCIÓN |
| `pStatusFinal` | `CHAR(2)` | `status`=estatus | 🔵 CONVENCIÓN |
| `pMontoAnterior` | `DECIMAL(18,2)` | — | — |
| `pMontoNuevo` | `DECIMAL(18,2)` | — | — |
| `pCausa` | `CHAR(3)` | — | — |
| `pComentario` | `CHAR(500)` | — | — |
| `pTipoMovto` | `CHAR(1)` | — | — |
| `pTipoBusqueda` | `CHAR(1)` | — | — |
| `pBanderaMotor` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cMensaje` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cBanderaMotorMC` | `CHAR(1)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃÂON DEL SP bdisolic:sp_mc_grabacambios` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00219';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `?olicitud` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?olicitud` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabarcargosmanualescre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_grabarcargosmanualescre.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba cargo y crédito (manual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_grabarcargosmanuales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_grabarcargosmanualescre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolioSuc                    CHAR(16)
  pNumCliente                  CHAR(20)
  pNumCuenta                   CHAR(20)
  pImporteCargo                MONEY(18,2)
  pTransaccion                 CHAR(4)
  pConcepto                    CHAR(20)
  pObservaciones               CHAR(200)
  pCapitalVigente              MONEY(18,2)
  pCapitalTransitorio          MONEY(18,2)
  pCapitalVencido              MONEY(18,2)
  pCapitalVdoNoExigible        MONEY(18,2)
  pCapitalTotal                MONEY(18,2)
  pInteresVigente              MONEY(18,2)
  pIvaInteresVigente           MONEY(18,2)
  pInteresVencido              MONEY(18,2)
  pIvaInteresVencido           MONEY(18,2)
  pInteresMoratorio            MONEY(18,2)
  pIvaInteresMoratorio         MONEY(18,2)
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pImporteCargo` | `MONEY(18,2)` | `cargo`=cargo / débito | ✅ CÓDIGO |
| `pTransaccion` | `CHAR(4)` | — | — |
| `pConcepto` | `CHAR(20)` | — | — |
| `pObservaciones` | `CHAR(200)` | — | — |
| `pCapitalVigente` | `MONEY(18,2)` | — | — |
| `pCapitalTransitorio` | `MONEY(18,2)` | — | — |
| `pCapitalVencido` | `MONEY(18,2)` | — | — |
| `pCapitalVdoNoExigible` | `MONEY(18,2)` | — | — |
| `pCapitalTotal` | `MONEY(18,2)` | — | — |
| `pInteresVigente` | `MONEY(18,2)` | — | — |
| `pIvaInteresVigente` | `MONEY(18,2)` | — | — |
| `pInteresVencido` | `MONEY(18,2)` | — | — |
| `pIvaInteresVencido` | `MONEY(18,2)` | — | — |
| `pInteresMoratorio` | `MONEY(18,2)` | — | — |
| `pIvaInteresMoratorio` | `MONEY(18,2)` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INT` | L20 |
| `mCapitalVigente` | `MONEY(18,2)` | L21 |
| `mCapitalTransitorio` | `MONEY(18,2)` | L22 |
| `mCapitalVencido` | `MONEY(18,2)` | L23 |
| `mCapitalVdoNoExigible` | `MONEY(18,2)` | L24 |
| `mCapitalTotal` | `MONEY(18,2)` | L25 |
| `mInteresVigente` | `MONEY(18,2)` | L26 |
| `mIvaInteresVigente` | `MONEY(18,2)` | L27 |
| `mInteresVencido` | `MONEY(18,2)` | L28 |
| `mIvaInteresVencido` | `MONEY(18,2)` | L29 |
| `mInteresMoratorio` | `MONEY(18,2)` | L30 |
| `mIvaInteresMoratorio` | `MONEY(18,2)` | L31 |
| `cEmpresa` | `CHAR(3)` | L32 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L69 |
| `sp_grabarcargosmanuales` | `bdicred` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `let cCodRet = '00156';` |  |
| L84 | CÓDIGO_RETORNO | `let cCodRet = '00157';` |  |
| L86 | CÓDIGO_RETORNO | `let cCodRet = '00158';` |  |
| L88 | CÓDIGO_RETORNO | `let cCodRet = '00159';` |  |
| L90 | CÓDIGO_RETORNO | `let cCodRet = '00160';` |  |
| L92 | CÓDIGO_RETORNO | `let cCodRet = '00161';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manual` | MODIF | manual | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?s`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabarpagosmanualescre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_grabarpagosmanualescre.sql` |
| **LOC (1er CREATE)** | 137 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 167 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba pagos y crédito (manual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_grabarpagosmanuales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_grabarpagosmanualescre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolioSuc                    CHAR(16)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pImportePago                 MONEY(18,2)
  pTransaccion                 CHAR(4)
  pConcepto                    CHAR(20)
  pObservaciones               CHAR(50)
  pCapitalVigente              MONEY(18,2)
  pCapitalTransitorio          MONEY(18,2)
  pCapitalVencido              MONEY(18,2)
  pCapitalVencidoNoExigible    MONEY(18,2)
  pCapitalTotal                MONEY(18,2)
  pInteresVigente              MONEY(18,2)
  pIvaInteresVigente           MONEY(18,2)
  pInteresVencido              MONEY(18,2)
  pIvaInteresVencido           MONEY(18,2)
  pInteresMoratorio            MONEY(18,2)
  pIvaInteresMoratorio         MONEY(18,2)
) RETURNING CHAR(5) AS CODRET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cre`=crédito | 🟡 INFERIDO |
| `pImportePago` | `MONEY(18,2)` | — | — |
| `pTransaccion` | `CHAR(4)` | — | — |
| `pConcepto` | `CHAR(20)` | — | — |
| `pObservaciones` | `CHAR(50)` | — | — |
| `pCapitalVigente` | `MONEY(18,2)` | — | — |
| `pCapitalTransitorio` | `MONEY(18,2)` | — | — |
| `pCapitalVencido` | `MONEY(18,2)` | — | — |
| `pCapitalVencidoNoExigible` | `MONEY(18,2)` | — | — |
| `pCapitalTotal` | `MONEY(18,2)` | — | — |
| `pInteresVigente` | `MONEY(18,2)` | — | — |
| `pIvaInteresVigente` | `MONEY(18,2)` | — | — |
| `pInteresVencido` | `MONEY(18,2)` | — | — |
| `pIvaInteresVencido` | `MONEY(18,2)` | — | — |
| `pInteresMoratorio` | `MONEY(18,2)` | — | — |
| `pIvaInteresMoratorio` | `MONEY(18,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INT` | L23 |
| `mCapitalVigente` | `MONEY(18,2)` | L24 |
| `mCapitalTransitorio` | `MONEY(18,2)` | L25 |
| `mCapitalVencido` | `MONEY(18,2)` | L26 |
| `mCapitalVdoNoExigible` | `MONEY(18,2)` | L27 |
| `mCapitalTotal` | `MONEY(18,2)` | L28 |
| `mInteresVigente` | `MONEY(18,2)` | L29 |
| `mIvaInteresVigente` | `MONEY(18,2)` | L30 |
| `mInteresVencido` | `MONEY(18,2)` | L31 |
| `mIvaInteresVencido` | `MONEY(18,2)` | L32 |
| `mInteresMoratorio` | `MONEY(18,2)` | L33 |
| `mIvaInteresMoratorio` | `MONEY(18,2)` | L34 |
| `cEmpresa` | `CHAR(3)` | L35 |
| `mInteresMoratorioBase` | `MONEY(18,2)` | L36 |
| `mInteresMoratorioCopete` | `MONEY(18,2)` | L37 |
| `mIvaInteresMoratorioBase` | `MONEY(18,2)` | L38 |
| `mIvaInteresMoratorioCopete` | `MONEY(18,2)` | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L82 |
| `sp_grabarpagosmanuales` | `bdicred` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00156';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00157';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00158';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00159';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00161';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00921';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00364';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00033';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00374';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00375';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00376';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00377';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00378';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pagos` | ENTIDAD | pagos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `manual` | MODIF | manual | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabarreversocargomancre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_grabarreversocargomancre.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba cargo y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_grabarreversocargomancre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolio                       CHAR(16)
  pEjecutivo                   CHAR(8)
  pObservacionRev              CHAR(200)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolio` | `CHAR(16)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pObservacionRev` | `CHAR(200)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `vCodRetSp` | `CHAR(6)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet   = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00165';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `?man` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?man` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabarreversopagosmancre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_grabarreversopagosmancre.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba pagos y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_grabarreversopagosmancre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolio                       CHAR(16)
  pEjecutivo                   CHAR(8)
  pObservacionRev              CHAR(50)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolio` | `CHAR(16)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pObservacionRev` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `vCodRetSp` | `CHAR(6)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet   = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00165';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pagos` | ENTIDAD | pagos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?man` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?man` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabazonacallexasignar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_grabazonacallexasignar.sql` |
| **LOC (1er CREATE)** | 149 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "graba zona y calle" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `grabazonacallexasignar` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_grabazonacallexasignar(
  pIdFuncion                   CHAR(10)
  pTipo                        SMALLINT
  pCiudad                      SMALLINT
  pClave                       INTEGER
  pDesc                        CHAR(30)
  pRumbo                       CHAR(40)
  pCodigoP                     INTEGER
  pMunicipio                   CHAR(27)
  pPoblacionZona               CHAR(30)
  pUsuario                     CHAR(8)
  pConfirma                    CHAR(1)
) RETURNING CHAR(5) AS CodRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `SMALLINT` | — | — |
| `pCiudad` | `SMALLINT` | — | — |
| `pClave` | `INTEGER` | — | — |
| `pDesc` | `CHAR(30)` | — | — |
| `pRumbo` | `CHAR(40)` | — | — |
| `pCodigoP` | `INTEGER` | — | — |
| `pMunicipio` | `CHAR(27)` | — | — |
| `pPoblacionZona` | `CHAR(30)` | `zona`=zona | 🔵 CONVENCIÓN |
| `pUsuario` | `CHAR(8)` | — | — |
| `pConfirma` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cExist` | `CHAR(1)` | L9 |
| `cNumeroCalle` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catzonas` | `bdinteg` | ⚠️ sí | SELECT | L42 |
| `si_catcalles` | `bdinteg` | ⚠️ sí | SELECT | L82 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `grabazonacallexasignar` | `bdinteg` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00107';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00195';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00107';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `zona` | ENTIDAD | zona | 🔵 CONVENCIÓN | nombre_sp |
| `calle` | ENTIDAD | calle (domicilio) | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_actualizasolicitudreporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_actualizasolicitudreporte.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza solicitud y reporte" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_actualizasolicitudreporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegSolicitud              INTEGER
  pNombreArchivo               CHAR(80)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegSolicitud` | `INTEGER` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pNombreArchivo` | `CHAR(80)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bInTransaction` | `BOOLEAN` | L7 |
| `iIdx` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_registrosolicitud` | `bdicnweb` | no | UPDATE | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegSolicitud IS NULL THEN` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00236';			ROLLBACK WORK;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_atencionsolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_atencionsolicitud.sql` |
| **LOC (1er CREATE)** | 191 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_atencionsolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegSolicitud              INTEGER
  pStatusRespuesta             INTEGER
  pFolioTransaccion            CHAR(16)
  pCodError                    CHAR(5)
  pDescripcionError            CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegSolicitud` | `INTEGER` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pStatusRespuesta` | `INTEGER` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFolioTransaccion` | `CHAR(16)` | — | — |
| `pCodError` | `CHAR(5)` | — | — |
| `pDescripcionError` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bInTransaction` | `BOOLEAN` | L7 |
| `iIdx` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | INSERT | L66 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegSolicitud IS NULL THEN` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_atencion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_atencion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_busquedasolicitudes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_busquedasolicitudes.sql` |
| **LOC (1er CREATE)** | 359 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda solicitudes" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_busquedasolicitudes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoGestor                  CHAR(1)
  pFechaDesde                  DATE
  pFechaHasta                  DATE
  pTipoCta                     CHAR(2)
  pSolicitud                   INTEGER
  pAreaResSol                  INTEGER
  pUsuarioResSol               CHAR(8)
  pStatus                      INTEGER
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pIsArea                      CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoGestor` | `CHAR(1)` | — | — |
| `pFechaDesde` | `DATE` | — | — |
| `pFechaHasta` | `DATE` | — | — |
| `pTipoCta` | `CHAR(2)` | — | — |
| `pSolicitud` | `INTEGER` | — | — |
| `pAreaResSol` | `INTEGER` | — | — |
| `pUsuarioResSol` | `CHAR(8)` | — | — |
| `pStatus` | `INTEGER` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pIsArea` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L31 |
| `iSqlErr` | `INTEGER` | L32 |
| `iIdRegistroSolicitud` | `INTEGER` | L33 |
| `dFechaSolicitud` | `DATE` | L34 |
| `dFechaAtencion` | `DATE` | L35 |
| `iFolioSolicitud` | `CHAR(20)` | L36 |
| `cIdSistemaCuenta` | `CHAR(2)` | L37 |
| `iIdFuncionSolicitud` | `CHAR(10)` | L38 |
| `cDescripcionSolicitud` | `CHAR(50)` | L39 |
| `cCliente` | `CHAR(20)` | L40 |
| `cCuenta` | `CHAR(20)` | L41 |
| `cDescripcionArea` | `CHAR(50)` | L42 |
| `cNumUsuario` | `CHAR(8)` | L43 |
| `cNombre` | `CHAR(45)` | L44 |
| `cDescStatusSolicitud` | `CHAR(50)` | L45 |
| `cDescripcionMotivo` | `CHAR(50)` | L46 |
| `iNoRegistros` | `INTEGER` | L47 |
| `iIdStatusSolicitud` | `INTEGER` | L48 |
| `iArea` | `INTEGER` | L49 |
| `iIdSolicitud` | `INTEGER` | L50 |
| `cCmd1` | `CHAR(5000)` | L51 |
| `cCmd2` | `CHAR(2500)` | L52 |
| `cNombreCliente` | `CHAR(120)` | L53 |
| `dMonto` | `DECIMAL(16,2)` | L54 |
| `cFolioTransaccion` | `CHAR(16)` | L55 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area_usuario` | `bdicnweb` | no | SELECT | L140 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L125 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L108 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoGestor = '' OR pFechaDesde = '' OR pFechaHasta = '' OR p` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L215 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L233 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L326 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L344 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `busqueda` | ACCION | búsqueda | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_busquedausuarioscat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_busquedausuarioscat.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda usuarios y catálogo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_busquedausuarioscat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pUsuarioBusqueda             CHAR(8)
  pNombre                      CHAR(45)
  pStatus                      CHAR(1)
  pArea                        INTEGER
  pJefe                        CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pUsuarioBusqueda` | `CHAR(8)` | `busqueda`=búsqueda | ✅ CÓDIGO |
| `pNombre` | `CHAR(45)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pArea` | `INTEGER` | — | — |
| `pJefe` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iIdArea` | `INTEGER` | L12 |
| `cClaveArea` | `CHAR(10)` | L13 |
| `cDescArea` | `CHAR(50)` | L14 |
| `cIdUsuario` | `CHAR(8)` | L15 |
| `cNombreUsuario` | `CHAR(45)` | L16 |
| `cStatus` | `CHAR(1)` | L17 |
| `cIsJefe` | `CHAR(1)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `iNoRegistros` | `INTEGER` | L20 |
| `bStatus` | `BOOLEAN` | L21 |
| `bJefe` | `BOOLEAN` | L22 |
| `cCmd1` | `CHAR(5000)` | L23 |
| `cCmd2` | `CHAR(2500)` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L95 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `busqueda` | ACCION | búsqueda | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_cancelarsolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_cancelarsolicitud.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela solicitud" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_cancelarsolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegSolicitud              INTEGER
  pIMotivo                     INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegSolicitud` | `INTEGER` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pIMotivo` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bInTransaction` | `BOOLEAN` | L7 |
| `iIdx` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | INSERT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegSolicitud IS NULL OR pIMotivo IS NULL THEN` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cancela` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_cancelarsolicitudreintento`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_cancelarsolicitudreintento.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela solicitud y interés" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_cancelarsolicitudreintento(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegSolicitud              INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegSolicitud` | `INTEGER` | `solicitud`=solicitud | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bInTransaction` | `BOOLEAN` | L7 |
| `iIdx` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | INSERT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegSolicitud IS NULL THEN` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cancela` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?ento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?r`, `?re`, `?ento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultaareasrespsol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultaareasrespsol.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud (especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultaareasrespsol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSolicitud                 INTEGER
  pTipoGestor                  CHAR(1)
  pOperacion                   INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSolicitud` | `INTEGER` | `sol`=solicitud | 🟡 INFERIDO |
| `pTipoGestor` | `CHAR(1)` | — | — |
| `pOperacion` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iIdArea` | `INTEGER` | L11 |
| `cClaveArea` | `CHAR(10)` | L12 |
| `cDescripcionArea` | `CHAR(50)` | L13 |
| `cStatus` | `CHAR(1)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area_permisos` | `bdicnweb` | no | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdSolicitud IS NULL OR pTipoGestor = '' OR pOperacion IS NUL` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L64 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L74 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L87 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L97 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?areasr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?areasr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultacatstatussolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultacatstatussolicitud.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo, estatus y solicitud" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultacatstatussolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIdStatusSolicitud` | `INTEGER` | L8 |
| `cDescStatusSolicitud` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_catstatussolicitud` | `bdicnweb` | no | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultacifrassolicitudes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultacifrassolicitudes.sql` |
| **LOC (1er CREATE)** | 216 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta ss — subsistema" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=4 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultacifrassolicitudes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoGestor                  CHAR(1)
  pOperacion                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoGestor` | `CHAR(1)` | — | — |
| `pOperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iTotalPendientes` | `INTEGER` | L11 |
| `iTotalCanceladas` | `INTEGER` | L12 |
| `iTotalRechazadas` | `INTEGER` | L13 |
| `iTotalExitosas` | `INTEGER` | L14 |
| `iTotalTodas` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_registrosolicitud` | `bdicnweb` | no | SELECT | L60 |
| `sw_gs_registrosolicitud_hist` | `bdicnweb` | no | SELECT | L91 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pOperacion IS NULL OR pTipoGestor = ''  THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?cifra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?olicitudes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?cifra`, `ss`, `?olicitudes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultacomentarios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultacomentarios.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Comisión bancaria — cobro de comisión sobre cuenta, Tarjeta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultacomentarios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegSol                    INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegSol` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cTipoGestor` | `CHAR(20)` | L8 |
| `cComentario` | `CHAR(200)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_comentarios` | `bdicnweb` | no | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegSol IS NULL THEN` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?en`, `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultamotivoscancelacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultamotivoscancelacion.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta motivo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultamotivoscancelacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSolicitud                 INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSolicitud` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIdMotivoCancelacion` | `INTEGER` | L9 |
| `cClaveMotivo` | `CHAR(10)` | L10 |
| `cDescripcionMotivo` | `CHAR(50)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_motivos_cancelacion` | `bdicnweb` | no | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdSolicitud IS NULL OR pRegistros IS NULL OR pRecuperacion I` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00361';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `motivo` | ENTIDAD | motivo / causa | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cancelacion` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultamtvoscancelacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultamtvoscancelacion.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultamtvoscancelacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iIdMotivoCancelacion` | `INTEGER` | L15 |
| `cSistemaCuenta` | `CHAR(2)` | L16 |
| `cDescSistemaCuenta` | `CHAR(30)` | L17 |
| `iIdTipoSolicitud` | `INTEGER` | L18 |
| `cDescTipoSolicitud` | `CHAR(50)` | L19 |
| `cClaveMotivo` | `CHAR(10)` | L20 |
| `cDescMotivo` | `CHAR(50)` | L21 |
| `cStatusMotivo` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_motivos_cancelacion` | `bdicnweb` | no | SELECT | L67 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L71 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mtv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cancelacion` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?mtv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultareas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultareas.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultareas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iIdArea` | `INTEGER` | L12 |
| `cClaveArea` | `CHAR(10)` | L13 |
| `cDescripcionArea` | `CHAR(50)` | L14 |
| `cStatus` | `CHAR(1)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?eas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?eas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultaregistrosolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultaregistrosolicitud.sql` |
| **LOC (1er CREATE)** | 286 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar ro — Rol Operativo y solicitud" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultaregistrosolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdStatusSolicitud           INTEGER
  pTipoGestor                  CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdStatusSolicitud` | `INTEGER` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pTipoGestor` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L30 |
| `iSqlErr` | `INTEGER` | L31 |
| `iIdRegistroSolicitud` | `INTEGER` | L32 |
| `dFechaSolicitud` | `DATE` | L33 |
| `dFechaAtencion` | `DATE` | L34 |
| `iFolioSolicitud` | `CHAR(20)` | L35 |
| `cIdSistemaCuenta` | `CHAR(2)` | L36 |
| `iIdFuncionSolicitud` | `CHAR(10)` | L37 |
| `cDescripcionSolicitud` | `CHAR(50)` | L38 |
| `cCliente` | `CHAR(20)` | L39 |
| `cCuenta` | `CHAR(20)` | L40 |
| `cDescripcionArea` | `CHAR(50)` | L41 |
| `cNumUsuario` | `CHAR(8)` | L42 |
| `cNombre` | `CHAR(45)` | L43 |
| `cDescStatusSolicitud` | `CHAR(50)` | L44 |
| `cDescripcionMotivo` | `CHAR(50)` | L45 |
| `iNoRegistros` | `INTEGER` | L46 |
| `iIdStatusSolicitud` | `INTEGER` | L47 |
| `iArea` | `INTEGER` | L48 |
| `iIdSolicitud` | `INTEGER` | L49 |
| `cCmd1` | `CHAR(5000)` | L50 |
| `cCmd2` | `CHAR(2500)` | L51 |
| `cNombreCliente` | `CHAR(120)` | L52 |
| `cNumUsuario2` | `CHAR(8)` | L53 |
| `cNombre2` | `CHAR(45)` | L54 |
| *…7 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area_usuario` | `bdicnweb` | no | SELECT | L155 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L135 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L110 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdStatusSolicitud IS NULL OR pTipoGestor = '' OR pRegistros ` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L184 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L202 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L254 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L272 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?egist` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?egist`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultareintentossolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultareintentossolicitud.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar interés, OS — Originación de Solicitudes y solicitud" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultareintentossolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolioRegistro               CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolioRegistro` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotalReintentos` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_registrosolicitud` | `bdicnweb` | no | SELECT | L40 |
| `sw_gs_registrosolicitud_hist` | `bdicnweb` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?ent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?e`, `?ent` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultasolicitudes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultasolicitudes.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitudes" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultasolicitudes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iIdSolicitud` | `INTEGER` | L12 |
| `cSistemaCuenta` | `CHAR(2)` | L13 |
| `cDescSistemaCuenta` | `CHAR(30)` | L14 |
| `cDescSolicitud` | `CHAR(50)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `cIdFuncion` | `CHAR(10)` | L17 |
| `cAbreReporte` | `CHAR(1)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_solicitudes` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultasolicitudusuarioarea`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultasolicitudusuarioarea.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta solicitud y usuario" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultasolicitudusuarioarea(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdAreaUsuario               INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdAreaUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `iIdAreaSolicitud` | `INTEGER` | L14 |
| `cSistemaCuenta` | `CHAR(2)` | L15 |
| `cDescSistemaCuenta` | `CHAR(30)` | L16 |
| `iIdSolicitud` | `INTEGER` | L17 |
| `cDescSolicitud` | `CHAR(50)` | L18 |
| `cIndResponsable` | `CHAR(1)` | L19 |
| `cIndSolicitante` | `CHAR(1)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area_solicitudes` | `bdicnweb` | no | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdAreaUsuario IS NULL OR pRegistros IS NULL OR pRecuperacion` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L70 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?area` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?area` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultatipocuenta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultatipocuenta.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cuenta (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultatipocuenta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `cSistemaCuenta` | `CHAR(2)` | L9 |
| `cDescSistemaCuenta` | `CHAR(30)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_sistema_cuenta` | `bdicnweb` | no | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultatotregistrosolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultatotregistrosolicitud.sql` |
| **LOC (1er CREATE)** | 139 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta registros" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultatotregistrosolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdStatusSolicitud           INTEGER
  pTipoGestor                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdStatusSolicitud` | `INTEGER` | — | — |
| `pTipoGestor` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `iArea` | `INTEGER` | L8 |
| `cCmd1` | `CHAR(2500)` | L9 |
| `cCmd2` | `CHAR(2500)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area_usuario` | `bdicnweb` | no | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdStatusSolicitud IS NULL OR pTipoGestor = '' THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L69 | FÓRMULA | `LET cCmd1="SELECT COUNT(*) ";` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L110 | FÓRMULA | `LET cCmd1="SELECT COUNT(*) ";` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `registros` | ENTIDAD | registros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?olicitud` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?tot`, `?olicitud` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultausuarioarea`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultausuarioarea.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuario" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultausuarioarea(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pUsuarioConsulta             CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pUsuarioConsulta` | `CHAR(8)` | `consulta`=consulta / lee · `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cUsuario` | `CHAR(8)` | L13 |
| `iIdAreaUsuario` | `INTEGER` | L14 |
| `cStatus` | `CHAR(1)` | L15 |
| `cIdArea` | `INTEGER` | L16 |
| `cDescArea` | `CHAR(50)` | L17 |
| `cJefeArea` | `CHAR(1)` | L18 |
| `iNoRegs` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area_usuario` | `bdicnweb` | no | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?area` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?area` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_consultausuariosrespsol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_consultausuariosrespsol.sql` |
| **LOC (1er CREATE)** | 172 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuarios y solicitud (especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_consultausuariosrespsol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdArea                      INTEGER
  pIdSolicitud                 INTEGER
  pTipoGestor                  CHAR(1)
  pOperacion                   INTEGER
  pMontoOperar                 DECIMAL(16,2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdArea` | `INTEGER` | — | — |
| `pIdSolicitud` | `INTEGER` | `sol`=solicitud | 🟡 INFERIDO |
| `pTipoGestor` | `CHAR(1)` | — | — |
| `pOperacion` | `INTEGER` | — | — |
| `pMontoOperar` | `DECIMAL(16,2)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iIdAreaUsuario` | `INTEGER` | L11 |
| `cIdUsuario` | `CHAR(8)` | L12 |
| `cNombre` | `CHAR(45)` | L13 |
| `iIdArea` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdArea IS NULL OR pIdSolicitud IS NULL OR pTipoGestor = '' O` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L85 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L96 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L131 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L142 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L156 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_detalleregistrosolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_detalleregistrosolicitud.sql` |
| **LOC (1er CREATE)** | 188 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "detalle y registros" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_detalleregistrosolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegistroSolicitud         INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegistroSolicitud` | `INTEGER` | `registros`=registros | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L36 |
| `iSqlErr` | `INTEGER` | L37 |
| `cCliente` | `CHAR(20)` | L38 |
| `cCuenta` | `CHAR(20)` | L39 |
| `cClaveTransaccion` | `CHAR(4)` | L40 |
| `iDocumentoCheque` | `INTEGER` | L41 |
| `dImporte` | `DECIMAL(16,2)` | L42 |
| `cReferencia` | `CHAR(40)` | L43 |
| `cSucursal` | `CHAR(4)` | L44 |
| `cUsuarioRev` | `CHAR(8)` | L45 |
| `cFolioReverso` | `CHAR(16)` | L46 |
| `iClaveBloqueo` | `INTEGER` | L47 |
| `iOpcionBloqueo` | `INTEGER` | L48 |
| `iAreaSolicitanteBloqueo` | `INTEGER` | L49 |
| `iMotivoBloqueo` | `INTEGER` | L50 |
| `cAreaPersonaSolicita` | `CHAR(150)` | L51 |
| `cMotivoBloqueoDesbloqueo` | `CHAR(150)` | L52 |
| `dFechaVencimiento` | `DATE` | L53 |
| `cCuentaReferencia` | `CHAR(20)` | L54 |
| `iInstruccionVencimiento` | `INTEGER` | L55 |
| `iTipoBloqueo` | `INTEGER` | L56 |
| `iCausaBloqueo` | `INTEGER` | L57 |
| `cConcepto` | `CHAR(2)` | L58 |
| `cObservaciones` | `CHAR(50)` | L59 |
| `iMotivoCancelacion` | `INTEGER` | L60 |
| *…10 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L130 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L121 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegistroSolicitud IS NULL THEN` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L166 | FÓRMULA | `LET iNoRegistros = iNoRegistros +  1;` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `registros` | ENTIDAD | registros | 🔵 CONVENCIÓN | nombre_sp |
| `?olicitud` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?olicitud` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_eliminareportesrepositorio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_eliminareportesrepositorio.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina reportes y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_eliminareportesrepositorio(
  pDiasVigenciaReportes        SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pDiasVigenciaReportes` | `SMALLINT` | `reportes`=reportes · `rep`=reporte | 🔵 CONVENCIÓN / ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cNombreReporte` | `CHAR(80)` | L8 |
| `iIdRegistroSolicitud` | `INTEGER` | L9 |
| `iRegistrosEncontrados` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_registrosolicitud_hist` | `bdicnweb` | no | SELECT | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pDiasVigenciaReportes IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L50 | FÓRMULA | `LET iRegistrosEncontrados = iRegistrosEncontrados + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?itorio` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?itorio` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_grabamtvoscancelacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_grabamtvoscancelacion.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_grabamtvoscancelacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               SMALLINT
  pIdMotivo                    INTEGER
  pClaveMotivo                 CHAR(10)
  pSistemaCuenta               CHAR(2)
  pIdSolicitud                 INTEGER
  pDescripcionMotivo           CHAR(50)
  pStatus                      CHAR(1)
  pIpUsuario                   CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pIdMotivo` | `INTEGER` | — | — |
| `pClaveMotivo` | `CHAR(10)` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |
| `pIdSolicitud` | `INTEGER` | — | — |
| `pDescripcionMotivo` | `CHAR(50)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pIpUsuario` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_motivos_cancelacion` | `bdicnweb` | no | SELECT | L61 |
| `sw_gs_motivos_cancelacion` | `bdicnweb` | no | INSERT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion IS NULL OR pSistemaCuenta = '' OR pIdSolicitud` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00077';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | VALIDACIÓN_NULL | `IF pIdMotivo IS NULL THEN` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?mtv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cancelacion` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?mtv` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_grabarea`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_grabarea.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_grabarea(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveArea                   CHAR(10)
  pDescripcion                 CHAR(50)
  pTipoOperacion               SMALLINT
  pStatus                      CHAR(1)
  pIpUsuario                   CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveArea` | `CHAR(10)` | — | — |
| `pDescripcion` | `CHAR(50)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pIpUsuario` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bActivo` | `BOOLEAN` | L7 |
| `iIdArea` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area` | `bdicnweb` | no | SELECT | L49 |
| `sw_gs_area` | `bdicnweb` | no | INSERT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pClaveArea = '' OR pDescripcion = '' OR pTipoOperacion IS NUL` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?rea` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?rea` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_grabarsolicitudusuarioarea`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_grabarsolicitudusuarioarea.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba solicitud y usuario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_grabarsolicitudusuarioarea(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               SMALLINT
  pIdAreaUsuario               INTEGER
  pPermisosUsuario             CHAR(250)
  pIpUsuario                   CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pIdAreaUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pPermisosUsuario` | `CHAR(250)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIpUsuario` | `CHAR(15)` | `usuario`=usuario | ✅ CÓDIGO |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCadenaValores` | `CHAR(15)` | L7 |
| `cCadena` | `CHAR(15)` | L8 |
| `iIdSolicitud` | `INTEGER` | L9 |
| `bEsResponsable` | `BOOLEAN` | L10 |
| `bEsSolicitante` | `BOOLEAN` | L11 |
| `iNoRegsProcesados` | `INTEGER` | L12 |
| `iParams` | `SMALLINT` | L13 |
| `bInTransaction` | `BOOLEAN` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area_solicitudes` | `bdicnweb` | no | SELECT | L64 |
| `sw_gs_area_solicitudes` | `bdicnweb` | no | INSERT | L101 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |
| `sp_split_cadena` | `bdicnweb` | no | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L54 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdAreaUsuario IS NULL OR pTipoOperacion IS NULL OR pPermisos` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L89 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L92 | FÓRMULA | `LET iParams = iParams + 1;` |  |
| L104 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |
| L121 | FÓRMULA | `LET iNoRegsProcesados = iNoRegsProcesados + DBINFO('sqlca.sqlerrd2');` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?area` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?r`, `?area` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_notificacioncorreoelectronico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_notificacioncorreoelectronico.sql` |
| **LOC (1er CREATE)** | 549 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "notifica correo electrónico y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `notifica` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=5 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_notificacioncorreoelectronico(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcionEnvio                 CHAR(1)
  pTipoOperacion               SMALLINT
  pIdSolicitud                 INTEGER
  pPlantilla                   CHAR(10)
  pTitulo                      CHAR(60)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcionEnvio` | `CHAR(1)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pIdSolicitud` | `INTEGER` | — | — |
| `pPlantilla` | `CHAR(10)` | — | — |
| `pTitulo` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cJefeAreaUsuarioSolic` | `CHAR(8)` | L9 |
| `cUsuarioSolic` | `CHAR(8)` | L10 |
| `cUsuarioResponsable` | `CHAR(8)` | L11 |
| `iIdAreaResponsable` | `INTEGER` | L12 |
| `cJefeUsuarioResponsable` | `CHAR(8)` | L13 |
| `iIdSolicitudAnterior` | `INTEGER` | L14 |
| `cTipoMsj` | `CHAR(1)` | L17 |
| `cIdMsj` | `CHAR(10)` | L18 |
| `cNumclt` | `CHAR(20)` | L19 |
| `cNumcta` | `CHAR(20)` | L20 |
| `cNumTarjeta` | `CHAR(16)` | L21 |
| `cTipoproc` | `CHAR(1)` | L22 |
| `cStr1` | `CHAR(30)` | L23 |
| `cStr2` | `CHAR(30)` | L24 |
| `cStr3` | `CHAR(30)` | L25 |
| `cStr4` | `CHAR(30)` | L26 |
| `cStr5` | `CHAR(150)` | L27 |
| `cStr6` | `CHAR(100)` | L28 |
| `cStr7` | `CHAR(60)` | L29 |
| `cStr8` | `CHAR(60)` | L30 |
| `cStr9` | `CHAR(15)` | L31 |
| *…10 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L121 |
| `sw_gs_area` | `bdicnweb` | no | SELECT | L122 |
| `sw_gs_registrosolicitud` | `bdicnweb` | no | SELECT | L132 |
| `sw_gs_area_usuario` | `bdicnweb` | no | SELECT | L258 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L97 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L210 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L91 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion IS NULL OR pPlantilla = '' OR pIdSolicitud IS ` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L239 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_registra_evento';` |  |
| L315 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_registra_evento';` |  |
| L453 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_registra_evento';` |  |
| L528 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_registra_evento';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `notifica` | ACCION | notifica | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `correo` | ENTIDAD | correo electrónico | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?elect` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?nico` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?cion`, `?elect`, `ro`, `?nico` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_reasignarsolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_reasignarsolicitud.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "asigna solicitud" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_reasignarsolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegSolicitud              INTEGER
  pAreaResponsable             INTEGER
  pUsuarioResponsable          CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegSolicitud` | `INTEGER` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pAreaResponsable` | `INTEGER` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pUsuarioResponsable` | `CHAR(8)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bInTransaction` | `BOOLEAN` | L7 |
| `iIdx` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | INSERT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegSolicitud IS NULL OR pAreaResponsable IS NULL OR  pUsua` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_re`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_registrocomentariosolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_registrocomentariosolicitud.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registro, Comisión bancaria — cobro de comisión sobre cuenta, Tarjeta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_registrocomentariosolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdReg                       INTEGER
  pComentario                  CHAR(200)
  pTipoGestor                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdReg` | `INTEGER` | — | — |
| `pComentario` | `CHAR(200)` | `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) · `tar`=Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, mover_his_tar, obtener_cta_con_num_tar) | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pTipoGestor` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bInTransaction` | `BOOLEAN` | L7 |
| `iIdx` | `INTEGER` | L8 |
| `iConsecutivo` | `INTEGER` | L9 |
| `iFolio` | `BIGINT` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_comentarios` | `bdicnweb` | no | SELECT | L63 |
| `sw_gs_registrosolicitud` | `bdicnweb` | no | SELECT | L76 |
| `sw_gs_comentarios` | `bdicnweb` | no | INSERT | L82 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdReg IS NULL OR pTipoGestor = '' THEN` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L69 | FÓRMULA | `LET iConsecutivo=iConsecutivo+1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00236';			ROLLBACK WORK;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `registro` | ENTIDAD | registro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?olicitud` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?en`, `?i`, `?olicitud` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_registrosolicitudhistorico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_registrosolicitudhistorico.sql` |
| **LOC (1er CREATE)** | 208 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registros (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_registrosolicitudhistorico(
  pOperacion                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pOperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bInTransaction` | `BOOLEAN` | L7 |
| `iTotReg` | `INTEGER` | L8 |
| `iIdRegistroSolicitud` | `INTEGER` | L9 |
| `iFolioSolicitud` | `BIGINT` | L10 |
| `iIdSolicitud` | `INTEGER` | L11 |
| `dFechaSolicitud` | `DATE` | L12 |
| `iIdAreaSolicitante` | `INTEGER` | L13 |
| `cUsuarioSolicitante` | `CHAR(8)` | L14 |
| `iIdAreaResponsable` | `INTEGER` | L15 |
| `cUsuarioResponsable` | `CHAR(8)` | L16 |
| `iIdStatusSolicitud` | `INTEGER` | L17 |
| `dFechaCambio` | `DATETIME YEAR to FRACTION(3)` | L18 |
| `dHoraCambio` | `DATETIME HOUR to FRACTION(3)` | L19 |
| `iCodError` | `CHAR(5)` | L20 |
| `cDescError` | `CHAR(100)` | L21 |
| `iIdMotivoCancelacion` | `INTEGER` | L22 |
| `cFolioTransaccion` | `CHAR(16)` | L23 |
| `dFechaAtencion` | `DATE` | L24 |
| `iNumReintentos` | `INTEGER` | L25 |
| `bStatus` | `BOOLEAN` | L26 |
| `cCliente` | `CHAR(20)` | L27 |
| `cCuenta` | `CHAR (20)` | L28 |
| `cClaveTransaccion` | `CHAR(4)` | L29 |
| *…24 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_registrosolicitud` | `bdicnweb` | no | SELECT | L151 |
| `sw_gs_registrosolicitud_hist` | `bdicnweb` | no | INSERT | L153 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L131 | VALIDACIÓN_NULL | `IF pOperacion IS NULL THEN` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L165 | FÓRMULA | `LET iTotReg = iTotReg + 1;` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `registros` | ENTIDAD | registros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?olicitud` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_`, `?olicitud` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_reintentarsolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_reintentarsolicitud.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "interés, Tarjeta y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_reintentarsolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegSolicitud              INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegSolicitud` | `INTEGER` | `solicitud`=solicitud | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bInTransaction` | `BOOLEAN` | L7 |
| `iIdx` | `INTEGER` | L8 |
| `iNumReintentos` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | INSERT | L67 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegSolicitud IS NULL THEN` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iNumReintentos = iNumReintentos + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_re`, `?en` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gs_totalregbusquedasolicitudes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_gs_totalregbusquedasolicitudes.sql` |
| **LOC (1er CREATE)** | 222 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registro solicitudes (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_gs_totalregbusquedasolicitudes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoGestor                  CHAR(1)
  pFechaDesde                  DATE
  pFechaHasta                  DATE
  pTipoCta                     CHAR(2)
  pSolicitud                   INTEGER
  pAreaResSol                  INTEGER
  pUsuarioResSol               CHAR(8)
  pStatus                      INTEGER
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pIsArea                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoGestor` | `CHAR(1)` | — | — |
| `pFechaDesde` | `DATE` | — | — |
| `pFechaHasta` | `DATE` | — | — |
| `pTipoCta` | `CHAR(2)` | — | — |
| `pSolicitud` | `INTEGER` | — | — |
| `pAreaResSol` | `INTEGER` | — | — |
| `pUsuarioResSol` | `CHAR(8)` | — | — |
| `pStatus` | `INTEGER` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pIsArea` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `iArea` | `INTEGER` | L11 |
| `cCmd1` | `CHAR(5000)` | L13 |
| `cCmd2` | `CHAR(2500)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gs_area_usuario` | `bdicnweb` | no | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L95 | FÓRMULA | `LET cCmd1="SELECT COUNT(*) ";` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L177 | FÓRMULA | `LET cCmd1="SELECT COUNT(*) ";` |  |
| L208 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_gs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `busqueda` | ACCION | búsqueda | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardaapoderadosctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardaapoderadosctemoral.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=149 / fan_out=4 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda apoderado y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardaapoderadosctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pSecuencia                   INTEGER
  pNumCteApode                 CHAR(20)
  pNomApodera                  CHAR(60)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pSecuencia` | `INTEGER` | — | — |
| `pNumCteApode` | `CHAR(20)` | — | — |
| `pNomApodera` | `CHAR(60)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iSecReal` | `INT` | L9 |
| `cNumCteAp` | `CHAR(20)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_apoderado` | `bdinteg` | ⚠️ sí | SELECT | L43 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | UPDATE | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `ctemoralapoderados` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | VALIDACIÓN_NULL | `IF cNumCteAp <> pNumCteApode OR cNumCteAp IS NULL THEN` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ctemoralapoderados';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `apoderado` | ENTIDAD | apoderado | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardaapoderadosctemoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardaapoderadosctemoral2.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=33 / fan_out=25 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda apoderado y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardaapoderadosctemoral2(
  pUsuario                     CHAR(8)
  pNumCte                      CHAR(20)
  pSecuencia                   INTEGER
  pNumCteApode                 CHAR(20)
  pNomApodera                  CHAR(60)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pSecuencia` | `INTEGER` | — | — |
| `pNumCteApode` | `CHAR(20)` | — | — |
| `pNomApodera` | `CHAR(60)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iSecReal` | `INT` | L9 |
| `cNumCteAp` | `CHAR(20)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_apoderado` | `bdinteg` | ⚠️ sí | SELECT | L37 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | UPDATE | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `ctemoralapoderados` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | VALIDACIÓN_NULL | `IF cNumCteAp <> pNumCteApode OR cNumCteAp IS NULL THEN` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ctemoralapoderados';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `apoderado` | ENTIDAD | apoderado | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardabitacoraccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardabitacoraccl.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda bitácora y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardabitacoraccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pElemento                    INTEGER
  pActividad                   CHAR(150)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pElemento` | `INTEGER` | — | — |
| `pActividad` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `cDescCodRet` | `CHAR(100)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L32 |
| `bditarjeta` | `bdicnweb` | no | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_concreing_guarda` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

---

## `sp_guardacambioinstruccionpagare_vtototal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardacambioinstruccionpagare_vtototal.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda cambio, cc — cuenta corriente y pagaré (total)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=2 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardacambioinstruccionpagare_vtototal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pFechaApertura               DATE
  pFechaVencimientoAnterior    DATE
  pFechaVencimientoActual      DATE
  pPlazoDiasAnt                INT
  pTasaInteresNeta             DECIMAL(9,6)
  pTasaIsr                     DECIMAL(9,6)
  pTasaInteresBruto            DECIMAL(9,6)
  pCapital                     MONEY(14,2)
  pInteresBrutoActual          MONEY(14,2)
  pInteresIsrActual            MONEY(14,2)
  pInteresNetoActual           MONEY(14,2)
  pIp                          CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaApertura` | `DATE` | — | — |
| `pFechaVencimientoAnterior` | `DATE` | — | — |
| `pFechaVencimientoActual` | `DATE` | — | — |
| `pPlazoDiasAnt` | `INT` | — | — |
| `pTasaInteresNeta` | `DECIMAL(9,6)` | — | — |
| `pTasaIsr` | `DECIMAL(9,6)` | — | — |
| `pTasaInteresBruto` | `DECIMAL(9,6)` | — | — |
| `pCapital` | `MONEY(14,2)` | — | — |
| `pInteresBrutoActual` | `MONEY(14,2)` | — | — |
| `pInteresIsrActual` | `MONEY(14,2)` | — | — |
| `pInteresNetoActual` | `MONEY(14,2)` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iPlazoDiasAct` | `INTEGER` | L9 |
| `mInteresBrutoNuevo` | `MONEY(14,2)` | L10 |
| `mInteresIsrNuevo` | `MONEY(14,2)` | L11 |
| `mInteresNetoNuevo` | `MONEY(14,2)` | L12 |
| `mInteresBrutoDiferencia` | `MONEY(14,2)` | L13 |
| `mInteresIsrDiferencia` | `MONEY(14,2)` | L14 |
| `mInteresNetoDiferencia` | `MONEY(14,2)` | L15 |
| `dFecha` | `DATETIME YEAR TO FRACTION` | L16 |
| `iRegistro` | `INTEGER` | L17 |
| `iNoRows` | `INTEGER` | L18 |
| `pPagare` | `CHAR(15)` | L19 |
| `pSucursal` | `CHAR(5)` | L20 |
| `pTranxAbono` | `CHAR(5)` | L21 |
| `pTranxCargo` | `CHAR(5)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L70 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L73 |
| `sw_tr_cambioinstruccion` | `bdicnweb` | no | SELECT | L99 |
| `sw_tr_cambioinstruccion` | `bdicnweb` | no | INSERT | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | FÓRMULA | `LET mInteresBrutoNuevo = pCapital * iPlazoDiasAct * pTasaInteresBruto / 36000;` | 🔴 MONEY/aritmética financiera |
| L83 | FÓRMULA | `LET mInteresIsrNuevo = pCapital * iPlazoDiasAct * pTasaIsr / 36000;` | 🔴 MONEY/aritmética financiera |
| L86 | FÓRMULA | `LET mInteresNetoNuevo = mInteresBrutoNuevo - mInteresIsrNuevo;` | 🔴 MONEY/aritmética financiera |
| L89 | FÓRMULA | `LET mInteresBrutoDiferencia = pInteresBrutoActual - mInteresBrutoNuevo;` | 🔴 MONEY/aritmética financiera |
| L92 | FÓRMULA | `LET mInteresIsrDiferencia = pInteresIsrActual - mInteresIsrNuevo;` | 🔴 MONEY/aritmética financiera |
| L95 | FÓRMULA | `LET mInteresNetoDiferencia = pInteresNetoActual - mInteresNetoNuevo;` | 🔴 MONEY/aritmética financiera |
| L107 | VALIDACIÓN_NULL | `IF iRegistro IS NULL THEN -- Se va a insertar el registro` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?tru` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pagare` | ENTIDAD | pagaré | 🔵 CONVENCIÓN | nombre_sp |
| `?_vto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tru`, `?ion`, `?_vto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardacambiosautorizadoscuentapm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardacambiosautorizadoscuentapm.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda cambio, OS — Originación de Solicitudes y cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardacambiosautorizadoscuentapm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cSecuencia` | `SMALLINT` | L9 |
| `cNumFirmas` | `CHAR(2)` | L10 |
| `cCombFirmas` | `CHAR(120)` | L11 |
| `cTipoFirma` | `CHAR(20)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_firmantes` | `bdicheq` | ⚠️ sí | SELECT | L49 |
| `sw_sc_firmantes` | `bdicnweb` | no | INSERT | L61 |
| `sq_catregimen` | `bdicntchq` | ⚠️ sí | SELECT | L66 |
| `sw_sq_catregimen` | `bdicnweb` | no | INSERT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | VALIDACIÓN_NULL | `IF cSecuencia IS NULL OR cSecuencia = '' THEN` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `autoriza` | ACCION | autoriza | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?d`, `?pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardacambiosdatosgeneralespm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardacambiosdatosgeneralespm.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 284 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda cambio, sd — saldo disponible y OS — Originación de Solicitudes (general, especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=2 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardacambiosdatosgeneralespm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iTotalReg` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_si_cliente` | `bdicnweb` | no | INSERT | L41 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L50 |
| `sw_si_ctepm` | `bdicnweb` | no | INSERT | L53 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L68 |
| `si_apoderado` | `bdinteg` | ⚠️ sí | SELECT | L74 |
| `sw_si_apoderado` | `bdicnweb` | no | INSERT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?at` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?at`, `?m` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardacambiosdatosgeneralespm2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardacambiosdatosgeneralespm2.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda cambio, sd — saldo disponible y OS — Originación de Solicitudes (general, especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=2 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardacambiosdatosgeneralespm2(
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iTotalReg` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_si_cliente` | `bdicnweb` | no | INSERT | L35 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L44 |
| `sw_si_ctepm` | `bdicnweb` | no | INSERT | L47 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L62 |
| `si_apoderado` | `bdinteg` | ⚠️ sí | SELECT | L68 |
| `sw_si_apoderado` | `bdicnweb` | no | INSERT | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00236';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?at` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?m2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?at`, `?m2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardacausaimpresionedocta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardacausaimpresionedocta.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda causa, Impago — pago vencido o fallido; confirmado: n_impagos_consec y Estado de Cuenta — documento periódico de movimientos y saldos; generado como PDF" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardacausaimpresionedocta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoCliente                   CHAR(20)
  pNoCuenta                    CHAR(20)
  pSistemaCuenta               CHAR(2)
  pIdMotivo                    INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoCliente` | `CHAR(20)` | — | — |
| `pNoCuenta` | `CHAR(20)` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |
| `pIdMotivo` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iRegs` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `kw_motivos_impresion_cfdi` | `bdicnweb` | no | INSERT | L33 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNoCliente = '' OR pNoCuenta = '' OR pSistemaCuenta = '' OR p` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `causa` | ENTIDAD | causa / motivo | 🔵 CONVENCIÓN | nombre_sp |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?resion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edocta` | ENTIDAD | Estado de Cuenta — documento periódico de movimientos y sald | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?resion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardactemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardactemoral.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ✅ fan_in=149 / fan_out=4 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `ctemoral` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardactemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfuncion                     CHAR(1)
  pnumcte                      CHAR(20)
  pstatuscte                   CHAR(2)
  psucursal                    CHAR(4)
  ptp_persona                  CHAR(2)
  ptp_cliente                  CHAR(1)
  prazon_social                CHAR(254)
  prfc                         CHAR(13)
  pfechaalta                   DATE
  pnacionalidad                CHAR(2)
  pnombrecorto                 CHAR(60)
  pnombrecontacto              CHAR(48)
  ptelefonocontacto            CHAR(13)
  psufijo                      CHAR(2)
  pgiro                        CHAR(20)
  pactividad_princ             CHAR(3)
  ppaginainternet              CHAR(30)
  pCURP                        CHAR(20)
  pRFCAlt                      CHAR(13)
  pRegimen                     CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfuncion` | `CHAR(1)` | — | — |
| `pnumcte` | `CHAR(20)` | — | — |
| `pstatuscte` | `CHAR(2)` | — | — |
| `psucursal` | `CHAR(4)` | — | — |
| `ptp_persona` | `CHAR(2)` | — | — |
| `ptp_cliente` | `CHAR(1)` | — | — |
| `prazon_social` | `CHAR(254)` | — | — |
| `prfc` | `CHAR(13)` | — | — |
| `pfechaalta` | `DATE` | — | — |
| `pnacionalidad` | `CHAR(2)` | — | — |
| `pnombrecorto` | `CHAR(60)` | — | — |
| `pnombrecontacto` | `CHAR(48)` | — | — |
| `ptelefonocontacto` | `CHAR(13)` | — | — |
| `psufijo` | `CHAR(2)` | — | — |
| `pgiro` | `CHAR(20)` | — | — |
| `pactividad_princ` | `CHAR(3)` | — | — |
| `ppaginainternet` | `CHAR(30)` | — | — |
| `pCURP` | `CHAR(20)` | — | — |
| `pRFCAlt` | `CHAR(13)` | — | — |
| `pRegimen` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `cCodRetSp` | `CHAR(5)` | L28 |
| `iCodRetSp` | `INTEGER` | L29 |
| `cEmpresa` | `CHAR(3)` | L30 |
| `cNumcte` | `CHAR(20)` | L31 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L80 |
| `ctemoral` | `bdinteg` | ⚠️ sí | L109 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00292';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00295';` |  |
| L73 | VALIDACIÓN_NULL | `IF psucursal IS NULL  OR ptp_persona IS NULL OR ptp_cliente IS NULL OR prfc IS NULL OR pactividad_pr` |  |
| L88 | FÓRMULA | `LET prazon_social = replace(prazon_social,"&+","\\");` |  |
| L89 | FÓRMULA | `LET pnombrecorto = replace(pnombrecorto,"&+","\\");` |  |
| L106 | FÓRMULA | `LET prazon_social = replace(prazon_social,"&.","-");` |  |
| L107 | FÓRMULA | `LET pnombrecorto = replace(pnombrecorto,"&.","-");` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00296';` |  |
| L120 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ctemoral';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00141';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00161';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00006';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00293';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00020';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_guardactemoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardactemoral2.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ✅ fan_in=33 / fan_out=59 |
| **Deps concatenadas** | 65 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `ctemoral2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardactemoral2(
  pUsuario                     CHAR(8)
  pfuncion                     CHAR(1)
  pnumcte                      CHAR(20)
  pstatuscte                   CHAR(2)
  psucursal                    CHAR(4)
  ptp_persona                  CHAR(2)
  ptp_cliente                  CHAR(1)
  prazon_social                CHAR(254)
  prfc                         CHAR(13)
  pfechaalta                   DATE
  pnacionalidad                CHAR(2)
  pnombrecorto                 CHAR(30)
  pnombrecontacto              CHAR(48)
  ptelefonocontacto            CHAR(13)
  psufijo                      CHAR(2)
  pgiro                        CHAR(20)
  pactividad_princ             CHAR(3)
  ppaginainternet              CHAR(30)
  pCURP                        CHAR(20)
  pRFCAlt                      CHAR(13)
  pRegimen                     CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pfuncion` | `CHAR(1)` | — | — |
| `pnumcte` | `CHAR(20)` | — | — |
| `pstatuscte` | `CHAR(2)` | — | — |
| `psucursal` | `CHAR(4)` | — | — |
| `ptp_persona` | `CHAR(2)` | — | — |
| `ptp_cliente` | `CHAR(1)` | — | — |
| `prazon_social` | `CHAR(254)` | — | — |
| `prfc` | `CHAR(13)` | — | — |
| `pfechaalta` | `DATE` | — | — |
| `pnacionalidad` | `CHAR(2)` | — | — |
| `pnombrecorto` | `CHAR(30)` | — | — |
| `pnombrecontacto` | `CHAR(48)` | — | — |
| `ptelefonocontacto` | `CHAR(13)` | — | — |
| `psufijo` | `CHAR(2)` | — | — |
| `pgiro` | `CHAR(20)` | — | — |
| `pactividad_princ` | `CHAR(3)` | — | — |
| `ppaginainternet` | `CHAR(30)` | — | — |
| `pCURP` | `CHAR(20)` | — | — |
| `pRFCAlt` | `CHAR(13)` | — | — |
| `pRegimen` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L25 |
| `iSqlErr` | `INTEGER` | L26 |
| `cCodRetSp` | `CHAR(5)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `cEmpresa` | `CHAR(3)` | L29 |
| `cNumcte` | `CHAR(20)` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `ctemoral2` | `bdinteg` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00292';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00295';` |  |
| L74 | VALIDACIÓN_NULL | `IF psucursal IS NULL  OR ptp_persona IS NULL OR ptp_cliente IS NULL OR prfc IS NULL OR pactividad_pr` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00296';` |  |
| L91 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ctemoral';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00141';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00161';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00006';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00293';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00020';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardacuentactemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardacuentactemoral.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda cuenta y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `cuenta1` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardacuentactemoral(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pProducto                    CHAR(4)
  pNumCte                      CHAR(20)
  pNumCot                      CHAR(2)
  pClaseCta                    CHAR(1)
  pRegFirmas                   CHAR(1)
  pTipoBca                     CHAR(3)
  pEjecutivo                   CHAR(8)
  pEnvioDirecc                 CHAR(1)
  pCuenta                      CHAR(20)
  pDireccEnvio                 SMALLINT
  pCliente2                    CHAR(20)
  pNombre                      CHAR(50)
  pInstcap                     CHAR(2)
  pCuentaCap                   CHAR(20)
  pInstInt                     CHAR(2)
  pCuentaInt                   CHAR(20)
  pPlazo                       SMALLINT
  pCobraISr                    CHAR(1)
  pProcedAperturaCta           CHAR(2)
  pProcedMantenerCta           CHAR(2)
  pMontoMensual                CHAR(2)
  pDepositosCantidad           CHAR(2)
  pDepositoMonto               CHAR(2)
  pRetirosCantidad             CHAR(2)
  pRetirosMonto                CHAR(2)
  pFormaApert                  CHAR(2)
  pMtoApertura                 MONEY(14,2)
) RETURNING CHAR(5) AS cCodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCot` | `CHAR(2)` | — | — |
| `pClaseCta` | `CHAR(1)` | — | — |
| `pRegFirmas` | `CHAR(1)` | — | — |
| `pTipoBca` | `CHAR(3)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pEnvioDirecc` | `CHAR(1)` | — | — |
| `pCuenta` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pDireccEnvio` | `SMALLINT` | — | — |
| `pCliente2` | `CHAR(20)` | — | — |
| `pNombre` | `CHAR(50)` | — | — |
| `pInstcap` | `CHAR(2)` | — | — |
| `pCuentaCap` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pInstInt` | `CHAR(2)` | — | — |
| `pCuentaInt` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pPlazo` | `SMALLINT` | — | — |
| `pCobraISr` | `CHAR(1)` | — | — |
| `pProcedAperturaCta` | `CHAR(2)` | — | — |
| `pProcedMantenerCta` | `CHAR(2)` | — | — |
| `pMontoMensual` | `CHAR(2)` | — | — |
| `pDepositosCantidad` | `CHAR(2)` | — | — |
| `pDepositoMonto` | `CHAR(2)` | — | — |
| `pRetirosCantidad` | `CHAR(2)` | — | — |
| `pRetirosMonto` | `CHAR(2)` | — | — |
| `pFormaApert` | `CHAR(2)` | — | — |
| `pMtoApertura` | `MONEY(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `cCtaClabe` | `CHAR(18)` | L13 |
| `cCuenta` | `CHAR(20)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `cuenta1` | `bdicheq` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

---

## `sp_guardacuentactemoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardacuentactemoral2.sql` |
| **LOC (1er CREATE)** | 162 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 117 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda cuenta y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `cuenta1_2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardacuentactemoral2(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pProducto                    CHAR(4)
  pNumCte                      CHAR(20)
  pNumCot                      CHAR(2)
  pClaseCta                    CHAR(1)
  pRegFirmas                   CHAR(1)
  pTipoBca                     CHAR(3)
  pEjecutivo                   CHAR(8)
  pEnvioDirecc                 CHAR(1)
  pCuenta                      CHAR(20)
  pDireccEnvio                 SMALLINT
  pCliente2                    CHAR(20)
  pNombre                      CHAR(50)
  pInstcap                     CHAR(2)
  pCuentaCap                   CHAR(20)
  pInstInt                     CHAR(2)
  pCuentaInt                   CHAR(20)
  pPlazo                       SMALLINT
  pCobraISr                    CHAR(1)
  pProcedAperturaCta           CHAR(2)
  pProcedMantenerCta           CHAR(2)
  pMontoMensual                CHAR(2)
  pDepositosCantidad           CHAR(2)
  pDepositoMonto               CHAR(2)
  pRetirosCantidad             CHAR(2)
  pRetirosMonto                CHAR(2)
  pFormaApert                  CHAR(2)
  pMtoApertura                 MONEY(14,2)
) RETURNING CHAR(5) AS cCodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCot` | `CHAR(2)` | — | — |
| `pClaseCta` | `CHAR(1)` | — | — |
| `pRegFirmas` | `CHAR(1)` | — | — |
| `pTipoBca` | `CHAR(3)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pEnvioDirecc` | `CHAR(1)` | — | — |
| `pCuenta` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pDireccEnvio` | `SMALLINT` | — | — |
| `pCliente2` | `CHAR(20)` | — | — |
| `pNombre` | `CHAR(50)` | — | — |
| `pInstcap` | `CHAR(2)` | — | — |
| `pCuentaCap` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pInstInt` | `CHAR(2)` | — | — |
| `pCuentaInt` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pPlazo` | `SMALLINT` | — | — |
| `pCobraISr` | `CHAR(1)` | — | — |
| `pProcedAperturaCta` | `CHAR(2)` | — | — |
| `pProcedMantenerCta` | `CHAR(2)` | — | — |
| `pMontoMensual` | `CHAR(2)` | — | — |
| `pDepositosCantidad` | `CHAR(2)` | — | — |
| `pDepositoMonto` | `CHAR(2)` | — | — |
| `pRetirosCantidad` | `CHAR(2)` | — | — |
| `pRetirosMonto` | `CHAR(2)` | — | — |
| `pFormaApert` | `CHAR(2)` | — | — |
| `pMtoApertura` | `MONEY(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `cCtaClabe` | `CHAR(18)` | L13 |
| `cCuenta` | `CHAR(20)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `cuenta1_2` | `bdicheq` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardacuentactemoral3`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardacuentactemoral3.sql` |
| **LOC (1er CREATE)** | 152 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda cuenta y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `cuenta1_2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardacuentactemoral3(
  pUsuario                     CHAR(8)
  pSucursal                    CHAR(4)
  pProducto                    CHAR(4)
  pNumCte                      CHAR(20)
  pNumCot                      CHAR(2)
  pClaseCta                    CHAR(1)
  pRegFirmas                   CHAR(1)
  pTipoBca                     CHAR(3)
  pEjecutivo                   CHAR(8)
  pEnvioDirecc                 CHAR(1)
  pCuenta                      CHAR(20)
  pDireccEnvio                 SMALLINT
  pCliente2                    CHAR(20)
  pNombre                      CHAR(50)
  pInstcap                     CHAR(2)
  pCuentaCap                   CHAR(20)
  pInstInt                     CHAR(2)
  pCuentaInt                   CHAR(20)
  pPlazo                       SMALLINT
  pCobraISr                    CHAR(1)
  pProcedAperturaCta           CHAR(2)
  pProcedMantenerCta           CHAR(2)
  pMontoMensual                CHAR(2)
  pDepositosCantidad           CHAR(2)
  pDepositoMonto               CHAR(2)
  pRetirosCantidad             CHAR(2)
  pRetirosMonto                CHAR(2)
  pFormaApert                  CHAR(2)
  pMtoApertura                 MONEY(14,2)
) RETURNING CHAR(5) AS cCodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCot` | `CHAR(2)` | — | — |
| `pClaseCta` | `CHAR(1)` | — | — |
| `pRegFirmas` | `CHAR(1)` | — | — |
| `pTipoBca` | `CHAR(3)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pEnvioDirecc` | `CHAR(1)` | — | — |
| `pCuenta` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pDireccEnvio` | `SMALLINT` | — | — |
| `pCliente2` | `CHAR(20)` | — | — |
| `pNombre` | `CHAR(50)` | — | — |
| `pInstcap` | `CHAR(2)` | — | — |
| `pCuentaCap` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pInstInt` | `CHAR(2)` | — | — |
| `pCuentaInt` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pPlazo` | `SMALLINT` | — | — |
| `pCobraISr` | `CHAR(1)` | — | — |
| `pProcedAperturaCta` | `CHAR(2)` | — | — |
| `pProcedMantenerCta` | `CHAR(2)` | — | — |
| `pMontoMensual` | `CHAR(2)` | — | — |
| `pDepositosCantidad` | `CHAR(2)` | — | — |
| `pDepositoMonto` | `CHAR(2)` | — | — |
| `pRetirosCantidad` | `CHAR(2)` | — | — |
| `pRetirosMonto` | `CHAR(2)` | — | — |
| `pFormaApert` | `CHAR(2)` | — | — |
| `pMtoApertura` | `MONEY(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `cCtaClabe` | `CHAR(18)` | L13 |
| `cCuenta` | `CHAR(20)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `cuenta1_2` | `bdicheq` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?3` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?3` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardadatoslegalesctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardadatoslegalesctemoral.sql` |
| **LOC (1er CREATE)** | 176 |
| **Callgraph** | ✅ fan_in=149 / fan_out=16 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda datos y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardadatoslegalesctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pEscConstitu                 CHAR(30)
  pNombNotario                 CHAR(30)
  pNumNotaria                  CHAR(5)
  pCdNotaria                   CHAR(30)
  pFecInscrip                  DATE
  pFecConstitu                 DATE
  pNumFolMerca                 CHAR(30)
  pCdFolMerca                  CHAR(30)
  pEscriPoder                  CHAR(30)
  pNombNotpd                   CHAR(30)
  pNumNotariopd                CHAR(30)
  pCdNotariopd                 CHAR(30)
  pFecInscripd                 DATE
  pFecEscritupd                DATE
  pFolMercapd                  CHAR(30)
  pCdFolMercaPd                CHAR(30)
  pNomSociedad                 CHAR(30)
  pEmail                       CHAR(100)
  pSat_fea                     CHAR(25)
  pDoc_legal                   CHAR(100)
  pTpo_Poder                   CHAR(3)
  pTpo_Admin                   CHAR(3)
  pTpo_Org                     CHAR(3)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pEscConstitu` | `CHAR(30)` | — | — |
| `pNombNotario` | `CHAR(30)` | — | — |
| `pNumNotaria` | `CHAR(5)` | — | — |
| `pCdNotaria` | `CHAR(30)` | — | — |
| `pFecInscrip` | `DATE` | — | — |
| `pFecConstitu` | `DATE` | — | — |
| `pNumFolMerca` | `CHAR(30)` | — | — |
| `pCdFolMerca` | `CHAR(30)` | — | — |
| `pEscriPoder` | `CHAR(30)` | — | — |
| `pNombNotpd` | `CHAR(30)` | — | — |
| `pNumNotariopd` | `CHAR(30)` | — | — |
| `pCdNotariopd` | `CHAR(30)` | — | — |
| `pFecInscripd` | `DATE` | — | — |
| `pFecEscritupd` | `DATE` | — | — |
| `pFolMercapd` | `CHAR(30)` | — | — |
| `pCdFolMercaPd` | `CHAR(30)` | — | — |
| `pNomSociedad` | `CHAR(30)` | — | — |
| `pEmail` | `CHAR(100)` | — | — |
| `pSat_fea` | `CHAR(25)` | — | — |
| `pDoc_legal` | `CHAR(100)` | — | — |
| `pTpo_Poder` | `CHAR(3)` | — | — |
| `pTpo_Admin` | `CHAR(3)` | — | — |
| `pTpo_Org` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L29 |
| `iSqlErr` | `INTEGER` | L30 |
| `cCodRetSp` | `CHAR(5)` | L31 |
| `iCodRetSp` | `INTEGER` | L32 |
| `cEmpresa` | `CHAR(3)` | L33 |
| `bInTrasaction` | `BOOLEAN` | L34 |
| `pTipoCorreo` | `CHAR(1)` | L35 |
| `pCanal` | `CHAR(1)` | L36 |
| `cExisteEmail` | `INTEGER` | L37 |
| `cNumCteApoder` | `CHAR(20)` | L38 |
| `cNomApoder` | `CHAR(60)` | L39 |
| `iExisteCorreo` | `INTEGER` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_apoderado` | `bdinteg` | ⚠️ sí | SELECT | L83 |
| `si_correos` | `bdinteg` | ⚠️ sí | SELECT | L109 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L75 |
| `ctemoraldatoslegales` | `bdinteg` | ⚠️ sí | L90 |
| `sp_registra_correos` | `bdinteg` | ⚠️ sí | L138 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ctemoraldatoslegales';` |  |
| L106 | VALIDACIÓN_NULL | `IF (pEmail = '' OR pEmail IS NULL) THEN` |  |
| L143 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_registra_correos';` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L150 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00866';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `?legales` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?legales` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardadireccionesctemoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardadireccionesctemoral2.sql` |
| **LOC (1er CREATE)** | 151 |
| **Callgraph** | ✅ fan_in=33 / fan_out=20 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda direcciones y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `direcciones_ctemoral` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardadireccionesctemoral2(
  pUsuario                     CHAR(8)
  pfuncion                     CHAR(1)
  pnumcte                      CHAR(20)
  psecuencia                   SMALLINT
  ptipodir                     CHAR(1)
  pcalle                       CHAR(40)
  pcolonia                     CHAR(60)
  pmunicipio                   CHAR(5)
  pentre_calles                CHAR(40)
  ppais                        CHAR(3)
  ptipotel1                    CHAR(1)
  ptelefono1                   CHAR(13)
  ptipotel2                    CHAR(1)
  ptelefono2                   CHAR(13)
  ptipotel3                    CHAR(1)
  ptelefono3                   CHAR(13)
  pextension                   CHAR(5)
  pestado_inegi                CHAR(2)
  pmunicipio_inegi             CHAR(3)
  plocalidad_inegi             CHAR(4)
  pnociudad                    SMALLINT
  pnoint                       CHAR(10)
  pdepto                       CHAR(6)
  pnocalle                     INTEGER
  pnocolonia                   INTEGER
  ppuntocar                    CHAR(1)
  punihabi                     CHAR(1)
  pmanz                        SMALLINT
  ppotros                      SMALLINT
  pandador                     SMALLINT
  petapa                       SMALLINT
  plote                        SMALLINT
  pedif                        SMALLINT
  pentrada                     SMALLINT
  pobserva                     CHAR(80)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pfuncion` | `CHAR(1)` | — | — |
| `pnumcte` | `CHAR(20)` | — | — |
| `psecuencia` | `SMALLINT` | — | — |
| `ptipodir` | `CHAR(1)` | — | — |
| `pcalle` | `CHAR(40)` | — | — |
| `pcolonia` | `CHAR(60)` | — | — |
| `pmunicipio` | `CHAR(5)` | — | — |
| `pentre_calles` | `CHAR(40)` | — | — |
| `ppais` | `CHAR(3)` | — | — |
| `ptipotel1` | `CHAR(1)` | — | — |
| `ptelefono1` | `CHAR(13)` | — | — |
| `ptipotel2` | `CHAR(1)` | — | — |
| `ptelefono2` | `CHAR(13)` | — | — |
| `ptipotel3` | `CHAR(1)` | — | — |
| `ptelefono3` | `CHAR(13)` | — | — |
| `pextension` | `CHAR(5)` | — | — |
| `pestado_inegi` | `CHAR(2)` | — | — |
| `pmunicipio_inegi` | `CHAR(3)` | — | — |
| `plocalidad_inegi` | `CHAR(4)` | — | — |
| `pnociudad` | `SMALLINT` | — | — |
| `pnoint` | `CHAR(10)` | — | — |
| `pdepto` | `CHAR(6)` | — | — |
| `pnocalle` | `INTEGER` | — | — |
| `pnocolonia` | `INTEGER` | — | — |
| `ppuntocar` | `CHAR(1)` | — | — |
| `punihabi` | `CHAR(1)` | — | — |
| `pmanz` | `SMALLINT` | — | — |
| `ppotros` | `SMALLINT` | — | — |
| `pandador` | `SMALLINT` | — | — |
| `petapa` | `SMALLINT` | — | — |
| `plote` | `SMALLINT` | — | — |
| `pedif` | `SMALLINT` | — | — |
| `pentrada` | `SMALLINT` | — | — |
| `pobserva` | `CHAR(80)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L43 |
| `iSqlErr` | `INTEGER` | L44 |
| `cCodRetSp` | `CHAR(5)` | L45 |
| `iCodRetSp` | `INTEGER` | L46 |
| `cEmpresa` | `CHAR(3)` | L47 |
| `v_ciudad_coppel` | `smallint` | L48 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ciudades` | `bdinteg` | ⚠️ sí | SELECT | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `direcciones_ctemoral` | `bdinteg` | ⚠️ sí | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00292';` |  |
| L131 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP direcciones_ctemoral';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00022';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00294';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `direcciones` | ENTIDAD | direcciones | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardadireccionescteprospecto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardadireccionescteprospecto.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda direcciones, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardadireccionescteprospecto(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pFuncion                     CHAR(1)
  pNumCte                      CHAR(20)
  pSecuencia                   SMALLINT
  pTipoDir                     CHAR(1)
  pCalle                       CHAR(40)
  pColonia                     CHAR(60)
  pMunicipio                   CHAR(5)
  pEntre_Calles                CHAR(40)
  pPais                        CHAR(3)
  pEntidad                     CHAR(2)
  pLocalidad                   CHAR(3)
  pCodPostal                   CHAR(5)
  pTipoTel1                    CHAR(1)
  pTelefono1                   CHAR(13)
  pTipoTel2                    CHAR(1)
  pTelefono2                   CHAR(13)
  pTipoTel3                    CHAR(1)
  pTelefono3                   CHAR(13)
  pExtension                   CHAR(5)
  pEstado_Inegi                CHAR(2)
  pMunicipio_Inegi             CHAR(3)
  pLocalidad_Inegi             CHAR(4)
  pNoCiudad                    SMALLINT
  pNoExt                       CHAR(10)
  pNoInt                       CHAR(10)
  pDepto                       CHAR(6)
  pNoCalle                     INTEGER
  pNoColonia                   INTEGER
  pPuntoCar                    CHAR(1)
  pUniHabi                     CHAR(1)
  pManz                        SMALLINT
  pPOtros                      SMALLINT
  pAndador                     SMALLINT
  pEtapa                       SMALLINT
  pLote                        SMALLINT
  pEdif                        SMALLINT
  pEntrada                     SMALLINT
  pObserva                     CHAR(80)
  pUser_Insert                 CHAR(8)
  pFecha_Insert                DATE
  cSucursal                    CHAR(4)
) RETURNING CHAR(5) AS cCodigoRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pFuncion` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pSecuencia` | `SMALLINT` | — | — |
| `pTipoDir` | `CHAR(1)` | — | — |
| `pCalle` | `CHAR(40)` | — | — |
| `pColonia` | `CHAR(60)` | — | — |
| `pMunicipio` | `CHAR(5)` | — | — |
| `pEntre_Calles` | `CHAR(40)` | — | — |
| `pPais` | `CHAR(3)` | — | — |
| `pEntidad` | `CHAR(2)` | — | — |
| `pLocalidad` | `CHAR(3)` | — | — |
| `pCodPostal` | `CHAR(5)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pTipoTel1` | `CHAR(1)` | — | — |
| `pTelefono1` | `CHAR(13)` | — | — |
| `pTipoTel2` | `CHAR(1)` | — | — |
| `pTelefono2` | `CHAR(13)` | — | — |
| `pTipoTel3` | `CHAR(1)` | — | — |
| `pTelefono3` | `CHAR(13)` | — | — |
| `pExtension` | `CHAR(5)` | — | — |
| `pEstado_Inegi` | `CHAR(2)` | — | — |
| `pMunicipio_Inegi` | `CHAR(3)` | — | — |
| `pLocalidad_Inegi` | `CHAR(4)` | — | — |
| `pNoCiudad` | `SMALLINT` | — | — |
| `pNoExt` | `CHAR(10)` | — | — |
| `pNoInt` | `CHAR(10)` | — | — |
| `pDepto` | `CHAR(6)` | — | — |
| `pNoCalle` | `INTEGER` | — | — |
| `pNoColonia` | `INTEGER` | — | — |
| `pPuntoCar` | `CHAR(1)` | — | — |
| `pUniHabi` | `CHAR(1)` | — | — |
| `pManz` | `SMALLINT` | — | — |
| `pPOtros` | `SMALLINT` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pAndador` | `SMALLINT` | — | — |
| `pEtapa` | `SMALLINT` | — | — |
| `pLote` | `SMALLINT` | — | — |
| `pEdif` | `SMALLINT` | — | — |
| `pEntrada` | `SMALLINT` | — | — |
| `pObserva` | `CHAR(80)` | — | — |
| `pUser_Insert` | `CHAR(8)` | — | — |
| `pFecha_Insert` | `DATE` | — | — |
| `cSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L48 |
| `cCodRet` | `CHAR(5)` | L49 |
| `cCodRetSp` | `CHAR(5)` | L50 |
| `iCarrier` | `INTEGER` | L51 |
| `v_ciudad_coppel` | `char (4)` | L52 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ciudades` | `bdinteg` | ⚠️ sí | SELECT | L84 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `direcciones` | `bdinteg` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `direcciones` | ENTIDAD | direcciones | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardadireccionescteprospecto2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardadireccionescteprospecto2.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda direcciones, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardadireccionescteprospecto2(
  pFuncion                     CHAR(1)
  pNumCte                      CHAR(20)
  pSecuencia                   SMALLINT
  pTipoDir                     CHAR(1)
  pCalle                       CHAR(40)
  pColonia                     CHAR(60)
  pMunicipio                   CHAR(5)
  pEntre_Calles                CHAR(40)
  pPais                        CHAR(3)
  pEntidad                     CHAR(2)
  pLocalidad                   CHAR(3)
  pCodPostal                   CHAR(5)
  pTipoTel1                    CHAR(1)
  pTelefono1                   CHAR(13)
  pTipoTel2                    CHAR(1)
  pTelefono2                   CHAR(13)
  pTipoTel3                    CHAR(1)
  pTelefono3                   CHAR(13)
  pExtension                   CHAR(5)
  pEstado_Inegi                CHAR(2)
  pMunicipio_Inegi             CHAR(3)
  pLocalidad_Inegi             CHAR(4)
  pNoCiudad                    SMALLINT
  pNoExt                       CHAR(10)
  pNoInt                       CHAR(10)
  pDepto                       CHAR(6)
  pNoCalle                     INTEGER
  pNoColonia                   INTEGER
  pPuntoCar                    CHAR(1)
  pUniHabi                     CHAR(1)
  pManz                        SMALLINT
  pPOtros                      SMALLINT
  pAndador                     SMALLINT
  pEtapa                       SMALLINT
  pLote                        SMALLINT
  pEdif                        SMALLINT
  pEntrada                     SMALLINT
  pObserva                     CHAR(80)
  pUser_Insert                 CHAR(8)
  pFecha_Insert                DATE
  cSucursal                    CHAR(4)
) RETURNING CHAR(5) AS cCodigoRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFuncion` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pSecuencia` | `SMALLINT` | — | — |
| `pTipoDir` | `CHAR(1)` | — | — |
| `pCalle` | `CHAR(40)` | — | — |
| `pColonia` | `CHAR(60)` | — | — |
| `pMunicipio` | `CHAR(5)` | — | — |
| `pEntre_Calles` | `CHAR(40)` | — | — |
| `pPais` | `CHAR(3)` | — | — |
| `pEntidad` | `CHAR(2)` | — | — |
| `pLocalidad` | `CHAR(3)` | — | — |
| `pCodPostal` | `CHAR(5)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pTipoTel1` | `CHAR(1)` | — | — |
| `pTelefono1` | `CHAR(13)` | — | — |
| `pTipoTel2` | `CHAR(1)` | — | — |
| `pTelefono2` | `CHAR(13)` | — | — |
| `pTipoTel3` | `CHAR(1)` | — | — |
| `pTelefono3` | `CHAR(13)` | — | — |
| `pExtension` | `CHAR(5)` | — | — |
| `pEstado_Inegi` | `CHAR(2)` | — | — |
| `pMunicipio_Inegi` | `CHAR(3)` | — | — |
| `pLocalidad_Inegi` | `CHAR(4)` | — | — |
| `pNoCiudad` | `SMALLINT` | — | — |
| `pNoExt` | `CHAR(10)` | — | — |
| `pNoInt` | `CHAR(10)` | — | — |
| `pDepto` | `CHAR(6)` | — | — |
| `pNoCalle` | `INTEGER` | — | — |
| `pNoColonia` | `INTEGER` | — | — |
| `pPuntoCar` | `CHAR(1)` | — | — |
| `pUniHabi` | `CHAR(1)` | — | — |
| `pManz` | `SMALLINT` | — | — |
| `pPOtros` | `SMALLINT` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pAndador` | `SMALLINT` | — | — |
| `pEtapa` | `SMALLINT` | — | — |
| `pLote` | `SMALLINT` | — | — |
| `pEdif` | `SMALLINT` | — | — |
| `pEntrada` | `SMALLINT` | — | — |
| `pObserva` | `CHAR(80)` | — | — |
| `pUser_Insert` | `CHAR(8)` | — | — |
| `pFecha_Insert` | `DATE` | — | — |
| `cSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L45 |
| `cCodRet` | `CHAR(5)` | L46 |
| `cCodRetSp` | `CHAR(5)` | L47 |
| `iCarrier` | `INTEGER` | L48 |
| `v_ciudad_coppel` | `char (4)` | L49 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ciudades` | `bdinteg` | ⚠️ sí | SELECT | L80 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `direcciones` | `bdinteg` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `direcciones` | ENTIDAD | direcciones | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardafirmantessif`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardafirmantessif.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda ss — subsistema (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_firmantessif` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=4 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardafirmantessif(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pSecuencia                   SMALLINT
  pNumcte                      CHAR(20)
  pApellidos                   CHAR(30)
  pNombre                      CHAR(30)
  pRegFirma                    CHAR(1)
  pTipoFirma                   CHAR(1)
  pCombinacion                 CHAR(120)
  pParentesco                  CHAR(2)
) RETURNING CHAR(5) AS cCodigoRetorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pSecuencia` | `SMALLINT` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pApellidos` | `CHAR(30)` | — | — |
| `pNombre` | `CHAR(30)` | — | — |
| `pRegFirma` | `CHAR(1)` | — | — |
| `pTipoFirma` | `CHAR(1)` | — | — |
| `pCombinacion` | `CHAR(120)` | — | — |
| `pParentesco` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(3)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_firmantessif` | `bdicheq` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?firm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?if` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?firm`, `?e`, `ss`, `?if` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardafirmantessif2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardafirmantessif2.sql` |
| **LOC (1er CREATE)** | 47 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda ss — subsistema (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_firmantessif` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=4 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardafirmantessif2(
  pCuenta                      CHAR(20)
  pSecuencia                   SMALLINT
  pNumcte                      CHAR(20)
  pApellidos                   CHAR(30)
  pNombre                      CHAR(30)
  pRegFirma                    CHAR(1)
  pTipoFirma                   CHAR(1)
  pCombinacion                 CHAR(120)
  pParentesco                  CHAR(2)
) RETURNING CHAR(5) AS cCodigoRetorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCuenta` | `CHAR(20)` | — | — |
| `pSecuencia` | `SMALLINT` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pApellidos` | `CHAR(30)` | — | — |
| `pNombre` | `CHAR(30)` | — | — |
| `pRegFirma` | `CHAR(1)` | — | — |
| `pTipoFirma` | `CHAR(1)` | — | — |
| `pCombinacion` | `CHAR(120)` | — | — |
| `pParentesco` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(3)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_firmantessif` | `bdicheq` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?firm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?if2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?firm`, `?e`, `ss`, `?if2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardafusioncte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardafusioncte.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda cliente" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardafusioncte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte1                     CHAR(20)
  pIdRegistro                  INTEGER
  pCteCorrecto                 CHAR(1)
  pEstadoFusion                CHAR(1)
  pCausaNoFus                  CHAR(2)
  pEstatusAsig                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumcte1` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pIdRegistro` | `INTEGER` | — | — |
| `pCteCorrecto` | `CHAR(1)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pEstadoFusion` | `CHAR(1)` | `fusion`=fusiona cuentas | ✅ CÓDIGO |
| `pCausaNoFus` | `CHAR(2)` | — | — |
| `pEstatusAsig` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistrosRecuperados` | `INTEGER` | L7 |
| `iNoRegistrosProcesados` | `INTEGER` | L8 |
| `iRowId` | `INTEGER` | L9 |
| `cNumcte_2` | `CHAR(20)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_clientesduplicados` | `bdicnweb` | no | UPDATE | L43 |
| `sw_tr_clientesduplicados` | `bdicnweb` | no | SELECT | L58 |
| `si_fusion_solic` | `bdinteg` | ⚠️ sí | INSERT | L61 |
| `sw_tr_clientesasignados_soc` | `bdicnweb` | no | UPDATE | L75 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumcte1 ='' OR pIdRegistro IS NULL OR pEstadoFusion ='' OR p` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `fusion` | ACCION | fusiona cuentas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_guardainfoctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardainfoctemoral.sql` |
| **LOC (1er CREATE)** | 284 |
| **Callgraph** | ✅ fan_in=0 / fan_out=68 |
| **Deps concatenadas** | 72 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda información y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardainfoctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfuncion                     CHAR(1)
  pnumcte                      CHAR(20)
  pstatuscte                   CHAR(2)
  psucursal                    CHAR(4)
  ptp_persona                  CHAR(2)
  ptp_cliente                  CHAR(1)
  prazon_social                CHAR(254)
  prfc                         CHAR(13)
  pfechaalta                   DATE
  pnacionalidad                CHAR(2)
  pnombrecorto                 CHAR(60)
  pnombrecontacto              CHAR(48)
  ptelefonocontacto            CHAR(13)
  psufijo                      CHAR(2)
  pgiro                        CHAR(20)
  pactividad_princ             CHAR(3)
  ppaginainternet              CHAR(30)
  psecuenciadirecciones        SMALLINT
  ptipodir                     CHAR(1)
  pcalle                       CHAR(40)
  pcolonia                     CHAR(60)
  pmunicipio                   CHAR(5)
  pentre_calles                CHAR(40)
  ppais                        CHAR(3)
  pentidad                     CHAR(2)
  plocalidad                   CHAR(3)
  pcodpostal                   CHAR(5)
  ptipotel1                    CHAR(1)
  ptelefono1                   CHAR(13)
  ptipotel2                    CHAR(1)
  ptelefono2                   CHAR(13)
  ptipotel3                    CHAR(1)
  ptelefono3                   CHAR(13)
  pextension                   CHAR(5)
  pestado_inegi                CHAR(2)
  pmunicipio_inegi             CHAR(3)
  plocalidad_inegi             CHAR(4)
  pnociudad                    SMALLINT
  pnoext                       CHAR(10)
  pnoint                       CHAR(10)
  pdepto                       CHAR(6)
  pnocalle                     INTEGER
  pnocolonia                   INTEGER
  ppuntocar                    CHAR(1)
  punihabi                     CHAR(1)
  pmanz                        SMALLINT
  ppotros                      SMALLINT
  pandador                     SMALLINT
  petapa                       SMALLINT
  plote                        SMALLINT
  pedif                        SMALLINT
  pentrada                     SMALLINT
  pobserva                     CHAR(80)
  psecuenciaapoderados         INTEGER
  pNumCteApode                 CHAR(20)
  pNomApodera                  CHAR(60)
  pFecha                       DATE
  pEscConstitu                 CHAR(30)
  pNombNotario                 CHAR(30)
  pNumNotaria                  CHAR(5)
  pCdNotaria                   CHAR(30)
  pFecInscrip                  DATE
  pFecConstitu                 DATE
  pNumFolMerca                 CHAR(30)
  pCdFolMerca                  CHAR(30)
  pEscriPoder                  CHAR(30)
  pNombNotpd                   CHAR(30)
  pNumNotariopd                CHAR(30)
  pCdNotariopd                 CHAR(30)
  pFecInscripd                 DATE
  pFecEscritupd                DATE
  pFolMercapd                  CHAR(30)
  pCdFolMercaPd                CHAR(30)
  pNomSociedad                 CHAR(30)
  pEmail                       CHAR(100)
  pSat_fea                     CHAR(25)
  pDoc_legal                   CHAR(100)
  pTpo_Poder                   CHAR(3)
  pTpo_Admin                   CHAR(3)
  pTpo_Org                     CHAR(3)
  pCURP                        CHAR(20)
  pRFCAlt                      CHAR(13)
  pRegimen                     CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfuncion` | `CHAR(1)` | — | — |
| `pnumcte` | `CHAR(20)` | — | — |
| `pstatuscte` | `CHAR(2)` | — | — |
| `psucursal` | `CHAR(4)` | — | — |
| `ptp_persona` | `CHAR(2)` | — | — |
| `ptp_cliente` | `CHAR(1)` | — | — |
| `prazon_social` | `CHAR(254)` | — | — |
| `prfc` | `CHAR(13)` | — | — |
| `pfechaalta` | `DATE` | — | — |
| `pnacionalidad` | `CHAR(2)` | — | — |
| `pnombrecorto` | `CHAR(60)` | — | — |
| `pnombrecontacto` | `CHAR(48)` | — | — |
| `ptelefonocontacto` | `CHAR(13)` | — | — |
| `psufijo` | `CHAR(2)` | — | — |
| `pgiro` | `CHAR(20)` | — | — |
| `pactividad_princ` | `CHAR(3)` | — | — |
| `ppaginainternet` | `CHAR(30)` | — | — |
| `psecuenciadirecciones` | `SMALLINT` | — | — |
| `ptipodir` | `CHAR(1)` | — | — |
| `pcalle` | `CHAR(40)` | — | — |
| `pcolonia` | `CHAR(60)` | — | — |
| `pmunicipio` | `CHAR(5)` | — | — |
| `pentre_calles` | `CHAR(40)` | — | — |
| `ppais` | `CHAR(3)` | — | — |
| `pentidad` | `CHAR(2)` | — | — |
| `plocalidad` | `CHAR(3)` | — | — |
| `pcodpostal` | `CHAR(5)` | — | — |
| `ptipotel1` | `CHAR(1)` | — | — |
| `ptelefono1` | `CHAR(13)` | — | — |
| `ptipotel2` | `CHAR(1)` | — | — |
| `ptelefono2` | `CHAR(13)` | — | — |
| `ptipotel3` | `CHAR(1)` | — | — |
| `ptelefono3` | `CHAR(13)` | — | — |
| `pextension` | `CHAR(5)` | — | — |
| `pestado_inegi` | `CHAR(2)` | — | — |
| `pmunicipio_inegi` | `CHAR(3)` | — | — |
| `plocalidad_inegi` | `CHAR(4)` | — | — |
| `pnociudad` | `SMALLINT` | — | — |
| `pnoext` | `CHAR(10)` | — | — |
| `pnoint` | `CHAR(10)` | — | — |
| `pdepto` | `CHAR(6)` | — | — |
| `pnocalle` | `INTEGER` | — | — |
| `pnocolonia` | `INTEGER` | — | — |
| `ppuntocar` | `CHAR(1)` | — | — |
| `punihabi` | `CHAR(1)` | — | — |
| `pmanz` | `SMALLINT` | — | — |
| `ppotros` | `SMALLINT` | — | — |
| `pandador` | `SMALLINT` | — | — |
| `petapa` | `SMALLINT` | — | — |
| `plote` | `SMALLINT` | — | — |
| `pedif` | `SMALLINT` | — | — |
| `pentrada` | `SMALLINT` | — | — |
| `pobserva` | `CHAR(80)` | — | — |
| `psecuenciaapoderados` | `INTEGER` | — | — |
| `pNumCteApode` | `CHAR(20)` | — | — |
| `pNomApodera` | `CHAR(60)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pEscConstitu` | `CHAR(30)` | — | — |
| `pNombNotario` | `CHAR(30)` | — | — |
| `pNumNotaria` | `CHAR(5)` | — | — |
| `pCdNotaria` | `CHAR(30)` | — | — |
| `pFecInscrip` | `DATE` | — | — |
| `pFecConstitu` | `DATE` | — | — |
| `pNumFolMerca` | `CHAR(30)` | — | — |
| `pCdFolMerca` | `CHAR(30)` | — | — |
| `pEscriPoder` | `CHAR(30)` | — | — |
| `pNombNotpd` | `CHAR(30)` | — | — |
| `pNumNotariopd` | `CHAR(30)` | — | — |
| `pCdNotariopd` | `CHAR(30)` | — | — |
| `pFecInscripd` | `DATE` | — | — |
| `pFecEscritupd` | `DATE` | — | — |
| `pFolMercapd` | `CHAR(30)` | — | — |
| `pCdFolMercaPd` | `CHAR(30)` | — | — |
| `pNomSociedad` | `CHAR(30)` | — | — |
| `pEmail` | `CHAR(100)` | — | — |
| `pSat_fea` | `CHAR(25)` | — | — |
| `pDoc_legal` | `CHAR(100)` | — | — |
| `pTpo_Poder` | `CHAR(3)` | — | — |
| `pTpo_Admin` | `CHAR(3)` | — | — |
| `pTpo_Org` | `CHAR(3)` | — | — |
| `pCURP` | `CHAR(20)` | — | — |
| `pRFCAlt` | `CHAR(13)` | — | — |
| `pRegimen` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L89 |
| `iSqlErr` | `INTEGER` | L90 |
| `cCodRetSp` | `CHAR(5)` | L91 |
| `iCodRetSp` | `INTEGER` | L92 |
| `cEmpresa` | `CHAR(3)` | L93 |
| `cNumcte` | `CHAR(20)` | L94 |
| `bInTrasaction` | `BOOLEAN` | L95 |
| `cCodRetLN` | `CHAR(6)` | L97 |
| `sFolio` | `CHAR(12)` | L98 |
| `sNumcte` | `CHAR(20)` | L99 |
| `sFechaLN` | `CHAR(10)` | L100 |
| `sApellPaterno` | `CHAR(20)` | L101 |
| `sApellMaterno` | `CHAR(20)` | L102 |
| `sNombre1` | `CHAR(20)` | L103 |
| `sNombre2` | `CHAR(20)` | L104 |
| `sFechaNac` | `CHAR(10)` | L105 |
| `iregistro` | `INTEGER` | L106 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L155 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | SELECT | L156 |
| `si_bitacora_lista_negra` | `bdinteg` | ⚠️ sí | INSERT | L164 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L215 |
| `si_fiscal` | `bdinteg` | ⚠️ sí | SELECT | L220 |
| `si_fiscal` | `bdinteg` | ⚠️ sí | UPDATE | L224 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L149 |
| `sp_busqueda_cte_listanegra` | `bdiauditor` | ⚠️ sí | L158 |
| `sp_guardactemoral` | `bdicnweb` | no | L174 |
| `sp_guardadireccionesctemoral` | `bdicnweb` | no | L190 |
| `sp_guardaapoderadosctemoral` | `bdicnweb` | no | L228 |
| `sp_guardadatoslegalesctemoral` | `bdicnweb` | no | L245 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L162 | CÓDIGO_RETORNO | `LET cCodRet = '00995';` |  |
| L180 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardactemoral';` |  |
| L198 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardadireccionesctemoral';` |  |
| L233 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardaapoderadosctemoral';` |  |
| L252 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardaapoderadosctemoral';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |

---

## `sp_guardainfoctemoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardainfoctemoral2.sql` |
| **LOC (1er CREATE)** | 273 |
| **Callgraph** | ✅ fan_in=0 / fan_out=58 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda información y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardainfoctemoral2(
  pUsuario                     CHAR(8)
  pfuncion                     CHAR(1)
  pnumcte                      CHAR(20)
  pstatuscte                   CHAR(2)
  psucursal                    CHAR(4)
  ptp_persona                  CHAR(2)
  ptp_cliente                  CHAR(1)
  prazon_social                CHAR(254)
  prfc                         CHAR(13)
  pfechaalta                   DATE
  pnacionalidad                CHAR(2)
  pnombrecorto                 CHAR(60)
  pnombrecontacto              CHAR(48)
  ptelefonocontacto            CHAR(13)
  psufijo                      CHAR(2)
  pgiro                        CHAR(20)
  pactividad_princ             CHAR(3)
  ppaginainternet              CHAR(30)
  psecuenciadirecciones        SMALLINT
  ptipodir                     CHAR(1)
  pcalle                       CHAR(40)
  pcolonia                     CHAR(60)
  pmunicipio                   CHAR(5)
  pentre_calles                CHAR(40)
  ppais                        CHAR(3)
  pentidad                     CHAR(2)
  plocalidad                   CHAR(3)
  pcodpostal                   CHAR(5)
  ptipotel1                    CHAR(1)
  ptelefono1                   CHAR(13)
  ptipotel2                    CHAR(1)
  ptelefono2                   CHAR(13)
  ptipotel3                    CHAR(1)
  ptelefono3                   CHAR(13)
  pextension                   CHAR(5)
  pestado_inegi                CHAR(2)
  pmunicipio_inegi             CHAR(3)
  plocalidad_inegi             CHAR(4)
  pnociudad                    SMALLINT
  pnoext                       CHAR(10)
  pnoint                       CHAR(10)
  pdepto                       CHAR(6)
  pnocalle                     INTEGER
  pnocolonia                   INTEGER
  ppuntocar                    CHAR(1)
  punihabi                     CHAR(1)
  pmanz                        SMALLINT
  ppotros                      SMALLINT
  pandador                     SMALLINT
  petapa                       SMALLINT
  plote                        SMALLINT
  pedif                        SMALLINT
  pentrada                     SMALLINT
  pobserva                     CHAR(80)
  psecuenciaapoderados         INTEGER
  pNumCteApode                 CHAR(20)
  pNomApodera                  CHAR(60)
  pFecha                       DATE
  pEscConstitu                 CHAR(30)
  pNombNotario                 CHAR(30)
  pNumNotaria                  CHAR(5)
  pCdNotaria                   CHAR(30)
  pFecInscrip                  DATE
  pFecConstitu                 DATE
  pNumFolMerca                 CHAR(30)
  pCdFolMerca                  CHAR(30)
  pEscriPoder                  CHAR(30)
  pNombNotpd                   CHAR(30)
  pNumNotariopd                CHAR(30)
  pCdNotariopd                 CHAR(30)
  pFecInscripd                 DATE
  pFecEscritupd                DATE
  pFolMercapd                  CHAR(30)
  pCdFolMercaPd                CHAR(30)
  pNomSociedad                 CHAR(30)
  pEmail                       CHAR(100)
  pSat_fea                     CHAR(25)
  pDoc_legal                   CHAR(100)
  pTpo_Poder                   CHAR(3)
  pTpo_Admin                   CHAR(3)
  pTpo_Org                     CHAR(3)
  pCURP                        CHAR(20)
  pRFCAlt                      CHAR(13)
  pRegimen                     CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pfuncion` | `CHAR(1)` | — | — |
| `pnumcte` | `CHAR(20)` | — | — |
| `pstatuscte` | `CHAR(2)` | — | — |
| `psucursal` | `CHAR(4)` | — | — |
| `ptp_persona` | `CHAR(2)` | — | — |
| `ptp_cliente` | `CHAR(1)` | — | — |
| `prazon_social` | `CHAR(254)` | — | — |
| `prfc` | `CHAR(13)` | — | — |
| `pfechaalta` | `DATE` | — | — |
| `pnacionalidad` | `CHAR(2)` | — | — |
| `pnombrecorto` | `CHAR(60)` | — | — |
| `pnombrecontacto` | `CHAR(48)` | — | — |
| `ptelefonocontacto` | `CHAR(13)` | — | — |
| `psufijo` | `CHAR(2)` | — | — |
| `pgiro` | `CHAR(20)` | — | — |
| `pactividad_princ` | `CHAR(3)` | — | — |
| `ppaginainternet` | `CHAR(30)` | — | — |
| `psecuenciadirecciones` | `SMALLINT` | — | — |
| `ptipodir` | `CHAR(1)` | — | — |
| `pcalle` | `CHAR(40)` | — | — |
| `pcolonia` | `CHAR(60)` | — | — |
| `pmunicipio` | `CHAR(5)` | — | — |
| `pentre_calles` | `CHAR(40)` | — | — |
| `ppais` | `CHAR(3)` | — | — |
| `pentidad` | `CHAR(2)` | — | — |
| `plocalidad` | `CHAR(3)` | — | — |
| `pcodpostal` | `CHAR(5)` | — | — |
| `ptipotel1` | `CHAR(1)` | — | — |
| `ptelefono1` | `CHAR(13)` | — | — |
| `ptipotel2` | `CHAR(1)` | — | — |
| `ptelefono2` | `CHAR(13)` | — | — |
| `ptipotel3` | `CHAR(1)` | — | — |
| `ptelefono3` | `CHAR(13)` | — | — |
| `pextension` | `CHAR(5)` | — | — |
| `pestado_inegi` | `CHAR(2)` | — | — |
| `pmunicipio_inegi` | `CHAR(3)` | — | — |
| `plocalidad_inegi` | `CHAR(4)` | — | — |
| `pnociudad` | `SMALLINT` | — | — |
| `pnoext` | `CHAR(10)` | — | — |
| `pnoint` | `CHAR(10)` | — | — |
| `pdepto` | `CHAR(6)` | — | — |
| `pnocalle` | `INTEGER` | — | — |
| `pnocolonia` | `INTEGER` | — | — |
| `ppuntocar` | `CHAR(1)` | — | — |
| `punihabi` | `CHAR(1)` | — | — |
| `pmanz` | `SMALLINT` | — | — |
| `ppotros` | `SMALLINT` | — | — |
| `pandador` | `SMALLINT` | — | — |
| `petapa` | `SMALLINT` | — | — |
| `plote` | `SMALLINT` | — | — |
| `pedif` | `SMALLINT` | — | — |
| `pentrada` | `SMALLINT` | — | — |
| `pobserva` | `CHAR(80)` | — | — |
| `psecuenciaapoderados` | `INTEGER` | — | — |
| `pNumCteApode` | `CHAR(20)` | — | — |
| `pNomApodera` | `CHAR(60)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pEscConstitu` | `CHAR(30)` | — | — |
| `pNombNotario` | `CHAR(30)` | — | — |
| `pNumNotaria` | `CHAR(5)` | — | — |
| `pCdNotaria` | `CHAR(30)` | — | — |
| `pFecInscrip` | `DATE` | — | — |
| `pFecConstitu` | `DATE` | — | — |
| `pNumFolMerca` | `CHAR(30)` | — | — |
| `pCdFolMerca` | `CHAR(30)` | — | — |
| `pEscriPoder` | `CHAR(30)` | — | — |
| `pNombNotpd` | `CHAR(30)` | — | — |
| `pNumNotariopd` | `CHAR(30)` | — | — |
| `pCdNotariopd` | `CHAR(30)` | — | — |
| `pFecInscripd` | `DATE` | — | — |
| `pFecEscritupd` | `DATE` | — | — |
| `pFolMercapd` | `CHAR(30)` | — | — |
| `pCdFolMercaPd` | `CHAR(30)` | — | — |
| `pNomSociedad` | `CHAR(30)` | — | — |
| `pEmail` | `CHAR(100)` | — | — |
| `pSat_fea` | `CHAR(25)` | — | — |
| `pDoc_legal` | `CHAR(100)` | — | — |
| `pTpo_Poder` | `CHAR(3)` | — | — |
| `pTpo_Admin` | `CHAR(3)` | — | — |
| `pTpo_Org` | `CHAR(3)` | — | — |
| `pCURP` | `CHAR(20)` | — | — |
| `pRFCAlt` | `CHAR(13)` | — | — |
| `pRegimen` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L89 |
| `iSqlErr` | `INTEGER` | L90 |
| `cCodRetSp` | `CHAR(5)` | L91 |
| `iCodRetSp` | `INTEGER` | L92 |
| `cEmpresa` | `CHAR(3)` | L93 |
| `cNumcte` | `CHAR(20)` | L94 |
| `bInTrasaction` | `BOOLEAN` | L95 |
| `cCodRetLN` | `CHAR(6)` | L97 |
| `sFolio` | `CHAR(12)` | L98 |
| `sNumcte` | `CHAR(20)` | L99 |
| `sFechaLN` | `CHAR(10)` | L100 |
| `sApellPaterno` | `CHAR(20)` | L101 |
| `sApellMaterno` | `CHAR(20)` | L102 |
| `sNombre1` | `CHAR(20)` | L103 |
| `sNombre2` | `CHAR(20)` | L104 |
| `sFechaNac` | `CHAR(10)` | L105 |
| `iregistro` | `INTEGER` | L106 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L151 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | SELECT | L152 |
| `si_bitacora_lista_negra` | `bdinteg` | ⚠️ sí | INSERT | L160 |
| `si_fiscal` | `bdinteg` | ⚠️ sí | SELECT | L211 |
| `si_fiscal` | `bdinteg` | ⚠️ sí | UPDATE | L215 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L221 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_busqueda_cte_listanegra` | `bdiauditor` | ⚠️ sí | L154 |
| `sp_guardactemoral2` | `bdicnweb` | no | L170 |
| `sp_guardadireccionesctemoral2` | `bdicnweb` | no | L186 |
| `sp_guardaapoderadosctemoral2` | `bdicnweb` | no | L224 |
| `sp_guardadatoslegalesctemoral2` | `bdicnweb` | no | L241 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00995';` |  |
| L176 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardactemoral';` |  |
| L194 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardadireccionesctemoral';` |  |
| L229 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardaapoderadosctemoral2';` |  |
| L248 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardaapoderadosctemoral';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardainfoctemoral_may29`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardainfoctemoral_may29.sql` |
| **LOC (1er CREATE)** | 264 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda información y ctemoral — cuenta temporal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardainfoctemoral_may29(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfuncion                     CHAR(1)
  pnumcte                      CHAR(20)
  pstatuscte                   CHAR(2)
  psucursal                    CHAR(4)
  ptp_persona                  CHAR(2)
  ptp_cliente                  CHAR(1)
  prazon_social                CHAR(40)
  prfc                         CHAR(13)
  pfechaalta                   DATE
  pnacionalidad                CHAR(2)
  pnombrecorto                 CHAR(30)
  pnombrecontacto              CHAR(48)
  ptelefonocontacto            CHAR(13)
  psufijo                      CHAR(2)
  pgiro                        CHAR(20)
  pactividad_princ             CHAR(3)
  ppaginainternet              CHAR(30)
  psecuenciadirecciones        SMALLINT
  ptipodir                     CHAR(1)
  pcalle                       CHAR(40)
  pcolonia                     CHAR(60)
  pmunicipio                   CHAR(5)
  pentre_calles                CHAR(40)
  ppais                        CHAR(3)
  pentidad                     CHAR(2)
  plocalidad                   CHAR(3)
  pcodpostal                   CHAR(5)
  ptipotel1                    CHAR(1)
  ptelefono1                   CHAR(13)
  ptipotel2                    CHAR(1)
  ptelefono2                   CHAR(13)
  ptipotel3                    CHAR(1)
  ptelefono3                   CHAR(13)
  pextension                   CHAR(5)
  pestado_inegi                CHAR(2)
  pmunicipio_inegi             CHAR(3)
  plocalidad_inegi             CHAR(4)
  pnociudad                    SMALLINT
  pnoext                       CHAR(10)
  pnoint                       CHAR(10)
  pdepto                       CHAR(6)
  pnocalle                     INTEGER
  pnocolonia                   INTEGER
  ppuntocar                    CHAR(1)
  punihabi                     CHAR(1)
  pmanz                        SMALLINT
  ppotros                      SMALLINT
  pandador                     SMALLINT
  petapa                       SMALLINT
  plote                        SMALLINT
  pedif                        SMALLINT
  pentrada                     SMALLINT
  pobserva                     CHAR(80)
  psecuenciaapoderados         INTEGER
  pNumCteApode                 CHAR(20)
  pNomApodera                  CHAR(60)
  pFecha                       DATE
  pEscConstitu                 CHAR(30)
  pNombNotario                 CHAR(30)
  pNumNotaria                  CHAR(5)
  pCdNotaria                   CHAR(30)
  pFecInscrip                  DATE
  pFecConstitu                 DATE
  pNumFolMerca                 CHAR(30)
  pCdFolMerca                  CHAR(30)
  pEscriPoder                  CHAR(30)
  pNombNotpd                   CHAR(30)
  pNumNotariopd                CHAR(30)
  pCdNotariopd                 CHAR(30)
  pFecInscripd                 DATE
  pFecEscritupd                DATE
  pFolMercapd                  CHAR(30)
  pCdFolMercaPd                CHAR(30)
  pNomSociedad                 CHAR(30)
  pEmail                       CHAR(100)
  pSat_fea                     CHAR(25)
  pDoc_legal                   CHAR(100)
  pTpo_Poder                   CHAR(3)
  pTpo_Admin                   CHAR(3)
  pTpo_Org                     CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfuncion` | `CHAR(1)` | — | — |
| `pnumcte` | `CHAR(20)` | — | — |
| `pstatuscte` | `CHAR(2)` | — | — |
| `psucursal` | `CHAR(4)` | — | — |
| `ptp_persona` | `CHAR(2)` | — | — |
| `ptp_cliente` | `CHAR(1)` | — | — |
| `prazon_social` | `CHAR(40)` | — | — |
| `prfc` | `CHAR(13)` | — | — |
| `pfechaalta` | `DATE` | — | — |
| `pnacionalidad` | `CHAR(2)` | — | — |
| `pnombrecorto` | `CHAR(30)` | — | — |
| `pnombrecontacto` | `CHAR(48)` | — | — |
| `ptelefonocontacto` | `CHAR(13)` | — | — |
| `psufijo` | `CHAR(2)` | — | — |
| `pgiro` | `CHAR(20)` | — | — |
| `pactividad_princ` | `CHAR(3)` | — | — |
| `ppaginainternet` | `CHAR(30)` | — | — |
| `psecuenciadirecciones` | `SMALLINT` | — | — |
| `ptipodir` | `CHAR(1)` | — | — |
| `pcalle` | `CHAR(40)` | — | — |
| `pcolonia` | `CHAR(60)` | — | — |
| `pmunicipio` | `CHAR(5)` | — | — |
| `pentre_calles` | `CHAR(40)` | — | — |
| `ppais` | `CHAR(3)` | — | — |
| `pentidad` | `CHAR(2)` | — | — |
| `plocalidad` | `CHAR(3)` | — | — |
| `pcodpostal` | `CHAR(5)` | — | — |
| `ptipotel1` | `CHAR(1)` | — | — |
| `ptelefono1` | `CHAR(13)` | — | — |
| `ptipotel2` | `CHAR(1)` | — | — |
| `ptelefono2` | `CHAR(13)` | — | — |
| `ptipotel3` | `CHAR(1)` | — | — |
| `ptelefono3` | `CHAR(13)` | — | — |
| `pextension` | `CHAR(5)` | — | — |
| `pestado_inegi` | `CHAR(2)` | — | — |
| `pmunicipio_inegi` | `CHAR(3)` | — | — |
| `plocalidad_inegi` | `CHAR(4)` | — | — |
| `pnociudad` | `SMALLINT` | — | — |
| `pnoext` | `CHAR(10)` | — | — |
| `pnoint` | `CHAR(10)` | — | — |
| `pdepto` | `CHAR(6)` | — | — |
| `pnocalle` | `INTEGER` | — | — |
| `pnocolonia` | `INTEGER` | — | — |
| `ppuntocar` | `CHAR(1)` | — | — |
| `punihabi` | `CHAR(1)` | — | — |
| `pmanz` | `SMALLINT` | — | — |
| `ppotros` | `SMALLINT` | — | — |
| `pandador` | `SMALLINT` | — | — |
| `petapa` | `SMALLINT` | — | — |
| `plote` | `SMALLINT` | — | — |
| `pedif` | `SMALLINT` | — | — |
| `pentrada` | `SMALLINT` | — | — |
| `pobserva` | `CHAR(80)` | — | — |
| `psecuenciaapoderados` | `INTEGER` | — | — |
| `pNumCteApode` | `CHAR(20)` | — | — |
| `pNomApodera` | `CHAR(60)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pEscConstitu` | `CHAR(30)` | — | — |
| `pNombNotario` | `CHAR(30)` | — | — |
| `pNumNotaria` | `CHAR(5)` | — | — |
| `pCdNotaria` | `CHAR(30)` | — | — |
| `pFecInscrip` | `DATE` | — | — |
| `pFecConstitu` | `DATE` | — | — |
| `pNumFolMerca` | `CHAR(30)` | — | — |
| `pCdFolMerca` | `CHAR(30)` | — | — |
| `pEscriPoder` | `CHAR(30)` | — | — |
| `pNombNotpd` | `CHAR(30)` | — | — |
| `pNumNotariopd` | `CHAR(30)` | — | — |
| `pCdNotariopd` | `CHAR(30)` | — | — |
| `pFecInscripd` | `DATE` | — | — |
| `pFecEscritupd` | `DATE` | — | — |
| `pFolMercapd` | `CHAR(30)` | — | — |
| `pCdFolMercaPd` | `CHAR(30)` | — | — |
| `pNomSociedad` | `CHAR(30)` | — | — |
| `pEmail` | `CHAR(100)` | — | — |
| `pSat_fea` | `CHAR(25)` | — | — |
| `pDoc_legal` | `CHAR(100)` | — | — |
| `pTpo_Poder` | `CHAR(3)` | — | — |
| `pTpo_Admin` | `CHAR(3)` | — | — |
| `pTpo_Org` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L86 |
| `iSqlErr` | `INTEGER` | L87 |
| `cCodRetSp` | `CHAR(5)` | L88 |
| `iCodRetSp` | `INTEGER` | L89 |
| `cEmpresa` | `CHAR(3)` | L90 |
| `cNumcte` | `CHAR(20)` | L91 |
| `bInTrasaction` | `BOOLEAN` | L92 |
| `cCodRetLN` | `CHAR(6)` | L94 |
| `sFolio` | `CHAR(12)` | L95 |
| `sNumcte` | `CHAR(20)` | L96 |
| `sFechaLN` | `CHAR(10)` | L97 |
| `sApellPaterno` | `CHAR(20)` | L98 |
| `sApellMaterno` | `CHAR(20)` | L99 |
| `sNombre1` | `CHAR(20)` | L100 |
| `sNombre2` | `CHAR(20)` | L101 |
| `sFechaNac` | `CHAR(10)` | L102 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L150 |
| `si_ctepf` | `bdinteg` | ⚠️ sí | SELECT | L151 |
| `si_bitacora_lista_negra` | `bdinteg` | ⚠️ sí | INSERT | L159 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L210 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L144 |
| `sp_busqueda_cte_listanegra` | `bdiauditor` | ⚠️ sí | L153 |
| `sp_guardactemoral` | `bdicnweb` | no | L169 |
| `sp_guardadireccionesctemoral` | `bdicnweb` | no | L185 |
| `sp_guardaapoderadosctemoral` | `bdicnweb` | no | L213 |
| `sp_guardadatoslegalesctemoral` | `bdicnweb` | no | L230 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet = '00995';` |  |
| L175 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardactemoral';` |  |
| L193 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardadireccionesctemoral';` |  |
| L218 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardaapoderadosctemoral';` |  |
| L237 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_guardaapoderadosctemoral';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctemoral` | ENTIDAD | ctemoral — cuenta temporal (sp_guarda*ctemoral — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?_may29` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_may29` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardainfocteprospecto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardainfocteprospecto.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda información, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `ctefisico` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardainfocteprospecto(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pFuncion                     CHAR(1)
  pNumcte                      CHAR(20)
  pSucursal                    CHAR(4)
  pEjecutivo                   CHAR(8)
  pTp_persona                  CHAR (2)
  pTp_cliente                  CHAR(1)
  pPaterno                     CHAR (26)
  pMaterno                     CHAR (26)
  pNombre1                     CHAR (26)
  pNombre2                     CHAR (26)
  pRfc                         CHAR (13)
  pSector                      CHAR (2)
  pSegmento                    CHAR (3)
  pActividad_princ             CHAR (3)
  pGrupo                       CHAR(3)
  pSubgrupo                    CHAR(3)
  pResidencia                  CHAR(1)
  pApell_casada                CHAR(20)
  pNumcte_ref                  CHAR(20)
  pDistrito                    CHAR(2)
  pPuesto_ppes                 CHAR(1)
  pFamiliar_ppes               CHAR(1)
  pActividad_esp               CHAR(11)
  pFecha_nac                   DATE
  pNacionalidad                CHAR(3)
  pFm3                         CHAR(18)
  pEstado_civil                CHAR(1)
  pRegimen_mat                 CHAR(1)
  pProfesion                   CHAR (3)
  pSexo                        CHAR(1)
  pCurp                        CHAR(20)
  pCodidentif                  CHAR(2)
  pNumidentif                  CHAR(30)
  pNo_imss                     CHAR(12)
  pDependientes                SMALLINT
  pTutor                       CHAR(60)
  pEmail                       CHAR(60)
  pNom_conyuge                 CHAR(60)
  pSeguro_defunc               CHAR(1)
  pEscolaridad                 CHAR(2)
  pHabita_en                   CHAR(20)
  pAnios_habita                SMALLINT
  pNombre_prop                 CHAR(60)
  pImphiporenta                MONEY(14,2)
  pNumeroife                   CHAR(20)
  pNumerotutor                 CHAR(20)
  pNumeroconyuge               CHAR(20)
  pEjecut_autoriza             CHAR(8)
  pPromocion                   CHAR(2)
  pNumhabitantes               CHAR (60)
) RETURNING CHAR(5) AS cCodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pFuncion` | `CHAR(1)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pTp_persona` | `CHAR (2)` | — | — |
| `pTp_cliente` | `CHAR(1)` | — | — |
| `pPaterno` | `CHAR (26)` | — | — |
| `pMaterno` | `CHAR (26)` | — | — |
| `pNombre1` | `CHAR (26)` | — | — |
| `pNombre2` | `CHAR (26)` | — | — |
| `pRfc` | `CHAR (13)` | — | — |
| `pSector` | `CHAR (2)` | — | — |
| `pSegmento` | `CHAR (3)` | — | — |
| `pActividad_princ` | `CHAR (3)` | — | — |
| `pGrupo` | `CHAR(3)` | — | — |
| `pSubgrupo` | `CHAR(3)` | — | — |
| `pResidencia` | `CHAR(1)` | — | — |
| `pApell_casada` | `CHAR(20)` | — | — |
| `pNumcte_ref` | `CHAR(20)` | — | — |
| `pDistrito` | `CHAR(2)` | — | — |
| `pPuesto_ppes` | `CHAR(1)` | — | — |
| `pFamiliar_ppes` | `CHAR(1)` | — | — |
| `pActividad_esp` | `CHAR(11)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFecha_nac` | `DATE` | — | — |
| `pNacionalidad` | `CHAR(3)` | — | — |
| `pFm3` | `CHAR(18)` | — | — |
| `pEstado_civil` | `CHAR(1)` | — | — |
| `pRegimen_mat` | `CHAR(1)` | — | — |
| `pProfesion` | `CHAR (3)` | — | — |
| `pSexo` | `CHAR(1)` | — | — |
| `pCurp` | `CHAR(20)` | — | — |
| `pCodidentif` | `CHAR(2)` | — | — |
| `pNumidentif` | `CHAR(30)` | — | — |
| `pNo_imss` | `CHAR(12)` | — | — |
| `pDependientes` | `SMALLINT` | — | — |
| `pTutor` | `CHAR(60)` | — | — |
| `pEmail` | `CHAR(60)` | — | — |
| `pNom_conyuge` | `CHAR(60)` | — | — |
| `pSeguro_defunc` | `CHAR(1)` | — | — |
| `pEscolaridad` | `CHAR(2)` | — | — |
| `pHabita_en` | `CHAR(20)` | — | — |
| `pAnios_habita` | `SMALLINT` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pNombre_prop` | `CHAR(60)` | — | — |
| `pImphiporenta` | `MONEY(14,2)` | — | — |
| `pNumeroife` | `CHAR(20)` | — | — |
| `pNumerotutor` | `CHAR(20)` | — | — |
| `pNumeroconyuge` | `CHAR(20)` | — | — |
| `pEjecut_autoriza` | `CHAR(8)` | — | — |
| `pPromocion` | `CHAR(2)` | — | — |
| `pNumhabitantes` | `CHAR (60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L59 |
| `cCodRet` | `CHAR(5)` | L60 |
| `cCodRetSp` | `CHAR(3)` | L61 |
| `cFuncion` | `CHAR(1)` | L62 |
| `cNumcte` | `CHAR(20)` | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `ctefisico` | `bdinteg` | ⚠️ sí | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L157 | FÓRMULA | `LET cCodRet = cCodRetSp; -- ERROR GENERICO.` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardainfocteprospecto2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardainfocteprospecto2.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda información, Cliente Prospecto — cliente potencial aún sin cuenta abierta y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `ctefisico` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardainfocteprospecto2(
  pFuncion                     CHAR(1)
  pNumcte                      CHAR(20)
  pSucursal                    CHAR(4)
  pEjecutivo                   CHAR(8)
  pTp_persona                  CHAR (2)
  pTp_cliente                  CHAR(1)
  pPaterno                     CHAR (26)
  pMaterno                     CHAR (26)
  pNombre1                     CHAR (26)
  pNombre2                     CHAR (26)
  pRfc                         CHAR (13)
  pSector                      CHAR (2)
  pSegmento                    CHAR (3)
  pActividad_princ             CHAR (3)
  pGrupo                       CHAR(3)
  pSubgrupo                    CHAR(3)
  pResidencia                  CHAR(1)
  pApell_casada                CHAR(20)
  pNumcte_ref                  CHAR(20)
  pDistrito                    CHAR(2)
  pPuesto_ppes                 CHAR(1)
  pFamiliar_ppes               CHAR(1)
  pActividad_esp               CHAR(11)
  pFecha_nac                   DATE
  pNacionalidad                CHAR(3)
  pFm3                         CHAR(18)
  pEstado_civil                CHAR(1)
  pRegimen_mat                 CHAR(1)
  pProfesion                   CHAR (3)
  pSexo                        CHAR(1)
  pCurp                        CHAR(20)
  pCodidentif                  CHAR(2)
  pNumidentif                  CHAR(30)
  pNo_imss                     CHAR(12)
  pDependientes                SMALLINT
  pTutor                       CHAR(60)
  pEmail                       CHAR(60)
  pNom_conyuge                 CHAR(60)
  pSeguro_defunc               CHAR(1)
  pEscolaridad                 CHAR(2)
  pHabita_en                   CHAR(20)
  pAnios_habita                SMALLINT
  pNombre_prop                 CHAR(60)
  pImphiporenta                MONEY(14,2)
  pNumeroife                   CHAR(20)
  pNumerotutor                 CHAR(20)
  pNumeroconyuge               CHAR(20)
  pEjecut_autoriza             CHAR(8)
  pPromocion                   CHAR(2)
  pNumhabitantes               CHAR (60)
) RETURNING CHAR(5) AS cCodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFuncion` | `CHAR(1)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pTp_persona` | `CHAR (2)` | — | — |
| `pTp_cliente` | `CHAR(1)` | — | — |
| `pPaterno` | `CHAR (26)` | — | — |
| `pMaterno` | `CHAR (26)` | — | — |
| `pNombre1` | `CHAR (26)` | — | — |
| `pNombre2` | `CHAR (26)` | — | — |
| `pRfc` | `CHAR (13)` | — | — |
| `pSector` | `CHAR (2)` | — | — |
| `pSegmento` | `CHAR (3)` | — | — |
| `pActividad_princ` | `CHAR (3)` | — | — |
| `pGrupo` | `CHAR(3)` | — | — |
| `pSubgrupo` | `CHAR(3)` | — | — |
| `pResidencia` | `CHAR(1)` | — | — |
| `pApell_casada` | `CHAR(20)` | — | — |
| `pNumcte_ref` | `CHAR(20)` | — | — |
| `pDistrito` | `CHAR(2)` | — | — |
| `pPuesto_ppes` | `CHAR(1)` | — | — |
| `pFamiliar_ppes` | `CHAR(1)` | — | — |
| `pActividad_esp` | `CHAR(11)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFecha_nac` | `DATE` | — | — |
| `pNacionalidad` | `CHAR(3)` | — | — |
| `pFm3` | `CHAR(18)` | — | — |
| `pEstado_civil` | `CHAR(1)` | — | — |
| `pRegimen_mat` | `CHAR(1)` | — | — |
| `pProfesion` | `CHAR (3)` | — | — |
| `pSexo` | `CHAR(1)` | — | — |
| `pCurp` | `CHAR(20)` | — | — |
| `pCodidentif` | `CHAR(2)` | — | — |
| `pNumidentif` | `CHAR(30)` | — | — |
| `pNo_imss` | `CHAR(12)` | — | — |
| `pDependientes` | `SMALLINT` | — | — |
| `pTutor` | `CHAR(60)` | — | — |
| `pEmail` | `CHAR(60)` | — | — |
| `pNom_conyuge` | `CHAR(60)` | — | — |
| `pSeguro_defunc` | `CHAR(1)` | — | — |
| `pEscolaridad` | `CHAR(2)` | — | — |
| `pHabita_en` | `CHAR(20)` | — | — |
| `pAnios_habita` | `SMALLINT` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pNombre_prop` | `CHAR(60)` | — | — |
| `pImphiporenta` | `MONEY(14,2)` | — | — |
| `pNumeroife` | `CHAR(20)` | — | — |
| `pNumerotutor` | `CHAR(20)` | — | — |
| `pNumeroconyuge` | `CHAR(20)` | — | — |
| `pEjecut_autoriza` | `CHAR(8)` | — | — |
| `pPromocion` | `CHAR(2)` | — | — |
| `pNumhabitantes` | `CHAR (60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L56 |
| `cCodRet` | `CHAR(5)` | L57 |
| `cCodRetSp` | `CHAR(3)` | L58 |
| `cFuncion` | `CHAR(1)` | L59 |
| `cNumcte` | `CHAR(20)` | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `ctefisico` | `bdinteg` | ⚠️ sí | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L144 | FÓRMULA | `LET cCodRet = cCodRetSp; -- ERROR GENERICO.` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `ctepr` | ENTIDAD | Cliente Prospecto — cliente potencial aún sin cuenta abierta | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?pecto2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecto2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardainfotercerosctamec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardainfotercerosctamec.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda información, ro — Rol Operativo y Cuenta Mecánica — tipo de cuenta de cheques empresarial para nómina y pagos automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_ctamec_regisrecterc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardainfotercerosctamec(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pTipoRec                     CHAR(1)
  pSecuencia                   SMALLINT
  pTipoPer                     CHAR(2)
  pNumPer                      CHAR(2)
  pNombre                      CHAR(40)
  pNacion                      CHAR(40)
  pRfc                         CHAR(13)
  pFirma                       CHAR(25)
  pDomicilio                   CHAR(200)
) RETURNING CHAR(5) AS cCodigoRetorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTipoRec` | `CHAR(1)` | — | — |
| `pSecuencia` | `SMALLINT` | — | — |
| `pTipoPer` | `CHAR(2)` | — | — |
| `pNumPer` | `CHAR(2)` | — | — |
| `pNombre` | `CHAR(40)` | — | — |
| `pNacion` | `CHAR(40)` | — | — |
| `pRfc` | `CHAR(13)` | — | — |
| `pFirma` | `CHAR(25)` | — | — |
| `pDomicilio` | `CHAR(200)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(3)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ctamec_regisrecterc` | `bdicheq` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?terce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctamec` | ENTIDAD | Cuenta Mecánica — tipo de cuenta de cheques empresarial para | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?terce`, `ro`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardainfotercerosctamec2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardainfotercerosctamec2.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda información, ro — Rol Operativo y Cuenta Mecánica — tipo de cuenta de cheques empresarial para nómina y pagos automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_ctamec_regisrecterc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardainfotercerosctamec2(
  pUsuario                     CHAR(8)
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pTipoRec                     CHAR(1)
  pSecuencia                   SMALLINT
  pTipoPer                     CHAR(2)
  pNumPer                      CHAR(2)
  pNombre                      CHAR(40)
  pNacion                      CHAR(40)
  pRfc                         CHAR(13)
  pFirma                       CHAR(25)
  pDomicilio                   CHAR(200)
) RETURNING CHAR(5) AS cCodigoRetorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTipoRec` | `CHAR(1)` | — | — |
| `pSecuencia` | `SMALLINT` | — | — |
| `pTipoPer` | `CHAR(2)` | — | — |
| `pNumPer` | `CHAR(2)` | — | — |
| `pNombre` | `CHAR(40)` | — | — |
| `pNacion` | `CHAR(40)` | — | — |
| `pRfc` | `CHAR(13)` | — | — |
| `pFirma` | `CHAR(25)` | — | — |
| `pDomicilio` | `CHAR(200)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(3)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ctamec_regisrecterc` | `bdicheq` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `?terce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctamec` | ENTIDAD | Cuenta Mecánica — tipo de cuenta de cheques empresarial para | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?terce`, `ro`, `?s`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guardamodificacionsaldosctascap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardamodificacionsaldosctascap.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 74 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda saldos, cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardamodificacionsaldosctascap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pImpchqsbcAntes              MONEY(14,2)
  pImpchqsbgAntes              MONEY(14,2)
  pSdoretenidoAntes            MONEY(14,2)
  pSdocongAntes                MONEY(14,2)
  pCompendienteAntes           MONEY(14,2)
  pFechaprocesoAntes           DATE
  pImpchqsbcDesp               MONEY(14,2)
  pImpchqsbgDesp               MONEY(14,2)
  pSdoretenidoDesp             MONEY(14,2)
  pSdocongDesp                 MONEY(14,2)
  pCompendienteDesp            MONEY(14,2)
  pFechaprocesoDesp            DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pImpchqsbcAntes` | `MONEY(14,2)` | — | — |
| `pImpchqsbgAntes` | `MONEY(14,2)` | — | — |
| `pSdoretenidoAntes` | `MONEY(14,2)` | — | — |
| `pSdocongAntes` | `MONEY(14,2)` | — | — |
| `pCompendienteAntes` | `MONEY(14,2)` | — | — |
| `pFechaprocesoAntes` | `DATE` | — | — |
| `pImpchqsbcDesp` | `MONEY(14,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pImpchqsbgDesp` | `MONEY(14,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pSdoretenidoDesp` | `MONEY(14,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pSdocongDesp` | `MONEY(14,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pCompendienteDesp` | `MONEY(14,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFechaprocesoDesp` | `DATE` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | UPDATE | L41 |
| `sc_docret_sbc` | `bdicheq` | ⚠️ sí | UPDATE | L51 |
| `sw_bitacora_maech` | `bdicnweb` | no | INSERT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCuenta = '' OR pFechaprocesoAntes IS NULL OR pFechaprocesoDe` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `modificacion` | ACCION | modificación | 🔵 CONVENCIÓN | nombre_sp |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_guardarespuestabts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardarespuestabts.sql` |
| **LOC (1er CREATE)** | 158 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda respuesta y beneficiarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_guardarespuestaqryi` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardarespuestabts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR (4)
  pTxn_Status                  CHAR(1)
  pConfirmation_nm             CHAR (11)
  pUser_name                   CHAR (20)
  pTerminal                    CHAR(15)
  pAgent_Dt                    CHAR(8)
  pAgent_Tm                    CHAR(6)
  pOpCode                      CHAR(4)
  pProcess_Msg                 CHAR(255)
  pError_Param_Full_Name       CHAR(255)
  pTrans_Status_Cd             CHAR(3)
  pTrans_Status_Dt             CHAR(8)
  pProcess_Dt                  CHAR(8)
  pProcess_Tm                  CHAR(6)
  pService_Cd                  CHAR(3)
  pPayment_Type_Cd             CHAR(3)
  pOrig_Country_Cd             CHAR(3)
  pOrig_Currency_Cd            CHAR(3)
  pDest_Country_Cd             CHAR(3)
  pDest_Currency_Cd            CHAR(3)
  pOrigin_Am                   CHAR(20)
  pDestination_Am              CHAR(20)
  pExch_Rate_Fx                CHAR(21)
  pS_Agent_Cd                  CHAR(3)
  pS_Payment_Type_Cd           CHAR(3)
  pS_Account_Type_Cd           CHAR(3)
  pS_Account_Nm                CHAR(30)
  pS_Bank_Cd                   CHAR(30)
  pS_Bank_Ref_Nm               CHAR(20)
  pR_Account_Type_Cd           CHAR(3)
  pR_Account_Nm                CHAR(30)
  pR_Agent_Cd                  CHAR(3)
  pR_Agent_Region_Sd           CHAR(15)
  pR_Agent_Branch_Sd           CHAR(15)
  pS_First_Name                CHAR(40)
  pS_Middle_Name               CHAR(40)
  pS_Last_Name                 CHAR(40)
  pS_Mother_M_Name             CHAR(40)
  pS_Address                   CHAR(80)
  pS_City                      CHAR(40)
  pS_State_Cd                  CHAR(3)
  pS_Country_Cd                CHAR(3)
  pS_Zip_Code                  CHAR(10)
  pS_Phone                     CHAR(15)
  pR_First_Name                CHAR(40)
  pR_Middle_Name               CHAR(40)
  pR_Last_Name                 CHAR(40)
  pR_Mother_M_Name             CHAR(40)
  pR_Identif_Type_Cd           CHAR(3)
  pR_Identif_Nm                CHAR(20)
  pF_First_Name                CHAR(40)
  pF_Middle_Name               CHAR(40)
  pF_Last_Name                 CHAR(40)
  pF_Mother_M_Name             CHAR(40)
  pR_Address                   CHAR(80)
  pR_City                      CHAR(40)
  pR_State_Cd                  CHAR(3)
  pR_Country_Cd                CHAR(3)
  pR_Zip_Code                  CHAR(10)
  pR_Phone                     CHAR(15)
  pR_Type_Cd                   CHAR(3)
  pR_Issuer_Cd                 CHAR(3)
  pR_Issuer_State_Cd           CHAR(3)
  pR_Issuer_Country_Cd         CHAR(3)
  pRi_Identif_Nm               CHAR(20)
  pR_Expiration_Dt             CHAR(8)
  pS_Type_Cd                   CHAR(3)
  pS_Issuer_Cd                 CHAR(3)
  pS_Issuer_State_Cd           CHAR(3)
  pS_Issuer_Country_Cd         CHAR(3)
  pS_Identif_Nm                CHAR(20)
  pS_Expiration_Dt             CHAR(8)
) RETURNING CHAR(5) AS cCodRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR (4)` | — | — |
| `pTxn_Status` | `CHAR(1)` | — | — |
| `pConfirmation_nm` | `CHAR (11)` | — | — |
| `pUser_name` | `CHAR (20)` | — | — |
| `pTerminal` | `CHAR(15)` | — | — |
| `pAgent_Dt` | `CHAR(8)` | — | — |
| `pAgent_Tm` | `CHAR(6)` | — | — |
| `pOpCode` | `CHAR(4)` | — | — |
| `pProcess_Msg` | `CHAR(255)` | — | — |
| `pError_Param_Full_Name` | `CHAR(255)` | — | — |
| `pTrans_Status_Cd` | `CHAR(3)` | — | — |
| `pTrans_Status_Dt` | `CHAR(8)` | — | — |
| `pProcess_Dt` | `CHAR(8)` | — | — |
| `pProcess_Tm` | `CHAR(6)` | — | — |
| `pService_Cd` | `CHAR(3)` | — | — |
| `pPayment_Type_Cd` | `CHAR(3)` | — | — |
| `pOrig_Country_Cd` | `CHAR(3)` | — | — |
| `pOrig_Currency_Cd` | `CHAR(3)` | — | — |
| `pDest_Country_Cd` | `CHAR(3)` | — | — |
| `pDest_Currency_Cd` | `CHAR(3)` | — | — |
| `pOrigin_Am` | `CHAR(20)` | — | — |
| `pDestination_Am` | `CHAR(20)` | — | — |
| `pExch_Rate_Fx` | `CHAR(21)` | — | — |
| `pS_Agent_Cd` | `CHAR(3)` | — | — |
| `pS_Payment_Type_Cd` | `CHAR(3)` | — | — |
| `pS_Account_Type_Cd` | `CHAR(3)` | — | — |
| `pS_Account_Nm` | `CHAR(30)` | — | — |
| `pS_Bank_Cd` | `CHAR(30)` | — | — |
| `pS_Bank_Ref_Nm` | `CHAR(20)` | — | — |
| `pR_Account_Type_Cd` | `CHAR(3)` | — | — |
| `pR_Account_Nm` | `CHAR(30)` | — | — |
| `pR_Agent_Cd` | `CHAR(3)` | — | — |
| `pR_Agent_Region_Sd` | `CHAR(15)` | — | — |
| `pR_Agent_Branch_Sd` | `CHAR(15)` | — | — |
| `pS_First_Name` | `CHAR(40)` | — | — |
| `pS_Middle_Name` | `CHAR(40)` | — | — |
| `pS_Last_Name` | `CHAR(40)` | — | — |
| `pS_Mother_M_Name` | `CHAR(40)` | — | — |
| `pS_Address` | `CHAR(80)` | — | — |
| `pS_City` | `CHAR(40)` | — | — |
| `pS_State_Cd` | `CHAR(3)` | — | — |
| `pS_Country_Cd` | `CHAR(3)` | — | — |
| `pS_Zip_Code` | `CHAR(10)` | — | — |
| `pS_Phone` | `CHAR(15)` | — | — |
| `pR_First_Name` | `CHAR(40)` | — | — |
| `pR_Middle_Name` | `CHAR(40)` | — | — |
| `pR_Last_Name` | `CHAR(40)` | — | — |
| `pR_Mother_M_Name` | `CHAR(40)` | — | — |
| `pR_Identif_Type_Cd` | `CHAR(3)` | — | — |
| `pR_Identif_Nm` | `CHAR(20)` | — | — |
| `pF_First_Name` | `CHAR(40)` | — | — |
| `pF_Middle_Name` | `CHAR(40)` | — | — |
| `pF_Last_Name` | `CHAR(40)` | — | — |
| `pF_Mother_M_Name` | `CHAR(40)` | — | — |
| `pR_Address` | `CHAR(80)` | — | — |
| `pR_City` | `CHAR(40)` | — | — |
| `pR_State_Cd` | `CHAR(3)` | — | — |
| `pR_Country_Cd` | `CHAR(3)` | — | — |
| `pR_Zip_Code` | `CHAR(10)` | — | — |
| `pR_Phone` | `CHAR(15)` | — | — |
| `pR_Type_Cd` | `CHAR(3)` | — | — |
| `pR_Issuer_Cd` | `CHAR(3)` | — | — |
| `pR_Issuer_State_Cd` | `CHAR(3)` | — | — |
| `pR_Issuer_Country_Cd` | `CHAR(3)` | — | — |
| `pRi_Identif_Nm` | `CHAR(20)` | — | — |
| `pR_Expiration_Dt` | `CHAR(8)` | — | — |
| `pS_Type_Cd` | `CHAR(3)` | — | — |
| `pS_Issuer_Cd` | `CHAR(3)` | — | — |
| `pS_Issuer_State_Cd` | `CHAR(3)` | — | — |
| `pS_Issuer_Country_Cd` | `CHAR(3)` | — | — |
| `pS_Identif_Nm` | `CHAR(20)` | — | — |
| `pS_Expiration_Dt` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L81 |
| `cCodRetSp` | `CHAR(50)` | L82 |
| `sql_err` | `INT` | L83 |
| `cCodRet` | `CHAR(5)` | L84 |
| `cAgent_Trans_Type_Code` | `CHAR(4)` | L85 |
| `cAgent_Cd` | `CHAR(3)` | L86 |
| `cRegion_Sd` | `CHAR(15)` | L87 |
| `cBranch_Sd` | `CHAR(15)` | L88 |
| `cState_Cd` | `CHAR(3)` | L89 |
| `cCountry_Cd` | `CHAR(3)` | L90 |
| `cStatus` | `CHAR(1)` | L91 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_guardarespuestaqryi` | `bdisac` | ⚠️ sí | L131 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L97 | CÓDIGO_RETORNO | `LET cCodRet                = '00000';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00245';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `respuesta` | ENTIDAD | respuesta | 🔵 CONVENCIÓN | nombre_sp |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_guardasaldosbitacoracre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_guardasaldosbitacoracre.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda saldos, bitácora y crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_guardasaldosbitacoracre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pTipoOperacion               SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cre`=crédito | 🟡 INFERIDO |
| `pTipoOperacion` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `cTipoMovimiento` | `CHAR(1)` | L9 |
| `cNumCliente` | `CHAR(20)` | L11 |
| `cSucursal` | `CHAR(4)` | L12 |
| `cStatusCred` | `CHAR(2)` | L13 |
| `iPlazo` | `INTEGER` | L14 |
| `dFechaApertura` | `DATE` | L15 |
| `dFechaVencimiento` | `DATE` | L16 |
| `dTasaInteres` | `DECIMAL(9,6)` | L17 |
| `dTasaMoratorios` | `DECIMAL(9,6)` | L18 |
| `dSdoRetenido` | `DECIMAL(18,2)` | L19 |
| `dSdoNoExig` | `DECIMAL(18,2)` | L20 |
| `dSdoContabMora` | `DECIMAL(18,2)` | L21 |
| `dSdoCapital` | `DECIMAL(18,2)` | L22 |
| `dSdoCapInsoluto` | `DECIMAL(18,2)` | L23 |
| `dSdoMtoVdo` | `DECIMAL(18,2)` | L24 |
| `dMtoVdoTrasp` | `DECIMAL(18,2)` | L25 |
| `dMtoFinanciado` | `DECIMAL(18,2)` | L26 |
| `dMtoOtorgado` | `DECIMAL(18,2)` | L27 |
| `dCapTrasnoVdo` | `DECIMAL(18,2)` | L28 |
| `dMtoVdoInt` | `DECIMAL(18,2)` | L29 |
| `dMtoVdoTrasInt` | `DECIMAL(18,2)` | L30 |
| `dIntTraNoExig` | `DECIMAL(18,2)` | L31 |
| *…17 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_bitacorasaldoscre` | `bdicnweb` | no | INSERT | L139 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L115 |
| `sp_consultamovtosnvasfuncre` | `bdicnweb` | no | L120 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L104 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCredito = '' OR pTipoOperacion IS NULL THEN` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_hojafirmactamec_complementoinfo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_hojafirmactamec_complementoinfo.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "dirección MAC y información (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 24/07/2024 |

### Firma

```sql
CREATE PROCEDURE sp_hojafirmactamec_complementoinfo(
  pCuenta                      CHAR(20)
) RETURNING CHAR(5)   AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INTEGER` | L19 |
| `cCodRet` | `CHAR(5)` | L20 |
| `cMensaje` | `CHAR(50)` | L21 |
| `numcliente` | `CHAR(20)` | L23 |
| `tipofirma` | `CHAR(50)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_firmantes` | `bdicheq` | ⚠️ sí | SELECT | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet           	= '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_hojafir` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mac` | ENTIDAD | dirección MAC | 🔵 CONVENCIÓN | nombre_sp |
| `?tamec_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?lemento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_hojafir`, `?tamec_`, `?lemento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_imprimecedulas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_imprimecedulas.sql` |
| **LOC (1er CREATE)** | 196 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "Impago — pago vencido o fallido; confirmado: n_impagos_consec y cédulas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_imprimecedulas(
  pFechaConcil                 DATE
  pTipo                        SMALLINT
) RETURNING CHAR(5), CHAR(40), CHAR(14), DECIMAL(18,2), DECIMAL(18,2), DECIMAL(18,2), CHAR(255)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaConcil` | `DATE` | — | — |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cNombre` | `CHAR(40)` | L11 |
| `cCtaContable` | `CHAR(14)` | L12 |
| `mSdoCheques` | `DECIMAL(18,2)` | L13 |
| `mSdoContab` | `DECIMAL(18,2)` | L14 |
| `mDifSaldos` | `DECIMAL(18,2)` | L15 |
| `cObservaciones` | `CHAR(255)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontable` | `bdicheq` | ⚠️ sí | SELECT | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | VALIDACIÓN_NULL | `IF ( ( pFechaConcil is null OR pFechaConcil = '' ) OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?rime` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cedulas` | ENTIDAD | cédulas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rime` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insermedianainflacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insermedianainflacion.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "insertar información (del día)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_insermedianainflacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  INTEGER
  pMedInflacion                DECIMAL(9,6)
  pFechaPublicacion            DATETIME YEAR TO FRACTION(3)
) RETURNING CHAR(5)    					 AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `INTEGER` | — | — |
| `pMedInflacion` | `DECIMAL(9,6)` | `inf`=información | 🟡 INFERIDO |
| `pFechaPublicacion` | `DATETIME YEAR TO FRACTION(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRet` | `CHAR(5)` | L16 |
| `dMedianInflacion` | `DECIMAL(9,6)` | L17 |
| `dfechaPubli` | `DATETIME YEAR TO FRACTION(3)` | L18 |
| `iregistros` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_medianainflacion` | `bdicheq` | ⚠️ sí | SELECT | L66 |
| `sc_medianainflacion` | `bdicheq` | ⚠️ sí | INSERT | L75 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | VALIDACIÓN_NULL | `IF pMedInflacion IS NULL OR pFechaPublicacion IS NULL OR pMedInflacion = '' OR pFechaPublicacion = '` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | FÓRMULA | `LET iregistros = iregistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?erme` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `?na` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `?lacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?erme`, `?na`, `?lacion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_inserta_creditoexcluir`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_inserta_creditoexcluir.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_inserta_creditoexcluir(
  pCuenta                      CHAR(20)
)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vStatusSol` | `CHAR(2)` | L10 |
| `vHoy` | `DATE` | L11 |
| `dFechaEnt` | `DATE` | L12 |
| `vCausaSol` | `CHAR(3)` | L13 |
| `P_COD_RET` | `VARCHAR(5)` | L14 |
| `cNumcte` | `CHAR(20)` | L15 |
| `cCodRet` | `CHAR(6)` | L16 |
| `cMensajeRet` | `CHAR(100)` | L17 |
| `iValido` | `INTEGER` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cNumProd` | `CHAR(4)` | L20 |
| `vRegistro` | `DECIMAL(18,2)` | L21 |
| `vMensajeStatus` | `CHAR(80)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_evc_excluidos` | `bdicnweb` | no | SELECT | L56 |
| `sw_evc_excluidos` | `bdicnweb` | no | DELETE | L64 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `credito` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?cluir` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?cluir` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertabitacoracomisiontc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertabitacoracomisiontc.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 86 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta bitácora y tc — Tarjeta de Crédito · comisión" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertabitacoracomisiontc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pComision                    CHAR(4)
  pTasaRend                    DECIMAL(11,6)
  pSdoPromMM                   MONEY(18,2)
  pMonMinAper                  MONEY(18,2)
  pComCgoNoSmm                 MONEY(18,2)
  pComCboAclaNP                MONEY(18,2)
  pComChqGirCob                MONEY(18,2)
  pComInaCta                   MONEY(18,2)
  pServTranSpei                MONEY(18,2)
  pServTranTef                 MONEY(18,2)
  pServAnualidad               MONEY(18,2)
  pServReenvToken              MONEY(18,2)
  pServReepToken               MONEY(18,2)
  pDispCtaBcoppel              MONEY(18,2)
  pDispCtaOtrobco              MONEY(18,2)
  pDispLinea                   MONEY(18,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pComision` | `CHAR(4)` | `comision`=comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN |
| `pTasaRend` | `DECIMAL(11,6)` | — | — |
| `pSdoPromMM` | `MONEY(18,2)` | — | — |
| `pMonMinAper` | `MONEY(18,2)` | — | — |
| `pComCgoNoSmm` | `MONEY(18,2)` | — | — |
| `pComCboAclaNP` | `MONEY(18,2)` | — | — |
| `pComChqGirCob` | `MONEY(18,2)` | — | — |
| `pComInaCta` | `MONEY(18,2)` | — | — |
| `pServTranSpei` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pServTranTef` | `MONEY(18,2)` | — | — |
| `pServAnualidad` | `MONEY(18,2)` | — | — |
| `pServReenvToken` | `MONEY(18,2)` | — | — |
| `pServReepToken` | `MONEY(18,2)` | — | — |
| `pDispCtaBcoppel` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDispCtaOtrobco` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDispLinea` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cMensajeRet` | `CHAR(100)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_guardabittascomi_pm` | `bdicheq` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_guardabittascomi_pm";` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comision` | REG | comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN | nombre_sp |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_insertabitacoracomisiontc2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertabitacoracomisiontc2.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta bitácora y tc — Tarjeta de Crédito · comisión" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertabitacoracomisiontc2(
  pUsuario                     CHAR(8)
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pComision                    CHAR(4)
  pTasaRend                    DECIMAL(11,6)
  pSdoPromMM                   MONEY(18,2)
  pMonMinAper                  MONEY(18,2)
  pComCgoNoSmm                 MONEY(18,2)
  pComCboAclaNP                MONEY(18,2)
  pComChqGirCob                MONEY(18,2)
  pComInaCta                   MONEY(18,2)
  pServTranSpei                MONEY(18,2)
  pServTranTef                 MONEY(18,2)
  pServAnualidad               MONEY(18,2)
  pServReenvToken              MONEY(18,2)
  pServReepToken               MONEY(18,2)
  pDispCtaBcoppel              MONEY(18,2)
  pDispCtaOtrobco              MONEY(18,2)
  pDispLinea                   MONEY(18,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pComision` | `CHAR(4)` | `comision`=comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN |
| `pTasaRend` | `DECIMAL(11,6)` | — | — |
| `pSdoPromMM` | `MONEY(18,2)` | — | — |
| `pMonMinAper` | `MONEY(18,2)` | — | — |
| `pComCgoNoSmm` | `MONEY(18,2)` | — | — |
| `pComCboAclaNP` | `MONEY(18,2)` | — | — |
| `pComChqGirCob` | `MONEY(18,2)` | — | — |
| `pComInaCta` | `MONEY(18,2)` | — | — |
| `pServTranSpei` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pServTranTef` | `MONEY(18,2)` | — | — |
| `pServAnualidad` | `MONEY(18,2)` | — | — |
| `pServReenvToken` | `MONEY(18,2)` | — | — |
| `pServReepToken` | `MONEY(18,2)` | — | — |
| `pDispCtaBcoppel` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDispCtaOtrobco` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDispLinea` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cMensajeRet` | `CHAR(100)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_guardabittascomi_pm` | `bdicheq` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_guardabittascomi_pm";` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `comision` | REG | comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN | nombre_sp |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertacreditocontaccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertacreditocontaccl.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta crédito y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertacreditocontaccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pNivelCcl                    CHAR(14)
  pSaldoConta                  MONEY(18,2)
  pSdoCargosConta              MONEY(18,2)
  pSdoAbonosConta              MONEY(18,2)
  pSdoFinDia                   MONEY(18,2)
  pSdoAbonos                   MONEY(18,2)
  pSdoCargos                   MONEY(18,2)
  pDescripcion                 CHAR(50)
  pTipoProd                    INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pNivelCcl` | `CHAR(14)` | `ccl`=módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (bdicnweb:sp_ccl_*) | 🟡 INFERIDO |
| `pSaldoConta` | `MONEY(18,2)` | `cont`=familia contabilidad | 🔵 CONVENCIÓN |
| `pSdoCargosConta` | `MONEY(18,2)` | `cont`=familia contabilidad | 🔵 CONVENCIÓN |
| `pSdoAbonosConta` | `MONEY(18,2)` | `cont`=familia contabilidad | 🔵 CONVENCIÓN |
| `pSdoFinDia` | `MONEY(18,2)` | — | — |
| `pSdoAbonos` | `MONEY(18,2)` | — | — |
| `pSdoCargos` | `MONEY(18,2)` | — | — |
| `pDescripcion` | `CHAR(50)` | — | — |
| `pTipoProd` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `sp_inserta_conciliador_cred_vs_conta` | `bdicred` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR  pNivelCcl = '' OR  pTipoProd IS NULL THEN` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicred:sp_inserta_conciliador_cred_vs_c` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `credito` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertactualizacomisiontc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertactualizacomisiontc.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 85 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta tc — Tarjeta de Crédito · comisión" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertactualizacomisiontc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pTasaRend                    DECIMAL(9,2)
  pSdoPromMM                   MONEY(18,2)
  pMonMinAper                  MONEY(18,2)
  pComCgoNoSmm                 MONEY(18,2)
  pComCboAclaNP                MONEY(18,2)
  pComChqGirCob                MONEY(18,2)
  pComInaCta                   MONEY(18,2)
  pServTranSpei                MONEY(18,2)
  pServTranTef                 MONEY(18,2)
  pServAnualidad               MONEY(18,2)
  pServReenvToken              MONEY(18,2)
  pServReepToken               MONEY(18,2)
  pDispCtaBcoppel              MONEY(18,2)
  pDispCtaOtrobco              MONEY(18,2)
  pDispLinea                   MONEY(18,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTasaRend` | `DECIMAL(9,2)` | — | — |
| `pSdoPromMM` | `MONEY(18,2)` | — | — |
| `pMonMinAper` | `MONEY(18,2)` | — | — |
| `pComCgoNoSmm` | `MONEY(18,2)` | — | — |
| `pComCboAclaNP` | `MONEY(18,2)` | — | — |
| `pComChqGirCob` | `MONEY(18,2)` | — | — |
| `pComInaCta` | `MONEY(18,2)` | — | — |
| `pServTranSpei` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pServTranTef` | `MONEY(18,2)` | — | — |
| `pServAnualidad` | `MONEY(18,2)` | — | — |
| `pServReenvToken` | `MONEY(18,2)` | — | — |
| `pServReepToken` | `MONEY(18,2)` | — | — |
| `pDispCtaBcoppel` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDispCtaOtrobco` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDispLinea` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cMensajeRet` | `CHAR (100)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_guardatascomi_pm` | `bdicheq` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_guardatascomi_pm";` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ctualiza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `comision` | REG | comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN | nombre_sp |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ctualiza` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertactualizacomisiontc2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertactualizacomisiontc2.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta tc — Tarjeta de Crédito · comisión" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertactualizacomisiontc2(
  pNumCte                      CHAR(20)
  pCuenta                      CHAR(20)
  pTasaRend                    DECIMAL(9,2)
  pSdoPromMM                   MONEY(18,2)
  pMonMinAper                  MONEY(18,2)
  pComCgoNoSmm                 MONEY(18,2)
  pComCboAclaNP                MONEY(18,2)
  pComChqGirCob                MONEY(18,2)
  pComInaCta                   MONEY(18,2)
  pServTranSpei                MONEY(18,2)
  pServTranTef                 MONEY(18,2)
  pServAnualidad               MONEY(18,2)
  pServReenvToken              MONEY(18,2)
  pServReepToken               MONEY(18,2)
  pDispCtaBcoppel              MONEY(18,2)
  pDispCtaOtrobco              MONEY(18,2)
  pDispLinea                   MONEY(18,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTasaRend` | `DECIMAL(9,2)` | — | — |
| `pSdoPromMM` | `MONEY(18,2)` | — | — |
| `pMonMinAper` | `MONEY(18,2)` | — | — |
| `pComCgoNoSmm` | `MONEY(18,2)` | — | — |
| `pComCboAclaNP` | `MONEY(18,2)` | — | — |
| `pComChqGirCob` | `MONEY(18,2)` | — | — |
| `pComInaCta` | `MONEY(18,2)` | — | — |
| `pServTranSpei` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pServTranTef` | `MONEY(18,2)` | — | — |
| `pServAnualidad` | `MONEY(18,2)` | — | — |
| `pServReenvToken` | `MONEY(18,2)` | — | — |
| `pServReepToken` | `MONEY(18,2)` | — | — |
| `pDispCtaBcoppel` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDispCtaOtrobco` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDispLinea` | `MONEY(18,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cMensajeRet` | `CHAR (100)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_guardatascomi_pm` | `bdicheq` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_guardatascomi_pm";` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?ctualiza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `comision` | REG | comisión (CONDUSEF — debe estar en RECO) | 🔵 CONVENCIÓN | nombre_sp |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ctualiza`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertadoctopendienteimgdigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertadoctopendienteimgdigitalizacion.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta documento y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertadoctopendienteimgdigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pProducto                    CHAR(4)
  pCodDocto                    CHAR(4)
  pProdNombre                  CHAR(40)
  pSecuencia                   INTEGER
  pCtlArchivoLocal             CHAR(25)
  pCtlArchivoLocalRuta         CHAR(150)
  pCtlImgEnviada               CHAR(1)
  pCtlProcesado                CHAR(1)
  pCtlLigado                   CHAR(1)
  pMacaddressLocal             CHAR(17)
  pDescrip2                    CHAR(30)
  pFechaAlta                   DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pCodDocto` | `CHAR(4)` | `docto`=documento | 🔵 CONVENCIÓN |
| `pProdNombre` | `CHAR(40)` | — | — |
| `pSecuencia` | `INTEGER` | — | — |
| `pCtlArchivoLocal` | `CHAR(25)` | — | — |
| `pCtlArchivoLocalRuta` | `CHAR(150)` | — | — |
| `pCtlImgEnviada` | `CHAR(1)` | — | — |
| `pCtlProcesado` | `CHAR(1)` | — | — |
| `pCtlLigado` | `CHAR(1)` | — | — |
| `pMacaddressLocal` | `CHAR(17)` | — | — |
| `pDescrip2` | `CHAR(30)` | — | — |
| `pFechaAlta` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_diginsertadoctopendienteimagen` | `bdidigital` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `docto` | ENTIDAD | documento | 🔵 CONVENCIÓN | nombre_sp |
| `?pendienteimg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendienteimg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertaimgpreviodigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertaimgpreviodigitalizacion.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 121 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta digitalización de documentos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertaimgpreviodigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCliente                     CHAR(20)
  pCodDocto                    CHAR(4)
  pImgFormato                  CHAR(3)
  pSvrRemoto                   CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCodDocto` | `CHAR(4)` | — | — |
| `pImgFormato` | `CHAR(3)` | — | — |
| `pSvrRemoto` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `sSecuencia` | `SMALLINT` | L10 |
| `cQuery` | `CHAR(1500)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?imgp` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?io` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imgp`, `?io` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertaimgpreviodigitalizacion2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertaimgpreviodigitalizacion2.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta digitalización de documentos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertaimgpreviodigitalizacion2(
  pUsuario                     CHAR(8)
  pCliente                     CHAR(20)
  pCodDocto                    CHAR(4)
  pImgFormato                  CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCodDocto` | `CHAR(4)` | — | — |
| `pImgFormato` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `sSecuencia` | `SMALLINT` | L10 |
| `cQuery` | `CHAR(1500)` | L11 |
| `cSvrRemoto` | `CHAR(30)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `dg_params` | `bdidigital` | ⚠️ sí | SELECT | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?imgp` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?io` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imgp`, `?io`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertaregexpedientedigitalizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertaregexpedientedigitalizacion.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta regex — motor de expresiones regulares Informix SPL y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertaregexpedientedigitalizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pProducto                    CHAR(4)
  pCodigoDocumento             CHAR(4)
  pSecuencia                   SMALLINT
  pProductonombre              CHAR(40)
  pDescrip2                    CHAR(30)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pCodigoDocumento` | `CHAR(4)` | — | — |
| `pSecuencia` | `SMALLINT` | — | — |
| `pProductonombre` | `CHAR(40)` | — | — |
| `pDescrip2` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `inserta_reg_expediente` | `bdidigital` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `regex` | ENTIDAD | regex — motor de expresiones regulares Informix SPL (infraes | 🔴 SINTÉTICO | nombre_sp |
| `?pediente` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `regex`, `?pediente` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertaregexpedientedigitalizacion2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertaregexpedientedigitalizacion2.sql` |
| **LOC (1er CREATE)** | 50 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta regex — motor de expresiones regulares Informix SPL y digitalización de documentos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertaregexpedientedigitalizacion2(
  pUsuario                     CHAR(8)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pProducto                    CHAR(4)
  pCodigoDocumento             CHAR(4)
  pSecuencia                   SMALLINT
  pProductonombre              CHAR(40)
  pDescrip2                    CHAR(30)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pCodigoDocumento` | `CHAR(4)` | — | — |
| `pSecuencia` | `SMALLINT` | — | — |
| `pProductonombre` | `CHAR(40)` | — | — |
| `pDescrip2` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `inserta_reg_expediente` | `bdidigital` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `regex` | ENTIDAD | regex — motor de expresiones regulares Informix SPL (infraes | 🔴 SINTÉTICO | nombre_sp |
| `?pediente` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digitalizacion` | ENTIDAD | digitalización de documentos | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `regex`, `?pediente`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertatipocambio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_insertatipocambio.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta cambio (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertatipocambio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDivisa                      CHAR(2)
  pVariacion_cpa               DECIMAL(9,7)
  pTipo_cpa_mn_dll             DECIMAL(14,6)
  pTipo_cpa_div_dll            DECIMAL(14,6)
  pTipo_cpa_mn_div             DECIMAL(14,6)
  pPc_abajo                    DECIMAL(14,6)
  pPc_arriba                   DECIMAL(14,6)
  pPrecio_compra               DECIMAL(14,6)
  pPv_abajo                    DECIMAL(14,6)
  pVariacion_vta               DECIMAL(9,7)
  pTipo_vta_div_dll            DECIMAL(14,6)
  pTipo_vta_mn_dll             DECIMAL(14,6)
  pTipo_vta_mn_div             DECIMAL(14,6)
  pPrecio_venta                DECIMAL(14,6)
  pPv_arriba                   DECIMAL(14,6)
  pFecha_tpcambio              DATE
  pClase_tpcambio              CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDivisa` | `CHAR(2)` | — | — |
| `pVariacion_cpa` | `DECIMAL(9,7)` | — | — |
| `pTipo_cpa_mn_dll` | `DECIMAL(14,6)` | `tipo`=tipo de | ✅ CÓDIGO |
| `pTipo_cpa_div_dll` | `DECIMAL(14,6)` | `tipo`=tipo de | ✅ CÓDIGO |
| `pTipo_cpa_mn_div` | `DECIMAL(14,6)` | `tipo`=tipo de | ✅ CÓDIGO |
| `pPc_abajo` | `DECIMAL(14,6)` | — | — |
| `pPc_arriba` | `DECIMAL(14,6)` | — | — |
| `pPrecio_compra` | `DECIMAL(14,6)` | — | — |
| `pPv_abajo` | `DECIMAL(14,6)` | — | — |
| `pVariacion_vta` | `DECIMAL(9,7)` | — | — |
| `pTipo_vta_div_dll` | `DECIMAL(14,6)` | `tipo`=tipo de | ✅ CÓDIGO |
| `pTipo_vta_mn_dll` | `DECIMAL(14,6)` | `tipo`=tipo de | ✅ CÓDIGO |
| `pTipo_vta_mn_div` | `DECIMAL(14,6)` | `tipo`=tipo de | ✅ CÓDIGO |
| `pPrecio_venta` | `DECIMAL(14,6)` | — | — |
| `pPv_arriba` | `DECIMAL(14,6)` | — | — |
| `pFecha_tpcambio` | `DATE` | `cambio`=cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO |
| `pClase_tpcambio` | `CHAR(1)` | `cambio`=cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tpcambio` | `bdinteg` | ⚠️ sí | SELECT | L47 |
| `si_tpcambio` | `bdinteg` | ⚠️ sí | INSERT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pVariacion_cpa IS NULL OR pTipo_cpa_mn_dll IS NULL OR pTipo_c` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ipab_consulta_casual_fraccion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_consulta_casual_fraccion.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta cc — cuenta corriente · IPAB — Instituto para la Protección al Ahorro Bancario" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_consulta_casual_fraccion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(10)
  pNumcte                      CHAR(12)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(10)` | — | — |
| `pNumcte` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cUsuario` | `CHAR(8)` | L13 |
| `icasual` | `INTEGER` | L14 |
| `cDesCasual` | `CHAR(40)` | L15 |
| `cFraccion` | `CHAR(2)` | L16 |
| `cConcepto` | `CHAR(2)` | L17 |
| `cDesFraccion` | `CHAR(40)` | L18 |
| `iRegistros` | `INTEGER` | L19 |
| `cInversion` | `CHAR(10)` | L20 |
| `cArea` | `CHAR(2)` | L21 |
| `cDescripion` | `CHAR(40)` | L22 |
| `cFecha` | `DATETIME YEAR TO FRACTION(3)` | L23 |
| `irowid` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | SELECT | L73 |
| `si_ipab_marcaje_fracciones` | `bdinteg` | ⚠️ sí | SELECT | L92 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_ipab_consultarea_usuario` | `bdicnweb` | no | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_casual_fra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_casual_fra`, `?ion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ipab_consulta_fideicomiso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_consulta_fideicomiso.sql` |
| **LOC (1er CREATE)** | 137 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta identificador y Comisión bancaria — cobro de comisión sobre cuenta · IPAB — Instituto para la Protección al Ahorro Bancario" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_consulta_fideicomiso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumcte                      CHAR(12)
  pIdcasual                    INTEGER
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumcte` | `CHAR(12)` | — | — |
| `pIdcasual` | `INTEGER` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cUsuario` | `CHAR(8)` | L23 |
| `icasual` | `INTEGER` | L24 |
| `iRegistros` | `INTEGER` | L25 |
| `cArea` | `CHAR(2)` | L26 |
| `cDescripion` | `CHAR(40)` | L27 |
| `cNumFideicomiso` | `CHAR(10)` | L29 |
| `cNombre_fideicomiso` | `CHAR(100)` | L30 |
| `cAlta_fideicomiso` | `char(40)` | L31 |
| `cFideicomitente` | `char(80)` | L32 |
| `cFideicomisario` | `CHAR(100)` | L33 |
| `cFines_fideicomiso` | `char(80)` | L34 |
| `cTipo_fideicomiso` | `char(80)` | L35 |
| `cPatrimonio_inicial` | `char(80)` | L36 |
| `cMoneda_nacional` | `char(40)` | L37 |
| `cMoneda_extranjera` | `char(40)` | L38 |
| `cRepresentante` | `CHAR(100)` | L39 |
| `iRowid` | `INTEGER` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | SELECT | L94 |
| `si_ipab_marcaje_fideicomiso` | `bdinteg` | ⚠️ sí | SELECT | L99 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L79 |
| `sp_ipab_consultarea_usuario` | `bdicnweb` | no | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?iso` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_f`, `?ei`, `?iso` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ipab_consulta_usuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_consulta_usuario.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuario · IPAB — Instituto para la Protección al Ahorro Bancario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_consulta_usuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cUsuario` | `CHAR(8)` | L8 |
| `cNombre` | `CHAR(45)` | L9 |
| `iRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET iregistros = iRegistros + 1;` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ipab_consulta_usuario_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_consulta_usuario_totales.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuario (totales) · IPAB — Instituto para la Protección al Ahorro Bancario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_consulta_usuario_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L35 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet  = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ipab_consultarea_usuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_consultarea_usuario.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=41 / fan_out=46 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar usuario · IPAB — Instituto para la Protección al Ahorro Bancario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_consultarea_usuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cUsuario` | `CHAR(8)` | L9 |
| `cArea` | `CHAR(2)` | L10 |
| `cPerfil` | `INTEGER` | L11 |
| `cDescripion` | `CHAR(40)` | L12 |
| `iRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ipab_marcaje_areas_usuarios` | `bdinteg` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?ea_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ea_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ipab_marcaje_areas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_marcaje_areas.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "marca · IPAB — Instituto para la Protección al Ahorro Bancario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_marcaje_areas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iArea` | `INTEGER` | L9 |
| `cDescripion` | `CHAR(40)` | L10 |
| `iRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ipab_marcaje_areas` | `bdinteg` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `marca` | ENTIDAD | marca | 🔵 CONVENCIÓN | nombre_sp |
| `?je_areas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?je_areas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ipab_marcaje_casual`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_marcaje_casual.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "marca · IPAB — Instituto para la Protección al Ahorro Bancario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_marcaje_casual(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `icasual` | `INTEGER` | L8 |
| `cDescripion` | `CHAR(40)` | L9 |
| `iRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ipab_marcaje_causal` | `bdinteg` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `marca` | ENTIDAD | marca | 🔵 CONVENCIÓN | nombre_sp |
| `?je_casual` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?je_casual` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ipab_ope_casual_fraccion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_ope_casual_fraccion.sql` |
| **LOC (1er CREATE)** | 232 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cc — cuenta corriente · IPAB — Instituto para la Protección al Ahorro Bancario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_ope_casual_fraccion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(11)
  pNumCliente                  CHAR(10)
  pFraccion                    CHAR(2)
  pConcpeto                    CHAR(2)
  pidCasual                    INTEGER
  pArea                        INTEGER
  pConsulta                    SMALLINT
) RETURNING CHAR(5)  AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(11)` | — | — |
| `pNumCliente` | `CHAR(10)` | — | — |
| `pFraccion` | `CHAR(2)` | `cc`=cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO |
| `pConcpeto` | `CHAR(2)` | — | — |
| `pidCasual` | `INTEGER` | — | — |
| `pArea` | `INTEGER` | — | — |
| `pConsulta` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cUsuario` | `CHAR(8)` | L15 |
| `icasual` | `INTEGER` | L16 |
| `cDesCasual` | `CHAR(40)` | L17 |
| `cFraccion` | `CHAR(2)` | L18 |
| `cConcepto` | `CHAR(2)` | L19 |
| `cDesFraccion` | `CHAR(40)` | L20 |
| `iRegistros` | `INTEGER` | L21 |
| `cTipoPersona` | `CHAR(2)` | L23 |
| `cNombre1` | `CHAR(50)` | L24 |
| `cNombre2` | `CHAR(26)` | L25 |
| `cApellidoPa` | `CHAR(26)` | L26 |
| `cApellidoMa` | `CHAR(26)` | L27 |
| `cExclusion` | `CHAR(50)` | L28 |
| `cTipoPersonaDes` | `CHAR(10)` | L29 |
| `cInversion` | `CHAR(10)` | L30 |
| `cBanco` | `CHAR(30)` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | SELECT | L80 |
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L99 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L104 |
| `si_ipab_marcaje_causal` | `bdinteg` | ⚠️ sí | SELECT | L115 |
| `si_ipab_marcaje_fracciones` | `bdinteg` | ⚠️ sí | SELECT | L122 |
| `sv_maeinv` | `bdinvers` | ⚠️ sí | SELECT | L126 |
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | INSERT | L136 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L144 |
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | UPDATE | L167 |
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | DELETE | L197 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | VALIDACIÓN_NULL | `IF iRegistros = 0 or iRegistros is null THEN` |  |
| L218 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_casual_fra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_casual_fra`, `?ion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ipab_ope_fideicomiso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_ope_fideicomiso.sql` |
| **LOC (1er CREATE)** | 212 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación identificador y Comisión bancaria — cobro de comisión sobre cuenta · IPAB — Instituto para la Protección al Ahorro Bancario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_ope_fideicomiso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(11)
  pNumCliente                  CHAR(10)
  pFraccion                    CHAR(2)
  pConcepto                    CHAR(2)
  pidCasual                    INTEGER
  pConsulta                    SMALLINT
  pno_fideicomiso              char(10)
  pnombre_fideicomiso          char(100)
  palta_fideicomiso            char(40)
  pfideicomitente              char(80)
  pfideicomisario              char(80)
  pfines_fideicomiso           char(100)
  ptipo_fideicomiso            char(80)
  pPatrimonio_inicial          char(80)
  pmoneda_nacional             char(40)
  pmoneda_extranjera           char(40)
  prepresentante               char(100)
  pArea                        INTEGER
  pRowId                       INTEGER
) RETURNING CHAR(5)  AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pCuenta` | `CHAR(11)` | — | — |
| `pNumCliente` | `CHAR(10)` | — | — |
| `pFraccion` | `CHAR(2)` | — | — |
| `pConcepto` | `CHAR(2)` | — | — |
| `pidCasual` | `INTEGER` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pConsulta` | `SMALLINT` | — | — |
| `pno_fideicomiso` | `char(10)` | `id`=identificador (de) · `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) | 🔵 CONVENCIÓN |
| `pnombre_fideicomiso` | `char(100)` | `id`=identificador (de) · `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) | 🔵 CONVENCIÓN |
| `palta_fideicomiso` | `char(40)` | `id`=identificador (de) · `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) | 🔵 CONVENCIÓN |
| `pfideicomitente` | `char(80)` | `id`=identificador (de) · `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) | 🔵 CONVENCIÓN |
| `pfideicomisario` | `char(80)` | `id`=identificador (de) · `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) | 🔵 CONVENCIÓN |
| `pfines_fideicomiso` | `char(100)` | `id`=identificador (de) · `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) | 🔵 CONVENCIÓN |
| `ptipo_fideicomiso` | `char(80)` | `id`=identificador (de) · `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) | 🔵 CONVENCIÓN |
| `pPatrimonio_inicial` | `char(80)` | — | — |
| `pmoneda_nacional` | `char(40)` | — | — |
| `pmoneda_extranjera` | `char(40)` | — | — |
| `prepresentante` | `char(100)` | — | — |
| `pArea` | `INTEGER` | — | — |
| `pRowId` | `INTEGER` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `cUsuario` | `CHAR(8)` | L28 |
| `icasual` | `INTEGER` | L29 |
| `cDesCasual` | `CHAR(40)` | L30 |
| `cFraccion` | `CHAR(2)` | L31 |
| `cConcepto` | `CHAR(2)` | L32 |
| `cDesFraccion` | `CHAR(40)` | L33 |
| `iRegistros` | `INTEGER` | L34 |
| `cTipoPersona` | `CHAR(2)` | L36 |
| `cNombre1` | `CHAR(50)` | L37 |
| `cNombre2` | `CHAR(26)` | L38 |
| `cApellidoPa` | `CHAR(26)` | L39 |
| `cApellidoMa` | `CHAR(26)` | L40 |
| `cInversion` | `CHAR(10)` | L41 |
| `cTipoPersonaDes` | `CHAR(10)` | L42 |
| `cExclusion` | `CHAR(50)` | L43 |
| `cNumFideicomiso` | `CHAR(10)` | L44 |
| `cBanco` | `CHAR(30)` | L45 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L94 |
| `si_ipab_marcaje_fideicomiso` | `bdinteg` | ⚠️ sí | INSERT | L97 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L103 |
| `si_ipab_marcaje_causal` | `bdinteg` | ⚠️ sí | SELECT | L114 |
| `sv_maeinv` | `bdinvers` | ⚠️ sí | SELECT | L117 |
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | INSERT | L126 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L134 |
| `si_ipab_marcaje_fracciones` | `bdinteg` | ⚠️ sí | SELECT | L151 |
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | SELECT | L155 |
| `si_ipab_marcaje_fideicomiso` | `bdinteg` | ⚠️ sí | UPDATE | L159 |
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | UPDATE | L181 |
| `si_ipab_marcaje_fideicomiso` | `bdinteg` | ⚠️ sí | SELECT | L196 |
| `si_ipab_marcaje_fideicomiso` | `bdinteg` | ⚠️ sí | DELETE | L196 |
| `si_ipab_marcaje_excluidos` | `bdinteg` | ⚠️ sí | DELETE | L198 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?iso` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_f`, `?ei`, `?iso` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ipab_repfideicomisomarcajeipab`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_repfideicomisomarcajeipab.sql` |
| **LOC (1er CREATE)** | 172 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte identificador, Comisión bancaria — cobro de comisión sobre cuenta y marca · IPAB — Instituto para la Protección al Ahorro Bancario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=4 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_repfideicomisomarcajeipab(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
  pCasual                      INTEGER
  pIdArea                      INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pCasual` | `INTEGER` | — | — |
| `pIdArea` | `INTEGER` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCmd1` | `CHAR(4000)` | L13 |
| `cSql` | `CHAR(4000)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cNombreArchivo` | `CHAR(100)` | L16 |
| `iTotal` | `INTEGER` | L17 |
| `dFechaHoy` | `DATE` | L18 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L19 |
| `cFechaHoraArchivo` | `CHAR(35)` | L20 |
| `nomFecha` | `CHAR(19)` | L21 |
| `cFecha` | `CHAR(10)` | L22 |
| `iRegistros` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ipab_marcaje_fideicomiso` | `bdinteg` | ⚠️ sí | SELECT | L64 |
| `systables` | `bdicnweb` | no | SELECT | L83 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | VALIDACIÓN_NULL | `IF iRegistros = 0  OR iRegistros IS NULL THEN` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L82 | FÓRMULA | `LET cCmd1 ="SELECT * FROM( ";` |  |
| L84 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT *  FROM (SELECT f.no_fideicomiso, f.nombre_fideic` |  |
| L96 | FÓRMULA | `LET cRutaGral = TRIM(pRutaDescarga)\|\|'/'\|\|TRIM(cNombreArchivo);` |  |
| L124 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L129 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L138 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L147 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?iso` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `marca` | ENTIDAD | marca | 🔵 CONVENCIÓN | nombre_sp |
| `?je` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?f`, `?ei`, `?iso`, `?je` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ipab_validausuariopass`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ipab_validausuariopass.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida usuario y ss — subsistema · IPAB — Instituto para la Protección al Ahorro Bancario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ipab_validausuariopass(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPass                        CHAR(50)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPass` | `CHAR(50)` | `ss`=ss — subsistema / canal de monitoreo (abreviación — envia_monitorsol_*_ss_* — bdisolic) | 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cUsuario` | `CHAR(8)` | L8 |
| `cExitoso` | `INTEGER` | L9 |
| `iRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_usuarios` | `bdinteg` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ipab` | REG | IPAB — Instituto para la Protección al Ahorro Bancario (segu | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pa`, `ss` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_kiosko_consultamovprestamos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_kiosko_consultamovprestamos.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes, movimiento y préstamos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_kiosko_consultamovprestamos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  cNUMCUENTA                   CHAR(20)
  dPERIODOI                    DATE
  dPERIODOF                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `cNUMCUENTA` | `CHAR(20)` | — | — |
| `dPERIODOI` | `DATE` | — | — |
| `dPERIODOF` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `iNoRegistros` | `INTEGER` | L24 |
| `iRegistros` | `INTEGER` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |
| `dFecha` | `DATE` | L27 |
| `dHora` | `DATETIME HOUR to FRACTION(3)` | L28 |
| `cTransaccion` | `CHAR(4)` | L29 |
| `cD_Transaccion` | `CHAR(120)` | L30 |
| `mMonto` | `MONEY(18,2)` | L31 |
| `cNaturaleza` | `CHAR(1)` | L32 |
| `cReferencia` | `CHAR(40)` | L33 |
| `cRfcComer` | `CHAR(10)` | L34 |
| `cReversos` | `CHAR(1)` | L35 |
| `cReversados` | `CHAR(1)` | L36 |
| `cSucursal` | `CHAR(4)` | L37 |
| `cFolio` | `CHAR(16)` | L38 |
| `dPeriodoI_1` | `DATE` | L39 |
| `dPeriodoF_1` | `DATE` | L40 |
| `sNUMSERIAL` | `INT8` | L41 |
| `sNumSecuencia` | `INT8` | L42 |
| `cUsuario` | `CHAR(8)` | L43 |
| `cReferencia23` | `CHAR(23)` | L44 |
| `cNumtarjeta` | `CHAR(20)` | L45 |
| *…3 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_movdiacrd` | `bdicred` | ⚠️ sí | SELECT | L118 |
| `sd_movhiscrd` | `bdicred` | ⚠️ sí | SELECT | L126 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L106 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L91 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR cNUMCUENTA = '' OR dPERIODOI IS NULL OR dPERIODOF IS NULL OR ` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L132 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00039';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ki` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ko_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `prestamos` | ENTIDAD | préstamos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ki`, `?ko_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_kiosko_consultasdos_prestamo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_kiosko_consultasdos_prestamo.sql` |
| **LOC (1er CREATE)** | 291 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes, saldos y préstamo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ofi_consultasdos`, `sp_consulta_saldos_general` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_kiosko_consultasdos_prestamo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS Cod_Ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `cCodRetSp2` | `CHAR(5)` | L26 |
| `cCodRetSp` | `CHAR(6)` | L27 |
| `iCodRetSp` | `INTEGER` | L28 |
| `cEmpresa` | `CHAR(3)` | L29 |
| `cMensajeRet` | `CHAR(80)` | L30 |
| `dSdoUltCorte` | `DECIMAL(18,2)` | L31 |
| `dAhorroPago` | `DECIMAL(18,2)` | L32 |
| `cNumCteCD` | `CHAR(20)` | L33 |
| `cNomProductoCD` | `CHAR(40)` | L34 |
| `cNomCteCD` | `CHAR(150)` | L35 |
| `dFecha_Origen` | `DATE` | L36 |
| `dFecha_Prox_Pago` | `DATE` | L37 |
| `mPago_Minimo` | `MONEY(18,2)` | L38 |
| `iPlazo_Sg` | `INTEGER` | L39 |
| `iPagos_Realizados` | `INTEGER` | L40 |
| `mLinea_Otorgada` | `MONEY(18,2)` | L41 |
| `mCap_Vdo_Exig` | `MONEY(18,2)` | L42 |
| `mInt_Moratorios` | `MONEY(18,2)` | L43 |
| `mIva_Int_Moratorios` | `MONEY(18,2)` | L44 |
| `mCom_Pend` | `MONEY(18,2)` | L45 |
| `mIva_Com` | `MONEY(18,2)` | L46 |
| `mTotal_Liquidacion` | `MONEY(18,2)` | L47 |
| `mInt_Devengado` | `MONEY(18,2)` | L48 |
| *…52 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L196 |
| `sp_ofi_consultasdos` | `bdicred` | ⚠️ sí | L202 |
| `sp_consulta_saldos_general` | `bdicred` | ⚠️ sí | L230 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L103 | CÓDIGO_RETORNO | `LET cCodRet           	  = '00000';` |  |
| L190 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L210 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicred:sp_ofi_consultasdos";` |  |
| L212 | CÓDIGO_RETORNO | `LET cCodRet = '00393';` |  |
| L216 | CÓDIGO_RETORNO | `LET cCodRet = '00394';` |  |
| L220 | CÓDIGO_RETORNO | `LET cCodRet = '00395';` |  |
| L224 | CÓDIGO_RETORNO | `LET cCodRet = '00396';` |  |
| L243 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicred:sp_consulta_saldos_general";` |  |
| L245 | CÓDIGO_RETORNO | `LET cCodRet = '00397';` |  |
| L249 | CÓDIGO_RETORNO | `LET cCodRet = '00397';` |  |
| L253 | CÓDIGO_RETORNO | `LET cCodRet = '00398';` |  |
| L257 | CÓDIGO_RETORNO | `LET cCodRet = '00398';` |  |
| L261 | CÓDIGO_RETORNO | `LET cCodRet = '00399';` |  |
| L265 | CÓDIGO_RETORNO | `LET cCodRet = '00400';` |  |
| L269 | CÓDIGO_RETORNO | `LET cCodRet = '00401';` |  |
| L273 | CÓDIGO_RETORNO | `LET cCodRet = '00401';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ki` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ko_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sdos` | ENTIDAD | saldos (abreviación) | 🟡 INFERIDO | nombre_sp |
| `prestamo` | ENTIDAD | préstamo (Personal / Nómina / Digital BanCoppel) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ki`, `?ko_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_kiosko_consultatotalmovprestamos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_kiosko_consultatotalmovprestamos.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta OS — Originación de Solicitudes, movimiento y préstamos (total)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_kiosko_consultatotalmovprestamos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  cNUMCUENTA                   CHAR(20)
  dPERIODOI                    DATE
  dPERIODOF                    DATE
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `cNUMCUENTA` | `CHAR(20)` | — | — |
| `dPERIODOI` | `DATE` | — | — |
| `dPERIODOF` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iexiste` | `INT` | L6 |
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iCont` | `INT` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_movdiacrd` | `bdicred` | ⚠️ sí | SELECT | L45 |
| `sd_movhiscrd` | `bdicred` | ⚠️ sí | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR cNUMCUENTA = '' OR dPERIODOI IS NULL OR dPERIODOF IS NULL THE` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iCont= iCont + iexiste;` |  |
| L60 | FÓRMULA | `LET iCont= iCont + iexiste;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ki` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ko_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `prestamos` | ENTIDAD | préstamos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ki`, `?ko_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_lecturarchivodatosimportar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_lecturarchivodatosimportar.sql` |
| **LOC (1er CREATE)** | 1739 |
| **Callgraph** | ✅ fan_in=9 / fan_out=7 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "archivo, datos, Impago — pago vencido o fallido; confirmado: n_impagos_consec y Tarjeta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_lecturarchivodatosimportar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pRutaArchivo                 CHAR(100)
  pDireccionMac                CHAR(12)
  pCodOperacion                CHAR(5)
  pFechaHoy                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `archivo`=archivo | ✅ CÓDIGO |
| `pRutaArchivo` | `CHAR(100)` | `archivo`=archivo | ✅ CÓDIGO |
| `pDireccionMac` | `CHAR(12)` | — | — |
| `pCodOperacion` | `CHAR(5)` | — | — |
| `pFechaHoy` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cIdCodRet` | `CHAR(5)` | L8 |
| `cDescIdCodRet` | `CHAR(100)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cIdCodRetSp` | `CHAR(6)` | L11 |
| `cDescMensajeError` | `CHAR(100)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cRenglon` | `CHAR(400)` | L15 |
| `iLinea` | `INTEGER` | L16 |
| `cSeccion` | `CHAR(12)` | L17 |
| `cCampo` | `CHAR(2)` | L18 |
| `cTexto` | `CHAR(100)` | L19 |
| `cEncTipoRegistro` | `CHAR(2)` | L20 |
| `cEncNumSecuencia` | `CHAR(7)` | L21 |
| `cEncNumBanco` | `CHAR(3)` | L22 |
| `cEncSentidoTransfer` | `CHAR(1)` | L23 |
| `cEncPlazaCecoban` | `CHAR(2)` | L24 |
| `cEncServicioTei` | `CHAR(1)` | L25 |
| `cEncDiaMesTransfer` | `CHAR(2)` | L26 |
| `cEncNumBloque` | `CHAR(5)` | L27 |
| `cEnpFechaPresenta` | `CHAR(8)` | L28 |
| `cEncUsoFuturo1` | `CHAR(9)` | L29 |
| `cEncTipoArchivo` | `CHAR(1)` | L30 |
| `cEncUsoFuturo2` | `CHAR(302)` | L31 |
| *…133 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_statuslecturaarchivos` | `bdicnweb` | no | SELECT | L377 |
| `sw_cr_statuslecturaarchivos` | `bdicnweb` | no | DELETE | L377 |
| `sw_cr_statuslecturaarchivos` | `bdicnweb` | no | INSERT | L382 |
| `systables` | `bdicnweb` | no | SELECT | L390 |
| `sw_cr_bitacoraerror` | `bdicnweb` | no | SELECT | L398 |
| `sw_cr_bitacoraerror` | `bdicnweb` | no | DELETE | L398 |
| `sw_cr_procesaencabezado_tmp` | `bdicnweb` | no | SELECT | L399 |
| `sw_cr_procesaencabezado_tmp` | `bdicnweb` | no | DELETE | L399 |
| `sw_cr_procesadetalle_tmp` | `bdicnweb` | no | SELECT | L400 |
| `sw_cr_procesadetalle_tmp` | `bdicnweb` | no | DELETE | L400 |
| `sw_cr_procesasumario_tmp` | `bdicnweb` | no | SELECT | L401 |
| `sw_cr_procesasumario_tmp` | `bdicnweb` | no | DELETE | L401 |
| `sw_cr_procesagransumario_tmp` | `bdicnweb` | no | SELECT | L402 |
| `sw_cr_procesagransumario_tmp` | `bdicnweb` | no | DELETE | L402 |
| `cr_cargaarchivos_tmp` | `bdicnweb` | no | INSERT | L406 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L417 |
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L420 |
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L423 |
| `cr_cargaarchivos_tmp` | `bdicnweb` | no | SELECT | L447 |
| `sw_cr_bitacoraerror` | `bdicnweb` | no | INSERT | L455 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | INSERT | L459 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | SELECT | L460 |
| `sw_cr_procesaencabezado_tmp` | `bdicnweb` | no | INSERT | L611 |
| `sw_cr_procesadetalle_tmp` | `bdicnweb` | no | INSERT | L766 |
| `si_coddevcam` | `bdinteg` | ⚠️ sí | SELECT | L820 |
| `sw_cr_procesasumario_tmp` | `bdicnweb` | no | INSERT | L925 |
| `sw_cr_procesagransumario_tmp` | `bdicnweb` | no | INSERT | L1023 |
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L1677 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L364 |
| `cal_habil_ant` | `bditef` | ⚠️ sí | L433 |
| `sp_validacaracter` | `bdicnweb` | no | L624 |
| `sp_calculadigitointercambio` | `bdicnweb` | no | L793 |
| `stat_cheque` | `bditef` | ⚠️ sí | L1628 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L169 | CÓDIGO_RETORNO | `LET cIdCodRet = '00000';` |  |
| L352 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pNombreArchivo = '' OR pRutaArchivo = '' OR pDireccionMac = ` |  |
| L353 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L411 | FÓRMULA | `Let cSQL = TRIM(cPathdbaccess)\|\|'dbaccess bdicnweb ' \|\|TRIM(pRutaArchivo)\|\| 'Temporal.sql'; --` |  |
| L449 | FÓRMULA | `LET iLinea = iLinea + 1;` |  |
| L488 | FÓRMULA | `LET iContEncabezado = iContEncabezado + 1;` |  |
| L620 | FÓRMULA | `LET iNroSecuencia = iNroSecuencia + 1;` |  |
| L621 | FÓRMULA | `LET iContDetalle = iContDetalle + 1;` |  |
| L757 | FÓRMULA | `LET mValorImporte = SUBSTR(cDetImporte,1,(LENGTH(cDetImporte)-2))\|\|"."\|\|SUBSTR(cDetImporte,(LENG` | 🔴 MONEY/aritmética financiera |
| L758 | FÓRMULA | `LET mTotalImporte = mTotalImporte + NVL(mValorImporte,0);` | 🔴 MONEY/aritmética financiera |
| L759 | FÓRMULA | `LET mTotalImpCecoban = mTotalImpCecoban + NVL(mValorImporte,0);` | 🔴 MONEY/aritmética financiera |
| L762 | FÓRMULA | `LET iTotImagenesTruncadas = iTotImagenesTruncadas + 1;` | 🔴 MONEY/aritmética financiera |
| L763 | FÓRMULA | `LET iImagenesTrucadas = iImagenesTrucadas + 1;` |  |
| L805 | FÓRMULA | `LET iSuma = (cDetChqCodSeguridad::INTEGER + (iDetOper) + cDetNumCuenta::BIGINT) + cDetNumCheque::INT` |  |
| L807 | FÓRMULA | `LET iDiv = CAST ((iSuma/9) AS BIGINT);` |  |
| L808 | FÓRMULA | `LET iDigitoDiv = iSuma - (iDiv * 9);` |  |
| L809 | FÓRMULA | `LET iDigitoDiv = 9 - iDigitoDiv;` |  |
| L828 | FÓRMULA | `LET iTotRegDet = iTotRegDet + 1;` |  |
| L829 | FÓRMULA | `LET iTotRegLeidos = iTotRegLeidos + 1;` |  |
| L837 | FÓRMULA | `LET iContSumario = iContSumario + 1;` |  |
| L883 | FÓRMULA | `LET mImporte = SUBSTR(cSumImporte,1,(LENGTH(cSumImporte)-2))\|\|"."\|\|SUBSTR(cSumImporte,(LENGTH(cS` | 🔴 MONEY/aritmética financiera |
| L934 | FÓRMULA | `LET iContGranSumario = iContGranSumario + 1;` |  |
| L985 | FÓRMULA | `LET mImporte = SUBSTR(cGranSumImporteTotal,1,(LENGTH(cGranSumImporteTotal)-2))\|\|"."\|\|SUBSTR(cGra` | 🔴 MONEY/aritmética financiera |
| L1174 | FÓRMULA | `LET iNroSecuencia = iNroSecuencia + 1;` |  |
| L1176 | FÓRMULA | `LET iContDetalle = iContDetalle + 1;` |  |
| L1312 | FÓRMULA | `LET mValorImporte = SUBSTR(cDetImporte,1,(LENGTH(cDetImporte)-2))\|\|"."\|\|SUBSTR(cDetImporte,(LENG` | 🔴 MONEY/aritmética financiera |
| L1313 | FÓRMULA | `LET mTotalImporte = mTotalImporte + NVL(mValorImporte,0);` | 🔴 MONEY/aritmética financiera |
| L1314 | FÓRMULA | `LET mTotalImpCecoban = mTotalImpCecoban + NVL(mValorImporte,0);` | 🔴 MONEY/aritmética financiera |
| L1317 | FÓRMULA | `LET iTotImagenesTruncadas = iTotImagenesTruncadas + 1;` | 🔴 MONEY/aritmética financiera |
| | *…9 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_lectur` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?or` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_lectur`, `?or` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_liberausuarioconcurrencia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_liberausuarioconcurrencia.sql` |
| **LOC (1er CREATE)** | 39 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuario" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_liberausuarioconcurrencia(
  pIdSession                   char(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdSession` | `char(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cStatusFuncionalidad` | `CHAR(1)` | L5 |
| `iSqlErr` | `INT` | L6 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_libera` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?currencia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_libera`, `?currencia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mantenimientobancos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mantenimientobancos.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "banco (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `sp_adminbancos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mantenimientobancos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               SMALLINT
  pFechaOperacion              DATE
  pClaveBanco                  INTEGER
  pNombreBanco                 CHAR(60)
  pNombreBancoCorto            CHAR(20)
  pSPEI                        CHAR(1)
  pCheques                     CHAR(1)
  pNomina                      CHAR(1)
  pTefRecibe                   CHAR(1)
  pTefPresentador              CHAR(1)
  pDomiRecibe                  CHAR(1)
  pDomiPresentador             CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |
| `pFechaOperacion` | `DATE` | — | — |
| `pClaveBanco` | `INTEGER` | `banco`=banco | ✅ CÓDIGO |
| `pNombreBanco` | `CHAR(60)` | `banco`=banco | ✅ CÓDIGO |
| `pNombreBancoCorto` | `CHAR(20)` | `banco`=banco | ✅ CÓDIGO |
| `pSPEI` | `CHAR(1)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pCheques` | `CHAR(1)` | — | — |
| `pNomina` | `CHAR(1)` | — | — |
| `pTefRecibe` | `CHAR(1)` | — | — |
| `pTefPresentador` | `CHAR(1)` | — | — |
| `pDomiRecibe` | `CHAR(1)` | — | — |
| `pDomiPresentador` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `sp_adminbancos` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoOperacion IS NULL OR pClaveBanco IS NULL OR pFechaOperac` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00440';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:"informix".sp_adminbancos';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?enimiento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `banco` | ENTIDAD | banco | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?enimiento`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mantenimientoparametroopeccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mantenimientoparametroopeccl.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación parámetro y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mantenimientoparametroopeccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFlag                        CHAR(1)
  pComDebito                   CHAR(4)
  pComCredito                  CHAR(4)
  pComPago                     CHAR(4)
  pComDeposito                 CHAR(4)
  pConcEjec                    CHAR(1)
  pComSaldoDebito              CHAR(4)
  pComSaldoCredito             CHAR(4)
  pComDisposicionDebito        CHAR(4)
  pComDisposicionCredito       CHAR(4)
  pComTranferenciaEfectiva     CHAR(4)
  pComPagTDCBCPLCargEfectiva   CHAR(4)
  pComPagTDCOtroCargEfectiva   CHAR(4)
  pComPagTDCOtroEfectivo       CHAR(4)
  pComTransferencia            CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFlag` | `CHAR(1)` | — | — |
| `pComDebito` | `CHAR(4)` | — | — |
| `pComCredito` | `CHAR(4)` | — | — |
| `pComPago` | `CHAR(4)` | — | — |
| `pComDeposito` | `CHAR(4)` | — | — |
| `pConcEjec` | `CHAR(1)` | — | — |
| `pComSaldoDebito` | `CHAR(4)` | — | — |
| `pComSaldoCredito` | `CHAR(4)` | — | — |
| `pComDisposicionDebito` | `CHAR(4)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pComDisposicionCredito` | `CHAR(4)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pComTranferenciaEfectiva` | `CHAR(4)` | — | — |
| `pComPagTDCBCPLCargEfectiva` | `CHAR(4)` | — | — |
| `pComPagTDCOtroCargEfectiva` | `CHAR(4)` | — | — |
| `pComPagTDCOtroEfectivo` | `CHAR(4)` | — | — |
| `pComTransferencia` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(5)` | L20 |
| `iCodRetSp` | `INTEGER` | L21 |
| `cComDebito` | `CHAR(4)` | L22 |
| `cComCredito` | `CHAR(4)` | L23 |
| `cComPago` | `CHAR(4)` | L24 |
| `cComDeposito` | `CHAR(4)` | L25 |
| `cConcEjec` | `CHAR(1)` | L26 |
| `cComSaldoDebito` | `CHAR(4)` | L27 |
| `cComSaldoCredito` | `CHAR(4)` | L28 |
| `cComDisposicionDebito` | `CHAR(4)` | L29 |
| `cComDisposicionCredito` | `CHAR(4)` | L30 |
| `cComTranferenciaEfectiva` | `CHAR(4)` | L31 |
| `cComPagTDCBCPLCargEfectiva` | `CHAR(4)` | L32 |
| `cComPagTDCOtroCargEfectiva` | `CHAR(4)` | L33 |
| `cComPagTDCOtroEfectivo` | `CHAR(4)` | L34 |
| `cComTransferencia` | `CHAR(4)` | L35 |
| `iNoRegistros` | `INTEGER` | L36 |
| `iRecuperacion` | `INTEGER` | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L84 |
| `bditarjeta` | `bdicnweb` | no | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L104 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_concreing_maparopecr';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?enimiento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `parametro` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?enimiento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mantolineacre_masivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mantolineacre_masivo.sql` |
| **LOC (1er CREATE)** | 256 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "línea y crédito (anterior, masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_actualiza_lincred_central`, `sp_totaleslineascredito`, `sp_registra_evento` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mantolineacre_masivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INTEGER
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INTEGER` | — | — |
| `pIdPlantilla` | `CHAR(25)` | `ant`=anterior | 🔵 CONVENCIÓN |
| `pTituloPlantilla` | `CHAR(255)` | `ant`=anterior | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cMensajeRetorno` | `CHAR(80)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iRegistrosExitosos` | `INTEGER` | L12 |
| `iRegistrosFallidos` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cCuenta` | `CHAR(20)` | L15 |
| `mMontoNuevaLinea` | `MONEY(18,2)` | L16 |
| `cResultado` | `CHAR(15)` | L17 |
| `cStatus` | `CHAR(1)` | L18 |
| `cMotivoRechazo` | `CHAR(80)` | L19 |
| `mSaldoCuenta` | `MONEY(14,2)` | L20 |
| `cCodRetSpSal` | `CHAR(5)` | L21 |
| `dFechaProceso` | `DATETIME YEAR TO FRACTION(3)` | L22 |
| `iIdRegistro` | `INTEGER` | L23 |
| `cFechaCargaLote` | `DATE` | L24 |
| `iTotalRegsLote` | `INTEGER` | L25 |
| `mMontoLote` | `MONEY(14, 2)` | L26 |
| `iRegsAceptadosLote` | `INTEGER` | L27 |
| `iRegsRechazoLote` | `INTEGER` | L28 |
| `cArchivo` | `CHAR(150)` | L29 |
| *…3 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_mantolineascredito` | `bdicnweb` | no | SELECT | L97 |
| `sw_tr_cargamasiva_mantolineascredito_hist` | `bdicnweb` | no | SELECT | L101 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L170 |
| `sw_tr_cargamasiva_mantolineascredito` | `bdicnweb` | no | DELETE | L203 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_actualiza_lincred_central` | `bdicred` | ⚠️ sí | L136 |
| `sp_totaleslineascredito` | `bdicnweb` | no | L182 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L215 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L80 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pLote IS NULL OR pIdPlantilla = '' OR pTituloPlantilla = '' T` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00200';` |  |
| L145 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_actualiza_lincred_central (PROC MASIV` |  |
| L149 | FÓRMULA | `LET iRegistrosExitosos = iRegistrosExitosos + 1;` |  |
| L154 | FÓRMULA | `LET iRegistrosFallidos = iRegistrosFallidos + 1;` |  |
| L174 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `linea` | ENTIDAD | línea (de crédito) | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?o` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_marcase`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_marcase.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 131 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "marcas de cuenta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_marcarse` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_marcase(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pSE                          CHAR(1)
  pCausa                       SMALLINT
  pTipoMovimiento              INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pSE` | `CHAR(1)` | — | — |
| `pCausa` | `SMALLINT` | — | — |
| `pTipoMovimiento` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNombreUsuario` | `CHAR(45)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_marcarse` | `bdisitesp` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisitesp:sp_marcarse';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `marcas` | ENTIDAD | marcas de cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_actinfosol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_actinfosol.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=36 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza mc — Mesa de Control, información y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_mc_actinfosol` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_actinfosol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pNumCliente                  CHAR(20)
  pNumClienteRef               CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud | 🟡 INFERIDO |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pNumClienteRef` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRetSp` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_mc_actinfosol` | `bdisolic` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_mc_actinfosol';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |

---

## `sp_mc_acttdcgarantizada`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_acttdcgarantizada.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza mc — Mesa de Control y tarjeta de crédito (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_actualizar_info_tdc_garantizada` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_acttdcgarantizada(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pNumCliente                  CHAR(20)
  pGarantizada                 CHAR(2)
  pLinCred                     DECIMAL(18,2)
  pNumCuenta                   CHAR(20)
  pFechaAlta                   CHAR(10)
  pFechaBaja                   CHAR(10)
  pMotivoBaja                  CHAR(11)
  pTpMovto                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pGarantizada` | `CHAR(2)` | `ant`=anterior | 🔵 CONVENCIÓN |
| `pLinCred` | `DECIMAL(18,2)` | — | — |
| `pNumCuenta` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pFechaAlta` | `CHAR(10)` | — | — |
| `pFechaBaja` | `CHAR(10)` | — | — |
| `pMotivoBaja` | `CHAR(11)` | — | — |
| `pTpMovto` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(80)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cMotivo` | `CHAR(11)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_actualizar_info_tdc_garantizada` | `bdicred` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_actualizar_info_tdc_` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | 🔵 CONVENCIÓN | nombre_sp |
| `?gar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?izada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gar`, `?izada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_actualizainfocac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_actualizainfocac.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=34 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza mc — Mesa de Control y información" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_actualiza_info_cac` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_actualizainfocac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(50)
  pIngreso                     DECIMAL(18,2)
  pOtrosCompromisos            DECIMAL(18,2)
  pComprobanteValido           CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(50)` | — | — |
| `pIngreso` | `DECIMAL(18,2)` | — | — |
| `pOtrosCompromisos` | `DECIMAL(18,2)` | — | — |
| `pComprobanteValido` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_actualiza_info_cac` | `bdisolic` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_actualiza_info_cac'` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |

---

## `sp_mc_actualizastatususuariomc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_actualizastatususuariomc.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 107 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza mc — Mesa de Control, estatus y usuario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_actualizastatususuariomc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS COD_RET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | 🔵 CONVENCIÓN |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `cCodRet` | `CHAR(6)` | L10 |
| `cMensajeRet` | `CHAR(80)` | L11 |
| `cEstatus` | `CHAR(8)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_analistaenatencion` | `bdisolic` | ⚠️ sí | UPDATE | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet       = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_buscasoliccac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_buscasoliccac.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 238 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "busca mc — Mesa de Control y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_buscasoliccac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solic`=solicitud | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cCodRetSp` | `CHAR(5)` | L20 |
| `cDescCodRetSp` | `CHAR(150)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |
| `cMensajeRet` | `CHAR(80)` | L25 |
| `cNumSolic` | `CHAR(20)` | L26 |
| `cSucursal` | `CHAR(4)` | L27 |
| `cNombreCte` | `CHAR(107)` | L28 |
| `cRfcCte` | `CHAR(13)` | L29 |
| `cEjec_autoriza` | `CHAR(8)` | L30 |
| `dtFechSolic` | `DATE` | L31 |
| `cTieneOS` | `CHAR(1)` | L32 |
| `cNomPdcto` | `CHAR(40)` | L33 |
| `dLinCalculada` | `DECIMAL(18,2)` | L34 |
| `cNomAnalista` | `CHAR(45)` | L35 |
| `cConsulta` | `CHAR(2)` | L36 |
| `cStatus` | `CHAR(2)` | L37 |
| `iNumPag` | `INTEGER` | L38 |
| `cEjec_Atde` | `CHAR(8)` | L39 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_buscasoliccac` | `bdicnweb` | no | SELECT | L102 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L105 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '01086';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |

---

## `sp_mc_buscasoliccac_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_buscasoliccac_totales.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=0 / fan_out=33 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "busca mc — Mesa de Control y solicitud (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_busca_solicitudes_cac` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_buscasoliccac_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solic`=solicitud | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `cMensajeRet` | `CHAR(80)` | L13 |
| `cNumSolic` | `CHAR(20)` | L14 |
| `cSucursal` | `CHAR(4)` | L15 |
| `cNombreCte` | `CHAR(107)` | L16 |
| `cRfcCte` | `CHAR(13)` | L17 |
| `cEjec_autoriza` | `CHAR(8)` | L18 |
| `dtFechSolic` | `DATE` | L19 |
| `cTieneOS` | `CHAR(1)` | L20 |
| `cNomPdcto` | `CHAR(40)` | L21 |
| `dLinCalculada` | `DECIMAL(18,2)` | L22 |
| `cNomAnalista` | `CHAR(45)` | L23 |
| `cConsulta` | `CHAR(2)` | L24 |
| `cStatus` | `CHAR(2)` | L25 |
| `iNumPag` | `INTEGER` | L26 |
| `cEjec_Atde` | `CHAR(8)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_buscasoliccac` | `bdicnweb` | no | SELECT | L72 |
| `status_buscasoliccac` | `bdicnweb` | no | DELETE | L72 |
| `status_buscasoliccac` | `bdicnweb` | no | INSERT | L73 |
| `sw_mc_buscasoliccac` | `bdicnweb` | no | SELECT | L90 |
| `sw_mc_buscasoliccac` | `bdicnweb` | no | DELETE | L90 |
| `sw_mc_buscasoliccac` | `bdicnweb` | no | INSERT | L115 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L83 |
| `sp_busca_solicitudes_cac` | `bdisolic` | ⚠️ sí | L93 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_busca_solicitudes_c` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '01073';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_cargaarchivomanttoctas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_cargaarchivomanttoctas.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga mc — Mesa de Control, archivo y cuentas (anterior)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_cargaarchivomanttoctas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBloqueInf                   CHAR(2000)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBloqueInf` | `CHAR(2000)` | — | — |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cSqlerr` | `INTEGER` | L5 |
| `cIsamErr` | `INTEGER` | L6 |
| `cDescErr` | `CHAR(50)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cRegistro` | `LVARCHAR` | L9 |
| `cCuenta` | `CHAR(20)` | L10 |
| `cImporte` | `CHAR(20)` | L11 |
| `cDescripcion` | `CHAR(40)` | L12 |
| `cFecha` | `CHAR(10)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_ctascanceladas_tmp` | `bdicnweb` | no | SELECT | L54 |
| `sw_mc_ctascanceladas_tmp` | `bdicnweb` | no | DELETE | L54 |
| `sw_mc_ctascanceladas_tmp` | `bdicnweb` | no | INSERT | L67 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `sp_split_cadena` | `bdicnweb` | no | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET cCuenta = SUBSTRING_INDEX(TRIM(cRegistro),'+',1);` |  |
| L63 | FÓRMULA | `LET cImporte = SUBSTRING_INDEX(SUBSTRING_INDEX(TRIM(cRegistro),'+',2),'+',-1);` | 🔴 MONEY/aritmética financiera |
| L64 | FÓRMULA | `LET cDescripcion = SUBSTRING_INDEX(SUBSTRING_INDEX(TRIM(cRegistro),'+',-2),'+',1);` |  |
| L65 | FÓRMULA | `LET cFecha = SUBSTRING_INDEX(TRIM(cRegistro),'+',-1);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?to` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_catmotivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_catmotivo.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, catálogo y motivo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_catmotivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(150)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iIdMotivo` | `SMALLINT` | L12 |
| `cDescMotivo` | `CHAR(50)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_motivocancelacion` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `motivo` | ENTIDAD | motivo / causa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_mc_cattiporeporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_cattiporeporte.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, catálogo y reporte (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_cattiporeporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(150)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iIdReporte` | `SMALLINT` | L12 |
| `cDescReporte` | `CHAR(50)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_tiporeporte` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_mc_conssolicitudaleatoria`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_conssolicitudaleatoria.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta mc — Mesa de Control y solicitud" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_conssolicitudaleatoria(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pEjecutivo                   CHAR(8)
  pEjecucion                   CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pEjecucion` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `cDescCodRetSp` | `CHAR(150)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cNum_solicitud` | `CHAR(20)` | L16 |
| `cNum_cliente` | `CHAR(20)` | L17 |
| `cNom_cliente` | `CHAR(100)` | L18 |
| `cNom_analista` | `CHAR(80)` | L19 |
| `dHora` | `DATETIME HOUR TO SECOND` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_solicitudaleatoria` | `bdicnweb` | no | SELECT | L75 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L78 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?aleatoria` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aleatoria` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_conssolicitudaleatoria_soc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_conssolicitudaleatoria_soc.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ✅ fan_in=0 / fan_out=33 |
| **Deps concatenadas** | 110 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta mc — Mesa de Control, solicitud y Sistema Operativo Central" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_asigna_solicitud_soc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_conssolicitudaleatoria_soc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pEjecutivo                   CHAR(8)
  pEjecucion                   CHAR(1)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pEjecucion` | `CHAR(1)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `cDescCodRetSp` | `CHAR(150)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cNum_solicitud` | `CHAR(20)` | L16 |
| `cNum_cliente` | `CHAR(20)` | L17 |
| `cNom_cliente` | `CHAR(100)` | L18 |
| `cNom_analista` | `CHAR(80)` | L19 |
| `dHora` | `DATETIME HOUR TO SECOND` | L20 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |
| `sp_asigna_solicitud_soc` | `bdisolic` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisolic:sp_asigna_solicitudal` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?aleatoria_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aleatoria_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_conssolicitudaleatoria_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_conssolicitudaleatoria_totales.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta mc — Mesa de Control y solicitud (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_conssolicitudaleatoria_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pEjecutivo                   CHAR(8)
  pEjecucion                   CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pEjecucion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `cNum_solicitud` | `CHAR(20)` | L13 |
| `cNum_cliente` | `CHAR(20)` | L14 |
| `cNom_cliente` | `CHAR(100)` | L15 |
| `cNom_analista` | `CHAR(80)` | L16 |
| `dHora` | `DATETIME HOUR TO SECOND` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_solicitudaleatoria` | `bdicnweb` | no | SELECT | L61 |
| `sw_mc_solicitudaleatoria` | `bdicnweb` | no | DELETE | L61 |
| `sw_mc_solicitudaleatoria` | `bdicnweb` | no | INSERT | L86 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_asigna_solicitudaleatoria_mc` | `bdisolic` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_asigna_solicitudale` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?aleatoria_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aleatoria_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_constdcgarantizada`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_constdcgarantizada.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta mc — Mesa de Control y tarjeta de crédito (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultar_info_tdc_garantizada` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_constdcgarantizada(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(6)` | L16 |
| `cDescCodRetSp` | `CHAR(80)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `cNumCredito` | `CHAR(20)` | L20 |
| `cNumCte` | `CHAR(20)` | L21 |
| `cNombreCte` | `CHAR(107)` | L22 |
| `dFechaSol` | `CHAR(10)` | L23 |
| `cGarantizada` | `CHAR(2)` | L24 |
| `dLineaCredito` | `DECIMAL(18,2)` | L25 |
| `cNumCta` | `CHAR(20)` | L26 |
| `dFechaAlta` | `CHAR(10)` | L27 |
| `dFechaBaja` | `CHAR(10)` | L28 |
| `cMotivoBaja` | `CHAR(11)` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L72 |
| `sp_consultar_info_tdc_garantizada` | `bdicred` | ⚠️ sí | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicred:sp_consultar_info_tdc_g` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | 🔵 CONVENCIÓN | nombre_sp |
| `?gar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?izada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gar`, `?izada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_consultaparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_consultaparam.sql` |
| **LOC (1er CREATE)** | 47 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta mc — Mesa de Control y parámetro" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_consultaparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSol                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iPuntos_parcn` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_resumen_scoring` | `bdisolic` | ⚠️ sí | SELECT | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_consultaperfilusuariomc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_consultaperfilusuariomc.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta mc — Mesa de Control, perfil de usuario y usuario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_consultaperfilusuariomc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS COD_RET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | 🔵 CONVENCIÓN |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cCodRet` | `CHAR(6)` | L9 |
| `cMensajeRet` | `CHAR(15)` | L10 |
| `cEstatus` | `CHAR(8)` | L11 |
| `cEjecutivo` | `CHAR(10)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_analistaenatencion` | `bdisolic` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet       = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | VALIDACIÓN_NULL | `IF cEjecutivo = '' OR cEjecutivo IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_consultaproducto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_consultaproducto.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=51 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta mc — Mesa de Control y producto" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_consultaproducto(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdProducto` | `CHAR(5)` | L9 |
| `cDescripcion` | `CHAR(50)` | L10 |
| `iRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cb_param_campania` | `bdicobranza` | ⚠️ sí | SELECT | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `producto` | ENTIDAD | producto | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_consultasolicitudescac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_consultasolicitudescac.sql` |
| **LOC (1er CREATE)** | 156 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta mc — Mesa de Control y solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consulta_solicitudes_cac` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_consultasolicitudescac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cCodRetSp` | `CHAR(6)` | L29 |
| `cDescCodRetSp` | `CHAR(100)` | L30 |
| `iCodRetSp` | `INTEGER` | L31 |
| `cEmpresa` | `CHAR(3)` | L32 |
| `cNumSolic` | `CHAR(20)` | L33 |
| `cNumCte` | `CHAR(20)` | L34 |
| `cNombreCte` | `CHAR(107)` | L35 |
| `cRfcCte` | `CHAR(13)` | L36 |
| `cAntecBC` | `CHAR(6)` | L37 |
| `deCompMensBC` | `DECIMAL(18,2)` | L38 |
| `cAntecCC` | `CHAR(6)` | L39 |
| `deCompMensCC` | `DECIMAL(18,2)` | L40 |
| `cNumCteCop` | `CHAR(20)` | L41 |
| `smAntiguedad` | `SMALLINT` | L42 |
| `cPuntualidad` | `CHAR(2)` | L43 |
| `deEficPago` | `DECIMAL(18,2)` | L44 |
| `deAbonMensual` | `DECIMAL(18,2)` | L45 |
| `deIngresoMen` | `DECIMAL(18,2)` | L46 |
| `deMontoSolic` | `DECIMAL(18,2)` | L47 |
| `cObservaciones` | `CHAR(300)` | L48 |
| `deCompromisos_cac` | `DECIMAL(18,2)` | L49 |
| `cComprobante_val` | `CHAR(1)` | L50 |
| `cFecha_insert` | `CHAR(10)` | L51 |
| *…5 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L116 |
| `sp_consulta_solicitudes_cac` | `bdisolic` | ⚠️ sí | L126 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L133 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_consulta_solicitude` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `solicitudes` | ENTIDAD | solicitudes (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_mc_detallectascanceladas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_detallectascanceladas.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela mc — Mesa de Control, detalle y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_detallectascanceladas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cSqlerr` | `INTEGER` | L9 |
| `cIsamErr` | `INTEGER` | L10 |
| `cDescErr` | `CHAR(50)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |
| `mImporte` | `MONEY(14,2)` | L14 |
| `cDescripcion` | `CHAR(40)` | L15 |
| `dFecha` | `DATE` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_detallectascanceladas` | `bdicnweb` | no | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L69 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?das` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?das` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_detallectascanceladas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_detallectascanceladas_totales.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela mc — Mesa de Control, detalle y cuentas (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_detallectascanceladas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cSqlerr` | `INTEGER` | L6 |
| `cIsamErr` | `INTEGER` | L7 |
| `cDescErr` | `CHAR(50)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_detallectascanceladas` | `bdicnweb` | no | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?das_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?das_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_detallectasrecuperacionesp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_detallectasrecuperacionesp.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recuperación mc — Mesa de Control, detalle y cuentas (especial)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `recupera` → `SELECT` encontrado en el cuerpo · `recupera` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_detallectasrecuperacionesp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | `recuperacion`=recuperación (cobranza) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `cSqlerr` | `INTEGER` | L11 |
| `cIsamErr` | `INTEGER` | L12 |
| `cDescErr` | `CHAR(50)` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cCuenta` | `CHAR(20)` | L15 |
| `mImporte` | `MONEY(14,2)` | L16 |
| `cDescripcion` | `CHAR(40)` | L17 |
| `dFecha` | `DATE` | L18 |
| `iIdRegistro` | `INTEGER` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_detallectasrecuperacionesp` | `bdicnweb` | no | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCuenta = '' OR pRegistros IS NULL OR pRecuperacion IS NULL T` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L73 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `recuperacion` | ACCION | recuperación (cobranza) | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_detallectasrecuperacionesp_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_detallectasrecuperacionesp_totales.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recuperación mc — Mesa de Control, detalle y cuentas (especial, totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `recupera` → `SELECT` encontrado en el cuerpo · `recupera` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_detallectasrecuperacionesp_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cSqlerr` | `INTEGER` | L6 |
| `cIsamErr` | `INTEGER` | L7 |
| `cDescErr` | `CHAR(50)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_statusctasrecuperacionesp` | `bdicnweb` | no | SELECT | L34 |
| `sw_mc_statusctasrecuperacionesp` | `bdicnweb` | no | DELETE | L34 |
| `sw_mc_detallectasrecuperacionesp` | `bdicnweb` | no | SELECT | L35 |
| `sw_mc_detallectasrecuperacionesp` | `bdicnweb` | no | DELETE | L35 |
| `sw_mc_statusctasrecuperacionesp` | `bdicnweb` | no | INSERT | L39 |
| `sw_mc_detallectasrecuperacionesp` | `bdicnweb` | no | INSERT | L60 |
| `cuentas` | `bdicheq` | ⚠️ sí | SELECT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `recuperacion` | ACCION | recuperación (cobranza) | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_ejecutadesbloqueoctas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_ejecutadesbloqueoctas.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ejecuta mc — Mesa de Control y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `desbloq_cuentas_corresp2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_ejecutadesbloqueoctas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cSqlerr` | `INTEGER` | L5 |
| `cIsamErr` | `INTEGER` | L6 |
| `cDescErr` | `CHAR(50)` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iExiste` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `desbloq_cuentas_corresp2` | `bdicheq` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdicheq:"informix".desbloq_cuen` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `ejecuta` | ACCION | ejecuta (verbo — proceso / operación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `desbloqueo` | ACCION | desbloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_ejecutamanttoctas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_ejecutamanttoctas.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ejecuta mc — Mesa de Control y cuentas (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bloqueoctas_corr2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_ejecutamanttoctas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cSqlerr` | `INTEGER` | L5 |
| `cIsamErr` | `INTEGER` | L6 |
| `cDescErr` | `CHAR(50)` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iExiste` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_statusctascanceladas` | `bdicnweb` | no | SELECT | L35 |
| `sw_mc_statusctascanceladas` | `bdicnweb` | no | DELETE | L35 |
| `sw_mc_detallectascanceladas` | `bdicnweb` | no | SELECT | L36 |
| `sw_mc_detallectascanceladas` | `bdicnweb` | no | DELETE | L36 |
| `sw_mc_statusctascanceladas` | `bdicnweb` | no | INSERT | L40 |
| `sw_mc_ctascanceladas_tmp` | `bdicnweb` | no | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |
| `bloqueoctas_corr2` | `bdicheq` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:"informix".bloqueoctas` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ejecuta` | ACCION | ejecuta (verbo — proceso / operación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?to` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_eliminasolicatenusuariomc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_eliminasolicatenusuariomc.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 106 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina mc — Mesa de Control, solicitud y usuario" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_eliminasolicatenusuariomc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS COD_RET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `cCodRet` | `CHAR(6)` | L10 |
| `cMensajeRet` | `CHAR(80)` | L11 |
| `cHora` | `CHAR(8)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacora_soc` | `bdicnweb` | no | INSERT | L34 |
| `sysshmvals` | `sysmaster` | ⚠️ sí | SELECT | L44 |
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | SELECT | L60 |
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | DELETE | L60 |
| `ss_solicitudes_mc` | `bdisolic` | ⚠️ sí | UPDATE | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet       = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `?aten` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aten` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_eliminasolicatenusuariomc_prueba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_eliminasolicatenusuariomc_prueba.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=84 |
| **Deps concatenadas** | 195 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina mc — Mesa de Control, solicitud y usuario" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_eliminasolicatenusuariomc_prueba(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS COD_RET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `cCodRet` | `CHAR(6)` | L10 |
| `cMensajeRet` | `CHAR(80)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | SELECT | L44 |
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | DELETE | L44 |
| `ss_solicitudes_mc` | `bdisolic` | ⚠️ sí | UPDATE | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet       = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `?aten` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_prueba` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aten`, `?_prueba` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_finalizarrevisioncac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_finalizarrevisioncac.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control y ARR — producto de ahorro/inversión recurrente (final)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `determina_lincred_tc_cjunk`, `sp_finalizar_revision_cac` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_finalizarrevisioncac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pEjecutivo                   CHAR(8)
  pLincredSugCAC               DECIMAL(18,2)
  pComprobante                 CHAR(1)
  pObservaciones               CHAR(200)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pEjecutivo` | `CHAR(8)` | — | — |
| `pLincredSugCAC` | `DECIMAL(18,2)` | `cac`=familia crédito (CAC) | 🟡 INFERIDO |
| `pComprobante` | `CHAR(1)` | — | — |
| `pObservaciones` | `CHAR(200)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `v_capacidad_pago` | `MONEY(14,2)` | L13 |
| `iPlazo` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |
| `determina_lincred_tc_cjunk` | `bdisolic` | ⚠️ sí | L54 |
| `sp_finalizar_revision_cac` | `bdisolic` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisolic:determina_lincred_tc_` |  |
| L65 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisolic:sp_finalizar_revision` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `final` | MODIF | final | 🔵 CONVENCIÓN | nombre_sp |
| `?iz` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `arr` | ENTIDAD | ARR — producto de ahorro/inversión recurrente (CLABE, interé | 🟡 INFERIDO | nombre_sp |
| `?evision` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iz`, `?evision` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_marcagrabatdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_marcagrabatdcoro.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba mc — Mesa de Control, marca, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_marcagraba_tdc_oro` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_marcagrabatdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoEjecucion               CHAR(1)
  pNvaLinea                    DECIMAL(18,2)
  pNumSolCred                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |
| `pNvaLinea` | `DECIMAL(18,2)` | — | — |
| `pNumSolCred` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `dNuevaLinea` | `DECIMAL(18,2)` | L12 |
| `cNumSolCred` | `CHAR(20)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |
| `sp_marcagraba_tdc_oro` | `bdisolic` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_marcagraba_tdc_oro'` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `marca` | ENTIDAD | marca | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_mc_reestablecersolicitudcac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reestablecersolicitudcac.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_reestablecer_solicitud_cac` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reestablecersolicitudcac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pEjecutivo                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pEjecutivo` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRetSp` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_reestablecer_solicitud_cac` | `bdisolic` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_reestablecer_solici` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `?_reestablecer` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_reestablecer` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_registrasolrevisioncac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_registrasolrevisioncac.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registra mc — Mesa de Control, solicitud y revisión" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_registra_sol_revision_cac` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_registrasolrevisioncac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pEjecutivo                   CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud | 🟡 INFERIDO |
| `pEjecutivo` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRetSp` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_registra_sol_revision_cac` | `bdisolic` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_registra_sol_revisi` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `revision` | ENTIDAD | revisión | 🔵 CONVENCIÓN | nombre_sp |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_mc_reportecacanalista`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecacanalista.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, reporte y analista" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecacanalista(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `cDescCodRetSp` | `CHAR(150)` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |
| `cDescripcion` | `CHAR(80)` | L22 |
| `cNum_empleado` | `CHAR(8)` | L23 |
| `cNom_empleado` | `CHAR(45)` | L24 |
| `iNum_atendidas` | `INTEGER` | L25 |
| `dPorc_atendidas` | `DECIMAL(5,2)` | L26 |
| `iNum_compingval` | `INTEGER` | L27 |
| `dPorc_compingval` | `DECIMAL(5,2)` | L28 |
| `iNum_compingnoval` | `INTEGER` | L29 |
| `dPorc_compingnoval` | `DECIMAL(5,2)` | L30 |
| `dLincredprom` | `DECIMAL(18,2)` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_rep_cac_analista` | `bdicnweb` | no | SELECT | L90 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `analista` | ENTIDAD | analista | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_mc_reportecacanalista_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecacanalista_totales.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, reporte y analista (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_reporte_cac_analista` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecacanalista_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `cDescripcion` | `CHAR(80)` | L13 |
| `cNum_empleado` | `CHAR(8)` | L14 |
| `cNom_empleado` | `CHAR(45)` | L15 |
| `iNum_atendidas` | `INTEGER` | L16 |
| `dPorc_atendidas` | `DECIMAL(5,2)` | L17 |
| `iNum_compingval` | `INTEGER` | L18 |
| `dPorc_compingval` | `DECIMAL(5,2)` | L19 |
| `iNum_compingnoval` | `INTEGER` | L20 |
| `dPorc_compingnoval` | `DECIMAL(5,2)` | L21 |
| `dLincredprom` | `DECIMAL(18,2)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_analista` | `bdicnweb` | no | SELECT | L61 |
| `status_rep_analista` | `bdicnweb` | no | DELETE | L61 |
| `status_rep_analista` | `bdicnweb` | no | INSERT | L62 |
| `sw_mc_rep_cac_analista` | `bdicnweb` | no | SELECT | L81 |
| `sw_mc_rep_cac_analista` | `bdicnweb` | no | DELETE | L81 |
| `sw_mc_rep_cac_analista` | `bdicnweb` | no | INSERT | L107 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L72 |
| `sp_reporte_cac_analista` | `bdisolic` | ⚠️ sí | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L64 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisolic:sp_reporte_cac_analist` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `analista` | ENTIDAD | analista | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_reportecaccompingreso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecaccompingreso.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, reporte y ingreso (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecaccompingreso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `cDescCodRetSp` | `CHAR(150)` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `cDescripcion` | `CHAR(80)` | L21 |
| `cCod_docto` | `CHAR(4)` | L22 |
| `cDescripcion_grupo` | `CHAR(50)` | L23 |
| `iNumero_doctos` | `INTEGER` | L24 |
| `dPorcentaje_total_grupos` | `DECIMAL(5,2)` | L25 |
| `iValidos_grupo` | `INTEGER` | L26 |
| `dPorcentaje_validos_grupo` | `DECIMAL(5,2)` | L27 |
| `iInvalidos_grupo` | `INTEGER` | L28 |
| `dPorcentaje_invalidos_grupo` | `DECIMAL(5,2)` | L29 |
| `dPorcentaje_final_grupo` | `DECIMAL(5,2)` | L30 |
| `iIndice_grupo` | `INTEGER` | L31 |
| `iRecuperacion` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_rep_cac_compingreso` | `bdicnweb` | no | SELECT | L92 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L68 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L95 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ingreso` | ENTIDAD | ingreso (del solicitante) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_reportecaccompingreso_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecaccompingreso_totales.sql` |
| **LOC (1er CREATE)** | 136 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 76 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, reporte y ingreso (complemento, totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_reporte_cac_compingreso` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecaccompingreso_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `cDescripcion` | `CHAR(80)` | L13 |
| `cCod_docto` | `CHAR(4)` | L14 |
| `cDescripcion_grupo` | `CHAR(50)` | L15 |
| `iNumero_doctos` | `INTEGER` | L16 |
| `dPorcentaje_total_grupos` | `DECIMAL(5,2)` | L17 |
| `iValidos_grupo` | `INTEGER` | L18 |
| `dPorcentaje_validos_grupo` | `DECIMAL(5,2)` | L19 |
| `iInvalidos_grupo` | `INTEGER` | L20 |
| `dPorcentaje_invalidos_grupo` | `DECIMAL(5,2)` | L21 |
| `dPorcentaje_final_grupo` | `DECIMAL(5,2)` | L22 |
| `iIndice_grupo` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_compingreso` | `bdicnweb` | no | SELECT | L63 |
| `status_rep_compingreso` | `bdicnweb` | no | DELETE | L63 |
| `status_rep_compingreso` | `bdicnweb` | no | INSERT | L64 |
| `sw_mc_rep_cac_compingreso` | `bdicnweb` | no | SELECT | L83 |
| `sw_mc_rep_cac_compingreso` | `bdicnweb` | no | DELETE | L83 |
| `sw_mc_rep_cac_compingreso` | `bdicnweb` | no | INSERT | L109 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L74 |
| `sp_reporte_cac_compingreso` | `bdisolic` | ⚠️ sí | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L66 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_reporte_cac_comping` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ingreso` | ENTIDAD | ingreso (del solicitante) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_reportecacdetallado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecacdetallado.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, reporte y detalle" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecacdetallado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `cCodRetSp` | `CHAR(5)` | L25 |
| `cDescCodRetSp` | `CHAR(150)` | L26 |
| `iCodRetSp` | `INTEGER` | L27 |
| `cEmpresa` | `CHAR(3)` | L28 |
| `cDescripcion` | `CHAR(80)` | L29 |
| `cFecha_autorizacion` | `CHAR(10)` | L30 |
| `cNum_solicitud` | `CHAR(20)` | L31 |
| `cNum_sucursal` | `CHAR(4)` | L32 |
| `cNum_cliente` | `CHAR(20)` | L33 |
| `cNombre_cte` | `CHAR(104)` | L34 |
| `cComp_ingreso_valido` | `CHAR(2)` | L35 |
| `cGrupo_cte` | `CHAR(1)` | L36 |
| `dIngreso_declarado` | `DECIMAL(20,2)` | L37 |
| `dCompromisos_sic` | `DECIMAL(20,2)` | L38 |
| `dCompromisos_bco` | `DECIMAL(20,2)` | L39 |
| `dCompromisos_cop` | `DECIMAL(20,2)` | L40 |
| `dLinea_coppel` | `DECIMAL(20,2)` | L41 |
| `dLinea_sug` | `DECIMAL(20,2)` | L42 |
| `dIngreso_valido_mc` | `DECIMAL(20,2)` | L43 |
| `dLinea_sug_mc` | `DECIMAL(20,2)` | L44 |
| `cStatus_final` | `CHAR(20)` | L45 |
| `cAnalista_cac_atend` | `CHAR(45)` | L46 |
| `cObservaciones` | `CHAR(300)` | L47 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_rep_cac_detallado` | `bdicnweb` | no | SELECT | L122 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L101 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L125 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `?allado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?allado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_reportecacdetallado_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecacdetallado_totales.sql` |
| **LOC (1er CREATE)** | 216 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, reporte y detalle (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_reporte_cac_detallado` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecacdetallado_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `cDescripcion` | `CHAR(80)` | L13 |
| `cFecha_autorizacion` | `CHAR(10)` | L14 |
| `cNum_solicitud` | `CHAR(20)` | L15 |
| `cNum_sucursal` | `CHAR(4)` | L16 |
| `cNum_cliente` | `CHAR(20)` | L17 |
| `cNombre_cte` | `CHAR(104)` | L18 |
| `cComp_ingreso_valido` | `CHAR(2)` | L19 |
| `cGrupo_cte` | `CHAR(1)` | L20 |
| `dIngreso_declarado` | `DECIMAL(20,2)` | L21 |
| `dCompromisos_sic` | `DECIMAL(20,2)` | L22 |
| `dCompromisos_bco` | `DECIMAL(20,2)` | L23 |
| `dCompromisos_cop` | `DECIMAL(20,2)` | L24 |
| `dLinea_coppel` | `DECIMAL(20,2)` | L25 |
| `dLinea_sug` | `DECIMAL(20,2)` | L26 |
| `dIngreso_valido_mc` | `DECIMAL(20,2)` | L27 |
| `dLinea_sug_mc` | `DECIMAL(20,2)` | L28 |
| `cStatus_final` | `CHAR(20)` | L29 |
| `cAnalista_cac_atend` | `CHAR(45)` | L30 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_detallado` | `bdicnweb` | no | UPDATE | L43 |
| `status_rep_detallado` | `bdicnweb` | no | SELECT | L98 |
| `status_rep_detallado` | `bdicnweb` | no | DELETE | L98 |
| `status_rep_detallado` | `bdicnweb` | no | INSERT | L99 |
| `sw_mc_rep_cac_detallado` | `bdicnweb` | no | SELECT | L129 |
| `sw_mc_rep_cac_detallado` | `bdicnweb` | no | DELETE | L129 |
| `sw_mc_rep_cac_detallado` | `bdicnweb` | no | INSERT | L175 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L115 |
| `sp_reporte_cac_detallado` | `bdisolic` | ⚠️ sí | L134 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L101 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisolic:sp_reporte_cac_detall` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `?allado_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?allado_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_reportecacgeneral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecacgeneral.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control y reporte (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecacgeneral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cDescCodRetSp` | `CHAR(150)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `cdescripcion` | `CHAR(80)` | L21 |
| `cestadoSol` | `CHAR(50)` | L22 |
| `itotalCasos` | `INTEGER` | L23 |
| `dporcTotCasos` | `DECIMAL(5,2)` | L24 |
| `igrupo1` | `INTEGER` | L25 |
| `dporcGrupo1` | `DECIMAL(5,2)` | L26 |
| `iGrupo2` | `INTEGER` | L27 |
| `dporcGrupo2` | `DECIMAL(5,2)` | L28 |
| `iGrupo3` | `INTEGER` | L29 |
| `dporcGrupo3` | `DECIMAL(5,2)` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_rep_cac_general` | `bdicnweb` | no | SELECT | L90 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L66 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `general` | MODIF | general | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_mc_reportecacgeneral_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecacgeneral_totales.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control y reporte (general, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecacgeneral_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `cdescripcion` | `CHAR(80)` | L13 |
| `cestadoSol` | `CHAR(50)` | L14 |
| `itotalCasos` | `INTEGER` | L15 |
| `dporcTotCasos` | `DECIMAL(5,2)` | L16 |
| `igrupo1` | `INTEGER` | L17 |
| `dporcGrupo1` | `DECIMAL(5,2)` | L18 |
| `iGrupo2` | `INTEGER` | L19 |
| `dporcGrupo2` | `DECIMAL(5,2)` | L20 |
| `iGrupo3` | `INTEGER` | L21 |
| `dporcGrupo3` | `DECIMAL(5,2)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_general` | `bdicnweb` | no | SELECT | L60 |
| `status_rep_general` | `bdicnweb` | no | DELETE | L60 |
| `status_rep_general` | `bdicnweb` | no | INSERT | L61 |
| `sw_mc_rep_cac_general` | `bdicnweb` | no | SELECT | L80 |
| `sw_mc_rep_cac_general` | `bdicnweb` | no | DELETE | L80 |
| `sw_mc_rep_cac_general` | `bdicnweb` | no | INSERT | L106 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |
| `sp_reporte_cac_general` | `bdisolic` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisolic:sp_reporte_cac_genera` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `general` | MODIF | general | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_reportecaclineacred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecaclineacred.sql` |
| **LOC (1er CREATE)** | 130 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, reporte, línea y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecaclineacred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `cDescCodRetSp` | `CHAR(150)` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |
| `cDescripcion` | `CHAR(80)` | L22 |
| `cEstadoSol` | `CHAR(50)` | L23 |
| `iGrupo1` | `INTEGER` | L24 |
| `dLinpromGrupo1` | `DECIMAL(18,2)` | L25 |
| `iGrupo2` | `INTEGER` | L26 |
| `dLinpromGrupo2` | `DECIMAL(18,2)` | L27 |
| `iGrupo3` | `INTEGER` | L28 |
| `dLinpromGrupo3` | `DECIMAL(18,2)` | L29 |
| `iLineasTotales` | `INTEGER` | L30 |
| `dLinpromTotal` | `DECIMAL(18,2)` | L31 |
| `dMontoTotAsig` | `DECIMAL(18,2)` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_rep_cac_lincred` | `bdicnweb` | no | SELECT | L92 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L68 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L95 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `linea` | ENTIDAD | línea (de crédito) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_reportecaclineacred_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_reportecaclineacred_totales.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "mc — Mesa de Control, reporte, línea y crédito (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_reporte_cac_lineacredito` |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_reportecaclineacred_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |
| `cdescripcion` | `CHAR(80)` | L13 |
| `cestado_sol` | `CHAR(50)` | L14 |
| `igrupo1` | `INTEGER` | L15 |
| `dlinprom_grupo1` | `DECIMAL(18,2)` | L16 |
| `igrupo2` | `INTEGER` | L17 |
| `dlinprom_grupo2` | `DECIMAL(18,2)` | L18 |
| `igrupo3` | `INTEGER` | L19 |
| `dlinprom_grupo3` | `DECIMAL(18,2)` | L20 |
| `ilineas_totales` | `INTEGER` | L21 |
| `dlinprom_total` | `DECIMAL(18,2)` | L22 |
| `dmonto_totasig` | `DECIMAL(18,2)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_lincred` | `bdicnweb` | no | SELECT | L62 |
| `status_rep_lincred` | `bdicnweb` | no | DELETE | L62 |
| `status_rep_lincred` | `bdicnweb` | no | INSERT | L63 |
| `sw_mc_rep_cac_lincred` | `bdicnweb` | no | SELECT | L82 |
| `sw_mc_rep_cac_lincred` | `bdicnweb` | no | DELETE | L82 |
| `sw_mc_rep_cac_lincred` | `bdicnweb` | no | INSERT | L108 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |
| `sp_reporte_cac_lineacredito` | `bdisolic` | ⚠️ sí | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisolic:sp_reporte_cac_lineacr` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `linea` | ENTIDAD | línea (de crédito) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_mc_verificastatusbuscasoliccac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_verificastatusbuscasoliccac.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica mc — Mesa de Control, estatus y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_verificastatusbuscasoliccac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_buscasoliccac` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |

---

## `sp_mc_verificastatusctasrecuperacionesp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_verificastatusctasrecuperacionesp.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica mc — Mesa de Control, estatus y cuentas (especial)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_verificastatusctasrecuperacionesp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_statusctasrecuperacionesp` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `recuperacion` | ACCION | recuperación (cobranza) | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_verificastatusmanttoctas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_verificastatusmanttoctas.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica mc — Mesa de Control, estatus y cuentas (anterior)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_verificastatusmanttoctas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(1)` | L10 |
| `cError` | `CHAR(5)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_statusctascanceladas` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?to` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_verificastatusrepanalista`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_verificastatusrepanalista.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica mc — Mesa de Control, estatus y analista" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_verificastatusrepanalista(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_analista` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `analista` | ENTIDAD | analista | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_mc_verificastatusrepcompingreso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_verificastatusrepcompingreso.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica mc — Mesa de Control, estatus y ingreso (complemento)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_verificastatusrepcompingreso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_compingreso` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ingreso` | ENTIDAD | ingreso (del solicitante) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_mc_verificastatusrepdetallado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_verificastatusrepdetallado.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica mc — Mesa de Control, estatus y detalle" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_verificastatusrepdetallado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_detallado` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `?allado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?allado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mc_verificastatusrepgeneral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_verificastatusrepgeneral.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica mc — Mesa de Control y estatus (general)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_verificastatusrepgeneral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_general` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `general` | MODIF | general | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_mc_verificastatusreplincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_mc_verificastatusreplincred.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica mc — Mesa de Control, estatus y línea de crédito" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mc_verificastatusreplincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_rep_lincred` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `lincred` | ENTIDAD | línea de crédito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_modifica_parametros_retiros`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_modifica_parametros_retiros.sql` |
| **LOC (1er CREATE)** | 201 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "modifica parámetros y retiros" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `modifica` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_modifica_parametros_retiros(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMonto_minimo                VARCHAR(100)
  pMonto_maximo                VARCHAR(100)
  pCodigo_activo               VARCHAR(100)
  pVigencia                    VARCHAR(100)
  pip                          VARCHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMonto_minimo` | `VARCHAR(100)` | — | — |
| `pMonto_maximo` | `VARCHAR(100)` | — | — |
| `pCodigo_activo` | `VARCHAR(100)` | — | — |
| `pVigencia` | `VARCHAR(100)` | — | — |
| `pip` | `VARCHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `cAccion` | `CHAR(50)` | L12 |
| `cEntidad` | `CHAR(100)` | L13 |
| `cip` | `CHAR(20)` | L14 |
| `crespaldo` | `CHAR(50)` | L15 |
| `cCmd1` | `CHAR(2000)` | L16 |
| `cArchivoRespaldo` | `CHAR(50)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `parametro_sistema` | `bdirst` | ⚠️ sí | SELECT | L51 |
| `systables` | `bdicnweb` | no | SELECT | L58 |
| `parametro_sistema` | `bdirst` | ⚠️ sí | UPDATE | L67 |
| `parametro_sistema` | `bdirst` | ⚠️ sí | INSERT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_generararchivo_rst` | `bdicnweb` | no | L62 |
| `sp_bitacora` | `bdicnweb` | no | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L92 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L127 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L163 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `modifica` | ACCION | modifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametros` | ENTIDAD | parámetros | 🔵 CONVENCIÓN | nombre_sp |
| `retiros` | ENTIDAD | retiros | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_modmaesdoscentralnvasfuncre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_modmaesdoscentralnvasfuncre.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "saldos y crédito (central)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_modmaesdos_central` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_modmaesdoscentralnvasfuncre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pTipoSaldo                   CHAR(2)
  pQuitaAbono                  DECIMAL(18,2)
  pCastigoAbono                DECIMAL(18,2)
  pQuebrantoAbono              DECIMAL(18,2)
  pAjusteCargo                 DECIMAL(18,2)
  pAjusteAbono                 DECIMAL(18,2)
  pCondonacionAbono            DECIMAL(18,2)
  pIvaInteresVigente           DECIMAL(18,2)
  pIvaInteresVencido           DECIMAL(18,2)
  pMontoActual                 DECIMAL(18,2)
  pDescripcionMovimiento       CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cre`=crédito | 🟡 INFERIDO |
| `pTipoSaldo` | `CHAR(2)` | — | — |
| `pQuitaAbono` | `DECIMAL(18,2)` | — | — |
| `pCastigoAbono` | `DECIMAL(18,2)` | — | — |
| `pQuebrantoAbono` | `DECIMAL(18,2)` | — | — |
| `pAjusteCargo` | `DECIMAL(18,2)` | — | — |
| `pAjusteAbono` | `DECIMAL(18,2)` | — | — |
| `pCondonacionAbono` | `DECIMAL(18,2)` | — | — |
| `pIvaInteresVigente` | `DECIMAL(18,2)` | — | — |
| `pIvaInteresVencido` | `DECIMAL(18,2)` | — | — |
| `pMontoActual` | `DECIMAL(18,2)` | — | — |
| `pDescripcionMovimiento` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(6)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cMensajeretorno` | `CHAR(80)` | L18 |
| `dMontoActual` | `DECIMAL(18,2)` | L19 |
| `dCantidadActuaizar` | `DECIMAL(18,2)` | L20 |
| `dMontoActualDespuesAfectacion` | `DECIMAL(18,2)` | L21 |
| `cFolio` | `CHAR(16)` | L22 |
| `bInTransaction` | `BOOLEAN` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L78 |
| `sp_modmaesdos_central` | `bdicred` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCredito = '' OR pTipoSaldo = '' OR pAjusteCargo IS NULL O` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L89 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_modmaesdos_central';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '90000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_modmae` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sdos` | ENTIDAD | saldos (abreviación) | 🟡 INFERIDO | nombre_sp |
| `central` | MODIF | central | 🔵 CONVENCIÓN | nombre_sp |
| `?nvasfun` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_modmae`, `?nvasfun` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_monitor_envio_cheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_monitor_envio_cheques.sql` |
| **LOC (1er CREATE)** | 168 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía monitor y cheques" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_monitor_envio_cheques(
  pBandera                     CHAR(2)
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L30 |
| `iSqlErr` | `INTEGER` | L31 |
| `cCodRetSp` | `CHAR(6)` | L32 |
| `iCodRetSp` | `INTEGER` | L33 |
| `cEmpresa` | `CHAR(3)` | L34 |
| `iNoRegistros` | `INTEGER` | L35 |
| `cBanco` | `CHAR(3)` | L36 |
| `cDescripcionBanco` | `CHAR(40)` | L37 |
| `cReferencia` | `CHAR(40)` | L38 |
| `cCuenta` | `CHAR(40)` | L39 |
| `iNumeroCheque` | `INTEGER` | L40 |
| `dMontoOrigen` | `DECIMAL(14,2)` | L41 |
| `dFechaAlta` | `DATE` | L42 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L43 |
| `cSucursal` | `CHAR(44)` | L44 |
| `sDiasRetorno` | `SMALLINT` | L45 |
| `cFolioSucursal` | `CHAR(16)` | L46 |
| `cTranscc` | `CHAR(4)` | L47 |
| `cClaveSucursal` | `CHAR(4)` | L48 |
| `cDigitalizado` | `CHAR(25)` | L49 |
| `cPre` | `CHAR(2)` | L50 |
| `cEstatusColor` | `CHAR(2)` | L51 |
| `bBanderaMovimientoCheques` | `BOOLEAN` | L52 |
| `bBanderaMovimientoCredito` | `BOOLEAN` | L53 |
| `cPresentado` | `CHAR(1)` | L54 |
| *…10 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_monitorenviochequesope` | `bdicnweb` | no | L144 |
| `sp_monitorenviochequesope_totales` | `bdicnweb` | no | L152 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_monitorconsultasolicitudxmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_monitorconsultasolicitudxmc.sql` |
| **LOC (1er CREATE)** | 188 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta monitor, solicitud y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_monitorconsultasolicitudxmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pEjecutivoAtiende            CHAR(8)
  pStatus                      CHAR(2)
  pCausa                       CHAR(3)
  pObservaciones               CHAR(100)
  pTipo                        SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pEjecutivoAtiende` | `CHAR(8)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pCausa` | `CHAR(3)` | — | — |
| `pObservaciones` | `CHAR(100)` | — | — |
| `pTipo` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `iNoRegistros` | `INTEGER` | L28 |
| `iRegistros` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |
| `cCodRetSp` | `CHAR(6)` | L31 |
| `cNumSolicitud` | `CHAR(20)` | L32 |
| `cNumCliente` | `CHAR(20)` | L33 |
| `cNombreCliente` | `CHAR(100)` | L34 |
| `cRfc` | `CHAR(13)` | L35 |
| `cSucursal` | `CHAR(4)` | L36 |
| `dFechaInsert` | `DATE` | L37 |
| `dFechaModificacion` | `DATE` | L38 |
| `mMontoSolicitado` | `DECIMAL(18,2)` | L39 |
| `mEficiencia` | `DECIMAL(18,2)` | L40 |
| `iHistorial` | `SMALLINT` | L41 |
| `cStatusInicial` | `CHAR(2)` | L42 |
| `mSeccion1` | `DECIMAL(18,2)` | L43 |
| `mSeccion2` | `DECIMAL(18,2)` | L44 |
| `cCausaSolic` | `CHAR(3)` | L45 |
| `cObservaciones` | `CHAR(300)` | L46 |
| `cNumProducto` | `CHAR(4)` | L47 |
| `cStatusFin` | `CHAR(2)` | L48 |
| `cEjecutivoAtiende` | `CHAR(8)` | L49 |
| `cEjcutivoAutoriza` | `CHAR(8)` | L50 |
| *…6 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L144 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L103 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pStatus = '' OR pTipo IS NULL OR pRegistros IS NULL OR pRecup` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00108';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L150 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L155 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00219';` |  |
| L165 | CÓDIGO_RETORNO | `LET cCodRet = '00220';` |  |
| L174 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_monitorconsultatotsolicitudxmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_monitorconsultatotsolicitudxmc.sql` |
| **LOC (1er CREATE)** | 153 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta monitor, solicitud y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_monitorconsultatotsolicitudxmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pEjecutivoAtiende            CHAR(8)
  pStatus                      CHAR(2)
  pCausa                       CHAR(3)
  pObservaciones               CHAR(100)
  pTipo                        SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pEjecutivoAtiende` | `CHAR(8)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pCausa` | `CHAR(3)` | — | — |
| `pObservaciones` | `CHAR(100)` | — | — |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `iRegistros` | `INTEGER` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cNumSolicitud` | `CHAR(20)` | L11 |
| `cNumCliente` | `CHAR(20)` | L12 |
| `cNombreCliente` | `CHAR(100)` | L13 |
| `cRfc` | `CHAR(13)` | L14 |
| `cSucursal` | `CHAR(4)` | L15 |
| `dFechaInsert` | `DATE` | L16 |
| `dFechaModificacion` | `DATE` | L17 |
| `mMontoSolicitado` | `DECIMAL(18,2)` | L18 |
| `mEficiencia` | `DECIMAL(18,2)` | L19 |
| `iHistorial` | `SMALLINT` | L20 |
| `cStatusInicial` | `CHAR(2)` | L21 |
| `mSeccion1` | `DECIMAL(18,2)` | L22 |
| `mSeccion2` | `DECIMAL(18,2)` | L23 |
| `cCausaSolic` | `CHAR(3)` | L24 |
| `cObservaciones` | `CHAR(300)` | L25 |
| `cNumProducto` | `CHAR(4)` | L26 |
| `cStatusFin` | `CHAR(2)` | L27 |
| `cEjecutivoAtiende` | `CHAR(8)` | L28 |
| `cEjcutivoAutoriza` | `CHAR(8)` | L29 |
| *…6 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L84 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pStatus = '' OR pTipo IS NULL THEN` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00108';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L123 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0 , 'ERROR EN LA EJECUCION DEL SP PRODUCTIVO bdisolic:sp_consult` |  |
| L125 | FÓRMULA | `LET iTotalRegs = iTotalRegs + 1;` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00219';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00220';` |  |
| L140 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_monitorconsultatotsolicitudxmc_error_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_monitorconsultatotsolicitudxmc_error_mc.sql` |
| **LOC (1er CREATE)** | 152 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 203 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta monitor, solicitud, mc — Mesa de Control y error" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_monitorconsultatotsolicitudxmc_error_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pEjecutivoAtiende            CHAR(8)
  pStatus                      CHAR(2)
  pCausa                       CHAR(3)
  pObservaciones               CHAR(100)
  pTipo                        SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pEjecutivoAtiende` | `CHAR(8)` | — | — |
| `pStatus` | `CHAR(2)` | — | — |
| `pCausa` | `CHAR(3)` | — | — |
| `pObservaciones` | `CHAR(100)` | — | — |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `iRegistros` | `INTEGER` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cNumSolicitud` | `CHAR(20)` | L11 |
| `cNumCliente` | `CHAR(20)` | L12 |
| `cNombreCliente` | `CHAR(100)` | L13 |
| `cRfc` | `CHAR(13)` | L14 |
| `cSucursal` | `CHAR(4)` | L15 |
| `dFechaInsert` | `DATE` | L16 |
| `dFechaModificacion` | `DATE` | L17 |
| `mMontoSolicitado` | `DECIMAL(18,2)` | L18 |
| `mEficiencia` | `DECIMAL(18,2)` | L19 |
| `iHistorial` | `SMALLINT` | L20 |
| `cStatusInicial` | `CHAR(2)` | L21 |
| `mSeccion1` | `DECIMAL(18,2)` | L22 |
| `mSeccion2` | `DECIMAL(18,2)` | L23 |
| `cCausaSolic` | `CHAR(3)` | L24 |
| `cObservaciones` | `CHAR(300)` | L25 |
| `cNumProducto` | `CHAR(4)` | L26 |
| `cStatusFin` | `CHAR(2)` | L27 |
| `cEjecutivoAtiende` | `CHAR(8)` | L28 |
| `cEjcutivoAutoriza` | `CHAR(8)` | L29 |
| *…6 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L116 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L84 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pStatus = '' OR pTipo IS NULL THEN` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00108';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0 , 'ERROR EN LA EJECUCION DEL SP PRODUCTIVO bdisolic:sp_consult` |  |
| L124 | FÓRMULA | `LET iTotalRegs = iTotalRegs + 1;` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00219';` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00220';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `error` | ENTIDAD | error | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_monitorenviochequesope`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_monitorenviochequesope.sql` |
| **LOC (1er CREATE)** | 185 |
| **Callgraph** | ✅ fan_in=39 / fan_out=12 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía monitor y cheques" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_monitorenviochequesope(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)             AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(6)` | L22 |
| `iCodRetSp` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `cBanco` | `CHAR(3)` | L26 |
| `cDescripcionBanco` | `CHAR(40)` | L27 |
| `cReferencia` | `CHAR(40)` | L28 |
| `cCuenta` | `CHAR(40)` | L29 |
| `iNumeroCheque` | `INTEGER` | L30 |
| `dMontoOrigen` | `DECIMAL(14,2)` | L31 |
| `dFechaAlta` | `DATE` | L32 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L33 |
| `cSucursal` | `CHAR(44)` | L34 |
| `sDiasRetorno` | `SMALLINT` | L35 |
| `cFolioSucursal` | `CHAR(16)` | L36 |
| `cTranscc` | `CHAR(4)` | L37 |
| `cClaveSucursal` | `CHAR(4)` | L38 |
| `cDigitalizado` | `CHAR(25)` | L39 |
| `cPre` | `CHAR(2)` | L40 |
| `cEstatusColor` | `CHAR(2)` | L41 |
| `bBanderaMovimientoCheques` | `BOOLEAN` | L42 |
| `bBanderaMovimientoCredito` | `BOOLEAN` | L43 |
| `cPresentado` | `CHAR(1)` | L44 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L132 |
| `sd_movdia` | `bdicred` | ⚠️ sí | SELECT | L137 |
| `cce_cheques_det` | `bditef` | ⚠️ sí | SELECT | L148 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L108 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L94 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |

---

## `sp_monitorenviochequesope_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_monitorenviochequesope_totales.sql` |
| **LOC (1er CREATE)** | 201 |
| **Callgraph** | ✅ fan_in=39 / fan_out=12 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía monitor y cheques (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `envía` → `INSERT` encontrado en el cuerpo · `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_monitorenviochequesope_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `cBanco` | `CHAR(3)` | L16 |
| `cDescripcionBanco` | `CHAR(40)` | L17 |
| `cReferencia` | `CHAR(40)` | L18 |
| `cCuenta` | `CHAR(40)` | L19 |
| `iNumeroCheque` | `INTEGER` | L20 |
| `dMontoOrigen` | `DECIMAL(14,2)` | L21 |
| `dFechaAlta` | `DATE` | L22 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L23 |
| `cSucursal` | `CHAR(44)` | L24 |
| `sDiasRetorno` | `SMALLINT` | L25 |
| `cFolioSucursal` | `CHAR(16)` | L26 |
| `cTranscc` | `CHAR(4)` | L27 |
| `cClaveSucursal` | `CHAR(4)` | L28 |
| `cDigitalizado` | `CHAR(25)` | L29 |
| `cPre` | `CHAR(2)` | L30 |
| `cEstatusColor` | `CHAR(2)` | L31 |
| `bBanderaMovimientoCheques` | `BOOLEAN` | L32 |
| `bBanderaMovimientoCredito` | `BOOLEAN` | L33 |
| `cPresentado` | `CHAR(1)` | L34 |
| *…10 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L142 |
| `sd_movdia` | `bdicred` | ⚠️ sí | SELECT | L145 |
| `cce_cheques_det` | `bditef` | ⚠️ sí | SELECT | L156 |
| `cep_monitorcheques_tmp` | `bdicnweb` | no | INSERT | L174 |
| `cep_monitorcheques_tmp` | `bdicnweb` | no | SELECT | L179 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L106 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L176 | FÓRMULA | `LET iTotalRegistros = iTotalRegistros + 1;` |  |
| L183 | FÓRMULA | `LET iTotalPorRecibir = iTotalOperados - iTotalDigitalizados;` |  |
| L186 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_msi_consultamsi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_consultamsi.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta meses sin intereses" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_consultamsi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `dSdoTotalLiq` | `DECIMAL(18,2)` | L20 |
| `dSaldo_pagar` | `DECIMAL(18,2)` | L21 |
| `cFecha` | `CHAR(10)` | L22 |
| `cHora` | `CHAR(10)` | L23 |
| `cTarjeta` | `CHAR(16)` | L24 |
| `cFolio` | `CHAR(16)` | L25 |
| `cCodFun` | `CHAR(3)` | L26 |
| `cDescripcion` | `CHAR(100)` | L27 |
| `cInfReceptor` | `CHAR(40)` | L28 |
| `cReferencia` | `CHAR(40)` | L29 |
| `dMontoOtorgado` | `DECIMAL(18,2)` | L30 |
| `cStatus` | `CHAR(60)` | L31 |
| `iNoRegistros` | `INTEGER` | L32 |
| `iLlave` | `INTEGER` | L33 |
| `iPlazo` | `INTEGER` | L34 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L64 | VALIDACIÓN_NULL | `IF pUsuario ='' OR pIdFuncion='' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L91 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '01276';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_msi_consultamsi_detalle`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_consultamsi_detalle.sql` |
| **LOC (1er CREATE)** | 119 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta meses sin intereses y detalle" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_consultamsi_detalle(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCred                     CHAR(30)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCred` | `CHAR(30)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `dSdoTotalLiq` | `DECIMAL(18,2)` | L21 |
| `dSaldo_pagar` | `DECIMAL(18,2)` | L22 |
| `cFecha` | `CHAR(10)` | L23 |
| `cHora` | `CHAR(10)` | L24 |
| `cTarjeta` | `CHAR(16)` | L25 |
| `cFolio` | `CHAR(16)` | L26 |
| `cCodFun` | `CHAR(3)` | L27 |
| `cDescripcion` | `CHAR(100)` | L28 |
| `cInfReceptor` | `CHAR(40)` | L29 |
| `cReferencia` | `CHAR(40)` | L30 |
| `dMontoOtorgado` | `DECIMAL(18,2)` | L31 |
| `cStatus` | `CHAR(60)` | L32 |
| `iNoRegistros` | `INTEGER` | L33 |
| `cPlazoA` | `CHAR(5)` | L34 |
| `cNumPago` | `CHAR(5)` | L35 |
| `iLlave` | `INTEGER` | L36 |
| `iPlazo` | `INTEGER` | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF pUsuario ='' OR pIdFuncion='' OR pNumCred='' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L96 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '01276';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_msi_consultamsi_detalle_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_consultamsi_detalle_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta meses sin intereses y detalle (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_consultamsi_detalle_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCred                     CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCred` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodret='01276';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_msi_consultamsi_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_consultamsi_totales.sql` |
| **LOC (1er CREATE)** | 486 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta meses sin intereses (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_consultamsi_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCred                     CHAR(30)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCred` | `CHAR(30)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSP` | `CHAR(6)` | L7 |
| `cMensajeRet` | `CHAR(80)` | L8 |
| `cNumCredito` | `CHAR(20)` | L9 |
| `cCodTipCred` | `CHAR(2)` | L10 |
| `cDescStatusCred` | `CHAR(60)` | L11 |
| `iIdUnidadProd` | `INTEGER` | L12 |
| `cCodCaract2` | `CHAR(3)` | L13 |
| `dtFechaOrigen` | `DATE` | L14 |
| `dtFechaProxPago` | `DATE` | L15 |
| `dPagoMinimo` | `DECIMAL(18,2)` | L16 |
| `dtFechaUltPago` | `DATE` | L17 |
| `iPlazo` | `INTEGER` | L18 |
| `iPlazoAux` | `INTEGER` | L19 |
| `iPagosRealizados` | `INTEGER` | L20 |
| `dLineaOtorgada` | `DECIMAL(18,2)` | L21 |
| `dTasaInteres` | `DECIMAL(9,6)` | L22 |
| `dTasaMoratorios` | `DECIMAL(9,6)` | L23 |
| `dMontoSBC` | `DECIMAL(14,2)` | L24 |
| `dCapVig` | `DECIMAL(18,2)` | L25 |
| `dCapTrans` | `DECIMAL(18,2)` | L26 |
| `dCapVdoExig` | `DECIMAL(18,2)` | L27 |
| `dCapVdoNoExig` | `DECIMAL(18,2)` | L28 |
| `dSdoActCap` | `DECIMAL(18,2)` | L29 |
| *…51 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_msi_consultagrid` | `bdicnweb` | no | SELECT | L196 |
| `sw_msi_consultagrid` | `bdicnweb` | no | DELETE | L196 |
| `sw_verificastatusconsmsi` | `bdicnweb` | no | SELECT | L197 |
| `sw_verificastatusconsmsi` | `bdicnweb` | no | DELETE | L197 |
| `sw_verificastatusconsmsi` | `bdicnweb` | no | INSERT | L201 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L204 |
| `sd_movdiacrd` | `bdicred` | ⚠️ sí | SELECT | L211 |
| `sd_promocion_credito` | `bdicred` | ⚠️ sí | SELECT | L212 |
| `sd_maesdoscrd` | `bdicred` | ⚠️ sí | SELECT | L213 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L214 |
| `sd_movhiscrd` | `bdicred` | ⚠️ sí | SELECT | L216 |
| `sd_movdiacrd_temp` | `bdicred` | ⚠️ sí | SELECT | L222 |
| `sd_movhiscrd_temp` | `bdicred` | ⚠️ sí | SELECT | L225 |
| `movimiento` | `intercard` | ⚠️ sí | SELECT | L247 |
| `movimientohistorico` | `intercard` | ⚠️ sí | SELECT | L264 |
| `sd_msi_cancela_credito_msi` | `bdicred` | ⚠️ sí | SELECT | L273 |
| `sd_amortiza_creditocrd` | `bdicred` | ⚠️ sí | SELECT | L293 |
| `sw_msi_consultagrid` | `bdicnweb` | no | INSERT | L313 |
| `sw_verificastatusconsmsi` | `bdicnweb` | no | UPDATE | L472 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L183 |
| `sp_consulta_saldos_general` | `bdicred` | ⚠️ sí | L283 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L175 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L295 | VALIDACIÓN_NULL | `IF dSaldo_pagar is null THEN` |  |
| L299 | VALIDACIÓN_NULL | `IF cNumPago is null THEN` |  |
| L305 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L306 | FÓRMULA | `LET iAuxCab = iAuxCab+1;` |  |
| L311 | FÓRMULA | `LET cPlazo = TRIM(cNumPago) \|\|'/'\|\| TRIM(iPlazoAux::CHAR(5));` |  |
| L419 | VALIDACIÓN_NULL | `IF dSaldo_pagar is null THEN` |  |
| L423 | VALIDACIÓN_NULL | `IF cNumPago is null THEN` |  |
| L429 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L430 | FÓRMULA | `LET iAuxCab = iAuxCab+1;` |  |
| L435 | FÓRMULA | `LET cPlazo = TRIM(cNumPago) \|\|'/'\|\| TRIM(iPlazoAux::CHAR(5));` |  |
| L454 | CÓDIGO_RETORNO | `LET cCodRet ='01276';` |  |
| L465 | CÓDIGO_RETORNO | `LET cCodRet ='01276';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_msi_consultamsicancel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_consultamsicancel.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta meses sin intereses y celular" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_consultamsicancel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `dSaldo_pagar` | `DECIMAL(18,2)` | L18 |
| `dSdoTotalLiq` | `DECIMAL(18,2)` | L19 |
| `cFecha` | `CHAR(10)` | L20 |
| `cPlazo` | `CHAR(10)` | L21 |
| `cTarjeta` | `CHAR(16)` | L22 |
| `cFolio` | `CHAR(16)` | L23 |
| `cInfReceptor` | `CHAR(40)` | L24 |
| `dMontoOtorgado` | `DECIMAL(18,2)` | L25 |
| `iPromo` | `INTEGER` | L26 |
| `cCanal` | `CHAR(1)` | L27 |
| `cSucursal` | `CHAR(4)` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| `cNumCredito` | `CHAR(20)` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | VALIDACIÓN_NULL | `IF pUsuario ='' OR pIdFuncion='' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L87 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '01278';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?can` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?can` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_msi_consultamsicancel_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_consultamsicancel_totales.sql` |
| **LOC (1er CREATE)** | 370 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta meses sin intereses y celular (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_consultamsicancel_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCred                     CHAR(30)
  pProducto                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCred` | `CHAR(30)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSP` | `CHAR(6)` | L7 |
| `cMensajeRet` | `CHAR(80)` | L8 |
| `cNumCredito` | `CHAR(20)` | L9 |
| `cCodTipCred` | `CHAR(2)` | L10 |
| `cDescStatusCred` | `CHAR(60)` | L11 |
| `iIdUnidadProd` | `INTEGER` | L12 |
| `cCodCaract2` | `CHAR(3)` | L13 |
| `dtFechaOrigen` | `DATE` | L14 |
| `dtFechaProxPago` | `DATE` | L15 |
| `dPagoMinimo` | `DECIMAL(18,2)` | L16 |
| `dtFechaUltPago` | `DATE` | L17 |
| `iPlazo` | `INTEGER` | L18 |
| `iPagosRealizados` | `INTEGER` | L19 |
| `dLineaOtorgada` | `DECIMAL(18,2)` | L20 |
| `dTasaInteres` | `DECIMAL(9,6)` | L21 |
| `dTasaMoratorios` | `DECIMAL(9,6)` | L22 |
| `dMontoSBC` | `DECIMAL(14,2)` | L23 |
| `dCapVig` | `DECIMAL(18,2)` | L24 |
| `dCapTrans` | `DECIMAL(18,2)` | L25 |
| `dCapVdoExig` | `DECIMAL(18,2)` | L26 |
| `dCapVdoNoExig` | `DECIMAL(18,2)` | L27 |
| `dSdoActCap` | `DECIMAL(18,2)` | L28 |
| `dIntVdo` | `DECIMAL(18,2)` | L29 |
| *…42 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_msi_consultagridcancel` | `bdicnweb` | no | SELECT | L165 |
| `sw_msi_consultagridcancel` | `bdicnweb` | no | DELETE | L165 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L167 |
| `sd_movdiacrd` | `bdicred` | ⚠️ sí | SELECT | L173 |
| `sd_promocion_credito` | `bdicred` | ⚠️ sí | SELECT | L174 |
| `sd_maesdoscrd` | `bdicred` | ⚠️ sí | SELECT | L175 |
| `sd_movhiscrd` | `bdicred` | ⚠️ sí | SELECT | L176 |
| `sd_msi_cancela_credito_msi` | `bdicred` | ⚠️ sí | SELECT | L177 |
| `sd_movdiacrd_temp` | `bdicred` | ⚠️ sí | SELECT | L184 |
| `sd_movhiscrd_temp` | `bdicred` | ⚠️ sí | SELECT | L189 |
| `movimiento` | `intercard` | ⚠️ sí | SELECT | L205 |
| `movimientohistorico` | `intercard` | ⚠️ sí | SELECT | L217 |
| `sd_amortiza_creditocrd` | `bdicred` | ⚠️ sí | SELECT | L232 |
| `sw_msi_consultagridcancel` | `bdicnweb` | no | INSERT | L244 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L157 |
| `sp_consulta_saldos_general` | `bdicred` | ⚠️ sí | L222 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L234 | VALIDACIÓN_NULL | `IF dSaldo_pagar is null THEN` |  |
| L238 | VALIDACIÓN_NULL | `IF cNumPago is null THEN` |  |
| L242 | FÓRMULA | `LET cPlazo =TRIM(iPlazo::CHAR(5))  \|\|'/'\|\| TRIM(cNumPago);` |  |
| L323 | VALIDACIÓN_NULL | `IF dSaldo_pagar is null THEN` |  |
| L327 | VALIDACIÓN_NULL | `IF cNumPago is null THEN` |  |
| L331 | FÓRMULA | `LET cPlazo =   TRIM(iPlazo::CHAR(5))\|\|'/'\|\|TRIM(cNumPago);` |  |
| L349 | CÓDIGO_RETORNO | `LET cCodRet ='01278';` |  |
| L357 | CÓDIGO_RETORNO | `LET cCodRet = '01278';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?can` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?can` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_msi_genrepmsi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_genrepmsi.sql` |
| **LOC (1er CREATE)** | 325 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte meses sin intereses" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_genrepmsi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRutaDescarga                CHAR(100)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `cBanDetError` | `CHAR(1)` | L13 |
| `iTotal` | `INTEGER` | L14 |
| `dFechaHoy` | `DATE` | L15 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L16 |
| `cFechaHoraArchivo` | `CHAR(35)` | L17 |
| `cInfReceptor` | `CHAR(40)` | L19 |
| `dMontoOtorgado` | `DECIMAL(18,2)` | L20 |
| `iPlazo` | `INTEGER` | L21 |
| `cIdRetailer` | `CHAR(20)` | L22 |
| `cFecha` | `CHAR(10)` | L23 |
| `cTarjeta` | `CHAR(16)` | L24 |
| `cSobretasa` | `DECIMAL(9,6)` | L25 |
| `cNumProd` | `CHAR(4)` | L26 |
| `cNumCredito` | `CHAR(20)` | L27 |
| `cNumCliente` | `CHAR(20)` | L28 |
| `cNombreRepXls` | `CHAR(100)` | L29 |
| `cNombreRepTxt` | `CHAR(100)` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_msi_reportemsi` | `bdicnweb` | no | SELECT | L96 |
| `sw_msi_reportemsi` | `bdicnweb` | no | DELETE | L96 |
| `sw_ctrlreportesmsi` | `bdicnweb` | no | SELECT | L107 |
| `sw_ctrlreportesmsi` | `bdicnweb` | no | DELETE | L115 |
| `bitacora_msi` | `intercard` | ⚠️ sí | SELECT | L129 |
| `sd_movdiacrd_temp` | `bdicnweb` | no | SELECT | L143 |
| `sd_movhiscrd_temp` | `bdicnweb` | no | SELECT | L154 |
| `sw_msi_reportemsi` | `bdicnweb` | no | INSERT | L174 |
| `systables` | `bdicnweb` | no | SELECT | L192 |
| `sw_ctrlreportesmsi` | `bdicnweb` | no | INSERT | L300 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |
| `sp_msi_genrepmsi2` | `bdicnweb` | no | L211 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L101 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L185 | CÓDIGO_RETORNO | `LET cCodRet ='01113';` |  |
| L193 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L194 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT infreceptor::CHAR(40),montootorgado::CHAR(20),plazo::CHAR(1` | 🔴 MONEY/aritmética financiera |
| L253 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L258 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L267 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L276 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_msi_genrepmsi2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_genrepmsi2.sql` |
| **LOC (1er CREATE)** | 193 |
| **Callgraph** | ✅ fan_in=35 / fan_out=49 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte meses sin intereses" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_genrepmsi2(
  pUsuario                     CHAR(8)
  pRutaDescarga                CHAR(100)
  cCmd1                        CHAR(4000)
  cNombreRepXls                CHAR(100)
  cNombreRepTxt                CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `cCmd1` | `CHAR(4000)` | — | — |
| `cNombreRepXls` | `CHAR(100)` | — | — |
| `cNombreRepTxt` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cSql` | `CHAR(2500)` | L6 |
| `cRutaGral` | `CHAR(150)` | L7 |
| `cRutaInformix` | `CHAR(100)` | L8 |
| `cUsrBin` | `CHAR(100)` | L9 |
| `cRutaGralXls` | `CHAR(150)` | L10 |
| `cRutaGralTxt` | `CHAR(150)` | L11 |
| `dFechaHoy` | `DATE` | L12 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlreportesmsi` | `bdicnweb` | no | INSERT | L176 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRutaDescarga)\|\|'repmsisoc.sql';` |  |
| L76 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L81 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L90 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L99 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L107 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGralXls)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGralXls` |  |
| L128 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRutaDescarga)\|\|'repmsisoc.sql';` |  |
| L137 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |
| L142 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralTxt);` |  |
| L151 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralTxt)\|\|".tmp > "\|\|TRIM(cRutaGralTxt);` |  |
| L160 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |
| L168 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGralTxt)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGralTxt` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_msi_genrepmsigrid`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_genrepmsigrid.sql` |
| **LOC (1er CREATE)** | 211 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte meses sin intereses y identificador" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_genrepmsigrid(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCred                     CHAR(300)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | ✅ CÓDIGO |
| `pNumCred` | `CHAR(300)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `cBanDetError` | `CHAR(1)` | L13 |
| `iTotal` | `INTEGER` | L14 |
| `dFechaHoy` | `DATE` | L15 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L16 |
| `cFechaHoraArchivo` | `CHAR(35)` | L17 |
| `cAux` | `CHAR(100)` | L19 |
| `cAux2` | `CHAR(100)` | L20 |
| `i` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_msi_numcred_tmp` | `bdicnweb` | no | SELECT | L78 |
| `sw_msi_numcred_tmp` | `bdicnweb` | no | DELETE | L78 |
| `sw_msi_numcred_tmp` | `bdicnweb` | no | INSERT | L83 |
| `sw_msi_consultagrid` | `bdicnweb` | no | SELECT | L95 |
| `systables` | `bdicnweb` | no | SELECT | L105 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L105 | FÓRMULA | `LET cCmd1 ="SELECT 'FECHA','HORA','TARJETA','FOLIO','TRANSACCION','DESCRIPCION','CONCEPTO','REFERENC` | 🔴 MONEY/aritmética financiera |
| L106 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L107 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(fecha),2,0)\|\|'/'\|\|LPAD(MONTH(fecha),2,0)\|\|'/` | 🔴 MONEY/aritmética financiera |
| L118 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L152 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L157 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L166 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L175 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?gr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_msi_registracancelacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_registracancelacion.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registra meses sin intereses" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_msi_cancela_msi_credito` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_registracancelacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCred                     CHAR(30)
  pFoliomvto                   CHAR(16)
  pPromo                       INTEGER
  pCanal                       CHAR(1)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCred` | `CHAR(30)` | — | — |
| `pFoliomvto` | `CHAR(16)` | — | — |
| `pPromo` | `INTEGER` | — | — |
| `pCanal` | `CHAR(1)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSP` | `CHAR(6)` | L8 |
| `cMsjResp` | `CHAR(80)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_msi_cancela_msi_credito` | `bdicred` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `cancelacion` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_msi_verificastatusmsigrid`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_msi_verificastatusmsigrid.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica meses sin intereses, estatus y identificador" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_msi_verificastatusmsigrid(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cProceso` | `CHAR(15)` | L14 |
| `iTotal` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusconsmsi` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `msi` | ENTIDAD | meses sin intereses (MSI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?gr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_notificar_evento_impresora`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_notificar_evento_impresora.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "notifica evento/notificación y Impago — pago vencido o fallido; confirmado: n_impagos_consec" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `notifica` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_notificar_evento_impresora(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoError                   INT
  pPlantilla                   CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoError` | `INT` | — | — |
| `pPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `dFechaMail` | `DATE` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoError IS NULL OR pPlantilla = '' OR pTituloPlantilla = '` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `notifica` | ACCION | notifica | 🔵 CONVENCIÓN | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `evento` | ENTIDAD | evento/notificación | 🔵 CONVENCIÓN | nombre_sp |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?resora` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_`, `?resora` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_observacionespreviasaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_observacionespreviasaumlincred.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 108 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reversión (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_observacionespreviasaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pFechaOrigenIncremento       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pFechaOrigenIncremento` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRet` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cMensajeRetorno` | `CHAR(107)` | L9 |
| `cJustificacion` | `CHAR(208)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_observacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?ias` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_observacion`, `?ias` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtencambiostatusmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtencambiostatusmc.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene cambio, estatus y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtencambiostatusmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
  pStatusInicial               CHAR(2)
  pStatusFinal                 CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pNumSolicitud` | `CHAR(20)` | — | — |
| `pStatusInicial` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pStatusFinal` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cMensajeRetorno` | `CHAR(80)` | L12 |
| `cStatusCambio` | `CHAR(2)` | L13 |
| `cDescripcionEstatusCambio` | `CHAR(40)` | L14 |
| `cValidaHuella` | `CHAR(1)` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L62 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN SP sp_mc_obtencambiostatus';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_obtenerconceptocargomanualescre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtenerconceptocargomanualescre.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene concepto de pago, cargo y crédito (manual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtenerconceptocargomanuales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenerconceptocargomanualescre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConceptoPago                CHAR(2)
  pTransaccion                 CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConceptoPago` | `CHAR(2)` | `concepto`=concepto de pago | 🔵 CONVENCIÓN |
| `pTransaccion` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cCodPago` | `CHAR(2)` | L9 |
| `cDescricpion` | `CHAR(50)` | L10 |
| `cTransaccion` | `CHAR(4)` | L11 |
| `iSqlErr` | `int` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtenerconceptocargomanuales` | `bdicred` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `concepto` | ENTIDAD | concepto de pago | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `manual` | MODIF | manual | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenerexcepcionescacaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtenerexcepcionescacaumlincred.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 107 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene Comprobante Electrónico de Pago" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenerexcepcionescacaumlincred(
  pUsuario                     CHAR(20)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(20)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescripcion` | `CHAR(80)` | L10 |
| `cCveExcepcion` | `CHAR(3)` | L11 |
| `cDescExcepcion` | `CHAR(100)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_obtenerexcepcionescac';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cep` | ENTIDAD | Comprobante Electrónico de Pago (SPEI · Banxico) | 🔵 CONVENCIÓN | nombre_sp |
| `?ciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?ciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenerfechahoy`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtenerfechahoy.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 105 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene fecha (de hoy)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtenerfechahoy` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenerfechahoy(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `dFechaHoy` | `DATE` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtenerfechahoy` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `hoy` | MODIF | de hoy / fecha actual | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_obtenerhomoclave`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtenerhomoclave.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=1 / fan_out=1 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene clave" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenerhomoclave(
  pNombre                      LVARCHAR
) RETURNING CHAR(2) AS homoclave
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNombre` | `LVARCHAR` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `i` | `INTEGER` | L4 |
| `cCaracter` | `CHAR(1)` | L5 |
| `cNombreNumerico` | `LVARCHAR` | L6 |
| `iCodigoLetra` | `SMALLINT` | L7 |
| `bBoolValue` | `BOOLEAN` | L8 |
| `iSuma` | `INTEGER` | L9 |
| `iValor1` | `INTEGER` | L10 |
| `iValor2` | `INTEGER` | L11 |
| `iValor3` | `INTEGER` | L12 |
| `iDiv1` | `INTEGER` | L13 |
| `iDiv2` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `letras_rfc` | `bdicnweb` | no | SELECT | L38 |
| `homoclaves_rfc` | `bdicnweb` | no | SELECT | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | VALIDACIÓN_NULL | `IF iCodigoLetra IS NULL THEN` |  |
| L69 | FÓRMULA | `LET iValor2 = SUBSTR(TRIM(cNombreNumerico), (i + 1), 1)::INTEGER;` |  |
| L70 | FÓRMULA | `LET iValor3 = iValor1 * iValor2;` |  |
| L71 | FÓRMULA | `LET iSuma = iSuma + iValor3;` |  |
| L76 | FÓRMULA | `LET iDiv1 = (iDiv1 - iDiv2) / 34;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?homo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?homo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenermontosautorizadosxusuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtenermontosautorizadosxusuario.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene monto, OS — Originación de Solicitudes y usuario" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenermontosautorizadosxusuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `mMontoMaxDebCargo` | `MONEY(14, 2)` | L14 |
| `mMontoMaxDebAbono` | `MONEY(14, 2)` | L15 |
| `mMontoMaxDebReverso` | `MONEY(14, 2)` | L16 |
| `mMontoMaxCredCargo` | `MONEY(14, 2)` | L17 |
| `mMontoMaxCredAbono` | `MONEY(14, 2)` | L18 |
| `mMontoMaxCredReverso` | `MONEY(14, 2)` | L19 |
| `cUsuarioAutoriza` | `CHAR(8)` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_montos_autorizados` | `bdinteg` | ⚠️ sí | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `autoriza` | ACCION | autoriza | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenernombresarchprocesardomi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtenernombresarchprocesardomi.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene nombre, archivo y domiciliación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenernombresarchprocesardomi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cNombreArchivo` | `CHAR(20)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `bInTransaccion` | `BOOLEAN` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `bdidomi` | `bdicnweb` | no | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdidomi:sp_obtenernombresarchprocesardo` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00494';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `nombre` | ENTIDAD | nombre | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `domi` | ENTIDAD | domiciliación | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenerparametroscce`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtenerparametroscce.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=25 / fan_out=23 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene parámetros y CCE — Cámara de Compensación Electrónica" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenerparametroscce(
  pEmpresa                     CHAR(3)
  pUsuario                     CHAR(8)
) RETURNING CHAR(5),		-- CODIGO RETORNO
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L11 |
| `cCodret` | `CHAR(5)` | L12 |
| `cDescripcion` | `CHAR(40)` | L13 |
| `cIPInteract` | `VARCHAR(100)` | L14 |
| `cPuerto` | `VARCHAR(100)` | L15 |
| `cBanco` | `CHAR(100)` | L16 |
| `cFecha_hoy` | `DATE` | L17 |
| `cNombre` | `CHAR(45)` | L18 |
| `cRazonSocial` | `CHAR(30)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L48 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L54 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L60 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L66 |
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodret			= '00000';` |  |
| L84 | VALIDACIÓN_NULL | `IF cPuerto = '' OR cPuerto IS NULL OR cIPInteract= '' OR cIPInteract IS NULL THEN` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodret= '10000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_obtenertransaccionpagosmanualescre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtenertransaccionpagosmanualescre.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene transacción, pagos y crédito (manual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtenertransaccionpagosmanuales` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenertransaccionpagosmanualescre(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  p_Codconcepto                CHAR(2)
  p_Transaccion                CHAR(4)
  p_CodFun                     CHAR(3)
  pNumCredito                  CHAR(20)
  pProducto                    CHAR(4)
) RETURNING CHAR(5)       AS CodRet,      --cod_ret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `p_Codconcepto` | `CHAR(2)` | — | — |
| `p_Transaccion` | `CHAR(4)` | `transaccion`=transacción | ✅ CÓDIGO |
| `p_CodFun` | `CHAR(3)` | — | — |
| `pNumCredito` | `CHAR(20)` | `cre`=crédito | 🟡 INFERIDO |
| `pProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INT` | L15 |
| `v_cod_ret` | `CHAR(6)` | L16 |
| `cCodigo` | `CHAR(2)` | L17 |
| `cConcepto` | `CHAR(50)` | L18 |
| `cTransaccion` | `CHAR(4)` | L19 |
| `sCod_Fun` | `CHAR(3)` | L20 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtenertransaccionpagosmanuales` | `bdicred` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00091';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00046';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00021';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00047';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `pagos` | ENTIDAD | pagos (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `manual` | MODIF | manual | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obteninforepnivelautorizacionaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obteninforepnivelautorizacionaumlincred.sql` |
| **LOC (1er CREATE)** | 219 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 102 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene información" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_obteninforepnivelautorizacionaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
  pCompIngreso                 CHAR(1)
  pFechaInc                    CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pCompIngreso` | `CHAR(1)` | — | — |
| `pFechaInc` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L50 |
| `iSqlErr` | `INTEGER` | L51 |
| `cCodRetSp` | `CHAR(6)` | L52 |
| `iCodRetSp` | `INTEGER` | L53 |
| `cEmpresa` | `CHAR(3)` | L54 |
| `cMensajeRetorno` | `CHAR(80)` | L55 |
| `cNombreCte` | `CHAR(107)` | L56 |
| `cRfc` | `CHAR(13)` | L57 |
| `cFechaNac` | `CHAR(20)` | L58 |
| `cEstadoCivil` | `CHAR(13)` | L59 |
| `cTelDomicilio` | `CHAR(13)` | L60 |
| `cTelMovil` | `CHAR(13)` | L61 |
| `cTelTrabajo` | `CHAR(13)` | L62 |
| `cCalle` | `CHAR(30)` | L63 |
| `iNumero` | `INTEGER` | L64 |
| `cColonia` | `CHAR(32)` | L65 |
| `cDelMunicipio` | `CHAR(20)` | L66 |
| `cNombreEstado` | `CHAR(30)` | L67 |
| `cCp` | `CHAR(5)` | L68 |
| `cNombreSucursal` | `CHAR(50)` | L69 |
| `cNumeroCuenta` | `CHAR(20)` | L70 |
| `cSucursal` | `CHAR(4)` | L71 |
| `cIncremento` | `CHAR(1)` | L72 |
| `cCreditoFuncionario` | `CHAR(1)` | L73 |
| `cObligadoSolidario` | `CHAR(1)` | L74 |
| *…27 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L180 |
| `bdicred` | `bdicnweb` | no | L189 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L171 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L198 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cac_obteninforepnivelautorizacion';` |  |
| L200 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L202 | CÓDIGO_RETORNO | `LET cCodRet = '00274';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obten` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?nivel` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `autoriza` | ACCION | autoriza | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nivel`, `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtienecalificacionestatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtienecalificacionestatus.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 91 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene estatus" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtienecalificacionestatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `sCalificacion` | `SMALLINT` | L13 |
| `vDesCalificacion` | `CHAR(50)` | L14 |
| `sStatus` | `SMALLINT` | L15 |
| `vDesSatus` | `CHAR(50)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_calificacion_cliente` | `bdinteg` | ⚠️ sí | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCte = 0 OR pNumCte IS NULL THEN` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `califica` | ACCION | califica / evalúa (scoring) | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `estatus` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtienecbparamcobranza`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtienecbparamcobranza.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene parámetro y cobranza" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicobranza` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtienecbparamcobranza(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigoParametro             INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigoParametro` | `INTEGER` | `param`=parámetro | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRet` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cValorPrametro` | `CHAR(100)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicobranza` | `bdicnweb` | no | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCodigoParametro IS NULL THEN` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_obtienecbparam';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?cb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `cobranza` | ENTIDAD | cobranza | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtienecverastreotef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtienecverastreotef.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene clave, rastreo y TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienecveratreo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtienecverastreotef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cCodRetSp2` | `CHAR(5)` | L7 |
| `cDescCodRet` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cCveRastreo` | `CHAR(30)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_obtienecveratreo` | `bditef` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_obtienecveratreo';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `cve` | ENTIDAD | clave (cve) | 🔵 CONVENCIÓN | nombre_sp |
| `rastreo` | ENTIDAD | rastreo (SPEI) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_obtienedigitoverificador_rfc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtienedigitoverificador_rfc.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=1 / fan_out=1 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene dígito verificador y RFC" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtienedigitoverificador_rfc(
  pRfc                         CHAR(13)
) RETURNING CHAR(1) AS digito_verificador
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRfc` | `CHAR(13)` | `rfc`=RFC (registro fiscal) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCaracter` | `CHAR(1)` | L4 |
| `bBoolValue` | `BOOLEAN` | L5 |
| `i` | `SMALLINT` | L6 |
| `iCodigoLetra` | `SMALLINT` | L7 |
| `iOperacion` | `INTEGER` | L8 |
| `iModulo` | `INTEGER` | L9 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | VALIDACIÓN_NULL | `IF iCodigoLetra IS NULL THEN` |  |
| L56 | FÓRMULA | `LET iOperacion = iOperacion + (iCodigoLetra * (14 - i));` |  |
| L65 | FÓRMULA | `LET iOperacion = 11 - iModulo;` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `digito` | ENTIDAD | dígito verificador | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `?dor_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rfc` | ENTIDAD | RFC (registro fiscal) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dor_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtieneencabezadomasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtieneencabezadomasivo.sql` |
| **LOC (1er CREATE)** | 43 |
| **Callgraph** | ✅ fan_in=314 / fan_out=0 |
| **Principales callers** | `sp_consultareportepagoscre` |
| **Propósito inferido** | "obtiene encabezado (masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtieneencabezadomasivo(
  pIdFuncion                   CHAR(10)
  pArchivoDescarga             CHAR(150)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivoDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEncabezados` | `CHAR(5000)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_encabezados_columnas_masivos` | `bdicnweb` | no | SELECT | L21 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_obtieneencabezadototalesmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtieneencabezadototalesmasivo.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=2 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene encabezado (totales, masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtieneencabezadototalesmasivo(
  pIdFuncion                   CHAR(10)
  pArchivoDescarga             CHAR(150)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivoDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEncabezados` | `CHAR(5000)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_encabezados_columnas_totales_masivos` | `bdicnweb` | no | SELECT | L24 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_obtieneinstruccionespagocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtieneinstruccionespagocap.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "obtiene cc — cuenta corriente y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtieneinstruccionespag` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtieneinstruccionespagocap(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodigoIns` | `CHAR(2)` | L10 |
| `cDescIns` | `CHAR(50)` | L11 |
| `iIdxBs` | `SMALLINT` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `i` | `SMALLINT` | L14 |
| `iRegs` | `SMALLINT` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneinstruccionespag` | `bdinvers` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | FÓRMULA | `LET iRegs = iRegs + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?tru` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?ago` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tru`, `?ion`, `?ago` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtieneparametrobts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtieneparametrobts.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene parámetro y beneficiarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtieneparametro` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtieneparametrobts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pParametro                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pParametro` | `INTEGER` | `parametro`=parámetro | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cValorParametro` | `CHAR(100)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneparametro` | `bdisac` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pParametro IS NULL THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisac:sp_obtieneparametro';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `parametro` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `bts` | ENTIDAD | beneficiarios (BTS) | 🟡 INFERIDO | nombre_sp |

---

## `sp_obtieneparametrochequeractamoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtieneparametrochequeractamoral.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene parámetro y cheque (persona moral)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtieneparametrochequera` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtieneparametrochequeractamoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodParam                    SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodParam` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(80)` | L9 |
| `cValor` | `CHAR(60)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_obtieneparametrochequera` | `bdicntchq` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCodParam IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_obtieneparametrochequera';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametro` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `cheque` | ENTIDAD | cheque | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `moral` | MODIF | persona moral | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?a` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtienesiguientelote`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtienesiguientelote.sql` |
| **LOC (1er CREATE)** | 49 |
| **Callgraph** | ✅ fan_in=9 / fan_out=2 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene teléfono" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtienesiguientelote(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iLote` | `INT` | L6 |
| `iSqlErr` | `INT` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_foliador_lotes` | `bdicnweb` | no | SELECT | L25 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF iLote IS NULL THEN` |  |
| L32 | FÓRMULA | `LET iLote = iLote + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?siguien` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tel` | ENTIDAD | teléfono | 🔵 CONVENCIÓN | nombre_sp |
| `?ote` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?siguien`, `?ote` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtieneultimasimagenesdigicte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtieneultimasimagenesdigicte.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 393 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene imágenes y cliente (últimas)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtiene_ultimas_img_digi_cte_soc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtieneultimasimagenesdigicte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(10)
) RETURNING CHAR(5) 	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(10)` | `cte`=cliente | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `CodigoDoc` | `CHAR(5)` | L16 |
| `sSecuenciaIdentReverso` | `SMALLINT` | L17 |
| `sSecuenciaIdentAnverso` | `SMALLINT` | L18 |
| `cDescripIden` | `CHAR(20)` | L19 |
| `cDescripIde2` | `CHAR(20)` | L20 |
| `cCodDctoComprobante` | `CHAR(5)` | L21 |
| `sSecuenciaMaxComprobante` | `SMALLINT` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |
| `sp_obtiene_ultimas_img_digi_cte_soc` | `bdinteg` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_obtiene_ultimas_img_digi_cte_soc";` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `ultimas` | MODIF | últimas | 🟡 INFERIDO | nombre_sp |
| `imagenes` | ENTIDAD | imágenes / documentos digitales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `digi` | ACCION | digitalización | 🟡 INFERIDO | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_obtieneultimg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_obtieneultimg.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 237 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdidigital` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtieneultimg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `cSqlerr` | `INTEGER` | L8 |
| `cIsamErr` | `INTEGER` | L9 |
| `cDescErr` | `CHAR(50)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cCodDctoComprobante` | `CHAR(5)` | L13 |
| `sSecuenciaMaxComprobante` | `SMALLINT` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |
| `bdidigital` | `bdicnweb` | no | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdidigital:sp_obtiene_ult_img_d` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?ultimg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ultimg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizarasigconfaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizarasigconfaltante.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=70 |
| **Deps concatenadas** | 104 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizarasigconfaltante` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizarasigconfaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pIdFaltante                  SMALLINT
  pFechaAsignacion             DATE
  pIdAsignado                  SMALLINT
  pUsuarioAutoriza             CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pIdFaltante` | `SMALLINT` | `fal`=fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancarias — bdiaclaracion) · `ant`=anterior | 🔵 CONVENCIÓN |
| `pFechaAsignacion` | `DATE` | — | — |
| `pIdAsignado` | `SMALLINT` | — | — |
| `pUsuarioAutoriza` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `spactualizarasigconfaltante` | `bdirech` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet ='00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='01228';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?rasig` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rasig`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizarconfaltantereversoasig`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizarconfaltantereversoasig.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 103 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizarconfaltantereversoasig` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=4 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizarconfaltantereversoasig(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pIdFaltante                  SMALLINT
  pFechaAsignacion             DATE
  pIdEstatus                   SMALLINT
  pUsuarioAutoriza             CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pIdFaltante` | `SMALLINT` | `fal`=fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancarias — bdiaclaracion) · `ant`=anterior | 🔵 CONVENCIÓN |
| `pFechaAsignacion` | `DATE` | — | — |
| `pIdEstatus` | `SMALLINT` | — | — |
| `pUsuarioAutoriza` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spactualizarconfaltantereversoasig` | `bdirech` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='01228';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?asig` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?t`, `?e`, `?asig` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizardepositofaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizardepositofaltante.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=68 |
| **Deps concatenadas** | 102 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio, depósito, OS — Originación de Solicitudes y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizardepositofaltante` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=4 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizardepositofaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pIdFaltante                  SMALLINT
  pDescuento                   MONEY(10,0)
  pFecha                       DATE
  pBancoCheque                 CHAR(40)
  pUsuarioAurotiza             CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pIdFaltante` | `SMALLINT` | `fal`=fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancarias — bdiaclaracion) · `ant`=anterior | 🔵 CONVENCIÓN |
| `pDescuento` | `MONEY(10,0)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pBancoCheque` | `CHAR(40)` | — | — |
| `pUsuarioAurotiza` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spactualizardepositofaltante` | `bdirech` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='01234';         END IF;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet ='01236';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ito` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?ito`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizardescconfaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizardescconfaltante.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=67 |
| **Deps concatenadas** | 101 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio, [polisemia] Descripción y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizardescconfaltante` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizardescconfaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaQuincena               DATE
  pUsuarioAutoriza             CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaQuincena` | `DATE` | — | — |
| `pUsuarioAutoriza` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(10)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |
| `spactualizardescconfaltante` | `bdirech` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet ='01226';         END IF;` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet ='01280'; 		END IF;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet ='01282';         END IF;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='01278'; 		END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizardescdescquincena`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizardescdescquincena.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=66 |
| **Deps concatenadas** | 100 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio, [polisemia] Descripción y quincena" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizardescdescquincena` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizardescdescquincena(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pDescAplicado                MONEY(10,0)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pDescAplicado` | `MONEY(10,0)` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) · `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `spactualizardescdescquincena` | `bdirech` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='01227';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `quincena` | ENTIDAD | quincena (periodo de pago nómina/crédito Coppel) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizarfijoconfaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizarfijoconfaltante.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=65 |
| **Deps concatenadas** | 99 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizarfijoconfaltante` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizarfijoconfaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSueldoQuincena              MONEY(16,2)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pIdFaltante                  SMALLINT
  pFechaAsignacion             DATE
  pDescQuincenaFijo            MONEY (10,0)
  pIdAsignado                  SMALLINT
  pUsuarioAutoriza             CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSueldoQuincena` | `MONEY(16,2)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pIdFaltante` | `SMALLINT` | `fal`=fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancarias — bdiaclaracion) · `ant`=anterior | 🔵 CONVENCIÓN |
| `pFechaAsignacion` | `DATE` | — | — |
| `pDescQuincenaFijo` | `MONEY (10,0)` | — | — |
| `pIdAsignado` | `SMALLINT` | — | — |
| `pUsuarioAutoriza` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `mMaximoDescuento` | `MONEY(10,0)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spactualizarfijoconfaltante` | `bdirech` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='01230'; 		END IF;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet ='01229';         END IF;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet ='01232';         END IF;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet ='01233'; 		END IF;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet ='01231';         END IF;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet ='00003';         END IF;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet ='01228';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?rfijo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rfijo`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizarfinconfaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizarfinconfaltante.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 98 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizarfinconfaltante` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizarfinconfaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pIdFaltante                  SMALLINT
  pDescuento                   MONEY(10,0)
  pFecha                       DATE
  pBancoCheque                 CHAR(40)
  pUsuarioAutoriza             CHAR (8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pIdFaltante` | `SMALLINT` | `fal`=fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancarias — bdiaclaracion) · `ant`=anterior | 🔵 CONVENCIÓN |
| `pDescuento` | `MONEY(10,0)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pBancoCheque` | `CHAR(40)` | — | — |
| `pUsuarioAutoriza` | `CHAR (8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `spactualizarfinconfaltante` | `bdirech` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet ='01234';         END IF;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='01236';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?rfin` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rfin`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizarimpconfaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizarimpconfaltante.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=63 |
| **Deps concatenadas** | 97 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio, Impago — pago vencido o fallido; confirmado: n_impagos_consec y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizarimpconfaltante` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizarimpconfaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pIdFaltante                  SMALLINT
  pDescuento                   MONEY(10,0)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pIdFaltante` | `SMALLINT` | `fal`=fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancarias — bdiaclaracion) · `ant`=anterior | 🔵 CONVENCIÓN |
| `pDescuento` | `MONEY(10,0)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(10)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `spactualizarimpconfaltante` | `bdirech` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet ='01267';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet ='01266';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet ='01257';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizarprocesos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizarprocesos.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=62 |
| **Deps concatenadas** | 96 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio y proceso" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizarprocesos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizarprocesos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaProceso                DATE
  pIdProceso                   SMALLINT
  pEstatus                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaProceso` | `DATE` | `proceso`=proceso | 🔵 CONVENCIÓN |
| `pIdProceso` | `SMALLINT` | `proceso`=proceso | 🔵 CONVENCIÓN |
| `pEstatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `spactualizarprocesos` | `bdirech` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet ='01266'; 		END IF;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet ='01238';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizarquebrantofaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizarquebrantofaltante.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=61 |
| **Deps concatenadas** | 95 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizarquebrantofaltante` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=4 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizarquebrantofaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pIdFaltante                  SMALLINT
  pFechaAsignacion             DATE
  pIdAsignado                  SMALLINT
  pUsuarioAutoriza             CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pIdFaltante` | `SMALLINT` | `ant`=anterior · `fal`=fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancarias — bdiaclaracion) | 🔵 CONVENCIÓN |
| `pFechaAsignacion` | `DATE` | — | — |
| `pIdAsignado` | `SMALLINT` | — | — |
| `pUsuarioAutoriza` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spactualizarquebrantofaltante` | `bdirech` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet ='01236';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `quebr` | ACCION | quebranto — write-off de cartera vencida (bdicred) | 🟡 INFERIDO | nombre_sp |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?o`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_actualizarsueldoempleado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_actualizarsueldoempleado.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=60 |
| **Deps concatenadas** | 94 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza oficio y empleado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spactualizarsueldoempleado` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_actualizarsueldoempleado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pSueldo                      MONEY(10,0)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | `empleado`=empleado | 🔵 CONVENCIÓN |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pSueldo` | `MONEY(10,0)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(10)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `spactualizarsueldoempleado` | `bdirech` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet ='01274';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?rsueldo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empleado` | ENTIDAD | empleado | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rsueldo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_calcularcifracontrol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_calcularcifracontrol.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 93 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "calcula oficio y rol" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spcalcularcifracontrol` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_calcularcifracontrol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaQuincena               DATE
  pTipoCifraCtrl               SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaQuincena` | `DATE` | — | — |
| `pTipoCifraCtrl` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `isuma` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spcalcularcifracontrol` | `bdirech` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet ='01266'; 		END IF;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='01240';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `calcular` | ACCION | calcula (infinitivo) | 🔵 CONVENCIÓN | nombre_sp |
| `?cifra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `rol` | ENTIDAD | rol / perfil | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cifra` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_calculardescuento`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_calculardescuento.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=58 |
| **Deps concatenadas** | 92 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "calcula oficio y [polisemia] Descripción" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spcalculardescuento` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_calculardescuento(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaQuincena               DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaQuincena` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(10)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `spcalculardescuento` | `bdirech` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet ='01275';         END IF;` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet ='01276';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | 🟡 INFERIDO | nombre_sp |
| `calcular` | ACCION | calcula (infinitivo) | 🔵 CONVENCIÓN | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `?uento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultadepositoscuentacpp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultadepositoscuentacpp.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=0 / fan_out=57 |
| **Deps concatenadas** | 91 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta oficio, depósito, OS — Originación de Solicitudes, cuenta y código postal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=4 · SINTÉTICO=2 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultadepositoscuentacpp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfechaDe                     DATE
  pfechaHasta                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfechaDe` | `DATE` | — | — |
| `pfechaHasta` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cNumsucursal` | `CHAR(4)` | L29 |
| `cNomsucursal` | `CHAR(40)` | L30 |
| `cNumempleado` | `CHAR(8)` | L31 |
| `cNomempleado` | `CHAR(45)` | L32 |
| `cDesconcepto` | `CHAR(80)` | L33 |
| `iDfaltant` | `SMALLINT` | L34 |
| `dFecharegistro` | `DATE` | L35 |
| `mSaldoinicial` | `MONEY(10,2)` | L36 |
| `mDescquincenafijo` | `MONEY(10,2)` | L37 |
| `mMontoabono` | `MONEY(10,2)` | L38 |
| `mSaldoactual` | `MONEY(10,2)` | L39 |
| `dFechaasigna` | `DATE` | L40 |
| `cDesasignado` | `CHAR(80)` | L41 |
| `iDestatu` | `SMALLINT` | L42 |
| `iMovimientoabono` | `SMALLINT` | L43 |
| `cTpomovimiento` | `CHAR(1)` | L44 |
| `dFechaasignadeposito` | `CHAR(10)` | L45 |
| `dFechaliquida` | `DATE` | L46 |
| `cAuxiliar` | `CHAR(12)` | L47 |
| `cReferencia` | `CHAR(26)` | L48 |
| `cUsuarioautoriza` | `CHAR(8)` | L49 |
| `mTotalSaldoinicial` | `MONEY(10,2)` | L50 |
| `mTotalSaldoactual` | `MONEY(10,2)` | L51 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_depfaltantes` | `bdicnweb` | no | SELECT | L107 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L97 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?it`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultadepositoscuentacpp_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultadepositoscuentacpp_totales.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=57 |
| **Deps concatenadas** | 90 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta oficio, depósito, OS — Originación de Solicitudes, cuenta y código postal (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=4 · SINTÉTICO=2 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultadepositoscuentacpp_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfechaDe                     DATE
  pfechaHasta                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfechaDe` | `DATE` | — | — |
| `pfechaHasta` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNumregistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_depfaltantes` | `bdicnweb` | no | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `?p_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?it`, `?p_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarcatasignado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarcatasignado.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=57 |
| **Deps concatenadas** | 89 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio y catálogo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarcatasignado2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarcatasignado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSecuencia                 SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSecuencia` | `SMALLINT` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cDesAsignado` | `CHAR(80)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(10)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `iIdAsignado` | `INTEGER` | L13 |
| `dFechaInsert` | `DATE` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `spconsultarcatasignado2` | `bdirech` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?do` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?do` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarcatplazas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarcatplazas.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=56 |
| **Deps concatenadas** | 88 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, catálogo y plaza" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarcatplazas` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarcatplazas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPlaza                       CHAR(3)
  pRegional                    CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPlaza` | `CHAR(3)` | `plaza`=plaza (regional) | ✅ CÓDIGO |
| `pRegional` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(10)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cPlaza` | `CHAR(3)` | L15 |
| `cNombre` | `CHAR(40)` | L16 |
| `cRegional` | `CHAR(3)` | L17 |
| `dFecha_Insert` | `DATE` | L18 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |
| `spconsultarcatplazas` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `plaza` | ENTIDAD | plaza (regional) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarcatregiones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarcatregiones.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=55 |
| **Deps concatenadas** | 87 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, catálogo y región" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarcatregiones2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarcatregiones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegional                    CHAR(3)
  pRecuperacion                INTEGER
  pRegistros                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegional` | `CHAR(3)` | `region`=región | 🔵 CONVENCIÓN |
| `pRecuperacion` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(10)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cNombre` | `CHAR(40)` | L14 |
| `cRegional` | `CHAR(3)` | L15 |
| `dFecha_Insert` | `DATE` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `spconsultarcatregiones2` | `bdinteg` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L64 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `region` | ENTIDAD | región | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarcatsucursales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarcatsucursales.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=54 |
| **Deps concatenadas** | 86 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, catálogo y sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarcatsucursales2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarcatsucursales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(10)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cNombre` | `CHAR(40)` | L20 |
| `cDireccion1` | `CHAR(40)` | L21 |
| `cDireccion2` | `CHAR(40)` | L22 |
| `cTelefono` | `CHAR(14)` | L23 |
| `cGerente` | `CHAR(40)` | L24 |
| `cSubgerente` | `CHAR(40)` | L25 |
| `cTpoSucursal` | `CHAR(2)` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L60 |
| `sp_consultarcatsucursales2` | `bdinteg` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L77 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarcatsucursales_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarcatsucursales_totales.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=53 |
| **Deps concatenadas** | 85 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, catálogo y sucursal (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarcatsucursales_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  p_sSucursal                  CHAR(4)
) RETURNING CHAR(6) AS retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `p_sSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L5 |
| `v_sValRetorno` | `CHAR(6)` | L6 |
| `iNumFilas` | `INTEGER` | L7 |
| `v_sMin` | `CHAR(4)` | L8 |
| `v_sMax` | `CHAR(4)` | L9 |
| `p_sEmpresa` | `CHAR(3)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | VALIDACIÓN_NULL | `IF p_sSucursal is null THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarconfaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarconfaltante.sql` |
| **LOC (1er CREATE)** | 155 |
| **Callgraph** | ✅ fan_in=0 / fan_out=53 |
| **Deps concatenadas** | 84 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarconfaltante2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarconfaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pNumZona                     CHAR(3)
  pNumRegional                 CHAR(3)
  pIdAsignado                  SMALLINT
  pFechaIni                    DATE
  pFechaFin                    DATE
  pIdEstatus                   SMALLINT
  pRecuperacion                INTEGER
  pRegistros                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pNumZona` | `CHAR(3)` | — | — |
| `pNumRegional` | `CHAR(3)` | — | — |
| `pIdAsignado` | `SMALLINT` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdEstatus` | `SMALLINT` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L32 |
| `cNumEmpleado` | `CHAR(8)` | L33 |
| `iSqlErr` | `INTEGER` | L34 |
| `cCodRetSp` | `CHAR(10)` | L35 |
| `iCodRetSp` | `INTEGER` | L36 |
| `cNomEmpleado` | `CHAR(45)` | L37 |
| `cSucursal` | `CHAR(4)` | L38 |
| `cNomSucursal` | `CHAR(40)` | L39 |
| `iIdFaltante` | `SMALLINT` | L40 |
| `cAuxiliar` | `CHAR(12)` | L41 |
| `cNumZona` | `CHAR(3)` | L42 |
| `cNumRegion` | `CHAR(3)` | L43 |
| `iIdConcepto` | `SMALLINT` | L44 |
| `cDesConcepto` | `CHAR(80)` | L45 |
| `iIdRecupera` | `SMALLINT` | L46 |
| `cDesRecupera` | `CHAR(80)` | L47 |
| `iIdAsignado` | `SMALLINT` | L48 |
| `cDesAsignado` | `CHAR(80)` | L49 |
| `iIdEstatus` | `SMALLINT` | L50 |
| `cDesEstatus` | `CHAR(80)` | L51 |
| `mSaldoInicial` | `MONEY(10,2)` | L52 |
| `mDescAcumulado` | `MONEY(10,2)` | L53 |
| `mDescCalculado` | `MONEY(10,2)` | L54 |
| `mSaldoActual` | `MONEY(10,2)` | L55 |
| `cBancoCheque` | `CHAR(40)` | L56 |
| *…7 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L115 |
| `spconsultarconfaltante2` | `bdirech` | ⚠️ sí | L130 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L132 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarconfaltante_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarconfaltante_totales.sql` |
| **LOC (1er CREATE)** | 171 |
| **Callgraph** | ✅ fan_in=0 / fan_out=52 |
| **Deps concatenadas** | 83 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio y fal — fallo/disputa (anterior, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarconfaltante_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR (10)
  p_sNumEmpleado               CHAR(8)
  p_sNumSucursal               CHAR(4)
  p_sNumZona                   CHAR(3)
  p_sNumRegional               CHAR(3)
  p_iIdAsignado                SMALLINT
  p_dFechaIni                  DATE
  p_dFechaFin                  DATE
  p_iIdEstatus                 SMALLINT
) RETURNING CHAR(5) AS CodigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR (10)` | — | — |
| `p_sNumEmpleado` | `CHAR(8)` | — | — |
| `p_sNumSucursal` | `CHAR(4)` | — | — |
| `p_sNumZona` | `CHAR(3)` | — | — |
| `p_sNumRegional` | `CHAR(3)` | — | — |
| `p_iIdAsignado` | `SMALLINT` | — | — |
| `p_dFechaIni` | `DATE` | — | — |
| `p_dFechaFin` | `DATE` | — | — |
| `p_iIdEstatus` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L8 |
| `v_sCodRet` | `CHAR(5)` | L9 |
| `iNumFilas` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_confaltante` | `bdirech` | ⚠️ sí | SELECT | L87 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET v_sCodRet='00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET v_sCodRet = '00003';` |  |
| L82 | VALIDACIÓN_NULL | `IF (p_iIdAsignado > 0 OR p_iIdAsignado IS NULL) and (p_iIdEstatus not in (4,7) or p_iIdEstatus IS NU` |  |
| L95 | CÓDIGO_RETORNO | `LET v_sCodRet ='01126';` |  |
| L101 | VALIDACIÓN_NULL | `if (p_iIdAsignado > 0 OR p_iIdAsignado IS NULL) THEN` |  |
| L116 | CÓDIGO_RETORNO | `LET v_sCodRet ='01126';` |  |
| L137 | CÓDIGO_RETORNO | `LET v_sCodRet ='01126';` |  |
| L156 | CÓDIGO_RETORNO | `LET v_sCodRet ='01126';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t`, `?e_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultardeschistorico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultardeschistorico.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=52 |
| **Deps concatenadas** | 82 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio y [polisemia] Descripción (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultardeschistorico2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultardeschistorico(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pFechaDescuento              DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pFechaDescuento` | `DATE` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cNumempleado` | `CHAR(8)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(10)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cAuxiliar` | `CHAR(12)` | L17 |
| `cNumsucursal` | `CHAR(4)` | L18 |
| `dFechadesc` | `DATE` | L19 |
| `mSueldoquincena` | `MONEY (10,0)` | L20 |
| `mDesccalculado` | `MONEY (10,0)` | L21 |
| `mDescaplicado` | `MONEY (10,0)` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `spconsultardeschistorico2` | `bdirech` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_consultardeschistorico_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultardeschistorico_totales.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=51 |
| **Deps concatenadas** | 81 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio y [polisemia] Descripción (histórico, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultardeschistorico_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  p_sNumEmpleado               CHAR(8)
  p_sNumSucursal               CHAR(4)
  p_dFechaDescuento            DATE
) RETURNING CHAR(5) AS retorno, INTEGER as numFilas
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `p_sNumEmpleado` | `CHAR(8)` | — | — |
| `p_sNumSucursal` | `CHAR(4)` | — | — |
| `p_dFechaDescuento` | `DATE` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L4 |
| `v_sCodRet` | `CHAR(5)` | L5 |
| `iNumFilas` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_deschistorico` | `bdirech` | ⚠️ sí | SELECT | L58 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET v_sCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET v_sCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET v_sCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET v_sCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_consultardescquincena`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultardescquincena.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=51 |
| **Deps concatenadas** | 80 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, [polisemia] Descripción y quincena" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultardescquincena2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultardescquincena(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pSucursal                    CHAR(4)
  pFechaDescuento              CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaDescuento` | `CHAR(10)` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cNumEmpleado` | `CHAR(8)` | L13 |
| `cNumSucursal` | `CHAR (4)` | L14 |
| `cAuxiliar` | `CHAR(12)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(10)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `dFechaDesc` | `DATE` | L19 |
| `mSueldoQuincena` | `MONEY(10,0)` | L20 |
| `mDescCalculado` | `MONEY(10,0)` | L21 |
| `mDescAplicado` | `MONEY(10,0)` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `spconsultardescquincena2` | `bdirech` | ⚠️ sí | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `quincena` | ENTIDAD | quincena (periodo de pago nómina/crédito Coppel) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_consultardescquincena_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultardescquincena_totales.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 79 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, [polisemia] Descripción y quincena (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultardescquincena_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  p_sNumEmpleado               CHAR(8)
  p_sNumSucursal               CHAR(4)
  p_dFechaDescuento            DATE
) RETURNING CHAR(5) AS retorno, INTEGER as numFilas
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `p_sNumEmpleado` | `CHAR(8)` | — | — |
| `p_sNumSucursal` | `CHAR(4)` | — | — |
| `p_dFechaDescuento` | `DATE` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L4 |
| `v_sCodRet` | `CHAR(5)` | L5 |
| `iNumFilas` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_descquincena` | `bdirech` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET v_sCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET v_sCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET v_sCodRet ='01265';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `quincena` | ENTIDAD | quincena (periodo de pago nómina/crédito Coppel) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_consultarempconfaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarempconfaltante.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=50 |
| **Deps concatenadas** | 78 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, Empresa — empleadora del cliente; vinculada a crédito de nómina y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarempconfaltante2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarempconfaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(10)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cNumEmpleado` | `CHAR(8)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spconsultarempconfaltante2` | `bdirech` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iNoRegistros = iNoRegistros+1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '01273';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarempleado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarempleado.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 77 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio y empleado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarempleado` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarempleado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | `empleado`=empleado | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(10)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cNumEmpleado` | `CHAR(8)` | L17 |
| `cNombre` | `CHAR(45)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cPuesto` | `CHAR(3)` | L20 |
| `cNombramiento` | `CHAR(20)` | L21 |
| `cAsistente` | `CHAR(10)` | L22 |
| `cPassword` | `CHAR(40)` | L23 |
| `iContador` | `INTEGER` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |
| `sp_consultarempleado` | `bdinteg` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | FÓRMULA | `LET iContador = iContador +1;` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '01264';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `empleado` | ENTIDAD | empleado | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_consultarerroresfaltantesarch_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarerroresfaltantesarch_totales.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 75 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, error y archivo (faltantes, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarerroresfaltantesarch_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(20)
) RETURNING CHAR(5) AS codret, INTEGER AS numRegistros
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | `arch`=archivo | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `inumRegistros` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(10)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_faltantesarch` | `bdirech` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `faltantes` | MODIF | faltantes | 🔵 CONVENCIÓN | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarfaltantesexcluidos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarfaltantesexcluidos.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 74 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, identificador y OS — Originación de Solicitudes (faltantes)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarfaltantesexcluidos2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarfaltantesexcluidos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pNumZona                     CHAR(3)
  pNumRegional                 CHAR(3)
  pIdAsignado                  SMALLINT
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,CHAR(8) AS NumEmpleado, CHAR(45) AS NomEmpleado, CHAR(4) AS Sucursal, CHAR(40) AS NomSucursal,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pNumZona` | `CHAR(3)` | — | — |
| `pNumRegional` | `CHAR(3)` | — | — |
| `pIdAsignado` | `SMALLINT` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cNumEmpleado` | `CHAR(8)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(10)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cNomEmpleado` | `CHAR(45)` | L14 |
| `cSucursal` | `CHAR(4)` | L15 |
| `cNomSucursal` | `CHAR(40)` | L16 |
| `iIdFaltante` | `SMALLINT` | L17 |
| `cAuxiliar` | `CHAR(12)` | L18 |
| `cNumZona` | `CHAR(3)` | L19 |
| `cNumRegion` | `CHAR(3)` | L20 |
| `iIdConcepto` | `SMALLINT` | L21 |
| `cDesConcepto` | `CHAR(80)` | L22 |
| `iIdRecupera` | `SMALLINT` | L23 |
| `cDesRecupera` | `CHAR(80)` | L24 |
| `iIdAsignado` | `SMALLINT` | L25 |
| `cDesAsignado` | `CHAR(80)` | L26 |
| `iIdEstatus` | `SMALLINT` | L27 |
| `cDesEstatus` | `CHAR(80)` | L28 |
| `mSaldoInicial` | `MONEY(10,0)` | L29 |
| `mDescAcumulado` | `MONEY(10,0)` | L30 |
| `mDescCalculado` | `MONEY(10,0)` | L31 |
| `mSaldoActual` | `MONEY(10,0)` | L32 |
| `cBancoCheque` | `CHAR(40)` | L33 |
| *…7 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L98 |
| `spconsultarfaltantesexcluidos2` | `bdirech` | ⚠️ sí | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L109 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `faltantes` | MODIF | faltantes | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?clu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?clu` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarfaltantesexcluidos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarfaltantesexcluidos_totales.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 73 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio, identificador y OS — Originación de Solicitudes (faltantes, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarfaltantesexcluidos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  p_sNumEmpleado               CHAR(8)
  p_sNumSucursal               CHAR(4)
  p_sNumZona                   CHAR(3)
  p_sNumRegional               CHAR(3)
  p_iIdAsignado                SMALLINT
  p_dFechaIni                  DATE
  p_dFechaFin                  DATE
) RETURNING CHAR(5) AS CodigoRetorno, INTEGER as numFilas
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `p_sNumEmpleado` | `CHAR(8)` | — | — |
| `p_sNumSucursal` | `CHAR(4)` | — | — |
| `p_sNumZona` | `CHAR(3)` | — | — |
| `p_sNumRegional` | `CHAR(3)` | — | — |
| `p_iIdAsignado` | `SMALLINT` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `p_dFechaIni` | `DATE` | — | — |
| `p_dFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cNumEmpleado` | `CHAR(8)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `v_sCodRet` | `CHAR(5)` | L12 |
| `iNumFilas` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_confaltante` | `bdirech` | ⚠️ sí | SELECT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET v_sCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | VALIDACIÓN_NULL | `IF p_iIdAsignado > 0 OR p_iIdAsignado IS NULL THEN` |  |
| L84 | CÓDIGO_RETORNO | `LET v_sCodRet ='01030';` |  |
| L102 | CÓDIGO_RETORNO | `LET v_sCodRet ='01030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `faltantes` | MODIF | faltantes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?clu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?clu` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarfaltantesreversoasig`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarfaltantesreversoasig.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 72 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio (faltantes)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarfaltantesreversoasig2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarfaltantesreversoasig(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pNumZona                     CHAR(3)
  pNumRegional                 CHAR(3)
  pIdAsignado                  SMALLINT
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pNumZona` | `CHAR(3)` | — | — |
| `pNumRegional` | `CHAR(3)` | — | — |
| `pIdAsignado` | `SMALLINT` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `cNumEmpleado` | `CHAR(8)` | L25 |
| `cNomEmpleado` | `CHAR (45)` | L26 |
| `cSucursal` | `CHAR(12)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cCodRetSp` | `CHAR(10)` | L29 |
| `iCodRetSp` | `INTEGER` | L30 |
| `cNomSucursal` | `CHAR(40)` | L31 |
| `iIdFaltante` | `SMALLINT` | L32 |
| `iIdConcepto` | `SMALLINT` | L33 |
| `cDesConcepto` | `CHAR(80)` | L34 |
| `iIdRecupera` | `SMALLINT` | L35 |
| `cDesRecupera` | `CHAR(80)` | L36 |
| `iIdAsignado` | `SMALLINT` | L37 |
| `cDesAsignado` | `CHAR(80)` | L38 |
| `iIdAsignadoAnt` | `SMALLINT` | L39 |
| `iIdEstatus` | `SMALLINT` | L40 |
| `cDesEstatus` | `CHAR(80)` | L41 |
| `mSaldoActual` | `MONEY(10,2)` | L42 |
| `dFechaAsigna` | `DATE` | L43 |
| `dFechaRegistro` | `DATE` | L44 |
| `iRecuperacion` | `INTEGER` | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L86 |
| `spconsultarfaltantesreversoasig2` | `bdirech` | ⚠️ sí | L103 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L105 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `faltantes` | MODIF | faltantes | 🔵 CONVENCIÓN | nombre_sp |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?asig` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?asig` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarfaltantesreversoasig_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarfaltantesreversoasig_totales.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=45 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio (faltantes, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarfaltantesreversoasig_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  p_sNumEmpleado               CHAR(8)
  p_sNumSucursal               CHAR(4)
  p_sNumZona                   CHAR(3)
  p_sNumRegional               CHAR(3)
  p_iIdAsignado                SMALLINT
  p_dFechaIni                  DATE
  p_dFechaFin                  DATE
) RETURNING CHAR(5) AS CodigoRetorno, INTEGER AS numFilas
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `p_sNumEmpleado` | `CHAR(8)` | — | — |
| `p_sNumSucursal` | `CHAR(4)` | — | — |
| `p_sNumZona` | `CHAR(3)` | — | — |
| `p_sNumRegional` | `CHAR(3)` | — | — |
| `p_iIdAsignado` | `SMALLINT` | — | — |
| `p_dFechaIni` | `DATE` | — | — |
| `p_dFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L6 |
| `v_sCodRet` | `CHAR(5)` | L7 |
| `inumFilas` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_confaltante` | `bdirech` | ⚠️ sí | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET v_sCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET v_sCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `faltantes` | MODIF | faltantes | 🔵 CONVENCIÓN | nombre_sp |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?asig_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?asig_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_consultarparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarparam.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=45 |
| **Deps concatenadas** | 70 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio y parámetro" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarparam` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdSecuencia                 SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdSecuencia` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cDescripcion` | `CHAR(60)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(10)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cValor` | `CHAR(20)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `spconsultarparam` | `bdirech` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | FÓRMULA | `LET cValor = SUBSTR(cValor,1,2)\|\|'/'\|\|SUBSTR(cValor,4,2)\|\|'/'\|\| SUBSTR(cValor,7,4);` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_consultarprocesos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_consultarprocesos.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar oficio y proceso" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spconsultarprocesos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_consultarprocesos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaProceso                DATE
  pIdproceso                   SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaProceso` | `DATE` | `proceso`=proceso | 🔵 CONVENCIÓN |
| `pIdproceso` | `SMALLINT` | `proceso`=proceso | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cDescripcionProceso` | `CHAR(40)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(10)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `iIdProceso` | `SMALLINT` | L14 |
| `dFechaProceso` | `DATE` | L15 |
| `cEstatus` | `CHAR(1)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `spconsultarprocesos` | `bdirech` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | FÓRMULA | `LET iRecuperacion = iRecuperacion+1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_eliminaerrores`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_eliminaerrores.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=43 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina oficio y error" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `speliminaerrores` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_eliminaerrores(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(10)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `speliminaerrores` | `bdirech` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarfechasquincenas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarfechasquincenas.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 67 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio, fechas y quincena" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgenerarfechasquincenas2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarfechasquincenas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `dFechaQuincena` | `DATE` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `spgenerarfechasquincenas2` | `bdirech` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L54 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '01268';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `fechas` | ENTIDAD | fechas | 🔵 CONVENCIÓN | nombre_sp |
| `quincena` | ENTIDAD | quincena (periodo de pago nómina/crédito Coppel) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarfechasquincenas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarfechasquincenas_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=41 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio, fechas y quincena (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarfechasquincenas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS CodigoRetorno,  INTEGER as numFilas
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(10)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iNumFilas` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_param` | `bdirech` | ⚠️ sí | SELECT | L45 |
| `rec_deschistorico` | `bdirech` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `fechas` | ENTIDAD | fechas | 🔵 CONVENCIÓN | nombre_sp |
| `quincena` | ENTIDAD | quincena (periodo de pago nómina/crédito Coppel) | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarpolizaasignacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarpolizaasignacion.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=41 |
| **Deps concatenadas** | 65 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio y póliza contable" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgenerarpolizaasignacion2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarpolizaasignacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaActual                 DATE
  pNumEmpleado                 CHAR(8)
  p_Usuario                    CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaActual` | `DATE` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `p_Usuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNumeroPoliza` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `spgenerarpolizaasignacion2` | `bdirech` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet ='00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet ='01247';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet ='01248';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet ='01244';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarpolizaliquidacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarpolizaliquidacion.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=40 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio, póliza contable y liquidación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgenerarpolizaliquidacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarpolizaliquidacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaActual                 DATE
  pNumEmpleado                 CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaActual` | `DATE` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `inumeroPoliza` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spgenerarpolizaliquidacion` | `bdirech` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet ='00000'; 		END IF;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='01247';         END IF;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet ='01272';         END IF;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet ='01244';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | 🔵 CONVENCIÓN | nombre_sp |
| `liquidacion` | ENTIDAD | liquidación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_generarpolizanomina`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarpolizanomina.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio, póliza contable y nómina" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarpolizanomina(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaActual                 DATE
  pFechaQuincena               DATE
  pUsuarioSistema              CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaActual` | `DATE` | — | — |
| `pFechaQuincena` | `DATE` | — | — |
| `pUsuarioSistema` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNumeroPoliza` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatuspoliza` | `bdicnweb` | no | SELECT | L41 |
| `sw_verificastatuspoliza` | `bdicnweb` | no | DELETE | L41 |
| `sw_verificastatuspoliza` | `bdicnweb` | no | INSERT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `spgenerarpolizanomina` | `bdirech` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet ='00000';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet ='00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet ='01245';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet ='01246';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet ='01241';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `nomina` | ENTIDAD | nómina | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_generarpolizareasignacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarpolizareasignacion.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=39 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio y póliza contable" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgenerarpolizareasignacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarpolizareasignacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaActual                 CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaActual` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iNumeroPoliza` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `dFechaActual` | `CHAR(10)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `spgenerarpolizareasignacion` | `bdirech` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet ='01247';         END IF;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet ='01248';         END IF;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet ='01244';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | 🔵 CONVENCIÓN | nombre_sp |
| `?re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?re`, `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarpolizareasignacion2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarpolizareasignacion2.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio y póliza contable" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarpolizareasignacion2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaActual                 CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaActual` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iNumeroPoliza` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `dFechaActual` | `CHAR(10)` | L11 |
| `iRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatuspoliza` | `bdicnweb` | no | SELECT | L56 |
| `sw_verificastatuspoliza` | `bdicnweb` | no | DELETE | L56 |
| `sw_verificastatuspoliza` | `bdicnweb` | no | INSERT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `spgenerarpolizareasignacion` | `bdirech` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet ='00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet ='01247';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet ='01248';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet ='01244';` |  |
| L100 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?cion2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?re`, `?cion2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarpolizareasigquebranto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarpolizareasigquebranto.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio y póliza contable (anterior)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarpolizareasigquebranto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaActual                 DATE
  p_Usuario                    CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaActual` | `DATE` | — | — |
| `p_Usuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNumeroPoliza` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatuspoliza` | `bdicnweb` | no | SELECT | L46 |
| `sw_verificastatuspoliza` | `bdicnweb` | no | DELETE | L46 |
| `sw_verificastatuspoliza` | `bdicnweb` | no | INSERT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `spgenerarpolizareasigquebranto` | `bdirech` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet ='01247';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet ='01248';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet ='01244';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?reasig` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `quebr` | ACCION | quebranto — write-off de cartera vencida (bdicred) | 🟡 INFERIDO | nombre_sp |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?reasig`, `?o` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarpolizareversoasig`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarpolizareversoasig.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=38 |
| **Deps concatenadas** | 62 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio y póliza contable" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgenerarpolizareversoasig` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarpolizareversoasig(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaActual                 DATE
  p_Usuario                    CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaActual` | `DATE` | — | — |
| `p_Usuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `inumeroPoliza` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spgenerarpolizareversoasig` | `bdirech` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet ='01271';         END IF;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet ='01248'; 		END IF;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet ='01244';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | 🔵 CONVENCIÓN | nombre_sp |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?asig` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?asig` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarpolizareversoasig_quebranto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarpolizareversoasig_quebranto.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=37 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio y póliza contable (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgenerarpolizareversoasig_quebranto` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarpolizareversoasig_quebranto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaActual                 DATE
  p_Usuario                    CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaActual` | `DATE` | — | — |
| `p_Usuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `inumeroPoliza` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spgenerarpolizareversoasig_quebranto` | `bdirech` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet ='00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='01247';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet ='01248';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet ='01244';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `poliza` | ENTIDAD | póliza contable | 🔵 CONVENCIÓN | nombre_sp |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `?asig_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `quebr` | ACCION | quebranto — write-off de cartera vencida (bdicred) | 🟡 INFERIDO | nombre_sp |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?asig_`, `?o` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_generarrespaldos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_generarrespaldos.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=36 |
| **Deps concatenadas** | 60 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar oficio y respaldo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgenerarrespaldos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_generarrespaldos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaQuincena               DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaQuincena` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(10)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `spgenerarrespaldos` | `bdirech` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet ='01253';         END IF;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='01254'; 		END IF;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet ='01255';         END IF;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet ='01251'; 		END IF;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet ='01252';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `respaldo` | ENTIDAD | respaldo / garantía de crédito (aval) | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_genrepdepositoscuentacpp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_genrepdepositoscuentacpp.sql` |
| **LOC (1er CREATE)** | 225 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte oficio, depósito, OS — Originación de Solicitudes, cuenta y código postal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=5 · SINTÉTICO=2 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_genrepdepositoscuentacpp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pfechaDe                     CHAR(10)
  pfechaHasta                  CHAR(10)
  pRutaDescarga                CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pfechaDe` | `CHAR(10)` | — | — |
| `pfechaHasta` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cNumsucursal` | `CHAR(4)` | L9 |
| `cNomsucursal` | `CHAR(40)` | L10 |
| `cNumempleado` | `CHAR(8)` | L11 |
| `cNomempleado` | `CHAR(45)` | L12 |
| `cDesconcepto` | `CHAR(80)` | L13 |
| `iDfaltante` | `SMALLINT` | L14 |
| `dFecharegistro` | `DATE` | L15 |
| `mSaldoinicial` | `MONEY(10,2)` | L16 |
| `mDescquincenafijo` | `MONEY(10,2)` | L17 |
| `mMontoabono` | `MONEY(10,2)` | L18 |
| `mSaldoactual` | `MONEY(10,2)` | L19 |
| `dFechaasigna` | `DATE` | L20 |
| `cDesasignado` | `CHAR(80)` | L21 |
| `iDestatus` | `SMALLINT` | L22 |
| `iMovimientoabono` | `SMALLINT` | L23 |
| `cTpomovimiento` | `CHAR(1)` | L24 |
| `dFechaasignadeposito` | `DATE` | L25 |
| `dFechaliquida` | `DATE` | L26 |
| `cAuxiliar` | `CHAR(12)` | L27 |
| `cReferencia` | `CHAR(26)` | L28 |
| `cUsuarioautoriza` | `CHAR(8)` | L29 |
| *…19 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L142 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L118 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L131 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L138 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT * FROM( ";` |  |
| L142 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L143 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT ''''\|\|numsucursal::CHAR(4),nomsucursal::CHAR(40),''''\|\|n` |  |
| L144 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" saldoactual::CHAR(12),TO_CHAR(fechaasigna, '%d/%m/%Y'),desasignado` |  |
| L170 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L175 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L184 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L193 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?it`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_genrepdescuentoquincenal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_genrepdescuentoquincenal.sql` |
| **LOC (1er CREATE)** | 221 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte oficio, [polisemia] Descripción y quincena" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_genrepdescuentoquincenal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pSucursal                    CHAR(4)
  pFechaDescuento              CHAR(10)
  pRutaDescarga                CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaDescuento` | `CHAR(10)` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |
| `pRutaDescarga` | `CHAR(50)` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cNumsucursal` | `CHAR(4)` | L9 |
| `cNomsucursal` | `CHAR(40)` | L10 |
| `cNumempleado` | `CHAR(8)` | L11 |
| `cNomempleado` | `CHAR(45)` | L12 |
| `cDesconcepto` | `CHAR(80)` | L13 |
| `iDfaltante` | `SMALLINT` | L14 |
| `dFecharegistro` | `DATE` | L15 |
| `mSaldoinicial` | `MONEY(10,2)` | L16 |
| `mDescquincenafijo` | `MONEY(10,2)` | L17 |
| `mMontoabono` | `MONEY(10,2)` | L18 |
| `mSaldoactual` | `MONEY(10,2)` | L19 |
| `dFechaasigna` | `DATE` | L20 |
| `cDesasignado` | `CHAR(80)` | L21 |
| `iDestatus` | `SMALLINT` | L22 |
| `iMovimientoabono` | `SMALLINT` | L23 |
| `cTpomovimiento` | `CHAR(1)` | L24 |
| `dFechaasignadeposito` | `DATE` | L25 |
| `dFechaliquida` | `DATE` | L26 |
| `cAuxiliar` | `CHAR(12)` | L27 |
| `cReferencia` | `CHAR(26)` | L28 |
| `cUsuarioautoriza` | `CHAR(8)` | L29 |
| *…19 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L143 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | FÓRMULA | `LET cFechaAux = SUBSTR(pFechaDescuento,1,2)\|\|'/'\|\|SUBSTR(pFechaDescuento,4,2)\|\|'/'\|\|SUBSTR(p` |  |
| L94 | FÓRMULA | `LET cFechaAux2 = SUBSTR(pFechaDescuento,4,2)\|\|'/'\|\|SUBSTR(pFechaDescuento,1,2)\|\|'/'\|\|SUBSTR(` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L134 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L141 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT * FROM( ";` |  |
| L143 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L172 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L177 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L186 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L195 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `?uento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `quincena` | ENTIDAD | quincena (periodo de pago nómina/crédito Coppel) | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uento`, `?l` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_grabarcifracontrol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_grabarcifracontrol.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba oficio y rol" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgrabarcifracontrol` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_grabarcifracontrol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaQuincena               DATE
  pTipoCifraCtrl               SMALLINT
  pMonto                       MONEY(18,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaQuincena` | `DATE` | — | — |
| `pTipoCifraCtrl` | `SMALLINT` | — | — |
| `pMonto` | `MONEY(18,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(10)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `spgrabarcifracontrol` | `bdirech` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '01266';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet ='01266'; 		END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?rcifra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `rol` | ENTIDAD | rol / perfil | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rcifra` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_grabarconfaltante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_grabarconfaltante.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=34 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba oficio y fal — fallo/disputa (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgrabarconfaltante` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_grabarconfaltante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
  pNumSucursal                 CHAR(4)
  pIdConcepto                  INTEGER
  pIdRecupera                  INTEGER
  pIdAsignado                  INTEGER
  pSaldo                       MONEY(10,0)
  pFechaRegistro               DATE
  pUsuarioAutoriza             CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | — | — |
| `pNumSucursal` | `CHAR(4)` | — | — |
| `pIdConcepto` | `INTEGER` | `con`=consulta | 🟡 INFERIDO |
| `pIdRecupera` | `INTEGER` | — | — |
| `pIdAsignado` | `INTEGER` | — | — |
| `pSaldo` | `MONEY(10,0)` | — | — |
| `pFechaRegistro` | `DATE` | — | — |
| `pUsuarioAutoriza` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(10)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iNumfaltante` | `INTEGER` | L12 |
| `cReferencia` | `CHAR(26)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `spgrabarconfaltante` | `bdirech` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet ='01262'; 		END IF;` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet ='01247';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_grabarconfaltantearch`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_grabarconfaltantearch.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=33 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "graba oficio, fal — fallo/disputa y archivo (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `spgrabarconfaltantearch2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_grabarconfaltantearch(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaAsigna                 CHAR(10)
  pNombreArchivo               CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaAsigna` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | `arch`=archivo | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cNombreArchivo` | `CHAR(20)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `dFechaAsigna` | `DATE` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `spgrabarconfaltantearch2` | `bdirech` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET pFechaAsigna = SUBSTRING(TRIM(pFechaAsigna) FROM 6 FOR 2)\|\|'/'\|\|SUBSTRING(TRIM(pFechaAsigna)` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?t`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_insertaerrores`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_insertaerrores.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta oficio y error" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_insertaerrores(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripcion                 CHAR(80)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripcion` | `CHAR(80)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(10)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `rec_errores` | `bdirech` | ⚠️ sí | INSERT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_lecturaarchivocargafaltantes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_lecturaarchivocargafaltantes.sql` |
| **LOC (1er CREATE)** | 299 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga oficio y archivo (faltantes)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=0 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_lecturaarchivocargafaltantes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaArchivo                 CHAR(100)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaArchivo` | `CHAR(100)` | `archivo`=archivo | ✅ CÓDIGO |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `cDesCodRetSp` | `CHAR(100)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cRenglon` | `CHAR(400)` | L11 |
| `iLinea` | `INTEGER` | L12 |
| `cCampo` | `CHAR(35)` | L13 |
| `cDesMensajeError` | `CHAR(120)` | L14 |
| `iContador` | `INTEGER` | L15 |
| `cObservaciones` | `CHAR(50)` | L17 |
| `bBanderaError` | `CHAR(1)` | L18 |
| `sEnTransacc` | `SMALLINT` | L19 |
| `cSqlerr` | `INTEGER` | L20 |
| `cIsamErr` | `INTEGER` | L21 |
| `cDescErr` | `CHAR(50)` | L22 |
| `cSQL` | `CHAR(500)` | L23 |
| `iNoProcesado` | `INTEGER` | L24 |
| `bInTransaction` | `BOOLEAN` | L25 |
| `ven_transacc` | `SMALLINT` | L26 |
| `cPathdbaccess` | `CHAR(20)` | L27 |
| `iNroSecuencia` | `INTEGER` | L28 |
| `iNroLinea` | `INTEGER` | L29 |
| *…37 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ofi_statuslecturaarchivocargafaltantes` | `bdicnweb` | no | UPDATE | L166 |
| `sw_ofi_statuslecturaarchivocargafaltantes` | `bdicnweb` | no | SELECT | L187 |
| `sw_ofi_statuslecturaarchivocargafaltantes` | `bdicnweb` | no | DELETE | L187 |
| `sw_ofi_statuslecturaarchivocargafaltantes` | `bdicnweb` | no | INSERT | L191 |
| `rec_faltantesarch` | `bdirech` | ⚠️ sí | SELECT | L203 |
| `rec_faltantesarch` | `bdirech` | ⚠️ sí | DELETE | L203 |
| `systables` | `bdicnweb` | no | SELECT | L207 |
| `sp_ofi_rec_faltantesarch_tmp` | `bdicnweb` | no | INSERT | L238 |
| `sp_ofi_rec_faltantesarch_tmp` | `bdicnweb` | no | SELECT | L258 |
| `rec_faltantesarch` | `bdirech` | ⚠️ sí | INSERT | L260 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L174 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L164 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L233 | FÓRMULA | `LET cSQL = "/usr/bin/rm -rf "\|\|TRIM(pRutaArchivo)\|\|TRIM(pNombrearchivo)\|\|'; /usr/bin/mv '\|\|T` |  |
| L238 | FÓRMULA | `LET cSQL = "/usr/bin/echo 'SET ISOLATION TO DIRTY READ; LOAD FROM "\|\|TRIM(pRutaArchivo)\|\|TRIM(pN` |  |
| L239 | FÓRMULA | `LET cSQL = TRIM(cSQL)\|\|"numsucursal,numempleado,saldoinicial,fecharegistro)' \| /informix/bin/dbac` |  |
| L264 | FÓRMULA | `LET iLinea = iLinea+1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?_lectura` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `faltantes` | MODIF | faltantes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_lectura` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_obtenerfechahoy`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_obtenerfechahoy.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene oficio y fecha (de hoy)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtenerfechahoy` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_obtenerfechahoy(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `dFecha` | `DATE` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `pEmpresa` | `CHAR(3)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_obtenerfechahoy` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `hoy` | MODIF | de hoy / fecha actual | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ofi_validaemp_reversoquebranto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_validaemp_reversoquebranto.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida oficio y Empresa — empleadora del cliente; vinculada a crédito de nómina (anterior)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_validaemp_reversoquebranto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumemp                      char(8)
  pValida                      INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumemp` | `char(8)` | `emp`=Empresa — empleadora del cliente; vinculada a crédito de nómina (ADN); SPs: sp_consulta_datos_emp_bei (phone+address), sp_genera_emp_gc (Grupo Coppel), inserta_rel_cte_emp | 🔵 CONVENCIÓN |
| `pValida` | `INTEGER` | `valida`=valida | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `inumeroPoliza` | `INTEGER` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_validaemp_reversoquebranto` | `bdirech` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='01258';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `reverso` | ACCION | reverso | 🔵 CONVENCIÓN | nombre_sp |
| `quebr` | ACCION | quebranto — write-off de cartera vencida (bdicred) | 🟡 INFERIDO | nombre_sp |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_validararchivos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_validararchivos.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida oficio y archivos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_validararchivos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaRegistro               CHAR(10)
  pNombreArchivo               CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaRegistro` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cNombreArchivo` | `CHAR(20)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(10)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `dFechaRegistro` | `DATE` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `spvalidararchivos` | `bdirech` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET pFechaRegistro = SUBSTRING(TRIM(pFechaRegistro) FROM 6 FOR 2)\|\|'/'\|\|SUBSTRING(TRIM(pFechaReg` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet ='00492';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet ='01260';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivos` | ENTIDAD | archivos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_validarcifracontrol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_validarcifracontrol.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida oficio y rol" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_validarcifracontrol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaQuincena               DATE
  pTipoCifraCtrl               SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaQuincena` | `DATE` | — | — |
| `pTipoCifraCtrl` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `mDiferencia` | `MONEY(18,2)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `spvalidarcifracontrol` | `bdirech` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet ='01261';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?rcifra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `rol` | ENTIDAD | rol / perfil | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rcifra` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_validarfaltantesarch`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_validarfaltantesarch.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida oficio y archivo (faltantes)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_validarfaltantesarch(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaAsigna                 CHAR(10)
  pNombreArchivo               CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaAsigna` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | `arch`=archivo | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iErrores` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(10)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `dFechaAsigna` | `DATE` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `spvalidarfaltantesarch2` | `bdirech` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | FÓRMULA | `LET pFechaAsigna = SUBSTRING(TRIM(pFechaAsigna) FROM 6 FOR 2)\|\|'/'\|\|SUBSTRING(TRIM(pFechaAsigna)` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet ='00003'; 		END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `faltantes` | MODIF | faltantes | 🔵 CONVENCIÓN | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_validarprocesos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_validarprocesos.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida oficio y proceso" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_validarprocesos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaProceso                DATE
  pIdproceso                   SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaProceso` | `DATE` | `proceso`=proceso | 🔵 CONVENCIÓN |
| `pIdproceso` | `SMALLINT` | `proceso`=proceso | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iCantErrores` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(10)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEstatus` | `CHAR(1)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `spvalidarprocesos` | `bdirech` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet ='01262'; 		END IF;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet ='01263';         END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ofi_verificastatusarchivocargafaltantes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ofi_verificastatusarchivocargafaltantes.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica oficio, estatus y archivo (faltantes)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ofi_verificastatusarchivocargafaltantes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(35)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(35)` | `archivo`=archivo | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cBanDetError` | `CHAR(1)` | L12 |
| `cErrorProceso` | `CHAR(1)` | L13 |
| `cError` | `CHAR(5)` | L14 |
| `iTotal` | `INTEGER` | L15 |
| `iProcesados` | `INTEGER` | L16 |
| `iNoProcesados` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ofi_statuslecturaarchivocargafaltantes` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `faltantes` | MODIF | faltantes | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_activaenviosnocobrados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_activaenviosnocobrados.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=60 |
| **Deps concatenadas** | 163 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación envíos y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dinya_activaenviosnocobrados` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_activaenviosnocobrados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoControl                   CHAR(12)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoControl` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |
| `sp_dinya_activaenviosnocobrados` | `bdisac` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP: bdisac:sp_dinya_activaenviosnocobrados"` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `activa` | ACCION | activa | 🔵 CONVENCIÓN | nombre_sp |
| `envios` | ENTIDAD | envíos | 🔵 CONVENCIÓN | nombre_sp |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cobra` | ACCION | cobra / aplica cobro / genera cargo | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?no`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_actualizaindicadorespei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_actualizaindicadorespei.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación indicadores" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_actualizaindicadorespei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(5)
  pIdindc                      INTEGER
  pPorcentaje                  DECIMAL(7,4)
  pRango                       CHAR(5)
  pPonderacion                 INT
  pPorcentajeFin               DECIMAL(5,2)
  pDescIndicador               CHAR(200)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(5)` | — | — |
| `pIdindc` | `INTEGER` | — | — |
| `pPorcentaje` | `DECIMAL(7,4)` | — | — |
| `pRango` | `CHAR(5)` | — | — |
| `pPonderacion` | `INT` | — | — |
| `pPorcentajeFin` | `DECIMAL(5,2)` | — | — |
| `pDescIndicador` | `CHAR(200)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iIdGral` | `INTEGER` | L6 |
| `iIdIndc` | `INTEGER` | L7 |
| `iIdSubindi` | `INTEGER` | L8 |
| `cTipo` | `CHAR(5)` | L9 |
| `dPorcentaje` | `DECIMAL(7,4)` | L10 |
| `cRango` | `CHAR(5)` | L11 |
| `sPonderacion` | `SMALLINT` | L12 |
| `dPorcentajeFin` | `DECIMAL(5,2)` | L13 |
| `cActividad` | `CHAR(50)` | L14 |
| `cTipoDescripcion` | `CHAR(50)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdibi` | `bdicnweb` | no | INSERT | L73 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pTipo='' OR  pIdindc IS NULL OR pPorcentaje IS NULL OR pRang` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | VALIDACIÓN_NULL | `IF (dPorcentaje IS NULL OR dPorcentaje <> pPorcentaje)THEN` |  |
| L92 | VALIDACIÓN_NULL | `IF (dPorcentajeFin IS NULL OR dPorcentajeFin <> pPorcentajeFin)THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `indicadores` | ENTIDAD | indicadores | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_actualizaparametrosgral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_actualizaparametrosgral.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación parámetros (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_actualizaparametrosgral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCveParametro                CHAR(8)
  cValor                       CHAR(10)
  cDescValor                   CHAR(50)
  pActaulizaValor              CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCveParametro` | `CHAR(8)` | — | — |
| `cValor` | `CHAR(10)` | — | — |
| `cDescValor` | `CHAR(50)` | — | — |
| `pActaulizaValor` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cDescValorAnt` | `CHAR(50)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoractualizaciondatos` | `bdicnweb` | no | UPDATE | L20 |
| `sl_parametros` | `bdilide` | ⚠️ sí | SELECT | L45 |
| `sw_bitacoractualizaciondatos` | `bdicnweb` | no | INSERT | L48 |
| `sl_parametros` | `bdilide` | ⚠️ sí | UPDATE | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_actualizaprefijosparticipantesspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_actualizaprefijosparticipantesspei.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 239 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación OS — Originación de Solicitudes y ss — subsistema (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: UPDATE, INSERT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=6 / 12 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_actualizaprefijosparticipantesspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoEjecucion               INTEGER
  pTipoOperacion               CHAR(1)
  pClaveBnco                   INTEGER
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoEjecucion` | `INTEGER` | — | — |
| `pTipoOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `pClaveBnco` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tblbcobloqueo` | `bdispei` | ⚠️ sí | INSERT | L37 |
| `tblbcobloqueo` | `bdispei` | ⚠️ sí | UPDATE | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoEjecucion IS NULL OR pTipoOperacion = '' OR pClaveBnco I` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ref` | AMBIGUO | referencia | 🟡 INFERIDO | nombre_sp |
| `?ij` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?particip` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `?ij`, `?particip`, `?e`, `ss`, `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_actualizarfechacheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_actualizarfechacheques.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=35 / fan_out=15 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación fecha y cheques" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cce_actualizarfechacheques` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_actualizarfechacheques(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaPresentacion           CHAR(10)
  pCtaDelCheque                CHAR(20)
  pNumCheque                   CHAR(7)
  pMonto                       DECIMAL(14,2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaPresentacion` | `CHAR(10)` | `fecha`=fecha | ✅ CÓDIGO |
| `pCtaDelCheque` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | — | — |
| `pMonto` | `DECIMAL(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cDescRet` | `CHAR(80)` | L9 |
| `dtFechaHoy` | `DATE` | L10 |
| `iNumDia` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_cce_actualizarfechacheques` | `bdicheq` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR cEmpresa = '' OR pFechaPresentacion = '' OR pCtaDelCheque = '` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00724';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00723';` |  |
| L68 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_cce_actualizarfechacheques';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00721';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_actualizastatusarch`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_actualizastatusarch.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=62 |
| **Deps concatenadas** | 165 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación estatus y archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sac_actualizastatusarch` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_actualizastatusarch(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaArch                   DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaArch` | `DATE` | `arch`=archivo | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cMensaje` | `CHAR(90)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |
| `sp_sac_actualizastatusarch` | `bdisac` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdisac:sp_sac_actualizastatusarch';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |

---

## `sp_ope_altabajabines_genrep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_altabajabines_genrep.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (de baja) · INE — Instituto Nacional Electoral" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_altabajabines_genrep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pRutaDescarga                CHAR(150)
  pNombreReporte               CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |
| `pNombreReporte` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cIsamErr` | `INTEGER` | L7 |
| `cDescErr` | `CHAR(50)` | L8 |
| `cCmd1` | `CHAR(3000)` | L9 |
| `cReporteGenerar` | `CHAR(150)` | L10 |
| `cBinesElimina` | `CHAR(150)` | L11 |
| `cArchDescarga` | `CHAR(150)` | L12 |
| `cRuta` | `CHAR(80)` | L13 |
| `cSql` | `CHAR(3000)` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `ven_transacc` | `SMALLINT` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L75 |
| `sw_ope_binesemisor` | `bdicnweb` | no | SELECT | L77 |
| `sc_bines` | `bdicheq` | ⚠️ sí | SELECT | L113 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `baja` | MODIF | de baja | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ine` | REG | INE — Instituto Nacional Electoral (validación de identidad  | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?b`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_altabajactaspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_altabajactaspei.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=68 |
| **Deps concatenadas** | 176 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cuentas (de baja)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_altactaspei`, `sp_bajactaspei` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_altabajactaspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoCuentaClabe               CHAR(18)
  pOperacion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoCuentaClabe` | `CHAR(18)` | — | — |
| `pOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |
| `sp_altactaspei` | `bdispei` | ⚠️ sí | L38 |
| `sp_bajactaspei` | `bdispei` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet     = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdispei:'informix'.sp_altactaspei";` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdispei:'informix'.sp_bajactaspei";` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `baja` | MODIF | de baja | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_bajacuentaspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_bajacuentaspei.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cuentas (de baja)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_bajacuentaspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pId                          INTEGER
) RETURNING CHAR(5)  AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pId` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCuenta` | `CHAR(20)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdibi` | `bdicnweb` | no | DELETE | L39 |
| `bdibi` | `bdicnweb` | no | INSERT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L27 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pId IS NULL THEN` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `baja` | MODIF | de baja | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_buscacuentaspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_buscacuentaspei.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_buscacuentaspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cClabe` | `CHAR(18)` | L8 |
| `cTarjeta` | `CHAR(20)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00121';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `busca` | ACCION | busca / localiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_capturatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_capturatms.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=35 |
| **Deps concatenadas** | 47 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_alta_atm` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_capturatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pIdAtm                       CHAR(8)
  pCajaGen                     CHAR(3)
  pNombreAtms                  CHAR(40)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pIdAtm` | `CHAR(8)` | — | — |
| `pCajaGen` | `CHAR(3)` | — | — |
| `pNombreAtms` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cSucursal` | `CHAR(4)` | L9 |
| `cMensaje` | `CHAR(50)` | L10 |
| `cBitacora` | `CHAR(6)` | L11 |
| `cMsjBitacora` | `CHAR(50)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |
| `sp_soc_alta_atm` | `bdisuc` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00836';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_alta_atm";` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00454';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `captura` | ACCION | captura | 🔵 CONVENCIÓN | nombre_sp |
| `?tms` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tms` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_cargarchivosolicitudtdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_cargarchivosolicitudtdc.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 116 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación CHI — formato/protocolo de consulta al Buró de Crédito, OS — Originación de Solicitudes y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_cargarchivosolicitudtdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistrosMas                CHAR(500)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistrosMas` | `CHAR(500)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cMeta_Diaria` | `DECIMAL(3,2)` | L7 |
| `cSucursal` | `CHAR(4)` | L8 |
| `cNombreSucursal` | `CHAR(40)` | L9 |
| `cRegistro` | `LVARCHAR` | L10 |
| `iTamReg` | `INTEGER` | L11 |
| `iPosCaracter` | `INTEGER` | L12 |
| `iInicioReg` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_cargarchivosolicitudtdc` | `bdicnweb` | no | SELECT | L50 |
| `sw_ope_cargarchivosolicitudtdc` | `bdicnweb` | no | DELETE | L50 |
| `mi_sucursalesinfo` | `bdmis` | ⚠️ sí | SELECT | L65 |
| `sw_ope_cargarchivosolicitudtdc` | `bdicnweb` | no | INSERT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_split_cadena` | `bdicnweb` | no | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | FÓRMULA | `LET cSucursal = SUBSTR(TRIM(cRegistro), iInicioReg, (iPosCaracter - 1));` |  |
| L61 | FÓRMULA | `LET cMeta_Diaria = SUBSTR(TRIM(cRegistro), (iPosCaracter + 1), (iTamReg - iPosCaracter));` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?olicitud` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?v`, `?olicitud` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_cargasucursalesvalidacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_cargasucursalesvalidacion.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 136 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal, sv — supervisión/servicio y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_cargasucursalesvalidacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistrosMas                CHAR(500)
  pBandera                     CHAR(1)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pRegistrosMas` | `CHAR(500)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cSucursal` | `CHAR(4)` | L7 |
| `cFuncionalidad` | `CHAR(20)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_validacionsucursalmeta` | `bdicnweb` | no | SELECT | L46 |
| `sw_ope_validacionsucursalmeta` | `bdicnweb` | no | DELETE | L46 |
| `mi_sucursalesinfo` | `bdmis` | ⚠️ sí | SELECT | L54 |
| `sw_ope_validacionsucursalmeta` | `bdicnweb` | no | INSERT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |
| `sp_split_cadena` | `bdicnweb` | no | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sv` | ENTIDAD | sv — supervisión/servicio (abreviación — bdiaclaracion) | 🔴 SINTÉTICO | nombre_sp |
| `?al` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?acion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `sv`, `?al`, `?acion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_catalogoclasificacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_catalogoclasificacion.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 101 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación catálogo y SIF — canal de estado de cuenta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=3 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_catalogoclasificacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cClasificacion` | `CHAR(1)` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_clase_tc` | `bdinteg` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?cla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sif` | ENTIDAD | SIF — canal de estado de cuenta (aclaraciones_edocta_sif, de | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cla`, `?i`, `?ion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_catalogomotivos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_catalogomotivos.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=15 / fan_out=8 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación catálogo y motivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultadevcam` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_catalogomotivos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cCodigo` | `CHAR(2)` | L11 |
| `cDescripcion` | `CHAR(35)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_consultadevcam` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultadevcam';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `motivo` | ENTIDAD | motivo / causa | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_catgerenciascomerciales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_catgerenciascomerciales.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 214 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación catálogo y Comisión bancaria — cobro de comisión sobre cuenta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_catgerenciascomerciales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iRegistros` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cPlaza` | `CHAR(3)` | L14 |
| `cNombre` | `CHAR(40)` | L15 |
| `cRegional` | `CHAR(3)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L67 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L72 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?gerencias` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?erciales` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gerencias`, `?erciales` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_catsucaralreporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_catsucaralreporte.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=22 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación catálogo y reporte (sucursal)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_catsucaralreporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cSucursal` | `CHAR(40)` | L11 |
| `cNombre` | `CHAR(40)` | L12 |
| `cPlaza` | `CHAR(3)` | L13 |
| `cPais` | `CHAR(3)` | L14 |
| `cEstado` | `CHAR(2)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L41 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L63 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `?aral` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aral` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_chequeduplicado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_chequeduplicado.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=112 / fan_out=15 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheque" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_chequeduplicado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCheque                    INTEGER
  pFecha                       DATE
  pCodigo                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCheque` | `INTEGER` | `cheque`=cheque | ✅ CÓDIGO |
| `pFecha` | `DATE` | — | — |
| `pCodigo` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `bIsChequeDuplicado` | `BOOLEAN` | L7 |
| `iChequeDuplicado` | `SMALLINT` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cCveBanco` | `CHAR(3)` | L10 |
| `cCuentaReferencia` | `CHAR(20)` | L11 |
| `iNumCheque` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdCheque IS NULL OR pFecha IS NULL OR pCodigo = '' THEN` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?duplicado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?duplicado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_chequesrevisados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_chequesrevisados.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=25 / fan_out=23 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheques y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cce_chequesrevisados` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_chequesrevisados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pTipoSubOperacion            CHAR(1)
  pClaveBanco                  CHAR(3)
  pNumeroCuenta                CHAR(20)
  pNumeroCheque                CHAR(7)
  pFechaPresenta               DATE
  pNumeroCliente               CHAR(20)
  pCuentaDeposito              CHAR(20)
  pMonto                       DECIMAL(16,2)
  pSucursal                    CHAR(4)
  pMotivo                      CHAR(2)
  pEjecutivoReviso             CHAR(8)
  pFechaRevision               DATE
  pTiempoIniRev                DATETIME HOUR TO SECOND
  pTiempoFinRev                DATETIME HOUR TO SECOND
  pDevuelto                    CHAR(1)
  pRevisado                    CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `pTipoSubOperacion` | `CHAR(1)` | `ope`=operación · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pClaveBanco` | `CHAR(3)` | — | — |
| `pNumeroCuenta` | `CHAR(20)` | — | — |
| `pNumeroCheque` | `CHAR(7)` | — | — |
| `pFechaPresenta` | `DATE` | — | — |
| `pNumeroCliente` | `CHAR(20)` | — | — |
| `pCuentaDeposito` | `CHAR(20)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pMonto` | `DECIMAL(16,2)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pMotivo` | `CHAR(2)` | — | — |
| `pEjecutivoReviso` | `CHAR(8)` | `rev`=reversión (abreviación de reversa/reverso) | 🟡 INFERIDO |
| `pFechaRevision` | `DATE` | `rev`=reversión (abreviación de reversa/reverso) | 🟡 INFERIDO |
| `pTiempoIniRev` | `DATETIME HOUR TO SECOND` | `rev`=reversión (abreviación de reversa/reverso) | 🟡 INFERIDO |
| `pTiempoFinRev` | `DATETIME HOUR TO SECOND` | `rev`=reversión (abreviación de reversa/reverso) | 🟡 INFERIDO |
| `pDevuelto` | `CHAR(1)` | — | — |
| `pRevisado` | `CHAR(1)` | `rev`=reversión (abreviación de reversa/reverso) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_cce_chequesrevisados` | `bditef` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cce_chequesrevisados';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';		ELIF iCodRetSp = 1 THEN` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00724';		END IF;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cheques` | ENTIDAD | cheques | 🔵 CONVENCIÓN | nombre_sp |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?isad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?isad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_conciliaarchivoptc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_conciliaarchivoptc.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=57 |
| **Deps concatenadas** | 153 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación archivo y tc — Tarjeta de Crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sac_conciliaarchivoptc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_conciliaarchivoptc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(8)` | L6 |
| `cDescCodRetSp` | `CHAR(120)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cIdReporte` | `CHAR(2)` | L9 |
| `cDescReporte` | `CHAR(45)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_sac_conciliaarchivoptc` | `bdisac` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_sac_conciliaarchivopt` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `concilia` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_conciliadeta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_conciliadeta.sql` |
| **LOC (1er CREATE)** | 254 |
| **Callgraph** | ✅ fan_in=0 / fan_out=58 |
| **Deps concatenadas** | 154 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación detalle" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sac_conciliadeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_conciliadeta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pIdEjecucion                 CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `cCodRetSp` | `CHAR(8)` | L29 |
| `cDescCodRetSp` | `CHAR(90)` | L30 |
| `iCodRetSp` | `INTEGER` | L31 |
| `cIdReporte` | `CHAR(2)` | L32 |
| `cDescReporte` | `CHAR(45)` | L33 |
| `iRecuperacion` | `INTEGER` | L34 |
| `cTipo` | `CHAR(1)` | L36 |
| `cCodTxn` | `CHAR(2)` | L37 |
| `cCodRegistro` | `CHAR(20)` | L38 |
| `cNumTarjeta` | `CHAR(16)` | L39 |
| `cNumReferencia` | `CHAR(23)` | L40 |
| `cFechaTxn` | `CHAR(4)` | L41 |
| `iImporteTxn` | `INTEGER` | L42 |
| `cCodActualDes` | `CHAR(3)` | L43 |
| `cNombreNegocio` | `CHAR(25)` | L44 |
| `cCodRazon` | `CHAR(30)` | L45 |
| `cIndDeposito` | `CHAR(1)` | L46 |
| `cSucursal` | `CHAR(4)` | L47 |
| `cCuentaCargo` | `CHAR(20)` | L48 |
| `cTransaccSuc` | `CHAR(4)` | L49 |
| `mImporte` | `MONEY(12,2)` | L50 |
| `cNumRefemov` | `CHAR(40)` | L51 |
| `iTotalMovCon` | `INTEGER` | L53 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tdc_verificaproceso` | `bdicnweb` | no | UPDATE | L100 |
| `sw_tdc_verificaproceso` | `bdicnweb` | no | SELECT | L111 |
| `sw_tdc_verificaproceso` | `bdicnweb` | no | DELETE | L111 |
| `sw_tdc_verificaproceso` | `bdicnweb` | no | INSERT | L114 |
| `sac_reporte` | `bdisac` | ⚠️ sí | SELECT | L200 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L138 |
| `sp_sac_conciliadeta` | `bdisac` | ⚠️ sí | L153 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L116 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L128 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L157 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_sac_conciliadeta';` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L212 | FÓRMULA | `LET iTotalGeneral = NVL(iTotalMovCon,0) + NVL(iTotalMovNoCon,0) + NVL(iTotalMovNoConTab,0);` |  |
| L225 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L232 | CÓDIGO_RETORNO | `LET cCodRet = '00939';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `concilia` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_conciliatotbancos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_conciliatotbancos.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 155 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación banco" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sac_conciliatotbancos` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_conciliatotbancos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(8)` | L17 |
| `cDescCodRetSp` | `CHAR(90)` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `cIdReporte` | `CHAR(2)` | L20 |
| `cDescReporte` | `CHAR(45)` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |
| `cBanamex` | `CHAR(15)` | L24 |
| `cBancomer` | `CHAR(15)` | L25 |
| `cProsa` | `CHAR(15)` | L26 |
| `iTotalBanamex` | `INTEGER` | L27 |
| `iTotalBancomer` | `INTEGER` | L28 |
| `iTotalProsa` | `INTEGER` | L29 |
| `iTotalGeneral` | `INTEGER` | L30 |
| `mImporteBanamex` | `MONEY(14,2)` | L31 |
| `mImporteBancomer` | `MONEY(14,2)` | L32 |
| `mImporteProsa` | `MONEY(14,2)` | L33 |
| `mImporteGeneral` | `MONEY(16,2)` | L34 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L73 |
| `sp_sac_conciliatotbancos` | `bdisac` | ⚠️ sí | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL THEN` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_sac_conciliatotbancos` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | FÓRMULA | `LET iTotalGeneral = NVL(iTotalBanamex,0) + NVL(iTotalBancomer,0) + NVL(iTotalProsa,0);` |  |
| L95 | FÓRMULA | `LET mImporteGeneral = NVL((mImporteBanamex/100),0) + NVL((mImporteBancomer/100),0) + NVL((mImportePr` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `concilia` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `banco` | ENTIDAD | banco | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_cons_ticketabonoapp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_cons_ticketabonoapp.sql` |
| **LOC (1er CREATE)** | 231 |
| **Callgraph** | ✅ fan_in=82 / fan_out=7 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación abono (canal app)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_cons_ticketabonoapp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReferencia                  CHAR(40)
  pFolioSuc                    CHAR(16)
  pHuella                      CHAR(1)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |
| `pHuella` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cNumConvenio` | `CHAR(5)` | L30 |
| `cNomConvenio` | `CHAR(40)` | L31 |
| `dFechaPago` | `DATE` | L32 |
| `cReferencia` | `CHAR(40)` | L33 |
| `cFormaPago` | `CHAR(1)` | L34 |
| `mImportePago` | `MONEY` | L35 |
| `cSucursal` | `CHAR(4)` | L36 |
| `cFechaInsert` | `CHAR(10)` | L37 |
| `cUsuario` | `CHAR(8)` | L38 |
| `cFolioSuc` | `CHAR(16)` | L39 |
| `cNombre1Ben` | `CHAR(40)` | L40 |
| `cNombre2Ben` | `CHAR(40)` | L41 |
| `cApPaternoBen` | `CHAR(40)` | L42 |
| `cApMaternoBen` | `CHAR(40)` | L43 |
| `cNumCteBen` | `CHAR(20)` | L44 |
| `cNumcliente` | `CHAR(20)` | L45 |
| `cCadenaTran` | `CHAR(942)` | L46 |
| `cNomSucursal` | `CHAR(40)` | L47 |
| `cPlaza` | `CHAR(3)` | L48 |
| `cNomPlaza` | `CHAR(40)` | L49 |
| `cNumcuenta` | `CHAR(20)` | L50 |
| `cNumTarjeta` | `CHAR(16)` | L51 |
| `cDirCompleta` | `VARCHAR(250)` | L52 |
| *…23 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L161 |
| `sac_pld_remesas` | `bdisac` | ⚠️ sí | SELECT | L178 |
| `si_cte_huella` | `bdinteg` | ⚠️ sí | SELECT | L183 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L199 |
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L204 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L153 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_ticket` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ticket` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_cons_ticketabonoapp_web`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_cons_ticketabonoapp_web.sql` |
| **LOC (1er CREATE)** | 280 |
| **Callgraph** | ✅ fan_in=32 / fan_out=3 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación abono (canal app, canal web)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_cons_ticketabonoapp_web(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReferencia                  CHAR(40)
  pFolioSuc                    CHAR(16)
  pHuella                      CHAR(1)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |
| `pHuella` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L31 |
| `iSqlErr` | `INTEGER` | L32 |
| `cNumConvenio` | `CHAR(5)` | L33 |
| `cNomConvenio` | `CHAR(40)` | L34 |
| `dFechaPago` | `DATE` | L35 |
| `cReferencia` | `CHAR(40)` | L36 |
| `cFormaPago` | `CHAR(1)` | L37 |
| `mImportePago` | `MONEY` | L38 |
| `cSucursal` | `CHAR(4)` | L39 |
| `cFechaInsert` | `CHAR(10)` | L40 |
| `cUsuario` | `CHAR(8)` | L41 |
| `cFolioSuc` | `CHAR(16)` | L42 |
| `cOrigen` | `CHAR(15)` | L43 |
| `cNombre1Ben` | `CHAR(40)` | L44 |
| `cNombre2Ben` | `CHAR(40)` | L45 |
| `cApPaternoBen` | `CHAR(40)` | L46 |
| `cApMaternoBen` | `CHAR(40)` | L47 |
| `cNumCteBen` | `CHAR(20)` | L48 |
| `cNumcliente` | `CHAR(20)` | L49 |
| `cTelefono` | `CHAR(10)` | L50 |
| `cCadenaTran` | `CHAR(942)` | L51 |
| `cNomSucursal` | `CHAR(40)` | L52 |
| `cPlaza` | `CHAR(3)` | L53 |
| `cNomPlaza` | `CHAR(40)` | L54 |
| `cNumcuenta` | `CHAR(20)` | L55 |
| *…26 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L166 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L185 |
| `sac_pld_remesas` | `bdisac` | ⚠️ sí | SELECT | L207 |
| `sac_pld_remesas_old` | `bdisac` | ⚠️ sí | SELECT | L215 |
| `si_cte_huella` | `bdinteg` | ⚠️ sí | SELECT | L232 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L248 |
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L253 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L160 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L202 | CÓDIGO_RETORNO | `LET cCodRet= '00029';` |  |
| L223 | CÓDIGO_RETORNO | `LET cCodRet= '00034';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_ticket` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `web` | MODIF | canal web | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ticket` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_cons_ticketabonovent`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_cons_ticketabonovent.sql` |
| **LOC (1er CREATE)** | 232 |
| **Callgraph** | ✅ fan_in=82 / fan_out=7 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación abono" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_cons_ticketabonovent(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReferencia                  CHAR(40)
  pHuella                      CHAR(1)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pHuella` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cNumConvenio` | `CHAR(3)` | L30 |
| `cNomConvenio` | `CHAR(40)` | L31 |
| `dFechaPago` | `DATE` | L32 |
| `cReferencia` | `CHAR(40)` | L33 |
| `cFormaPago` | `CHAR(1)` | L34 |
| `mImportePago` | `MONEY` | L35 |
| `cSucursal` | `CHAR(4)` | L36 |
| `cFechaInsert` | `CHAR(10)` | L37 |
| `cUsuario` | `CHAR(8)` | L38 |
| `cFolioSuc` | `CHAR(16)` | L39 |
| `cNombre1Ben` | `CHAR(40)` | L40 |
| `cNombre2Ben` | `CHAR(40)` | L41 |
| `cApPaternoBen` | `CHAR(40)` | L42 |
| `cApMaternoBen` | `CHAR(40)` | L43 |
| `cNumCteBen` | `CHAR(20)` | L44 |
| `cNumcliente` | `CHAR(20)` | L45 |
| `cCadenaTran` | `CHAR(942)` | L46 |
| `cNomSucursal` | `CHAR(40)` | L47 |
| `cPlaza` | `CHAR(3)` | L48 |
| `cNomPlaza` | `CHAR(40)` | L49 |
| `cNumcuenta` | `CHAR(20)` | L50 |
| `cNumTarjeta` | `CHAR(16)` | L51 |
| `cDirCompleta` | `VARCHAR(250)` | L52 |
| *…23 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L160 |
| `sac_pld_remesas` | `bdisac` | ⚠️ sí | SELECT | L177 |
| `si_cte_huella` | `bdinteg` | ⚠️ sí | SELECT | L182 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L198 |
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L203 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L152 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_ticket` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?vent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ticket`, `?vent` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_cons_ticketabonovent_web`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_cons_ticketabonovent_web.sql` |
| **LOC (1er CREATE)** | 283 |
| **Callgraph** | ✅ fan_in=32 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación abono (canal web)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_cons_ticketabonovent_web(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReferencia                  CHAR(40)
  pHuella                      CHAR(1)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pHuella` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L32 |
| `iSqlErr` | `INTEGER` | L33 |
| `cNumConvenio` | `CHAR(3)` | L34 |
| `cNomConvenio` | `CHAR(40)` | L35 |
| `dFechaPago` | `DATE` | L36 |
| `cReferencia` | `CHAR(40)` | L37 |
| `cFormaPago` | `CHAR(1)` | L38 |
| `mImportePago` | `MONEY` | L39 |
| `cSucursal` | `CHAR(4)` | L40 |
| `cFechaInsert` | `CHAR(10)` | L41 |
| `cUsuario` | `CHAR(8)` | L42 |
| `cFolioSuc` | `CHAR(16)` | L43 |
| `cOrigen` | `CHAR(15)` | L44 |
| `cNombre1Ben` | `CHAR(40)` | L45 |
| `cNombre2Ben` | `CHAR(40)` | L46 |
| `cApPaternoBen` | `CHAR(40)` | L47 |
| `cApMaternoBen` | `CHAR(40)` | L48 |
| `cNumCteBen` | `CHAR(20)` | L49 |
| `cNumcliente` | `CHAR(20)` | L50 |
| `cTelefono` | `CHAR(10)` | L51 |
| `cCadenaTran` | `CHAR(942)` | L52 |
| `cNomSucursal` | `CHAR(40)` | L53 |
| `cPlaza` | `CHAR(3)` | L54 |
| `cNomPlaza` | `CHAR(40)` | L55 |
| `cNumcuenta` | `CHAR(20)` | L56 |
| *…26 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L167 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L188 |
| `sac_pld_remesas` | `bdisac` | ⚠️ sí | SELECT | L209 |
| `sac_pld_remesas_old` | `bdisac` | ⚠️ sí | SELECT | L217 |
| `si_cte_huella` | `bdinteg` | ⚠️ sí | SELECT | L234 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L250 |
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L255 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L161 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L203 | CÓDIGO_RETORNO | `LET cCodRet= '00021';` |  |
| L225 | CÓDIGO_RETORNO | `LET cCodRet= '00033';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_ticket` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?vent_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `web` | MODIF | canal web | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ticket`, `?vent_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_cons_ticketefectivovent`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_cons_ticketefectivovent.sql` |
| **LOC (1er CREATE)** | 385 |
| **Callgraph** | ✅ fan_in=82 / fan_out=7 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_cons_ticketefectivovent(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReferencia                  CHAR(40)
  pHuella                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pHuella` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cNumConvenio` | `CHAR(5)` | L30 |
| `cNomConvenio` | `CHAR(40)` | L31 |
| `dFechaPago` | `DATE` | L32 |
| `cReferencia` | `CHAR(40)` | L33 |
| `cFormaPago` | `CHAR(1)` | L34 |
| `mImportePago` | `MONEY` | L35 |
| `cSucursal` | `CHAR(4)` | L36 |
| `cFechaInsert` | `CHAR(10)` | L37 |
| `cUsuario` | `CHAR(8)` | L38 |
| `cFolioSuc` | `CHAR(16)` | L39 |
| `cNombre1Ben` | `CHAR(40)` | L40 |
| `cNombre2Ben` | `CHAR(40)` | L41 |
| `cApPaternoBen` | `CHAR(40)` | L42 |
| `cApMaternoBen` | `CHAR(40)` | L43 |
| `cNumCteBen` | `CHAR(20)` | L44 |
| `cNumcliente` | `CHAR(20)` | L45 |
| `cCadenaTran` | `CHAR(942)` | L46 |
| `cNomSucursal` | `CHAR(40)` | L47 |
| `cPlaza` | `CHAR(3)` | L48 |
| `cNomPlaza` | `CHAR(40)` | L49 |
| `cDirCompleta` | `VARCHAR(250)` | L50 |
| `cSecuenciaMax` | `CHAR(3)` | L51 |
| `cRetorno3` | `CHAR(150)` | L52 |
| *…23 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L162 |
| `sac_pld_remesas` | `bdisac` | ⚠️ sí | SELECT | L331 |
| `si_cte_huella` | `bdinteg` | ⚠️ sí | SELECT | L336 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L352 |
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L357 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L152 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |
| L199 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L251 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L301 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_ticke` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `?ectivovent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ticke`, `?ectivovent` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_cons_ticketefectivovent_web`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_cons_ticketefectivovent_web.sql` |
| **LOC (1er CREATE)** | 546 |
| **Callgraph** | ✅ fan_in=32 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación TEF — transferencia electrónica de fondos (canal web)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_cons_ticketefectivovent_web(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReferencia                  CHAR(40)
  pHuella                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReferencia` | `CHAR(40)` | — | — |
| `pHuella` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L31 |
| `iSqlErr` | `INTEGER` | L32 |
| `cNumConvenio` | `CHAR(5)` | L33 |
| `cNomConvenio` | `CHAR(40)` | L34 |
| `dFechaPago` | `DATE` | L35 |
| `cReferencia` | `CHAR(40)` | L36 |
| `cFormaPago` | `CHAR(1)` | L37 |
| `mImportePago` | `MONEY` | L38 |
| `cSucursal` | `CHAR(4)` | L39 |
| `cFechaInsert` | `CHAR(10)` | L40 |
| `cUsuario` | `CHAR(8)` | L41 |
| `cFolioSuc` | `CHAR(16)` | L42 |
| `cOrigen` | `CHAR(15)` | L43 |
| `cNombre1Ben` | `CHAR(40)` | L44 |
| `cNombre2Ben` | `CHAR(40)` | L45 |
| `cApPaternoBen` | `CHAR(40)` | L46 |
| `cApMaternoBen` | `CHAR(40)` | L47 |
| `cNumCteBen` | `CHAR(20)` | L48 |
| `cNumcliente` | `CHAR(20)` | L49 |
| `cTelefono` | `CHAR(10)` | L50 |
| `cCadenaTran` | `CHAR(942)` | L51 |
| `cNomSucursal` | `CHAR(40)` | L52 |
| `cPlaza` | `CHAR(3)` | L53 |
| `cNomPlaza` | `CHAR(40)` | L54 |
| `cDirCompleta` | `VARCHAR(250)` | L55 |
| *…26 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L168 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L181 |
| `sac_wu_pay` | `bdisac` | ⚠️ sí | SELECT | L201 |
| `sac_wu_pay_old` | `bdisac` | ⚠️ sí | SELECT | L246 |
| `sac_bts_payi` | `bdisac` | ⚠️ sí | SELECT | L294 |
| `sac_bts_payi_old` | `bdisac` | ⚠️ sí | SELECT | L339 |
| `sac_app_payi` | `bdisac` | ⚠️ sí | SELECT | L387 |
| `sac_app_payi_old` | `bdisac` | ⚠️ sí | SELECT | L434 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L483 |
| `si_telefonos_actual` | `bdinteg` | ⚠️ sí | SELECT | L492 |
| `si_cte_huella` | `bdinteg` | ⚠️ sí | SELECT | L499 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L515 |
| `si_plazas` | `bdinteg` | ⚠️ sí | SELECT | L520 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L161 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L154 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L193 | CÓDIGO_RETORNO | `LET cCodRet= '00022';` |  |
| L218 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L235 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L238 | CÓDIGO_RETORNO | `LET cCodRet= '00023';` |  |
| L262 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L279 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L282 | CÓDIGO_RETORNO | `LET cCodRet= '00024';` |  |
| L311 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L328 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L331 | CÓDIGO_RETORNO | `LET cCodRet= '00025';` |  |
| L355 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L372 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L375 | CÓDIGO_RETORNO | `LET cCodRet= '00026';` |  |
| L406 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L423 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L426 | CÓDIGO_RETORNO | `LET cCodRet= '00027';` |  |
| L450 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L467 | VALIDACIÓN_NULL | `IF 	TRIM(cNombre1Ben) 	IS NULL OR TRIM(cNombre2Ben) IS NULL OR TRIM(cApPaternoBen) IS NULL OR` |  |
| L470 | CÓDIGO_RETORNO | `LET cCodRet= '00028';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_ticke` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | 🔵 CONVENCIÓN | nombre_sp |
| `?ectivovent_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `web` | MODIF | canal web | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ticke`, `?ectivovent_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consahorros`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consahorros.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 48 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consahorros(
  pUsuario                     char(8)
  pIdFuncion                   CHAR(10)
  pcuenta_eje                  CHAR(20)
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pcuenta_eje` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `scod_ret` | `CHAR(5)` | L13 |
| `vsqlerr` | `INTEGER` | L14 |
| `vcuenta_eje` | `CHAR(20)` | L15 |
| `vproducto` | `SMALLINT` | L16 |
| `vestatus` | `SMALLINT` | L17 |
| `cNombre_ahorro` | `CHAR(18)` | L19 |
| `cSaldo_ahorro` | `MONEY (14,2)` | L20 |
| `cEstatus_ahorro` | `CHAR(12)` | L21 |
| `cFecha_creacion` | `DATE` | L22 |
| `cCuenta_sd` | `CHAR(20)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_mae_sd` | `bdicheq` | ⚠️ sí | SELECT | L80 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L89 | VALIDACIÓN_NULL | `IF cNombre_ahorro="" OR cNombre_ahorro is null THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ahor` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ahor`, `ro`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consbloquearchivopresentado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consbloquearchivopresentado.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=78 / fan_out=27 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación CHI — formato/protocolo de consulta al Buró de Crédito (presentado)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consbloquearchivopresentado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `dFecha` | `DATE` | L8 |
| `cFechaHoy` | `CHAR(8)` | L9 |
| `iNoBloque` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_encabezado` | `bditef` | ⚠️ sí | SELECT | L61 |
| `cce_gransumario` | `bditef` | ⚠️ sí | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | FÓRMULA | `LET iNoBloque = NVL(iNoBloque,0) + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?vo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `presentado` | MODIF | presentado (a cobro) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?vo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_conscambiostatustarjetas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_conscambiostatustarjetas.sql` |
| **LOC (1er CREATE)** | 175 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cambio, estatus y tarjetas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_conscambiostatustarjetas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaBusqueda               DATE
  pNoTarjeta                   CHAR(16)
  pEstatus                     CHAR(20)
  pTramaTarjeta                CHAR(250)
  pOperacion                   CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaBusqueda` | `DATE` | — | — |
| `pNoTarjeta` | `CHAR(16)` | — | — |
| `pEstatus` | `CHAR(20)` | `status`=estatus | 🔵 CONVENCIÓN |
| `pTramaTarjeta` | `CHAR(250)` | — | — |
| `pOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iTipo` | `INTEGER` | L13 |
| `cDescripcion` | `CHAR(60)` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `iTotalDisponibles` | `INTEGER` | L16 |
| `cNumeroInicial` | `CHAR(19)` | L17 |
| `cNumeroFinal` | `CHAR(19)` | L18 |
| `cStatus` | `CHAR(20)` | L19 |
| `cPeriodoExp` | `CHAR(5)` | L20 |
| `dFechaSolicitud` | `DATE` | L21 |
| `cComentario` | `CHAR(60)` | L22 |
| `cBin` | `CHAR(10)` | L23 |
| `cUsuario` | `CHAR(8)` | L24 |
| `cSerialFinal` | `CHAR(19)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `13` | `bdicnweb` | no | SELECT | L96 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |
| `sp_split_cadena` | `bdicnweb` | no | L139 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L83 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L106 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L120 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `tarjetas` | ENTIDAD | tarjetas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_consdatahorros`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consdatahorros.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consdatahorros(
  pUsuario                     char(8)
  pIdFuncion                   CHAR(10)
  pcuenta_eje                  CHAR(20)
  pcuenta_sd                   CHAR(20)
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pcuenta_eje` | `CHAR(20)` | — | — |
| `pcuenta_sd` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `scod_ret` | `CHAR(5)` | L11 |
| `vsqlerr` | `INTEGER` | L12 |
| `vcuenta_eje` | `CHAR(11)` | L13 |
| `vproducto` | `SMALLINT` | L14 |
| `vestatus` | `SMALLINT` | L15 |
| `cCuenta_relacionada` | `CHAR(20)` | L17 |
| `cNombre_ahorro` | `CHAR(18)` | L18 |
| `cEstatus_ahorro` | `CHAR(12)` | L19 |
| `cMonto_objetivo` | `MONEY (14,2)` | L20 |
| `cFecha_creacion` | `DATE` | L21 |
| `cFecha_finalizacion` | `DATE` | L22 |
| `cPeriodicidad` | `CHAR(10)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_mae_sd` | `bdicheq` | ⚠️ sí | SELECT | L80 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L86 | VALIDACIÓN_NULL | `IF cCuenta_relacionada IS NULL OR cCuenta_relacionada = "" THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?datahor` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?datahor`, `ro`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consdescripcionerror`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consdescripcionerror.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=15 / fan_out=37 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación descripción y error" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consdescripcionerror(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodError                    CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodError` | `CHAR(3)` | `error`=error | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cDesError` | `CHAR(50)` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_codret` | `bdinteg` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `descripcion` | ENTIDAD | descripción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `error` | ENTIDAD | error | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_consestatustarjeta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consestatustarjeta.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación estatus y tarjeta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consestatustarjeta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iTipo` | `INTEGER` | L9 |
| `cDescripcion` | `CHAR(60)` | L10 |
| `cDesCorta` | `CHAR(4)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tarjeta` | ENTIDAD | tarjeta | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_consfechahabilanterior`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consfechahabilanterior.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=5 / fan_out=9 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación fecha y día hábil — día bancario operativo (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `cal_habil_ant` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consfechahabilanterior(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `DATE` | L10 |
| `dFechaHabilAnt` | `DATE` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `cal_habil_ant` | `bditef` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:cal_habil_ant';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `habil` | ENTIDAD | día hábil — día bancario operativo (spei_validafecha, sp_cam | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?erior` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?erior` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consmovahorro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consmovahorro.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimiento y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consmovahorro(
  pUsuario                     char(8)
  pIdFuncion                   CHAR(10)
  pcuenta_eje                  CHAR(20)
  pcuenta_sd                   CHAR(20)
  pfechadel                    DATE
  pfechaal                     DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pcuenta_eje` | `CHAR(20)` | — | — |
| `pcuenta_sd` | `CHAR(20)` | — | — |
| `pfechadel` | `DATE` | — | — |
| `pfechaal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `scod_ret` | `CHAR(5)` | L11 |
| `vsqlerr` | `INTEGER` | L12 |
| `vcuenta_eje` | `CHAR(11)` | L13 |
| `vproducto` | `SMALLINT` | L14 |
| `vestatus` | `SMALLINT` | L15 |
| `iTotalRegistros` | `INTEGER` | L16 |
| `cFecha` | `DATE` | L18 |
| `cFolio` | `CHAR(10)` | L19 |
| `cImporte` | `MONEY (14,2)` | L20 |
| `cReferencia` | `CHAR(19)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_mae_sd` | `bdicheq` | ⚠️ sí | SELECT | L78 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L86 | FÓRMULA | `LET iTotalRegistros = iTotalRegistros+1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ahor` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ahor`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consmovahorro_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consmovahorro_totales.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimiento y ro — Rol Operativo (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consmovahorro_totales(
  pUsuario                     char(8)
  pIdFuncion                   CHAR(10)
  pcuenta_eje                  CHAR(20)
  pcuenta_sd                   CHAR(20)
  pfechadel                    DATE
  pfechaal                     DATE
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pcuenta_eje` | `CHAR(20)` | — | — |
| `pcuenta_sd` | `CHAR(20)` | — | — |
| `pfechadel` | `DATE` | — | — |
| `pfechaal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `scod_ret` | `CHAR(5)` | L8 |
| `vsqlerr` | `INTEGER` | L9 |
| `cTotal` | `SMALLINT` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_mae_sd` | `bdicheq` | ⚠️ sí | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | FÓRMULA | `LET cTotal = -1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ahor` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ahor`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consmovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consmovimientos.sql` |
| **LOC (1er CREATE)** | 163 |
| **Callgraph** | ✅ fan_in=82 / fan_out=7 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimientos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consmovimientos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRemesadora                  CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pCveRemesa                   CHAR(20)
  pNumCliente                  CHAR(9)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRemesadora` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pCveRemesa` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cNumconvenio` | `CHAR(3)` | L18 |
| `cNomconvenio` | `CHAR(40)` | L19 |
| `cNum_cte` | `CHAR(20)` | L20 |
| `dFech_oper` | `DATE` | L21 |
| `cSucursal` | `CHAR(4)` | L22 |
| `cFolio_suc` | `CHAR(16)` | L23 |
| `iTotRegistros` | `INTEGER` | L24 |
| `iTotRegistros2` | `INTEGER` | L25 |
| `cReferencia1` | `CHAR(40)` | L26 |
| `cNomCliente` | `CHAR(100)` | L27 |
| `cNombre1` | `CHAR(26)` | L28 |
| `cNombre2` | `CHAR(26)` | L29 |
| `cAppPaterno` | `CHAR(26)` | L30 |
| `cAppMaterno` | `CHAR(26)` | L31 |
| `cRetorno3` | `CHAR(150)` | L32 |
| `cFormaPago` | `CHAR(1)` | L33 |
| `cUsuario` | `CHAR(8)` | L34 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L91 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRecuperacion IS NULL OR pRegistros IS NULL THEN` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L115 | FÓRMULA | `LET iTotRegistros = iTotRegistros + 1;` |  |
| L141 | FÓRMULA | `LET iTotRegistros = iTotRegistros + 1;` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_consmovimientos_web`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consmovimientos_web.sql` |
| **LOC (1er CREATE)** | 218 |
| **Callgraph** | ✅ fan_in=32 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimientos (canal web)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consmovimientos_web(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRemesadora                  CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pCveRemesa                   CHAR(20)
  pNumCliente                  CHAR(9)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRemesadora` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pCveRemesa` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cNumconvenio` | `CHAR(3)` | L18 |
| `cNomconvenio` | `CHAR(40)` | L19 |
| `cNum_cte` | `CHAR(20)` | L20 |
| `dFech_oper` | `DATE` | L21 |
| `cSucursal` | `CHAR(4)` | L22 |
| `cFolio_suc` | `CHAR(16)` | L23 |
| `iTotRegistros` | `INTEGER` | L24 |
| `iTotRegistros2` | `INTEGER` | L25 |
| `cReferencia1` | `CHAR(40)` | L26 |
| `cNomCliente` | `CHAR(100)` | L27 |
| `cNombre1` | `CHAR(26)` | L28 |
| `cNombre2` | `CHAR(26)` | L29 |
| `cAppPaterno` | `CHAR(26)` | L30 |
| `cAppMaterno` | `CHAR(26)` | L31 |
| `cRetorno3` | `CHAR(150)` | L32 |
| `cFormaPago` | `CHAR(1)` | L33 |
| `cUsuario` | `CHAR(8)` | L34 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L84 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L146 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L104 | VALIDACIÓN_NULL | `IF 	TRIM(cNum_cte) 	IS NULL OR TRIM(cFolio_suc) IS NULL OR TRIM(cReferencia1) IS NULL OR` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |
| L131 | VALIDACIÓN_NULL | `IF 	TRIM(cNum_cte) 	IS NULL OR TRIM(cFolio_suc) IS NULL OR TRIM(cReferencia1) IS NULL OR` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |
| L140 | CÓDIGO_RETORNO | `LET cCodRet= '00018';` |  |
| L166 | VALIDACIÓN_NULL | `IF 	TRIM(cNum_cte) 	IS NULL OR TRIM(cFolio_suc) IS NULL OR TRIM(cReferencia1) IS NULL OR` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |
| L193 | VALIDACIÓN_NULL | `IF 	TRIM(cNum_cte) 	IS NULL OR TRIM(cFolio_suc) IS NULL OR TRIM(cReferencia1) IS NULL OR` |  |
| L196 | CÓDIGO_RETORNO | `LET cCodRet= '00017';` |  |
| L202 | CÓDIGO_RETORNO | `LET cCodRet= '00018';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | 🔵 CONVENCIÓN | nombre_sp |
| `web` | MODIF | canal web | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_consparamsolicitartarjeta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consparamsolicitartarjeta.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación parámetro, solicitud, Tarjeta y tarjeta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consparamsolicitartarjeta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumTarjSolicitar            CHAR(4)
  pFechaSolicitud              DATE
  pPeriodoExpTarjeta           CHAR(3)
  pOperacion                   CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumTarjSolicitar` | `CHAR(4)` | `solic`=solicitud · `tar`=Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, mover_his_tar, obtener_cta_con_num_tar) | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pFechaSolicitud` | `DATE` | `solic`=solicitud | 🔵 CONVENCIÓN |
| `pPeriodoExpTarjeta` | `CHAR(3)` | `tar`=Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, mover_his_tar, obtener_cta_con_num_tar) · `tarjeta`=tarjeta | 🟡 INFERIDO / ✅ CÓDIGO |
| `pOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cBin` | `CHAR(10)` | L8 |
| `cSerialFinal` | `CHAR(100)` | L9 |
| `iLote` | `INTEGER` | L10 |
| `iSerialFinal` | `INTEGER` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `index_var` | `INTEGER` | L13 |
| `iInicio` | `INTEGER` | L14 |
| `dFechaCancelada` | `DATE` | L15 |
| `cTarjetaSustituta` | `CHAR(16)` | L16 |
| `cStatus` | `CHAR(3)` | L17 |
| `cDescripComent` | `CHAR(60)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `13` | `bdicnweb` | no | SELECT | L119 |
| `bdiatmist` | `bdicnweb` | no | INSERT | L122 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L113 | FÓRMULA | `LET iSerialFinal = cSerialFinal - 1;` |  |
| L116 | FÓRMULA | `LET iInicio = iInicio + 1;` |  |
| L117 | FÓRMULA | `LET iSerialFinal = iSerialFinal + 1;` |  |
| L126 | FÓRMULA | `LET iSerialFinal = iSerialFinal + 1;` |  |
| L127 | FÓRMULA | `LET iLote = iLote + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `tarjeta` | ENTIDAD | tarjeta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_constarjetasadmin_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_constarjetasadmin_totales.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación tarjetas y Administrador — rol de usuario con privilegios administrativos (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_constarjetasadmin_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEstatus                     CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEstatus` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `iNoRegPorAsignar` | `INTEGER` | L9 |
| `cStatus` | `CHAR(3)` | L10 |
| `cNumeroTarjeta` | `CHAR(16)` | L11 |
| `dFecha` | `DATE` | L12 |
| `cBin` | `CHAR(10)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tarjetas` | ENTIDAD | tarjetas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_constarjetasmotivoreposicion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_constarjetasmotivoreposicion.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación tarjetas, motivo y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_constarjetasmotivoreposicion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iTipo` | `INTEGER` | L8 |
| `cDescripcion` | `CHAR(60)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tarjetas` | ENTIDAD | tarjetas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `motivo` | ENTIDAD | motivo / causa | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?icion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?icion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_constdevolucion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_constdevolucion.sql` |
| **LOC (1er CREATE)** | 118 |
| **Callgraph** | ✅ fan_in=15 / fan_out=37 |
| **Deps concatenadas** | 57 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cce_consultar_chequesdev_devcoppel2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_constdevolucion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(10)
  pCtaDev                      CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(10)` | — | — |
| `pCtaDev` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cCodRetSp` | `CHAR(6)` | L18 |
| `iCodRetSp` | `INTEGER` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `cBanco` | `CHAR(3)` | L21 |
| `cNumCuenta` | `CHAR(20)` | L22 |
| `cNumCheque` | `CHAR(7)` | L23 |
| `dMonto` | `DECIMAL(16,2)` | L24 |
| `cCta_Deposito` | `CHAR(20)` | L25 |
| `cCodigoRetDev` | `CHAR(5)` | L26 |
| `cMotivo` | `CHAR(2)` | L27 |
| `cDescMotivo` | `CHAR(35)` | L28 |
| `cSucursal` | `CHAR(4)` | L29 |
| `cNomSucursal` | `CHAR(40)` | L30 |
| `cDescripcion` | `CHAR(40)` | L31 |
| `iRegistros` | `INTEGER` | L32 |
| `iRecuperacion` | `INTEGER` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L94 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cce_consultar_chequesdev_devcoppel2` | `bditef` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L64 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFecha = '' OR pCtaDev = '' OR  pRegistros IS NULL OR pRecup` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L87 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_consultar_chequesdev_devco` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_constdevolucion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_constdevolucion_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=36 |
| **Deps concatenadas** | 56 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cce_consultar_chequesdev_devcoppel2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_constdevolucion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(10)
  pCtaDev                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(10)` | — | — |
| `pCtaDev` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cce_consultar_chequesdev_devcoppel2_totales` | `bditef` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:sp_cce_consultar_chequ` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_constransacioneschqs`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_constransacioneschqs.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=15 / fan_out=35 |
| **Deps concatenadas** | 55 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación [polisemia] Transferencia y cheque" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_constransacioneschqs(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cTransacCom` | `CHAR(4)` | L9 |
| `cTransacIva` | `CHAR(4)` | L10 |
| `cComision` | `CHAR(4)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_comisiones` | `bdicheq` | ⚠️ sí | SELECT | L45 |
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `?aciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aciones`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_constransferbancos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_constransferbancos.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación transferencia y banco" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_transfer_bancos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_constransferbancos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMnsjeRespuesta` | `CHAR(150)` | L10 |
| `cIdBanco` | `CHAR(3)` | L11 |
| `cNomBanco` | `CHAR(100)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_transfer_bancos` | `bditransfer` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditransfer:sp_transfer_bancos` |  |
| L57 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `banco` | ENTIDAD | banco | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultabitacoraerroresmetas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultabitacoraerroresmetas.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 135 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación bitácora y error" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultabitacoraerroresmetas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFunc                        CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFunc` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cNumSuc` | `CHAR(4)` | L10 |
| `cNomSuc` | `CHAR(40)` | L11 |
| `cDesc` | `CHAR(50)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_bitacoraerrorescargarchivo` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00981';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?esmetas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?esmetas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultabitacoraerroresmetas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultabitacoraerroresmetas_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 134 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación bitácora y error (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultabitacoraerroresmetas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFunc                        CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFunc` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cNumSuc` | `CHAR(4)` | L8 |
| `cNomSuc` | `CHAR(40)` | L9 |
| `cDesc` | `CHAR(50)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_bitacoraerrorescargarchivo` | `bdicnweb` | no | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?esmetas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?esmetas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultacajageneneralatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacajageneneralatms.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación caja y cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_combocajagen_atm2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacajageneneralatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensaje` | `CHAR(30)` | L10 |
| `cIdCajaGeneral` | `CHAR(3)` | L11 |
| `cDescCajaGeneral` | `CHAR(40)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_soc_combocajagen_atm2` | `bdisuc` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_combocajagen_atm";` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `?eneral` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?eneral` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultacajageneral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacajageneral.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 109 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación caja (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacajageneral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPlaza                       CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPlaza` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodProveedor` | `CHAR(4)` | L10 |
| `cDescripcion` | `CHAR(30)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | 🔵 CONVENCIÓN | nombre_sp |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_consultacentrocostosatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacentrocostosatms.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 89 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ro — Rol Operativo, OS — Originación de Solicitudes y cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_combocc_atm2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=4 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacentrocostosatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(30)` | L9 |
| `cIdCentroCostos` | `CHAR(4)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_soc_combocc_atm2` | `bdisuc` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = ''  OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_combocc_atm2";` |  |
| L59 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?cent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?c` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cent`, `ro`, `?c`, `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultacheqsimg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacheqsimg.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=52 / fan_out=40 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheque" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacheqsimg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pNumCheque                   CHAR(7)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | `cheq`=cheque | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_cheques_img` | `bditef` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheq` | ENTIDAD | cheque | 🔵 CONVENCIÓN | nombre_sp |
| `?simg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?simg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultacheqsxpresentar_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacheqsxpresentar_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheque (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicheq` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacheqsxpresentar_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(100)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `bdicheq` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bdicheq:sp_cce_consultarchqsx` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheq` | ENTIDAD | cheque | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `presenta` | ACCION | presenta | 🔵 CONVENCIÓN | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultachequesdevueltos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultachequesdevueltos.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=52 / fan_out=31 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheques y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultarchequesdevueltos3` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultachequesdevueltos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `cCodRetSp` | `CHAR(5)` | L21 |
| `iCodRetSp` | `INTEGER` | L22 |
| `cMotivoDevolucion` | `CHAR(2)` | L23 |
| `cDecripcion` | `CHAR(35)` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `cClaveBanco` | `CHAR(3)` | L26 |
| `cDescripcionBanco` | `CHAR(40)` | L27 |
| `cCuenta` | `CHAR(20)` | L28 |
| `NumCheque` | `CHAR(7)` | L29 |
| `fechaPresenta` | `DATE` | L30 |
| `mMonto` | `MONEY(18,2)` | L31 |
| `mMontoAplica` | `MONEY(18,2)` | L32 |
| `mSumaComision` | `MONEY(18,2)` | L33 |
| `cFormatoImagen` | `CHAR(3)` | L34 |
| `iTamanoImagenA` | `INTEGER` | L35 |
| `iTamanoImagenB` | `INTEGER` | L36 |
| `iNoRegistros` | `INTEGER` | L37 |
| `iRegistros` | `INTEGER` | L38 |
| `iRecuperacion` | `INTEGER` | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L85 |
| `sp_consultarchequesdevueltos3` | `bditef` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L73 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCte = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL ` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L96 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_consultarchequesdevueltos3';` |  |
| L98 | VALIDACIÓN_NULL | `IF pNumCte = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL  OR pRegistros IS NULL AND cCodRetSp` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00718';` |  |
| L106 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?uelt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uelt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultachequesdevueltos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultachequesdevueltos_totales.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=52 / fan_out=30 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheques y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `sp_consultarchequesdevueltos3_totales` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultachequesdevueltos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(80)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `sp_consultarchequesdevueltos3_totales` | `bditef` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pNumCte = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:sp_consultarchequesdev` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |
| `?uelt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?uelt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultachequetamdif`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultachequetamdif.sql` |
| **LOC (1er CREATE)** | 170 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheque" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultachequetamdif(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| `cCveBanco` | `CHAR(3)` | L31 |
| `cDescripcionBanco` | `CHAR(40)` | L32 |
| `cCuentaReferencia` | `CHAR(20)` | L33 |
| `iNoCheque` | `INTEGER` | L34 |
| `mImporte` | `DECIMAL(14,2)` | L35 |
| `cCuentaDeposito` | `CHAR(20)` | L36 |
| `cSuscursalOperadora` | `CHAR(44)` | L37 |
| `cChqProcesado` | `CHAR(1)` | L38 |
| `cChqCompensacion` | `CHAR(3)` | L39 |
| `cChqTransaccion` | `CHAR(2)` | L40 |
| `cChqCodSeguridad` | `CHAR(3)` | L41 |
| `cChqDigVerPre` | `CHAR(1)` | L42 |
| `cChqDigVerInter` | `CHAR(1)` | L43 |
| `cIndImgCheque` | `CHAR(1)` | L44 |
| `iTamAnversoImagen` | `INTEGER` | L45 |
| `iTamReversoImagen` | `INTEGER` | L46 |
| `cTransaccion` | `CHAR(4)` | L47 |
| `cNombreCliente` | `CHAR(60)` | L48 |
| `cRfcCliente` | `CHAR(13)` | L49 |
| `cCurpCliente` | `CHAR(20)` | L50 |
| `cTipoCtaDeposito` | `CHAR(2)` | L51 |
| `iIdRegistro` | `INTEGER` | L52 |
| *…1 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L99 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pDireccionMac = '' OR pRegistros IS NULL OR pRecuperacion IS ` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L143 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cheque` | ENTIDAD | cheque | 🔵 CONVENCIÓN | nombre_sp |
| `?tamdif` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tamdif` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultacomboparametro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacomboparametro.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación combo y parámetro" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacomboparametro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iRecuperacion` | `INTEGER` | L8 |
| `cCveParam` | `CHAR(8)` | L9 |
| `cDescValor` | `CHAR(80)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L26 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L41 |
| `sl_parametros` | `bdilide` | ⚠️ sí | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametro` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_consultacomprobante`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacomprobante.sql` |
| **LOC (1er CREATE)** | 204 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ro — Rol Operativo (complemento, anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 5 llamada(s): `sp_tk_consultaremesadoras`, `sp_ope_consmovimientos`, `sp_ope_cons_ticketabonovent` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacomprobante(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pRemesadora                  CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pCveRemesa                   CHAR(20)
  pNumCliente                  CHAR(9)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pFolioSuc                    CHAR(16)
  pHuella                      CHAR(1)
  cParam1                      CHAR(50)
  cParam2                      CHAR(100)
  cParam3                      CHAR(150)
) RETURNING CHAR(5)	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pRemesadora` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pCveRemesa` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |
| `pHuella` | `CHAR(1)` | — | — |
| `cParam1` | `CHAR(50)` | — | — |
| `cParam2` | `CHAR(100)` | — | — |
| `cParam3` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L37 |
| `iSqlErr` | `INTEGER` | L38 |
| `iTotalReg` | `INTEGER` | L39 |
| `cNumconvenio` | `CHAR(3)` | L40 |
| `cNomconvenio` | `CHAR(40)` | L41 |
| `cNumcategoria` | `CHAR(2)` | L42 |
| `cNum_cte` | `CHAR(20)` | L43 |
| `dFech_oper` | `DATE` | L44 |
| `cSucursal` | `CHAR(4)` | L45 |
| `cFolio_suc` | `CHAR(16)` | L46 |
| `cReferencia1` | `CHAR(40)` | L47 |
| `iTotRegistros` | `INTEGER` | L48 |
| `cFormaPago` | `CHAR(1)` | L49 |
| `mImportePago` | `MONEY` | L50 |
| `cFechaInsert` | `CHAR(10)` | L51 |
| `cUsuario` | `CHAR(8)` | L52 |
| `cFolioSuc` | `CHAR(16)` | L53 |
| `cNombre1Ben` | `CHAR(40)` | L54 |
| `cNombre2Ben` | `CHAR(40)` | L55 |
| `cApPaternoBen` | `CHAR(40)` | L56 |
| `cApMaternoBen` | `CHAR(40)` | L57 |
| `cNumCteBen` | `CHAR(20)` | L58 |
| `cNumcliente` | `CHAR(20)` | L59 |
| `cCadenaTran` | `CHAR(942)` | L60 |
| `cNomSucursal` | `CHAR(40)` | L61 |
| *…9 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_tk_consultaremesadoras` | `bdicnweb` | no | L136 |
| `sp_ope_consmovimientos` | `bdicnweb` | no | L156 |
| `sp_ope_cons_ticketabonovent` | `bdicnweb` | no | L167 |
| `sp_ope_cons_ticketefectivovent` | `bdicnweb` | no | L177 |
| `sp_ope_cons_ticketabonoapp` | `bdicnweb` | no | L187 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | FÓRMULA | `LET iTotalReg = iTotalReg + 1;` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ro`, `?b`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultacomprobante_web`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacomprobante_web.sql` |
| **LOC (1er CREATE)** | 209 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ro — Rol Operativo (complemento, anterior, canal web)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 5 llamada(s): `sp_tk_consultaremesadoras`, `sp_ope_consmovimientos_web`, `sp_ope_cons_ticketabonovent_web` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacomprobante_web(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pRemesadora                  CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pCveRemesa                   CHAR(20)
  pNumCliente                  CHAR(9)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pFolioSuc                    CHAR(16)
  pHuella                      CHAR(1)
  cParam1                      CHAR(50)
  cParam2                      CHAR(100)
  cParam3                      CHAR(150)
) RETURNING CHAR(5)	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pRemesadora` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pCveRemesa` | `CHAR(20)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |
| `pHuella` | `CHAR(1)` | — | — |
| `cParam1` | `CHAR(50)` | — | — |
| `cParam2` | `CHAR(100)` | — | — |
| `cParam3` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L39 |
| `iSqlErr` | `INTEGER` | L40 |
| `iTotalReg` | `INTEGER` | L41 |
| `cNumconvenio` | `CHAR(3)` | L42 |
| `cNomconvenio` | `CHAR(40)` | L43 |
| `cNumcategoria` | `CHAR(2)` | L44 |
| `cNum_cte` | `CHAR(20)` | L45 |
| `dFech_oper` | `DATE` | L46 |
| `cSucursal` | `CHAR(4)` | L47 |
| `cFolio_suc` | `CHAR(16)` | L48 |
| `cReferencia1` | `CHAR(40)` | L49 |
| `iTotRegistros` | `INTEGER` | L50 |
| `cFormaPago` | `CHAR(1)` | L51 |
| `mImportePago` | `MONEY` | L52 |
| `cFechaInsert` | `CHAR(10)` | L53 |
| `cUsuario` | `CHAR(8)` | L54 |
| `cFolioSuc` | `CHAR(16)` | L55 |
| `cOrigen` | `CHAR(10)` | L56 |
| `cNombre1Ben` | `CHAR(40)` | L57 |
| `cNombre2Ben` | `CHAR(40)` | L58 |
| `cApPaternoBen` | `CHAR(40)` | L59 |
| `cApMaternoBen` | `CHAR(40)` | L60 |
| `cNumCteBen` | `CHAR(20)` | L61 |
| `cNumcliente` | `CHAR(20)` | L62 |
| `cTelefono` | `CHAR(10)` | L63 |
| *…11 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_tk_consultaremesadoras` | `bdicnweb` | no | L142 |
| `sp_ope_consmovimientos_web` | `bdicnweb` | no | L162 |
| `sp_ope_cons_ticketabonovent_web` | `bdicnweb` | no | L172 |
| `sp_ope_cons_ticketefectivovent_web` | `bdicnweb` | no | L182 |
| `sp_ope_cons_ticketabonoapp_web` | `bdicnweb` | no | L192 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L76 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L145 | FÓRMULA | `LET iTotalReg = iTotalReg + 1;` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `web` | MODIF | canal web | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ro`, `?b`, `?e_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaconcmovtotal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaconcmovtotal.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=56 |
| **Deps concatenadas** | 152 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimiento" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sac_concimovtotal` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaconcmovtotal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaProceso                DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaProceso` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `vmensaje` | `CHAR(90)` | L15 |
| `vNombre` | `CHAR(20)` | L16 |
| `iTotalEnvi` | `INTEGER` | L17 |
| `iErrores` | `INTEGER` | L18 |
| `iAplicados` | `INTEGER` | L19 |
| `iConciliado` | `INTEGER` | L20 |
| `ifaltantes` | `INTEGER` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |
| `sp_sac_concimovtotal` | `bdisac` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaProceso IS NULL THEN` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP: bdisac:sp_sac_concimovtotal";` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00950';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00948';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00949';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?c` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?tal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?c`, `?tal` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaconsecutivoarch`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaconsecutivoarch.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=52 / fan_out=40 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación consecutivo y archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_consultaconsecutivoarchivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaconsecutivoarch(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistema                     CHAR(20)
  pConsecutivo                 CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(20)` | — | — |
| `pConsecutivo` | `CHAR(3)` | `consecutivo`=consecutivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iConsecutivo` | `INTEGER` | L10 |
| `dFechaHoy` | `DATE` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_consultaconsecutivoarchivo` | `bditef` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditarjeta:sp_consultaconsecutivoarchivo` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `consecutivo` | ENTIDAD | consecutivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |

---

## `sp_ope_consultactascoppelcolateral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultactascoppelcolateral.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=15 / fan_out=35 |
| **Deps concatenadas** | 54 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cuentas y Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultactascoppelcolateral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCuenta` | `CHAR(20)` | L8 |
| `cCuentaCol` | `CHAR(20)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_colateral_tmp` | `bdicnweb` | no | INSERT | L46 |
| `sc_colateral` | `bdicheq` | ⚠️ sí | SELECT | L48 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L51 |
| `sc_colateral_tmp` | `bdicnweb` | no | SELECT | L58 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?colateral` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?colateral` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultacuentaspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacuentaspei.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacuentaspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iRegistros` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `iIdReg` | `INTEGER` | L13 |
| `cCuenta` | `CHAR(20)` | L14 |
| `cClabe` | `CHAR(20)` | L15 |
| `cTarjeta` | `CHAR(20)` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L62 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultacuentaspei_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultacuentaspei_totales.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cuentas (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultacuentaspei_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pei_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultadetallechequecodigo40`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultadetallechequecodigo40.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación detalle, cheque y código" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultadetallechequecodigo40(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INTEGER` | L28 |
| `iNoRegistros` | `INTEGER` | L29 |
| `cCveBanco` | `CHAR(3)` | L31 |
| `cDescripcionBanco` | `CHAR(40)` | L32 |
| `cCuentaReferencia` | `CHAR(20)` | L33 |
| `iNoCheque` | `INTEGER` | L34 |
| `mImporte` | `DECIMAL(14,2)` | L35 |
| `cCuentaDeposito` | `CHAR(20)` | L36 |
| `cSuscursalOperadora` | `CHAR(44)` | L37 |
| `cChqProcesado` | `CHAR(1)` | L38 |
| `cChqCompensacion` | `CHAR(3)` | L39 |
| `cChqTransaccion` | `CHAR(2)` | L40 |
| `cChqCodSeguridad` | `CHAR(3)` | L41 |
| `cChqDigVerPre` | `CHAR(1)` | L42 |
| `cChqDigVerInter` | `CHAR(1)` | L43 |
| `cIndImgCheque` | `CHAR(1)` | L44 |
| `iTamAnversoImagen` | `INTEGER` | L45 |
| `iTamReversoImagen` | `INTEGER` | L46 |
| `cTransaccion` | `CHAR(4)` | L47 |
| `cNombreCliente` | `CHAR(60)` | L48 |
| `cRfcCliente` | `CHAR(13)` | L49 |
| `cCurpCliente` | `CHAR(20)` | L50 |
| `cTipoCtaDeposito` | `CHAR(2)` | L51 |
| `iIdRegistro` | `INTEGER` | L52 |
| *…1 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L108 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L133 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheque` | ENTIDAD | cheque | 🔵 CONVENCIÓN | nombre_sp |
| `codigo` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?40` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?40` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultadetallechequecodigo40_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultadetallechequecodigo40_totales.sql` |
| **LOC (1er CREATE)** | 381 |
| **Callgraph** | ✅ fan_in=78 / fan_out=16 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación detalle, cheque y código (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `sp_ope_chequeduplicado` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultadetallechequecodigo40_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(18)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(18)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `dFechaHoy` | `DATE` | L14 |
| `dMontoImagen` | `DECIMAL(16,2)` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `cCveBanco` | `CHAR(3)` | L17 |
| `cDescBanco` | `CHAR(40)` | L18 |
| `cCtaReferencia` | `CHAR(40)` | L19 |
| `iNumCheque` | `INTEGER` | L20 |
| `mImporte` | `MONEY(14,2)` | L21 |
| `cCuentaDeposito` | `CHAR(20)` | L22 |
| `cSucursal` | `CHAR(44)` | L23 |
| `cNoTransaccion` | `CHAR(4)` | L24 |
| `cChqCompensacion` | `CHAR(3)` | L25 |
| `cChqTransaccion` | `CHAR(2)` | L26 |
| `cChqCodSeguridad` | `CHAR(3)` | L27 |
| `cChqDigVerPre` | `CHAR(1)` | L28 |
| `cChqDigVerInter` | `CHAR(1)` | L29 |
| `iTamImgChqAnverso` | `INTEGER` | L30 |
| `iTamImgChqReverso` | `INTEGER` | L31 |
| `cIsImagenCheque` | `CHAR(1)` | L32 |
| `cTipoCuentaDep` | `CHAR(2)` | L33 |
| `cCampoCliente` | `CHAR(20)` | L34 |
| *…20 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | DELETE | L151 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L250 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L271 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L122 |
| `sp_ope_chequeduplicado` | `bdicnweb` | no | L305 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L134 | VALIDACIÓN_NULL | `IF dFechaHoy IS NULL THEN` |  |
| L145 | VALIDACIÓN_NULL | `IF dMontoImagen IS NULL THEN` |  |
| L167 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_cce_consultar_cheques40';` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L244 | VALIDACIÓN_NULL | `IF cTipoCuentaDep IS NULL THEN -- ERROR DE QUE NO SE ENCONTRO EL MAPEO CECOBAN` |  |
| L309 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_ope_chequeduplica` |  |
| L329 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L341 | FÓRMULA | `LET iNoImagenes = iNoImagenes * 2;` |  |
| L351 | FÓRMULA | `LET iNoDocsIncompletos = iNoRegistros - iNoChequesValidos;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `codigo` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?40_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?40_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultadetallechequecodigo46`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultadetallechequecodigo46.sql` |
| **LOC (1er CREATE)** | 147 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación detalle, cheque y código" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultadetallechequecodigo46(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `iNoRegistros` | `INTEGER` | L30 |
| `iIddetallechq` | `INTEGER` | L31 |
| `cBanco` | `CHAR(3)` | L32 |
| `cDesc_banco` | `CHAR(40)` | L33 |
| `cCtareferencia` | `CHAR(14)` | L34 |
| `iNum_cheque` | `INTEGER` | L35 |
| `dMonto_orig` | `DECIMAL(14,2)` | L36 |
| `cNum_cuentadep` | `CHAR(20)` | L37 |
| `cSi_transacc` | `CHAR(4)` | L38 |
| `cAplica` | `CHAR(2)` | L39 |
| `cMotivo_dev` | `CHAR(37)` | L40 |
| `cDigitalizado` | `CHAR(1)` | L41 |
| `cCompensacion` | `CHAR(3)` | L42 |
| `cTransacc` | `CHAR(2)` | L43 |
| `cCodseguridad` | `CHAR(3)` | L44 |
| `cDigverpre` | `CHAR(1)` | L45 |
| `cDigverinter` | `CHAR(1)` | L46 |
| `cTipo_cta_dep` | `CHAR(2)` | L47 |
| `cNombreBen` | `CHAR(60)` | L48 |
| `cRfcCte` | `CHAR(13)` | L49 |
| `cCurpCte` | `CHAR(20)` | L50 |
| `cCodAlertamiento` | `CHAR(2)` | L51 |
| `iTamImgChqAnverso` | `INTEGER` | L52 |
| *…1 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L127 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `codigo` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?46` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?46` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultadetallechequecodigo47`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultadetallechequecodigo47.sql` |
| **LOC (1er CREATE)** | 145 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación detalle, cheque y código" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultadetallechequecodigo47(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INTEGER` | L27 |
| `iNoRegistros` | `INTEGER` | L28 |
| `iIddetallechq` | `INTEGER` | L30 |
| `cCveBanco` | `CHAR(3)` | L31 |
| `cDescBanco` | `CHAR(40)` | L32 |
| `cCtaRef` | `CHAR(14)` | L33 |
| `iNumCheque` | `INTEGER` | L34 |
| `mImporte` | `DECIMAL(14,2)` | L35 |
| `cCuentaDeposito` | `CHAR(20)` | L36 |
| `cTipoEliminacion` | `CHAR(50)` | L37 |
| `cDigitalizado` | `CHAR(1)` | L38 |
| `cCompensacion` | `CHAR(3)` | L39 |
| `cTransacc` | `CHAR(2)` | L40 |
| `cCodseguridad` | `CHAR(3)` | L41 |
| `cDigverpre` | `CHAR(1)` | L42 |
| `cDigverinter` | `CHAR(1)` | L43 |
| `cSiTransaccion` | `CHAR(4)` | L44 |
| `cNombreCte` | `CHAR(60)` | L45 |
| `cRfcCte` | `CHAR(13)` | L46 |
| `cCurpCte` | `CHAR(20)` | L47 |
| `cTipoCuentaDep` | `CHAR(2)` | L48 |
| `cCodAlertamiento` | `CHAR(2)` | L49 |
| `iTamImgChqAnverso` | `INTEGER` | L50 |
| `iTamImgChqReverso` | `INTEGER` | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L104 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L125 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheque` | ENTIDAD | cheque | 🔵 CONVENCIÓN | nombre_sp |
| `codigo` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?47` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?47` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaentidadfederativa`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaentidadfederativa.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 108 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación identificador · IVA" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaentidadfederativa(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPais                        CHAR(3)
  pEstado                      CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pPais` | `CHAR(3)` | — | — |
| `pEstado` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNombre` | `CHAR(30)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_estados` | `bdinteg` | ⚠️ sí | SELECT | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?adfederat` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ent`, `?adfederat` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultafechaproceso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultafechaproceso.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación fecha y proceso" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ope_consultarutalmacenamientoxml` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultafechaproceso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaProceso                DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaProceso` | `DATE` | `fecha`=fecha · `proceso`=proceso | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cDescripcion` | `CHAR(50)` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `dFecha` | `DATE` | L12 |
| `cStatus` | `CHAR(1)` | L13 |
| `dUltimoDiaMes` | `DATE` | L14 |
| `iexisteFechaProceso` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L35 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L49 |
| `sl_procesos` | `bdilide` | ⚠️ sí | SELECT | L65 |
| `systables` | `bdicnweb` | no | SELECT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L53 |
| `sp_ope_consultarutalmacenamientoxml` | `bdicnweb` | no | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaProceso IS NULL THEN` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_consultafechaprocesoanual`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultafechaprocesoanual.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación fecha y proceso" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ope_consultarutalmacenamientoxml` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultafechaprocesoanual(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaProceso                DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaProceso` | `DATE` | `fecha`=fecha · `proceso`=proceso | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cDescripcion` | `CHAR(50)` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `dFecha` | `DATE` | L12 |
| `cStatus` | `CHAR(1)` | L13 |
| `dUltimoDiaMes` | `DATE` | L14 |
| `iexisteFechaProceso` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L36 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L51 |
| `sl_procesos` | `bdilide` | ⚠️ sí | SELECT | L67 |
| `systables` | `bdicnweb` | no | SELECT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |
| `sp_ope_consultarutalmacenamientoxml` | `bdicnweb` | no | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaProceso IS NULL THEN` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?anual` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?anual` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultafechaultimasolicitudestdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultafechaultimasolicitudestdc.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 133 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación fecha y tarjeta de crédito (últimas)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultafechaultimasolicitudestdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `dFechaUltima` | `DATE` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_metascoppel` | `bdmis` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | VALIDACIÓN_NULL | `IF dFechaUltima IS NULL THEN` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ultimas` | MODIF | últimas | 🟡 INFERIDO | nombre_sp |
| `?olicitudes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?olicitudes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaimportececoban`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaimportececoban.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación importe y cob — cobranza" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaimportececoban(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `mImporte` | `MONEY(14,2)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `importe` | ENTIDAD | importe | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cob` | ENTIDAD | cob — cobranza (abreviación de dominio — sp_repcob_*, sp_obt | 🔵 CONVENCIÓN | nombre_sp |
| `?an` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ce`, `?an` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaindicadorespei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaindicadorespei.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación indicadores" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaindicadorespei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(5)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(5)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iIdGral` | `INTEGER` | L14 |
| `iIdIndc` | `INTEGER` | L15 |
| `iIdSubindi` | `INTEGER` | L16 |
| `cTipo` | `CHAR(5)` | L17 |
| `dPorcentaje` | `DECIMAL(7,4)` | L18 |
| `cRango` | `CHAR(5)` | L19 |
| `sPonderacion` | `SMALLINT` | L20 |
| `dPorcentajeFin` | `DECIMAL(5,2)` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `indicadores` | ENTIDAD | indicadores | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaindicadorespei_pba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaindicadorespei_pba.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación indicadores (PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaindicadorespei_pba(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(5)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(5)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iIdGral` | `INTEGER` | L14 |
| `iIdIndc` | `INTEGER` | L15 |
| `iIdSubindi` | `INTEGER` | L16 |
| `cTipo` | `CHAR(5)` | L17 |
| `dPorcentaje` | `DECIMAL(7,4)` | L18 |
| `cRango` | `CHAR(5)` | L19 |
| `sPonderacion` | `SMALLINT` | L20 |
| `dPorcentajeFin` | `DECIMAL(5,2)` | L21 |
| `iNoRegistros` | `INTEGER` | L22 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | FÓRMULA | `LET  iNoRegistros = iNoRegistros + 1;` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `indicadores` | ENTIDAD | indicadores | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pei_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaiprutacarga`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaiprutacarga.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ruta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaiprutacarga(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cDescripcion` | `CHAR(50)` | L7 |
| `iRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L23 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L38 |
| `sl_parametros` | `bdilide` | ⚠️ sí | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ip` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ruta` | ENTIDAD | ruta (de archivo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ip` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamantenimientosucursal_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamantenimientosucursal_totales.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 131 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación OS — Originación de Solicitudes (anterior, totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamantenimientosucursal_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pSucursal                    CHAR(4)
  pTipoSucursal                CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotalRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_sucursalesinfo` | `bdmis` | ⚠️ sí | SELECT | L39 |
| `sw_ope_cargarchivotiopsolicitud` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?enimient` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ucursal_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?enimient`, `?ucursal_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamanttosolicitudtdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamanttosolicitudtdc.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 130 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación OS — Originación de Solicitudes y tarjeta de crédito (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamanttosolicitudtdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaUltima                 DATE
  pBandera                     CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaUltima` | `DATE` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `cSucursal` | `CHAR(4)` | L11 |
| `cNombre` | `CHAR(40)` | L12 |
| `dMetaDiaria` | `DECIMAL(3,2)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_metascoppel` | `bdmis` | ⚠️ sí | SELECT | L55 |
| `sw_ope_cargarchivosolicitudtdc` | `bdicnweb` | no | SELECT | L67 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaUltima IS NULL OR pBandera = '' OR pRegistros IS NULL O` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L70 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?olicitud` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?t`, `?olicitud` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamanttosolicitudtdc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamanttosolicitudtdc_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 129 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación OS — Originación de Solicitudes y tarjeta de crédito (anterior, totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamanttosolicitudtdc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaUltima                 DATE
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaUltima` | `DATE` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_metascoppel` | `bdmis` | ⚠️ sí | SELECT | L40 |
| `sw_ope_cargarchivosolicitudtdc` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaUltima IS NULL OR pBandera = '' THEN` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?olicitud` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?t`, `?olicitud` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultametascump`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultametascump.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultametascump(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cDescripcion` | `CHAR(60)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iParametro` | `INTEGER` | L12 |
| `dValor` | `DECIMAL(16,2)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_paramcump` | `bdmis` | ⚠️ sí | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L58 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00981';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?metascump` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?metascump` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultametascump_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultametascump_totales.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultametascump_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_paramcump` | `bdmis` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00981';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?metascump_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?metascump_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultametasproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultametasproductos.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ro — Rol Operativo y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultametasproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pAnioMes                     CHAR(10)
  pTipoSucursal                INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pAnioMes` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `cProducto` | `CHAR(4)` | L11 |
| `cDesProducto` | `CHAR(40)` | L12 |
| `mMeta` | `DECIMAL(16,2)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_metasprod` | `bdmis` | ⚠️ sí | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pAnioMes = '' OR pTipoSucursal IS NULL OR pRegistros IS NULL ` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L62 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00981';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?meta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?duct` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?meta`, `ro`, `?duct` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultametasproductos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultametasproductos_totales.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 128 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ro — Rol Operativo y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultametasproductos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pAnioMes                     CHAR(10)
  pTipoSucursal                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pAnioMes` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_metasprod` | `bdmis` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pAnioMes = '' OR pTipoSucursal IS NULL THEN` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00981';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?meta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?duct` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?meta`, `ro`, `?duct` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultametassaldos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultametassaldos.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ss — subsistema y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultametassaldos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSuc                     INTEGER
  pAnioMes                     CHAR(6)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSuc` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pAnioMes` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `dMonto_cap` | `DECIMAL(18,2)` | L10 |
| `dMonto_col` | `DECIMAL(18,2)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_tiposuc` | `bdmis` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pAnioMes = '' OR pTipoSuc IS NULL THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00981';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?meta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ald` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?meta`, `ss`, `?ald` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamodcajagenatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamodcajagenatms.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=34 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación caja y cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_cgmod_atm` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamodcajagenatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cMensaje` | `CHAR(50)` | L10 |
| `cIdCajaGen` | `CHAR(3)` | L11 |
| `cDescCajaGen` | `CHAR(80)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_soc_cgmod_atm` | `bdisuc` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet 	 = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_cgmod_atm";` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mod` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mod` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamodcentrocostoatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamodcentrocostoatms.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=33 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ro — Rol Operativo, OS — Originación de Solicitudes y cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_combocc_mod_atm2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamodcentrocostoatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(30)` | L9 |
| `cIdCentroCosto` | `CHAR(4)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_soc_combocc_mod_atm2` | `bdisuc` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_combocc_mod_atm";` |  |
| L54 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?modcent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?c` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?modcent`, `ro`, `?c`, `?to` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamodidatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamodidatms.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación identificador y cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_idmod_atm` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamodidatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | ✅ CÓDIGO |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(50)` | L9 |
| `cDesIdAtm` | `CHAR(6)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_soc_idmod_atm` | `bdisuc` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_idmod_atm";` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mod` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mod` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamovaplicacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamovaplicacion.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ✅ fan_in=5 / fan_out=8 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimiento" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cce_consultar_chequesdev_consdev2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamovaplicacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(5)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `cBanco` | `CHAR(3)` | L20 |
| `cNumCuenta` | `CHAR(20)` | L21 |
| `cNumCheque` | `CHAR(7)` | L22 |
| `dMonto` | `DECIMAL(16,2)` | L23 |
| `cCtaDeposito` | `CHAR(20)` | L24 |
| `cCodRetDev` | `CHAR(5)` | L25 |
| `cMotivo` | `CHAR(2)` | L26 |
| `cDescMotivo` | `CHAR(35)` | L27 |
| `cDesBanco` | `CHAR(40)` | L28 |
| `cDesRetDev` | `CHAR(50)` | L29 |
| `cStatus` | `CHAR(5)` | L30 |
| `iNoRegistros` | `INTEGER` | L31 |
| `iRegistros` | `INTEGER` | L32 |
| `iRecuperacion` | `INTEGER` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L94 |
| `si_codret` | `bdinteg` | ⚠️ sí | SELECT | L103 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L77 |
| `sp_cce_consultar_chequesdev_consdev2` | `bditef` | ⚠️ sí | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL OR pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L87 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_consultar_cheques` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L108 | VALIDACIÓN_NULL | `IF cDesRetDev IS NULL OR cDesRetDev = '' THEN` |  |
| L114 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamovaplicacion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamovaplicacion_totales.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=5 / fan_out=7 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimiento (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cce_consultar_chequesdev_consdev2_totales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamovaplicacion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cDescCodRet` | `CHAR(80)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_cce_consultar_chequesdev_consdev2_totales` | `bditef` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFecha IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_consultar_cheques` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?cion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultamovimientosatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamovimientosatms.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=31 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimientos y cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_combomov_atm` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamovimientosatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(30)` | L9 |
| `cTipoMov` | `CHAR(15)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_soc_combomov_atm` | `bdisuc` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_combomov_atm";` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_consultamovimientosmetas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultamovimientosmetas.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 127 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimientos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultamovimientosmetas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `dFecha` | `DATE` | L12 |
| `cNombre` | `CHAR(40)` | L13 |
| `cCambio` | `CHAR(500)` | L14 |
| `iId` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?metas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?metas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultanombrearchivometastdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultanombrearchivometastdc.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 125 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación nombre, archivo y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultanombrearchivometastdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombre                      CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombre` | `CHAR(100)` | `nombre`=nombre | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_nombrearchivosmetasolicitudtdc` | `bdicnweb` | no | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?metas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?metas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultanombrecentrocostosatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultanombrecentrocostosatms.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=30 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación nombre, ro — Rol Operativo, OS — Originación de Solicitudes y cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_datonombresuc_atm` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=4 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultanombrecentrocostosatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(40)` | L9 |
| `cDescCentroCostos` | `CHAR(70)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_soc_datonombresuc_atm` | `bdisuc` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_datonombresuc_atm ";` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `nombre` | ENTIDAD | nombre | 🔵 CONVENCIÓN | nombre_sp |
| `?cent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?c` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cent`, `ro`, `?c`, `?t` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaparametrosenviosat`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaparametrosenviosat.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación parámetros y envíos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaparametrosenviosat(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `existenParametros` | `INTEGER` | L9 |
| `cCveParam` | `CHAR(8)` | L10 |
| `cValor` | `CHAR(10)` | L11 |
| `cDescripcion` | `CHAR(50)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L28 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L42 |
| `sl_parametros` | `bdilide` | ⚠️ sí | SELECT | L58 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `envios` | ENTIDAD | envíos | 🔵 CONVENCIÓN | nombre_sp |
| `?at` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?at` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaparametrosgralide`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaparametrosgralide.sql` |
| **LOC (1er CREATE)** | 177 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación parámetros y identificador (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaparametrosgralide(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCveParam                    CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pCveParam` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |
| `cCveParam` | `CHAR(8)` | L10 |
| `cValor` | `CHAR(10)` | L11 |
| `cDescValor` | `CHAR(50)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L29 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L43 |
| `sl_parametros` | `bdilide` | ⚠️ sí | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCveParam = '' OR pRegistros IS NULL OR pRecuperacion IS NULL` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L82 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L97 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L112 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L127 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L142 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L151 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultaparametroxsd`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaparametroxsd.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación parámetro y sd — saldo disponible" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaparametroxsd(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `existe_ArchivoXSD` | `INTEGER` | L7 |
| `cDescripcion` | `CHAR(50)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L23 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L37 |
| `sl_parametros` | `bdilide` | ⚠️ sí | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametro` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |

---

## `sp_ope_consultaprefijosparticipantesspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultaprefijosparticipantesspei.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 240 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación OS — Originación de Solicitudes y ss — subsistema (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=6 / 12 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultaprefijosparticipantesspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `iClaveCesif` | `INTEGER` | L9 |
| `cNombrecorto` | `CHAR(20)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tblbanco` | `bdispei` | ⚠️ sí | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L56 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ref` | AMBIGUO | referencia | 🟡 INFERIDO | nombre_sp |
| `?ij` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?particip` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `?ij`, `?particip`, `?e`, `ss`, `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultarutalmacenamientoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultarutalmacenamientoxml.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=372 / fan_out=10 |
| **Principales callers** | `sp_bitacora`, `sp_consultareportepagoscre`, `sp_generararchivo_rst`, `sp_ope_consultarutalmacenamientoxml` |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación dirección MAC y XML" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultarutalmacenamientoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cDescripcion` | `CHAR(50)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L21 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L36 |
| `sl_parametros` | `bdilide` | ⚠️ sí | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?utal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mac` | ENTIDAD | dirección MAC | 🔵 CONVENCIÓN | nombre_sp |
| `?enamiento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?utal`, `?enamiento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultastatusprefijosparticipantesspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultastatusprefijosparticipantesspei.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 241 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación estatus, OS — Originación de Solicitudes y ss — subsistema (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=3 · SINTÉTICO=6 / 13 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultastatusprefijosparticipantesspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClave                       INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClave` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cStatus` | `CHAR(1)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tblbcobloqueo` | `bdispei` | ⚠️ sí | SELECT | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pClave IS NULL OR pClave < 0 THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ref` | AMBIGUO | referencia | 🟡 INFERIDO | nombre_sp |
| `?ij` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?particip` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `?ij`, `?particip`, `?e`, `ss`, `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultasucursalesvalidacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultasucursalesvalidacion.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 124 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal, sv — supervisión/servicio y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultasucursalesvalidacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFuncionalidad               CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pFuncionalidad` | `CHAR(20)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cSucursal` | `CHAR(4)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_validacionsucursalmeta` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFuncionalidad= '' OR pRegistros IS NULL OR pRecuperacion IS ` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sv` | ENTIDAD | sv — supervisión/servicio (abreviación — bdiaclaracion) | 🔴 SINTÉTICO | nombre_sp |
| `?al` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?acion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `sv`, `?al`, `?acion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultatiporep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultatiporep.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=55 |
| **Deps concatenadas** | 149 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultatiporep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cIdReporte` | `CHAR(2)` | L10 |
| `cDescReporte` | `CHAR(45)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tdc_tiporeporte` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_consultatiposucursalmetas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultatiposucursalmetas.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 123 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultatiposucursalmetas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `ctipoSucursal` | `CHAR(3)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_tiposuc_mis` | `bdmis` | ⚠️ sí | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?metas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?metas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_consultausuariosaut`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_consultausuariosaut.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=22 / fan_out=32 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación usuarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cce_consultausuariosaut` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_consultausuariosaut(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEjecut                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEjecut` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iIdEjecutivo` | `INTEGER` | L10 |
| `cNombreEjecutivo` | `CHAR (100)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_cce_consultausuariosaut` | `bditef` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bditef:sp_cce_consultausuariosaut ";` |  |
| L53 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |

---

## `sp_ope_datoscarga_genarchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_datoscarga_genarchivo.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación datos y archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditef` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_datoscarga_genarchivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `dFecha` | `DATE` | L11 |
| `dFechaHabilAnt` | `DATE` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `cNoBanco` | `CHAR(3)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L44 |
| `bditef` | `bdicnweb` | no | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:cal_habil_ant';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_datosgral_archivocod46_ccep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_datosgral_archivocod46_ccep.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación datos, archivo, código y CCE — Cámara de Compensación Electrónica (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_datosgral_archivocod46_ccep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `dFecha` | `DATE` | L14 |
| `dFechaHabilAnt` | `DATE` | L15 |
| `dFechaHabilAnt2` | `DATE` | L16 |
| `dFechaHabilProx` | `DATE` | L17 |
| `cNoBanco` | `CHAR(3)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L49 |
| `bditef` | `bdicnweb` | no | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:cal_fecha_pre_fh';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:cal_habil_ant';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:cal_habil_ant';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?46_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?46_`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_detalleclasetipocambio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_detalleclasetipocambio.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 99 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación detalle y cambio (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_detalleclasetipocambio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pClase_TpCambio              CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | `ope`=operación · `tipo`=tipo de | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pClase_TpCambio` | `CHAR(1)` | `cambio`=cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodTpCambio` | `CHAR(1)` | L9 |
| `cDescripcion` | `CHAR(40)` | L10 |
| `cClasificacion` | `CHAR(1)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_clase_tc` | `bdinteg` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `?clase` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?clase` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_diasret`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_diasret.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=42 / fan_out=6 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (del día)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicheq` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_diasret(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDiaslib                     CHAR(6)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDiaslib` | `CHAR(6)` | `dia`=del día | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `bInTransaccion` | `BOOLEAN` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L40 |
| `bdicheq` | `bdicnweb` | no | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP dias_ret';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `?sret` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sret` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_eliminamovimientosmetas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_eliminamovimientosmetas.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 122 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimientos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_eliminamovimientosmetas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegElim                     CHAR(500)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegElim` | `CHAR(500)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iIdConRegistro` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | SELECT | L40 |
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | DELETE | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |
| `sp_split_cadena` | `bdicnweb` | no | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?metas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?metas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_escribirarchivodedeclaracionide`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_escribirarchivodedeclaracionide.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación archivo, declaración y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_escribirarchivodedeclaracionide2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_escribirarchivodedeclaracionide(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivo                     CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pArchivo` | `CHAR(20)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cFile` | `CHAR(25)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L25 |
| `sw_verificastatusarchivodeclaracionide` | `bdicnweb` | no | SELECT | L41 |
| `sw_verificastatusarchivodeclaracionide` | `bdicnweb` | no | DELETE | L41 |
| `sw_verificastatusarchivodeclaracionide` | `bdicnweb` | no | INSERT | L44 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_escribirarchivodedeclaracionide2` | `bdilide` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_escribirarchivodedeclaracionide";` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_escribir` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `declaracion` | ENTIDAD | declaración | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_escribir`, `?de`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_formararchivodedeclaracion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_formararchivodedeclaracion.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación archivo y declaración" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_formararchivodedeclaracion2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_formararchivodedeclaracion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaXML                    CHAR(6)
  pDeclaracion                 CHAR(1)
  pTipoDeclaracion             CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaXML` | `CHAR(6)` | — | — |
| `pDeclaracion` | `CHAR(1)` | `declaracion`=declaración | ✅ CÓDIGO |
| `pTipoDeclaracion` | `CHAR(1)` | `declaracion`=declaración | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cArchivo` | `CHAR(20)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L25 |
| `sw_verificastatusformarchivo` | `bdicnweb` | no | SELECT | L44 |
| `sw_verificastatusformarchivo` | `bdicnweb` | no | DELETE | L44 |
| `sw_verificastatusformarchivo` | `bdicnweb` | no | INSERT | L46 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_formararchivodedeclaracion2` | `bdilide` | ⚠️ sí | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP sp_formararchivodedeclaracion";` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `forma` | ACCION | construye / arma | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `declaracion` | ENTIDAD | declaración | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?de` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_gc_altamodificacionsucursaleswuovvg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_gc_altamodificacionsucursaleswuovvg.sql` |
| **LOC (1er CREATE)** | 153 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 104 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal y sw — SoftWare/Switch" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_gc_altamodificacionsucursaleswuovvg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pMarca                       CHAR(2)
  pSucursal                    CHAR(4)
  pFsid                        CHAR(12)
  pAccountNumber               CHAR(9)
  pCounterId                   CHAR(13)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `pMarca` | `CHAR(2)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pFsid` | `CHAR(12)` | — | — |
| `pAccountNumber` | `CHAR(9)` | — | — |
| `pCounterId` | `CHAR(13)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cTipoOperacion` | `CHAR(1)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cMarca` | `CHAR(2)` | L9 |
| `cSucursal` | `CHAR(4)` | L10 |
| `cFsid` | `CHAR(12)` | L11 |
| `cAccountNumber` | `CHAR(9)` | L12 |
| `cCounterId` | `CHAR(13)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `dFechaActual` | `DATETIME YEAR TO SECOND` | L15 |
| `cEmpresaC` | `CHAR(4)` | L17 |
| `cMarcaC` | `CHAR(2)` | L18 |
| `cSucursalC` | `CHAR(4)` | L19 |
| `cFsidC` | `CHAR(12)` | L20 |
| `cAccountNumberC` | `CHAR(9)` | L21 |
| `cCounterIdC` | `CHAR(13)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_wu_identificadores` | `bdisac` | ⚠️ sí | INSERT | L72 |
| `sw_gc_sucursaleswuovvigo_identificadores_hist` | `bdicnweb` | no | INSERT | L75 |
| `sac_wu_identificadores` | `bdisac` | ⚠️ sí | SELECT | L85 |
| `sac_wu_identificadores` | `bdisac` | ⚠️ sí | UPDATE | L89 |
| `sac_wu_identificadores` | `bdisac` | ⚠️ sí | DELETE | L113 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00454';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_gc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `modificacion` | ACCION | modificación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?uovvg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gc_`, `?e`, `?uovvg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_gc_consultasucursaleswuovvg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_gc_consultasucursaleswuovvg.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 103 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal y sw — SoftWare/Switch" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_gc_consultasucursaleswuovvg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cMarca` | `CHAR(2)` | L15 |
| `cSucursal` | `CHAR(4)` | L16 |
| `cFsid` | `CHAR(12)` | L17 |
| `cAccountNumber` | `CHAR(9)` | L18 |
| `cCounterId` | `CHAR(13)` | L19 |
| `cFecha` | `DATE` | L20 |
| `iNoRegistros` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_wu_identificadores` | `bdisac` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_gc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?uovvg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gc_`, `?e`, `sw`, `?uovvg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_gc_consultasucursaleswuovvgreporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_gc_consultasucursaleswuovvgreporte.sql` |
| **LOC (1er CREATE)** | 167 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 102 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal, sw — SoftWare/Switch y reporte" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_gc_consultasucursaleswuovvgreporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pPeriodoDel                  DATE
  pPeriodoAl                   DATE
  pMarcawu                     CHAR(2)
  pMarcaov                     CHAR(2)
  pMarcavg                     CHAR(2)
  pBandera                     CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pPeriodoDel` | `DATE` | — | — |
| `pPeriodoAl` | `DATE` | — | — |
| `pMarcawu` | `CHAR(2)` | — | — |
| `pMarcaov` | `CHAR(2)` | — | — |
| `pMarcavg` | `CHAR(2)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `cSucursal` | `CHAR(4)` | L19 |
| `cSucursalNombre` | `CHAR(40)` | L20 |
| `cMarca` | `CHAR(2)` | L21 |
| `cFsid` | `CHAR(12)` | L22 |
| `cAccountNumber` | `CHAR(9)` | L23 |
| `cCounterId` | `CHAR(13)` | L24 |
| `cStatusOperacion` | `CHAR(12)` | L25 |
| `dFecha` | `DATE` | L26 |
| `cEjecutivo` | `CHAR(8)` | L27 |
| `cNombreEjecutivo` | `CHAR(45)` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_wu_identificadores` | `bdisac` | ⚠️ sí | SELECT | L90 |
| `sw_gc_sucursaleswuovvigo_identificadores_hist` | `bdicnweb` | no | SELECT | L99 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL OR pBandera = '' ` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L74 | VALIDACIÓN_NULL | `IF  pPeriodoDel IS NULL OR pPeriodoAl IS NULL THEN` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L106 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L137 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L143 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_gc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?uovvg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gc_`, `?e`, `?uovvg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_gc_consultasucursaleswuovvgreportetotal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_gc_consultasucursaleswuovvgreportetotal.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 101 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal, sw — SoftWare/Switch y reporte (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_gc_consultasucursaleswuovvgreportetotal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pPeriodoDel                  DATE
  pPeriodoAl                   DATE
  pMarcawu                     CHAR(2)
  pMarcaov                     CHAR(2)
  pMarcavg                     CHAR(2)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pPeriodoDel` | `DATE` | — | — |
| `pPeriodoAl` | `DATE` | — | — |
| `pMarcawu` | `CHAR(2)` | — | — |
| `pMarcaov` | `CHAR(2)` | — | — |
| `pMarcavg` | `CHAR(2)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_wu_identificadores` | `bdisac` | ⚠️ sí | SELECT | L51 |
| `sw_gc_sucursaleswuovvigo_identificadores_hist` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L29 | VALIDACIÓN_NULL | `IF  pPeriodoDel IS NULL OR pPeriodoAl IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_gc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?uovvg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_gc_`, `?e`, `?uovvg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_genera_archivo_img_presentado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_genera_archivo_img_presentado.sql` |
| **LOC (1er CREATE)** | 356 |
| **Callgraph** | ✅ fan_in=78 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación archivo (presentado)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_genera_archivo_img_presentado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoBloque                    INTEGER
  pRutaDescarga                CHAR(50)
  pDireccionMac                CHAR(15)
  totalRegTrunc                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoBloque` | `INTEGER` | — | — |
| `pRutaDescarga` | `CHAR(50)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |
| `totalRegTrunc` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cNombreArchivo` | `CHAR(30)` | L10 |
| `iTotalCheques` | `INTEGER` | L11 |
| `mTotalImporte` | `DECIMAL(20,2)` | L12 |
| `cArchivoAI` | `CHAR(30)` | L13 |
| `cBanco` | `CHAR(3)` | L14 |
| `cMiBanco` | `CHAR(3)` | L15 |
| `pFechaHoy` | `DATE` | L16 |
| `iDayFecha` | `INTEGER` | L17 |
| `iNumBloqueImg` | `INTEGER` | L18 |
| `cNumBloqueImg` | `CHAR(7)` | L19 |
| `cCadenaImgEncabezado` | `CHAR(250)` | L20 |
| `cTipoRegistro` | `CHAR(2)` | L21 |
| `cNumSecuencia` | `CHAR(7)` | L22 |
| `cVersion` | `CHAR(3)` | L23 |
| `cCodOperacion` | `CHAR(2)` | L24 |
| `cSentido` | `CHAR(1)` | L25 |
| `cMoneda` | `CHAR(1)` | L26 |
| `cFechaProceso` | `CHAR(8)` | L27 |
| `cUsoFuturo` | `CHAR(1)` | L28 |
| `cCadenaImgDetalle` | `CHAR(5000)` | L29 |
| *…48 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L211 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L212 |
| `cce_gransumario` | `bditef` | ⚠️ sí | SELECT | L218 |
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L253 |
| `ccep_procesdetalleimg_tmp` | `bdicnweb` | no | SELECT | L257 |
| `ccep_procesdetalleimg_tmp` | `bdicnweb` | no | DELETE | L257 |
| `ccep_procesdetalleimg_tmp` | `bdicnweb` | no | INSERT | L297 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L175 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L169 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNoBloque IS NULL OR pRutaDescarga = '' OR pDireccionMac = ''` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L203 | CÓDIGO_RETORNO | `LET cCodRet = '00780';` |  |
| L278 | FÓRMULA | `LET iContIntercambio = iContIntercambio + 1;` |  |
| L287 | FÓRMULA | `LET cMonto = substr(cImported, 1, CHARINDEX('.', cImported) - 1);` | 🔴 MONEY/aritmética financiera |
| L289 | FÓRMULA | `LET cCents = substr(cImported, CHARINDEX('.', cImported) + 1);` | 🔴 MONEY/aritmética financiera |
| L309 | FÓRMULA | `LET iTotalChq = iTotalChq + 1;` |  |
| L310 | FÓRMULA | `LET mTotalImporte = mTotalImporte + mImporte;` | 🔴 MONEY/aritmética financiera |
| L322 | FÓRMULA | `LET cNumSecuencia = LPAD(TO_CHAR(iContIntercambio + 1),7,'0');` |  |
| L326 | FÓRMULA | `LET cMontos = substr(cImportes, 1, CHARINDEX('.', cImportes) - 1);` | 🔴 MONEY/aritmética financiera |
| L328 | FÓRMULA | `LET cCents = substr(cImportes, CHARINDEX('.', cImportes) + 1);` | 🔴 MONEY/aritmética financiera |
| L340 | FÓRMULA | `LET cCommand = TRIM(cRutaJava)\|\|"/bin/java -jar "\|\|TRIM(cRutaArchivo)\|\|"GeneraArchivoCCEAI.jar` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_img_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `presentado` | MODIF | presentado (a cobro) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_img_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_generarchivopresentado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_generarchivopresentado.sql` |
| **LOC (1er CREATE)** | 546 |
| **Callgraph** | ✅ fan_in=78 / fan_out=15 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación CHI — formato/protocolo de consulta al Buró de Crédito (presentado)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=3 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_generarchivopresentado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoBloque                    INTEGER
  pRutaDescarga                CHAR(50)
  pDireccionMac                CHAR(15)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoBloque` | `INTEGER` | — | — |
| `pRutaDescarga` | `CHAR(50)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNombreArchivo` | `CHAR(30)` | L12 |
| `iExistenImgsDigitalizadas` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iChequeDuplicado` | `SMALLINT` | L15 |
| `dFechaHoy` | `DATE` | L16 |
| `iBloqueInicial` | `SMALLINT` | L17 |
| `bIsChequeDuplicado` | `BOOLEAN` | L18 |
| `cTipoRegistro` | `CHAR(2)` | L19 |
| `iNumeroSecuenca` | `INTEGER` | L20 |
| `cNumeroBanco` | `CHAR(3)` | L21 |
| `cSentidoTransfer` | `CHAR(1)` | L22 |
| `cPlazaCecoban` | `CHAR(2)` | L23 |
| `cServicioTEI` | `CHAR(1)` | L24 |
| `iDiaMesTransfer` | `SMALLINT` | L25 |
| `cFechaPresenta` | `CHAR(8)` | L26 |
| `cUsoFuturo1` | `CHAR(1)` | L27 |
| `cTipoArchivo` | `CHAR(1)` | L28 |
| `cUsoFuturo2` | `CHAR(1)` | L29 |
| `cCveBanco` | `CHAR(3)` | L30 |
| `iNumCheque` | `INTEGER` | L31 |
| *…67 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L211 |
| `cce_encabezado` | `bditef` | ⚠️ sí | INSERT | L315 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L323 |
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L324 |
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L408 |
| `cce_cheques_det` | `bditef` | ⚠️ sí | UPDATE | L451 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ope_reversodetallechequecodigo40` | `bdicnweb` | no | L188 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L204 |
| `cta_alertamiento` | `bditef` | ⚠️ sí | L413 |
| `sp_cce_guardar_detalle` | `bditef` | ⚠️ sí | L440 |
| `sp_cce_guardar_sumario` | `bditef` | ⚠️ sí | L494 |
| `sp_cce_guardar_gransumario` | `bditef` | ⚠️ sí | L524 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L198 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNoBloque IS NULL OR pRutaDescarga = '' OR pDireccionMac = ''` |  |
| L199 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L237 | CÓDIGO_RETORNO | `LET cCodRet = '00780';` |  |
| L281 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |
| L363 | FÓRMULA | `LET cNumSecuencia = LPAD(TO_CHAR(iBloqueInicial + iTotalChqProcesar),7,'0');` |  |
| L371 | FÓRMULA | `LET cMonto = substr(cImported, 1, CHARINDEX('.', cImported) - 1);` | 🔴 MONEY/aritmética financiera |
| L373 | FÓRMULA | `LET cCents = substr(cImported, CHARINDEX('.', cImported) + 1);` | 🔴 MONEY/aritmética financiera |
| L385 | FÓRMULA | `LET iTotalRegTruncados = iTotalRegTruncados + 1;` | 🔴 MONEY/aritmética financiera |
| L394 | VALIDACIÓN_NULL | `IF cRfcCte IS NULL OR  TRIM(cRfcCte) = '' THEN` |  |
| L400 | VALIDACIÓN_NULL | `IF TRIM(cCurpCte) = '' OR cCurpCte IS NULL THEN` |  |
| L435 | FÓRMULA | `LET iTotalCheques = iTotalCheques + 1;` |  |
| L436 | FÓRMULA | `LET mTotalImporte = mTotalImporte + mImporte;` | 🔴 MONEY/aritmética financiera |
| L448 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_detalle';` |  |
| L464 | FÓRMULA | `LET iTotalChqProcesar = iTotalChqProcesar + 1;` |  |
| L475 | FÓRMULA | `LET cNumSecuencia = LPAD(TO_CHAR(iTotalCheques + 2),7,'0');` |  |
| L482 | FÓRMULA | `LET cMontos = substr(cImportes, 1, CHARINDEX('.', cImportes) - 1);` | 🔴 MONEY/aritmética financiera |
| L484 | FÓRMULA | `LET cCents = substr(cImportes, CHARINDEX('.', cImportes) + 1);` | 🔴 MONEY/aritmética financiera |
| L497 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_sumario';` |  |
| L514 | FÓRMULA | `LET cMontos = substr(cImportes, 1, CHARINDEX('.', cImportes) - 1);` | 🔴 MONEY/aritmética financiera |
| L516 | FÓRMULA | `LET cCents = substr(cImportes, CHARINDEX('.', cImportes) + 1);` | 🔴 MONEY/aritmética financiera |
| L529 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_cce_guardar_gransumario';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?vo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `presentado` | MODIF | presentado (a cobro) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?vo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_generarepordpago`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_generarepordpago.sql` |
| **LOC (1er CREATE)** | 325 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 86 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación pago" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 10 tabla(s) con operaciones: DELETE, INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_generarepordpago(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iIsamErr` | `INTEGER` | L7 |
| `cDescErr` | `CHAR(50)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cDescCodRet` | `CHAR(80)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cBanDetError` | `CHAR(1)` | L13 |
| `cDesCodRet` | `CHAR(250)` | L14 |
| `dFechaHoraInicio` | `DATETIME YEAR TO FRACTION(5)` | L15 |
| `dFechaHoraFin` | `DATETIME YEAR TO FRACTION(5)` | L16 |
| `dFechaInicio` | `DATE` | L17 |
| `dFechaFin` | `DATE` | L18 |
| `dFecha` | `DATE` | L19 |
| `cHora` | `CHAR(8)` | L20 |
| `cMes` | `CHAR(10)` | L21 |
| `cAnio` | `CHAR(4)` | L22 |
| `cRutaGral` | `CHAR(150)` | L23 |
| `cDias` | `CHAR(5)` | L25 |
| `dFechaHoy` | `DATE` | L26 |
| `cHoraHoy` | `CHAR(6)` | L27 |
| `cFechaHoraArchivo` | `CHAR(15)` | L28 |
| `cNombreArchivo` | `CHAR(50)` | L29 |
| `cNombreArchivoHist` | `CHAR(50)` | L30 |
| *…11 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sysshmvals` | `sysmaster` | ⚠️ sí | SELECT | L89 |
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L147 |
| `sw_ope_statusrepordpago` | `bdicnweb` | no | SELECT | L157 |
| `sw_ope_statusrepordpago` | `bdicnweb` | no | DELETE | L157 |
| `sw_ope_statusrepordpago` | `bdicnweb` | no | INSERT | L160 |
| `sw_ope_repgenerados` | `bdicnweb` | no | SELECT | L171 |
| `sw_ope_repgenerados` | `bdicnweb` | no | DELETE | L178 |
| `sac_enviosdineroya` | `bdisac` | ⚠️ sí | SELECT | L192 |
| `systables` | `bdicnweb` | no | SELECT | L213 |
| `sw_ope_repgenerados` | `bdicnweb` | no | INSERT | L302 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L115 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRutaDescarga = ` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L153 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L175 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRutaDescarga)\|\|TRIM(cNombreArchivoHist);` |  |
| L200 | CÓDIGO_RETORNO | `LET cCodRet = '01053';` |  |
| L214 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM (SELECT TO_CHAR(a.fecha_envio, '%d/%m/%Y') AS dia, b` | 🔴 MONEY/aritmética financiera |
| L218 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" TRIM(c.descripcion) AS desc_estatus, TO_CHAR(a.fecha_envio + "\|\|` |  |
| L247 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRutaDescarga)\|\|'query.sql';` |  |
| L256 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L261 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L270 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L279 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L287 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_generareporteespantad`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_generareporteespantad.sql` |
| **LOC (1er CREATE)** | 167 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (especial, anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_generareporteespantad(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCmd1` | `CHAR(3500)` | L6 |
| `cSql` | `CHAR(3500)` | L7 |
| `cRutaInformix` | `CHAR(100)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreReporte` | `CHAR(100)` | L10 |
| `dFechaHoy` | `DATE` | L11 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L12 |
| `bInTransaction` | `BOOLEAN` | L13 |
| `ven_transacc` | `SMALLINT` | L14 |
| `iTotal` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepantad` | `bdicnweb` | no | SELECT | L65 |
| `sw_verificastatusrepantad` | `bdicnweb` | no | DELETE | L65 |
| `sw_verificastatusrepantad` | `bdicnweb` | no | INSERT | L66 |
| `sw_consdetallereportesolsupmc` | `bdicnweb` | no | SELECT | L75 |
| `sw_consdetallereportesolsupmc` | `bdicnweb` | no | DELETE | L75 |
| `systables` | `bdicnweb` | no | SELECT | L100 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRutaDescarga = '' OR pFechaInicio IS NULL OR pFechaFin IS NU` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L101 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT 'DETALLE DE COBRANZA POR OPERACION - CONCILIACION` |  |
| L103 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL  SELECT 'Periodo del : " \|\| TO_CHAR( pFechaInicio, "%d` |  |
| L105 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM (SELECT ''''\|\| CAST(A.id_sucursal AS CHA` | 🔴 MONEY/aritmética financiera |
| L120 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?ad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te`, `?ad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_genreporteaplicaciondev`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_genreporteaplicaciondev.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ✅ fan_in=5 / fan_out=6 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_genreporteaplicaciondev(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `iSqlErr` | `INTEGER` | L20 |
| `cClaveBanco` | `CHAR(3)` | L21 |
| `cDesBanco` | `CHAR(40)` | L22 |
| `cNumCuenta` | `CHAR(20)` | L23 |
| `cNumCheque` | `CHAR(7)` | L24 |
| `dMonto` | `DECIMAL(16,2)` | L25 |
| `cCtaDeposito` | `CHAR(20)` | L26 |
| `cNumCliente` | `CHAR(20)` | L27 |
| `cRazonSocial` | `CHAR(60)` | L28 |
| `cSegundoNombre` | `CHAR(26)` | L29 |
| `cApePaterno` | `CHAR(26)` | L30 |
| `cApeMaterno` | `CHAR(26)` | L31 |
| `cPrimerNombre` | `CHAR(26)` | L32 |
| `cDescripcion` | `CHAR(35)` | L33 |
| `cMotivo` | `CHAR(2)` | L34 |
| `cCodRetDev` | `CHAR(5)` | L35 |
| `cDesCodRetDev` | `CHAR(50)` | L36 |
| `iNoRegistros` | `INTEGER` | L37 |
| `iRegistros` | `INTEGER` | L38 |
| `iRecuperacion` | `INTEGER` | L39 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_cheques_dev` | `bditef` | ⚠️ sí | SELECT | L94 |
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L100 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L105 |
| `si_coddevcam` | `bdinteg` | ⚠️ sí | SELECT | L110 |
| `si_codret` | `bdinteg` | ⚠️ sí | SELECT | L118 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L73 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L122 | VALIDACIÓN_NULL | `IF cDesCodRetDev IS NULL OR cDesCodRetDev = '' THEN` |  |
| L127 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dev` | ACCION | devolución | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_genreportedevpresentado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_genreportedevpresentado.sql` |
| **LOC (1er CREATE)** | 118 |
| **Callgraph** | ✅ fan_in=5 / fan_out=6 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (presentado)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_genreportedevpresentado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cClaveBanco` | `CHAR(3)` | L16 |
| `cDesBanco` | `CHAR(40)` | L17 |
| `cNumCuenta` | `CHAR(20)` | L18 |
| `cNumCheque` | `CHAR(7)` | L19 |
| `dMonto` | `DECIMAL(16,2)` | L20 |
| `cCtaDeposito` | `CHAR(20)` | L21 |
| `cNumCliente` | `CHAR(20)` | L22 |
| `cSucursal` | `CHAR(60)` | L23 |
| `cSucursales` | `CHAR(4)` | L24 |
| `cDesSucursal` | `CHAR(40)` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |
| `iRegistros` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_cheques_dev` | `bditef` | ⚠️ sí | SELECT | L78 |
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L84 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L89 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L56 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L95 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dev` | ACCION | devolución | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `presentado` | MODIF | presentado (a cobro) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_genreportediariodev`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_genreportediariodev.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=15 / fan_out=35 |
| **Deps concatenadas** | 53 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (diario)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `cons_dev_coppel2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_genreportediariodev(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaDev                    DATE
  pCuenta1                     CHAR(20)
  pCuenta2                     CHAR(20)
  pTransDev                    CHAR(4)
  pTranCom                     CHAR(4)
  pTransIva                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaDev` | `DATE` | `dev`=devolución | 🔵 CONVENCIÓN |
| `pCuenta1` | `CHAR(20)` | — | — |
| `pCuenta2` | `CHAR(20)` | — | — |
| `pTransDev` | `CHAR(4)` | `dev`=devolución | 🔵 CONVENCIÓN |
| `pTranCom` | `CHAR(4)` | — | — |
| `pTransIva` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cSucursal` | `CHAR(45)` | L16 |
| `cBanco` | `CHAR(100)` | L17 |
| `cNumcheque` | `CHAR(7)` | L18 |
| `dImporte` | `DECIMAL(16,2)` | L19 |
| `dComision` | `DECIMAL(8,2)` | L20 |
| `dIva` | `DECIMAL(8,2)` | L21 |
| `cMotivodev` | `CHAR(45)` | L22 |
| `iNoRegistros` | `INTEGER` | L23 |
| `iRegistros` | `INTEGER` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |
| `cons_dev_coppel2` | `bditef` | ⚠️ sí | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaDev IS NULL  OR pCuenta1 = '' OR pCuenta2 = '' OR pTran` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bditef:cons_dev_coppel2';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `diario` | MODIF | diario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_genreporteimagenchqs`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_genreporteimagenchqs.sql` |
| **LOC (1er CREATE)** | 145 |
| **Callgraph** | ✅ fan_in=22 / fan_out=31 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación imagen digital y cheque" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cce_reporterevimg2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_genreporteimagenchqs(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 INTEGER
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pNumEjecut                   CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `INTEGER` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pNumEjecut` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `cCodRetSp` | `CHAR(5)` | L19 |
| `iCodRetSp` | `INTEGER` | L20 |
| `iNumEjecutivo` | `INTEGER` | L21 |
| `cCheque` | `CHAR(7)` | L22 |
| `dTiempoInicio` | `DATETIME HOUR TO SECOND` | L23 |
| `dTiempoFinal` | `DATETIME HOUR TO SECOND` | L24 |
| `dIndicadorRevisado` | `CHAR(1)` | L25 |
| `dTiempoTotal` | `DATETIME HOUR TO SECOND` | L26 |
| `dFechaRevision` | `DATE` | L27 |
| `iRevisionTotal` | `INTEGER` | L28 |
| `iNoRevisionTotal` | `INTEGER` | L29 |
| `iNoRegistros` | `INTEGER` | L30 |
| `iRegistros` | `INTEGER` | L31 |
| `iRecuperacion` | `INTEGER` | L32 |
| `cNoRevisados` | `DATETIME HOUR TO SECOND` | L33 |
| `cRevisados` | `DATETIME HOUR TO SECOND` | L34 |
| `iTotalCheques` | `INTEGER` | L35 |
| `iTotalOperadores` | `INTEGER` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_cheques_revisados` | `bditef` | ⚠️ sí | SELECT | L107 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L81 |
| `sp_cce_reporterevimg2` | `bdicheq` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pTipoReporte IS NULL  OR pRegistros IS NULL OR pRecuperacion` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicheq:sp_cce_reporterevimg2';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L129 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imagen` | ENTIDAD | imagen digital | 🔵 CONVENCIÓN | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_genreportemovimientosatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_genreportemovimientosatms.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=29 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimientos y cajeros automáticos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_reporte_mov_atm2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_genreportemovimientosatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pTipoMov                     CHAR(15)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTipoMov` | `CHAR(15)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(6)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cCentroCosto` | `CHAR(4)` | L17 |
| `cNombreAtms` | `CHAR(40)` | L18 |
| `cTipoMovimiento` | `CHAR(15)` | L19 |
| `dFecha` | `DATE` | L20 |
| `cProveedor` | `CHAR(4)` | L21 |
| `cCajaGen` | `CHAR(40)` | L22 |
| `cUsuario` | `CHAR(8)` | L23 |
| `cNombreUsuario` | `CHAR(45)` | L24 |
| `iNoRegistros` | `INTEGER` | L25 |
| `iRegistros` | `INTEGER` | L26 |
| `iRecuperacion` | `INTEGER` | L27 |
| `cMensaje` | `CHAR(50)` | L28 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L69 |
| `sp_soc_reporte_mov_atm2` | `bdisuc` | ⚠️ sí | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L57 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL  OR pFechaFin IS NULL OR pTipoMov = '' O` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L81 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_reporte_mov_atm2";` |  |
| L84 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_genreportemovimientosatms_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_genreportemovimientosatms_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación movimientos y cajeros automáticos (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_reporte_mov_atm2_totales` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_genreportemovimientosatms_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pTipoMov                     CHAR(15)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTipoMov` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `sp_soc_reporte_mov_atm2_totales` | `bdisuc` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pFechaInicio IS NULL OR pFechaFin IS NULL OR pTipoMov = '' T` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_reporte_mov_atm2_totales";` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_grabaimagenchqsdevueltos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_grabaimagenchqsdevueltos.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=52 / fan_out=39 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación imagen digital, cheque, sd — saldo disponible y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_grabaimageneschqdevueltos` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=4 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_grabaimagenchqsdevueltos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodBanco                    CHAR(3)
  pCuenta                      CHAR(20)
  pNumCheque                   CHAR(7)
  pFechaPresenta               DATE
  pArchivo                     CHAR(100)
  pLado                        CHAR(1)
  pStatusImg                   SMALLINT
  pLimpia                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodBanco` | `CHAR(3)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | — | — |
| `pFechaPresenta` | `DATE` | — | — |
| `pArchivo` | `CHAR(100)` | — | — |
| `pLado` | `CHAR(1)` | — | — |
| `pStatusImg` | `SMALLINT` | — | — |
| `pLimpia` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cMensaje` | `CHAR(50)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_grabaimageneschqdevueltos` | `bditef` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pCodBanco = '' OR  pCuenta = '' OR  pNumCheque= '' OR  pFech` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_grabaimageneschqdevueltos';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `imagen` | ENTIDAD | imagen digital | 🔵 CONVENCIÓN | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | 🟡 INFERIDO | nombre_sp |
| `?evuelt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?evuelt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_guardamanttometassaldos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_guardamanttometassaldos.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ss — subsistema y OS — Originación de Solicitudes (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_guardamanttometassaldos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMontoCap                    DECIMAL(18,2)
  pMontoColocacion             DECIMAL(18,2)
  pTipoSuc                     INTEGER
  pAnioMes                     CHAR(6)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMontoCap` | `DECIMAL(18,2)` | — | — |
| `pMontoColocacion` | `DECIMAL(18,2)` | — | — |
| `pTipoSuc` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pAnioMes` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cNombre` | `CHAR(45)` | L6 |
| `iNoRegistroBitacora` | `INTEGER` | L7 |
| `iIdRegistroBitacora` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `iIdRegistro` | `INTEGER` | L10 |
| `cCambio` | `CHAR(500)` | L11 |
| `cMovimiento` | `CHAR(500)` | L12 |
| `cCambioBitacora` | `CHAR(500)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | SELECT | L52 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | SELECT | L67 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L81 |
| `mi_tiposuc` | `bdmis` | ⚠️ sí | UPDATE | L86 |
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | INSERT | L93 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | INSERT | L96 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pMontoCap IS NULL OR pMontoColocacion IS NULL OR pTipoSuc IS ` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | FÓRMULA | `LET iIdRegistro = iIdRegistro + 1;` |  |
| L76 | FÓRMULA | `LET iIdRegistroBitacora = iIdRegistroBitacora + 1;` |  |
| L86 | FÓRMULA | `LET cCambio = "Actividad realizada: update bdmis:mi_tiposuc set meta_monto_cap = *" \|\| pMontoCap \` | 🔴 MONEY/aritmética financiera |
| L87 | FÓRMULA | `LET cMovimiento = "Actualización Modulo Metas Saldo, Monto Captacion: " \|\| pMontoCap \|\| ", Monto` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?tometa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ald` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?tometa`, `ss`, `?ald` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_guardametacumplimiento`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_guardametacumplimiento.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_guardametacumplimiento(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pParametro                   INTEGER
  pValor                       DECIMAL(16,2)
  pDescripcion                 CHAR(60)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pParametro` | `INTEGER` | — | — |
| `pValor` | `DECIMAL(16,2)` | — | — |
| `pDescripcion` | `CHAR(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNombre` | `CHAR(45)` | L7 |
| `iNoRegistroBitacora` | `INTEGER` | L8 |
| `iIdRegistroBitacora` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iIdRegistro` | `INTEGER` | L11 |
| `cCambio` | `CHAR(500)` | L12 |
| `cMovimiento` | `CHAR(500)` | L13 |
| `cCambioBitacora` | `CHAR(500)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | SELECT | L53 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | SELECT | L68 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L82 |
| `mi_paramcump` | `bdmis` | ⚠️ sí | UPDATE | L87 |
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | INSERT | L94 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | INSERT | L97 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pValor IS NULL OR pParametro IS NULL OR pDescripcion = '' THE` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iIdRegistro = iIdRegistro + 1;` |  |
| L77 | FÓRMULA | `LET iIdRegistroBitacora = iIdRegistroBitacora + 1;` |  |
| L87 | FÓRMULA | `LET cCambio = "Actividad realizada: update bdmis:mi_paramcump set valor = *" \|\| pValor \|\| "* whe` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?metacumplimiento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?metacumplimiento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_guardametaproducto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_guardametaproducto.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 121 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación producto" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_guardametaproducto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSuc                     INTEGER
  pMeta                        DECIMAL(16,2)
  pAnioMes                     CHAR(6)
  pProducto                    CHAR(4)
  pDescripcion                 CHAR(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSuc` | `INTEGER` | — | — |
| `pMeta` | `DECIMAL(16,2)` | — | — |
| `pAnioMes` | `CHAR(6)` | — | — |
| `pProducto` | `CHAR(4)` | `producto`=producto | ✅ CÓDIGO |
| `pDescripcion` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNombre` | `CHAR(45)` | L7 |
| `iNoRegistroBitacora` | `INTEGER` | L8 |
| `iIdRegistroBitacora` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `iIdRegistro` | `INTEGER` | L11 |
| `cCambio` | `CHAR(500)` | L12 |
| `cMovimiento` | `CHAR(500)` | L13 |
| `cCambioBitacora` | `CHAR(500)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | SELECT | L52 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | SELECT | L67 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L81 |
| `mi_metasprod` | `bdmis` | ⚠️ sí | UPDATE | L86 |
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | INSERT | L93 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | INSERT | L96 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSuc IS NULL OR pMeta IS NULL  OR pAnioMes = '' OR pProdu` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | FÓRMULA | `LET iIdRegistro = iIdRegistro + 1;` |  |
| L76 | FÓRMULA | `LET iIdRegistroBitacora = iIdRegistroBitacora + 1;` |  |
| L86 | FÓRMULA | `LET cCambio = "Actividad realizada: update  bdmis:mi_metasprod set metanum = *" \|\| pMeta \|\| "* w` |  |
| L87 | FÓRMULA | `LET cMovimiento = "Actualización de Meta, Modulo Metas Mantenimiento, Producto: " \|\| pDescripcion ` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?meta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `producto` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?meta` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_guardatiposucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_guardatiposucursal.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 120 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_guardatiposucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSuc                     INTEGER
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSuc` | `INTEGER` | `tipo`=tipo de | ✅ CÓDIGO |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cNombre` | `CHAR(45)` | L6 |
| `iNoRegistroBitacora` | `INTEGER` | L7 |
| `iIdRegistroBitacora` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `iIdRegistro` | `INTEGER` | L10 |
| `cCambio` | `CHAR(500)` | L11 |
| `cMovimiento` | `CHAR(500)` | L12 |
| `cCambioBitacora` | `CHAR(500)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | SELECT | L51 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | SELECT | L66 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L80 |
| `mi_sucursalesinfo` | `bdmis` | ⚠️ sí | UPDATE | L85 |
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | INSERT | L92 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | INSERT | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSucursal = '' OR pTipoSuc IS NULL THEN` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | FÓRMULA | `LET iIdRegistro = iIdRegistro + 1;` |  |
| L75 | FÓRMULA | `LET iIdRegistroBitacora = iIdRegistroBitacora + 1;` |  |
| L85 | FÓRMULA | `LET cCambio = "Actividad realizada: update bdmis:mi_sucursalesinfo set tipo_suc = *" \|\| pTipoSuc \` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_insertacuentaspei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_insertacuentaspei.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_insertacuentaspei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pClabe                       CHAR(20)
  pTarjeta                     CHAR(18)
) RETURNING CHAR(5)  AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pClabe` | `CHAR(20)` | — | — |
| `pTarjeta` | `CHAR(18)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iId` | `INTEGER` | L6 |
| `iCuentas` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdibi` | `bdicnweb` | no | INSERT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | VALIDACIÓN_NULL | `IF iId IS NULL THEN` |  |
| L58 | FÓRMULA | `LET iId = iId + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00350';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pei` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pei` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_insertasolicitudtdcmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_insertasolicitudtdcmasivo.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 119 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación solicitud y tarjeta de crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_insertasolicitudtdcmasivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArch                  CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArch` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cSucursal` | `CHAR(4)` | L7 |
| `dMeta_Diaria` | `DECIMAL(3,2)` | L8 |
| `cNombre` | `CHAR(45)` | L9 |
| `iIdRegistroBitacora` | `INTEGER` | L10 |
| `cCambioBitacora` | `CHAR(500)` | L11 |
| `cNombreSucursal` | `CHAR(40)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_bitacoraerrorescargarchivo` | `bdicnweb` | no | SELECT | L46 |
| `sw_ope_bitacoraerrorescargarchivo` | `bdicnweb` | no | DELETE | L46 |
| `sw_ope_cargarchivosolicitudtdc` | `bdicnweb` | no | SELECT | L54 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | SELECT | L60 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L65 |
| `mi_metascoppel` | `bdmis` | ⚠️ sí | INSERT | L70 |
| `mi_metascoppel` | `bdmis` | ⚠️ sí | SELECT | L71 |
| `mi_metascoppel` | `bdmis` | ⚠️ sí | UPDATE | L73 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | INSERT | L78 |
| `sw_ope_nombrearchivosmetasolicitudtdc` | `bdicnweb` | no | SELECT | L93 |
| `sw_ope_nombrearchivosmetasolicitudtdc` | `bdicnweb` | no | INSERT | L94 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | FÓRMULA | `LET cCambioBitacora = "Actividad realizada: insert into bdmis:mi_metascoppel (fecha, sucursal, produ` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_insertatiposucursalmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_insertatiposucursalmasivo.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 118 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal (tipo de, masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_insertatiposucursalmasivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cSucursal` | `CHAR(4)` | L7 |
| `cTipoSuc` | `CHAR(2)` | L8 |
| `cNombre` | `CHAR(45)` | L9 |
| `iIdRegistroBitacora` | `INTEGER` | L10 |
| `iIdRegistro` | `INTEGER` | L11 |
| `cCambio` | `CHAR(500)` | L12 |
| `cMovimiento` | `CHAR(500)` | L13 |
| `cCambioBitacora` | `CHAR(500)` | L14 |
| `cNombreSucursal` | `CHAR(40)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_bitacoraerrorescargarchivo` | `bdicnweb` | no | SELECT | L52 |
| `sw_ope_bitacoraerrorescargarchivo` | `bdicnweb` | no | DELETE | L52 |
| `sw_ope_cargarchivotiopsolicitud` | `bdicnweb` | no | SELECT | L59 |
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | SELECT | L64 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | SELECT | L68 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L72 |
| `mi_sucursalesinfo` | `bdmis` | ⚠️ sí | UPDATE | L77 |
| `sw_ope_bitacoraerrorescargarchivo` | `bdicnweb` | no | INSERT | L81 |
| `mi_movimientosmetas` | `bdmis` | ⚠️ sí | INSERT | L85 |
| `mi_bitacora_metas` | `bdmis` | ⚠️ sí | INSERT | L88 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | FÓRMULA | `LET cCambio = "Actividad realizada: update bdmis:mi_sucursalesinfo set tipo_suc = *" \|\| cTipoSuc \` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_insertconssucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_insertconssucursal.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación TCO — Tarjetas Coppel y ss — subsistema" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: UPDATE, INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_insertconssucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoOperacion               CHAR(1)
  pSucursal                    CHAR(4)
  pMonto                       MONEY(18,2)
  pNumTransacciones            INT
  pPlazo                       INT
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `pSucursal` | `CHAR(4)` | — | — |
| `pMonto` | `MONEY(18,2)` | — | — |
| `pNumTransacciones` | `INT` | — | — |
| `pPlazo` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cDescCodRet` | `CHAR(80)` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cSucursal` | `CHAR(4)` | L15 |
| `mMonto` | `MONEY(18,2)` | L16 |
| `iNumTransacciones` | `INT` | L17 |
| `iPlazo` | `INT` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_limitedeposito` | `bdicheq` | ⚠️ sí | INSERT | L65 |
| `sc_limitedeposito` | `bdicheq` | ⚠️ sí | UPDATE | L75 |
| `sc_limitedeposito` | `bdicheq` | ⚠️ sí | SELECT | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | VALIDACIÓN_NULL | `IF pMonto IS NULL OR pNumTransacciones IS NULL OR pPlazo IS NULL THEN` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?er` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tco` | ENTIDAD | TCO — Tarjetas Coppel / TCoppel (producto de crédito Grupo C | 🔵 CONVENCIÓN | nombre_sp |
| `?n` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ucursal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?er`, `?n`, `ss`, `?ucursal` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_liberasalret`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_liberasalret.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=42 / fan_out=6 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicheq` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_liberasalret(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cIdDevolucion` | `CHAR(2)` | L8 |
| `cDescipcionMotivo` | `CHAR(70)` | L9 |
| `bInTransaccion` | `BOOLEAN` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusliberaret` | `bdicnweb` | no | UPDATE | L26 |
| `sw_verificastatusliberaret` | `bdicnweb` | no | SELECT | L40 |
| `sw_verificastatusliberaret` | `bdicnweb` | no | DELETE | L40 |
| `sw_verificastatusliberaret` | `bdicnweb` | no | INSERT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L53 |
| `bdicheq` | `bdicnweb` | no | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP liberasalret';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_liberasalret` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_liberasalret` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_manteminientoatms`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_manteminientoatms.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cajeros automáticos (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_soc_modifica_atm` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_manteminientoatms(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pIdAtms                      CHAR(8)
  pCajaGen                     CHAR(3)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pIdAtms` | `CHAR(8)` | `atms`=cajeros automáticos (ATM) | 🔵 CONVENCIÓN |
| `pCajaGen` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cSucursal` | `CHAR(4)` | L10 |
| `cMensaje` | `CHAR(50)` | L11 |
| `cBitacora` | `CHAR(6)` | L12 |
| `cMsgBitacora` | `CHAR(50)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_soc_modifica_atm` | `bdisuc` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisuc.sp_soc_modifica_atm";` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "El ATM cuenta con efectivo, favor de validar";` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00454';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?eminiento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `atms` | ENTIDAD | cajeros automáticos (ATM) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?eminiento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_obtienecheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_obtienecheques.sql` |
| **LOC (1er CREATE)** | 245 |
| **Callgraph** | ✅ fan_in=25 / fan_out=22 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheques" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_actualizastatusmonitorproceso`, `sp_cnsif_confirmaejecutivo`, `sp_obtienecheques2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_obtienecheques(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBanco                       CHAR(3)
  pCuenta                      CHAR(20)
  pNumCheque                   CHAR(7)
  pFechaPresenta               DATE
  pMac                         CHAR(18)
  pIp                          VARCHAR(16)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBanco` | `CHAR(3)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | — | — |
| `pFechaPresenta` | `DATE` | — | — |
| `pMac` | `CHAR(18)` | — | — |
| `pIp` | `VARCHAR(16)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `cCodRetSp` | `CHAR(5)` | L24 |
| `iCodRetSp` | `INTEGER` | L25 |
| `cEmpresa` | `CHAR(3)` | L26 |
| `cClaveBanco` | `CHAR(3)` | L27 |
| `cDescripcion` | `CHAR(40)` | L28 |
| `cCuenta` | `CHAR(20)` | L29 |
| `cNumCheque` | `CHAR(7)` | L30 |
| `dFechaALta` | `DATE` | L31 |
| `dFechaPresenta` | `DATE` | L32 |
| `cUsuario` | `CHAR(8)` | L33 |
| `cProducto` | `CHAR(4)` | L34 |
| `cCliente` | `CHAR(20)` | L35 |
| `cNombreCliente` | `CHAR(104)` | L36 |
| `cNombreProducto` | `CHAR(40)` | L37 |
| `cCuentaDep` | `CHAR(20)` | L38 |
| `cRevisado` | `CHAR(1)` | L39 |
| `cEjecutivoReviso` | `CHAR(8)` | L40 |
| `dMonto` | `DECIMAL(16,2)` | L41 |
| `cSucursal` | `CHAR(4)` | L42 |
| `cRevisadoStado` | `CHAR(9)` | L43 |
| `iRecuperacion` | `INTEGER` | L44 |
| `iTotalRegistrosRevisados` | `INTEGER` | L45 |
| `iHayDatos` | `INTEGER` | L46 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L126 |
| `sw_cp_obtienecheques` | `bdicnweb` | no | INSERT | L179 |
| `sw_cp_obtienecheques` | `bdicnweb` | no | SELECT | L187 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_actualizastatusmonitorproceso` | `bdicnweb` | no | L80 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L111 |
| `sp_obtienecheques2` | `bditef` | ⚠️ sí | L155 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L161 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_obtienecheques2';` |  |
| L178 | FÓRMULA | `LET iIdConsecutivo = iIdConsecutivo + 1;` |  |
| L215 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L221 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_obtienecheques_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_obtienecheques_totales.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=25 / fan_out=20 |
| **Deps concatenadas** | 41 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheques (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienecheques2_totales` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_obtienecheques_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBanco                       CHAR(3)
  pCuenta                      CHAR(20)
  pNumCheque                   CHAR(7)
  pFechaPresenta               DATE
  pMacAdress                   CHAR(18)
  pDireccionIp                 CHAR(16)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBanco` | `CHAR(3)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | — | — |
| `pFechaPresenta` | `DATE` | — | — |
| `pMacAdress` | `CHAR(18)` | — | — |
| `pDireccionIp` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iTotalRegistrosRevisados` | `INTEGER` | L11 |
| `iTotalRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_obtienecheques2_totales` | `bditef` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_obtienecheques2_totales';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_obtienefuncionalidadesmetas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_obtienefuncionalidadesmetas.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 117 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_obtienefuncionalidadesmetas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iIdCatalogo` | `INTEGER` | L10 |
| `idFuncion` | `CHAR(10)` | L11 |
| `cDescCatalogo` | `CHAR(100)` | L12 |
| `idSubmodulo` | `INTEGER` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_catalogometafunciones` | `bdicnweb` | no | SELECT | L50 |
| `si_seg_usuarios_funciones` | `bdinteg` | ⚠️ sí | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00749';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?funcional` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?adesmetas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?funcional`, `?adesmetas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_obtienenombrempresa`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_obtienenombrempresa.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación nombre" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_obtienenombrempresa(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cDescripcion` | `CHAR(50)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | UPDATE | L21 |
| `sw_bitacoraprocedimientoside` | `bdicnweb` | no | INSERT | L35 |
| `sl_parametros` | `bdilide` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?mpresa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mpresa` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_obtieneparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_obtieneparam.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=63 |
| **Deps concatenadas** | 166 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación parámetro" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_dinya_obtieneparam` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_obtieneparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iCodRetSp` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cLongitudCliente` | `CHAR(2)` | L18 |
| `cCodMonNac` | `CHAR(2)` | L19 |
| `cPathRep` | `CHAR(100)` | L20 |
| `cNombreUsuario` | `CHAR(45)` | L21 |
| `cNombreEmpresa` | `CHAR(30)` | L22 |
| `dFecha_Hoy` | `DATE` | L23 |
| `cSistema` | `CHAR(2)` | L24 |
| `cLongitudNoControl` | `CHAR(5)` | L25 |
| `cLongitudCuenta` | `CHAR(5)` | L26 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |
| `sp_dinya_obtieneparam` | `bdisac` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP: bdisac:sp_dinya_obtieneparam";` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_pagoprogtipopagos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_pagoprogtipopagos.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=55 |
| **Deps concatenadas** | 150 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación pago, ro — Rol Operativo y pagos (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_pagoprogtipopagos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cIdPago` | `CHAR(2)` | L10 |
| `cDescPago` | `CHAR(70)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_tppago` | `bdiprog` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00960';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `pagos` | ENTIDAD | pagos (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_pagoprogtiporep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_pagoprogtiporep.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=55 |
| **Deps concatenadas** | 151 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación pago y ro — Rol Operativo (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_pagoprogtiporep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cIdReporte` | `CHAR(2)` | L10 |
| `cDescReporte` | `CHAR(45)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_tiporeportepagoprog` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `pago` | ENTIDAD | pago | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_reportecodigo46`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_reportecodigo46.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación reporte y código" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_reportecodigo46(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |
| `cNumerocuenta` | `CHAR(20)` | L16 |
| `cNumerocheque` | `CHAR(7)` | L17 |
| `dMonto` | `DECIMAL(16,2)` | L18 |
| `cCuentaDeposito` | `CHAR(20)` | L19 |
| `cNumcte` | `CHAR(20)` | L20 |
| `cRazonSocial` | `CHAR(60)` | L21 |
| `cNombre2` | `CHAR(26)` | L22 |
| `cApellidoPaterno` | `CHAR(26)` | L23 |
| `cApellidoMaterno` | `CHAR(26)` | L24 |
| `cNombre1` | `CHAR(26)` | L25 |
| `cCodigoRetorno` | `CHAR(5)` | L26 |
| `cCliente` | `CHAR(130)` | L27 |
| `cBanco` | `CHAR(43)` | L28 |
| `cMotivoDevolucion` | `CHAR(40)` | L29 |
| `cAplicado` | `CHAR(54)` | L30 |
| `cMotivo` | `CHAR(2)` | L31 |
| `cDescripcionMotivo` | `CHAR(35)` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_codret` | `bdinteg` | ⚠️ sí | SELECT | L113 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `codigo` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?46` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?46` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_reportectaclave`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_reportectaclave.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ✅ fan_in=0 / fan_out=66 |
| **Deps concatenadas** | 175 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación reporte, cuenta y clave" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_reportectaclave(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pOperacion                   CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `cFecha` | `CHAR(10)` | L18 |
| `cCuentaOrd` | `CHAR(18)` | L19 |
| `cBanco` | `CHAR(3)` | L20 |
| `cDescripcionBanco` | `CHAR(40)` | L21 |
| `cCuentaBenef` | `CHAR(18)` | L22 |
| `cNumCte` | `CHAR(20)` | L23 |
| `cNombre1` | `CHAR(26)` | L24 |
| `cNombre2` | `CHAR(26)` | L25 |
| `cApePaterno` | `CHAR(26)` | L26 |
| `cApeMaterno` | `CHAR(26)` | L27 |
| `cRazonSocial` | `CHAR(60)` | L28 |
| `cCuenta` | `CHAR(20)` | L29 |
| `iNoRegistros` | `INTEGER` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tblintfallo` | `bdispei` | ⚠️ sí | SELECT | L80 |
| `tblclabebloqueo` | `bdispei` | ⚠️ sí | SELECT | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet     = '00000';` |  |
| L58 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio='' OR pFechaFin=''  OR pOperacion='' OR pRegistr` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L86 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L99 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `clave` | ENTIDAD | clave | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_reporteprogramacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_reporteprogramacion.sql` |
| **LOC (1er CREATE)** | 226 |
| **Callgraph** | ✅ fan_in=0 / fan_out=55 |
| **Deps concatenadas** | 147 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación reporte, ro — Rol Operativo y dirección MAC" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_reporteprogramacion2`, `sp_reporteprogramacionsegundo2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_reporteprogramacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(2)
  pNumcte                      CHAR(20)
  pCvePago                     CHAR(2)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pOperacion                   CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(2)` | `reporte`=reporte | ✅ CÓDIGO |
| `pNumcte` | `CHAR(20)` | — | — |
| `pCvePago` | `CHAR(2)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `cCodRetSp` | `CHAR(6)` | L19 |
| `iCodRetSp` | `INTEGER` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `iRecuperacion` | `INTEGER` | L22 |
| `cMensajeRet` | `CHAR(60)` | L23 |
| `dFechaSolicitud` | `DATE` | L24 |
| `dFechaAplicacion` | `DATE` | L25 |
| `cSucursal` | `CHAR(100)` | L26 |
| `cPeriodoPago` | `CHAR(10)` | L27 |
| `cMedioSolicitud` | `CHAR(10)` | L28 |
| `cCuentaOrigen` | `CHAR(20)` | L29 |
| `cCuentaDestino` | `CHAR(20)` | L30 |
| `mMonto` | `MONEY(16,2)` | L31 |
| `cTipoPagoDesc` | `CHAR(60)` | L32 |
| `cAviso` | `CHAR(16)` | L33 |
| `cEstatus` | `CHAR(10)` | L34 |
| `cNumeroPago` | `CHAR(10)` | L35 |
| `cCausaRechazo` | `CHAR(50)` | L36 |
| `dFechaCancela` | `DATE` | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L87 |
| `sp_reporteprogramacion2` | `bdiprog` | ⚠️ sí | L97 |
| `sp_reporteprogramacionsegundo2` | `bdiprog` | ⚠️ sí | L152 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoReporte = '' OR pOperacion ='' OR pRegistros IS NULL OR ` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L102 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdiprog:sp_reporteprogramacion2` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00297';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00956';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00957';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00958';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00959';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00025';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |
| L142 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L157 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdiprog:sp_reporteprogramacions` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |
| L167 | CÓDIGO_RETORNO | `LET cCodRet = '00297';` |  |
| L171 | CÓDIGO_RETORNO | `LET cCodRet = '00956';` |  |
| L175 | CÓDIGO_RETORNO | `LET cCodRet = '00957';` |  |
| L179 | CÓDIGO_RETORNO | `LET cCodRet = '00958';` |  |
| L183 | CÓDIGO_RETORNO | `LET cCodRet = '00959';` |  |
| L187 | CÓDIGO_RETORNO | `LET cCodRet = '00025';` |  |
| L191 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |
| L196 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L205 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?gra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mac` | ENTIDAD | dirección MAC | 🔵 CONVENCIÓN | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?gra`, `?ion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_reportesbc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_reportesbc.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=42 / fan_out=6 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación reportes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicheq` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_reportesbc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaReporte                CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaReporte` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cCodRetSp` | `CHAR(3)` | L17 |
| `iCodRetSp` | `INTEGER` | L18 |
| `cEmpresa` | `CHAR(3)` | L19 |
| `cCuenta` | `CHAR(20)` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cFechaAlta` | `DATE` | L22 |
| `cTransacc` | `CHAR(4)` | L23 |
| `cReferencia` | `CHAR(40)` | L24 |
| `iNumeroChque` | `INTEGER` | L25 |
| `sDiasOri` | `SMALLINT` | L26 |
| `mMontoOri` | `MONEY(14,2)` | L27 |
| `cSiglas` | `CHAR(2)` | L28 |
| `cBanco` | `CHAR (40)` | L29 |
| `cFecharep` | `CHAR (10)` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L69 |
| `bdicheq` | `bdicnweb` | no | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_reportesbc';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?bc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_reportesbc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_reportesbc_totales.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=42 / fan_out=6 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación reportes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicheq` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_reportesbc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaReporte                CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaReporte` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |
| `bdicheq` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_reportesbc';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?bc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?bc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_reversodetallechequecodigo40`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_reversodetallechequecodigo40.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=36 / fan_out=16 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación detalle, cheque y código" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_reversodetallechequecodigo40(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(50)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `dFechaHoy` | `DATE` | L6 |
| `cFechaAlta` | `CHAR(10)` | L7 |
| `cNumCuenta` | `CHAR(20)` | L8 |
| `cNumCheque` | `CHAR(20)` | L9 |
| `icodSeguridad` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_cheques_det` | `bditef` | ⚠️ sí | SELECT | L53 |
| `cce_cheques_det` | `bditef` | ⚠️ sí | UPDATE | L64 |
| `cce_gransumario` | `bditef` | ⚠️ sí | SELECT | L74 |
| `cce_gransumario` | `bditef` | ⚠️ sí | DELETE | L75 |
| `cce_sumario` | `bditef` | ⚠️ sí | SELECT | L79 |
| `cce_sumario` | `bditef` | ⚠️ sí | DELETE | L79 |
| `cce_encabezado` | `bditef` | ⚠️ sí | SELECT | L81 |
| `cce_encabezado` | `bditef` | ⚠️ sí | DELETE | L81 |
| `cce_detalle` | `bditef` | ⚠️ sí | SELECT | L85 |
| `cce_detalle` | `bditef` | ⚠️ sí | DELETE | L86 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cheque` | ENTIDAD | cheque | 🔵 CONVENCIÓN | nombre_sp |
| `codigo` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?40` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?40` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_tarjetamotivoreposicion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_tarjetamotivoreposicion.sql` |
| **LOC (1er CREATE)** | 203 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación tarjeta, motivo y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_tarjetamotivoreposicion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRangoDe                     CHAR(16)
  pRangoHasta                  CHAR(16)
  pPeriodoExpTarjeta           CHAR(3)
  pTotalRango                  CHAR(4)
  pFechaCancelacion            DATE
  pComentario                  CHAR(60)
  pOperacion                   CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRangoDe` | `CHAR(16)` | — | — |
| `pRangoHasta` | `CHAR(16)` | — | — |
| `pPeriodoExpTarjeta` | `CHAR(3)` | `tarjeta`=tarjeta | ✅ CÓDIGO |
| `pTotalRango` | `CHAR(4)` | — | — |
| `pFechaCancelacion` | `DATE` | — | — |
| `pComentario` | `CHAR(60)` | — | — |
| `pOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iTipo` | `INTEGER` | L10 |
| `cDescripcion` | `CHAR(60)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `iTotalDisponibles` | `INTEGER` | L13 |
| `cTarjetaSustituir` | `CHAR(19)` | L14 |
| `cTarjetaSutituta` | `CHAR(19)` | L15 |
| `cBin` | `CHAR(10)` | L16 |
| `cTotalRango` | `CHAR(4)` | L17 |
| `cDe` | `CHAR(19)` | L18 |
| `cHasta` | `CHAR(19)` | L19 |
| `cFechaExp` | `CHAR(5)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `13` | `bdicnweb` | no | SELECT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00891';` |  |
| L119 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `tarjeta` | ENTIDAD | tarjeta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `motivo` | ENTIDAD | motivo / causa | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?icion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?icion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_tarjetamotivoreposicion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_tarjetamotivoreposicion_totales.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación tarjeta, motivo y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_tarjetamotivoreposicion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRangoDe                     CHAR(16)
  pRangoHasta                  CHAR(16)
  pPeriodoExpTarjeta           CHAR(3)
  pTotalRango                  CHAR(4)
  pFechaCancelacion            DATE
  pComentario                  CHAR(60)
  pOperacion                   CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRangoDe` | `CHAR(16)` | — | — |
| `pRangoHasta` | `CHAR(16)` | — | — |
| `pPeriodoExpTarjeta` | `CHAR(3)` | `tarjeta`=tarjeta | ✅ CÓDIGO |
| `pTotalRango` | `CHAR(4)` | — | — |
| `pFechaCancelacion` | `DATE` | — | — |
| `pComentario` | `CHAR(60)` | — | — |
| `pOperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `iNoRegPorAsignar` | `INTEGER` | L10 |
| `cStatus` | `CHAR(3)` | L11 |
| `cNumeroTarjeta` | `CHAR(3)` | L12 |
| `dFecha` | `DATE` | L13 |
| `cBin` | `CHAR(10)` | L14 |
| `cDe` | `CHAR(19)` | L15 |
| `cHasta` | `CHAR(19)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `13` | `bdicnweb` | no | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `tarjeta` | ENTIDAD | tarjeta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `motivo` | ENTIDAD | motivo / causa | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?icion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?icion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_totalregistrosbines`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_totalregistrosbines.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación registros (total) · INE — Instituto Nacional Electoral" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_totalregistrosbines(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iTotalEncontrados` | `INTEGER` | L8 |
| `iTotalNoEncontrados` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_binesemisor` | `bdicnweb` | no | SELECT | L42 |
| `sc_bines` | `bdicheq` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `registros` | ENTIDAD | registros | 🔵 CONVENCIÓN | nombre_sp |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ine` | REG | INE — Instituto Nacional Electoral (validación de identidad  | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?b`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_validachequeduplicado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_validachequeduplicado.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=78 / fan_out=29 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación cheque" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_validachequeduplicado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pBanco                       CHAR(3)
  pNumCheque                   CHAR(10)
  pImporte                     MONEY(14,2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pBanco` | `CHAR(3)` | — | — |
| `pNumCheque` | `CHAR(10)` | `cheque`=cheque | ✅ CÓDIGO |
| `pImporte` | `MONEY(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `dFecha` | `CHAR(10)` | L10 |
| `cFechaHoy` | `CHAR(8)` | L11 |
| `cNomArchivo` | `CHAR(22)` | L12 |
| `mMontoTotalV` | `MONEY(14,2)` | L13 |
| `iNoRegistros` | `INTEGER` | L15 |
| `dOtraFechaDate` | `DATE` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_detalle` | `bditef` | ⚠️ sí | SELECT | L69 |
| `cce_cheques_det` | `bditef` | ⚠️ sí | UPDATE | L80 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | FÓRMULA | `LET mMontoTotalV = mMontoTotalV - ROUND(pImporte);` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?duplicado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?duplicado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_validadiaferiado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_validadiaferiado.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación (del día)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_validadiaferiado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `dFecha` | `DATE` | L13 |
| `dDiaFeriado` | `DATE` | L14 |
| `cDescDiaFeriado` | `CHAR(30)` | L15 |
| `cLaborable` | `CHAR(1)` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdinteg:sp_validardiaferiado';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `dia` | MODIF | del día | 🔵 CONVENCIÓN | nombre_sp |
| `?feriado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?feriado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_validaimagencheque`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_validaimagencheque.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=13 / fan_out=10 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación imagen digital y cheque" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_validaimagenescheques` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_validaimagencheque(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pClaveBanco                  CHAR(3)
  pCuenta                      CHAR(20)
  pNumCheque                   CHAR(7)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pClaveBanco` | `CHAR(3)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | `cheque`=cheque | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(50)` | L9 |
| `cImgFormato` | `CHAR(3)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_validaimagenescheques` | `bditef` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_validaimagenescheques';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00754';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `imagen` | ENTIDAD | imagen digital | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_validaimagenchequedev`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_validaimagenchequedev.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=52 / fan_out=38 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación imagen digital y cheque" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_validaimagencheque_dev` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_validaimagenchequedev(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCveBanco                    CHAR(3)
  pCuenta                      CHAR(20)
  pNumCheque                   CHAR(7)
  pLadoFt                      CHAR(1)
  dFechaPresenta               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCveBanco` | `CHAR(3)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pNumCheque` | `CHAR(7)` | `cheque`=cheque | ✅ CÓDIGO |
| `pLadoFt` | `CHAR(1)` | — | — |
| `dFechaPresenta` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cMensaje` | `CHAR(50)` | L9 |
| `cImgFormato` | `CHAR(3)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_validaimagencheque_dev` | `bditef` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pCveBanco = '' OR pCuenta = '' OR pNumCheque = '' OR pLadoFt ` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_validaimagencheque_dev';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00719';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00720';` |  |
| L56 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `imagen` | ENTIDAD | imagen digital | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheque` | ENTIDAD | cheque | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dev` | ACCION | devolución | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_ope_validapassword`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_validapassword.sql` |
| **LOC (1er CREATE)** | 50 |
| **Callgraph** | ✅ fan_in=42 / fan_out=6 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación ss — subsistema y ordenante" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_validapassword(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPassword                    CHAR(40)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPassword` | `CHAR(40)` | `ss`=ss — subsistema / canal de monitoreo (abreviación — envia_monitorsol_*_ss_* — bdisolic) · `ord`=ordenante / orden (SPEI) | 🔵 CONVENCIÓN / 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cPassword` | `CHAR(40)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L35 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?pa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?w` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pa`, `ss`, `?w` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_validapermisosusuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_validapermisosusuario.sql` |
| **LOC (1er CREATE)** | 44 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación OS — Originación de Solicitudes y usuario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_validapermisosusuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | 🔵 CONVENCIÓN |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_usuarios_revision` | `bditef` | ⚠️ sí | SELECT | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L25 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?permis` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `usuario` | ENTIDAD | usuario | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?permis` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ope_validasucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_validasucursal.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_validasucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRet` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cSucursal` | `CHAR(4)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_verificastatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_verificastatus.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=55 |
| **Deps concatenadas** | 148 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_verificastatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(1)` | L10 |
| `cError` | `CHAR(5)` | L11 |
| `cPathFile` | `CHAR(100)` | L12 |
| `cNomFile` | `CHAR(50)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tdc_verificaproceso` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ope_verificastatusrepordpago`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ope_verificastatusrepordpago.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 85 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación estatus, ordenante y pago" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ope_verificastatusrepordpago(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cNombre_archivo` | `CHAR(50)` | L11 |
| `cError_spl` | `CHAR(6)` | L12 |
| `cDescripcion_error_spl` | `CHAR(250)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ope_statusrepordpago` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_operaciones_encabezadotipocambio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_operaciones_encabezadotipocambio.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operaciones, encabezado y cambio (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_operaciones_encabezadotipocambio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cDepto` | `CHAR(30)` | L12 |
| `cNombre` | `CHAR(45)` | L13 |
| `cSistema` | `CHAR(50)` | L14 |
| `cEmpres` | `CHAR(30)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L52 |
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L58 |
| `si_vbparam` | `bdinteg` | ⚠️ sí | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet 	= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `encabezado` | ENTIDAD | encabezado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_operacionestipocambio_actualiza_mercado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_operacionestipocambio_actualiza_mercado.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza operaciones y cambio (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_operacionestipocambio_actualiza_mercado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdOperacion                 CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdOperacion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `cProceso` | `CHAR(15)` | L7 |
| `cDivisa` | `CHAR(2)` | L8 |
| `cClase_TpCambio` | `CHAR(1)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tpcambio` | `bdinteg` | ⚠️ sí | SELECT | L49 |
| `si_tpcambio` | `bdinteg` | ⚠️ sí | DELETE | L55 |
| `si_contproc` | `bdinteg` | ⚠️ sí | INSERT | L89 |
| `si_histtcdiario` | `bdinteg` | ⚠️ sí | SELECT | L102 |
| `si_histtcdiario` | `bdinteg` | ⚠️ sí | INSERT | L112 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_mercado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_mercado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pagomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pagomasivocre.sql` |
| **LOC (1er CREATE)** | 279 |
| **Callgraph** | ✅ fan_in=0 / fan_out=73 |
| **Deps concatenadas** | 83 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "pago y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_obtenertransaccionpagosmanuales`, `sp_grabarpagosmasivos`, `sp_consaldodisp` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pagomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pPlantilla                   CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vUsuario` | `CHAR(10)` | L5 |
| `cFolioGrupo` | `CHAR(16)` | L6 |
| `dFechaHoy` | `DATE` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cCodRetSpSal` | `CHAR(5)` | L11 |
| `cCodPago` | `CHAR(2)` | L12 |
| `cDescPago` | `CHAR(50)` | L13 |
| `cCodFuncion` | `CHAR(3)` | L14 |
| `cConcepto` | `CHAR(50)` | L15 |
| `cFolioGen` | `CHAR(16)` | L16 |
| `dFechaProc` | `DATETIME YEAR TO FRACTION` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `iNoRegs` | `INTEGER` | L19 |
| `cCredito` | `CHAR(16)` | L21 |
| `mImportePago` | `MONEY(14,2)` | L22 |
| `cTransaccion` | `CHAR(4)` | L23 |
| `cResultado` | `CHAR(15)` | L24 |
| `iIdRegistro` | `INTEGER` | L25 |
| `cStatus` | `CHAR(1)` | L26 |
| `iInTrans` | `INTEGER` | L27 |
| `iRegRechazados` | `INTEGER` | L28 |
| `mSaldoCuenta` | `MONEY(14,2)` | L29 |
| `iTotalRegs` | `INTEGER` | L30 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_pago` | `bdicnweb` | no | SELECT | L96 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L130 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L134 |
| `sd_conceptospagomanual` | `bdicred` | ⚠️ sí | SELECT | L142 |
| `sd_conceptospagomanualcrd` | `bdicred` | ⚠️ sí | SELECT | L147 |
| `sw_tr_cargamasiva_pago_hist` | `bdicnweb` | no | INSERT | L206 |
| `sw_tr_cargamasiva_pago` | `bdicnweb` | no | DELETE | L214 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L235 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtenertransaccionpagosmanuales` | `bdicred` | ⚠️ sí | L151 |
| `sp_grabarpagosmasivos` | `bdicred` | ⚠️ sí | L157 |
| `sp_consaldodisp` | `bdicnweb` | no | L171 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L183 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L191 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_param_conexion_postgres`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_param_conexion_postgres.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro y punto de venta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtiene_conexion_param` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_param_conexion_postgres(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cEmpresa` | `CHAR(3)` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |
| `cNumIp` | `CHAR(100)` | L18 |
| `cPuerto` | `CHAR(100)` | L19 |
| `cNomUsuario` | `CHAR(100)` | L20 |
| `cPassword` | `CHAR(100)` | L21 |
| `cNomBd` | `CHAR(100)` | L22 |
| `cTiempo` | `CHAR(100)` | L23 |
| `cLimite` | `CHAR(100)` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |
| `sp_obtiene_conexion_param` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL bdinteg:sp_obtiene_conexion_param";` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pos` | ENTIDAD | punto de venta (POS) | 🔵 CONVENCIÓN | nombre_sp |
| `?tgres` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?ion_`, `?tgres` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_parametros_kiosko`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_parametros_kiosko.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "parámetros y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_parametros_kiosko(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMac                         CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cUsaTouch` | `CHAR(1)` | L15 |
| `cUsaImgHuella` | `CHAR(1)` | L16 |
| `cUsaImgLectora` | `CHAR(1)` | L17 |
| `cIndImgHuella` | `CHAR(1)` | L18 |
| `cIndImgLectora` | `CHAR(1)` | L19 |
| `iPosTopHuella` | `INTEGER` | L20 |
| `iPosLeftHuella` | `INTEGER` | L21 |
| `iPosTopLectora` | `INTEGER` | L22 |
| `iPosLeftLectora` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `kw_sucursal_touch` | `bdicnweb` | no | SELECT | L62 |
| `kw_sucursal_indimg` | `bdicnweb` | no | SELECT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_ki` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ko` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ki`, `?ko` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_parametros_retiros`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_parametros_retiros.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 66 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "parámetros y retiros" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bitacora` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_parametros_retiros(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pip                          CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pip` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cMontominimo` | `CHAR(20)` | L15 |
| `cMontomaximo` | `CHAR(20)` | L16 |
| `cCodigosActivos` | `CHAR(20)` | L17 |
| `cVigencia` | `CHAR(20)` | L18 |
| `campo` | `CHAR(20)` | L20 |
| `valorCampo` | `CHAR(20)` | L21 |
| `cAccion` | `CHAR(50)` | L23 |
| `cEntidad` | `CHAR(100)` | L24 |
| `cIp` | `CHAR(20)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `parametro_sistema` | `bdirst` | ⚠️ sí | SELECT | L67 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_bitacora` | `bdicnweb` | no | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '01222';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '01222';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `retiros` | ENTIDAD | retiros | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_parametros_status`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_parametros_status.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 65 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "parámetros y estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bitacora` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_parametros_status(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIp                          CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIp` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cIdStatus` | `CHAR(2)` | L14 |
| `cCodStatus` | `CHAR(1)` | L15 |
| `cDescricpiconStatus` | `CHAR(30)` | L16 |
| `cAccion` | `CHAR(50)` | L18 |
| `cEntidad` | `CHAR(100)` | L19 |
| `cip` | `CHAR(20)` | L20 |
| `crespaldo` | `CHAR(50)` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |
| `sp_bitacora` | `bdicnweb` | no | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_parametros_status_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_parametros_status_totales.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "parámetros y estatus (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_parametros_status_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iTotales` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cat_status` | `bdirst` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pm_califica_scoring2_cjunk`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_califica_scoring2_cjunk.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "califica scoring crediticio" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `califica_scoring2_cjunk` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_califica_scoring2_cjunk(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(3)
  pNumSol                      CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L26 |
| `califica_scoring2_cjunk` | `bdisolic` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet ='00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `califica` | ACCION | califica / evalúa (scoring) | 🔵 CONVENCIÓN | nombre_sp |
| `scoring` | ENTIDAD | scoring crediticio | 🔵 CONVENCIÓN | nombre_sp |
| `?2_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cjunk` | AMBIGUO | variable temporal (ruido de código, se ignora) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_`, `?2_`, `cjunk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_cons_empleado_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_cons_empleado_mc.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta empleado y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_cons_empleado_mc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_cons_empleado_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | `empleado`=empleado | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNomEmp` | `CHAR(60)` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |
| `sp_cons_empleado_mc` | `bdisolic` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `empleado` | ENTIDAD | empleado | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_consulparam_cambioestatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_consulparam_cambioestatus.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro, cambio y estatus" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_consulparam_cambioestatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cValor` | `CHAR(50)` | L8 |
| `cEstatus` | `CHAR(1)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_paramcambioestatus` | `bdisolic` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_`, `?ul` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_consultaempleado_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_consultaempleado_mc.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta empleado y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_consultaempleado_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iExiste` | `INTEGER` | L7 |
| `cExiste` | `CHAR(1)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_emp_revingresos_mc` | `bdisolic` | ⚠️ sí | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `empleado` | ENTIDAD | empleado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_determina_lincred_tc_cjunk`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_determina_lincred_tc_cjunk.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "determina línea de crédito y tc — Tarjeta de Crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `determina_lincred_tc_cjunk` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_determina_lincred_tc_cjunk(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(3)
  pNumSol                      CHAR(20)
  pCteNvo                      CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNumSol` | `CHAR(20)` | — | — |
| `pCteNvo` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `mLinCred` | `MONEY(14,2)` | L9 |
| `mCapPago` | `MONEY(14,2)` | L10 |
| `iPlazo` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `determina_lincred_tc_cjunk` | `bdisolic` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet ='00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `determina` | ACCION | determina | 🔵 CONVENCIÓN | nombre_sp |
| `lincred` | ENTIDAD | línea de crédito | 🔵 CONVENCIÓN | nombre_sp |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `cjunk` | AMBIGUO | variable temporal (ruido de código, se ignora) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_`, `cjunk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_elim_empleado_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_elim_empleado_mc.sql` |
| **LOC (1er CREATE)** | 48 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "empleado y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_elimina_emp_mc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_elim_empleado_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | `empleado`=empleado | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L26 |
| `sp_elimina_emp_mc` | `bdisolic` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_elim_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empleado` | ENTIDAD | empleado | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_elim_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_insert_empleados_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_insert_empleados_mc.sql` |
| **LOC (1er CREATE)** | 202 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "insertar empleado y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_insert_empleados_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | `empleado`=empleado | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |
| `cNombreC` | `CHAR(60)` | L8 |
| `i` | `INTEGER` | L9 |
| `iTamCad` | `INTEGER` | L10 |
| `iInicioCadena` | `INTEGER` | L11 |
| `iRecuperarCaracteres` | `INTEGER` | L12 |
| `cCaracter` | `CHAR(1)` | L13 |
| `iEspacios` | `INTEGER` | L14 |
| `cNombre` | `CHAR(25)` | L15 |
| `cApat` | `CHAR(15)` | L16 |
| `cAmat` | `CHAR(15)` | L17 |
| `iContador` | `INTEGER` | L18 |
| `cPalabra` | `LVARCHAR` | L19 |
| `iAux1` | `INTEGER` | L20 |
| `iAux2` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L67 |
| `ss_emp_revingresos_mc` | `bdisolic` | ⚠️ sí | INSERT | L187 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L83 | FÓRMULA | `LET iEspacios = iEspacios+1;` |  |
| L88 | FÓRMULA | `LET iAux1 = iEspacios - 1;` |  |
| L102 | FÓRMULA | `LET iRecuperarCaracteres = iRecuperarCaracteres + 1;` |  |
| L106 | FÓRMULA | `LET iRecuperarCaracteres = iRecuperarCaracteres - 1;` |  |
| L107 | FÓRMULA | `LET iContador = iContador+1;` |  |
| L109 | FÓRMULA | `LET iInicioCadena = i + 1;` |  |
| L129 | FÓRMULA | `LET iRecuperarCaracteres = iRecuperarCaracteres + 1;` |  |
| L133 | FÓRMULA | `LET iRecuperarCaracteres = iRecuperarCaracteres - 1;` |  |
| L134 | FÓRMULA | `LET iContador = iContador+1;` |  |
| L136 | FÓRMULA | `LET iInicioCadena = i + 1;` |  |
| L160 | FÓRMULA | `LET iRecuperarCaracteres = iRecuperarCaracteres + 1;` |  |
| L164 | FÓRMULA | `LET iRecuperarCaracteres = iRecuperarCaracteres - 1;` |  |
| L165 | FÓRMULA | `LET iContador = iContador+1;` |  |
| L167 | FÓRMULA | `LET iInicioCadena = i + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?ert_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empleado` | ENTIDAD | empleado | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_`, `?ert_`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_lista_empleados_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_lista_empleados_mc.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "empleado y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_lista_empleados_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cNomEmp` | `CHAR(60)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `cNumEmp` | `CHAR(8)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ss_emp_revingresos_mc` | `bdicnweb` | no | SELECT | L48 |
| `sw_ss_emp_revingresos_mc` | `bdicnweb` | no | DELETE | L48 |
| `ss_emp_revingresos_mc` | `bdisolic` | ⚠️ sí | SELECT | L53 |
| `sw_ss_emp_revingresos_mc` | `bdicnweb` | no | INSERT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L65 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_lista_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empleado` | ENTIDAD | empleado | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_lista_`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_lista_empleados_mc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_lista_empleados_mc_totales.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "empleado y mc — Mesa de Control (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_lista_empleados_mc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iTotal` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_emp_revingresos_mc` | `bdisolic` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_lista_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empleado` | ENTIDAD | empleado | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_lista_`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_nombreemp_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_nombreemp_mc.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "nombre, Empresa — empleadora del cliente; vinculada a crédito de nómina y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_nombreemp_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEmpleado                 CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEmpleado` | `CHAR(8)` | `emp`=Empresa — empleadora del cliente; vinculada a crédito de nómina (ADN); SPs: sp_consulta_datos_emp_bei (phone+address), sp_genera_emp_gc (Grupo Coppel), inserta_rel_cte_emp | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iTotal` | `INTEGER` | L8 |
| `cNombreC` | `CHAR(60)` | L9 |
| `i` | `INTEGER` | L10 |
| `iTamCad` | `INTEGER` | L11 |
| `iInicioCadena` | `INTEGER` | L12 |
| `iRecuperarCaracteres` | `INTEGER` | L13 |
| `cCaracter` | `CHAR(1)` | L14 |
| `iEspacios` | `INTEGER` | L15 |
| `cNombre` | `CHAR(25)` | L16 |
| `cApat` | `CHAR(15)` | L17 |
| `cAmat` | `CHAR(15)` | L18 |
| `iContador` | `INTEGER` | L19 |
| `cPalabra` | `LVARCHAR` | L20 |
| `iAux1` | `INTEGER` | L21 |
| `iAux2` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_obtiene_ultimas_img_digi_cte_soc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_obtiene_ultimas_img_digi_cte_soc.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 108 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene cliente y Sistema Operativo Central (últimas)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_obtiene_ultimas_img_digi_cte_soc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpresa                     CHAR(3)
  pNumCte                      CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNumCte` | `CHAR(10)` | `cte`=cliente | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodigoDoc` | `CHAR(5)` | L8 |
| `sSecuenciaMaxComprobante` | `SMALLINT` | L9 |
| `iSolMC` | `INTEGER` | L10 |
| `cSolicitudMC` | `CHAR(20)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_solicitudes` | `bdisolic` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `bdidigital` | `bdicnweb` | no | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `ultimas` | MODIF | últimas | 🟡 INFERIDO | nombre_sp |
| `?_img_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `digi` | ACCION | digitalización | 🟡 INFERIDO | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_`, `?_img_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_obtienecompingresos_mc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_obtienecompingresos_mc.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene ingreso y mc — Mesa de Control (complemento)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienecompingresos_mc` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_obtienecompingresos_mc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cComprobante` | `CHAR(60)` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |
| `sp_obtienecompingresos_mc` | `bdisolic` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | FÓRMULA | `LET iNoRegistros = iNoRegistros+1;` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ingreso` | ENTIDAD | ingreso (del solicitante) | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pm_obtienegrupo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pm_obtienegrupo.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene grupo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtienegrupo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pm_obtienegrupo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSol                      CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSol` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSP` | `CHAR(6)` | L8 |
| `cTipo` | `CHAR(2)` | L9 |
| `cHit` | `CHAR(6)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `sp_obtienegrupo` | `bdisolic` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet ='00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_pm_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `grupo` | ENTIDAD | grupo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_pm_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_portadactamec2_complementoinfo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_portadactamec2_complementoinfo.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza información (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 10/10/2024 |

### Firma

```sql
CREATE PROCEDURE sp_portadactamec2_complementoinfo(
  pNumCtaEmp                   CHAR(20)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCtaEmp` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INTEGER` | L23 |
| `cCodRet` | `CHAR(5)` | L24 |
| `cMensaje` | `CHAR(50)` | L25 |
| `pproced_aperturacta` | `CHAR(2)` | L27 |
| `pproced_mantenercta` | `CHAR(2)` | L28 |
| `pmonto_mensual` | `CHAR(2)` | L29 |
| `pdepositos_cantidad` | `CHAR(2)` | L30 |
| `pdepositos_monto` | `CHAR(2)` | L31 |
| `pretiros_cantidad` | `CHAR(2)` | L32 |
| `pretiros_monto` | `CHAR(2)` | L33 |
| `pfecha_alta` | `DATE` | L34 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet           	= '00000';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?tad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?amec2_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `?lemento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tad`, `?amec2_`, `?lemento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_portanom_generacionarchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_portanom_generacionarchivo.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera Portabilidad de Nómina — portabilidad de domiciliación de nómina entre bancos y archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicheq` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_portanom_generacionarchivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaRegistro               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaRegistro` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(3)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `cRutaArchivo` | `CHAR(80)` | L11 |
| `cArchivo` | `CHAR(20)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `bdicheq` | `bdicnweb` | no | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaRegistro IS NULL THEN` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:'informix'.sp_generarchivoportab` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `portanom` | ENTIDAD | Portabilidad de Nómina — portabilidad de domiciliación de nó | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_portanom_generacionarchivocancel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_portanom_generacionarchivocancel.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=107 |
| **Deps concatenadas** | 324 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera Portabilidad de Nómina — portabilidad de domiciliación de nómina entre bancos, archivo y celular" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_portanom_generacionarchivocancel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaRegistro               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaRegistro` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(3)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `cRutaArchivo` | `CHAR(80)` | L11 |
| `cArchivo` | `CHAR(20)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_genarchportanomcan` | `bdicnweb` | no | SELECT | L36 |
| `status_genarchportanomcan` | `bdicnweb` | no | DELETE | L36 |
| `status_genarchportanomcan` | `bdicnweb` | no | INSERT | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |
| `sp_generarchivoportab_cancelaciones` | `bdicheq` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaRegistro IS NULL THEN` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:'informix'.sp_generarchivoportab` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `portanom` | ENTIDAD | Portabilidad de Nómina — portabilidad de domiciliación de nó | 🔵 CONVENCIÓN | nombre_sp |
| `genera` | ACCION | genera / produce | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?can` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion`, `?can` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_portanom_obtieneruta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_portanom_obtieneruta.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "obtiene Portabilidad de Nómina — portabilidad de domiciliación de nómina entre bancos y ruta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtrutasportabilidad` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_portanom_obtieneruta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOperacion                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cRutaArchivo` | `CHAR(50)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_obtrutasportabilidad` | `bdicheq` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pOperacion IS NULL THEN` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP ";` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `portanom` | ENTIDAD | Portabilidad de Nómina — portabilidad de domiciliación de nó | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `ruta` | ENTIDAD | ruta (de archivo) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_portanom_reporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_portanom_reporte.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Portabilidad de Nómina — portabilidad de domiciliación de nómina entre bancos y reporte" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rptportab` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_portanom_reporte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipoSolicitud               INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipoSolicitud` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCodRetSp` | `CHAR(5)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cFechaArchivo` | `CHAR(10)` | L16 |
| `cFolioSolicitud` | `CHAR(30)` | L17 |
| `cEstatusSolicitud` | `CHAR(20)` | L18 |
| `cEstatusRechazo` | `CHAR(20)` | L19 |
| `cFechaRespuesta` | `CHAR(10)` | L20 |
| `cCuenta` | `CHAR(18)` | L21 |
| `cBanco` | `CHAR(20)` | L22 |
| `cCliente` | `CHAR(90)` | L23 |
| `iNoRegistros` | `INTEGER` | L24 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |
| `sp_rptportab` | `bdicheq` | ⚠️ sí | L67 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pTipoSolicitu` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L72 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:sp_rptportab";` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00450';` |  |
| L81 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `portanom` | ENTIDAD | Portabilidad de Nómina — portabilidad de domiciliación de nó | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_portanom_reporte_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_portanom_reporte_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Portabilidad de Nómina — portabilidad de domiciliación de nómina entre bancos y reporte (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicheq` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_portanom_reporte_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipoSolicitud               INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipoSolicitud` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(3)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |
| `bdicheq` | `bdicnweb` | no | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pTipoSolicitu` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:'informix'.sp_rptportab2_totales` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00450';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `portanom` | ENTIDAD | Portabilidad de Nómina — portabilidad de domiciliación de nó | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_portanom_verificastatusgenarchcancel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_portanom_verificastatusgenarchcancel.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=106 |
| **Deps concatenadas** | 322 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica Portabilidad de Nómina — portabilidad de domiciliación de nómina entre bancos, estatus, archivo y celular" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_portanom_verificastatusgenarchcancel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `iNumRegistros` | `INTEGER` | L14 |
| `cRutaArchivo` | `CHAR(80)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_genarchportanomcan` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `portanom` | ENTIDAD | Portabilidad de Nómina — portabilidad de domiciliación de nó | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `?can` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?can` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pos_busquedabitacorapos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_busquedabitacorapos.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 93 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda punto de venta y bitácora" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_busquedabitacorapos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecut                      CHAR(8)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecut` | `CHAR(8)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpleado` | `CHAR(8)` | L10 |
| `cNombre` | `CHAR(45)` | L11 |
| `cMovimiento` | `CHAR(50)` | L12 |
| `dFecha` | `DATE` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bitacora_pos` | `bdinteg` | ⚠️ sí | SELECT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '01273';` |  |
| L79 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L105 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '01273';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_pos_busquedabitacorapos_total`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_busquedabitacorapos_total.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 92 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda punto de venta y bitácora (total)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_busquedabitacorapos_total(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecut                      CHAR(8)
  pFechaIni                    DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecut` | `CHAR(8)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bitacora_pos` | `bdinteg` | ⚠️ sí | SELECT | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet ='01273';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet ='01273';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pos_busquedapos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_busquedapos.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 91 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda punto de venta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_busquedapos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripLarg                 CHAR(152)
  pDescripCort                 CHAR(32)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripLarg` | `CHAR(152)` | — | — |
| `pDescripCort` | `CHAR(32)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cDescripLarg` | `CHAR(150)` | L9 |
| `cDescripCort` | `CHAR(30)` | L10 |
| `iFolio` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | SELECT | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L77 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_pos_busquedapos_total`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_busquedapos_total.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 90 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda punto de venta (total)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_busquedapos_total(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripLarg                 CHAR(152)
  pDescripCort                 CHAR(32)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripLarg` | `CHAR(152)` | — | — |
| `pDescripCort` | `CHAR(32)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | SELECT | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pos_consultapos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_consultapos.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 89 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta punto de venta" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_consultapos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cDescripLarg` | `CHAR(150)` | L9 |
| `cDescripCort` | `CHAR(30)` | L10 |
| `iFolio` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | SELECT | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '01272';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_pos_consultapos_total`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_consultapos_total.sql` |
| **LOC (1er CREATE)** | 47 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 88 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta punto de venta (total)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_consultapos_total(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | SELECT | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet ='01272';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pos_eliminapos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_eliminapos.sql` |
| **LOC (1er CREATE)** | 48 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 87 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "elimina punto de venta" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_eliminapos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdPosAct                    INT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdPosAct` | `INT` | `pos`=punto de venta (POS) · `pos`=punto de venta (POS) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cNombre` | `CHAR(45)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L30 |
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | SELECT | L32 |
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | DELETE | L32 |
| `si_bitacora_pos` | `bdinteg` | ⚠️ sí | INSERT | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_pos_genrepbitacora`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_genrepbitacora.sql` |
| **LOC (1er CREATE)** | 277 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 86 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte punto de venta y bitácora" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_genrepbitacora(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecut                      CHAR(8)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRutaDescarga                CHAR(100)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecut` | `CHAR(8)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `cBanDetError` | `CHAR(1)` | L13 |
| `iTotal` | `INTEGER` | L14 |
| `cEmpleado` | `CHAR(8)` | L16 |
| `cNombre` | `CHAR(45)` | L17 |
| `cMovimiento` | `CHAR(50)` | L18 |
| `dFecha` | `DATE` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_pos_reportebitacorapostmp` | `bdicnweb` | no | SELECT | L85 |
| `sw_verificastatusrepposbitacora` | `bdicnweb` | no | SELECT | L86 |
| `sw_verificastatusrepposbitacora` | `bdicnweb` | no | INSERT | L90 |
| `si_bitacora_pos` | `bdinteg` | ⚠️ sí | SELECT | L100 |
| `sw_pos_reportebitacorapostmp` | `bdicnweb` | no | INSERT | L104 |
| `systables` | `bdicnweb` | no | SELECT | L160 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L150 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L161 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ( ";` |  |
| L162 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT usuario_modifica::CHAR(20), nombre_usuario_modifica::CHAR(8` | 🔴 MONEY/aritmética financiera |
| L173 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L213 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L218 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L227 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L236 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_pos_insertapos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_insertapos.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 85 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta punto de venta" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_insertapos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDescripLarg                 CHAR(150)
  pDescripCort                 CHAR(30)
  pIdPosAct                    INT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDescripLarg` | `CHAR(150)` | — | — |
| `pDescripCort` | `CHAR(30)` | — | — |
| `pIdPosAct` | `INT` | `pos`=punto de venta (POS) · `pos`=punto de venta (POS) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cDescripLarg` | `CHAR(150)` | L6 |
| `cDescripCort` | `CHAR(30)` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `cNombre` | `CHAR(45)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | SELECT | L37 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L48 |
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | INSERT | L52 |
| `si_bitacora_pos` | `bdinteg` | ⚠️ sí | INSERT | L55 |
| `si_descrip_pos` | `bdinteg` | ⚠️ sí | UPDATE | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet ='01274';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_pos_recuperanombre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_recuperanombre.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 84 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recupera estado punto de venta y nombre" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `recupera` → `SELECT` encontrado en el cuerpo · `recupera` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_recuperanombre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecut                      CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecut` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNombre` | `CHAR(45)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet ='99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | 🔵 CONVENCIÓN | nombre_sp |
| `recupera` | ACCION | recupera estado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pos_verificastatusrepbitacora`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pos_verificastatusrepbitacora.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 83 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica punto de venta, estatus y bitácora" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pos_verificastatusrepbitacora(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cProceso` | `CHAR(15)` | L14 |
| `cNombre_archivo` | `CHAR(50)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepposbitacora` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pos` | ENTIDAD | punto de venta (POS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pp_actualizacategoriapp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_actualizacategoriapp.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza PP — Pago Programado y categoría" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_actualizacategoriapp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCategoria                   CHAR(1)
  pNumCredito                  CHAR(20)
  pNumTarjeta                  CHAR(20)
  pBandera                     CHAR(1)
) RETURNING CHAR(5)	AS codigo_retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCategoria` | `CHAR(1)` | `categoria`=categoría | ✅ CÓDIGO |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `vNoTarjeta` | `VARCHAR(20)` | L11 |
| `cStatusTar` | `CHAR(1)` | L12 |
| `cStatus` | `CHAR(2)` | L13 |
| `iMaximo` | `INTEGER` | L14 |
| `vTipoTarjeta` | `VARCHAR(20)` | L15 |
| `iAccesoGratis` | `INTEGER` | L16 |
| `iAccesoGratisAdic` | `INTEGER` | L17 |
| `cMtoVen` | `DECIMAL(14,2)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `catcategoriappass` | `bdicred` | ⚠️ sí | SELECT | L64 |
| `sd_tarjeta_ppass` | `bdicred` | ⚠️ sí | UPDATE | L67 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet				= '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `categoria` | ENTIDAD | categoría | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_pp_actualizacategorias`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_actualizacategorias.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 270 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza PP — Pago Programado y categoría" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_actualizacategorias(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCategoria                 SMALLINT
  pAccesoGratis                INTEGER
  pAccesoGratisAd              INTEGER
  pCuota                       DECIMAL(14,6)
) RETURNING CHAR(5)		AS codigo_retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCategoria` | `SMALLINT` | `categoria`=categoría | 🔵 CONVENCIÓN |
| `pAccesoGratis` | `INTEGER` | — | — |
| `pAccesoGratisAd` | `INTEGER` | — | — |
| `pCuota` | `DECIMAL(14,6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `catcategoriappass` | `bdicred` | ⚠️ sí | UPDATE | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `categoria` | ENTIDAD | categoría | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_actualizacorreo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_actualizacorreo.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 269 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza PP — Pago Programado y correo electrónico" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_actualizacorreo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdCorreo                    SMALLINT
  pCompany                     CHAR(25)
  pEmail                       CHAR(100)
  pStatus                      CHAR(1)
  pTipoEnvio                   CHAR(1)
) RETURNING CHAR(5)		AS codigo_retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdCorreo` | `SMALLINT` | `correo`=correo electrónico | ✅ CÓDIGO |
| `pCompany` | `CHAR(25)` | — | — |
| `pEmail` | `CHAR(100)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pTipoEnvio` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `iMaximo` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `catcorreosppass` | `bdicred` | ⚠️ sí | SELECT | L49 |
| `catcorreosppass` | `bdicred` | ⚠️ sí | INSERT | L53 |
| `catcorreosppass` | `bdicred` | ⚠️ sí | UPDATE | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iMaximo = NVL(iMaximo, 0) + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `correo` | ENTIDAD | correo electrónico | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pp_cambiostatusgeneralpp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_cambiostatusgeneralpp.sql` |
| **LOC (1er CREATE)** | 232 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "PP — Pago Programado, cambio y estatus (general)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_cambiostatusgeneralpp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOperacion                   CHAR(1)
  pStatus                      CHAR(1)
  pNumCredito                  CHAR(20)
  pNumTarjeta                  CHAR(20)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOperacion` | `CHAR(1)` | — | — |
| `pStatus` | `CHAR(1)` | `status`=estatus | 🔵 CONVENCIÓN |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iIsamErr` | `INTEGER` | L10 |
| `cErrorInfo` | `CHAR(80)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cStatus` | `CHAR(2)` | L13 |
| `vNoTarjeta` | `VARCHAR(20)` | L14 |
| `iMaximo` | `INTEGER` | L15 |
| `cReposicion` | `CHAR(1)` | L16 |
| `vTipoTarjeta` | `VARCHAR(20)` | L17 |
| `cNuevoNumTarjeta` | `CHAR(20)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_tarjeta_ppass` | `bdicred` | ⚠️ sí | SELECT | L63 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L70 |
| `sd_tarjeta_ppass` | `bdicred` | ⚠️ sí | UPDATE | L80 |
| `sd_info_layout_ppass` | `bdicred` | ⚠️ sí | SELECT | L102 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L105 |
| `sd_inven_tarppass` | `bdicred` | ⚠️ sí | SELECT | L119 |
| `sd_inven_tarppass` | `bdicred` | ⚠️ sí | UPDATE | L127 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '01130';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '01131';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '01132';` |  |
| L206 | CÓDIGO_RETORNO | `LET cCodRet = '01134';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `general` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pp_cargaarchivomensual_genrep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_cargaarchivomensual_genrep.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 268 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga PP — Pago Programado y archivo (mensual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_cargaarchivomensual_genrep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(150)
  pNombreReporte               CHAR(70)
  fecha_reporte                CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | `carga`=carga / ingresa | ✅ CÓDIGO |
| `pNombreReporte` | `CHAR(70)` | — | — |
| `fecha_reporte` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIsamErr` | `INTEGER` | L8 |
| `cDescErr` | `CHAR(50)` | L9 |
| `cCmd1` | `CHAR(3000)` | L10 |
| `cReporteGenerar` | `CHAR(150)` | L11 |
| `cBinesElimina` | `CHAR(150)` | L12 |
| `cNombreArchivo` | `CHAR(150)` | L13 |
| `cRutaInformix` | `CHAR(100)` | L15 |
| `cUsrBin` | `CHAR(100)` | L16 |
| `cSql` | `CHAR(3000)` | L17 |
| `bInTransaction` | `BOOLEAN` | L18 |
| `ven_transacc` | `SMALLINT` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L75 |
| `sd_movmes_ppass` | `bdicred` | ⚠️ sí | SELECT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cReporteGenerar)\|\|" > "\|\|TRIM(cReporteGen` |  |
| L110 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cReporteGenerar);` |  |
| L119 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cReporteGenerar)\|\|".tmp > "\|\|TRIM(cReporteGenerar);` |  |
| L128 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cReporteGenerar)\|\|" > "\|\|TRIM(cReporteGen` |  |
| L136 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cReporteGenerar)\|\|'; /usr/bin/mv '\|\|TRIM(cReporteG` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mensual` | MODIF | mensual | 🔵 CONVENCIÓN | nombre_sp |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |

---

## `sp_pp_cargaarchivoprioritypass`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_cargaarchivoprioritypass.sql` |
| **LOC (1er CREATE)** | 206 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 267 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga PP — Pago Programado, archivo y ss — subsistema" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_cargaarchivoprioritypass(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha_Aplicacion            DATE
  pNombreArchivo               CHAR(80)
  pBloqueInf                   CHAR(2000)
  pIteracion                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha_Aplicacion` | `DATE` | — | — |
| `pNombreArchivo` | `CHAR(80)` | `archivo`=archivo | ✅ CÓDIGO |
| `pBloqueInf` | `CHAR(2000)` | — | — |
| `pIteracion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cSqlerr` | `INTEGER` | L6 |
| `cIsamErr` | `INTEGER` | L7 |
| `cDescErr` | `CHAR(50)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cRegistro` | `LVARCHAR` | L10 |
| `cBloqueInf2` | `LVARCHAR` | L11 |
| `iContador` | `INTEGER` | L12 |
| `iIdItem` | `INTEGER` | L14 |
| `cBank` | `CHAR(30)` | L15 |
| `cDealType` | `CHAR(15)` | L16 |
| `cAssociate` | `CHAR(6)` | L17 |
| `cIca` | `CHAR(5)` | L18 |
| `cPriorityId` | `CHAR(20)` | L19 |
| `cHoldName` | `CHAR(30)` | L20 |
| `dVisit` | `CHAR(10)` | L21 |
| `cLounName` | `CHAR(65)` | L22 |
| `cLoungeCode` | `CHAR(6)` | L23 |
| `cDomesticInt` | `CHAR(1)` | L24 |
| `cIsoCountry` | `CHAR(30)` | L25 |
| `cVisitIso` | `CHAR(3)` | L26 |
| `iMembersCout` | `INTEGER` | L27 |
| `iGuestsCount` | `INTEGER` | L28 |
| `iTotalVisitCount` | `INTEGER` | L29 |
| `iBatch` | `INTEGER` | L30 |
| *…9 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_movmes_ppass` | `bdicred` | ⚠️ sí | SELECT | L108 |
| `sd_movmes_ppass` | `bdicred` | ⚠️ sí | DELETE | L108 |
| `sd_bitacora_cargaarchivo_ppass` | `bdicnweb` | no | SELECT | L113 |
| `sd_bitacora_cargaarchivo_ppass` | `bdicnweb` | no | INSERT | L117 |
| `sd_bitacora_cargaarchivo_ppass` | `bdicnweb` | no | UPDATE | L120 |
| `sd_movmes_ppass` | `bdicred` | ⚠️ sí | INSERT | L186 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L98 |
| `sp_split_cadena` | `bdicnweb` | no | L126 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L92 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha_Aplicacion IS NULL OR pNombreArchivo = '' OR pBloqueIn` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | FÓRMULA | `LET cBloqueInf2 = TRIM(cBloqueInf2)\|\|'+';` |  |
| L138 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?prioritypa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?prioritypa`, `ss` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_combocategoriapp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_combocategoriapp.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 266 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "PP — Pago Programado, combo y categoría" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_combocategoriapp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `sCategoria` | `SMALLINT` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `catcategoriappass` | `bdicred` | ⚠️ sí | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `categoria` | ENTIDAD | categoría | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_pp_comboestatuspp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_comboestatuspp.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 265 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "PP — Pago Programado, combo y estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_comboestatuspp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIsamErr` | `INTEGER` | L9 |
| `cErrorInfo` | `CHAR(80)` | L10 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cClaveStatus` | `CHAR(1)` | L13 |
| `cDescripcion` | `CHAR(40)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusppass` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_pp_combotarjetaspp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_combotarjetaspp.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 264 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "PP — Pago Programado, combo y tarjetas" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_combotarjetaspp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iIsamErr` | `INTEGER` | L14 |
| `cErrorInfo` | `CHAR(80)` | L15 |
| `cTipoTarjeta` | `CHAR(1)` | L17 |
| `cTarjetasPp` | `CHAR(20)` | L18 |
| `cNumTarjeta` | `CHAR(20)` | L19 |
| `cNombre` | `CHAR(30)` | L20 |
| `dExpiracion` | `DATE` | L21 |
| `cStatusTar` | `CHAR(1)` | L22 |
| `sCategoria` | `SMALLINT` | L23 |
| `iRecuperacion` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_tarjeta_ppass` | `bdicred` | ⚠️ sí | SELECT | L69 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | 🔵 CONVENCIÓN | nombre_sp |
| `tarjetas` | ENTIDAD | tarjetas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_pp_combotipoenvio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_combotipoenvio.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 263 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía PP — Pago Programado y combo (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_combotipoenvio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIsamErr` | `INTEGER` | L9 |
| `cErrorInfo` | `CHAR(80)` | L10 |
| `cDescEnvio` | `CHAR(35)` | L11 |
| `sTipoEnvio` | `SMALLINT` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tipo_envio` | `bdicred` | ⚠️ sí | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `envio` | ACCION | envía | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_pp_combotipooperacionpp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_combotipooperacionpp.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 262 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación PP — Pago Programado y combo (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_combotipooperacionpp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIsamErr` | `INTEGER` | L9 |
| `cErrorInfo` | `CHAR(80)` | L10 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cClaveOperacion` | `CHAR(1)` | L13 |
| `cDescripcion` | `CHAR(40)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tipomanttoppass` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `operacion` | ACCION | operación | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_pp_consultacategorias`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_consultacategorias.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 261 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta PP — Pago Programado y categoría" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultacategorias(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)		AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iIsamErr` | `INTEGER` | L11 |
| `cErrorInfo` | `CHAR(80)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `sIdCategoria` | `SMALLINT` | L14 |
| `iAccesoGratisAd` | `INTEGER` | L15 |
| `iAccesoGratis` | `INTEGER` | L16 |
| `dCuota` | `DECIMAL(14,6)` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `catcategoriappass` | `bdicred` | ⚠️ sí | SELECT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L47 | VALIDACIÓN_NULL | `IF NVL(pUsuario,'') = '' OR NVL(pIdFuncion,'') = '' OR pRegistros IS NULL OR pRecuperacion IS NULL T` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L71 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `categoria` | ENTIDAD | categoría | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_consultacategorias_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_consultacategorias_totales.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 260 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta PP — Pago Programado y categoría (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultacategorias_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iTotalRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `catcategoriappass` | `bdicred` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `categoria` | ENTIDAD | categoría | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_consultacorreo_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_consultacorreo_totales.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 258 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta PP — Pago Programado y correo electrónico (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultacorreo_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iTotalRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `catcorreosppass` | `bdicred` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `correo` | ENTIDAD | correo electrónico | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pp_consultacorreos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_consultacorreos.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 259 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta PP — Pago Programado y correos electrónicos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultacorreos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iIsamErr` | `INTEGER` | L12 |
| `cErrorInfo` | `CHAR(80)` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `sIdCorreo` | `SMALLINT` | L15 |
| `cCompany` | `CHAR(25)` | L16 |
| `cEmail` | `CHAR(100)` | L17 |
| `cStatus` | `CHAR(1)` | L18 |
| `cDescEnvio` | `CHAR(35)` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `catcorreosppass` | `bdicred` | ⚠️ sí | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF NVL(pUsuario,'') = '' OR NVL(pIdFuncion,'') = '' OR pRegistros IS NULL OR pRecuperacion IS NULL T` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `correos` | ENTIDAD | correos electrónicos (email) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_pp_consultarchivoscarga`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_consultarchivoscarga.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 257 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar PP — Pago Programado, CHI — formato/protocolo de consulta al Buró de Crédito y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultarchivoscarga(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iIsamErr` | `INTEGER` | L13 |
| `cErrorInfo` | `CHAR(80)` | L14 |
| `iRecuperacion` | `INTEGER` | L16 |
| `cFechaAplicacion` | `CHAR(10)` | L17 |
| `cFechaAlta` | `CHAR(10)` | L18 |
| `cFechaMod` | `CHAR(10)` | L19 |
| `cNombreArchivo` | `CHAR(70)` | L20 |
| `cIdUsuario` | `CHAR(8)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_bitacora_cargaarchivo_ppass` | `bdicnweb` | no | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L50 | VALIDACIÓN_NULL | `IF NVL(pUsuario,'') = '' OR NVL(pIdFuncion,'') = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THE` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L75 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?v` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_consultarchivoscarga_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_consultarchivoscarga_totales.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 256 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar PP — Pago Programado, CHI — formato/protocolo de consulta al Buró de Crédito y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultarchivoscarga_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_bitacora_cargaarchivo_ppass` | `bdicnweb` | no | SELECT | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?v` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_consultastatusprogpp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_consultastatusprogpp.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 255 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta PP — Pago Programado, estatus y ro — Rol Operativo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultastatusprogpp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCredito                  CHAR(20)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIsamErr` | `INTEGER` | L8 |
| `cErrorInfo` | `CHAR(80)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cClaveStatus` | `CHAR(1)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_tarjeta_ppass` | `bdicred` | ⚠️ sí | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_generareportedetallemovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_generareportedetallemovimientos.sql` |
| **LOC (1er CREATE)** | 248 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 254 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar PP — Pago Programado, detalle y movimientos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_generareportedetallemovimientos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pInicio                      DATE
  pFin                         DATE
  pTarjeta                     CHAR(20)
  pRuta                        CHAR(100)
  pDolares                     CHAR(3)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pInicio` | `DATE` | — | — |
| `pFin` | `DATE` | — | — |
| `pTarjeta` | `CHAR(20)` | — | — |
| `pRuta` | `CHAR(100)` | — | — |
| `pDolares` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cDescCodRet` | `CHAR(80)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `iIdRegistro` | `INTEGER` | L13 |
| `iTotal` | `INTEGER` | L14 |
| `iSerial` | `INTEGER` | L16 |
| `cRespMensaje` | `CHAR(45)` | L17 |
| `iRegistros` | `INTEGER` | L18 |
| `cQuery` | `CHAR(255)` | L19 |
| `cCmd1` | `CHAR(7000)` | L20 |
| `cSql` | `CHAR(7500)` | L21 |
| `cRutaGral` | `CHAR(150)` | L22 |
| `cRutaInformix` | `CHAR(100)` | L23 |
| `cUsrBin` | `CHAR(100)` | L24 |
| `iCountRep` | `INTEGER` | L25 |
| `iProcesaRep` | `INT` | L26 |
| `iArmaReporte` | `INT` | L27 |
| `cArchivoCP` | `CHAR(45)` | L28 |
| `cCmdQuery` | `CHAR(7000)` | L29 |
| `bInTransaction` | `BOOLEAN` | L30 |
| `ven_transacc` | `SMALLINT` | L31 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(5)` | L32 |
| *…11 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_tarjeta_ppass` | `bdicred` | ⚠️ sí | SELECT | L132 |
| `catcategoriappass` | `bdicred` | ⚠️ sí | SELECT | L136 |
| `sd_movmes_ppass` | `bdicred` | ⚠️ sí | SELECT | L140 |
| `systables` | `bdicnweb` | no | SELECT | L151 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L111 | VALIDACIÓN_NULL | `IF NVL(pUsuario,'') = '' OR NVL(pIdFuncion,'') = '' OR NVL(pTarjeta,'') = '' OR pInicio IS NULL OR p` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L123 | FÓRMULA | `LET pRuta = TRIM(pRuta) \|\| '/';` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L152 | FÓRMULA | `LET cCmd1 = "" \|\| TRIM(cCmd1)\|\| " SELECT * FROM (SELECT TO_CHAR(A.fecha_visita, '%d/%m/%Y'), TO_` |  |
| L154 | FÓRMULA | `LET cCmd1 = "" \|\| TRIM(cCmd1)\|\| " TO_CHAR((CASE WHEN ("\|\|iAccGratis\|\|" >= A.total_visitas) T` |  |
| L155 | FÓRMULA | `LET cCmd1 = "" \|\| TRIM(cCmd1)\|\| " TO_CHAR(((CASE WHEN ("\|\|iAccGratis\|\|" >= A.total_visitas) ` |  |
| L157 | FÓRMULA | `LET cCmd1 = "" \|\| TRIM(cCmd1)\|\| " AND hora_tc = (SELECT MAX(hora_tc) FROM bdinteg:""informix"".s` |  |
| L174 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRuta)\|\|'query.sql';` |  |
| L183 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L188 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L197 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L206 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L214 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_generareporteportafolio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_generareporteportafolio.sql` |
| **LOC (1er CREATE)** | 214 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 253 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar PP — Pago Programado y folio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_generareporteportafolio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pRuta                        CHAR(100)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | `por`=por (criterio) · `por`=por (criterio) | ✅ CÓDIGO |
| `pRuta` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iIdRegistro` | `INTEGER` | L12 |
| `iSerial` | `INTEGER` | L14 |
| `cRespMensaje` | `CHAR(45)` | L15 |
| `iRegistros` | `INTEGER` | L16 |
| `cQuery` | `CHAR(255)` | L17 |
| `cCmd1` | `CHAR(2000)` | L18 |
| `cSql` | `CHAR(2500)` | L19 |
| `cRutaGral` | `CHAR(150)` | L20 |
| `cRutaInformix` | `CHAR(100)` | L21 |
| `cUsrBin` | `CHAR(100)` | L22 |
| `iCountRep` | `INTEGER` | L23 |
| `iProcesaRep` | `INT` | L24 |
| `iArmaReporte` | `INT` | L25 |
| `cArchivoCP` | `CHAR(45)` | L26 |
| `cCmdQuery` | `CHAR(2500)` | L27 |
| `bInTransaction` | `BOOLEAN` | L28 |
| `ven_transacc` | `SMALLINT` | L29 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(5)` | L30 |
| `cReporte` | `CHAR(100)` | L31 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L119 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L101 | FÓRMULA | `LET pRuta = TRIM(pRuta) \|\| '/';` |  |
| L119 | FÓRMULA | `LET cCmd1 = "SELECT 'Numero de Credito','Numero de Tarjeta Relacionado al Credito','Titular/Adiciona` |  |
| L120 | FÓRMULA | `LET cCmd1 = ""\|\|TRIM(cCmd1)\|\| " UNION ALL SELECT ''''\|\|TO_CHAR(A.num_credito), ''''\|\|TO_CHAR` |  |
| L121 | FÓRMULA | `LET cCmd1 = ""\|\|TRIM(cCmd1)\|\| " TO_CHAR((A.accesos_adic + A.accesos)) AS accesos_usados, TO_CHAR` |  |
| L122 | FÓRMULA | `LET cCmd1 = ""\|\|TRIM(cCmd1)\|\| " TO_CHAR((C.acceso_gratis + C.acceso_gratis_ad)) AS accesos_factu` |  |
| L129 | FÓRMULA | `LET cCmd1 = "SELECT 'Numero de Credito','Numero de Tarjeta Relacionado al Credito','Titular/Adiciona` |  |
| L131 | FÓRMULA | `LET cCmd1 = ""\|\|TRIM(cCmd1)\|\| " CASE WHEN (A.status_tar = 'A') THEN 'ACTIVA' ELSE 'CANCELADA' EN` | 🔴 MONEY/aritmética financiera |
| L149 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRuta)\|\|'query.sql';` |  |
| L158 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L163 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L172 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L181 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L189 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `folio` | ENTIDAD | folio | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?te`, `?ta` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_insertalayoutpp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_pp_insertalayoutpp.sql` |
| **LOC (1er CREATE)** | 323 |
| **Callgraph** | ✅ fan_in=0 / fan_out=89 |
| **Deps concatenadas** | 252 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta PP — Pago Programado y layout — formato de archivo de intercambio interbancario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_insertalayoutpp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEnvio                       CHAR(1)
  pSucursal                    CHAR(4)
  pNumTarjeta                  CHAR(20)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEnvio` | `CHAR(1)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIsamErr` | `INTEGER` | L9 |
| `cErrorInfo` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNumCredito` | `CHAR(20)` | L13 |
| `cNumCte` | `CHAR(20)` | L14 |
| `cNumTarjeta` | `CHAR(20)` | L15 |
| `cNumTarjetaPP` | `CHAR(20)` | L16 |
| `cNombreTarjeta` | `CHAR(20)` | L17 |
| `cSucursal` | `CHAR(4)` | L18 |
| `dExpiracion` | `DATE` | L19 |
| `dMiembroDesde` | `DATE` | L20 |
| `iMaximo` | `INTEGER` | L21 |
| `cNombre1` | `CHAR(26)` | L23 |
| `cApellPaterno` | `CHAR(26)` | L24 |
| `cTipoTarjeta` | `CHAR(1)` | L25 |
| `cDescTipoTarjeta` | `CHAR(10)` | L26 |
| `cDomicilio` | `CHAR(150)` | L27 |
| `cDireccion1` | `CHAR(40)` | L28 |
| `cDireccion2` | `CHAR(40)` | L29 |
| `cCiudad` | `CHAR(3)` | L30 |
| `cEstado` | `CHAR(2)` | L31 |
| `cDescCiudad` | `CHAR(30)` | L32 |
| `cDescEstado` | `CHAR(30)` | L33 |
| *…15 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_tarjeta_ppass` | `bdicred` | ⚠️ sí | SELECT | L131 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L137 |
| `solicitudtarjeta` | `intercard` | ⚠️ sí | SELECT | L142 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L149 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L169 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L183 |
| `si_estados` | `bdinteg` | ⚠️ sí | SELECT | L189 |
| `si_catciudades` | `bdinteg` | ⚠️ sí | SELECT | L194 |
| `sd_info_layout_ppass` | `bdicred` | ⚠️ sí | SELECT | L211 |
| `sd_info_layout_ppass` | `bdicred` | ⚠️ sí | INSERT | L215 |
| `systables` | `bdicnweb` | no | SELECT | L230 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L124 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L56 | CÓDIGO_RETORNO | `LET cCodRet					= '00000';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L213 | FÓRMULA | `LET iMaximo = NVL(iMaximo, 0) + 1;` |  |
| L230 | FÓRMULA | `LET cCmd1 = "SELECT '#','PAN','MIEMBRO DESDE','NOMBRE COMPLETO','NOMBRE','APELLIDO','ID','FECHA EXP'` |  |
| L248 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRutaDescarga)\|\|'query.sql';` |  |
| L257 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L262 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L271 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L280 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L288 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `layout` | ENTIDAD | layout — formato de archivo de intercambio interbancario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_presentadordomi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_presentadordomi.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 58 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "domiciliación (presentado)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_actualizamonitorprocesos`, `sp_cnsif_confirmaejecutivo`, `bdidomi` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_presentadordomi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(20)
  pMac                         CHAR(18)
  pIp                          VARCHAR(16)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(20)` | — | — |
| `pMac` | `CHAR(18)` | — | — |
| `pIp` | `VARCHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cNombreArchivo` | `CHAR(20)` | L9 |
| `cMensajeRespuesta` | `CHAR(100)` | L10 |
| `bInTransaccion` | `BOOLEAN` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_actualizamonitorprocesos` | `bdicnweb` | no | L26 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `bdidomi` | `bdicnweb` | no | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "bdidomi:'informix'.sp_domi_presentador";` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00521';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00522';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00675';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00676';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00084';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00526';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00677';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00574';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00678';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00530';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00679';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00680';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00532';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00533';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00534';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00535';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00594';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00492';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00538';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00549';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00550';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00551';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00552';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00540';` |  |
| L125 | CÓDIGO_RETORNO | `LET cCodRet = '00553';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `presentado` | MODIF | presentado (a cobro) | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `domi` | ENTIDAD | domiciliación | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_procesaarchivodepositocoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_procesaarchivodepositocoppel.sql` |
| **LOC (1er CREATE)** | 272 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 82 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa archivo, depósito, OS — Originación de Solicitudes y Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 9 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_procesaarchivodepositocoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cClave_transaccion` | `CHAR(4)` | L6 |
| `cDesc_transaccion` | `CHAR(50)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `dFecha` | `DATE` | L11 |
| `cComprobante` | `CHAR(10)` | L12 |
| `cSucCoppel` | `CHAR(10)` | L13 |
| `cNombreSuc` | `CHAR(40)` | L14 |
| `cCajaGeneral` | `CHAR(4)` | L15 |
| `cPlaza` | `CHAR(20)` | L16 |
| `mImpComprobante` | `MONEY(14,2)` | L17 |
| `mImpFicha` | `MONEY(14,2)` | L18 |
| `iCantidad1` | `INTEGER` | L19 |
| `iCantidad2` | `INTEGER` | L20 |
| `iCantidad3` | `INTEGER` | L21 |
| `iCantidad4` | `INTEGER` | L22 |
| `iCantidad5` | `INTEGER` | L23 |
| `iCantidad6` | `INTEGER` | L24 |
| `iCantidad7` | `MONEY(14,2)` | L25 |
| `mFaltante` | `MONEY(14,2)` | L26 |
| `mSobrante` | `MONEY(14,2)` | L27 |
| `iTotReg` | `INTEGER` | L28 |
| `cMensaje` | `CHAR(60)` | L29 |
| *…8 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatuscargaarchivocoppel` | `bdicnweb` | no | SELECT | L93 |
| `sw_verificastatuscargaarchivocoppel` | `bdicnweb` | no | INSERT | L95 |
| `sw_cargatmpdepositoscoppel` | `bdicnweb` | no | SELECT | L125 |
| `ss_cajageneral` | `bdisuc` | ⚠️ sí | SELECT | L129 |
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L144 |
| `ss_temp_deposito_coppel` | `bdisuc` | ⚠️ sí | SELECT | L197 |
| `ss_bitacora_deposito_coppel` | `bdisuc` | ⚠️ sí | SELECT | L206 |
| `ss_param_cajagen` | `bdisuc` | ⚠️ sí | SELECT | L230 |
| `ss_temp_deposito_coppel` | `bdisuc` | ⚠️ sí | INSERT | L244 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L162 | FÓRMULA | `LET iCajasFalta = iCajasFalta +1;` |  |
| L214 | FÓRMULA | `LET iCalculo = mFaltante / 100;` |  |
| L215 | FÓRMULA | `LET iCantidad4 = iCantidad4 + iCalculo;` |  |
| L217 | FÓRMULA | `LET iCantidad7 = iCantidad7 + iResto;` |  |
| L221 | FÓRMULA | `LET iCalculo = mSobrante / 100;` |  |
| L222 | FÓRMULA | `LET iCantidad4 = iCantidad4 - iCalculo;` |  |
| L224 | FÓRMULA | `LET iCantidad7 = iCantidad7 - iResto;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ito` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ito` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_procesacargoscuenta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_procesacargoscuenta.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=20 / fan_out=6 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa cargo y cuenta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `procesa_cargos` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_procesacargoscuenta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdProceso                   CHAR(1)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdProceso` | `CHAR(1)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iTotalRegistros` | `INTEGER` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `bInTransaccion` | `BOOLEAN` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_statuscargocta` | `bdicnweb` | no | SELECT | L41 |
| `sw_cr_statuscargocta` | `bdicnweb` | no | DELETE | L41 |
| `sw_cr_statuscargocta` | `bdicnweb` | no | INSERT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `procesa_cargos` | `bditef` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdProceso = '' OR pFecha IS NULL THEN` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIï¿½N DEL SP bditef:procesa_cargos';` |  |
| L102 | FÓRMULA | `LET cCodRet = TRIM(cCodRetSp); --ocurrio un error al procesar` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_procesarsolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_procesarsolicitudmc.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa solicitud y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_procesarsolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pTipoMotivo                  SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pTipoMotivo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cNombreAtiende` | `CHAR(80)` | L9 |
| `bInTransaction` | `BOOLEAN` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_cte_procesando` | `bdisolic` | ⚠️ sí | SELECT | L73 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L44 |
| `bdicnweb` | `bdicnweb` | no | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pTipoMotivo IS NULL THEN` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_solicitudprocesandomc';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '90000';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:sp_solicitudprocesandomc';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_procesartratamientoarchivo_tef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_procesartratamientoarchivo_tef.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 38 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa archivo y TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_procesartratamientoarchivo_tef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRuta                        CHAR(100)
  pNombreArchivo               CHAR(50)
  pTipoOperacion               SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRuta` | `CHAR(100)` | — | — |
| `pNombreArchivo` | `CHAR(50)` | `archivo`=archivo | ✅ CÓDIGO |
| `pTipoOperacion` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cRutaArchivo` | `CHAR(150)` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRuta = '' OR pNombreArchivo = '' OR pTipoOperacion IS NULL T` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00440';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?rtratamiento` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rtratamiento` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_puntoscompro_consulta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_puntoscompro_consulta.sql` |
| **LOC (1er CREATE)** | 173 |
| **Callgraph** | ✅ fan_in=0 / fan_out=112 |
| **Deps concatenadas** | 334 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta puntos y ro — Rol Operativo (complemento)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_puntoscompromiso` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_puntoscompro_consulta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  psProdInd                    CHAR(2)
  psIdPOSATM                   CHAR(19)
  psGiro                       CHAR(4)
  psTarjetas                   CHAR(570)
  psModoCaptura                CHAR(2)
  psCodigoIso                  CHAR(2)
  pdtFechaIni                  DATE
  pdtFechaFin                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `psProdInd` | `CHAR(2)` | `sp`=stored procedure · `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔵 CONVENCIÓN / 🔴 SINTÉTICO |
| `psIdPOSATM` | `CHAR(19)` | — | — |
| `psGiro` | `CHAR(4)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `psTarjetas` | `CHAR(570)` | — | — |
| `psModoCaptura` | `CHAR(2)` | — | — |
| `psCodigoIso` | `CHAR(2)` | — | — |
| `pdtFechaIni` | `DATE` | — | — |
| `pdtFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cCodRetSp` | `CHAR(6)` | L30 |
| `iCodRetSp` | `INTEGER` | L31 |
| `iRecuperacion` | `INTEGER` | L32 |
| `iContador` | `INTEGER` | L34 |
| `cStatus` | `CHAR(3)` | L35 |
| `cNumCliente` | `CHAR(13)` | L36 |
| `cSucursal` | `CHAR(5)` | L37 |
| `cNumTarjeta` | `CHAR(16)` | L38 |
| `cInfReceptor` | `CHAR(40)` | L39 |
| `cFechaHoraInAuth` | `CHAR(25)` | L40 |
| `dFechaHoraInAuth` | `DATETIME YEAR TO FRACTION(5)` | L41 |
| `mMonto` | `MONEY (19,4)` | L42 |
| `cIdTerminal` | `CHAR(16)` | L43 |
| `cIdReceptor` | `CHAR(4)` | L44 |
| `cMetodoCaptura` | `CHAR(2)` | L45 |
| `cCodigoIso` | `CHAR(2)` | L46 |
| `cMotivo` | `CHAR(70)` | L47 |
| `iLoteTarjeta` | `INTEGER` | L48 |
| `cSecuencia` | `CHAR(7)` | L49 |
| `cReferencia` | `CHAR(12)` | L50 |
| `cNumCuenta` | `CHAR(13)` | L51 |
| `cCiudadComercio` | `CHAR(50)` | L52 |
| `cGiroComercio` | `CHAR(4)` | L53 |
| *…4 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L107 |
| `sp_puntoscompromiso` | `intercard` | ⚠️ sí | L118 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L100 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR psProdInd ='' OR pdtFechaIni IS NULL OR pdtFechaFin IS NULL T` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicheq:'informix'.sp_puntoscompromiso";` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L145 | FÓRMULA | `LET cFechaHoraInAuth= TO_CHAR(dFechaHoraInAuth, '%d/%m/%Y %I:%M:%S %p');			LET cHoraOperacion=TO_CHA` |  |
| L146 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_puntoscompro_consulta_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_puntoscompro_consulta_totales.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=111 |
| **Deps concatenadas** | 333 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta puntos y ro — Rol Operativo (complemento, totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_puntoscompro_consulta_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  psProdInd                    VARCHAR(2)
  psIdPOSATM                   VARCHAR(19)
  psGiro                       VARCHAR(4)
  psTarjetas                   CHAR(570)
  psModoCaptura                VARCHAR(2)
  psCodigoIso                  VARCHAR(2)
  pdtFechaIni                  DATE
  pdtFechaFin                  DATE
) RETURNING CHAR(5) 	AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `psProdInd` | `VARCHAR(2)` | `sp`=stored procedure · `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔵 CONVENCIÓN / 🔴 SINTÉTICO |
| `psIdPOSATM` | `VARCHAR(19)` | — | — |
| `psGiro` | `VARCHAR(4)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `psTarjetas` | `CHAR(570)` | — | — |
| `psModoCaptura` | `VARCHAR(2)` | — | — |
| `psCodigoIso` | `VARCHAR(2)` | — | — |
| `pdtFechaIni` | `DATE` | — | — |
| `pdtFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |
| `iTotal` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusconsultapuntoscompromiso` | `bdicnweb` | no | SELECT | L40 |
| `statusconsultapuntoscompromiso` | `bdicnweb` | no | DELETE | L40 |
| `statusconsultapuntoscompromiso` | `bdicnweb` | no | INSERT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |
| `sp_puntoscompromiso2_totales` | `intercard` | ⚠️ sí | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR psProdInd ='' OR pdtFechaIni IS NULL OR pdtFechaFin IS NULL T` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP intercard:sp_puntoscompromiso2_` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00450';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00450';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_puntoscompro_metodocaptura`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_puntoscompro_metodocaptura.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "captura puntos y ro — Rol Operativo (complemento)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_metodocaptura` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_puntoscompro_metodocaptura(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iErr` | `INTEGER` | L10 |
| `cCveMetodo` | `CHAR(2)` | L11 |
| `cDescripcion` | `CHAR(30)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |
| `sp_metodocaptura` | `intercard` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION iErr, 0, 'ERROR EN LA EJECUCIÓN DEL SP intercard:sp_metodocaptura';` |  |
| L55 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?_metodo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `captura` | ACCION | captura | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ro`, `?_metodo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_puntoscoppelmax`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_puntoscoppelmax.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=53 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "puntos y Coppel (máximo)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_coppelmax_consultarpuntos`, `sp_coppelmax_modificarpuntos` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_puntoscoppelmax(
  pBandera                     CHAR(1)
  pSBandera                    CHAR(1)
  pNumcte                      CHAR(20)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pRow_id                      INTEGER
  pPuntos_beneficio            DECIMAL(16,2)
  pReferencia23                CHAR(23)
  pOrigen                      CHAR(30)
  pNumcredito                  CHAR(20)
  pNumtarjeta                  CHAR(16)
  pNumProducto                 CHAR(4)
) RETURNING VARCHAR(5,0)  AS codRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(1)` | — | — |
| `pSBandera` | `CHAR(1)` | — | — |
| `pNumcte` | `CHAR(20)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pRow_id` | `INTEGER` | — | — |
| `pPuntos_beneficio` | `DECIMAL(16,2)` | `puntos`=puntos (recompensas) | ✅ CÓDIGO |
| `pReferencia23` | `CHAR(23)` | — | — |
| `pOrigen` | `CHAR(30)` | — | — |
| `pNumcredito` | `CHAR(20)` | — | — |
| `pNumtarjeta` | `CHAR(16)` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `cRow_id` | `INTEGER` | L26 |
| `cFechaMovimiento` | `CHAR(30)` | L27 |
| `cDescripcion` | `CHAR(40)` | L28 |
| `cNaturaleza` | `CHAR(2)` | L29 |
| `cMonto` | `DECIMAL(16,2)` | L30 |
| `cBeneficio_calculado` | `DECIMAL(16,2)` | L31 |
| `cPuntos_totales` | `DECIMAL(16,2)` | L32 |
| `cSaldo_actual` | `DECIMAL(16,2)` | L33 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_coppelmax_consultarpuntos` | `bdicnweb` | no | L74 |
| `sp_coppelmax_modificarpuntos` | `bdicnweb` | no | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `max` | MODIF | máximo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_realizarreversocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_realizarreversocre.sql` |
| **LOC (1er CREATE)** | 49 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "realiza crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_realizarreversocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              CHAR(10)
  pLote                        INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `CHAR(10)` | — | — |
| `pLote` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iReversar` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00171';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `realiza` | ACCION | realiza / ejecuta una operación SPEI | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reasignacionctesusuariofusion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reasignacionctesusuariofusion.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 46 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "asigna clientes y usuario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_reasignacionctesusuariofusion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegistroCliente           INTEGER
  pUsuarioOrigen               CHAR(20)
  pUsuarioDestino              CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegistroCliente` | `INTEGER` | — | — |
| `pUsuarioOrigen` | `CHAR(20)` | `usuario`=usuario | ✅ CÓDIGO |
| `pUsuarioDestino` | `CHAR(20)` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `iRowId` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegistroCliente IS NULL OR pUsuarioOrigen = '' OR pUsuario` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00281';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fusion` | ACCION | fusiona cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_re`, `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_recibedatosarchivoimagenes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_recibedatosarchivoimagenes.sql` |
| **LOC (1er CREATE)** | 452 |
| **Callgraph** | ✅ fan_in=9 / fan_out=3 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recepción datos, archivo y imágenes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_recibedatosarchivoimagenes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEntrada                   CHAR(1)
  pNombreArchivo               CHAR(22)
  pIdRegistro                  CHAR(2)
  pBloqueArchivo               CHAR(117)
  pNroSecuencia                INTEGER
  pDireccionMac                CHAR(12)
  pFechaHoy                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEntrada` | `CHAR(1)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `archivo`=archivo | ✅ CÓDIGO |
| `pIdRegistro` | `CHAR(2)` | — | — |
| `pBloqueArchivo` | `CHAR(117)` | `archivo`=archivo | ✅ CÓDIGO |
| `pNroSecuencia` | `INTEGER` | — | — |
| `pDireccionMac` | `CHAR(12)` | `rec`=recepción / recibe | 🔵 CONVENCIÓN |
| `pFechaHoy` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cIdCodRet` | `CHAR(5)` | L7 |
| `cDescIdCodRet` | `CHAR(100)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cIdCodRetSp` | `CHAR(6)` | L10 |
| `cDescMensajeError` | `CHAR(100)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cRenglon` | `CHAR(400)` | L14 |
| `iLinea` | `INTEGER` | L15 |
| `cSeccion` | `CHAR(12)` | L16 |
| `cCampo` | `CHAR(2)` | L17 |
| `cTexto` | `CHAR(100)` | L18 |
| `cBloqueArchivo` | `CHAR(117)` | L19 |
| `cEncTipoRegistro` | `CHAR(2)` | L20 |
| `cEncNumSecuencia` | `CHAR(7)` | L21 |
| `cEncVersion` | `CHAR(3)` | L22 |
| `cEncCodOperacion` | `CHAR(2)` | L23 |
| `cEncBancoPresenta` | `CHAR(3)` | L24 |
| `cEncSentido` | `CHAR(1)` | L25 |
| `cEncMoneda` | `CHAR(1)` | L26 |
| `cEncNumBloque` | `CHAR(7)` | L27 |
| `cEncFechaProceso` | `CHAR(8)` | L28 |
| `cEncUsoFuturo` | `CHAR(83)` | L29 |
| `iContEncabezado` | `INTEGER` | L30 |
| *…53 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_bitacoraerrorimg` | `bdicnweb` | no | SELECT | L201 |
| `sw_cr_bitacoraerrorimg` | `bdicnweb` | no | DELETE | L201 |
| `sw_cr_procesaencabezadoimg_tmp` | `bdicnweb` | no | SELECT | L202 |
| `sw_cr_procesaencabezadoimg_tmp` | `bdicnweb` | no | DELETE | L202 |
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | SELECT | L203 |
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | DELETE | L203 |
| `sw_cr_procesasumarioimg_tmp` | `bdicnweb` | no | SELECT | L204 |
| `sw_cr_procesasumarioimg_tmp` | `bdicnweb` | no | DELETE | L204 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L208 |
| `si_empresas` | `bdinteg` | ⚠️ sí | SELECT | L210 |
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L212 |
| `sw_cr_bitacoraerrorimg` | `bdicnweb` | no | INSERT | L226 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | INSERT | L230 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | SELECT | L231 |
| `sw_cr_procesaencabezadoimg_tmp` | `bdicnweb` | no | INSERT | L300 |
| `sw_cr_procesadetalleimg_tmp` | `bdicnweb` | no | INSERT | L382 |
| `sw_cr_procesasumarioimg_tmp` | `bdicnweb` | no | INSERT | L427 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L181 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L86 | CÓDIGO_RETORNO | `LET cIdCodRet = '00000';` |  |
| L174 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdEntrada = '' OR pNombreArchivo = '' OR pIdRegistro = '' OR` |  |
| L176 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L218 | FÓRMULA | `LET iLinea = NVL(pNroSecuencia,0) + 1; --(entra con 0 en la primera ejecuciï¿½n, el conteo increment` |  |
| L239 | FÓRMULA | `LET iContEncabezado = iContEncabezado + 1;` |  |
| L250 | FÓRMULA | `LET cEncUsoFuturo = SUBSTR(pBloqueArchivo,35,83); --= 118` |  |
| L311 | FÓRMULA | `LET iNroSecuencia = NVL(pNroSecuencia,0) + 1; --(entra con 1 en la primera ejecuciï¿½n)` |  |
| L333 | FÓRMULA | `LET cDetTamImgReverso = SUBSTR(pBloqueArchivo,103,15); --= 118` |  |
| L372 | FÓRMULA | `LET mValorImporte = SUBSTR(cDetImporte,1,(LENGTH(cDetImporte)-2))\|\|"."\|\|SUBSTR(cDetImporte,(LENG` | 🔴 MONEY/aritmética financiera |
| L401 | FÓRMULA | `LET iContSumario = iContSumario + 1;` |  |
| L407 | FÓRMULA | `LET cSumUsoFuturo = SUBSTR(pBloqueArchivo,35,83); --= 118` |  |
| L410 | FÓRMULA | `LET mTotalImporte = SUBSTR(cSumTotalImporte,1,(LENGTH(cSumTotalImporte)-2))\|\|"."\|\|SUBSTR(cSumTot` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?ibe` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `imagenes` | ENTIDAD | imágenes / documentos digitales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ibe` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_regaccesoautenticacionfusioncte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_regaccesoautenticacionfusioncte.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 82 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registro acceso, autenticación y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_regaccesoautenticacionfusioncte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pUsuarioIntentoAcceso        CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pUsuarioIntentoAcceso` | `CHAR(8)` | `acceso`=acceso | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `cAbreAplicacion` | `CHAR(2)` | L9 |
| `cStatus` | `CHAR(100)` | L10 |
| `cTipoReporte` | `CHAR(1)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fusbitacora` | `bdinteg` | ⚠️ sí | INSERT | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `acceso` | ENTIDAD | acceso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `autenticacion` | ENTIDAD | autenticación | 🔵 CONVENCIÓN | nombre_sp |
| `fusion` | ACCION | fusiona cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_regimenfiscales_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_regimenfiscales_totales.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registro [polisemia] Cálculo (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_regimenfiscales_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRet` | `CHAR(5)` | L9 |
| `cRegimenFiscal` | `CHAR(3)` | L10 |
| `cDescripcion` | `CHAR(50)` | L11 |
| `ctipo` | `CHAR(1)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_regimen_fiscal` | `bdinteg` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet         = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?imenfis` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cal` | ENTIDAD | [polisemia] Cálculo (cal_fecha, cal_riesgo_cliente, cal_trad | 🟡 INFERIDO | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imenfis`, `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_registractualizacelulasccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_registractualizacelulasccl.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registra celular y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_registractualizacelulasccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEmpleado                    CHAR(8)
  pNombre                      CHAR(104)
  pStatus                      CHAR(1)
  pFuncion                     SMALLINT
  pCedula                      SMALLINT
  pTipo                        SMALLINT
  pFuncionAnt                  SMALLINT
  pCedulaAnt                   SMALLINT
  pStatusAnt                   SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEmpleado` | `CHAR(8)` | — | — |
| `pNombre` | `CHAR(104)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pFuncion` | `SMALLINT` | — | — |
| `pCedula` | `SMALLINT` | — | — |
| `pTipo` | `SMALLINT` | — | — |
| `pFuncionAnt` | `SMALLINT` | — | — |
| `pCedulaAnt` | `SMALLINT` | — | — |
| `pStatusAnt` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |
| `bdicnweb` | `bdicnweb` | no | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pEmpleado = '' OR pNombre = '' OR pStatus = '' OR pFuncion IS` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_usuarioscedulasmantto ";` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00582';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `?ctualiza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |
| `?ulas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ctualiza`, `?ulas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_registractualizacelulascclb5`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_registractualizacelulascclb5.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registra celular y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (sufijo de versión de SP)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_registractualizacelulascclb5(
  pEmpleado                    CHAR(8)
  pNombre                      CHAR(104)
  pStatus                      CHAR(1)
  pFuncion                     SMALLINT
  pCedula                      SMALLINT
  pTipo                        SMALLINT
  pFuncionAnt                  SMALLINT
  pCedulaAnt                   SMALLINT
  pStatusAnt                   SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpleado` | `CHAR(8)` | — | — |
| `pNombre` | `CHAR(104)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pFuncion` | `SMALLINT` | — | — |
| `pCedula` | `SMALLINT` | — | — |
| `pTipo` | `SMALLINT` | — | — |
| `pFuncionAnt` | `SMALLINT` | — | — |
| `pCedulaAnt` | `SMALLINT` | — | — |
| `pStatusAnt` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicnweb` | `bdicnweb` | no | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pEmpleado = '' OR pNombre = '' OR pStatus = '' OR pFuncion IS NULL OR  pCedula IS NULL OR  pTipo ` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L41 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdicnweb:sp_usuarioscedulasmantto ";` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00582';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00004';` |  |
| L47 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `?ctualiza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |
| `?ulas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |
| `b5` | MODIF | sufijo de versión de SP (Bloque/Build 5) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ctualiza`, `?ulas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_registractualizactaspld`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_registractualizactaspld.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 114 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registra" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `registra` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_registractualizactaspld(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pCveBanco                    CHAR(3)
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pCveBanco` | `CHAR(3)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cExiste` | `CHAR(1)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cuentas_pld` | `bdicheq` | ⚠️ sí | SELECT | L49 |
| `sc_cuentas_pld` | `bdicheq` | ⚠️ sí | INSERT | L58 |
| `sc_cuentas_pld` | `bdicheq` | ⚠️ sí | DELETE | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `?ctualiz` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?ld` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ctualiz`, `?a`, `?ld` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_actlimiteremesa_edo_suc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_actlimiteremesa_edo_suc.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza remesa y estado (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_actlimiteremesa_edo_suc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pIdRemesadora                SMALLINT
  pValor                       CHAR(4)
  pPesos                       MONEY(16,2)
  pUsd                         MONEY(16,2)
  pStatus                      SMALLINT
  pLimitecte                   INTEGER
  pBandera                     INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pIdRemesadora` | `SMALLINT` | `rem`=remesa (forma corta) · `remesa`=remesa (Western Union / MoneyGram) | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pValor` | `CHAR(4)` | — | — |
| `pPesos` | `MONEY(16,2)` | — | — |
| `pUsd` | `MONEY(16,2)` | — | — |
| `pStatus` | `SMALLINT` | — | — |
| `pLimitecte` | `INTEGER` | — | — |
| `pBandera` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cAbreviatura` | `CHAR(20)` | L7 |
| `cMarca` | `CHAR(3)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_limite_edo` | `bdisac` | ⚠️ sí | UPDATE | L65 |
| `sac_limite_suc` | `bdisac` | ⚠️ sí | UPDATE | L84 |
| `sac_limite_suc` | `bdisac` | ⚠️ sí | INSERT | L103 |
| `sac_limite_edo_usuario` | `bdisac` | ⚠️ sí | UPDATE | L118 |
| `sac_limite_suc_usuario` | `bdisac` | ⚠️ sí | UPDATE | L137 |
| `sac_limite_suc_usuario` | `bdisac` | ⚠️ sí | INSERT | L156 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdEjecucion = '' OR pIdRemesadora IS NULL OR pValor = '' OR` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L160 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?limite` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | 🔵 CONVENCIÓN | nombre_sp |
| `edo` | ENTIDAD | estado | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `suc` | MODIF | sucursal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?limite` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_actualizalimite`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_actualizalimite.sql` |
| **LOC (1er CREATE)** | 176 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 235 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza remesa" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_actualizalimite(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pRemesa                      CHAR(3)
  pLimiteDom                   INTEGER
  pLimiteTel                   INTEGER
  pActMaxLim                   CHAR(1)
  pValorMaxLim                 CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRemesa` | `CHAR(3)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |
| `pLimiteDom` | `INTEGER` | — | — |
| `pLimiteTel` | `INTEGER` | — | — |
| `pActMaxLim` | `CHAR(1)` | — | — |
| `pValorMaxLim` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iCodParam` | `INTEGER` | L8 |
| `cSucursal` | `CHAR(4)` | L9 |
| `cRemesa` | `CHAR(3)` | L10 |
| `iLimDom` | `INTEGER` | L11 |
| `iLimTel` | `INTEGER` | L12 |
| `cUsuario_insert` | `CHAR(8)` | L13 |
| `dFecha_insert` | `DATETIME YEAR TO SECOND` | L14 |
| `iExiste` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_pldlimite_teldom_excpsuc` | `bdisac` | ⚠️ sí | SELECT | L58 |
| `sac_pldlimite_teldom_excpsuc` | `bdisac` | ⚠️ sí | INSERT | L62 |
| `sac_pldlimite_bitacoramodsoc` | `bdisac` | ⚠️ sí | INSERT | L68 |
| `sac_pldlimite_teldom_excpsuc` | `bdisac` | ⚠️ sí | UPDATE | L78 |
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L107 |
| `sac_param` | `bdisac` | ⚠️ sí | UPDATE | L109 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?limite` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?limite` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_cambioestatusctes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_cambioestatusctes.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 142 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa, cambio, estatus y clientes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: UPDATE, INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_cambioestatusctes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumCliente                  CHAR(20)
  pStatus                      CHAR(1)
  pStatusAnt                   CHAR(1)
  pMotivo                      CHAR(250)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pStatusAnt` | `CHAR(1)` | — | — |
| `pMotivo` | `CHAR(250)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cDescCodRet` | `CHAR(80)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cStatusCte` | `CHAR(1)` | L13 |
| `dFechaStatus` | `CHAR(25)` | L14 |
| `dFechaUltAct` | `DATETIME YEAR TO FRACTION(3)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_cte_remesas` | `bdisac` | ⚠️ sí | SELECT | L56 |
| `sac_cte_remesas` | `bdisac` | ⚠️ sí | UPDATE | L76 |
| `sac_log_cte_his_mov` | `bdisac` | ⚠️ sí | SELECT | L83 |
| `sac_log_cte_his_mov` | `bdisac` | ⚠️ sí | INSERT | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rem_catalogoestado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_catalogoestado.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa, catálogo y estado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_catalogoestado(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdEstado` | `CHAR(2)` | L8 |
| `cDescEstado` | `CHAR(30)` | L9 |
| `cMarca` | `CHAR(3)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_limite_edo` | `bdisac` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `estado` | ENTIDAD | estado (entidad federativa / estatus) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_catalogostatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_catalogostatus.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa, catálogo y estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_catalogostatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdStatus` | `CHAR(2)` | L8 |
| `cDescStatus` | `CHAR(30)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_rem_statuslimite` | `bdicnweb` | no | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_catalogotipolimite`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_catalogotipolimite.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 101 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa y catálogo (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_catalogotipolimite(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdLimite` | `SMALLINT` | L8 |
| `cDescLimite` | `CHAR(100)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_rem_tipolimite` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L68 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?limite` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?limite` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_cattipolimite`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_cattipolimite.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 234 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa y catálogo (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_cattipolimite(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdLimite` | `SMALLINT` | L8 |
| `cDescLimite` | `CHAR(35)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tipolimite` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?limite` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?limite` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_cattiporemesa`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_cattiporemesa.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 233 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa y catálogo (tipo de)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_cattiporemesa(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdRemesa` | `CHAR(10)` | L8 |
| `cDescRemesa` | `CHAR(35)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_cat_tiporemesa` | `bdisac` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cat` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_consactlimiteremesa_monto_acum`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consactlimiteremesa_monto_acum.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y monto" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consactlimiteremesa_monto_acum(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pTipoMonto                   CHAR(1)
  pIdRemesadora                SMALLINT
  pPesos                       MONEY(16,2)
  pUsd                         MONEY(16,2)
  pStatus                      SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pTipoMonto` | `CHAR(1)` | `monto`=monto | ✅ CÓDIGO |
| `pIdRemesadora` | `SMALLINT` | `rem`=remesa (forma corta) · `remesa`=remesa (Western Union / MoneyGram) | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pPesos` | `MONEY(16,2)` | — | — |
| `pUsd` | `MONEY(16,2)` | — | — |
| `pStatus` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cAbreviatura` | `CHAR(20)` | L10 |
| `mUsd` | `MONEY(16,2)` | L11 |
| `mPesos` | `MONEY(16,2)` | L12 |
| `sStatus` | `SMALLINT` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_limite_monto` | `bdisac` | ⚠️ sí | SELECT | L90 |
| `sac_limite_monto` | `bdisac` | ⚠️ sí | UPDATE | L105 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdEjecucion = '' OR pTipoMonto = '' OR pIdRemesadora IS NULL` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L100 | VALIDACIÓN_NULL | `IF pPesos IS NULL OR pUsd IS NULL OR pStatus IS NULL THEN` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?limite` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | 🔵 CONVENCIÓN | nombre_sp |
| `monto` | ENTIDAD | monto | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_acum` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?limite`, `?_acum` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consactlimiteremesa_numtrans`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consactlimiteremesa_numtrans.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, número y [polisemia] Transferencia" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=6 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consactlimiteremesa_numtrans(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pIdRemesadora                SMALLINT
  pNoTransacciones             SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pIdRemesadora` | `SMALLINT` | `rem`=remesa (forma corta) · `remesa`=remesa (Western Union / MoneyGram) | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pNoTransacciones` | `SMALLINT` | `trans`=[polisemia] Transferencia (bditransfer, bditrans: transferencias y remesas con campos pbco_dest/ppais_dest) | Transacción (sufijo genérico en SPs de reversión y procesamiento) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cAbreviatura` | `CHAR(20)` | L7 |
| `sNoTransacciones` | `SMALLINT` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_limite_monto` | `bdisac` | ⚠️ sí | SELECT | L59 |
| `sac_limite_monto` | `bdisac` | ⚠️ sí | UPDATE | L74 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdEjecucion = '' OR pIdRemesadora IS NULL THEN` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L69 | VALIDACIÓN_NULL | `IF pNoTransacciones IS NULL THEN` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?limite` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | 🔵 CONVENCIÓN | nombre_sp |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?limite` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consdetalleremesascte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consdetalleremesascte.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 141 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, detalle, remesas internacionales y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consdetalleremesascte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INT` | L13 |
| `cCodRetSp` | `CHAR(6)` | L14 |
| `iCodRetSp` | `INTEGER` | L15 |
| `cDescCodRet` | `CHAR(80)` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cNum_convenio` | `CHAR(3)` | L19 |
| `cNom_convenio` | `CHAR(40)` | L20 |
| `cFecha_pago` | `CHAR(25)` | L21 |
| `cClave_envio` | `CHAR(16)` | L22 |
| `cMonto_pago` | `CHAR(20)` | L23 |
| `cTipo_pago` | `CHAR(15)` | L24 |
| `cSucursal` | `CHAR(4)` | L25 |
| `cEmpleado` | `CHAR(8)` | L26 |
| `iRecuperacion` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_detalle_rem` | `bdicnweb` | no | SELECT | L85 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L60 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pFechaIni IS NULL OR pFechaFin IS NULL OR` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L89 | FÓRMULA | `LET cFecha_pago = SUBSTR(cFecha_pago, 7, 2) \|\| '/' \|\| SUBSTR(cFecha_pago, 5, 2) \|\| '/' \|\| SU` |  |
| L91 | FÓRMULA | `LET cFecha_pago = SUBSTR(cFecha_pago, 9, 2) \|\| '/' \|\| SUBSTR(cFecha_pago, 6, 2) \|\| '/' \|\| SU` |  |
| L93 | FÓRMULA | `LET cFecha_pago = SUBSTR(cFecha_pago, 7, 2) \|\| '/' \|\| SUBSTR(cFecha_pago, 5, 2) \|\| '/' \|\| SU` |  |
| L96 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rem_consdetalleremesascte_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consdetalleremesascte_totales.sql` |
| **LOC (1er CREATE)** | 491 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 140 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, detalle, remesas internacionales y cliente (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consdetalleremesascte_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pFechaIni                    DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cNumConvenio` | `CHAR(3)` | L13 |
| `cNomConvenio` | `CHAR(40)` | L14 |
| `dFechaPagBts` | `CHAR(25)` | L15 |
| `dFechaPagWu` | `CHAR(25)` | L16 |
| `dFechaPagOv` | `CHAR(25)` | L17 |
| `dFechaPagVg` | `CHAR(25)` | L18 |
| `dFechaPagApp` | `CHAR(25)` | L19 |
| `cCveEnvioBts` | `CHAR(16)` | L20 |
| `cCveEnvioWu` | `CHAR(16)` | L21 |
| `cCveEnvioOv` | `CHAR(16)` | L22 |
| `cCveEnvioVg` | `CHAR(16)` | L23 |
| `cCveEnvioApp` | `CHAR(16)` | L24 |
| `mMontoPagBts` | `CHAR(20)` | L25 |
| `mMontoPagWu` | `CHAR(20)` | L26 |
| `mMontoPagOv` | `CHAR(20)` | L27 |
| `mMontoPagVg` | `CHAR(20)` | L28 |
| `mMontoPagApp` | `CHAR(20)` | L29 |
| `cTipoPagoBts` | `CHAR(15)` | L30 |
| `cTipoPagoWu` | `CHAR(15)` | L31 |
| *…20 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_detalle_rem` | `bdicnweb` | no | SELECT | L131 |
| `sac_detalle_rem` | `bdicnweb` | no | DELETE | L131 |
| `sac_detalle_rem` | `bdicnweb` | no | INSERT | L134 |
| `sac_bts_payi` | `bdisac` | ⚠️ sí | SELECT | L137 |
| `sac_wu_pay` | `bdisac` | ⚠️ sí | SELECT | L146 |
| `sac_app_payi` | `bdisac` | ⚠️ sí | SELECT | L176 |
| `sac_bts_qryi` | `bdisac` | ⚠️ sí | SELECT | L196 |
| `sac_movimientos` | `bdisac` | ⚠️ sí | SELECT | L202 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L222 |
| `sac_detalle_rem` | `bdicnweb` | no | UPDATE | L296 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L116 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pFechaIni IS NULL OR pFechaFin IS NULL TH` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L477 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rem_consdethistoricomovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consdethistoricomovimientos.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 138 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, detalle y movimientos (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consdethistoricomovimientos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INT` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cDescCodRet` | `CHAR(80)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `dFechaMod` | `CHAR(25)` | L14 |
| `cTipoMod` | `CHAR(1)` | L15 |
| `cMotivo` | `CHAR(250)` | L16 |
| `cEmpleado` | `CHAR(8)` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_log_cte_his_mov` | `bdisac` | ⚠️ sí | SELECT | L67 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pRegistros IS NULL OR pRecuperacion IS NU` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L70 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_consdethistoricomovimientos_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consdethistoricomovimientos_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 139 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, detalle y movimientos (histórico, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consdethistoricomovimientos_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_log_cte_his_mov` | `bdisac` | ⚠️ sí | SELECT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rem_consgralremesascte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consgralremesascte.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 137 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, remesas internacionales y cliente (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consgralremesascte(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `cCodRetSp` | `CHAR(6)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cDescCodRet` | `CHAR(80)` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cNum_convenio` | `CHAR(3)` | L16 |
| `cNom_convenio` | `CHAR(40)` | L17 |
| `iTotal_pagadas` | `INTEGER` | L18 |
| `cMonto_pagado` | `CHAR(20)` | L19 |
| `cFecha_ult` | `CHAR(25)` | L20 |
| `iRecuperacion` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_general_rem` | `bdicnweb` | no | SELECT | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pFechaIni IS NULL OR pFechaFin IS NULL OR` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L77 | FÓRMULA | `LET cFecha_ult = SUBSTR(cFecha_ult, 7, 2) \|\| '/' \|\| SUBSTR(cFecha_ult, 5, 2) \|\| '/' \|\| SUBST` |  |
| L79 | FÓRMULA | `LET cFecha_ult = SUBSTR(cFecha_ult, 9, 2) \|\| '/' \|\| SUBSTR(cFecha_ult, 6, 2) \|\| '/' \|\| SUBST` |  |
| L81 | FÓRMULA | `LET cFecha_ult = SUBSTR(cFecha_ult, 7, 2) \|\| '/' \|\| SUBSTR(cFecha_ult, 5, 2) \|\| '/' \|\| SUBST` |  |
| L84 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rem_consgralremesascte_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consgralremesascte_totales.sql` |
| **LOC (1er CREATE)** | 264 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, remesas internacionales y cliente (general, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consgralremesascte_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pFechaIni                    DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNumConvenio` | `CHAR(3)` | L12 |
| `cNomConvenio` | `CHAR(40)` | L13 |
| `iPagadasBts` | `INTEGER` | L14 |
| `iPagadasWu` | `INTEGER` | L15 |
| `iPagadasOv` | `INTEGER` | L16 |
| `iPagadasVg` | `INTEGER` | L17 |
| `iPagadasApp` | `INTEGER` | L18 |
| `mMontoPagBts` | `CHAR(20)` | L19 |
| `mMontoPagWu` | `CHAR(20)` | L20 |
| `mMontoPagOv` | `CHAR(20)` | L21 |
| `mMontoPagVg` | `CHAR(20)` | L22 |
| `mMontoPagApp` | `CHAR(20)` | L23 |
| `cStatusCte` | `CHAR(10)` | L24 |
| `dFechaStatus` | `CHAR(25)` | L25 |
| `dFechaUltBts` | `CHAR(25)` | L26 |
| `dFechaUltWu` | `CHAR(25)` | L27 |
| `dFechaUltOv` | `CHAR(25)` | L28 |
| `dFechaUltVg` | `CHAR(25)` | L29 |
| `dFechaUltApp` | `CHAR(25)` | L30 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cons_statusprocesorem` | `bdicnweb` | no | SELECT | L91 |
| `sw_cons_statusprocesorem` | `bdicnweb` | no | DELETE | L91 |
| `sw_cons_statusprocesorem` | `bdicnweb` | no | INSERT | L97 |
| `sac_general_rem` | `bdicnweb` | no | SELECT | L119 |
| `sac_general_rem` | `bdicnweb` | no | DELETE | L119 |
| `sac_general_rem` | `bdicnweb` | no | INSERT | L122 |
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L125 |
| `sac_bts_payi` | `bdisac` | ⚠️ sí | SELECT | L131 |
| `sac_wu_pay` | `bdisac` | ⚠️ sí | SELECT | L136 |
| `sac_app_payi` | `bdisac` | ⚠️ sí | SELECT | L151 |
| `sac_bts_qryi` | `bdisac` | ⚠️ sí | SELECT | L163 |
| `sac_general_rem` | `bdicnweb` | no | UPDATE | L214 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L100 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pFechaIni IS NULL OR pFechaFin IS NULL TH` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L240 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rem_conslimiteremesa_edo_suc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_conslimiteremesa_edo_suc.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y estado (sucursal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_conslimiteremesa_edo_suc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pIdRemesadora                SMALLINT
  pValor                       CHAR(4)
  pBandera                     INT
) RETURNING CHAR(5) 	AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pIdRemesadora` | `SMALLINT` | `rem`=remesa (forma corta) · `remesa`=remesa (Western Union / MoneyGram) | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pValor` | `CHAR(4)` | — | — |
| `pBandera` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cAbreviatura` | `CHAR(20)` | L10 |
| `mUsd` | `MONEY(16,2)` | L11 |
| `mPesos` | `MONEY(16,2)` | L12 |
| `sStatus` | `SMALLINT` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |
| `iLimiteCte` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_limite_edo` | `bdisac` | ⚠️ sí | SELECT | L66 |
| `sac_limite_suc` | `bdisac` | ⚠️ sí | SELECT | L78 |
| `sac_limite_edo_usuario` | `bdisac` | ⚠️ sí | SELECT | L94 |
| `sac_limite_suc_usuario` | `bdisac` | ⚠️ sí | SELECT | L106 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pIdRemesadora IS NULL OR pValor = '' OR p` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?limite` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | 🔵 CONVENCIÓN | nombre_sp |
| `edo` | ENTIDAD | estado | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `suc` | MODIF | sucursal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?limite` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consparametrostransaccionapp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consparametrostransaccionapp.sql` |
| **LOC (1er CREATE)** | 143 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, parámetros y transacción (canal app)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consparametrostransaccionapp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumRem                      CHAR(12)
  pFecha                       DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `cons`=consulta | 🔵 CONVENCIÓN |
| `pNumRem` | `CHAR(12)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cDescCodRet` | `CHAR(80)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `cFecha` | `CHAR(10)` | L13 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L14 |
| `cFolio_suc` | `CHAR(16)` | L16 |
| `cId_sucursal` | `CHAR(4)` | L17 |
| `cDesc_sucursal` | `CHAR(40)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientos` | `bdisac` | ⚠️ sí | SELECT | L65 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L81 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L98 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L126 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pNumRem = '' OR pFecha IS NULL THEN` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `app` | MODIF | canal app | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rem_consparametrostransaccionbts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consparametrostransaccionbts.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, parámetros, transacción y beneficiarios" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consparametrostransaccionbts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumBts                      CHAR(11)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumBts` | `CHAR(11)` | `bts`=beneficiarios (BTS) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INT` | L11 |
| `cCodRetSp` | `CHAR(6)` | L12 |
| `iCodRetSp` | `INTEGER` | L13 |
| `cDescCodRet` | `CHAR(80)` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cFecha` | `CHAR(10)` | L16 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L17 |
| `cNombreUsuario` | `CHAR(80)` | L19 |
| `dFechaSistema` | `DATE` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cTerminal` | `CHAR(15)` | L22 |
| `cFormatFechaSistema` | `CHAR(8)` | L23 |
| `cHora` | `CHAR(8)` | L24 |
| `cFormatHora` | `CHAR(6)` | L25 |
| `cTramaBts` | `CHAR(64)` | L26 |
| `cIdTransaccionBts` | `CHAR(100)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L83 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consinfobtssif` | `bdisac` | ⚠️ sí | L74 |
| `sp_obtieneparametro` | `bdisac` | ⚠️ sí | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_consinfobtssif';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_obtieneparametro';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `parametros` | ENTIDAD | parámetros | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_consremcambiobts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consremcambiobts.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa, cambio y beneficiarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_consremcambiost` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consremcambiobts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumBts                      CHAR(11)
  pTransStatusDt               DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumBts` | `CHAR(11)` | `bts`=beneficiarios (BTS) | ✅ CÓDIGO |
| `pTransStatusDt` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iSqlErr` | `INT` | L25 |
| `cCodRetSp` | `CHAR(6)` | L26 |
| `iCodRetSp` | `INTEGER` | L27 |
| `cDescCodRet` | `CHAR(80)` | L28 |
| `cEmpresa` | `CHAR(3)` | L29 |
| `cSucursal` | `CHAR(15)` | L31 |
| `cTerminal` | `CHAR(15)` | L32 |
| `cR_Type_Cd` | `CHAR(3)` | L33 |
| `cR_Identif_Nm` | `CHAR(20)` | L34 |
| `cR_Nom_Calle` | `CHAR(50)` | L35 |
| `cR_Num_Ext` | `CHAR(5)` | L36 |
| `cR_Num_Int` | `CHAR(5)` | L37 |
| `cR_Depto` | `CHAR(10)` | L38 |
| `cR_Colonia` | `CHAR(80)` | L39 |
| `cR_Cp` | `CHAR(5)` | L40 |
| `cR_Mncpo_Deleg` | `CHAR(50)` | L41 |
| `cR_Ciudad` | `CHAR(50)` | L42 |
| `cR_Estado` | `CHAR(50)` | L43 |
| `cR_Issuer_Country_Cd` | `CHAR(3)` | L44 |
| `cR_Telefono` | `CHAR(15)` | L45 |
| `cTipo_Pago` | `CHAR(1)` | L46 |
| `cR_Fecha_Nac` | `CHAR(8)` | L47 |
| `cR_Nacionalidad` | `CHAR(50)` | L48 |
| `cR_pais_nac` | `CHAR(20)` | L49 |
| *…1 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consremcambiost` | `bdisac` | ⚠️ sí | L112 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L94 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumBts = '' OR pTransStatusDt IS NULL THEN` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L118 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_consremcambiost';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_consultalimite`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultalimite.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 232 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultalimite(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pRemesa                      CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRemesa` | `CHAR(3)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `iCodParam` | `INTEGER` | L9 |
| `cSucursal` | `CHAR(4)` | L10 |
| `cRemesa` | `CHAR(3)` | L11 |
| `iLimDom` | `INTEGER` | L12 |
| `iLimTel` | `INTEGER` | L13 |
| `iExiste` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_pldlimite_teldom_excpsuc` | `bdisac` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?limite` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?limite` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consultapaises`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultapaises.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y país" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultapaises(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cNumPais` | `INTEGER` | L11 |
| `cCodPais` | `CHAR(3)` | L12 |
| `cPais` | `CHAR(50)` | L13 |
| `cFlagBanco` | `CHAR(1)` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_app_paises` | `bdisac` | ⚠️ sí | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet     = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pais` | ENTIDAD | país | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consultaparametros`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultaparametros.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y parámetros" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultaparametros(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pOpCode                      CHAR(4)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pOpCode` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cSubTransaccion` | `CHAR(5)` | L11 |
| `cOpCode` | `CHAR(50)` | L12 |
| `cStatecode` | `CHAR(3)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_intrfz_serv` | `bdisac` | ⚠️ sí | SELECT | L56 |
| `sac_app_cat_mensajes` | `bdisac` | ⚠️ sí | SELECT | L73 |
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `parametros` | ENTIDAD | parámetros | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rem_consultasacappqryi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultasacappqryi.sql` |
| **LOC (1er CREATE)** | 521 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 72 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y Servicios de Atención al Cliente — subsistema de atención en sucursal (canal app)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultasacappqryi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumRem                      CHAR(12)
  pRCode                       CHAR(4)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumRem` | `CHAR(12)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |
| `pRCode` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L75 |
| `iSqlErr` | `INT` | L76 |
| `cCodRetSp` | `CHAR(6)` | L77 |
| `iCodRetSp` | `INTEGER` | L78 |
| `cDescCodRet` | `CHAR(80)` | L79 |
| `cEmpresa` | `CHAR(3)` | L80 |
| `cTxn_status` | `CHAR(1)` | L82 |
| `cUnirefnum` | `CHAR(16)` | L83 |
| `cCode_Company` | `CHAR(3)` | L84 |
| `cChanneldid` | `CHAR(3)` | L85 |
| `cLocationunit` | `CHAR(15)` | L86 |
| `cNnumber` | `CHAR(15)` | L87 |
| `cTypecode_Branch` | `CHAR(3)` | L88 |
| `cCountrycode_Branch` | `CHAR(3)` | L89 |
| `cStatecode_Branch` | `CHAR(3)` | L90 |
| `cTerminalid` | `CHAR(15)` | L91 |
| `cProcessdate_Qry` | `CHAR(8)` | L92 |
| `cProcesstime_Qry` | `CHAR(6)` | L93 |
| `cCode_Operacion` | `CHAR(5)` | L94 |
| `cCode` | `CHAR(4)` | L95 |
| `cMensCode` | `CHAR(255)` | L96 |
| `cCode_d` | `CHAR(4)` | L97 |
| `cMensajeD` | `CHAR(255)` | L98 |
| `cProcessDate` | `CHAR(8)` | L99 |
| `cProcessTime` | `CHAR(6)` | L100 |
| *…94 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_app_qryi` | `bdisac` | ⚠️ sí | SELECT | L381 |
| `sac_app_estatusrem` | `bdisac` | ⚠️ sí | SELECT | L392 |
| `sac_movimientos` | `bdisac` | ⚠️ sí | SELECT | L399 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L419 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L440 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L466 |
| `sac_app_payi` | `bdisac` | ⚠️ sí | SELECT | L474 |
| `sac_app_payi_old` | `bdisac` | ⚠️ sí | SELECT | L490 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L196 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L336 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L387 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?qryi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?qryi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consultasacappqryi2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultasacappqryi2.sql` |
| **LOC (1er CREATE)** | 512 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 70 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y Servicios de Atención al Cliente — subsistema de atención en sucursal (canal app)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultasacappqryi2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumRem                      CHAR(12)
  pRCode                       CHAR(4)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumRem` | `CHAR(12)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |
| `pRCode` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L52 |
| `iSqlErr` | `INT` | L53 |
| `cCodRetSp` | `CHAR(6)` | L54 |
| `iCodRetSp` | `INTEGER` | L55 |
| `cDescCodRet` | `CHAR(80)` | L56 |
| `cEmpresa` | `CHAR(3)` | L57 |
| `cTxn_status` | `CHAR(1)` | L59 |
| `cUnirefnum` | `CHAR(16)` | L60 |
| `cCode_Company` | `CHAR(3)` | L61 |
| `cChanneldid` | `CHAR(3)` | L62 |
| `cLocationunit` | `CHAR(15)` | L63 |
| `cNnumber` | `CHAR(15)` | L64 |
| `cTypecode_Branch` | `CHAR(3)` | L65 |
| `cCountrycode_Branch` | `CHAR(3)` | L66 |
| `cStatecode_Branch` | `CHAR(3)` | L67 |
| `cTerminalid` | `CHAR(15)` | L68 |
| `cProcessdate_Qry` | `CHAR(8)` | L69 |
| `cProcesstime_Qry` | `CHAR(6)` | L70 |
| `cCode_Operacion` | `CHAR(5)` | L71 |
| `cCode` | `CHAR(4)` | L72 |
| `cMensCode` | `CHAR(255)` | L73 |
| `cCode_d` | `CHAR(4)` | L74 |
| `cMensajeD` | `CHAR(255)` | L75 |
| `cProcessDate` | `CHAR(8)` | L76 |
| `cProcessTime` | `CHAR(6)` | L77 |
| *…98 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_app_qryi` | `bdisac` | ⚠️ sí | SELECT | L367 |
| `sac_app_estatusrem` | `bdisac` | ⚠️ sí | SELECT | L378 |
| `sac_movimientos` | `bdisac` | ⚠️ sí | SELECT | L385 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L405 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L426 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L452 |
| `sac_app_payi` | `bdisac` | ⚠️ sí | SELECT | L460 |
| `sac_app_payi_old` | `bdisac` | ⚠️ sí | SELECT | L476 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L178 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L322 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L373 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?qryi2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?qryi2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consultasacappqryi2_2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultasacappqryi2_2.sql` |
| **LOC (1er CREATE)** | 417 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 72 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y Servicios de Atención al Cliente — subsistema de atención en sucursal (canal app)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultasacappqryi2_2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumRem                      CHAR(12)
  pRCode                       CHAR(4)
  pIdRegistro                  INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumRem` | `CHAR(12)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |
| `pRCode` | `CHAR(4)` | — | — |
| `pIdRegistro` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L52 |
| `iSqlErr` | `INT` | L53 |
| `cCodRetSp` | `CHAR(6)` | L54 |
| `iCodRetSp` | `INTEGER` | L55 |
| `cDescCodRet` | `CHAR(80)` | L56 |
| `cEmpresa` | `CHAR(3)` | L57 |
| `cTxn_status` | `CHAR(1)` | L59 |
| `cUnirefnum` | `CHAR(16)` | L60 |
| `cCode_Company` | `CHAR(3)` | L61 |
| `cChanneldid` | `CHAR(3)` | L62 |
| `cLocationunit` | `CHAR(15)` | L63 |
| `cNnumber` | `CHAR(15)` | L64 |
| `cTypecode_Branch` | `CHAR(3)` | L65 |
| `cCountrycode_Branch` | `CHAR(3)` | L66 |
| `cStatecode_Branch` | `CHAR(3)` | L67 |
| `cTerminalid` | `CHAR(15)` | L68 |
| `cProcessdate_Qry` | `CHAR(8)` | L69 |
| `cProcesstime_Qry` | `CHAR(6)` | L70 |
| `cCode_Operacion` | `CHAR(5)` | L71 |
| `cCode` | `CHAR(4)` | L72 |
| `cMensCode` | `CHAR(255)` | L73 |
| `cCode_d` | `CHAR(4)` | L74 |
| `cMensajeD` | `CHAR(255)` | L75 |
| `cProcessDate` | `CHAR(8)` | L76 |
| `cProcessTime` | `CHAR(6)` | L77 |
| *…107 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verifica_estatusrem` | `bdicnweb` | no | SELECT | L378 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L186 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L338 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumRem = '' OR pRCode = '' OR pIdRegistro IS NULL THEN` |  |
| L339 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L383 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `app` | MODIF | canal app | 🔵 CONVENCIÓN | nombre_sp |
| `?qryi2_2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?qryi2_2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consultasacappqryi_2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultasacappqryi_2.sql` |
| **LOC (1er CREATE)** | 601 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y Servicios de Atención al Cliente — subsistema de atención en sucursal (canal app)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultasacappqryi_2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumRem                      CHAR(12)
  pRCode                       CHAR(4)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumRem` | `CHAR(12)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |
| `pRCode` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L75 |
| `iSqlErr` | `INT` | L76 |
| `cCodRetSp` | `CHAR(6)` | L77 |
| `iCodRetSp` | `INTEGER` | L78 |
| `cDescCodRet` | `CHAR(80)` | L79 |
| `cEmpresa` | `CHAR(3)` | L80 |
| `cTxn_status` | `CHAR(1)` | L82 |
| `cUnirefnum` | `CHAR(16)` | L83 |
| `cCode_Company` | `CHAR(3)` | L84 |
| `cChanneldid` | `CHAR(3)` | L85 |
| `cLocationunit` | `CHAR(15)` | L86 |
| `cNnumber` | `CHAR(15)` | L87 |
| `cTypecode_Branch` | `CHAR(3)` | L88 |
| `cCountrycode_Branch` | `CHAR(3)` | L89 |
| `cStatecode_Branch` | `CHAR(3)` | L90 |
| `cTerminalid` | `CHAR(15)` | L91 |
| `cProcessdate_Qry` | `CHAR(8)` | L92 |
| `cProcesstime_Qry` | `CHAR(6)` | L93 |
| `cCode_Operacion` | `CHAR(5)` | L94 |
| `cCode` | `CHAR(4)` | L95 |
| `cMensCode` | `CHAR(255)` | L96 |
| `cCode_d` | `CHAR(4)` | L97 |
| `cMensajeD` | `CHAR(255)` | L98 |
| `cProcessDate` | `CHAR(8)` | L99 |
| `cProcessTime` | `CHAR(6)` | L100 |
| *…100 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verifica_estatusrem` | `bdicnweb` | no | SELECT | L352 |
| `sw_verifica_estatusrem` | `bdicnweb` | no | DELETE | L352 |
| `sw_verifica_estatusrem` | `bdicnweb` | no | INSERT | L356 |
| `sw_verifica_estatusrem` | `bdicnweb` | no | UPDATE | L362 |
| `sac_app_qryi` | `bdisac` | ⚠️ sí | SELECT | L415 |
| `sac_app_estatusrem` | `bdisac` | ⚠️ sí | SELECT | L438 |
| `sac_fechas` | `bdisac` | ⚠️ sí | SELECT | L441 |
| `sac_app_payi` | `bdisac` | ⚠️ sí | SELECT | L446 |
| `sac_movimientos` | `bdisac` | ⚠️ sí | SELECT | L459 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L476 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L494 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L513 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L202 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L360 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L422 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?qryi_2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?qryi_2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_consultasucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultasucursal.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 231 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultasucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cSucursal` | `CHAR(4)` | L9 |
| `cNombreSucursal` | `CHAR(40)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_consultasucursalremrechazadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_consultasucursalremrechazadas.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 230 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesa y sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_consultasucursalremrechazadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cSucursal` | `CHAR(4)` | L9 |
| `cIdSucursal` | `CHAR(4)` | L10 |
| `cNombreSucursal` | `CHAR(40)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_pldlimite_teldom_rechazos` | `bdisac` | ⚠️ sí | SELECT | L47 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?hazadas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hazadas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_detremesasrechazadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_detremesasrechazadas.sql` |
| **LOC (1er CREATE)** | 191 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 229 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recepción remesa, detalle y remesas internacionales" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_detremesasrechazadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPeriodoDel                  DATE
  pPeriodoAl                   DATE
  pSucursal                    CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPeriodoDel` | `DATE` | — | — |
| `pPeriodoAl` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | `rec`=recepción / recibe | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L35 |
| `iSqlErr` | `INTEGER` | L36 |
| `iNoRegistros` | `INTEGER` | L37 |
| `cTipo_remesa` | `VARCHAR(3)` | L38 |
| `cId_sucursal` | `VARCHAR(4)` | L39 |
| `cDireccion` | `VARCHAR(200)` | L40 |
| `cMunicipio` | `VARCHAR(100)` | L41 |
| `cEstado` | `VARCHAR(30)` | L42 |
| `cCodigo_postal` | `VARCHAR(50)` | L43 |
| `cPeriodo` | `VARCHAR(6)` | L44 |
| `iConteodom` | `INTEGER` | L45 |
| `ilimitedom` | `INTEGER` | L46 |
| `cTelefono` | `VARCHAR(10)` | L47 |
| `cCelular` | `VARCHAR(10)` | L48 |
| `iConteotel` | `INTEGER` | L49 |
| `iLimitetel` | `INTEGER` | L50 |
| `iTiporechazo` | `INTEGER` | L51 |
| `cMotivorechazo` | `VARCHAR(40)` | L52 |
| `cNumremesa` | `VARCHAR(20)` | L53 |
| `cFolio_suc` | `VARCHAR(16)` | L54 |
| `cUsuario_insert` | `VARCHAR(8)` | L55 |
| `dFecha_insert` | `DATETIME YEAR TO SECOND` | L56 |
| `cEmisor_nombre1` | `CHAR(40)` | L57 |
| `cEmisor_nombre2` | `CHAR(40)` | L58 |
| `cEmisor_appaterno` | `CHAR(40)` | L59 |
| *…8 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_det_remesasrechazadas` | `bdicnweb` | no | SELECT | L156 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L134 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L116 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pPeriodoDel IS NULL OR pPeriodoAl IS NULL OR pRegistros IS NU` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L159 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `det` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?hazadas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hazadas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_detremesasrechazadas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_detremesasrechazadas_totales.sql` |
| **LOC (1er CREATE)** | 320 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 228 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "recepción remesa, detalle y remesas internacionales (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_detremesasrechazadas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPeriodoDel                  DATE
  pPeriodoAl                   DATE
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPeriodoDel` | `DATE` | — | — |
| `pPeriodoAl` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `cTipo_remesa` | `VARCHAR(3)` | L8 |
| `cId_sucursal` | `VARCHAR(4)` | L9 |
| `cDireccion` | `VARCHAR(200)` | L10 |
| `cMunicipio` | `VARCHAR(100)` | L11 |
| `cEstado` | `VARCHAR(30)` | L12 |
| `cCodigo_postal` | `VARCHAR(50)` | L13 |
| `cPeriodo` | `VARCHAR(6)` | L14 |
| `iConteodom` | `INTEGER` | L15 |
| `ilimitedom` | `INTEGER` | L16 |
| `cTelefono` | `VARCHAR(10)` | L17 |
| `cCelular` | `VARCHAR(10)` | L18 |
| `iConteotel` | `INTEGER` | L19 |
| `iLimitetel` | `INTEGER` | L20 |
| `iTiporechazo` | `INTEGER` | L21 |
| `cMotivorechazo` | `VARCHAR(40)` | L22 |
| `cNumremesa` | `VARCHAR(20)` | L23 |
| `cFolio_suc` | `VARCHAR(16)` | L24 |
| `cUsuario_insert` | `VARCHAR(8)` | L25 |
| `dFecha_insert` | `DATETIME YEAR TO SECOND` | L26 |
| `cEmisor_nombre1` | `CHAR(40)` | L27 |
| `cEmisor_nombre2` | `CHAR(40)` | L28 |
| `cEmisor_appaterno` | `CHAR(40)` | L29 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_det_remesasrechazadas` | `bdicnweb` | no | SELECT | L121 |
| `sw_det_remesasrechazadas` | `bdicnweb` | no | DELETE | L123 |
| `sac_pldlimite_teldom_rechazos` | `bdisac` | ⚠️ sí | SELECT | L154 |
| `sac_app_qryi` | `bdisac` | ⚠️ sí | SELECT | L165 |
| `sac_bts_qryi` | `bdisac` | ⚠️ sí | SELECT | L178 |
| `sac_wu_search` | `bdisac` | ⚠️ sí | SELECT | L191 |
| `sw_det_remesasrechazadas` | `bdicnweb` | no | INSERT | L198 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L104 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pPeriodoDel IS NULL OR pPeriodoAl IS NULL THEN` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L125 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L197 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L205 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L275 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L283 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L302 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `det` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?hazadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hazadas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_estadosac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_estadosac.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa, estado y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_estadosac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `cCodPais` | `CHAR(3)` | L14 |
| `cCveEstado` | `CHAR(2)` | L15 |
| `cNombreEstado` | `CHAR(40)` | L16 |
| `cStateCd` | `CHAR(3)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet     = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `estado` | ENTIDAD | estado (entidad federativa / estatus) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_generarepremesasnopagadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_generarepremesasnopagadas.sql` |
| **LOC (1er CREATE)** | 277 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar remesa y remesas internacionales" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_generarepremesasnopagadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pIdLimite                    SMALLINT
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pClaveId                     CHAR(100)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pIdLimite` | `SMALLINT` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pClaveId` | `CHAR(100)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L36 |
| `iSqlErr` | `INT` | L37 |
| `cCodRetSp` | `CHAR(5)` | L38 |
| `iCodRetSp` | `INTEGER` | L39 |
| `cDescCodRet` | `CHAR(80)` | L40 |
| `cEmpresa` | `CHAR(3)` | L41 |
| `iIdRegistro` | `INTEGER` | L42 |
| `cAutoridad` | `CHAR(8)` | L43 |
| `cReporte` | `CHAR(35)` | L44 |
| `cDescripcion` | `CHAR(100)` | L45 |
| `cStatus` | `CHAR(1)` | L46 |
| `cDescStatus` | `CHAR(10)` | L47 |
| `iSerial` | `INTEGER` | L49 |
| `cRespMensaje` | `CHAR(45)` | L50 |
| `iRegistros` | `INTEGER` | L52 |
| `iGraba` | `INTEGER` | L53 |
| `iFormatoAnt` | `INTEGER` | L54 |
| `cDato` | `CHAR(25)` | L55 |
| `cDatoFormat` | `CHAR(20)` | L56 |
| `cRenglon` | `CHAR(255)` | L57 |
| `cFormat` | `CHAR(11)` | L58 |
| `cSeleccion` | `CHAR(255)` | L59 |
| `cQuery` | `CHAR(255)` | L60 |
| `cCmd1` | `CHAR(2000)` | L62 |
| `cSql` | `CHAR(2500)` | L63 |
| *…43 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_detalleremesasnopagadas` | `bdicnweb` | no | SELECT | L240 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L198 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdEjecucion = '' OR pIdLimite IS NULL OR pFechaInicio IS NUL` |  |
| L200 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L209 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L246 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L255 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?nopagadas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep`, `?nopagadas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_generarepremesasnopagadas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_generarepremesasnopagadas_totales.sql` |
| **LOC (1er CREATE)** | 1216 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar remesa y remesas internacionales (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_generarepremesasnopagadas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pIdLimite                    SMALLINT
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pIdLimite` | `SMALLINT` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cDescCodRet` | `CHAR(80)` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `iIdRegistro` | `INTEGER` | L13 |
| `cAutoridad` | `CHAR(8)` | L14 |
| `cReporte` | `CHAR(35)` | L15 |
| `cDescripcion` | `CHAR(100)` | L16 |
| `cStatus` | `CHAR(1)` | L17 |
| `cDescStatus` | `CHAR(10)` | L18 |
| `iSerial` | `INTEGER` | L20 |
| `cRespMensaje` | `CHAR(45)` | L21 |
| `iRegistros` | `INTEGER` | L23 |
| `iGraba` | `INTEGER` | L24 |
| `iFormatoAnt` | `INTEGER` | L25 |
| `cDato` | `CHAR(25)` | L26 |
| `cDatoFormat` | `CHAR(20)` | L27 |
| `cRenglon` | `CHAR(255)` | L28 |
| `cFormat` | `CHAR(11)` | L29 |
| `cSeleccion` | `CHAR(255)` | L30 |
| `cQuery` | `CHAR(255)` | L31 |
| `cCmd1` | `CHAR(2000)` | L33 |
| `cSql` | `CHAR(2500)` | L34 |
| *…46 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusprocesoremnopag` | `bdicnweb` | no | SELECT | L186 |
| `sw_statusprocesoremnopag` | `bdicnweb` | no | DELETE | L186 |
| `sw_statusprocesoremnopag` | `bdicnweb` | no | INSERT | L189 |
| `sw_detalleremesasnopagadas` | `bdicnweb` | no | SELECT | L217 |
| `sw_detalleremesasnopagadas` | `bdicnweb` | no | DELETE | L231 |
| `sw_detalleremesasnopagadas` | `bdicnweb` | no | INSERT | L256 |
| `sac_remesaslimitepld_app` | `bdisac` | ⚠️ sí | SELECT | L264 |
| `sac_app_qryi` | `bdisac` | ⚠️ sí | SELECT | L268 |
| `sac_remesaslimitepld_bts` | `bdisac` | ⚠️ sí | SELECT | L279 |
| `sac_bts_qryi` | `bdisac` | ⚠️ sí | SELECT | L283 |
| `sac_remesaslimitepld_wu` | `bdisac` | ⚠️ sí | SELECT | L295 |
| `sac_wu_search` | `bdisac` | ⚠️ sí | SELECT | L299 |
| `sac_app_getorder` | `bdisac` | ⚠️ sí | SELECT | L414 |
| `sac_bts_sdep` | `bdisac` | ⚠️ sí | SELECT | L429 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L192 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdEjecucion = '' OR pIdLimite IS NULL OR pFechaInicio IS NUL` |  |
| L193 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L232 | FÓRMULA | `LET iContBorra = iContBorra + 1;` |  |
| L1197 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?nopagadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep`, `?nopagadas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_generareptxtremesasnopagadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_generareptxtremesasnopagadas.sql` |
| **LOC (1er CREATE)** | 335 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar remesa y remesas internacionales" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_generareptxtremesasnopagadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pRuta                        CHAR(100)
  pIdLimite                    SMALLINT
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pClaveId                     CHAR(100)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pRuta` | `CHAR(100)` | — | — |
| `pIdLimite` | `SMALLINT` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pClaveId` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iIdRegistro` | `INTEGER` | L12 |
| `cAutoridad` | `CHAR(8)` | L13 |
| `cReporte` | `CHAR(35)` | L14 |
| `cDescripcion` | `CHAR(100)` | L15 |
| `cStatus` | `CHAR(1)` | L16 |
| `cDescStatus` | `CHAR(10)` | L17 |
| `iSerial` | `INTEGER` | L19 |
| `cRespMensaje` | `CHAR(45)` | L20 |
| `iRegistros` | `INTEGER` | L22 |
| `iGraba` | `INTEGER` | L23 |
| `iFormatoAnt` | `INTEGER` | L24 |
| `cDato` | `CHAR(25)` | L25 |
| `cDatoFormat` | `CHAR(20)` | L26 |
| `cRenglon` | `CHAR(255)` | L27 |
| `cFormat` | `CHAR(11)` | L28 |
| `cSeleccion` | `CHAR(255)` | L29 |
| `cQuery` | `CHAR(255)` | L30 |
| `cCmd1` | `CHAR(2000)` | L32 |
| `cSql` | `CHAR(2500)` | L33 |
| *…45 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L238 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L183 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L196 | FÓRMULA | `LET pRuta = TRIM(pRuta) \|\| '/';` |  |
| L238 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM (SELECT lim` |  |
| L239 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"tipoid_ord,numeroid_ord,ciudadid_ord,paisid_ord,moneda_ord,SUBSTR(m` | 🔴 MONEY/aritmética financiera |
| L240 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SUBSTR(fechanacimiento_ben,7,2)\|\|'/'\|\|SUBSTR(fechanacimiento_be` |  |
| L253 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM (SELECT lim` |  |
| L254 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"tipoid_ord,numeroid_ord,ciudadid_ord,paisid_ord,moneda_ord,SUBSTR(m` | 🔴 MONEY/aritmética financiera |
| L255 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SUBSTR(fechanacimiento_ben,7,2)\|\|'/'\|\|SUBSTR(fechanacimiento_be` |  |
| L286 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L291 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L300 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L309 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ept` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?t` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?nopagadas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ept`, `?t`, `?nopagadas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_guardarespuestapayibts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_guardarespuestapayibts.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda remesa, respuesta y beneficiarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_guardarespuestapayi` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_guardarespuestapayibts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR (4)
  pTxn_Status                  CHAR(1)
  pConfirmation_nm             CHAR (11)
  pBank_Ref_Num                CHAR(20)
  pUser_name                   CHAR(20)
  pTerminal                    CHAR(15)
  pAgent_Dt                    CHAR(8)
  pAgent_Tm                    CHAR(6)
  pR_First_Name                CHAR(40)
  pR_Middle_Name               CHAR(40)
  pR_Last_Name                 CHAR(40)
  pR_Mother_M_Name             CHAR(40)
  pR_Type_Cd                   CHAR(3)
  pR_Issuer_Cd                 CHAR(3)
  pR_Issuer_State_Cd           CHAR(3)
  pR_Issuer_Country_Cd         CHAR(3)
  pR_Identif_Type              CHAR(5)
  pR_Identif_Nm                CHAR(20)
  pR_Expiration_Dt             CHAR(8)
  pR_Fecha_Nac                 CHAR(8)
  pR_Nacionalidad              CHAR(50)
  pR_pais_nac                  CHAR(20)
  pR_Nom_Calle                 CHAR(50)
  pR_Num_Ext                   CHAR(5)
  pR_Num_Int                   CHAR(5)
  pR_Depto                     CHAR(10)
  pR_Colonia                   CHAR(80)
  pR_Cp                        CHAR(5)
  pR_Mncpo_Delg                CHAR(50)
  pR_Ciudad                    CHAR(50)
  pR_Estado                    CHAR(50)
  pR_Telefono                  CHAR(15)
  pTipo_Pago                   CHAR(1)
  pOpCode                      CHAR(4)
  pProcess_Msg                 CHAR(255)
  pError_Param_Full_Name       CHAR(255)
  pTrans_Status_Cd             CHAR(3)
  pTrans_Status_Dt             CHAR(8)
  pProcess_Dt                  CHAR(8)
  pProcess_Tm                  CHAR(6)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR (4)` | — | — |
| `pTxn_Status` | `CHAR(1)` | — | — |
| `pConfirmation_nm` | `CHAR (11)` | — | — |
| `pBank_Ref_Num` | `CHAR(20)` | — | — |
| `pUser_name` | `CHAR(20)` | — | — |
| `pTerminal` | `CHAR(15)` | — | — |
| `pAgent_Dt` | `CHAR(8)` | — | — |
| `pAgent_Tm` | `CHAR(6)` | — | — |
| `pR_First_Name` | `CHAR(40)` | — | — |
| `pR_Middle_Name` | `CHAR(40)` | — | — |
| `pR_Last_Name` | `CHAR(40)` | — | — |
| `pR_Mother_M_Name` | `CHAR(40)` | — | — |
| `pR_Type_Cd` | `CHAR(3)` | — | — |
| `pR_Issuer_Cd` | `CHAR(3)` | — | — |
| `pR_Issuer_State_Cd` | `CHAR(3)` | — | — |
| `pR_Issuer_Country_Cd` | `CHAR(3)` | — | — |
| `pR_Identif_Type` | `CHAR(5)` | — | — |
| `pR_Identif_Nm` | `CHAR(20)` | — | — |
| `pR_Expiration_Dt` | `CHAR(8)` | — | — |
| `pR_Fecha_Nac` | `CHAR(8)` | — | — |
| `pR_Nacionalidad` | `CHAR(50)` | — | — |
| `pR_pais_nac` | `CHAR(20)` | — | — |
| `pR_Nom_Calle` | `CHAR(50)` | — | — |
| `pR_Num_Ext` | `CHAR(5)` | — | — |
| `pR_Num_Int` | `CHAR(5)` | — | — |
| `pR_Depto` | `CHAR(10)` | — | — |
| `pR_Colonia` | `CHAR(80)` | — | — |
| `pR_Cp` | `CHAR(5)` | — | — |
| `pR_Mncpo_Delg` | `CHAR(50)` | — | — |
| `pR_Ciudad` | `CHAR(50)` | — | — |
| `pR_Estado` | `CHAR(50)` | — | — |
| `pR_Telefono` | `CHAR(15)` | — | — |
| `pTipo_Pago` | `CHAR(1)` | — | — |
| `pOpCode` | `CHAR(4)` | — | — |
| `pProcess_Msg` | `CHAR(255)` | — | — |
| `pError_Param_Full_Name` | `CHAR(255)` | — | — |
| `pTrans_Status_Cd` | `CHAR(3)` | — | — |
| `pTrans_Status_Dt` | `CHAR(8)` | — | — |
| `pProcess_Dt` | `CHAR(8)` | — | — |
| `pProcess_Tm` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L45 |
| `iSqlErr` | `INT` | L46 |
| `cCodRetSp` | `CHAR(6)` | L47 |
| `iCodRetSp` | `INTEGER` | L48 |
| `cDescCodRet` | `CHAR(80)` | L49 |
| `cEmpresa` | `CHAR(3)` | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_guardarespuestapayi` | `bdisac` | ⚠️ sí | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_guardarespuestapayi';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `respuesta` | ENTIDAD | respuesta | 🔵 CONVENCIÓN | nombre_sp |
| `?payi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?payi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_guardarespuestarevibts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_guardarespuestarevibts.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda remesa, respuesta y beneficiarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_guardarespuestarevi` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_guardarespuestarevibts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR (4)
  pTxn_Status                  CHAR(1)
  pConfirmation_nm             CHAR (11)
  pProcess_Reason_Cd           CHAR(3)
  pBank_Ref_Num                CHAR(20)
  pRev_Bank_Ref_Nm             CHAR(20)
  pUser_name                   CHAR(20)
  pSup_User_Name               CHAR(20)
  pTerminal                    CHAR(15)
  pAgent_Dt                    CHAR(8)
  pAgent_Tm                    CHAR(6)
  pOpCode                      CHAR(4)
  pProcess_Msg                 CHAR(255)
  pError_Param_Full_Name       CHAR(255)
  pTrans_Status_Cd             CHAR(3)
  pTrans_Status_Dt             CHAR(8)
  pProcess_Dt                  CHAR(8)
  pProcess_Tm                  CHAR(6)
  pService_Cd                  CHAR(3)
  pPaymet_Type_Cd              CHAR(3)
  pOrig_Country_Cd             CHAR(3)
  pOrig_Currency_Cd            CHAR(3)
  pDest_Country_Cd             CHAR(3)
  pDest_Currency_Cd            CHAR(3)
  pOrig_Am                     CHAR(20)
  pDestination_Am              CHAR(20)
  pExch_Rate_Fx                CHAR(21)
  pR_First_Name                CHAR(40)
  pR_Middle_Name               CHAR(40)
  pR_Last_Name                 CHAR(40)
  pR_Mother_M_Name             CHAR(40)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR (4)` | — | — |
| `pTxn_Status` | `CHAR(1)` | — | — |
| `pConfirmation_nm` | `CHAR (11)` | — | — |
| `pProcess_Reason_Cd` | `CHAR(3)` | — | — |
| `pBank_Ref_Num` | `CHAR(20)` | — | — |
| `pRev_Bank_Ref_Nm` | `CHAR(20)` | `rev`=reversión (abreviación de reversa/reverso) | 🟡 INFERIDO |
| `pUser_name` | `CHAR(20)` | — | — |
| `pSup_User_Name` | `CHAR(20)` | — | — |
| `pTerminal` | `CHAR(15)` | — | — |
| `pAgent_Dt` | `CHAR(8)` | — | — |
| `pAgent_Tm` | `CHAR(6)` | — | — |
| `pOpCode` | `CHAR(4)` | — | — |
| `pProcess_Msg` | `CHAR(255)` | — | — |
| `pError_Param_Full_Name` | `CHAR(255)` | — | — |
| `pTrans_Status_Cd` | `CHAR(3)` | — | — |
| `pTrans_Status_Dt` | `CHAR(8)` | — | — |
| `pProcess_Dt` | `CHAR(8)` | — | — |
| `pProcess_Tm` | `CHAR(6)` | — | — |
| `pService_Cd` | `CHAR(3)` | — | — |
| `pPaymet_Type_Cd` | `CHAR(3)` | — | — |
| `pOrig_Country_Cd` | `CHAR(3)` | — | — |
| `pOrig_Currency_Cd` | `CHAR(3)` | — | — |
| `pDest_Country_Cd` | `CHAR(3)` | — | — |
| `pDest_Currency_Cd` | `CHAR(3)` | — | — |
| `pOrig_Am` | `CHAR(20)` | — | — |
| `pDestination_Am` | `CHAR(20)` | — | — |
| `pExch_Rate_Fx` | `CHAR(21)` | — | — |
| `pR_First_Name` | `CHAR(40)` | — | — |
| `pR_Middle_Name` | `CHAR(40)` | — | — |
| `pR_Last_Name` | `CHAR(40)` | — | — |
| `pR_Mother_M_Name` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L36 |
| `iSqlErr` | `INT` | L37 |
| `cCodRetSp` | `CHAR(6)` | L38 |
| `iCodRetSp` | `INTEGER` | L39 |
| `cDescCodRet` | `CHAR(80)` | L40 |
| `cEmpresa` | `CHAR(3)` | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_guardarespuestarevi` | `bdisac` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_guardarespuestarevi';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `respuesta` | ENTIDAD | respuesta | 🔵 CONVENCIÓN | nombre_sp |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_maximoreg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_maximoreg.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 227 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "registro remesa (máximo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_maximoreg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdLimite                    CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdLimite` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `iCodParam` | `INTEGER` | L8 |
| `cMaxValor` | `CHAR(100)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `max` | MODIF | máximo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?imo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_mensajes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_mensajes.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa y mensajes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_mensajes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pAgentTransTypeCode          CHAR(4)
  pOpCode                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pAgentTransTypeCode` | `CHAR(4)` | — | — |
| `pOpCode` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cAgentTransTypeCode` | `CHAR(4)` | L10 |
| `cOpcode` | `CHAR(4)` | L11 |
| `cOpcodesd` | `CHAR(50)` | L12 |
| `cOpcodeds` | `CHAR(255)` | L13 |
| `iNoRegistros` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_app_cat_mensajes` | `bdisac` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet     = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `mensajes` | ENTIDAD | mensajes | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_rem_mostrarpaises_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_mostrarpaises_totales.sql` |
| **LOC (1er CREATE)** | 118 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 76 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa, OS — Originación de Solicitudes y país (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_mostrarpaises_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRemesadora                SMALLINT
  pIdConsulta                  INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRemesadora` | `SMALLINT` | `rem`=remesa (forma corta) | ✅ CÓDIGO |
| `pIdConsulta` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `cIdPais` | `CHAR(3)` | L10 |
| `cNombrePais` | `CHAR(20)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_paises` | `bdinteg` | ⚠️ sí | SELECT | L50 |
| `si_paises_remesadoras` | `bdinteg` | ⚠️ sí | SELECT | L51 |
| `sw_rem_remesadora` | `bdicnweb` | no | SELECT | L91 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet         = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L76 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L95 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?trar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pais` | ENTIDAD | país | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?trar`, `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_mostrarpaisesremesadora`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_mostrarpaisesremesadora.sql` |
| **LOC (1er CREATE)** | 121 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 75 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa, OS — Originación de Solicitudes, país y remesadora" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_mostrarpaisesremesadora(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRemesadora                SMALLINT
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRemesadora` | `SMALLINT` | `rem`=remesa (forma corta) · `remesadora`=remesadora (envío de remesas) | ✅ CÓDIGO |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |
| `cIdPais` | `CHAR(3)` | L10 |
| `cNombrePais` | `CHAR(20)` | L11 |
| `iIdRemesa` | `SMALLINT` | L12 |
| `cRemesadora` | `CHAR(100)` | L13 |
| `iIdRemesadora` | `SMALLINT` | L15 |
| `iContador` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_paises` | `bdinteg` | ⚠️ sí | SELECT | L58 |
| `si_paises_remesadoras` | `bdinteg` | ⚠️ sí | SELECT | L59 |
| `sw_rem_remesadora` | `bdicnweb` | no | SELECT | L94 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet         = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L80 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L98 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?trar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pais` | ENTIDAD | país | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesadora` | ENTIDAD | remesadora (envío de remesas) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_m`, `?trar`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_obtieneinfoidentificacionbts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_obtieneinfoidentificacionbts.sql` |
| **LOC (1er CREATE)** | 136 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene remesa, información, identificación y beneficiarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_bts_obtieneinfoidentificacion`, `sp_app_obtieneinfoidentificacion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_obtieneinfoidentificacionbts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pCodTipoIdent                CHAR(3)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pCodTipoIdent` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cCodigo` | `CHAR(3)` | L11 |
| `cTipoIdentificacion` | `CHAR(3)` | L12 |
| `cCodTipoIdentificacion` | `CHAR(3)` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_bts_obtieneinfoidentificacion` | `bdisac` | ⚠️ sí | L54 |
| `sp_app_obtieneinfoidentificacion` | `bdisac` | ⚠️ sí | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_bts_obtieneinfoidenti` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_bts_obtieneinfoidenti` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L95 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_app_obtieneinfoidenti` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L116 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_app_obtieneinfoidenti` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `identificacion` | ENTIDAD | identificación | 🔵 CONVENCIÓN | nombre_sp |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_ope_paisesremesadoras`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_ope_paisesremesadoras.sql` |
| **LOC (1er CREATE)** | 297 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 74 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operación remesa, país y remesadora" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_ope_paisesremesadoras(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRemesadora                SMALLINT
  pTramPaises                  CHAR(900)
  pIdConsulta                  INTEGER
  pRegistros                   INTEGER
) RETURNING CHAR(5)  AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRemesadora` | `SMALLINT` | `rem`=remesa (forma corta) · `remesadora`=remesadora (envío de remesas) | ✅ CÓDIGO |
| `pTramPaises` | `CHAR(900)` | `pais`=país | ✅ CÓDIGO |
| `pIdConsulta` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |
| `cIdPais` | `CHAR(3)` | L9 |
| `cNombrePais` | `CHAR(20)` | L10 |
| `dhora` | `DATETIME HOUR TO FRACTION(3)` | L11 |
| `cAccion` | `SMALLINT` | L12 |
| `cDescAccion` | `CHAR(100)` | L13 |
| `cRemesadora` | `CHAR(100)` | L14 |
| `iIdRemesadora` | `SMALLINT` | L15 |
| `icount` | `INTEGER` | L16 |
| `cMensaje` | `CHAR(25)` | L17 |
| `iP1` | `INTEGER` | L19 |
| `iContador` | `INTEGER` | L20 |
| `iPosRegistro` | `INTEGER` | L21 |
| `iLonRegistro` | `INTEGER` | L22 |
| `cRegistro` | `CHAR(180)` | L23 |
| `cPais` | `CHAR(3)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `iposregistro` | `bdicnweb` | no | SELECT | L88 |
| `si_paises_remesadoras` | `bdinteg` | ⚠️ sí | SELECT | L95 |
| `si_paises_remesadoras` | `bdinteg` | ⚠️ sí | DELETE | L100 |
| `sw_bitacora_paises_remesadoras` | `bdicnweb` | no | INSERT | L104 |
| `sw_rem_remesadora` | `bdicnweb` | no | SELECT | L115 |
| `si_paises_remesadoras` | `bdinteg` | ⚠️ sí | INSERT | L197 |
| `si_paises` | `bdinteg` | ⚠️ sí | SELECT | L202 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L55 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRemesadora = '' OR pIdConsulta = '' OR pTramPaises = '' OR` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | FÓRMULA | `LET iLonRegistro = (INSTR(pTramPaises,",",1,iContador)) - 1;` |  |
| L81 | FÓRMULA | `LET iPosRegistro = INSTR(pTramPaises,",",1,iContador - 1) + 1;` |  |
| L83 | FÓRMULA | `LET iLonRegistro = (INSTR(pTramPaises,",",1,iContador) - INSTR(pTramPaises,",",1,iContador - 1))-1;` |  |
| L85 | FÓRMULA | `LET iLonRegistro = LENGTH(pTramPaises) - INSTR(pTramPaises,",",1,iContador - 1);` |  |
| L132 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L159 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L175 | FÓRMULA | `LET iLonRegistro = (INSTR(pTramPaises,",",1,iContador)) - 1;` |  |
| L177 | FÓRMULA | `LET iPosRegistro = INSTR(pTramPaises,",",1,iContador - 1) + 1;` |  |
| L179 | FÓRMULA | `LET iLonRegistro = (INSTR(pTramPaises,",",1,iContador) - INSTR(pTramPaises,",",1,iContador - 1))-1;` |  |
| L181 | FÓRMULA | `LET iLonRegistro = LENGTH(pTramPaises) - INSTR(pTramPaises,",",1,iContador - 1);` |  |
| L248 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L281 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `pais` | ENTIDAD | país | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `remesadora` | ENTIDAD | remesadora (envío de remesas) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_parametrostransrevpagbts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_parametrostransrevpagbts.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reversión remesa, parámetros, [polisemia] Transferencia y beneficiarios" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtieneparametro` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_parametrostransrevpagbts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdParametro                 INTEGER
  pNumBts                      CHAR(11)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdParametro` | `INTEGER` | — | — |
| `pNumBts` | `CHAR(11)` | `bts`=beneficiarios (BTS) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cAgentUser` | `CHAR(20)` | L12 |
| `cIdTransaccionBts` | `CHAR(100)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_bts_payi` | `bdisac` | ⚠️ sí | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneparametro` | `bdisac` | ⚠️ sí | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdParametro IS NULL THEN` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_obtieneparametro';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `parametros` | ENTIDAD | parámetros | 🔵 CONVENCIÓN | nombre_sp |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?pag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pag` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_queryorder`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_queryorder.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa y ordenante" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_app_queryorder` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_queryorder(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTxn_status                  CHAR(1)
  pUnirefnum                   CHAR(16)
  pCode_Company                CHAR(3)
  pChanneldid                  CHAR(3)
  pLocationunit                CHAR(15)
  pNnumber                     CHAR(15)
  pTypecode_Branch             CHAR(3)
  pCountrycode_Branch          CHAR(3)
  pStatecode_Branch            CHAR(3)
  pTerminalid                  CHAR(15)
  pProcessdate_Qry             CHAR(8)
  pProcesstime_Qry             CHAR(6)
  pCode_Operacion              CHAR(5)
  pCode                        CHAR(4)
  pMessage                     CHAR(255)
  pCode_d                      CHAR(4)
  pMessage_d                   CHAR(255)
  pProcessDate                 CHAR(8)
  pProcessTime                 CHAR(6)
  pRule                        CHAR(3)
  pValue                       CHAR(3)
  pGlobalTrackingNumber        CHAR(20)
  pOrderStatusCode             CHAR(3)
  pOrderStatusDate             CHAR(8)
  pOrderStatusTime             CHAR(6)
  pUniqueReferenceNumber       CHAR(16)
  pCodesalecom                 CHAR(3)
  pCountryCode                 CHAR(3)
  pStateCodeSale               CHAR(3)
  pSaleDate                    CHAR(8)
  pSaleTime                    CHAR(6)
  pCountryCode_o               CHAR(3)
  pCurrencyCode                CHAR(3)
  pServiceCode                 CHAR(3)
  pCountryCode_d               CHAR(3)
  pCurrencyCode_d              CHAR(3)
  pDeliveryMethodCode          CHAR(3)
  pPayNetworkCode              CHAR(3)
  pPaySubNetworkCode           CHAR(15)
  pBranchNumber                CHAR(15)
  pAccountTypeCode             CHAR(3)
  pAccountNumber               CHAR(30)
  pOriginAmount                CHAR(20)
  pDestinationAmount           CHAR(20)
  pRetailExchangeRate          CHAR(21)
  pWholesaleExchangeRate       CHAR(21)
  pDestinExchangeRate          CHAR(21)
  pServiceFeeAmount            CHAR(20)
  pDiscountAmount              CHAR(20)
  pTypeCode                    CHAR(3)
  pAccountNumber_c             CHAR(30)
  pBicCode                     CHAR(11)
  pReferenceNumber             CHAR(30)
  pCustomerNumber              CHAR(20)
  pFirstName                   CHAR(40)
  pMiddleName                  CHAR(40)
  pLastName                    CHAR(40)
  pMotherMaidenName            CHAR(40)
  pAddress                     CHAR(80)
  pCity                        CHAR(40)
  pCountryCode_a               CHAR(3)
  pStateCode                   CHAR(3)
  pZipCode                     CHAR(10)
  pTypeCode_i                  CHAR(3)
  pNumber                      CHAR(20)
  pExpirationDate              CHAR(8)
  pIssuerCountryCode           CHAR(3)
  pIssuerStateCode             CHAR(3)
  pDateOfBirth                 CHAR(8)
  pCustomerNumber_b            CHAR(20)
  pFirstName_b                 CHAR(40)
  pMiddleName_b                CHAR(40)
  pLastName_b                  CHAR(40)
  pMotherMaidenName_b          CHAR(40)
  pFirstName_f                 CHAR(40)
  pMiddleName_f                CHAR(40)
  pLastName_f                  CHAR(40)
  pMotherMaidenName_f          CHAR(40)
  pAddress_b                   CHAR(80)
  pCity_b                      CHAR(40)
  pCountryCode_b               CHAR(3)
  pStateCode_b                 CHAR(3)
  pZipCode_b                   CHAR(10)
  pEmail                       CHAR(100)
  pHomePhoneNumber             CHAR(15)
  pWorkPhoneNumber             CHAR(15)
  pNumber_cl                   CHAR(15)
  pReceiveEmail                CHAR(3)
  pReceiveSMS                  CHAR(3)
  pTypeCode_ib                 CHAR(3)
  pNumber_ib                   CHAR(20)
  pExpirationDate_ib           CHAR(8)
  pIssuerCountryCode_ib        CHAR(3)
  pIssuerStateCode_ib          CHAR(3)
  pReasonTypeCode              CHAR(3)
  pReasonForTransfer           CHAR(40)
  pSourceOfFunds               CHAR(40)
  pSecurityPhrase              CHAR(40)
  pFreeMessage                 CHAR(255)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTxn_status` | `CHAR(1)` | — | — |
| `pUnirefnum` | `CHAR(16)` | — | — |
| `pCode_Company` | `CHAR(3)` | — | — |
| `pChanneldid` | `CHAR(3)` | — | — |
| `pLocationunit` | `CHAR(15)` | — | — |
| `pNnumber` | `CHAR(15)` | — | — |
| `pTypecode_Branch` | `CHAR(3)` | — | — |
| `pCountrycode_Branch` | `CHAR(3)` | — | — |
| `pStatecode_Branch` | `CHAR(3)` | — | — |
| `pTerminalid` | `CHAR(15)` | — | — |
| `pProcessdate_Qry` | `CHAR(8)` | — | — |
| `pProcesstime_Qry` | `CHAR(6)` | — | — |
| `pCode_Operacion` | `CHAR(5)` | — | — |
| `pCode` | `CHAR(4)` | — | — |
| `pMessage` | `CHAR(255)` | — | — |
| `pCode_d` | `CHAR(4)` | — | — |
| `pMessage_d` | `CHAR(255)` | — | — |
| `pProcessDate` | `CHAR(8)` | — | — |
| `pProcessTime` | `CHAR(6)` | — | — |
| `pRule` | `CHAR(3)` | — | — |
| `pValue` | `CHAR(3)` | — | — |
| `pGlobalTrackingNumber` | `CHAR(20)` | — | — |
| `pOrderStatusCode` | `CHAR(3)` | `ord`=ordenante / orden (SPEI) | 🔵 CONVENCIÓN |
| `pOrderStatusDate` | `CHAR(8)` | `ord`=ordenante / orden (SPEI) | 🔵 CONVENCIÓN |
| `pOrderStatusTime` | `CHAR(6)` | `ord`=ordenante / orden (SPEI) | 🔵 CONVENCIÓN |
| `pUniqueReferenceNumber` | `CHAR(16)` | — | — |
| `pCodesalecom` | `CHAR(3)` | — | — |
| `pCountryCode` | `CHAR(3)` | — | — |
| `pStateCodeSale` | `CHAR(3)` | — | — |
| `pSaleDate` | `CHAR(8)` | — | — |
| `pSaleTime` | `CHAR(6)` | — | — |
| `pCountryCode_o` | `CHAR(3)` | — | — |
| `pCurrencyCode` | `CHAR(3)` | — | — |
| `pServiceCode` | `CHAR(3)` | — | — |
| `pCountryCode_d` | `CHAR(3)` | — | — |
| `pCurrencyCode_d` | `CHAR(3)` | — | — |
| `pDeliveryMethodCode` | `CHAR(3)` | — | — |
| `pPayNetworkCode` | `CHAR(3)` | — | — |
| `pPaySubNetworkCode` | `CHAR(15)` | — | — |
| `pBranchNumber` | `CHAR(15)` | — | — |
| `pAccountTypeCode` | `CHAR(3)` | — | — |
| `pAccountNumber` | `CHAR(30)` | — | — |
| `pOriginAmount` | `CHAR(20)` | — | — |
| `pDestinationAmount` | `CHAR(20)` | — | — |
| `pRetailExchangeRate` | `CHAR(21)` | — | — |
| `pWholesaleExchangeRate` | `CHAR(21)` | — | — |
| `pDestinExchangeRate` | `CHAR(21)` | — | — |
| `pServiceFeeAmount` | `CHAR(20)` | — | — |
| `pDiscountAmount` | `CHAR(20)` | — | — |
| `pTypeCode` | `CHAR(3)` | — | — |
| `pAccountNumber_c` | `CHAR(30)` | — | — |
| `pBicCode` | `CHAR(11)` | — | — |
| `pReferenceNumber` | `CHAR(30)` | — | — |
| `pCustomerNumber` | `CHAR(20)` | — | — |
| `pFirstName` | `CHAR(40)` | — | — |
| `pMiddleName` | `CHAR(40)` | — | — |
| `pLastName` | `CHAR(40)` | — | — |
| `pMotherMaidenName` | `CHAR(40)` | — | — |
| `pAddress` | `CHAR(80)` | — | — |
| `pCity` | `CHAR(40)` | — | — |
| `pCountryCode_a` | `CHAR(3)` | — | — |
| `pStateCode` | `CHAR(3)` | — | — |
| `pZipCode` | `CHAR(10)` | — | — |
| `pTypeCode_i` | `CHAR(3)` | — | — |
| `pNumber` | `CHAR(20)` | — | — |
| `pExpirationDate` | `CHAR(8)` | — | — |
| `pIssuerCountryCode` | `CHAR(3)` | — | — |
| `pIssuerStateCode` | `CHAR(3)` | — | — |
| `pDateOfBirth` | `CHAR(8)` | — | — |
| `pCustomerNumber_b` | `CHAR(20)` | — | — |
| `pFirstName_b` | `CHAR(40)` | — | — |
| `pMiddleName_b` | `CHAR(40)` | — | — |
| `pLastName_b` | `CHAR(40)` | — | — |
| `pMotherMaidenName_b` | `CHAR(40)` | — | — |
| `pFirstName_f` | `CHAR(40)` | — | — |
| `pMiddleName_f` | `CHAR(40)` | — | — |
| `pLastName_f` | `CHAR(40)` | — | — |
| `pMotherMaidenName_f` | `CHAR(40)` | — | — |
| `pAddress_b` | `CHAR(80)` | — | — |
| `pCity_b` | `CHAR(40)` | — | — |
| `pCountryCode_b` | `CHAR(3)` | — | — |
| `pStateCode_b` | `CHAR(3)` | — | — |
| `pZipCode_b` | `CHAR(10)` | — | — |
| `pEmail` | `CHAR(100)` | — | — |
| `pHomePhoneNumber` | `CHAR(15)` | — | — |
| `pWorkPhoneNumber` | `CHAR(15)` | — | — |
| `pNumber_cl` | `CHAR(15)` | — | — |
| `pReceiveEmail` | `CHAR(3)` | — | — |
| `pReceiveSMS` | `CHAR(3)` | — | — |
| `pTypeCode_ib` | `CHAR(3)` | — | — |
| `pNumber_ib` | `CHAR(20)` | — | — |
| `pExpirationDate_ib` | `CHAR(8)` | — | — |
| `pIssuerCountryCode_ib` | `CHAR(3)` | — | — |
| `pIssuerStateCode_ib` | `CHAR(3)` | — | — |
| `pReasonTypeCode` | `CHAR(3)` | — | — |
| `pReasonForTransfer` | `CHAR(40)` | — | — |
| `pSourceOfFunds` | `CHAR(40)` | — | — |
| `pSecurityPhrase` | `CHAR(40)` | — | — |
| `pFreeMessage` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L106 |
| `iSqlErr` | `INT` | L107 |
| `cCodRetSp` | `CHAR(5)` | L108 |
| `iCodRetSp` | `INTEGER` | L109 |
| `cDescCodRet` | `CHAR(255)` | L110 |
| `cMensajeD` | `CHAR(255)` | L111 |
| `cEmpresa` | `CHAR(3)` | L112 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_app_queryorder` | `bdisac` | ⚠️ sí | L149 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L166 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_app_queryorder';` |  |
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `?_query` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `?er` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_query`, `?er` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_repgenremesasrechazadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_repgenremesasrechazadas.sql` |
| **LOC (1er CREATE)** | 224 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 226 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte remesa y remesas internacionales" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_repgenremesasrechazadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cIdLimite` | `SMALLINT` | L8 |
| `cDescLimite` | `CHAR(35)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |
| `cQuery` | `CHAR(255)` | L12 |
| `cCmd1` | `CHAR(2000)` | L13 |
| `cSql` | `CHAR(2500)` | L14 |
| `cRutaGral` | `CHAR(150)` | L15 |
| `cRutaInformix` | `CHAR(100)` | L16 |
| `cUsrBin` | `CHAR(100)` | L17 |
| `cNombreRepXls` | `CHAR(45)` | L18 |
| `cNombreRepTxt` | `CHAR(45)` | L19 |
| `cRutaGralXls` | `CHAR(150)` | L20 |
| `cRutaGralTxt` | `CHAR(150)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L77 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( ";` |  |
| L111 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L116 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralXls);` |  |
| L125 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralXls)\|\|".tmp > "\|\|TRIM(cRutaGralXls);` |  |
| L134 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralXls)\|\|" > "\|\|TRIM(cRutaGralXls)\` |  |
| L174 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |
| L179 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGralTxt);` |  |
| L188 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGralTxt)\|\|".tmp > "\|\|TRIM(cRutaGralTxt);` |  |
| L197 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGralTxt)\|\|" > "\|\|TRIM(cRutaGralTxt)\` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?hazadas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hazadas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_submitpayment`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_submitpayment.sql` |
| **LOC (1er CREATE)** | 194 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa (sub-, tipo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_app_submitpayment` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_submitpayment(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  ptxn_status                  CHAR(1)
  punirefnum                   CHAR(16)
  prefnum                      CHAR(30)
  pcode                        CHAR(3)
  pchanneldid                  CHAR(3)
  plocationunit                CHAR(15)
  pnnumber                     CHAR(15)
  ptypecode                    CHAR(3)
  pcountrycode                 CHAR(3)
  pstatecode                   CHAR(3)
  pterminalid                  CHAR(15)
  pprocessdate                 CHAR(8)
  pprocesstime                 CHAR(6)
  pcustomernumber              CHAR(20)
  pfirstname                   CHAR(40)
  pmiddlename                  CHAR(40)
  plastname                    CHAR(40)
  pmommaidenname               CHAR(40)
  padress                      CHAR(80)
  pcity                        CHAR(40)
  pcountrycodeadr              CHAR(3)
  pstatecodeadr                CHAR(3)
  pzipcode                     CHAR(10)
  pemail                       CHAR(100)
  phomephonenum                CHAR(15)
  pnumbercel                   CHAR(15)
  preceiveemail                CHAR(3)
  preceivesms                  CHAR(3)
  ptypecodeci                  CHAR(3)
  pnumberci                    CHAR(20)
  pexpirationdate              CHAR(8)
  pissuercc                    CHAR(3)
  pdateofbirth                 CHAR(8)
  pcontrycode                  CHAR(5)
  pr_operacion                 CHAR(5)
  pr_code                      CHAR(4)
  pr_message                   CHAR(255)
  pr_code_d                    CHAR(4)
  pr_message_d                 CHAR(255)
  pr_processdate               CHAR(8)
  pr_processtime               CHAR(6)
  pr_rule                      CHAR(3)
  pr_value                     CHAR(3)
  pr_globtracknum              CHAR(20)
  pr_ordstatuscode             CHAR(3)
  pr_ordstatusdate             CHAR(8)
  pr_ordstatustime             CHAR(6)
  pr_uniquerefnum              CHAR(16)
  pr_codesalecom               CHAR(3)
  pr_countrycode               CHAR(3)
  pr_statecodesale             CHAR(3)
  pr_saledate                  CHAR(8)
  pr_saletime                  CHAR(6)
  pr_countrycode_o             CHAR(3)
  pr_currencycode              CHAR(3)
  pr_servicecode               CHAR(3)
  pr_countrycode_d             CHAR(3)
  pr_currencycod_d             CHAR(3)
  pr_delimethodcod             CHAR(3)
  pr_playnwcode                CHAR(3)
  pr_paysubnwcode              CHAR(15)
  pr_branchnumber              CHAR(15)
  pr_accounttcod               CHAR(3)
  pr_accountnumber             CHAR(30)
  pr_originamount              CHAR(20)
  pr_destinamount              CHAR(20)
  pr_rexchangerate             CHAR(21)
  pr_wholesalerate             CHAR(21)
  pr_deexhangerate             CHAR(21)
  pr_servfeeamount             CHAR(20)
  pr_discountamoun             CHAR(20)
  pr_typecode                  CHAR(3)
  pr_accountnum                CHAR(30)
  pr_biccode                   CHAR(11)
  pr_refnumber                 CHAR(30)
  pr_customernum               CHAR(20)
  pr_firstname                 CHAR(40)
  pr_middlename                CHAR(40)
  pr_lastname                  CHAR(40)
  pr_mommaidenname             CHAR(40)
  pr_address                   CHAR(80)
  pr_city                      CHAR(40)
  pr_countrycode_a             CHAR(3)
  pr_statecode                 CHAR(3)
  pr_zipcode                   CHAR(10)
  pr_typecode_i                CHAR(3)
  pr_number                    CHAR(20)
  pr_expirdate                 CHAR(8)
  pr_isscontrycode             CHAR(3)
  pr_issstatecode              CHAR(3)
  pr_dateofbirth               CHAR(8)
  pr_customernum_b             CHAR(20)
  pr_firstname_b               CHAR(40)
  pr_middlename_b              CHAR(40)
  pr_lastname_b                CHAR(40)
  pr_mommaidenna_b             CHAR(40)
  pr_firstname_f               CHAR(40)
  pr_middlename_f              CHAR(40)
  pr_lastname_f                CHAR(40)
  pr_mommaidenna_f             CHAR(40)
  pr_address_b                 CHAR(80)
  pr_city_b                    CHAR(40)
  pr_countrycode_b             CHAR(3)
  pr_statecode_b               CHAR(3)
  pr_zipcode_b                 CHAR(10)
  pr_email                     CHAR(100)
  pr_homephonenum              CHAR(15)
  pr_workphonenum              CHAR(15)
  pr_number_cl                 CHAR(15)
  pr_receiveemail              CHAR(3)
  pr_receivesms                CHAR(3)
  pr_typecode_ib               CHAR(3)
  pr_number_ib                 CHAR(20)
  pr_expirdate_ib              CHAR(8)
  pr_issconcode_ib             CHAR(3)
  pr_issstacode_ib             CHAR(3)
  pr_reastypecode              CHAR(3)
  pr_refortransfer             CHAR(40)
  pr_sourceoffunds             CHAR(40)
  pr_securphrase               CHAR(40)
  pr_feemessage                CHAR(255)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `ptxn_status` | `CHAR(1)` | — | — |
| `punirefnum` | `CHAR(16)` | — | — |
| `prefnum` | `CHAR(30)` | — | — |
| `pcode` | `CHAR(3)` | — | — |
| `pchanneldid` | `CHAR(3)` | — | — |
| `plocationunit` | `CHAR(15)` | — | — |
| `pnnumber` | `CHAR(15)` | — | — |
| `ptypecode` | `CHAR(3)` | — | — |
| `pcountrycode` | `CHAR(3)` | — | — |
| `pstatecode` | `CHAR(3)` | — | — |
| `pterminalid` | `CHAR(15)` | — | — |
| `pprocessdate` | `CHAR(8)` | — | — |
| `pprocesstime` | `CHAR(6)` | — | — |
| `pcustomernumber` | `CHAR(20)` | — | — |
| `pfirstname` | `CHAR(40)` | — | — |
| `pmiddlename` | `CHAR(40)` | — | — |
| `plastname` | `CHAR(40)` | — | — |
| `pmommaidenname` | `CHAR(40)` | — | — |
| `padress` | `CHAR(80)` | — | — |
| `pcity` | `CHAR(40)` | — | — |
| `pcountrycodeadr` | `CHAR(3)` | — | — |
| `pstatecodeadr` | `CHAR(3)` | — | — |
| `pzipcode` | `CHAR(10)` | — | — |
| `pemail` | `CHAR(100)` | — | — |
| `phomephonenum` | `CHAR(15)` | — | — |
| `pnumbercel` | `CHAR(15)` | — | — |
| `preceiveemail` | `CHAR(3)` | — | — |
| `preceivesms` | `CHAR(3)` | — | — |
| `ptypecodeci` | `CHAR(3)` | — | — |
| `pnumberci` | `CHAR(20)` | — | — |
| `pexpirationdate` | `CHAR(8)` | — | — |
| `pissuercc` | `CHAR(3)` | — | — |
| `pdateofbirth` | `CHAR(8)` | — | — |
| `pcontrycode` | `CHAR(5)` | — | — |
| `pr_operacion` | `CHAR(5)` | — | — |
| `pr_code` | `CHAR(4)` | — | — |
| `pr_message` | `CHAR(255)` | — | — |
| `pr_code_d` | `CHAR(4)` | — | — |
| `pr_message_d` | `CHAR(255)` | — | — |
| `pr_processdate` | `CHAR(8)` | — | — |
| `pr_processtime` | `CHAR(6)` | — | — |
| `pr_rule` | `CHAR(3)` | — | — |
| `pr_value` | `CHAR(3)` | — | — |
| `pr_globtracknum` | `CHAR(20)` | — | — |
| `pr_ordstatuscode` | `CHAR(3)` | — | — |
| `pr_ordstatusdate` | `CHAR(8)` | — | — |
| `pr_ordstatustime` | `CHAR(6)` | — | — |
| `pr_uniquerefnum` | `CHAR(16)` | — | — |
| `pr_codesalecom` | `CHAR(3)` | — | — |
| `pr_countrycode` | `CHAR(3)` | — | — |
| `pr_statecodesale` | `CHAR(3)` | — | — |
| `pr_saledate` | `CHAR(8)` | — | — |
| `pr_saletime` | `CHAR(6)` | — | — |
| `pr_countrycode_o` | `CHAR(3)` | — | — |
| `pr_currencycode` | `CHAR(3)` | — | — |
| `pr_servicecode` | `CHAR(3)` | — | — |
| `pr_countrycode_d` | `CHAR(3)` | — | — |
| `pr_currencycod_d` | `CHAR(3)` | — | — |
| `pr_delimethodcod` | `CHAR(3)` | — | — |
| `pr_playnwcode` | `CHAR(3)` | — | — |
| `pr_paysubnwcode` | `CHAR(15)` | `sub`=sub- | 🟡 INFERIDO |
| `pr_branchnumber` | `CHAR(15)` | — | — |
| `pr_accounttcod` | `CHAR(3)` | — | — |
| `pr_accountnumber` | `CHAR(30)` | — | — |
| `pr_originamount` | `CHAR(20)` | — | — |
| `pr_destinamount` | `CHAR(20)` | — | — |
| `pr_rexchangerate` | `CHAR(21)` | — | — |
| `pr_wholesalerate` | `CHAR(21)` | — | — |
| `pr_deexhangerate` | `CHAR(21)` | — | — |
| `pr_servfeeamount` | `CHAR(20)` | — | — |
| `pr_discountamoun` | `CHAR(20)` | — | — |
| `pr_typecode` | `CHAR(3)` | — | — |
| `pr_accountnum` | `CHAR(30)` | — | — |
| `pr_biccode` | `CHAR(11)` | — | — |
| `pr_refnumber` | `CHAR(30)` | — | — |
| `pr_customernum` | `CHAR(20)` | — | — |
| `pr_firstname` | `CHAR(40)` | — | — |
| `pr_middlename` | `CHAR(40)` | — | — |
| `pr_lastname` | `CHAR(40)` | — | — |
| `pr_mommaidenname` | `CHAR(40)` | — | — |
| `pr_address` | `CHAR(80)` | — | — |
| `pr_city` | `CHAR(40)` | — | — |
| `pr_countrycode_a` | `CHAR(3)` | — | — |
| `pr_statecode` | `CHAR(3)` | — | — |
| `pr_zipcode` | `CHAR(10)` | — | — |
| `pr_typecode_i` | `CHAR(3)` | — | — |
| `pr_number` | `CHAR(20)` | — | — |
| `pr_expirdate` | `CHAR(8)` | — | — |
| `pr_isscontrycode` | `CHAR(3)` | — | — |
| `pr_issstatecode` | `CHAR(3)` | — | — |
| `pr_dateofbirth` | `CHAR(8)` | — | — |
| `pr_customernum_b` | `CHAR(20)` | — | — |
| `pr_firstname_b` | `CHAR(40)` | — | — |
| `pr_middlename_b` | `CHAR(40)` | — | — |
| `pr_lastname_b` | `CHAR(40)` | — | — |
| `pr_mommaidenna_b` | `CHAR(40)` | — | — |
| `pr_firstname_f` | `CHAR(40)` | — | — |
| `pr_middlename_f` | `CHAR(40)` | — | — |
| `pr_lastname_f` | `CHAR(40)` | — | — |
| `pr_mommaidenna_f` | `CHAR(40)` | — | — |
| `pr_address_b` | `CHAR(80)` | — | — |
| `pr_city_b` | `CHAR(40)` | — | — |
| `pr_countrycode_b` | `CHAR(3)` | — | — |
| `pr_statecode_b` | `CHAR(3)` | — | — |
| `pr_zipcode_b` | `CHAR(10)` | — | — |
| `pr_email` | `CHAR(100)` | — | — |
| `pr_homephonenum` | `CHAR(15)` | — | — |
| `pr_workphonenum` | `CHAR(15)` | — | — |
| `pr_number_cl` | `CHAR(15)` | — | — |
| `pr_receiveemail` | `CHAR(3)` | — | — |
| `pr_receivesms` | `CHAR(3)` | — | — |
| `pr_typecode_ib` | `CHAR(3)` | — | — |
| `pr_number_ib` | `CHAR(20)` | — | — |
| `pr_expirdate_ib` | `CHAR(8)` | — | — |
| `pr_issconcode_ib` | `CHAR(3)` | — | — |
| `pr_issstacode_ib` | `CHAR(3)` | — | — |
| `pr_reastypecode` | `CHAR(3)` | — | — |
| `pr_refortransfer` | `CHAR(40)` | — | — |
| `pr_sourceoffunds` | `CHAR(40)` | — | — |
| `pr_securphrase` | `CHAR(40)` | — | — |
| `pr_feemessage` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L126 |
| `iSqlErr` | `INTEGER` | L127 |
| `cCodRetSp` | `CHAR(5)` | L128 |
| `iCodRetSp` | `INTEGER` | L129 |
| `iNoRegistros` | `INTEGER` | L130 |
| `cEmpresa` | `CHAR(3)` | L131 |
| `cError_Desc` | `CHAR(255)` | L132 |
| `cError_Desc_Detail` | `CHAR (255)` | L133 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L160 |
| `sp_app_submitpayment` | `bdisac` | ⚠️ sí | L165 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L155 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L178 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisac:sp_app_submitpayment";` |  |
| L180 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `sub` | MODIF | sub- | 🟡 INFERIDO | nombre_sp |
| `?mi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?ayment` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mi`, `?ayment` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_submitpayreversal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_submitpayreversal.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Reversión — anula/revierte una operación remesa (sub-, tipo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_app_submitpayreversal` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_submitpayreversal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  ptxn_status                  CHAR(1)
  punirefnum                   CHAR(16)
  prefnum                      CHAR(30)
  pprocretypecode              CHAR(3)
  pcode                        CHAR(3)
  pchanneldid                  CHAR(3)
  plocationunit                CHAR(15)
  pnnumber                     CHAR(15)
  ptypecode                    CHAR(3)
  pcountrycode                 CHAR(3)
  pstatecode                   CHAR(3)
  terminalid                   CHAR(15)
  pprocessdate                 CHAR(8)
  pprocesstime                 CHAR(6)
  pr_operacion                 CHAR(5)
  pr_code                      CHAR(4)
  pr_message                   CHAR(255)
  pr_code_d                    CHAR(4)
  pr_message_d                 CHAR(255)
  pr_processdate               CHAR(8)
  pr_processtime               CHAR(6)
  pr_uniquerefnum              CHAR(16)
  pr_globtracknum              CHAR(20)
  pr_ordstatuscode             CHAR(3)
  pr_ordstatusdate             CHAR(8)
  pr_ordstatustime             CHAR(6)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `ptxn_status` | `CHAR(1)` | — | — |
| `punirefnum` | `CHAR(16)` | — | — |
| `prefnum` | `CHAR(30)` | — | — |
| `pprocretypecode` | `CHAR(3)` | — | — |
| `pcode` | `CHAR(3)` | — | — |
| `pchanneldid` | `CHAR(3)` | — | — |
| `plocationunit` | `CHAR(15)` | — | — |
| `pnnumber` | `CHAR(15)` | — | — |
| `ptypecode` | `CHAR(3)` | — | — |
| `pcountrycode` | `CHAR(3)` | — | — |
| `pstatecode` | `CHAR(3)` | — | — |
| `terminalid` | `CHAR(15)` | — | — |
| `pprocessdate` | `CHAR(8)` | — | — |
| `pprocesstime` | `CHAR(6)` | — | — |
| `pr_operacion` | `CHAR(5)` | — | — |
| `pr_code` | `CHAR(4)` | — | — |
| `pr_message` | `CHAR(255)` | — | — |
| `pr_code_d` | `CHAR(4)` | — | — |
| `pr_message_d` | `CHAR(255)` | — | — |
| `pr_processdate` | `CHAR(8)` | — | — |
| `pr_processtime` | `CHAR(6)` | — | — |
| `pr_uniquerefnum` | `CHAR(16)` | — | — |
| `pr_globtracknum` | `CHAR(20)` | — | — |
| `pr_ordstatuscode` | `CHAR(3)` | — | — |
| `pr_ordstatusdate` | `CHAR(8)` | — | — |
| `pr_ordstatustime` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L31 |
| `iSqlErr` | `INTEGER` | L32 |
| `cCodRetSp` | `CHAR(5)` | L33 |
| `iCodRetSp` | `INTEGER` | L34 |
| `iNoRegistros` | `INTEGER` | L35 |
| `cEmpresa` | `CHAR(3)` | L36 |
| `cError_Desc` | `CHAR(255)` | L37 |
| `cError_Desc_Detail` | `CHAR (255)` | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |
| `sp_app_submitpayreversal` | `bdisac` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisac:sp_app_submitpayreversal";` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `sub` | MODIF | sub- | 🟡 INFERIDO | nombre_sp |
| `?mi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?ay` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mi`, `?ay`, `?l` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_validaprocesosappriza`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_validaprocesosappriza.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida remesa y proceso (canal app)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_validaprocesosappriza(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumRem                      CHAR(12)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pNumRem` | `CHAR(12)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRet` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_app_valdigito` | `bdisac` | ⚠️ sí | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_app_valdigito';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `app` | MODIF | canal app | 🔵 CONVENCIÓN | nombre_sp |
| `?riza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?riza` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_validaprocesosbts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_validaprocesosbts.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida remesa, proceso y beneficiarios" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_validaprocesosbts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pNumBts                      CHAR(12)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pNumBts` | `CHAR(12)` | `bts`=beneficiarios (BTS) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cFecha` | `CHAR(10)` | L12 |
| `dHora` | `DATETIME HOUR TO FRACTION(3)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtenfechahrasistema` | `bdinteg` | ⚠️ sí | L53 |
| `sp_validabts` | `bdisac` | ⚠️ sí | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdinteg:sp_obtenfechahrasistema` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_validabts';` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_validabts';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_validaremesaapp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_validaremesaapp.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida remesa (canal app)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_validaremesaapp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaPago                   CHAR(10)
  pFolioSuc                    CHAR(16)
  pNoConfirmacion              CHAR(12)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaPago` | `CHAR(10)` | — | — |
| `pFolioSuc` | `CHAR(16)` | — | — |
| `pNoConfirmacion` | `CHAR(12)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |
| `cFolioSuc` | `CHAR(16)` | L19 |
| `cAdress` | `CHAR(80)` | L20 |
| `cCity` | `CHAR(40)` | L21 |
| `cCountrycodeadr` | `CHAR(3)` | L22 |
| `cStatecodeadr` | `CHAR(3)` | L23 |
| `cZipcode` | `CHAR(10)` | L24 |
| `cEmail` | `CHAR(100)` | L25 |
| `cHomephonenum` | `CHAR(15)` | L26 |
| `cNumbercel` | `CHAR(15)` | L27 |
| `cReceiveemail` | `CHAR(3)` | L28 |
| `cReceivesms` | `CHAR(3)` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientos` | `bdisac` | ⚠️ sí | SELECT | L74 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L86 |
| `sac_app_payi` | `bdisac` | ⚠️ sí | SELECT | L107 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L121 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet      = '00000';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L97 | CÓDIGO_RETORNO | `LET cCodRet = '00975';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_rem_validaremesabts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_validaremesabts.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida remesa y beneficiarios" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_validaremesabts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumBts                      CHAR(11)
  pFolioSucursal               CHAR(16)
  pFechaRealPago               DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumBts` | `CHAR(11)` | `bts`=beneficiarios (BTS) | ✅ CÓDIGO |
| `pFolioSucursal` | `CHAR(16)` | — | — |
| `pFechaRealPago` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L26 |
| `iSqlErr` | `INT` | L27 |
| `cCodRetSp` | `CHAR(6)` | L28 |
| `iCodRetSp` | `INTEGER` | L29 |
| `cDescCodRet` | `CHAR(80)` | L30 |
| `cEmpresa` | `CHAR(3)` | L31 |
| `cSucursal` | `CHAR(15)` | L33 |
| `cTerminal` | `CHAR(15)` | L34 |
| `cR_Type_Cd` | `CHAR(3)` | L35 |
| `cR_Identif_Nm` | `CHAR(20)` | L36 |
| `cR_Nom_Calle` | `CHAR(50)` | L37 |
| `cR_Num_Ext` | `CHAR(5)` | L38 |
| `cR_Num_Int` | `CHAR(5)` | L39 |
| `cR_Depto` | `CHAR(10)` | L40 |
| `cR_Colonia` | `CHAR(80)` | L41 |
| `cR_Cp` | `CHAR(5)` | L42 |
| `cR_Mncpo_Deleg` | `CHAR(50)` | L43 |
| `cR_Ciudad` | `CHAR(50)` | L44 |
| `cR_Estado` | `CHAR(50)` | L45 |
| `cR_Issuer_Country_Cd` | `CHAR(3)` | L46 |
| `cR_Telefono` | `CHAR(15)` | L47 |
| `cTipo_Pago` | `CHAR(1)` | L48 |
| `cR_Fecha_Nac` | `CHAR(8)` | L49 |
| `cR_Nacionalidad` | `CHAR(50)` | L50 |
| `cR_Pais_Nac` | `CHAR(20)` | L51 |
| *…3 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_validarembtsensac` | `bdisac` | ⚠️ sí | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L56 | CÓDIGO_RETORNO | `LET cCodRet 				= '00000';` |  |
| L101 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumBts = '' OR pFolioSucursal = '' OR pFechaRealPago IS NULL` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L128 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisac:sp_validarembtsensac';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rem_verificastatusappqryi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_verificastatusappqryi.sql` |
| **LOC (1er CREATE)** | 441 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 71 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica remesa y estatus (canal app)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_verificastatusappqryi(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumRem                      CHAR(12)
  pRCode                       CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumRem` | `CHAR(12)` | `rem`=remesa (forma corta) | 🟡 INFERIDO |
| `pRCode` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L80 |
| `iSqlErr` | `INTEGER` | L81 |
| `cEmpresa` | `CHAR(3)` | L82 |
| `cStatus` | `CHAR(1)` | L83 |
| `cErrorProceso` | `CHAR(1)` | L84 |
| `cError` | `CHAR(5)` | L85 |
| `iIdRegistro` | `INTEGER` | L86 |
| `cTxn_status` | `CHAR(1)` | L88 |
| `cUnirefnum` | `CHAR(16)` | L89 |
| `cCode_Company` | `CHAR(3)` | L90 |
| `cChanneldid` | `CHAR(3)` | L91 |
| `cLocationunit` | `CHAR(15)` | L92 |
| `cNnumber` | `CHAR(15)` | L93 |
| `cTypecode_Branch` | `CHAR(3)` | L94 |
| `cCountrycode_Branch` | `CHAR(3)` | L95 |
| `cStatecode_Branch` | `CHAR(3)` | L96 |
| `cTerminalid` | `CHAR(15)` | L97 |
| `cProcessdate_Qry` | `CHAR(8)` | L98 |
| `cProcesstime_Qry` | `CHAR(6)` | L99 |
| `cCode_Operacion` | `CHAR(5)` | L100 |
| `cCode` | `CHAR(4)` | L101 |
| `cMensCode` | `CHAR(255)` | L102 |
| `cCode_d` | `CHAR(4)` | L103 |
| `cMensajeD` | `CHAR(255)` | L104 |
| `cProcessDate` | `CHAR(8)` | L105 |
| *…101 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verifica_estatusrem` | `bdicnweb` | no | SELECT | L410 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L376 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L211 | CÓDIGO_RETORNO | `LET cCodRet 					= '00000';` |  |
| L363 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `app` | MODIF | canal app | 🔵 CONVENCIÓN | nombre_sp |
| `?qryi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?qryi` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rem_verificastatusremesasnopagadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rem_verificastatusremesasnopagadas.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica remesa, estatus y remesas internacionales" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_rem_verificastatusremesasnopagadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `iNumRegistros` | `INTEGER` | L12 |
| `cClaveId` | `CHAR(100)` | L13 |
| `cErrorProceso` | `CHAR(1)` | L14 |
| `cError` | `CHAR(5)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusprocesoremnopag` | `bdicnweb` | no | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?nopagadas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nopagadas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_remesamensajeserrorwu`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_remesamensajeserrorwu.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "remesa, mensajes, error y Western Union — servicio de remesas/transferencias internacionales" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_remesamensajeserrorwu(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCode                        CHAR(6)
  pDescripcion                 CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCode` | `CHAR(6)` | — | — |
| `pDescripcion` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodError` | `CHAR(6)` | L8 |
| `cTraduccion` | `CHAR(255)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `wswesternunionerrores` | `intercard` | ⚠️ sí | SELECT | L42 |
| `wswesternunionerrores` | `intercard` | ⚠️ sí | INSERT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet         = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | 🔵 CONVENCIÓN | nombre_sp |
| `mensajes` | ENTIDAD | mensajes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `error` | ENTIDAD | error | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `wu` | ENTIDAD | Western Union — servicio de remesas/transferencias internaci | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_remesasconsultapagowu`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_remesasconsultapagowu.sql` |
| **LOC (1er CREATE)** | 295 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 68 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesas internacionales, pago y Western Union — servicio de remesas/transferencias internacionales" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_remesasconsultapagowu(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMtcn                        CHAR(10)
  pFecha                       CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMtcn` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L64 |
| `iSqlErr` | `INTEGER` | L65 |
| `cCodRetSp` | `CHAR(5)` | L66 |
| `iCodRetSp` | `INTEGER` | L67 |
| `iNoRegistros` | `INTEGER` | L68 |
| `cTxnStatus` | `CHAR(1)` | L69 |
| `cChannelType` | `CHAR(3)` | L70 |
| `cChannelName` | `CHAR(3)` | L71 |
| `cChannelVersion` | `CHAR(4)` | L72 |
| `cBenefNametype` | `CHAR(1)` | L73 |
| `cBenefNombre1` | `CHAR(40)` | L74 |
| `cBenefNombre2` | `CHAR(40)` | L75 |
| `cBenefAppaterno` | `CHAR(40)` | L76 |
| `cBenefApmaterno` | `CHAR(40)` | L77 |
| `cBenefCiudad` | `CHAR(24)` | L78 |
| `cBenefEdo` | `CHAR(40)` | L79 |
| `cBenefCp` | `CHAR(9)` | L80 |
| `cTemplateId` | `CHAR(10)` | L81 |
| `cBenefIdType` | `CHAR(1)` | L82 |
| `cBenefIdPaisExpedicion` | `CHAR(45)` | L83 |
| `cBenefIdNumber` | `CHAR(20)` | L84 |
| `cIdBenefTieneFechaVenc` | `CHAR(1)` | L85 |
| `cBenefIdFechaVencimiento` | `CHAR(8)` | L86 |
| `cBenefFechaNac` | `CHAR(8)` | L87 |
| `cBenefOcupacion` | `CHAR(30)` | L88 |
| *…40 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_wu_pay` | `bdisac` | ⚠️ sí | SELECT | L224 |
| `sac_wu_pay_old` | `bdisac` | ⚠️ sí | SELECT | L261 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L130 | CÓDIGO_RETORNO | `LET cCodRet 						='00000';` |  |
| L212 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L275 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `wu` | ENTIDAD | Western Union — servicio de remesas/transferencias internaci | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_remesasconsultawu`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_remesasconsultawu.sql` |
| **LOC (1er CREATE)** | 351 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 70 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta remesas internacionales y Western Union — servicio de remesas/transferencias internacionales" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_remesasconsultawu(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMtcn                        CHAR(10)
  pNumeroConvenio              CHAR(3)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMtcn` | `CHAR(10)` | — | — |
| `pNumeroConvenio` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L66 |
| `iSqlErr` | `INTEGER` | L67 |
| `cCodRetSp` | `CHAR(5)` | L68 |
| `iCodRetSp` | `INTEGER` | L69 |
| `iNoRegistros` | `INTEGER` | L70 |
| `cTxnStatus` | `CHAR(1)` | L71 |
| `cChannelType` | `CHAR(3)` | L72 |
| `cChannelName` | `CHAR(3)` | L73 |
| `cChannelVersion` | `CHAR(4)` | L74 |
| `cForeignRsSystemIdRq` | `CHAR(11)` | L75 |
| `cForeignRsRefnumRq` | `CHAR(16)` | L76 |
| `cForeignRsCntidRq` | `CHAR(11)` | L77 |
| `cMtcn` | `CHAR(10)` | L78 |
| `cFechaHoraRq` | `DATETIME YEAR to SECOND` | L79 |
| `cRetcode` | `CHAR(5)` | L80 |
| `cEmisorNametype` | `CHAR(1)` | L81 |
| `cEmisorNombre1` | `CHAR(40)` | L82 |
| `cEmisorNombre2` | `CHAR(40)` | L83 |
| `cEmisorAppaterno` | `CHAR(40)` | L84 |
| `cEmisorApmaterno` | `CHAR(40)` | L85 |
| `cEmisorCiudad` | `CHAR(20)` | L86 |
| `cEmisorEdo` | `CHAR(3)` | L87 |
| `cEmisorCodPais` | `CHAR(3)` | L88 |
| `cEmisorCodMoneda` | `CHAR(3)` | L89 |
| `cEmisorCp` | `CHAR(8)` | L90 |
| *…43 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_wu_search` | `bdisac` | ⚠️ sí | SELECT | L239 |
| `sac_movimientos` | `bdisac` | ⚠️ sí | SELECT | L259 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L283 |
| `sac_movimientoshistorial_old` | `bdisac` | ⚠️ sí | SELECT | L307 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L135 | CÓDIGO_RETORNO | `LET cCodRet 			 	='00000';` |  |
| L218 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L248 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L332 | CÓDIGO_RETORNO | `LET cCodRet = '00975';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `wu` | ENTIDAD | Western Union — servicio de remesas/transferencias internaci | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_remesasguardarespuestawu`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_remesasguardarespuestawu.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda remesas internacionales, respuesta y Western Union — servicio de remesas/transferencias internacionales" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sac_wu_guardarespuesta_search` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_remesasguardarespuestawu(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMarca                       CHAR(2)
  pForeignRsRefNumRq           CHAR(16)
  pMtcn                        CHAR(10)
  pFechaHoraRq                 CHAR(25)
  pRetCode                     CHAR(5)
  pEmisorNameType              CHAR(1)
  pEmisorNombre1               CHAR(40)
  pEmisorNombre2               CHAR(40)
  pEmisorApPaterno             CHAR(40)
  pEmisorApMaterno             CHAR(40)
  pEmisorCiudad                CHAR(20)
  pEmisorEdo                   CHAR(40)
  pEmisorCodPais               CHAR(3)
  pEmisorCodMoneda             CHAR(3)
  pEmisorCp                    CHAR(8)
  pEmisorCalle                 CHAR(30)
  pEmisorTel                   CHAR(15)
  pBenefNameType               CHAR(1)
  pBenefNombre1                CHAR(40)
  pBenefNombre2                CHAR(40)
  pBenefApaterno               CHAR(40)
  pBenefAmaterno               CHAR(40)
  pBenefCiudad                 CHAR(20)
  pBenefEdo                    CHAR(40)
  pBenefCodPais                CHAR(3)
  pBenefCodMoneda              CHAR(3)
  pBenefCp                     CHAR(8)
  pBenefCalle                  CHAR(30)
  pBenefTelPart                CHAR(15)
  pBenefTelCel                 CHAR(10)
  pMontoTotalOrigen            CHAR(10)
  pMontoToTDestino             CHAR(10)
  pMontoOrigen                 CHAR(10)
  pMontoCargos                 CHAR(10)
  pCdOrigenPago                CHAR(30)
  pTipoCambio                  CHAR(10)
  pFechaAltaRemesa             CHAR(8)
  pHoraAltaRemesa              CHAR(16)
  pMoneyTransKey               CHAR(10)
  pEstatusRemesa               CHAR(4)
  pNewMtcn                     CHAR(16)
  pFusionStatus                CHAR(4)
  pNoPaginas                   CHAR(2)
  pPaginaActual                CHAR(2)
  pNumCoincidencias            CHAR(2)
  pForeignRsSystemIdRp         CHAR(11)
  pForeignRsRefNumRp           CHAR(16)
  pForeingRsCantIdRp           CHAR(11)
  pDescError                   CHAR(250)
  pPartnerIdErr                CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMarca` | `CHAR(2)` | — | — |
| `pForeignRsRefNumRq` | `CHAR(16)` | — | — |
| `pMtcn` | `CHAR(10)` | — | — |
| `pFechaHoraRq` | `CHAR(25)` | — | — |
| `pRetCode` | `CHAR(5)` | — | — |
| `pEmisorNameType` | `CHAR(1)` | — | — |
| `pEmisorNombre1` | `CHAR(40)` | — | — |
| `pEmisorNombre2` | `CHAR(40)` | — | — |
| `pEmisorApPaterno` | `CHAR(40)` | — | — |
| `pEmisorApMaterno` | `CHAR(40)` | — | — |
| `pEmisorCiudad` | `CHAR(20)` | — | — |
| `pEmisorEdo` | `CHAR(40)` | — | — |
| `pEmisorCodPais` | `CHAR(3)` | — | — |
| `pEmisorCodMoneda` | `CHAR(3)` | — | — |
| `pEmisorCp` | `CHAR(8)` | — | — |
| `pEmisorCalle` | `CHAR(30)` | — | — |
| `pEmisorTel` | `CHAR(15)` | — | — |
| `pBenefNameType` | `CHAR(1)` | — | — |
| `pBenefNombre1` | `CHAR(40)` | — | — |
| `pBenefNombre2` | `CHAR(40)` | — | — |
| `pBenefApaterno` | `CHAR(40)` | — | — |
| `pBenefAmaterno` | `CHAR(40)` | — | — |
| `pBenefCiudad` | `CHAR(20)` | — | — |
| `pBenefEdo` | `CHAR(40)` | — | — |
| `pBenefCodPais` | `CHAR(3)` | — | — |
| `pBenefCodMoneda` | `CHAR(3)` | — | — |
| `pBenefCp` | `CHAR(8)` | — | — |
| `pBenefCalle` | `CHAR(30)` | — | — |
| `pBenefTelPart` | `CHAR(15)` | — | — |
| `pBenefTelCel` | `CHAR(10)` | — | — |
| `pMontoTotalOrigen` | `CHAR(10)` | — | — |
| `pMontoToTDestino` | `CHAR(10)` | — | — |
| `pMontoOrigen` | `CHAR(10)` | — | — |
| `pMontoCargos` | `CHAR(10)` | — | — |
| `pCdOrigenPago` | `CHAR(30)` | — | — |
| `pTipoCambio` | `CHAR(10)` | — | — |
| `pFechaAltaRemesa` | `CHAR(8)` | — | — |
| `pHoraAltaRemesa` | `CHAR(16)` | — | — |
| `pMoneyTransKey` | `CHAR(10)` | — | — |
| `pEstatusRemesa` | `CHAR(4)` | — | — |
| `pNewMtcn` | `CHAR(16)` | — | — |
| `pFusionStatus` | `CHAR(4)` | — | — |
| `pNoPaginas` | `CHAR(2)` | — | — |
| `pPaginaActual` | `CHAR(2)` | — | — |
| `pNumCoincidencias` | `CHAR(2)` | — | — |
| `pForeignRsSystemIdRp` | `CHAR(11)` | — | — |
| `pForeignRsRefNumRp` | `CHAR(16)` | — | — |
| `pForeingRsCantIdRp` | `CHAR(11)` | — | — |
| `pDescError` | `CHAR(250)` | — | — |
| `pPartnerIdErr` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L55 |
| `iSqlErr` | `INTEGER` | L56 |
| `cCodRetSp` | `CHAR(3)` | L57 |
| `iCodRetSp` | `INTEGER` | L58 |
| `iNoRegistros` | `INTEGER` | L59 |
| `cEmpresa` | `CHAR(3)` | L60 |
| `cError_Desc` | `CHAR(30)` | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |
| `sp_sac_wu_guardarespuesta_search` | `bdisac` | ⚠️ sí | L93 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L103 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisac:sp_sac_wu_guardarespuesta_search"` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `respuesta` | ENTIDAD | respuesta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `wu` | ENTIDAD | Western Union — servicio de remesas/transferencias internaci | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_remesasguardarespuestawu2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_remesasguardarespuestawu2.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda remesas internacionales, respuesta y Western Union — servicio de remesas/transferencias internacionales" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sac_wu_guardarespuesta_search_web` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_remesasguardarespuestawu2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMarca                       CHAR(2)
  pForeignRsRefNumRq           CHAR(16)
  pMtcn                        CHAR(10)
  pFechaHoraRq                 CHAR(25)
  pRetCode                     CHAR(5)
  pEmisorNameType              CHAR(1)
  pEmisorNombre1               CHAR(40)
  pEmisorNombre2               CHAR(40)
  pEmisorApPaterno             CHAR(40)
  pEmisorApMaterno             CHAR(40)
  pEmisorCiudad                CHAR(20)
  pEmisorEdo                   CHAR(40)
  pEmisorCodPais               CHAR(3)
  pEmisorCodMoneda             CHAR(3)
  pEmisorCp                    CHAR(8)
  pEmisorCalle                 CHAR(30)
  pEmisorTel                   CHAR(15)
  pBenefNameType               CHAR(1)
  pBenefNombre1                CHAR(40)
  pBenefNombre2                CHAR(40)
  pBenefApaterno               CHAR(40)
  pBenefAmaterno               CHAR(40)
  pBenefCiudad                 CHAR(20)
  pBenefEdo                    CHAR(40)
  pBenefCodPais                CHAR(3)
  pBenefCodMoneda              CHAR(3)
  pBenefCp                     CHAR(8)
  pBenefCalle                  CHAR(30)
  pBenefTelPart                CHAR(15)
  pBenefTelCel                 CHAR(10)
  pMontoTotalOrigen            CHAR(10)
  pMontoToTDestino             CHAR(10)
  pMontoOrigen                 CHAR(10)
  pMontoCargos                 CHAR(10)
  pCdOrigenPago                CHAR(30)
  pTipoCambio                  CHAR(10)
  pFechaAltaRemesa             CHAR(8)
  pHoraAltaRemesa              CHAR(16)
  pMoneyTransKey               CHAR(10)
  pEstatusRemesa               CHAR(4)
  pNewMtcn                     CHAR(16)
  pFusionStatus                CHAR(4)
  pNoPaginas                   CHAR(2)
  pPaginaActual                CHAR(2)
  pNumCoincidencias            CHAR(2)
  pForeignRsSystemIdRp         CHAR(11)
  pForeignRsRefNumRp           CHAR(16)
  pForeingRsCantIdRp           CHAR(11)
  pDescError                   CHAR(250)
  pPartnerIdErr                CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMarca` | `CHAR(2)` | — | — |
| `pForeignRsRefNumRq` | `CHAR(16)` | — | — |
| `pMtcn` | `CHAR(10)` | — | — |
| `pFechaHoraRq` | `CHAR(25)` | — | — |
| `pRetCode` | `CHAR(5)` | — | — |
| `pEmisorNameType` | `CHAR(1)` | — | — |
| `pEmisorNombre1` | `CHAR(40)` | — | — |
| `pEmisorNombre2` | `CHAR(40)` | — | — |
| `pEmisorApPaterno` | `CHAR(40)` | — | — |
| `pEmisorApMaterno` | `CHAR(40)` | — | — |
| `pEmisorCiudad` | `CHAR(20)` | — | — |
| `pEmisorEdo` | `CHAR(40)` | — | — |
| `pEmisorCodPais` | `CHAR(3)` | — | — |
| `pEmisorCodMoneda` | `CHAR(3)` | — | — |
| `pEmisorCp` | `CHAR(8)` | — | — |
| `pEmisorCalle` | `CHAR(30)` | — | — |
| `pEmisorTel` | `CHAR(15)` | — | — |
| `pBenefNameType` | `CHAR(1)` | — | — |
| `pBenefNombre1` | `CHAR(40)` | — | — |
| `pBenefNombre2` | `CHAR(40)` | — | — |
| `pBenefApaterno` | `CHAR(40)` | — | — |
| `pBenefAmaterno` | `CHAR(40)` | — | — |
| `pBenefCiudad` | `CHAR(20)` | — | — |
| `pBenefEdo` | `CHAR(40)` | — | — |
| `pBenefCodPais` | `CHAR(3)` | — | — |
| `pBenefCodMoneda` | `CHAR(3)` | — | — |
| `pBenefCp` | `CHAR(8)` | — | — |
| `pBenefCalle` | `CHAR(30)` | — | — |
| `pBenefTelPart` | `CHAR(15)` | — | — |
| `pBenefTelCel` | `CHAR(10)` | — | — |
| `pMontoTotalOrigen` | `CHAR(10)` | — | — |
| `pMontoToTDestino` | `CHAR(10)` | — | — |
| `pMontoOrigen` | `CHAR(10)` | — | — |
| `pMontoCargos` | `CHAR(10)` | — | — |
| `pCdOrigenPago` | `CHAR(30)` | — | — |
| `pTipoCambio` | `CHAR(10)` | — | — |
| `pFechaAltaRemesa` | `CHAR(8)` | — | — |
| `pHoraAltaRemesa` | `CHAR(16)` | — | — |
| `pMoneyTransKey` | `CHAR(10)` | — | — |
| `pEstatusRemesa` | `CHAR(4)` | — | — |
| `pNewMtcn` | `CHAR(16)` | — | — |
| `pFusionStatus` | `CHAR(4)` | — | — |
| `pNoPaginas` | `CHAR(2)` | — | — |
| `pPaginaActual` | `CHAR(2)` | — | — |
| `pNumCoincidencias` | `CHAR(2)` | — | — |
| `pForeignRsSystemIdRp` | `CHAR(11)` | — | — |
| `pForeignRsRefNumRp` | `CHAR(16)` | — | — |
| `pForeingRsCantIdRp` | `CHAR(11)` | — | — |
| `pDescError` | `CHAR(250)` | — | — |
| `pPartnerIdErr` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L57 |
| `iSqlErr` | `INTEGER` | L58 |
| `cCodRetSp` | `CHAR(3)` | L59 |
| `iCodRetSp` | `INTEGER` | L60 |
| `iNoRegistros` | `INTEGER` | L61 |
| `cEmpresa` | `CHAR(3)` | L62 |
| `cError_Desc` | `CHAR(30)` | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L90 |
| `sp_sac_wu_guardarespuesta_search_web` | `bdisac` | ⚠️ sí | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L85 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdisac:sp_sac_wu_guardarespuesta_search_` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `respuesta` | ENTIDAD | respuesta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `wu` | ENTIDAD | Western Union — servicio de remesas/transferencias internaci | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_rep_prod_genarchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rep_prod_genarchivo.sql` |
| **LOC (1er CREATE)** | 233 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte producto y archivo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rep_prod_upgrade2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rep_prod_genarchivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipo                        CHAR(1)
  pStatus                      CHAR(1)
  pArchivo                     CHAR(50)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pArchivo` | `CHAR(50)` | `archivo`=archivo | ✅ CÓDIGO |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `cRutaGral` | `CHAR(150)` | L13 |
| `cNombreArchivo` | `CHAR(100)` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `ven_transacc` | `SMALLINT` | L16 |
| `cNombreEmbozado` | `CHAR(100)` | L18 |
| `cNumCredito` | `CHAR(20)` | L19 |
| `cTipoTarjeta` | `CHAR(10)` | L20 |
| `cMiembro` | `CHAR(2)` | L21 |
| `dFecha` | `DATE` | L22 |
| `cResultado` | `CHAR(15)` | L23 |
| `cDescripcion` | `CHAR(100)` | L24 |
| `cNumTarjeta` | `CHAR(20)` | L25 |
| `cMarcaje` | `CHAR(3)` | L26 |
| `cSolPlastico` | `CHAR(2)` | L27 |
| `cMensajeError` | `CHAR(100)` | L28 |
| `cTipoArchivo` | `CHAR(1)` | L29 |
| `cFechaInicial` | `CHAR(10)` | L30 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cred_cambioproducto` | `bdicnweb` | no | SELECT | L102 |
| `sw_cred_cambioproducto` | `bdicnweb` | no | DELETE | L102 |
| `sw_cred_cambioproducto` | `bdicnweb` | no | INSERT | L121 |
| `systables` | `bdicnweb` | no | SELECT | L136 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L96 |
| `sp_rep_prod_upgrade2` | `bdicred` | ⚠️ sí | L108 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L90 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pTipo = '' OR` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L113 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_rep_prod_upgrade2";` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00973';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L138 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT numero_credito, num_tarjeta, tipo_tarjeta, nombre_embozado,` |  |
| L146 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L177 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L182 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L191 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L200 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rep_prod_genarchivotdcoro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rep_prod_genarchivotdcoro.sql` |
| **LOC (1er CREATE)** | 295 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte producto, archivo, tarjeta de crédito y Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Clásica < Oro < Platino; path de upgrade desde crédito Grupo Coppel" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_rep_prod_upgradeoro` |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_rep_prod_genarchivotdcoro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipo                        CHAR(1)
  pStatus                      CHAR(1)
  pArchivo                     CHAR(50)
  pRutaDescarga                CHAR(100)
  pTipoR                       CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pArchivo` | `CHAR(50)` | `archivo`=archivo | ✅ CÓDIGO |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pTipoR` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `cRutaGral` | `CHAR(150)` | L13 |
| `cNombreArchivo` | `CHAR(100)` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `ven_transacc` | `SMALLINT` | L16 |
| `iProcesados` | `INTEGER` | L17 |
| `cNombreEmbozado` | `CHAR(100)` | L18 |
| `cNumCredito` | `CHAR(20)` | L19 |
| `cTipoTarjeta` | `CHAR(10)` | L20 |
| `cMiembro` | `CHAR(2)` | L21 |
| `dFecha` | `DATE` | L22 |
| `cResultado` | `CHAR(15)` | L23 |
| `cDescripcion` | `CHAR(100)` | L24 |
| `cNumTarjeta` | `CHAR(20)` | L25 |
| `cMarcaje` | `CHAR(3)` | L26 |
| `cSolPlastico` | `CHAR(2)` | L27 |
| `cMensajeError` | `CHAR(100)` | L28 |
| `cTipoArchivo` | `CHAR(1)` | L29 |
| *…9 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cred_cambioproducto` | `bdicnweb` | no | SELECT | L117 |
| `sw_cred_cambioproducto` | `bdicnweb` | no | DELETE | L117 |
| `sw_cp_statusgenrepstdcoro2` | `bdicnweb` | no | SELECT | L118 |
| `sw_cp_statusgenrepstdcoro2` | `bdicnweb` | no | DELETE | L118 |
| `sw_cp_statusgenrepstdcoro2` | `bdicnweb` | no | INSERT | L122 |
| `sw_cred_cambioproducto` | `bdicnweb` | no | INSERT | L147 |
| `systables` | `bdicnweb` | no | SELECT | L167 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L106 |
| `sp_rep_prod_upgradeoro` | `bdicred` | ⚠️ sí | L130 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L97 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL OR pTipo = '' OR` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L135 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP:bdicred:sp_rep_prod_upgrade2";` |  |
| L137 | CÓDIGO_RETORNO | `LET cCodRet = '00973';` |  |
| L145 | FÓRMULA | `LET iProcesados = iProcesados+1;` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L170 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT ''''\|\|numero_credito,''''\|\| num_tarjeta, tipo_tarjeta, ` |  |
| L172 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT  numero_credito,  num_tarjeta, tipo_tarjeta, nombre_embozad` |  |
| L188 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L222 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L227 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L236 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L245 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L262 | FÓRMULA | `LET iLineaError_Rep = iNumRegistros-iProcesados;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `prod` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oro` | ENTIDAD | Tier medio de la Tarjeta de Crédito BanCoppel — jerarquía Cl | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rep_prod_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rep_prod_totales.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte producto (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_rep_prod_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cDescMensaje` | `CHAR(100)` | L11 |
| `iExitoso` | `INTEGER` | L12 |
| `iNoExitoso` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cred_cambioproducto` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rep_sac_reportediario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rep_sac_reportediario.sql` |
| **LOC (1er CREATE)** | 217 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "reporte Servicios de Atención al Cliente — subsistema de atención en sucursal y reporte (diario)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rep_sac_reportediario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRutaDescarga                CHAR(100)
  pTipoR                       CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pTipoR` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `cRutaGral` | `CHAR(150)` | L13 |
| `cNombreArchivo` | `CHAR(100)` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `ven_transacc` | `SMALLINT` | L16 |
| `iProcesados` | `INTEGER` | L17 |
| `iReg` | `INTEGER` | L18 |
| `iRec` | `INTEGER` | L19 |
| `iNumRegistros` | `INTEGER` | L20 |
| `dFechaHoy` | `DATE` | L21 |
| `cFechaHoraArchivo` | `CHAR(15)` | L22 |
| `iLineaError_Rep` | `INTEGER` | L23 |
| `cBanDetError` | `CHAR(1)` | L24 |
| `cFecha1` | `CHAR(10)` | L25 |
| `cFecha2` | `CHAR(10)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_reportediario_seg` | `bdisac` | ⚠️ sí | SELECT | L89 |
| `systables` | `bdicnweb` | no | SELECT | L101 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L79 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L73 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL  OR pRutaDescarg` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L97 | FÓRMULA | `LET cFecha1= LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|YEAR(pF` |  |
| L98 | FÓRMULA | `LET cFecha2= LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|YEAR(pFecha` |  |
| L103 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT  LPAD(DAY(fecha_proceso),2,0)\|\|'/'\|\|LPAD(MONTH(fecha_pr` | 🔴 MONEY/aritmética financiera |
| L122 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L161 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L166 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L175 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L184 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `diario` | MODIF | diario | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_rep_sac_reportedomiciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rep_sac_reportedomiciliacion.sql` |
| **LOC (1er CREATE)** | 259 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "reporte Servicios de Atención al Cliente — subsistema de atención en sucursal, reporte y domiciliación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rep_sac_reportedomiciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pSucursal                    CHAR(4)
  pRutaDescarga                CHAR(100)
  pTipoR                       CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pTipoR` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `cRutaGral` | `CHAR(150)` | L13 |
| `cNombreArchivo` | `CHAR(100)` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `ven_transacc` | `SMALLINT` | L16 |
| `iProcesados` | `INTEGER` | L17 |
| `iReg` | `INTEGER` | L18 |
| `iRec` | `INTEGER` | L19 |
| `iNumRegistros` | `INTEGER` | L20 |
| `dFechaHoy` | `DATE` | L21 |
| `cFechaHoraArchivo` | `CHAR(15)` | L22 |
| `iLineaError_Rep` | `INTEGER` | L23 |
| `cBanDetError` | `CHAR(1)` | L24 |
| `cFecha1` | `CHAR(10)` | L25 |
| `cFecha2` | `CHAR(10)` | L26 |
| `cSuc` | `CHAR(80)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sac_reportedomiciliaciongridrep` | `bdicnweb` | no | SELECT | L103 |
| `sw_sac_reportedomiciliaciongridrep` | `bdicnweb` | no | DELETE | L103 |
| `sw_sac_reportedomiciliaciongridrep` | `bdicnweb` | no | INSERT | L107 |
| `sac_reportediariodomi_seg` | `bdisac` | ⚠️ sí | SELECT | L113 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L131 |
| `systables` | `bdicnweb` | no | SELECT | L139 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L79 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL  OR pRutaDescarg` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L133 | FÓRMULA | `LET cFecha1= LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|YEAR(pF` |  |
| L134 | FÓRMULA | `LET cFecha2= LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|YEAR(pFecha` |  |
| L139 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT ' ',' ',' ',' ',' ',' ','REPORTE DOMICILIACION CLUB DE PROTE` |  |
| L141 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT ' ',' ',' ',' ',' ',' ','REPORTE DOMICILIACION CLUB DE PROTE` |  |
| L145 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT  LPAD(DAY(fecha_pago),2,0)\|\|'/'\|\|LPAD(MONTH(fecha_pago)` | 🔴 MONEY/aritmética financiera |
| L163 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L201 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L206 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L215 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L224 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `domiciliacion` | ENTIDAD | domiciliación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_rep_sac_reporteventanilla`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_rep_sac_reporteventanilla.sql` |
| **LOC (1er CREATE)** | 263 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "reporte Servicios de Atención al Cliente — subsistema de atención en sucursal y reporte" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_rep_sac_reporteventanilla(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pSucursal                    CHAR(4)
  pRutaDescarga                CHAR(100)
  pTipoR                       CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pTipoR` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |
| `cCmd1` | `CHAR(2000)` | L11 |
| `cSql` | `CHAR(2500)` | L12 |
| `cRutaGral` | `CHAR(150)` | L13 |
| `cNombreArchivo` | `CHAR(100)` | L14 |
| `bInTransaction` | `BOOLEAN` | L15 |
| `ven_transacc` | `SMALLINT` | L16 |
| `iProcesados` | `INTEGER` | L17 |
| `iReg` | `INTEGER` | L18 |
| `iRec` | `INTEGER` | L19 |
| `iNumRegistros` | `INTEGER` | L20 |
| `dFechaHoy` | `DATE` | L21 |
| `cFechaHoraArchivo` | `CHAR(15)` | L22 |
| `iLineaError_Rep` | `INTEGER` | L23 |
| `cBanDetError` | `CHAR(1)` | L24 |
| `cFecha1` | `CHAR(10)` | L25 |
| `cFecha2` | `CHAR(10)` | L26 |
| `cSuc` | `CHAR(80)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sac_reporteventanillagridrep` | `bdicnweb` | no | SELECT | L99 |
| `sw_sac_reporteventanillagridrep` | `bdicnweb` | no | DELETE | L99 |
| `informix` | `bdicnweb` | no | INSERT | L103 |
| `sac_reportediariovent_seg` | `bdisac` | ⚠️ sí | SELECT | L114 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L132 |
| `systables` | `bdicnweb` | no | SELECT | L142 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL  OR pRutaDescarg` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L135 | FÓRMULA | `LET cFecha1= LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|YEAR(pF` |  |
| L136 | FÓRMULA | `LET cFecha2= LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|YEAR(pFecha` |  |
| L142 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT ' ',' ',' ',' ',' ',' ','REPORTE VENTANILLA CLUB DE PROTECCI` |  |
| L144 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"SELECT ' ',' ',' ',' ',' ',' ','REPORTE VENTANILLA CLUB DE PROTECCI` |  |
| L148 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT  LPAD(DAY(fecha_pago),2,0)\|\|'/'\|\|LPAD(MONTH(fecha_pago)` | 🔴 MONEY/aritmética financiera |
| L166 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L205 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L210 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L219 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L228 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ventanilla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ventanilla` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_repctasinactivasart61`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_repctasinactivasart61.sql` |
| **LOC (1er CREATE)** | 825 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte cuentas inactivas · Art. 61 LIC" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_registra_evento` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_repctasinactivasart61(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReporte                     CHAR(2)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(10)
  pTituloPlantilla             CHAR(60)
  pIdReporte                   CHAR(20)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReporte` | `CHAR(2)` | `rep`=reporte | 🔵 CONVENCIÓN |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(10)` | — | — |
| `pTituloPlantilla` | `CHAR(60)` | — | — |
| `pIdReporte` | `CHAR(20)` | `rep`=reporte | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `cCmd1` | `CHAR(3000)` | L7 |
| `cSql` | `CHAR(2500)` | L8 |
| `bInTransaction` | `BOOLEAN` | L9 |
| `ven_transacc` | `SMALLINT` | L10 |
| `iCont` | `INTEGER` | L11 |
| `sCommit` | `SMALLINT` | L12 |
| `cRutaInformix` | `CHAR(100)` | L13 |
| `cUsrBin` | `CHAR(100)` | L14 |
| `dFechaConsulta` | `DATE` | L15 |
| `dFechaMax` | `DATE` | L16 |
| `dFechaMin` | `DATE` | L17 |
| `cReporte` | `CHAR(100)` | L18 |
| `cRutaGral` | `CHAR(100)` | L19 |
| `iNumRegistros` | `INTEGER` | L20 |
| `cNombreReporte` | `CHAR(100)` | L21 |
| `cNombreReporteHist` | `CHAR(100)` | L22 |
| `dHoraHoy` | `DATETIME HOUR TO MINUTE` | L23 |
| `dFechaHoy` | `DATE` | L24 |
| `cFechaHoraArchivo` | `CHAR(15)` | L25 |
| `cEstatus` | `CHAR(1)` | L27 |
| `cDescEstatus` | `CHAR(30)` | L28 |
| `cNum_cuenta` | `CHAR(20)` | L29 |
| *…37 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L184 |
| `sc_mae_estatus` | `bdicheq` | ⚠️ sí | SELECT | L207 |
| `sc_ctasinformadas` | `bdicheq` | ⚠️ sí | SELECT | L210 |
| `tmp_infdas` | `bdicnweb` | no | SELECT | L224 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L234 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L236 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L240 |
| `sc_producto` | `bdicheq` | ⚠️ sí | SELECT | L243 |
| `sc_maenoc` | `bdicheq` | ⚠️ sí | SELECT | L244 |
| `sw_det_ctasinformadas` | `bdicnweb` | no | INSERT | L254 |
| `systables` | `bdicnweb` | no | SELECT | L300 |
| `tmp_ctas_concentra` | `bdicnweb` | no | SELECT | L356 |
| `tmp_ctas_concentra_fin` | `bdicnweb` | no | SELECT | L370 |
| `sw_det_ctasconcentradas` | `bdicnweb` | no | INSERT | L394 |
| `sw_det_ctasconcentradas` | `bdicnweb` | no | SELECT | L447 |
| `sc_cuentas_concentradas` | `bdicheq` | ⚠️ sí | SELECT | L478 |
| `sw_det_ctasdesconcentradas` | `bdicnweb` | no | INSERT | L519 |
| `sw_det_ctasdesconcentradas` | `bdicnweb` | no | SELECT | L575 |
| `sw_det_ctascanceladas` | `bdicnweb` | no | INSERT | L656 |
| `sw_det_ctascanceladas` | `bdicnweb` | no | SELECT | L715 |
| `sw_ctrlgenreportesart61` | `bdicnweb` | no | SELECT | L730 |
| `sw_ctrlgenreportesart61` | `bdicnweb` | no | DELETE | L738 |
| `sw_ctrlgenreportesart61` | `bdicnweb` | no | INSERT | L743 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L174 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L755 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L186 | FÓRMULA | `LET dFechaMax = LAST_DAY(dFechaConsulta - 1 UNITS MONTH);` |  |
| L187 | FÓRMULA | `LET dFechaMin = TO_DATE(1\|\|'/'\|\|MONTH(dFechaMax)\|\|'/'\|\|YEAR(dFechaMax),'%d/%m/%Y');` |  |
| L202 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L258 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L259 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L290 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L320 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L398 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L399 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L435 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L465 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L523 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L524 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L563 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L572 | FÓRMULA | `LET cCmd1 = "SELECT 'FECHA DE CONSULTA','NUMERO DE CUENTA','PRODUCTO','NUMERO DE CLIENTE','NOMBRE DE` | 🔴 MONEY/aritmética financiera |
| L593 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L660 | FÓRMULA | `LET iCont = iCont + 1;` |  |
| L661 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L703 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L786 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdimnsj:sp_registra_evento';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `ctasinactivas` | ENTIDAD | cuentas inactivas | 🔵 CONVENCIÓN | nombre_sp |
| `art61` | REG | Art. 61 LIC (cuentas inactivas cuyos saldos, tras años sin m | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_repctasinactivasart61_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_repctasinactivasart61_totales.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 353 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte cuentas inactivas (totales) · Art. 61 LIC" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_repctasinactivasart61_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReporte                     CHAR(2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReporte` | `CHAR(2)` | `rep`=reporte | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `dFechaConsulta` | `DATE` | L7 |
| `dFechaMax` | `DATE` | L8 |
| `dFechaMin` | `DATE` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |
| `cEstatus` | `CHAR(1)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L47 |
| `sc_ctasinformadas` | `bdicheq` | ⚠️ sí | SELECT | L59 |
| `sc_cuentas_concentradas` | `bdicheq` | ⚠️ sí | SELECT | L66 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | FÓRMULA | `LET dFechaMax = LAST_DAY(dFechaConsulta - 1 UNITS MONTH);` |  |
| L50 | FÓRMULA | `LET dFechaMin = TO_DATE(1\|\|'/'\|\|MONTH(dFechaMax)\|\|'/'\|\|YEAR(dFechaMax),'%d/%m/%Y');` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `ctasinactivas` | ENTIDAD | cuentas inactivas | 🔵 CONVENCIÓN | nombre_sp |
| `art61` | REG | Art. 61 LIC (cuentas inactivas cuyos saldos, tras años sin m | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_reporte_cantidades_claves_retiro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporte_cantidades_claves_retiro.sql` |
| **LOC (1er CREATE)** | 131 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 63 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, cantidad, clave y retiro" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_bitacora` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporte_cantidades_claves_retiro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcionCanal                 CHAR(1)
  pFecha_inicial               DATE
  pFecha_final                 DATE
  pStatus                      CHAR(1)
  pCliente                     CHAR(20)
  pCuenta                      CHAR(20)
  pTarjeta                     CHAR(20)
  pIp                          CHAR(20)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcionCanal` | `CHAR(1)` | — | — |
| `pFecha_inicial` | `DATE` | — | — |
| `pFecha_final` | `DATE` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pTarjeta` | `CHAR(20)` | — | — |
| `pIp` | `CHAR(20)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cCodRetSp` | `CHAR(6)` | L13 |
| `iCodRetSp` | `INTEGER` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `iRegistros` | `INTEGER` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |
| `cCanalInicial` | `CHAR(10)` | L18 |
| `cCanalFinal` | `CHAR(10)` | L19 |
| `iMonto` | `MONEY` | L20 |
| `cAccion` | `CHAR(50)` | L21 |
| `cEntidad` | `CHAR(100)` | L22 |
| `cDescCanalInicial` | `CHAR(50)` | L23 |
| `cDescCanalFinal` | `CHAR(50)` | L24 |
| `mMonto` | `MONEY(16,2)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `claves_retiro` | `bdirst` | ⚠️ sí | SELECT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L62 |
| `sp_bitacora` | `bdicnweb` | no | L67 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL OR pOpcionCanal =` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L88 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L104 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cantidad` | ENTIDAD | cantidad | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reporte_diario_devoluciones_deposito`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporte_diario_devoluciones_deposito.sql` |
| **LOC (1er CREATE)** | 277 |
| **Callgraph** | ✅ fan_in=0 / fan_out=43 |
| **Deps concatenadas** | 59 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "devuelve reporte, depósito y OS — Originación de Solicitudes (diario)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 9 llamada(s): `cons_dev_coppel2`, `cons_dev_coppel2_totales`, `sp_cce_consultar_chequesdev_devcoppel2` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporte_diario_devoluciones_deposito(
  pBandera                     CHAR(2)
  pFechaDevo                   DATE
  pCuenta1                     CHAR(20)
  pCuenta2                     CHAR(20)
  pTranDev                     CHAR(4)
  pTranCom                     CHAR(4)
  pTranIva                     CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pFecha                       DATE
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodError                    CHAR(3)
) RETURNING char(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pFechaDevo` | `DATE` | — | — |
| `pCuenta1` | `CHAR(20)` | — | — |
| `pCuenta2` | `CHAR(20)` | — | — |
| `pTranDev` | `CHAR(4)` | — | — |
| `pTranCom` | `CHAR(4)` | — | — |
| `pTranIva` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |
| `pFecha` | `DATE` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodError` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_codret` | `char(5)` | L42 |
| `v_nom_sucursal` | `char(45)` | L43 |
| `v_banco` | `char(4)` | L44 |
| `v_numcheque` | `char(7)` | L45 |
| `v_importe` | `decimal(16,2)` | L46 |
| `v_com` | `decimal(8,2)` | L47 |
| `v_iva` | `decimal(8,2)` | L48 |
| `v_folio` | `char(16)` | L49 |
| `v_motivodev` | `char(45)` | L50 |
| `iSqlErr` | `INT` | L51 |
| `isam_err` | `INT` | L52 |
| `v_empresa` | `CHAR(3)` | L53 |
| `v_noregistros` | `INTEGER` | L54 |
| `v_numcuenta` | `CHAR(20)` | L55 |
| `v_monto` | `DECIMAL(16,2)` | L56 |
| `v_cuentadeposito` | `CHAR(20)` | L57 |
| `v_codretdev` | `CHAR(5)` | L58 |
| `v_desc_motivo` | `CHAR(35)` | L59 |
| `v_sucursal` | `CHAR(4)` | L60 |
| `v_desc_error` | `CHAR(50)` | L61 |
| `v_transacc_com` | `CHAR(4)` | L62 |
| `v_transacc_iva` | `CHAR(4)` | L63 |
| `v_transacc_comision` | `CHAR(4)` | L64 |
| `v_cta_col` | `CHAR(20)` | L65 |
| `v_nom_banco` | `CHAR(45)` | L66 |
| *…1 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `cons_dev_coppel2` | `bditef` | ⚠️ sí | L183 |
| `cons_dev_coppel2_totales` | `bditef` | ⚠️ sí | L195 |
| `sp_cce_consultar_chequesdev_devcoppel2` | `bditef` | ⚠️ sí | L207 |
| `bditef` | `bdicnweb` | no | L215 |
| `sp_ope_consdescripcionerror` | `bdicnweb` | no | L221 |
| `sp_ope_constdevolucion` | `bdicnweb` | no | L228 |
| `sp_ope_constransacioneschqs` | `bdicnweb` | no | L240 |
| `sp_ope_consultactascoppelcolateral` | `bdicnweb` | no | L247 |
| `sp_ope_genreportediariodev` | `bdicnweb` | no | L255 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L119 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L125 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L131 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L137 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L144 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L150 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L156 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L162 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L168 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L174 | CÓDIGO_RETORNO | `LET v_codret = '00003';` |  |
| L187 | CÓDIGO_RETORNO | `LET v_codRet = '00000';` |  |
| L199 | CÓDIGO_RETORNO | `LET v_codRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `diario` | MODIF | diario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `devolucion` | ACCION | devuelve | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ito` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_`, `?ito` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reporte_resultado_aplicacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporte_resultado_aplicacion.sql` |
| **LOC (1er CREATE)** | 246 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "aplica reporte y resultado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 7 llamada(s): `sp_cce_consultar_chequesdev_consdev2`, `sp_cce_consultar_chequesdev_consdev2_totales`, `sp_ope_consfechahabilanterior` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporte_resultado_aplicacion(
  pBandera                     CHAR(2)
  pFecha                       DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) 		AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pFecha` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L40 |
| `iIsamErr` | `INTEGER` | L41 |
| `cErrorInfo` | `CHAR(80)` | L42 |
| `cCodRet` | `CHAR(6)` | L43 |
| `cBanco` | `CHAR(3)` | L44 |
| `cNumCuenta` | `CHAR(20)` | L45 |
| `cNumCheque` | `CHAR(7)` | L46 |
| `dMonto` | `DECIMAL(16,2)` | L47 |
| `cCta_Deposito` | `CHAR(20)` | L48 |
| `cCodigoRetDev` | `CHAR(5)` | L49 |
| `cMotivo` | `CHAR(2)` | L50 |
| `cDescMotivo` | `CHAR(35)` | L51 |
| `cEmpresa` | `CHAR(3)` | L52 |
| `cNo_registros` | `INTEGER` | L53 |
| `cFecha_habil_ant` | `DATE` | L54 |
| `cCodRetDev` | `CHAR(5)` | L55 |
| `cDescBanco` | `CHAR(40)` | L56 |
| `cDesRetDev` | `CHAR(50)` | L57 |
| `cStatus` | `CHAR(5)` | L58 |
| `cClaveBanco` | `CHAR(3)` | L59 |
| `cNumCliente` | `CHAR(20)` | L60 |
| `cRazonSocial` | `CHAR(60)` | L61 |
| `cSegundoNombre` | `CHAR(26)` | L62 |
| `cApePaterno` | `CHAR(26)` | L63 |
| `cApeMaterno` | `CHAR(26)` | L64 |
| *…7 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cce_consultar_chequesdev_consdev2` | `bditef` | ⚠️ sí | L186 |
| `sp_cce_consultar_chequesdev_consdev2_totales` | `bditef` | ⚠️ sí | L193 |
| `sp_ope_consfechahabilanterior` | `bdicnweb` | no | L199 |
| `sp_ope_consultamovaplicacion` | `bdicnweb` | no | L206 |
| `sp_ope_consultamovaplicacion_totales` | `bdicnweb` | no | L213 |
| `sp_ope_genreporteaplicaciondev` | `bdicnweb` | no | L220 |
| `sp_ope_genreportedevpresentado` | `bdicnweb` | no | L230 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L156 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L170 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L177 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `resultado` | ENTIDAD | resultado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aplica` | ACCION | aplica / ejecuta | 🔵 CONVENCIÓN | nombre_sp |
| `?cion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reporte_revision_imagenes_cheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporte_revision_imagenes_cheques.sql` |
| **LOC (1er CREATE)** | 158 |
| **Callgraph** | ✅ fan_in=0 / fan_out=34 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, revisión, imágenes y cheques" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_ope_consultausuariosaut`, `sp_ope_genreporteimagenchqs` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporte_revision_imagenes_cheques(
  pBandera                     CHAR(2)
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumEjecut                   INTEGER
  pTipoReporte                 INTEGER
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(2)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumEjecut` | `INTEGER` | — | — |
| `pTipoReporte` | `INTEGER` | `reporte`=reporte | ✅ CÓDIGO |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L32 |
| `iSqlErr` | `INTEGER` | L33 |
| `cCodRetSp` | `CHAR(5)` | L34 |
| `iCodRetSp` | `INTEGER` | L35 |
| `iIdEjecutivo` | `INTEGER` | L36 |
| `cNombreEjecutivo` | `CHAR (100)` | L37 |
| `iNoRegistros` | `INTEGER` | L38 |
| `iNumEjecutivo` | `INTEGER` | L41 |
| `cCheque` | `CHAR(7)` | L42 |
| `dTiempoInicio` | `DATETIME HOUR TO SECOND` | L43 |
| `dTiempoFinal` | `DATETIME HOUR TO SECOND` | L44 |
| `dIndicadorRevisado` | `CHAR(1)` | L45 |
| `dTiempoTotal` | `DATETIME HOUR TO SECOND` | L46 |
| `dFechaRevision` | `DATE` | L47 |
| `iRevisionTotal` | `INTEGER` | L48 |
| `iNoRevisionTotal` | `INTEGER` | L49 |
| `iRegistros` | `INTEGER` | L50 |
| `iRecuperacion` | `INTEGER` | L51 |
| `cNoRevisados` | `DATETIME HOUR TO SECOND` | L52 |
| `cRevisados` | `DATETIME HOUR TO SECOND` | L53 |
| `iTotalCheques` | `INTEGER` | L54 |
| `iTotalOperadores` | `INTEGER` | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_ope_consultausuariosaut` | `bdicnweb` | no | L123 |
| `sp_ope_genreporteimagenchqs` | `bdicnweb` | no | L136 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `revision` | ENTIDAD | revisión | 🔵 CONVENCIÓN | nombre_sp |
| `imagenes` | ENTIDAD | imágenes / documentos digitales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_reportebloqueoctascap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportebloqueoctascap.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloquea cuenta reporte, cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportebloqueoctascap(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iExiste` | `INT` | L7 |
| `cCmd1` | `CHAR(1500)` | L8 |
| `cCmd2` | `CHAR(1500)` | L9 |
| `cCmd3` | `CHAR(1500)` | L10 |
| `cCmd4` | `CHAR(1500)` | L11 |
| `cUser` | `CHAR(8)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_histbloq` | `bdicheq` | ⚠️ sí | SELECT | L44 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L65 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L85 | FÓRMULA | `LET cCmd3 = "lote, numcte, nombre, cuenta, nvl(trim(to_char(sdo_actual, '#,###,###,###,##&.&&')), ''` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bloqueo` | ACCION | bloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_reportebloqueoctasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportebloqueoctasmasivocre.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 395 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloquea cuenta reporte, cuentas y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportebloqueoctasmasivocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iExiste` | `INT` | L8 |
| `cCmd1` | `CHAR(1500)` | L9 |
| `cCmd2` | `CHAR(1500)` | L10 |
| `cCmd3` | `CHAR(1500)` | L11 |
| `cCmd4` | `CHAR(1500)` | L12 |
| `cUser` | `CHAR(8)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L58 |
| `sd_maesdos` | `bdicred` | ⚠️ sí | SELECT | L92 |
| `sd_maesdoscrd` | `bdicred` | ⚠️ sí | SELECT | L92 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicnweb` | `bdicnweb` | no | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L70 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L92 | FÓRMULA | `LET cCmd1 = "select a.id_registro, a.lote, a.numcte, NVL(TRIM(TRIM(TRIM(c.nombre1)\|\|' '\|\|TRIM(c.` | 🔴 MONEY/aritmética financiera |
| L93 | FÓRMULA | `LET cCmd3 = "lote, trim(numcte), trim(nombre_cliente), trim(num_credito), trim(to_char(nvl(saldo_cue` |  |
| L100 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bloqueo` | ACCION | bloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_reportecambioinstruccionespagareinv`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportecambioinstruccionespagareinv.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "insertar reporte, cambio, cc — cuenta corriente y inv — inversión (especial)" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportecambioinstruccionespagareinv(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `iExiste` | `INT` | L6 |
| `cCmd1` | `CHAR(1500)` | L7 |
| `cCmd2` | `CHAR(1500)` | L8 |
| `cCmd3` | `CHAR(1500)` | L9 |
| `cCmd4` | `CHAR(1500)` | L10 |
| `cUser` | `CHAR(8)` | L11 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L60 | FÓRMULA | `LET cCmd1 = "select to_char(fecha_proceso, '%d/%m/%Y') as fecha_proceso, to_char(DATE('"\|\|pFechaIn` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?tru` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?agare` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inv` | ENTIDAD | inv — inversión (abreviación — calsdoinvcrec, cierrechqinvcr | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tru`, `?ion`, `?agare` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportecancelacionctasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportecancelacionctasmasivocre.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela reporte, cuentas y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtieneencabezadomasivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportecancelacionctasmasivocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iExiste` | `INT` | L8 |
| `cCmd1` | `CHAR(1500)` | L9 |
| `cCmd2` | `CHAR(1500)` | L10 |
| `cCmd3` | `CHAR(1500)` | L11 |
| `cCmd4` | `CHAR(1500)` | L12 |
| `cUser` | `CHAR(8)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L53 |
| `sd_indicador_cred` | `bdicred` | ⚠️ sí | SELECT | L85 |
| `sd_indicador_cred_crd` | `bdicred` | ⚠️ sí | SELECT | L85 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneencabezadomasivo` | `bdicnweb` | no | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L65 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L85 | FÓRMULA | `LET cCmd1 = "select a.id_registro, a.lote, a.numcte, nvl(trim(trim(trim(b.nombre1)\|\|' '\|\|trim(b.` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cancelacion` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_reportecargoindividualcredito`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportecargoindividualcredito.sql` |
| **LOC (1er CREATE)** | 262 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, cargo, identificador y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportecargoindividualcredito(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(6)
  pReversado                   CHAR(1)
  pFech_Ini                    CHAR(10)
  pFech_Fin                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pTipoReporte` | `CHAR(6)` | `reporte`=reporte | ✅ CÓDIGO |
| `pReversado` | `CHAR(1)` | — | — |
| `pFech_Ini` | `CHAR(10)` | — | — |
| `pFech_Fin` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRetSp` | `CHAR(6)` | L30 |
| `cCodRet` | `CHAR(5)` | L31 |
| `iSqlErr` | `INTEGER` | L32 |
| `cMensajeRet` | `CHAR(80)` | L33 |
| `cFolio_Grupo` | `CHAR(16)` | L34 |
| `mImporte` | `MONEY(14,2)` | L35 |
| `cNum_credito` | `CHAR(20)` | L36 |
| `cCodigo_cargo` | `CHAR(2)` | L37 |
| `cDesc_cargo` | `CHAR(100)` | L38 |
| `cFolio` | `CHAR(16)` | L39 |
| `dFecha_Aplic` | `DATE` | L40 |
| `mSaldoAntRev` | `MONEY(14,2)` | L41 |
| `mSaldoPostRev` | `MONEY(14,2)` | L42 |
| `cTransaccion` | `CHAR(4)` | L43 |
| `m_cap_vigente` | `MONEY(18,2)` | L44 |
| `m_cap_transitorio` | `MONEY(18,2)` | L45 |
| `m_cap_vencido` | `MONEY(18,2)` | L46 |
| `m_cap_vdo_noexigible` | `MONEY(18,2)` | L47 |
| `m_int_vigente` | `MONEY(18,2)` | L48 |
| `m_iva_intvigente` | `MONEY(18,2)` | L49 |
| `m_interes_vencido` | `MONEY(18,2)` | L50 |
| `m_iva_interesvencido` | `MONEY(18,2)` | L51 |
| `m_int_moratorio` | `MONEY(18,2)` | L52 |
| `m_iva_intmoratorio` | `MONEY(18,2)` | L53 |
| `m_capital_anterior` | `MONEY(18,2)` | L54 |
| *…11 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_fechas` | `bdicred` | ⚠️ sí | SELECT | L129 |
| `sd_bitacora_cargos` | `bdicred` | ⚠️ sí | SELECT | L175 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L219 |
| `sd_conceptoscargoscredito` | `bdicred` | ⚠️ sí | SELECT | L224 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet='00154';` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet='00155';` |  |
| L184 | FÓRMULA | `LET iReg = iReg + 1;` |  |
| L227 | FÓRMULA | `LET iReg = iReg + 1;` |  |
| L238 | CÓDIGO_RETORNO | `LET cCodRet='00151';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?indiv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ual` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `credito` | ENTIDAD | crédito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?indiv`, `?ual` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportecargosctasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportecargosctasmasivocre.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, cargo, cuentas y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtieneencabezadomasivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportecargosctasmasivocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iExiste` | `INT` | L8 |
| `cCmd1` | `CHAR(1500)` | L9 |
| `cCmd2` | `CHAR(1500)` | L10 |
| `cCmd3` | `CHAR(1500)` | L11 |
| `cCmd4` | `CHAR(1500)` | L12 |
| `cUser` | `CHAR(8)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneencabezadomasivo` | `bdicnweb` | no | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L64 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L83 | FÓRMULA | `LET cCmd1 = "select cm.id_registro, lote, (select folio_grupo from bdicnweb:sw_tr_totales_masivo whe` | 🔴 MONEY/aritmética financiera |
| L91 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportecargosreversoctasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportecargosreversoctasmasivocre.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reverso reporte, cargo, cuentas y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_obtieneencabezadomasivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportecargosreversoctasmasivocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iExiste` | `INT` | L8 |
| `cCmd1` | `CHAR(1600)` | L9 |
| `cCmd2` | `CHAR(1600)` | L10 |
| `cCmd3` | `CHAR(1600)` | L11 |
| `cCmd4` | `CHAR(1600)` | L12 |
| `cUser` | `CHAR(8)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L53 |
| `sd_bitacora_cargos` | `bdicred` | ⚠️ sí | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneencabezadomasivo` | `bdicnweb` | no | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L71 | FÓRMULA | `LET cCmd1 = "select a.id_registro, a.lote, b.folio_grupo, a.numcte, nvl(trim(trim(trim(c.nombre1)\|\` | 🔴 MONEY/aritmética financiera |
| L79 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportecargotarjetareversadomanualcre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportecargotarjetareversadomanualcre.sql` |
| **LOC (1er CREATE)** | 280 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Reversión — anula/revierte una operación reporte, cargo, tarjeta y crédito (manual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 5 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportecargotarjetareversadomanualcre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(6)
  pReversado                   CHAR(1)
  pFech_Ini                    CHAR(10)
  pFech_Fin                    CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(6)` | `reporte`=reporte | ✅ CÓDIGO |
| `pReversado` | `CHAR(1)` | `reversa`=Reversión — anula/revierte una operación (bdibei:sp_reversa_solicitudes_bei, sp_reversa_tokenasociados_bei) | 🔵 CONVENCIÓN |
| `pFech_Ini` | `CHAR(10)` | — | — |
| `pFech_Fin` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRetSp` | `CHAR(6)` | L31 |
| `cCodRet` | `CHAR(5)` | L32 |
| `iSqlErr` | `INTEGER` | L33 |
| `cMensajeRet` | `CHAR(80)` | L34 |
| `cFolio_Grupo` | `CHAR(16)` | L35 |
| `mImporte` | `MONEY(14,2)` | L36 |
| `cNum_credito` | `CHAR(20)` | L37 |
| `cCodigo_cargo` | `CHAR(2)` | L38 |
| `cDesc_cargo` | `CHAR(100)` | L39 |
| `cFolio` | `CHAR(16)` | L40 |
| `dFecha_Aplic` | `DATE` | L41 |
| `mSaldoAntRev` | `MONEY(14,2)` | L42 |
| `mSaldoPostRev` | `MONEY(14,2)` | L43 |
| `cTransaccion` | `CHAR(4)` | L44 |
| `m_cap_vigente` | `MONEY(18,2)` | L45 |
| `m_cap_transitorio` | `MONEY(18,2)` | L46 |
| `m_cap_vencido` | `MONEY(18,2)` | L47 |
| `m_cap_vdo_noexigible` | `MONEY(18,2)` | L48 |
| `m_int_vigente` | `MONEY(18,2)` | L49 |
| `m_iva_intvigente` | `MONEY(18,2)` | L50 |
| `m_interes_vencido` | `MONEY(18,2)` | L51 |
| `m_iva_interesvencido` | `MONEY(18,2)` | L52 |
| `m_int_moratorio` | `MONEY(18,2)` | L53 |
| `m_iva_intmoratorio` | `MONEY(18,2)` | L54 |
| `m_capital_anterior` | `MONEY(18,2)` | L55 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_fechas` | `bdicred` | ⚠️ sí | SELECT | L131 |
| `sd_bitacora_cargos` | `bdicred` | ⚠️ sí | SELECT | L177 |
| `sd_bitacorapagos` | `bdicred` | ⚠️ sí | SELECT | L187 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L234 |
| `sd_conceptoscargoscredito` | `bdicred` | ⚠️ sí | SELECT | L239 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L150 | CÓDIGO_RETORNO | `LET cCodRet='00154';` |  |
| L161 | CÓDIGO_RETORNO | `LET cCodRet='00155';` |  |
| L186 | FÓRMULA | `LET iReg = iReg + 1;` |  |
| L242 | FÓRMULA | `LET iReg = iReg + 1;` |  |
| L254 | CÓDIGO_RETORNO | `LET cCodRet='00151';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `tarjeta` | ENTIDAD | tarjeta | 🔵 CONVENCIÓN | nombre_sp |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | 🔵 CONVENCIÓN | nombre_sp |
| `?do` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manual` | MODIF | manual | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?do` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reporteconciliacionconveniosucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporteconciliacionconveniosucursal.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación reporte, convenio y sucursal" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_sacreporteconciliacionconveniosucursal` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporteconciliacionconveniosucursal(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pConvenio                    CHAR(5)
  pSucursal                    CHAR(4)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codigoretorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | `convenio`=convenio (nómina/empresarial) | 🔵 CONVENCIÓN |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | 🔵 CONVENCIÓN |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cIdSucursal` | `CHAR(5)` | L17 |
| `cNumPagos` | `INTEGER` | L18 |
| `cNomconvenio` | `CHAR(40)` | L19 |
| `mImportePago` | `MONEY(16,2)` | L20 |
| `mImporteComisionConvenio` | `MONEY(16,2)` | L21 |
| `mIvaComisionConvenio` | `MONEY(16,2)` | L22 |
| `mImporteComisionCte` | `MONEY(16,2)` | L23 |
| `mIvaComisionCte` | `MONEY(16,2)` | L24 |
| `iFlagConfirmacionCentral` | `INTEGER` | L25 |
| `iFlagConfirmacionSucursal` | `INTEGER` | L26 |
| `iRegistros` | `INTEGER` | L27 |
| `iNoRegs` | `INTEGER` | L28 |
| `iRecuperacion` | `INTEGER` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sacreporteconciliacionconveniosucursal` | `bdisac` | ⚠️ sí | L76 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodret = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodret = '00098';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L101 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L104 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L107 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_reporteconciliacionsac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporteconciliacionsac.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación reporte y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_reportebts_conciliacion`, `sp_reportewu_conciliacion` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporteconciliacionsac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pdtFechaIni                  DATE
  pdtFechaFin                  DATE
  pConvenio                    CHAR(5)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS  codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pdtFechaIni` | `DATE` | — | — |
| `pdtFechaFin` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `dDia` | `DATE` | L15 |
| `cFormaPago` | `CHAR(20)` | L16 |
| `mCargoPagoRemesa` | `MONEY(16,2)` | L17 |
| `mSaldoCuentaBTSWU` | `MONEY(16,2)` | L18 |
| `cFolioOperacion` | `CHAR(16)` | L19 |
| `cClaveEnvio` | `CHAR(11)` | L20 |
| `cSucursal` | `CHAR(4)` | L21 |
| `cCajero` | `CHAR(8)` | L22 |
| `cImportePag` | `CHAR(20)` | L23 |
| `iRegistros` | `INTEGER` | L24 |
| `iNoRegs` | `INTEGER` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |
| `iNumRows` | `INTEGER` | L27 |
| `cCategoria` | `CHAR(2)` | L28 |
| `cConvenio` | `CHAR(3)` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_reportebts_conciliacion` | `bdisac` | ⚠️ sí | L76 |
| `sp_reportewu_conciliacion` | `bdisac` | ⚠️ sí | L90 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET	cCodRet = '00000';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L80 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L82 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L87 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L95 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L97 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L102 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L106 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `conciliacion` | ACCION | conciliación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_reportecreditoscancelacion_nocancelacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportecreditoscancelacion_nocancelacion.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela reporte y créditos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_generareportecreditoscancynocanc` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportecreditoscancelacion_nocancelacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pMotivoCancelacion           CHAR(3)
  pTipoCancelacion             CHAR(1)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pMotivoCancelacion` | `CHAR(3)` | `cancelacion`=cancela · `cancelacion`=cancela | 🔵 CONVENCIÓN |
| `pTipoCancelacion` | `CHAR(1)` | `cancelacion`=cancela · `cancelacion`=cancela | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INTEGER` | L18 |
| `iRegistros` | `INTEGER` | L19 |
| `iRecuperacion` | `INTEGER` | L20 |
| `iNoRegs` | `INTEGER` | L21 |
| `cCodRetSp` | `CHAR(6)` | L22 |
| `cMensaje` | `CHAR(50)` | L23 |
| `dFechaHoy` | `DATE` | L24 |
| `cNumCredito` | `CHAR(20)` | L25 |
| `cResultado` | `CHAR(2)` | L26 |
| `cDescCancel` | `CHAR(45)` | L27 |
| `dFechaCancel` | `DATE` | L28 |
| `cEjecutivo` | `CHAR(8)` | L29 |
| `cNombreEjecutivo` | `CHAR(60)` | L30 |
| `cSupervisor` | `CHAR(8)` | L31 |
| `cNombreSupervisor` | `CHAR(60)` | L32 |
| `cCodTipoCancel` | `CHAR(1)` | L33 |
| `cDescTipoCancel` | `CHAR(15)` | L34 |
| `cSucursal` | `CHAR(4)` | L35 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_generareportecreditoscancynocanc` | `bdicred` | ⚠️ sí | L80 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L96 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L99 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L103 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `creditos` | ENTIDAD | créditos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `cancelacion` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?_no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cancelacion` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_no` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportectasconcentradas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportectasconcentradas.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte y cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportectasconcentradas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pArchDescarga                CHAR(500)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pArchDescarga` | `CHAR(500)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `INTEGER` | L10 |
| `cCmd1` | `CHAR(1600)` | L11 |
| `cCmd2` | `CHAR(1600)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_ctasinactinfor3anios3meses` | `bdicheq` | ⚠️ sí | SELECT | L55 |
| `sc_cuentas_concentradas_procesadas` | `bdicnweb` | no | SELECT | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet  = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L76 | FÓRMULA | `LET cCmd2 = "/usr/bin/echo 'SET ISOLATION TO DIRTY READ; UNLOAD TO "\|\|TRIM(pArchDescarga)\|\|" "\|` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?centradas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?centradas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportectasdesconcentradas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportectasdesconcentradas.sql` |
| **LOC (1er CREATE)** | 166 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, cuentas y [polisemia] Descripción" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportectasdesconcentradas(
  cID_USUARIOC                 CHAR(8)
  cID_FUNCIONC                 CHAR(10)
  pIfechaProceso               DATE
  pFfechaProceso               DATE
  pArchivoDescarga             CHAR(100)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `CHAR(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `pIfechaProceso` | `DATE` | — | — |
| `pFfechaProceso` | `DATE` | — | — |
| `pArchivoDescarga` | `CHAR(100)` | `desc`=[polisemia] Descripción (sp_desc_ret: devuelve descripción del código de retorno) | Descarga (sp_desc_archivos_cfdi/conc: descarga archivos CFDI y conciliación) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSql_err` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iExiste` | `INTEGER` | L12 |
| `cCmd` | `CHAR(355)` | L14 |
| `cCmd1` | `CHAR(355)` | L15 |
| `cCmd2` | `CHAR(355)` | L16 |
| `cCmd3` | `CHAR(355)` | L17 |
| `cCmd4` | `CHAR(355)` | L18 |
| `cCmd5` | `CHAR(355)` | L19 |
| `cCmd6` | `CHAR(355)` | L20 |
| `cCmd7` | `CHAR(355)` | L21 |
| `vFechaHoy` | `DATE` | L23 |
| `vCuenta` | `CHAR(20)` | L24 |
| `vStatusCta` | `CHAR(1)` | L25 |
| `vSucursal` | `CHAR(4)` | L26 |
| `vSdoActual` | `DECIMAL(18,2)` | L27 |
| `vSdoRetenido` | `DECIMAL(18,2)` | L28 |
| `vSdoCongelado` | `DECIMAL(18,2)` | L29 |
| `vSdoSobregirado` | `DECIMAL(18,2)` | L30 |
| `vSdoDispCuenta` | `DECIMAL(18,2)` | L31 |
| `vFechaUltimoDep` | `DATE` | L32 |
| `vFechaUltimoRet` | `DATE` | L33 |
| `vFechaAlta` | `DATE` | L34 |
| `vFechaCompara` | `DATE` | L35 |
| *…14 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L131 |
| `sc_tarjeta` | `bdicheq` | ⚠️ sí | SELECT | L134 |
| `sc_producto` | `bdicheq` | ⚠️ sí | SELECT | L145 |
| `sc_mae_estatus` | `bdicheq` | ⚠️ sí | SELECT | L147 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `?oncentradas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?oncentradas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportedesbloqueoctascap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportedesbloqueoctascap.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloquea cuenta reporte, cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportedesbloqueoctascap(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iExiste` | `INT` | L7 |
| `cCmd1` | `CHAR(1500)` | L8 |
| `cCmd2` | `CHAR(1500)` | L9 |
| `cCmd3` | `CHAR(1500)` | L10 |
| `cCmd4` | `CHAR(1500)` | L11 |
| `cUser` | `CHAR(8)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_histbloq` | `bdicheq` | ⚠️ sí | SELECT | L45 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L66 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L86 | FÓRMULA | `LET cCmd3 = "lote, trim(numcte), trim(nombre), trim(cuenta), nvl(trim(to_char(sdo_actual, '#,###,###` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `desbloqueo` | ACCION | desbloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_reportedesbloqueoctasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportedesbloqueoctasmasivocre.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 394 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloquea cuenta reporte, cuentas y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportedesbloqueoctasmasivocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iExiste` | `INT` | L8 |
| `cCmd1` | `CHAR(1500)` | L9 |
| `cCmd2` | `CHAR(1500)` | L10 |
| `cCmd3` | `CHAR(1500)` | L11 |
| `cCmd4` | `CHAR(1500)` | L12 |
| `cUser` | `CHAR(8)` | L13 |
| `dFechaInicio` | `DATE` | L14 |
| `dFechaFin` | `DATE` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L62 |
| `sd_maesdos` | `bdicred` | ⚠️ sí | SELECT | L96 |
| `sd_maesdoscrd` | `bdicred` | ⚠️ sí | SELECT | L96 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicnweb` | `bdicnweb` | no | L102 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L74 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L96 | FÓRMULA | `LET cCmd1 = "select a.id_registro, a.lote, a.numcte, NVL(TRIM(TRIM(TRIM(c.nombre1)\|\|' '\|\|TRIM(c.` | 🔴 MONEY/aritmética financiera |
| L97 | FÓRMULA | `LET cCmd3 = "lote, trim(numcte), trim(nombre_cliente), trim(num_credito), trim(to_char(nvl(saldo_cue` |  |
| L104 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `desbloqueo` | ACCION | desbloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_reportedetalletransucursalsac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportedetalletransucursalsac.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=28 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, detalle, [polisemia] Transferencia y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_sacreportedetalletransaccionsucursal` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportedetalletransucursalsac(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pConvenio                    CHAR(5)
  pSucursal                    CHAR(4)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codigoretorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `cCodRetSp` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `cUsuario` | `CHAR(16)` | L23 |
| `cFolioSuc` | `CHAR(16)` | L24 |
| `cNomConvenio` | `CHAR(40)` | L25 |
| `cReferencia1` | `CHAR(20)` | L26 |
| `cReferencia2` | `CHAR(20)` | L27 |
| `mImportePago` | `MONEY(16,2)` | L28 |
| `mImporteComisionConvenio` | `MONEY(16,2)` | L29 |
| `mIvaComisionConvenio` | `MONEY(16,2)` | L30 |
| `mImporteComisionCte` | `MONEY(16,2)` | L31 |
| `mIivaComisionCte` | `MONEY(16,2)` | L32 |
| `cFormaCago` | `CHAR(1)` | L33 |
| `cCuentaCargo` | `CHAR(12)` | L34 |
| `cRegion` | `CHAR(40)` | L35 |
| `iRegistros` | `INTEGER` | L36 |
| `iNoRegs` | `INTEGER` | L37 |
| `iRecuperacion` | `INTEGER` | L38 |
| `cNumConvenio` | `CHAR(3)` | L39 |
| `cNumCategoria` | `CHAR(2)` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L98 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sacreportedetalletransaccionsucursal` | `bdisac` | ⚠️ sí | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodret = '00003';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodret = '00098';` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L103 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L106 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `?ucursal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ucursal` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportediarioremesasac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportediarioremesasac.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Propósito inferido** | "reporte y remesas internacionales (diario)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_reportebts_diario`, `sp_reportewu_diario`, `sp_reporteapp_diario` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportediarioremesasac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPeriodo                     DATE
  pConvenio                    CHAR(5)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codigoretorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPeriodo` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `dDia` | `DATE` | L14 |
| `cNum_confirmacion` | `CHAR(16)` | L15 |
| `mImporte` | `MONEY(16,2)` | L16 |
| `cForma_pago` | `CHAR (20)` | L17 |
| `cFolio_op` | `CHAR (16)` | L18 |
| `cSucursal` | `CHAR (5)` | L19 |
| `cCajero` | `CHAR (8)` | L20 |
| `cNom_benef` | `CHAR (120)` | L21 |
| `iSqlerr` | `INTEGER` | L22 |
| `iRegistros` | `INTEGER` | L23 |
| `iNoRegs` | `INTEGER` | L24 |
| `iRecuperacion` | `INTEGER` | L25 |
| `iNumRows` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L74 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_reportebts_diario` | `bdisac` | ⚠️ sí | L80 |
| `sp_reportewu_diario` | `bdisac` | ⚠️ sí | L92 |
| `sp_reporteapp_diario` | `bdisac` | ⚠️ sí | L104 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L84 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L89 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L96 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L98 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L101 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L108 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L110 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L113 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L121 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `diario` | MODIF | diario | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | 🔵 CONVENCIÓN | nombre_sp |
| `?ac` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ac` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reporteestadocuentasac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporteestadocuentasac.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, estado y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_reportebts_edocta`, `sp_reportewu_edocta` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporteestadocuentasac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pConvenio                    CHAR(5)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cFecha` | `CHAR(10)` | L17 |
| `cSaldoInicial` | `CHAR(14)` | L18 |
| `cTotalAbonos` | `CHAR(10)` | L19 |
| `cMontoTotalAbonos` | `CHAR(14)` | L20 |
| `cTotalCargos` | `CHAR(10)` | L21 |
| `cMontoTotalCargos` | `CHAR(14)` | L22 |
| `cSaldoFinal` | `CHAR(14)` | L23 |
| `cCtaConcentradora` | `CHAR(20)` | L24 |
| `cCtaClabe` | `CHAR(20)` | L25 |
| `cCategoria` | `CHAR(2)` | L26 |
| `cConvenio` | `CHAR(3)` | L27 |
| `iRegistros` | `INTEGER` | L28 |
| `iNoRegs` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_reportebts_edocta` | `bdisac` | ⚠️ sí | L77 |
| `sp_reportewu_edocta` | `bdisac` | ⚠️ sí | L94 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L84 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L87 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L101 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L104 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estado` | ENTIDAD | estado (entidad federativa / estatus) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?ac` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ac` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reporteestadocuentasac_pba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporteestadocuentasac_pba.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, estado y cuentas (PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_reportebts_edocta`, `sp_reportewu_edocta` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporteestadocuentasac_pba(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pConvenio                    CHAR(5)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS codRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cFecha` | `CHAR(10)` | L17 |
| `cSaldoInicial` | `CHAR(14)` | L18 |
| `cTotalAbonos` | `CHAR(10)` | L19 |
| `cMontoTotalAbonos` | `CHAR(14)` | L20 |
| `cTotalCargos` | `CHAR(10)` | L21 |
| `cMontoTotalCargos` | `CHAR(14)` | L22 |
| `cSaldoFinal` | `CHAR(14)` | L23 |
| `cCtaConcentradora` | `CHAR(20)` | L24 |
| `cCtaClabe` | `CHAR(20)` | L25 |
| `cCategoria` | `CHAR(2)` | L26 |
| `cConvenio` | `CHAR(3)` | L27 |
| `iRegistros` | `INTEGER` | L28 |
| `iNoRegs` | `INTEGER` | L29 |
| `iRecuperacion` | `INTEGER` | L30 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_reportebts_edocta` | `bdisac` | ⚠️ sí | L77 |
| `sp_reportewu_edocta` | `bdisac` | ⚠️ sí | L94 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L82 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L84 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L87 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L101 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L104 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L112 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estado` | ENTIDAD | estado (entidad federativa / estatus) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?ac_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ac_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportefechaclaveblodescre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportefechaclaveblodescre.sql` |
| **LOC (1er CREATE)** | 137 |
| **Callgraph** | ✅ fan_in=0 / fan_out=42 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, fecha, clave y [polisemia] Descripción" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicred` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportefechaclaveblodescre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOpcion                      SMALLINT
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pClave                       INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOpcion` | `SMALLINT` | — | — |
| `pFechaInicio` | `DATE` | `fecha`=fecha | ✅ CÓDIGO |
| `pFechaFin` | `DATE` | `fecha`=fecha | ✅ CÓDIGO |
| `pClave` | `INTEGER` | `clave`=clave | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `cCodRetSp` | `CHAR(6)` | L19 |
| `iCodRet` | `INTEGER` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cEmpresa` | `CHAR(3)` | L22 |
| `cCuenta` | `CHAR(20)` | L23 |
| `cCliente` | `CHAR(9)` | L24 |
| `cNombre` | `CHAR(50)` | L25 |
| `cSucursal` | `CHAR(4)` | L26 |
| `cBloqueo` | `CHAR(30)` | L27 |
| `cCausa` | `CHAR(50)` | L28 |
| `cEstatus` | `CHAR(2)` | L29 |
| `dFechaBloq` | `DATE` | L30 |
| `dSaldoCapital` | `DECIMAL(18, 2)` | L31 |
| `cPersonaAplica` | `CHAR(60)` | L32 |
| `cAreaSolicita` | `CHAR(150)` | L33 |
| `cJustificacion` | `CHAR(150)` | L34 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L93 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L64 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pOpcion IS NULL THEN` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L75 | VALIDACIÓN_NULL | `IF pClave IS NULL THEN` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | VALIDACIÓN_NULL | `IF pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L98 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultaclave';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L113 | EXCEPCIÓN | `RAISE EXCEPTION iCodRet, 0, 'ERROR EN LA EJECUCION DEL SP sp_consultafecha';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clave` | ENTIDAD | clave | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?blo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `desc` | ENTIDAD | [polisemia] Descripción (sp_desc_ret: devuelve descripción d | 🔵 CONVENCIÓN | nombre_sp |
| `?re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?blo`, `?re` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportehistoricomovcapcre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportehistoricomovcapcre.sql` |
| **LOC (1er CREATE)** | 330 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, movimiento, Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 y crédito (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportehistoricomovcapcre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistemaCuenta               CHAR(2)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pCancelado                   CHAR(1)
  pNumCliente                  CHAR(20)
  pCccMayor                    CHAR(10)
  pCccSub                      CHAR(10)
  pCccSubsub                   CHAR(10)
  pCccSssub                    CHAR(10)
  pCccSsssub                   CHAR(10)
  pAccMayor                    CHAR(10)
  pAccSub                      CHAR(10)
  pAccSubsub                   CHAR(10)
  pAccSssub                    CHAR(10)
  pAccSsssub                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pCancelado` | `CHAR(1)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pCccMayor` | `CHAR(10)` | — | — |
| `pCccSub` | `CHAR(10)` | — | — |
| `pCccSubsub` | `CHAR(10)` | — | — |
| `pCccSssub` | `CHAR(10)` | — | — |
| `pCccSsssub` | `CHAR(10)` | — | — |
| `pAccMayor` | `CHAR(10)` | — | — |
| `pAccSub` | `CHAR(10)` | — | — |
| `pAccSubsub` | `CHAR(10)` | — | — |
| `pAccSssub` | `CHAR(10)` | — | — |
| `pAccSsssub` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L28 |
| `iSqlErr` | `INTEGER` | L29 |
| `cCuenta` | `CHAR(20)` | L30 |
| `cSucursal` | `CHAR(4)` | L31 |
| `cNumCliente` | `CHAR(20)` | L32 |
| `cProducto` | `CHAR(4)` | L33 |
| `mMontoTotal` | `MONEY(14,2)` | L34 |
| `cTransaccion` | `CHAR(4)` | L35 |
| `cDescripcion` | `CHAR(50)` | L36 |
| `cSeContabiliza` | `CHAR(1)` | L37 |
| `cCccMayor` | `CHAR(10)` | L38 |
| `cCccSub` | `CHAR(10)` | L39 |
| `cCccSubsub` | `CHAR(10)` | L40 |
| `cCccSssub` | `CHAR(10)` | L41 |
| `cCccSsssub` | `CHAR(10)` | L42 |
| `cAccMayor` | `CHAR(10)` | L43 |
| `cAccSub` | `CHAR(10)` | L44 |
| `cAccSubsub` | `CHAR(10)` | L45 |
| `cAccSssub` | `CHAR(10)` | L46 |
| `cAccSsssub` | `CHAR(10)` | L47 |
| `cAttrQry` | `CHAR(500)` | L48 |
| `cAttrAliasQry` | `CHAR(500)` | L49 |
| `cFromQry` | `CHAR(500)` | L50 |
| `cWhereQry` | `CHAR(500)` | L51 |
| `cAndQry` | `CHAR(500)` | L52 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L216 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L217 |
| `sd_param` | `bdicred` | ⚠️ sí | SELECT | L228 |
| `sd_movdia` | `bdicred` | ⚠️ sí | SELECT | L281 |
| `sd_movhis` | `bdicred` | ⚠️ sí | SELECT | L282 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L122 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L104 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSistemaCuenta = '' OR pFechaInicial IS NULL` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L133 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00109';` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L296 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_reportemantolineasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportemantolineasmasivocre.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, línea y crédito (anterior, masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdicnweb` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportemantolineasmasivocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iExiste` | `INT` | L8 |
| `cCmd1` | `CHAR(1500)` | L9 |
| `cCmd2` | `CHAR(1500)` | L10 |
| `cCmd3` | `CHAR(1500)` | L11 |
| `cCmd4` | `CHAR(1500)` | L12 |
| `cUser` | `CHAR(8)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicnweb` | `bdicnweb` | no | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L67 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L86 | FÓRMULA | `LET cCmd1 = "select a.id_registro, a.lote, nvl(a.numcte, '') as numcte, nvl(a.nombre_cliente, '') as` | 🔴 MONEY/aritmética financiera |
| L94 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?o` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `linea` | ENTIDAD | línea (de crédito) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?o`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportemensualcondonacionescre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportemensualcondonacionescre.sql` |
| **LOC (1er CREATE)** | 211 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reporte y crédito (mensual)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportemensualcondonacionescre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pArchDescarga                CHAR(150)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iExiste` | `INT` | L7 |
| `iPid` | `INTEGER` | L8 |
| `cTmpTable` | `CHAR(5000)` | L9 |
| `cCmd1` | `CHAR(6000)` | L10 |
| `cCmd2` | `CHAR(6000)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `iIdxSecReg` | `INTEGER` | L13 |
| `cSecuencia` | `CHAR(1)` | L14 |
| `cProducto` | `CHAR(45)` | L15 |
| `cNumCredito` | `CHAR(20)` | L16 |
| `cFechaMov` | `CHAR(10)` | L17 |
| `mInteresVencido` | `MONEY(18,2)` | L18 |
| `mIvaInteresVencido` | `MONEY(18,2)` | L19 |
| `mIvaInteresMoratorio` | `MONEY(18,2)` | L20 |
| `mInteresMoratorioBase` | `MONEY(18,2)` | L21 |
| `mInteresMoratorioCopete` | `MONEY(18,2)` | L22 |
| `mIvaInteresMoratorioBase` | `MONEY(18,2)` | L23 |
| `mIvaInteresMoratorioCopete` | `MONEY(18,2)` | L24 |
| `mImportePago` | `MONEY(18,2)` | L25 |
| `mCapitalVigente` | `MONEY(18,2)` | L26 |
| `cSecuenciaData` | `CHAR(1)` | L27 |
| `cProductoData` | `CHAR(45)` | L28 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_bitacorapagos` | `bdicred` | ⚠️ sí | SELECT | L104 |
| `sd_conceptospagomanual` | `bdicred` | ⚠️ sí | SELECT | L107 |
| `sd_conceptospagomanualcrd` | `bdicred` | ⚠️ sí | SELECT | L111 |
| `sw_tr_registrosreportepago` | `bdicnweb` | no | INSERT | L144 |
| `sw_tr_registrosreportepago` | `bdicnweb` | no | SELECT | L191 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_obtieneencabezadomasivo` | `bdicnweb` | no | L194 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L115 | FÓRMULA | `LET iNoRegistros = iNoRegistros + DBINFO('sqlca.sqlerrd2');` |  |
| L117 | FÓRMULA | `LET iIdxSecReg = iIdxSecReg + 1;` |  |
| L134 | FÓRMULA | `LET mInteresMoratorio = mInteresMoratorioBaseData + mInteresMoratorioCopeteData + mIvaInteresMorator` | 🔴 MONEY/aritmética financiera |
| L186 | FÓRMULA | `LET cCmd1 ="producto,cuenta,TO_CHAR(DATE(TO_DATE(fecha_ult_mov, '%m/%d/%Y')),'%d/%m/%Y') as fecha_mo` |  |
| L187 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"TRIM(NVL(TO_CHAR(mIvaIntVencImpAplicado, '- #,###,###,###,##&.&&'),` |  |
| L188 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"TRIM(NVL(TO_CHAR(mInteresMoratorio, '- #,###,###,###,##&.&&'), ''))` | 🔴 MONEY/aritmética financiera |
| L189 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|"TRIM(NVL(TO_CHAR(mImportePagoCondona, '- #,###,###,###,##&.&&'), ''` | 🔴 MONEY/aritmética financiera |
| L196 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mensual` | MODIF | mensual | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?donaciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?donaciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportemensualconveniosac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportemensualconveniosac.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, convenio y Servicios de Atención al Cliente — subsistema de atención en sucursal (mensual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_sacreportemensual` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportemensualconveniosac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(5)
  pPeriodo                     CHAR(6)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR (5) AS retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | `convenio`=convenio (nómina/empresarial) | 🔵 CONVENCIÓN |
| `pPeriodo` | `CHAR(6)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `cCodRetSP` | `CHAR(6)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cAniomes` | `CHAR(6)` | L13 |
| `dFecha` | `DATE` | L14 |
| `iNumOperaciones` | `INTEGER` | L15 |
| `mComision` | `MONEY (16,2)` | L16 |
| `mIva` | `MONEY(16,2)` | L17 |
| `cTabla` | `CHAR(1500)` | L18 |
| `cQuery` | `CHAR(1500)` | L19 |
| `iNumRows` | `INTEGER` | L20 |
| `iRegistros` | `INTEGER` | L21 |
| `iNoRegs` | `INTEGER` | L22 |
| `iRecuperacion` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_liquidacionmensual` | `bdisac` | ⚠️ sí | SELECT | L64 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sacreportemensual` | `bdisac` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L76 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L79 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |
| L91 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `mensual` | MODIF | mensual | 🔵 CONVENCIÓN | nombre_sp |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_reportemensualremesasac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportemensualremesasac.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte y remesas internacionales (mensual)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_reportebts_mensual`, `sp_reportewu_mensual` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportemensualremesasac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPeriodo                     DATE
  pConvenio                    CHAR(5)
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPeriodo` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iDia` | `INTEGER` | L9 |
| `iTotOperaciones` | `INTEGER` | L10 |
| `mMonto` | `MONEY(16,2)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iRegistros` | `INTEGER` | L13 |
| `cNumConvenio` | `CHAR(3)` | L14 |
| `iNumRows` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_reportebts_mensual` | `bdisac` | ⚠️ sí | L63 |
| `sp_reportewu_mensual` | `bdisac` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `mensual` | MODIF | mensual | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | 🔵 CONVENCIÓN | nombre_sp |
| `?ac` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ac` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportemensualremesasac_pba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportemensualremesasac_pba.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=52 |
| **Deps concatenadas** | 64 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte y remesas internacionales (mensual, PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportemensualremesasac_pba(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPeriodo                     DATE
  pConvenio                    CHAR(5)
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPeriodo` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iDia` | `INTEGER` | L9 |
| `iTotOperaciones` | `INTEGER` | L10 |
| `mMonto` | `MONEY(16,2)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iRegistros` | `INTEGER` | L13 |
| `cNumConvenio` | `CHAR(3)` | L14 |
| `iNumRows` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_reportebts_mensual` | `bdisac` | ⚠️ sí | L63 |
| `sp_reportewu_mensual` | `bdisac` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `mensual` | MODIF | mensual | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | 🔵 CONVENCIÓN | nombre_sp |
| `?ac_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ac_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportepagosctasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportepagosctasmasivocre.sql` |
| **LOC (1er CREATE)** | 938 |
| **Callgraph** | ✅ fan_in=0 / fan_out=83 |
| **Deps concatenadas** | 193 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, pagos, cuentas y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_consultareportepagoscre`, `sp_obtieneencabezadomasivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportepagosctasmasivocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pArchDescarga                CHAR(150)
  pLote                        INT
  pUsuarioC                    CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `INT` | — | — |
| `pUsuarioC` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `iExiste` | `INT` | L9 |
| `cCmd1` | `CHAR(6000)` | L10 |
| `cCmd2` | `CHAR(5000)` | L11 |
| `cCmd3` | `CHAR(5000)` | L12 |
| `cCmd4` | `CHAR(5000)` | L13 |
| `cUser` | `CHAR(8)` | L14 |
| `cReg` | `CHAR(3)` | L16 |
| `cSecuencia` | `CHAR(1)` | L17 |
| `cConcepto` | `CHAR(30)` | L18 |
| `cNumCredito` | `CHAR(20)` | L19 |
| `cNombreCte` | `CHAR(104)` | L20 |
| `m_ImportePago` | `MONEY(18,2)` | L21 |
| `mCapVigente` | `MONEY(18,2)` | L22 |
| `mCapTransitorio` | `MONEY(18,2)` | L23 |
| `mCapVencido` | `MONEY(18,2)` | L24 |
| `mCapVdoNoExigible` | `MONEY(18,2)` | L25 |
| `mCapitalTotal` | `MONEY(18,2)` | L26 |
| `mIntVigente` | `MONEY(18,2)` | L27 |
| `mIvaIntVigente` | `MONEY(18,2)` | L28 |
| `mInteresVencido` | `MONEY(18,2)` | L29 |
| `mIvaIntVencido` | `MONEY(18,2)` | L30 |
| `mIntMoraCopete` | `MONEY(18,2)` | L31 |
| *…134 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmpcapitalescta_` | `bdicnweb` | no | INSERT | L466 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L798 |
| `sw_tr_registrosreportepago` | `bdicnweb` | no | INSERT | L866 |
| `sw_tr_registrosreportepago` | `bdicnweb` | no | SELECT | L900 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultareportepagoscre` | `bdicnweb` | no | L403 |
| `sp_obtieneencabezadomasivo` | `bdicnweb` | no | L903 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L342 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L408 | FÓRMULA | `LET iNoRegistros = iNoRegistros + DBINFO('sqlca.sqlerrd2');` |  |
| L410 | FÓRMULA | `LET iIdxSecReg = iIdxSecReg + 1;` |  |
| L622 | FÓRMULA | `LET iNoRegistrosC = iNoRegistrosC + DBINFO('sqlca.sqlerrd2');` |  |
| L624 | FÓRMULA | `LET iIdxSecReg = iIdxSecReg + 1;` |  |
| L782 | FÓRMULA | `LET iTotalRegistros = iNoRegistros + iNoRegistrosC;` |  |
| L788 | VALIDACIÓN_NULL | `IF pUsuarioC <> '' AND pLote IS NULL THEN` |  |
| L796 | FÓRMULA | `LET cCmd1 = "SELECT COUNT(*)";` |  |
| L798 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|"   	   SELECT cm.id_registro, lote, (SELECT folio_grupo FROM bdicnwe` | 🔴 MONEY/aritmética financiera |
| L801 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|"   	   SELECT cm.id_registro, lote, (SELECT folio_grupo FROM bdicnwe` | 🔴 MONEY/aritmética financiera |
| L805 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|" WHERE (DATE(TO_DATE(tmp1.fecha_operacion, '%d/%m/%Y')) BETWEEN '"\|` |  |
| L818 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L836 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|"   	   SELECT cm.id_registro, lote, (SELECT folio_grupo FROM bdicnwe` | 🔴 MONEY/aritmética financiera |
| L839 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|"   	   SELECT cm.id_registro, lote, (SELECT folio_grupo FROM bdicnwe` | 🔴 MONEY/aritmética financiera |
| L843 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|" WHERE (DATE(TO_DATE(tmp1.fecha_operacion, '%d/%m/%Y')) BETWEEN '"\|` |  |
| L859 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L893 | FÓRMULA | `LET cCmd4 ="lote,folio_grupo, numcte, nombre_cte, cuenta, cReg, TRIM(NVL(TO_CHAR(mImportePagoCondona` | 🔴 MONEY/aritmética financiera |
| L894 | FÓRMULA | `LET cCmd4 =""\|\|TRIM(cCmd4)\|\|"TRIM(NVL(TO_CHAR(mCapTransiSdoNuevo, '- #,###,###,###,##&.&&'), '')` |  |
| L895 | FÓRMULA | `LET cCmd4 =""\|\|TRIM(cCmd4)\|\|"TRIM(NVL(TO_CHAR(mTotCapImpAplicado, '- #,###,###,###,##&.&&'), '')` |  |
| L896 | FÓRMULA | `LET cCmd4 =""\|\|TRIM(cCmd4)\|\|"TRIM(NVL(TO_CHAR(mIntVencImpAplicado, '- #,###,###,###,##&.&&'), ''` |  |
| L897 | FÓRMULA | `LET cCmd4 =""\|\|TRIM(cCmd4)\|\|"TRIM(NVL(TO_CHAR(mIntMoraCopeteSdoAnterior, '- #,###,###,###,##&.&&` |  |
| L908 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pagos` | ENTIDAD | pagos (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_reportepagosreversoctasmasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportepagosreversoctasmasivocre.sql` |
| **LOC (1er CREATE)** | 922 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reverso reporte, pagos, cuentas y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_consultareportepagoscre`, `sp_obtieneencabezadomasivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportepagosreversoctasmasivocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iExiste` | `INT` | L8 |
| `cCmd1` | `CHAR(6000)` | L9 |
| `cCmd2` | `CHAR(6000)` | L10 |
| `cCmd3` | `CHAR(6000)` | L11 |
| `cCmd4` | `CHAR(6000)` | L12 |
| `cUser` | `CHAR(8)` | L13 |
| `cReg` | `CHAR(3)` | L15 |
| `cSecuencia` | `CHAR(1)` | L16 |
| `cConcepto` | `CHAR(30)` | L17 |
| `cNumCredito` | `CHAR(20)` | L18 |
| `cNombreCte` | `CHAR(104)` | L19 |
| `m_ImportePago` | `MONEY(18,2)` | L20 |
| `mCapVigente` | `MONEY(18,2)` | L21 |
| `mCapTransitorio` | `MONEY(18,2)` | L22 |
| `mCapVencido` | `MONEY(18,2)` | L23 |
| `mCapVdoNoExigible` | `MONEY(18,2)` | L24 |
| `mCapitalTotal` | `MONEY(18,2)` | L25 |
| `mIntVigente` | `MONEY(18,2)` | L26 |
| `mIvaIntVigente` | `MONEY(18,2)` | L27 |
| `mInteresVencido` | `MONEY(18,2)` | L28 |
| `mIvaIntVencido` | `MONEY(18,2)` | L29 |
| `mIntMoraCopete` | `MONEY(18,2)` | L30 |
| *…133 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmpcapitalescta_` | `bdicnweb` | no | INSERT | L457 |
| `sd_bitacora_pagos` | `bdicred` | ⚠️ sí | SELECT | L789 |
| `sw_tr_registrosreportepago` | `bdicnweb` | no | INSERT | L856 |
| `sw_tr_registrosreportepago` | `bdicnweb` | no | SELECT | L890 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_consultareportepagoscre` | `bdicnweb` | no | L394 |
| `sp_obtieneencabezadomasivo` | `bdicnweb` | no | L893 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L166 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L337 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L399 | FÓRMULA | `LET iNoRegistros = iNoRegistros + DBINFO('sqlca.sqlerrd2');` |  |
| L401 | FÓRMULA | `LET iIdxSecReg = iIdxSecReg + 1;` |  |
| L612 | FÓRMULA | `LET iNoRegistrosC = iNoRegistrosC + DBINFO('sqlca.sqlerrd2');` |  |
| L614 | FÓRMULA | `LET iIdxSecReg = iIdxSecReg + 1;` |  |
| L773 | FÓRMULA | `LET iTotalRegistros = iNoRegistros + iNoRegistrosC;` |  |
| L779 | VALIDACIÓN_NULL | `IF pUsuarioC <> '' AND pLote IS NULL THEN` |  |
| L787 | FÓRMULA | `LET cCmd1 = "SELECT COUNT (*)";` |  |
| L789 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|"       SELECT a.id_registro, a.lote, b.folio_grupo, a.numcte, nvl(tr` | 🔴 MONEY/aritmética financiera |
| L800 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|" WHERE (DATE(TO_DATE(tmp1.fecha_operacion, '%d/%m/%Y')) BETWEEN '"\|` |  |
| L813 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L831 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|"       SELECT a.id_registro, a.lote, b.folio_grupo, a.numcte, nvl(tr` | 🔴 MONEY/aritmética financiera |
| L842 | FÓRMULA | `LET cCmd1=""\|\|TRIM(cCmd1)\|\|" WHERE (DATE(TO_DATE(tmp1.fecha_operacion, '%d/%m/%Y')) BETWEEN '"\|` |  |
| L883 | FÓRMULA | `LET cCmd4 ="lote,folio_grupo, numcte, nombre_cte, cuenta, cReg, TRIM(NVL(TO_CHAR(mImportePagoCondona` | 🔴 MONEY/aritmética financiera |
| L884 | FÓRMULA | `LET cCmd4 =""\|\|TRIM(cCmd4)\|\|"TRIM(NVL(TO_CHAR(mCapTransiSdoNuevo, '- #,###,###,###,##&.&&'), '')` |  |
| L885 | FÓRMULA | `LET cCmd4 =""\|\|TRIM(cCmd4)\|\|"TRIM(NVL(TO_CHAR(mTotCapImpAplicado, '- #,###,###,###,##&.&&'), '')` |  |
| L886 | FÓRMULA | `LET cCmd4 =""\|\|TRIM(cCmd4)\|\|"TRIM(NVL(TO_CHAR(mIntVencImpAplicado, '- #,###,###,###,##&.&&'), ''` |  |
| L887 | FÓRMULA | `LET cCmd4 =""\|\|TRIM(cCmd4)\|\|"TRIM(NVL(TO_CHAR(mIntMoraCopeteSdoAnterior, '- #,###,###,###,##&.&&` |  |
| L898 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pagos` | ENTIDAD | pagos (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_reporteretiroctasmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reporteretiroctasmasivo.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte, retiro y cuentas (masivo)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reporteretiroctasmasivo(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              char(10)
  pFechaInicio                 date
  pFechaFin                    date
  pArchDescarga                CHAR(150)
  pLote                        int
  pUsuarioC                    char(8)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `char(10)` | — | — |
| `pFechaInicio` | `date` | — | — |
| `pFechaFin` | `date` | — | — |
| `pArchDescarga` | `CHAR(150)` | — | — |
| `pLote` | `int` | — | — |
| `pUsuarioC` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iExiste` | `INT` | L7 |
| `cCmd1` | `CHAR(1500)` | L8 |
| `cCmd2` | `CHAR(1500)` | L9 |
| `cCmd3` | `CHAR(1500)` | L10 |
| `cCmd4` | `CHAR(1500)` | L11 |
| `cUser` | `CHAR(8)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | FÓRMULA | `LET cCmd1 = "select *";` |  |
| L53 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L57 | FÓRMULA | `LET cCmd3 = "select *";` |  |
| L66 | VALIDACIÓN_NULL | `IF pLote IS NULL OR pUsuarioC = '' THEN` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00151';` |  |
| L85 | FÓRMULA | `LET cCmd1 = "select cm.id_registro, lote, trim(nvl(cm.numcte, '')) as numcte, nvl(trim(trim(trim(c.n` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_reportesemanalconveniosac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportesemanalconveniosac.sql` |
| **LOC (1er CREATE)** | 176 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "reportes, convenio y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_sacreportesemanal` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportesemanalconveniosac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR (5)
  pKeyCons                     INTEGER
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR (5)` | `convenio`=convenio (nómina/empresarial) | 🔵 CONVENCIÓN |
| `pKeyCons` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR (5)` | L40 |
| `cCodRetSp` | `CHAR (6)` | L41 |
| `iSqlErr` | `INTEGER` | L42 |
| `iNumRows` | `INTEGER` | L43 |
| `iRecLunes` | `INTEGER` | L44 |
| `iRecMartes` | `INTEGER` | L45 |
| `iRecMiercoles` | `INTEGER` | L46 |
| `iRecJueves` | `INTEGER` | L47 |
| `iRecViernes` | `INTEGER` | L48 |
| `iRecSabado` | `INTEGER` | L49 |
| `iRecDomingo` | `INTEGER` | L50 |
| `mCobLunes` | `MONEY(16,2)` | L51 |
| `mCobMartes` | `MONEY(16,2)` | L52 |
| `mCobMiercoles` | `MONEY(16,2)` | L53 |
| `mCobJueves` | `MONEY(16,2)` | L54 |
| `mCobViernes` | `MONEY(16,2)` | L55 |
| `mCobSabado` | `MONEY(16,2)` | L56 |
| `mCobDomingo` | `MONEY(16,2)` | L57 |
| `iRecEfectivo` | `INTEGER` | L58 |
| `iRecChequemb` | `INTEGER` | L59 |
| `iRecChequeob` | `INTEGER` | L60 |
| `iRecTarcred` | `INTEGER` | L61 |
| `mCobEfectivo` | `INTEGER` | L62 |
| `mCobCheqmb` | `MONEY(16,2)` | L63 |
| `mCobCheqob` | `MONEY(16,2)` | L64 |
| *…14 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_liquidacionsemanal` | `bdisac` | ⚠️ sí | SELECT | L150 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sacreportesemanal` | `bdisac` | ⚠️ sí | L154 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L134 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L172 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?emanal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?emanal` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportesemanalecisac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportesemanalecisac.sql` |
| **LOC (1er CREATE)** | 178 |
| **Callgraph** | ✅ fan_in=0 / fan_out=27 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reportes y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_sacreportesemanaleci` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportesemanalecisac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR (5)
  pConsecutivo                 INTEGER
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR (5)` | — | — |
| `pConsecutivo` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR (5)` | L38 |
| `cCodRetSp` | `CHAR (6)` | L39 |
| `iSqlErr` | `INTEGER` | L40 |
| `iNumRows` | `INTEGER` | L41 |
| `iRecLunes` | `INTEGER` | L42 |
| `iRecMartes` | `INTEGER` | L43 |
| `iRecMiercoles` | `INTEGER` | L44 |
| `iRecJueves` | `INTEGER` | L45 |
| `iRecViernes` | `INTEGER` | L46 |
| `iRecSabado` | `INTEGER` | L47 |
| `iRecDomingo` | `INTEGER` | L48 |
| `mCobLunes` | `MONEY(16,2)` | L49 |
| `mCobMartes` | `MONEY(16,2)` | L50 |
| `mCobMiercoles` | `MONEY(16,2)` | L51 |
| `mCobJueves` | `MONEY(16,2)` | L52 |
| `mCobViernes` | `MONEY(16,2)` | L53 |
| `mCobSabado` | `MONEY(16,2)` | L54 |
| `mCobDomingo` | `MONEY(16,2)` | L55 |
| `iRecEfectivo` | `INTEGER` | L56 |
| `iRecChequemb` | `INTEGER` | L57 |
| `iRecChequeob` | `INTEGER` | L58 |
| `iRecTarcred` | `INTEGER` | L59 |
| `mCobEfectivo` | `INTEGER` | L60 |
| `mCobCheqmb` | `MONEY(16,2)` | L61 |
| `mCobCheqob` | `MONEY(16,2)` | L62 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_liquidacionsemanal` | `bdisac` | ⚠️ sí | SELECT | L146 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sacreportesemanaleci` | `bdisac` | ⚠️ sí | L152 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L171 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?emanaleci` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?emanaleci` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportesemanaltelmexsac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportesemanaltelmexsac.sql` |
| **LOC (1er CREATE)** | 175 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reportes, teléfono y Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportesemanaltelmexsac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pKeyx                        INTEGER
) RETURNING CHAR(5) AS codigoRetorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pKeyx` | `INTEGER` | `x`=por (criterio) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L38 |
| `iSqlErr` | `INTEGER` | L39 |
| `iNumRows` | `INTEGER` | L40 |
| `iRecLunes` | `INTEGER` | L41 |
| `iRecMartes` | `INTEGER` | L42 |
| `iRecMiercoles` | `INTEGER` | L43 |
| `iRecJueves` | `INTEGER` | L44 |
| `iRecViernes` | `INTEGER` | L45 |
| `iRecSabado` | `INTEGER` | L46 |
| `iRecDomingo` | `INTEGER` | L47 |
| `mCobLunes` | `MONEY(16,2)` | L48 |
| `mCobMartes` | `MONEY(16,2)` | L49 |
| `mCobMiercoles` | `MONEY(16,2)` | L50 |
| `mCobJueves` | `MONEY(16,2)` | L51 |
| `mCobViernes` | `MONEY(16,2)` | L52 |
| `mCobSabado` | `MONEY(16,2)` | L53 |
| `mCobDomingo` | `MONEY(16,2)` | L54 |
| `iRecEfectivo` | `INTEGER` | L55 |
| `iRecChequemb` | `INTEGER` | L56 |
| `iRecChequeob` | `INTEGER` | L57 |
| `iRecTarcred` | `INTEGER` | L58 |
| `mCobEfectivo` | `MONEY(16,2)` | L59 |
| `mCobCheqmb` | `MONEY(16,2)` | L60 |
| `mCobCheqob` | `MONEY(16,2)` | L61 |
| `mCobTarcred` | `MONEY(16,2)` | L62 |
| *…11 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_liquidacionestelmex` | `bdisac` | ⚠️ sí | SELECT | L145 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L165 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reportes` | ENTIDAD | reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?emanal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tel` | ENTIDAD | teléfono | 🔵 CONVENCIÓN | nombre_sp |
| `?me` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?emanal`, `?me` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reportetipocambio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportetipocambio.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte y cambio (tipo de)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportetipocambio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `cDivisa` | `CHAR(2)` | L22 |
| `cClase_tpcambio` | `CHAR(1)` | L23 |
| `dPrecio_compra` | `DECIMAL(14,6)` | L24 |
| `dPc_arriba` | `DECIMAL(14,6)` | L25 |
| `dPc_abajo` | `DECIMAL(14,6)` | L26 |
| `dPrecio_venta` | `DECIMAL(14,6)` | L27 |
| `dPv_abajo` | `DECIMAL(14,6)` | L28 |
| `dPv_arriba` | `DECIMAL(14,6)` | L29 |
| `dTipo_cpa_div_dll` | `DECIMAL(14,6)` | L30 |
| `dTipo_cpa_mn_dll` | `DECIMAL(14,6)` | L31 |
| `dTipo_cpa_mn_div` | `DECIMAL(14,6)` | L32 |
| `dTipo_vta_div_dll` | `DECIMAL(14,6)` | L33 |
| `dTipo_vta_mn_dll` | `DECIMAL(14,6)` | L34 |
| `dTipo_vta_mn_div` | `DECIMAL(14,6)` | L35 |
| `cDescripcion` | `CHAR(30)` | L36 |
| `cDesc_clase_tc` | `CHAR(30)` | L37 |
| `iRecuperacion` | `INTEGER` | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet           = '00000';` |  |
| L73 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L105 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_reportetraspasoctabeneficencia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reportetraspasoctabeneficencia.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso entre cuentas reporte, cuenta y beneficiario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_obtieneencabezadomasivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_reportetraspasoctabeneficencia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionPadre              CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pArchivoDescarga             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdFuncionPadre` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pArchivoDescarga` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `cCodRet2` | `CHAR(5)` | L11 |
| `cCodRet3` | `CHAR(50)` | L12 |
| `cCodRetSp` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iSamErr` | `INTEGER` | L15 |
| `cDesErr` | `CHAR(50)` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |
| `vsql` | `CHAR(500)` | L18 |
| `vstmt` | `CHAR(300)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cuentas_traspbenef` | `bdicheq` | ⚠️ sí | SELECT | L65 |
| `sc_cuentas_traspbenef` | `bdicnweb` | no | SELECT | L75 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L55 |
| `sp_obtieneencabezadomasivo` | `bdicnweb` | no | L82 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L78 | FÓRMULA | `LET vstmt = "/ifxsif01/bin/dbaccess bdicheq /tmp/ctastraspbenef.sql";` |  |
| L86 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, '';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `traspaso` | ACCION | traspaso entre cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `benef` | ENTIDAD | beneficiario | 🔵 CONVENCIÓN | nombre_sp |
| `?icencia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?icencia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_respaldohistoricotpcambio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_respaldohistoricotpcambio.sql` |
| **LOC (1er CREATE)** | 147 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "respaldo y cambio (histórico, tipo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_respaldohistoricotpcambio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `c_empresa` | `CHAR(3)` | L8 |
| `c_divisa` | `CHAR(3)` | L9 |
| `dt_fecha_tpcambio` | `DATE` | L10 |
| `dt_hora_tpcambio` | `DATETIME HOUR TO MINUTE` | L11 |
| `c_clase_tpcambio` | `CHAR(1)` | L12 |
| `d_precio_compra` | `DECIMAL (14,6)` | L13 |
| `d_pc_arriba` | `DECIMAL (14,6)` | L14 |
| `d_pc_abajo` | `DECIMAL (14,6)` | L15 |
| `d_precio_venta` | `DECIMAL (14,6)` | L16 |
| `d_pv_abajo` | `DECIMAL (14,6)` | L17 |
| `d_pv_arriba` | `DECIMAL (14,6)` | L18 |
| `d_variacion_cpa` | `DECIMAL (9,7)` | L19 |
| `d_variacion_vta` | `DECIMAL (9,7)` | L20 |
| `d_tipo_cpa_div_dll` | `DECIMAL (14,6)` | L21 |
| `d_tipo_cpa_mn_dll` | `DECIMAL (14,6)` | L22 |
| `d_tipo_cpa_mn_div` | `DECIMAL (14,6)` | L23 |
| `d_tipo_vta_div_dll` | `DECIMAL (14,6)` | L24 |
| `d_tipo_vta_mn_dll` | `DECIMAL (14,6)` | L25 |
| `d_tipo_vta_mn_div` | `DECIMAL (14,6)` | L26 |
| `d_precio_base` | `DECIMAL (14,6)` | L27 |
| `d_var_cpa_base` | `DECIMAL (9,7)` | L28 |
| `d_var_vta_base` | `DECIMAL (9,7)` | L29 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_contproc` | `bdinteg` | ⚠️ sí | SELECT | L94 |
| `si_tpcambio` | `bdinteg` | ⚠️ sí | SELECT | L106 |
| `si_histdiv` | `bdinteg` | ⚠️ sí | SELECT | L108 |
| `si_histdiv` | `bdinteg` | ⚠️ sí | INSERT | L112 |
| `si_contproc` | `bdinteg` | ⚠️ sí | INSERT | L127 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pFecha IS NULL THEN` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `respaldo` | ENTIDAD | respaldo / garantía de crédito (aval) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_respuesta_ws_dotacion_masiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_respuesta_ws_dotacion_masiva.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "respuesta y dotación de efectivo (masiva)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_respuesta_ws_dotacion_masiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pId_solicitud                char(25)
  pEstatus                     char(60)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pId_solicitud` | `char(25)` | — | — |
| `pEstatus` | `char(60)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `arch_dotacion_sucursal` | `bdicnweb` | no | UPDATE | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L28 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `respuesta` | ENTIDAD | respuesta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_ws_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `masiva` | MODIF | masiva | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ws_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_restablecerevisionsoliaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_restablecerevisionsoliaumlincred.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 105 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Aumento de Línea de Crédito — proceso de incremento del límite crediticio; 26+ SPs en bdicred revisión y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdicred` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_restablecerevisionsoliaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(20)
  pMismoUsuario                CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(20)` | `sol`=solicitud | 🟡 INFERIDO |
| `pMismoUsuario` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cMensajeRetorno` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L34 |
| `bdicred` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_restablecerrevisionsolicitud';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_restablece` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `revision` | ENTIDAD | revisión | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_restablece`, `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_revaluasolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_revaluasolicitudmc.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=74 |
| **Deps concatenadas** | 85 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reversión solicitud y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_mc_revaluasol` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_revaluasolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumSolicitud                CHAR(20)
  pBandera                     CHAR(1)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pNumSolicitud` | `CHAR(20)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cMensaje` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cBanderaMC` | `CHAR(4)` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_mc_revaluasol` | `bdisolic` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisolic:sp_mc_revaluasol';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `?alua` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?alua` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reversardotacioncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reversardotacioncaja.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Reversión — anula/revierte una operación dotación de efectivo y caja" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reversardotacioncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFolioOperacion              CHAR(8)
  pRespuesta                   CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFolioOperacion` | `CHAR(8)` | — | — |
| `pRespuesta` | `CHAR(1)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iRegistrosAfectados` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reversioncap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reversioncap.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "reversa Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `reversion` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_reversioncap(
  pUsuario                     char(10)
  pIdFuncion                   char(10)
  pFolioMovimiento             char(16)
  pSucursalFolio               char(4)
  pTransacc                    char(4)
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(10)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pFolioMovimiento` | `char(16)` | — | — |
| `pSucursalFolio` | `char(4)` | — | — |
| `pTransacc` | `char(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L8 |
| `cConstante` | `char(1)` | L9 |
| `cEmpresa` | `char(3)` | L10 |
| `iSqlErr` | `int` | L11 |
| `cReversable` | `char(1)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `reversion` | `bdicheq` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | VALIDACIÓN_NULL | `IF cReversable IS NULL OR cReversable='' THEN` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00112';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00113';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00114';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00115';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00116';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00117';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reversion` | ACCION | reversa / rollback | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_reversioncap_pba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reversioncap_pba.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "reversa Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_reversioncap_pba(
  pUsuario                     char(10)
  pIdFuncion                   char(10)
  pFolioMovimiento             char(16)
  pSucursalFolio               char(4)
  pTransacc                    char(4)
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(10)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pFolioMovimiento` | `char(16)` | — | — |
| `pSucursalFolio` | `char(4)` | — | — |
| `pTransacc` | `char(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L8 |
| `cConstante` | `char(1)` | L9 |
| `cEmpresa` | `char(3)` | L10 |
| `iSqlErr` | `int` | L11 |
| `cReversable` | `char(1)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `reversion` | `bdicheq` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | VALIDACIÓN_NULL | `IF cReversable IS NULL OR cReversable='' THEN` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet = '00112';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00113';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00114';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00115';` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00116';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00117';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reversion` | ACCION | reversa / rollback | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_reversopagomasivocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_reversopagomasivocre.sql` |
| **LOC (1er CREATE)** | 191 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reverso pago y crédito (masivo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_grabarreversopagosmasivos`, `sp_constotalesreversopagocre` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_reversopagomasivocre(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
  pPlantilla                   CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |
| `pPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cFolioGen` | `CHAR(16)` | L7 |
| `dFechaProc` | `DATETIME YEAR TO FRACTION` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegs` | `INTEGER` | L10 |
| `cResultadoSp` | `CHAR(100)` | L11 |
| `cResultado` | `CHAR(15)` | L12 |
| `iIdRegistro` | `INTEGER` | L13 |
| `cStatus` | `CHAR(1)` | L14 |
| `wBegin` | `SMALLINT` | L15 |
| `iReversar` | `SMALLINT` | L16 |
| `dFechaMail` | `DATETIME YEAR TO SECOND` | L17 |
| `iTotalRegs` | `INTEGER` | L18 |
| `mTotalMonto` | `MONEY(14,2)` | L19 |
| `iTotalRever` | `INTEGER` | L20 |
| `iTotNoRever` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_cargamasiva_pago_hist` | `bdicnweb` | no | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_grabarreversopagosmasivos` | `bdicred` | ⚠️ sí | L86 |
| `sp_constotalesreversopagocre` | `bdicnweb` | no | L141 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L107 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reverso` | ACCION | reverso | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_ris_actualizainsertaprodtransaccion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_actualizainsertaprodtransaccion.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 37 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza Riesgo — módulo de gestión de riesgo crediticio, producto y transacción" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_actualizainsertaprodtransaccion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pCccmayor                    CHAR(10)
  pCccsub                      CHAR(10)
  pCccsubsub                   CHAR(10)
  pCccsssub                    CHAR(10)
  pCccssssub                   CHAR(10)
  pCsector                     CHAR(10)
  pAccmayor                    CHAR(10)
  pAccsub                      CHAR(10)
  pAccsubsub                   CHAR(10)
  pAccsssub                    CHAR(10)
  pAccssssub                   CHAR(10)
  pAsector                     CHAR(10)
  pSistema                     CHAR(10)
  pSecuencia                   CHAR(10)
  pTransaccion                 CHAR(10)
  pProducto                    CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pCccmayor` | `CHAR(10)` | — | — |
| `pCccsub` | `CHAR(10)` | — | — |
| `pCccsubsub` | `CHAR(10)` | — | — |
| `pCccsssub` | `CHAR(10)` | — | — |
| `pCccssssub` | `CHAR(10)` | — | — |
| `pCsector` | `CHAR(10)` | — | — |
| `pAccmayor` | `CHAR(10)` | — | — |
| `pAccsub` | `CHAR(10)` | — | — |
| `pAccsubsub` | `CHAR(10)` | — | — |
| `pAccsssub` | `CHAR(10)` | — | — |
| `pAccssssub` | `CHAR(10)` | — | — |
| `pAsector` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(10)` | — | — |
| `pSecuencia` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(10)` | `transaccion`=transacción | ✅ CÓDIGO |
| `pProducto` | `CHAR(10)` | `prod`=producto | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iTotales` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_prodtran` | `bdinteg` | ⚠️ sí | UPDATE | L41 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L62 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | DELETE | L62 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | INSERT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ris_actualizainsertaprodtransaccionb5`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_actualizainsertaprodtransaccionb5.sql` |
| **LOC (1er CREATE)** | 146 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza Riesgo — módulo de gestión de riesgo crediticio, producto y transacción (sufijo de versión de SP)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=2 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_actualizainsertaprodtransaccionb5(
  pBandera                     CHAR(1)
  pCccmayor                    CHAR(10)
  pCccsub                      CHAR(10)
  pCccsubsub                   CHAR(10)
  pCccsssub                    CHAR(10)
  pCccssssub                   CHAR(10)
  pCsector                     CHAR(10)
  pAccmayor                    CHAR(10)
  pAccsub                      CHAR(10)
  pAccsubsub                   CHAR(10)
  pAccsssub                    CHAR(10)
  pAccssssub                   CHAR(10)
  pAsector                     CHAR(10)
  pSistema                     CHAR(10)
  pSecuencia                   CHAR(10)
  pTransaccion                 CHAR(10)
  pProducto                    CHAR(10)
  pUsuario                     CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBandera` | `CHAR(1)` | — | — |
| `pCccmayor` | `CHAR(10)` | — | — |
| `pCccsub` | `CHAR(10)` | — | — |
| `pCccsubsub` | `CHAR(10)` | — | — |
| `pCccsssub` | `CHAR(10)` | — | — |
| `pCccssssub` | `CHAR(10)` | — | — |
| `pCsector` | `CHAR(10)` | — | — |
| `pAccmayor` | `CHAR(10)` | — | — |
| `pAccsub` | `CHAR(10)` | — | — |
| `pAccsubsub` | `CHAR(10)` | — | — |
| `pAccsssub` | `CHAR(10)` | — | — |
| `pAccssssub` | `CHAR(10)` | — | — |
| `pAsector` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(10)` | — | — |
| `pSecuencia` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(10)` | `transaccion`=transacción | ✅ CÓDIGO |
| `pProducto` | `CHAR(10)` | `prod`=producto | 🟡 INFERIDO |
| `pUsuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iTotales` | `INTEGER` | L10 |
| `iMaxCommit` | `INTEGER` | L11 |
| `iContBloque` | `INTEGER` | L12 |
| `cAux` | `CHAR(10)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L45 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | UPDATE | L52 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | DELETE | L90 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | INSERT | L109 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | FÓRMULA | `LET iContBloque = iContBloque + 1;` |  |
| L97 | FÓRMULA | `LET iContBloque = iContBloque + 1;` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `b5` | MODIF | sufijo de versión de SP (Bloque/Build 5) | 🟡 INFERIDO | nombre_sp |

---

## `sp_ris_consultacatalogo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultacatalogo.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y catálogo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultacatalogo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCcmayor                     CHAR(10)
  pCcsub                       CHAR(10)
  pCcsubsub                    CHAR(10)
  pCcssubsub                   CHAR(10)
  pCcsssubsub                  CHAR(10)
  pSector                      CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCcmayor` | `CHAR(10)` | — | — |
| `pCcsub` | `CHAR(10)` | — | — |
| `pCcsubsub` | `CHAR(10)` | — | — |
| `pCcssubsub` | `CHAR(10)` | — | — |
| `pCcsssubsub` | `CHAR(10)` | — | — |
| `pSector` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cCccmayor` | `CHAR(10)` | L14 |
| `cCccsub` | `CHAR(10)` | L15 |
| `cCccsubsub` | `CHAR(10)` | L16 |
| `cCccsssub` | `CHAR(10)` | L17 |
| `cCccssssub` | `CHAR(10)` | L18 |
| `cCsector` | `CHAR(10)` | L19 |
| `cNombre` | `CHAR(50)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catalog` | `bdinteg` | ⚠️ sí | SELECT | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00030';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ris_consultacriteriostransacciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultacriteriostransacciones.sql` |
| **LOC (1er CREATE)** | 219 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio, OS — Originación de Solicitudes y transacción" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultacriteriostransacciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistema                     CHAR(2)
  pProducto                    CHAR(10)
  pTransaccion                 CHAR(4)
  pDescripcion                 CHAR(50)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(2)` | — | — |
| `pProducto` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(4)` | `transaccion`=transacción | ✅ CÓDIGO |
| `pDescripcion` | `CHAR(50)` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L20 |
| `iSqlErr` | `INTEGER` | L21 |
| `iInicioReg` | `INTEGER` | L22 |
| `iTamReg` | `INTEGER` | L23 |
| `iPosCaracter` | `INTEGER` | L24 |
| `iPosCaracter2` | `INTEGER` | L25 |
| `iRecuperacion` | `INTEGER` | L26 |
| `cEmpresa` | `CHAR(3)` | L27 |
| `cTabla` | `CHAR(50)` | L28 |
| `cDato2` | `CHAR(50)` | L29 |
| `cCampo1` | `CHAR(50)` | L30 |
| `cCampo2` | `CHAR(50)` | L31 |
| `cBase` | `CHAR(50)` | L32 |
| `cDescripcion` | `CHAR(100)` | L33 |
| `cUsrBin` | `CHAR(15)` | L34 |
| `cIfxBin` | `CHAR(15)` | L35 |
| `cCmd1` | `CHAR(2000)` | L36 |
| `cCmd2` | `CHAR(2000)` | L37 |
| `cCmd3` | `CHAR(2000)` | L38 |
| `cCmd4` | `CHAR(2000)` | L39 |
| `cCmd5` | `CHAR(2000)` | L40 |
| `cCmd6` | `CHAR(2000)` | L41 |
| `cFile` | `CHAR(100)` | L42 |
| `cCodigo` | `CHAR(10)` | L43 |
| `cDescProd` | `CHAR(100)` | L44 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ristras_consultacriteriostransacciones` | `bdicnweb` | no | SELECT | L135 |
| `sw_ristras_consultacriteriostransacciones` | `bdicnweb` | no | DELETE | L135 |
| `si_sistema` | `bdinteg` | ⚠️ sí | SELECT | L139 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L150 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L150 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L120 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L144 | FÓRMULA | `LET cTabla = SUBSTR(TRIM(cDescripcion), iInicioReg, (iPosCaracter - 1));` |  |
| L145 | FÓRMULA | `LET cDato2 = SUBSTR(TRIM(cDescripcion), (iPosCaracter + 1), (iTamReg - iPosCaracter));` |  |
| L147 | FÓRMULA | `LET cCampo1 = SUBSTR(TRIM(cDato2), iInicioReg, (iPosCaracter2 - 1));` |  |
| L148 | FÓRMULA | `LET cCampo2 = SUBSTR(TRIM(cDato2), (iPosCaracter2 + 1), (iTamReg - iPosCaracter2));` |  |
| L171 | FÓRMULA | `LET cCmd6 = TRIM(cCmd5) \|\| " ORDER BY 1,2,3, a.secuencia"" > /tmp/mfinis/" \|\| cFile;` |  |
| L176 | FÓRMULA | `LET cCmd1 = TRIM(cIfxBin)\|\|"dbaccess bdicnweb < /tmp/mfinis/" \|\| cFile;` |  |
| L180 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf /tmp/mfinis/" \|\| TRIM(cFile);` |  |
| L192 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L199 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?criteri` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?criteri`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ris_consultacriteriostransaccionesb5`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultacriteriostransaccionesb5.sql` |
| **LOC (1er CREATE)** | 204 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio, OS — Originación de Solicitudes y transacción (sufijo de versión de SP)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultacriteriostransaccionesb5(
  pSistema                     CHAR(2)
  pProducto                    CHAR(10)
  pTransaccion                 CHAR(4)
  pDescripcion                 CHAR(50)
  pUsuario                     CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSistema` | `CHAR(2)` | — | — |
| `pProducto` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(4)` | `transaccion`=transacción | ✅ CÓDIGO |
| `pDescripcion` | `CHAR(50)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `iInicioReg` | `INTEGER` | L24 |
| `iTamReg` | `INTEGER` | L25 |
| `iPosCaracter` | `INTEGER` | L26 |
| `iPosCaracter2` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |
| `cEmpresa` | `CHAR(3)` | L29 |
| `cTabla` | `CHAR(50)` | L30 |
| `cDato2` | `CHAR(50)` | L31 |
| `cCampo1` | `CHAR(50)` | L32 |
| `cCampo2` | `CHAR(50)` | L33 |
| `cBase` | `CHAR(50)` | L34 |
| `cDescripcion` | `CHAR(100)` | L35 |
| `cUsrBin` | `CHAR(15)` | L36 |
| `cIfxBin` | `CHAR(15)` | L37 |
| `cCmd1` | `CHAR(2000)` | L38 |
| `cCmd2` | `CHAR(2000)` | L39 |
| `cCmd3` | `CHAR(2000)` | L40 |
| `cCmd4` | `CHAR(2000)` | L41 |
| `cCmd5` | `CHAR(2000)` | L42 |
| `cCmd6` | `CHAR(2000)` | L43 |
| `cFile` | `CHAR(100)` | L44 |
| `cCodigo` | `CHAR(10)` | L45 |
| `cDescProd` | `CHAR(100)` | L46 |
| *…16 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ristras_consultacriteriostransacciones` | `bdicnweb` | no | SELECT | L125 |
| `sw_ristras_consultacriteriostransacciones` | `bdicnweb` | no | DELETE | L125 |
| `si_sistema` | `bdinteg` | ⚠️ sí | SELECT | L129 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L140 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L140 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L134 | FÓRMULA | `LET cTabla = SUBSTR(TRIM(cDescripcion), iInicioReg, (iPosCaracter - 1));` |  |
| L135 | FÓRMULA | `LET cDato2 = SUBSTR(TRIM(cDescripcion), (iPosCaracter + 1), (iTamReg - iPosCaracter));` |  |
| L137 | FÓRMULA | `LET cCampo1 = SUBSTR(TRIM(cDato2), iInicioReg, (iPosCaracter2 - 1));` |  |
| L138 | FÓRMULA | `LET cCampo2 = SUBSTR(TRIM(cDato2), (iPosCaracter2 + 1), (iTamReg - iPosCaracter2));` |  |
| L161 | FÓRMULA | `LET cCmd6 = TRIM(cCmd5) \|\| " ORDER BY 1,2,3, a.secuencia"" > /tmp/mfinis/" \|\| cFile;` |  |
| L166 | FÓRMULA | `LET cCmd1 = TRIM(cIfxBin)\|\|"dbaccess bdicnweb < /tmp/mfinis/" \|\| cFile;` |  |
| L170 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf /tmp/mfinis/" \|\| TRIM(cFile);` |  |
| L187 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?criteri` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `b5` | MODIF | sufijo de versión de SP (Bloque/Build 5) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?criteri`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ris_consultanombrecatalogo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultanombrecatalogo.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio, nombre y catálogo" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultanombrecatalogo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCcmayor                     CHAR(10)
  pCcsub                       CHAR(10)
  pCcsubsub                    CHAR(10)
  pCcssubsub                   CHAR(10)
  pCcsssubsub                  CHAR(10)
  pSector                      CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCcmayor` | `CHAR(10)` | — | — |
| `pCcsub` | `CHAR(10)` | — | — |
| `pCcsubsub` | `CHAR(10)` | — | — |
| `pCcssubsub` | `CHAR(10)` | — | — |
| `pCcsssubsub` | `CHAR(10)` | — | — |
| `pSector` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNombre` | `CHAR(50)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_catalog` | `bdinteg` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `catalogo` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ris_consultaprodtransaccion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultaprodtransaccion.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 33 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio, producto y transacción" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultaprodtransaccion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistema                     CHAR(10)
  pSecuencia                   CHAR(10)
  pTransaccion                 CHAR(10)
  pProducto                    CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(10)` | — | — |
| `pSecuencia` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(10)` | `transaccion`=transacción | ✅ CÓDIGO |
| `pProducto` | `CHAR(10)` | `prod`=producto | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `iTotales` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L38 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ris_consultaprodtransaccionb5`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultaprodtransaccionb5.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio, producto y transacción (sufijo de versión de SP)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultaprodtransaccionb5(
  pSistema                     CHAR(10)
  pSecuencia                   CHAR(10)
  pTransaccion                 CHAR(10)
  pProducto                    CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSistema` | `CHAR(10)` | — | — |
| `pSecuencia` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(10)` | `transaccion`=transacción | ✅ CÓDIGO |
| `pProducto` | `CHAR(10)` | `prod`=producto | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `iTotales` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '99999';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `prod` | ENTIDAD | producto | 🟡 INFERIDO | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `b5` | MODIF | sufijo de versión de SP (Bloque/Build 5) | 🟡 INFERIDO | nombre_sp |

---

## `sp_ris_consultaproductos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultaproductos.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y productos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultaproductos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistema                     CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iInicioReg` | `INTEGER` | L8 |
| `iTamReg` | `INTEGER` | L9 |
| `iPosCaracter` | `INTEGER` | L10 |
| `iPosCaracter2` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cTabla` | `CHAR(50)` | L14 |
| `cDato2` | `CHAR(50)` | L15 |
| `cCampo1` | `CHAR(50)` | L16 |
| `cCampo2` | `CHAR(50)` | L17 |
| `cBase` | `CHAR(50)` | L18 |
| `cDescripcion` | `CHAR(100)` | L19 |
| `cUsrBin` | `CHAR(15)` | L20 |
| `cIfxBin` | `CHAR(15)` | L21 |
| `cCmd1` | `CHAR(1000)` | L22 |
| `cFile` | `CHAR(100)` | L23 |
| `cCodigo` | `CHAR(10)` | L24 |
| `cDescProd` | `CHAR(100)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ristras_cmbproducto` | `bdicnweb` | no | SELECT | L72 |
| `sw_ristras_cmbproducto` | `bdicnweb` | no | DELETE | L72 |
| `si_sistema` | `bdinteg` | ⚠️ sí | SELECT | L76 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L87 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L81 | FÓRMULA | `LET cTabla = SUBSTR(TRIM(cDescripcion), iInicioReg, (iPosCaracter - 1));` |  |
| L82 | FÓRMULA | `LET cDato2 = SUBSTR(TRIM(cDescripcion), (iPosCaracter + 1), (iTamReg - iPosCaracter));` |  |
| L84 | FÓRMULA | `LET cCampo1 = SUBSTR(TRIM(cDato2), iInicioReg, (iPosCaracter2 - 1));` |  |
| L85 | FÓRMULA | `LET cCampo2 = SUBSTR(TRIM(cDato2), (iPosCaracter2 + 1), (iTamReg - iPosCaracter2));` |  |
| L87 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"echo " \|\| '"' \|\| "INSERT INTO bdicnweb:""informix"".sw_ristras_cmb` |  |
| L92 | FÓRMULA | `LET cCmd1 = TRIM(cIfxBin)\|\|"dbaccess bdicnweb < /tmp/mfinis/" \|\| cFile;` |  |
| L96 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf /tmp/mfinis/" \|\| TRIM(cFile);` |  |
| L108 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ris_consultaproductosb5`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultaproductosb5.sql` |
| **LOC (1er CREATE)** | 127 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y productos (sufijo de versión de SP)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultaproductosb5(
  pSistema                     CHAR(2)
  pUsuario                     char(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSistema` | `CHAR(2)` | — | — |
| `pUsuario` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iInicioReg` | `INTEGER` | L8 |
| `iTamReg` | `INTEGER` | L9 |
| `iPosCaracter` | `INTEGER` | L10 |
| `iPosCaracter2` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cTabla` | `CHAR(50)` | L14 |
| `cDato2` | `CHAR(50)` | L15 |
| `cCampo1` | `CHAR(50)` | L16 |
| `cCampo2` | `CHAR(50)` | L17 |
| `cBase` | `CHAR(50)` | L18 |
| `cDescripcion` | `CHAR(100)` | L19 |
| `cUsrBin` | `CHAR(15)` | L20 |
| `cIfxBin` | `CHAR(15)` | L21 |
| `cCmd1` | `CHAR(1000)` | L22 |
| `cFile` | `CHAR(100)` | L23 |
| `cCodigo` | `CHAR(10)` | L24 |
| `cDescProd` | `CHAR(100)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ristras_cmbproducto` | `bdicnweb` | no | SELECT | L67 |
| `sw_ristras_cmbproducto` | `bdicnweb` | no | DELETE | L67 |
| `si_sistema` | `bdinteg` | ⚠️ sí | SELECT | L71 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | FÓRMULA | `LET cTabla = SUBSTR(TRIM(cDescripcion), iInicioReg, (iPosCaracter - 1));` |  |
| L77 | FÓRMULA | `LET cDato2 = SUBSTR(TRIM(cDescripcion), (iPosCaracter + 1), (iTamReg - iPosCaracter));` |  |
| L79 | FÓRMULA | `LET cCampo1 = SUBSTR(TRIM(cDato2), iInicioReg, (iPosCaracter2 - 1));` |  |
| L80 | FÓRMULA | `LET cCampo2 = SUBSTR(TRIM(cDato2), (iPosCaracter2 + 1), (iTamReg - iPosCaracter2));` |  |
| L86 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"echo " \|\| '"' \|\| "INSERT INTO bdicnweb:""informix"".sw_ristras_cmb` |  |
| L91 | FÓRMULA | `LET cCmd1 = TRIM(cIfxBin)\|\|"dbaccess bdicnweb < /tmp/mfinis/" \|\| cFile;` |  |
| L95 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf /tmp/mfinis/" \|\| TRIM(cFile);` |  |
| L107 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `productos` | ENTIDAD | productos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `b5` | MODIF | sufijo de versión de SP (Bloque/Build 5) | 🟡 INFERIDO | nombre_sp |

---

## `sp_ris_consultarepsistema`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultarepsistema.sql` |
| **LOC (1er CREATE)** | 430 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar Riesgo — módulo de gestión de riesgo crediticio y sistema" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultarepsistema(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pBandera                     CHAR(1)
  pSistema                     CHAR(2)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |
| `pSistema` | `CHAR(2)` | `sistema`=sistema | ✅ CÓDIGO |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iInicioReg` | `INTEGER` | L8 |
| `iTamReg` | `INTEGER` | L9 |
| `iPosCaracter` | `INTEGER` | L10 |
| `iPosCaracter2` | `INTEGER` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cTabla` | `CHAR(50)` | L14 |
| `cDato2` | `CHAR(50)` | L15 |
| `cCampo1` | `CHAR(50)` | L16 |
| `cCampo2` | `CHAR(50)` | L17 |
| `cBase` | `CHAR(50)` | L18 |
| `cDescripcion` | `CHAR(100)` | L19 |
| `cUsrBin` | `CHAR(15)` | L20 |
| `cIfxBin` | `CHAR(15)` | L21 |
| `cCmd1` | `CHAR(3000)` | L22 |
| `cCmd2` | `CHAR(3000)` | L23 |
| `cFile` | `CHAR(100)` | L24 |
| `cCodigo` | `CHAR(10)` | L25 |
| `cDescProd` | `CHAR(100)` | L26 |
| `cProducto` | `CHAR(4)` | L27 |
| `cProductoFinal` | `LVARCHAR(MAX)` | L28 |
| `cNumProducto` | `LVARCHAR(MAX)` | L29 |
| `cDescProdFinal` | `LVARCHAR(MAX)` | L30 |
| *…18 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepxsist` | `bdicnweb` | no | SELECT | L116 |
| `sw_verificastatusrepxsist` | `bdicnweb` | no | DELETE | L116 |
| `sw_verificastatusrepxsist` | `bdicnweb` | no | INSERT | L117 |
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L142 |
| `sw_ristras_reporteporsistema` | `bdicnweb` | no | SELECT | L163 |
| `sw_ristras_reporteporsistema` | `bdicnweb` | no | DELETE | L163 |
| `sw_ristras_reporteporsistemaproductos` | `bdicnweb` | no | SELECT | L164 |
| `sw_ristras_reporteporsistemaproductos` | `bdicnweb` | no | DELETE | L164 |
| `si_sistema` | `bdinteg` | ⚠️ sí | SELECT | L176 |
| `sw_ristras_reporteporsistema` | `bdicnweb` | no | INSERT | L188 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L196 |
| `sw_ristras_reporteporsistema` | `bdicnweb` | no | UPDATE | L263 |
| `systables` | `bdicnweb` | no | SELECT | L335 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L127 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L182 | FÓRMULA | `LET cTabla = SUBSTR(TRIM(cDescripcion), iInicioReg, (iPosCaracter - 1));` |  |
| L183 | FÓRMULA | `LET cDato2 = SUBSTR(TRIM(cDescripcion), (iPosCaracter + 1), (iTamReg - iPosCaracter));` |  |
| L185 | FÓRMULA | `LET cCampo1 = SUBSTR(TRIM(cDato2), iInicioReg, (iPosCaracter2 - 1));` |  |
| L186 | FÓRMULA | `LET cCampo2 = SUBSTR(TRIM(cDato2), (iPosCaracter2 + 1), (iTamReg - iPosCaracter2));` |  |
| L196 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"echo " \|\| '"' \|\| "INSERT INTO bdicnweb:""informix"".sw_ristras_rep` |  |
| L200 | FÓRMULA | `LET cCmd1 = TRIM(cIfxBin)\|\|"dbaccess bdicnweb < " \|\| TRIM(pRutaDescarga) \|\| "/" \|\| cFile;` |  |
| L204 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf " \|\| TRIM(pRutaDescarga) \|\| "/" \|\| TRIM(cFile);` |  |
| L215 | FÓRMULA | `LET iAux = iAux+1;` |  |
| L221 | FÓRMULA | `LET cDescProdFinal = TRIM(cDescProdFinal) \|\|TRIM(cProducto) \|\|" "\|\|TRIM(NVL(cDescProd,"")) \|\` |  |
| L224 | FÓRMULA | `LET cDescProdFinal2 = TRIM(cDescProdFinal2) \|\|TRIM(cProducto) \|\|" "\|\|TRIM(NVL(cDescProd,"")) \` |  |
| L227 | FÓRMULA | `LET cDescProdFinal3 = TRIM(cDescProdFinal3) \|\|TRIM(cProducto) \|\|" "\|\|TRIM(NVL(cDescProd,"")) \` |  |
| L233 | FÓRMULA | `LET cNumProducto = SUBSTR(TRIM(cNumProducto), 0, (iTamReg - 1));` |  |
| L240 | FÓRMULA | `LET cDescProdFinal = SUBSTR(TRIM(cDescProdFinal), 0, (iTamReg - 1));` |  |
| L245 | FÓRMULA | `LET cDescProdFinal = SUBSTR(TRIM(cDescProdFinal), 0, (iTamReg - 1));` |  |
| L247 | FÓRMULA | `LET cDescProdFinal2 = SUBSTR(TRIM(cDescProdFinal2), 0, (iTamReg - 1));` |  |
| L252 | FÓRMULA | `LET cDescProdFinal = SUBSTR(TRIM(cDescProdFinal), 0, (iTamReg - 1));` |  |
| L254 | FÓRMULA | `LET cDescProdFinal2 = SUBSTR(TRIM(cDescProdFinal2), 0, (iTamReg - 1));` |  |
| L257 | FÓRMULA | `LET cDescProdFinal3 = SUBSTR(TRIM(cDescProdFinal3), 0, (iTamReg - 1));` |  |
| L315 | FÓRMULA | `LET cCtaContablePaso = SUBSTR(TRIM(cCtaContablePaso), 0, (iTamReg - 1));` |  |
| L325 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L335 | FÓRMULA | `LET cCmd2 =" FROM systables WHERE tabid = 1 UNION ALL SELECT * FROM( SELECT transaccion, descripcion` |  |
| L363 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L368 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L377 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L386 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L394 | FÓRMULA | `LET cSql = "sed 's/\\//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sistema` | ENTIDAD | sistema | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ris_consultasistemas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultasistemas.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y sistema" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultasistemas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cSistema` | `CHAR(2)` | L8 |
| `cDescripcion` | `CHAR(35)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sistema` | `bdinteg` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sistema` | ENTIDAD | sistema | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ris_consultasistemasb5`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultasistemasb5.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y sistema (sufijo de versión de SP)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultasistemasb5(
) RETURNING CHAR(5) AS codret,
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cSistema` | `CHAR(2)` | L8 |
| `cDescripcion` | `CHAR(35)` | L9 |
| `iRecuperacion` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sistema` | `bdinteg` | ⚠️ sí | SELECT | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `sistema` | ENTIDAD | sistema | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `b5` | MODIF | sufijo de versión de SP (Bloque/Build 5) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ris_consultatransaccion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultatransaccion.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 34 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y transacción" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultatransaccion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistema                     CHAR(10)
  pSecuencia                   CHAR(10)
  pTransaccion                 CHAR(10)
  pProducto                    CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(10)` | — | — |
| `pSecuencia` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(10)` | `transaccion`=transacción | ✅ CÓDIGO |
| `pProducto` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `cEmpresa` | `CHAR(3)` | L24 |
| `cSistema` | `CHAR(3)` | L28 |
| `cProducto` | `CHAR(4)` | L29 |
| `cTransaccion` | `CHAR(4)` | L30 |
| `cDescTran` | `CHAR(50)` | L31 |
| `cNaturaleza` | `CHAR(1)` | L32 |
| `iSecuencia` | `INTEGER` | L33 |
| `cCccmayor` | `CHAR(10)` | L34 |
| `cCccsub` | `CHAR(10)` | L35 |
| `cCccsubsub` | `CHAR(10)` | L36 |
| `cCccsssub` | `CHAR(10)` | L37 |
| `cCccssssub` | `CHAR(10)` | L38 |
| `cCsector` | `CHAR(10)` | L39 |
| `cAccmayor` | `CHAR(10)` | L40 |
| `cAccsub` | `CHAR(10)` | L41 |
| `cAccsubsub` | `CHAR(10)` | L42 |
| `cAccsssub` | `CHAR(10)` | L43 |
| `cAccssssub` | `CHAR(10)` | L44 |
| `cAsector` | `CHAR(10)` | L45 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L99 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ris_consultatransaccionb5`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultatransaccionb5.sql` |
| **LOC (1er CREATE)** | 145 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y transacción (sufijo de versión de SP)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultatransaccionb5(
  pSistema                     CHAR(10)
  pSecuencia                   CHAR(10)
  pTransaccion                 CHAR(10)
  pProducto                    CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSistema` | `CHAR(10)` | — | — |
| `pSecuencia` | `CHAR(10)` | — | — |
| `pTransaccion` | `CHAR(10)` | `transaccion`=transacción | ✅ CÓDIGO |
| `pProducto` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L23 |
| `iSqlErr` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `cSistema` | `CHAR(3)` | L29 |
| `cProducto` | `CHAR(4)` | L30 |
| `cDescProd` | `CHAR(200)` | L31 |
| `cTransaccion` | `CHAR(4)` | L32 |
| `cDescTran` | `CHAR(50)` | L33 |
| `cNaturaleza` | `CHAR(1)` | L34 |
| `iSecuencia` | `INTEGER` | L35 |
| `cCccmayor` | `CHAR(10)` | L36 |
| `cCccsub` | `CHAR(10)` | L37 |
| `cCccsubsub` | `CHAR(10)` | L38 |
| `cCccsssub` | `CHAR(10)` | L39 |
| `cCccssssub` | `CHAR(10)` | L40 |
| `cCsector` | `CHAR(10)` | L41 |
| `cAccmayor` | `CHAR(10)` | L42 |
| `cAccsub` | `CHAR(10)` | L43 |
| `cAccsubsub` | `CHAR(10)` | L44 |
| `cAccsssub` | `CHAR(10)` | L45 |
| `cAccssssub` | `CHAR(10)` | L46 |
| `cAsector` | `CHAR(10)` | L47 |
| `cDescripcionProducto` | `CHAR(200)` | L49 |
| `cDescripcionTrans` | `CHAR(200)` | L50 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_prodtran` | `bdinteg` | ⚠️ sí | SELECT | L103 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L116 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | VALIDACIÓN_NULL | `IF cDescripcionProducto IS NULL THEN` |  |
| L122 | FÓRMULA | `LET cDescProd = "N/A";` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `b5` | MODIF | sufijo de versión de SP (Bloque/Build 5) | 🟡 INFERIDO | nombre_sp |

---

## `sp_ris_consultatransacciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultatransacciones.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 35 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y transacción" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultatransacciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSistema                     CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSistema` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cNumero` | `CHAR(4)` | L10 |
| `cDescripcion` | `CHAR(50)` | L11 |
| `cNaturaleza` | `CHAR(15)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSistema = '' OR pRegistros IS NULL OR pRecuperacion IS NULL ` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L62 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ris_consultatransaccionesb5`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ris_consultatransaccionesb5.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=2 / fan_out=24 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Riesgo — módulo de gestión de riesgo crediticio y transacción (sufijo de versión de SP)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ris_consultatransaccionesb5(
  pSistema                     CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSistema` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cNumero` | `CHAR(4)` | L10 |
| `cDescripcion` | `CHAR(50)` | L11 |
| `cNaturaleza` | `CHAR(15)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ris` | ENTIDAD | Riesgo — módulo de gestión de riesgo crediticio (bdicnweb:sp | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `b5` | MODIF | sufijo de versión de SP (Bloque/Build 5) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_buscarpersonas_xml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_buscarpersonas_xml.sql` |
| **LOC (1er CREATE)** | 388 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "búsqueda/buscar ro — Rol Operativo y XML" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sw_ro_buscapersona`, `sp_registra_evento` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_buscarpersonas_xml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
  pIpUsuario                   CHAR(15)
  pMac                         CHAR(12)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             char(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |
| `pIpUsuario` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(12)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `char(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iRegistros` | `INTEGER` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |
| `cNumOficio` | `CHAR(60)` | L14 |
| `cNumExpediente` | `CHAR(60)` | L15 |
| `iTipoOficio` | `INTEGER` | L16 |
| `iInst1Enlace` | `INTEGER` | L17 |
| `iInst2Enlace` | `INTEGER` | L18 |
| `dFechaRecepcion` | `DATE` | L19 |
| `dFechaOficio` | `DATE` | L20 |
| `iIdOficio` | `INTEGER` | L21 |
| `iTipoOperacion` | `INTEGER` | L22 |
| `cNombre1` | `CHAR(60)` | L24 |
| `cNombre2` | `CHAR(26)` | L25 |
| `cApPaterno` | `CHAR(26)` | L26 |
| `cApMaterno` | `CHAR(26)` | L27 |
| `cRfc` | `CHAR(15)` | L28 |
| `sTipoPersona` | `SMALLINT` | L29 |
| `iNoRegistrosSolicPartes` | `INTEGER` | L30 |
| `iNoRegistrosSolicCuentas` | `INTEGER` | L31 |
| `iNoRegistrosSolicEspecificas` | `INTEGER` | L32 |
| `sTipoBusqueda` | `SMALLINT` | L33 |
| *…31 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_insenlace1nivel` | `bdicnweb` | no | SELECT | L155 |
| `sw_ro_encabezadoexparchxml` | `bdicnweb` | no | SELECT | L161 |
| `sw_ro_tipooficios` | `bdicnweb` | no | SELECT | L167 |
| `sw_ro_inssolic2nivel` | `bdicnweb` | no | SELECT | L185 |
| `sw_ro_solicitudpartes` | `bdicnweb` | no | SELECT | L223 |
| `sw_ro_cuentasconocidas` | `bdicnweb` | no | SELECT | L263 |
| `sw_ro_personassolicitud` | `bdicnweb` | no | SELECT | L296 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L145 |
| `sp_sw_ro_buscapersona` | `bdicnweb` | no | L234 |
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L340 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L69 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L77 | FÓRMULA | `LET iTipoOficio = 1; -- POR DEFAULT ES LA CNBV` |  |
| L139 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL OR pIpUsuario = '' OR pMac = '' OR pIdP` |  |
| L140 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L171 | VALIDACIÓN_NULL | `IF iTipoOficio IS NULL THEN` |  |
| L198 | FÓRMULA | `LET iTipoOperacion = 1;  -- Inserción de nuevo registro` |  |
| L204 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_sw_ro_ofiocioalta` |  |
| L228 | FÓRMULA | `LET cNombre2 = SUBSTR(cNombre1, CHARINDEX(' ', cNombre1) + 1);` |  |
| L241 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_sw_ro_buscaperson` |  |
| L250 | FÓRMULA | `LET iNoRegistrosSolicPartes = iNoRegistrosSolicPartes + 1;` |  |
| L273 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_sw_ro_buscaperson` |  |
| L282 | FÓRMULA | `LET iNoRegistrosSolicCuentas = iNoRegistrosSolicCuentas + 1;` |  |
| L301 | FÓRMULA | `LET cNombre2 = SUBSTR(cNombre1, CHARINDEX(' ', cNombre1) + 1);` |  |
| L316 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdicnweb:"informix".sp_sw_ro_buscaperson` |  |
| L325 | FÓRMULA | `LET iNoRegistrosSolicEspecificas = iNoRegistrosSolicEspecificas + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `buscar` | ACCION | búsqueda/buscar | 🔵 CONVENCIÓN | nombre_sp |
| `?personas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?personas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_cargaarchivooficiosxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_cargaarchivooficiosxml.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga ro — Rol Operativo, archivo, oficio y XML" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_cargaarchivooficiosxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchivoProcesar             CHAR(255)
  pRutaCarga                   CHAR(100)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pArchivoProcesar` | `CHAR(255)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `archivo`=archivo | ✅ CÓDIGO / 🔴 SINTÉTICO |
| `pRutaCarga` | `CHAR(100)` | `carga`=carga / ingresa | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCmd` | `CHAR(2000)` | L6 |
| `cArchivoTmp` | `CHAR(255)` | L7 |
| `cScriptCarga` | `CHAR(255)` | L8 |
| `cRutaInformix` | `CHAR(100)` | L9 |
| `cNombreArchivoTmp` | `CHAR(50)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bdicnweb` | `bdicnweb` | no | INSERT | L87 |
| `statistics` | `bdicnweb` | no | UPDATE | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | FÓRMULA | `LET pRutaCarga = TRIM(pRutaCarga) \|\| '/';` |  |
| L56 | FÓRMULA | `LET cCmd = "iconv -s -f UTF-8 -t IBM-1252 "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|" > "\|` |  |
| L59 | FÓRMULA | `LET cCmd = "/usr/bin/mv "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|'.iconv '\|\|TRIM(pRutaCa` |  |
| L62 | FÓRMULA | `LET cCmd = "/usr/bin/rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|'.iconv';` |  |
| L66 | FÓRMULA | `LET cCmd = "sed '/<?xml/d' "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|" \| awk '{if($1 ~ /<E` |  |
| L77 | FÓRMULA | `LET cCmd = "/usr/bin/rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|'; /usr/bin/mv '\|\|T` |  |
| L84 | FÓRMULA | `LET cCmd = "/usr/bin/rm -rf "\|\|TRIM(pRutaCarga)\|\|TRIM(pArchivoProcesar)\|\|'; /usr/bin/mv '\|\|T` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ro`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_catalogooficios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_catalogooficios.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ro — Rol Operativo, catálogo y oficio" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_catalogooficios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIdExpediente` | `INTEGER` | L8 |
| `cNoOficio` | `CHAR(60)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_encabezadoexparchxml` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `catalogo` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_consencabezadoarchivoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_consencabezadoarchivoxml.sql` |
| **LOC (1er CREATE)** | 160 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta ro — Rol Operativo, encabezado, archivo y XML" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_consencabezadoarchivoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L24 |
| `iSqlErr` | `INTEGER` | L25 |
| `iIdExpediente` | `INTEGER` | L26 |
| `cNumOficio` | `CHAR(60)` | L27 |
| `cNumExpediente` | `CHAR(60)` | L28 |
| `cSolicitudSiara` | `CHAR(60)` | L29 |
| `iFolio` | `INTEGER` | L30 |
| `dAnioOficio` | `CHAR(4)` | L31 |
| `cArea` | `CHAR(32)` | L32 |
| `iIdArea` | `CHAR(2)` | L33 |
| `cDescArea` | `CHAR(30)` | L34 |
| `dFechaPublicacion` | `CHAR(25)` | L35 |
| `iDiasPlazo` | `CHAR(2)` | L36 |
| `cNombreAutoridad` | `CHAR(60)` | L37 |
| `cNombreAutoridadEspecifica` | `CHAR(60)` | L38 |
| `cNombreSolicitante` | `CHAR(60)` | L39 |
| `cReferencia` | `CHAR(60)` | L40 |
| `cReferencia1` | `CHAR(60)` | L41 |
| `cReferencia2` | `CHAR(60)` | L42 |
| `cAseguramiento` | `CHAR(5)` | L43 |
| `cTieneAseguramiento` | `CHAR(2)` | L44 |
| `iTotalRegistros` | `INTEGER` | L45 |
| `cUsuarioInsert` | `CHAR(8)` | L46 |
| `dFechaInsert` | `CHAR(25)` | L47 |
| `iTotalPartes` | `INTEGER` | L48 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_encabezadoexparchxml` | `bdicnweb` | no | SELECT | L116 |
| `sw_ro_solicitudpartes` | `bdicnweb` | no | SELECT | L129 |
| `sw_ro_personassolicitud` | `bdicnweb` | no | SELECT | L130 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L101 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL THEN` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L143 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ro_consreportes_estatusoficios_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_consreportes_estatusoficios_totales.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes ro — Rol Operativo, estatus y oficio (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_consreportes_estatusoficios_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
  pTipoBusqueda                SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |
| `pTipoBusqueda` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIdOficio` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_encabezadoexparchxml` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL OR pTipoBusqueda IS NULL THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L49 | VALIDACIÓN_NULL | `IF iIdOficio IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00110';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L140 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_consreportestatusoficioneg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_consreportestatusoficioneg.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_consreportestatusoficioneg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `iIdExpediente` | `INTEGER` | L16 |
| `iIdSolicitudEsp` | `INTEGER` | L17 |
| `iIdOficio` | `INTEGER` | L18 |
| `iFolio` | `INTEGER` | L19 |
| `dAnioOficio` | `CHAR(4)` | L20 |
| `cNumOficio` | `CHAR(60)` | L21 |
| `cNumExpediente` | `CHAR(60)` | L22 |
| `dFechaPublicacion` | `CHAR(25)` | L23 |
| `cArea` | `CHAR(32)` | L24 |
| `iIdArea` | `CHAR(2)` | L25 |
| `cDescArea` | `CHAR(30)` | L26 |
| `iDiasPlazo` | `CHAR(2)` | L27 |
| `dFecha_Ven` | `DATE` | L28 |
| `cFechaVencimiento` | `DATE` | L29 |
| `iPersonaFisica` | `INTEGER` | L30 |
| `iPersonaMoral` | `INTEGER` | L31 |
| `iPerFisicaPartes` | `INTEGER` | L32 |
| `iPerMoralPartes` | `INTEGER` | L33 |
| `iPerFisicaSol` | `INTEGER` | L34 |
| `iPerMoralSol` | `INTEGER` | L35 |
| `iHayDatos` | `INTEGER` | L36 |
| `iRecuperacion` | `INTEGER` | L37 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_encabezadoexparchxml` | `bdicnweb` | no | SELECT | L105 |
| `sw_ro_solicitudpartes` | `bdicnweb` | no | SELECT | L115 |
| `sw_ro_personassolicitud` | `bdicnweb` | no | SELECT | L127 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L75 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL OR pRegistros IS NULL OR pRecuperacion ` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L137 | FÓRMULA | `LET iPersonaFisica = NVL(iPerFisicaPartes, 0) + NVL(iPerFisicaSol, 0);` |  |
| L138 | FÓRMULA | `LET iPersonaMoral = NVL(iPerMoralPartes, 0) + NVL(iPerMoralSol, 0);` |  |
| L140 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?tatus` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |
| `?neg` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tatus`, `?neg` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_consreportestatusoficioneg_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_consreportestatusoficioneg_totales.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta reportes ro — Rol Operativo y oficio (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_consreportestatusoficioneg_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iIdExpediente` | `INTEGER` | L7 |
| `iIdSolicitudEsp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_encabezadoexparchxml` | `bdicnweb` | no | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL THEN` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consreportes` | ACCION | consulta reportes | 🔵 CONVENCIÓN | nombre_sp |
| `?tatus` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |
| `?neg_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tatus`, `?neg_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_conssolicitudespecificaarchivoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_conssolicitudespecificaarchivoxml.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta ro — Rol Operativo, solicitudes, archivo y XML" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_conssolicitudespecificaarchivoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
  pIdSolEspecifica             INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |
| `pIdSolEspecifica` | `INTEGER` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `iSqlErr` | `INTEGER` | L17 |
| `iIdExpediente` | `INTEGER` | L18 |
| `iIdPersona` | `INTEGER` | L19 |
| `cCaracter` | `CHAR(30)` | L20 |
| `cDescTipoPersona` | `CHAR(10)` | L21 |
| `cNombre` | `CHAR(150)` | L22 |
| `cApellPaterno` | `CHAR(26)` | L23 |
| `cApellMaterno` | `CHAR(26)` | L24 |
| `cRFC` | `CHAR(15)` | L25 |
| `cRelacion` | `CHAR(50)` | L26 |
| `cDomicilio` | `CHAR(150)` | L27 |
| `cComplementarios` | `CHAR(150)` | L28 |
| `iExiste` | `INTEGER` | L29 |
| `cIndicador` | `CHAR(1)` | L30 |
| `iRecuperacion` | `INTEGER` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_personassolicitud` | `bdicnweb` | no | SELECT | L91 |
| `sw_ro_cuentasconocidas` | `bdicnweb` | no | SELECT | L97 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL OR pIdSolEspecifica IS NULL` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L108 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `solicitudes` | ENTIDAD | solicitudes (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?pecifica` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pecifica` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_conssolicitudpartesarchivoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_conssolicitudpartesarchivoxml.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta ro — Rol Operativo, solicitud, archivo y XML" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_conssolicitudpartesarchivoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iIdExpediente` | `INTEGER` | L15 |
| `iIdPersona` | `INTEGER` | L16 |
| `cCaracter` | `CHAR(30)` | L17 |
| `cDescTipoPersona` | `CHAR(10)` | L18 |
| `cNombre` | `CHAR(150)` | L19 |
| `cApellPaterno` | `CHAR(26)` | L20 |
| `cApellMaterno` | `CHAR(26)` | L21 |
| `cRFC` | `CHAR(15)` | L22 |
| `iRecuperacion` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_solicitudpartes` | `bdicnweb` | no | SELECT | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL OR pRegistros IS NULL OR pRecuperacion ` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L74 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?partes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?partes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_cuentasconocidasarchivoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_cuentasconocidasarchivoxml.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta ro — Rol Operativo, cuentas, identificador, archivo y XML" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_cuentasconocidasarchivoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
  pIdSolEspecifica             INTEGER
  pIdPersona                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pIdExpediente` | `INTEGER` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pIdSolEspecifica` | `INTEGER` | `sp`=stored procedure · `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pIdPersona` | `INTEGER` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iIdExpediente` | `INTEGER` | L10 |
| `iIdPersona` | `INTEGER` | L11 |
| `cEntidad` | `CHAR(50)` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |
| `cInstrucciones` | `CHAR(9000)` | L14 |
| `iRecuperacion` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_cuentasconocidas` | `bdicnweb` | no | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL OR pIdSolEspecifica IS NULL OR pIdPerso` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?oc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?as` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?oc`, `?as` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_extraevalor_tagxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_extraevalor_tagxml.sql` |
| **LOC (1er CREATE)** | 47 |
| **Callgraph** | ✅ fan_in=58 / fan_out=5 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "trae ro — Rol Operativo, valor y XML" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_extraevalor_tagxml(
  pIdRow                       INTEGER
  pEtiqueta                    VARCHAR(50)
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdRow` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pEtiqueta` | `VARCHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cValor` | `LVARCHAR(32739)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `oficios_xml_tmp` | `bdicnweb` | no | SELECT | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | VALIDACIÓN_NULL | `IF pIdRow IS NULL OR pEtiqueta = '' THEN` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `trae` | ACCION | trae / recupera (verbo — sp_*_trae — bdisuc) | 🟡 INFERIDO | nombre_sp |
| `valor` | ENTIDAD | valor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_tag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ro`, `?_e`, `?_tag` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_instruccionescuentasxconocerarchivoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_instruccionescuentasxconocerarchivoxml.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "insertar ro — Rol Operativo, cc — cuenta corriente, cuentas, archivo y XML" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=3 · SINTÉTICO=3 / 12 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_instruccionescuentasxconocerarchivoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | `x`=por (criterio) | 🔵 CONVENCIÓN |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIdExpediente` | `INTEGER` | L9 |
| `iIdSolicitudEsp` | `INTEGER` | L10 |
| `cInstruccionesCxC` | `CHAR (9000)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_solicitudespecifica` | `bdicnweb` | no | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL OR pRegistros IS NULL OR pRecuperacion ` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L56 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?tru` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?iones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |
| `?ocer` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tru`, `?iones`, `?ocer` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_procesaencabezadoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_procesaencabezadoxml.sql` |
| **LOC (1er CREATE)** | 236 |
| **Callgraph** | ✅ fan_in=57 / fan_out=5 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa ro — Rol Operativo, encabezado y XML" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ro_extraevalor_tagxml` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_procesaencabezadoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNumIndiceEnc` | `INTEGER` | L11 |
| `iTopeEnc` | `INTEGER` | L12 |
| `cNumOficio` | `CHAR(60)` | L13 |
| `cNumExpediente` | `CHAR(60)` | L14 |
| `cSolSiara` | `CHAR(60)` | L15 |
| `iNumFolio` | `INTEGER` | L16 |
| `cAnioOficio` | `CHAR(4)` | L17 |
| `cIdArea` | `CHAR(2)` | L18 |
| `cDescArea` | `CHAR(30)` | L19 |
| `dFechaPublicacion` | `CHAR(10)` | L20 |
| `cDiazPlazo` | `CHAR(2)` | L21 |
| `cNomAutoridad` | `CHAR(60)` | L22 |
| `cNomAutoridadEsp` | `CHAR(60)` | L23 |
| `cNomSolicitante` | `CHAR(60)` | L24 |
| `cReferencia` | `CHAR(60)` | L25 |
| `cReferencia1` | `CHAR(60)` | L26 |
| `cReferencia2` | `CHAR(60)` | L27 |
| `cAseguramiento` | `CHAR(5)` | L28 |
| `iTotalRegistros` | `INTEGER` | L29 |
| `iIdOficio` | `INTEGER` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `oficios_xml_tmp` | `bdicnweb` | no | SELECT | L85 |
| `sw_ro_encabezadoexparchxml` | `bdicnweb` | no | INSERT | L211 |
| `sw_ro_encabezadoexparchxml` | `bdicnweb` | no | SELECT | L221 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L75 |
| `sp_ro_extraevalor_tagxml` | `bdicnweb` | no | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_ro_procesararchivoxml`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_procesararchivoxml.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa ro — Rol Operativo, archivo y XML" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ro_procesaencabezadoxml`, `sp_ro_procesarsolic_partes` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_procesararchivoxml(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cNumOficio` | `CHAR(60)` | L10 |
| `iIdOficio` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `oficios_xml_tmp` | `bdicnweb` | no | SELECT | L71 |
| `oficios_xml_tmp` | `bdicnweb` | no | DELETE | L71 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_ro_procesaencabezadoxml` | `bdicnweb` | no | L42 |
| `sp_ro_procesarsolic_partes` | `bdicnweb` | no | L52 |
| `sp_ro_procesarsolic_especifica` | `bdicnweb` | no | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ''informix''.sp_ro_procesaencabezadoxml'` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP "informix".sp_ro_procesarsolic_partes';` |  |
| L65 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP "informix".sp_ro_procesarsolic_especific` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `procesa` | ACCION | procesa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `xml` | ENTIDAD | XML | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ro`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ro_procesarsolic_especifica`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ro_procesarsolic_especifica.sql` |
| **LOC (1er CREATE)** | 299 |
| **Callgraph** | ✅ fan_in=57 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa ro — Rol Operativo y solicitud (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_ro_extraevalor_tagxml` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_ro_procesarsolic_especifica(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdExpediente                INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdExpediente` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iIdSolicitudEspecifica` | `INTEGER` | L8 |
| `iIdRowMinEspecifica` | `INTEGER` | L9 |
| `iIdRowMaxEspecifica` | `INTEGER` | L10 |
| `iIdRowMin` | `INTEGER` | L11 |
| `iIdRowMax` | `INTEGER` | L12 |
| `iIdRow` | `INTEGER` | L13 |
| `iIdMinRowInstruccionesXConocer` | `INTEGER` | L14 |
| `iIdMaxRowInstruccionesXConocer` | `INTEGER` | L15 |
| `iPersonaId` | `INTEGER` | L16 |
| `cCaracter` | `CHAR(30)` | L17 |
| `cDescTipoPersona` | `CHAR(10)` | L18 |
| `cApellidoPaterno` | `CHAR(26)` | L19 |
| `cApellidoMaterno` | `CHAR(26)` | L20 |
| `cNombre` | `CHAR(150)` | L21 |
| `cRfc` | `CHAR(15)` | L22 |
| `cRelacion` | `CHAR(50)` | L23 |
| `cDomicilio` | `CHAR(150)` | L24 |
| `cComplementarios` | `CHAR(50)` | L25 |
| `cInstruccionesCuentasPorConocer` | `LVARCHAR(2000)` | L26 |
| `cEntidad` | `CHAR(50)` | L28 |
| `cCuenta` | `CHAR(20)` | L29 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `oficios_xml_tmp` | `bdicnweb` | no | SELECT | L86 |
| `sw_ro_solicitudespecifica` | `bdicnweb` | no | INSERT | L125 |
| `sw_ro_personassolicitud` | `bdicnweb` | no | INSERT | L234 |
| `sw_ro_cuentasconocidas` | `bdicnweb` | no | INSERT | L280 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L77 |
| `sp_ro_extraevalor_tagxml` | `bdicnweb` | no | L102 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L71 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdExpediente IS NULL THEN` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solic` | ENTIDAD | solicitud | 🔵 CONVENCIÓN | nombre_sp |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?ecifica` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?ecifica` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_actualizaabonoparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_actualizaabonoparam.sql` |
| **LOC (1er CREATE)** | 48 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 132 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza Servicios de Atención al Cliente — subsistema de atención en sucursal, abono y parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_actualizaabonoparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pValor                       VARCHAR(40)
  pCodigo                      CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pValor` | `VARCHAR(40)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param_abono_coppel_atm` | `bdicheq` | ⚠️ sí | UPDATE | L34 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L26 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `param` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_sac_catsucaralreporte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_catsucaralreporte.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 107 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal, catálogo y reporte (sucursal)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_catsucaralreporte(
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cSucursal` | `CHAR(40)` | L11 |
| `cNombre` | `CHAR(40)` | L12 |
| `cPlaza` | `CHAR(3)` | L13 |
| `cPais` | `CHAR(3)` | L14 |
| `cEstado` | `CHAR(2)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet 			= '00000';` |  |
| L41 | VALIDACIÓN_NULL | `IF pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L61 | FÓRMULA | `LET iNoRegistros = iNoRegistros +1;` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `?aral` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?aral` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_consulta_remesas_abonadas_tdc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consulta_remesas_abonadas_tdc.sql` |
| **LOC (1er CREATE)** | 344 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 357 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, remesas internacionales y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consulta_remesas_abonadas_tdc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L38 |
| `iSqlErr` | `INT` | L39 |
| `cTipoRemesa` | `CHAR(8)` | L41 |
| `cNumeroConfirmacion` | `CHAR(16)` | L42 |
| `cFechaPago` | `CHAR(8)` | L43 |
| `cHoraPago` | `CHAR(6)` | L44 |
| `cNombreOrdenante` | `CHAR(40)` | L45 |
| `cSegundoNombreOrdenante` | `CHAR(40)` | L46 |
| `cApellidoPaternoOrdenante` | `CHAR(40)` | L47 |
| `cApellidoMaternoOrdenante` | `CHAR(40)` | L48 |
| `cDireccionOrdenante` | `CHAR(80)` | L49 |
| `cCpOrdenante` | `CHAR(10)` | L50 |
| `cCdOrdenante` | `CHAR(40)` | L51 |
| `cEstadoOrdenante` | `CHAR(3)` | L52 |
| `cPaisOrdenante` | `CHAR(3)` | L53 |
| `cTelefonoOrdenante` | `CHAR(15)` | L54 |
| `cTipoIdOrdenante` | `CHAR(3)` | L55 |
| `cNumeroIdOrdenante` | `CHAR(3)` | L56 |
| `cPaisIdOrdenante` | `CHAR(3)` | L57 |
| `cMonedaOrdenante` | `CHAR(3)` | L58 |
| `cMontoOrigen` | `CHAR(20)` | L59 |
| `cMontoPesos` | `CHAR(20)` | L60 |
| `cNombreBeneficiario` | `CHAR(40)` | L61 |
| `cSegundoNombreBeneficiario` | `CHAR(40)` | L62 |
| `cApellidoPaternoBeneficiario` | `CHAR(40)` | L63 |
| *…11 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_bts_sdep` | `bdisac` | ⚠️ sí | SELECT | L266 |
| `sac_app_getorder` | `bdisac` | ⚠️ sí | SELECT | L306 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L131 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L142 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L311 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L320 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_abonadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_abonadas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_consulta_remesas_abonadas_tdc_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consulta_remesas_abonadas_tdc_totales.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 359 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, remesas internacionales y tarjeta de crédito (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consulta_remesas_abonadas_tdc_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iNumRegistros` | `INTEGER` | L7 |
| `iRecuperacion` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_bts_sdep` | `bdisac` | ⚠️ sí | SELECT | L45 |
| `sac_app_getorder` | `bdisac` | ⚠️ sí | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_abonadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_abonadas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_consulta_remesas_abonadas_tdc_xls`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consulta_remesas_abonadas_tdc_xls.sql` |
| **LOC (1er CREATE)** | 214 |
| **Callgraph** | ✅ fan_in=0 / fan_out=115 |
| **Deps concatenadas** | 358 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, remesas internacionales y tarjeta de crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consulta_remesas_abonadas_tdc_xls(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRutaDescarga                CHAR(150)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCmd1` | `CHAR(8000)` | L7 |
| `cCmd2` | `CHAR(8000)` | L8 |
| `cArchDescarga` | `CHAR(200)` | L9 |
| `iNoRegistros` | `INT` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `cRutaInformix` | `CHAR(100)` | L12 |
| `cUsrBin` | `CHAR(100)` | L13 |
| `cNameReport` | `CHAR(60)` | L14 |
| `ven_transacc` | `SMALLINT` | L15 |
| `bInTransaction` | `BOOLEAN` | L16 |
| `cSql` | `CHAR(8000)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pIdUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRutaDescarga ` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L73 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SUBSTR(fecha_pago,7, 2)\|\|'/'\|\|SUBSTR(fecha_pago,5, 2)\|\|'/'\|` |  |
| L193 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf "\|\|TRIM(pRutaDescarga)\|\|'queryTDC.sql';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `remesas` | ENTIDAD | remesas internacionales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_abonadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tdc` | ENTIDAD | tarjeta de crédito (TDC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ls` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_abonadas_`, `?ls` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_consultaabonoatm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consultaabonoatm.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 131 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, abono y cajero automático" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consultaabonoatm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `itotal_pagos` | `INTEGER` | L12 |
| `cconvenio` | `CHAR(40)` | L13 |
| `mimporte_suc` | `MONEY(16,2)` | L14 |
| `mcom_conv` | `MONEY(16,2)` | L15 |
| `miva_conv` | `MONEY(16,2)` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `abono` | ENTIDAD | abono / crédito | 🔵 CONVENCIÓN | nombre_sp |
| `atm` | ENTIDAD | cajero automático (ATM) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sac_consultaabonoatm_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consultaabonoatm_totales.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 130 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, abono y cajero automático (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consultaabonoatm_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iTotal` | `INTEGER` | L8 |
| `dIva` | `DECIMAL(10,3)` | L9 |
| `dCom` | `DECIMAL(10,3)` | L10 |
| `itotal_pagos` | `INTEGER` | L11 |
| `cconvenio` | `VARCHAR(40)` | L12 |
| `mimporte_suc` | `MONEY(16,2)` | L13 |
| `mcom_conv` | `MONEY(16,2)` | L14 |
| `miva_conv` | `MONEY(16,2)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sac_reporteabonoatmtmp` | `bdicnweb` | no | SELECT | L49 |
| `sc_param_abono_coppel_atm` | `bdicheq` | ⚠️ sí | SELECT | L58 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L65 |
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L68 |
| `informix` | `bdicnweb` | no | INSERT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `atm` | ENTIDAD | cajero automático (ATM) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sac_consultaabonoparam`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consultaabonoparam.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 129 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, abono y parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consultaabonoparam(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodigo                      CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodigo` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodigo` | `CHAR(4)` | L8 |
| `cValor` | `CHAR(40)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param_abono_coppel_atm` | `bdicheq` | ⚠️ sí | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `param` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_sac_consultabitacoraiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consultabitacoraiva.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 128 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal y bitácora · IVA" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consultabitacoraiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iId` | `INTEGER` | L12 |
| `iUsuario` | `INTEGER` | L13 |
| `cNombre` | `CHAR(80)` | L14 |
| `cAccion` | `CHAR(50)` | L15 |
| `dFecha` | `DATE` | L16 |
| `iRecuperacion` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_bitacora_abono_coppel_atm` | `bdicheq` | ⚠️ sí | SELECT | L56 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sac_consultareportes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consultareportes.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 106 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar Servicios de Atención al Cliente — subsistema de atención en sucursal" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consultareportes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cNombre` | `CHAR(100)` | L11 |
| `cTipo` | `CHAR(60)` | L12 |
| `dFecha` | `DATE` | L13 |
| `cHora` | `CHAR(20)` | L14 |
| `cUsuario` | `CHAR(8)` | L15 |
| `iTotal` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | FÓRMULA | `LET iTotal = iTotal+1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?tes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_consultareportes_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consultareportes_totales.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 105 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar Servicios de Atención al Cliente — subsistema de atención en sucursal (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consultareportes_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iTotal` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tes_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?tes_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_consultasucursalesfiltro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_consultasucursalesfiltro.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 104 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta Servicios de Atención al Cliente — subsistema de atención en sucursal, sucursal y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_consultasucursalesfiltro(
  pFiltro                      CHAR(2)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFiltro` | `CHAR(2)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRegistros` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cNumSucursal` | `CHAR(4)` | L8 |
| `cNombreSucursal` | `CHAR(40)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L57 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L75 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?esfilt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?esfilt`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_insertbitacoraabonoatm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_insertbitacoraabonoatm.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 127 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "insertar Servicios de Atención al Cliente — subsistema de atención en sucursal, bitácora, abono y cajero automático" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_insertbitacoraabonoatm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreUsu                   VARCHAR(80)
  pAccion                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreUsu` | `VARCHAR(80)` | — | — |
| `pAccion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cAccion` | `VARCHAR(50)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_bitacora_abono_coppel_atm` | `bdicheq` | ⚠️ sí | INSERT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?ert` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `atm` | ENTIDAD | cajero automático (ATM) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ert` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_montototal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_montototal.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 103 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal y monto (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_montototal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFinal                  DATE
  pTransaccion1                CHAR(4)
  pTransaccion2                CHAR(4)
  pSucursal                    CHAR(4)
  pCategoria                   CHAR(2)
  pConvenio                    CHAR(5)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTransaccion1` | `CHAR(4)` | `sac`=Servicios de Atención al Cliente — subsistema de atención en sucursal (ventanilla, domiciliación, abonos ATM, remesas WU); base de datos propia bdisac: con tabla sac_movimientoshistorial | ✅ CÓDIGO |
| `pTransaccion2` | `CHAR(4)` | `sac`=Servicios de Atención al Cliente — subsistema de atención en sucursal (ventanilla, domiciliación, abonos ATM, remesas WU); base de datos propia bdisac: con tabla sac_movimientoshistorial | ✅ CÓDIGO |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCategoria` | `CHAR(2)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iNoRegistros` | `INTEGER` | L8 |
| `iTotalOperacion` | `INTEGER` | L9 |
| `mMonto` | `MONEY(14,2)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificasacmontototal` | `bdicnweb` | no | SELECT | L32 |
| `sw_verificasacmontototal` | `bdicnweb` | no | DELETE | L32 |
| `sw_verificasacmontototal` | `bdicnweb` | no | INSERT | L36 |
| `sw_sac_montototal` | `bdicnweb` | no | SELECT | L60 |
| `sw_sac_montototal` | `bdicnweb` | no | DELETE | L60 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L68 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L75 |
| `sw_sac_montototal` | `bdicnweb` | no | INSERT | L91 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `monto` | ENTIDAD | monto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sac_repabonoatm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_repabonoatm.sql` |
| **LOC (1er CREATE)** | 206 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 126 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte Servicios de Atención al Cliente — subsistema de atención en sucursal, abono y cajero automático" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_repabonoatm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pRutaDescarga                CHAR(100)
  pCuentaCheques               CHAR(30)
  pCuentaContable              CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pCuentaCheques` | `CHAR(30)` | — | — |
| `pCuentaContable` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(15)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cFecha1` | `CHAR(10)` | L16 |
| `cFecha2` | `CHAR(10)` | L17 |
| `cFecha3` | `VARCHAR(25)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L79 |
| `sw_sac_reporteabonoatmtmp` | `bdicnweb` | no | SELECT | L87 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L68 | FÓRMULA | `LET cFecha1= LPAD(DAY(pFechaInicial),2,0)\|\|'/'\|\|LPAD(MONTH(pFechaInicial),2,0)\|\|'/'\|\|YEAR(pF` |  |
| L74 | FÓRMULA | `LET cFecha2= LPAD(DAY(pFechaFinal),2,0)\|\|'/'\|\|LPAD(MONTH(pFechaFinal),2,0)\|\|'/'\|\|YEAR(pFecha` |  |
| L75 | FÓRMULA | `LET cFecha3= cFecha1\|\|'-'\|\|cFecha2;` |  |
| L81 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT ' ',' ',' ','REPORTE TOTAL DE COBRANZA - CONCILIACION DE PA` |  |
| L93 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'ABONOS COPPEL ATM','',total_pagos::CHAR(20), importe_suc::` | 🔴 MONEY/aritmética financiera |
| L95 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT ' ','TOTAL DE COBRANZA',total_pagos::CHAR(20), importe_suc:` | 🔴 MONEY/aritmética financiera |
| L103 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT '"\|\|cFecha3\|\|"',' ',' ', (importe_suc-com_conv-iva_conv` | 🔴 MONEY/aritmética financiera |
| L105 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'TOTALES',' ',' ', (importe_suc-com_conv-iva_conv)::CHAR(20` | 🔴 MONEY/aritmética financiera |
| L115 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L146 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L151 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L160 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L169 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `abono` | ENTIDAD | abono / crédito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `atm` | ENTIDAD | cajero automático (ATM) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sac_repbitacoraiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_repbitacoraiva.sql` |
| **LOC (1er CREATE)** | 189 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 125 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "reporte Servicios de Atención al Cliente — subsistema de atención en sucursal y bitácora · IVA" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_repbitacoraiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRutaDescarga                CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(15)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepbitacoraiva` | `bdicnweb` | no | SELECT | L74 |
| `sw_verificastatusrepbitacoraiva` | `bdicnweb` | no | INSERT | L78 |
| `systables` | `bdicnweb` | no | SELECT | L83 |
| `sc_bitacora_abono_coppel_atm` | `bdicheq` | ⚠️ sí | SELECT | L85 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L85 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT id::CHAR(10), usuario_modifica::CHAR(11),nombre_usuario_mod` | 🔴 MONEY/aritmética financiera |
| L94 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L125 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L130 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L139 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L148 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `iva` | REG | IVA (impuesto — SAT) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sac_reportedetalletransucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_reportedetalletransucursal.sql` |
| **LOC (1er CREATE)** | 380 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 102 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal, reporte, detalle y [polisemia] Transferencia" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_reportedetalletransucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L18 |
| `iTotal` | `INTEGER` | L19 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L20 |
| `cNombre` | `CHAR(30)` | L21 |
| `pIdMensaje` | `CHAR(10)` | L22 |
| `cUsuario` | `CHAR(8)` | L23 |
| `cFolioSuc` | `CHAR(16)` | L24 |
| `cNumcategoria` | `CHAR(2)` | L25 |
| `cNumconvenio` | `CHAR(3)` | L26 |
| `cNomconvenio` | `CHAR(40)` | L27 |
| `cReferencia1` | `CHAR(20)` | L28 |
| `cReferencia2` | `CHAR(20)` | L29 |
| `mImpComisionConvenio` | `MONEY(16,2)` | L30 |
| *…8 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesacdetalletranssucbtstmp` | `bdicnweb` | no | SELECT | L124 |
| `sw_cb_reportesacdetalletranssucbtstmp` | `bdicnweb` | no | DELETE | L124 |
| `sw_cb_reportesacdetalletranssucapptmp` | `bdicnweb` | no | SELECT | L128 |
| `sw_cb_reportesacdetalletranssucapptmp` | `bdicnweb` | no | DELETE | L128 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L136 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L144 |
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L152 |
| `sw_cb_reportesacdetalletranssucbtstmp` | `bdicnweb` | no | INSERT | L170 |
| `sw_cb_reportesacdetalletranssucapptmp` | `bdicnweb` | no | INSERT | L175 |
| `systables` | `bdicnweb` | no | SELECT | L233 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L351 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L84 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L113 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L224 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L233 | FÓRMULA | `LET cCmd1 ="SELECT ' ',' ',' ',' ',' ',' ',' ',' ',' ',' ','FECHA:','"\|\|TO_CHAR(dFechaHoy, '%d/%m/` |  |
| L240 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIOS:','''"\|\|pConvenio\|\|"',' ','SUCURSAL:','''"\|` |  |
| L242 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIOS:','''"\|\|pConvenio\|\|"',' ','SUCURSAL:','TODAS` |  |
| L245 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'USUARIO','SECUENCIA','CONVENIO','REFERENCIA 1','REFERENCIA` | 🔴 MONEY/aritmética financiera |
| L268 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L302 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L307 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L316 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L325 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `?ucursal` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ucursal` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_reportesconciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_reportesconciliacion.sql` |
| **LOC (1er CREATE)** | 553 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 101 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación Servicios de Atención al Cliente — subsistema de atención en sucursal y reportes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_reportesconciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L18 |
| `dDia` | `DATE` | L19 |
| `cFormaPago` | `CHAR(20)` | L20 |
| `mCargoPagoRemesa` | `MONEY(16,2)` | L21 |
| `mSaldoCuentaBTSWU` | `MONEY(16,2)` | L22 |
| `cFolioOperacion` | `CHAR(16)` | L23 |
| `cClaveEnvio` | `CHAR(11)` | L24 |
| `cSucursal` | `CHAR(4)` | L25 |
| `cCajero` | `CHAR(8)` | L26 |
| `cImportePag` | `CHAR(20)` | L27 |
| `iTotal` | `INTEGER` | L28 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L29 |
| `cNombre` | `CHAR(30)` | L30 |
| *…8 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesacconciliacionwutmp` | `bdicnweb` | no | SELECT | L129 |
| `sw_cb_reportesacconciliacionwutmp` | `bdicnweb` | no | DELETE | L129 |
| `sw_cb_reportesacconciliacionovtmp` | `bdicnweb` | no | SELECT | L133 |
| `sw_cb_reportesacconciliacionovtmp` | `bdicnweb` | no | DELETE | L133 |
| `sw_cb_reportesacconciliacionvgtmp` | `bdicnweb` | no | SELECT | L137 |
| `sw_cb_reportesacconciliacionvgtmp` | `bdicnweb` | no | DELETE | L137 |
| `sw_cb_reportesacconciliacionbtstmp` | `bdicnweb` | no | SELECT | L141 |
| `sw_cb_reportesacconciliacionbtstmp` | `bdicnweb` | no | DELETE | L141 |
| `sw_cb_reportesacconciliacionapptmp` | `bdicnweb` | no | SELECT | L145 |
| `sw_cb_reportesacconciliacionapptmp` | `bdicnweb` | no | DELETE | L145 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L148 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L148 |
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L154 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L170 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L176 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L202 |
| `sw_cb_reportesacconciliacionwutmp` | `bdicnweb` | no | INSERT | L214 |
| `sw_cb_reportesacconciliacionovtmp` | `bdicnweb` | no | INSERT | L219 |
| `sw_cb_reportesacconciliacionvgtmp` | `bdicnweb` | no | INSERT | L224 |
| `sw_cb_reportesacconciliacionbtstmp` | `bdicnweb` | no | INSERT | L297 |
| `sw_cb_reportesacconciliacionapptmp` | `bdicnweb` | no | INSERT | L366 |
| `systables` | `bdicnweb` | no | SELECT | L394 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L537 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L82 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L252 | FÓRMULA | `LET cImportePag = CAST(vsImportePago + vsImportePago_old AS CHAR (20));` | 🔴 MONEY/aritmética financiera |
| L321 | FÓRMULA | `LET cImportePag = CAST(vsImportePago + vsImportePago_old AS CHAR (20));` | 🔴 MONEY/aritmética financiera |
| L386 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L394 | FÓRMULA | `LET cCmd1 ="SELECT ' ',' ','SISTEMA DE ADMINISTRACION DE CONVENIOS',' ',' ',' ','FECHA:','"\|\|TO_CH` |  |
| L414 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` |  |
| L416 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` |  |
| L418 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` |  |
| L420 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` |  |
| L422 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` |  |
| L446 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L480 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L485 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L494 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L503 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `conciliacion` | ACCION | conciliación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sac_reportesdiario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_reportesdiario.sql` |
| **LOC (1er CREATE)** | 570 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal y reportes (diario)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_reportesdiario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPeriodo                     DATE
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPeriodo` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L18 |
| `dDia` | `DATE` | L19 |
| `cNum_confirmacion` | `CHAR(16)` | L20 |
| `mImporte` | `MONEY(16,2)` | L21 |
| `cForma_pago` | `CHAR (20)` | L22 |
| `cFolio_op` | `CHAR (16)` | L23 |
| `cSucursal` | `CHAR (5)` | L24 |
| `cCajero` | `CHAR (8)` | L25 |
| `cNom_benef` | `CHAR (120)` | L26 |
| `iTotal` | `INTEGER` | L27 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L28 |
| `cNombre` | `CHAR(30)` | L29 |
| `pIdMensaje` | `CHAR(10)` | L30 |
| *…5 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesacdiariowutmp` | `bdicnweb` | no | SELECT | L128 |
| `sw_cb_reportesacdiariowutmp` | `bdicnweb` | no | DELETE | L128 |
| `sw_cb_reportesacdiarioovtmp` | `bdicnweb` | no | SELECT | L132 |
| `sw_cb_reportesacdiarioovtmp` | `bdicnweb` | no | DELETE | L132 |
| `sw_cb_reportesacdiariovgtmp` | `bdicnweb` | no | SELECT | L136 |
| `sw_cb_reportesacdiariovgtmp` | `bdicnweb` | no | DELETE | L136 |
| `sw_cb_reportesacdiariobtstmp` | `bdicnweb` | no | SELECT | L140 |
| `sw_cb_reportesacdiariobtstmp` | `bdicnweb` | no | DELETE | L140 |
| `sw_cb_reportesacdiarioapptmp` | `bdicnweb` | no | SELECT | L144 |
| `sw_cb_reportesacdiarioapptmp` | `bdicnweb` | no | DELETE | L144 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L153 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L165 |
| `sw_cb_reportesacdiariowutmp` | `bdicnweb` | no | INSERT | L214 |
| `sw_cb_reportesacdiarioovtmp` | `bdicnweb` | no | INSERT | L219 |
| `sw_cb_reportesacdiariovgtmp` | `bdicnweb` | no | INSERT | L224 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L274 |
| `sw_cb_reportesacdiariobtstmp` | `bdicnweb` | no | INSERT | L282 |
| `sw_cb_reportesacdiarioapptmp` | `bdicnweb` | no | INSERT | L336 |
| `systables` | `bdicnweb` | no | SELECT | L379 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L539 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L81 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L117 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L230 | FÓRMULA | `LET vCuenta = vCuenta + 1;` |  |
| L287 | FÓRMULA | `LET vCuenta = vCuenta + 1;` |  |
| L340 | FÓRMULA | `LET vCuenta = vCuenta + 1;` |  |
| L371 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L379 | FÓRMULA | `LET cCmd1 ="SELECT ' ',' ','SISTEMA DE ADMINISTRACION DE CONVENIOS ',' ',' ',' ','FECHA:','"\|\|TO_C` |  |
| L393 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIO:','''"\|\|pConvenio\|\|"','SUCURSAL:TODAS',' ','R` |  |
| L399 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` | 🔴 MONEY/aritmética financiera |
| L401 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` | 🔴 MONEY/aritmética financiera |
| L403 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` | 🔴 MONEY/aritmética financiera |
| L405 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` | 🔴 MONEY/aritmética financiera |
| L407 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(dia),2,0)\|\|'/'\|\|LPAD(MONTH(dia),2,0)\|\|'/'\|\` | 🔴 MONEY/aritmética financiera |
| L431 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L465 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L470 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L479 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L488 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `diario` | MODIF | diario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sac_reportesedocuenta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_reportesedocuenta.sql` |
| **LOC (1er CREATE)** | 1002 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 100 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal, reportes y Estado de Cuenta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_reportesedocuenta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L17 |
| `iTotal` | `INTEGER` | L18 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L19 |
| `cNombre` | `CHAR(45)` | L20 |
| `pIdMensaje` | `CHAR(10)` | L21 |
| `dFechaIni` | `DATE` | L23 |
| `dFechaFin` | `DATE` | L24 |
| `cFechaIni` | `CHAR(10)` | L25 |
| `cFechaAnt` | `CHAR(10)` | L26 |
| `cFechaParam` | `CHAR(10)` | L27 |
| `dFechaParam` | `DATE` | L28 |
| `cAnioMes` | `CHAR(6)` | L29 |
| `cAnioMesAnt` | `CHAR(6)` | L30 |
| *…32 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesacedoctawutmp` | `bdicnweb` | no | SELECT | L179 |
| `sw_cb_reportesacedoctawutmp` | `bdicnweb` | no | DELETE | L179 |
| `sw_cb_reportesacedoctaovtmp` | `bdicnweb` | no | SELECT | L183 |
| `sw_cb_reportesacedoctaovtmp` | `bdicnweb` | no | DELETE | L183 |
| `sw_cb_reportesacedoctavgtmp` | `bdicnweb` | no | SELECT | L187 |
| `sw_cb_reportesacedoctavgtmp` | `bdicnweb` | no | DELETE | L187 |
| `sw_cb_reportesacedoctabtstmp` | `bdicnweb` | no | SELECT | L191 |
| `sw_cb_reportesacedoctabtstmp` | `bdicnweb` | no | DELETE | L191 |
| `sw_cb_reportesacedoctaapptmp` | `bdicnweb` | no | SELECT | L195 |
| `sw_cb_reportesacedoctaapptmp` | `bdicnweb` | no | DELETE | L195 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L203 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L211 |
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L222 |
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L226 |
| `sc_sdodiarioc` | `bdicheq` | ⚠️ sí | SELECT | L249 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L386 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L388 |
| `sw_cb_reportesacedoctawutmp` | `bdicnweb` | no | INSERT | L402 |
| `sw_cb_reportesacedoctaovtmp` | `bdicnweb` | no | INSERT | L407 |
| `sw_cb_reportesacedoctavgtmp` | `bdicnweb` | no | INSERT | L412 |
| `sw_cb_reportesacedoctabtstmp` | `bdicnweb` | no | INSERT | L612 |
| `sw_cb_reportesacedoctaapptmp` | `bdicnweb` | no | INSERT | L809 |
| `systables` | `bdicnweb` | no | SELECT | L840 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L985 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L133 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L168 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L240 | FÓRMULA | `LET cFechaAnt = dFechaIni - INTERVAL (1) DAY TO DAY;` |  |
| L274 | FÓRMULA | `LET cAnioMes2 = Year(dFechaIni)-1 \|\| Month(dFechaIni - 1 units month);` |  |
| L418 | FÓRMULA | `LET dFechaIni = dFechaIni + INTERVAL (1) DAY TO DAY;` |  |
| L447 | FÓRMULA | `LET vsFechaAnt = vdFechaIni - INTERVAL (1) DAY TO DAY;` |  |
| L616 | FÓRMULA | `LET vdFechaIni = vdFechaIni + INTERVAL (1) DAY TO DAY;` |  |
| L644 | FÓRMULA | `LET vsFechaAnt = vdFechaIni - INTERVAL (1) DAY TO DAY;` |  |
| L813 | FÓRMULA | `LET vdFechaIni = vdFechaIni + INTERVAL (1) DAY TO DAY;` |  |
| L832 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L840 | FÓRMULA | `LET cCmd1 ="SELECT ' ',' ','SISTEMA DE ADMINISTRACION DE CONVENIOS ',' ',' ','FECHA:','"\|\|TO_CHAR(` |  |
| L854 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIO:','''"\|\|pConvenio\|\|"','SUCURSAL:TODAS','RANGO` |  |
| L894 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L928 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L933 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L942 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L951 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `edocuenta` | ENTIDAD | Estado de Cuenta (variante ortográfica de edocta — bdicheq/b | 🟡 INFERIDO | nombre_sp |

---

## `sp_sac_reportesmensual`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_reportesmensual.sql` |
| **LOC (1er CREATE)** | 418 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 99 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal y reportes (mensual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_reportesmensual(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pPeriodo                     DATE
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pPeriodo` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L18 |
| `iDia` | `INTEGER` | L19 |
| `iTotOperaciones` | `INTEGER` | L20 |
| `mMonto` | `MONEY(16,2)` | L21 |
| `iTotal` | `INTEGER` | L22 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L23 |
| `cNombre` | `CHAR(30)` | L24 |
| `pIdMensaje` | `CHAR(10)` | L25 |
| `cCategoria` | `CHAR(2)` | L26 |
| `cConvenio` | `CHAR(3)` | L27 |
| `dFechaIni` | `DATE` | L28 |
| `dFechaFin` | `DATE` | L29 |
| `cNombreReporteHist` | `CHAR(100)` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesacmensualwutmp` | `bdicnweb` | no | SELECT | L113 |
| `sw_cb_reportesacmensualwutmp` | `bdicnweb` | no | DELETE | L113 |
| `sw_cb_reportesacmensualovtmp` | `bdicnweb` | no | SELECT | L117 |
| `sw_cb_reportesacmensualovtmp` | `bdicnweb` | no | DELETE | L117 |
| `sw_cb_reportesacmensualvgtmp` | `bdicnweb` | no | SELECT | L121 |
| `sw_cb_reportesacmensualvgtmp` | `bdicnweb` | no | DELETE | L121 |
| `sw_cb_reportesacmensualbtstmp` | `bdicnweb` | no | SELECT | L125 |
| `sw_cb_reportesacmensualbtstmp` | `bdicnweb` | no | DELETE | L125 |
| `sw_cb_reportesacmensualapptmp` | `bdicnweb` | no | SELECT | L129 |
| `sw_cb_reportesacmensualapptmp` | `bdicnweb` | no | DELETE | L129 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L137 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L145 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L158 |
| `sw_cb_reportesacmensualwutmp` | `bdicnweb` | no | INSERT | L166 |
| `sw_cb_reportesacmensualovtmp` | `bdicnweb` | no | INSERT | L171 |
| `sw_cb_reportesacmensualvgtmp` | `bdicnweb` | no | INSERT | L176 |
| `sw_cb_reportesacmensualbtstmp` | `bdicnweb` | no | INSERT | L201 |
| `sw_cb_reportesacmensualapptmp` | `bdicnweb` | no | INSERT | L225 |
| `systables` | `bdicnweb` | no | SELECT | L258 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L401 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L67 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L102 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L150 | FÓRMULA | `LET dFechaIni = MONTH(pPeriodo) \|\| '/01/' \|\| YEAR(pPeriodo);` |  |
| L151 | FÓRMULA | `LET dFechaFin = dFechaIni + INTERVAL (1) MONTH TO MONTH;` |  |
| L181 | FÓRMULA | `LET dFechaIni = dFechaIni + INTERVAL (1) DAY TO DAY;` |  |
| L187 | FÓRMULA | `LET dFechaIni = MONTH(pPeriodo) \|\| '/01/' \|\| YEAR(pPeriodo);` |  |
| L188 | FÓRMULA | `LET dFechaFin = dFechaIni + INTERVAL (1) MONTH TO MONTH;` |  |
| L204 | FÓRMULA | `LET dFechaIni = dFechaIni + INTERVAL (1) DAY TO DAY;` |  |
| L211 | FÓRMULA | `LET dFechaIni = MONTH(pPeriodo) \|\| '/01/' \|\| YEAR(pPeriodo);` |  |
| L212 | FÓRMULA | `LET dFechaFin = dFechaIni + INTERVAL (1) MONTH TO MONTH;` |  |
| L228 | FÓRMULA | `LET dFechaIni = dFechaIni + INTERVAL (1) DAY TO DAY;` |  |
| L248 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L252 | FÓRMULA | `LET dFechaIni = MONTH(pPeriodo) \|\| '/01/' \|\| YEAR(pPeriodo);` |  |
| L253 | FÓRMULA | `LET dFechaFin = dFechaIni + INTERVAL (1) MONTH TO MONTH;` |  |
| L254 | FÓRMULA | `LET dFechaFin = dFechaFin - INTERVAL (1) DAY TO DAY;` |  |
| L258 | FÓRMULA | `LET cCmd1 ="SELECT ' ',' ','SISTEMA DE ADMINISTRACION DE CONVENIOS ',' ',' ','FECHA:','"\|\|TO_CHAR(` |  |
| L272 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIO:','''"\|\|pConvenio\|\|"','SUCURSAL:TODAS','RANGO` |  |
| L310 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L344 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L349 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L358 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L367 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mensual` | MODIF | mensual | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sac_reportesmontototal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_reportesmontototal.sql` |
| **LOC (1er CREATE)** | 419 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 98 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal, reportes y monto (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_reportesmontototal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pTransaccion1                CHAR(4)
  pTransaccion2                CHAR(4)
  pSucursal                    CHAR(4)
  pCategoria                   CHAR(2)
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTransaccion1` | `CHAR(4)` | `sac`=Servicios de Atención al Cliente — subsistema de atención en sucursal (ventanilla, domiciliación, abonos ATM, remesas WU); base de datos propia bdisac: con tabla sac_movimientoshistorial | 🟡 INFERIDO |
| `pTransaccion2` | `CHAR(4)` | `sac`=Servicios de Atención al Cliente — subsistema de atención en sucursal (ventanilla, domiciliación, abonos ATM, remesas WU); base de datos propia bdisac: con tabla sac_movimientoshistorial | 🟡 INFERIDO |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCategoria` | `CHAR(2)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L18 |
| `cSucursal` | `CHAR(44)` | L19 |
| `cCajaGeneral` | `CHAR(45)` | L20 |
| `cEntidad` | `CHAR(30)` | L21 |
| `iTotalOperacion` | `INTEGER` | L22 |
| `mMonto` | `MONEY(14,2)` | L23 |
| `iTotal` | `INTEGER` | L24 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L25 |
| `cNombre` | `CHAR(45)` | L26 |
| `pIdMensaje` | `CHAR(10)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesacmtototwutmp` | `bdicnweb` | no | SELECT | L107 |
| `sw_cb_reportesacmtototwutmp` | `bdicnweb` | no | DELETE | L107 |
| `sw_cb_reportesacmtototovtmp` | `bdicnweb` | no | SELECT | L111 |
| `sw_cb_reportesacmtototovtmp` | `bdicnweb` | no | DELETE | L111 |
| `sw_cb_reportesacmtototvgtmp` | `bdicnweb` | no | SELECT | L115 |
| `sw_cb_reportesacmtototvgtmp` | `bdicnweb` | no | DELETE | L115 |
| `sw_cb_reportesacmtototbtstmp` | `bdicnweb` | no | SELECT | L119 |
| `sw_cb_reportesacmtototbtstmp` | `bdicnweb` | no | DELETE | L119 |
| `sw_cb_reportesacmtototapptmp` | `bdicnweb` | no | SELECT | L123 |
| `sw_cb_reportesacmtototapptmp` | `bdicnweb` | no | DELETE | L123 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L126 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L126 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L134 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L136 |
| `sw_cb_reportesacmtototwutmp` | `bdicnweb` | no | INSERT | L145 |
| `sw_cb_reportesacmtototovtmp` | `bdicnweb` | no | INSERT | L148 |
| `sw_cb_reportesacmtototvgtmp` | `bdicnweb` | no | INSERT | L151 |
| `sw_cb_reportesacmtototbtstmp` | `bdicnweb` | no | INSERT | L154 |
| `sw_cb_reportesacmtototapptmp` | `bdicnweb` | no | INSERT | L157 |
| `systables` | `bdicnweb` | no | SELECT | L239 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L393 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L61 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L96 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L228 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L250 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'PERIODO DEL:','"\|\|TO_CHAR(pFechaIni, '%d/%m/%Y')\|\|"','` |  |
| L256 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT '"\|\|TO_CHAR(pFechaIni, '%d/%m/%Y')\|\|"','"\|\|TO_CHAR(pF` | 🔴 MONEY/aritmética financiera |
| L258 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT '"\|\|TO_CHAR(pFechaIni, '%d/%m/%Y')\|\|"','"\|\|TO_CHAR(pF` | 🔴 MONEY/aritmética financiera |
| L260 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT '"\|\|TO_CHAR(pFechaIni, '%d/%m/%Y')\|\|"','"\|\|TO_CHAR(pF` | 🔴 MONEY/aritmética financiera |
| L262 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT '"\|\|TO_CHAR(pFechaIni, '%d/%m/%Y')\|\|"','"\|\|TO_CHAR(pF` | 🔴 MONEY/aritmética financiera |
| L264 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT '"\|\|TO_CHAR(pFechaIni, '%d/%m/%Y')\|\|"','"\|\|TO_CHAR(pF` | 🔴 MONEY/aritmética financiera |
| L298 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L332 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L337 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L346 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L355 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monto` | ENTIDAD | monto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sac_reportesremnoconciliadas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_reportesremnoconciliadas.sql` |
| **LOC (1er CREATE)** | 1248 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 97 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación Servicios de Atención al Cliente — subsistema de atención en sucursal, reportes y remesa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_reportesremnoconciliadas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L18 |
| `iTotal` | `INTEGER` | L19 |
| `iTotal2` | `INTEGER` | L20 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L21 |
| `cNombre` | `CHAR(45)` | L22 |
| `pIdMensaje` | `CHAR(10)` | L23 |
| `cDiferencia` | `CHAR(16)` | L25 |
| `dFechaIni` | `DATE` | L26 |
| `dFechaFin` | `DATE` | L27 |
| `cReferencia` | `CHAR(11)` | L28 |
| `cCuentaPrestadora` | `CHAR(11)` | L29 |
| `iSumaServicios` | `INTEGER` | L30 |
| `iCantidadPagos` | `INTEGER` | L31 |
| *…36 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesacnoconcilwutmp` | `bdicnweb` | no | SELECT | L192 |
| `sw_cb_reportesacnoconcilwutmp` | `bdicnweb` | no | DELETE | L192 |
| `sw_cb_reportesacnoconcilrevwutmp` | `bdicnweb` | no | SELECT | L193 |
| `sw_cb_reportesacnoconcilrevwutmp` | `bdicnweb` | no | DELETE | L193 |
| `sw_cb_reportesacnoconcilovtmp` | `bdicnweb` | no | SELECT | L197 |
| `sw_cb_reportesacnoconcilovtmp` | `bdicnweb` | no | DELETE | L197 |
| `sw_cb_reportesacnoconcilrevovtmp` | `bdicnweb` | no | SELECT | L198 |
| `sw_cb_reportesacnoconcilrevovtmp` | `bdicnweb` | no | DELETE | L198 |
| `sw_cb_reportesacnoconcilvgtmp` | `bdicnweb` | no | SELECT | L202 |
| `sw_cb_reportesacnoconcilvgtmp` | `bdicnweb` | no | DELETE | L202 |
| `sw_cb_reportesacnoconcilrevvgtmp` | `bdicnweb` | no | SELECT | L203 |
| `sw_cb_reportesacnoconcilrevvgtmp` | `bdicnweb` | no | DELETE | L203 |
| `sw_cb_reportesacnoconcilbtstmp` | `bdicnweb` | no | SELECT | L207 |
| `sw_cb_reportesacnoconcilbtstmp` | `bdicnweb` | no | DELETE | L207 |
| `sw_cb_reportesacnoconcilrevbtstmp` | `bdicnweb` | no | SELECT | L208 |
| `sw_cb_reportesacnoconcilrevbtstmp` | `bdicnweb` | no | DELETE | L208 |
| `sw_cb_reportesacnoconcilapptmp` | `bdicnweb` | no | SELECT | L212 |
| `sw_cb_reportesacnoconcilapptmp` | `bdicnweb` | no | DELETE | L212 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L221 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L229 |
| `sac_wu_remesasnoconciliadas` | `bdisac` | ⚠️ sí | SELECT | L245 |
| `sw_cb_reportesacnoconcilwutmp` | `bdicnweb` | no | INSERT | L252 |
| `sw_cb_reportesacnoconcilovtmp` | `bdicnweb` | no | INSERT | L257 |
| `sw_cb_reportesacnoconcilvgtmp` | `bdicnweb` | no | INSERT | L262 |
| `sw_cb_reportesacnoconcilrevwutmp` | `bdicnweb` | no | INSERT | L290 |
| `sw_cb_reportesacnoconcilrevovtmp` | `bdicnweb` | no | INSERT | L294 |
| `sw_cb_reportesacnoconcilrevvgtmp` | `bdicnweb` | no | INSERT | L298 |
| `sac_servicios_paso` | `bdisac` | ⚠️ sí | SELECT | L319 |
| `sac_cheques_paso` | `bdisac` | ⚠️ sí | SELECT | L325 |
| `sac_abono_paso` | `bdisac` | ⚠️ sí | SELECT | L330 |
| `sac_btscaja_paso` | `bdisac` | ⚠️ sí | SELECT | L335 |
| `sw_cb_reportesacnoconcilbtstmp` | `bdicnweb` | no | INSERT | L344 |
| `sac_conciliacionbts_paso` | `bdisac` | ⚠️ sí | SELECT | L347 |
| `sac_conciliacionbts_paso` | `bdisac` | ⚠️ sí | DELETE | L347 |
| `sac_conciliacionbts_paso` | `bdisac` | ⚠️ sí | INSERT | L350 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L618 |
| `sac_conciliacionrev_paso` | `bdisac` | ⚠️ sí | SELECT | L648 |
| `sac_conciliacionrev_paso` | `bdisac` | ⚠️ sí | DELETE | L648 |
| `sac_fechas` | `bdisac` | ⚠️ sí | SELECT | L652 |
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L657 |
| `sac_serviciosrev_paso` | `bdisac` | ⚠️ sí | SELECT | L669 |
| `sac_chequesrev_paso` | `bdisac` | ⚠️ sí | SELECT | L679 |
| `sac_btsrevi_paso` | `bdisac` | ⚠️ sí | SELECT | L687 |
| `sw_cb_reportesacnoconcilrevbtstmp` | `bdicnweb` | no | INSERT | L695 |
| `sac_conciliacionrev_paso` | `bdisac` | ⚠️ sí | INSERT | L704 |
| `sac_app_remesasnoconciliadas` | `bdisac` | ⚠️ sí | SELECT | L1020 |
| `sw_cb_reportesacnoconcilapptmp` | `bdicnweb` | no | INSERT | L1024 |
| `systables` | `bdicnweb` | no | SELECT | L1060 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L1221 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L146 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L181 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L172 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L268 | FÓRMULA | `LET dFechaIni = dFechaIni + INTERVAL(1) DAY TO DAY;` |  |
| L304 | FÓRMULA | `LET dFechaIni = dFechaIni + INTERVAL(1) DAY TO DAY;` |  |
| L338 | FÓRMULA | `LET sumapgssac = iCantidadPagosPAYC + iCantidadPagosPAYI;` |  |
| L363 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L379 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L401 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L418 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L441 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L458 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L472 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L488 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L501 | FÓRMULA | `LET iAcumulado = iAcumulado + 1;` |  |
| L518 | FÓRMULA | `LET iCount = iCount + iSumaServicios ;` |  |
| L520 | FÓRMULA | `LET iCount = iCount + 1;` |  |
| L634 | FÓRMULA | `LET dFechaIni          = dFechaIni + INTERVAL(1) DAY TO DAY;` |  |
| L877 | FÓRMULA | `LET iCount = iCount + iSumaServicios;` |  |
| L879 | FÓRMULA | `LET iCount = iCount + 1;` |  |
| L998 | FÓRMULA | `LET dFechaIni = dFechaIni + INTERVAL(1) DAY TO DAY;` |  |
| L1028 | FÓRMULA | `LET dFechaIni = dFechaIni + INTERVAL(1) DAY TO DAY;` |  |
| L1051 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L1060 | FÓRMULA | `LET cCmd1 ="SELECT ' ',' ',' ','SISTEMA DE ADMINISTRACION DE CONVENIOS ',' ',' ',' ','FECHA:','"\|\|` |  |
| L1064 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT ' ',' ',' ',' ',' ','RANGO DE FECHAS DEL:','"\|\|TO_CHAR(pF` |  |
| L1068 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT unirefnum::CHAR(20),refnum::CHAR(30),nnumber::CHAR(15),r_de` |  |
| L1072 | FÓRMULA | `LET cCmd1 ="SELECT ' ','SISTEMA DE ADMINISTRACION DE CONVENIOS ',' ','FECHA:','"\|\|TO_CHAR(dFechaHo` |  |
| L1084 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT  ' ','RANGO DE FECHAS DEL:','"\|\|TO_CHAR(pFechaIni, '%d/%m` |  |
| L1090 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(fecha_ini),2,0)\|\|'/'\|\|LPAD(MONTH(fecha_ini),2,` |  |
| L1092 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(fecha_ini),2,0)\|\|'/'\|\|LPAD(MONTH(fecha_ini),2,` |  |
| L1094 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT LPAD(DAY(fecha_ini),2,0)\|\|'/'\|\|LPAD(MONTH(fecha_ini),2,` |  |
| | *…10 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rem` | ENTIDAD | remesa (forma corta) | 🟡 INFERIDO | nombre_sp |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `concilia` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `?das` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?no`, `?das` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_sacreporteconciliacionconveniosucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_sacreporteconciliacionconveniosucursal.sql` |
| **LOC (1er CREATE)** | 596 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 95 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación Servicios de Atención al Cliente — subsistema de atención en sucursal, reporte, convenio y sucursal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=6 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_sacreporteconciliacionconveniosucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |
| `pConvenio` | `CHAR(5)` | `convenio`=convenio (nómina/empresarial) | ✅ CÓDIGO |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L18 |
| `iTotal` | `INTEGER` | L19 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L20 |
| `cNombre` | `CHAR(30)` | L21 |
| `pIdMensaje` | `CHAR(10)` | L22 |
| `cNumcategoria` | `CHAR(2)` | L24 |
| `cIdSucursal` | `CHAR(4)` | L25 |
| `cNumconvenio` | `CHAR(3)` | L26 |
| `cNomconvenio` | `CHAR(40)` | L27 |
| `mImpComisionConvenio` | `MONEY(16,2)` | L28 |
| `mIVAComisionConvenio` | `MONEY(16,2)` | L29 |
| `mImpComisionCte` | `MONEY(16,2)` | L30 |
| `mIVAComisionCte` | `MONEY(16,2)` | L31 |
| *…6 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesacconciliacionconvsucbtstmp` | `bdicnweb` | no | SELECT | L121 |
| `sw_cb_reportesacconciliacionconvsucbtstmp` | `bdicnweb` | no | DELETE | L121 |
| `sw_cb_reportesacconciliacionconvsucapptmp` | `bdicnweb` | no | SELECT | L125 |
| `sw_cb_reportesacconciliacionconvsucapptmp` | `bdicnweb` | no | DELETE | L125 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L133 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L141 |
| `sac_conciliaciontotalporconvenio` | `bdisac` | ⚠️ sí | SELECT | L146 |
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L159 |
| `sw_cb_reportesacconciliacionconvsucbtstmp` | `bdicnweb` | no | INSERT | L175 |
| `sw_cb_reportesacconciliacionconvsucapptmp` | `bdicnweb` | no | INSERT | L180 |
| `systables` | `bdicnweb` | no | SELECT | L448 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L570 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L81 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L101 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L283 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L441 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L448 | FÓRMULA | `LET cCmd1 ="SELECT ' ',' ',' ',' ',' ',' ',' ',' ','FECHA:','"\|\|TO_CHAR(dFechaHoy, '%d/%m/%Y') \|\` |  |
| L454 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT ' ',' ',' ','DETALLE DE COBRANZA POR SUCURSAL - CONCILIACIO` |  |
| L457 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIOS:','''"\|\|pConvenio\|\|"',' ','SUCURSAL:',' ',''` |  |
| L459 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIOS:','''"\|\|pConvenio\|\|"',' ','SUCURSAL:','TODAS` |  |
| L486 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L520 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L525 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L534 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L543 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `conciliacion` | ACCION | conciliación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sac_sacreportetotalporconvenio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_sacreportetotalporconvenio.sql` |
| **LOC (1er CREATE)** | 953 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 96 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal, reporte y convenio (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=6 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_sacreportetotalporconvenio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaIni                    DATE
  pFechaFin                    DATE
  pSucursal                    CHAR(4)
  pConvenio                    CHAR(5)
  pRutaDescarga                CHAR(100)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaIni` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pConvenio` | `CHAR(5)` | `convenio`=convenio (nómina/empresarial) | ✅ CÓDIGO |
| `pRutaDescarga` | `CHAR(100)` | — | — |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCmd1` | `CHAR(4000)` | L7 |
| `cSql` | `CHAR(4000)` | L8 |
| `cRutaGral` | `CHAR(150)` | L9 |
| `cNombreArchivo` | `CHAR(100)` | L10 |
| `bInTransaction` | `BOOLEAN` | L11 |
| `ven_transacc` | `SMALLINT` | L12 |
| `dFechaHoy` | `DATE` | L13 |
| `cFechaHoraArchivo` | `CHAR(35)` | L14 |
| `cBanDetError` | `CHAR(1)` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cTipo` | `CHAR(50)` | L18 |
| `iTotal` | `INTEGER` | L19 |
| `iTotal2` | `INTEGER` | L20 |
| `dHoraHoy` | `DATETIME HOUR TO SECOND` | L21 |
| `cNombre` | `CHAR(30)` | L22 |
| `pIdMensaje` | `CHAR(10)` | L23 |
| `cNumcategoria` | `CHAR(2)` | L25 |
| `cCategoria` | `CHAR(2)` | L26 |
| `cNumconvenio` | `CHAR(3)` | L27 |
| `cNomconvenio` | `CHAR(40)` | L28 |
| `mImpComisionConvenio` | `MONEY(16,2)` | L29 |
| `mIVAComisionConvenio` | `MONEY(16,2)` | L30 |
| `mImpComisionCte` | `MONEY(16,2)` | L31 |
| *…29 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cb_reportesactotalconvbtstmp` | `bdicnweb` | no | SELECT | L169 |
| `sw_cb_reportesactotalconvbtstmp` | `bdicnweb` | no | DELETE | L169 |
| `sw_cb_reportesacconciltotalconvbtstmp` | `bdicnweb` | no | SELECT | L170 |
| `sw_cb_reportesacconciltotalconvbtstmp` | `bdicnweb` | no | DELETE | L170 |
| `sw_cb_reportesactotalconvapptmp` | `bdicnweb` | no | SELECT | L174 |
| `sw_cb_reportesactotalconvapptmp` | `bdicnweb` | no | DELETE | L174 |
| `sw_cb_reportesacconciltotalconvapptmp` | `bdicnweb` | no | SELECT | L175 |
| `sw_cb_reportesacconciltotalconvapptmp` | `bdicnweb` | no | DELETE | L175 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | SELECT | L183 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | DELETE | L191 |
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L200 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L209 |
| `sw_cb_reportesactotalconvbtstmp` | `bdicnweb` | no | INSERT | L231 |
| `sw_cb_reportesactotalconvapptmp` | `bdicnweb` | no | INSERT | L236 |
| `sac_conciliaciontotalporconvenio` | `bdisac` | ⚠️ sí | SELECT | L389 |
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L391 |
| `sw_cb_reportesacconciltotalconvbtstmp` | `bdicnweb` | no | INSERT | L448 |
| `sw_cb_reportesacconciltotalconvapptmp` | `bdicnweb` | no | INSERT | L453 |
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L551 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L581 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L615 |
| `systables` | `bdicnweb` | no | SELECT | L796 |
| `sw_ctrlgenreportesac` | `bdicnweb` | no | INSERT | L927 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L129 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L158 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L100 | FÓRMULA | `LET dFecha_pago  = "01-01-1990";` |  |
| L149 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L456 | FÓRMULA | `LET dFecha_pago =  dFecha_pago + 1 UNITS DAY;` |  |
| L529 | FÓRMULA | `LET dFecha_pago =  dFecha_pago + 1 UNITS DAY;` |  |
| L542 | FÓRMULA | `LET dFecha_pago =  dFecha_pago + 1 UNITS DAY;` |  |
| L655 | FÓRMULA | `LET dFecha_pago =  dFecha_pago + 1 UNITS DAY;` |  |
| L757 | FÓRMULA | `LET dFecha_pago =  dFecha_pago + 1 UNITS DAY;` |  |
| L770 | FÓRMULA | `LET dFecha_pago =  dFecha_pago + 1 UNITS DAY;` |  |
| L789 | CÓDIGO_RETORNO | `LET cCodRet ='00017';` |  |
| L796 | FÓRMULA | `LET cCmd1 ="SELECT ' ',' ',' ',' ',' ',' ',' ','FECHA:','"\|\|TO_CHAR(dFechaHoy, '%d/%m/%Y') \|\|"' ` |  |
| L802 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT ' ',' ','REPORTE TOTAL DE COBRANZA - CONCILIACION DE PAGOS ` |  |
| L805 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIOS:','''"\|\|pConvenio\|\|"','SUCURSAL:',' ','''"\|` |  |
| L807 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT 'CONVENIOS:','''"\|\|pConvenio\|\|"','SUCURSAL:','TODAS',' ` |  |
| L824 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM ( SELECT nom_convenio::CHAR(40),LPAD(DAY(fecha_pago)` | 🔴 MONEY/aritmética financiera |
| L826 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" SELECT * FROM ( SELECT nom_convenio::CHAR(40),LPAD(DAY(fecha_pago)` | 🔴 MONEY/aritmética financiera |
| L845 | FÓRMULA | `LET pRutaDescarga = TRIM(pRutaDescarga) \|\| '/';` |  |
| L879 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L884 | FÓRMULA | `LET cSql = "rm -rf "\|\|TRIM(cRutaGral);` |  |
| L893 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L902 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sac_verificastatusbitacoraiva`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_verificastatusbitacoraiva.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=76 |
| **Deps concatenadas** | 124 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica Servicios de Atención al Cliente — subsistema de atención en sucursal, estatus y bitácora · IVA" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_verificastatusbitacoraiva(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cProceso` | `CHAR(15)` | L14 |
| `cNombre_archivo` | `CHAR(40)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepbitacoraiva` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `iva` | REG | IVA (impuesto — SAT) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sac_verificastatusctaside`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_verificastatusctaside.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica Servicios de Atención al Cliente — subsistema de atención en sucursal, estatus, cuentas y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_verificastatusctaside(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(1)` | L10 |
| `cError` | `CHAR(5)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificactaside` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_verificastatusgridrepdomiciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_verificastatusgridrepdomiciliacion.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "verifica Servicios de Atención al Cliente — subsistema de atención en sucursal, estatus, identificador y domiciliación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_verificastatusgridrepdomiciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cBanDetError` | `CHAR(1)` | L12 |
| `cErrorProceso` | `CHAR(1)` | L13 |
| `cError` | `CHAR(5)` | L14 |
| `iTotal` | `INTEGER` | L15 |
| `iProcesados` | `INTEGER` | L16 |
| `iNoProcesados` | `INTEGER` | L17 |
| `cProceso` | `CHAR(15)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sac_reportedomiciliaciongrid` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?gr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `domiciliacion` | ENTIDAD | domiciliación | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_verificastatusgridrepventanilla`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_verificastatusgridrepventanilla.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "verifica Servicios de Atención al Cliente — subsistema de atención en sucursal, estatus y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_verificastatusgridrepventanilla(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cBanDetError` | `CHAR(1)` | L12 |
| `cErrorProceso` | `CHAR(1)` | L13 |
| `cError` | `CHAR(5)` | L14 |
| `iTotal` | `INTEGER` | L15 |
| `iProcesados` | `INTEGER` | L16 |
| `iNoProcesados` | `INTEGER` | L17 |
| `cProceso` | `CHAR(15)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sac_reporteventanillagrid` | `bdicnweb` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?gr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?ventanilla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?gr`, `?ventanilla` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_verificastatusrepdomiciliacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_verificastatusrepdomiciliacion.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "verifica Servicios de Atención al Cliente — subsistema de atención en sucursal, estatus y domiciliación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_verificastatusrepdomiciliacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cStatus` | `CHAR(1)` | L12 |
| `cBanDetError` | `CHAR(1)` | L13 |
| `cErrorProceso` | `CHAR(1)` | L14 |
| `cError` | `CHAR(5)` | L15 |
| `iTotal` | `INTEGER` | L16 |
| `iProcesados` | `INTEGER` | L17 |
| `iNoProcesados` | `INTEGER` | L18 |
| `cProceso` | `CHAR(15)` | L19 |
| `cNombre_archivo` | `CHAR(40)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sac_reportedomiciliaciongridrep` | `bdicnweb` | no | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `domiciliacion` | ENTIDAD | domiciliación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sac_verificastatusrepventanilla`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_verificastatusrepventanilla.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "verifica Servicios de Atención al Cliente — subsistema de atención en sucursal y estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_verificastatusrepventanilla(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cStatus` | `CHAR(1)` | L12 |
| `cBanDetError` | `CHAR(1)` | L13 |
| `cErrorProceso` | `CHAR(1)` | L14 |
| `cError` | `CHAR(5)` | L15 |
| `iTotal` | `INTEGER` | L16 |
| `iProcesados` | `INTEGER` | L17 |
| `iNoProcesados` | `INTEGER` | L18 |
| `cProceso` | `CHAR(15)` | L19 |
| `cNombre_archivo` | `CHAR(40)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_sac_reporteventanillagridrep` | `bdicnweb` | no | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?ventanilla` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ventanilla` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sac_verificastatussacmontototal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sac_verificastatussacmontototal.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=32 |
| **Deps concatenadas** | 94 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica Servicios de Atención al Cliente — subsistema de atención en sucursal, estatus y monto (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sac_verificastatussacmontototal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `iTotalOperacion` | `INTEGER` | L14 |
| `mMonto` | `MONEY(14,2)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificasacmontototal` | `bdicnweb` | no | SELECT | L54 |
| `sw_sac_montototal` | `bdicnweb` | no | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `monto` | ENTIDAD | monto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sacreportemensualtelmex`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sacreportemensualtelmex.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Servicios de Atención al Cliente — subsistema de atención en sucursal, reporte y teléfono (mensual)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sacreportemensualtelmex(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pConvenio                    CHAR(5)
  pPeriodo                     CHAR(6)
) RETURNING CHAR (6) AS retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pPeriodo` | `CHAR(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cAnioMes` | `CHAR(6)` | L12 |
| `cInfoErr` | `CHAR(100)` | L13 |
| `dFecha` | `DATE` | L14 |
| `iNumOperaciones` | `INTEGER` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `iIsamErr` | `INTEGER` | L17 |
| `mComision` | `MONEY(16,2)` | L18 |
| `mIva` | `MONEY(16,2)` | L19 |
| `iNumRows` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_liquidacionmensual` | `bdisac` | ⚠️ sí | SELECT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mensual` | MODIF | mensual | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tel` | ENTIDAD | teléfono | 🔵 CONVENCIÓN | nombre_sp |
| `?me` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?me` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sc_catproducto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sc_catproducto.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 218 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y producto" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sc_catproducto(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cProducto` | `CHAR(4)` | L11 |
| `cDescProducto` | `CHAR(40)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_producto` | `bdicheq` | ⚠️ sí | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_sc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `producto` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sc_ctasproductomesiversario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sc_ctasproductomesiversario.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 206 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cuentas, producto y mes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sc_ctasproductomesiversario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
  pProducto                    CHAR(4)
  pCtasConsulta                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | — | — |
| `pProducto` | `CHAR(4)` | `producto`=producto | ✅ CÓDIGO |
| `pCtasConsulta` | `INTEGER` | `ctas`=cuentas | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INT` | L15 |
| `cCodRetSp` | `CHAR(5)` | L16 |
| `cDescCodRet` | `CHAR(80)` | L17 |
| `cEmpresa` | `CHAR(3)` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |
| `dFechaFin` | `DATE` | L20 |
| `cProducto` | `CHAR(4)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `mAcumSdoPos` | `MONEY(18,2)` | L23 |
| `sDiasSdoPos` | `SMALLINT` | L24 |
| `mSdoProm` | `MONEY(18,2)` | L25 |
| `dTasaBruta` | `MONEY(18,2)` | L26 |
| `mTotIntPag` | `MONEY(18,2)` | L27 |
| `mTotIsrCobrado` | `MONEY(18,2)` | L28 |
| `iTotal` | `INTEGER` | L29 |
| `iTotalCta` | `INTEGER` | L30 |
| `cCuentaAux` | `CHAR(20)` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_ctas_mesiversario` | `bdicheq` | ⚠️ sí | SELECT | L83 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET cCodRet 		    = '00000';` |  |
| L64 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta IS NULL OR pProducto = '' OR pCtasConsulta IS ` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_sc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctas` | ENTIDAD | cuentas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `producto` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mes` | ENTIDAD | mes | 🔵 CONVENCIÓN | nombre_sp |
| `?iversario` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sc_`, `?iversario` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sc_volumetriatranssumarizacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sc_volumetriatranssumarizacion.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=112 |
| **Deps concatenadas** | 342 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Transferencia" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sc_volumetriatranssumarizacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaDel                    DATE
  pFechaAl                     DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INT` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |
| `dFechaAlt` | `DATE` | L16 |
| `cTransacc` | `CHAR(4)` | L17 |
| `cProducto` | `CHAR(4)` | L18 |
| `cDescripcion` | `CHAR(50)` | L19 |
| `iNumRegistros` | `INTEGER` | L20 |
| `mMontoSumado` | `MONEY(18,2)` | L21 |
| `cCuentaCargo` | `CHAR(30)` | L22 |
| `cCuentaAbono` | `CHAR(30)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_transsac_sumas` | `bdicheq` | ⚠️ sí | SELECT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L64 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L49 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaDel IS NULL OR pFechaAl IS NULL THEN` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | VALIDACIÓN_NULL | `IF pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L79 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L94 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_sc_volumetria` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `?sumarizacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sc_volumetria`, `?sumarizacion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sc_volumetriatranssumarizacion_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sc_volumetriatranssumarizacion_totales.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=112 |
| **Deps concatenadas** | 341 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Transferencia (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sc_volumetriatranssumarizacion_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaDel                    DATE
  pFechaAl                     DATE
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaDel` | `DATE` | — | — |
| `pFechaAl` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iRecuperacion` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_transsac_sumas` | `bdicheq` | ⚠️ sí | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L24 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaDel IS NULL OR pFechaAl IS NULL THEN` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_sc_volumetria` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `?sumarizacion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sc_volumetria`, `?sumarizacion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sc_volumetriatranssumarizacion_xls`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sc_volumetriatranssumarizacion_xls.sql` |
| **LOC (1er CREATE)** | 137 |
| **Callgraph** | ✅ fan_in=0 / fan_out=112 |
| **Deps concatenadas** | 340 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Transferencia" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sc_volumetriatranssumarizacion_xls(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoReporte                 CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRutaDescarga                CHAR(150)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoReporte` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCmd1` | `CHAR(8000)` | L7 |
| `cCmd2` | `CHAR(8000)` | L8 |
| `cArchDescarga` | `CHAR(200)` | L9 |
| `iNoRegistros` | `INT` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `cRutaInformix` | `CHAR(100)` | L12 |
| `cUsrBin` | `CHAR(100)` | L13 |
| `cNameReport` | `CHAR(60)` | L14 |
| `ven_transacc` | `SMALLINT` | L15 |
| `bInTransaction` | `BOOLEAN` | L16 |
| `cSql` | `CHAR(8000)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L51 | VALIDACIÓN_NULL | `IF pIdUsuario = '' OR pIdFuncion = '' OR pTipoReporte = '' OR pFechaInicio IS NULL OR pFechaFin IS N` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L75 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" TO_CHAR(fecha_alt,'%d/%m/%Y'),";` |  |
| L90 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" UNION ALL SELECT * FROM ";` |  |
| L92 | FÓRMULA | `LET cCmd1 =""\|\|TRIM(cCmd1)\|\|" TO_CHAR(fecha_alt,'%d/%m/%Y'),";` |  |
| L116 | FÓRMULA | `LET cCmd1 = TRIM(cUsrBin)\|\|"rm -rf "\|\|TRIM(pRutaDescarga)\|\|'queryVS.sql';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_sc_volumetria` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `?sumarizacion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ls` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sc_volumetria`, `?sumarizacion_`, `?ls` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_seg_actualizausuarios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_actualizausuarios.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza [polisemia] Seguridad y usuarios" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_actualizausuarios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdTabla                     CHAR(1)
  pNumUsuario                  CHAR(10)
  pPassword                    CHAR(88)
  pPassEncriptado              CHAR(88)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdTabla` | `CHAR(1)` | — | — |
| `pNumUsuario` | `CHAR(10)` | — | — |
| `pPassword` | `CHAR(88)` | — | — |
| `pPassEncriptado` | `CHAR(88)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `cId_usuario` | `CHAR(10)` | L7 |
| `cPass_encrip` | `CHAR(1)` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_usuarios` | `bdinteg` | ⚠️ sí | UPDATE | L44 |
| `sw_seg_valida_usuarios` | `bdicnweb` | no | UPDATE | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_seg_comboperfilappmovil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_comboperfilappmovil.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 190 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Seguridad, combo y perfil de usuario (canal app, canal móvil)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_comboperfilappmovil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)		AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIsamErr` | `INTEGER` | L9 |
| `cErrorInfo` | `CHAR(80)` | L10 |
| `cCodParam` | `SMALLINT` | L11 |
| `cDescripcion` | `CHAR(480)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param_movil` | `bdinteg` | ⚠️ sí | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `app` | MODIF | canal app | 🔵 CONVENCIÓN | nombre_sp |
| `movil` | MODIF | canal móvil | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_seg_comboreporteappmovil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_comboreporteappmovil.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 189 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "[polisemia] Seguridad, combo y reporte (canal app, canal móvil)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_comboreporteappmovil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5)		AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iIsamErr` | `INTEGER` | L9 |
| `cErrorInfo` | `CHAR(80)` | L10 |
| `cClaveReporte` | `CHAR(1)` | L11 |
| `cDescripcion` | `CHAR(20)` | L12 |
| `iRecuperacion` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_rptappmovil` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet			= '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | 🔵 CONVENCIÓN | nombre_sp |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movil` | MODIF | canal móvil | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_seg_consfunusapass`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_consfunusapass.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 202 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Seguridad y ss — subsistema" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_consfunusapass(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cIdFuncion` | `CHAR(10)` | L9 |
| `cDescFuncion` | `CHAR(100)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_funciones` | `bdinteg` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?funusapa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?funusapa`, `ss` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_seg_conssisegusuarios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_conssisegusuarios.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Seguridad y usuarios" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_conssisegusuarios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumUsuario                  CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumUsuario` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cId_usuario` | `CHAR(10)` | L11 |
| `cPassword` | `CHAR(20)` | L12 |
| `cPass_encrip` | `CHAR(1)` | L13 |
| `cEncriptacion` | `CHAR(88)` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_usuarios` | `bdinteg` | ⚠️ sí | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?si` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?si` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_seg_consswsegvalidausuarios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_consswsegvalidausuarios.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=84 |
| **Deps concatenadas** | 199 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Seguridad, sw — SoftWare/Switch y usuarios" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_consswsegvalidausuarios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumUsuario                  CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumUsuario` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cId_usuario` | `CHAR(10)` | L10 |
| `cPassword` | `CHAR(50)` | L11 |
| `cPass_encrip` | `CHAR(1)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_seg_valida_usuarios` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_seg_consultaparamoviles`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_consultaparamoviles.sql` |
| **LOC (1er CREATE)** | 151 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta [polisemia] Seguridad y parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_consultaparamoviles(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipOperacion                CHAR(1)
  pCodigo                      INTEGER
  pDescripcion                 CHAR(480)
  pStatus                      CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipOperacion` | `CHAR(1)` | — | — |
| `pCodigo` | `INTEGER` | — | — |
| `pDescripcion` | `CHAR(480)` | — | — |
| `pStatus` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iCodigo` | `INTEGER` | L9 |
| `cDescripcion` | `CHAR(480)` | L10 |
| `cStatus` | `CHAR(10)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |
| `cDesc` | `CHAR(480)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param_movil` | `bdinteg` | ⚠️ sí | SELECT | L67 |
| `si_param_movil` | `bdinteg` | ⚠️ sí | INSERT | L99 |
| `si_param_movil` | `bdinteg` | ⚠️ sí | UPDATE | L122 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR  pTipOperacion = ''  OR pRegistros IS NULL OR pRecuperacion I` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00148';` |  |
| L50 | VALIDACIÓN_NULL | `IF pCodigo IS NULL OR pDescripcion = '' OR pStatus = '' THEN` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L95 | CÓDIGO_RETORNO | `LET cCodRet = '00004';				RETURN cCodRet, iCodigo, cDescripcion, cStatus;` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00520';				RETURN cCodRet, iCodigo, cDescripcion, cStatus;` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00001';				RETURN cCodRet, iCodigo, cDescripcion, cStatus;` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00520';				RETURN cCodRet, iCodigo, cDescripcion, cStatus;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `param` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?oviles` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?oviles` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_seg_generareportesappmovil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_generareportesappmovil.sql` |
| **LOC (1er CREATE)** | 324 |
| **Callgraph** | ✅ fan_in=0 / fan_out=80 |
| **Deps concatenadas** | 188 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar [polisemia] Seguridad (canal app, canal móvil)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_generareportesappmovil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pReporte                     CHAR(1)
  pInicio                      DATE
  pFin                         DATE
  pEjecutivo                   CHAR(20)
  pRuta                        CHAR(100)
) RETURNING CHAR(5)	AS codigo_retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pReporte` | `CHAR(1)` | `por`=por (criterio) | ✅ CÓDIGO |
| `pInicio` | `DATE` | — | — |
| `pFin` | `DATE` | — | — |
| `pEjecutivo` | `CHAR(20)` | — | — |
| `pRuta` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iIdRegistro` | `INTEGER` | L12 |
| `iTotal` | `INTEGER` | L13 |
| `iSerial` | `INTEGER` | L15 |
| `cRespMensaje` | `CHAR(45)` | L16 |
| `iRegistros` | `INTEGER` | L17 |
| `cQuery` | `CHAR(255)` | L18 |
| `cCmd1` | `CHAR(2000)` | L19 |
| `cSql` | `CHAR(2500)` | L20 |
| `cRutaGral` | `CHAR(150)` | L21 |
| `cRutaInformix` | `CHAR(100)` | L22 |
| `cUsrBin` | `CHAR(100)` | L23 |
| `iCountRep` | `INTEGER` | L24 |
| `iProcesaRep` | `INT` | L25 |
| `iArmaReporte` | `INT` | L26 |
| `cArchivoCP` | `CHAR(45)` | L27 |
| `cCmdQuery` | `CHAR(2500)` | L28 |
| `bInTransaction` | `BOOLEAN` | L29 |
| `ven_transacc` | `SMALLINT` | L30 |
| `dFechaHora` | `DATETIME YEAR TO FRACTION(5)` | L31 |
| *…14 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_rpt_consultappmovil` | `bdicnweb` | no | SELECT | L134 |
| `sw_rpt_consultappmovil` | `bdicnweb` | no | DELETE | L134 |
| `sw_rpt_consultappmovil` | `bdicnweb` | no | INSERT | L157 |
| `systables` | `bdicnweb` | no | SELECT | L217 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L129 |
| `sp_reporte_accesos_amov` | `bdinteg` | ⚠️ sí | L148 |
| `sp_reporte_usuarios_amov` | `bdinteg` | ⚠️ sí | L171 |
| `sp_perfiles_amov` | `bdinteg` | ⚠️ sí | L195 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L118 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L123 | VALIDACIÓN_NULL | `IF pReporte = 1 AND (pInicio IS NULL OR pFin IS NULL ) THEN` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L143 | FÓRMULA | `LET pRuta = TRIM(pRuta) \|\| '/';` |  |
| L152 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L160 | FÓRMULA | `LET iTotalRegistros = iTotalRegistros + 1;` |  |
| L166 | FÓRMULA | `LET pRuta = TRIM(pRuta) \|\| '/';` |  |
| L176 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L184 | FÓRMULA | `LET iTotalRegistros = iTotalRegistros + 1;` |  |
| L190 | FÓRMULA | `LET pRuta = TRIM(pRuta) \|\| '/';` |  |
| L201 | FÓRMULA | `LET iTotalRegistros = iTotalRegistros + 1;` |  |
| L206 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L259 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(pRuta)\|\|'query.sql';` |  |
| L268 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L273 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral);` |  |
| L282 | FÓRMULA | `LET cSql =  "sed 's/..$//g' "\|\|TRIM(cRutaGral)\|\|".tmp > "\|\|TRIM(cRutaGral);` |  |
| L291 | FÓRMULA | `LET cSql = "sed "\|\|"'s/$'""/`echo \\\r`/"" "\|\|TRIM(cRutaGral)\|\|" > "\|\|TRIM(cRutaGral)\|\|".t` |  |
| L299 | FÓRMULA | `LET cSql = TRIM(cUsrBin)\|\|'rm -rf '\|\|TRIM(cRutaGral)\|\|'; /usr/bin/mv '\|\|TRIM(cRutaGral)\|\|'` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `app` | MODIF | canal app | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `movil` | MODIF | canal móvil | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?tes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_seg_validacoordinador`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_seg_validacoordinador.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=84 |
| **Deps concatenadas** | 198 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida [polisemia] Seguridad y ordenante" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_seg_validacoordinador(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pIdCoordinador               CHAR(10)
  pPassword                    CHAR(50)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pIdCoordinador` | `CHAR(10)` | `ord`=ordenante / orden (SPEI) | 🔵 CONVENCIÓN |
| `pPassword` | `CHAR(50)` | `ord`=ordenante / orden (SPEI) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRet` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cIdCoordinador` | `CHAR(10)` | L10 |
| `cNombreCoordinador` | `CHAR(45)` | L11 |
| `iExiste` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_seg_valida_usuarios` | `bdicnweb` | no | SELECT | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L80 | VALIDACIÓN_NULL | `IF iExiste = 0 OR iExiste IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `seg` | ENTIDAD | [polisemia] Seguridad (bdicnweb: usuarios, perfiles, app móv | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?co` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `?inador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?co`, `?inador` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_solicitudprocesandomc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_solicitudprocesandomc.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa solicitud y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdisolic` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_solicitudprocesandomc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pTipoMotivo                  SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | `mc`=mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN |
| `pTipoMotivo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cMensaje` | `CHAR(80)` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdisolic` | `bdicnweb` | no | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pTipoMotivo IS NULL THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00108';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?ndo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ndo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sololetrasnumeros`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sololetrasnumeros.sql` |
| **LOC (1er CREATE)** | 28 |
| **Callgraph** | ✅ fan_in=1 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "solicitud, número y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sololetrasnumeros(
  pCaracter                    CHAR(1)
) RETURNING BOOLEAN AS es_letra_numero
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCaracter` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `bEsLetraNumero` | `BOOLEAN` | L4 |
| `iAscii` | `SMALLINT` | L5 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?oletras` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?oletras`, `?e`, `ro`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_spei_ctasinretencion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_spei_ctasinretencion.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_spei_ctasinretencion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pBandera                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCuenta` | `CHAR(20)` | L10 |
| `cUsuario` | `CHAR(10)` | L11 |
| `cEstatus` | `CHAR(1)` | L12 |
| `dFecha_alt` | `DATE` | L13 |
| `dFecha_baj` | `DATE` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cBandera` | `CHAR(1)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_spei_cta_sin_retencion` | `bdicheq` | ⚠️ sí | SELECT | L63 |
| `sc_spei_cta_sin_retencion` | `bdicheq` | ⚠️ sí | UPDATE | L75 |
| `sc_spei_cta_sin_retencion` | `bdicheq` | ⚠️ sí | INSERT | L91 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `spei` | PREFIJO | familia SPEI (pagos interbancarios) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?inretencion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?inretencion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_split_cadena`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_split_cadena.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=857 / fan_out=1 |
| **Principales callers** | `sp_bitacora`, `sp_calificacion_scoring`, `sp_consultareportepagoscre`, `sp_generararchivo_rst`, `sp_ope_consultarutalmacenamientoxml` |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "split — divide/parsea cadena cadena — string" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Tokens confirmados en el vocab pero DML no correlaciona con el propósito |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_split_cadena(
  pCadena                      LVARCHAR
  pDelimitador                 CHAR(1)
) RETURNING LVARCHAR AS palabra
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCadena` | `LVARCHAR` | `cadena`=cadena — string / cadena de texto (sp_split_cadena) | ✅ CÓDIGO |
| `pDelimitador` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `tPalabra` | `LVARCHAR` | L4 |
| `i` | `INTEGER` | L5 |
| `iTamCad` | `INTEGER` | L6 |
| `iInicioCadena` | `INTEGER` | L7 |
| `iRecuperarCaracteres` | `INTEGER` | L8 |
| `cCaracter` | `CHAR(1)` | L9 |
| `cPalabra` | `LVARCHAR` | L10 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | FÓRMULA | `LET iRecuperarCaracteres = iRecuperarCaracteres + 1;` |  |
| L32 | FÓRMULA | `LET iRecuperarCaracteres = iRecuperarCaracteres - 1;` |  |
| L36 | FÓRMULA | `LET iInicioCadena = i + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `split` | ACCION | split — divide/parsea cadena (sp_split_cadena fan_in=857 — # | 🔵 CONVENCIÓN | nombre_sp |
| `cadena` | ENTIDAD | cadena — string / cadena de texto (sp_split_cadena) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_ss_cnsif_confirmaejecutivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_ss_cnsif_confirmaejecutivo.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "confirma ss — subsistema, CNSIF — sistema de confirmación de ejecutivo y ejecutivo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_ss_cnsif_confirmaejecutivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L35 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, "ERROR EN LA EJECUCION DEL SP bdinteg:sp_cnsif_confirmaejecutivo";` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00028';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `cnsif` | ENTIDAD | CNSIF — sistema de confirmación de ejecutivo (sp_cnsif_confi | 🔵 CONVENCIÓN | nombre_sp |
| `confirma` | ACCION | confirma | 🔵 CONVENCIÓN | nombre_sp |
| `ejecutivo` | ENTIDAD | ejecutivo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ss` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_statusdescarga`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_statusdescarga.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=14 / fan_out=9 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "estatus y descarga" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_split_cadena` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_statusdescarga(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTramaConsecutivo            CHAR(250)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTramaConsecutivo` | `CHAR(250)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iConsecutivo` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |
| `sp_split_cadena` | `bdicnweb` | no | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `descarga` | ENTIDAD | descarga | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sustituyese`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sustituyese.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 130 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "sp_sustituyese" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_sustituirse` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 2 términos |

### Firma

```sql
CREATE PROCEDURE sp_sustituyese(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumCredito                  CHAR(20)
  pSE                          CHAR(1)
  pCausa                       SMALLINT
  pTipoMovimiento              INTEGER
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCredito` | `CHAR(20)` | — | — |
| `pSE` | `CHAR(1)` | — | — |
| `pCausa` | `SMALLINT` | — | — |
| `pTipoMovimiento` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRetSp` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNombreUsuario` | `CHAR(45)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_sustituirse` | `bdisitesp` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdisitesp:sp_sustituirse';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_sustituyese` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sustituyese` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_catcoordinador`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_catcoordinador.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 169 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "sw — SoftWare/Switch, catálogo y ordenante" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_catcoordinador(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cDescCodRet` | `CHAR(80)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cIdCoordinador` | `CHAR(10)` | L12 |
| `cNombreCoordinador` | `CHAR(45)` | L13 |
| `iRecuperacion` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_seg_valida_usuarios` | `bdicnweb` | no | SELECT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?co` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `?inador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?co`, `?inador` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_combostatusmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_combostatusmc.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "sw — SoftWare/Switch, combo, estatus y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_combostatusmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(150)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cDescripcion` | `CHAR(50)` | L13 |
| `cId` | `CHAR(2)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_mc_combostatus` | `bdicnweb` | no | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L68 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '01086';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `combo` | ENTIDAD | combo / lista desplegable (control de UI en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_sw_consenclace1n`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_consenclace1n.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_consenclace1n(
  pUsuarioC                    char(8)
  pIdFuncion                   char(10)
  pTipoBusqueda                int
) RETURNING char(5) as CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pTipoBusqueda` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L10 |
| `iIdInstitucion` | `int` | L11 |
| `cDescNombreCorto` | `char(50)` | L12 |
| `cDescNombreLargo` | `char(100)` | L13 |
| `cDirectorioOficio` | `char(255)` | L14 |
| `cStatus` | `char(1)` | L15 |
| `iSqlErr` | `int` | L16 |
| `iNoRows` | `int` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_insenlace1nivel` | `bdicnweb` | no | SELECT | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `let cCodRet='00000';` |  |
| L48 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L53 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L65 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |
| L85 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?enclace1n` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?enclace1n` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_consenclace2n`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_consenclace2n.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_consenclace2n(
  pUsuarioC                    char(8)
  pIdFuncion                   char(10)
  pTipoBusqueda                int
) RETURNING char(5) as CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pTipoBusqueda` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L9 |
| `iIdInstitucion` | `int` | L10 |
| `cDescNombreCorto` | `char(50)` | L11 |
| `cDescNombreLargo` | `char(100)` | L12 |
| `cStatus` | `char(1)` | L13 |
| `iSqlErr` | `int` | L14 |
| `iNoRows` | `int` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_inssolic2nivel` | `bdicnweb` | no | SELECT | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `let cCodRet='00000';` |  |
| L44 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L61 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |
| L80 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?enclace2n` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?enclace2n` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_consfacultados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_consfacultados.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_consfacultados(
  pUsuarioC                    char(8)
  pIdFuncion                   char(10)
  pTipoBusqueda                int
) RETURNING char(5) as CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pTipoBusqueda` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L11 |
| `iIdFacultado` | `int` | L12 |
| `cRolFuncion` | `char(50)` | L13 |
| `cNombreCorto` | `char(50)` | L14 |
| `cNombreFacultado` | `char(80)` | L15 |
| `cPuestoFacultado` | `char(50)` | L16 |
| `cStatus` | `char(1)` | L17 |
| `iSqlErr` | `int` | L18 |
| `iNoRows` | `int` | L19 |
| `iIdRolFuncion` | `smallint` | L20 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L57 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L70 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |
| L90 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?facultad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `?facultad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_constipooficios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_constipooficios.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch y oficio (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_constipooficios(
  pUsuarioC                    char(8)
  pIdFuncion                   char(10)
  iTipoBusqueda                int
) RETURNING char(5) as CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `iTipoBusqueda` | `int` | `tipo`=tipo de | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L7 |
| `iIdTipoOficio` | `int` | L8 |
| `cDescOficio` | `char(100)` | L9 |
| `cStatus` | `char(1)` | L10 |
| `iSqlErr` | `int` | L11 |
| `iNoRows` | `int` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_tipooficios` | `bdicnweb` | no | SELECT | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L61 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |
| L81 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_consultainfocatarula`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_consultainfocatarula.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=77 |
| **Deps concatenadas** | 187 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, información y catálogo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_consultainfocatarula(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumProducto                 CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumProducto` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cNumProducto` | `CHAR(4)` | L11 |
| `cRangoPromedio` | `CHAR(100)` | L12 |
| `cTasasCete` | `CHAR(100)` | L13 |
| `cGatNominal` | `CHAR(100)` | L14 |
| `cGatReal` | `CHAR(100)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_infocaratula` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet 		= '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `?arula` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?arula` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_enlace1n_actualiza`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_enlace1n_actualiza.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza sw — SoftWare/Switch" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_enlace1n_actualiza(
  pId_UsuarioC                 char(8)
  pId_FuncionC                 char(10)
  pDesc_i1n_NombreCorto        char(50)
  pDesc_i1n_NombreLargo        char(100)
  pDireccion_Oficio            char(255)
  pStatus_i1n                  char(1)
  pTipoOperacion               int
  pId_Institucion1n            int
  pIp                          char(15)
  pMacAddress                  char(12)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pId_UsuarioC` | `char(8)` | — | — |
| `pId_FuncionC` | `char(10)` | — | — |
| `pDesc_i1n_NombreCorto` | `char(50)` | — | — |
| `pDesc_i1n_NombreLargo` | `char(100)` | — | — |
| `pDireccion_Oficio` | `char(255)` | — | — |
| `pStatus_i1n` | `char(1)` | — | — |
| `pTipoOperacion` | `int` | — | — |
| `pId_Institucion1n` | `int` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMacAddress` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `iSqlErr` | `int` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_insenlace1nivel` | `bdicnweb` | no | INSERT | L52 |
| `sw_ro_insenlace1nivel` | `bdicnweb` | no | UPDATE | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L46 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L56 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_enlace1n_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_enlace1n_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_enlace2n_actualiza`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_enlace2n_actualiza.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza sw — SoftWare/Switch" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_enlace2n_actualiza(
  pId_UsuarioC                 char(8)
  pId_FuncionC                 char(10)
  pDesc_i2n_NombreCorto        char(50)
  pDesc_i2n_NombreLargo        char(100)
  pStatus_i2n                  char(1)
  pTipoOperacion               int
  pId_Institucion2n            int
  pIp                          char(15)
  pMacAddress                  char(12)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pId_UsuarioC` | `char(8)` | — | — |
| `pId_FuncionC` | `char(10)` | — | — |
| `pDesc_i2n_NombreCorto` | `char(50)` | — | — |
| `pDesc_i2n_NombreLargo` | `char(100)` | — | — |
| `pStatus_i2n` | `char(1)` | — | — |
| `pTipoOperacion` | `int` | — | — |
| `pId_Institucion2n` | `int` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMacAddress` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `iSqlErr` | `int` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_inssolic2nivel` | `bdicnweb` | no | INSERT | L51 |
| `sw_ro_inssolic2nivel` | `bdicnweb` | no | UPDATE | L57 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L55 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_enlace2n_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_enlace2n_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_facultados_actualiza`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_facultados_actualiza.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza sw — SoftWare/Switch y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_facultados_actualiza(
  pId_UsuarioC                 char(8)
  pId_FuncionC                 char(10)
  pRolFuncion                  int
  pIdInstitucion1n             int
  pNombreFacultado             char(80)
  pPuestoFacultado             char(40)
  pStatus                      char(1)
  pTipoOperacion               int
  pId_Facultado                int
  pIp                          char(15)
  pMacAddress                  char(12)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pId_UsuarioC` | `char(8)` | — | — |
| `pId_FuncionC` | `char(10)` | — | — |
| `pRolFuncion` | `int` | — | — |
| `pIdInstitucion1n` | `int` | — | — |
| `pNombreFacultado` | `char(80)` | — | — |
| `pPuestoFacultado` | `char(40)` | — | — |
| `pStatus` | `char(1)` | — | — |
| `pTipoOperacion` | `int` | — | — |
| `pId_Facultado` | `int` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMacAddress` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `iSqlErr` | `int` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_facultados` | `bdicnweb` | no | INSERT | L49 |
| `sw_ro_facultados` | `bdicnweb` | no | UPDATE | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L53 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_facultad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_facultad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_gralsino`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_gralsino.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "sw — SoftWare/Switch (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_gralsino(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(150)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iRecuperacion` | `INTEGER` | L12 |
| `cDescripcion` | `CHAR(50)` | L13 |
| `sId` | `SMALLINT` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_gral_sino` | `bdicnweb` | no | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pRegistros IS NULL OR pRecuperacion IS NULL THEN` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L68 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '01086';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `gral` | MODIF | general | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?sino` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sino` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_insercteexp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_insercteexp.sql` |
| **LOC (1er CREATE)** | 21 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "insertar sw — SoftWare/Switch y cliente (sufijo Exportar — SP genera/exporta archivo de salida)" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_insercteexp(
  pidresulcte                  integer
  pidbusqueda                  integer
  pidoficio                    integer
) RETURNING char(5) as CodRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pidresulcte` | `integer` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pidbusqueda` | `integer` | — | — |
| `pidoficio` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L4 |
| `iSqlErr` | `int` | L5 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?er` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `exp` | MODIF | sufijo Exportar — SP genera/exporta archivo de salida (sp_*_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `?er` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_actualizactecta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_actualizactecta.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "actualiza sw — SoftWare/Switch, ro — Rol Operativo, cliente y cuenta" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_actualizactecta(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdCtecta                    INT
  pIndicadores                 CHAR(14)
  pIp                          CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdCtecta` | `INT` | `cte`=cliente · `cta`=cuenta | 🔵 CONVENCIÓN |
| `pIndicadores` | `CHAR(14)` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `cctayabloque` | `CHAR(1)` | L11 |
| `cfchaApertura` | `CHAR(1)` | L12 |
| `csucApertura` | `CHAR(1)` | L13 |
| `cdomiSuc` | `CHAR(1)` | L14 |
| `csaldo` | `CHAR(1)` | L15 |
| `creportarStatus` | `CHAR(1)` | L16 |
| `cbeneficiarios` | `CHAR(1)` | L17 |
| `cfacultados` | `CHAR(1)` | L18 |
| `cdatosTitular` | `CHAR(1)` | L19 |
| `cterminado` | `CHAR(1)` | L20 |
| `ccertificaImg` | `CHAR(1)` | L21 |
| `ccertificaEdocta` | `CHAR(1)` | L22 |
| `cdetMov` | `CHAR(1)` | L23 |
| `cbloqueoCtaPorSistema` | `CHAR(1)` | L24 |
| `iResulCte` | `INT` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `10` | `bdicnweb` | no | SELECT | L76 |
| `11` | `bdicnweb` | no | SELECT | L77 |
| `12` | `bdicnweb` | no | SELECT | L78 |
| `13` | `bdicnweb` | no | SELECT | L79 |
| `14` | `bdicnweb` | no | SELECT | L80 |
| `sw_ro_ctecta` | `bdicnweb` | no | UPDATE | L81 |
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L95 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_actualizastatuscliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_actualizastatuscliente.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "actualiza sw — SoftWare/Switch, ro — Rol Operativo, estatus y cliente" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_actualizastatuscliente(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCliente                   INT
  pIndicadores                 CHAR(4)
  pIp                          CHAR(15)
  pMac                         CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCliente` | `INT` | `cliente`=cliente | 🔵 CONVENCIÓN |
| `pIndicadores` | `CHAR(4)` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `iRegsAfectados` | `SMALLINT` | L11 |
| `cIndRfc` | `CHAR(1)` | L12 |
| `cIndEmpleo` | `CHAR(1)` | L13 |
| `cIndDomicilio` | `CHAR(1)` | L14 |
| `cIndNacionalidad` | `CHAR(1)` | L15 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `cliente` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_bitacorabusqueda`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_bitacorabusqueda.sql` |
| **LOC (1er CREATE)** | 31 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "búsqueda sw — SoftWare/Switch, ro — Rol Operativo y bitácora" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_bitacorabusqueda(
  pTipoBusqueda                smallint
  pIdOficio                    int
  pApPaterno                   char(26)
  pApMaterno                   char(26)
  pNombre1                     char(26)
  pNombre2                     char(26)
  pRazonSocial                 char(60)
  pRfc                         char(13)
  pNumcte                      char(20)
  pCuenta                      char(20)
  pNumTarjeta                  char(20)
  pUsuario                     char(8)
  pIp                          char(15)
  pMac                         char(12)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pTipoBusqueda` | `smallint` | `busqueda`=búsqueda | 🔵 CONVENCIÓN |
| `pIdOficio` | `int` | — | — |
| `pApPaterno` | `char(26)` | — | — |
| `pApMaterno` | `char(26)` | — | — |
| `pNombre1` | `char(26)` | — | — |
| `pNombre2` | `char(26)` | — | — |
| `pRazonSocial` | `char(60)` | — | — |
| `pRfc` | `char(13)` | — | — |
| `pNumcte` | `char(20)` | — | — |
| `pCuenta` | `char(20)` | — | — |
| `pNumTarjeta` | `char(20)` | — | — |
| `pUsuario` | `char(8)` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L7 |
| `iSqlErr` | `int` | L8 |
| `iRegsAfectados` | `int` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_buscaper` | `bdicnweb` | no | INSERT | L24 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_bitacoracteenc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_bitacoracteenc.sql` |
| **LOC (1er CREATE)** | 39 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo, bitácora y cliente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_bitacoracteenc(
  pUsuario                     char(8)
  pIdBusqueda                  int
  pIdOficio                    int
  pIdResulPer                  int
  pNumCliente                  char(20)
  pApPaterno                   char(26)
  pApMaterno                   char(26)
  pNombre1                     char(26)
  pNombre2                     char(26)
  pRazonSocial                 char(60)
  pRfc                         char(13)
  pIp                          char(15)
  pMac                         char(12)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdResulPer` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pApPaterno` | `char(26)` | — | — |
| `pApMaterno` | `char(26)` | — | — |
| `pNombre1` | `char(26)` | — | — |
| `pNombre2` | `char(26)` | — | — |
| `pRazonSocial` | `char(60)` | — | — |
| `pRfc` | `char(13)` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L10 |
| `cCodRet` | `char(5)` | L11 |
| `iRows` | `int` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulcte` | `bdicnweb` | no | INSERT | L24 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?enc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?enc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_bitacoraresultados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_bitacoraresultados.sql` |
| **LOC (1er CREATE)** | 33 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo, bitácora y resultado" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_bitacoraresultados(
  pUsuario                     char(8)
  pIdBusqueda                  int
  pIdOficio                    int
  pApPaterno                   char(26)
  pApMaterno                   char(26)
  pNombre1                     char(26)
  pNombre2                     char(26)
  pRazonSocial                 char(60)
  pRfc                         char(13)
  pNumCliente                  char(20)
  pNumCuenta                   char(20)
  pNumTarjeta                  char(20)
  pTipoCuenta                  char(2)
  pTipoCliente                 char(1)
  pStatusBus                   int
  pIp                          char(15)
  pMac                         char(12)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdOficio` | `int` | — | — |
| `pApPaterno` | `char(26)` | — | — |
| `pApMaterno` | `char(26)` | — | — |
| `pNombre1` | `char(26)` | — | — |
| `pNombre2` | `char(26)` | — | — |
| `pRazonSocial` | `char(60)` | — | — |
| `pRfc` | `char(13)` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pNumTarjeta` | `char(20)` | — | — |
| `pTipoCuenta` | `char(2)` | — | — |
| `pTipoCliente` | `char(1)` | — | — |
| `pStatusBus` | `int` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L10 |
| `cCodRet` | `char(5)` | L11 |
| `iRows` | `int` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | INSERT | L24 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `resultado` | ENTIDAD | resultado | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_bitacoraresultadoshom`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_bitacoraresultadoshom.sql` |
| **LOC (1er CREATE)** | 33 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo, bitácora y resultado" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_bitacoraresultadoshom(
  pUsuario                     char(8)
  pIdBusqueda                  int
  pIdOficio                    int
  pApPaterno                   char(26)
  pApMaterno                   char(26)
  pNombre1                     char(26)
  pNombre2                     char(26)
  pRazonSocial                 char(60)
  pRfc                         char(13)
  pNumCliente                  char(20)
  pNumCuenta                   char(20)
  pNumTarjeta                  char(20)
  pTipoCuenta                  char(2)
  pTipoCliente                 char(1)
  pStatusBus                   int
  pIp                          char(15)
  pMac                         char(12)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdOficio` | `int` | — | — |
| `pApPaterno` | `char(26)` | — | — |
| `pApMaterno` | `char(26)` | — | — |
| `pNombre1` | `char(26)` | — | — |
| `pNombre2` | `char(26)` | — | — |
| `pRazonSocial` | `char(60)` | — | — |
| `pRfc` | `char(13)` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pNumTarjeta` | `char(20)` | — | — |
| `pTipoCuenta` | `char(2)` | — | — |
| `pTipoCliente` | `char(1)` | — | — |
| `pStatusBus` | `int` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L10 |
| `cCodRet` | `char(5)` | L11 |
| `iRows` | `int` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper_homonimos` | `bdicnweb` | no | INSERT | L24 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `resultado` | ENTIDAD | resultado | 🔵 CONVENCIÓN | nombre_sp |
| `?shom` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?shom` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_bloqueactacap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_bloqueactacap.sql` |
| **LOC (1er CREATE)** | 182 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Propósito inferido** | "bloquea cuenta sw — SoftWare/Switch, ro — Rol Operativo, cuenta y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_permisosejecutivo`, `bloqueo_cta`, `sp_sw_ro_guardabloqueoctas` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_bloqueactacap(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(8)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCliente                   INT
  pNumCliente                  CHAR(20)
  pCuenta                      CHAR(20)
  pMonto                       money(14,2)
  pFechaBloqueo                CHAR(10)
  pCodBloqueo                  CHAR(2)
  pOpcBloq                     INT
  pAreaSolic                   CHAR(2)
  pTipoBloq                    CHAR(2)
  pTipoBloqueo                 INT
  pIp                          CHAR(16)
  pMac                         CHAR(12)
  pOficios                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(8)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCliente` | `INT` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pMonto` | `money(14,2)` | — | — |
| `pFechaBloqueo` | `CHAR(10)` | — | — |
| `pCodBloqueo` | `CHAR(2)` | — | — |
| `pOpcBloq` | `INT` | — | — |
| `pAreaSolic` | `CHAR(2)` | — | — |
| `pTipoBloq` | `CHAR(2)` | — | — |
| `pTipoBloqueo` | `INT` | — | — |
| `pIp` | `CHAR(16)` | — | — |
| `pMac` | `CHAR(12)` | — | — |
| `pOficios` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cEmpresa` | `CHAR(3)` | L20 |
| `cCodArea` | `CHAR(2)` | L21 |
| `cCodTipoBloq` | `CHAR(2)` | L22 |
| `cCodRet` | `CHAR(5)` | L23 |
| `cClave` | `CHAR(5)` | L24 |
| `cDescRet` | `CHAR(10)` | L25 |
| `iSqlErr` | `INT` | L26 |
| `cCodRetMask` | `CHAR(5)` | L27 |
| `cFolioMask` | `CHAR(10)` | L28 |
| `iLenCodRet` | `INT` | L29 |
| `iLenFolio` | `INT` | L30 |
| `cSistemaCta` | `CHAR(2)` | L31 |
| `cDescripcionMotivo` | `CHAR(35)` | L32 |
| `cCodRetSp` | `CHAR(5)` | L33 |
| `iRegsAfectados` | `INT` | L34 |
| `pFechaBloqueo1` | `date` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_areabloqueo` | `bdicheq` | ⚠️ sí | SELECT | L87 |
| `sc_tipobloqueo` | `bdicheq` | ⚠️ sí | SELECT | L93 |
| `sw_ro_foliador` | `bdicnweb` | no | INSERT | L118 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L76 |
| `bloqueo_cta` | `bdicheq` | ⚠️ sí | L100 |
| `sp_sw_ro_guardabloqueoctas` | `bdicnweb` | no | L123 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L115 | FÓRMULA | `LET cCodRet = SUBSTR(cCodRetMask, 0, (5-iLenCodRet)) \|\| cCodRet;` |  |
| L121 | FÓRMULA | `LET cDescRet = SUBSTR(cFolioMask, 0, (LENGTH(cFolioMask) - iLenFolio)) \|\| cDescRet;` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L153 | CÓDIGO_RETORNO | `LET cCodRet = '00101';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00009';` |  |
| L168 | CÓDIGO_RETORNO | `LET cCodRet = '00103';` |  |
| L173 | CÓDIGO_RETORNO | `LET cCodRet = '00104';` |  |
| L178 | CÓDIGO_RETORNO | `LET cCodRet = '00173';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_bloqueactacre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_bloqueactacre.sql` |
| **LOC (1er CREATE)** | 148 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloquea cuenta sw — SoftWare/Switch, ro — Rol Operativo, cuenta y crédito" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_permisosejecutivo`, `sp_bloqueocuenta`, `sp_sw_ro_guardabloqueoctas` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_bloqueactacre(
  pIdUsuario                   CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCliente                   INT
  pNumCliente                  CHAR(20)
  pNumCuenta                   CHAR(20)
  pCveBloqueo                  INT
  pCveCausa                    CHAR(2)
  pTipo                        INT
  pIp                          CHAR(15)
  pMac                         CHAR(12)
  pOficios                     CHAR(1)
  pAreaPersonaSolicita         CHAR(150)
  pMotivoBloqueo               CHAR(150)
) RETURNING CHAR(5) AS cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCliente` | `INT` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pCveBloqueo` | `INT` | — | — |
| `pCveCausa` | `CHAR(2)` | — | — |
| `pTipo` | `INT` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(12)` | — | — |
| `pOficios` | `CHAR(1)` | — | — |
| `pAreaPersonaSolicita` | `CHAR(150)` | — | — |
| `pMotivoBloqueo` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRetSp` | `CHAR(6)` | L19 |
| `cCodRet` | `CHAR(5)` | L20 |
| `cMensajeRet` | `CHAR(16)` | L21 |
| `iSqlErr` | `INT` | L22 |
| `cEmpresa` | `CHAR(3)` | L23 |
| `cSistemaCta` | `CHAR(2)` | L24 |
| `cFolioMask` | `CHAR(16)` | L25 |
| `iLenFolio` | `INT` | L26 |
| `cDescCausa` | `CHAR(40)` | L27 |
| `iRegsAfectados` | `INT` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_foliador` | `bdicnweb` | no | INSERT | L108 |
| `sd_causa_bloqueo` | `bdicred` | ⚠️ sí | SELECT | L116 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L64 |
| `sp_bloqueocuenta` | `bdicred` | ⚠️ sí | L69 |
| `sp_sw_ro_guardabloqueoctas` | `bdicnweb` | no | L118 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L59 | VALIDACIÓN_NULL | `IF pTipo is NULL OR pTipo NOT IN(1,2) THEN` |  |
| L60 | CÓDIGO_RETORNO | `LET cCodRet = '00005';` |  |
| L111 | FÓRMULA | `LET cMensajeRet = SUBSTR(cFolioMask, 0, (LENGTH(cFolioMask) - iLenFolio)) \|\| cMensajeRet;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_sw_ro_borraimagenes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_borraimagenes.sql` |
| **LOC (1er CREATE)** | 140 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "borra sw — SoftWare/Switch, ro — Rol Operativo y imágenes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 6 tabla(s) con operaciones: UPDATE, DELETE, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_borraimagenes(
  pUsuarioC                    CHAR(8)
  pIdFuncionC                  CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCte                       INT
  pNumCliente                  CHAR(20)
  pTipoCuenta                  CHAR(2)
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `CHAR(8)` | — | — |
| `pIdFuncionC` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCte` | `INT` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pTipoCuenta` | `CHAR(2)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `iNoRegistros` | `INT` | L13 |
| `cStatus` | `CHAR(1)` | L14 |
| `cStatus2` | `CHAR(1)` | L15 |
| `cNumCtaAux` | `CHAR(20)` | L16 |
| `iNoRegistrosAux` | `INT` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_cteexp` | `bdicnweb` | no | SELECT | L59 |
| `sw_ro_cteexp` | `bdicnweb` | no | DELETE | L59 |
| `sw_ro_resulcte` | `bdicnweb` | no | UPDATE | L68 |
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L74 |
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L89 |
| `sw_ro_ctecta` | `bdicnweb` | no | UPDATE | L103 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L21 | FÓRMULA | `LET iNoRegistros = -1;` |  |
| L23 | FÓRMULA | `LET iNoRegistrosAux = -1;` |  |
| L92 | FÓRMULA | `LET cStatus = cStatus + cStatus2;` |  |
| L127 | FÓRMULA | `LET cStatus = cStatus + cStatus2;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `borra` | ACCION | borra / elimina registros (borramovs_movhis, borramovscfd*) | 🟡 INFERIDO | nombre_sp |
| `imagenes` | ENTIDAD | imágenes / documentos digitales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_buscabeneficiarioxcta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscabeneficiarioxcta.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "busca sw — SoftWare/Switch, ro — Rol Operativo, beneficiario y cuenta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscabeneficiarioxcta(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pTipoCuenta                  char(2)
  pCuenta                      char(20)
) RETURNING char(5) as cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pTipoCuenta` | `char(2)` | — | — |
| `pCuenta` | `char(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L12 |
| `cNumCte` | `char(20)` | L13 |
| `cTipoPersona` | `char(2)` | L14 |
| `cNombre1` | `char(26)` | L15 |
| `cNombre2` | `char(26)` | L16 |
| `cApellidoPaterno` | `char(26)` | L17 |
| `cApellidoMaterno` | `char(26)` | L18 |
| `cRazonSocial` | `char(60)` | L19 |
| `iRegistros` | `int` | L20 |
| `iSqlErr` | `int` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_benefic` | `bdinvers` | ⚠️ sí | SELECT | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L61 | FÓRMULA | `let iRegistros = iRegistros + 1;` |  |
| L71 | CÓDIGO_RETORNO | `let cCodRet = '00037';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `beneficiario` | ENTIDAD | beneficiario (receptor del pago SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_buscacte_ctatar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscacte_ctatar.sql` |
| **LOC (1er CREATE)** | 121 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "busca sw — SoftWare/Switch, ro — Rol Operativo, cliente, cuenta y Tarjeta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 8 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscacte_ctatar(
  pBusqueda                    smallint
  pNumCtaTar                   char(20)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pBusqueda` | `smallint` | — | — |
| `pNumCtaTar` | `char(20)` | `cta`=cuenta · `tar`=Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, mover_his_tar, obtener_cta_con_num_tar) | 🟡 INFERIDO / 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cSubCta` | `char(1)` | L12 |
| `cNumCliente` | `char(20)` | L13 |
| `cCodRet` | `char(5)` | L14 |
| `cTipoPersona` | `char(1)` | L15 |
| `cApPaterno` | `char(26)` | L16 |
| `cApMaterno` | `char(26)` | L17 |
| `cNombre1` | `char(26)` | L18 |
| `cNombre2` | `char(26)` | L19 |
| `cRazonSocial` | `char(26)` | L20 |
| `cTipoCuenta` | `char(2)` | L21 |
| `iSqlErr` | `int` | L22 |
| `iRegEncontrados` | `int` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L52 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L56 |
| `sv_maeinv` | `bdinvers` | ⚠️ sí | SELECT | L64 |
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L70 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L76 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L84 |
| `sc_tarjeta` | `bdicheq` | ⚠️ sí | SELECT | L89 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L25 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L87 | VALIDACIÓN_NULL | `IF cNumCliente = '0' or cNumCliente is null then` |  |
| L98 | VALIDACIÓN_NULL | `if cNumCliente is null or cNumCliente = '' then` |  |
| L111 | FÓRMULA | `let iRegEncontrados = iRegEncontrados + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_buscaparticipacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscaparticipacion.sql` |
| **LOC (1er CREATE)** | 649 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "busca sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 13 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscaparticipacion(
  pUsuario                     CHAR(8)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCliente                   INT
  pNumCliente                  CHAR(20)
  pIp                          CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCliente` | `INT` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `cSistemaCuenta` | `CHAR(2)` | L11 |
| `cDescPArticipaciON` | `CHAR(30)` | L12 |
| `cParticipaciON` | `CHAR(1)` | L13 |
| `cNumCuenta` | `CHAR(20)` | L14 |
| `cNumClienteTitular` | `CHAR(20)` | L15 |
| `cNombre1` | `CHAR(26)` | L16 |
| `cNombre2` | `CHAR(26)` | L17 |
| `cApPaterno` | `CHAR(26)` | L18 |
| `cApMaterno` | `CHAR(26)` | L19 |
| `cRazonSocial` | `CHAR(60)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_firmantes` | `bdicheq` | ⚠️ sí | SELECT | L50 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L56 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L64 |
| `sw_ro_cta_participes` | `bdicnweb` | no | INSERT | L67 |
| `sw_ro_cte_ctatitular` | `bdicnweb` | no | INSERT | L90 |
| `sw_ro_ctecta` | `bdicnweb` | no | INSERT | L125 |
| `sc_beneficiario` | `bdicheq` | ⚠️ sí | SELECT | L171 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L291 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L296 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L298 |
| `sv_cotitular` | `bdinvers` | ⚠️ sí | SELECT | L412 |
| `sv_maeinv` | `bdinvers` | ⚠️ sí | SELECT | L418 |
| `sv_benefic` | `bdinvers` | ⚠️ sí | SELECT | L534 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `?participacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?participacion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_buscapersona`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscapersona.sql` |
| **LOC (1er CREATE)** | 542 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "busca sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 7 llamada(s): `sp_sw_ro_bitacorabusqueda`, `sp_sw_ro_consultimoidbusqueda`, `sp_sw_ro_buscaxnombre` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscapersona(
  pId_UsuarioC                 CHAR(8)
  pId_FuncionC                 CHAR(10)
  pTipoBusqueda                SMALLINT
  pIdOficio                    INT
  pNombre1                     CHAR(60)
  pNombre2                     CHAR(26)
  pApPaterno                   CHAR(26)
  pApMaterno                   CHAR(26)
  pFechaNacimiento             DATE
  pPagina                      SMALLINT
  pRegistros                   SMALLINT
  pIp                          CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pId_UsuarioC` | `CHAR(8)` | — | — |
| `pId_FuncionC` | `CHAR(10)` | — | — |
| `pTipoBusqueda` | `SMALLINT` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pNombre1` | `CHAR(60)` | — | — |
| `pNombre2` | `CHAR(26)` | — | — |
| `pApPaterno` | `CHAR(26)` | — | — |
| `pApMaterno` | `CHAR(26)` | — | — |
| `pFechaNacimiento` | `DATE` | — | — |
| `pPagina` | `SMALLINT` | — | — |
| `pRegistros` | `SMALLINT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pIp` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L40 |
| `cCodRetSp` | `CHAR(5)` | L41 |
| `cNumeroCliente` | `CHAR(20)` | L42 |
| `cNumeroCuenta` | `CHAR(20)` | L43 |
| `cNumeroTarjeta` | `CHAR(20)` | L44 |
| `cRfc` | `CHAR(13)` | L45 |
| `iIdNumConsulta` | `INT` | L46 |
| `cNombre1` | `CHAR(26)` | L47 |
| `cNombre2` | `CHAR(26)` | L48 |
| `cApPaterno` | `CHAR(26)` | L49 |
| `cApMaterno` | `CHAR(26)` | L50 |
| `cRazonSocial` | `CHAR(60)` | L51 |
| `iSqlErr` | `INT` | L52 |
| `iNoRows` | `INT` | L53 |
| `iExiste` | `INT` | L54 |
| `cCriterio` | `CHAR(60)` | L55 |
| `iIdBusqueda` | `INT` | L56 |
| `cTipoBusquedaPersona` | `CHAR(1)` | L57 |
| `cTipoPersona` | `CHAR(2)` | L58 |
| `cTipoCliente` | `CHAR(1)` | L59 |
| `cIdEncontrado` | `INT` | L60 |
| `iStatusBusqueda` | `INT` | L61 |
| `cDescStatusBusqueda` | `CHAR(20)` | L62 |
| `iRegsProc` | `INT` | L63 |
| `cOmitido` | `CHAR(1)` | L64 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_maeoficios` | `bdicnweb` | no | SELECT | L152 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L281 |
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L297 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sw_ro_bitacorabusqueda` | `bdicnweb` | no | L180 |
| `sp_sw_ro_consultimoidbusqueda` | `bdicnweb` | no | L187 |
| `sp_sw_ro_buscaxnombre` | `bdicnweb` | no | L198 |
| `sp_sw_ro_bitacoraresultados` | `bdicnweb` | no | L314 |
| `sp_sw_ro_buscaxrfc2` | `bdicnweb` | no | L331 |
| `sp_sw_ro_buscaxctectatar` | `bdicnweb` | no | L394 |
| `sp_sw_ro_buscacte_ctatar` | `bdicnweb` | no | L430 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L80 | CÓDIGO_RETORNO | `LET cCodRet     = '00000';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L155 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L166 | CÓDIGO_RETORNO | `LET cCodRet = '00087';` |  |
| L374 | VALIDACIÓN_NULL | `IF cTipoBusquedaPersona is null THEN` |  |
| L433 | VALIDACIÓN_NULL | `IF cTipoBusquedaPersona is null THEN` |  |
| L498 | VALIDACIÓN_NULL | `IF cTipoBusquedaPersona is null THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `?persona` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?persona` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_buscapersona_pba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscapersona_pba.sql` |
| **LOC (1er CREATE)** | 503 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "busca sw — SoftWare/Switch y ro — Rol Operativo (PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscapersona_pba(
  pId_UsuarioC                 CHAR(8)
  pId_FuncionC                 CHAR(10)
  pTipoBusqueda                SMALLINT
  pIdOficio                    INT
  pNombre1                     CHAR(60)
  pNombre2                     CHAR(26)
  pApPaterno                   CHAR(26)
  pApMaterno                   CHAR(26)
  pPagina                      SMALLINT
  pRegistros                   SMALLINT
  pIp                          CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pId_UsuarioC` | `CHAR(8)` | — | — |
| `pId_FuncionC` | `CHAR(10)` | — | — |
| `pTipoBusqueda` | `SMALLINT` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pNombre1` | `CHAR(60)` | — | — |
| `pNombre2` | `CHAR(26)` | — | — |
| `pApPaterno` | `CHAR(26)` | — | — |
| `pApMaterno` | `CHAR(26)` | — | — |
| `pPagina` | `SMALLINT` | — | — |
| `pRegistros` | `SMALLINT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pIp` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L39 |
| `cCodRetSp` | `CHAR(5)` | L40 |
| `cNumeroCliente` | `CHAR(20)` | L41 |
| `cNumeroCuenta` | `CHAR(20)` | L42 |
| `cNumeroTarjeta` | `CHAR(20)` | L43 |
| `cRfc` | `CHAR(13)` | L44 |
| `iIdNumConsulta` | `INT` | L45 |
| `cNombre1` | `CHAR(26)` | L46 |
| `cNombre2` | `CHAR(26)` | L47 |
| `cApPaterno` | `CHAR(26)` | L48 |
| `cApMaterno` | `CHAR(26)` | L49 |
| `cRazonSocial` | `CHAR(60)` | L50 |
| `iSqlErr` | `INT` | L51 |
| `iNoRows` | `INT` | L52 |
| `iExiste` | `INT` | L53 |
| `cCriterio` | `CHAR(60)` | L54 |
| `iIdBusqueda` | `INT` | L55 |
| `cTipoBusquedaPersona` | `CHAR(1)` | L56 |
| `cTipoPersona` | `CHAR(2)` | L57 |
| `cTipoCliente` | `CHAR(1)` | L58 |
| `cIdEncontrado` | `INT` | L59 |
| `iStatusBusqueda` | `INT` | L60 |
| `cDescStatusBusqueda` | `CHAR(20)` | L61 |
| `iRegsProc` | `INT` | L62 |
| `cOmitido` | `CHAR(1)` | L63 |
| *…10 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_maeoficios` | `bdicnweb` | no | SELECT | L145 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L269 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sw_ro_bitacorabusqueda` | `bdicnweb` | no | L173 |
| `sp_sw_ro_consultimoidbusqueda` | `bdicnweb` | no | L180 |
| `sp_sw_ro_buscaxnombre` | `bdicnweb` | no | L191 |
| `sp_sw_ro_bitacoraresultados` | `bdicnweb` | no | L283 |
| `sp_sw_ro_buscaxrfc2` | `bdicnweb` | no | L300 |
| `sp_sw_ro_buscaxctectatar` | `bdicnweb` | no | L355 |
| `sp_sw_ro_buscacte_ctatar` | `bdicnweb` | no | L391 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L76 | CÓDIGO_RETORNO | `LET cCodRet	= '00000';` |  |
| L131 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L148 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00087';` |  |
| L335 | VALIDACIÓN_NULL | `IF cTipoBusquedaPersona is null THEN` |  |
| L394 | VALIDACIÓN_NULL | `IF cTipoBusquedaPersona is null THEN` |  |
| L459 | VALIDACIÓN_NULL | `IF cTipoBusquedaPersona is null THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `?persona_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?persona_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_buscaxctectatar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscaxctectatar.sql` |
| **LOC (1er CREATE)** | 217 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "busca sw — SoftWare/Switch, ro — Rol Operativo, cliente, cuenta y Tarjeta" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 6 llamada(s): `sp_cnsif_consnumcte`, `sp_sw_ro_buscacte_ctatar`, `sp_sw_ro_bitacoraresultados` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscaxctectatar(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pTipoConsulta                int
  pCriterio                    char(20)
  pRecuperacion                int
  pIp                          char(15)
  pMac                         char(12)
) RETURNING char(5)  as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pTipoConsulta` | `int` | — | — |
| `pCriterio` | `char(20)` | — | — |
| `pRecuperacion` | `int` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L18 |
| `iSqlErr` | `int` | L19 |
| `cNumCte` | `char(20)` | L20 |
| `cNumCta` | `char(20)` | L21 |
| `cNumTar` | `char(20)` | L22 |
| `cNumeroCliente` | `char(20)` | L25 |
| `cRfc` | `char(13)` | L26 |
| `cNivelCliente` | `char(1)` | L27 |
| `cNombre1` | `char(26)` | L28 |
| `cNombre2` | `char(26)` | L29 |
| `cApPaterno` | `char(26)` | L30 |
| `cApMaterno` | `char(26)` | L31 |
| `cRazonSocial` | `char(60)` | L32 |
| `iNoRegistros` | `int` | L33 |
| `cEtiqueta` | `char(20)` | L34 |
| `iStatusBusqueda` | `smallint` | L35 |
| `cTipoPersona` | `char(2)` | L36 |
| `cTipoCliente` | `char(1)` | L37 |
| `cCodRetSp` | `char(5)` | L38 |
| `iRegsProc` | `int` | L39 |
| `iIdGenerado` | `int` | L40 |
| `cDescTipoCliente` | `char(40)` | L42 |
| `dFechaNacimiento` | `date` | L43 |
| `cSexo` | `char(1)` | L44 |
| `cDescTipoPersona` | `char(20)` | L45 |
| *…24 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_consnumcte` | `bdinteg` | ⚠️ sí | L134 |
| `sp_sw_ro_buscacte_ctatar` | `bdicnweb` | no | L146 |
| `sp_sw_ro_bitacoraresultados` | `bdicnweb` | no | L158 |
| `sp_sw_ro_bitacoracteenc` | `bdicnweb` | no | L171 |
| `sp_sw_ro_consctascteparticipacion` | `bdicnweb` | no | L182 |
| `sp_sw_ro_buscaparticipacion` | `bdicnweb` | no | L188 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L84 | FÓRMULA | `let iStatusBusqueda = 0; -- 0. No encontrado, 1. Encontrado, 2. Homonimo` |  |
| L210 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `busca` | ACCION | busca / localiza | 🔵 CONVENCIÓN | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_buscaxnombre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscaxnombre.sql` |
| **LOC (1er CREATE)** | 266 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "busca sw — SoftWare/Switch, ro — Rol Operativo y nombre" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 6 llamada(s): `sp_cnsif_buscacte`, `sp_sw_ro_bitacoraresultados`, `sp_cnsif_valida_nivelacceso_funcionalidad` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscaxnombre(
  pIdUsuario                   CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdBusqueda                  INT
  pIdOficio                    INT
  pTipoBusquedaPersona         CHAR(1)
  pNombre1                     CHAR(26)
  pNombre2                     CHAR(26)
  pApPaterno                   CHAR(26)
  pApMaterno                   CHAR(26)
  pRazonSocial                 CHAR(60)
  pFechaNacimiento             DATE
  pTipoCuenta                  CHAR(2)
  pIp                          CHAR(15)
  pMac                         CHAR(12)
  pNumRegistro                 INT
  pRecuperaciON                INT
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pTipoBusquedaPersona` | `CHAR(1)` | — | — |
| `pNombre1` | `CHAR(26)` | `nombre`=nombre | 🔵 CONVENCIÓN |
| `pNombre2` | `CHAR(26)` | `nombre`=nombre | 🔵 CONVENCIÓN |
| `pApPaterno` | `CHAR(26)` | — | — |
| `pApMaterno` | `CHAR(26)` | — | — |
| `pRazonSocial` | `CHAR(60)` | — | — |
| `pFechaNacimiento` | `DATE` | — | — |
| `pTipoCuenta` | `CHAR(2)` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(12)` | — | — |
| `pNumRegistro` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L19 |
| `dFechaNacimiento` | `DATE` | L20 |
| `cNumeroCliente` | `CHAR(20)` | L22 |
| `cRfc` | `CHAR(13)` | L23 |
| `cNivelCliente` | `CHAR(1)` | L24 |
| `cNombre1` | `CHAR(26)` | L25 |
| `cNombre2` | `CHAR(26)` | L26 |
| `cApPaterno` | `CHAR(26)` | L27 |
| `cApMaterno` | `CHAR(26)` | L28 |
| `cRazonSocial` | `CHAR(60)` | L29 |
| `iNoRegistros` | `INT` | L30 |
| `cEtiqueta` | `CHAR(20)` | L31 |
| `iStatusBusqueda` | `SMALLINT` | L32 |
| `cTipoPersona` | `CHAR(2)` | L33 |
| `cTipoCliente` | `CHAR(1)` | L34 |
| `cCodRetSp` | `CHAR(5)` | L35 |
| `iRegsProc` | `INT` | L36 |
| `iRegsProcHomonimos` | `INT` | L37 |
| `iIdGenerado` | `INT` | L38 |
| `iNivel` | `INT` | L39 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L200 |
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L204 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_buscacte` | `bdinteg` | ⚠️ sí | L65 |
| `sp_sw_ro_bitacoraresultados` | `bdicnweb` | no | L74 |
| `sp_cnsif_valida_nivelacceso_funcionalidad` | `bdinteg` | ⚠️ sí | L185 |
| `sp_sw_ro_bitacoracteenc` | `bdicnweb` | no | L224 |
| `sp_sw_ro_consctascteparticipacion` | `bdicnweb` | no | L237 |
| `sp_sw_ro_buscaparticipacion` | `bdicnweb` | no | L248 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | FÓRMULA | `LET iStatusBusqueda = 0; -- 0. No encontrado, 1. Encontrado, 2. Homonimo` |  |
| L71 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L187 | CÓDIGO_RETORNO | `LET cCodRet = '00076';` |  |
| L191 | CÓDIGO_RETORNO | `LET cCodRet = '00075';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `busca` | ACCION | busca / localiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `nombre` | ENTIDAD | nombre | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_buscaxrfc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscaxrfc.sql` |
| **LOC (1er CREATE)** | 263 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "busca sw — SoftWare/Switch, ro — Rol Operativo y RFC" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 4 llamada(s): `sp_sw_ro_bitacoraresultados`, `sp_sw_ro_bitacoracteenc`, `sp_sw_ro_consctascteparticipacion` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscaxrfc(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pRfc                         char(15)
  pIp                          char(15)
  pMac                         char(12)
) RETURNING char(5)  as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pRfc` | `char(15)` | `rfc`=RFC (registro fiscal) | ✅ CÓDIGO |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L17 |
| `iSqlErr` | `int` | L18 |
| `cNumCte` | `char(20)` | L19 |
| `cNumCta` | `char(20)` | L20 |
| `cNumTar` | `char(20)` | L21 |
| `cNumeroCliente` | `char(20)` | L24 |
| `cRfc` | `char(13)` | L25 |
| `cNivelCliente` | `char(1)` | L26 |
| `cNombre1` | `char(26)` | L27 |
| `cNombre2` | `char(26)` | L28 |
| `cApPaterno` | `char(26)` | L29 |
| `cApMaterno` | `char(26)` | L30 |
| `cRazonSocial` | `char(60)` | L31 |
| `iNoRegistros` | `int` | L32 |
| `cEtiqueta` | `char(20)` | L33 |
| `iStatusBusqueda` | `smallint` | L34 |
| `cTipoPersona` | `char(2)` | L35 |
| `cTipoCliente` | `char(1)` | L36 |
| `cCodRetSp` | `char(5)` | L37 |
| `iRegsProc` | `int` | L38 |
| `iIdGenerado` | `int` | L39 |
| `cDescTipoCliente` | `char(40)` | L41 |
| `dFechaNacimiento` | `date` | L42 |
| `cSexo` | `char(1)` | L43 |
| `cDescTipoPersona` | `char(20)` | L44 |
| *…26 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L135 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sw_ro_bitacoraresultados` | `bdicnweb` | no | L164 |
| `sp_sw_ro_bitacoracteenc` | `bdicnweb` | no | L177 |
| `sp_sw_ro_consctascteparticipacion` | `bdicnweb` | no | L188 |
| `sp_sw_ro_buscaparticipacion` | `bdicnweb` | no | L194 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L72 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L84 | FÓRMULA | `let iStatusBusqueda = 0; -- 0. No encontrado, 1. Encontrado, 2. Homonimo` |  |
| L139 | VALIDACIÓN_NULL | `if cNoCteRfcAlterno is null or cNoCteRfcAlterno = '' then` |  |
| L158 | FÓRMULA | `let iNivelCliente = 9; -- Falta buscar el nivel del cliente` |  |
| L213 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L217 | FÓRMULA | `let iNivelCliente = 9; -- Falta buscar el nivel del cliente` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `busca` | ACCION | busca / localiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `rfc` | ENTIDAD | RFC (registro fiscal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_buscaxrfc2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_buscaxrfc2.sql` |
| **LOC (1er CREATE)** | 363 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "busca sw — SoftWare/Switch, ro — Rol Operativo y RFC" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 5 llamada(s): `sp_sw_ro_bitacoraresultados`, `sp_cnsif_valida_nivelacceso_funcionalidad`, `sp_sw_ro_bitacoracteenc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_buscaxrfc2(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pRfc                         CHAR(15)
  pRegistros                   INT
  pRecuperaciON                INT
  pIp                          CHAR(15)
  pMac                         CHAR(12)
) RETURNING CHAR(5)  AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pRfc` | `CHAR(15)` | `rfc`=RFC (registro fiscal) | ✅ CÓDIGO |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperaciON` | `INT` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INT` | L19 |
| `cNumCte` | `CHAR(20)` | L20 |
| `cNumCta` | `CHAR(20)` | L21 |
| `cNumTar` | `CHAR(20)` | L22 |
| `cNumeroCliente` | `CHAR(20)` | L24 |
| `cRfc` | `CHAR(13)` | L25 |
| `cNivelCliente` | `CHAR(1)` | L26 |
| `cNombre1` | `CHAR(26)` | L27 |
| `cNombre2` | `CHAR(26)` | L28 |
| `cApPaterno` | `CHAR(26)` | L29 |
| `cApMaterno` | `CHAR(26)` | L30 |
| `cRazonSocial` | `CHAR(60)` | L31 |
| `iNoRegistros` | `INT` | L32 |
| `cEtiqueta` | `CHAR(20)` | L33 |
| `iStatusBusqueda` | `SMALLINT` | L34 |
| `cTipoPersona` | `CHAR(2)` | L35 |
| `cTipoCliente` | `CHAR(1)` | L36 |
| `cCodRetSp` | `CHAR(5)` | L37 |
| `iRegsProc` | `INT` | L38 |
| `iIdGenerado` | `INT` | L39 |
| `cDescTipoCliente` | `CHAR(40)` | L41 |
| `dFechaNacimiento` | `DATE` | L42 |
| `cSexo` | `CHAR(1)` | L43 |
| `cDescTipoPersona` | `CHAR(20)` | L44 |
| *…30 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L146 |
| `si_cliente_nivel` | `bdinteg` | ⚠️ sí | SELECT | L298 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sw_ro_bitacoraresultados` | `bdicnweb` | no | L188 |
| `sp_cnsif_valida_nivelacceso_funcionalidad` | `bdinteg` | ⚠️ sí | L293 |
| `sp_sw_ro_bitacoracteenc` | `bdicnweb` | no | L323 |
| `sp_sw_ro_consctascteparticipacion` | `bdicnweb` | no | L336 |
| `sp_sw_ro_buscaparticipacion` | `bdicnweb` | no | L347 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L87 | FÓRMULA | `LET iStatusBusqueda = 0; -- 0. No encontrado, 1. Encontrado, 2. Homonimo` |  |
| L200 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L207 | FÓRMULA | `LET iNivelCliente = 9; -- Falta buscar el nivel del cliente` |  |
| L295 | CÓDIGO_RETORNO | `LET cCodRet = '00076';` |  |
| L300 | CÓDIGO_RETORNO | `LET cCodRet = '00075';` |  |
| L305 | FÓRMULA | `LET iNivelCliente = 9; -- Falta buscar el nivel del cliente` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `busca` | ACCION | busca / localiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `rfc` | ENTIDAD | RFC (registro fiscal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consareabloqcap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consareabloqcap.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consareabloqcap(
  pIdUsuario                   char(8)
  pIdFuncion                   char(10)
) RETURNING char(5) as codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `cCodArea` | `char(2)` | L7 |
| `cDescripcion` | `char(35)` | L8 |
| `iSqlErr` | `int` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_areabloqueo` | `bdicheq` | ⚠️ sí | SELECT | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?area` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?area` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consbloqctacap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consbloqctacap.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, cuenta y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_blqconsultabloqueo` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consbloqctacap(
  pIdUsuario                   char(8)
  pIdFuncion                   char(10)
  pNumCta                      char(20)
) RETURNING char(5) as codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pNumCta` | `char(20)` | `cta`=cuenta | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L13 |
| `cMensaje` | `char(45)` | L14 |
| `cMovimiento` | `char(2)` | L15 |
| `cClave` | `char(5)` | L16 |
| `cStatus` | `char(2)` | L17 |
| `cImporteBloq` | `money(14,2)` | L18 |
| `cFechaBloq` | `date` | L19 |
| `cIdUsuario` | `char(10)` | L20 |
| `cDescMotivoBloq` | `char(45)` | L21 |
| `cDescOpcBloq` | `char(45)` | L22 |
| `cDescAreaSolic` | `char(25)` | L23 |
| `cDescTipoBloq` | `char(25)` | L24 |
| `cNumCliente` | `char(15)` | L25 |
| `cClaveArea` | `char(2)` | L26 |
| `cCodigoArea` | `char(1)` | L27 |
| `cCodTipoBloq` | `char(1)` | L28 |
| `cClaveTipoBloq` | `char(2)` | L29 |
| `iSqlErr` | `int` | L30 |
| `iNoRows` | `int` | L31 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_blqconsultabloqueo` | `bdicheq` | ⚠️ sí | L79 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L68 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L84 | FÓRMULA | `let iNoRows = iNoRows + 1;` |  |
| L95 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_conscausasbloqcred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_conscausasbloqcred.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, causa y crédito" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_conscausasbloqcred(
  pIdUsuario                   char(8)
  pIdFuncion                   char(10)
) RETURNING char(5) as codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `cCodBloq` | `char(2)` | L7 |
| `cDescripcion` | `char(35)` | L8 |
| `iSqlErr` | `int` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_causa_bloqueo` | `bdicred` | ⚠️ sí | SELECT | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `causa` | ENTIDAD | causa / motivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_conscodbloqcap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_conscodbloqcap.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, código y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_conscodbloqcap(
  pIdUsuario                   char(8)
  pIdFuncion                   char(10)
  pOpcion                      char(1)
) RETURNING char(5) as codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pOpcion` | `char(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `cCodBloq` | `char(2)` | L7 |
| `cDescripcion` | `char(35)` | L8 |
| `iSqlErr` | `int` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_bloqueo` | `bdicheq` | ⚠️ sí | SELECT | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `let cCodRet = '00044';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consctascte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consctascte.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, cuentas y cliente" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consctascte(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNumCliente                  char(20)
  pRegistros                   int
  pRecuperacion                int
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pRegistros` | `int` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L21 |
| `iSqlErr` | `int` | L22 |
| `iIdCtecta` | `int` | L23 |
| `cCuenta` | `char(20)` | L24 |
| `iIdTipoParticipe` | `int` | L25 |
| `cTipoParticipe` | `char(30)` | L26 |
| `cTipoCuenta` | `char(2)` | L27 |
| `cCveProducto` | `char(4)` | L28 |
| `cDescProducto` | `char(40)` | L29 |
| `cStatusCuenta` | `char(60)` | L30 |
| `cFechaApertura` | `char(10)` | L31 |
| `cSucursal` | `char(4)` | L32 |
| `saldo` | `money(14,2)` | L33 |
| `indicadores` | `char(14)` | L34 |
| `iNoRegs` | `int` | L35 |
| `cFechaAperturaOriginal` | `char(10)` | L36 |
| `cCuentaClabe` | `char(18)` | L37 |
| `cEjecutivoApertura` | `char(8)` | L38 |
| `cMotivoBloqueo` | `char(40)` | L39 |
| `cFechaBloqueo` | `char(10)` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L81 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L102 | FÓRMULA | `let iNoRegs = iNoRegs + 1;` |  |
| L109 | CÓDIGO_RETORNO | `let cCodRet = '00024';` |  |
| L113 | CÓDIGO_RETORNO | `let cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_consctascteoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consctascteoficio.sql` |
| **LOC (1er CREATE)** | 383 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, cuentas, cliente y oficio" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consctascteoficio(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdCliente                   INT
  pNumCliente                  CHAR(20)
  pRegistros                   INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |
| `pIdCliente` | `INT` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L37 |
| `iSqlErr` | `INT` | L38 |
| `iNoRegistros` | `INT` | L39 |
| `iIdCte` | `INT` | L40 |
| `iTipoCliente` | `CHAR(1)` | L41 |
| `cNombreTitular` | `CHAR(100)` | L42 |
| `cTipoCuenta` | `CHAR(2)` | L43 |
| `cDescTipoCuenta` | `CHAR(10)` | L44 |
| `cCuenta` | `CHAR(20)` | L45 |
| `cIndDatosTitular` | `CHAR(1)` | L46 |
| `cIndFechaApertura` | `CHAR(1)` | L47 |
| `cIndSucursalApertura` | `CHAR(1)` | L48 |
| `cIndDomicilioSucursal` | `CHAR(1)` | L49 |
| `cIndSaldo` | `CHAR(1)` | L50 |
| `cIndReportarStatusCuenta` | `CHAR(1)` | L51 |
| `cIndCtaBloqueada` | `CHAR(1)` | L52 |
| `cIndCtaBloqueadaPorSistema` | `CHAR(1)` | L53 |
| `dFechaApertura` | `DATE` | L54 |
| `cSucursalApertura` | `CHAR(60)` | L55 |
| `cDomicilioSucursal` | `CHAR(150)` | L56 |
| `mSaldo` | `money(14,2)` | L57 |
| `cStatusCuenta` | `CHAR(30)` | L58 |
| `dFechaBloqueo` | `DATE` | L59 |
| `cMotivoBloqueo` | `CHAR(40)` | L60 |
| `cOficio` | `CHAR(60)` | L61 |
| *…33 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L267 |
| `sw_ro_cte_ctatitular` | `bdicnweb` | no | SELECT | L283 |
| `v_sw_ro_tarjetasclientes` | `bdicnweb` | no | SELECT | L328 |
| `sc_producto` | `bdicheq` | ⚠️ sí | SELECT | L336 |
| `sv_instrum` | `bdinvers` | ⚠️ sí | SELECT | L341 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L348 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L183 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L194 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L205 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L302 | VALIDACIÓN_NULL | `IF cSucursalApertura is null THEN` |  |
| L317 | VALIDACIÓN_NULL | `IF cSucursalApertura is null THEN` |  |
| L320 | VALIDACIÓN_NULL | `IF cDomicilioSucursal is null THEN` |  |
| L364 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L367 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sw_ro_consctascteparticipacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consctascteparticipacion.sql` |
| **LOC (1er CREATE)** | 137 |
| **Callgraph** | ✅ fan_in=36 / fan_out=4 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, cuentas y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consctascteparticipacion(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNumCliente                  char(20)
  pRecuperacion                int
  pIp                          char(15)
  pMacAddress                  char(12)
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pRecuperacion` | `int` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMacAddress` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L5 |
| `iSqlErr` | `int` | L6 |
| `cSitemaCuentaConsulta` | `char(2)` | L7 |
| `cIndicadorChequera` | `char(1)` | L9 |
| `cSistemaCuenta` | `char(2)` | L10 |
| `cNoCuenta` | `char(20)` | L11 |
| `cClaveProducto` | `char(4)` | L12 |
| `cNombreProducto` | `char(40)` | L13 |
| `dFechaApertura` | `date` | L14 |
| `cStatusCuenta` | `char(60)` | L15 |
| `dFechaStatusCuenta` | `date` | L16 |
| `cClaveSucursal` | `char(4)` | L17 |
| `cEjecutivoAperturaCuenta` | `char(8)` | L18 |
| `mSaldoActual` | `money(14,2)` | L19 |
| `cNumTarjeta` | `char(20)` | L20 |
| `cStatusTarjeta` | `char(15)` | L21 |
| `cCuentaClabe` | `char(18)` | L22 |
| `dFechaAperturaOriginal` | `date` | L23 |
| `cCodRetSp` | `char(5)` | L24 |
| `iRegistros` | `int` | L25 |
| `iDiaCorte` | `int` | L26 |
| `cTipoParticipacion` | `char(1)` | L27 |
| `iExiste` | `int` | L28 |
| `cNumCuentaParticipe` | `char(20)` | L29 |
| `cStatusBloq` | `char(1)` | L30 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_ctascliente_temp` | `bdicnweb` | no | INSERT | L100 |
| `sw_ro_ctecta` | `bdicnweb` | no | INSERT | L116 |
| `sw_ro_ctascliente_temp` | `bdicnweb` | no | SELECT | L124 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_consprodcte` | `bdinteg` | ⚠️ sí | L81 |
| `sp_sw_ro_consstatusbloqueo` | `bdicnweb` | no | L97 |
| `sp_sw_ro_tarjetascte` | `bdicnweb` | no | L129 |
| `sp_sw_ro_buscaparticipacion` | `bdicnweb` | no | L132 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L39 | FÓRMULA | `let cSitemaCuentaConsulta = '00'; -- Todas la cuentas` |  |
| L77 | FÓRMULA | `let cTipoParticipacion = 'T'; -- en estas cuentas el cliente es titular` |  |
| L93 | VALIDACIÓN_NULL | `if iDiaCorte is null then` |  |
| L110 | FÓRMULA | `let iRegistros = iRegistros + pRecuperacion;` |  |
| L128 | FÓRMULA | `let pRecuperacion = iRegistros + pRecuperacion;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?participacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?participacion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consctesimgsctas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consctesimgsctas.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, clientes y cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consctesimgsctas(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L5 |
| `iNoRows` | `INT` | L6 |
| `cCodRet` | `CHAR(5)` | L7 |
| `iIdCte` | `INT` | L8 |
| `cNombreCte` | `CHAR(164)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00111';` |  |
| L61 | FÓRMULA | `LET iNoRows = iNoRows + 1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `?imgs` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imgs` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consctesimgsctas2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consctesimgsctas2.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, clientes y cuentas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consctesimgsctas2(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdCliente                   INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdCliente` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L8 |
| `iNoRows` | `INT` | L9 |
| `cCodRet` | `CHAR(5)` | L10 |
| `cTipoCuenta` | `CHAR(2)` | L11 |
| `cDescTipoCuenta` | `CHAR(10)` | L12 |
| `cNumCuenta` | `CHAR(20)` | L13 |
| `cCodRetSp` | `CHAR(5)` | L15 |
| `cProducto` | `CHAR(4)` | L16 |
| `cDescripcionProducto` | `CHAR(40)` | L17 |
| `cNumCliente` | `CHAR(20)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sw_ro_consultanombreproducto` | `bdicnweb` | no | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00111';` |  |
| L77 | FÓRMULA | `LET iNoRows = iNoRows + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `?imgs` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?imgs`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consdatoscteoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consdatoscteoficio.sql` |
| **LOC (1er CREATE)** | 143 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, datos, cliente y oficio" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consdatoscteoficio(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pRecuperacion                int
  pRegistros                   int
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | `oficio`=oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN |
| `pRecuperacion` | `int` | — | — |
| `pRegistros` | `int` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L21 |
| `cCodRet` | `char(5)` | L22 |
| `cNumCte` | `char(20)` | L23 |
| `cNombre` | `char(164)` | L24 |
| `cNumCta` | `char(20)` | L25 |
| `cNumTarjeta` | `char(20)` | L26 |
| `cRfc` | `char(13)` | L27 |
| `cIndEmpleo` | `char(1)` | L28 |
| `cIndDomicilio` | `char(1)` | L29 |
| `cIndNacionalidad` | `char(1)` | L30 |
| `cIndRfc` | `char(1)` | L31 |
| `cIndExpDig` | `char(1)` | L32 |
| `cDirEmpleo` | `char(255)` | L33 |
| `cDirActual` | `char(255)` | L34 |
| `cNacionalidad` | `char(15)` | L35 |
| `cTelOficina` | `char(15)` | L36 |
| `iNoRows` | `int` | L37 |
| `iIdTipoBusqueda` | `smallint` | L38 |
| `iIdBusqueda` | `int` | L39 |
| `iIdCliente` | `int` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ctepf` | `bdinteg` | ⚠️ sí | SELECT | L123 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |
| `sp_sw_ro_consdireccion` | `bdicnweb` | no | L116 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L73 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L78 | CÓDIGO_RETORNO | `let cCodRet = '00098';` |  |
| L83 | CÓDIGO_RETORNO | `let cCodRet = '00098';` |  |
| L133 | FÓRMULA | `let iNoRows = iNoRows + 1;` |  |
| L139 | CÓDIGO_RETORNO | `let cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consdireccion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consdireccion.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y dirección" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_consdirec` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consdireccion(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pNumCliente                  CHAR(20)
  pTipoDirecciON               INT
) RETURNING CHAR(255) AS direccion,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pTipoDirecciON` | `INT` | `direccion`=dirección | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vcodret` | `CHAR(5)` | L4 |
| `cnumcliente` | `char (20)` | L5 |
| `vtipo_dir` | `CHAR(1)` | L6 |
| `vsecuencia` | `INT` | L7 |
| `vcalle` | `CHAR(40)` | L8 |
| `vnumeroextcalle` | `CHAR(10)` | L9 |
| `vnumerointcalle` | `CHAR(10)` | L10 |
| `vdepartamento` | `CHAR(6)` | L11 |
| `vcolonia` | `CHAR(60)` | L12 |
| `vmunicipio` | `CHAR(60)` | L13 |
| `vciudad` | `CHAR(60)` | L14 |
| `vestado` | `CHAR(30)` | L15 |
| `vpais` | `CHAR(20)` | L16 |
| `vcod_postal` | `CHAR(5)` | L17 |
| `vtelefono1` | `CHAR(13)` | L18 |
| `vtelefono2` | `CHAR(13)` | L19 |
| `vtelefono3` | `CHAR(13)` | L20 |
| `vextensiON` | `CHAR(5)` | L21 |
| `vpuntocardinal` | `CHAR(1)` | L22 |
| `vmanzana` | `CHAR(30)` | L23 |
| `votros` | `CHAR(30)` | L24 |
| `vandador` | `CHAR(30)` | L25 |
| `vetapa` | `CHAR(30)` | L26 |
| `vlote` | `CHAR(30)` | L27 |
| `ventrada` | `CHAR(30)` | L28 |
| *…6 más…* | | |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_consdirec` | `bdinteg` | ⚠️ sí | L65 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `direccion` | ENTIDAD | dirección | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consedosctaoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consedosctaoficio.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, estado, cuenta y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consedosctaoficio(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pRecuperaciON                INT
  pRegistros                   INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L12 |
| `iNoRows` | `INT` | L13 |
| `iExiste` | `INT` | L14 |
| `cCodRet` | `CHAR(5)` | L15 |
| `cCliente` | `CHAR(20)` | L16 |
| `cCuenta` | `CHAR(20)` | L17 |
| `cTipoCuenta` | `CHAR(2)` | L18 |
| `dFechaInicio` | `DATE` | L19 |
| `dFechaFin` | `DATE` | L20 |
| `cNombreCliente` | `CHAR(164)` | L21 |
| `cDescTipoCuenta` | `CHAR(10)` | L22 |
| `cCodRetSp` | `CHAR(5)` | L23 |
| `cProducto` | `CHAR(4)` | L24 |
| `cDescripcionProduto` | `CHAR(40)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L63 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sw_ro_consultanombreproducto` | `bdicnweb` | no | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L94 | FÓRMULA | `LET iNoRows = iNoRows + 1;` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `edo` | ENTIDAD | estado | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consfacultadosxcta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consfacultadosxcta.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, OS — Originación de Solicitudes y cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consfacultadosxcta(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pNumCuenta                   CHAR(20)
  pNumRegistros                INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pNumRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / 🔴 SINTÉTICO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `cNoCliente` | `CHAR(20)` | L11 |
| `cNoCuenta` | `CHAR(20)` | L12 |
| `cNombre` | `CHAR(140)` | L13 |
| `cParticipaciON` | `CHAR(1)` | L14 |
| `cDescParticipaciON` | `CHAR(30)` | L15 |
| `iRegs` | `INT` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_firmantes` | `bdicheq` | ⚠️ sí | SELECT | L43 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L48 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L63 |
| `sv_cotitular` | `bdinvers` | ⚠️ sí | SELECT | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iRegs = iRegs + 1;` |  |
| L71 | FÓRMULA | `LET iRegs = iRegs + 1;` |  |
| L91 | FÓRMULA | `LET iRegs = iRegs + 1;` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?facultad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?facultad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consfiltrosmovtoscta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consfiltrosmovtoscta.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, movimiento y cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consfiltrosmovtoscta(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pNumCliente                  char(20)
  pIdOficio                    int
  pIdCliente                   int
  pNumCuenta                   char(20)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCuenta` | `char(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L12 |
| `cCodRet` | `char(5)` | L13 |
| `cNoCte` | `char(20)` | L14 |
| `cSistemaCuenta` | `char(2)` | L15 |
| `dFechaInicio` | `date` | L16 |
| `dFechaFin` | `date` | L17 |
| `cSucursal` | `char(4)` | L18 |
| `cEmpleado` | `char(8)` | L19 |
| `decMonto` | `decimal(18,2)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_filtros_movtos` | `bdicnweb` | no | SELECT | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L44 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?filt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?filt`, `?s`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consimagenes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consimagenes.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y imágenes" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consimagenes(
  pUsuarioC                    CHAR(8)
  pIdFuncionC                  CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCte                       INT
  pNumCliente                  CHAR(20)
  pTipoCuenta                  CHAR(2)
  pNumeroCuenta                CHAR(20)
  pRegistros                   INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `CHAR(8)` | — | — |
| `pIdFuncionC` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCte` | `INT` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pTipoCuenta` | `CHAR(2)` | — | — |
| `pNumeroCuenta` | `CHAR(20)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L17 |
| `iSqlErr` | `INT` | L18 |
| `cGrupo` | `CHAR(3)` | L19 |
| `cDescGrupo` | `CHAR(30)` | L20 |
| `cCveDocto` | `CHAR(4)` | L21 |
| `cDescDocto` | `CHAR(35)` | L22 |
| `iSecuencia` | `SMALLINT` | L23 |
| `cCuenta` | `CHAR(20)` | L24 |
| `cProducto` | `CHAR(4)` | L25 |
| `cDescProducto` | `CHAR(40)` | L26 |
| `dFechaRegistro` | `DATE` | L27 |
| `cSistemaCuenta` | `CHAR(2)` | L28 |
| `cOmitido` | `CHAR(1)` | L29 |
| `iNoRegistros` | `INT` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_cteexp` | `bdicnweb` | no | SELECT | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L72 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `imagenes` | ENTIDAD | imágenes / documentos digitales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_consimgsdoctosoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consimgsdoctosoficio.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, saldo, OS — Originación de Solicitudes y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consimgsdoctosoficio(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdCliente                   INT
  pNumCuenta                   CHAR(20)
  pTipoBusqueda                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN |
| `pIdCliente` | `INT` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pTipoBusqueda` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cDescDocto` | `CHAR(40)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iNoRegs` | `INT` | L8 |
| `cNumCuenta` | `CHAR(20)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00005';` |  |
| L48 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?img` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `?ct` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?img`, `?ct` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consimsctectaoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consimsctectaoficio.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, cliente, cuenta y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consimsctectaoficio(
  pUsuarioC                    CHAR(8)
  pIdFuncionC                  CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCte                       INT
  pNumCliente                  CHAR(20)
  pRegistros                   INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `CHAR(8)` | — | — |
| `pIdFuncionC` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCte` | `INT` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INT` | L10 |
| `cCveDocto` | `CHAR(4)` | L11 |
| `iSecuencia` | `INT` | L12 |
| `cSistemaCuenta` | `CHAR(2)` | L13 |
| `iNoRegistros` | `INT` | L14 |
| `cGrupoDocto` | `CHAR(3)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_cteexp` | `bdicnweb` | no | SELECT | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ims` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ims` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consmotivobloqcap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consmotivobloqcap.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, motivo y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consmotivobloqcap(
  pIdUsuario                   char(8)
  pIdFuncion                   char(10)
) RETURNING char(5) as codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `cCodMotivo` | `char(2)` | L7 |
| `cDescripcion` | `char(35)` | L8 |
| `iSqlErr` | `int` | L9 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `motivo` | ENTIDAD | motivo / causa | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consmovtos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consmovtos.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y movimiento" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consmovtos(
  pUsuarioC                    char(8)
  pFuncionC                    char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNumCuenta                   char(20)
  pNumRegistros                int
  pRecuperacion                int
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `char(8)` | — | — |
| `pFuncionC` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pNumRegistros` | `int` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L19 |
| `iSqlErr` | `int` | L20 |
| `cFechaMovto` | `char(10)` | L21 |
| `cHora` | `char(12)` | L22 |
| `cFolioSucursal` | `char(16)` | L23 |
| `cTransaccion` | `char(4)` | L24 |
| `cDescTransaccion` | `char(50)` | L25 |
| `cReversado` | `char(1)` | L26 |
| `dMonto` | `decimal(18,2)` | L27 |
| `cSucursal` | `char(4)` | L28 |
| `cNaturaleza` | `char(1)` | L29 |
| `mSaldo` | `money(14,2)` | L30 |
| `cProcedencia` | `char(20)` | L31 |
| `cDescProcedencia` | `char(50)` | L32 |
| `cReferencia` | `char(40)` | L33 |
| `cIndOmitir` | `char(1)` | L34 |
| `iNoRegs` | `int` | L35 |
| `cNumTarjeta` | `char(20)` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_movtos` | `bdicnweb` | no | SELECT | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L67 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L89 | FÓRMULA | `let iNoRegs = iNoRegs + 1;` |  |
| L95 | CÓDIGO_RETORNO | `let cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consmovtosctaoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consmovtosctaoficio.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, movimiento, cuenta y oficio" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consmovtosctaoficio(
  pUsuario                     char(8)
  pIdFuncion                   char(20)
  pIdOficio                    int
  pIdCliente                   int
  pNumCuenta                   char(20)
  pRegistros                   int
  pRecuperacion                int
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(20)` | — | — |
| `pIdOficio` | `int` | `oficio`=oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN |
| `pIdCliente` | `int` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pRegistros` | `int` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L11 |
| `iSqlErr` | `int` | L12 |
| `dFechaMovto` | `date` | L13 |
| `cDescMovto` | `char(50)` | L14 |
| `dMonto` | `decimal(18,2)` | L15 |
| `cSucursal` | `char(4)` | L16 |
| `cNombreSuc` | `char(40)` | L17 |
| `cCiudad` | `char(60)` | L18 |
| `cEstado` | `char(30)` | L19 |
| `iNoRegistros` | `int` | L20 |
| `cBuscarMovtos` | `char(1)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L68 |
| `sw_ro_movtos` | `bdicnweb` | no | SELECT | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `let cCodRet = '00098';` |  |
| L55 | CÓDIGO_RETORNO | `let cCodRet = '00098';` |  |
| L79 | FÓRMULA | `let iNoRegistros = iNoRegistros + 1;` |  |
| L85 | CÓDIGO_RETORNO | `let cCodRet = '01001';` |  |
| L89 | CÓDIGO_RETORNO | `let cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consnotas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consnotas.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consnotas(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L7 |
| `iSqlErr` | `int` | L8 |
| `iNoRegistros` | `int` | L9 |
| `iSecuenciaNota` | `int` | L10 |
| `cNota` | `char(255)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_notascte` | `bdicnweb` | no | SELECT | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L47 | FÓRMULA | `let iNoRegistros = iNoRegistros + 1;` |  |
| L54 | CÓDIGO_RETORNO | `let cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?notas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?notas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consnotasoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consnotasoficio.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consnotasoficio(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `iNoRegistros` | `INT` | L9 |
| `iSecuenciaNota` | `INT` | L10 |
| `cNota` | `CHAR(255)` | L11 |
| `cStatusBus` | `CHAR(1)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L43 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?notas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?notas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consopcbloqcap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consopcbloqcap.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consopcbloqcap(
  pIdUsuario                   char(8)
  pIdFuncion                   char(10)
  pOpcion                      char(1)
) RETURNING char(5) as codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pOpcion` | `char(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `cCodOpc` | `int` | L7 |
| `cDescripcion` | `char(35)` | L8 |
| `iSqlErr` | `int` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_opcionbloqueo` | `bdicheq` | ⚠️ sí | SELECT | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L40 | CÓDIGO_RETORNO | `let cCodRet = '00044';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?opc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?opc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consopcoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consopcoficio.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consopcoficio(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L5 |
| `iSqlErr` | `int` | L6 |
| `cIndCertificaImgs` | `char(1)` | L7 |
| `cIndCertificaEdosCta` | `char(1)` | L8 |
| `cDetalleMovotos` | `char(1)` | L9 |
| `cCtasBloqueadasPorSistema` | `char(1)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L41 |
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L45 |
| `sw_ro_maeoficios` | `bdicnweb` | no | SELECT | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L50 | VALIDACIÓN_NULL | `if cIndCertificaImgs is null and cIndCertificaEdosCta is null and cDetalleMovotos is null and cCtasB` |  |
| L51 | FÓRMULA | `let cCodRet = '00110'; -- No existe el numero de oficio buscado` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?opc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?opc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consparamsedocta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consparamsedocta.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, parámetro y Estado de Cuenta — documento periódico de movimientos y saldos; generado como PDF" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consparamsedocta(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNumCliente                  char(20)
  pNumCuenta                   char(20)
  pTipoCuenta                  char(2)
  pIp                          char(15)
  pMacAddress                  char(12)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pTipoCuenta` | `char(2)` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMacAddress` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L8 |
| `cCodRet` | `char(5)` | L9 |
| `dFechaInicio` | `char(10)` | L10 |
| `dFechaFin` | `char(10)` | L11 |
| `cProducto` | `char(4)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_edocta` | `bdicnweb` | no | SELECT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L43 | CÓDIGO_RETORNO | `let cCodRet = '00048';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edocta` | ENTIDAD | Estado de Cuenta — documento periódico de movimientos y sald | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_conspermaeoficios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_conspermaeoficios.sql` |
| **LOC (1er CREATE)** | 429 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_conspermaeoficios(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pFchInicio                   CHAR(10)
  pFchFin                      CHAR(10)
  pStatusBusqueda              CHAR(1)
  pRegistros                   INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pFchInicio` | `CHAR(10)` | — | — |
| `pFchFin` | `CHAR(10)` | — | — |
| `pStatusBusqueda` | `CHAR(1)` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L27 |
| `iSqlErr` | `INT` | L29 |
| `cNombre` | `CHAR(164)` | L31 |
| `crfc` | `CHAR(13)` | L33 |
| `cNumcte` | `CHAR(20)` | L35 |
| `cCuenta` | `CHAR(20)` | L37 |
| `cMotivoBlo` | `CHAR(40)` | L39 |
| `mMontoBlo` | `MONEY(14,2)` | L41 |
| `cfchOficio` | `CHAR(10)` | L43 |
| `cOficio` | `CHAR(60)` | L45 |
| `cUserINSERT` | `CHAR(12)` | L47 |
| `cCodRetorno` | `CHAR(5)` | L49 |
| `cNumEmple` | `CHAR(8)` | L51 |
| `cUsuEstado` | `CHAR(1)` | L53 |
| `cUsuIp` | `CHAR(15)` | L55 |
| `cUsuMac` | `CHAR(17)` | L57 |
| `cNombreUser` | `CHAR(45)` | L59 |
| `iUsuBloqueo` | `INTEGER` | L61 |
| `iContador` | `INTEGER` | L63 |
| `iRegistros` | `INTEGER` | L65 |
| `ibusq` | `INTEGER` | L67 |
| `iTipoBus` | `INTEGER` | L69 |
| `iIdBusq` | `INTEGER` | L71 |
| `iIdOficio` | `INTEGER` | L73 |
| `idTipBusq` | `INTEGER` | L75 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L187 |
| `sw_ro_bloqueos` | `bdicnweb` | no | SELECT | L339 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_consultaejecutivo` | `bdinteg` | ⚠️ sí | L259 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L79 | CÓDIGO_RETORNO | `LET cCodRet     = '00000';` |  |
| L175 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L205 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |
| L265 | FÓRMULA | `LET iContador= iContador + 1;` |  |
| L361 | FÓRMULA | `LET iContador= iContador + 1;` |  |
| L411 | FÓRMULA | `LET iContador= iContador + 1;` |  |
| L421 | CÓDIGO_RETORNO | `LET cCodRet='01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?permae` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?permae`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consprimerparrafo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consprimerparrafo.sql` |
| **LOC (1er CREATE)** | 24 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y ARR — producto de ahorro/inversión recurrente (primer)" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consprimerparrafo(
) RETURNING char(255)
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `lParrafo` | `lvarchar(1024)` | L4 |
| `cSeccion` | `char(255)` | L5 |
| `iTamCorteCadena` | `int` | L6 |
| `iTamTotalParrafo` | `int` | L7 |
| `iSaltoCadena` | `int` | L8 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | FÓRMULA | `let lParrafo = ', en nombre y representación de BanCoppel, S.A., Institución de Banca Mpultiple ("Ba` |  |
| L20 | FÓRMULA | `let iSaltoCadena = iSaltoCadena + iTamCorteCadena;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `primer` | MODIF | primer | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `arr` | ENTIDAD | ARR — producto de ahorro/inversión recurrente (CLABE, interé | 🟡 INFERIDO | nombre_sp |
| `?afo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?p`, `?afo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consrepoficon`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consrepoficon.sql` |
| **LOC (1er CREATE)** | 163 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consrepoficon(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pFchInicio                   CHAR(10)
  pFchFin                      CHAR(10)
  pRegistros                   INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pFchInicio` | `CHAR(10)` | — | — |
| `pFchFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INT` | L14 |
| `cUsuariosIn` | `CHAR(8)` | L15 |
| `iOficios` | `INTEGER` | L16 |
| `iPerLoca` | `INTEGER` | L17 |
| `iPerHomo` | `INTEGER` | L18 |
| `iPerNoLo` | `INTEGER` | L19 |
| `cStatusAux` | `CHAR(1)` | L20 |
| `cStatusAux2` | `CHAR(1)` | L21 |
| `cStatusAux0` | `CHAR(1)` | L22 |
| `cStatusAux3` | `CHAR(1)` | L23 |
| `cNombreUser` | `CHAR(45)` | L24 |
| `cfchOficio` | `CHAR(10)` | L25 |
| `cfchRecep` | `CHAR(10)` | L26 |
| `cOficio` | `CHAR(60)` | L27 |
| `cExpediente` | `CHAR(60)` | L28 |
| `cCodRetorno` | `CHAR(5)` | L29 |
| `cNumEmple` | `CHAR(8)` | L30 |
| `cUsuEstado` | `CHAR(1)` | L31 |
| `cUsuIp` | `CHAR(15)` | L32 |
| `cUsuMac` | `CHAR(17)` | L33 |
| `iUsuBloqueo` | `INTEGER` | L34 |
| `iContador` | `INTEGER` | L35 |
| `iRegistros` | `INTEGER` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L95 |
| `sw_ro_maeoficios` | `bdicnweb` | no | SELECT | L143 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_consultaejecutivo` | `bdinteg` | ⚠️ sí | L147 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | CÓDIGO_RETORNO | `LET cCodRet  = '00000';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |
| L150 | FÓRMULA | `LET iContador= iContador + 1;` |  |
| L157 | CÓDIGO_RETORNO | `LET cCodRet='01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `ofi` | ENTIDAD | oficio | 🟡 INFERIDO | nombre_sp |
| `con` | ACCION | consulta | 🟡 INFERIDO | nombre_sp |

---

## `sp_sw_ro_consrepopermaeofi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consrepopermaeofi.sql` |
| **LOC (1er CREATE)** | 187 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consrepopermaeofi(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pFchInicio                   CHAR(10)
  pFchFin                      CHAR(10)
  pRegistros                   INT
  pRecuperaciON                INT
  pUsuarioINSERT               CHAR(8)
) RETURNING CHAR(5) AS     CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pFchInicio` | `CHAR(10)` | — | — |
| `pFchFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |
| `pUsuarioINSERT` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INT` | L16 |
| `cUserINSERT` | `CHAR (8)` | L17 |
| `cNombreUser` | `CHAR(45)` | L18 |
| `cNombre` | `CHAR(164)` | L19 |
| `crfc` | `CHAR(13)` | L20 |
| `cNumcte` | `CHAR(20)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cIndBloSis` | `CHAR(1)` | L23 |
| `cMotivoBlo` | `CHAR(40)` | L24 |
| `mMontoBlo` | `MONEY(14,2)` | L25 |
| `cRespuesta` | `CHAR(15)` | L26 |
| `cfchOficio` | `CHAR(10)` | L27 |
| `cOficio` | `CHAR(60)` | L28 |
| `cCodRetorno` | `CHAR(5)` | L29 |
| `cNumEmple` | `CHAR(8)` | L30 |
| `cUsuEstado` | `CHAR(1)` | L31 |
| `cUsuIp` | `CHAR(15)` | L32 |
| `cUsuMac` | `CHAR(17)` | L33 |
| `iUsuBloqueo` | `INTEGER` | L34 |
| `iContador` | `INTEGER` | L35 |
| `iRegistros` | `INTEGER` | L36 |
| `iId` | `INTEGER` | L37 |
| `idBusq` | `INTEGER` | L38 |
| `iIdBusqueda` | `INTEGER` | L39 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L104 |
| `sw_ro_buscaper` | `bdicnweb` | no | SELECT | L155 |
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L167 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_consultaejecutivo` | `bdinteg` | ⚠️ sí | L144 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet     = '00000';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L115 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |
| L174 | FÓRMULA | `LET iContador= iContador + 1;` |  |
| L182 | CÓDIGO_RETORNO | `LET cCodRet='01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?rmae` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ofi` | ENTIDAD | oficio | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rmae` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consreppermaeofi`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consreppermaeofi.sql` |
| **LOC (1er CREATE)** | 184 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consreppermaeofi(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pFchInicio                   CHAR(10)
  pFchFin                      CHAR(10)
  pRegistros                   INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS     CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pFchInicio` | `CHAR(10)` | — | — |
| `pFchFin` | `CHAR(10)` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INT` | L16 |
| `cUserINSERT` | `CHAR (8)` | L17 |
| `cNombreUser` | `CHAR(45)` | L18 |
| `cNombre` | `CHAR(164)` | L19 |
| `crfc` | `CHAR(13)` | L20 |
| `cNumcte` | `CHAR(20)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cIndBloSis` | `CHAR(1)` | L23 |
| `cMotivoBlo` | `CHAR(40)` | L24 |
| `mMontoBlo` | `MONEY(14,2)` | L25 |
| `cRespuesta` | `CHAR(15)` | L26 |
| `cfchOficio` | `CHAR(10)` | L27 |
| `cOficio` | `CHAR(60)` | L28 |
| `cCodRetorno` | `CHAR(5)` | L29 |
| `cNumEmple` | `CHAR(8)` | L30 |
| `cUsuEstado` | `CHAR(1)` | L31 |
| `cUsuIp` | `CHAR(15)` | L32 |
| `cUsuMac` | `CHAR(17)` | L33 |
| `iUsuBloqueo` | `INTEGER` | L34 |
| `iContador` | `INTEGER` | L35 |
| `iRegistros` | `INTEGER` | L36 |
| `idResPer` | `INTEGER` | L37 |
| `idBusq` | `INTEGER` | L38 |
| `idOficio` | `INTEGER` | L39 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_buscaper` | `bdicnweb` | no | SELECT | L104 |
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L132 |
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L155 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_consultaejecutivo` | `bdinteg` | ⚠️ sí | L140 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET cCodRet  	= '00000';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet='00017';` |  |
| L171 | FÓRMULA | `LET iContador= iContador + 1;` |  |
| L179 | CÓDIGO_RETORNO | `LET cCodRet='01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?permae` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ofi` | ENTIDAD | oficio | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?permae` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consresponsablesoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consresponsablesoficio.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y oficio (especial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consresponsablesoficio(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L7 |
| `iSqlErr` | `int` | L8 |
| `iIdFacultado` | `int` | L9 |
| `cNombreFacultado` | `char(70)` | L10 |
| `cPuestoFacultado` | `char(40)` | L11 |
| `iNoRegistros` | `int` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_oficio_facultados` | `bdicnweb` | no | SELECT | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L52 | FÓRMULA | `let iNoRegistros = iNoRegistros + 1;` |  |
| L58 | CÓDIGO_RETORNO | `let cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?onsables` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?onsables` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consrolesfunciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consrolesfunciones.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y rol" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consrolesfunciones(
  pUsuarioC                    CHAR(8)
  pIdFunciON                   CHAR(10)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iIdRolFunciON` | `INT` | L7 |
| `cDescripciON` | `CHAR(25)` | L8 |
| `iSqlErr` | `INT` | L9 |
| `iNoRows` | `INT` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_roles_funciones` | `bdicnweb` | no | SELECT | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rol` | ENTIDAD | rol / perfil | 🔵 CONVENCIÓN | nombre_sp |
| `?esfunciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?esfunciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consstatusbloqueo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consstatusbloqueo.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y estatus" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consstatusbloqueo(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pTipoCuenta                  char(2)
  pNumCuenta                   char(20)
) RETURNING char(1) as status_bloqueo,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pTipoCuenta` | `char(2)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cStatusBloqueo` | `char(1)` | L6 |
| `cMotivoBloqueo` | `char(40)` | L7 |
| `cFechaBloqueo` | `char(10)` | L8 |
| `cCodErrCred` | `char(6)` | L10 |
| `cMensErrCred` | `char(80)` | L11 |
| `cEmpresaCred` | `char(3)` | L12 |
| `cNumCuentaCred` | `char(20)` | L13 |
| `cNumCteCred` | `char(20)` | L14 |
| `cNombreCteCred` | `char(50)` | L15 |
| `cSucursalCred` | `char(4)` | L16 |
| `cBloqueoCred` | `char(30)` | L17 |
| `cCausaCred` | `char(50)` | L18 |
| `cStatusCred` | `char(2)` | L19 |
| `dFechaBloqCred` | `date` | L20 |
| `cCodRetCap` | `char(5)` | L23 |
| `cMovimientoCap` | `char(2)` | L24 |
| `cStatusCap` | `char(2)` | L25 |
| `cImporteBloqCap` | `money(14,2)` | L26 |
| `cFechaBloqCap` | `date` | L27 |
| `cNumClienteCap` | `char(15)` | L28 |
| `cClaveAreaCap` | `char(2)` | L29 |
| `cCodigoAreaCap` | `char(1)` | L30 |
| `cCodTipoBloqCap` | `char(1)` | L31 |
| `cClaveTipoBloqCap` | `char(2)` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_tipobloqueo` | `bdicheq` | ⚠️ sí | SELECT | L72 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sw_ro_consbloqctacap` | `bdicnweb` | no | L64 |
| `sp_consultacuenta` | `bdicred` | ⚠️ sí | L75 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_constarjetasctacteoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_constarjetasctacteoficio.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, tarjetas, cuenta, cliente y oficio" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_constarjetasctacteoficio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(20)
  pIdOficio                    INT
  pIdCliente                   INT
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(20)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN |
| `pIdCliente` | `INT` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L5 |
| `cCodRet` | `CHAR(5)` | L6 |
| `cNumTarjeta` | `CHAR(20)` | L7 |
| `iNoRegs` | `INT` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `v_sw_ro_tarjetasclientes` | `bdicnweb` | no | SELECT | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tarjetas` | ENTIDAD | tarjetas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_constarjetascte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_constarjetascte.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, tarjetas y cliente" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_constarjetascte(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCliente                   INT
  pRegistros                   INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCliente` | `INT` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cTipoCuenta` | `CHAR(2)` | L12 |
| `cNumTarjeta` | `CHAR(20)` | L13 |
| `cStatusTarjeta` | `CHAR(20)` | L14 |
| `cCuenta` | `CHAR(20)` | L15 |
| `iNoRegistros` | `INTEGER` | L16 |
| `iIdTarjeta` | `INTEGER` | L17 |
| `cIndReporte` | `CHAR(1)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `v_sw_ro_tarjetasclientes` | `bdicnweb` | no | SELECT | L48 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tarjetas` | ENTIDAD | tarjetas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_constipobloqcred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_constipobloqcred.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y crédito (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_constipobloqcred(
  pIdUsuario                   char(8)
  pIdFuncion                   char(10)
) RETURNING char(5) as codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `cCodBloq` | `char(2)` | L7 |
| `cDescripcion` | `char(35)` | L8 |
| `iSqlErr` | `int` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sd_bloqueoscuenta` | `bdicred` | ⚠️ sí | SELECT | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L12 | FÓRMULA | `let cCodBloq = -1;` |  |
| L34 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consultahomonimosrfc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consultahomonimosrfc.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, OS — Originación de Solicitudes y RFC" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consultahomonimosrfc(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pRegistros                   int
  pRecuperacion                int
) RETURNING char(5) as  codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pRegistros` | `int` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO / ✅ CÓDIGO |
| `pRecuperacion` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L5 |
| `cRfc` | `char(13)` | L6 |
| `iSqlErr` | `int` | L7 |
| `iIdBusqueda` | `int` | L8 |
| `iNoRegs` | `int` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L51 | FÓRMULA | `let iNoRegs = iNoRegs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?ho` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mon` | PREFIJO | monitor / módulo | 🟡 INFERIDO | nombre_sp |
| `?im` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `rfc` | ENTIDAD | RFC (registro fiscal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ho`, `?im` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consultamovtosdiarioscta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consultamovtosdiarioscta.sql` |
| **LOC (1er CREATE)** | 710 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, movimiento, sd — saldo disponible, OS — Originación de Solicitudes y cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consultamovtosdiarioscta(
  cID_USUARIOC                 char(8)
  cID_FUNCIONC                 CHAR(10)
  cNUMCUENTA                   CHAR(20)
  dPERIODOI                    DATE
  dPERIODOF                    DATE
  cSISTEMACUENTA               CHAR(20)
  pUsuario                     CHAR(8)
  cSuc                         CHAR(4)
  mImporte                     MONEY(14,2)
  pNumRegistro                 INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5)  AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cID_USUARIOC` | `char(8)` | — | — |
| `cID_FUNCIONC` | `CHAR(10)` | — | — |
| `cNUMCUENTA` | `CHAR(20)` | — | — |
| `dPERIODOI` | `DATE` | — | — |
| `dPERIODOF` | `DATE` | — | — |
| `cSISTEMACUENTA` | `CHAR(20)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `cSuc` | `CHAR(4)` | — | — |
| `mImporte` | `MONEY(14,2)` | — | — |
| `pNumRegistro` | `INTEGER` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iexiste` | `INT` | L25 |
| `cCodRet` | `CHAR(5)` | L26 |
| `iSql_err` | `INT` | L27 |
| `dFecha` | `DATE` | L29 |
| `dHora` | `DATETIME HOUR to FRACTION(3)` | L30 |
| `cTransaccion` | `CHAR(4)` | L31 |
| `cD_Transaccion` | `CHAR(50)` | L32 |
| `mMonto` | `MONEY(14,2)` | L33 |
| `cNaturaleza` | `CHAR(1)` | L34 |
| `mSaldo` | `MONEY(14,2)` | L35 |
| `cReferencia` | `CHAR(40)` | L36 |
| `cReversos` | `CHAR(1)` | L37 |
| `cReversados` | `CHAR(1)` | L38 |
| `cSucursal` | `CHAR(4)` | L39 |
| `cFolio` | `CHAR(16)` | L40 |
| `cProcedencia` | `CHAR(20)` | L41 |
| `cD_Procedencia` | `CHAR(50)` | L42 |
| `dPeriodoI_1` | `DATE` | L43 |
| `dPeriodoF_1` | `DATE` | L44 |
| `sNUMSERIAL` | `INT8` | L45 |
| `sNumSecuencia` | `INT8` | L46 |
| `cUsuario` | `CHAR(8)` | L47 |
| `cReferencia23` | `CHAR(23)` | L48 |
| `cNumtarjeta` | `CHAR(20)` | L50 |
| `cconsmovhis` | `CHAR(10)` | L52 |
| *…8 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L166 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L198 |
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L200 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L212 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L222 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L232 |
| `sc_movhis_old2` | `bdicheq` | ⚠️ sí | SELECT | L242 |
| `sc_movhis_old3` | `bdicheq` | ⚠️ sí | SELECT | L252 |
| `sc_movhis_old4` | `bdicheq` | ⚠️ sí | SELECT | L262 |
| `tf_success_transac` | `bditransfer` | ⚠️ sí | SELECT | L272 |
| `si_procedencia` | `bdinteg` | ⚠️ sí | SELECT | L398 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L425 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L427 |
| `sd_movdia` | `bdicred` | ⚠️ sí | SELECT | L438 |
| `sd_movhis` | `bdicred` | ⚠️ sí | SELECT | L448 |
| `sd_movhis_new` | `bdicred` | ⚠️ sí | SELECT | L458 |
| `sd_movdiacrd` | `bdicred` | ⚠️ sí | SELECT | L468 |
| `sd_movhiscrd` | `bdicred` | ⚠️ sí | SELECT | L478 |
| `sv_maeinv` | `bdinvers` | ⚠️ sí | SELECT | L616 |
| `sv_movdia` | `bdinvers` | ⚠️ sí | SELECT | L627 |
| `sv_movhis` | `bdinvers` | ⚠️ sí | SELECT | L635 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_permisosejecutivo` | `bdinteg` | ⚠️ sí | L143 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L125 | CÓDIGO_RETORNO | `LET cCodRet='00098';` |  |
| L130 | CÓDIGO_RETORNO | `LET cCodRet='00098';` |  |
| L410 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L587 | FÓRMULA | `LET iCont=iCont+1;` |  |
| L591 | FÓRMULA | `LET iCont=iCont - 1;` |  |
| L595 | FÓRMULA | `LET iCont=iCont - 1;` |  |
| L606 | CÓDIGO_RETORNO | `LET cCodRet = '00039';` |  |
| L681 | FÓRMULA | `LET iCont=iCont+1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `sd` | ENTIDAD | sd — saldo disponible (abreviación en código de crédito) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?iari` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?iari` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consultaoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consultaoficio.sql` |
| **LOC (1er CREATE)** | 83 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consultaoficio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pOficio                      CHAR(60)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pOficio` | `CHAR(60)` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `iIdOficio` | `INTEGER` | L14 |
| `dFechaOficio` | `DATE` | L15 |
| `dFechaRecepcion` | `DATE` | L16 |
| `cExpediente` | `CHAR(60)` | L17 |
| `iTipoOficio` | `INTEGER` | L18 |
| `iInstitucion1n` | `INTEGER` | L19 |
| `iInstitucion2n` | `INTEGER` | L20 |
| `cIndTerminado` | `CHAR(1)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_maeoficios` | `bdicnweb` | no | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | VALIDACIÓN_NULL | `IF iIdOficio IS NULL THEN` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00110';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_sw_ro_consultaperiodosedoscta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consultaperiodosedoscta.sql` |
| **LOC (1er CREATE)** | 270 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 82 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo, periodo, estado y cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consultaperiodosedoscta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
  pSistemaCuenta               CHAR(2)
  pNumCliente                  CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipoOperacion               SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pSistemaCuenta` | `CHAR(2)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipoOperacion` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cPeriodo` | `CHAR(6)` | L8 |
| `dFechaInicio` | `DATE` | L9 |
| `dFechaFin` | `DATE` | L10 |
| `iDiaCorte` | `SMALLINT` | L11 |
| `iAnioMesInicio` | `INTEGER` | L12 |
| `iAnioMesFin` | `INTEGER` | L13 |
| `iFecha1Valida` | `SMALLINT` | L14 |
| `iFecha2Valida` | `SMALLINT` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cNumProducto` | `CHAR(4)` | L17 |
| `dFechaApertura` | `DATE` | L18 |
| `cTpoPersona` | `CHAR(2)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L74 |
| `sc_maenoc` | `bdicheq` | ⚠️ sí | SELECT | L82 |
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L88 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L99 |
| `sd_maecred_old` | `bdicred` | ⚠️ sí | SELECT | L107 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L115 |
| `sd_definicion` | `bdicred` | ⚠️ sí | SELECT | L148 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00044';` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00048';` |  |
| L84 | VALIDACIÓN_NULL | `IF idiacorte IS NULL THEN` |  |
| L182 | FÓRMULA | `LET dFechaFin = ADD_MONTHS(pFechaFinal, 1); -- Aumento de un mes a la decha original` |  |
| L183 | FÓRMULA | `LET dFechaFin = MDY(MONTH(dFechaFin), iDiaCorte - 1, YEAR(dFechaFin));` |  |
| L192 | FÓRMULA | `LET dFechaInicio = DATE(pFechaInicial - 1 UNITS MONTH); -- Resta de un mes a la fecha original` |  |
| L193 | FÓRMULA | `LET dFechaInicio = MDY(MONTH(dFechaInicio), iDiaCorte + 1, YEAR(dFechaInicio));` |  |
| L224 | FÓRMULA | `LET dFechaFin = ADD_MONTHS(pFechaInicial, 1); -- Aumento de un mes a la decha original` |  |
| L225 | FÓRMULA | `LET dFechaFin = MDY(MONTH(dFechaFin), iDiaCorte - 1, YEAR(dFechaFin));` |  |
| L240 | FÓRMULA | `LET dFechaInicio = DATE(pFechaInicial - 1 UNITS MONTH); -- Resta de un mes a la fecha original` |  |
| L241 | FÓRMULA | `LET dFechaInicio = MDY(MONTH(dFechaInicio), iDiaCorte + 1, YEAR(dFechaInicio));` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `periodo` | ENTIDAD | periodo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edo` | ENTIDAD | estado | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?s`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consultapersonasencontradas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consultapersonasencontradas.sql` |
| **LOC (1er CREATE)** | 166 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consultapersonasencontradas(
  pUsuarioC                    CHAR(8)
  pFuncionC                    CHAR(10)
  pIdOficio                    INT
  pIp                          CHAR(15)
  pMacAddress                  CHAR(12)
  pNumRegistro                 INT
  pNumRecuperaciON             INT
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `CHAR(8)` | — | — |
| `pFuncionC` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |
| `pNumRegistro` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pNumRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L28 |
| `cCodRet` | `CHAR(5)` | L29 |
| `cNumCliente` | `CHAR(20)` | L30 |
| `cRfc` | `CHAR(15)` | L31 |
| `cNombre1` | `CHAR(26)` | L32 |
| `cNombre2` | `CHAR(26)` | L33 |
| `cApPaterno` | `CHAR(26)` | L34 |
| `cApMaterno` | `CHAR(26)` | L35 |
| `cRazonSocial` | `CHAR(60)` | L36 |
| `cNumCuenta` | `CHAR(20)` | L37 |
| `cNumTarjeta` | `CHAR(20)` | L38 |
| `cTipoPersona` | `CHAR(2)` | L39 |
| `cTipoCliente` | `CHAR(1)` | L40 |
| `cStatusBusq` | `INT` | L41 |
| `cDescStatusBusqueda` | `CHAR(20)` | L42 |
| `iIdEncontrado` | `INT` | L43 |
| `iIdCte` | `INT` | L44 |
| `iRegistros` | `INT` | L45 |
| `cOmitido` | `CHAR(1)` | L46 |
| `cBloqueado` | `CHAR(1)` | L47 |
| `cTerminado` | `CHAR(1)` | L48 |
| `cTipoCuenta` | `CHAR(2)` | L49 |
| `cIndRfc` | `CHAR(1)` | L50 |
| `cIndEmpleo` | `CHAR(1)` | L51 |
| `cIndDomicilio` | `CHAR(1)` | L52 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L122 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L149 | FÓRMULA | `LET iRegistros = iRegistros + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?personasen` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `?radas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?personasen`, `?radas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_consultimoidbusqueda`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_consultimoidbusqueda.sql` |
| **LOC (1er CREATE)** | 40 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta sw — SoftWare/Switch, ro — Rol Operativo y identificador" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_consultimoidbusqueda(
  pTipoBusqueda                smallint
  pIdOficio                    int
  pApPaterno                   char(26)
  pApMaterno                   char(26)
  pNombre1                     char(26)
  pNombre2                     char(26)
  pRazonSocial                 char(60)
  pRfc                         char(13)
  pNumcte                      char(20)
  pCuenta                      char(20)
  pNumTarjeta                  char(20)
  pUsuario                     char(8)
  pIp                          char(15)
  pMac                         char(12)
) RETURNING int as id_busqueda
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pTipoBusqueda` | `smallint` | `busqueda`=búsqueda | 🔵 CONVENCIÓN |
| `pIdOficio` | `int` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pApPaterno` | `char(26)` | — | — |
| `pApMaterno` | `char(26)` | — | — |
| `pNombre1` | `char(26)` | — | — |
| `pNombre2` | `char(26)` | — | — |
| `pRazonSocial` | `char(60)` | — | — |
| `pRfc` | `char(13)` | — | — |
| `pNumcte` | `char(20)` | — | — |
| `pCuenta` | `char(20)` | — | — |
| `pNumTarjeta` | `char(20)` | — | — |
| `pUsuario` | `char(8)` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iIdBusqueda` | `int` | L6 |
| `iSqlErr` | `int` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_buscaper` | `bdicnweb` | no | SELECT | L21 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ultimo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `busqueda` | ACCION | búsqueda | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ultimo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_eliminafiltrosmovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_eliminafiltrosmovimientos.sql` |
| **LOC (1er CREATE)** | 47 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "elimina sw — SoftWare/Switch, ro — Rol Operativo y movimientos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_eliminafiltrosmovimientos(
  pUsuario                     char(10)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNumCliente                  char(20)
  pNumCuenta                   char(20)
  pSistemaCuenta               char(2)
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(10)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pSistemaCuenta` | `char(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L6 |
| `cCodRet` | `char(5)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_filtros_movtos` | `bdicnweb` | no | SELECT | L39 |
| `sw_ro_filtros_movtos` | `bdicnweb` | no | DELETE | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L30 | CÓDIGO_RETORNO | `let cCodRet = '00077';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `?filt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movimientos` | ENTIDAD | movimientos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?filt`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_eliminamovtos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_eliminamovtos.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "elimina sw — SoftWare/Switch, ro — Rol Operativo y movimiento" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_eliminamovtos(
  pUsuario                     char(8)
  pFuncion                     char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCte                       int
  pNumeroCuenta                char(20)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCte` | `int` | — | — |
| `pNumeroCuenta` | `char(20)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L5 |
| `cCodRet` | `char(5)` | L6 |
| `iRegBorrados` | `int` | L7 |
| `iRegs` | `int` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_movtos` | `bdicnweb` | no | SELECT | L33 |
| `sw_ro_movtos` | `bdicnweb` | no | DELETE | L33 |
| `sw_ro_ctecta` | `bdicnweb` | no | UPDATE | L37 |
| `sw_ro_resulcte` | `bdicnweb` | no | UPDATE | L41 |
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L46 |
| `sw_ro_maeoficios` | `bdicnweb` | no | UPDATE | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_eliminanotas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_eliminanotas.sql` |
| **LOC (1er CREATE)** | 36 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "elimina sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_eliminanotas(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCte                       int
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCte` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L5 |
| `iSqlErr` | `int` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_notascte` | `bdicnweb` | no | SELECT | L30 |
| `sw_ro_notascte` | `bdicnweb` | no | DELETE | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L21 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `?notas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?notas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_eliminaoficiofacul`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_eliminaoficiofacul.sql` |
| **LOC (1er CREATE)** | 38 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "elimina sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_eliminaoficiofacul(
  pUsuario                     char(8)
  pFuncionC                    char(10)
  pIdOficio                    int
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pFuncionC` | `char(10)` | — | — |
| `pIdOficio` | `int` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L4 |
| `cCodRet` | `char(5)` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_oficio_facultados` | `bdicnweb` | no | SELECT | L31 |
| `sw_ro_oficio_facultados` | `bdicnweb` | no | DELETE | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?facul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?facul` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_eliminaparamsedocta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_eliminaparamsedocta.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "elimina sw — SoftWare/Switch, ro — Rol Operativo, parámetro y Estado de Cuenta — documento periódico de movimientos y saldos; generado como PDF" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_eliminaparamsedocta(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNumCliente                  char(20)
  pNumCuenta                   char(20)
  pTipoCuenta                  char(2)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pTipoCuenta` | `char(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L6 |
| `cCodRet` | `char(5)` | L7 |
| `iNoRegsProcesados` | `smallint` | L8 |
| `iRegs` | `int` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_edocta` | `bdicnweb` | no | SELECT | L42 |
| `sw_ro_edocta` | `bdicnweb` | no | DELETE | L42 |
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L57 |
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `let cCodRet = '00048';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `elimina` | ACCION | elimina | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edocta` | ENTIDAD | Estado de Cuenta — documento periódico de movimientos y sald | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_eliminapersona`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_eliminapersona.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "elimina sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_eliminapersona(
  pUsuarioC                    char(8)
  pFuncionC                    char(10)
  pIdResultPer                 int
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `char(8)` | — | — |
| `pFuncionC` | `char(10)` | — | — |
| `pIdResultPer` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L4 |
| `cCodRet` | `char(5)` | L5 |
| `iIdResultPer` | `int` | L6 |
| `iIdOficio` | `int` | L7 |
| `iResultCte` | `int` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L35 |
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L39 |
| `sw_ro_notascte` | `bdicnweb` | no | SELECT | L43 |
| `sw_ro_notascte` | `bdicnweb` | no | DELETE | L43 |
| `sw_ro_filtros_movtos` | `bdicnweb` | no | SELECT | L46 |
| `sw_ro_filtros_movtos` | `bdicnweb` | no | DELETE | L46 |
| `sw_ro_movtos` | `bdicnweb` | no | SELECT | L49 |
| `sw_ro_movtos` | `bdicnweb` | no | DELETE | L49 |
| `sw_ro_edocta` | `bdicnweb` | no | SELECT | L52 |
| `sw_ro_edocta` | `bdicnweb` | no | DELETE | L52 |
| `sw_ro_cteexp` | `bdicnweb` | no | SELECT | L55 |
| `sw_ro_cteexp` | `bdicnweb` | no | DELETE | L55 |
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L58 |
| `sw_ro_ctecta` | `bdicnweb` | no | DELETE | L58 |
| `sw_ro_cta_participes` | `bdicnweb` | no | SELECT | L61 |
| `sw_ro_cta_participes` | `bdicnweb` | no | DELETE | L61 |
| `sw_ro_cte_ctatitular` | `bdicnweb` | no | SELECT | L64 |
| `sw_ro_cte_ctatitular` | `bdicnweb` | no | DELETE | L64 |
| `sw_ro_bloqueos` | `bdicnweb` | no | SELECT | L67 |
| `sw_ro_bloqueos` | `bdicnweb` | no | DELETE | L67 |
| `sw_ro_resulcte` | `bdicnweb` | no | DELETE | L70 |
| `sw_ro_resulper` | `bdicnweb` | no | DELETE | L73 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?persona` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?persona` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_encabezadooficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_encabezadooficio.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo, encabezado y oficio" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Tokens confirmados en el vocab pero DML no correlaciona con el propósito |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_encabezadooficio(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INT` | L11 |
| `dFechaRecepciON` | `DATE` | L12 |
| `cOficio` | `CHAR(60)` | L13 |
| `cExpediente` | `CHAR(60)` | L14 |
| `cInstitucion1n` | `CHAR(50)` | L15 |
| `cDireccionInstitucion1n` | `CHAR(255)` | L16 |
| `cNombreFacultado` | `CHAR(70)` | L17 |
| `cPuestoFacultado` | `CHAR(70)` | L18 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | VALIDACIÓN_NULL | `IF dFechaRecepciON is null OR dFechaRecepciON = '' THEN` |  |
| L64 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `encabezado` | ENTIDAD | encabezado | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_evalua_fecha`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_evalua_fecha.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo y fecha" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_evalua_fecha(
  pFechaEvaluar                char(10)
) RETURNING date as fecha_valida
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaEvaluar` | `char(10)` | `fecha`=fecha | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `dFechaEvaluada` | `date` | L4 |
| `cFecha` | `char(10)` | L5 |
| `iMes` | `smallint` | L6 |
| `iAnio` | `integer` | L7 |
| `iMAxDia` | `smallint` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdicnweb` | no | SELECT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L32 | FÓRMULA | `let cFecha = trim(cFecha)\|\|'-01-'\|\|iAnio;` |  |
| L38 | FÓRMULA | `let cFecha = substr(cFecha,1,2)\|\|trim('-0'\|\|iMAxDia)\|\|'-'\|\|substr(cFecha,7,4);` |  |
| L40 | FÓRMULA | `let cFecha = substr(cFecha,1,2)\|\|trim('-'\|\|iMAxDia)\|\|'-'\|\|substr(cFecha,7,4);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?_evalua_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fecha` | ENTIDAD | fecha | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?_evalua_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_facultadosseleccionados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_facultadosseleccionados.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo, OS — Originación de Solicitudes y cc — cuenta corriente" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=3 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_facultadosseleccionados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdOficio                    INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdOficio` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `idRolFuncion` | `INTEGER` | L9 |
| `idFacultado` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_maeoficios` | `bdicnweb` | no | SELECT | L35 |
| `sw_ro_oficio_facultados` | `bdicnweb` | no | SELECT | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdOficio IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00110';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_facultad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?sele` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?ionad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_facultad`, `?sele`, `?ionad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_finalizaoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_finalizaoficio.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo y oficio (final)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: DELETE, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_finalizaoficio(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L7 |
| `cCodRet` | `CHAR(5)` | L8 |
| `iMovtos` | `INT` | L9 |
| `iImagenes` | `INT` | L10 |
| `iTmp` | `INT` | L11 |
| `iCtas` | `INT` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_ctascliente_temp` | `bdicnweb` | no | SELECT | L42 |
| `sw_ro_ctascliente_temp` | `bdicnweb` | no | DELETE | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `final` | MODIF | final | 🔵 CONVENCIÓN | nombre_sp |
| `?iza` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iza` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_guardabloqueoctas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_guardabloqueoctas.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=44 / fan_out=1 |
| **Propósito inferido** | "guarda sw — SoftWare/Switch, ro — Rol Operativo y cuentas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_guardabloqueoctas(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pTipoBloqueo                 char(1)
  pNumCliente                  char(20)
  pTipoCuenta                  char(2)
  pNumCuenta                   char(20)
  pMontoBloqueo                money(14,2)
  pIdCausaBloqueo              char(3)
  pCausaBloqueo                char(40)
  pFolioOperacion              char(16)
  pIp                          char(15)
  pMac                         char(12)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pTipoBloqueo` | `char(1)` | `bloqueo`=bloquea cuenta | 🔵 CONVENCIÓN |
| `pNumCliente` | `char(20)` | — | — |
| `pTipoCuenta` | `char(2)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pMontoBloqueo` | `money(14,2)` | `bloqueo`=bloquea cuenta | 🔵 CONVENCIÓN |
| `pIdCausaBloqueo` | `char(3)` | `bloqueo`=bloquea cuenta | 🔵 CONVENCIÓN |
| `pCausaBloqueo` | `char(40)` | `bloqueo`=bloquea cuenta | 🔵 CONVENCIÓN |
| `pFolioOperacion` | `char(16)` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L7 |
| `iSqlErr` | `int` | L8 |
| `iRegGuardados` | `smallint` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_bloqueos` | `bdicnweb` | no | INSERT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L42 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_guardafacultadosoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_guardafacultadosoficio.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda sw — SoftWare/Switch, ro — Rol Operativo, OS — Originación de Solicitudes y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_guardafacultadosoficio(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdFacultado                 INT
  pIdRolFunciON                INT
  pIp                          CHAR(15)
  pMac                         CHAR(12)
  pStatus                      CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |
| `pIdFacultado` | `INT` | — | — |
| `pIdRolFunciON` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pIp` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(12)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cExiste` | `CHAR(1)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_oficio_facultados` | `bdicnweb` | no | SELECT | L35 |
| `sw_ro_oficio_facultados` | `bdicnweb` | no | UPDATE | L44 |
| `sw_ro_oficio_facultados` | `bdicnweb` | no | INSERT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?facultad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?facultad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_guardafiltrosmovimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_guardafiltrosmovimientos.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "guarda sw — SoftWare/Switch, ro — Rol Operativo y movimientos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_guardafiltrosmovimientos(
  pUsuario                     char(10)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNumCliente                  char(20)
  pNumCuenta                   char(20)
  pSistemaCuenta               char(2)
  pFechaInicio                 char(10)
  pFechaFin                    char(10)
  pSucursal                    char(4)
  pEmpleadoConsulta            char(10)
  pMonto                       decimal(18,2)
  pIp                          char(15)
  pMac                         char(12)
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(10)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pSistemaCuenta` | `char(2)` | — | — |
| `pFechaInicio` | `char(10)` | — | — |
| `pFechaFin` | `char(10)` | — | — |
| `pSucursal` | `char(4)` | — | — |
| `pEmpleadoConsulta` | `char(10)` | — | — |
| `pMonto` | `decimal(18,2)` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMac` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L7 |
| `cCodRet` | `char(5)` | L8 |
| `pfechaInicio1` | `date` | L9 |
| `pfechafin1` | `date` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_filtros_movtos` | `bdicnweb` | no | SELECT | L46 |
| `sw_ro_filtros_movtos` | `bdicnweb` | no | DELETE | L46 |
| `sw_ro_filtros_movtos` | `bdicnweb` | no | INSERT | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L37 | CÓDIGO_RETORNO | `let cCodRet = '00077';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?filt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movimientos` | ENTIDAD | movimientos | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?filt`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_guardaimagenes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_guardaimagenes.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "guarda sw — SoftWare/Switch, ro — Rol Operativo y imágenes" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_guardaimagenes(
  pUsuarioC                    CHAR(8)
  pIdFuncionC                  CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCte                       INT
  pTipoCuenta                  CHAR(2)
  pNumCte                      CHAR(20)
  pNumCta                      CHAR(20)
  pProducto                    CHAR(4)
  pDescProducto                CHAR(40)
  pCodDocto                    CHAR(4)
  pDescripcionDocto            CHAR(35)
  pSecuencia                   INT
  pCodGpo                      CHAR(3)
  pDescGpo                     CHAR(30)
  pFechaRegistro               CHAR(10)
  pIndOmitido                  CHAR(1)
  pIp                          CHAR(15)
  pMac                         CHAR(15)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `CHAR(8)` | — | — |
| `pIdFuncionC` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCte` | `INT` | — | — |
| `pTipoCuenta` | `CHAR(2)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |
| `pProducto` | `CHAR(4)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pDescProducto` | `CHAR(40)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pCodDocto` | `CHAR(4)` | — | — |
| `pDescripcionDocto` | `CHAR(35)` | — | — |
| `pSecuencia` | `INT` | — | — |
| `pCodGpo` | `CHAR(3)` | — | — |
| `pDescGpo` | `CHAR(30)` | — | — |
| `pFechaRegistro` | `CHAR(10)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pIndOmitido` | `CHAR(1)` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `pFechaRegistro1` | `date` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_cteexp` | `bdicnweb` | no | INSERT | L61 |
| `sw_ro_resulcte` | `bdicnweb` | no | UPDATE | L72 |
| `sw_ro_maeoficios` | `bdicnweb` | no | UPDATE | L78 |
| `sw_ro_ctecta` | `bdicnweb` | no | UPDATE | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `imagenes` | ENTIDAD | imágenes / documentos digitales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_ro_guardamovtos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_guardamovtos.sql` |
| **LOC (1er CREATE)** | 188 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "guarda sw — SoftWare/Switch, ro — Rol Operativo y movimiento" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_guardamovtos(
  pUsuario                     CHAR(8)
  pFunciON                     CHAR(10)
  pIdOficio                    INT
  pIdBusqueda                  INT
  pIdCte                       INT
  pNumCliente                  CHAR(20)
  pNumCuenta                   CHAR(20)
  pNumTarjeta                  CHAR(20)
  pTipoCuenta                  CHAR(2)
  pFechaMovto                  CHAR(10)
  pHora                        CHAR(15)
  pFolioSucursal               CHAR(16)
  pTransacciON                 CHAR(4)
  pDescTransacciON             CHAR(50)
  pReversado                   CHAR(1)
  pMonto                       decimal(18,2)
  pSucursal                    CHAR(4)
  pNaturaleza                  CHAR(1)
  pSaldo                       money(14,2)
  pProcedencia                 CHAR(20)
  pDescProcedencia             CHAR(50)
  pReferencia                  CHAR(40)
  pIndOmitir                   CHAR(1)
  pIp                          CHAR(15)
  pMac                         CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pIdBusqueda` | `INT` | — | — |
| `pIdCte` | `INT` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |
| `pNumTarjeta` | `CHAR(20)` | — | — |
| `pTipoCuenta` | `CHAR(2)` | — | — |
| `pFechaMovto` | `CHAR(10)` | `movto`=movimiento | 🔵 CONVENCIÓN |
| `pHora` | `CHAR(15)` | — | — |
| `pFolioSucursal` | `CHAR(16)` | — | — |
| `pTransacciON` | `CHAR(4)` | — | — |
| `pDescTransacciON` | `CHAR(50)` | — | — |
| `pReversado` | `CHAR(1)` | — | — |
| `pMonto` | `decimal(18,2)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNaturaleza` | `CHAR(1)` | — | — |
| `pSaldo` | `money(14,2)` | — | — |
| `pProcedencia` | `CHAR(20)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pDescProcedencia` | `CHAR(50)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pReferencia` | `CHAR(40)` | — | — |
| `pIndOmitir` | `CHAR(1)` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L10 |
| `iSecuencia` | `INT` | L11 |
| `cCodRet` | `CHAR(5)` | L12 |
| `cNombreSucursal` | `CHAR(40)` | L13 |
| `cEstado` | `CHAR(2)` | L14 |
| `cCiudad` | `CHAR(3)` | L15 |
| `cDescEstado` | `CHAR(30)` | L16 |
| `cDescCiudad` | `CHAR(60)` | L17 |
| `dFechaMovto` | `date` | L18 |
| `cCodRetPtf` | `varchar(5)` | L20 |
| `cIdptf` | `varchar(5)` | L21 |
| `cTipos` | `varchar(1)` | L22 |
| `cClavesit` | `char(3)` | L23 |
| `cFechasit` | `date` | L24 |
| `cCalles` | `varchar(100)` | L25 |
| `cNumext` | `varchar(6)` | L26 |
| `cNumint` | `varchar(5)` | L27 |
| `cCvecol` | `char(8)` | L28 |
| `cColonias` | `varchar(100)` | L29 |
| `cCvemun` | `char(5)` | L30 |
| `cMunicipio` | `varchar(60)` | L31 |
| `cVelocalidades` | `char(14)` | L32 |
| `cLocalidades` | `varchar(60)` | L33 |
| `cCps` | `char(5)` | L34 |
| `cCiudades` | `char(3)` | L35 |
| *…5 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L133 |
| `si_estados` | `bdinteg` | ⚠️ sí | SELECT | L138 |
| `si_ciudades` | `bdinteg` | ⚠️ sí | SELECT | L143 |
| `sw_ro_movtos` | `bdicnweb` | no | INSERT | L149 |
| `sw_ro_ctecta` | `bdicnweb` | no | UPDATE | L167 |
| `sw_ro_resulcte` | `bdicnweb` | no | UPDATE | L175 |
| `sw_ro_maeoficios` | `bdicnweb` | no | UPDATE | L182 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L44 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L103 | CÓDIGO_RETORNO | `LET cCodRet = '00077';` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00077';` |  |
| L119 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `movto` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_guardanota`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_guardanota.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "guarda sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_guardanota(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNota                        varchar(255)
  pIp                          char(15)
  pMacAddress                  char(12)
) RETURNING char(5) as codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNota` | `varchar(255)` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMacAddress` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L7 |
| `iSqlErr` | `int` | L8 |
| `iNumRegistro` | `int` | L9 |
| `iResulPer` | `int` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L40 |
| `sw_ro_notascte` | `bdicnweb` | no | INSERT | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?nota` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nota` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_guardanotaoficio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_guardanotaoficio.sql` |
| **LOC (1er CREATE)** | 47 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "guarda sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_guardanotaoficio(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pIdNota                      INT
  pStatusBus                   CHAR(1)
  pNota                        VARCHAR(255)
  pIp                          CHAR(15)
  pMacAddress                  CHAR(12)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | `oficio`=oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN |
| `pIdNota` | `INT` | — | — |
| `pStatusBus` | `CHAR(1)` | — | — |
| `pNota` | `VARCHAR(255)` | — | — |
| `pIp` | `CHAR(15)` | — | — |
| `pMacAddress` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `iNumRegistro` | `INT` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_notasoficio` | `bdicnweb` | no | INSERT | L37 |
| `sw_ro_notasoficio` | `bdicnweb` | no | UPDATE | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?nota` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nota` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_guardaparamsedocta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_guardaparamsedocta.sql` |
| **LOC (1er CREATE)** | 270 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "guarda sw — SoftWare/Switch, ro — Rol Operativo, parámetro y Estado de Cuenta — documento periódico de movimientos y saldos; generado como PDF" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_guardaparamsedocta(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pTipoOperacion               int
  pNumCliente                  char(20)
  pNumCuenta                   char(20)
  pTipoCuenta                  char(2)
  pFechaInicio                 char(10)
  pFechaFin                    char(10)
  pIp                          char(15)
  pMacAddress                  char(12)
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pTipoOperacion` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pNumCuenta` | `char(20)` | — | — |
| `pTipoCuenta` | `char(2)` | — | — |
| `pFechaInicio` | `char(10)` | — | — |
| `pFechaFin` | `char(10)` | — | — |
| `pIp` | `char(15)` | — | — |
| `pMacAddress` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `int` | L5 |
| `cCodRet` | `char(5)` | L6 |
| `iRegistros` | `int` | L7 |
| `cDiaCorte` | `char(2)` | L8 |
| `pFechaInicio1` | `date` | L9 |
| `pFechaFin1` | `date` | L10 |
| `iAnioMesApertura` | `INTEGER` | L11 |
| `iAnioMesInicio` | `INTEGER` | L12 |
| `iExiste` | `SMALLINT` | L13 |
| `cProducto` | `CHAR(4)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maenoc` | `bdicheq` | ⚠️ sí | SELECT | L69 |
| `sw_ro_ctecta` | `bdicnweb` | no | SELECT | L82 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L92 |
| `sd_maecredcrd` | `bdicred` | ⚠️ sí | SELECT | L102 |
| `sd_encabezado_edocta` | `bdicred` | ⚠️ sí | SELECT | L144 |
| `sd_encabezado_edoctacrd` | `bdicred` | ⚠️ sí | SELECT | L172 |
| `sc_maehis_factelect` | `bdicheq` | ⚠️ sí | SELECT | L203 |
| `sw_ro_edocta` | `bdicnweb` | no | INSERT | L237 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_sw_ro_evalua_fecha` | `bdicnweb` | no | L234 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `let cCodRet = '00005';` |  |
| L59 | CÓDIGO_RETORNO | `let cCodRet = '00048';` |  |
| L233 | FÓRMULA | `let pFechaFin = SUBSTR(pFechaFin,6,2)\|\|'-'\|\|SUBSTR(pFechaFin,9,2)\|\|'-'\|\|SUBSTR(pFechaFin,1,4` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `param` | ENTIDAD | parámetro | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edocta` | ENTIDAD | Estado de Cuenta — documento periódico de movimientos y sald | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_indinfocliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_indinfocliente.sql` |
| **LOC (1er CREATE)** | 39 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo, información y cliente" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_indinfocliente(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pIndRfc                      char(1)
  pIndEmpleo                   char(1)
  pIndDomicilio                char(1)
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | `cliente`=cliente | 🔵 CONVENCIÓN |
| `pIndRfc` | `char(1)` | — | — |
| `pIndEmpleo` | `char(1)` | — | — |
| `pIndDomicilio` | `char(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L5 |
| `iSqlErr` | `int` | L6 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L27 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?_ind` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `info` | ENTIDAD | información | 🔵 CONVENCIÓN | nombre_sp |
| `cliente` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `sw`, `ro`, `?_ind` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_listhomoni`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_listhomoni.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_listhomoni(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pIdOficio                    INT
  pRegistros                   INT
  pRecuperaciON                INT
) RETURNING CHAR(5) AS  CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pIdOficio` | `INT` | — | — |
| `pRegistros` | `INT` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperaciON` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cNombre` | `CHAR(164)` | L7 |
| `cCliente` | `CHAR(164)` | L8 |
| `cCuenta` | `CHAR(164)` | L9 |
| `cTarjeta` | `CHAR(164)` | L10 |
| `iBusq` | `INTEGER` | L11 |
| `iContador` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet  = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iContador= iContador + 1;` |  |
| L74 | CÓDIGO_RETORNO | `LET cCodRet='01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_listho` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mon` | PREFIJO | monitor / módulo | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_listho`, `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_listpernolocali`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_listpernolocali.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "sw — SoftWare/Switch y ro — Rol Operativo (tipo, local)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_listpernolocali(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pRegistros                   int
  pRecuperacion                int
) RETURNING CHAR(5) as  CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pRegistros` | `int` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INT` | L15 |
| `iContador` | `INTEGER` | L16 |
| `iTipoBusq` | `SMALLINT` | L17 |
| `cNombre` | `CHAR(164)` | L18 |
| `iBusq` | `INTEGER` | L19 |
| `crfc` | `CHAR(13)` | L20 |
| `cNumcte` | `CHAR(20)` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `cTarjeta` | `CHAR(20)` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | SELECT | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | CÓDIGO_RETORNO | `let cCodRet     = '00000';` |  |
| L59 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L73 | FÓRMULA | `let iContador= iContador + 1;` |  |
| L78 | CÓDIGO_RETORNO | `let cCodRet='01001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_lis` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?erno` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `local` | MODIF | local | 🔵 CONVENCIÓN | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_lis`, `?erno`, `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_ofiocioalta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_ofiocioalta.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ✅ fan_in=45 / fan_out=37 |
| **Deps concatenadas** | 61 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta sw — SoftWare/Switch, ro — Rol Operativo y oficio" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_ofiocioalta(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaRecepcion              CHAR(10)
  pFechaOficio                 CHAR(10)
  pIdTipoOficio                INT
  pOficio                      CHAR(60)
  pExpediente                  CHAR(60)
  pInstitucion1n               INT
  pInstitucion2n               INT
  pTipoOperacion               INT
  pIdOficio                    INT
  pIp                          CHAR(15)
  pMac                         CHAR(12)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaRecepcion` | `CHAR(10)` | — | — |
| `pFechaOficio` | `CHAR(10)` | `ofi`=oficio | 🟡 INFERIDO |
| `pIdTipoOficio` | `INT` | `ofi`=oficio | 🟡 INFERIDO |
| `pOficio` | `CHAR(60)` | `ofi`=oficio | 🟡 INFERIDO |
| `pExpediente` | `CHAR(60)` | — | — |
| `pInstitucion1n` | `INT` | — | — |
| `pInstitucion2n` | `INT` | — | — |
| `pTipoOperacion` | `INT` | — | — |
| `pIdOficio` | `INT` | `ofi`=oficio | 🟡 INFERIDO |
| `pIp` | `CHAR(15)` | — | — |
| `pMac` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iNumOficio` | `INT` | L8 |
| `iExiste` | `INT` | L9 |
| `pFechaRecepcion1` | `DATE` | L10 |
| `pFechaOficio1` | `DATE` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_maeoficios` | `bdicnweb` | no | INSERT | L57 |
| `sw_ro_maeoficios` | `bdicnweb` | no | SELECT | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L66 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `let cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ofi` | ENTIDAD | oficio | 🟡 INFERIDO | nombre_sp |
| `?ocio` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ocio` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_omitirpersona`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_omitirpersona.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "sw — SoftWare/Switch y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_omitirpersona(
  pUsuarioC                    CHAR(8)
  pFuncionC                    CHAR(10)
  pIdResultPer                 INT
  pOmitir                      CHAR(1)
  pOperacion                   INT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuarioC` | `CHAR(8)` | — | — |
| `pFuncionC` | `CHAR(10)` | — | — |
| `pIdResultPer` | `INT` | — | — |
| `pOmitir` | `CHAR(1)` | — | — |
| `pOperacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INT` | L3 |
| `cCodRet` | `CHAR(5)` | L4 |
| `cOmitir` | `CHAR(1)` | L5 |
| `iExiste` | `INT` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_resulper` | `bdicnweb` | no | UPDATE | L50 |
| `sw_ro_resulcte` | `bdicnweb` | no | SELECT | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L31 | CÓDIGO_RETORNO | `let cCodRet = '00044';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_omitirpersona` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_omitirpersona` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_ro_tarjetascte`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_ro_tarjetascte.sql` |
| **LOC (1er CREATE)** | 48 |
| **Callgraph** | ✅ fan_in=59 / fan_out=2 |
| **Propósito inferido** | "sw — SoftWare/Switch, ro — Rol Operativo, tarjetas y cliente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_cons_tarjetas_cte` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_ro_tarjetascte(
  pUsuario                     char(8)
  pIdFuncion                   char(10)
  pIdOficio                    int
  pIdBusqueda                  int
  pIdCliente                   int
  pNumCliente                  char(20)
  pRegistros                   int
  pRecuperacion                int
) RETURNING char(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(8)` | — | — |
| `pIdFuncion` | `char(10)` | — | — |
| `pIdOficio` | `int` | — | — |
| `pIdBusqueda` | `int` | — | — |
| `pIdCliente` | `int` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pRegistros` | `int` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO |
| `pRecuperacion` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L4 |
| `cCodRetSp` | `char(5)` | L5 |
| `cChequera` | `char(1)` | L6 |
| `cCveProducto` | `char(4)` | L7 |
| `cProducto` | `char(40)` | L8 |
| `cNumCuenta` | `char(20)` | L9 |
| `cNumTarjeta` | `char(20)` | L10 |
| `cStatusTarjeta` | `char(15)` | L11 |
| `dFecExpira` | `date` | L12 |
| `cTipoTarjeta` | `char(15)` | L13 |
| `cSistemaCuenta` | `char(2)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `v_sw_ro_tarjetasclientes` | `bdicnweb` | no | INSERT | L37 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_cons_tarjetas_cte` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L21 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `tarjetas` | ENTIDAD | tarjetas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_tipooficio_actualiza`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_tipooficio_actualiza.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza sw — SoftWare/Switch y oficio (tipo de)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_tipooficio_actualiza(
  pId_UsuarioC                 char(8)
  pId_FuncionC                 char(10)
  pDescripcionOficio           char(100)
  pStatus                      char(1)
  pTipoOperacion               int
  pIdOficio                    int
  pIp                          char(15)
  pMacAddress                  char(12)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pId_UsuarioC` | `char(8)` | — | — |
| `pId_FuncionC` | `char(10)` | — | — |
| `pDescripcionOficio` | `char(100)` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |
| `pStatus` | `char(1)` | — | — |
| `pTipoOperacion` | `int` | `tipo`=tipo de | 🔵 CONVENCIÓN |
| `pIdOficio` | `int` | `oficio`=oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO |
| `pIp` | `char(15)` | — | — |
| `pMacAddress` | `char(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `char(5)` | L6 |
| `iSqlErr` | `int` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ro_tipooficios` | `bdicnweb` | no | INSERT | L51 |
| `sw_ro_tipooficios` | `bdicnweb` | no | UPDATE | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `let cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `let cCodRet = '00087';` |  |
| L55 | CÓDIGO_RETORNO | `let cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `tipo` | MODIF | tipo de | 🔵 CONVENCIÓN | nombre_sp |
| `oficio` | ENTIDAD | oficio (requerimiento judicial/autoridad) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_sw_validacoordinador`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_validacoordinador.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 168 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida sw — SoftWare/Switch y ordenante" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_validacoordinador(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdEjecucion                 CHAR(1)
  pIdCoordinador               CHAR(10)
  pPassword                    CHAR(20)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdEjecucion` | `CHAR(1)` | — | — |
| `pIdCoordinador` | `CHAR(10)` | `ord`=ordenante / orden (SPEI) | 🔵 CONVENCIÓN |
| `pPassword` | `CHAR(20)` | `sw`=sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) · `ord`=ordenante / orden (SPEI) | 🔵 CONVENCIÓN / ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cDescCodRet` | `CHAR(80)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cIdCoordinador` | `CHAR(10)` | L10 |
| `cNombreCoordinador` | `CHAR(45)` | L11 |
| `iExiste` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_seg_valida_usuarios` | `bdicnweb` | no | SELECT | L51 |
| `si_seg_usuarios` | `bdinteg` | ⚠️ sí | SELECT | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | VALIDACIÓN_NULL | `IF iExiste = 0 OR iExiste IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?co` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `?inador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?co`, `?inador` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_sw_validacoordinadorgerente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_sw_validacoordinadorgerente.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=90 |
| **Deps concatenadas** | 287 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida sw — SoftWare/Switch y ordenante" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_sw_validacoordinadorgerente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdUsuarioValidar            CHAR(8)
) RETURNING CHAR(5) AS codRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdUsuarioValidar` | `CHAR(8)` | `valida`=valida | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INT` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `cUsuario` | `CHAR(8)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_seg_puesto_usuarios` | `bdicnweb` | no | SELECT | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `sw` | ENTIDAD | sw — SoftWare/Switch (subsistema sp_sw_ro_* — bdicnweb) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?co` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `?inadorgerente` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?co`, `?inadorgerente` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tc_consultalotepend`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tc_consultalotepend.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta tc — Tarjeta de Crédito y lote" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_conslotepend` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_tc_consultalotepend(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cSucursal` | `CHAR(4)` | L14 |
| `cTipoTarjeta` | `CHAR(1)` | L15 |
| `iNumEnvio` | `INTEGER` | L16 |
| `iRangoIni` | `INTEGER` | L17 |
| `iRangoFin` | `INTEGER` | L18 |
| `cStatus` | `CHAR(1)` | L19 |
| `dFechaSurtido` | `DATE` | L20 |
| `cFecha` | `CHAR(10)` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_conslotepend` | `bditarjcop` | ⚠️ sí | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '01276';` |  |
| L69 | FÓRMULA | `LET cFecha = LPAD(DAY(dFechaSurtido),2,0)\|\|'/'\|\|LPAD(MONTH(dFechaSurtido),2,0)\|\|'/'\|\|YEAR(dF` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `tc` | ENTIDAD | tc — Tarjeta de Crédito (abreviación en solicitudes y crédit | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `lote` | ENTIDAD | lote (proceso batch) | 🔵 CONVENCIÓN | nombre_sp |
| `?pend` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pend` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tf_cattransacciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tf_cattransacciones.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 27 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "catálogo y transacción" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_transfer_transacciones` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_tf_cattransacciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdTransaccion               CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdTransaccion` | `CHAR(1)` | `transaccion`=transacción | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `cDescCodRetSp` | `CHAR(150)` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `cTransaccion` | `CHAR(5)` | L12 |
| `cDescripcion` | `CHAR(50)` | L13 |
| `cNaturaleza` | `CHAR(1)` | L14 |
| `iNumRegistros` | `INTEGER` | L15 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |
| `sp_transfer_transacciones` | `bditransfer` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditransfer:sp_transfer_transac` |  |
| L62 | FÓRMULA | `LET iNumRegistros = iNumRegistros + 1;` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tf_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `transaccion` | ENTIDAD | transacción | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tf_`, `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tf_conciliacionadmin_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tf_conciliacionadmin_totales.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=20 |
| **Deps concatenadas** | 26 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "conciliación Administrador — rol de usuario con privilegios administrativos (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_transfer_mconadmin_success2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_tf_conciliacionadmin_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pTransaccion                 CHAR(5)
  pNaturaleza                  CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pTransaccion` | `CHAR(5)` | — | — |
| `pNaturaleza` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cDescCodRetSp` | `CHAR(150)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iNumRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_transfer_mconadmin_success2_totales` | `bditransfer` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditransfer:sp_transfer_mconadm` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tf_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `conciliacion` | ACCION | conciliación | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tf_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tf_transaccionesexitosas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tf_transaccionesexitosas.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 25 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "transacción y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_transfer_msuccess2` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=4 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_tf_transaccionesexitosas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pRegistros` | `INTEGER` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `cDescCodRetSp` | `CHAR(150)` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `cNombreArchivo` | `CHAR(50)` | L14 |
| `cAplicacion` | `CHAR(1)` | L15 |
| `cCantidad` | `INTEGER` | L16 |
| `mMonto` | `MONEY(16,2)` | L17 |
| `iRecuperacion` | `INTEGER` | L18 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L54 |
| `sp_transfer_msuccess2` | `bditransfer` | ⚠️ sí | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L67 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditransfer:sp_transfer_msucces` |  |
| L73 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tf_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `?ese` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?as` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tf_`, `?ese`, `?it`, `?as` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tf_transaccionesexitosas_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tf_transaccionesexitosas_totales.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=18 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "transacción y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_transfer_msuccess2_totales` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_tf_transaccionesexitosas_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `cDescCodRetSp` | `CHAR(150)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iNumRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_transfer_msuccess2_totales` | `bditransfer` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bditransfer:sp_transfer_msucces` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tf_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `transaccion` | ENTIDAD | transacción | 🔵 CONVENCIÓN | nombre_sp |
| `?ese` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?it` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?as_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tf_`, `?ese`, `?it`, `?as_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tf_transfermsettlement`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tf_transfermsettlement.sql` |
| **LOC (1er CREATE)** | 175 |
| **Callgraph** | ✅ fan_in=0 / fan_out=66 |
| **Deps concatenadas** | 174 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "transferencia" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_transfer_msettlement2` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_tf_transfermsettlement(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoEjecucion               CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pBancOrigen                  CHAR(3)
  pBancDestino                 CHAR(3)
  pRutaDescarga                CHAR(150)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pBancOrigen` | `CHAR(3)` | — | — |
| `pBancDestino` | `CHAR(3)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cCodRetSp` | `CHAR(5)` | L12 |
| `cMensajeCodRetSp` | `CHAR(150)` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `cNombreArchivo` | `CHAR(50)` | L15 |
| `cBancoOrigen` | `CHAR(3)` | L16 |
| `cBancoDestino` | `CHAR(3)` | L17 |
| `mMonto` | `MONEY(16,2)` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |
| `cCmd1` | `CHAR(3000)` | L21 |
| `cArchDescarga` | `CHAR(150)` | L22 |
| `cArchivoTxt` | `CHAR(150)` | L23 |
| `bInTransaction` | `BOOLEAN` | L24 |
| `cSql` | `CHAR(2500)` | L25 |
| `cDelFile` | `CHAR(200)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tmp_settlement` | `bdicnweb` | no | INSERT | L135 |
| `systables` | `bdicnweb` | no | SELECT | L140 |
| `tmp_settlement` | `bdicnweb` | no | SELECT | L140 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L78 |
| `sp_transfer_msettlement2` | `bditransfer` | ⚠️ sí | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L66 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL OR pRegistros IS NU` |  |
| L67 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L93 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditransfer:sp_transfer_msettl` |  |
| L99 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L104 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tf_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?msettlement` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tf_`, `?msettlement` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tf_transfermsettlement_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tf_transfermsettlement_totales.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "transferencia (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_transfer_msettlement2_totales` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_tf_transfermsettlement_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoEjecucion               CHAR(1)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pBancOrigen                  CHAR(3)
  pBancDestino                 CHAR(3)
  pRutaDescarga                CHAR(150)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoEjecucion` | `CHAR(1)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pBancOrigen` | `CHAR(3)` | — | — |
| `pBancDestino` | `CHAR(3)` | — | — |
| `pRutaDescarga` | `CHAR(150)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |
| `sp_transfer_msettlement2_totales` | `bditransfer` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL  THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditransfer:sp_transfer_msettl` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tf_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?msettlement_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tf_`, `?msettlement_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tk_consultaremesadoras`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tk_consultaremesadoras.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=114 / fan_out=8 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar Mesa de Control — equipo de revisión y autorización de solicitudes de crédito; status codes MC/CM; valida comprobantes de ingreso; comentario explícito en código" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_tk_consultaremesadoras(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cNumconvenio` | `CHAR(3)` | L12 |
| `cNomconvenio` | `CHAR(40)` | L13 |
| `cNumcategoria` | `CHAR(2)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario IS NULL OR pIdFuncion IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tk_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mesa` | ENTIDAD | Mesa de Control — equipo de revisión y autorización de solic | 🔵 CONVENCIÓN | nombre_sp |
| `?doras` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tk_`, `?e`, `?doras` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalctasmontobeneficencia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalctasmontobeneficencia.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ✅ fan_in=0 / fan_out=48 |
| **Deps concatenadas** | 90 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cuentas, monto y beneficiario (total)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalctasmontobeneficencia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumDiasSMVDF                SMALLINT
) RETURNING CHAR(5)        AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumDiasSMVDF` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRet2` | `CHAR(5)` | L9 |
| `cCodRet3` | `CHAR(50)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iSamErr` | `INTEGER` | L12 |
| `cDesErr` | `CHAR(50)` | L13 |
| `dValorSM` | `DECIMAL(14,2)` | L14 |
| `iTotalCuentas` | `INTEGER` | L15 |
| `dTotalSaldo` | `DECIMAL(14,2)` | L16 |
| `iNoAnios` | `SMALLINT` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L62 |
| `sc_cuentas_concentradas` | `bdicheq` | ⚠️ sí | SELECT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumDiasSMVDF IS NULL THEN` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00429';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `benef` | ENTIDAD | beneficiario | 🔵 CONVENCIÓN | nombre_sp |
| `?icencia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?icencia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesarchivodotacaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesarchivodotacaja.sql` |
| **LOC (1er CREATE)** | 222 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 91 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "archivo y caja (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesarchivodotacaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
  pIdSucursal                  CHAR(4)
  pIdProvCaja                  CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cTrans1` | `CHAR(30)` | L10 |
| `cTrans2` | `CHAR(30)` | L11 |
| `cTrans3` | `CHAR(30)` | L12 |
| `cFolioOperacion` | `CHAR(8)` | L13 |
| `dFechaOperacion` | `DATE` | L14 |
| `dFechaSolicitud` | `DATE` | L15 |
| `dFechaEnvio` | `DATE` | L16 |
| `cIdSucursal` | `CHAR(4)` | L17 |
| `cDescSucursal` | `CHAR(40)` | L18 |
| `mMonto` | `MONEY(14,2)` | L19 |
| `cCajaGeneral` | `CHAR(30)` | L20 |
| `status` | `CHAR(1)` | L21 |
| `iTotalRegistros` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusdotacioncaja` | `bdicnweb` | no | SELECT | L58 |
| `sw_verificastatusdotacioncaja` | `bdicnweb` | no | DELETE | L58 |
| `sw_verificastatusdotacioncaja` | `bdicnweb` | no | INSERT | L59 |
| `bdicnweb` | `bdicnweb` | no | DELETE | L90 |
| `bdicnweb` | `bdicnweb` | no | INSERT | L115 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L197 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `?dota` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?dota` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesarchivosprocesarafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesarchivosprocesarafore.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "procesa archivos y AFORE (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_aforebuscararchivosprocesar2_totales` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesarchivosprocesarafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoBusqueda                CHAR(1)
  pTipoArchivo                 CHAR(1)
  pNombreArchivo               CHAR(30)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pEstatus                     CHAR(2)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoBusqueda` | `CHAR(1)` | — | — |
| `pTipoArchivo` | `CHAR(1)` | — | — |
| `pNombreArchivo` | `CHAR(30)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pEstatus` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cEmpresa` | `CHAR(3)` | L11 |
| `iTotalRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L37 |
| `sp_aforebuscararchivosprocesar2_totales` | `bdiprog` | ⚠️ sí | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_aforebuscararchivos` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `archivos` | ENTIDAD | archivos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `procesa` | ACCION | procesa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesarqueosucucaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesarqueosucucaja.sql` |
| **LOC (1er CREATE)** | 286 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "OS — Originación de Solicitudes y caja (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesarqueosucucaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdPlaza                     CHAR(3)
  pIdSucursal                  CHAR(4)
  pFechaInicial                DATE
  pFechaFinal                  DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdPlaza` | `CHAR(3)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `iTotalRegistros` | `INTEGER` | L13 |
| `cSucursal` | `CHAR(4)` | L14 |
| `cNombreSuc` | `CHAR(40)` | L15 |
| `cDescPlazaCajaGeneralArq` | `CHAR(40)` | L16 |
| `fCantidad_1` | `FLOAT` | L17 |
| `fCantidad_2` | `FLOAT` | L18 |
| `fCantidad_3` | `FLOAT` | L19 |
| `fCantidad_4` | `FLOAT` | L20 |
| `fCantidad_5` | `FLOAT` | L21 |
| `fCantidad_6` | `FLOAT` | L22 |
| `fCantidad_7` | `FLOAT` | L23 |
| `mSaldoTotalArq` | `MONEY(14,2)` | L24 |
| `dFechaArq` | `DATE` | L25 |
| `cIdCajeroPrincArq` | `CHAR(8)` | L26 |
| `iTotSucursales` | `INTEGER` | L27 |
| `iSucAbrieron` | `INTEGER` | L28 |
| `iSucNoAbrio` | `INTEGER` | L29 |
| `iSucCerraron` | `INTEGER` | L30 |
| `iSucPenCerrar` | `INTEGER` | L31 |
| `mSaldoTotal` | `MONEY(14,2)` | L32 |
| `mTotalDotaciones` | `MONEY(14,2)` | L33 |
| `cIdDivisaArq` | `CHAR(2)` | L34 |
| `cDescDivisaArq` | `CHAR(30)` | L35 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusarqueosucaja` | `bdicnweb` | no | SELECT | L97 |
| `sw_verificastatusarqueosucaja` | `bdicnweb` | no | DELETE | L97 |
| `sw_verificastatusarqueosucaja` | `bdicnweb` | no | INSERT | L98 |
| `sw_cg_arqueosucajatmp` | `bdicnweb` | no | SELECT | L102 |
| `sw_cg_arqueosucajatmp` | `bdicnweb` | no | DELETE | L102 |
| `ss_saldossuc` | `bdisuc` | ⚠️ sí | SELECT | L137 |
| `informix` | `bdicnweb` | no | INSERT | L155 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L78 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdPlaza = '' OR pFechaInicial IS NULL OR pFechaFinal IS NULL` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L114 | FÓRMULA | `LET iSucNoAbrio = iTotSucursales - iSucAbrieron;` |  |
| L115 | FÓRMULA | `LET iSucPenCerrar = iSucAbrieron - iSucCerraron;` |  |
| L129 | FÓRMULA | `LET mTotalDotaciones = mTotalDotaciones + mSaldoTotal;` |  |
| L258 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?arque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ucu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?arque`, `?ucu` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesbloqueocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesbloqueocap.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloquea cuenta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesbloqueocap(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L58 |
| `sw_tr_cargamasiva_bloqueocap` | `bdicnweb` | no | SELECT | L65 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L106 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `let cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bloqueo` | ACCION | bloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_totalesbloqueocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesbloqueocre.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=9 / fan_out=7 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "bloquea cuenta crédito (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 5 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesbloqueocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L62 |
| `sw_tr_cargamasiva_bloqueocre` | `bdicnweb` | no | SELECT | L71 |
| `sw_tr_cargamasiva_bloqueocre_hist` | `bdicnweb` | no | SELECT | L75 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L116 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `let cCodRet = '00200';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bloqueo` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_totalescancelacioncre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalescancelacioncre.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela crédito (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalescancelacioncre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L61 |
| `sw_tr_cargamasiva_cancelacioncre` | `bdicnweb` | no | SELECT | L68 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L116 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L72 | CÓDIGO_RETORNO | `let cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cancelacion` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_totalescargocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalescargocre.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cargo y crédito (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalescargocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L58 |
| `sw_tr_cargamasiva_cargo` | `bdicnweb` | no | SELECT | L65 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L106 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `let cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_totalesconsultacod41`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesconsultacod41.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta código (totales)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesconsultacod41(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(18)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(18)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ccep_procesacod41detalle_tmp` | `bdicnweb` | no | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?41` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?41` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesconsultarchivosafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesconsultarchivosafore.sql` |
| **LOC (1er CREATE)** | 179 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar CHI — formato/protocolo de consulta al Buró de Crédito, OS — Originación de Solicitudes y AFORE (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesconsultarchivosafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(30)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `pTipoArchivo` | `CHAR(1)` | L11 |
| `iTotalRegistros` | `INTEGER` | L12 |
| `cTipoRegistro` | `CHAR(1)` | L14 |
| `cNoContratoEmpresaTmp` | `CHAR(10)` | L15 |
| `dFechaGenTmp` | `DATE` | L16 |
| `dFechaInicialInformacionTmp` | `DATE` | L17 |
| `dFechaFinalInformacionTmp` | `DATE` | L18 |
| `cNoMovimientosContenidosTmp` | `CHAR(9)` | L19 |
| `cFiller` | `CHAR(232)` | L20 |
| `cFinLinea` | `CHAR(2)` | L21 |
| `cNSS` | `CHAR(11)` | L22 |
| `cNombreBeneficiario` | `CHAR(40)` | L23 |
| `cApellidoPaternoBeneficiario` | `CHAR(40)` | L24 |
| `cApellidoMaternoBeneficiario` | `CHAR(40)` | L25 |
| `cFormasPago` | `CHAR(1)` | L26 |
| `cCLABE` | `CHAR(18)` | L27 |
| `dFechaCaptura` | `DATE` | L28 |
| `cImporteDocumentoNetoPagar` | `CHAR(15)` | L29 |
| `cImporteDocumentoAntesImpuesto` | `CHAR(15)` | L30 |
| `cImpuestoRetenido` | `CHAR(11)` | L31 |
| *…19 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `15` | `bdicnweb` | no | SELECT | L124 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L118 |
| `sp_aforeconsultaarchivos` | `bdiprog` | ⚠️ sí | L134 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L113 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L144 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_aforeconsultaarchiv` |  |
| L146 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L151 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L156 | FÓRMULA | `LET iTotalRegistros = iNoRegistros - 2;` |  |
| L159 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?v` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?v` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesdepositocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesdepositocap.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=3 / fan_out=1 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "depósito, OS — Originación de Solicitudes y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesdepositocap(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L58 |
| `sw_tr_cargamasiva_deposito` | `bdicnweb` | no | SELECT | L65 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L106 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `let cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `dep` | ENTIDAD | depósito | 🟡 INFERIDO | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ito` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ito` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesdesbloqueocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesdesbloqueocap.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloquea cuenta Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesdesbloqueocap(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L58 |
| `sw_tr_cargamasiva_desbloqueocap` | `bdicnweb` | no | SELECT | L65 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L106 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `let cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `desbloqueo` | ACCION | desbloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_totalesdesbloqueocre`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesdesbloqueocre.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=11 / fan_out=7 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "desbloquea cuenta crédito (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 5 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesdesbloqueocre(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L62 |
| `sw_tr_cargamasiva_desbloqueocre` | `bdicnweb` | no | SELECT | L71 |
| `sw_tr_cargamasiva_desbloqueocre_hist` | `bdicnweb` | no | SELECT | L75 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L116 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `let cCodRet = '00200';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `desbloqueo` | ACCION | desbloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |

---

## `sp_totalesdotacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesdotacionescaja.sql` |
| **LOC (1er CREATE)** | 305 |
| **Callgraph** | ✅ fan_in=0 / fan_out=49 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "dotaciones de efectivo y caja (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesdotacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
  pIdProvCaja                  CHAR(4)
  pIdSucursal                  CHAR(4)
  pFechaInic                   DATE
  pFechaFin                    DATE
  pIdLote                      INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | — | — |
| `pIdProvCaja` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdLote` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cSucursal` | `CHAR(50)` | L9 |
| `cCajaGen` | `CHAR(50)` | L10 |
| `dFecha` | `DATE` | L11 |
| `cStatus` | `CHAR(50)` | L12 |
| `cFolioOperacion` | `CHAR(8)` | L13 |
| `dMonto` | `DECIMAL(14,2)` | L14 |
| `cUsuario` | `CHAR(50)` | L15 |
| `cIdUsuario` | `CHAR(8)` | L16 |
| `cIdStatus` | `CHAR(2)` | L17 |
| `cCodTrans` | `CHAR(4)` | L18 |
| `iIdRegistro` | `INTEGER` | L19 |
| `bEnTransaccion` | `BOOLEAN` | L21 |
| `iContador` | `INTEGER` | L22 |
| `iMaxCommit` | `INTEGER` | L23 |
| `iNoRegistros` | `INTEGER` | L24 |
| `vPSuc` | `CHAR(4)` | L26 |
| `vSucursal` | `CHAR(4)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_cg_dotaciones` | `bdicnweb` | no | INSERT | L77 |
| `sw_cg_enviodotaciones` | `bdicnweb` | no | SELECT | L100 |
| `ss_operaciones` | `bdisuc` | ⚠️ sí | SELECT | L144 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L148 |
| `ss_proveedores` | `bdisuc` | ⚠️ sí | SELECT | L160 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L162 |
| `ss_catstatus` | `bdisuc` | ⚠️ sí | SELECT | L164 |
| `sw_cg_enviodotaciones` | `bdicnweb` | no | INSERT | L170 |
| `statistics` | `bdicnweb` | no | UPDATE | L279 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L104 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L169 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L175 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L210 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L216 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L255 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L261 | FÓRMULA | `LET iContador = iContador + 1;` |  |
| L282 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_totalesentradasalidacaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesentradasalidacaja.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ✅ fan_in=0 / fan_out=17 |
| **Deps concatenadas** | 52 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "salida y caja (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_entrada_salida` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesentradasalidacaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(1)
  pIdSucursal                  CHAR(4)
  pIdPlaza                     CHAR(3)
  pFechaInic                   DATE
  pFechaFin                    DATE
  pMes                         CHAR(2)
  pAnio                        CHAR(4)
  pIdStatus                    CHAR(2)
  pMac                         CHAR(18)
  pIp                          VARCHAR(16)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(1)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pIdPlaza` | `CHAR(3)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pMes` | `CHAR(2)` | — | — |
| `pAnio` | `CHAR(4)` | — | — |
| `pIdStatus` | `CHAR(2)` | — | — |
| `pMac` | `CHAR(18)` | — | — |
| `pIp` | `VARCHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cCodRetSp` | `CHAR(5)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iTotalRegistros_S` | `INTEGER` | L11 |
| `iTotalRegistros_C` | `INTEGER` | L12 |
| `iTotalRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusentradasalida` | `bdicnweb` | no | SELECT | L39 |
| `sw_verificastatusentradasalida` | `bdicnweb` | no | INSERT | L42 |
| `tmp_entradasalida` | `bdicnweb` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L56 |
| `sp_entrada_salida` | `bdisuc` | ⚠️ sí | L67 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L71 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_entrada_salida';` |  |
| L87 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_entrada_salida';` |  |
| L96 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisuc:sp_entrada_salida';` |  |
| L100 | FÓRMULA | `LET iTotalRegistros = (iTotalRegistros_S + iTotalRegistros_C);` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?entrada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `salida` | ENTIDAD | salida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?entrada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesfacturacionos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesfacturacionos.sql` |
| **LOC (1er CREATE)** | 119 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 84 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "facturación y OS — Originación de Solicitudes (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesfacturacionos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
  pNumCte                      CHAR(9)
  pFechaInicio                 CHAR(10)
  pFechaFin                    CHAR(10)
  pTipoFecha                   SMALLINT
  pTipoConsulta                SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pFechaInicio` | `CHAR(10)` | — | — |
| `pFechaFin` | `CHAR(10)` | — | — |
| `pTipoFecha` | `SMALLINT` | — | — |
| `pTipoConsulta` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `cCodRetSp` | `CHAR(6)` | L24 |
| `cDesCodRetSp` | `CHAR(50)` | L25 |
| `cEmpresa` | `CHAR(3)` | L26 |
| `cSucursal` | `CHAR (4)` | L27 |
| `iTotalEnviadas` | `INTEGER` | L28 |
| `iImpresas` | `INTEGER` | L29 |
| `dImpresasPor` | `DECIMAL(10,2)` | L30 |
| `iNoImpresas` | `INTEGER` | L31 |
| `dNoImpresasPor` | `DECIMAL(10,2)` | L32 |
| `iStatusA` | `INTEGER` | L33 |
| `dStatusAPor` | `DECIMAL(10,2)` | L34 |
| `iStatusR` | `INTEGER` | L35 |
| `dStatusRPor` | `DECIMAL(10,2)` | L36 |
| `iStatusD` | `INTEGER` | L37 |
| `dStatusDPor` | `DECIMAL(10,2)` | L38 |
| `iStatusS` | `INTEGER` | L39 |
| `dStatusSPor` | `DECIMAL(10,2)` | L40 |
| `iBancoppel` | `INTEGER` | L41 |
| `iCoppel` | `INTEGER` | L42 |
| `iMixta` | `INTEGER` | L43 |
| `iTotal` | `INTEGER` | L44 |
| `iRecuperacion` | `INTEGER` | L45 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_facturacion_os` | `bdicnweb` | no | SELECT | L104 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L90 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | CÓDIGO_RETORNO | `LET cCodRet 		  = '00000';` |  |
| L83 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio = '' OR pFechaFin = '' OR pTipoFecha IS NULL OR ` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `facturacion` | ENTIDAD | facturación | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_totaleslineascredito`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totaleslineascredito.sql` |
| **LOC (1er CREATE)** | 142 |
| **Callgraph** | ✅ fan_in=31 / fan_out=9 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "línea y crédito (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 5 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_totaleslineascredito(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L62 |
| `sw_tr_cargamasiva_mantolineascredito` | `bdicnweb` | no | SELECT | L71 |
| `sw_tr_cargamasiva_mantolineascredito_hist` | `bdicnweb` | no | SELECT | L75 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L116 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L79 | CÓDIGO_RETORNO | `let cCodRet = '00200';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `linea` | ENTIDAD | línea (de crédito) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `credito` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesmonitorefectivocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesmonitorefectivocaja.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "monitor, efectivo y caja (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdisuc` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesmonitorefectivocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodProveedor                CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodProveedor` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `iTotalRegistros` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L37 |
| `bdisuc` | `bdicnweb` | no | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:consultacajageneral2_to` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `efectivo` | ENTIDAD | efectivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_totalesmonitoroperacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesmonitoroperacionescaja.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "monitor, operaciones y caja (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdisuc` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesmonitoroperacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSucursal                CHAR(2)
  pIdSucursal                  CHAR(4)
  pIdMostrar                   CHAR(4)
  pFechaInic                   DATE
  pFechaFin                    DATE
  pIdProvCaja                  CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSucursal` | `CHAR(2)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pIdMostrar` | `CHAR(4)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdProvCaja` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L34 |
| `bdisuc` | `bdicnweb` | no | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pTipoSucursal = '' OR pFechaInic IS NULL OR pFechaFin IS NULL` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L44 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_monitor_operaciones2` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_totalesmovpendientesccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesmovpendientesccl.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "movimiento y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bditarjeta` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesmovpendientesccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |
| `iContador` | `INTEGER` | L11 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L37 |
| `bditarjeta` | `bdicnweb` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha IS NULL THEN` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_concreing_consul` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?pendientes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientes` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesoperacionescaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesoperacionescaja.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "operaciones y caja (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdisuc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesoperacionescaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNoFolio                     CHAR(8)
  pIdSucursal                  CHAR(4)
  pCodTransaccion              CHAR(4)
  pTipoConsulta                CHAR(1)
  pIdCajaGen                   CHAR(4)
  pTipoSucursal                CHAR(1)
  pFechaInic                   DATE
  pFechaFin                    DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNoFolio` | `CHAR(8)` | — | — |
| `pIdSucursal` | `CHAR(4)` | — | — |
| `pCodTransaccion` | `CHAR(4)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | — | — |
| `pIdCajaGen` | `CHAR(4)` | `caja`=caja / ventanilla | ✅ CÓDIGO |
| `pTipoSucursal` | `CHAR(1)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `iTotalRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L39 |
| `bdisuc` | `bdicnweb` | no | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L56 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_consul_operaciones2_` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_totalesretirocap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesretirocap.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ✅ fan_in=38 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "retiro y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (totales)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesretirocap(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pLote                        INT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pLote` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INT` | L12 |
| `dFechaCarga` | `DATETIME YEAR TO FRACTION(3)` | L13 |
| `iTotalRegistros` | `INT` | L14 |
| `mTotalMonto` | `money(14,2)` | L15 |
| `iTotalRegistrosAceptados` | `INT` | L16 |
| `iTotalRegistrosRechazados` | `INT` | L17 |
| `iExiste` | `int` | L18 |
| `cNombreEjecutivo` | `char(45)` | L19 |
| `cNombreArchivoCarga` | `char(150)` | L20 |
| `cSistemaCuenta` | `char(2)` | L21 |
| `cStatusLote` | `CHAR(1)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_totales_masivo` | `bdicnweb` | no | SELECT | L58 |
| `sw_tr_cargamasiva_retiro` | `bdicnweb` | no | SELECT | L65 |
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L106 |
| `sw_tr_totales_masivo` | `bdicnweb` | no | INSERT | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L69 | CÓDIGO_RETORNO | `let cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `retiro` | ENTIDAD | retiro | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_totalesucabrieroncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesucabrieroncaja.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ro — Rol Operativo y caja (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesucabrieroncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iTotalRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta = '' OR pFechaConsulta IS NULL THEN` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `?ucabrie` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?n` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ucabrie`, `ro`, `?n` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalesucnoabrieroncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalesucnoabrieroncaja.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "ro — Rol Operativo y caja (totales)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `bdinteg` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalesucnoabrieroncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaConsulta               DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaConsulta` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `dFechaAnterior` | `DATE` | L10 |
| `sSucursal` | `CHAR(4)` | L11 |
| `iContPase` | `INTEGER` | L12 |
| `iTotalRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `ss_pase_sucursal` | `bdisuc` | ⚠️ sí | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaConsulta = '' OR pFechaConsulta IS NULL THEN` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | FÓRMULA | `LET dFechaAnterior = DATE(pFechaConsulta) -1;` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |
| `?ucnoabrie` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?n` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ucnoabrie`, `ro`, `?n` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_totalsdohistoricocaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_totalsdohistoricocaja.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "saldo y caja (total, histórico)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `bdinteg`, `bdisuc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_totalsdohistoricocaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoSaldo                   CHAR(1)
  pCcMayor                     CHAR(4)
  pCcSub                       CHAR(2)
  pCcsubsub                    CHAR(2)
  pCcssubsub                   CHAR(2)
  pCcsssubsub                  CHAR(2)
  pSector                      CHAR(2)
  pFechaMes                    CHAR(2)
  pFechaAnio                   CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoSaldo` | `CHAR(1)` | — | — |
| `pCcMayor` | `CHAR(4)` | — | — |
| `pCcSub` | `CHAR(2)` | — | — |
| `pCcsubsub` | `CHAR(2)` | — | — |
| `pCcssubsub` | `CHAR(2)` | — | — |
| `pCcsssubsub` | `CHAR(2)` | — | — |
| `pSector` | `CHAR(2)` | — | — |
| `pFechaMes` | `CHAR(2)` | — | — |
| `pFechaAnio` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cEmpresa` | `CHAR(3)` | L13 |
| `sTipo` | `SMALLINT` | L14 |
| `dFechaMinSel` | `DATE` | L15 |
| `dFechaMaxSel` | `DATE` | L16 |
| `dFechaMaxima` | `DATE` | L17 |
| `iTotalRegistros` | `INTEGER` | L18 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L48 |
| `bdisuc` | `bdicnweb` | no | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | FÓRMULA | `LET dFechaMinSel = TO_DATE(1\|\|'/'\|\|(pFechaMes::INTEGER)\|\|'/'\|\|(pFechaAnio::INTEGER),'%d/%m/%` |  |
| L62 | FÓRMULA | `LET dFechaMaxima = dFechaMinSel + 1 UNITS MONTH;` |  |
| L63 | FÓRMULA | `LET dFechaMaxSel = dFechaMaxima - 1 UNITS DAY;` |  |
| L70 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdisuc:sp_sel_sdohistorico2_to` |  |
| L76 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `total` | MODIF | total | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `historico` | MODIF | histórico | 🔵 CONVENCIÓN | nombre_sp |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_tpoaire_transfer`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tpoaire_transfer.sql` |
| **LOC (1er CREATE)** | 128 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "transferencia (tipo)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 5 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_tpoaire_transfer(
) RETURNING CHAR(5)     AS Cod_Retorno,
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cMsjRetorno` | `CHAR(100)` | L7 |
| `iSql_err` | `INT` | L8 |
| `iRegistros` | `INT` | L9 |
| `dFecha` | `DATE` | L10 |
| `iConsecutivo` | `INT` | L11 |
| `mMonto` | `MONEY(6)` | L12 |
| `cTelefono` | `CHAR(12)` | L13 |
| `cTransaccion` | `CHAR(4)` | L14 |
| `cFolioSuc` | `CHAR(16)` | L15 |
| `cIntegridad` | `CHAR(1)` | L16 |
| `cAplicacion` | `CHAR(1)` | L17 |
| `cCuenta` | `CHAR(20)` | L18 |
| `cReferencia2` | `CHAR(40)` | L19 |
| `cStatus` | `CHAR(1)` | L20 |
| `mImpComisionConvenio` | `MONEY(16,2)` | L21 |
| `mIVAComision` | `MONEY(16,2)` | L22 |
| `mIVAComisionConv` | `MONEY(16,2)` | L23 |
| `mImpComisionCte` | `MONEY(16,2)` | L24 |
| `mComisionCte` | `MONEY(16,2)` | L25 |
| `mIVAComisionCte` | `MONEY(16,2)` | L26 |
| `dFechaAyer` | `DATE` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_convenios` | `bdisac` | ⚠️ sí | SELECT | L69 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L83 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L89 |
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L108 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | INSERT | L116 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L111 | FÓRMULA | `LET mIVAComisionConv = mImpComisionConvenio * (mIVAComision/100);` |  |
| L112 | FÓRMULA | `LET mComisionCte = mMonto * (mImpComisionCte/100);` | 🔴 MONEY/aritmética financiera |
| L113 | FÓRMULA | `LET mIVAComisionCte = mComisionCte * (mIVAComision/100);` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?oaire_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?oaire_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tr_actualizastatuslotemasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tr_actualizastatuslotemasivo.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza estatus y lote (masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_tr_actualizastatuslotemasivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pStatus                      CHAR(2)
  pIdLote                      INTEGER
  pUsarHistorico               CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pIdLote` | `INTEGER` | `lote`=lote (proceso batch) | ✅ CÓDIGO |
| `pUsarHistorico` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INT` | L7 |
| `iExitosos` | `INT` | L8 |
| `cBaseDatos` | `CHAR(50)` | L9 |
| `cTablaDst` | `CHAR(50)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_info_tablas` | `bdicnweb` | no | SELECT | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFunciON = '' OR pStatus = '' OR pIdLote IS NULL OR pUsarHistorico = '' THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L46 | VALIDACIÓN_NULL | `IF cBaseDatos IS NULL OR cBaseDatos = '' THEN` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `lote` | ENTIDAD | lote (proceso batch) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tr_actualizastatusmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tr_actualizastatusmasivo.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "actualiza estatus (masivo)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_tr_actualizastatusmasivo(
  pUsuario                     CHAR(8)
  pIdFunciON                   CHAR(10)
  pStatus                      CHAR(2)
  pIdRegistros                 VARCHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFunciON` | `CHAR(10)` | — | — |
| `pStatus` | `CHAR(2)` | `status`=estatus | ✅ CÓDIGO |
| `pIdRegistros` | `VARCHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INT` | L8 |
| `iExitosos` | `INT` | L9 |
| `iFracasos` | `INT` | L10 |
| `iCount` | `INT` | L11 |
| `iStart` | `INT` | L12 |
| `iLength` | `INT` | L13 |
| `iRow` | `INT` | L14 |
| `cBaseDatos` | `CHAR(50)` | L15 |
| `cTablaDst` | `CHAR(50)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_info_tablas` | `bdicnweb` | no | SELECT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L56 | VALIDACIÓN_NULL | `IF cBaseDatos IS NULL OR cBaseDatos = '' THEN` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00154';` |  |
| L63 | FÓRMULA | `LET iRow = SUBSTR(pIdRegistros, iStart, iCount - iStart);` |  |
| L68 | FÓRMULA | `LET iExitosos = iExitosos + 1;` |  |
| L70 | FÓRMULA | `LET iFracasos = iFracasos + 1;` |  |
| L72 | FÓRMULA | `LET iStart = iCount + 1;` |  |
| L81 | FÓRMULA | `LET iExitosos = iExitosos + 1;` |  |
| L83 | FÓRMULA | `LET iFracasos = iFracasos + 1;` |  |
| L85 | FÓRMULA | `LET iStart = iCount + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `masivo` | MODIF | masivo | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tr_consulta_catmotivos_pendiente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tr_consulta_catmotivos_pendiente.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 45 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta catálogo y motivo" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_tr_consulta_catmotivos_pendiente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iIdMotivoPendiente` | `INTEGER` | L10 |
| `cDescMotivoPendiente` | `CHAR(50)` | L11 |
| `cRequiereCaptura` | `CHAR(1)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_fusioncat_motivos_pendiente` | `bdicnweb` | no | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L56 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | 🔵 CONVENCIÓN | nombre_sp |
| `motivo` | ENTIDAD | motivo / causa | 🔵 CONVENCIÓN | nombre_sp |
| `?s_pendiente` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr_`, `?s_pendiente` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tr_genreportectesfusionados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tr_genreportectesfusionados.sql` |
| **LOC (1er CREATE)** | 264 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 113 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte clientes (fusionados)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_tr_genreportectesfusionados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pIdEmpleado                  CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdEmpleado` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L34 |
| `iSqlErr` | `INTEGER` | L35 |
| `cNumAnalista` | `CHAR(8)` | L36 |
| `cNombreAnalista` | `CHAR(45)` | L37 |
| `dFechaValidacion` | `DATETIME YEAR TO FRACTION(3)` | L38 |
| `cNumCte1` | `CHAR(20)` | L39 |
| `cNumCte2` | `CHAR(20)` | L40 |
| `cCteCorrecto` | `CHAR(1)` | L41 |
| `cIdCliente1` | `CHAR(1)` | L42 |
| `cIdCliente2` | `CHAR(1)` | L43 |
| `cNumCliente1` | `CHAR(20)` | L45 |
| `cPrimerNombreCte1` | `CHAR(26)` | L46 |
| `cSegundoNombreCte1` | `CHAR(26)` | L47 |
| `cApellPaternoCte1` | `CHAR(26)` | L48 |
| `cApellMaternoCte1` | `CHAR(26)` | L49 |
| `dFechaNacCte1` | `DATE` | L50 |
| `cGeneroCte1` | `CHAR(1)` | L51 |
| `cActaNacCte1` | `CHAR(2)` | L52 |
| `cCorrecto1` | `CHAR(2)` | L53 |
| `cNumCliente2` | `CHAR(20)` | L54 |
| `cPrimerNombreCte2` | `CHAR(26)` | L55 |
| `cSegundoNombreCte2` | `CHAR(26)` | L56 |
| `cApellPaternoCte2` | `CHAR(26)` | L57 |
| `cApellMaternoCte2` | `CHAR(26)` | L58 |
| `dFechaNacCte2` | `DATE` | L59 |
| *…14 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L164 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L142 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L127 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L136 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L222 | FÓRMULA | `LET iIdCliente = iIdCliente + 1;` |  |
| L232 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L242 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `fusionados` | MODIF | fusionados | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr_`, `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tr_genreportectesfusionados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tr_genreportectesfusionados_totales.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 44 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte clientes (fusionados, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_tr_genreportectesfusionados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pIdEmpleado                  CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdEmpleado` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `iNoRegistros` | `INTEGER` | L9 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `fusionados` | MODIF | fusionados | 🔵 CONVENCIÓN | nombre_sp |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr_`, `?orte` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tr_genreportectesnofusionados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tr_genreportectesnofusionados.sql` |
| **LOC (1er CREATE)** | 234 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte clientes (fusionados)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_tr_genreportectesnofusionados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pIdEmpleado                  CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdEmpleado` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L32 |
| `iSqlErr` | `INTEGER` | L33 |
| `cNumAnalista` | `CHAR(8)` | L34 |
| `cNombreAnalista` | `CHAR(45)` | L35 |
| `dFechaValidacion` | `DATETIME YEAR TO FRACTION(3)` | L36 |
| `cNumCte1` | `CHAR(20)` | L37 |
| `cNumCte2` | `CHAR(20)` | L38 |
| `cCteCorrecto` | `CHAR(1)` | L39 |
| `cFlagFusion` | `CHAR(1)` | L40 |
| `cIdCliente1` | `CHAR(1)` | L41 |
| `cIdCliente2` | `CHAR(1)` | L42 |
| `cIdRegistro` | `INTEGER` | L43 |
| `cNumCliente1` | `CHAR(20)` | L45 |
| `cPrimerNombreCte1` | `CHAR(26)` | L46 |
| `cSegundoNombreCte1` | `CHAR(26)` | L47 |
| `cApellPaternoCte1` | `CHAR(26)` | L48 |
| `cApellMaternoCte1` | `CHAR(26)` | L49 |
| `dFechaNacCte1` | `DATE` | L50 |
| `cGeneroCte1` | `CHAR(1)` | L51 |
| `cObservacionCte1` | `CHAR(100)` | L52 |
| `cNumCliente2` | `CHAR(20)` | L53 |
| `cPrimerNombreCte2` | `CHAR(26)` | L54 |
| `cSegundoNombreCte2` | `CHAR(26)` | L55 |
| `cApellPaternoCte2` | `CHAR(26)` | L56 |
| `cApellMaternoCte2` | `CHAR(26)` | L57 |
| *…12 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L157 |
| `sw_tr_fusion_motivos_pendiente` | `bdicnweb` | no | SELECT | L182 |
| `sw_tr_fusioncat_motivos_pendiente` | `bdicnweb` | no | SELECT | L192 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L135 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L71 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L120 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL` |  |
| L122 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L198 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |
| L208 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fusionados` | MODIF | fusionados | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr_`, `?orte`, `?no` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tr_genreportectesnofusionados_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tr_genreportectesnofusionados_totales.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 43 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera reporte clientes (fusionados, totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_tr_genreportectesnofusionados_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFechaInicio                 DATE
  pFechaFin                    DATE
  pIdEmpleado                  CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pIdEmpleado` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFechaInicio IS NULL OR pFechaFin IS NULL THEN` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `?orte` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ctes` | ENTIDAD | clientes | 🔵 CONVENCIÓN | nombre_sp |
| `?no` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fusionados` | MODIF | fusionados | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr_`, `?orte`, `?no` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_tr_guarda_motpendiente_fusion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_tr_guarda_motpendiente_fusion.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "guarda (tipo)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_tr_guarda_motpendiente_fusion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdRegistro                  INTEGER
  pIdMotivoPendiente           INTEGER
  pOtroMotivo                  CHAR(100)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdRegistro` | `INTEGER` | — | — |
| `pIdMotivoPendiente` | `INTEGER` | — | — |
| `pOtroMotivo` | `CHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iNoRegistros` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_tr_fusioncat_motivos_pendiente` | `bdicnweb` | no | SELECT | L29 |
| `sw_tr_fusion_motivos_pendiente` | `bdicnweb` | no | INSERT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00284';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdRegistro IS NULL OR pIdMotivoPendiente IS NULL THEN` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?_mo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tp` | MODIF | tipo | 🔵 CONVENCIÓN | nombre_sp |
| `?endiente_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fusion` | ACCION | fusiona cuentas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr_`, `?_mo`, `?endiente_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_traspasoctabeneficencia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_traspasoctabeneficencia.sql` |
| **LOC (1er CREATE)** | 1488 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "traspaso entre cuentas cuenta y beneficiario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 10 llamada(s): `sp_registra_evento`, `sp_cnsif_confirmaejecutivo`, `sp_obtfoliosuc` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_traspasoctabeneficencia(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoTransferencia           CHAR(1)
  pNumDiasSMVDF                SMALLINT
  pBancoDestino                INTEGER
  pNombreBeneficiario          CHAR(40)
  pCuentaBeneficiario          CHAR(20)
  pConceptoPagoSPEI            CHAR(40)
  pIdPlantilla                 CHAR(25)
  pTituloPlantilla             CHAR(255)
) RETURNING CHAR(5)       AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoTransferencia` | `CHAR(1)` | — | — |
| `pNumDiasSMVDF` | `SMALLINT` | — | — |
| `pBancoDestino` | `INTEGER` | — | — |
| `pNombreBeneficiario` | `CHAR(40)` | `benef`=beneficiario | 🔵 CONVENCIÓN |
| `pCuentaBeneficiario` | `CHAR(20)` | `benef`=beneficiario | 🔵 CONVENCIÓN |
| `pConceptoPagoSPEI` | `CHAR(40)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pIdPlantilla` | `CHAR(25)` | — | — |
| `pTituloPlantilla` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L16 |
| `cCodRet2` | `CHAR(5)` | L17 |
| `cCodRet3` | `CHAR(50)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `iSamErr` | `INTEGER` | L20 |
| `cDesErr` | `CHAR(50)` | L21 |
| `dValorSM` | `DECIMAL(14,2)` | L22 |
| `iTotalCuentas` | `INTEGER` | L23 |
| `dTotalSaldo` | `DECIMAL(14,2)` | L24 |
| `iNoAnios` | `INTEGER` | L25 |
| `cCodRetSp` | `CHAR(5)` | L26 |
| `iCodRetSp` | `INTEGER` | L27 |
| `cMensajeError` | `CHAR(100)` | L28 |
| `cCveRastreo` | `CHAR(30)` | L29 |
| `cCodRetTEF` | `CHAR(5)` | L30 |
| `cSucursalEmpleado` | `CHAR(4)` | L31 |
| `cCuenta` | `CHAR(20)` | L32 |
| `cCuentaOrden` | `CHAR(20)` | L33 |
| `cCuentaOrdentef` | `CHAR (20)` | L34 |
| `cNombreOrden` | `CHAR(40)` | L35 |
| `cRFCOrden` | `CHAR(18)` | L36 |
| `bInTransaction` | `BOOLEAN` | L37 |
| `cEmpresa` | `CHAR(3)` | L39 |
| `cSucursalSPEI` | `CHAR(4)` | L40 |
| `cFolioSucursalSPEI` | `CHAR(16)` | L41 |
| *…115 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L339 |
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L405 |
| `sc_cuentas_concentradas` | `bdicheq` | ⚠️ sí | SELECT | L421 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L464 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L486 |
| `si_fechavalor` | `bdinteg` | ⚠️ sí | SELECT | L540 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | INSERT | L603 |
| `sc_cuentas_traspbenef` | `bdicheq` | ⚠️ sí | INSERT | L714 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_evento` | `bdimnsj` | ⚠️ sí | L324 |
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L389 |
| `sp_obtfoliosuc` | `bdispei` | ⚠️ sí | L796 |
| `sp_regordenpagospei_pp` | `bdispei` | ⚠️ sí | L816 |
| `cargo_ref` | `bdicheq` | ⚠️ sí | L825 |
| `sp_validadiahabiltef` | `bditef` | ⚠️ sí | L1014 |
| `sp_validahorariotef` | `bditef` | ⚠️ sí | L1044 |
| `sp_obtienecveratreo` | `bditef` | ⚠️ sí | L1074 |
| `sp_validafecha` | `bdispei` | ⚠️ sí | L1109 |
| `sp_grabaoperaciontef` | `bditef` | ⚠️ sí | L1142 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L163 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L272 | FÓRMULA | `LET v_c_vcomienza = -1;` |  |
| L349 | VALIDACIÓN_NULL | `IF ( pUsuario = '' OR pIdFuncion = '' OR pNumDiasSMVDF IS NULL OR pTipoTransferencia = '' OR` |  |
| L357 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L369 | CÓDIGO_RETORNO | `LET cCodRet = '00102';` |  |
| L375 | VALIDACIÓN_NULL | `IF pBancoDestino IS NULL OR pConceptoPagoSPEI = '' THEN` |  |
| L382 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L436 | VALIDACIÓN_NULL | `IF iTotalCuentas = 0 OR dTotalSaldo IS NULL THEN` |  |
| L443 | CÓDIGO_RETORNO | `LET cCodRet = '00462';` |  |
| L571 | VALIDACIÓN_NULL | `IF vbase_exenta is null THEN` |  |
| L575 | FÓRMULA | `LET iDias = vFechaHoy - dFechaConcentra;` |  |
| L576 | FÓRMULA | `LET vBase_gravable = mMontoConcentrado - vbase_exenta;` | 🔴 MONEY/aritmética financiera |
| L581 | FÓRMULA | `LET vIsrCalc = (vBase_gravable * (dPorRetSuj/100)) * iDias / iAniobase;` |  |
| L586 | FÓRMULA | `LET vIsrCalc = (mMontoConcentrado * (dPorRetSuj/100)) * iDias / iAniobase;` | 🔴 MONEY/aritmética financiera |
| L594 | FÓRMULA | `LET vValSaldo = NVL(mMontoConcentrado,0.00) + NVL(vAcum_sdo_int,0.00) + NVL(vInts_prov_acum,0.00) - ` | 🔴 MONEY/aritmética financiera |
| L609 | FÓRMULA | `LET vPagoInts =  NVL(vAcum_sdo_int,0.00) + NVL(vInts_prov_acum,0.00);` |  |
| L616 | FÓRMULA | `LET iNoAbonos = iNoAbonos + 1;` |  |
| L626 | FÓRMULA | `LET iNoCargos = iNoCargos + 1;` |  |
| L630 | FÓRMULA | `LET vSdoIntIsr = NVL(mSdoActCargo,0.00) + NVL(vPagoInts,0.00) - NVL(vIsrCalc,0.00);` |  |
| L743 | FÓRMULA | `LET v_c_vcontador = v_c_vcontador + 1;` |  |
| L802 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdispei:sp_obtfoliosuc';` |  |
| L810 | CÓDIGO_RETORNO | `LET cCodRet = '00427';` |  |
| L839 | FÓRMULA | `LET cCodRet = cCodRetCgo; -- Codigo de Retorno del Cargo a la Cuenta Global` |  |
| L848 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdispei:.sp_regordenpagospei_pp';` |  |
| L1020 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_validadiahabiltef';` |  |
| L1028 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L1050 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_validahorariotef';` |  |
| L1058 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L1080 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bditef:sp_obtienecveratreo';` |  |
| L1088 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| | *…6 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `traspaso` | ACCION | traspaso entre cuentas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `benef` | ENTIDAD | beneficiario | 🔵 CONVENCIÓN | nombre_sp |
| `?icencia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?icencia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_trxsabonotransfersoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_trxsabonotransfersoc.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "abono, transferencia y Sistema Operativo Central" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_trxsabonotransfersoc(
  pEmpresa                     CHAR(3)
) RETURNING CHAR(5), CHAR(4), CHAR(80)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cTransacc` | `CHAR(4)` | L11 |
| `cDescripcion` | `CHAR(80)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_trxtrfabonosoc` | `bdicheq` | ⚠️ sí | SELECT | L45 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `abono` | ENTIDAD | abono / crédito | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | 🔵 CONVENCIÓN | nombre_sp |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_trxscargotransfersoc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_trxscargotransfersoc.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "cargo, transferencia y Sistema Operativo Central" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_trxscargotransfersoc(
  pEmpresa                     CHAR(3)
) RETURNING CHAR(5), CHAR(4), CHAR(80)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cTransacc` | `CHAR(4)` | L11 |
| `cDescripcion` | `CHAR(80)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_trxtrfcargosoc` | `bdicheq` | ⚠️ sí | SELECT | L45 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_tr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cargo` | ENTIDAD | cargo / débito | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | 🔵 CONVENCIÓN | nombre_sp |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tr`, `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_usuario_movil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_usuario_movil.sql` |
| **LOC (1er CREATE)** | 174 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 69 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "usuario (canal móvil)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_usuario_movil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pEjecutivoAlta               CHAR(8)
  pPassword                    CHAR(20)
  pImei                        CHAR(60)
  pImeiAnt                     CHAR(60)
  pActivo                      CHAR(1)
  pNombre                      CHAR(60)
  pCentro_costos               CHAR(8)
  pSucursal                    CHAR(4)
  pNo_telefono                 CHAR(10)
  pGenerico1                   CHAR(20)
  pGenerico2                   CHAR(30)
  pGenerico3                   CHAR(40)
  ptipoOperacion               INTEGER
  pPerfil                      SMALLINT
  pDeviceId                    CHAR(20)
) RETURNING CHAR(5) AS codret,INTEGER AS iNoRegistros
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pEjecutivoAlta` | `CHAR(8)` | — | — |
| `pPassword` | `CHAR(20)` | — | — |
| `pImei` | `CHAR(60)` | — | — |
| `pImeiAnt` | `CHAR(60)` | — | — |
| `pActivo` | `CHAR(1)` | — | — |
| `pNombre` | `CHAR(60)` | — | — |
| `pCentro_costos` | `CHAR(8)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pNo_telefono` | `CHAR(10)` | — | — |
| `pGenerico1` | `CHAR(20)` | — | — |
| `pGenerico2` | `CHAR(30)` | — | — |
| `pGenerico3` | `CHAR(40)` | — | — |
| `ptipoOperacion` | `INTEGER` | — | — |
| `pPerfil` | `SMALLINT` | — | — |
| `pDeviceId` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |
| `bExisteUsuario` | `BOOLEAN` | L12 |
| `inoImei` | `INTEGER` | L13 |
| `bInTransaction` | `BOOLEAN` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_usuario_movil` | `bdinteg` | ⚠️ sí | SELECT | L54 |
| `si_usuario_movil` | `bdinteg` | ⚠️ sí | INSERT | L57 |
| `si_usuario_movil` | `bdinteg` | ⚠️ sí | UPDATE | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00479';` |  |
| L65 | CÓDIGO_RETORNO | `LET cCodRet = '00282';` |  |
| L98 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `movil` | MODIF | canal móvil | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_usuariocedulacons`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_usuariocedulacons.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 401 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta usuario y cédula de identificación" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_usuariocedulacons(
  pRol                         SMALLINT
  pCedula                      SMALLINT
) RETURNING CHAR(5), CHAR(8), CHAR(104), SMALLINT, SMALLINT
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRol` | `SMALLINT` | — | — |
| `pCedula` | `SMALLINT` | `cedula`=cédula de identificación | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cUsuario` | `CHAR(8)` | L11 |
| `cNombre` | `CHAR(104)` | L12 |
| `cConcepto` | `CHAR(10)` | L13 |
| `iFuncion` | `SMALLINT` | L14 |
| `iCedula` | `SMALLINT` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontablenombre2` | `bdicheq` | ⚠️ sí | SELECT | L71 |
| `sc_cedulacontableusuarios` | `bdicheq` | ⚠️ sí | SELECT | L75 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | VALIDACIÓN_NULL | `IF ( ( pRol IS NULL OR pRol NOT IN(1,2,3) )` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cedula` | ENTIDAD | cédula de identificación | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_usuarioscedulas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_usuarioscedulas.sql` |
| **LOC (1er CREATE)** | 179 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "usuarios y cédulas" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_usuarioscedulas(
  pFechaConcil                 DATE
  pTipo                        SMALLINT
) RETURNING CHAR(5), CHAR(104), SMALLINT
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaConcil` | `DATE` | — | — |
| `pTipo` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cNombre` | `CHAR(104)` | L11 |
| `iFuncion` | `SMALLINT` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontableusuarios` | `bdicheq` | ⚠️ sí | SELECT | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L43 | VALIDACIÓN_NULL | `IF ( ( pFechaConcil is null OR pFechaConcil = '' ) OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `usuarios` | ENTIDAD | usuarios | 🔵 CONVENCIÓN | nombre_sp |
| `cedulas` | ENTIDAD | cédulas | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_usuarioscedulascons`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_usuarioscedulascons.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuarios y cédulas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_usuarioscedulascons(
  pEmpresa                     CHAR(3)
) RETURNING CHAR(5), CHAR(8), CHAR(104), CHAR(8), CHAR(10), CHAR(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L4 |
| `cCodRet2` | `CHAR(5)` | L5 |
| `cCodRet3` | `CHAR(50)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iSamErr` | `INTEGER` | L8 |
| `cDesErr` | `CHAR(50)` | L9 |
| `iExiste` | `SMALLINT` | L10 |
| `cUsuario` | `CHAR(8)` | L11 |
| `cNombre` | `CHAR(104)` | L12 |
| `cFuncion` | `CHAR(8)` | L13 |
| `cConcepto` | `CHAR(10)` | L14 |
| `cEstatus` | `CHAR(10)` | L15 |
| `iFuncion` | `SMALLINT` | L16 |
| `cStatus` | `CHAR(1)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontableusuarios` | `bdicheq` | ⚠️ sí | SELECT | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L53 | VALIDACIÓN_NULL | `IF pEmpresa is null OR pEmpresa <> '001' THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `usuarios` | ENTIDAD | usuarios | 🔵 CONVENCIÓN | nombre_sp |
| `cedulas` | ENTIDAD | cédulas | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_usuarioscedulasmantto`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_usuarioscedulasmantto.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 400 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "usuarios y cédulas (anterior)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_usuarioscedulasmantto(
  pEmpleado                    CHAR(8)
  pNombre                      CHAR(104)
  pStatus                      CHAR(1)
  pFuncion                     SMALLINT
  pCedula                      SMALLINT
  pTipo                        SMALLINT
  pFuncionAnt                  SMALLINT
  pCedulaAnt                   SMALLINT
  pStatusAnt                   SMALLINT
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpleado` | `CHAR(8)` | — | — |
| `pNombre` | `CHAR(104)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |
| `pFuncion` | `SMALLINT` | — | — |
| `pCedula` | `SMALLINT` | — | — |
| `pTipo` | `SMALLINT` | — | — |
| `pFuncionAnt` | `SMALLINT` | `ant`=anterior | 🔵 CONVENCIÓN |
| `pCedulaAnt` | `SMALLINT` | `ant`=anterior | 🔵 CONVENCIÓN |
| `pStatusAnt` | `SMALLINT` | `ant`=anterior | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet1` | `CHAR(5)` | L12 |
| `cCodRet2` | `CHAR(5)` | L13 |
| `cCodRet3` | `CHAR(50)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `iSamErr` | `INTEGER` | L16 |
| `cDesErr` | `CHAR(50)` | L17 |
| `cConcepto` | `CHAR(10)` | L18 |
| `cConceptoAnt` | `CHAR(10)` | L19 |
| `iExiste` | `SMALLINT` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_cedulacontablenombre2` | `bdicheq` | ⚠️ sí | SELECT | L76 |
| `sc_cedulacontableusuarios` | `bdicheq` | ⚠️ sí | SELECT | L82 |
| `sc_cedulacontableusuarios` | `bdicheq` | ⚠️ sí | INSERT | L92 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | VALIDACIÓN_NULL | `IF ( ( pEmpleado is null OR pEmpleado = '' ) OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `usuarios` | ENTIDAD | usuarios | 🔵 CONVENCIÓN | nombre_sp |
| `cedulas` | ENTIDAD | cédulas | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?to` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?m`, `?to` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_usuariosfuncionalidad`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_usuariosfuncionalidad.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=46 |
| **Deps concatenadas** | 81 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "usuarios y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_usuariosfuncionalidad(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdFuncionConsulta           CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pIdFuncionConsulta` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEjecutivo` | `CHAR(8)` | L8 |
| `cNombreEjecutivo` | `CHAR(45)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_seg_usuarios_funciones` | `bdinteg` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?funcional` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?funcional`, `?ad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_bancoctaclabe`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_valida_bancoctaclabe.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida banco y cuenta CLABE" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_valida_bancoctaclabe(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCtaClabe                    CHAR(18)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCtaClabe` | `CHAR(18)` | `ctaclabe`=cuenta CLABE | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cNombreCorto` | `CHAR(25)` | L9 |
| `iCveCeSif` | `INTEGER` | L10 |
| `cCodCtaCbe` | `CHAR(3)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00431';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `banco` | ENTIDAD | banco | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctaclabe` | ENTIDAD | cuenta CLABE | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_valida_correo_ob`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_valida_correo_ob.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida correo electrónico" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_valida_correo_ob(
  pRFC                         CHAR(13)
  pCorreoElec                  CHAR(100)
) RETURNING CHAR(5) AS vcodret1,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRFC` | `CHAR(13)` | — | — |
| `pCorreoElec` | `CHAR(100)` | `correo`=correo electrónico | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vcodret1` | `CHAR(5)` | L6 |
| `vcodret2` | `CHAR(5)` | L7 |
| `vcodret3` | `CHAR(50)` | L8 |
| `sql_err` | `INTEGER` | L9 |
| `isam_err` | `INTEGER` | L10 |
| `desc_err` | `CHAR(50)` | L11 |
| `vExisteCte` | `INTEGER` | L13 |
| `vExisteCorreo` | `SMALLINT` | L14 |
| `vExisteCteCorreo` | `INTEGER` | L15 |
| `vCorreoNoValido` | `INTEGER` | L16 |
| `vNumCte` | `CHAR(20)` | L17 |
| `vMensaje` | `CHAR(50)` | L18 |
| `vRfc` | `CHAR(50)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cat_correos_novalidos` | `bdinteg` | ⚠️ sí | SELECT | L68 |
| `si_correos` | `bdinteg` | ⚠️ sí | SELECT | L80 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L103 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L58 | VALIDACIÓN_NULL | `IF (pRFC is null OR pRFC = '') OR` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `correo` | ENTIDAD | correo electrónico | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_ob` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ob` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_cuentacan`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_valida_cuentacan.sql` |
| **LOC (1er CREATE)** | 521 |
| **Callgraph** | ✅ fan_in=18 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_valida_cuentacan(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCuenta` | `CHAR(20)` | `cuenta`=cuenta | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `v_Cuenta` | `CHAR(20)` | L8 |
| `v_Cliente` | `CHAR(20)` | L9 |
| `v_RazonSocial` | `CHAR(120)` | L10 |
| `v_SdoActual` | `MONEY` | L11 |
| `v_SdoCongelado` | `MONEY` | L12 |
| `v_LimSbgCCC` | `MONEY` | L13 |
| `v_ImpChqSbg` | `MONEY` | L14 |
| `v_ComPendiente` | `MONEY` | L15 |
| `v_FecUltMov` | `DATE` | L16 |
| `v_Producto` | `CHAR(4)` | L17 |
| `v_ProdNoCancelacion` | `INTEGER` | L18 |
| `anio_actual` | `INTEGER` | L19 |
| `anio_pasado` | `INTEGER` | L20 |
| `mes_actual` | `INTEGER` | L21 |
| `v_Anio` | `SMALLINT` | L22 |
| `v_capvigprom1` | `MONEY` | L24 |
| `v_capvigprom2` | `MONEY` | L25 |
| `v_capvigprom3` | `MONEY` | L26 |
| `v_capvigprom4` | `MONEY` | L27 |
| `v_capvigprom5` | `MONEY` | L28 |
| `v_capvigprom6` | `MONEY` | L29 |
| `v_capvigprom7` | `MONEY` | L30 |
| `v_capvigprom8` | `MONEY` | L31 |
| *…33 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L172 |
| `sc_productonocancelacion` | `bdicheq` | ⚠️ sí | SELECT | L178 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L394 |
| `tf_account_balance_customer` | `bditransfer` | ⚠️ sí | SELECT | L406 |
| `sd_ctascarg` | `bdicred` | ⚠️ sí | SELECT | L410 |
| `acl_producto` | `bdiaclaracion` | ⚠️ sí | SELECT | L428 |
| `si_cliente_emp_pru` | `bdicnweb` | no | SELECT | L434 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L440 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L447 |
| `sw_ctasnocanceladas` | `bdicnweb` | no | INSERT | L452 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L162 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L127 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L158 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L182 | FÓRMULA | `LET v_mes_anio_actual = mes_actual - 1;` |  |
| L183 | FÓRMULA | `LET v_mes_anio_anterior = 12 - v_mes_anio_actual;` |  |
| L389 | FÓRMULA | `LET v_SaldoPromedioTotal = v_capvigprom1 + v_capvigprom2 + v_capvigprom3 + v_capvigprom4 + v_capvigp` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?can` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?can` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_descargaimg_ccep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_valida_descargaimg_ccep.sql` |
| **LOC (1er CREATE)** | 218 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida descarga y CCE — Cámara de Compensación Electrónica" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_valida_descargaimg_ccep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pDireccionMac                CHAR(15)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pDireccionMac` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iIdConsultaDetalleCheque40` | `INTEGER` | L8 |
| `cBanco` | `CHAR(3)` | L9 |
| `cCuentaReferencia` | `CHAR(20)` | L10 |
| `cNumCheque` | `INTEGER` | L11 |
| `mImporte` | `DECIMAL(14,2)` | L12 |
| `cIndImgCheque` | `CHAR(1)` | L13 |
| `iTamAnvImgCheque` | `INTEGER` | L14 |
| `iTamRevImgCheque` | `INTEGER` | L15 |
| `cEjecutivo` | `CHAR(8)` | L16 |
| `cDireccionMac` | `CHAR(15)` | L17 |
| `iContadorDescargaImg` | `INTEGER` | L18 |
| `mMontoImagen` | `DECIMAL(16,2)` | L19 |
| `tamImgOrigF` | `INTEGER` | L20 |
| `tamImgOrigT` | `INTEGER` | L21 |
| `bTamDiferente` | `BOOLEAN` | L22 |
| `bExistTamDifImg` | `CHAR(1)` | L23 |
| `iExistenImgsDigitalizadas` | `INTEGER` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `iTamImgChqAnverso` | `INTEGER` | L26 |
| `iTamImgChqReverso` | `INTEGER` | L27 |
| `cIsImagenCheque` | `CHAR(1)` | L28 |
| `bImagenF` | `BLOB` | L29 |
| `bImagenT` | `BLOB` | L30 |
| *…3 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L100 |
| `cce_cheques_img` | `bditef` | ⚠️ sí | SELECT | L118 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L84 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L79 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L125 | FÓRMULA | `LET iContadorDescargaImg = iContadorDescargaImg + 1;` |  |
| L135 | FÓRMULA | `LET iContadorDescargaImg = iContadorDescargaImg + 1;` |  |
| L205 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `descarga` | ENTIDAD | descarga | 🔵 CONVENCIÓN | nombre_sp |
| `?img_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cce` | ENTIDAD | CCE — Cámara de Compensación Electrónica (CECOBAN/Banxico) — | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?img_`, `?p` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validaarchcod60tef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validaarchcod60tef.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida archivo, código y TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_validaarchcod60tef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `cCodRetSp` | `CHAR(6)` | L10 |
| `cDescCodRet` | `CHAR(100)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cTipoProceso` | `CHAR(1)` | L14 |
| `dFechaProceso` | `CHAR(10)` | L15 |
| `cClaveProceso` | `CHAR(20)` | L16 |
| `cDescripcion` | `CHAR(60)` | L17 |
| `cEstatus` | `CHAR(1)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |
| `sp_tef_validarchcod60` | `bditef` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_tef_validarchcod60';` |  |
| L72 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `cod` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?60` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?60` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validacaracter`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validacaracter.sql` |
| **LOC (1er CREATE)** | 154 |
| **Callgraph** | ✅ fan_in=7 / fan_out=3 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_validacaracter(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCadena                      CHAR(500)
  pTipoCompara                 CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCadena` | `CHAR(500)` | — | — |
| `pTipoCompara` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(6)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `cTamCadena` | `CHAR(500)` | L10 |
| `iPos` | `INTEGER` | L11 |
| `cComparaCadena` | `CHAR(65)` | L12 |
| `cCaracter` | `CHAR(1)` | L13 |
| `cCaracterInvalido` | `CHAR(1)` | L14 |
| `bPositionFind` | `boolean` | L15 |
| `iPosTrama` | `INTEGER` | L16 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L18 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET cTamCadena = LENGTH(pCadena) - 1;` |  |
| L73 | FÓRMULA | `LET iPos = iPos + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?car` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?er` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?car`, `?er` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validacarga_desactualizadasbc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validacarga_desactualizadasbc.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida Servicios de Atención al Cliente — subsistema de atención en sucursal y saldo básico de cuenta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_validacarga_desactualizadasbc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(4)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEncontrado` | `CHAR(1)` | L7 |
| `iNoRegistros` | `INT` | L8 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `?_de` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sac` | ENTIDAD | Servicios de Atención al Cliente — subsistema de atención en | 🟡 INFERIDO | nombre_sp |
| `?tualizada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sbc` | ENTIDAD | saldo básico de cuenta (SBC) | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_de`, `?tualizada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validacodigoproveedorcaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validacodigoproveedorcaja.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida código, ro — Rol Operativo, estado y caja" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_validacodigoproveedorcaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodProveedor                CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodProveedor` | `CHAR(4)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) · `edo`=estado | 🟡 INFERIDO / 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `iSqlErr` | `INTEGER` | L8 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `codigo` | ENTIDAD | código | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?ve` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `edo` | ENTIDAD | estado | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?ve`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validactabeneficiariotef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validactabeneficiariotef.sql` |
| **LOC (1er CREATE)** | 155 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida cuenta, beneficiario y TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_validactabeneficiariotef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoCuenta                  CHAR(2)
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoCuenta` | `CHAR(2)` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cCodRetSp2` | `CHAR(5)` | L8 |
| `cDescCodRet` | `CHAR(100)` | L9 |
| `iCodRetSp` | `INTEGER` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `iTipo` | `INTEGER` | L13 |
| `cTarjeta` | `CHAR(20)` | L14 |
| `cClaveBanco` | `CHAR(3)` | L15 |
| `cCtaClabe` | `CHAR(20)` | L16 |
| `cDigito` | `CHAR(1)` | L17 |
| `cDigVerificador` | `CHAR(1)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `17` | `bdicnweb` | no | SELECT | L94 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_obtbines_sif` | `bditef` | ⚠️ sí | L72 |
| `digverclabe` | `bdicheq` | ⚠️ sí | L97 |
| `sp_tef_validarecepcion` | `bditef` | ⚠️ sí | L114 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L76 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_obtbines_sif';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00526';` |  |
| L101 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdicheq:digverclabe';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00240';` |  |
| L118 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_tef_validarecepcion'` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00431';` |  |
| L131 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `beneficiario` | ENTIDAD | beneficiario (receptor del pago SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validacte_transfer`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validacte_transfer.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=66 / fan_out=0 |
| **Principales callers** | `sp_cnsif_consprodcte` |
| **Propósito inferido** | "valida cliente y transferencia" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_validacte_transfer(
  cNumCtePrincipal             CHAR(20)
) RETURNING CHAR(5)     AS Cod_Retorno,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cNumCtePrincipal` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iTpo_cliente` | `INT` | L7 |
| `cCodRet` | `CHAR(5)` | L8 |
| `iExiste` | `INT` | L9 |
| `iSql_err` | `INT` | L10 |
| `cNumCte` | `CHAR(20)` | L11 |
| `cNumCteTf` | `CHAR(20)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tf_maecte` | `bditransfer` | ⚠️ sí | SELECT | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | VALIDACIÓN_NULL | `IF cNumCteTf IS NOT NULL AND (cNumCte IS NULL or cNumCte = '') THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validadecimales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validadecimales.sql` |
| **LOC (1er CREATE)** | 49 |
| **Callgraph** | ✅ fan_in=38 / fan_out=5 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_validadecimales(
  cadena                       CHAR(50)
  pProducto                    CHAR(4)
  pBandera                     CHAR(1)
) RETURNING BOOLEAN
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cadena` | `CHAR(50)` | — | — |
| `pProducto` | `CHAR(4)` | — | — |
| `pBandera` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `resultado` | `BOOLEAN` | L4 |
| `d` | `DECIMAL` | L5 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?decimales` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?decimales` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validadetallerevcheques`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validadetallerevcheques.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ✅ fan_in=14 / fan_out=9 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida detalle y cheques" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_validadetallerevcheques(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L15 |
| `iSqlErr` | `INTEGER` | L16 |
| `cEmpresa` | `CHAR(3)` | L17 |
| `cCuenta` | `CHAR(20)` | L18 |
| `iCheque` | `INTEGER` | L19 |
| `mImporte` | `MONEY(16,2)` | L20 |
| `cMotivoDevol` | `CHAR(38)` | L21 |
| `cNumCte` | `CHAR(20)` | L22 |
| `cCliente` | `CHAR(125)` | L23 |
| `cSucursal` | `CHAR(45)` | L24 |
| `cFirmas` | `CHAR(32)` | L25 |
| `cHayFirmas` | `CHAR(1)` | L26 |
| `iIdConsCheque` | `INTEGER` | L27 |
| `iRecuperacion` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | SELECT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha = '' OR pRegistros IS NULL OR pRecuperacion IS NULL TH` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L79 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validadetallerevcheques_totales`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validadetallerevcheques_totales.sql` |
| **LOC (1er CREATE)** | 223 |
| **Callgraph** | ✅ fan_in=14 / fan_out=9 |
| **Deps concatenadas** | 31 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida detalle y cheques (totales)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_validadetallerevcheques_totales(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cInstImagenes` | `CHAR(25)` | L10 |
| `cTabla` | `CHAR(38)` | L11 |
| `cCodDocumento` | `CHAR(4)` | L12 |
| `cCuenta` | `CHAR(20)` | L13 |
| `iCheque` | `INTEGER` | L14 |
| `mImporte` | `MONEY(16,2)` | L15 |
| `cMotivoDevol` | `CHAR(38)` | L16 |
| `cBancoReceptor` | `CHAR(3)` | L17 |
| `cNombreArchivo` | `CHAR(22)` | L18 |
| `iSecuencia` | `INTEGER` | L19 |
| `cNumCliente` | `CHAR(20)` | L20 |
| `cNombreCliente` | `CHAR(107)` | L21 |
| `cRfc` | `CHAR(13)` | L22 |
| `cCveSucursal` | `CHAR(4)` | L23 |
| `cDescSucursal` | `CHAR(40)` | L24 |
| `cNumCte` | `CHAR(20)` | L25 |
| `cCliente` | `CHAR(125)` | L26 |
| `cSucursal` | `CHAR(45)` | L27 |
| `cCombFirmantes` | `CHAR(120)` | L28 |
| `iImgF` | `SMALLINT` | L29 |
| `iImgT` | `SMALLINT` | L30 |
| `iSecExpediente` | `SMALLINT` | L31 |
| *…11 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `cce_param` | `bditef` | ⚠️ sí | SELECT | L105 |
| `tmp_img_cheques` | `bdicnweb` | no | SELECT | L118 |
| `tmp_img_cheques` | `bdicnweb` | no | DELETE | L118 |
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | SELECT | L119 |
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | DELETE | L119 |
| `tmp_img_cheques` | `bdicnweb` | no | INSERT | L122 |
| `cce_cheques_img` | `bditef` | ⚠️ sí | SELECT | L126 |
| `cce_propios_det` | `bditef` | ⚠️ sí | SELECT | L145 |
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | INSERT | L187 |
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | UPDATE | L192 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L98 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L174 | FÓRMULA | `LET iContImagenes = iContImagenes + 1;` |  |
| L185 | FÓRMULA | `LET iIdConsCheque = iIdConsCheque + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `rev` | ACCION | reversión (abreviación de reversa/reverso) | 🟡 INFERIDO | nombre_sp |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `totales` | MODIF | totales | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_validaejecucion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validaejecucion.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ✅ fan_in=1 / fan_out=24 |
| **Deps concatenadas** | 30 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida ejecución" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_validaejecucion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cEjecutaProceso` | `CHAR(1)` | L8 |
| `cPuesto` | `CHAR(3)` | L9 |
| `cSucursal` | `CHAR(4)` | L10 |
| `cNombre` | `CHAR(40)` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ejecut` | `bdinteg` | ⚠️ sí | SELECT | L52 |
| `si_vbparam` | `bdinteg` | ⚠️ sí | SELECT | L54 |
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L66 |
| `si_contproc` | `bdinteg` | ⚠️ sí | SELECT | L74 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pIdConsulta = '' OR pFecha IS NULL THEN` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ejecucion` | ENTIDAD | ejecución (de proceso) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_validaestatuspoliza`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validaestatuspoliza.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 22 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida estatus y póliza contable" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_validaestatuspoliza(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cPoliza` | `INTEGER` | L14 |
| `iNumRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatuspoliza` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `poliza` | ENTIDAD | póliza contable | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validafechacarga`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validafechacarga.sql` |
| **LOC (1er CREATE)** | 31 |
| **Callgraph** | ✅ fan_in=38 / fan_out=5 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida fecha" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_validafechacarga(
  cadena                       CHAR(10)
) RETURNING BOOLEAN
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `cadena` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `fecha` | `DATE` | L4 |
| `valido` | `BOOLEAN` | L5 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | FÓRMULA | `LET fecha = TO_DATE(cadena, '%d-%m-%Y');` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `carga` | ACCION | carga / ingresa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validafrcctemoral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validafrcctemoral.sql` |
| **LOC (1er CREATE)** | 194 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 90 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida cc — cuenta corriente (persona moral)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_validafrcctemoral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRfc                         CHAR(14)
  pTipoCte                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRfc` | `CHAR(14)` | — | — |
| `pTipoCte` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cLetra` | `CHAR(3)` | L6 |
| `cFecha` | `CHAR(6)` | L7 |
| `cAnio` | `CHAR(4)` | L8 |
| `cAnioActual` | `CHAR(4)` | L9 |
| `iAscii` | `SMALLINT` | L10 |
| `dFechaInicio` | `DATE` | L11 |
| `iAnio` | `INTEGER` | L12 |
| `iAnioActual` | `INTEGER` | L13 |
| `iTotal` | `INTEGER` | L14 |
| `t_count` | `INTEGER` | L15 |
| `u_count` | `INTEGER` | L16 |
| `cDia` | `CHAR(2)` | L17 |
| `cMes` | `CHAR(2)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `t_count` | `bdicnweb` | no | SELECT | L69 |
| `u_count` | `bdicnweb` | no | SELECT | L83 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L174 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00313';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00286';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00287';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00288';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00314';` |  |
| L110 | CÓDIGO_RETORNO | `LET cCodRet = '00315';` |  |
| L124 | CÓDIGO_RETORNO | `LET cCodRet = '00316';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00289';` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00290';` |  |
| L182 | CÓDIGO_RETORNO | `LET cCodRet = '00291';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?fr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `moral` | MODIF | persona moral | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?fr`, `?te` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validafrcctemoral2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validafrcctemoral2.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 28 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida cc — cuenta corriente (persona moral)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_validafrcctemoral2(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRfc                         CHAR(14)
  pTipoCte                     CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRfc` | `CHAR(14)` | — | — |
| `pTipoCte` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iTotal` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L32 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00313';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00291';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?fr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cc` | ENTIDAD | cc — cuenta corriente (sp_*_cc_* — bdicnweb) | 🟡 INFERIDO | nombre_sp |
| `?te` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `moral` | MODIF | persona moral | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?fr`, `?te`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validahrfechaprocaptef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validahrfechaprocaptef.sql` |
| **LOC (1er CREATE)** | 105 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida fecha, proceso y TEF — transferencia electrónica de fondos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_validahrfechaprocaptef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `cCodRetSp` | `CHAR(6)` | L6 |
| `cCodRetSp2` | `CHAR(5)` | L7 |
| `cDescCodRet` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cFechaHoy` | `CHAR(10)` | L11 |
| `cHora` | `CHAR(10)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sv_fechas` | `bdinvers` | ⚠️ sí | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |
| `sp_validadiahabiltef` | `bditef` | ⚠️ sí | L53 |
| `sp_tef_validahorario` | `bditef` | ⚠️ sí | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L57 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_validadiahabiltef';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_tef_validahorario';` |  |
| L82 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L88 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?hr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proc` | ENTIDAD | proceso | 🟡 INFERIDO | nombre_sp |
| `?ap` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?hr`, `?ap` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validaimagenchequesfirmas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validaimagenchequesfirmas.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=14 / fan_out=9 |
| **Deps concatenadas** | 32 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida imagen digital, cheques y firmas mancomunadas" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_validaimagenchequesfirmas(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pFecha                       CHAR(8)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pFecha` | `CHAR(8)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L18 |
| `iSqlErr` | `INTEGER` | L19 |
| `cEmpresa` | `CHAR(3)` | L20 |
| `iIdConsCheque` | `INTEGER` | L21 |
| `cCuenta` | `CHAR(20)` | L22 |
| `iCheque` | `INTEGER` | L23 |
| `mImporte` | `MONEY(16,2)` | L24 |
| `cCveSucursal` | `CHAR(4)` | L25 |
| `cDescSucursal` | `CHAR(40)` | L26 |
| `cNumCliente` | `CHAR(20)` | L27 |
| `cNombreCliente` | `CHAR(107)` | L28 |
| `cRfc` | `CHAR(13)` | L29 |
| `cCombFirmantes` | `CHAR(120)` | L30 |
| `cRegFirmas` | `CHAR(1)` | L31 |
| `cCveBanco` | `CHAR(3)` | L32 |
| `dFechaPresenta` | `DATE` | L33 |
| `cCodDocumento` | `CHAR(4)` | L34 |
| `cSecuencia` | `SMALLINT` | L35 |
| `cSucursal` | `CHAR(45)` | L36 |
| `cCliente` | `CHAR(125)` | L37 |
| `iRecuperacion` | `INTEGER` | L38 |
| `dFechaPres` | `CHAR(10)` | L39 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_firmantes` | `bdicheq` | ⚠️ sí | SELECT | L107 |
| `sw_ccer_validadetallecheques` | `bdicnweb` | no | SELECT | L109 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L74 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pFecha = '' OR pRegistros IS NULL OR pRecuperacion IS NULL TH` |  |
| L75 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00098';` |  |
| L96 | FÓRMULA | `LET dFechaPres =  SUBSTRING(pFecha from  5 for 2) \|\| '/' \|\|  SUBSTRING(pFecha from 7 for 2) \|\|` |  |
| L120 | FÓRMULA | `LET iRecuperacion = iRecuperacion + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `imagen` | ENTIDAD | imagen digital | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cheques` | ENTIDAD | cheques | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `firmas` | ENTIDAD | firmas mancomunadas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validaintegridadccl`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validaintegridadccl.sql` |
| **LOC (1er CREATE)** | 543 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida interés, identificador y módulo de Cédulas de Captación e inversión — pagaré, ISR, saldos diarios, inversión auto-creciente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_validaintegridadccl(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pArchOrigen                  CHAR(3)
  pIntegridad                  CHAR(1)
  pConsecutivo                 INTEGER
  pNumTarjeta                  CHAR(16)
  pTipoTransaccion325          CHAR(15)
  pMonto325                    CHAR(13)
  pIdComercio325               CHAR(9)
  pNomComercio325              CHAR(30)
  pReferencia23_325            CHAR(23)
  pSecuencia325                CHAR(6)
  pDivisa325                   CHAR(3)
  pRfc325                      CHAR(16)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pArchOrigen` | `CHAR(3)` | — | — |
| `pIntegridad` | `CHAR(1)` | `int`=interés · `id`=identificador (de) | 🟡 INFERIDO / 🔵 CONVENCIÓN |
| `pConsecutivo` | `INTEGER` | — | — |
| `pNumTarjeta` | `CHAR(16)` | — | — |
| `pTipoTransaccion325` | `CHAR(15)` | — | — |
| `pMonto325` | `CHAR(13)` | — | — |
| `pIdComercio325` | `CHAR(9)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNomComercio325` | `CHAR(30)` | — | — |
| `pReferencia23_325` | `CHAR(23)` | — | — |
| `pSecuencia325` | `CHAR(6)` | — | — |
| `pDivisa325` | `CHAR(3)` | — | — |
| `pRfc325` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `cDescCodRet` | `CHAR(100)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cIntegridad` | `CHAR(1)` | L11 |
| `cErrorActividad` | `CHAR(250)` | L12 |
| `cIntegridadError` | `CHAR(20)` | L13 |
| `cSistema` | `CHAR(1)` | L14 |
| `cMontoCB325` | `CHAR(13)` | L15 |
| `cEsNoNumTarjeta` | `CHAR(1)` | L16 |
| `cEsNoIdComercio325` | `CHAR(1)` | L17 |
| `cEsNoSecuencia325` | `CHAR(1)` | L18 |
| `cEsNoDivisa325` | `CHAR(1)` | L19 |
| `cEsNoMonto325` | `CHAR(1)` | L20 |
| `cEsNoRef23_325` | `CHAR(1)` | L21 |
| `cEsNoMontoCB325` | `CHAR(1)` | L22 |
| `cMonto325` | `MONEY (18,2)` | L23 |
| `cMontoCashBack325` | `MONEY (18,2)` | L24 |
| `cBine` | `CHAR(6)` | L25 |
| `iNoRegistros` | `INTEGER` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `td_archivo_origen` | `bditarjeta` | ⚠️ sí | SELECT | L77 |
| `td_movimientos_conciliacion` | `bditarjeta` | ⚠️ sí | SELECT | L85 |
| `bines` | `intercard` | ⚠️ sí | SELECT | L160 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdinteg` | `bdicnweb` | no | L67 |
| `sp_concreing_esnumerico` | `bditarjeta` | ⚠️ sí | L90 |
| `bditarjeta` | `bdicnweb` | no | L506 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L151 | FÓRMULA | `LET cMonto325 = ((REPLACE(pMonto325,'.',''))::MONEY/100);` | 🔴 MONEY/aritmética financiera |
| L239 | FÓRMULA | `LET cMonto325 = ((REPLACE(pMonto325,'.',''))::MONEY/100);` | 🔴 MONEY/aritmética financiera |
| L311 | FÓRMULA | `LET cMonto325 = ((REPLACE(pMonto325,'.',''))::MONEY/100);` | 🔴 MONEY/aritmética financiera |
| L334 | FÓRMULA | `LET cMontoCashBack325 = ((REPLACE(cMontoCB325,'.',''))::MONEY/100);` | 🔴 MONEY/aritmética financiera |
| L427 | FÓRMULA | `LET cMonto325 = ((REPLACE(pMonto325,'.',''))::MONEY/100);` | 🔴 MONEY/aritmética financiera |
| L472 | FÓRMULA | `LET cMonto325 = ((REPLACE(pMonto325,'.',''))::MONEY/100);` | 🔴 MONEY/aritmética financiera |
| L512 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditarjeta:sp_concreing_compva` |  |
| L516 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L521 | CÓDIGO_RETORNO | `LET cCodRet = '00283';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?egr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ccl` | ENTIDAD | módulo de Cédulas de Captación e inversión — pagaré, ISR, sa | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?egr`, `?ad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validamodificacionsaldosctascap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validamodificacionsaldosctascap.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=64 |
| **Deps concatenadas** | 171 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida saldos, cuentas y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_validamodificacionsaldosctascap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pCuenta                      CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cEmpresa` | `CHAR(3)` | L7 |
| `cMuestraMsj` | `CHAR(1)` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_docret_sbc` | `bdicheq` | ⚠️ sí | SELECT | L43 |
| `sc_docret` | `bdicheq` | ⚠️ sí | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L33 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `modificacion` | ACCION | modificación | 🔵 CONVENCIÓN | nombre_sp |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_validamonitorproceso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validamonitorproceso.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 79 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida monitor y proceso" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_validamonitorproceso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMacAdress                   CHAR(18)
  pDireccionIp                 CHAR(16)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMacAdress` | `CHAR(18)` | — | — |
| `pDireccionIp` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cEmpresa` | `CHAR(3)` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(5)` | L11 |
| `cGenerico` | `CHAR(255)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L61 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `monitor` | ENTIDAD | monitor | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_validamontoremesabts_pba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validamontoremesabts_pba.sql` |
| **LOC (1er CREATE)** | 144 |
| **Callgraph** | ✅ fan_in=0 / fan_out=21 |
| **Deps concatenadas** | 99 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida monto, remesa y beneficiarios (PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_validamontoremesabts_pba(
  p_cEmpresa                   CHAR(3)
  p_cNombre1                   CHAR(40)
  p_cNombre2                   CHAR(40)
  p_cApellidoPaterno           CHAR(40)
  p_cApellidoMaterno           CHAR(40)
  p_cFechaNacimiento           CHAR(8)
  p_cFechaHoy                  CHAR(8)
  p_cMontoAPagar               CHAR(20)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_cEmpresa` | `CHAR(3)` | — | — |
| `p_cNombre1` | `CHAR(40)` | — | — |
| `p_cNombre2` | `CHAR(40)` | — | — |
| `p_cApellidoPaterno` | `CHAR(40)` | — | — |
| `p_cApellidoMaterno` | `CHAR(40)` | — | — |
| `p_cFechaNacimiento` | `CHAR(8)` | — | — |
| `p_cFechaHoy` | `CHAR(8)` | — | — |
| `p_cMontoAPagar` | `CHAR(20)` | `monto`=monto | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cMontoMaximo` | `CHAR(10)` | L8 |
| `mImporte_pago` | `MONEY(14,2)` | L9 |
| `mSuma` | `MONEY(14,2)` | L10 |
| `mSumaHist` | `MONEY(14,2)` | L11 |
| `mTotal` | `MONEY(14,2)` | L12 |
| `mMontoMaxMensual` | `CHAR(10)` | L13 |
| `iNumOperaciones` | `INTEGER` | L14 |
| `dPri_dia_mes` | `DATE` | L15 |
| `cPrim_dia_mes` | `CHAR(10)` | L16 |
| `iNumOperHist` | `INTEGER` | L17 |
| `iNumOper` | `INTEGER` | L18 |
| `acumulado` | `INTEGER` | L19 |
| `numOper` | `INTEGER` | L20 |
| `mSumaMensual` | `MONEY(16,2)` | L21 |
| `mTotalMesnual` | `MONEY(16,2)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_param` | `bdicnweb` | no | SELECT | L63 |
| `sac_fechas` | `bdisac` | ⚠️ sí | SELECT | L82 |
| `sac_movimientoshistorial` | `bdisac` | ⚠️ sí | SELECT | L90 |
| `sac_movimientos` | `bdisac` | ⚠️ sí | SELECT | L104 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L116 | FÓRMULA | `LET numOper = iNumOper + iNumOperHist;` |  |
| L117 | FÓRMULA | `LET mTotal = mSuma + CAST(p_cMontoAPagar AS MONEY(14,2));` | 🔴 MONEY/aritmética financiera |
| L118 | FÓRMULA | `LET mTotalMesnual = mSuma + CAST(p_cMontoAPagar AS MONEY(14,2)) + mSumaHist;` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monto` | ENTIDAD | monto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `remesa` | ENTIDAD | remesa (Western Union / MoneyGram) | 🔵 CONVENCIÓN | nombre_sp |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_validanumtelefono`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validanumtelefono.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 40 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida número y teléfono" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_validanumtelefono(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
  pTelefonoCasa                CHAR(10)
  pTelefonoCelular             CHAR(10)
  pTelefonoOficina             CHAR(10)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |
| `pTelefonoCasa` | `CHAR(10)` | `telefono`=teléfono | ✅ CÓDIGO |
| `pTelefonoCelular` | `CHAR(10)` | `telefono`=teléfono | ✅ CÓDIGO |
| `pTelefonoOficina` | `CHAR(10)` | `telefono`=teléfono | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRet` | `CHAR(5)` | L10 |
| `cCodRetSp` | `CHAR(3)` | L11 |
| `cTelCasa` | `CHAR(1)` | L12 |
| `cTelCelular` | `CHAR(1)` | L13 |
| `cTelOficina` | `CHAR(1)` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_validatelefono` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `telefono` | ENTIDAD | teléfono | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validaperfil`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validaperfil.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=45 |
| **Deps concatenadas** | 129 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida perfil de usuario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_validaperfil(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(6)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRetSp` | `CHAR(100)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `sTipo` | `SMALLINT` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |
| `sp_valida_perfil_usuario` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP bdinteg:sp_valida_perfil_usuario';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validapermisousuariocacmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validapermisousuariocacmc.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida usuario y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_validapermisousuariocacmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSupervisor                  CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSupervisor` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `bdicred` | `bdicnweb` | no | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?permiso` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?permiso` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validaprocesosreg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validaprocesosreg.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ✅ fan_in=0 / fan_out=59 |
| **Deps concatenadas** | 156 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida proceso" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_validaprocesosreg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pIdConsulta                  CHAR(1)
  pLatitud                     CHAR(10)
  pLongitud                    CHAR(11)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pIdConsulta` | `CHAR(1)` | — | — |
| `pLatitud` | `CHAR(10)` | — | — |
| `pLongitud` | `CHAR(11)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cEmpresa` | `CHAR(3)` | L6 |
| `cLatitud` | `CHAR(10)` | L7 |
| `cLongitud` | `CHAR(11)` | L8 |
| `iRecuperacion` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ptf` | `bdinteg` | ⚠️ sí | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validaproductotef`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validaproductotef.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida producto y TEF — transferencia electrónica de fondos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_validaproductotef(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pProducto                    CHAR(4)
  pNumCliente                  CHAR(9)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pProducto` | `CHAR(4)` | `producto`=producto | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `cCodRetSp` | `CHAR(6)` | L9 |
| `cCodRetSp2` | `CHAR(5)` | L10 |
| `cDescCodRet` | `CHAR(100)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cEmpresa` | `CHAR(3)` | L14 |
| `dImpComision` | `DECIMAL(6,2)` | L15 |
| `cRFC` | `CHAR(13)` | L16 |
| `cDescripcionIva` | `CHAR(50)` | L17 |
| `cValorIva` | `CHAR(100)` | L18 |
| `iNoRegistros` | `INTEGER` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |
| `sp_validaproductopermitido` | `bditef` | ⚠️ sí | L57 |
| `sp_obtenerparametros` | `bdinteg` | ⚠️ sí | L74 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L61 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bditef:sp_validaproductopermit` |  |
| L63 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L78 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdinteg:sp_obtenerparametros';` |  |
| L80 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L89 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `producto` | ENTIDAD | producto | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tef` | ENTIDAD | TEF — transferencia electrónica de fondos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validarcargarchivoafore`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validarcargarchivoafore.sql` |
| **LOC (1er CREATE)** | 203 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida CHI — formato/protocolo de consulta al Buró de Crédito y AFORE" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_validarcargarchivoafore(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(30)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(30)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cHoraProceso` | `CHAR(21)` | L7 |
| `cHoraServidor` | `CHAR(21)` | L8 |
| `pTipoArchivo` | `CHAR(1)` | L9 |
| `cMensajeRet` | `CHAR(200)` | L10 |
| `iRecuperacion` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L44 |
| `15` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |
| `sp_validahoraejec` | `bdiprog` | ⚠️ sí | L51 |
| `sp_aforevalidacargaarchivo` | `bdiprog` | ⚠️ sí | L72 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | VALIDACIÓN_NULL | `IF cHoraProceso = '' OR cHoraProceso IS NULL THEN` |  |
| L53 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_validahoraejec';` |  |
| L62 | CÓDIGO_RETORNO | `LET cCodRet = '00434';` |  |
| L76 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÃN DEL SP bdiprog:sp_aforevalidacargaarc` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00482';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00483';` |  |
| L87 | CÓDIGO_RETORNO | `LET cCodRet = '00484';` |  |
| L90 | CÓDIGO_RETORNO | `LET cCodRet = '00485';` |  |
| L93 | CÓDIGO_RETORNO | `LET cCodRet = '00486';` |  |
| L96 | CÓDIGO_RETORNO | `LET cCodRet = '00487';` |  |
| L99 | CÓDIGO_RETORNO | `LET cCodRet = '00488';` |  |
| L102 | CÓDIGO_RETORNO | `LET cCodRet = '00489';` |  |
| L105 | CÓDIGO_RETORNO | `LET cCodRet = '00490';` |  |
| L108 | CÓDIGO_RETORNO | `LET cCodRet = '00491';` |  |
| L111 | CÓDIGO_RETORNO | `LET cCodRet = '00492';` |  |
| L114 | CÓDIGO_RETORNO | `LET cCodRet = '00493';` |  |
| L117 | CÓDIGO_RETORNO | `LET cCodRet = '00494';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00438';` |  |
| L123 | CÓDIGO_RETORNO | `LET cCodRet = '00495';` |  |
| L126 | CÓDIGO_RETORNO | `LET cCodRet = '00496';` |  |
| L129 | CÓDIGO_RETORNO | `LET cCodRet = '00497';` |  |
| L132 | CÓDIGO_RETORNO | `LET cCodRet = '00498';` |  |
| L135 | CÓDIGO_RETORNO | `LET cCodRet = '00499';` |  |
| L138 | CÓDIGO_RETORNO | `LET cCodRet = '00500';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00501';` |  |
| L144 | CÓDIGO_RETORNO | `LET cCodRet = '00496';` |  |
| L147 | CÓDIGO_RETORNO | `LET cCodRet = '00502';` |  |
| | *…13 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?vo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `afore` | ENTIDAD | AFORE (Afore Coppel — 2ª mayor de México, ~14.5M cuentas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?r`, `?vo` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validarchivoimportar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validarchivoimportar.sql` |
| **LOC (1er CREATE)** | 147 |
| **Callgraph** | ✅ fan_in=9 / fan_out=3 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida CHI — formato/protocolo de consulta al Buró de Crédito, Impago — pago vencido o fallido; confirmado: n_impagos_consec y Tarjeta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_validarchivoimportar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNombreArchivo               CHAR(22)
  pCodOperacion                CHAR(5)
  pFecha                       DATE
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `chi`=CHI — formato/protocolo de consulta al Buró de Crédito (bdiburo/bdicred:sp_chi_cre_consulta_sic, sp_chi_cre_layout_sics, sp_chi_cre_result_consulta_sic; SICS = Sociedad de Información Crediticia) | 🟡 INFERIDO |
| `pCodOperacion` | `CHAR(5)` | — | — |
| `pFecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `cDesCodRetSp` | `CHAR(80)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cBanco` | `CHAR(3)` | L9 |
| `cArchivo` | `CHAR(22)` | L10 |
| `cPrefijoArch` | `CHAR(20)` | L11 |
| `cArchInvalido` | `CHAR(1)` | L12 |
| `cFechaConsulta` | `CHAR(10)` | L13 |
| `cDiaArch` | `CHAR(2)` | L14 |
| `iCountArch` | `INTEGER` | L15 |
| `cProcesoDatos` | `CHAR(1)` | L17 |
| `cProcesoImg` | `CHAR(1)` | L18 |
| `iRecuperacion` | `INTEGER` | L19 |
| `cCmd1` | `CHAR(1000)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L65 |
| `cce_archivos_ctl` | `bditef` | ⚠️ sí | SELECT | L126 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L52 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNombreArchivo = '' OR pCodOperacion = '' OR pFecha IS NULL T` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L100 | CÓDIGO_RETORNO | `LET cCodRet = '00481';` |  |
| L109 | FÓRMULA | `LET cCmd1 = ""\|\|TRIM(cCmd1)\|\| "SELECT COUNT(*) ";` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `chi` | ENTIDAD | CHI — formato/protocolo de consulta al Buró de Crédito (bdib | 🟡 INFERIDO | nombre_sp |
| `?vo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `?or` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?vo`, `?or` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validareferenciabts`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validareferenciabts.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ✅ fan_in=0 / fan_out=12 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida referencia y beneficiarios" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_validareferenciabts(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumeroBTS                   CHAR(11)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumeroBTS` | `CHAR(11)` | `bts`=beneficiarios (BTS) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `cCodRetSp` | `CHAR(6)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cMensaje` | `CHAR(80)` | L7 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_validabts` | `bdisac` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L39 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCION DEL SP bdisac:sp_validabts';` |  |
| L41 | CÓDIGO_RETORNO | `LET cCodRet = '00240';` |  |
| L43 | CÓDIGO_RETORNO | `LET cCodRet = '00241';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00242';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `referencia` | ENTIDAD | referencia | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bts` | ENTIDAD | beneficiarios (BTS) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_validarfcexistentecli`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validarfcexistentecli.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=26 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida RFC" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_validarfcexistentecli(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pRfc                         CHAR(13)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pRfc` | `CHAR(13)` | `rfc`=RFC (registro fiscal) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cNumCliente` | `CHAR(20)` | L12 |
| `cNombre1` | `CHAR(26)` | L13 |
| `cNombre2` | `CHAR(26)` | L14 |
| `cApPaterno` | `CHAR(26)` | L15 |
| `cApMaterno` | `CHAR(26)` | L16 |
| `cRfc` | `CHAR(13)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L58 | VALIDACIÓN_NULL | `IF cNumCliente IS NULL OR TRIM(cNumCliente) = '' THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `rfc` | ENTIDAD | RFC (registro fiscal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?istentecli` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?istentecli` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validastatusproceso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validastatusproceso.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=19 |
| **Deps concatenadas** | 39 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida estatus y proceso" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_validastatusproceso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pMacAdress                   CHAR(18)
  pDireccionIp                 CHAR(16)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pMacAdress` | `CHAR(18)` | — | — |
| `pDireccionIp` | `CHAR(16)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(5)` | L10 |
| `iNoRegistros` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_monitoreostatusproceso` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L52 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L58 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proceso` | ENTIDAD | proceso | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_validasucursal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_validasucursal.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida sucursal" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_validasucursal(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | `sucursal`=sucursal | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INT` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cDescCodRet` | `CHAR(80)` | L9 |
| `cEmpresa` | `CHAR(3)` | L10 |
| `cNombreSucursal` | `CHAR(45)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L61 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_valida_sucursal` | `bdinteg` | ⚠️ sí | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L50 | EXCEPCIÓN | `RAISE EXCEPTION cCodRetSp::INTEGER, 0, 'ERROR EN LA EJECUCIÓN DEL SP bdinteg:sp_valida_sucursal';` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `sucursal` | ENTIDAD | sucursal | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verifica_estatus_conveniossac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verifica_estatus_conveniossac.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=69 |
| **Deps concatenadas** | 81 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, convenio y ss — subsistema" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verifica_estatus_conveniossac(
  pUsuario                     CHAR(8)
  pIdfuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdfuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusconveniosac` | `bdicnweb` | no | SELECT | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `convenio` | ENTIDAD | convenio (nómina/empresarial) | 🔵 CONVENCIÓN | nombre_sp |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ac` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ss`, `?ac` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificacambioestatusmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificacambioestatusmc.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=4 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica cambio, estatus y mc — Mesa de Control" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificacambioestatusmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_repcambioestatusmc` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_verificaconsultasaldoshistcap`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificaconsultasaldoshistcap.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica saldos y Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (histórico/historial)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificaconsultasaldoshistcap(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(1)` | L10 |
| `cError` | `CHAR(5)` | L11 |
| `cPathFile` | `CHAR(100)` | L12 |
| `cNomFile` | `CHAR(50)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificaconsultasaldoshistcap` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_verificareportespendientesarqueosuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificareportespendientesarqueosuc.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 51 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica reportes y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificareportespendientesarqueosuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNumRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesarqueos` | `bdicnweb` | no | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pendientesarque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?uc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientesarque`, `?uc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificareportespendientesconcilacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificareportespendientesconcilacion.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 42 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica reportes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificareportespendientesconcilacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNumRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_con_genreportesconciliacion` | `bdicnweb` | no | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pendientes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `con` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?cilacion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientes`, `?cilacion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificareportespendientesentradasalida`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificareportespendientesentradasalida.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 50 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica reportes y salida" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificareportespendientesentradasalida(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iNumRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_ctrlgenreportesentradasalida` | `bdicnweb` | no | SELECT | L41 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reportes` | ENTIDAD | reportes | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?pendientesentrada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `salida` | ENTIDAD | salida | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pendientesentrada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatus_consultaportanomcancel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatus_consultaportanomcancel.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=106 |
| **Deps concatenadas** | 323 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, Portabilidad de Nómina — portabilidad de domiciliación de nómina entre bancos y celular" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatus_consultaportanomcancel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_consulportanomcancelaciones` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `portanom` | ENTIDAD | Portabilidad de Nómina — portabilidad de domiciliación de nó | 🔵 CONVENCIÓN | nombre_sp |
| `?can` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cel` | ENTIDAD | celular | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?can` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusarchivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusarchivo.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=9 / fan_out=14 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y archivo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusarchivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipoProceso                 CHAR(12)
  pNombreArchivo               CHAR(22)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipoProceso` | `CHAR(12)` | — | — |
| `pNombreArchivo` | `CHAR(22)` | `archivo`=archivo | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cBanDetError` | `CHAR(1)` | L12 |
| `cMuestraMsn` | `CHAR(1)` | L13 |
| `cErrorProceso` | `CHAR(1)` | L14 |
| `cError` | `CHAR(5)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_statuslecturaarchivos` | `bdicnweb` | no | SELECT | L53 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L38 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusarchivodeclaracionide`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusarchivodeclaracionide.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=11 |
| **Deps concatenadas** | 23 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, archivo, declaración y identificador" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusarchivodeclaracionide(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `cNomArchivo` | `CHAR(25)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusarchivodeclaracionide` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `declaracion` | ENTIDAD | declaración | 🔵 CONVENCIÓN | nombre_sp |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusarqueosucaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusarqueosucaja.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, OS — Originación de Solicitudes y caja" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusarqueosucaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusarqueosucaja` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?arque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?u` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `caja` | ENTIDAD | caja / ventanilla | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?arque`, `?u` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatuscargocta`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatuscargocta.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=20 / fan_out=5 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, cargo y cuenta" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatuscargocta(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalRegistros` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_cr_statuscargocta` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cargo` | ENTIDAD | cargo / débito | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_verificastatusclientescoppel`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusclientescoppel.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 224 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, clientes y Coppel" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusclientescoppel(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusctescoppel` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clientes` | ENTIDAD | clientes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `coppel` | ENTIDAD | Coppel (grupo) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusconscaccentral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconscaccentral.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=113 |
| **Deps concatenadas** | 354 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus (central)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconscaccentral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultacaccentral` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cac` | PREFIJO | familia crédito (CAC) | 🟡 INFERIDO | nombre_sp |
| `central` | MODIF | central | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_verificastatusconspuntoscompromiso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconspuntoscompromiso.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=110 |
| **Deps concatenadas** | 331 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, puntos y compromiso de pago — promesa formal de liquidación" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconspuntoscompromiso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusconsultapuntoscompromiso` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `puntos` | ENTIDAD | puntos (recompensas) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `compromiso` | ENTIDAD | compromiso de pago — promesa formal de liquidación (sp_consu | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_verificastatusconsrepexcepciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsrepexcepciones.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 392 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y Comprobante Electrónico de Pago" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=2 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsrepexcepciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusconsultaincrementosexcepciones` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `cep` | ENTIDAD | Comprobante Electrónico de Pago (SPEI · Banxico) | 🔵 CONVENCIÓN | nombre_sp |
| `?ciones` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?ciones` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusconsrepgralaut`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsrepgralaut.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 390 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus (general)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsrepgralaut(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosgralaut` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `gral` | MODIF | general | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |

---

## `sp_verificastatusconsrepperfilusuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsrepperfilusuario.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 388 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, perfil de usuario y usuario" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsrepperfilusuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosperfilusuario` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusconsrepreenvioanalista`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsrepreenvioanalista.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 384 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y analista" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsrepreenvioanalista(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultasolsupreenvioanalista` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `analista` | ENTIDAD | analista | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?re` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusconsreprevisioncac`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsreprevisioncac.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 387 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y revisión" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsreprevisioncac(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosrevisioncac` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `revision` | ENTIDAD | revisión | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cac` | PREFIJO | familia crédito (CAC) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusconsreprevisioncentral`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsreprevisioncentral.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 386 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y revisión (central)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsreprevisioncentral(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosrevisioncentral` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `revision` | ENTIDAD | revisión | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `central` | MODIF | central | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusconsrepsolsupdetsol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsrepsolsupdetsol.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 385 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=3 · SINTÉTICO=2 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsrepsolsupdetsol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultasolsupdetsol` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `upd` | ACCION | actualiza (update) | 🟡 INFERIDO | nombre_sp |
| `?et` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?et` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusconsrepsolsupstatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsrepsolsupstatus.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 383 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y solicitud" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsrepsolsupstatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultasolsupstatus` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?sup` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sup` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusconsrepstatus`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsrepstatus.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=123 |
| **Deps concatenadas** | 382 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsrepstatus(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `iTotalReg` | `INTEGER` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusconsultaincrementosgralstatus` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusconsrepuntoscompromiso`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsrepuntoscompromiso.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=110 |
| **Deps concatenadas** | 332 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, OS — Originación de Solicitudes y compromiso de pago — promesa formal de liquidación" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsrepuntoscompromiso(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cRutaArchivo` | `CHAR(80)` | L12 |
| `cNombreArchivo` | `CHAR(30)` | L13 |
| `cErrorProceso` | `CHAR(1)` | L14 |
| `cError` | `CHAR(5)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusconsultareportepuntoscompromiso` | `bdicnweb` | no | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?unt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `compromiso` | ENTIDAD | compromiso de pago — promesa formal de liquidación (sp_consu | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?unt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusconsultafechasart61`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsultafechasart61.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=124 |
| **Deps concatenadas** | 396 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y fechas · Art. 61 LIC" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsultafechasart61(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L11 |
| `iSqlErr` | `INTEGER` | L12 |
| `cStatus` | `CHAR(1)` | L13 |
| `cFechaInf` | `CHAR(10)` | L14 |
| `cFechaCon` | `CHAR(10)` | L15 |
| `cFechaDes` | `CHAR(10)` | L16 |
| `cFechaCan` | `CHAR(10)` | L17 |
| `cErrorProceso` | `CHAR(1)` | L18 |
| `cError` | `CHAR(5)` | L19 |
| `iNumRegistros` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_consultafechasart61` | `bdicnweb` | no | SELECT | L60 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `fechas` | ENTIDAD | fechas | 🔵 CONVENCIÓN | nombre_sp |
| `art61` | REG | Art. 61 LIC (cuentas inactivas cuyos saldos, tras años sin m | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_verificastatusconsultasdodiarios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusconsultasdodiarios.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y saldo (diarios)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusconsultasdodiarios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(1)` | L10 |
| `cError` | `CHAR(5)` | L11 |
| `cPathFile` | `CHAR(100)` | L12 |
| `cNomFile` | `CHAR(50)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificaconsultasaldosdiarioscap` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L40 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L35 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `sdo` | ENTIDAD | saldo | 🔵 CONVENCIÓN | nombre_sp |
| `diarios` | MODIF | diarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatuscredbloqfall`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatuscredbloqfall.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 128 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, crédito y fal — fallo/disputa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatuscredbloqfall(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `cStatus` | `CHAR(1)` | L11 |
| `cErrorProceso` | `CHAR(1)` | L12 |
| `cError` | `CHAR(5)` | L13 |
| `cErrorSp` | `CHAR(6)` | L14 |
| `iNumRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepqueb` | `bdicnweb` | no | SELECT | L54 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cred` | ENTIDAD | crédito | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `fal` | ENTIDAD | fal — fallo/disputa (prefijo sp_fal_* — aclaraciones bancari | 🔵 CONVENCIÓN | nombre_sp |
| `?l` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?l` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusdeclide`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusdeclide.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusdeclide(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `cMensaje` | `CHAR(80)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusdeclide` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?decl` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?decl`, `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusdotacioncaja`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusdotacioncaja.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=24 |
| **Deps concatenadas** | 93 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, dotación de efectivo y caja" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusdotacioncaja(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iTotalReg` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusdotacioncaja` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dotacion` | ENTIDAD | dotación de efectivo (a cajero/sucursal) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `caja` | ENTIDAD | caja / ventanilla | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusdotaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusdotaciones.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 208 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y dotaciones de efectivo" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusdotaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_cg_dotaciones` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_verificastatusdotacionesmasivo`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusdotacionesmasivo.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 207 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y dotaciones de efectivo (masivo)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusdotacionesmasivo(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `cStatus` | `CHAR(1)` | L12 |
| `cErrorProceso` | `CHAR(1)` | L13 |
| `cError` | `CHAR(5)` | L14 |
| `iNumRegistrosTotales` | `INTEGER` | L15 |
| `iNumRegistros` | `INTEGER` | L16 |
| `iRegistrosNoProcesados` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_cg_dotaciones_masivo` | `bdicnweb` | no | SELECT | L55 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `dotaciones` | ENTIDAD | dotaciones de efectivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `masivo` | MODIF | masivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_verificastatusentradasalida`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusentradasalida.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=16 |
| **Deps concatenadas** | 49 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y salida" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusentradasalida(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusentradasalida` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?entrada` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `salida` | ENTIDAD | salida | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?entrada` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusexpediente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusexpediente.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y expediente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusexpediente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_consultaexpediente` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `expediente` | ENTIDAD | expediente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusexpedientes`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusexpedientes.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=13 |
| **Deps concatenadas** | 29 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y expediente" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusexpedientes(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusexpediente` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `expediente` | ENTIDAD | expediente | 🔵 CONVENCIÓN | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusfacturacionos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusfacturacionos.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=47 |
| **Deps concatenadas** | 83 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, facturación y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusfacturacionos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_statusproceso_os` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `facturacion` | ENTIDAD | facturación | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_verificastatusformaarchivoide`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusformaarchivoide.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 21 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, archivo y identificador" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusformaarchivoide(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `cNombreArch` | `CHAR(20)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusformarchivo` | `bdicnweb` | no | SELECT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `forma` | ACCION | construye / arma | 🟡 INFERIDO | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusgenerarep`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusgenerarep.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ✅ fan_in=0 / fan_out=44 |
| **Deps concatenadas** | 127 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusgenerarep(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrep` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `?ep` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ep` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusgenrepsistema`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusgenrepsistema.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ✅ fan_in=0 / fan_out=14 |
| **Deps concatenadas** | 36 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y sistema" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusgenrepsistema(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `cNombreArchivo` | `CHAR(150)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepxsist` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `genrep` | ACCION | genera reporte (abreviación genrep) | 🟡 INFERIDO | nombre_sp |
| `sistema` | ENTIDAD | sistema | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_verificastatusinfprospectos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusinfprospectos.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 219 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, información y prospectos" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusinfprospectos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `statusinfprospectos` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inf` | ENTIDAD | información | 🟡 INFERIDO | nombre_sp |
| `prospectos` | ENTIDAD | prospectos (nuevos clientes potenciales) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusliberaret`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusliberaret.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ✅ fan_in=42 / fan_out=6 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusliberaret(
  pUsuario                     CHAR(8)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(1)` | L10 |
| `cError` | `CHAR(5)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusliberaret` | `bdicnweb` | no | SELECT | L37 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L27 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?liberaret` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?liberaret` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusprocesrepantad`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusprocesrepantad.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=25 |
| **Deps concatenadas** | 72 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y proceso (anterior)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusprocesrepantad(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `cRutaIfx` | `CHAR(100)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificastatusrepantad` | `bdicnweb` | no | SELECT | L48 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `proc` | ENTIDAD | proceso | 🟡 INFERIDO | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `ant` | MODIF | anterior | 🔵 CONVENCIÓN | nombre_sp |
| `?ad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es`, `?ad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusregistraevt`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusregistraevt.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusregistraevt(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(1)` | L10 |
| `cError` | `CHAR(5)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_deb_statusregistraevento` | `bdicnweb` | no | SELECT | L46 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `?evt` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?evt` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusrepdetsolicitudmc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusrepdetsolicitudmc.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=23 |
| **Deps concatenadas** | 24 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, detalle, solicitud y mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusrepdetsolicitudmc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_repsolicitudmc` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatusreportedetallessuc`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusreportedetallessuc.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, reporte, detalle y ss — subsistema" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusreportedetallessuc(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cStatus` | `CHAR(1)` | L9 |
| `cErrorProceso` | `CHAR(1)` | L10 |
| `cError` | `CHAR(5)` | L11 |
| `cPathFile` | `CHAR(100)` | L12 |
| `cNomFile` | `CHAR(50)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificageneracionreportedetallessuc` | `bdicnweb` | no | SELECT | L51 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?uc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `ss`, `?uc` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatusreporterelacionbcpcp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatusreporterelacionbcpcp.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 246 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus, reporte y código postal" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatusreporterelacionbcpcp(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_relacionbcpcp` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `reporte` | ENTIDAD | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?relacionb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |
| `cp` | ENTIDAD | código postal | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?relacionb` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatussaldos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatussaldos.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=9 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y saldos" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatussaldos(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `cPathFile` | `CHAR(100)` | L13 |
| `cNomFile` | `CHAR(50)` | L14 |
| `iTotalRegistros` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificaprocesosaldos` | `bdicnweb` | no | SELECT | L54 |
| `sw_compara_sdo_fis_con` | `bdicnweb` | no | SELECT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L44 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L39 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_verificastatussaldos_sucg`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatussaldos_sucg.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ✅ fan_in=0 / fan_out=15 |
| **Deps concatenadas** | 75 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y saldos (sucursal)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatussaldos_sucg(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sw_verificaprocesosaldos_sucg` | `bdicnweb` | no | SELECT | L52 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `saldos` | ENTIDAD | saldos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `suc` | MODIF | sucursal | 🟡 INFERIDO | nombre_sp |
| `?g` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?g` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificastatussolicitudesaumlincred`

| Campo | Valor |
|-------|-------|
| **Dominio** | D01 · `bdicnweb` · Canal Digital Web |
| **Archivo fuente** | `bdicnweb_sp_verificastatussolicitudesaumlincred.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=85 |
| **Deps concatenadas** | 245 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "verifica estatus y solicitudes" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_cnsif_confirmaejecutivo` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_verificastatussolicitudesaumlincred(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cStatus` | `CHAR(1)` | L10 |
| `cErrorProceso` | `CHAR(1)` | L11 |
| `cError` | `CHAR(5)` | L12 |
| `iNumRegistros` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `status_aumlincred` | `bdicnweb` | no | SELECT | L49 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `solicitudes` | ENTIDAD | solicitudes (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `aumlincred` | ACCION | Aumento de Línea de Crédito — proceso de incremento del lími | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---
