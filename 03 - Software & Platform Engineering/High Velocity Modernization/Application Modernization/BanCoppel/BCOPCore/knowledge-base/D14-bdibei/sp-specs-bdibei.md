# SP Specs — D14 · `bdibei` · BEI

> Generado por `build-sp-specs.py` · Grounding Pass v1.0
> Objetivo: verificar el conocimiento del Gemelo Cognitivo contra el código fuente real
> **Convención:** solo se analiza el PRIMER `CREATE PROCEDURE` de cada archivo
> (los siguientes son dependencias concatenadas — ver memoria del proyecto)

## Resumen de validación del dominio

| Métrica | Valor |
|---------|-------|
| SPs analizados | **336** |
| Presentes en callgraph | 42 |
| SPs aislados (⚠️ no estaban en el análisis previo) | 294 |
| Propósito **VERIFICADO** | 248 |
| Propósito **PARCIAL** | 86 |
| Propósito **NO_VERIFICABLE** | 2 |
| SPs con tokens **SINTÉTICOS** detectados | 240 |

> Los **294 SPs aislados** no aparecen en el callgraph y por tanto
> **no fueron incluidos en el análisis de journeys, reglas ni vocabulario previo**.
> Este grounding pass los analiza por primera vez directamente desde el código.

---

## `desbloque`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_desbloque.sql` |
| **LOC (1er CREATE)** | 9 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "desbloqueo" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=0 · INFERIDO=1 · SINTÉTICO=1 / 2 términos |

### Firma

```sql
CREATE PROCEDURE desbloque(
) RETURNING integer
```

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `desb` | ACCION | desbloqueo | 🟡 INFERIDO | nombre_sp |
| `?loque` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?loque` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `getrandomcode`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_getrandomcode.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "mc — Mesa de Control" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 3 términos |

### Firma

```sql
CREATE PROCEDURE getrandomcode(
) RETURNING INT, CHAR(8)
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `m` | `INT8` | L4 |
| `a` | `INT8` | L5 |
| `time` | `INT8` | L6 |
| `x` | `INT8` | L7 |
| `_x` | `DECIMAL(24)` | L8 |
| `c` | `INT8` | L9 |
| `k` | `INT8` | L10 |
| `_rnd` | `CHAR(8)` | L11 |
| `i` | `INTEGER` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cCadRnds` | `CHAR(64)` | L14 |
| `cAscii` | `CHAR(1)` | L15 |
| `iRows` | `INTEGER` | L16 |
| `y` | `INT8` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `systables` | `bdibei` | no | SELECT | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | FÓRMULA | `LET x = (a * x) + c;` |  |
| L48 | FÓRMULA | `LET _x = x / m;` |  |
| L50 | FÓRMULA | `LET y = 65 + (_x * (116 - 65));` |  |
| L62 | FÓRMULA | `LET iRows = iRows + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `?getrando` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mc` | ENTIDAD | mc — Mesa de Control (abreviación — sp_cons_empleado_mc) | 🔵 CONVENCIÓN | nombre_sp |
| `?ode` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?getrando`, `?ode` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_act_statususuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_act_statususuario_bei.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza Status de usuario por Nombre de Usuario |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_act_statususuario_bei(
  pUsuario                     char(50)
  pStatus                      integer
  pIp                          char (15)
  pSuc                         char (4)
  pUsuCambio                   char (8)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(50)` | `usuario`=usuario | ✅ CÓDIGO |
| `pStatus` | `integer` | `status`=estatus | ✅ CÓDIGO |
| `pIp` | `char (15)` | — | — |
| `pSuc` | `char (4)` | — | — |
| `pUsuCambio` | `char (8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L18 |
| `sql_err` | `integer` | L19 |
| `iIdUsuario` | `integer` | L20 |
| `iStatus` | `integer` | L21 |
| `iEmpresa` | `char(3)` | L22 |
| `iNumCliente` | `char(20)` | L23 |
| `iIdentAdmin` | `char(30)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L55 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L62 |
| `bei_servicio` | `bdibei` | no | SELECT | L71 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_activaciontkn_porrenovacion_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_activaciontkn_porrenovacion_bei.sql` |
| **LOC (1er CREATE)** | 139 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "activa BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_set_statustoken_admtoken` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que activacion de tokens por renovacion |
| FECHA | 30/08/2018 |

### Firma

```sql
CREATE PROCEDURE sp_activaciontkn_porrenovacion_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pNsToken                     CHAR(10)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pNsToken` | `CHAR(10)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L13 |
| `cod_ret` | `CHAR(5)` | L14 |
| `vExisteTkn` | `INTEGER` | L15 |
| `vStatusTknAnt` | `INTEGER` | L16 |
| `vStatus_manco` | `SMALLINT` | L17 |
| `vid_tipo_usuario` | `SMALLINT` | L18 |
| `cCodRet_Token` | `CHAR(5)` | L19 |
| `pns_token` | `CHAR(9)` | L20 |
| `psolicitud` | `CHAR(10)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L60 |
| `informix` | `bdibei` | no | INSERT | L82 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L97 |
| `bei_tokensolicitud` | `bdibei` | no | UPDATE | L98 |
| `bei_tokenexpira` | `bdibei` | no | SELECT | L105 |
| `bei_servicio` | `bdibei` | no | SELECT | L111 |
| `bei_servicio` | `bdibei` | no | UPDATE | L115 |
| `bei_tokenexpira` | `bdibei` | no | UPDATE | L128 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_set_statustoken_admtoken` | `bdibpi` | ⚠️ sí | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L26 | FÓRMULA | `LET vStatus_manco = -1;` |  |
| L119 | FÓRMULA | `LET vStatus_manco = -1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `activa` | ACCION | activa | 🔵 CONVENCIÓN | nombre_sp |
| `?ciontkn_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?renovacion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ciontkn_`, `?renovacion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_activausuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_activausuario_bei.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "activa usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Activa y Registra el usuario y registra el cambio de status |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_activausuario_bei(
  pNumCte                      char(20)
  pPass                        char(50)
  pStatus                      integer
  pIp                          char (15)
  pSuc                         char (4)
  pUsuCambio                   char (8)
  pIdentAdmin                  char(30)
  pIdUsuario                   INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `char(20)` | — | — |
| `pPass` | `char(50)` | — | — |
| `pStatus` | `integer` | — | — |
| `pIp` | `char (15)` | — | — |
| `pSuc` | `char (4)` | — | — |
| `pUsuCambio` | `char (8)` | — | — |
| `pIdentAdmin` | `char(30)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L20 |
| `cCod_ret` | `char(5)` | L21 |
| `iCont` | `smallint` | L22 |
| `iStatus` | `INTEGER` | L23 |
| `sIdUsuario` | `INTEGER` | L24 |
| `sIdentAdmin` | `char(30)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L50 |
| `bei_usuario` | `bdibei` | no | SELECT | L75 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L79 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `activa` | ACCION | activa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_actualiza_archivo_eval_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_archivo_eval_bei.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza archivo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza la informaci del archivo que se evalua |
| FECHA | 08/MARZO/2016 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_archivo_eval_bei(
  p_idEvaluacion               INTEGER
  p_estatus                    INTEGER
  p_cta_origen                 VARCHAR(20)
  p_numctasb                   INTEGER
  p_numctaso                   INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_idEvaluacion` | `INTEGER` | — | — |
| `p_estatus` | `INTEGER` | — | — |
| `p_cta_origen` | `VARCHAR(20)` | — | — |
| `p_numctasb` | `INTEGER` | — | — |
| `p_numctaso` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L14 |
| `sql_err` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos_eval` | `bdibei` | no | UPDATE | L43 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_eval_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_eval_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualiza_aut_manc_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_aut_manc_bei.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza Autorizacion de Mancomunidad por Cuenta |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_aut_manc_bei(
  pIdUser                      INTEGER
  pNumCte                      CHAR(9)
  pNumCta                      CHAR(16)
  pAutoriza                    CHAR(1)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUser` | `INTEGER` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumCta` | `CHAR(16)` | — | — |
| `pAutoriza` | `CHAR(1)` | `aut`=autorización | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad` | `bdibei` | no | SELECT | L53 |
| `bei_mancomunidad` | `bdibei` | no | INSERT | L66 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualiza_avatar_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_avatar_bei.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza avatar y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza la información del avatar |
| FECHA | 22/09/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_avatar_bei(
  pIdUsuario                   INTEGER
  pNumCliente                  CHAR(9)
  pIdAvatar                    SMALLINT
  pNumIntento                  SMALLINT
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdAvatar` | `SMALLINT` | `avatar`=avatar (foto de perfil del usuario en app) | ✅ CÓDIGO |
| `pNumIntento` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `avatar` | ENTIDAD | avatar (foto de perfil del usuario en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_actualiza_datoscontacto_empnet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_datoscontacto_empnet.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza datos y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_datoscontacto_empnet(
  pIdCliente                   CHAR(9)
  pIdUsuario                   INTEGER
  pRepresentanteLegal          CHAR(100)
  pTelFijo                     CHAR(15)
  pTelCel                      CHAR(15)
  pEmail                       CHAR(35)
  pEmailAlternativo            CHAR(35)
  pPagInternet                 CHAR(35)
) RETURNING CHAR (5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pRepresentanteLegal` | `CHAR(100)` | — | — |
| `pTelFijo` | `CHAR(15)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pEmail` | `CHAR(35)` | — | — |
| `pEmailAlternativo` | `CHAR(35)` | — | — |
| `pPagInternet` | `CHAR(35)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L13 |
| `vCod_ret` | `CHAR (5)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_empnet` | `bdibei` | no | SELECT | L36 |
| `bei_datos_empnet` | `bdibei` | no | UPDATE | L37 |
| `bei_datos_empnet` | `bdibei` | no | INSERT | L45 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?o_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o_`, `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualiza_estatus_solrenovtkn`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_estatus_solrenovtkn.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus y solicitud" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| AUTOR | Solser |
| DESCRIPCION | Actualiza el campo id_status_solicitud por numero de cliente |
| FECHA | 29/08/2018 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_estatus_solrenovtkn(
  pNumCte                      CHAR(9)
  pStatus                      CHAR(1)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(9)` | — | — |
| `pStatus` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L10 |
| `sql_err` | `INTEGER` | L11 |
| `vid_usuario` | `INTEGER` | L12 |
| `vid_tipo_usuario` | `SMALLINT` | L13 |
| `vns_token` | `CHAR(10)` | L14 |
| `vid_status_solicitud` | `CHAR(1)` | L15 |
| `vExisteTkn` | `CHAR(10)` | L16 |
| `vCantidad` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokenexpira` | `bdibei` | no | SELECT | L53 |
| `bei_token` | `bdibei` | no | SELECT | L57 |
| `bei_tokenexpira` | `bdibei` | no | UPDATE | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET vCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estatus` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `sol` | ENTIDAD | solicitud | 🟡 INFERIDO | nombre_sp |
| `?renovtkn` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?renovtkn` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualiza_foliopagoprog_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_foliopagoprog_bei.sql` |
| **LOC (1er CREATE)** | 42 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza folio, pago, ro — Rol Operativo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Modifica Folio de Pago Programado |
| FECHA | 26/09/2014 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_foliopagoprog_bei(
  pIdOperacion                 INTEGER
  folioPagoProg                CHAR(20)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | — | — |
| `folioPagoProg` | `CHAR(20)` | `folio`=folio · `pago`=pago | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L11 |
| `sql_err` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperadorresumen` | `bdibei` | no | UPDATE | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L27 | FÓRMULA | `LET cod_ret = "001"; --Folio esta vacio` |  |
| L32 | FÓRMULA | `LET cod_ret = "002"; --Id de Operacion incorrecto` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `folio` | ENTIDAD | folio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualiza_monto_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_monto_manco_bei.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza monto, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 5 tabla(s) con operaciones: INSERT, DELETE, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza Status Mancomunidad |
| FECHA | 08/01/2015 |
| MODIFICACION | Se agrego el valor de retorno integer que es el id_adminMancoTemp (06/06/2018) |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_monto_manco_bei(
  pIdAdminManco                INTEGER
  pNumCliente                  CHAR(12)
  pIdUsuario                   INTEGER
  pIdUsuarioAut                INTEGER
  pStatusAut                   INTEGER
  pFechaAut                    CHAR (20)
) RETURNING CHAR(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdAdminManco` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(12)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdUsuarioAut` | `INTEGER` | — | — |
| `pStatusAut` | `INTEGER` | — | — |
| `pFechaAut` | `CHAR (20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L21 |
| `cCod_ret` | `CHAR (5)` | L22 |
| `tipo_opera` | `INTEGER` | L24 |
| `tipo_movi` | `INTEGER` | L25 |
| `id_adminMancoTemp` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L74 |
| `bei_admin_manco_temp_hist` | `bdibei` | no | INSERT | L92 |
| `bei_admin_manco_montos_temp` | `bdibei` | no | SELECT | L107 |
| `bei_admin_manco_montos_temp` | `bdibei` | no | DELETE | L107 |
| `bei_admin_manco_temp` | `bdibei` | no | DELETE | L113 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_actualiza_status_manco_acttknadmin`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_status_manco_acttknadmin.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y Administrador — rol de usuario con privilegios administrativos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que activacion de tokens por renovacion |
| FECHA | 30/08/2018 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_status_manco_acttknadmin(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L13 |
| `cod_ret` | `CHAR(5)` | L14 |
| `vid_tipo_usuario` | `SMALLINT` | L15 |
| `vStatus_manco` | `SMALLINT` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L50 |
| `bei_servicio` | `bdibei` | no | SELECT | L56 |
| `bei_servicio` | `bdibei` | no | UPDATE | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | FÓRMULA | `LET vStatus_manco = -1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?tkn` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tkn` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualiza_status_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_status_manco_bei.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza Status Mancomunidad |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_status_manco_bei(
  pNumCliente                  CHAR(9)
  pIdentAdmin                  CHAR(30)
) RETURNING CHAR (5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdentAdmin` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L10 |
| `cCod_ret` | `CHAR (5)` | L11 |
| `sStatusManco` | `SMALLINT` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L49 |
| `bei_servicio` | `bdibei` | no | UPDATE | L57 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_actualiza_status_token_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_status_token_bei.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus, token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Actualiza el status y fecha de status del token asignado al cliente |
| ACTIVIDAD | Se agrego la ejecucion del sp sp_set_statustoken_admtoken |
| MODIFICACION | para detectar cuando la SOLICITUD este en 120 y se quiere ACTIVAR el token, cambia a 130 la solicitud. |
| FECHA | Septiembre 2014 |
| AUTOR | Berenice Noriega |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_status_token_bei(
  pIdUsuario                   INTEGER
  pNumCte                      char(9)
  pStatus                      char(3)
  pNSToken                     char(10)
) RETURNING char (5), integer
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCte` | `char(9)` | — | — |
| `pStatus` | `char(3)` | `status`=estatus | ✅ CÓDIGO |
| `pNSToken` | `char(10)` | `token`=token (autenticación) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L17 |
| `cCod_ret` | `char (5)` | L18 |
| `statusAntToken` | `char(3)` | L19 |
| `vsolicitud` | `char(10)` | L20 |
| `vid_status` | `smallint` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L42 |
| `bei_usuario` | `bdibei` | no | SELECT | L45 |
| `bei_token` | `bdibei` | no | UPDATE | L56 |
| `bei_servicio` | `bdibei` | no | UPDATE | L59 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L67 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L72 |
| `bei_tokensolicitud` | `bdibei` | no | UPDATE | L73 |
| `bei_envios` | `bdibei` | no | UPDATE | L74 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L76 |
| `tkn_status_token` | `bdibpi` | ⚠️ sí | INSERT | L77 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_set_statustoken_admtoken` | `bdibpi` | ⚠️ sí | L47 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_actualiza_statusoperacion_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_statusoperacion_bei.sql` |
| **LOC (1er CREATE)** | 50 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Modifica Status Operacion |
| FECHA | 27/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_statusoperacion_bei(
  pIdOperacion                 INTEGER
  status_oper                  CHAR(1)
  pIdusuarioCambiaStatus       INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | `operacion`=operación | ✅ CÓDIGO |
| `status_oper` | `CHAR(1)` | `status`=estatus | ✅ CÓDIGO |
| `pIdusuarioCambiaStatus` | `INTEGER` | `status`=estatus | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L11 |
| `sql_err` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | UPDATE | L42 |
| `bei_operacionesmancomunadasoperadorresumen` | `bdibei` | no | UPDATE | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | FÓRMULA | `LET cod_ret = "001"; --STATUS DE LA OPERACION VACIO` |  |
| L33 | FÓRMULA | `LET cod_ret = "002"; --ID DE LA OPERACION INCORRECTO` |  |
| L38 | FÓRMULA | `LET cod_ret = "003"; --ID DEL USUARIO QUE CAMBIA EL STATUS INVALIDO` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `operacion` | ACCION | operación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_actualiza_usuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_usuario_bei.sql` |
| **LOC (1er CREATE)** | 209 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza usuario Bei |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza para que al recibir el dato del PASS tambien borre el registro de Avatar |
| MODIFICACION | Berenice Noriega Guevara |
| FECHA | 09-Enero-2015 |
| FECHA | 22-ENERO-2015 |
| MODIFICACION | Se actualiza para que al recibir el dato del PASS tambien borre el registro de Avatar |
| MODIFICACION | Berenice Noriega Guevara |
| FECHA | 29-Enero-2015 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_usuario_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pPass                        CHAR(250)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            Integer
  pUsuario                     CHAR(250)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pPass` | `CHAR(250)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `Integer` | `usuario`=usuario | ✅ CÓDIGO |
| `pUsuario` | `CHAR(250)` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L11 |
| `cod_ret2` | `CHAR(5)` | L12 |
| `sql_err` | `INTEGER` | L13 |
| `vid_tipo_usuario` | `SMALLINT` | L14 |
| `vid_status` | `SMALLINT` | L15 |
| `sPass` | `CHAR(250)` | L17 |
| `sFpass` | `DATE` | L18 |
| `sPass1` | `CHAR(250)` | L19 |
| `sFpass1` | `DATE` | L20 |
| `sPass2` | `CHAR(250)` | L21 |
| `sFpass2` | `DATE` | L22 |
| `sPass3` | `CHAR(250)` | L23 |
| `sFpass3` | `DATE` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L101 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L147 |
| `bei_avatar` | `bdibei` | no | SELECT | L158 |
| `bei_datos_usuario` | `bdibei` | no | UPDATE | L179 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_reset_avatar_bei` | `bdibei` | no | L159 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_actualiza_usuario_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualiza_usuario_bei_historico.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza usuario Bei |
| FECHA | 05/06/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_actualiza_usuario_bei_historico(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pPass                        CHAR(250)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            INTEGER
  pUsuario                     CHAR(250)
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pPass` | `CHAR(250)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pUsuario` | `CHAR(250)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L14 |
| `sql_err` | `INTEGER` | L15 |
| `sIdPerfil` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario_historico` | `bdibei` | no | INSERT | L43 |
| `bei_usuario` | `bdibei` | no | SELECT | L55 |
| `informix` | `bdibei` | no | INSERT | L58 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L66 |
| `bei_usuario_perfil` | `bdibei` | no | SELECT | L71 |
| `bei_perfil_historico` | `bdibei` | no | INSERT | L74 |
| `bei_perfil` | `bdibei` | no | SELECT | L79 |
| `bei_usuario_perfil_historico` | `bdibei` | no | INSERT | L82 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_actualizabitacora_for_pagogdf`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualizabitacora_for_pagogdf.sql` |
| **LOC (1er CREATE)** | 35 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza bitácora, pago y gdf — código geográfico" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| AUTOR | Solser |
| ACTIVIDAD | Reimpresion de comprobante de pago de GDF |
| FECHA | 25/04/2018 |

### Firma

```sql
CREATE PROCEDURE sp_actualizabitacora_for_pagogdf(
  pLineaCaptura                char(40)
  pCertificado                 char(40)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pLineaCaptura` | `char(40)` | — | — |
| `pCertificado` | `char(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L8 |
| `sql_err` | `integer` | L9 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | VALIDACIÓN_NULL | `IF pLineaCaptura IS NULL OR pCertificado IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | 🔵 CONVENCIÓN | nombre_sp |
| `?_for_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gdf` | ENTIDAD | gdf — código geográfico / Gobierno CDMX (abreviación — bdisa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_for_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizadatoscontacto_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualizadatoscontacto_bei.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza datos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizadatoscontacto_bei(
  pIdUsuario                   VARCHAR(9)
  pCel                         VARCHAR(15)
  pCiaCel                      INT
  pEmail                       VARCHAR(80)
) RETURNING CHAR (5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `VARCHAR(9)` | — | — |
| `pCel` | `VARCHAR(15)` | — | — |
| `pCiaCel` | `INT` | — | — |
| `pEmail` | `VARCHAR(80)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L7 |
| `cCod_ret` | `CHAR (5)` | L8 |
| `v_Empresa` | `CHAR(3)` | L10 |
| `v_TipoTel` | `SMALLINT` | L11 |
| `v_Extension` | `CHAR(5)` | L12 |
| `v_Canal` | `SMALLINT` | L13 |
| `v_UserInsert` | `CHAR(8)` | L14 |
| `v_codret1` | `CHAR(5)` | L15 |
| `v_codret2` | `CHAR(5)` | L16 |
| `v_TipoCorreo` | `SMALLINT` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | UPDATE | L41 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?o_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizadatoscontacto_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualizadatoscontacto_bei_historico.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza datos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 05/06/2018 |
| FECHA | 07-AGOSTO-2018 |
| MODIFICACION | Se actualiza el campo fecha_mov_hist por el correcto nombre del campo f_mov_historico |
| MODIFICACION | Berenice Noriega |
| FECHA | 22 Octubre 2018 |
| FECHA | 25-Octubre-2018 |

### Firma

```sql
CREATE PROCEDURE sp_actualizadatoscontacto_bei_historico(
  pIdUsuario                   VARCHAR(9)
  pCel                         VARCHAR(15)
  pCiaCel                      INT
  pEmail                       VARCHAR(80)
  pidBitacoraAdmin             INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `VARCHAR(9)` | — | — |
| `pCel` | `VARCHAR(15)` | — | — |
| `pCiaCel` | `INT` | — | — |
| `pEmail` | `VARCHAR(80)` | — | — |
| `pidBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INT` | L8 |
| `cCod_ret` | `CHAR(5)` | L9 |
| `vid_usuario` | `INTEGER` | L12 |
| `vnombre` | `CHAR(150)` | L13 |
| `vtel_celular` | `CHAR(15)` | L14 |
| `vcia_cel` | `SMALLINT` | L15 |
| `ve_mail` | `CHAR(100)` | L16 |
| `vactivo` | `BOOLEAN` | L17 |
| `vid_ultima_oper` | `SMALLINT` | L18 |
| `vfecha_bloqueo` | `DATETIME YEAR to SECOND` | L19 |
| `vfecha_bloqueo_camb_pass` | `DATETIME YEAR to SECOND` | L20 |
| `vfecha_bloqueo_camb_pregs` | `DATETIME YEAR to SECOND` | L21 |
| `vtipo_bloqueo_temp_pass` | `SMALLINT` | L22 |
| `vtipo_bloqueo_temp_resp` | `SMALLINT` | L23 |
| `vcomienza` | `INTEGER` | L25 |
| `vcuantos` | `INTEGER` | L26 |
| `vregistros` | `INTEGER` | L27 |
| `vcontador` | `INTEGER` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | SELECT | L74 |
| `bei_datos_usuario_historico` | `bdibei` | no | INSERT | L83 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | FÓRMULA | `LET vcontador = -1;` |  |
| L37 | FÓRMULA | `LET vcomienza   = -1;` |  |
| L92 | FÓRMULA | `LET vcomienza = -1;` |  |
| L94 | FÓRMULA | `LET vcontador = vcontador + 1 ;` |  |
| L102 | FÓRMULA | `LET vcomienza = -1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?o_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizaestatuspagoprog_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualizaestatuspagoprog_bei.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus, pago, ro — Rol Operativo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizaestatuspagoprog_bei(
  pIdUsuario                   INTEGER
  pNumCte                      char(9)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCte` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `idOperacion` | `INT` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_pagoprog` | `bdiprog` | ⚠️ sí | SELECT | L36 |
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L40 |
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | UPDATE | L51 |
| `bei_operacionesmancomunadasoperadorresumen` | `bdibei` | no | UPDATE | L55 |
| `sc_nominaencabezadosumariohist` | `bdicheq` | ⚠️ sí | SELECT | L66 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | FÓRMULA | `LET cod_ret = "001"; --Numero de cliente incorrecto` |  |
| L29 | FÓRMULA | `LET cod_ret = "002"; --Id operador es incorrecto` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `estatus` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `pago` | ENTIDAD | pago | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizafolio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualizafolio.sql` |
| **LOC (1er CREATE)** | 45 |
| **Callgraph** | ✅ fan_in=3 / fan_out=10 |
| **Deps concatenadas** | 20 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza folio" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_actualizafolio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pValor                       CHAR(3)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pValor` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bpi_parametros_contenido` | `bdibpi` | ⚠️ sí | UPDATE | L33 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L27 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L32 | FÓRMULA | `LET pValor = pValor + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `folio` | ENTIDAD | folio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_actualizapass_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualizapass_bei.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| MODIFICACION | SOLSER |
| ACTIVIDAD | activa el usuario y registra el cambio de status |
| FECHA | 09-07-2013 |

### Firma

```sql
CREATE PROCEDURE sp_actualizapass_bei(
  pEmpresa                     char(3)
  pNumCte                      char(20)
  pPass                        char(50)
  pIp                          char (15)
  pSucVirtual                  char (4)
  pUsuVirtual                  char(8)
  pIdUsuario                   integer
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char(3)` | — | — |
| `pNumCte` | `char(20)` | — | — |
| `pPass` | `char(50)` | `ss`=ss — subsistema / canal de monitoreo (abreviación — envia_monitorsol_*_ss_* — bdisolic) | 🔴 SINTÉTICO |
| `pIp` | `char (15)` | — | — |
| `pSucVirtual` | `char (4)` | — | — |
| `pUsuVirtual` | `char(8)` | — | — |
| `pIdUsuario` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L16 |
| `sql_err` | `integer` | L17 |
| `iStatus` | `smallint` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L32 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | VALIDACIÓN_NULL | `IF (iStatus IS NULL) THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?pa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pa`, `ss` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_actualizastatususuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualizastatususuario_bei.sql` |
| **LOC (1er CREATE)** | 179 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza Status de usuario Admin y Operador |
| FECHA | 24/05/2013 |
| MODIFICACION | Para en caso de ser administrador y no tener id de usuario, ya no sea necesario |
| FECHA | 21 Agosto 2014 |

### Firma

```sql
CREATE PROCEDURE sp_actualizastatususuario_bei(
  pEmpresa                     char(3)
  pNumCliente                  char(20)
  pIdUsuario                   INTEGER
  pStatus                      integer
  pIp                          char (15)
  pSuc                         char (4)
  pUsuCambio                   char (8)
  pIdentAdmin                  CHAR(30)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char(3)` | — | — |
| `pNumCliente` | `char(20)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pStatus` | `integer` | `status`=estatus | ✅ CÓDIGO |
| `pIp` | `char (15)` | — | — |
| `pSuc` | `char (4)` | — | — |
| `pUsuCambio` | `char (8)` | — | — |
| `pIdentAdmin` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L26 |
| `sql_err` | `integer` | L27 |
| `iStatus` | `integer` | L28 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L63 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L75 |
| `bei_usuario` | `bdibei` | no | SELECT | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L73 | VALIDACIÓN_NULL | `IF ((NVL(pIdUsuario,'')='') OR pIdUsuario is null) THEN --si esta vacio o es null` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_actualizastusermanco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_actualizastusermanco_bei.sql` |
| **LOC (1er CREATE)** | 146 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_actualizastatususuario_bei`, `sp_actualiza_status_token_bei` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Ejecuta Proceso de Mancomuniadd Bloqueo Desbloqueo de Usuarios |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_actualizastusermanco_bei(
  pIdAdminManco                INTEGER
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pAutoriza                    SMALLINT
  pIp                          CHAR(20)
  pSucursal                    CHAR(20)
  pUser                        CHAR(20)
) RETURNING char(5) ,INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdAdminManco` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pAutoriza` | `SMALLINT` | — | — |
| `pIp` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(20)` | — | — |
| `pUser` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L7 |
| `sql_err` | `INTEGER` | L8 |
| `stipoOper` | `SMALLINT` | L10 |
| `sTipoMov` | `SMALLINT` | L11 |
| `sIdUserAdmin` | `INTEGER` | L13 |
| `sIdUsuario` | `INTEGER` | L15 |
| `sNumCliente` | `CHAR(9)` | L16 |
| `sIdStatus` | `SMALLINT` | L18 |
| `sEmp` | `CHAR(3)` | L19 |
| `sIdStatusToken` | `SMALLINT` | L21 |
| `sNsToken` | `CHAR(20)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L77 |
| `bei_token` | `bdibei` | no | SELECT | L113 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_actualizastatususuario_bei` | `bdibei` | no | L107 |
| `sp_actualiza_status_token_bei` | `bdibei` | no | L119 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?stuser` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?stuser` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_administradorespm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_administradorespm.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Administrador — rol de usuario con privilegios administrativos (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_generafoliosolicitudtoken` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 07/11/2013 |

### Firma

```sql
CREATE PROCEDURE sp_administradorespm(
  pNumCteEmp                   CHAR(9)
  pNumCteAdmin                 CHAR(9)
  pAdminTipo                   CHAR(3)
  pIdAdmin                     CHAR(30)
  pApellidoPater               CHAR(30)
  pApellidoMater               CHAR(30)
  pNombre1                     CHAR(30)
  pNombre2                     CHAR(30)
  pRepLegal                    CHAR(1)
) RETURNING CHAR(6) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCteEmp` | `CHAR(9)` | — | — |
| `pNumCteAdmin` | `CHAR(9)` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | 🔵 CONVENCIÓN |
| `pAdminTipo` | `CHAR(3)` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | 🔵 CONVENCIÓN |
| `pIdAdmin` | `CHAR(30)` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | 🔵 CONVENCIÓN |
| `pApellidoPater` | `CHAR(30)` | — | — |
| `pApellidoMater` | `CHAR(30)` | — | — |
| `pNombre1` | `CHAR(30)` | — | — |
| `pNombre2` | `CHAR(30)` | — | — |
| `pRepLegal` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INTEGER` | L21 |
| `cCodRet` | `CHAR(6)` | L22 |
| `cMensaje` | `CHAR(50)` | L23 |
| `cFolioSolToken` | `CHAR(12)` | L24 |
| `cMancomunado` | `SMALLINT` | L26 |
| `vOper_no_token` | `smallint` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L62 |
| `bei_servicio` | `bdibei` | no | INSERT | L77 |
| `bei_servicio` | `bdibei` | no | SELECT | L88 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_generafoliosolicitudtoken` | `bdinteg` | ⚠️ sí | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?istrador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?istrador`, `?m` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_agregar_bitacora_geolocalizacion_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_agregar_bitacora_geolocalizacion_bei.sql` |
| **LOC (1er CREATE)** | 203 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "registro bitácora y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (local)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_agregarbitacora_bei`, `sp_agregarbitacora_admin_bei` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 9 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | SP para ejecutar el registro de bitacoras y como adicional por normativa, los datos de geolocalizacion para |
| FECHA | 29/03/2021 |

### Firma

```sql
CREATE PROCEDURE sp_agregar_bitacora_geolocalizacion_bei(
  pFechaOper                   DATETIME YEAR TO SECOND
  pIdOper                      CHAR(4)
  pNumSuc                      CHAR(4)
  pIdUsuario                   INTEGER
  pIpUsuario                   CHAR(15)
  pFechaApli                   DATE
  pCtaOrigen                   CHAR(12)
  pCtaDesti                    CHAR(12)
  pMonto                       MONEY
  pSecTrans                    CHAR(16)
  pCgen1                       CHAR(40)
  pCgen2                       CHAR(40)
  pCgen3                       CHAR(40)
  pCgen4                       CHAR(40)
  pCgen5                       CHAR(40)
  pCgen6                       CHAR(40)
  pCgen7                       CHAR(40)
  pCgen8                       CHAR(40)
  pCgen9                       CHAR(40)
  pLatitud                     VARCHAR(10)
  pLongitud                    VARCHAR(11)
  pPlataforma                  CHAR(40)
  pFlgModo                     INTEGER
) RETURNING CHAR(5) AS codigo, CHAR(100) AS mensaje
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaOper` | `DATETIME YEAR TO SECOND` | — | — |
| `pIdOper` | `CHAR(4)` | — | — |
| `pNumSuc` | `CHAR(4)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIpUsuario` | `CHAR(15)` | — | — |
| `pFechaApli` | `DATE` | — | — |
| `pCtaOrigen` | `CHAR(12)` | — | — |
| `pCtaDesti` | `CHAR(12)` | — | — |
| `pMonto` | `MONEY` | — | — |
| `pSecTrans` | `CHAR(16)` | — | — |
| `pCgen1` | `CHAR(40)` | — | — |
| `pCgen2` | `CHAR(40)` | — | — |
| `pCgen3` | `CHAR(40)` | — | — |
| `pCgen4` | `CHAR(40)` | — | — |
| `pCgen5` | `CHAR(40)` | — | — |
| `pCgen6` | `CHAR(40)` | — | — |
| `pCgen7` | `CHAR(40)` | — | — |
| `pCgen8` | `CHAR(40)` | — | — |
| `pCgen9` | `CHAR(40)` | — | — |
| `pLatitud` | `VARCHAR(10)` | — | — |
| `pLongitud` | `VARCHAR(11)` | — | — |
| `pPlataforma` | `CHAR(40)` | — | — |
| `pFlgModo` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L44 |
| `iSqlErr` | `INTEGER` | L45 |
| `cNumCliente` | `CHAR(50)` | L46 |
| `cCodSPGuardaBitacora` | `CHAR(5)` | L47 |
| `sIdBitacoraAdmin` | `INTEGER` | L48 |
| `cReferencia` | `CHAR(40)` | L49 |
| `cMensaje` | `CHAR(100)` | L50 |
| `cParamSP` | `CHAR(10)` | L51 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L80 |
| `bei_bitacora_geolocalizacion` | `bdibei` | no | INSERT | L167 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_agregarbitacora_bei` | `bdibei` | no | L102 |
| `sp_agregarbitacora_admin_bei` | `bdibei` | no | L144 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet               = '00000';` |  |
| L66 | CÓDIGO_RETORNO | `LET cCodRet = '00002';` |  |
| L83 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?ar_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?_geo` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `local` | MODIF | local | 🔵 CONVENCIÓN | nombre_sp |
| `?izacion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ag`, `?ar_`, `?_geo`, `?izacion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_agregarbitacora_admin_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_agregarbitacora_admin_bei.sql` |
| **LOC (1er CREATE)** | 117 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "registro bitácora, Administrador — rol de usuario con privilegios administrativos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | SP para la insercion en bei_bitacora_admin |
| FECHA | 04/Mayo/2018 |
| FECHA | 02-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_agregarbitacora_admin_bei(
  pFechaOper                   DATETIME YEAR TO SECOND
  pIdOperacion                 CHAR(4)
  pIpUsuario                   CHAR(15)
  pIdAdmin                     INTEGER
  pIdOperador                  INTEGER
  pFechaAplic                  DATE
  pCtaOrigen                   CHAR(12)
  pCtaDestino                  CHAR(12)
  pToken                       CHAR(9)
  pCgen3                       CHAR(50)
  pCgen4                       CHAR(50)
  pCgen5                       CHAR(50)
  pCgen6                       CHAR(50)
) RETURNING CHAR(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaOper` | `DATETIME YEAR TO SECOND` | — | — |
| `pIdOperacion` | `CHAR(4)` | — | — |
| `pIpUsuario` | `CHAR(15)` | — | — |
| `pIdAdmin` | `INTEGER` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | ✅ CÓDIGO |
| `pIdOperador` | `INTEGER` | — | — |
| `pFechaAplic` | `DATE` | — | — |
| `pCtaOrigen` | `CHAR(12)` | — | — |
| `pCtaDestino` | `CHAR(12)` | — | — |
| `pToken` | `CHAR(9)` | — | — |
| `pCgen3` | `CHAR(50)` | — | — |
| `pCgen4` | `CHAR(50)` | — | — |
| `pCgen5` | `CHAR(50)` | — | — |
| `pCgen6` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L28 |
| `sql_err` | `INTEGER` | L29 |
| `cNumCliente` | `CHAR(50)` | L30 |
| `cNombreAdmin` | `CHAR(150)` | L31 |
| `cNombreOperador` | `CHAR(150)` | L32 |
| `sIdBitacoraAdmin` | `INTEGER` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L56 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L66 |
| `bei_bitacora_admin` | `bdibei` | no | INSERT | L77 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?ar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ag`, `?ar` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_agregarbitacora_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_agregarbitacora_bei.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "registro bitácora y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Insercion en bitacora con los campos genericos extra (originalmente eran 4 y el 11/10/2017 se agregaron 5 campos genericos) |
| FECHA | 11/Oct/2017 |

### Firma

```sql
CREATE PROCEDURE sp_agregarbitacora_bei(
  pFechaOper                   datetime year to second
  pNumTrans                    char(4)
  pNumSuc                      char(4)
  pIdUsuario                   integer
  pIpUsuario                   char(15)
  pFechaApli                   date
  pCtaOrigen                   char(12)
  pCtaDesti                    char(12)
  pMonto                       money
  pSecTrans                    char(16)
  pCgen1                       char(40)
  pCgen2                       char(40)
  pCgen3                       char(40)
  pCgen4                       char(40)
  pCgen5                       char(40)
  pCgen6                       char(40)
  pCgen7                       char(40)
  pCgen8                       char(40)
  pCgen9                       char(40)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaOper` | `datetime year to second` | — | — |
| `pNumTrans` | `char(4)` | — | — |
| `pNumSuc` | `char(4)` | — | — |
| `pIdUsuario` | `integer` | — | — |
| `pIpUsuario` | `char(15)` | — | — |
| `pFechaApli` | `date` | — | — |
| `pCtaOrigen` | `char(12)` | — | — |
| `pCtaDesti` | `char(12)` | — | — |
| `pMonto` | `money` | — | — |
| `pSecTrans` | `char(16)` | — | — |
| `pCgen1` | `char(40)` | — | — |
| `pCgen2` | `char(40)` | — | — |
| `pCgen3` | `char(40)` | — | — |
| `pCgen4` | `char(40)` | — | — |
| `pCgen5` | `char(40)` | — | — |
| `pCgen6` | `char(40)` | — | — |
| `pCgen7` | `char(40)` | — | — |
| `pCgen8` | `char(40)` | — | — |
| `pCgen9` | `char(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L16 |
| `sql_err` | `integer` | L17 |
| `cNumCliente` | `CHAR(50)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L35 |
| `bei_bitacora` | `bdibei` | no | INSERT | L43 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ag` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?ar` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ag`, `?ar` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_altamod_limites_cuenta_oper_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_altamod_limites_cuenta_oper_bei.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "da de alta cuenta y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| AUTOR | Berenice Noriega Guevara |
| FECHA | 29/Agosto/2014 |
| MODIFICACION | 28/Enero/2015 |
| DESCRIPCION | Se ajusta para que no regrese error si se intenta |
| MODIFICACION | Berenice Noriega - BanCoppel |

### Firma

```sql
CREATE PROCEDURE sp_altamod_limites_cuenta_oper_bei(
  pcliente                     CHAR(9)
  prestricc                    CHAR(2)
  pcuenta                      CHAR(16)
  poperacion                   CHAR(2)
  plimite                      DECIMAL(16,2)
  pcanal                       CHAR(2)
  pusuario                     CHAR(10)
) RETURNING CHAR(3), CHAR(80)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pcliente` | `CHAR(9)` | — | — |
| `prestricc` | `CHAR(2)` | — | — |
| `pcuenta` | `CHAR(16)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `poperacion` | `CHAR(2)` | `ope`=operación | 🟡 INFERIDO |
| `plimite` | `DECIMAL(16,2)` | — | — |
| `pcanal` | `CHAR(2)` | — | — |
| `pusuario` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INT` | L22 |
| `cCodRet` | `CHAR(3)` | L23 |
| `cErrorInfo` | `CHAR(80)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plimites_empresas` | `bdinteg` | ⚠️ sí | SELECT | L92 |
| `si_plimites_empresas` | `bdinteg` | ⚠️ sí | INSERT | L99 |
| `si_plimites_empresas` | `bdinteg` | ⚠️ sí | UPDATE | L112 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L47 | VALIDACIÓN_NULL | `IF nvl(pcliente,'') = ''  OR pcliente IS NULL THEN` |  |
| L53 | VALIDACIÓN_NULL | `IF nvl(prestricc,'') = '' OR prestricc IS NULL THEN` |  |
| L59 | VALIDACIÓN_NULL | `IF nvl(poperacion,'') = '' OR poperacion IS NULL THEN` |  |
| L65 | VALIDACIÓN_NULL | `IF nvl(pcanal,'') = '' OR pcanal IS NULL THEN` |  |
| L71 | VALIDACIÓN_NULL | `IF nvl(pusuario,'') = '' OR pusuario IS NULL THEN` |  |
| L77 | VALIDACIÓN_NULL | `IF nvl(plimite,'') = '' OR plimite IS NULL THEN` |  |
| L84 | VALIDACIÓN_NULL | `IF nvl(pcuenta,'') = '' OR pcuenta IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?mod_limites_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mod_limites_`, `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_altamod_limites_cuenta_oper_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_altamod_limites_cuenta_oper_bei_historico.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "da de alta cuenta y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| AUTOR | Berenice Noriega Guevara |
| FECHA | 29/Agosto/2014 |
| MODIFICACION | 28/Enero/2015 |
| DESCRIPCION | Se ajusta para que no regrese error si se intenta |
| MODIFICACION | Berenice Noriega - BanCoppel |
| MODIFICACION | Solser (el 16/05/2018) |
| FECHA | 16/05/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_altamod_limites_cuenta_oper_bei_historico(
  pcliente                     CHAR(9)
  prestricc                    CHAR(2)
  pcuenta                      CHAR(16)
  poperacion                   CHAR(2)
  plimite                      DECIMAL(16,2)
  pcanal                       CHAR(2)
  pusuario                     CHAR(10)
  pidBitacoraAdmin             INTEGER
) RETURNING CHAR(3), CHAR(80)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pcliente` | `CHAR(9)` | — | — |
| `prestricc` | `CHAR(2)` | — | — |
| `pcuenta` | `CHAR(16)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `poperacion` | `CHAR(2)` | `ope`=operación | 🟡 INFERIDO |
| `plimite` | `DECIMAL(16,2)` | — | — |
| `pcanal` | `CHAR(2)` | — | — |
| `pusuario` | `CHAR(10)` | — | — |
| `pidBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INT` | L33 |
| `cCodRet` | `CHAR(3)` | L34 |
| `cErrorInfo` | `CHAR(80)` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plimites_empresas_historico` | `bdinteg` | ⚠️ sí | INSERT | L57 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?mod_limites_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mod_limites_`, `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_asigna_token_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_asigna_token_bei.sql` |
| **LOC (1er CREATE)** | 132 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "asigna token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 7 tabla(s) con operaciones: INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Asocia y Desasocia los Tokens de los Usuarios |
| FECHA | 24/05/2013 |
| MODIFICACION | se modifica para que al elegir un token (activacion o asignacion de token) se realice un |
| FECHA | 04 de Septiembre 2013 |
| MODIFICACION | Correccin de Flujo, se agrego validacion ya que tabla bei_token no permite valores nulos |
| FECHA | 11 de Septiembre 2013 |
| MODIFICACION | Corregio Flujo,ya que no se puede borrar bei_token con un id de usuario nulo |
| FECHA | 12 de Septiembre 2013 |

### Firma

```sql
CREATE PROCEDURE sp_asigna_token_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pNsToken                     VARCHAR(10)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pNsToken` | `VARCHAR(10)` | `token`=token (autenticación) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `vNsToken` | `VARCHAR(10)` | L6 |
| `vfolio` | `CHAR(12)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L57 |
| `bei_token` | `bdibei` | no | UPDATE | L63 |
| `bei_servicio` | `bdibei` | no | UPDATE | L70 |
| `bei_token` | `bdibei` | no | SELECT | L85 |
| `bei_servicio` | `bdibei` | no | SELECT | L93 |
| `bei_token` | `bdibei` | no | INSERT | L107 |
| `bei_usuario` | `bdibei` | no | SELECT | L116 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_asigna_tokensolicitud_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_asigna_tokensolicitud_bei.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "asigna token, solicitud y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |

### Firma

```sql
CREATE PROCEDURE sp_asigna_tokensolicitud_bei(
  pNseries                     char(90)
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
  pStatus                      char(6)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNseries` | `char(90)` | — | — |
| `pNumSolicitud` | `char(10)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pNumCliente` | `char(9)` | — | — |
| `pStatus` | `char(6)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L11 |
| `sql_err` | `integer` | L12 |
| `vNstoken` | `char(9)` | L13 |
| `vLength` | `smallint` | L14 |
| `vContador` | `smallint` | L15 |
| `vVar` | `smallint` | L16 |
| `i` | `INTEGER` | L17 |
| `vTrama` | `char(90)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | INSERT | L51 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | FÓRMULA | `LET vContador = vLength/9;` |  |
| L49 | FÓRMULA | `LET i = i + 9;` |  |
| L50 | FÓRMULA | `LET vVar= vVAr + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_asignaperfil_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_asignaperfil_bei.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "asigna perfil de usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Asgina un Perfil a un Usuario |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_asignaperfil_bei(
  pIdUsuario                   INTEGER
  pIdPerfil                    INTEGER
) RETURNING CHAR (5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdPerfil` | `INTEGER` | `perfil`=perfil de usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L10 |
| `cCod_ret` | `CHAR (5)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario_perfil` | `bdibei` | no | INSERT | L48 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_bei_obtenerreportcodrast`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bei_obtenerreportcodrast.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad y TCO — Tarjetas Coppel" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_bei_obtenerreportcodrast(
  pCodRast                     char(10)
  pRegistros                   int
) RETURNING char(5) as CodRet, char(10) as CodRast, char(30) as NumGuia, char(10) as FechaSolicitud,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCodRast` | `char(10)` | — | — |
| `pRegistros` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L6 |
| `sql_err` | `integer` | L7 |
| `vCodRast` | `char(10)` | L8 |
| `vNumGuia` | `char(30)` | L9 |
| `vFechaSolicitud` | `char(10)` | L10 |
| `vSolicitud` | `char(10)` | L11 |
| `vCliente` | `char(9)` | L12 |
| `vSucursal` | `char(4)` | L13 |
| `vNumEnvio` | `char(3)` | L14 |
| `vFechaRegistro` | `char(10)` | L15 |
| `vEstatus` | `char(3)` | L16 |
| `vEnvio` | `char(8)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_envios` | `bdibei` | no | SELECT | L50 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?or` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tco` | ENTIDAD | TCO — Tarjetas Coppel / TCoppel (producto de crédito Grupo C | 🔵 CONVENCIÓN | nombre_sp |
| `?drast` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?or`, `?drast` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bei_obtenerreportguia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bei_obtenerreportguia.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_bei_obtenerreportguia(
  pCliente                     char(9)
  pRegistros                   int
) RETURNING char(5) as CodRet, char(4) as sucursal, char(10) as FechaSolicitud, char(10) as Solicitud, char(9) as Cliente,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCliente` | `char(9)` | — | — |
| `pRegistros` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L6 |
| `sql_err` | `integer` | L7 |
| `vSucursal` | `char(4)` | L8 |
| `vFechaSolicitud` | `char(10)` | L9 |
| `vSolicitud` | `char(10)` | L10 |
| `vCliente` | `char(9)` | L11 |
| `vNumGuia` | `char(30)` | L12 |
| `vNumEnvio` | `char(3)` | L13 |
| `vFechaEnvio` | `char(10)` | L14 |
| `vEstatusEnvio` | `char(3)` | L15 |
| `vPerEnvio` | `char(8)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_envios` | `bdibei` | no | SELECT | L48 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?ortguia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ortguia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bei_obtenerreportinventario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bei_obtenerreportinventario.sql` |
| **LOC (1er CREATE)** | 169 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad, inv — inversión y Tarjeta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_bei_obtenerreportinventario(
  pToken                       CHAR(9)
  pEstatus                     CHAR(3)
  pRegistros                   INT
) RETURNING CHAR(5) AS CodRet, CHAR(9) AS Token, CHAR(3) AS Estatus, CHAR(10) AS FechaCambio, CHAR(9) AS Cliente, CHAR(10) as Solicitud,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pToken` | `CHAR(9)` | — | — |
| `pEstatus` | `CHAR(3)` | — | — |
| `pRegistros` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L6 |
| `sql_err` | `INTEGER` | L7 |
| `vToken` | `CHAR(9)` | L8 |
| `vEstatus` | `CHAR(3)` | L9 |
| `vFechaCambio` | `CHAR(10)` | L10 |
| `vCliente` | `CHAR(9)` | L11 |
| `vSolicitud` | `CHAR(10)` | L12 |
| `vNumGuia` | `CHAR(30)` | L13 |
| `vFechaIngreso` | `CHAR(10)` | L14 |
| `vNs_token` | `CHAR(9)` | L15 |
| `vId_status` | `CHAR(3)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L50 |
| `tkn_status_token` | `bdibpi` | ⚠️ sí | SELECT | L59 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L63 |
| `bei_envios` | `bdibei` | no | SELECT | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L75 | VALIDACIÓN_NULL | `IF ((vToken = '' OR vToken IS NULL) AND (vCliente = '' OR vCliente IS NULL))THEN` |  |
| L140 | VALIDACIÓN_NULL | `IF (vEstatus = '' OR vEstatus IS NULL) THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?ort` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `inv` | ENTIDAD | inv — inversión (abreviación — calsdoinvcrec, cierrechqinvcr | 🟡 INFERIDO | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?io` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ort`, `?en`, `?io` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bei_obtenerreportnumguia`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bei_obtenerreportnumguia.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad y número" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_bei_obtenerreportnumguia(
  pNumGuia                     char(30)
  pRegistros                   int
) RETURNING char(5) as CodRet, char(30) as NumGuia, char(10) as FechaSolicitud, char(10) as Solicitud, char(9) as Numcliente,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumGuia` | `char(30)` | `num`=número (de) | 🔵 CONVENCIÓN |
| `pRegistros` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L7 |
| `sql_err` | `integer` | L8 |
| `vNumGuia` | `char(30)` | L9 |
| `vFechaSolicitud` | `char(10)` | L10 |
| `vSolicitud` | `char(10)` | L11 |
| `vCliente` | `char(9)` | L12 |
| `vSucursal` | `char(4)` | L13 |
| `vNumEnvio` | `char(3)` | L14 |
| `vFechaRegistro` | `char(10)` | L15 |
| `vEstatus` | `char(3)` | L16 |
| `vEnvio` | `char(8)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_envios` | `bdibei` | no | SELECT | L49 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?ort` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?guia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ort`, `?guia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bei_obtenersolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bei_obtenersolicitud.sql` |
| **LOC (1er CREATE)** | 109 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad y solicitud" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_bei_obtenersolicitud(
  pSolicitud                   char(10)
  pToken                       char(10)
  pCliente                     char(9)
  pRegistros                   int
) RETURNING char(5) as codRet, char(4) as Sucursal, char(10) as FechaSolicitud, char(10) as Solicitud, char(10) as Token, char(9) as Cliente, char(3) as Estatus, char(8) as solicito, char(8) as Envio
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSolicitud` | `char(10)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pToken` | `char(10)` | — | — |
| `pCliente` | `char(9)` | — | — |
| `pRegistros` | `int` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L6 |
| `sql_err` | `integer` | L7 |
| `vSucursal` | `char(4)` | L8 |
| `vFechaSolicitud` | `char(10)` | L9 |
| `vToken` | `char(10)` | L10 |
| `vCliente` | `char(9)` | L11 |
| `vEstatus` | `char(3)` | L12 |
| `vSolicitud` | `char(10)` | L13 |
| `vSolicito` | `char(8)` | L14 |
| `vEnvio` | `char(8)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L46 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_bloq_temp_user_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bloq_temp_user_bei.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "bloqueo BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (temporal)" `[partial]` |
| **Propósito verificado** | ❓ NO_VERIFICABLE — Propósito inferido; sin evidencia DML para verificar |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Bloquea Usuario Temporalmente |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_bloq_temp_user_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pMinute                      SMALLINT
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pMinute` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bloq` | ACCION | bloqueo | 🟡 INFERIDO | nombre_sp |
| `temp` | MODIF | temporal | 🔵 CONVENCIÓN | nombre_sp |
| `?_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bloquea_token_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bloquea_token_bei.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "bloquea cuenta token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: UPDATE |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Cambia Estatus Token |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_bloquea_token_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pNsToken                     VARCHAR(10)
  pIdStatusToken               INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pNsToken` | `VARCHAR(10)` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pIdStatusToken` | `INTEGER` | `token`=token (autenticación) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | UPDATE | L54 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_bloquear_usuarios_sin_actividad_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bloquear_usuarios_sin_actividad_bei.sql` |
| **LOC (1er CREATE)** | 242 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "bloquea cuenta usuarios, identificador y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 6 tabla(s) con operaciones: INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=4 / 10 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | SP que realizara un cambio de estatus (BLOQUEO) a aquellos usuarios sin actividad en la banca empresarial |
| FECHA | 28/02/2022 |

### Firma

```sql
CREATE PROCEDURE sp_bloquear_usuarios_sin_actividad_bei(
) RETURNING CHAR(5) AS codigo, CHAR(65) AS mensaje
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L21 |
| `cCodRet` | `CHAR(5)` | L22 |
| `cCodRet2` | `CHAR(5)` | L23 |
| `cMensaje` | `CHAR(100)` | L24 |
| `cMensajeEjec` | `CHAR(50)` | L25 |
| `cRegistroEjec` | `CHAR(50)` | L26 |
| `iTransaccion` | `INTEGER` | L27 |
| `iUsuario` | `INTEGER` | L30 |
| `cNumCliente` | `CHAR(9)` | L31 |
| `vNumTokenAsociado` | `VARCHAR(10)` | L32 |
| `cNumTokenSerial` | `CHAR(10)` | L33 |
| `dFechaUltimoAcceso` | `DATE` | L34 |
| `iTotalRegistros` | `INTEGER` | L35 |
| `sIdEstatusUsr` | `SMALLINT` | L36 |
| `sIdEstatusTknAsoc` | `SMALLINT` | L37 |
| `sIdEstatusTknSerial` | `SMALLINT` | L38 |
| `sEstatusBloqueoUsr` | `SMALLINT` | L41 |
| `sEstatusBloqueoTkn` | `SMALLINT` | L42 |
| `cSucursal` | `CHAR(4)` | L43 |
| `cUsuarioPortal` | `CHAR(8)` | L44 |
| `cDireccionIpGenerica` | `CHAR(15)` | L45 |
| `cCanal` | `CHAR(2)` | L46 |
| `iContandor` | `INTEGER` | L47 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L100 |
| `bei_usuario` | `bdibei` | no | UPDATE | L178 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L182 |
| `bei_token` | `bdibei` | no | UPDATE | L204 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | UPDATE | L209 |
| `tkn_status_token` | `bdibpi` | ⚠️ sí | INSERT | L213 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L51 | CÓDIGO_RETORNO | `LET cCodRet                 = '00000';` |  |
| L81 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L120 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L230 | FÓRMULA | `LET iContandor = iContandor + 1;` |  |
| L236 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bloquea` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_sin_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?iv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?ad_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_`, `?_sin_`, `?iv`, `?ad_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_bloqueo_avatar_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bloqueo_avatar_bei.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "bloquea cuenta avatar y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Tokens confirmados en el vocab pero DML no correlaciona con el propósito |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza la Intentos Avatar |
| FECHA | 22/09/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_bloqueo_avatar_bei(
  pIdUsuario                   INTEGER
  pNumCliente                  CHAR(9)
  pIntetno                     INTEGER
  pMinBlqueo                   INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIntetno` | `INTEGER` | — | — |
| `pMinBlqueo` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sFBloqueoTemp` | `DATETIME YEAR to SECOND` | L7 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L60 | FÓRMULA | `LET sFBloqueoTemp =CURRENT+ pMinBlqueo units minute;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `avatar` | ENTIDAD | avatar (foto de perfil del usuario en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_bloqueo_tktemp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_bloqueo_tktemp_bei.sql` |
| **LOC (1er CREATE)** | 48 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "bloquea cuenta BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (temporal)" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: UPDATE |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Ejecuta Proceso de Mancomuniadd |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_bloqueo_tktemp_bei(
  pIdUsuario                   INTEGER
  pFechaBloqueo                DATETIME YEAR to SECOND
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pFechaBloqueo` | `DATETIME YEAR to SECOND` | `bloqueo`=bloquea cuenta | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L17 |
| `sql_err` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | UPDATE | L42 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bloqueo` | ACCION | bloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_tk` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `temp` | MODIF | temporal | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_borra_resumen_oper_manc_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_borra_resumen_oper_manc_bei.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "borra BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Borra el Historial de los ultimos N dias, Esto no afecta la tabla de Concentrado |

### Firma

```sql
CREATE PROCEDURE sp_borra_resumen_oper_manc_bei(
) RETURNING char (5)
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L8 |
| `cCod_ret` | `char (5)` | L9 |
| `vCantidad` | `smallint` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L30 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `borra` | ACCION | borra / elimina registros (borramovs_movhis, borramovscfd*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_resumen_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_resumen_`, `?r_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_borrarrespuestas_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_borrarrespuestas_bei.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "borra respuesta y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: DELETE, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | BORRA LAS RESPUESTAS DE SEGURIDAD DE UN USUARIO |
| FECHA | 05/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_borrarrespuestas_bei(
  pIdUsuario                   INTEGER
) RETURNING CHAR (5) AS cCod_ret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L11 |
| `cCod_ret` | `CHAR (5)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_respuestas` | `bdibei` | no | SELECT | L28 |
| `bei_respuestas` | `bdibei` | no | DELETE | L28 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `borra` | ACCION | borra / elimina registros (borramovs_movhis, borramovscfd*) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `respuesta` | ENTIDAD | respuesta | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cambiarpass_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cambiarpass_bei.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Actualiza el password del usuario de BPI |
| DESCRIPCION | Cambio de pass de los usuarios |

### Firma

```sql
CREATE PROCEDURE sp_cambiarpass_bei(
  pEmpresa                     char(3)
  pNumCte                      char(20)
  pIdUsuario                   INTEGER
  pPass                        char(50)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char(3)` | — | — |
| `pNumCte` | `char(20)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pPass` | `char(50)` | `ss`=ss — subsistema / canal de monitoreo (abreviación — envia_monitorsol_*_ss_* — bdisolic) | 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L18 |
| `sql_err` | `integer` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L34 |
| `bei_usuario` | `bdibei` | no | UPDATE | L47 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cam` | PREFIJO | cámara / captura contable | 🟡 INFERIDO | nombre_sp |
| `?biarpa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?biarpa`, `ss` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cambiostatus_token_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cambiostatus_token_bei.sql` |
| **LOC (1er CREATE)** | 153 |
| **Callgraph** | ✅ fan_in=0 / fan_out=10 |
| **Deps concatenadas** | 19 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cambio, estatus, token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_cnsif_confirmaejecutivo`, `sp_set_estatus_tokenasociados_bei`, `sp_set_solicitudstatus_admtoken_bei` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_cambiostatus_token_bei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(9)
  pStatusNuevo                 CHAR(3)
  pCanal                       CHAR(2)
  pNumSolicitud                CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pStatusNuevo` | `CHAR(3)` | `status`=estatus | ✅ CÓDIGO |
| `pCanal` | `CHAR(2)` | — | — |
| `pNumSolicitud` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cCodRetSp` | `CHAR(5)` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cCod_ret_toksasociad` | `CHAR(5)` | L10 |
| `pStatusViejo` | `CHAR(3)` | L11 |
| `cSolicitud` | `CHAR(10)` | L13 |
| `sStatusViejo` | `SMALLINT` | L14 |
| `cCod_ret_solictoken` | `CHAR(5)` | L15 |
| `iTotalReg` | `INTEGER` | L16 |
| `iNoRegistros` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L64 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L70 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L79 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L58 |
| `sp_set_estatus_tokenasociados_bei` | `bdibei` | no | L85 |
| `sp_set_solicitudstatus_admtoken_bei` | `bdibei` | no | L121 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L90 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_set_estatus_tokenasociados_bei'; -- '` |  |
| L107 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L126 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_set_solicitudstatus_admtoken_bei'; --` |  |
| L139 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cambio` | ENTIDAD | cambio (de estatus, domicilio, etc.) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_can_tknxpira_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_can_tknxpira_bei.sql` |
| **LOC (1er CREATE)** | 138 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 11 tabla(s) con operaciones: INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| AUTOR | Gabriela Aguilar |
| ACTIVIDAD | Valida los token que se encuentra en la tabla bei_tokenexpira los cuales ya paso su fecha de caducidad y no fueron renovados. |
| FECHA | 30/07/2019 |

### Firma

```sql
CREATE PROCEDURE sp_can_tknxpira_bei(
  pfecha                       DATE
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pfecha` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsCodRet` | `CHAR(10)` | L12 |
| `viSqlErr` | `INTEGER` | L13 |
| `vTransaccion` | `INTEGER` | L14 |
| `vNum_cliente` | `CHAR(9)` | L15 |
| `vNs_token` | `CHAR(9)` | L16 |
| `vid_usuario` | `INTEGER` | L17 |
| `vid_tipo_usuario` | `SMALLINT` | L18 |
| `vnombre` | `CHAR(150)` | L19 |
| `vid_status_token` | `INTEGER` | L20 |
| `vid_status_servicio` | `INTEGER` | L21 |
| `vid_status_solicitud` | `CHAR(1)` | L22 |
| `vsuc_registro` | `CHAR(4)` | L23 |
| `vfolio_token` | `VARCHAR(25)` | L24 |
| `vSolicitud` | `CHAR(10)` | L25 |
| `vid_status` | `CHAR(4)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokenexpira` | `bdibei` | no | SELECT | L90 |
| `bei_token` | `bdibei` | no | SELECT | L94 |
| `bei_tokensolicitud` | `bdibei` | no | UPDATE | L99 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | UPDATE | L101 |
| `tkn_status_token` | `bdibpi` | ⚠️ sí | INSERT | L103 |
| `bei_tokenhis` | `bdibei` | no | INSERT | L107 |
| `bei_tokenexpira_his` | `bdibei` | no | INSERT | L112 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L117 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L119 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L121 |
| `informix` | `bdibei` | no | INSERT | L126 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET vsCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_can_tkn` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?pira_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_can_tkn`, `?pira_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cancela_oper_manc_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cancela_oper_manc_bei.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "cancela BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: UPDATE |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Actualiza el status de operacioens mancomunadas a Cancelado |

### Firma

```sql
CREATE PROCEDURE sp_cancela_oper_manc_bei(
  pFecha                       CHAR(10)
) RETURNING char (5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFecha` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L10 |
| `cCod_ret` | `char (5)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperadorresumen` | `bdibei` | no | UPDATE | L31 |
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | UPDATE | L35 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cancela_servicio_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cancela_servicio_bei.sql` |
| **LOC (1er CREATE)** | 217 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "cancela servicio y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 3 llamada(s): `sp_actualizastatususuario_bei`, `sp_set_statustoken_admtoken`, `sp_set_solicitudstatus_admtoken_bei` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Cacela el servicio de EmpresaNetPlus pasando los estatus de usuario a 99 |
| FECHA | 2014 |
| MODIFICACION | Berenice Noriega, para considerar los clientes sin solicitud atendida. regresa un codigo |
| FECHA | 21 Agosto 2014. |

### Firma

```sql
CREATE PROCEDURE sp_cancela_servicio_bei(
  pEmpresa                     char (3)
  pNumCliente                  char(9)
  pSucursal                    char(4)
  pUsuario                     char(8)
) RETURNING CHAR(5), CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char (3)` | — | — |
| `pNumCliente` | `char(9)` | — | — |
| `pSucursal` | `char(4)` | — | — |
| `pUsuario` | `char(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L27 |
| `vCodRet` | `CHAR(5)` | L28 |
| `vCodRet2` | `CHAR(5)` | L29 |
| `vIdStatus` | `CHAR(4)` | L30 |
| `vServicio` | `smallint` | L31 |
| `vSolicitud` | `CHAR(10)` | L32 |
| `vIdStaSol` | `CHAR(4)` | L34 |
| `cSolicitud` | `CHAR(10)` | L35 |
| `vToken` | `CHAR(10)` | L36 |
| `vIdStaToken` | `CHAR(3)` | L37 |
| `vSucRegistra` | `CHAR(4)` | L38 |
| `vF_Status` | `DATE` | L39 |
| `vF_Registro` | `DATE` | L40 |
| `vIdStaAdm` | `CHAR(4)` | L41 |
| `vNstoken` | `CHAR(10)` | L42 |
| `vIdentAdmin` | `CHAR(30)` | L43 |
| `vIdusuario` | `INTEGER` | L44 |
| `vStatusT` | `CHAR (4)` | L45 |
| `id_tipo_usuario` | `CHAR(4)` | L46 |
| `vNumCte` | `CHAR(9)` | L47 |
| `iTotalReg` | `integer` | L48 |
| `vfoliotoken` | `CHAR(12)` | L49 |
| `vSucregistro` | `CHAR(9)` | L50 |
| `vIdusuario2` | `INTEGER` | L51 |
| `vFecharegistro` | `DATE` | L52 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L106 |
| `bei_contratacion` | `bdibei` | no | UPDATE | L117 |
| `bei_usuario` | `bdibei` | no | SELECT | L128 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L161 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L167 |
| `bei_token` | `bdibei` | no | SELECT | L173 |
| `bei_tokenhis` | `bdibei` | no | INSERT | L184 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_actualizastatususuario_bei` | `bdibei` | no | L110 |
| `sp_set_statustoken_admtoken` | `bdibpi` | ⚠️ sí | L177 |
| `sp_set_solicitudstatus_admtoken_bei` | `bdibei` | no | L204 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L56 | CÓDIGO_RETORNO | `LET vCodRet =   '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cargar_tkn_tokenexpira_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cargar_tkn_tokenexpira_bei.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "carga token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (sufijo Exportar — SP genera/exporta archivo de salida)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| AUTOR | Gabrieal Aguilar |
| ACTIVIDAD | Segun la fecha, identifica los usararios que tengan token que esten proximo a vencer y esten en status validos para insertalos |
| FECHA | 06/06/2019 |

### Firma

```sql
CREATE PROCEDURE sp_cargar_tkn_tokenexpira_bei(
  pFechaExpira                 DATE
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaExpira` | `DATE` | `exp`=sufijo Exportar — SP genera/exporta archivo de salida (sp_*_exp: generaarchivocuentasnomina_exp, perfisica_listanegra_exp, reversion_exp) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsCodRet` | `CHAR(10)` | L14 |
| `viSqlErr` | `INTEGER` | L15 |
| `pid_usuario` | `INTEGER` | L16 |
| `pnum_cliente` | `CHAR(9)` | L17 |
| `pns_token` | `CHAR(9)` | L18 |
| `pid_status_token` | `INTEGER` | L19 |
| `pnombre` | `CHAR(150)` | L20 |
| `pid_tipo_usuario` | `INTEGER` | L21 |
| `pid_status` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L79 |
| `bei_tokenexpira` | `bdibei` | no | INSERT | L87 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | CÓDIGO_RETORNO | `LET vsCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `?r_tkn_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `exp` | MODIF | sufijo Exportar — SP genera/exporta archivo de salida (sp_*_ | 🟡 INFERIDO | nombre_sp |
| `?ira_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_tkn_`, `?ira_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cargarreversarcuentatoken_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cargarreversarcuentatoken_bei.sql` |
| **LOC (1er CREATE)** | 237 |
| **Callgraph** | ✅ fan_in=0 / fan_out=5 |
| **Propósito inferido** | "carga cuenta, token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 5 llamada(s): `reversion`, `reversion`, `sp_cons_tar_divisa` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Se clona SP sp_cargarreversarcuentatoken_bpi para hacer el cargo y generar solicitud de Reposicion de token por Vencimiento |
| FECHA | 21/08/2018 |

### Firma

```sql
CREATE PROCEDURE sp_cargarreversarcuentatoken_bei(
  pTipo                        CHAR(1)
  pSistema                     CHAR(1)
  pEmpresa                     CHAR(3)
  pNumCte                      CHAR(20)
  pSucursal                    CHAR(4)
  pSecDomicilio                SMALLINT
  pNumEmpleado                 CHAR(9)
  pFolio_Suc                   CHAR (16)
  pCuenta                      CHAR(12)
  pMonto                       MONEY
  punidades                    CHAR(10)
  pTransac                     CHAR(4)
  pTransacIva                  CHAR(4)
  pStatusToken                 SMALLINT
) RETURNING CHAR(5), CHAR(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pTipo` | `CHAR(1)` | — | — |
| `pSistema` | `CHAR(1)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pSecDomicilio` | `SMALLINT` | — | — |
| `pNumEmpleado` | `CHAR(9)` | — | — |
| `pFolio_Suc` | `CHAR (16)` | — | — |
| `pCuenta` | `CHAR(12)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pMonto` | `MONEY` | — | — |
| `punidades` | `CHAR(10)` | — | — |
| `pTransac` | `CHAR(4)` | — | — |
| `pTransacIva` | `CHAR(4)` | — | — |
| `pStatusToken` | `SMALLINT` | `token`=token (autenticación) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsCodRet` | `CHAR(10)` | L14 |
| `viSqlErr` | `INTEGER` | L15 |
| `viEdoCte` | `SMALLINT` | L16 |
| `vsSolicitud` | `CHAR(10)` | L17 |
| `sDivisa` | `CHAR(2)` | L18 |
| `sNumTarjeta` | `CHAR(20)` | L19 |
| `vsTipoSer` | `CHAR(2)` | L20 |
| `vTrans` | `CHAR(4)` | L21 |
| `dFecha` | `DATE` | L22 |
| `sSaldo` | `MONEY(14,2)` | L23 |
| `vTransaccion` | `INTEGER` | L24 |
| `iCargo` | `INTEGER` | L25 |
| `vCodRet` | `CHAR(10)` | L26 |
| `vParam` | `CHAR(2)` | L27 |
| `vsNumSolicitud` | `CHAR(10)` | L28 |
| `mIva` | `MONEY(16,2)` | L29 |
| `cTipoPersona` | `CHAR(2)` | L30 |
| `pMontoSinIva` | `MONEY(12,2)` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L149 |
| `bei_solicitudtoken` | `bdibei` | no | INSERT | L194 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L197 |
| `bei_tokenexpira` | `bdibei` | no | UPDATE | L200 |
| `bdibpi` | `bdibei` | no | INSERT | L223 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `reversion` | `bdicheq` | ⚠️ sí | L73 |
| `reversion` | `bdicred` | ⚠️ sí | L76 |
| `sp_cons_tar_divisa` | `bdibpi` | ⚠️ sí | L121 |
| `cargo_ref` | `bdicheq` | ⚠️ sí | L136 |
| `cargoref_tc_ofi` | `bdicred` | ⚠️ sí | L172 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | CÓDIGO_RETORNO | `LET vsCodRet = '00000';` |  |
| L50 | CÓDIGO_RETORNO | `LET vCodRet = '00000';` |  |
| L102 | VALIDACIÓN_NULL | `IF NVL(pTipo, '') = '' OR  NVL(pEmpresa, '') = '' OR NVL(pNumCte, '') = ''  OR  NVL(pStatusToken, ''` |  |
| L113 | CÓDIGO_RETORNO | `LET vsCodRet = '00000';` |  |
| L151 | FÓRMULA | `LET mIva = pMonto * mIva;` | 🔴 MONEY/aritmética financiera |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_archivobatch_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_archivobatch_bei.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta archivo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (proceso batch)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta los archivos de dispersión en proceso batch |
| FECHA | 08/MARZO/2016 |

### Firma

```sql
CREATE PROCEDURE sp_cons_archivobatch_bei(
  pIdEvaluacion                INTEGER
) RETURNING CHAR(5), INTEGER, char(10), char(17), char(10), INTEGER,MONEY, char(10),char(20),INTEGER,INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdEvaluacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L13 |
| `sql_err` | `integer` | L14 |
| `iId_evaluacion` | `INTEGER` | L15 |
| `vConcepto` | `char(10)` | L16 |
| `vArchivo` | `char(17)` | L17 |
| `vhora_alta` | `char(10)` | L18 |
| `iCant_empleados` | `INTEGER` | L19 |
| `mImporte` | `MONEY` | L20 |
| `vFecha_aplicacion` | `char(10)` | L21 |
| `vCta_origen` | `char(20)` | L22 |
| `iNumCtasO` | `INTEGER` | L23 |
| `iNumCtasB` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos_eval` | `bdibei` | no | SELECT | L58 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `batch` | MODIF | proceso batch (por lotes) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cons_cuentas_aut_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_cuentas_aut_bei.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Datos de Usuario para Presentar en Pantalla |
| FECHA | 24/05/2013 |
| MODIFICACION | Se ajusta spl para mejor manejo de la variable autoriza |
| MODIFICACION | Solser |
| FECHA | 2016 ENERO , para version 22Enero2016 |

### Firma

```sql
CREATE PROCEDURE sp_cons_cuentas_aut_bei(
  pIdUsuario                   Integer
  pNumCliente                  CHAR(9)
  pNoReg                       INTEGER
  pRegIni                      INTEGER
) RETURNING char(5), Integer,CHAR(16),CHAR(2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `Integer` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L6 |
| `sql_err` | `integer` | L7 |
| `iTotalReg` | `INTEGER` | L8 |
| `pNum_Cta` | `CHAR(16)` | L10 |
| `pAutoriza` | `CHAR(2)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad` | `bdibei` | no | SELECT | L62 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cons_cuentas_aut_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_cuentas_aut_bei_historico.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Datos de Usuario para Presentar en Pantalla |
| FECHA | 24/05/2013 |
| MODIFICACION | Se ajusta spl para mejor manejo de la variable autoriza |
| MODIFICACION | Solser |
| FECHA | 2016 ENERO , para version 22Enero2016 |
| FECHA | 12/Junio/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_cons_cuentas_aut_bei_historico(
  pIdUsuario                   INTEGER
  pNumCliente                  CHAR(9)
  pNoReg                       INTEGER
  pRegIni                      INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5), INTEGER, CHAR(16), CHAR(2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `iTotalReg` | `INTEGER` | L6 |
| `pNum_Cta` | `CHAR(16)` | L8 |
| `pAutoriza` | `CHAR(2)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad_historico` | `bdibei` | no | SELECT | L60 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cons_cuentas_aut_detalle_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_cuentas_aut_detalle_bei.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuentas, detalle y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Datos de Usuario para Presentar en Pantalla |
| FECHA | 25/02/2014 |

### Firma

```sql
CREATE PROCEDURE sp_cons_cuentas_aut_detalle_bei(
  pIdMancomunidad              Integer
  pNumCliente                  CHAR(9)
  pNoReg                       INTEGER
  pRegIni                      INTEGER
) RETURNING char(5), Integer,CHAR(16),CHAR(2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `Integer` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L6 |
| `sql_err` | `integer` | L7 |
| `iTotalReg` | `INTEGER` | L8 |
| `pNum_Cta` | `CHAR(16)` | L10 |
| `pAutoriza` | `BOOLEAN` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_det_temp` | `bdibei` | no | SELECT | L60 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cons_detenvios_token_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_detenvios_token_bei.sql` |
| **LOC (1er CREATE)** | 178 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta detalle, envíos, token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |
| FECHA | 31-08-2015 |

### Firma

```sql
CREATE PROCEDURE sp_cons_detenvios_token_bei(
  pEmpresa                     char(3)
  pSolicitud                   char(10)
) RETURNING char(5),char(16), char(20), date, char(4), money(16,2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char(3)` | — | — |
| `pSolicitud` | `char(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L18 |
| `vFolioSuc` | `char(16)` | L19 |
| `vCuenta` | `char(20)` | L20 |
| `vCliente` | `char(9)` | L21 |
| `vFecha` | `date` | L22 |
| `vFechaHoy` | `date` | L23 |
| `vSucursal` | `char(4)` | L24 |
| `vCargoTot` | `money(16,2)` | L25 |
| `vMonto` | `money(16,2)` | L26 |
| `vFechaReg` | `datetime year to second` | L27 |
| `sql_err` | `integer` | L28 |
| `cFech_param` | `date` | L29 |
| `cFech_param_ini` | `date` | L30 |
| `cFech_param_ini_old2` | `date` | L31 |
| `cFech_param_ini_old3` | `date` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L65 |
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L73 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L98 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L106 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L121 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L132 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L140 |
| `sc_movhis_old2` | `bdicheq` | ⚠️ sí | SELECT | L150 |
| `sc_movhis_old3` | `bdicheq` | ⚠️ sí | SELECT | L158 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |
| `envios` | ENTIDAD | envíos | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_cons_disp_manco_orden_pago_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_disp_manco_orden_pago_bei.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos, orden, pago y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=6 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_cons_disp_manco_orden_pago_bei(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5), CHAR(10), CHAR(20), CHAR(17), CHAR(10), CHAR(20),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L8 |
| `cCod_ret` | `CHAR (5)` | L9 |
| `vf_aplicacion` | `CHAR(10)` | L10 |
| `vcuenta_origen` | `CHAR(20)` | L11 |
| `vnombre_archivo` | `CHAR(17)` | L12 |
| `vf_dispersion` | `CHAR(10)` | L13 |
| `vcte_empresa` | `CHAR(20)` | L14 |
| `vid_empresa` | `CHAR(3)` | L15 |
| `vid_oper` | `CHAR(4)` | L16 |
| `vid_catOperacion` | `INTEGER` | L17 |
| `vid_usuario` | `INTEGER` | L18 |
| `vtamano_archivo` | `INTEGER` | L19 |
| `vtipo_archivo` | `INTEGER` | L20 |
| `vtipo_cuentas` | `INTEGER` | L21 |
| `vtipo_oper` | `SMALLINT` | L22 |
| `vstatus_archivo` | `SMALLINT` | L23 |
| `vmontoTotal` | `MONEY(14,2)` | L24 |
| `vCantidadEmpleados` | `INTEGER` | L25 |
| `vConcepto` | `CHAR(30)` | L26 |
| `vTipoDispersion` | `INTEGER` | L27 |
| `vcargoDispersion` | `DECIMAL(18,2)` | L28 |
| `vtotalSinComision` | `DECIMAL (18,2)` | L29 |
| `vComsion` | `MONEY(14,2)` | L30 |
| `viva` | `MONEY(14,2)` | L31 |
| `vivaComsion` | `MONEY(14,2)` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L89 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_di` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `orden` | ENTIDAD | orden | 🔵 CONVENCIÓN | nombre_sp |
| `pago` | ENTIDAD | pago | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_di` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_fechasdispersion_odp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_fechasdispersion_odp_bei.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta fechas, dispersión — dispersión de nómina y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LAS FECHAS EN QUE SE REALIZARON DISPERSIONES DE ORDENES DE PAGO INDIVIDUAL |
| FECHA | 27/03/2015 |
| MODIFICACION | SE MODIFICA PARA QUE TAMBIEN CONSIDERE LOS REIGISTROS DE LA BEI_BITACORA_HISTORIAL (DE AYER PARA ATRAS) Y |
| MODIFICACION | Berenice Noriega Guevara - G3 - Coordinaci internet |
| FECHA | 09 Octubre 2019 |

### Firma

```sql
CREATE PROCEDURE sp_cons_fechasdispersion_odp_bei(
  pNumCte                      CHAR(9)
  pIdOperacion                 CHAR(4)
  pRegInical                   INTEGER
) RETURNING char(5), CHAR(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(9)` | — | — |
| `pIdOperacion` | `CHAR(4)` | — | — |
| `pRegInical` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sFecha` | `CHAR(10)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_bitacora` | `bdibei` | no | SELECT | L55 |
| `bei_bitacora_historial` | `bdibei` | no | SELECT | L60 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `fechas` | ENTIDAD | fechas | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `dispersion` | ENTIDAD | dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN | nombre_sp |
| `?_odp_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_odp_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_nom_user_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_nom_user_bei.sql` |
| **LOC (1er CREATE)** | 44 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta nómina y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS NOMBRE DE LA PERSONA , PARA VALIDACION INICIAL EMPRESANET |
| FECHA | 26/04/2013 |

### Firma

```sql
CREATE PROCEDURE sp_cons_nom_user_bei(
  pUsuario                     char(50)
) RETURNING char(5), char (150)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sIdUsuario` | `integer` | L6 |
| `sNombre` | `CHAR(150)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L32 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |
| `?_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_num_oper_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_num_oper_bei.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta número y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta La cantidad de operadores de una empresa |
| FECHA | 25/02/2014 |

### Firma

```sql
CREATE PROCEDURE sp_cons_num_oper_bei(
  pNumCliente                  char(9)
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | `num`=número (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `iTotalOpers` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_contratacion` | `bdibei` | no | SELECT | L40 |
| `bei_usuario` | `bdibei` | no | SELECT | L47 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_oper_usr_ad_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_oper_usr_ad_bei.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Datos de Proceso de Mancomuniadd |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_cons_oper_usr_ad_bei(
  pIdAdminManco                INTEGER
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING char(5) ,SMALLINT,SMALLINT,INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdAdminManco` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `stipoOper` | `SMALLINT` | L8 |
| `sTipoMov` | `SMALLINT` | L9 |
| `sIdUsuario` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L59 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_usr_ad_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_usr_ad_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_opermanco_tkadmin_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_opermanco_tkadmin_bei.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos, Administrador — rol de usuario con privilegios administrativos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Ejecuta Proceso de Mancomuniadd |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_cons_opermanco_tkadmin_bei(
  pIdAdminManco                INTEGER
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING char(5) ,SMALLINT,SMALLINT,INTEGER, CHAR(10) ,INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdAdminManco` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') · `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `stipoOper` | `SMALLINT` | L8 |
| `sTipoMov` | `SMALLINT` | L9 |
| `sIdUsuario` | `INTEGER` | L11 |
| `sNsToken` | `CHAR(10)` | L13 |
| `sIdStatusToken` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L63 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?_tk` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?_tk` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_sec_archivobatch_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_sec_archivobatch_bei.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta archivo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (proceso batch)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se consulta los archivos para recuperar la secuencia |
| FECHA | 10/MARZO/2016 |

### Firma

```sql
CREATE PROCEDURE sp_cons_sec_archivobatch_bei(
  pCodEmpresa                  VARCHAR(3)
  pRegistro                    integer
) RETURNING CHAR(5),  char(17)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCodEmpresa` | `VARCHAR(3)` | — | — |
| `pRegistro` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L14 |
| `sql_err` | `INTEGER` | L15 |
| `pNombre` | `char(17)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos_eval` | `bdibei` | no | SELECT | L43 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_sec_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `batch` | MODIF | proceso batch (por lotes) | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sec_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cons_user_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cons_user_bei.sql` |
| **LOC (1er CREATE)** | 48 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE DATOS USUARIO POR NOMBRE DE USUARIO , PARA VALIDACION INICIAL EMPRESANET |
| FECHA | 28/08/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_cons_user_bei(
  pUsuario                     char(50)
) RETURNING char(5),Integer,CHAR(9)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `char(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sIdUsuario` | `Integer` | L7 |
| `sNumCliente` | `CHAR(9)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L37 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_constotmovschq_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_constotmovschq_bei.sql` |
| **LOC (1er CREATE)** | 284 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta movimientos, cheque y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_constotmovschq_bei(
  pEmpresa                     CHAR(3)
  pCuenta                      CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipoMov                     CHAR(1)
  pBanderaUltHora              INTEGER
) RETURNING CHAR(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipoMov` | `CHAR(1)` | — | — |
| `pBanderaUltHora` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L8 |
| `cFech_param` | `CHAR(10)` | L11 |
| `cFech_param_ini` | `CHAR(10)` | L12 |
| `vFechaHoy` | `DATE` | L13 |
| `vHrInicial` | `DATETIME YEAR TO FRACTION(3)` | L14 |
| `vHrFinal` | `DATETIME YEAR TO FRACTION(3)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L40 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | FÓRMULA | `LET vHrInicial= vHrFinal - INTERVAL(1) HOUR TO HOUR;` |  |
| L129 | FÓRMULA | `LET	iReg = iReg + iAux;` |  |
| L205 | FÓRMULA | `LET	iReg = iReg + iAux;` |  |
| L226 | FÓRMULA | `LET	iReg = iReg + iAux;` |  |
| L273 | FÓRMULA | `LET	iReg = iReg + iAux;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?tot` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movs` | ENTIDAD | movimientos (abreviación) | 🟡 INFERIDO | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tot` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulctafrecuentes_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulctafrecuentes_manco_bei.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuenta, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulctafrecuentes_manco_bei(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5), CHAR(20), CHAR(20), MONEY(14,2), CHAR(40), CHAR(40),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L7 |
| `cCod_ret` | `CHAR(5)` | L8 |
| `vcuenta_origen` | `CHAR(20)` | L9 |
| `vcuenta_destino` | `CHAR(20)` | L10 |
| `vimporte` | `MONEY(14, 2)` | L11 |
| `vreferencia` | `CHAR(40)` | L12 |
| `vreferenciabe` | `CHAR(40)` | L13 |
| `vnombre_beneficiario` | `CHAR(100)` | L14 |
| `vid_usuario` | `INTEGER` | L15 |
| `vid_cat_operacion` | `INTEGER` | L16 |
| `vempresa` | `CHAR(3)` | L17 |
| `vsucursal_virtual` | `CHAR(4)` | L18 |
| `vusuario_virtual` | `CHAR(4)` | L19 |
| `vclave_banco` | `INTEGER` | L20 |
| `vnombre_usuario` | `CHAR(40)` | L21 |
| `vrfc` | `CHAR(18)` | L22 |
| `vtipo_cuenta_beneficiario` | `CHAR(40)` | L23 |
| `vbanco_receptor` | `CHAR(100)` | L24 |
| `vcategoria` | `CHAR(2)` | L25 |
| `vconvenio` | `CHAR(3)` | L26 |
| `vreftelefono` | `CHAR(20)` | L27 |
| `vrefverificador` | `CHAR(20)` | L28 |
| `vnombre_archivo` | `CHAR(17)` | L29 |
| `vstatusoperacion` | `CHAR(1)` | L30 |
| `vid_cliente` | `CHAR(9)` | L31 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L85 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?uentes_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul`, `?f`, `?uentes_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consuloperctapropias_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consuloperctapropias_manco_bei.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuenta, ro — Rol Operativo, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=5 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_consuloperctapropias_manco_bei(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5),char(4),char(4),char(4),char(16),char(12),char(12),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L5 |
| `cCod_ret` | `CHAR (5)` | L6 |
| `vTransCargo` | `char(4)` | L7 |
| `vTransAbono` | `char(4)` | L8 |
| `vTransSuc` | `char(4)` | L9 |
| `vfoliosuc` | `char(16)` | L10 |
| `vcuenta_origen` | `char(12)` | L11 |
| `vcuenta_destino` | `char(12)` | L12 |
| `vimporte` | `money(14,2)` | L13 |
| `vmoneda` | `char(2)` | L14 |
| `vreferencia` | `char(40)` | L15 |
| `vmontototal` | `money(14,2)` | L16 |
| `vid_usuario` | `INTEGER` | L17 |
| `vid_cat_operacion` | `INTEGER` | L18 |
| `vstatusoperacion` | `CHAR(1)` | L19 |
| `vf_aplicacion` | `CHAR(10)` | L20 |
| `vf_operacion` | `CHAR(10)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L57 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?pias_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul`, `?r`, `?p`, `ro`, `?pias_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consuloperctaterceros_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consuloperctaterceros_manco_bei.sql` |
| **LOC (1er CREATE)** | 62 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuenta, ro — Rol Operativo, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=5 / 11 términos |

### Firma

```sql
CREATE PROCEDURE sp_consuloperctaterceros_manco_bei(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5),CHAR(16),CHAR(20),CHAR(20),MONEY(14,2),CHAR(2),CHAR(40),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vfoliosuc` | `CHAR(16)` | L5 |
| `vcuenta_origen` | `CHAR(20)` | L6 |
| `vcuenta_destino` | `CHAR(20)` | L7 |
| `vimporte` | `MONEY(14,2)` | L8 |
| `vmoneda` | `CHAR(2)` | L9 |
| `vreferencia` | `CHAR(40)` | L10 |
| `vreferenciabe` | `CHAR(40)` | L11 |
| `vnombre_beneficiario` | `VARCHAR(100)` | L12 |
| `vf_aplicacion` | `CHAR(10)` | L13 |
| `vf_operacion` | `CHAR(10)` | L14 |
| `vid_usuario` | `INTEGER` | L15 |
| `vid_cat_operacion` | `INTEGER` | L16 |
| `vstatusoperacion` | `CHAR(1)` | L17 |
| `sql_err` | `INTEGER` | L18 |
| `cCod_ret` | `CHAR (5)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L52 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?terce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul`, `?r`, `?terce`, `ro`, `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consuloperdispersion_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consuloperdispersion_manco_bei.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta dispersión — dispersión de nómina, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consuloperdispersion_manco_bei(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5), CHAR(10), CHAR(20), CHAR(17), CHAR(10), CHAR(20),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L8 |
| `cCod_ret` | `CHAR (5)` | L9 |
| `vf_aplicacion` | `CHAR(10)` | L10 |
| `vcuenta_origen` | `CHAR(20)` | L11 |
| `vnombre_archivo` | `CHAR(17)` | L12 |
| `vf_dispersion` | `CHAR(10)` | L13 |
| `vcte_empresa` | `CHAR(20)` | L14 |
| `vid_empresa` | `CHAR(3)` | L15 |
| `vid_oper` | `CHAR(4)` | L16 |
| `vid_catOperacion` | `INTEGER` | L17 |
| `vid_usuario` | `INTEGER` | L18 |
| `vtamano_archivo` | `INTEGER` | L19 |
| `vtipo_archivo` | `INTEGER` | L20 |
| `vtipo_cuentas` | `INTEGER` | L21 |
| `vtipo_oper` | `SMALLINT` | L22 |
| `vstatus_archivo` | `SMALLINT` | L23 |
| `vmontoTotal` | `MONEY(14,2)` | L24 |
| `vCantidadEmpleados` | `INTEGER` | L25 |
| `vConcepto` | `INTEGER` | L26 |
| `vTipoDispersion` | `INTEGER` | L27 |
| `vcargoDispersion` | `DECIMAL(18,2)` | L28 |
| `vtotalSinComision` | `DECIMAL (18,2)` | L29 |
| `vComsion` | `MONEY(14,2)` | L30 |
| `viva` | `MONEY(14,2)` | L31 |
| `vivaComsion` | `MONEY(14,2)` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L89 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dispersion` | ENTIDAD | dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consuloperdispersion_manco_odp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consuloperdispersion_manco_odp_bei.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta dispersión — dispersión de nómina, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consuloperdispersion_manco_odp_bei(
  pRegIni                      INTEGER
  pIdOperacion                 INTEGER
) RETURNING CHAR(5), CHAR(10),CHAR(10), CHAR(20),MONEY,MONEY,MONEY,MONEY,MONEY,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRegIni` | `INTEGER` | — | — |
| `pIdOperacion` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L6 |
| `cCod_ret` | `CHAR (5)` | L7 |
| `vf_aplicacion` | `CHAR(10)` | L8 |
| `vf_operacion` | `CHAR(10)` | L9 |
| `vcuenta_origen` | `CHAR(20)` | L10 |
| `vImporte` | `MONEY` | L11 |
| `vComision` | `MONEY` | L12 |
| `vIva` | `MONEY` | L13 |
| `vIvaComision` | `MONEY` | L14 |
| `vmontoTotal` | `MONEY` | L15 |
| `vid_usuario` | `INTEGER` | L16 |
| `vConcepto` | `CHAR(30)` | L17 |
| `vAlias` | `CHAR(20)` | L18 |
| `vPrimerNombre` | `CHAR(30)` | L19 |
| `vSegundoNombre` | `CHAR(30)` | L20 |
| `vApellidoPaterno` | `CHAR(30)` | L21 |
| `vApellidoMaterno` | `CHAR(30)` | L22 |
| `vTelefono` | `CHAR(25)` | L23 |
| `vDireccion` | `CHAR(50)` | L24 |
| `vImporteBene` | `MONEY` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L64 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dispersion` | ENTIDAD | dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?_odp_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul`, `?r`, `?_odp_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consuloperpagoservi_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consuloperpagoservi_manco_bei.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta pagos, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_consuloperpagoservi_manco_bei(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5),CHAR(3),CHAR(4),CHAR(4),CHAR(4),CHAR(4),CHAR(4),CHAR(16),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L6 |
| `cCod_ret` | `CHAR (5)` | L7 |
| `vempresa` | `CHAR(3)` | L8 |
| `vsucursal_virtual` | `CHAR(4)` | L9 |
| `vusuario_virtual` | `CHAR(4)` | L10 |
| `vnumtransferenciacargo` | `CHAR(4)` | L11 |
| `vnumtransferenciaabono` | `CHAR(4)` | L12 |
| `vtransaccion_sucursal` | `CHAR(4)` | L13 |
| `vfoliosuc` | `CHAR(16)` | L14 |
| `vcuenta_origen` | `CHAR(20)` | L15 |
| `vcuenta_destino` | `CHAR(20)` | L16 |
| `vimporte` | `MONEY` | L17 |
| `vmoneda` | `CHAR(2)` | L18 |
| `vreferencia` | `CHAR(40)` | L19 |
| `vmontototal` | `MONEY` | L20 |
| `vcategoria` | `CHAR(2)` | L21 |
| `vconvenio` | `CHAR(3)` | L22 |
| `vreftelefono` | `CHAR(20)` | L23 |
| `vrefverificador` | `CHAR(20)` | L24 |
| `vf_operacion` | `CHAR(10)` | L25 |
| `vnombre_beneficiario` | `CHAR(100)` | L26 |
| `vf_aplicacion` | `CHAR(10)` | L27 |
| `vid_usuario` | `INTEGER` | L28 |
| `vid_cat_operacion` | `INTEGER` | L29 |
| `vstatusoperacion` | `CHAR(1)` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L78 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pagos` | ENTIDAD | pagos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?ervi_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul`, `?r`, `?ervi_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consuloperpagoservi_manco_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consuloperpagoservi_manco_bei2.sql` |
| **LOC (1er CREATE)** | 104 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta pagos, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=4 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_consuloperpagoservi_manco_bei2(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5),CHAR(3),CHAR(4),CHAR(4),CHAR(4),CHAR(4),CHAR(4),CHAR(16),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L6 |
| `cCod_ret` | `CHAR (5)` | L7 |
| `vempresa` | `CHAR(3)` | L8 |
| `vsucursal_virtual` | `CHAR(4)` | L9 |
| `vusuario_virtual` | `CHAR(4)` | L10 |
| `vnumtransferenciacargo` | `CHAR(4)` | L11 |
| `vnumtransferenciaabono` | `CHAR(4)` | L12 |
| `vtransaccion_sucursal` | `CHAR(4)` | L13 |
| `vfoliosuc` | `CHAR(16)` | L14 |
| `vcuenta_origen` | `CHAR(20)` | L15 |
| `vcuenta_destino` | `CHAR(20)` | L16 |
| `vimporte` | `MONEY` | L17 |
| `vmoneda` | `CHAR(2)` | L18 |
| `vreferencia` | `CHAR(40)` | L19 |
| `vmontototal` | `MONEY` | L20 |
| `vcategoria` | `CHAR(2)` | L21 |
| `vconvenio` | `CHAR(3)` | L22 |
| `vreftelefono` | `CHAR(20)` | L23 |
| `vrefverificador` | `CHAR(20)` | L24 |
| `vf_operacion` | `CHAR(10)` | L25 |
| `vnombre_beneficiario` | `CHAR(100)` | L26 |
| `vf_aplicacion` | `CHAR(10)` | L27 |
| `vid_usuario` | `INTEGER` | L28 |
| `vid_cat_operacion` | `INTEGER` | L29 |
| `vstatusoperacion` | `CHAR(1)` | L30 |
| *…7 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L93 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pagos` | ENTIDAD | pagos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?ervi_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul`, `?r`, `?ervi_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consuloperspei_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consuloperspei_manco_bei.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consuloperspei_manco_bei(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5), CHAR(3),CHAR(4),CHAR(4),INTEGER,MONEY,CHAR(4),CHAR(16),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vempresa` | `CHAR(3)` | L7 |
| `vsucursal_virtual` | `CHAR(4)` | L8 |
| `vusuario_virtual` | `CHAR(4)` | L9 |
| `vclave_banco` | `INTEGER` | L10 |
| `vimporte` | `MONEY` | L11 |
| `vtransaccion_sucursal` | `CHAR(4)` | L12 |
| `vfoliosuc` | `CHAR(16)` | L13 |
| `vf_operacion` | `DATE` | L14 |
| `vcomision` | `MONEY` | L15 |
| `vivacomision` | `MONEY` | L16 |
| `vnombre_usuario` | `CHAR(40)` | L17 |
| `vcuenta_origen` | `CHAR(20)` | L18 |
| `vrfc` | `CHAR(18)` | L19 |
| `vnombre_beneficiario` | `CHAR(100)` | L20 |
| `vtipo_cuenta_beneficiario` | `CHAR(40)` | L21 |
| `vcuenta_destino` | `CHAR(20)` | L22 |
| `vreferencia` | `CHAR(40)` | L23 |
| `vvalor_iva` | `MONEY` | L24 |
| `vreferencia_cobranza` | `CHAR(40)` | L25 |
| `vbanco_receptor` | `CHAR(100)` | L26 |
| `vmontototal` | `MONEY` | L27 |
| `vf_aplicacion` | `CHAR(10)` | L28 |
| `vid_usuario` | `INTEGER` | L29 |
| `vid_cat_operacion` | `INTEGER` | L30 |
| `vstatusoperacion` | `CHAR(1)` | L31 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L87 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ul` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `spei` | PREFIJO | familia SPEI (pagos interbancarios) | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ul`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_administradores`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_administradores.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta numero de operadores |
| FECHA | Octubre 2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_administradores(
  pNumCliente                  CHAR(9)
  pIdOperacion                 CHAR(8)
) RETURNING char(5),INTEGER, CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdOperacion` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sIdUsuario` | `INTEGER` | L8 |
| `sEmail` | `CHAR(50)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L52 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?istradores` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?istradores` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_archivobatch_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_archivobatch_bei.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta archivo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (proceso batch)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se valida que el archivo a dispersar se encuentra en estatus 6 |
| FECHA | 08/MARZO/2016 |
| FECHA | 12/MARZO/2018 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_archivobatch_bei(
  pIdEvaluacion                INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdEvaluacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L15 |
| `sql_err` | `INTEGER` | L16 |
| `dFechaActual` | `DATE` | L17 |
| `iTotalReg` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L46 |
| `bei_archivos_eval` | `bdibei` | no | SELECT | L51 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `batch` | MODIF | proceso batch (por lotes) | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_avatar_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_avatar_bei.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta avatar y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta el avatar del usuario |
| FECHA | 22/09/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_avatar_bei(
  pIdUsuario                   Integer
  pNumCliente                  CHAR(9)
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `Integer` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L10 |
| `sql_err` | `INTEGER` | L11 |
| `vIdAvatar` | `SMALLINT` | L13 |
| `vNumIntento` | `SMALLINT` | L14 |
| `vFBloqueoTemp` | `DATETIME YEAR to SECOND` | L15 |
| `vTokenVirtual` | `VARCHAR(20)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_avatar` | `bdibei` | no | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET vCodRet='00000';` |  |
| L57 | CÓDIGO_RETORNO | `LET vCodRet='00003';` |  |
| L65 | CÓDIGO_RETORNO | `LET vCodRet='00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `avatar` | ENTIDAD | avatar (foto de perfil del usuario en app) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_claves_envio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_claves_envio.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta clave" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta direccion y telefono de la empresa |
| FECHA | 23/02/2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_claves_envio(
  pNumControl                  CHAR(12)
) RETURNING char(5), char(26), char(26),char(26),char(26),decimal(16,2),char(12)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumControl` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `priNomBen` | `char(26)` | L7 |
| `segNomBen` | `char(26)` | L8 |
| `apellPatBen` | `char(26)` | L9 |
| `apellMatBen` | `char(26)` | L10 |
| `importeTotal` | `decimal(16,2)` | L11 |
| `numControl` | `char(12)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_enviosdineroya` | `bdisac` | ⚠️ sí | SELECT | L55 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `clave` | ENTIDAD | clave | 🔵 CONVENCIÓN | nombre_sp |
| `?s_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_ctafrec_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_ctafrec_bei.sql` |
| **LOC (1er CREATE)** | 183 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuenta y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_ctafrec_bei(
  pNumCte                      CHAR(20)
  pCvePago                     CHAR(2)
  pCveBanco                    CHAR(3)
) RETURNING CHAR(6) as cod_ret, ---cod_ret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pCvePago` | `CHAR(2)` | — | — |
| `pCveBanco` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_cod_ret` | `CHAR(5)` | L30 |
| `iSqlErr` | `INTEGER` | L31 |
| `iSamErr` | `INTEGER` | L32 |
| `vDesErr` | `CHAR(60)` | L33 |
| `v_CodDesc` | `CHAR(50)` | L34 |
| `v_CtaDestino` | `CHAR(20)` | L35 |
| `v_Nombre` | `CHAR(100)` | L36 |
| `v_Banco` | `CHAR(50)` | L37 |
| `v_CompCel` | `CHAR(2)` | L38 |
| `v_NumCel` | `CHAR(10)` | L39 |
| `v_CorreoE` | `CHAR(40)` | L40 |
| `v_CveCuenta` | `CHAR(2)` | L41 |
| `v_DescCta` | `CHAR(20)` | L42 |
| `v_Rfc` | `CHAR(13)` | L43 |
| `v_Canal` | `CHAR(2)` | L44 |
| `v_FechaInsert` | `DATE` | L45 |
| `v_HoraInsert` | `DATETIME HOUR TO SECOND` | L46 |
| `v_FechaHoraInsert` | `DATETIME YEAR TO FRACTION` | L47 |
| `v_MontoMaximo` | `MONEY(16,2)` | L48 |
| `v_CveCaducidad` | `INTEGER` | L49 |
| `v_StatusHabilitado` | `SMALLINT` | L50 |
| `v_descripcion` | `CHAR(40)` | L51 |
| `v_vchrnombrecorto` | `VARCHAR(20)` | L52 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_ctasterceros` | `bdiprog` | ⚠️ sí | SELECT | L146 |
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L169 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L158 | FÓRMULA | `LET v_FechaHoraInsert = ( YEAR(v_FechaInsert) \|\| '-' \|\| MONTH(v_FechaInsert) \|\| '-' \|\| DAY(v` |  |
| L160 | FÓRMULA | `LET v_StatusHabilitado=2; --deshabilitado` |  |
| L162 | FÓRMULA | `LET v_StatusHabilitado=1;  --habilitado` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?f` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_ctas_mod_perfilar_oper_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_ctas_mod_perfilar_oper_historico.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuentas y perfil de usuario (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se consultan las cuentas modificadas durante el alta o modificacion de un operador |
| FECHA | 17/07/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_ctas_mod_perfilar_oper_historico(
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5), INTEGER, CHAR(20)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `vid_historico` | `INTEGER` | L7 |
| `vnum_cta` | `CHAR(20)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_perfil_oper_ctas_mod_historico` | `bdibei` | no | SELECT | L49 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_mod_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?ar_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_mod_`, `?ar_`, `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_cuenta_op_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_cuenta_op_bei.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuenta y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Cuentas de Operador |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_cuenta_op_bei(
  pIdUsuario                   INTEGER
  pRegistro                    Integer
) RETURNING char(5),CHAR(16),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pRegistro` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sNumCta` | `CHAR(16)` | L7 |
| `sIdCatOper` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operaciones` | `bdibei` | no | SELECT | L39 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cuenta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?_op_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_op_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_cuentas_oper_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_cuentas_oper_bei.sql` |
| **LOC (1er CREATE)** | 44 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_cuentas_oper_bei(
  pIdUsuario                   INTEGER
  pRegistro                    Integer
) RETURNING char(5),CHAR(16)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pRegistro` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sNumCta` | `CHAR(16)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operaciones` | `bdibei` | no | SELECT | L31 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_datos_detalle_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_datos_detalle_bei.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta datos, detalle y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consultar los datos para presentar el detalle de un operacion con mancomunidad |
| FECHA | 11/02/2014 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_datos_detalle_bei(
  pIdMancomunidad              Integer
) RETURNING char(5), Integer, CHAR(50),CHAR(50),CHAR(150),CHAR(15),CHAR(50),CHAR(50), CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sIdUsuario` | `INTEGER` | L8 |
| `sIdStatusToken` | `CHAR(50)` | L9 |
| `sTipoMov` | `INTEGER` | L10 |
| `sSolicita` | `CHAR(50)` | L11 |
| `sUsuario` | `CHAR(50)` | L12 |
| `sNombre` | `CHAR(150)` | L13 |
| `sTelCelular` | `CHAR(15)` | L14 |
| `sEmail` | `CHAR(50)` | L15 |
| `sToken` | `CHAR(50)` | L16 |
| `sTokenAux` | `CHAR(50)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L62 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L69 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_datos_detalle_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_datos_detalle_bei2.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta datos, detalle y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | se clona y modifica el spl para mostrar el nombre de la persona en lugar de usuario |
| FECHA | NA |
| DESCRIPCION | Consultar los datos para presentar el detalle de un operacion con mancomunidad |
| FECHA | 11/02/2014 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_datos_detalle_bei2(
  pIdMancomunidad              Integer
) RETURNING char(5), Integer, CHAR(50),CHAR(50),CHAR(150),CHAR(15),CHAR(50),CHAR(50), CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sIdUsuario` | `INTEGER` | L8 |
| `sIdStatusToken` | `CHAR(50)` | L9 |
| `sTipoMov` | `INTEGER` | L10 |
| `sSolicita` | `CHAR(50)` | L11 |
| `sUsuario` | `CHAR(50)` | L12 |
| `sNombre` | `CHAR(150)` | L13 |
| `sTelCelular` | `CHAR(15)` | L14 |
| `sEmail` | `CHAR(50)` | L15 |
| `sToken` | `CHAR(50)` | L16 |
| `sTokenAux` | `CHAR(50)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L71 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L81 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_datos_detalle_renovtoken`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_datos_detalle_renovtoken.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta datos, detalle y token" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | sp que consulta el detalle de la solicitud de renovacion de token mancomunada |
| FECHA | 24/Agosto/2018 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_datos_detalle_renovtoken(
  pIdMancomunidad              INTEGER
) RETURNING CHAR(5), INTEGER, CHAR(50), INTEGER, CHAR(20), CHAR(10), CHAR(10), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L6 |
| `sql_err` | `INTEGER` | L7 |
| `vNumCliente` | `CHAR(9)` | L8 |
| `sTipoMov` | `INTEGER` | L10 |
| `sSolicita` | `CHAR(50)` | L11 |
| `sNumTokens` | `INTEGER` | L12 |
| `sCuentaCargo` | `CHAR(20)` | L13 |
| `sImporte` | `CHAR(10)` | L14 |
| `sFechaOperacion` | `CHAR(10)` | L15 |
| `sIdUsrSolicito` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L59 |
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L63 |
| `bei_tokenexpira` | `bdibei` | no | SELECT | L70 |
| `bei_admin_manco_det_temp` | `bdibei` | no | SELECT | L77 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_renov` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_renov` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_datos_emp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_datos_emp_bei.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta datos, Empresa — empleadora del cliente; vinculada a crédito de nómina y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta direccion y telefono de la empresa |
| FECHA | 23/02/2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_datos_emp_bei(
  pNumCliente                  CHAR(9)
) RETURNING char(5), char(13),char(40),char(60),char(60),char(60)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sTelefono` | `char(13)` | L7 |
| `sCalle` | `char(40)` | L8 |
| `sColonia` | `char(60)` | L9 |
| `sCiudad` | `char(60)` | L10 |
| `sEstado` | `char(60)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_telefonos_actual` | `bdinteg` | ⚠️ sí | SELECT | L52 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L60 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consulta_datos_rep_dop`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_datos_rep_dop.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta datos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Beneficiarios por Dispersion |
| FECHA | 24/02/2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_datos_rep_dop(
  pRegIni                      integer
  pNumCliente                  char(9)
  pTipoDisp                    char(4)
  pFecha                       varchar(10)
  pArchivo                     varchar(12)
) RETURNING char(5),char(20),char(104),char(30),money(12,2),char(12),char(12)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRegIni` | `integer` | — | — |
| `pNumCliente` | `char(9)` | — | — |
| `pTipoDisp` | `char(4)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFecha` | `varchar(10)` | — | — |
| `pArchivo` | `varchar(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L10 |
| `cod_ret` | `char(5)` | L11 |
| `sAlias` | `char(20)` | L12 |
| `sNombre` | `char(104)` | L13 |
| `sConcepto` | `char(100)` | L14 |
| `mImporte` | `money(12,2)` | L15 |
| `sClave` | `char(12)` | L16 |
| `sArchivo` | `CHAR(12)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_dispersiones_odp` | `bdibei` | no | SELECT | L71 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?_dop` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dop` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_datos_rep_dopago`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_datos_rep_dopago.sql` |
| **LOC (1er CREATE)** | 186 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta datos y pago" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| MODIFICACION | Berenice Noriega |
| FECHA | 29-Mayo-2019 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_datos_rep_dopago(
  pRegIni                      integer
  pNumCliente                  CHAR(9)
  pTipoDisp                    CHAR(4)
  pFecha                       DATE
  pArchivo                     CHAR(12)
) RETURNING CHAR(5),CHAR(20),CHAR(104),CHAR(30), MONEY,CHAR(12),CHAR(12), CHAR(2), DATE
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRegIni` | `integer` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pTipoDisp` | `CHAR(4)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFecha` | `DATE` | — | — |
| `pArchivo` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L10 |
| `cod_ret` | `CHAR(5)` | L11 |
| `sAlias` | `CHAR(20)` | L12 |
| `sNombre` | `CHAR(104)` | L13 |
| `sConcepto` | `CHAR(30)` | L14 |
| `mImporte` | `MONEY` | L15 |
| `sClave` | `CHAR(12)` | L16 |
| `sArchivo` | `CHAR(12)` | L17 |
| `vestatus` | `CHAR(2)` | L18 |
| `vfechaStatus` | `DATE` | L19 |
| `vfenvio` | `DATE` | L20 |
| `vfpago` | `DATE` | L21 |
| `vfcance` | `DATE` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_dispersiones_odp` | `bdibei` | no | SELECT | L86 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | 🔵 CONVENCIÓN | nombre_sp |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?_do` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_do` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_datoscontacto_empnet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_datoscontacto_empnet.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta datos y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_datoscontacto_empnet(
  pIdCliente                   CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING CHAR(5), CHAR(100),CHAR(15),CHAR(15),CHAR(35),CHAR(35),CHAR(35)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `sIdUsuario` | `INTEGER` | L7 |
| `sRepresentanteLegal` | `CHAR(100)` | L8 |
| `sTelFijo` | `CHAR(15)` | L9 |
| `sTelCel` | `CHAR(15)` | L10 |
| `sEmail` | `CHAR(35)` | L11 |
| `sEmailAlternativo` | `CHAR(35)` | L12 |
| `sPagInternet` | `CHAR(35)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_empnet` | `bdibei` | no | SELECT | L45 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?o_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o_`, `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_detalle_admon_manco`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_detalle_admon_manco.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta detalle y Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta la administración de montos |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_detalle_admon_manco(
  pIdMancomunidad              INTEGER
  pNum_cliente                 CHAR(9)
) RETURNING char(5), char(2),char(20),DECIMAL(16,2),DECIMAL(16,2),DECIMAL(16,2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |
| `pNum_cliente` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `Monto03` | `DECIMAL(16,2)` | L7 |
| `Monto05` | `DECIMAL(16,2)` | L8 |
| `Monto08` | `DECIMAL(16,2)` | L9 |
| `vIdOper` | `char(4)` | L10 |
| `sIdRest` | `char(2)` | L11 |
| `sNumCta` | `char(20)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L58 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `adm` | ACCION | administración/administrar (abreviación de admin) | 🟡 INFERIDO | nombre_sp |
| `?on_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?on_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_dynatrace`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_dynatrace.sql` |
| **LOC (1er CREATE)** | 35 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_dynatrace(
) RETURNING CHAR(5)
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `codRet` | `CHAR(5)` | L3 |
| `viSqlErr` | `INTEGER` | L4 |
| `fechaMonitoreo` | `CHAR(50)` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_monitoreo_dynatrace` | `bdibei` | no | SELECT | L23 |
| `bei_monitoreo_dynatrace` | `bdibei` | no | UPDATE | L25 |
| `bei_monitoreo_dynatrace` | `bdibei` | no | INSERT | L27 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L7 | CÓDIGO_RETORNO | `LET codRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `?_dynatrace` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_dynatrace` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_errorarchivo_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_errorarchivo_bei.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta error, archivo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta los errores de los archivos de dispersion |
| FECHA | 08/MARZO/2016 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_errorarchivo_bei(
  pIdEvaluacion                INTEGER
  pNoReg                       INTEGER
  pRegIni                      INTEGER
) RETURNING CHAR(5), INTEGER,CHAR(20), SMALLINT, char(500), char(17), char(10), SMALLINT, MONEY, char(12)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdEvaluacion` | `INTEGER` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L13 |
| `sql_err` | `integer` | L14 |
| `iTotalReg` | `INTEGER` | L15 |
| `pSeccion` | `char(17)` | L17 |
| `pRenglon` | `SMALLINT` | L18 |
| `pDescripcion` | `char(500)` | L19 |
| `pNombre` | `char(17)` | L20 |
| `pHoraAlta` | `char(10)` | L21 |
| `pEmpleados` | `SMALLINT` | L22 |
| `pImporte` | `MONEY` | L23 |
| `pFechaApli` | `char(12)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_errores_archivo` | `bdibei` | no | SELECT | L61 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_ideval_archivobatch_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_ideval_archivobatch_bei.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta identificador, archivo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (proceso batch)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se consulta el id_evaluacion del archivo para dispersion en batch |
| FECHA | 27/DIC/2017 |
| FECHA | 11/04/2017 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_ideval_archivobatch_bei(
  p_idOperacionManco           INTEGER
) RETURNING CHAR(5), CHAR(11)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_idOperacionManco` | `INTEGER` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L14 |
| `sql_err` | `INTEGER` | L15 |
| `iTotalReg` | `INTEGER` | L16 |
| `vIdEvaluacion` | `CHAR(11)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos_eval` | `bdibei` | no | SELECT | L44 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?eval_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `batch` | MODIF | proceso batch (por lotes) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?eval_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_limites_beneficiarios_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_limites_beneficiarios_bei.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta beneficiarios y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Limite de Beneficiarios para Dispersion Ordenes Pago Individual |
| FECHA | 12/02/2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_limites_beneficiarios_bei(
  pIdParam                     integer
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdParam` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `iLimiteBeneficiario` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `enet_parametros` | `bdibpi` | ⚠️ sí | SELECT | L40 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_limites_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `beneficiarios` | ENTIDAD | beneficiarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_limites_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_limites_empresas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_limites_empresas.sql` |
| **LOC (1er CREATE)** | 180 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta empresas" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta limites personalisados de las empresas |
| FECHA | 22-ENERO-2015 |
| MODIFICACION | para manejo de SPEI. |
| MODIFICACION | SOLSER |
| FECHA | 30-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_limites_empresas(
  pNum_cliente                 CHAR(9)
  pNum_cta                     CHAR(20)
  pIdOper                      CHAR(4)
  pMonto                       DECIMAL(16,2)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNum_cliente` | `CHAR(9)` | — | — |
| `pNum_cta` | `CHAR(20)` | — | — |
| `pIdOper` | `CHAR(4)` | — | — |
| `pMonto` | `DECIMAL(16,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `vOpeMaxPesos` | `DECIMAL(16,2)` | L7 |
| `vMontoOper` | `DECIMAL(16,2)` | L8 |
| `vMontoOperManco` | `DECIMAL(16,2)` | L9 |
| `vTotalMontoOper` | `DECIMAL(16,2)` | L10 |
| `vIdOper` | `char(4)` | L11 |
| `vIdOperProg` | `char(4)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plimites_empresas` | `bdinteg` | ⚠️ sí | SELECT | L80 |
| `bei_bitacora` | `bdibei` | no | SELECT | L89 |
| `bei_operacionesmancomunadasoperadorresumen` | `bdibei` | no | SELECT | L109 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L118 | FÓRMULA | `LET vTotalMontoOper=NVL(vMontoOper,0)+NVL(vMontoOperManco,0)+pMonto;` | 🔴 MONEY/aritmética financiera |
| L121 | CÓDIGO_RETORNO | `LET vCodRet='00010';` |  |
| L167 | FÓRMULA | `LET vTotalMontoOper=NVL(vMontoOper,0)+NVL(vMontoOperManco,0)+pMonto;` | 🔴 MONEY/aritmética financiera |
| L170 | CÓDIGO_RETORNO | `LET vCodRet='00020';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_limites_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empresas` | ENTIDAD | empresas (nómina empresarial) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_limites_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_oper_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_oper_manco_bei.sql` |
| **LOC (1er CREATE)** | 154 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_oper_manco_bei(
  pNumCliente                  CHAR(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L23 |
| `sql_err` | `INTEGER` | L24 |
| `iTotalReg` | `INTEGER` | L26 |
| `iTotalReg1` | `INTEGER` | L27 |
| `iTotalReg2` | `INTEGER` | L28 |
| `sIdManco` | `INTEGER` | L30 |
| `sIdUsuario` | `INTEGER` | L31 |
| `sUserNom` | `CHAR(50)` | L32 |
| `sIdUserAdmin` | `INTEGER` | L34 |
| `sUserNomAdmin` | `CHAR(50)` | L35 |
| `sIdStatusAut` | `SMALLINT` | L38 |
| `sFechaAut` | `DATE` | L39 |
| `sIdTipoOper` | `SMALLINT` | L41 |
| `sIdTipoMov` | `SMALLINT` | L42 |
| `id_status_token` | `CHAR(5)` | L43 |
| `id_status_user` | `INTEGER` | L44 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L97 |
| `bei_admin_manco_temp_hist` | `bdibei` | no | SELECT | L103 |
| `bei_usuario` | `bdibei` | no | SELECT | L145 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L108 | FÓRMULA | `LET iTotalReg=iTotalReg1+iTotalReg2;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_oper_manco_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_oper_manco_bei2.sql` |
| **LOC (1er CREATE)** | 168 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_oper_manco_bei2(
  pNumCliente                  CHAR(9)
  pIdUser                      INTEGER
  pNoReg                       INTEGER
  pRegIni                      INTEGER
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUser` | `INTEGER` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L21 |
| `sql_err` | `INTEGER` | L22 |
| `iTotalReg` | `INTEGER` | L24 |
| `iTotalReg1` | `INTEGER` | L25 |
| `iTotalReg2` | `INTEGER` | L26 |
| `sIdManco` | `INTEGER` | L28 |
| `sIdUsuario` | `INTEGER` | L29 |
| `sUserNom` | `CHAR(50)` | L30 |
| `sIdUserAdmin` | `INTEGER` | L32 |
| `sUserNomAdmin` | `CHAR(50)` | L33 |
| `sIdStatusAut` | `SMALLINT` | L35 |
| `sFechaAut` | `DATE` | L36 |
| `sIdTipoOper` | `SMALLINT` | L38 |
| `sIdTipoMov` | `SMALLINT` | L39 |
| `id_status_token` | `CHAR(5)` | L40 |
| `id_status_user` | `INTEGER` | L41 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L101 |
| `bei_admin_manco_temp_hist` | `bdibei` | no | SELECT | L107 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L146 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L112 | FÓRMULA | `LET iTotalReg=iTotalReg1+iTotalReg2;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_oper_manco_bei2_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_oper_manco_bei2_historico.sql` |
| **LOC (1er CREATE)** | 178 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_consulta_oper_manco_bei2_historico(
  pNumCliente                  CHAR(9)
  pIdUser                      INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUser` | `INTEGER` | — | — |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L20 |
| `sql_err` | `INTEGER` | L21 |
| `iTotalReg` | `INTEGER` | L23 |
| `iTotalRegT` | `INTEGER` | L24 |
| `iTotalRegH` | `INTEGER` | L25 |
| `sIdManco` | `INTEGER` | L27 |
| `sIdUsuario` | `INTEGER` | L28 |
| `sUserNom` | `CHAR(50)` | L29 |
| `sIdUserAdmin` | `INTEGER` | L31 |
| `sUserNomAdmin` | `CHAR(50)` | L32 |
| `sIdStatusAut` | `SMALLINT` | L34 |
| `sFechaSolicitud` | `DATE` | L35 |
| `sIdTipoOper` | `SMALLINT` | L37 |
| `sIdTipoMov` | `SMALLINT` | L38 |
| `id_status_token` | `CHAR(5)` | L39 |
| `id_status_user` | `INTEGER` | L40 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp_historico` | `bdibei` | no | SELECT | L104 |
| `bei_admin_manco_temp_hist_historico` | `bdibei` | no | SELECT | L109 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L147 |
| `bei_bitacora_admin` | `bdibei` | no | SELECT | L170 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L114 | FÓRMULA | `LET iTotalReg = iTotalRegT + iTotalRegH;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_`, `?2_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_oper_user_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_oper_user_bei.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Operaciones de usuario Para Modificacion de Usuario |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_oper_user_bei(
  pIdPerfil                    INTEGER
  pNoReg                       INTEGER
  pRegIni                      INTEGER
) RETURNING char(5), INTEGER,INTEGER,INTEGER,CHAR(50),DECIMAL(16,2),DECIMAL(16,2),CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdPerfil` | `INTEGER` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `iTotalReg` | `INTEGER` | L8 |
| `sIdOper` | `INTEGER` | L10 |
| `sIdMenuOper` | `INTEGER` | L11 |
| `sNumCta` | `CHAR(20)` | L12 |
| `sMontoMin` | `DECIMAL(16,2)` | L13 |
| `sMontoMax` | `DECIMAL(16,2)` | L14 |
| `sMancomunado` | `BOOLEAN` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operaciones` | `bdibei` | no | SELECT | L71 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_oper_user_detalle_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_oper_user_detalle_bei.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta detalle y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Operaciones de usuario Para Mostrar Detalle de Usuario |
| FECHA | 24/02/2014 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_oper_user_detalle_bei(
  pIdMancomunidad              INTEGER
  pNoReg                       INTEGER
  pRegIni                      INTEGER
) RETURNING CHAR(5), INTEGER, INTEGER, CHAR(50), DECIMAL(16,2), DECIMAL(16,2),CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `INTEGER` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `iTotalReg` | `INTEGER` | L8 |
| `sIdOper` | `INTEGER` | L10 |
| `sIdMenuOper` | `INTEGER` | L11 |
| `sNumCta` | `CHAR(20)` | L12 |
| `sMontoMin` | `DECIMAL(16,2)` | L13 |
| `sMontoMax` | `DECIMAL(16,2)` | L14 |
| `sMancomunado` | `CHAR(5)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_det_temp` | `bdibei` | no | SELECT | L71 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_oper_user_withnumcliente_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_oper_user_withnumcliente_bei.sql` |
| **LOC (1er CREATE)** | 145 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta número de cliente y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Operaciones de usuario para modificacion de usuario filtrado por número de cliente |
| FECHA | 07/10/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_oper_user_withnumcliente_bei(
  pNumCliente                  CHAR(9)
  pIdPerfil                    INTEGER
  pNoReg                       INTEGER
  pRegIni                      INTEGER
) RETURNING char(5), INTEGER,INTEGER,INTEGER,CHAR(50),DECIMAL(16,2),DECIMAL(16,2),CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | `numcliente`=número de cliente | 🔵 CONVENCIÓN |
| `pIdPerfil` | `INTEGER` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L3 |
| `sql_err` | `INTEGER` | L4 |
| `iTotalReg` | `INTEGER` | L5 |
| `iExistePerfilXCliente` | `INTEGER` | L6 |
| `sIdOper` | `INTEGER` | L7 |
| `sIdMenuOper` | `INTEGER` | L8 |
| `sNumCta` | `CHAR(20)` | L9 |
| `sMontoMin` | `DECIMAL(16,2)` | L10 |
| `sMontoMax` | `DECIMAL(16,2)` | L11 |
| `sMancomunado` | `BOOLEAN` | L12 |
| `cMancomunado` | `CHAR(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario_perfil` | `bdibei` | no | SELECT | L98 |
| `bei_operaciones` | `bdibei` | no | SELECT | L115 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_user_with` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `numcliente` | ENTIDAD | número de cliente | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_user_with` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_oper_user_withnumcliente_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_oper_user_withnumcliente_bei_historico.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta número de cliente y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Operaciones de usuario para modificacion de usuario filtrado por número de cliente |
| FECHA | 07/10/2014 |
| FECHA | 22-ENERO-2015 |
| FECHA | 11/Junio/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_oper_user_withnumcliente_bei_historico(
  pNumCliente                  CHAR(9)
  pIdPerfil                    INTEGER
  pNoReg                       INTEGER
  pRegIni                      INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5), INTEGER, INTEGER, INTEGER, CHAR(50), DECIMAL(16,2), DECIMAL(16,2), CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | `numcliente`=número de cliente | 🔵 CONVENCIÓN |
| `pIdPerfil` | `INTEGER` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `iTotalReg` | `INTEGER` | L6 |
| `iExistePerfilXCliente` | `INTEGER` | L7 |
| `sIdOper` | `INTEGER` | L8 |
| `sIdMenuOper` | `INTEGER` | L9 |
| `sNumCta` | `CHAR(20)` | L10 |
| `sMontoMin` | `DECIMAL(16,2)` | L11 |
| `sMontoMax` | `DECIMAL(16,2)` | L12 |
| `sMancomunado` | `BOOLEAN` | L13 |
| `cMancomunado` | `CHAR(5)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario_perfil_historico` | `bdibei` | no | SELECT | L104 |
| `bei_operaciones_historico` | `bdibei` | no | SELECT | L119 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_user_with` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `numcliente` | ENTIDAD | número de cliente | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_user_with` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_operaciones_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_operaciones_bei.sql` |
| **LOC (1er CREATE)** | 119 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta operaciones y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Operaciones para Alta de Usuario |
| FECHA | 24/05/2013 |
| MODIFICACION | se castea la variable de entrada ptipomenu a integer para asignarla a sTipoMenu |
| FECHA | 27 Abril 2015 |
| MODIFICACION | Berenice Noriega Guevara |

### Firma

```sql
CREATE PROCEDURE sp_consulta_operaciones_bei(
  pTipoMenu                    CHAR(5)
  pRegInicial                  INTEGER
) RETURNING char(5), integer,INTEGER,INTEGER,CHAR(50),CHAR(5),CHAR(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pTipoMenu` | `CHAR(5)` | — | — |
| `pRegInicial` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `iTotalReg` | `INTEGER` | L8 |
| `sid_cat_oper` | `INTEGER` | L10 |
| `sid_cat_padre` | `INTEGER` | L11 |
| `sdesc_oper` | `CHAR(50)` | L12 |
| `srango` | `BOOLEAN` | L13 |
| `smancomunidad` | `BOOLEAN` | L14 |
| `crango` | `CHAR(5)` | L15 |
| `cmancomunidad` | `CHAR(5)` | L16 |
| `sidmenuoper` | `INTEGER` | L17 |
| `sTipoMenu` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_menu_oper` | `bdibei` | no | SELECT | L74 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_operaciones_bei_pba`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_operaciones_bei_pba.sql` |
| **LOC (1er CREATE)** | 95 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta operaciones y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (PBA — sufijo de SPs para Pruebas; confirmado por SME)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Operaciones para Alta de Usuario |
| FECHA | 24/05/2013 |
| MODIFICACION | se castea la variable de entrada ptipomenu a integer para asignarla a sTipoMenu |
| FECHA | 27 Abril 2015 |
| MODIFICACION | Berenice Noriega Guevara |

### Firma

```sql
CREATE PROCEDURE sp_consulta_operaciones_bei_pba(
  pTipoMenu                    CHAR(5)
  pRegInicial                  INTEGER
) RETURNING char(5), integer,INTEGER,INTEGER,CHAR(50),CHAR(5),CHAR(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pTipoMenu` | `CHAR(5)` | — | — |
| `pRegInicial` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `iTotalReg` | `INTEGER` | L8 |
| `sid_cat_oper` | `INTEGER` | L10 |
| `sid_cat_padre` | `INTEGER` | L11 |
| `sdesc_oper` | `CHAR(50)` | L12 |
| `srango` | `CHAR(5)` | L13 |
| `smancomunidad` | `CHAR(5)` | L14 |
| `sidmenuoper` | `INTEGER` | L15 |
| `sTipoMenu` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_menu_oper` | `bdibei` | no | SELECT | L62 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `pba` | MODIF | PBA — sufijo de SPs para Pruebas; confirmado por SME (Guerra | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consulta_operadores`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_operadores.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta numero de operadores |
| FECHA | Octubre 2014 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_operadores(
  pNumCliente                  CHAR(9)
  pIdOperacion                 CHAR(8)
) RETURNING char(5),INTEGER, CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdOperacion` | `CHAR(8)` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sIdUsuario` | `INTEGER` | L8 |
| `sEmail` | `CHAR(50)` | L9 |
| `NumCta` | `CHAR(20)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L51 |
| `bei_usuario` | `bdibei` | no | SELECT | L57 |
| `bei_mancomunidad` | `bdibei` | no | SELECT | L65 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?radores` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?radores` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_perfil_ctas_frecuentes_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_perfil_ctas_frecuentes_bei.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta perfil de usuario, cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta si usuario cuenta con permiso para cuentas frecuentes (mancomunidad) |
| FECHA | 29/Agosto/2016 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_perfil_ctas_frecuentes_bei(
  pNumCliente                  CHAR(9)
  pIdPerfil                    INTEGER
  pIdUsuario                   INTEGER
) RETURNING CHAR(5), CHAR(1), CHAR(1)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdPerfil` | `INTEGER` | `perfil`=perfil de usuario | 🔵 CONVENCIÓN |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L11 |
| `cod_ret` | `CHAR(5)` | L12 |
| `vNum_cta` | `CHAR(8)` | L14 |
| `vIdMenuOper` | `INTEGER` | L15 |
| `vMancomunidad` | `CHAR(1)` | L16 |
| `vAutoriza` | `CHAR(1)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad` | `bdibei` | no | SELECT | L31 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?_f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?uentes_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_f`, `?uentes_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_perfil_ctas_frecuentes_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_perfil_ctas_frecuentes_bei2.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta perfil de usuario, cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta si usuario cuenta con permiso para cuentas frecuentes (mancomunidad) |
| FECHA | 29/Agosto/2016 |
| DESCRIPCION | Consulta si usuario cuenta con permiso para cuentas frecuentes (mancomunidad) |
| FECHA | 18/04/2017 |
| MODIFICACION | Se adecua la excepción para cuando son alta de usuarios |

### Firma

```sql
CREATE PROCEDURE sp_consulta_perfil_ctas_frecuentes_bei2(
  pNumCliente                  CHAR(9)
  pIdPerfil                    INTEGER
  pIdUsuario                   INTEGER
  pIdMancomunidad              INTEGER
) RETURNING CHAR(5), CHAR(1), CHAR(1)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdPerfil` | `INTEGER` | `perfil`=perfil de usuario | 🔵 CONVENCIÓN |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdMancomunidad` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L19 |
| `cod_ret` | `CHAR(5)` | L20 |
| `vNum_cta` | `CHAR(8)` | L22 |
| `vIdMenuOper` | `INTEGER` | L23 |
| `vMancomunidad` | `CHAR(1)` | L24 |
| `vAutoriza` | `CHAR(1)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad` | `bdibei` | no | SELECT | L43 |
| `bei_admin_manco_det_temp` | `bdibei` | no | SELECT | L65 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?_f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?uentes_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_f`, `?uentes_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_perfil_ctas_frecuentes_bei2_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_perfil_ctas_frecuentes_bei2_historico.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta perfil de usuario, cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 10 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta si usuario cuenta con permiso para cuentas frecuentes (mancomunidad) |
| FECHA | 29/Agosto/2016 |
| DESCRIPCION | Consulta si usuario cuenta con permiso para cuentas frecuentes (mancomunidad) |
| FECHA | 18/04/2017 |
| MODIFICACION | Se adecua la excepción para cuando son alta de usuarios |
| FECHA | 05/06/2018 |
| MODIFICACION | Se ajusta para cumplir las politicas de BD |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_perfil_ctas_frecuentes_bei2_historico(
  pNumCliente                  CHAR(9)
  pIdPerfil                    INTEGER
  pIdUsuario                   INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5), CHAR(1), CHAR(1)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdPerfil` | `INTEGER` | `perfil`=perfil de usuario | 🔵 CONVENCIÓN |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L33 |
| `cod_ret` | `CHAR(5)` | L34 |
| `vNum_cta` | `CHAR(8)` | L37 |
| `vIdMenuOper` | `INTEGER` | L38 |
| `vMancomunidad` | `CHAR(1)` | L39 |
| `vAutoriza` | `CHAR(1)` | L40 |
| `vMancomunidadTem` | `BOOLEAN` | L42 |
| `vAutorizaTem` | `BOOLEAN` | L43 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad_historico` | `bdibei` | no | SELECT | L72 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?_f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?uentes_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_f`, `?uentes_`, `?2_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_token_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_token_bei.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS Tokens EXISTENTES |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_token_bei(
  pNumCliente                  char(9)
) RETURNING char(5),   INTEGER ,char (9) ,CHAR(10)   , INTEGER ,CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `iTotalReg` | `INTEGER` | L8 |
| `sIdUsuario` | `INTEGER` | L10 |
| `sNumCliente` | `CHAR (9)` | L11 |
| `sNsToken` | `CHAR(10)` | L12 |
| `sIdStatusToken` | `INTEGER` | L13 |
| `sNomUser` | `CHAR(50)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L51 |
| `bei_servicio` | `bdibei` | no | SELECT | L54 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_token_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_token_bei2.sql` |
| **LOC (1er CREATE)** | 91 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | se clona y modifica el spl para mostrar el nombre de la persona en lugar de usuario |
| FECHA | NA |
| DESCRIPCION | OBTIENE LOS DATOS DE LOS Tokens EXISTENTES |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_token_bei2(
  pNumCliente                  char(9)
) RETURNING char(5),   INTEGER ,char (9) ,CHAR(10)   , INTEGER ,CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `iTotalReg` | `INTEGER` | L8 |
| `sIdUsuario` | `INTEGER` | L10 |
| `sNumCliente` | `CHAR (9)` | L11 |
| `sNsToken` | `CHAR(10)` | L12 |
| `sIdStatusToken` | `INTEGER` | L13 |
| `sNomUser` | `CHAR(50)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L62 |
| `bei_servicio` | `bdibei` | no | SELECT | L65 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_tokensnoasociados_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_tokensnoasociados_bei.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta token, Sistema Operativo Central, OS — Originación de Solicitudes y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS TOKENS EXISTENTES SIN ASOCIAR |
| FECHA | 24/05/2013 |
| MODIFICACION | SE MODIFICA PARA QUE CONSULTE LOS TOKESN DISPONIBLES |
| FECHA | 04 Septiembre 2013 3:18 PM |
| MODIFICACION | SE MODIFICA PARA OMITA LOS TOKEN QUE TIENEN ESTATUS 199-CANCELADOS |
| FECHA | 21 octubre 2014 |
| MODIFICACION | Se agrega validci para estatus 220-CANCELADOS por renovaci |
| FECHA | 02 octubre 2018 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_tokensnoasociados_bei(
  pNumCliente                  char(9)
) RETURNING char(5), CHAR(10)   , INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sNsToken` | `CHAR(10)` | L9 |
| `sIdStatusToken` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L79 |
| `bei_token` | `bdibei` | no | SELECT | L82 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?snoa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?iad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?snoa`, `?iad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_user_token_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_user_token_bei.sql` |
| **LOC (1er CREATE)** | 78 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS Y TOKENS EXISTENTES |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_user_token_bei(
  pNumCliente                  char(9)
) RETURNING char(5), INTEGER ,  INTEGER ,CHAR (9) ,CHAR(10)   , INTEGER ,CHAR(50),CHAR(150)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `iTotalReg` | `INTEGER` | L8 |
| `sIdUsuario` | `INTEGER` | L10 |
| `sNumCliente` | `CHAR (9)` | L11 |
| `sNsToken` | `CHAR(10)` | L12 |
| `sIdStatusToken` | `INTEGER` | L13 |
| `sNomUser` | `CHAR(50)` | L14 |
| `sNombre` | `CHAR(150)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L53 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consulta_usuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_usuario_bei.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Verifica si Nombre de Usuario ya Existe en la base de Datos |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_usuario_bei(
  pIdUsuario                   Integer
  pNumCliente                  CHAR(9)
) RETURNING char(5), Integer,CHAR(50),CHAR(50),CHAR(150),CHAR(15),SMALLINT,CHAR(50),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `Integer` | `usuario`=usuario | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sUsuario` | `CHAR(50)` | L9 |
| `sPass` | `CHAR(50)` | L10 |
| `sNombre` | `CHAR(150)` | L11 |
| `sTelCelular` | `CHAR(15)` | L12 |
| `sCiaCel` | `SMALLINT` | L13 |
| `sEmail` | `CHAR(50)` | L14 |
| `sIdPerfil` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L61 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_usuario_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_usuario_bei_historico.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se clona el sp sp_consulta_usuario_bei para la consulta del detalle |
| FECHA | 11/Junio/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_usuario_bei_historico(
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5), INTEGER, CHAR(50), CHAR(50), CHAR(150), CHAR(15), SMALLINT, CHAR(50), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L6 |
| `sql_err` | `INTEGER` | L7 |
| `sUsuario` | `CHAR(50)` | L9 |
| `sPass` | `CHAR(50)` | L10 |
| `sNombre` | `CHAR(150)` | L11 |
| `sTelCelular` | `CHAR(15)` | L12 |
| `sCiaCel` | `SMALLINT` | L13 |
| `sEmail` | `CHAR(50)` | L14 |
| `sIdPerfil` | `INTEGER` | L15 |
| `sIdUsuario` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario_historico` | `bdibei` | no | SELECT | L59 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_usuario_detalle_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_usuario_detalle_bei.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuario, detalle y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Verifica si Nombre de Usuario ya Existe en la base de Datos |
| FECHA | 21/02/2014 |

### Firma

```sql
CREATE PROCEDURE sp_consulta_usuario_detalle_bei(
  pIdMancomunidad              Integer
) RETURNING char(5), Integer, Integer,CHAR(50),CHAR(50),CHAR(150),CHAR(15),SMALLINT,CHAR(50),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sUsuario` | `CHAR(50)` | L9 |
| `sPass` | `CHAR(50)` | L10 |
| `sNombre` | `CHAR(150)` | L11 |
| `sTelCelular` | `CHAR(15)` | L12 |
| `sCiaCel` | `SMALLINT` | L13 |
| `sEmail` | `CHAR(50)` | L14 |
| `sIdPerfil` | `INTEGER` | L15 |
| `sIdAdminManco` | `INTEGER` | L16 |
| `sTipoMov` | `INTEGER` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L62 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consulta_usuarios_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consulta_usuarios_bei.sql` |
| **LOC (1er CREATE)** | 176 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuarios y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR EXISTENTES |
| FECHA | 24/05/2013 |
| MODIFICACION | CONSULTA EL BLOQUEO TEMPORAL EN LA TABLA DE AVATAR |
| FECHA | 22-ENERO-2015 |
| MODIFICACION | SOLSER |

### Firma

```sql
CREATE PROCEDURE sp_consulta_usuarios_bei(
  pNumCliente                  char(9)
  pNoReg                       integer
  pRegIni                      integer
  pUserNom                     CHAR(50)
  pNombrem                     CHAR(150)
  pIdStatus                    smallint
  pIdTipoUser                  smallint
) RETURNING char(5),   integer ,char (9) ,integer   , char(50),smallint, char(150),smallint,  CHAR(15),smallint, CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |
| `pUserNom` | `CHAR(50)` | — | — |
| `pNombrem` | `CHAR(150)` | — | — |
| `pIdStatus` | `smallint` | — | — |
| `pIdTipoUser` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L12 |
| `sql_err` | `integer` | L13 |
| `iTotalReg` | `integer` | L15 |
| `sIdUsuario` | `integer` | L17 |
| `sNumCliente` | `char (9)` | L18 |
| `sUserNom` | `CHAR(50)` | L19 |
| `sNombre` | `CHAR(150)` | L20 |
| `sTelCelular` | `CHAR(15)` | L21 |
| `sCiaCel` | `smallint` | L22 |
| `sEmail` | `CHAR(50)` | L23 |
| `sIdStatus` | `smallint` | L24 |
| `sIdTipoUser` | `smallint` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L68 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consultaaltaempleado_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultaaltaempleado_manco_bei.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta empleado, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaaltaempleado_manco_bei(
  pIdOperacion                 INTEGER
) RETURNING CHAR(5), INTEGER, CHAR(100), CHAR(100), CHAR(100),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L6 |
| `cCod_ret` | `CHAR (5)` | L7 |
| `vnumero_empleado` | `INTEGER` | L8 |
| `vnombre_empleado` | `CHAR(100)` | L9 |
| `vapellidos_pat_empleado` | `CHAR(100)` | L10 |
| `vapellidos_mat_empleado` | `CHAR(100)` | L11 |
| `vcuenta_destino` | `CHAR(12)` | L12 |
| `vclave_banco` | `INTEGER` | L13 |
| `vid_usuario` | `INTEGER` | L14 |
| `vnum_cliente` | `CHAR(9)` | L15 |
| `vf_aplicacion` | `DATE` | L16 |
| `vf_operacion` | `DATE` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L48 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `empleado` | ENTIDAD | empleado | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_consultabitacora_emp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultabitacora_emp.sql` |
| **LOC (1er CREATE)** | 515 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta bitácora y Empresa — empleadora del cliente; vinculada a crédito de nómina" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtener el detalle de las operaciones para reimpresion de comprobante |
| FECHA | 28/Sept/2017 |

### Firma

```sql
CREATE PROCEDURE sp_consultabitacora_emp(
  idUsuario                    CHAR(4)
  pIdOperacion                 CHAR(4)
  pIdOperacionFch              CHAR(4)
  pcvePago                     CHAR(4)
  pTipoConsulta                CHAR(1)
  pFechaConsulta               CHAR(10)
  pPaginacion                  INT
) RETURNING CHAR(5) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `idUsuario` | `CHAR(4)` | — | — |
| `pIdOperacion` | `CHAR(4)` | — | — |
| `pIdOperacionFch` | `CHAR(4)` | — | — |
| `pcvePago` | `CHAR(4)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pFechaConsulta` | `CHAR(10)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pPaginacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L39 |
| `cod_ret` | `CHAR (5)` | L40 |
| `vfecha_periodo_base` | `DATE` | L43 |
| `vfecha_periodo_inicio` | `DATE` | L44 |
| `vfecha_periodo_fin` | `DATE` | L45 |
| `vfecha_format` | `CHAR(8)` | L46 |
| `vfecha_oper` | `DATETIME year to second` | L49 |
| `vfecha_apli` | `DATE` | L50 |
| `vcuenta_origen` | `CHAR(12)` | L51 |
| `vdestino` | `CHAR(20)` | L52 |
| `vmonto` | `CHAR(16)` | L53 |
| `vsec_transaccion` | `CHAR(16)` | L54 |
| `vcgenerico1` | `CHAR(100)` | L55 |
| `vcgenerico2` | `CHAR(200)` | L56 |
| `vcgenerico3` | `CHAR(60)` | L57 |
| `vcgenerico4` | `CHAR(60)` | L58 |
| `vcgenerico5` | `CHAR(60)` | L59 |
| `vcgenerico6` | `CHAR(100)` | L60 |
| `vreferencia` | `CHAR(100)` | L61 |
| `vfolio` | `CHAR(16)` | L62 |
| `vid_operacion` | `CHAR(4)` | L63 |
| `vnum_reg_xpag` | `INTEGER` | L66 |
| `vtotal_registros` | `INTEGER` | L67 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_bitacora` | `bdibei` | no | SELECT | L181 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L95 | FÓRMULA | `LET pPaginacion = pPaginacion * vnum_reg_xpag;` |  |
| L123 | VALIDACIÓN_NULL | `IF (idUsuario IS NULL OR pIdOperacion IS NULL OR pTipoConsulta IS NULL) THEN` |  |
| L162 | FÓRMULA | `LET vfecha_periodo_inicio = ADD_MONTHS(vfecha_periodo_base, -1);` |  |
| L166 | FÓRMULA | `LET vfecha_periodo_inicio = ADD_MONTHS(vfecha_periodo_base, -2);` |  |
| L170 | FÓRMULA | `LET vfecha_periodo_inicio = ADD_MONTHS(vfecha_periodo_base, -3);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `emp` | ENTIDAD | Empresa — empleadora del cliente; vinculada a crédito de nóm | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_consultabitacora_moves_admin`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultabitacora_moves_admin.sql` |
| **LOC (1er CREATE)** | 286 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta bitácora, movimiento y Administrador — rol de usuario con privilegios administrativos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtener la bitacora de las operaciones realizadas por el usuario Administradior |
| FECHA | 04/05/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_consultabitacora_moves_admin(
  pIdAdmin                     CHAR(4)
  pRangoConsulta               CHAR(1)
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdAdmin` | `CHAR(4)` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | ✅ CÓDIGO |
| `pRangoConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L40 |
| `cod_ret` | `CHAR(5)` | L41 |
| `vfecha_periodo_base` | `DATE` | L43 |
| `vfecha_periodo_inicio` | `DATE` | L44 |
| `vfecha_periodo_fin` | `DATE` | L45 |
| `vfecha_format` | `CHAR(8)` | L46 |
| `vid_bitacora_admin` | `INTEGER` | L48 |
| `vfecha_oper` | `DATETIME year to second` | L49 |
| `vfecha_aplic` | `DATE` | L50 |
| `vnum_cliente` | `CHAR(9)` | L51 |
| `vip_usuario` | `CHAR(15)` | L52 |
| `vid_admin` | `INTEGER` | L53 |
| `vid_operador` | `INTEGER` | L54 |
| `vcuenta_origen` | `CHAR(12)` | L55 |
| `vdestino` | `CHAR(12)` | L56 |
| `vtoken` | `CHAR(9)` | L57 |
| `vid_cat_oper` | `INTEGER` | L58 |
| `vnombre_corto` | `CHAR(50)` | L59 |
| `vcgenerico1` | `CHAR(50)` | L60 |
| `vcgenerico2` | `CHAR(50)` | L61 |
| `vcgenerico3` | `CHAR(50)` | L62 |
| `vcgenerico4` | `CHAR(50)` | L63 |
| `vcgenerico5` | `CHAR(50)` | L64 |
| `vid_operacion` | `CHAR(4)` | L65 |
| `vnum_reg_xpag` | `INTEGER` | L67 |
| *…1 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_bitacora_admin` | `bdibei` | no | SELECT | L187 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L96 | FÓRMULA | `LET pPaginacion = pPaginacion * vnum_reg_xpag;` |  |
| L129 | VALIDACIÓN_NULL | `IF (pIdAdmin IS NULL OR pRangoConsulta IS NULL) THEN` |  |
| L166 | FÓRMULA | `LET vfecha_periodo_inicio = EXTEND(MDY(MONTH(TODAY),DAY(TODAY),YEAR(TODAY))-3);` |  |
| L170 | FÓRMULA | `LET vfecha_periodo_inicio = EXTEND(MDY(MONTH(TODAY),DAY(TODAY),YEAR(TODAY))-7);` |  |
| L174 | FÓRMULA | `LET vfecha_periodo_inicio =  EXTEND(MDY(MONTH(TODAY),DAY(TODAY),YEAR(TODAY))-30);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `?es_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultabitacora_reimp_comp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultabitacora_reimp_comp.sql` |
| **LOC (1er CREATE)** | 673 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta bitácora y Impago — pago vencido o fallido; confirmado: n_impagos_consec (complemento)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtener el detalle de las operaciones para reimpresion de comprobante |
| FECHA | 28/Sept/2017 |
| MODIFICACION | 23/Jul/2018 (Cambio de tamaño de valores de retorno para campos genericos) |

### Firma

```sql
CREATE PROCEDURE sp_consultabitacora_reimp_comp(
  idUsuario                    CHAR(4)
  pIdOperacion                 CHAR(4)
  pIdOperacionFch              CHAR(4)
  pcvePago                     CHAR(4)
  pTipoConsulta                CHAR(1)
  pFechaConsulta               CHAR(10)
  pPaginacion                  INT
) RETURNING CHAR(5) AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `idUsuario` | `CHAR(4)` | — | — |
| `pIdOperacion` | `CHAR(4)` | — | — |
| `pIdOperacionFch` | `CHAR(4)` | — | — |
| `pcvePago` | `CHAR(4)` | — | — |
| `pTipoConsulta` | `CHAR(1)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pFechaConsulta` | `CHAR(10)` | `consulta`=consulta / lee | ✅ CÓDIGO |
| `pPaginacion` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L44 |
| `cod_ret` | `CHAR (5)` | L45 |
| `vfecha_periodo_base` | `DATE` | L48 |
| `vfecha_periodo_inicio` | `DATE` | L49 |
| `vfecha_periodo_fin` | `DATE` | L50 |
| `vfecha_format` | `CHAR(8)` | L51 |
| `vfecha_oper` | `DATETIME year to second` | L54 |
| `vfecha_apli` | `DATE` | L55 |
| `vcuenta_origen` | `CHAR(12)` | L56 |
| `vdestino` | `CHAR(20)` | L57 |
| `vmonto` | `CHAR(16)` | L58 |
| `vsec_transaccion` | `CHAR(16)` | L59 |
| `vcgenerico1` | `CHAR(40)` | L60 |
| `vcgenerico2` | `CHAR(40)` | L61 |
| `vcgenerico3` | `CHAR(40)` | L62 |
| `vcgenerico4` | `CHAR(40)` | L63 |
| `vcgenerico5` | `CHAR(40)` | L64 |
| `vcgenerico6` | `CHAR(40)` | L65 |
| `vcgenerico7` | `CHAR(40)` | L66 |
| `vcgenerico8` | `CHAR(40)` | L67 |
| `vcgenerico9` | `CHAR(40)` | L68 |
| `vcgenerico10` | `CHAR(40)` | L69 |
| `vreferencia` | `CHAR(40)` | L70 |
| `vfolio` | `CHAR(16)` | L71 |
| `vid_operacion` | `CHAR(4)` | L72 |
| *…4 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_bitacora` | `bdibei` | no | SELECT | L210 |
| `bei_bitacora_historial` | `bdibei` | no | SELECT | L217 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L117 | FÓRMULA | `LET pPaginacion = pPaginacion * vnum_reg_xpag;` |  |
| L151 | VALIDACIÓN_NULL | `IF (idUsuario IS NULL OR pIdOperacion IS NULL OR pTipoConsulta IS NULL) THEN` |  |
| L194 | FÓRMULA | `LET vfecha_periodo_inicio = ADD_MONTHS(vfecha_periodo_base, -1);` |  |
| L198 | FÓRMULA | `LET vfecha_periodo_inicio = ADD_MONTHS(vfecha_periodo_base, -2);` |  |
| L202 | FÓRMULA | `LET vfecha_periodo_inicio = ADD_MONTHS(vfecha_periodo_base, -3);` |  |
| L223 | FÓRMULA | `LET vtotal_registros= vtotal_registros1 + vtotal_registros2;` |  |
| L329 | FÓRMULA | `LET vtotal_registros= vtotal_registros1 + vtotal_registros2;` |  |
| L437 | FÓRMULA | `LET vtotal_registros= vtotal_registros1 + vtotal_registros2;` |  |
| L554 | FÓRMULA | `LET vtotal_registros= vtotal_registros1 + vtotal_registros2;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `imp` | ENTIDAD | Impago — pago vencido o fallido; confirmado: n_impagos_conse | 🔵 CONVENCIÓN | nombre_sp |
| `comp` | MODIF | complemento | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_re` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaoperacionesmancomunadas_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultaoperacionesmancomunadas_bei.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta operaciones, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | CONSULTA DE OPERACIONES MANCOMUNADAS |
| FECHA | 26/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_consultaoperacionesmancomunadas_bei(
  pNumCte                      CHAR(20)
  pIdUsuario                   INTEGER
  pRegInical                   INTEGER
) RETURNING CHAR(5) AS vCodRet, CHAR(50) AS cOperacionDesc, CHAR(150) AS cOperador,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pRegInical` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L14 |
| `sql_err` | `INT` | L15 |
| `cOperacionDesc` | `CHAR(50)` | L16 |
| `cOperador` | `CHAR(150)` | L17 |
| `dFechaOperacion` | `DATE` | L18 |
| `dFechaAplicacion` | `DATE` | L19 |
| `cCuentaOrigen` | `CHAR(20)` | L20 |
| `mMonto` | `MONEY` | L21 |
| `cEstatusOperacion` | `CHAR(1)` | L22 |
| `cId_operacion` | `INTEGER` | L23 |
| `cIdCatOper` | `INTEGER` | L24 |
| `iTotalReg` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperadorresumen` | `bdibei` | no | SELECT | L57 |
| `bei_mancomunidad` | `bdibei` | no | SELECT | L61 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET vCodRet = '00000';` |  |
| L76 | CÓDIGO_RETORNO | `LET vCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?munadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?munadas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaoperacionespropias_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultaoperacionespropias_bei.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta operaciones, ro — Rol Operativo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | COSULTA DE OPERACIONES MANCOMUNADAS |
| FECHA | 26/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_consultaoperacionespropias_bei(
  pIdUsuario                   INTEGER
  pRegInical                   INTEGER
) RETURNING CHAR (5) AS cCod_ret, CHAR(50) AS cOperacionDesc, CHAR(150) AS cOperador,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pRegInical` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INT` | L14 |
| `cCod_ret` | `CHAR (5)` | L15 |
| `cOperacionDesc` | `CHAR(50)` | L16 |
| `cOperador` | `CHAR(150)` | L17 |
| `dFechaOperacion` | `DATE` | L18 |
| `dFechaAplicacion` | `DATE` | L19 |
| `cCuentaOrigen` | `CHAR(20)` | L20 |
| `mMonto` | `MONEY` | L21 |
| `cEstatusOperacion` | `CHAR(1)` | L22 |
| `cId_operacion` | `INTEGER` | L23 |
| `cIdCatOper` | `INTEGER` | L24 |
| `iTotalReg` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperadorresumen` | `bdibei` | no | SELECT | L52 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?pias_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?pias_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultaparametro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultaparametro.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ✅ fan_in=0 / fan_out=8 |
| **Deps concatenadas** | 18 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta parámetro" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultaparametro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTabla                       CHAR(3)
  pSistemaBase                 CHAR(2)
  pCampo                       CHAR(15)
  pWhere                       CHAR(15)
  pParametro                   CHAR(15)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTabla` | `CHAR(3)` | — | — |
| `pSistemaBase` | `CHAR(2)` | — | — |
| `pCampo` | `CHAR(15)` | — | — |
| `pWhere` | `CHAR(15)` | — | — |
| `pParametro` | `CHAR(15)` | `parametro`=parámetro | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iCodRetSp` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `cValor` | `CHAR(20)` | L9 |
| `cQuery` | `CHAR(500)` | L10 |
| `cBaseDatos` | `CHAR(12)` | L11 |
| `cTabla` | `CHAR(40)` | L12 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L72 |
| `sp_actualizafolio` | `bdibei` | no | L134 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L139 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP ';` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | 🔵 CONVENCIÓN | nombre_sp |
| `parametro` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_consultarctepmempresanet_clon`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultarctepmempresanet_clon.sql` |
| **LOC (1er CREATE)** | 374 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consultar cliente, empresa y [polisemia] Clon de SP" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_consultarctepmempresanet_clon(
  pNumcte                      CHAR(20)
) RETURNING CHAR(6) 		AS COD_RET,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumcte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L31 |
| `cCodRet` | `CHAR(6)` | L32 |
| `cNumcte` | `CHAR(20)` | L33 |
| `cEsFisica` | `CHAR(1)` | L34 |
| `cTpoPersona` | `CHAR(2)` | L35 |
| `sNumAdmon` | `SMALLINT` | L36 |
| `cFolioactiva` | `CHAR(12)` | L37 |
| `cFolioActiva1` | `CHAR(12)` | L38 |
| `cFolioActiva2` | `CHAR(12)` | L39 |
| `iFolContrato1` | `INTEGER` | L40 |
| `iFolContrato2` | `INTEGER` | L41 |
| `cNumIdent` | `CHAR(2)` | L42 |
| `cCodIndentif` | `CHAR(30)` | L43 |
| `cDescripcion` | `CHAR(50)` | L44 |
| `cCodIndentifusu1` | `CHAR(30)` | L45 |
| `cCodIndentifusu2` | `CHAR(30)` | L46 |
| `cDescUsu1` | `CHAR(50)` | L47 |
| `cDescUsu2` | `CHAR(50)` | L48 |
| `cNumIdentUsuar1` | `CHAR(2)` | L49 |
| `cNumIdentUsuar2` | `CHAR(2)` | L50 |
| `sNumToken` | `SMALLINT` | L51 |
| `sFolio` | `INTEGER` | L52 |
| `cUsuarioAut1Nombre1` | `CHAR(50)` | L53 |
| `cUsuarioAut1Nombre2` | `CHAR(50)` | L54 |
| `cUsuarioAut1ApellPat` | `CHAR(50)` | L55 |
| *…13 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_contratacion` | `bdibei` | no | SELECT | L187 |
| `bei_servicio` | `bdibei` | no | SELECT | L222 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L228 | FÓRMULA | `LET sNumAdmon = sNumAdmon + 1; --LLEVA UN CONTEO PARA ASIGNAR ADMINISTRADOR 1 O 2.` |  |
| L299 | FÓRMULA | `LET sNumAdmon = sNumAdmon + 1; --LLEVA UN CONTEO PARA ASIGNAR ADMINISTRADOR 1 O 2.` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consultar` | ACCION | consultar | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empresa` | ENTIDAD | empresa (entidad bancaria) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?net_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `clon` | ENTIDAD | [polisemia] Clon de SP (réplica funcional para variante de e | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pm`, `?net_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultasolicitudes_renovtkn_manco`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultasolicitudes_renovtkn_manco.sql` |
| **LOC (1er CREATE)** | 87 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta solicitudes y Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | se consultan las solicitudes de renovacion de token pendientes de autorizar |
| FECHA | 22/Agosto/2018 |

### Firma

```sql
CREATE PROCEDURE sp_consultasolicitudes_renovtkn_manco(
  pNumCliente                  CHAR(9)
  pIdUsuarioSesion             INTEGER
  pNoReg                       INTEGER
  pRegIni                      INTEGER
) RETURNING CHAR(5), INTEGER, INTEGER, INTEGER, INTEGER, CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuarioSesion` | `INTEGER` | — | — |
| `pNoReg` | `INTEGER` | — | — |
| `pRegIni` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L6 |
| `sql_err` | `INTEGER` | L7 |
| `iTotalReg` | `INTEGER` | L9 |
| `sNumTokensRenov` | `INTEGER` | L10 |
| `sIdMancomunidad` | `INTEGER` | L11 |
| `sIdUsuarioSolicito` | `INTEGER` | L12 |
| `sNomUsuarioSolicito` | `CHAR(50)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L55 |
| `bei_tokenexpira` | `bdibei` | no | SELECT | L79 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_renovtkn_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_renovtkn_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatk_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultatk_bei.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE EL Token |

### Firma

```sql
CREATE PROCEDURE sp_consultatk_bei(
  pIdUsuario                   INTEGER
) RETURNING char(5),CHAR(10)   , INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sNsToken` | `CHAR(10)` | L10 |
| `sIdStatusToken` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L49 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tk_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tk_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultatkns_activtknadmin`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultatkns_activtknadmin.sql` |
| **LOC (1er CREATE)** | 76 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta Administrador — rol de usuario con privilegios administrativos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que consulta los tokens para la activacion del token del adminitrador |
| FECHA | 31/08/2018 |

### Firma

```sql
CREATE PROCEDURE sp_consultatkns_activtknadmin(
  pNumCliente                  CHAR(9)
  pSolicitud                   CHAR(10)
  pPaginacion                  INTEGER
) RETURNING CHAR(5), CHAR(10), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pSolicitud` | `CHAR(10)` | — | — |
| `pPaginacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L14 |
| `cod_ret` | `CHAR(5)` | L15 |
| `vnum_reg_xpag` | `INTEGER` | L16 |
| `sns_token` | `CHAR(10)` | L17 |
| `vnum_tokens` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L52 |
| `bei_token` | `bdibei` | no | SELECT | L65 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L23 | FÓRMULA | `LET pPaginacion = pPaginacion * vnum_reg_xpag;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tkns_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?ivtkn` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tkns_`, `?ivtkn` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_consultmovschq_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_consultmovschq_bei.sql` |
| **LOC (1er CREATE)** | 616 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta movimientos, cheque y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 14/04/2014, |
| MODIFICACION | Se crea sp para la consulta de movimientos de EmpresaNet , |
| MODIFICACION | Se cambie el parametro de entrada para la paginacion pRegistro de smallint a integer. |
| MODIFICACION | Berenice Noriega Guevara - BanCoppel - Internet. |
| FECHA | 13Julio2015 |
| MODIFICACION | Se cambia la descripcion de la trasaccion 0274 por la descripcion de la transaccion 0331. |
| MODIFICACION | Alejandro Vazquez - BanCoppel - Internet. |
| FECHA | 10Mayo2016 |
| MODIFICACION | Se modifica cuando es SPEI para ignorar los registros C |
| MODIFICACION | Berenice Noriega Guevara - BanCoppel - Internet. |
| FECHA | 22Mayo2018 |
| MODIFICACION | Se agrega a la salida la referencia numerica (SPEI) |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 30 Noviembre 2021 |
| MODIFICACION | Se modifica el ORDER de los queries de movimientos a solo fecha y serial |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 16 Marzo 2022 |
| MODIFICACION | Ajuste en los queries para detalles de SPEI agregando fecha del movimiento y beneficiario como filtros para evitar duplicaciones de claves |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 09 Mayo 2022 |
| MODIFICACION | Se modifica el ORDER de los queries de movimientos a solo fecha y hora, debido a un descuadre del ID SERIAL |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 08 Diciembre 2022 |
| MODIFICACION | A solicitud de usuario, se reversa el cambio del 8 de Diciembre porque no resolvo el problema de ordenamiento de raiz |
| MODIFICACION | Armando Barrientos - BanCoppel - Internet. |
| FECHA | 14 Diciembre 2022 |
| MODIFICACION | Se implementa fix a la consulta de movimientos para ordenar saldos con num_serial simulado con tipo BIGINT |
| MODIFICACION | Marco Tinajero - Luis David Espinoza - Luis Baldivia - BanCoppel - Internet. |
| FECHA | 14 Octubre 2025 |
| MODIFICACION | Se actualiza la consulta para excluir resversos |
| MODIFICACION | Luis Baldivia - BanCoppel - Internet. |
| FECHA | 11 noviembre 2025 |

### Firma

```sql
CREATE PROCEDURE sp_consultmovschq_bei(
  pEmpresa                     CHAR(3)
  pCuenta                      CHAR(20)
  pFechaInicial                DATE
  pFechaFinal                  DATE
  pTipoMov                     CHAR(1)
  pBanderaUltHora              INTEGER
  pRegistro                    INTEGER
) RETURNING CHAR(5),DATE,CHAR(40),CHAR(40),CHAR(1),MONEY(14,2),MONEY(14,2),CHAR(40),CHAR(4),CHAR(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pFechaInicial` | `DATE` | — | — |
| `pFechaFinal` | `DATE` | — | — |
| `pTipoMov` | `CHAR(1)` | — | — |
| `pBanderaUltHora` | `INTEGER` | — | — |
| `pRegistro` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L44 |
| `iAux` | `BIGINT` | L46 |
| `dFechaMov` | `DATE` | L47 |
| `cReferencia` | `CHAR(40)` | L48 |
| `cDescripcion` | `CHAR(50)` | L49 |
| `cNaturaleza` | `CHAR(1)` | L54 |
| `cFech_param` | `CHAR(10)` | L55 |
| `cFech_param_ini` | `CHAR(10)` | L56 |
| `vFechaHoy` | `DATE` | L57 |
| `vTrans` | `CHAR(4)` | L58 |
| `vHrInicial` | `DATETIME YEAR TO FRACTION(3)` | L59 |
| `vHrFinal` | `DATETIME YEAR TO FRACTION(3)` | L60 |
| `cConceptoPago` | `CHAR(40)` | L61 |
| `cDescripcionSpei` | `CHAR(40)` | L62 |
| `dHoraMov` | `DATETIME YEAR TO FRACTION(3)` | L63 |
| `cRefNumerica` | `CHAR(10)` | L64 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L97 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L110 |
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L114 |
| `23` | `bdibei` | no | SELECT | L162 |
| `tblpago` | `bdispei` | ⚠️ sí | SELECT | L172 |
| `tblhistpago` | `bdispei` | ⚠️ sí | SELECT | L279 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L119 | FÓRMULA | `LET vHrInicial= vHrFinal - INTERVAL(1) HOUR TO HOUR;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?ult` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movs` | ENTIDAD | movimientos (abreviación) | 🟡 INFERIDO | nombre_sp |
| `chq` | ENTIDAD | cheque (abreviación — bdicheq) | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ult` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_crea_usuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_crea_usuario_bei.sql` |
| **LOC (1er CREATE)** | 118 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "crédito, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Crea usuario Bei |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_crea_usuario_bei(
  pNumCliente                  CHAR(9)
  pIdStatus                    SMALLINT
  pUsuario                     CHAR(50)
  pPass                        CHAR(50)
  pTipoUsuario                 SMALLINT
  pNombrePerfil                CHAR(50)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            Integer
) RETURNING char(5),INTEGER,INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdStatus` | `SMALLINT` | — | — |
| `pUsuario` | `CHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |
| `pPass` | `CHAR(50)` | — | — |
| `pTipoUsuario` | `SMALLINT` | `usuario`=usuario | ✅ CÓDIGO |
| `pNombrePerfil` | `CHAR(50)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `Integer` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L7 |
| `sql_err` | `INTEGER` | L8 |
| `sIdUsuario` | `INTEGER` | L10 |
| `sIdPerfil` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_perfil` | `bdibei` | no | INSERT | L95 |
| `bei_usuario` | `bdibei` | no | INSERT | L101 |
| `bei_datos_usuario` | `bdibei` | no | INSERT | L107 |
| `bei_usuario_perfil` | `bdibei` | no | INSERT | L111 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `?a_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_crea_usuario_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_crea_usuario_bei_historico.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "crédito, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Crea usuario Bei |
| FECHA | 04/06/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_crea_usuario_bei_historico(
  pNumCliente                  CHAR(9)
  pIdStatus                    SMALLINT
  pUsuario                     CHAR(50)
  pPass                        CHAR(50)
  pTipoUsuario                 SMALLINT
  pNombrePerfil                CHAR(50)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            INTEGER
  pidBitacoraAdmin             INTEGER
  pIdPerfil                    INTEGER
  pIdUsuario                   INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdStatus` | `SMALLINT` | — | — |
| `pUsuario` | `CHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |
| `pPass` | `CHAR(50)` | — | — |
| `pTipoUsuario` | `SMALLINT` | `usuario`=usuario | ✅ CÓDIGO |
| `pNombrePerfil` | `CHAR(50)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pidBitacoraAdmin` | `INTEGER` | — | — |
| `pIdPerfil` | `INTEGER` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L8 |
| `sql_err` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_perfil_historico` | `bdibei` | no | INSERT | L37 |
| `bei_usuario_historico` | `bdibei` | no | INSERT | L41 |
| `bei_datos_usuario_historico` | `bdibei` | no | INSERT | L45 |
| `bei_usuario_perfil_historico` | `bdibei` | no | INSERT | L49 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `?a_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ctasfrec_consulta_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_ctasfrec_consulta_bei.sql` |
| **LOC (1er CREATE)** | 589 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "recepción cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_ctasfrec_consulta_bei(
  pNum_Cte                     CHAR(20)
  pAlias                       CHAR(20)
  pClave_banco                 INTEGER
  pOtro                        CHAR(20)
) RETURNING CHAR(6) as cod_ret, ---cod_ret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNum_Cte` | `CHAR(20)` | — | — |
| `pAlias` | `CHAR(20)` | — | — |
| `pClave_banco` | `INTEGER` | — | — |
| `pOtro` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_cod_ret` | `CHAR(5)` | L42 |
| `iSqlErr` | `INTEGER` | L43 |
| `iSamErr` | `INTEGER` | L44 |
| `vDesErr` | `CHAR(60)` | L45 |
| `v_CodDesc` | `CHAR(50)` | L46 |
| `v_CvePago` | `CHAR(2)` | L47 |
| `v_CtaDestino` | `CHAR(20)` | L48 |
| `v_Nombre` | `CHAR(100)` | L49 |
| `v_Banco` | `CHAR(50)` | L50 |
| `v_CompCel` | `CHAR(2)` | L51 |
| `v_NumCel` | `CHAR(10)` | L52 |
| `v_CorreoE` | `CHAR(40)` | L53 |
| `v_CveCuenta` | `CHAR(2)` | L54 |
| `v_DescCta` | `CHAR(20)` | L55 |
| `v_Rfc` | `CHAR(13)` | L56 |
| `v_Canal` | `CHAR(2)` | L57 |
| `v_FechaInsert` | `DATE` | L58 |
| `v_HoraInsert` | `DATETIME HOUR TO SECOND` | L59 |
| `v_FechaHoraInsert` | `DATETIME YEAR TO FRACTION` | L60 |
| `v_MontoMaximo` | `MONEY(16,2)` | L61 |
| `v_CveCaducidad` | `INTEGER` | L62 |
| `v_StatusHabilitado` | `SMALLINT` | L63 |
| `v_Alias` | `CHAR(20)` | L64 |
| `v_descripcion` | `CHAR(40)` | L65 |
| `v_vchrnombrecorto` | `VARCHAR(20)` | L66 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_ctasterceros` | `bdiprog` | ⚠️ sí | SELECT | L195 |
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L216 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L230 | VALIDACIÓN_NULL | `IF ((pAlias IS NULL) or ( NVL(pAlias,'')=='')) THEN` |  |
| L418 | VALIDACIÓN_NULL | `IF ((pOtro IS NULL) or ( NVL(pOtro,'')=='')) THEN` |  |
| L502 | VALIDACIÓN_NULL | `IF ((pOtro2 IS NULL) or ( NVL(pOtro2,'')=='')) THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?f` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ctasfrec_conteo_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_ctasfrec_conteo_bei.sql` |
| **LOC (1er CREATE)** | 161 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "recepción cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Cuenta la cantidad de registros de cuentas frecuentes SPEI o Terceros para saber |
| FECHA | 16-Julio-2018 |
| MODIFICACION | Se agrega consulta por nombre de titular y por cuenta. |
| FECHA | 17/Junio/2019 |

### Firma

```sql
CREATE PROCEDURE sp_ctasfrec_conteo_bei(
  pNum_Cte                     CHAR(20)
  pAlias                       CHAR(20)
  pClave_banco                 INTEGER
  pOtro                        CHAR(20)
) RETURNING CHAR(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNum_Cte` | `CHAR(20)` | — | — |
| `pAlias` | `CHAR(20)` | — | — |
| `pClave_banco` | `INTEGER` | — | — |
| `pOtro` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L28 |
| `cod_ret` | `CHAR(5)` | L29 |
| `iTotalReg` | `INTEGER` | L30 |
| `VAlias` | `CHAR(20)` | L31 |
| `VOtro` | `CHAR(20)` | L32 |
| `VOtro2` | `CHAR(60)` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_ctasterceros` | `bdiprog` | ⚠️ sí | SELECT | L81 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L88 | VALIDACIÓN_NULL | `IF ((pAlias IS NULL) or ( NVL(pAlias,'')=='')) THEN` |  |
| L119 | VALIDACIÓN_NULL | `IF ((pOtro IS NULL) or ( NVL(pOtro,'')=='')) THEN` |  |
| L135 | VALIDACIÓN_NULL | `IF ((pOtro2 IS NULL) or ( NVL(pOtro2,'')=='')) THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `?eo_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?f`, `?eo_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_cuentas_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cuentas_bei.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "cuentas y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LAS CUENTAS |
| FECHA | 05/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_cuentas_bei(
  pempresa                     CHAR(3)
  pnum_cte                     CHAR(20)
  pRegistros                   smallint
) RETURNING char(5) as vCodRet, char(20) as vCuenta,char(4) as vProducto,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pempresa` | `CHAR(3)` | — | — |
| `pnum_cte` | `CHAR(20)` | — | — |
| `pRegistros` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L15 |
| `vCuenta` | `char(20)` | L16 |
| `vProducto` | `char(4)` | L17 |
| `vProdNom` | `char(40)` | L18 |
| `iCont` | `integer` | L19 |
| `sql_err` | `integer` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L53 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L60 | FÓRMULA | `LET iCont = iCont + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_cuentasfrecuentesmancomunadas_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_cuentasfrecuentesmancomunadas_bei.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "recepción cuentas, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_insertaoperacionesmancomunadasoperador_bei` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guardar la operaci como pendiente para el alta/baja/edicion |
| FECHA | 15/Agosto/2016 |

### Firma

```sql
CREATE PROCEDURE sp_cuentasfrecuentesmancomunadas_bei(
  pcuenta_origen               CHAR(20)
  pcuenta_destino              CHAR(20)
  pimporte                     MONEY(14,2)
  preferencia                  CHAR(40)
  preferenciabe                CHAR(40)
  pnombre_beneficiario         VARCHAR(100)
  pid_usuario                  INTEGER
  pid_cat_operacion            INTEGER
  pempresa                     CHAR(3)
  psucursal_virtual            CHAR(4)
  pusuario_virtual             CHAR(4)
  pclave_banco                 INTEGER
  pnombre_usuario              VARCHAR(40)
  prfc                         VARCHAR(18)
  ptipo_cuenta_beneficiario    VARCHAR(40)
  pbanco_receptor              VARCHAR(100)
  pcategoria                   CHAR(2)
  pconvenio                    CHAR(3)
  preftelefono                 CHAR(20)
  prefverificador              CHAR(20)
  pnumtransferenciacargo       CHAR(4)
  pstatusoperacion             CHAR(1)
  pid_cliente                  CHAR(9)
  cve_cuenta                   CHAR(2)
) RETURNING CHAR(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pcuenta_origen` | `CHAR(20)` | — | — |
| `pcuenta_destino` | `CHAR(20)` | — | — |
| `pimporte` | `MONEY(14,2)` | — | — |
| `preferencia` | `CHAR(40)` | — | — |
| `preferenciabe` | `CHAR(40)` | — | — |
| `pnombre_beneficiario` | `VARCHAR(100)` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `pid_cat_operacion` | `INTEGER` | — | — |
| `pempresa` | `CHAR(3)` | — | — |
| `psucursal_virtual` | `CHAR(4)` | — | — |
| `pusuario_virtual` | `CHAR(4)` | — | — |
| `pclave_banco` | `INTEGER` | — | — |
| `pnombre_usuario` | `VARCHAR(40)` | — | — |
| `prfc` | `VARCHAR(18)` | — | — |
| `ptipo_cuenta_beneficiario` | `VARCHAR(40)` | — | — |
| `pbanco_receptor` | `VARCHAR(100)` | `rec`=recepción / recibe | 🔵 CONVENCIÓN |
| `pcategoria` | `CHAR(2)` | — | — |
| `pconvenio` | `CHAR(3)` | — | — |
| `preftelefono` | `CHAR(20)` | — | — |
| `prefverificador` | `CHAR(20)` | — | — |
| `pnumtransferenciacargo` | `CHAR(4)` | — | — |
| `pstatusoperacion` | `CHAR(1)` | — | — |
| `pid_cliente` | `CHAR(9)` | — | — |
| `cve_cuenta` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L37 |
| `cod_ret` | `CHAR(5)` | L38 |
| `sIdOper` | `INTEGER` | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L119 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |
| `?uentes` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?munadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?f`, `?uentes`, `?munadas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_danado_paquete_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_danado_paquete_bei.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "paquete — paquete de pago SPEI y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 10 tabla(s) con operaciones: DELETE, INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que cambia el estatus de los paquetes de PM a dañados. |
| FECHA | 19-10-2015 |

### Firma

```sql
CREATE PROCEDURE sp_danado_paquete_bei(
  pNumGuia                     char(30)
  pNumSolicitud                char(10)
  pUsuario                     char(8)
  pComentario                  char(200)
  pCanal                       char(2)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumGuia` | `char(30)` | — | — |
| `pNumSolicitud` | `char(10)` | — | — |
| `pUsuario` | `char(8)` | — | — |
| `pComentario` | `char(200)` | — | — |
| `pCanal` | `char(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsCodRet` | `CHAR(5)` | L16 |
| `vNumCliente` | `CHAR(9)` | L17 |
| `vStatusAnt` | `CHAR(4)` | L18 |
| `vSqlErr` | `INTEGER` | L19 |
| `vStatusAct` | `SMALLINT` | L20 |
| `vStatusSolAnt` | `SMALLINT` | L21 |
| `vNumSToken` | `CHAR(9)` | L22 |
| `vTotTokens` | `INT` | L23 |
| `vTotAux` | `INT` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_envios` | `bdibei` | no | SELECT | L52 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L58 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L70 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | UPDATE | L77 |
| `tkn_status_token` | `bdibpi` | ⚠️ sí | INSERT | L80 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L89 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L92 |
| `bei_envios` | `bdibei` | no | UPDATE | L95 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L100 |
| `bei_tokensolicitud` | `bdibei` | no | DELETE | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET vsCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET vsCodRet = '00100';` |  |
| L72 | VALIDACIÓN_NULL | `IF (vStatusAnt=='' OR vStatusAnt IS NULL) THEN` |  |
| L73 | CÓDIGO_RETORNO | `LET vsCodRet = '00200';` |  |
| L83 | FÓRMULA | `LET vTotAux = vTotAux + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_danado_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `paq` | ENTIDAD | paquete — paquete de pago SPEI (bloque/lote de órdenes de tr | 🟡 INFERIDO | nombre_sp |
| `?uete_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_danado_`, `?uete_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_depura_arch_movimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_depura_arch_movimientos.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "depura archivo y movimientos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `depura` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Depurar los registros de movimientos por folio de un archivo de movimientos generado sobre bdibei:bei_movimientos_cons |
| FECHA | 06/Agosto/2025 |

### Firma

```sql
CREATE PROCEDURE sp_depura_arch_movimientos(
  pFolio                       CHAR(25)
) RETURNING CHAR(5) AS cod_ret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFolio` | `CHAR(25)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vIntSqlErr` | `INTEGER` | L12 |
| `vChrCodRet` | `CHAR(5)` | L13 |
| `vIntIdMovimiento` | `INTEGER` | L16 |
| `vChrFolio` | `CHAR(25)` | L17 |
| `vIntContadorMovs` | `INTEGER` | L20 |
| `vIntIniciarBegin` | `INTEGER` | L21 |
| `vIntRegistros` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_movimientos_cons` | `bdibei` | no | SELECT | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | FÓRMULA | `LET vIntContadorMovs = vIntContadorMovs + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `depura` | ACCION | depura / limpia | 🔵 CONVENCIÓN | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_depura_bitacora_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_depura_bitacora_bei.sql` |
| **LOC (1er CREATE)** | 126 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "depura bitácora y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `depura` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_depura_bitacora_bei(
) RETURNING char(6)
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L6 |
| `isam_err` | `INTEGER` | L7 |
| `error_info` | `CHAR(150)` | L8 |
| `cMensaje` | `CHAR(150)` | L9 |
| `cCod_ret` | `CHAR(6)` | L10 |
| `Vid_oper` | `CHAR(4)` | L11 |
| `Vid_usuario` | `INTEGER` | L12 |
| `iCont` | `INTEGER` | L13 |
| `cValor` | `CHAR(1)` | L14 |
| `dFecha` | `DATE` | L15 |
| `vcomienza` | `INTEGER` | L17 |
| `vcuantos` | `INTEGER` | L18 |
| `vregistros` | `INTEGER` | L19 |
| `vcontador` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bpi_param` | `bdibpi` | ⚠️ sí | SELECT | L52 |
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L56 |
| `temp_bei_bitacora_depurar` | `bdibei` | no | SELECT | L60 |
| `bpi_param` | `bdibpi` | ⚠️ sí | UPDATE | L64 |
| `bei_bitacora` | `bdibei` | no | SELECT | L84 |
| `bei_bitacora` | `bdibei` | no | DELETE | L94 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L35 | FÓRMULA | `LET vcontador = -1;` |  |
| L37 | FÓRMULA | `LET vcomienza   = -1;` |  |
| L102 | FÓRMULA | `LET vcomienza = -1;` |  |
| L104 | FÓRMULA | `LET vcontador = vcontador + 1 ;` |  |
| L114 | FÓRMULA | `LET vcomienza = -1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `depura` | ACCION | depura / limpia | 🔵 CONVENCIÓN | nombre_sp |
| `bitacora` | ENTIDAD | bitácora | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_destoken_manc_aut_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_destoken_manc_aut_bei.sql` |
| **LOC (1er CREATE)** | 115 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza para que retorne el nombre de usuario (sUserNomAdmin) con una longitud |
| MODIFICACION | Jos el Hernez Gonzz |
| FECHA | 05-Diciembre-2016 |

### Firma

```sql
CREATE PROCEDURE sp_destoken_manc_aut_bei(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L16 |
| `sql_err` | `INTEGER` | L17 |
| `iTotalReg` | `INTEGER` | L18 |
| `sNsToken` | `VARCHAR(10)` | L19 |
| `sIdStatusToken` | `INTEGER` | L20 |
| `sUserNomAdmin` | `CHAR(250)` | L21 |
| `sUserNom` | `CHAR(50)` | L22 |
| `sIdUser` | `INTEGER` | L23 |
| `sIdMancomunidad` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L76 |
| `bei_usuario` | `bdibei` | no | SELECT | L107 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_des` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_des`, `?_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_destoken_manc_aut_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_destoken_manc_aut_bei2.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | se clona y modifica el spl para mostrar el nombre de la persona en lugar de usuario |
| FECHA | NA |
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_destoken_manc_aut_bei2(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L16 |
| `sql_err` | `INTEGER` | L17 |
| `iTotalReg` | `INTEGER` | L18 |
| `sNsToken` | `VARCHAR(10)` | L19 |
| `sIdStatusToken` | `INTEGER` | L20 |
| `sUserNomAdmin` | `CHAR(50)` | L21 |
| `sUserNom` | `CHAR(50)` | L22 |
| `sIdUser` | `INTEGER` | L23 |
| `sIdMancomunidad` | `INTEGER` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L79 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L111 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_des` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_des`, `?_manc_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ejecuta_opermanco_admin_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_ejecuta_opermanco_admin_bei.sql` |
| **LOC (1er CREATE)** | 300 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ejecuta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos, Administrador — rol de usuario con privilegios administrativos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_actualiza_usuario_bei`, `sp_elimina_operaciones_bei` |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Ejecuta Proceso de Mancomuniadd |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_ejecuta_opermanco_admin_bei(
  pIdAdminManco                INTEGER
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pAutoriza                    SMALLINT
) RETURNING char(5) ,INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdAdminManco` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') · `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pAutoriza` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L6 |
| `sql_err` | `INTEGER` | L7 |
| `stipoOper` | `SMALLINT` | L9 |
| `sTipoMov` | `SMALLINT` | L10 |
| `sIdUserAdmin` | `INTEGER` | L12 |
| `sIdUsuario` | `INTEGER` | L14 |
| `sNumCliente` | `CHAR(9)` | L15 |
| `sIdStatus` | `SMALLINT` | L17 |
| `sUserNom` | `CHAR(250)` | L18 |
| `sPass` | `CHAR(250)` | L19 |
| `sIdTipoUser` | `SMALLINT` | L20 |
| `sNombre` | `CHAR(150)` | L22 |
| `sTelCelular` | `CHAR(15)` | L23 |
| `sCiaCel` | `SMALLINT` | L24 |
| `sEmail` | `CHAR(50)` | L25 |
| `sIdPerfil` | `INTEGER` | L27 |
| `sNomPerfil` | `CHAR(50)` | L28 |
| `sNsToken` | `VARCHAR(10)` | L30 |
| `sIdStatusToken` | `INTEGER` | L31 |
| `sAut` | `BOOLEAN` | L33 |
| `sCount` | `SMALLINT` | L34 |
| `sIdOper` | `INTEGER` | L35 |
| `sIdMenuOper` | `INTEGER` | L36 |
| `sNumCta` | `CHAR(20)` | L37 |
| `sMontoMin` | `DECIMAL(16,2)` | L38 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L96 |
| `bei_perfil` | `bdibei` | no | INSERT | L131 |
| `bei_usuario` | `bdibei` | no | INSERT | L137 |
| `bei_datos_usuario` | `bdibei` | no | INSERT | L145 |
| `bei_usuario_perfil` | `bdibei` | no | INSERT | L149 |
| `bei_admin_manco_det_temp` | `bdibei` | no | SELECT | L158 |
| `bei_mancomunidad` | `bdibei` | no | INSERT | L162 |
| `bei_operaciones` | `bdibei` | no | INSERT | L176 |
| `bei_mancomunidad` | `bdibei` | no | SELECT | L226 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_actualiza_usuario_bei` | `bdibei` | no | L195 |
| `sp_elimina_operaciones_bei` | `bdibei` | no | L264 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ejecuta` | ACCION | ejecuta (verbo — proceso / operación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_elimina_asociaperfil_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_elimina_asociaperfil_bei.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "elimina Sistema Operativo Central, perfil de usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Elimina Asociacion de perfil y usuario |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_elimina_asociaperfil_bei(
  pIdPerfil                    INTEGER
  pIdUsuario                   INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdPerfil` | `INTEGER` | `perfil`=perfil de usuario | ✅ CÓDIGO |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?ia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_a`, `?ia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_elimina_datosuser_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_elimina_datosuser_bei.sql` |
| **LOC (1er CREATE)** | 33 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "elimina datos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Elimina Datos de Usuario Bei |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_elimina_datosuser_bei(
  pIdUsuario                   INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_elimina_operaciones_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_elimina_operaciones_bei.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "elimina operaciones y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Elimina Operaciones de Perfil |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_elimina_operaciones_bei(
  pIdPerfil                    INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdPerfil` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_elimina_perfil_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_elimina_perfil_bei.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "elimina perfil de usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Elimina Perfil Bei |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_elimina_perfil_bei(
  pIdPerfil                    INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdPerfil` | `INTEGER` | `perfil`=perfil de usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_elimina_tokenasociados_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_elimina_tokenasociados_bei.sql` |
| **LOC (1er CREATE)** | 59 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "elimina token, Sistema Operativo Central, OS — Originación de Solicitudes y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |

### Firma

```sql
CREATE PROCEDURE sp_elimina_tokenasociados_bei(
  pNseries                     char(90)
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNseries` | `char(90)` | — | — |
| `pNumSolicitud` | `char(10)` | — | — |
| `pNumCliente` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L12 |
| `sql_err` | `integer` | L13 |
| `vNstoken` | `char(9)` | L14 |
| `vLength` | `smallint` | L15 |
| `vContador` | `smallint` | L16 |
| `vVar` | `smallint` | L17 |
| `i` | `INTEGER` | L18 |
| `vTrama` | `char(90)` | L19 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L29 | FÓRMULA | `LET vContador = vLength/9;` |  |
| L50 | FÓRMULA | `LET i = i + 9;` |  |
| L51 | FÓRMULA | `LET vVar= vVAr + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?iad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a`, `?iad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_elimina_total_usuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_elimina_total_usuario_bei.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "elimina usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (total)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | elimina Usuario Bei |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_elimina_total_usuario_bei(
  pIdUsuario                   INTEGER
  pNumCliente                  CHAR(9)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pNumCliente` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sIdPerfil` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario_perfil` | `bdibei` | no | SELECT | L46 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `total` | MODIF | total | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_elimina_usuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_elimina_usuario_bei.sql` |
| **LOC (1er CREATE)** | 34 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "elimina usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `elimina` → `DELETE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | elimina Usuario Bei |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_elimina_usuario_bei(
  pIdUsuario                   INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `elimina` | ACCION | elimina | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_extraviado_paquete_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_extraviado_paquete_bei.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "paquete — paquete de pago SPEI y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 10 tabla(s) con operaciones: DELETE, INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que cambia el estatus de los paquetes de PM a extraviados. |
| FECHA | 19-10-2015 |

### Firma

```sql
CREATE PROCEDURE sp_extraviado_paquete_bei(
  pNumGuia                     char(30)
  pNumSolicitud                char(10)
  pUsuario                     char(8)
  pComentario                  char(200)
  pCanal                       char(2)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumGuia` | `char(30)` | — | — |
| `pNumSolicitud` | `char(10)` | — | — |
| `pUsuario` | `char(8)` | — | — |
| `pComentario` | `char(200)` | — | — |
| `pCanal` | `char(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vsCodRet` | `CHAR(5)` | L16 |
| `vNumCliente` | `CHAR(9)` | L17 |
| `vStatusAnt` | `CHAR(4)` | L18 |
| `vSqlErr` | `INTEGER` | L19 |
| `vStatusAct` | `SMALLINT` | L20 |
| `vStatusSolAnt` | `SMALLINT` | L21 |
| `vNumSToken` | `CHAR(9)` | L22 |
| `vTotTokens` | `INT` | L23 |
| `vTotAux` | `INT` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_envios` | `bdibei` | no | SELECT | L52 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L58 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L70 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | UPDATE | L77 |
| `tkn_status_token` | `bdibpi` | ⚠️ sí | INSERT | L80 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L89 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L92 |
| `bei_envios` | `bdibei` | no | UPDATE | L95 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L100 |
| `bei_tokensolicitud` | `bdibei` | no | DELETE | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET vsCodRet = '00000';` |  |
| L63 | CÓDIGO_RETORNO | `LET vsCodRet = '00100';` |  |
| L72 | VALIDACIÓN_NULL | `IF (vStatusAnt=='' OR vStatusAnt IS NULL) THEN` |  |
| L73 | CÓDIGO_RETORNO | `LET vsCodRet = '00200';` |  |
| L83 | FÓRMULA | `LET vTotAux = vTotAux + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?traviado_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `paq` | ENTIDAD | paquete — paquete de pago SPEI (bloque/lote de órdenes de tr | 🟡 INFERIDO | nombre_sp |
| `?uete_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_e`, `?traviado_`, `?uete_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gen_arch_datos_contacto_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_gen_arch_datos_contacto_bei.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "genera archivo, datos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=1 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | SP que generar un archivo a partir de los resultados de la tabla de datos de contacto de los clientes de empresanet |
| FECHA | 29/08/2024 |

### Firma

```sql
CREATE PROCEDURE sp_gen_arch_datos_contacto_bei(
) RETURNING CHAR(5), VARCHAR(80)
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L12 |
| `cMensaje` | `VARCHAR(80)` | L13 |
| `cCodResp` | `CHAR(5)` | L14 |
| `vNombreDeArchivo` | `VARCHAR(50)` | L17 |
| `cComandoEjec` | `CHAR(1000)` | L18 |
| `vNombreArchivoSql` | `VARCHAR(200)` | L19 |
| `vRutaDir` | `CHAR(50)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `enet_parametros` | `bdibpi` | ⚠️ sí | SELECT | L44 |
| `systables` | `bdibei` | no | SELECT | L64 |
| `bei_datos_empnet` | `bdibei` | no | SELECT | L68 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?o_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_gen_arch_movimientos`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_gen_arch_movimientos.sql` |
| **LOC (1er CREATE)** | 1247 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "genera archivo y movimientos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Generar archivo de movimientos para los registros de bei_consulta_mov |
| FECHA | 11/Julio/2016 |
| FECHA | 29/Julio/2016 |
| FECHA | 18/Agosto/2016 |
| MODIFICACION | se modifica para incluir mas detalle en la consulta de abono SPEI |
| FECHA | 01/JULIO/2019 |
| MODIFICACION | Se modifica el ORDER de los queries de movimientos a solo fecha y serial |
| FECHA | 16/Marzo/2022 |
| MODIFICACION | Ajuste en los queries para detalles de SPEI agregando fecha del movimiento y beneficiario como filtros para evitar duplicaciones de claves |
| FECHA | 09/Mayo/2022 |
| MODIFICACION | Se modifica el ORDER de los queries de movimientos a solo fecha y hora, debido a un descuadre del ID SERIAL |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 08 Diciembre 2022 |
| MODIFICACION | A solicitud de usuario, se reversa el cambio del 8 de Diciembre porque no resolv o el problema de ordenamiento de raiz |
| MODIFICACION | Armando Barrientos - BanCoppel - Internet. |
| FECHA | 14 Diciembre 2022 |
| MODIFICACION | Se modifica el tiempo para la depuracion de archivos de movs con 2 o m s dias aplazandolo a 7 dias |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 27 Marzo 2023 |
| MODIFICACION | Se agrega a los queries de obtencion de referencias de transacciones 0273 los filtros de estatus de envio <> C y clave tipo de pago <> 0 |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 01 Julio 2025 |
| MODIFICACION | Se actualiza el numero de serie por reset de num_serie |
| MODIFICACION | Luis Baldivia - Luis David Espinoza - BanCoppel - Internet. |
| FECHA | 14 Octubre 2025 |
| MODIFICACION | Se actualiza la consulta para excluir resversos |
| MODIFICACION | Luis Baldivia - BanCoppel - Internet. |
| FECHA | 11 noviembre 2025 |

### Firma

```sql
CREATE PROCEDURE sp_gen_arch_movimientos(
) RETURNING CHAR(5) AS cod_ret
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vStatus` | `CHAR(3)` | L50 |
| `vFolio` | `CHAR(25)` | L51 |
| `vTotalReg` | `BIGINT` | L52 |
| `vDiferencia` | `INTEGER` | L53 |
| `vEmpresa` | `CHAR(3)` | L55 |
| `vCuenta` | `CHAR(20)` | L56 |
| `xFecha` | `CHAR(10)` | L57 |
| `vFinicial` | `DATE` | L58 |
| `vFfinal` | `DATE` | L59 |
| `vFechaDia` | `DATE` | L60 |
| `vFechaHis` | `DATE` | L61 |
| `vFechaOld` | `DATE` | L62 |
| `vFechaOld2` | `DATE` | L63 |
| `vDescripcionSpei` | `CHAR(40)` | L64 |
| `vPath` | `CHAR(200)` | L65 |
| `vfecha` | `DATE` | L68 |
| `vcontador` | `INTEGER` | L69 |
| `vcuantos1` | `INTEGER` | L70 |
| `vfech_alt` | `DATE` | L71 |
| `vfech_hor` | `DATETIME HOUR to FRACTION (3)` | L72 |
| `vnumero` | `CHAR(4)` | L73 |
| `vnaturaleza` | `CHAR(4)` | L74 |
| `vsdo_cuenta` | `MONEY(15,2)` | L75 |
| `vmonto_tot` | `MONEY(15,2)` | L76 |
| `vDescripcion` | `CHAR(40)` | L77 |
| *…15 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L124 |
| `sc_param` | `bdicheq` | ⚠️ sí | SELECT | L132 |
| `si_transacc` | `bdinteg` | ⚠️ sí | SELECT | L171 |
| `enet_parametros` | `bdibpi` | ⚠️ sí | SELECT | L178 |
| `bei_consulta_mov` | `bdibei` | no | SELECT | L185 |
| `sc_movdia` | `bdicheq` | ⚠️ sí | SELECT | L209 |
| `23` | `bdibei` | no | SELECT | L231 |
| `tblpago` | `bdispei` | ⚠️ sí | SELECT | L240 |
| `bei_movimientos_cons` | `bdibei` | no | INSERT | L290 |
| `sc_movhis` | `bdicheq` | ⚠️ sí | SELECT | L315 |
| `tblhistpago` | `bdispei` | ⚠️ sí | SELECT | L346 |
| `sc_movhis_old` | `bdicheq` | ⚠️ sí | SELECT | L422 |
| `sc_movhis_old2` | `bdicheq` | ⚠️ sí | SELECT | L747 |
| `bei_movimientos_cons` | `bdibei` | no | SELECT | L1175 |
| `bei_consulta_mov` | `bdibei` | no | UPDATE | L1236 |
| `bei_consulta_mov` | `bdibei` | no | DELETE | L1241 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `trace_movimientos_cons` | `bdibei` | no | L1192 |
| `sp_depura_arch_movimientos` | `bdibei` | no | L1209 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L99 | FÓRMULA | `LET vcontador = -1;` |  |
| L101 | FÓRMULA | `LET vcomienza   = -1;` |  |
| L139 | FÓRMULA | `LET vFechaHis =  TO_DATE(xFecha, '%m/%d/%Y');` |  |
| L152 | FÓRMULA | `LET vFechaOld =  TO_DATE(xFecha, '%m/%d/%Y');` |  |
| L165 | FÓRMULA | `LET vFechaOld2 =  TO_DATE(xFecha, '%m/%d/%Y');` |  |
| L221 | FÓRMULA | `LET vmonto_tot =( vmonto_tot * -1);` | 🔴 MONEY/aritmética financiera |
| L251 | VALIDACIÓN_NULL | `IF (vvchrnombrecorto is null)THEN` |  |
| L255 | VALIDACIÓN_NULL | `IF (vvchrcuentaord is null)THEN` |  |
| L259 | VALIDACIÓN_NULL | `IF (vvchrnombreord is null)THEN` |  |
| L263 | VALIDACIÓN_NULL | `IF (vvchrconceptopago2 is null)THEN` |  |
| L296 | FÓRMULA | `LET vcomienza = -1;` |  |
| L298 | FÓRMULA | `LET vcontador = vcontador + 1 ;` |  |
| L328 | FÓRMULA | `LET vmonto_tot =( vmonto_tot * -1);` | 🔴 MONEY/aritmética financiera |
| L357 | VALIDACIÓN_NULL | `IF (vvchrnombrecorto is null)THEN` |  |
| L361 | VALIDACIÓN_NULL | `IF (vvchrcuentaord is null)THEN` |  |
| L365 | VALIDACIÓN_NULL | `IF (vvchrnombreord is null)THEN` |  |
| L369 | VALIDACIÓN_NULL | `IF (vvchrconceptopago2 is null)THEN` |  |
| L403 | FÓRMULA | `LET vcomienza = -1;` |  |
| L405 | FÓRMULA | `LET vcontador = vcontador + 1 ;` |  |
| L435 | FÓRMULA | `LET vmonto_tot =( vmonto_tot * -1);` | 🔴 MONEY/aritmética financiera |
| L465 | VALIDACIÓN_NULL | `IF (vvchrnombrecorto is null)THEN` |  |
| L469 | VALIDACIÓN_NULL | `IF (vvchrcuentaord is null)THEN` |  |
| L473 | VALIDACIÓN_NULL | `IF (vvchrnombreord is null)THEN` |  |
| L477 | VALIDACIÓN_NULL | `IF (vvchrconceptopago2 is null)THEN` |  |
| L512 | FÓRMULA | `LET vcomienza = -1;` |  |
| L514 | FÓRMULA | `LET vcontador = vcontador + 1 ;` |  |
| L540 | FÓRMULA | `LET vmonto_tot =( vmonto_tot * -1);` | 🔴 MONEY/aritmética financiera |
| L570 | VALIDACIÓN_NULL | `IF (vvchrnombrecorto is null)THEN` |  |
| L574 | VALIDACIÓN_NULL | `IF (vvchrcuentaord is null)THEN` |  |
| L578 | VALIDACIÓN_NULL | `IF (vvchrnombreord is null)THEN` |  |
| | *…39 más…* | | |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `arch` | ENTIDAD | archivo | 🟡 INFERIDO | nombre_sp |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_gen_hist_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_gen_hist_manco_bei.sql` |
| **LOC (1er CREATE)** | 188 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "genera Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico/historial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 24/05/2013 |
| MODIFICACION | Se agrego el valor de retorno integer que es el id_adminMancoTemp (06/06/2018) |
| FECHA | 21/03/2017 |

### Firma

```sql
CREATE PROCEDURE sp_gen_hist_manco_bei(
  pNumCliente                  VARCHAR(9)
  pIdUsuario                   INTEGER
  pIdAdminManco                INTEGER
  pStatusAut                   SMALLINT
) RETURNING CHAR(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `VARCHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdAdminManco` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |
| `pStatusAut` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INT` | L16 |
| `cod_ret` | `CHAR(5)` | L17 |
| `stipoOper` | `SMALLINT` | L18 |
| `sTipoMov` | `SMALLINT` | L19 |
| `sCountDet` | `SMALLINT` | L20 |
| `sCountH` | `SMALLINT` | L21 |
| `id_adminMancoTemp` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L72 |
| `bei_admin_manco_det_temp` | `bdibei` | no | SELECT | L107 |
| `bei_admin_manco_det_temp_hist` | `bdibei` | no | SELECT | L110 |
| `bei_admin_manco_det_temp_hist` | `bdibei` | no | DELETE | L113 |
| `bei_admin_manco_temp_hist` | `bdibei` | no | SELECT | L119 |
| `bei_admin_manco_temp_hist` | `bdibei` | no | DELETE | L124 |
| `bei_admin_manco_temp_hist` | `bdibei` | no | INSERT | L132 |
| `bei_admin_manco_temp` | `bdibei` | no | DELETE | L156 |
| `bei_admin_manco_det_temp_hist` | `bdibei` | no | INSERT | L169 |
| `bei_admin_manco_det_temp` | `bdibei` | no | DELETE | L178 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `hist` | MODIF | histórico/historial | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_gen_hist_servicioplus_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_gen_hist_servicioplus_bei.sql` |
| **LOC (1er CREATE)** | 242 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "genera servicio y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico/historial)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se guarda la información relevante del servicio anterior de la empresa a tablas historicas |
| FECHA | 12/08/2014 |
| MODIFICACION | para borrar el registro de los avatar que tenian lo usuarios. |
| MODIFICACION | Berenice Noriega Guevara - BanCoppel - Coordinación Internet |
| FECHA | 24-Feb-2015 |

### Firma

```sql
CREATE PROCEDURE sp_gen_hist_servicioplus_bei(
  pNumcte                      VARCHAR(9)
) RETURNING CHAR (5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumcte` | `VARCHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L19 |
| `cod_ret` | `CHAR (5)` | L20 |
| `vNumCte` | `VARCHAR(9)` | L21 |
| `vIdusuario` | `INTEGER` | L22 |
| `vIdperfil` | `INTEGER` | L23 |
| `sTipoMov` | `SMALLINT` | L25 |
| `sCountDet` | `SMALLINT` | L26 |
| `sCountH` | `SMALLINT` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_contratacion` | `bdibei` | no | SELECT | L57 |
| `bei_contratacion_his` | `bdibei` | no | INSERT | L60 |
| `bei_servicio` | `bdibei` | no | SELECT | L76 |
| `bei_servicio_his` | `bdibei` | no | INSERT | L79 |
| `bei_usuario` | `bdibei` | no | SELECT | L104 |
| `bei_usuario_perfil` | `bdibei` | no | SELECT | L109 |
| `bei_datos_usuario_his` | `bdibei` | no | INSERT | L134 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L140 |
| `bei_usuario_his` | `bdibei` | no | INSERT | L145 |
| `bei_avatar` | `bdibei` | no | SELECT | L158 |
| `bei_solicitudtoken_his` | `bdibei` | no | INSERT | L174 |
| `bei_tokensolicitud_his` | `bdibei` | no | INSERT | L194 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L197 |
| `bei_envios_his` | `bdibei` | no | INSERT | L212 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_doctos_previos_recontrata` | `bdidigital` | ⚠️ sí | L228 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `hist` | MODIF | histórico/historial | 🔵 CONVENCIÓN | nombre_sp |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?plus_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?plus_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabardatosusuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_grabardatosusuario_bei.sql` |
| **LOC (1er CREATE)** | 118 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "graba datos, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_registra_telefonos`, `sp_registra_correos` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Registra Datos de Usuario |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_grabardatosusuario_bei(
  pNumCliente                  VARCHAR(9)
  pIdUsuario                   INTEGER
  pNumTel                      VARCHAR(15)
  pCiaCel                      INT
  pEmail                       VARCHAR(80)
) RETURNING CHAR (5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `VARCHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pNumTel` | `VARCHAR(15)` | — | — |
| `pCiaCel` | `INT` | — | — |
| `pEmail` | `VARCHAR(80)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L18 |
| `cCod_ret` | `CHAR (5)` | L19 |
| `vNumCliente` | `VARCHAR(9)` | L20 |
| `v_Empresa` | `CHAR(3)` | L22 |
| `v_TipoTel` | `SMALLINT` | L23 |
| `v_Extension` | `CHAR(5)` | L24 |
| `v_Canal` | `SMALLINT` | L25 |
| `v_UserInsert` | `CHAR(8)` | L26 |
| `v_codret1` | `CHAR(5)` | L27 |
| `v_codret2` | `CHAR(5)` | L28 |
| `v_TipoCorreo` | `SMALLINT` | L29 |
| `sIdusuario` | `INTEGER` | L31 |
| `sNombre` | `CHAR(250)` | L33 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L76 |
| `bei_datos_usuario` | `bdibei` | no | UPDATE | L90 |
| `si_telefonos_actual` | `bdinteg` | ⚠️ sí | SELECT | L93 |
| `si_correos` | `bdinteg` | ⚠️ sí | SELECT | L103 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_telefonos` | `bdinteg` | ⚠️ sí | L96 |
| `sp_registra_correos` | `bdinteg` | ⚠️ sí | L105 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L38 | FÓRMULA | `LET sIdusuario = -1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_grabarrespuesta_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_grabarrespuesta_bei.sql` |
| **LOC (1er CREATE)** | 70 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "graba respuesta y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 5 tabla(s) con operaciones: INSERT, DELETE, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | GRABA LAS RESPUESTAS DE SEGURIDAD UNA POR UNA |
| FECHA | 05/06/2013 |
| MODIFICACION | En caso de que hacer un Reset y se pase por la activación nuevamanete, se valida |
| MODIFICACION | Berenice Noriega - BanCoppel |
| FECHA | 11 Diciembre 2014 |

### Firma

```sql
CREATE PROCEDURE sp_grabarrespuesta_bei(
  pIdUsuario                   INTEGER
  pIdPregunta                  INT
  pDescResp                    VARCHAR(80)
  pBorrar                      INT
) RETURNING CHAR (5) AS cCod_ret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdPregunta` | `INT` | — | — |
| `pDescResp` | `VARCHAR(80)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pBorrar` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L19 |
| `cCod_ret` | `CHAR (5)` | L20 |
| `cIdUsuario` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L38 |
| `bei_respuestas` | `bdibei` | no | SELECT | L48 |
| `bei_respuestas` | `bdibei` | no | DELETE | L48 |
| `bei_preguntas_his` | `bdibei` | no | SELECT | L53 |
| `bei_respuestas` | `bdibei` | no | INSERT | L63 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `graba` | ACCION | graba / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `respuesta` | ENTIDAD | respuesta | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_archivonom_batch_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_archivonom_batch_bei.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda archivo, nómina y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (proceso batch)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda el nombre de los archivos de dispersion en proceso batch |
| FECHA | 08/MARZO/2016 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_archivonom_batch_bei(
  p_nombreTemporal             CHAR(17)
  p_codEmpresa                 CHAR(3)
  p_idUsuario                  INTEGER
  p_fechaApp                   CHAR(10)
  p_idEstatus                  INTEGER
  p_concepto                   CHAR(10)
  p_importe                    MONEY(10,2)
  p_empleados                  INTEGER
  p_motivo                     VARCHAR(30)
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_nombreTemporal` | `CHAR(17)` | `nom`=nómina | 🟡 INFERIDO |
| `p_codEmpresa` | `CHAR(3)` | — | — |
| `p_idUsuario` | `INTEGER` | — | — |
| `p_fechaApp` | `CHAR(10)` | — | — |
| `p_idEstatus` | `INTEGER` | — | — |
| `p_concepto` | `CHAR(10)` | — | — |
| `p_importe` | `MONEY(10,2)` | — | — |
| `p_empleados` | `INTEGER` | — | — |
| `p_motivo` | `VARCHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L23 |
| `sql_err` | `INTEGER` | L24 |
| `sIdOper` | `INTEGER` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos_eval` | `bdibei` | no | INSERT | L74 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |
| `batch` | MODIF | proceso batch (por lotes) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_guarda_aut_manc_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_aut_manc_bei.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda Autorizacion de Mancomunidad por Cuenta |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_aut_manc_bei(
  pIdUser                      INTEGER
  pNumCte                      CHAR(9)
  pNumCta                      CHAR(16)
  pAutoriza                    CHAR(1)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUser` | `INTEGER` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumCta` | `CHAR(16)` | — | — |
| `pAutoriza` | `CHAR(1)` | `aut`=autorización | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_Ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad` | `bdibei` | no | INSERT | L52 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_aut_manc_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_aut_manc_bei_historico.sql` |
| **LOC (1er CREATE)** | 45 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda Autorizacion de Mancomunidad por Cuenta |
| FECHA | 04/06/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_aut_manc_bei_historico(
  pIdUser                      INTEGER
  pNumCte                      CHAR(9)
  pNumCta                      CHAR(16)
  pAutoriza                    CHAR(1)
  pidBitacoraAdmin             INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUser` | `INTEGER` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumCta` | `CHAR(16)` | — | — |
| `pAutoriza` | `CHAR(1)` | `aut`=autorización | 🟡 INFERIDO |
| `pidBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_Ret` | `CHAR(5)` | L9 |
| `sql_err` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad_historico` | `bdibei` | no | INSERT | L38 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_bei_mancomunidad_temporal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_bei_mancomunidad_temporal.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y identificador (temporal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que guarda en la tabla bei_mancomunidad_temporal |
| FECHA | 18/07/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_bei_mancomunidad_temporal(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L15 |
| `cod_ret` | `CHAR(5)` | L16 |
| `vnum_cta` | `CHAR(20)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_mancomunidad` | `bdibei` | no | SELECT | L41 |
| `bei_mancomunidad_temporal` | `bdibei` | no | INSERT | L45 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?mun` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ad_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `temp` | MODIF | temporal | 🔵 CONVENCIÓN | nombre_sp |
| `?oral` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?mun`, `?ad_`, `?oral` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_bei_operaciones_temporal`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_bei_operaciones_temporal.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad y operaciones (temporal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que guarda en la tabla bei_operaciones_temporal |
| FECHA | 18/07/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_bei_operaciones_temporal(
  pIdPerfil                    INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdPerfil` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L16 |
| `cod_ret` | `CHAR(5)` | L17 |
| `vid_oper` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operaciones` | `bdibei` | no | SELECT | L42 |
| `bei_operaciones_temporal` | `bdibei` | no | INSERT | L46 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `temp` | MODIF | temporal | 🔵 CONVENCIÓN | nombre_sp |
| `?oral` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?oral` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_cons_mov`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_cons_mov.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda movimiento" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guardar la informacion para solicitud de generacion de archivo de movimientos |
| FECHA | 08/Julio/2016 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_cons_mov(
  pEmpresa                     CHAR(3)
  pNum_cliente                 CHAR(9)
  pId_usuario                  CHAR(20)
  pCuenta                      CHAR(20)
  pF_inicial                   CHAR(12)
  pF_final                     CHAR(12)
) RETURNING CHAR(5) AS cod_ret, CHAR(25) AS folio
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNum_cliente` | `CHAR(9)` | — | — |
| `pId_usuario` | `CHAR(20)` | — | — |
| `pCuenta` | `CHAR(20)` | — | — |
| `pF_inicial` | `CHAR(12)` | — | — |
| `pF_final` | `CHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCount` | `INTEGER` | L13 |
| `vNumCliente` | `CHAR(9)` | L14 |
| `vConsecutivo` | `CHAR(7)` | L15 |
| `vFolio` | `CHAR(25)` | L16 |
| `sql_err` | `INTEGER` | L18 |
| `cod_ret` | `CHAR(5)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `informix` | `bdibei` | no | INSERT | L75 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_guarda_ctas_mod_alta_oper_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_ctas_mod_alta_oper_historico.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda cuentas (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se obtienen cuentas de usuario y se llama a sp que valida qué cuentas se modificaron al realizar |
| FECHA | 16/07/2018 |
| FECHA | 07-AGOSTO-2018 |
| FECHA | 15-FEB-2019, SE AGREGO VALIDACION DE PIDPERFIL Y SU CONSULTA DESDE LA TABLA BEI_USUARIO_PERFIL |

### Firma

```sql
CREATE PROCEDURE sp_guarda_ctas_mod_alta_oper_historico(
  pNumCliente                  CHAR(20)
  pIdUsuario                   INTEGER
  pIdPerfil                    INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(20)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdPerfil` | `INTEGER` | — | — |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `vnum_cta` | `CHAR(20)` | L7 |
| `vis_cta_mod` | `BOOLEAN` | L8 |
| `vIdPerfil` | `INTEGER` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario_perfil` | `bdibei` | no | SELECT | L50 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L57 |
| `bei_mancomunidad` | `bdibei` | no | SELECT | L64 |
| `bei_operaciones` | `bdibei` | no | SELECT | L70 |
| `bei_menu_oper` | `bdibei` | no | SELECT | L75 |
| `bei_perfil_oper_ctas_mod_historico` | `bdibei` | no | INSERT | L86 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `ctas` | ENTIDAD | cuentas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_mod_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_mod_`, `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_ctas_mod_edicion_oper_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_ctas_mod_edicion_oper_historico.sql` |
| **LOC (1er CREATE)** | 238 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda cuentas y [polisemia] Dictamen (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se obtienen cuentas de usuario y se valida qué cuentas se modificaron al realizar |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_ctas_mod_edicion_oper_historico(
  pNumCliente                  CHAR(20)
  pIdUsuario                   INTEGER
  pIdPerfil                    INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(20)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdPerfil` | `INTEGER` | — | — |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `vnum_cta` | `CHAR(20)` | L8 |
| `vis_cta_mod` | `BOOLEAN` | L9 |
| `vautoriza_ant` | `BOOLEAN` | L10 |
| `vautoriza_act` | `BOOLEAN` | L11 |
| `vid_menu_oper` | `INTEGER` | L12 |
| `vcont_oper_ant` | `INTEGER` | L13 |
| `vcont_oper_act` | `INTEGER` | L14 |
| `vmanco_ant` | `BOOLEAN` | L15 |
| `vmanco_act` | `BOOLEAN` | L16 |
| `vmonto_min_ant` | `DECIMAL(16,2)` | L17 |
| `vmonto_min_act` | `DECIMAL(16,2)` | L18 |
| `vmonto_max_ant` | `DECIMAL(16,2)` | L19 |
| `vmonto_max_act` | `DECIMAL(16,2)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L71 |
| `bei_mancomunidad` | `bdibei` | no | SELECT | L80 |
| `bei_mancomunidad_temporal` | `bdibei` | no | SELECT | L84 |
| `bei_menu_oper` | `bdibei` | no | SELECT | L95 |
| `bei_operaciones` | `bdibei` | no | SELECT | L105 |
| `bei_operaciones_temporal` | `bdibei` | no | SELECT | L112 |
| `bei_perfil_oper_ctas_mod_historico` | `bdibei` | no | INSERT | L220 |
| `bei_mancomunidad_temporal` | `bdibei` | no | DELETE | L232 |
| `bei_operaciones_temporal` | `bdibei` | no | DELETE | L233 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ctas` | ENTIDAD | cuentas | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_mod_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dic` | ENTIDAD | [polisemia] Dictamen (bdicnweb:sp_dic_* — decisión creditici | 🔵 CONVENCIÓN | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_mod_e`, `?ion_`, `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_erroresarchivo_batch`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_erroresarchivo_batch.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda error y archivo (proceso batch)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se registran los errores que se encuentran en los archivos de dispersión en proceso batch |
| FECHA | 08/MARZO/2016 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_erroresarchivo_batch(
  p_idEvaluacion               INTEGER
  p_nombreTemporal             CHAR(17)
  p_renglon                    INTEGER
  p_idCatError                 INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_idEvaluacion` | `INTEGER` | — | — |
| `p_nombreTemporal` | `CHAR(17)` | — | — |
| `p_renglon` | `INTEGER` | — | — |
| `p_idCatError` | `INTEGER` | `error`=error | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L19 |
| `sql_err` | `INTEGER` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_errores_archivo` | `bdibei` | no | INSERT | L50 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `error` | ENTIDAD | error | 🔵 CONVENCIÓN | nombre_sp |
| `?es` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `batch` | MODIF | proceso batch (por lotes) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?es` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_oper_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_oper_bei.sql` |
| **LOC (1er CREATE)** | 50 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda OPERACIONES de Mancomunidad por Cuenta |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_oper_bei(
  pIdPerfil                    INTEGER
  pIdOper                      INTEGER
  pNumCta                      CHAR(16)
  pMontoMin                    DECIMAL(16,2)
  pMontoMax                    DECIMAL(16,2)
  pMancomunado                 CHAR(1)
  pCreatedBy                   INTEGER
) RETURNING char(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdPerfil` | `INTEGER` | — | — |
| `pIdOper` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |
| `pNumCta` | `CHAR(16)` | — | — |
| `pMontoMin` | `DECIMAL(16,2)` | — | — |
| `pMontoMax` | `DECIMAL(16,2)` | — | — |
| `pMancomunado` | `CHAR(1)` | — | — |
| `pCreatedBy` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L12 |
| `sql_err` | `INTEGER` | L13 |
| `sIdOper` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operaciones` | `bdibei` | no | INSERT | L40 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_oper_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_oper_bei_historico.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo · `guarda` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda OPERACIONES de Mancomunidad por Cuenta |
| FECHA | 04/06/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_guarda_oper_bei_historico(
  pIdPerfil                    INTEGER
  pIdOper                      INTEGER
  pNumCta                      CHAR(16)
  pMontoMin                    DECIMAL(16,2)
  pMontoMax                    DECIMAL(16,2)
  pMancomunado                 CHAR(1)
  pCreatedBy                   INTEGER
  pidBitacoraAdmin             INTEGER
  sIdOper                      INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdPerfil` | `INTEGER` | — | — |
| `pIdOper` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |
| `pNumCta` | `CHAR(16)` | — | — |
| `pMontoMin` | `DECIMAL(16,2)` | — | — |
| `pMontoMax` | `DECIMAL(16,2)` | — | — |
| `pMancomunado` | `CHAR(1)` | — | — |
| `pCreatedBy` | `INTEGER` | — | — |
| `pidBitacoraAdmin` | `INTEGER` | — | — |
| `sIdOper` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L14 |
| `sql_err` | `INTEGER` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operaciones_historico` | `bdibei` | no | INSERT | L42 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_guarda_token_manc_temp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_guarda_token_manc_temp_bei.sql` |
| **LOC (1er CREATE)** | 38 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "guarda token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (temporal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `guarda` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_guarda_token_manc_temp_bei(
  id_usuario_admin             INTEGER
  tipo_mov                     SMALLINT
  id_usuario                   INTEGER
  num_cliente                  CHAR(9)
  ns_token                     VARCHAR(10)
  id_status_token              INTEGER
) RETURNING CHAR(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `id_usuario_admin` | `INTEGER` | — | — |
| `tipo_mov` | `SMALLINT` | — | — |
| `id_usuario` | `INTEGER` | — | — |
| `num_cliente` | `CHAR(9)` | — | — |
| `ns_token` | `VARCHAR(10)` | `token`=token (autenticación) | ✅ CÓDIGO |
| `id_status_token` | `INTEGER` | `token`=token (autenticación) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iIdOperacion` | `INTEGER` | L11 |
| `cod_ret` | `CHAR(5)` | L12 |
| `sql_err` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | INSERT | L27 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `guarda` | ACCION | guarda / almacena | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `temp` | MODIF | temporal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ini_session_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_ini_session_bei.sql` |
| **LOC (1er CREATE)** | 166 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=3 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS PARA INICIAR LA SESION EN LA BANCA EMPRESARIAL |
| FECHA | 26/04/2013 |
| MODIFICACION | Se inicializa la variabe de fechabloqueo cuando ele status es 95 o 10. |
| MODIFICACION | Berenice Noriega - BanCoppel. |
| FECHA | 23/Junio/2014 |

### Firma

```sql
CREATE PROCEDURE sp_ini_session_bei(
  pEmpresa                     char(3)
  pUsuario                     char(50)
  pPass                        char(50)
  pIp                          char(15)
) RETURNING char(5), char (20), char(50), smallint, integer, char(19),DATETIME YEAR TO SECOND, integer,char(3), char(20),char(50),smallint,DATETIME YEAR TO SECOND
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char(3)` | — | — |
| `pUsuario` | `char(50)` | — | — |
| `pPass` | `char(50)` | `ss`=ss — subsistema / canal de monitoreo (abreviación — envia_monitorsol_*_ss_* — bdisolic) | 🔴 SINTÉTICO |
| `pIp` | `char(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sIdUsuario` | `integer` | L7 |
| `sNumCliente` | `char (20)` | L8 |
| `iIdStatus` | `smallint` | L9 |
| `fecPrimAcceso` | `date` | L10 |
| `fecUltAcceso` | `char(19)` | L11 |
| `iIdStatusToken` | `integer` | L12 |
| `sNombre` | `CHAR(150)` | L13 |
| `vFecha` | `DATETIME YEAR TO SECOND` | L14 |
| `vIdEmpresa` | `char(3)` | L15 |
| `nomEmpresa` | `char(50)` | L16 |
| `sCuenta` | `CHAR(20)` | L17 |
| `sIdTipoUsuario` | `smallint` | L18 |
| `sFBloqueoTemp` | `DATETIME YEAR TO SECOND` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L66 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L82 |
| `bei_usuario` | `bdibei` | no | UPDATE | L98 |
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L101 | VALIDACIÓN_NULL | `IF fecPrimAcceso IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_ini_se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ini_se`, `ss`, `?ion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_inserta_altaempleado_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_altaempleado_manco_bei.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta empleado, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_inserta_altaempleado_manco_bei(
  pnumero_empleado             INTEGER
  pnombre_empleado             VARCHAR(100)
  papellidos_pat_empleado      VARCHAR(100)
  papellidos_mat_empleado      VARCHAR(100)
  pcuenta_destino              CHAR(12)
  pclave_banco                 INTEGER
  pid_usuario                  INTEGER
) RETURNING CHAR(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pnumero_empleado` | `INTEGER` | `empleado`=empleado | ✅ CÓDIGO |
| `pnombre_empleado` | `VARCHAR(100)` | `empleado`=empleado | ✅ CÓDIGO |
| `papellidos_pat_empleado` | `VARCHAR(100)` | `empleado`=empleado | ✅ CÓDIGO |
| `papellidos_mat_empleado` | `VARCHAR(100)` | `empleado`=empleado | ✅ CÓDIGO |
| `pcuenta_destino` | `CHAR(12)` | — | — |
| `pclave_banco` | `INTEGER` | — | — |
| `pid_usuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L12 |
| `sql_err` | `INTEGER` | L13 |
| `sIdOper` | `INTEGER` | L14 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L64 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `empleado` | ENTIDAD | empleado | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_inserta_aut_manco_temp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_aut_manco_temp_bei.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (temporal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda Autorizacion de Mancomunidad por Cuenta |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_aut_manco_temp_bei(
  pIdUser                      INTEGER
  pNumCte                      CHAR(9)
  pNumCta                      CHAR(20)
  pAutoriza                    CHAR(1)
  id_admin_manco               INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUser` | `INTEGER` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |
| `pAutoriza` | `CHAR(1)` | `aut`=autorización | 🟡 INFERIDO |
| `id_admin_manco` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_Ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_det_temp` | `bdibei` | no | INSERT | L50 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `temp` | MODIF | temporal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_inserta_aut_manco_temp_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_aut_manco_temp_bei2.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (temporal)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda Autorizacion de Mancomunidad por Cuenta |
| FECHA | 24/05/2013 |
| DESCRIPCION | Guarda Autorizacion de Mancomunidad por Cuenta |
| FECHA | 20/04/2017 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_aut_manco_temp_bei2(
  pIdUser                      INTEGER
  pNumCte                      CHAR(9)
  pNumCta                      CHAR(20)
  pAutoriza                    CHAR(1)
  id_admin_manco               INTEGER
  Mancomunidad                 CHAR(1)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUser` | `INTEGER` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumCta` | `CHAR(20)` | — | — |
| `pAutoriza` | `CHAR(1)` | `aut`=autorización | 🟡 INFERIDO |
| `id_admin_manco` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |
| `Mancomunidad` | `CHAR(1)` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_Ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_det_temp` | `bdibei` | no | INSERT | L58 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `temp` | MODIF | temporal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_inserta_bei_admin_manco_temp_hist_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_bei_admin_manco_temp_hist_historico.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad, Administrador — rol de usuario con privilegios administrativos y Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (temporal, histórico/historial, histórico)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=6 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que guarda en la tabla bei_admin_manco_temp_hist_historico para poder |
| FECHA | 06/06/2018 |
| FECHA | 07-AGOSTO-2018 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_bei_admin_manco_temp_hist_historico(
  pNumCliente                  VARCHAR(9)
  pIdUsuario                   INTEGER
  pIdAdminMancoTemp            INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `VARCHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdAdminMancoTemp` | `INTEGER` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos · `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |
| `pIdBitacoraAdmin` | `INTEGER` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L19 |
| `cod_ret` | `CHAR(5)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp_hist` | `bdibei` | no | SELECT | L40 |
| `bei_admin_manco_temp_hist_historico` | `bdibei` | no | INSERT | L42 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `temp` | MODIF | temporal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `hist` | MODIF | histórico/historial | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_inserta_bei_admin_manco_temp_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_bei_admin_manco_temp_historico.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad, Administrador — rol de usuario con privilegios administrativos y Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (temporal, histórico)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=5 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que guarda en la tabla bei_admin_manco_temp_historico para poder consultar el historico de las operaciones mancomunadas |
| FECHA | 30/05/2018 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_bei_admin_manco_temp_historico(
  pNumCliente                  VARCHAR(9)
  pIdUsuario                   INTEGER
  pIdAdminManco                INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `VARCHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdAdminManco` | `INTEGER` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos · `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |
| `pIdBitacoraAdmin` | `INTEGER` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L12 |
| `cod_ret` | `CHAR(5)` | L13 |
| `vCount` | `SMALLINT` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L38 |
| `bei_admin_manco_temp_historico` | `bdibei` | no | INSERT | L49 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `temp` | MODIF | temporal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_inserta_bendispmanco_odp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_bendispmanco_odp_bei.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Inserta Dispersion de Orden de Pago |
| FECHA | 24/02/2015 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_bendispmanco_odp_bei(
  pIdMancomunidad              INTEGER
  pNumCliente                  CHAR(9)
  pTipoDisp                    INTEGER
  pFecha                       DATE
  pConcepto                    VARCHAR(30)
  pCta_origen                  VARCHAR(12)
  pAlias                       VARCHAR(20)
  pPrimerNombre                VARCHAR(30)
  pSegundoNombre               VARCHAR(30)
  pApellidoPaterno             VARCHAR(30)
  pApellidoMaterno             VARCHAR(30)
  pImporte                     MONEY(14,2)
  pTelefono                    VARCHAR(25)
  pDireccion                   VARCHAR(100)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | 🔵 CONVENCIÓN |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pTipoDisp` | `INTEGER` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFecha` | `DATE` | — | — |
| `pConcepto` | `VARCHAR(30)` | — | — |
| `pCta_origen` | `VARCHAR(12)` | — | — |
| `pAlias` | `VARCHAR(20)` | — | — |
| `pPrimerNombre` | `VARCHAR(30)` | — | — |
| `pSegundoNombre` | `VARCHAR(30)` | — | — |
| `pApellidoPaterno` | `VARCHAR(30)` | — | — |
| `pApellidoMaterno` | `VARCHAR(30)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pTelefono` | `VARCHAR(25)` | — | — |
| `pDireccion` | `VARCHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L6 |
| `sql_err` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_beneficiariosmanco_odp` | `bdibei` | no | INSERT | L70 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET vCodRet='00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_bendi` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?_odp_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_bendi`, `?_odp_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_inserta_disp_ordpago_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_disp_ordpago_bei.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta ordenante, pago y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Inserta Dispersion de Nomina |
| FECHA | 24/02/2015 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_disp_ordpago_bei(
  pNumCliente                  CHAR(9)
  pTipoDisp                    INTEGER
  pFecha                       VARCHAR(10)
  pNumReferencia               VARCHAR(16)
  pConcepto                    VARCHAR(30)
  pArchivo                     VARCHAR(12)
  pCta_origen                  VARCHAR(12)
  pAlias                       VARCHAR(20)
  pNombre_Completo             VARCHAR(104)
  pImporte                     MONEY(14,2)
  pClave_Envio                 VARCHAR(12)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pTipoDisp` | `INTEGER` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFecha` | `VARCHAR(10)` | — | — |
| `pNumReferencia` | `VARCHAR(16)` | — | — |
| `pConcepto` | `VARCHAR(30)` | — | — |
| `pArchivo` | `VARCHAR(12)` | — | — |
| `pCta_origen` | `VARCHAR(12)` | — | — |
| `pAlias` | `VARCHAR(20)` | — | — |
| `pNombre_Completo` | `VARCHAR(104)` | — | — |
| `pImporte` | `MONEY(14,2)` | — | — |
| `pClave_Envio` | `VARCHAR(12)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_dispersiones_odp` | `bdibei` | no | INSERT | L71 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET vCodRet='00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_di` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `pago` | ENTIDAD | pago | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_di` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_inserta_monto_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_monto_manco_bei.sql` |
| **LOC (1er CREATE)** | 186 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta monto, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Inserta Mancomunidad Administracion Montos |
| FECHA | 05/01/2015 |
| FECHA | 22-ENERO-2015 |
| MODIFICACION | Se agrega filtro extra para bei_admin_manco_montos_temp por prestricc |
| FECHA | 30 Junio 2015 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_monto_manco_bei(
  pIdusuario                   INTEGER
  pNumCliente                  CHAR(9)
  tipo_mov                     SMALLINT
  prestricc                    CHAR(2)
  pcuenta                      CHAR(16)
  poperacionPropias            CHAR(2)
  plimitePropias               DECIMAL(16,2)
  poperacionTerceros           CHAR(2)
  plimiteTerceros              DECIMAL(16,2)
  poperacionSPEI               CHAR(2)
  plimiteSPEI                  DECIMAL(16,2)
) RETURNING char(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdusuario` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `tipo_mov` | `SMALLINT` | — | — |
| `prestricc` | `CHAR(2)` | — | — |
| `pcuenta` | `CHAR(16)` | — | — |
| `poperacionPropias` | `CHAR(2)` | — | — |
| `plimitePropias` | `DECIMAL(16,2)` | — | — |
| `poperacionTerceros` | `CHAR(2)` | — | — |
| `plimiteTerceros` | `DECIMAL(16,2)` | — | — |
| `poperacionSPEI` | `CHAR(2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `plimiteSPEI` | `DECIMAL(16,2)` | `sp`=stored procedure | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L17 |
| `sql_err` | `INTEGER` | L18 |
| `Id_adminMancoTemp` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_montos_temp` | `bdibei` | no | SELECT | L66 |
| `bei_admin_manco_temp` | `bdibei` | no | INSERT | L72 |
| `bei_admin_manco_montos_temp` | `bdibei` | no | INSERT | L145 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L107 | VALIDACIÓN_NULL | `IF NVL(plimitePropias,'') = '' OR plimitePropias IS NULL THEN` |  |
| L122 | VALIDACIÓN_NULL | `IF NVL(plimiteTerceros,'') = '' OR plimiteTerceros IS NULL THEN` |  |
| L137 | VALIDACIÓN_NULL | `IF NVL(plimiteSPEI,'') = '' OR plimiteSPEI IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_inserta_oper_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_oper_manco_bei.sql` |
| **LOC (1er CREATE)** | 80 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda OPERACIONES de Mancomunidad por Cuenta |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_oper_manco_bei(
  pIdPerfil                    INTEGER
  pIdMenuOper                  INTEGER
  pNumCta                      CHAR(16)
  pMontoMin                    DECIMAL(16,2)
  pMontoMax                    DECIMAL(16,2)
  pMancomunado                 CHAR(1)
  id_admin_manco               INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdPerfil` | `INTEGER` | — | — |
| `pIdMenuOper` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |
| `pNumCta` | `CHAR(16)` | — | — |
| `pMontoMin` | `DECIMAL(16,2)` | — | — |
| `pMontoMax` | `DECIMAL(16,2)` | — | — |
| `pMancomunado` | `CHAR(1)` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |
| `id_admin_manco` | `INTEGER` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L12 |
| `sql_err` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_det_temp` | `bdibei` | no | INSERT | L65 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_inserta_renovtoken_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_renovtoken_manco_bei.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta token, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Inserta en las tablas de mancomunidad los datos para la renovacion de token mancomunado |
| FECHA | 20/08/2018 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_renovtoken_manco_bei(
  pNumClienteAdmin             CHAR(9)
  pIdUsuarioAdmin              INTEGER
  pTipoOper                    SMALLINT
  pTipoMov                     SMALLINT
  pNumCuenta                   CHAR(20)
) RETURNING CHAR(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumClienteAdmin` | `CHAR(9)` | — | — |
| `pIdUsuarioAdmin` | `INTEGER` | — | — |
| `pTipoOper` | `SMALLINT` | — | — |
| `pTipoMov` | `SMALLINT` | — | — |
| `pNumCuenta` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `vIdAdminMancoTemp` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | INSERT | L43 |
| `bei_admin_manco_det_temp` | `bdibei` | no | INSERT | L48 |
| `bei_tokenexpira` | `bdibei` | no | UPDATE | L52 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_renov` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_renov` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_inserta_usuario_adminmanco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_inserta_usuario_adminmanco_bei.sql` |
| **LOC (1er CREATE)** | 261 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta usuario, Administrador — rol de usuario con privilegios administrativos, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Crea usuario Bei |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza a 250 caracteres la longitud de los parametros correspondientes al usuario |
| MODIFICACION | Jos el Hernez Gonzz |
| FECHA | 05-Diciembre-2016 |

### Firma

```sql
CREATE PROCEDURE sp_inserta_usuario_adminmanco_bei(
  pNumCliente                  CHAR(9)
  pIdStatus                    SMALLINT
  pUsuario                     CHAR(250)
  pPass                        CHAR(250)
  pTipoUsuario                 SMALLINT
  pNombrePerfil                CHAR(50)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            Integer
  tipo_mov                     SMALLINT
  pIdusuario                   INTEGER
  pIdPerfil                    INTEGER
) RETURNING char(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdStatus` | `SMALLINT` | — | — |
| `pUsuario` | `CHAR(250)` | `usuario`=usuario | ✅ CÓDIGO |
| `pPass` | `CHAR(250)` | — | — |
| `pTipoUsuario` | `SMALLINT` | `usuario`=usuario | ✅ CÓDIGO |
| `pNombrePerfil` | `CHAR(50)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `Integer` | `usuario`=usuario | ✅ CÓDIGO |
| `tipo_mov` | `SMALLINT` | — | — |
| `pIdusuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdPerfil` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L8 |
| `sql_err` | `INTEGER` | L9 |
| `Id_adminMancoTemp` | `INTEGER` | L10 |
| `sPass` | `CHAR(250)` | L12 |
| `sFpass` | `DATE` | L13 |
| `sPass1` | `CHAR(250)` | L14 |
| `sFpass1` | `DATE` | L15 |
| `sPass2` | `CHAR(250)` | L16 |
| `sFpass2` | `DATE` | L17 |
| `sPass3` | `CHAR(250)` | L18 |
| `sFpass3` | `DATE` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L70 |
| `informix` | `bdibei` | no | INSERT | L89 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_insertacuentatercerosmancomunadas_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertacuentatercerosmancomunadas_bei.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta cuenta, ro — Rol Operativo, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=4 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertacuentatercerosmancomunadas_bei(
  pfoliosuc                    CHAR(16)
  pcuenta_origen               CHAR(12)
  pcuenta_destino              CHAR(12)
  pimporte                     MONEY(14,2)
  pmoneda                      CHAR(2)
  preferencia                  CHAR(40)
  preferenciabe                CHAR(40)
  pnombre_beneficiario         VARCHAR(100)
  pf_aplicacion                CHAR(10)
  pf_operacion                 CHAR(10)
  pid_usuario                  INTEGER
  pid_cat_operacion            INTEGER
  pstatusoperacion             CHAR(1)
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pfoliosuc` | `CHAR(16)` | — | — |
| `pcuenta_origen` | `CHAR(12)` | `cuenta`=cuenta | 🔵 CONVENCIÓN |
| `pcuenta_destino` | `CHAR(12)` | `cuenta`=cuenta | 🔵 CONVENCIÓN |
| `pimporte` | `MONEY(14,2)` | — | — |
| `pmoneda` | `CHAR(2)` | — | — |
| `preferencia` | `CHAR(40)` | — | — |
| `preferenciabe` | `CHAR(40)` | — | — |
| `pnombre_beneficiario` | `VARCHAR(100)` | — | — |
| `pf_aplicacion` | `CHAR(10)` | — | — |
| `pf_operacion` | `CHAR(10)` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `pid_cat_operacion` | `INTEGER` | — | — |
| `pstatusoperacion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L17 |
| `sql_err` | `INTEGER` | L18 |
| `sIdOper` | `INTEGER` | L19 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L89 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `cuenta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?terce` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?munadas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?terce`, `ro`, `?s`, `?munadas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertadisp_orden_pago_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertadisp_orden_pago_manco_bei.sql` |
| **LOC (1er CREATE)** | 190 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta orden, pago, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| MODIFICACION | INC03527 EmpresaNet - Dispersi ODP - Cardiff |
| FECHA | 2025/12/11 |

### Firma

```sql
CREATE PROCEDURE sp_insertadisp_orden_pago_manco_bei(
  pf_aplicacion                CHAR(10)
  pcuenta_origen               CHAR(12)
  pnombre_archivo              CHAR(17)
  pf_dispersion                CHAR(10)
  pcte_empresa                 CHAR(20)
  pid_empresa                  CHAR(3)
  pid_catOperacion             INTEGER
  pid_usuario                  INTEGER
  ptamano_archivo              INTEGER
  ptipo_archivo                INTEGER
  ptipo_cuentas                INTEGER
  ptipo_oper                   SMALLINT
  pstatus_archivo              SMALLINT
  pmontoTotal                  MONEY(14,2)
  piva                         MONEY(14,2)
  pivaComsion                  MONEY(14,2)
  pComsion                     MONEY(14,2)
  pCantidadEmpleados           INTEGER
  pConcepto                    CHAR(30)
  pTipoDispersion              INTEGER
  PcargoDispersion             DECIMAL
  PtotalSinComision            DECIMAL
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pf_aplicacion` | `CHAR(10)` | — | — |
| `pcuenta_origen` | `CHAR(12)` | — | — |
| `pnombre_archivo` | `CHAR(17)` | — | — |
| `pf_dispersion` | `CHAR(10)` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pcte_empresa` | `CHAR(20)` | — | — |
| `pid_empresa` | `CHAR(3)` | — | — |
| `pid_catOperacion` | `INTEGER` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `ptamano_archivo` | `INTEGER` | — | — |
| `ptipo_archivo` | `INTEGER` | — | — |
| `ptipo_cuentas` | `INTEGER` | — | — |
| `ptipo_oper` | `SMALLINT` | — | — |
| `pstatus_archivo` | `SMALLINT` | — | — |
| `pmontoTotal` | `MONEY(14,2)` | — | — |
| `piva` | `MONEY(14,2)` | — | — |
| `pivaComsion` | `MONEY(14,2)` | — | — |
| `pComsion` | `MONEY(14,2)` | — | — |
| `pCantidadEmpleados` | `INTEGER` | — | — |
| `pConcepto` | `CHAR(30)` | — | — |
| `pTipoDispersion` | `INTEGER` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `PcargoDispersion` | `DECIMAL` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `PtotalSinComision` | `DECIMAL` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L34 |
| `sql_err` | `INTEGER` | L35 |
| `sIdOper` | `INTEGER` | L36 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos` | `bdibei` | no | SELECT | L141 |
| `bei_archivos` | `bdibei` | no | INSERT | L142 |
| `bei_archivos_orden_pago` | `bdibei` | no | UPDATE | L184 |
| `bei_archivos` | `bdibei` | no | UPDATE | L185 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertadispordenpagoarchivo_manco_bei` | `bdibei` | no | L147 |
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L161 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L55 | FÓRMULA | `LET cod_ret="00001"; -- pf_aplicacion Invalida` |  |
| L59 | FÓRMULA | `LET cod_ret="00002"; -- pcuenta_origen Invalida` |  |
| L63 | FÓRMULA | `LET cod_ret="00003"; -- pnombre_archivo Invalida` |  |
| L67 | FÓRMULA | `LET cod_ret="00004"; -- pf_dispersion Invalida` |  |
| L71 | FÓRMULA | `LET cod_ret="00005"; -- pcte_empresa Invalida` |  |
| L77 | FÓRMULA | `LET cod_ret="00006"; -- pid_empresa Invalida` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?di` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `orden` | ENTIDAD | orden | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `pago` | ENTIDAD | pago | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?di` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertadispersion_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertadispersion_manco_bei.sql` |
| **LOC (1er CREATE)** | 177 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta dispersión — dispersión de nómina, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertadispersion_manco_bei(
  pf_aplicacion                CHAR(10)
  pcuenta_origen               CHAR(12)
  pnombre_archivo              CHAR(17)
  pf_dispersion                CHAR(10)
  pcte_empresa                 CHAR(20)
  pid_empresa                  CHAR(3)
  pid_catOperacion             INTEGER
  pid_usuario                  INTEGER
  ptamano_archivo              INTEGER
  ptipo_archivo                INTEGER
  ptipo_cuentas                INTEGER
  ptipo_oper                   SMALLINT
  pstatus_archivo              SMALLINT
  pmontoTotal                  MONEY(14,2)
  piva                         MONEY(14,2)
  pivaComsion                  MONEY(14,2)
  pComsion                     MONEY(14,2)
  pCantidadEmpleados           INTEGER
  pConcepto                    CHAR(10)
  pTipoDispersion              INTEGER
  PcargoDispersion             DECIMAL
  PtotalSinComision            DECIMAL
  pMotivoPago                  CHAR(30)
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pf_aplicacion` | `CHAR(10)` | — | — |
| `pcuenta_origen` | `CHAR(12)` | — | — |
| `pnombre_archivo` | `CHAR(17)` | — | — |
| `pf_dispersion` | `CHAR(10)` | `sp`=stored procedure · `dispersion`=dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN / ✅ CÓDIGO |
| `pcte_empresa` | `CHAR(20)` | — | — |
| `pid_empresa` | `CHAR(3)` | — | — |
| `pid_catOperacion` | `INTEGER` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `ptamano_archivo` | `INTEGER` | — | — |
| `ptipo_archivo` | `INTEGER` | — | — |
| `ptipo_cuentas` | `INTEGER` | — | — |
| `ptipo_oper` | `SMALLINT` | — | — |
| `pstatus_archivo` | `SMALLINT` | — | — |
| `pmontoTotal` | `MONEY(14,2)` | — | — |
| `piva` | `MONEY(14,2)` | — | — |
| `pivaComsion` | `MONEY(14,2)` | — | — |
| `pComsion` | `MONEY(14,2)` | — | — |
| `pCantidadEmpleados` | `INTEGER` | — | — |
| `pConcepto` | `CHAR(10)` | — | — |
| `pTipoDispersion` | `INTEGER` | `sp`=stored procedure · `dispersion`=dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN / ✅ CÓDIGO |
| `PcargoDispersion` | `DECIMAL` | `sp`=stored procedure · `dispersion`=dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN / ✅ CÓDIGO |
| `PtotalSinComision` | `DECIMAL` | — | — |
| `pMotivoPago` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L28 |
| `sql_err` | `INTEGER` | L29 |
| `sIdOper` | `INTEGER` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos` | `bdibei` | no | UPDATE | L172 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertadispersionarchivo_manco_bei` | `bdibei` | no | L135 |
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L149 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | FÓRMULA | `LET cod_ret="00001"; -- pf_aplicacion Invalida` |  |
| L50 | FÓRMULA | `LET cod_ret="00002"; -- pcuenta_origen Invalida` |  |
| L54 | FÓRMULA | `LET cod_ret="00003"; -- pnombre_archivo Invalida` |  |
| L58 | FÓRMULA | `LET cod_ret="00004"; -- pf_dispersion Invalida` |  |
| L62 | FÓRMULA | `LET cod_ret="00005"; -- pcte_empresa Invalida` |  |
| L66 | FÓRMULA | `LET cod_ret="00006"; -- pid_empresa Invalida` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `dispersion` | ENTIDAD | dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_insertadispersion_manco_odp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertadispersion_manco_odp_bei.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta dispersión — dispersión de nómina, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertadispersion_manco_odp_bei(
  pf_aplicacion                CHAR(10)
  pcuenta_origen               CHAR(12)
  pid_empresa                  CHAR(3)
  pid_catOperacion             INTEGER
  pid_usuario                  INTEGER
  pmontoTotal                  MONEY(14,2)
  piva                         MONEY(14,2)
  pivaComsion                  MONEY(14,2)
  pComsion                     MONEY(14,2)
  PtotalSinComision            DECIMAL
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pf_aplicacion` | `CHAR(10)` | — | — |
| `pcuenta_origen` | `CHAR(12)` | — | — |
| `pid_empresa` | `CHAR(3)` | — | — |
| `pid_catOperacion` | `INTEGER` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `pmontoTotal` | `MONEY(14,2)` | — | — |
| `piva` | `MONEY(14,2)` | — | — |
| `pivaComsion` | `MONEY(14,2)` | — | — |
| `pComsion` | `MONEY(14,2)` | — | — |
| `PtotalSinComision` | `DECIMAL` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L15 |
| `sql_err` | `INTEGER` | L16 |
| `sIdOper` | `INTEGER` | L17 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L77 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | FÓRMULA | `LET cod_ret="00001"; -- pf_aplicacion Invalida` |  |
| L37 | FÓRMULA | `LET cod_ret="00002"; -- pcuenta_origen Invalida` |  |
| L41 | FÓRMULA | `LET cod_ret="00003"; -- pid_empresa Invalida` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `dispersion` | ENTIDAD | dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?_odp_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_odp_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertadispersionarchivo_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertadispersionarchivo_manco_bei.sql` |
| **LOC (1er CREATE)** | 50 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta dispersión — dispersión de nómina, archivo, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertadispersionarchivo_manco_bei(
  pnombre_archivo              CHAR(17)
  pf_dispersion                CHAR(10)
  pcte_empresa                 CHAR(20)
  pid_empresa                  CHAR(3)
  pid_oper                     CHAR(4)
  pid_usuario                  INTEGER
  ptamano_archivo              INTEGER
  ptipo_archivo                INTEGER
  ptipo_cuentas                INTEGER
  ptipo_oper                   SMALLINT
  pstatus_archivo              SMALLINT
  pCantidadEmpleados           INTEGER
  pConcepto                    CHAR(10)
  pTipoDispersion              INTEGER
  PcargoDispersion             DECIMAL(16,2)
  PtotalSinComision            DECIMAL(16,2)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pnombre_archivo` | `CHAR(17)` | `archivo`=archivo | ✅ CÓDIGO |
| `pf_dispersion` | `CHAR(10)` | `sp`=stored procedure · `dispersion`=dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN / ✅ CÓDIGO |
| `pcte_empresa` | `CHAR(20)` | — | — |
| `pid_empresa` | `CHAR(3)` | — | — |
| `pid_oper` | `CHAR(4)` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `ptamano_archivo` | `INTEGER` | `archivo`=archivo | ✅ CÓDIGO |
| `ptipo_archivo` | `INTEGER` | `archivo`=archivo | ✅ CÓDIGO |
| `ptipo_cuentas` | `INTEGER` | — | — |
| `ptipo_oper` | `SMALLINT` | — | — |
| `pstatus_archivo` | `SMALLINT` | `archivo`=archivo | ✅ CÓDIGO |
| `pCantidadEmpleados` | `INTEGER` | — | — |
| `pConcepto` | `CHAR(10)` | — | — |
| `pTipoDispersion` | `INTEGER` | `sp`=stored procedure · `dispersion`=dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN / ✅ CÓDIGO |
| `PcargoDispersion` | `DECIMAL(16,2)` | `sp`=stored procedure · `dispersion`=dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | 🔵 CONVENCIÓN / ✅ CÓDIGO |
| `PtotalSinComision` | `DECIMAL(16,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L21 |
| `sql_err` | `INTEGER` | L22 |
| `sIdOper` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos` | `bdibei` | no | INSERT | L38 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `dispersion` | ENTIDAD | dispersión — dispersión de nómina (sp_dispercionnomina_bpi) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_insertadispordenpagoarchivo_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertadispordenpagoarchivo_manco_bei.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta orden de pago, archivo, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertadispordenpagoarchivo_manco_bei(
  pnombre_archivo              CHAR(17)
  pf_dispersion                CHAR(10)
  pcte_empresa                 CHAR(20)
  pid_empresa                  CHAR(3)
  pid_oper                     CHAR(4)
  pid_usuario                  INTEGER
  ptamano_archivo              INTEGER
  ptipo_archivo                INTEGER
  ptipo_cuentas                INTEGER
  ptipo_oper                   SMALLINT
  pstatus_archivo              SMALLINT
  pCantidadEmpleados           INTEGER
  pConcepto                    CHAR(30)
  pTipoDispersion              INTEGER
  PcargoDispersion             DECIMAL(16,2)
  PtotalSinComision            DECIMAL(16,2)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pnombre_archivo` | `CHAR(17)` | `archivo`=archivo | ✅ CÓDIGO |
| `pf_dispersion` | `CHAR(10)` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pcte_empresa` | `CHAR(20)` | — | — |
| `pid_empresa` | `CHAR(3)` | — | — |
| `pid_oper` | `CHAR(4)` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `ptamano_archivo` | `INTEGER` | `archivo`=archivo | ✅ CÓDIGO |
| `ptipo_archivo` | `INTEGER` | `archivo`=archivo | ✅ CÓDIGO |
| `ptipo_cuentas` | `INTEGER` | — | — |
| `ptipo_oper` | `SMALLINT` | — | — |
| `pstatus_archivo` | `SMALLINT` | `archivo`=archivo | ✅ CÓDIGO |
| `pCantidadEmpleados` | `INTEGER` | — | — |
| `pConcepto` | `CHAR(30)` | — | — |
| `pTipoDispersion` | `INTEGER` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `PcargoDispersion` | `DECIMAL(16,2)` | `sp`=stored procedure · `sp`=stored procedure | 🔵 CONVENCIÓN |
| `PtotalSinComision` | `DECIMAL(16,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L21 |
| `sql_err` | `INTEGER` | L22 |
| `sIdOper` | `INTEGER` | L23 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_archivos_orden_pago` | `bdibei` | no | INSERT | L41 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?di` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ordenpago` | ENTIDAD | orden de pago | 🔵 CONVENCIÓN | nombre_sp |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?di` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertaoperacionesmancomunadasoperador_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertaoperacionesmancomunadasoperador_bei.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta operaciones, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertaoperacionesmancomunadasoperador_bei(
  pfoliosuc                    CHAR(16)
  pcuenta_origen               CHAR(20)
  pcuenta_destino              CHAR(20)
  pimporte                     MONEY(14,2)
  pmoneda                      CHAR(2)
  preferencia                  CHAR(40)
  preferenciabe                CHAR(40)
  pnombre_beneficiario         VARCHAR(100)
  pf_aplicacion                DATE
  pf_operacion                 DATE
  pid_usuario                  INTEGER
  pid_cat_operacion            INTEGER
  pempresa                     CHAR(3)
  psucursal_virtual            CHAR(4)
  pusuario_virtual             CHAR(4)
  pclave_banco                 INTEGER
  ptransaccion_sucursal        CHAR(4)
  pcomision                    MONEY(14,2)
  pivacomision                 MONEY(14,2)
  pnombre_usuario              VARCHAR(40)
  prfc                         VARCHAR(18)
  ptipo_cuenta_beneficiario    VARCHAR(40)
  pvalor_iva                   MONEY(14,2)
  preferencia_cobranza         VARCHAR(40)
  pbanco_receptor              VARCHAR(100)
  pnumtransferenciacargo       CHAR(4)
  pnumtransferenciaabono       CHAR(4)
  pmontototal                  MONEY(14,2)
  pcategoria                   CHAR(2)
  pconvenio                    CHAR(3)
  preftelefono                 CHAR(20)
  prefverificador              CHAR(20)
  pnombre_archivo              CHAR(17)
  pstatusoperacion             CHAR(1)
  numero_Empleado              INTEGER
  nombre_empleado              VARCHAR(100)
  apellidos_pat_empleado       VARCHAR(100)
  apellidos_mat_empleado       VARCHAR(100)
) RETURNING char(5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pfoliosuc` | `CHAR(16)` | — | — |
| `pcuenta_origen` | `CHAR(20)` | — | — |
| `pcuenta_destino` | `CHAR(20)` | — | — |
| `pimporte` | `MONEY(14,2)` | — | — |
| `pmoneda` | `CHAR(2)` | — | — |
| `preferencia` | `CHAR(40)` | — | — |
| `preferenciabe` | `CHAR(40)` | — | — |
| `pnombre_beneficiario` | `VARCHAR(100)` | — | — |
| `pf_aplicacion` | `DATE` | — | — |
| `pf_operacion` | `DATE` | `ope`=operación | 🟡 INFERIDO |
| `pid_usuario` | `INTEGER` | — | — |
| `pid_cat_operacion` | `INTEGER` | `ope`=operación | 🟡 INFERIDO |
| `pempresa` | `CHAR(3)` | — | — |
| `psucursal_virtual` | `CHAR(4)` | — | — |
| `pusuario_virtual` | `CHAR(4)` | — | — |
| `pclave_banco` | `INTEGER` | — | — |
| `ptransaccion_sucursal` | `CHAR(4)` | — | — |
| `pcomision` | `MONEY(14,2)` | — | — |
| `pivacomision` | `MONEY(14,2)` | — | — |
| `pnombre_usuario` | `VARCHAR(40)` | — | — |
| `prfc` | `VARCHAR(18)` | — | — |
| `ptipo_cuenta_beneficiario` | `VARCHAR(40)` | — | — |
| `pvalor_iva` | `MONEY(14,2)` | — | — |
| `preferencia_cobranza` | `VARCHAR(40)` | — | — |
| `pbanco_receptor` | `VARCHAR(100)` | — | — |
| `pnumtransferenciacargo` | `CHAR(4)` | — | — |
| `pnumtransferenciaabono` | `CHAR(4)` | — | — |
| `pmontototal` | `MONEY(14,2)` | — | — |
| `pcategoria` | `CHAR(2)` | — | — |
| `pconvenio` | `CHAR(3)` | — | — |
| `preftelefono` | `CHAR(20)` | — | — |
| `prefverificador` | `CHAR(20)` | — | — |
| `pnombre_archivo` | `CHAR(17)` | — | — |
| `pstatusoperacion` | `CHAR(1)` | `ope`=operación | 🟡 INFERIDO |
| `numero_Empleado` | `INTEGER` | — | — |
| `nombre_empleado` | `VARCHAR(100)` | — | — |
| `apellidos_pat_empleado` | `VARCHAR(100)` | — | — |
| `apellidos_mat_empleado` | `VARCHAR(100)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L43 |
| `sql_err` | `INTEGER` | L44 |
| `sIdOper` | `INTEGER` | L45 |
| `NombreOperador` | `varchar(50)` | L46 |
| `id_cliente` | `CHAR(9)` | L47 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | SELECT | L66 |
| `bei_usuario` | `bdibei` | no | SELECT | L71 |
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | INSERT | L76 |
| `bei_operacionesmancomunadasoperadorresumen` | `bdibei` | no | INSERT | L108 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?munadas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?rador_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?munadas`, `?rador_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertapagoserviciosgdfmancomunada_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertapagoserviciosgdfmancomunada_bei.sql` |
| **LOC (1er CREATE)** | 183 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta pagos, OS — Originación de Solicitudes, gdf — código geográfico, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertapagoserviciosgdfmancomunada_bei(
  pempresa                     CHAR(3)
  psucursal_virtual            CHAR(4)
  pusuario_virtual             CHAR(4)
  pnumtransferenciacargo       CHAR(4)
  pnumtransferenciaabono       CHAR(4)
  ptransaccion_sucursal        CHAR(4)
  pfoliosuc                    CHAR(16)
  pcuenta_origen               CHAR(20)
  pcuenta_destino              CHAR(20)
  pimporte                     MONEY
  pmoneda                      CHAR(2)
  preferencia                  CHAR(40)
  pmontototal                  MONEY
  pcategoria                   CHAR(2)
  pconvenio                    CHAR(3)
  preftelefono                 CHAR(20)
  prefverificador              CHAR(20)
  pf_operacion                 CHAR(10)
  pnombre_beneficiario         VARCHAR(100)
  pf_aplicacion                CHAR(10)
  pid_usuario                  INTEGER
  pid_cat_operacion            INTEGER
  pstatusoperacion             CHAR(1)
  pGen1                        char(40)
  pGen2                        char(40)
  pGen3                        char(40)
  pGen4                        char(40)
  pTipoServicio                char(6)
  pConcepto                    char(200)
  pListaCampos                 char(250)
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pempresa` | `CHAR(3)` | — | — |
| `psucursal_virtual` | `CHAR(4)` | — | — |
| `pusuario_virtual` | `CHAR(4)` | — | — |
| `pnumtransferenciacargo` | `CHAR(4)` | — | — |
| `pnumtransferenciaabono` | `CHAR(4)` | — | — |
| `ptransaccion_sucursal` | `CHAR(4)` | — | — |
| `pfoliosuc` | `CHAR(16)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pcuenta_origen` | `CHAR(20)` | — | — |
| `pcuenta_destino` | `CHAR(20)` | — | — |
| `pimporte` | `MONEY` | — | — |
| `pmoneda` | `CHAR(2)` | — | — |
| `preferencia` | `CHAR(40)` | — | — |
| `pmontototal` | `MONEY` | — | — |
| `pcategoria` | `CHAR(2)` | — | — |
| `pconvenio` | `CHAR(3)` | — | — |
| `preftelefono` | `CHAR(20)` | — | — |
| `prefverificador` | `CHAR(20)` | — | — |
| `pf_operacion` | `CHAR(10)` | — | — |
| `pnombre_beneficiario` | `VARCHAR(100)` | — | — |
| `pf_aplicacion` | `CHAR(10)` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `pid_cat_operacion` | `INTEGER` | — | — |
| `pstatusoperacion` | `CHAR(1)` | — | — |
| `pGen1` | `char(40)` | — | — |
| `pGen2` | `char(40)` | — | — |
| `pGen3` | `char(40)` | — | — |
| `pGen4` | `char(40)` | — | — |
| `pTipoServicio` | `char(6)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pConcepto` | `char(200)` | — | — |
| `pListaCampos` | `char(250)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L34 |
| `sql_err` | `INTEGER` | L35 |
| `sIdOper` | `INTEGER` | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L143 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `pagos` | ENTIDAD | pagos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?ervici` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `gdf` | ENTIDAD | gdf — código geográfico / Gobierno CDMX (abreviación — bdisa | 🔴 SINTÉTICO | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?munada_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ervici`, `gdf`, `?munada_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertapagoserviciosmancomunada_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertapagoserviciosmancomunada_bei.sql` |
| **LOC (1er CREATE)** | 179 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta pagos, OS — Originación de Solicitudes, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertapagoserviciosmancomunada_bei(
  pempresa                     CHAR(3)
  psucursal_virtual            CHAR(4)
  pusuario_virtual             CHAR(4)
  pnumtransferenciacargo       CHAR(4)
  pnumtransferenciaabono       CHAR(4)
  ptransaccion_sucursal        CHAR(4)
  pfoliosuc                    CHAR(16)
  pcuenta_origen               CHAR(20)
  pcuenta_destino              CHAR(20)
  pimporte                     MONEY
  pmoneda                      CHAR(2)
  preferencia                  CHAR(40)
  pmontototal                  MONEY
  pcategoria                   CHAR(2)
  pconvenio                    CHAR(3)
  preftelefono                 CHAR(20)
  prefverificador              CHAR(20)
  pf_operacion                 CHAR(10)
  pnombre_beneficiario         VARCHAR(100)
  pf_aplicacion                CHAR(10)
  pid_usuario                  INTEGER
  pid_cat_operacion            INTEGER
  pstatusoperacion             CHAR(1)
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pempresa` | `CHAR(3)` | — | — |
| `psucursal_virtual` | `CHAR(4)` | — | — |
| `pusuario_virtual` | `CHAR(4)` | — | — |
| `pnumtransferenciacargo` | `CHAR(4)` | — | — |
| `pnumtransferenciaabono` | `CHAR(4)` | — | — |
| `ptransaccion_sucursal` | `CHAR(4)` | — | — |
| `pfoliosuc` | `CHAR(16)` | `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🟡 INFERIDO |
| `pcuenta_origen` | `CHAR(20)` | — | — |
| `pcuenta_destino` | `CHAR(20)` | — | — |
| `pimporte` | `MONEY` | — | — |
| `pmoneda` | `CHAR(2)` | — | — |
| `preferencia` | `CHAR(40)` | — | — |
| `pmontototal` | `MONEY` | — | — |
| `pcategoria` | `CHAR(2)` | — | — |
| `pconvenio` | `CHAR(3)` | — | — |
| `preftelefono` | `CHAR(20)` | — | — |
| `prefverificador` | `CHAR(20)` | — | — |
| `pf_operacion` | `CHAR(10)` | — | — |
| `pnombre_beneficiario` | `VARCHAR(100)` | — | — |
| `pf_aplicacion` | `CHAR(10)` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `pid_cat_operacion` | `INTEGER` | — | — |
| `pstatusoperacion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L27 |
| `sql_err` | `INTEGER` | L28 |
| `sIdOper` | `INTEGER` | L29 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L143 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `pagos` | ENTIDAD | pagos (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `?ervici` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?munada_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ervici`, `?munada_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertar_avatar_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertar_avatar_bei.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta avatar y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Inserta un nuevo avatar para el usuario |
| FECHA | 22/09/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_insertar_avatar_bei(
  pIdUsuario                   INTEGER
  pNumCliente                  CHAR(9)
  pIdAvatar                    SMALLINT
  pNumIntento                  SMALLINT
  pTokenVirtual                VARCHAR(20)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdAvatar` | `SMALLINT` | `avatar`=avatar (foto de perfil del usuario en app) | ✅ CÓDIGO |
| `pNumIntento` | `SMALLINT` | — | — |
| `pTokenVirtual` | `VARCHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_avatar` | `bdibei` | no | INSERT | L54 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET vCodRet='00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `avatar` | ENTIDAD | avatar (foto de perfil del usuario en app) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertarusuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertarusuario_bei.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Registra numero de cliente y el usuario |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_insertarusuario_bei(
  pNumCliente                  VARCHAR(9)
  pUsuario                     VARCHAR(50)
  pIdentAdmin                  CHAR(30)
  pIdUsuario                   INTEGER
) RETURNING CHAR (5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `VARCHAR(9)` | — | — |
| `pUsuario` | `VARCHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdentAdmin` | `CHAR(30)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L13 |
| `cCod_ret` | `CHAR (5)` | L14 |
| `vNumCliente` | `VARCHAR(9)` | L15 |
| `vUsuario` | `VARCHAR(50)` | L16 |
| `iUsuario` | `VARCHAR(50)` | L17 |
| `sIdUsuario` | `INTEGER` | L18 |
| `sIdStatus` | `SMALLINT` | L19 |
| `sNombre` | `CHAR(250)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L44 |
| `bei_servicio` | `bdibei` | no | SELECT | L59 |
| `bei_usuario` | `bdibei` | no | INSERT | L72 |
| `bei_datos_usuario` | `bdibei` | no | INSERT | L82 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertatransferenciaspei_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertatransferenciaspei_bei.sql` |
| **LOC (1er CREATE)** | 193 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta transferencia y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertatransferenciaspei_bei(
  pempresa                     CHAR(3)
  psucursal_virtual            CHAR(4)
  pusuario_virtual             CHAR(4)
  pclave_banco                 INTEGER
  pimporte                     MONEY(14,2)
  ptransaccion_sucursal        CHAR(4)
  pfoliosuc                    CHAR(16)
  pf_operacion                 CHAR(10)
  pcomision                    MONEY(14,2)
  pivacomision                 MONEY(14,2)
  pnombre_usuario              VARCHAR(40)
  pcuenta_origen               CHAR(20)
  prfc                         VARCHAR(18)
  pnombre_beneficiario         VARCHAR(100)
  ptipo_cuenta_beneficiario    VARCHAR(40)
  pcuenta_destino              CHAR(20)
  preferencia                  CHAR(40)
  pvalor_iva                   MONEY(14,2)
  preferencia_cobranza         CHAR(40)
  pbanco_receptor              VARCHAR(100)
  pmontototal                  MONEY(14,2)
  pf_aplicacion                CHAR(10)
  pid_usuario                  INTEGER
  pid_cat_operacion            INTEGER
  pstatusoperacion             CHAR(1)
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pempresa` | `CHAR(3)` | — | — |
| `psucursal_virtual` | `CHAR(4)` | — | — |
| `pusuario_virtual` | `CHAR(4)` | — | — |
| `pclave_banco` | `INTEGER` | — | — |
| `pimporte` | `MONEY(14,2)` | — | — |
| `ptransaccion_sucursal` | `CHAR(4)` | — | — |
| `pfoliosuc` | `CHAR(16)` | — | — |
| `pf_operacion` | `CHAR(10)` | — | — |
| `pcomision` | `MONEY(14,2)` | — | — |
| `pivacomision` | `MONEY(14,2)` | — | — |
| `pnombre_usuario` | `VARCHAR(40)` | — | — |
| `pcuenta_origen` | `CHAR(20)` | — | — |
| `prfc` | `VARCHAR(18)` | — | — |
| `pnombre_beneficiario` | `VARCHAR(100)` | — | — |
| `ptipo_cuenta_beneficiario` | `VARCHAR(40)` | — | — |
| `pcuenta_destino` | `CHAR(20)` | — | — |
| `preferencia` | `CHAR(40)` | — | — |
| `pvalor_iva` | `MONEY(14,2)` | — | — |
| `preferencia_cobranza` | `CHAR(40)` | — | — |
| `pbanco_receptor` | `VARCHAR(100)` | — | — |
| `pmontototal` | `MONEY(14,2)` | — | — |
| `pf_aplicacion` | `CHAR(10)` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `pid_cat_operacion` | `INTEGER` | — | — |
| `pstatusoperacion` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L34 |
| `sql_err` | `INTEGER` | L35 |
| `sIdOper` | `INTEGER` | L36 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L153 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `transfer` | ENTIDAD | transferencia (forma larga de 'trans') | 🔵 CONVENCIÓN | nombre_sp |
| `?encia` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `spei` | PREFIJO | familia SPEI (pagos interbancarios) | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?encia` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_insertoperacionesmancoctapropias_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_insertoperacionesmancoctapropias_bei.sql` |
| **LOC (1er CREATE)** | 119 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "insertar operaciones, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos, cuenta, ro — Rol Operativo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=5 · INFERIDO=1 · SINTÉTICO=4 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_insertoperacionesmancoctapropias_bei(
  pTransCargo                  char(4)
  pTransAbono                  char(4)
  pTransSuc                    char(4)
  pfoliosuc                    char(16)
  pcuenta_origen               char(12)
  pcuenta_destino              char(12)
  pimporte                     money(14,2)
  pmoneda                      char(2)
  preferencia                  char(40)
  pmontototal                  money(14,2)
  pid_usuario                  INTEGER
  pid_cat_operacion            INTEGER
  pstatusoperacion             CHAR(1)
  pf_aplicacion                CHAR(10)
  pf_operacion                 CHAR(10)
) RETURNING char(5), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pTransCargo` | `char(4)` | — | — |
| `pTransAbono` | `char(4)` | — | — |
| `pTransSuc` | `char(4)` | — | — |
| `pfoliosuc` | `char(16)` | — | — |
| `pcuenta_origen` | `char(12)` | — | — |
| `pcuenta_destino` | `char(12)` | — | — |
| `pimporte` | `money(14,2)` | — | — |
| `pmoneda` | `char(2)` | — | — |
| `preferencia` | `char(40)` | — | — |
| `pmontototal` | `money(14,2)` | — | — |
| `pid_usuario` | `INTEGER` | — | — |
| `pid_cat_operacion` | `INTEGER` | — | — |
| `pstatusoperacion` | `CHAR(1)` | — | — |
| `pf_aplicacion` | `CHAR(10)` | — | — |
| `pf_operacion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L19 |
| `sql_err` | `INTEGER` | L20 |
| `sIdOper` | `INTEGER` | L21 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_insertaoperacionesmancomunadasoperador_bei` | `bdibei` | no | L91 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ins` | ACCION | insertar | 🟡 INFERIDO | nombre_sp |
| `?ert` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `operaciones` | ENTIDAD | operaciones (plural) | 🔵 CONVENCIÓN | nombre_sp |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `cta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?pias_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ert`, `?p`, `ro`, `?pias_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_intentos_ctas_frec`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_intentos_ctas_frec.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "recepción interés, OS — Originación de Solicitudes y cuentas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT, UPDATE, DELETE, SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=2 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Guarda hasta 20 intentos por d al realizar la consulta a alta de cuentas frecuentes, |
| FECHA | 03/12/2018 |

### Firma

```sql
CREATE PROCEDURE sp_intentos_ctas_frec(
  id_usuariosp                 Integer
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `id_usuariosp` | `Integer` | `sp`=stored procedure · `os`=OS — Originación de Solicitudes / subsistema de ofertas (sp_os_*, sp_calcula_estatus_os — bdisolic) | 🔵 CONVENCIÓN / 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L16 |
| `iExist` | `INTEGER` | L17 |
| `contIntento` | `INTEGER` | L18 |
| `primerIntento` | `INTEGER` | L19 |
| `cCodSp` | `CHAR(5)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_intentos_alta_cta_frec` | `bdibei` | no | SELECT | L34 |
| `bei_intentos_alta_cta_frec` | `bdibei` | no | DELETE | L38 |
| `bei_intentos_alta_cta_frec` | `bdibei` | no | INSERT | L39 |
| `bei_intentos_alta_cta_frec` | `bdibei` | no | UPDATE | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L42 | FÓRMULA | `LET contIntento = contIntento + 1 ;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `int` | ENTIDAD | interés | 🟡 INFERIDO | nombre_sp |
| `?ent` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `ctas` | ENTIDAD | cuentas | 🔵 CONVENCIÓN | nombre_sp |
| `?_f` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rec` | ACCION | recepción / recibe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ent`, `?_f` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_limites_empresas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_limites_empresas.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "empresas" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Para manejo de los limites de las empresas |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_limites_empresas(
  pNum_cliente                 CHAR(9)
  pNum_cta                     CHAR(20)
  pIdRest                      CHAR(4)
) RETURNING char(5),DECIMAL(16,2),DECIMAL(16,2),DECIMAL(16,2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNum_cliente` | `CHAR(9)` | — | — |
| `pNum_cta` | `CHAR(20)` | — | — |
| `pIdRest` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `Monto03` | `DECIMAL(16,2)` | L7 |
| `Monto05` | `DECIMAL(16,2)` | L8 |
| `Monto08` | `DECIMAL(16,2)` | L9 |
| `vIdOper` | `char(4)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plimites_empresas` | `bdinteg` | ⚠️ sí | SELECT | L66 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_limites_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empresas` | ENTIDAD | empresas (nómina empresarial) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_limites_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_limites_empresas_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_limites_empresas_historico.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "empresas (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Para manejo de los limites de las empresas |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_limites_empresas_historico(
  pNum_cliente                 CHAR(9)
  pNum_cta                     CHAR(20)
  pIdRest                      CHAR(4)
  pIdBitacoraAdmin             INTEGER
) RETURNING char(5),DECIMAL(16,2),DECIMAL(16,2),DECIMAL(16,2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNum_cliente` | `CHAR(9)` | — | — |
| `pNum_cta` | `CHAR(20)` | — | — |
| `pIdRest` | `CHAR(4)` | — | — |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `Monto03` | `DECIMAL(16,2)` | L7 |
| `Monto05` | `DECIMAL(16,2)` | L8 |
| `Monto08` | `DECIMAL(16,2)` | L9 |
| `vIdOper` | `char(4)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_plimites_empresas_historico` | `bdinteg` | ⚠️ sí | SELECT | L63 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_limites_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `empresas` | ENTIDAD | empresas (nómina empresarial) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_limites_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mod_administradorespm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_mod_administradorespm.sql` |
| **LOC (1er CREATE)** | 124 |
| **Callgraph** | ✅ fan_in=36 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "Administrador — rol de usuario con privilegios administrativos (especial)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_generafoliosolicitudtoken` |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mod_administradorespm(
  pNumCteEmp                   CHAR(9)
  pAdminTipo                   CHAR(3)
  pIdAdmin                     CHAR(30)
  pApellidoPater               CHAR(30)
  pApellidoMater               CHAR(30)
  pNombre1                     CHAR(30)
  pNombre2                     CHAR(30)
  pRepLegal                    CHAR(1)
) RETURNING CHAR(6) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCteEmp` | `CHAR(9)` | — | — |
| `pAdminTipo` | `CHAR(3)` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | 🔵 CONVENCIÓN |
| `pIdAdmin` | `CHAR(30)` | `admin`=Administrador — rol de usuario con privilegios administrativos (pIdAdmin INTEGER en bdibei/bdibpi); también administración de tasas y procesos | 🔵 CONVENCIÓN |
| `pApellidoPater` | `CHAR(30)` | — | — |
| `pApellidoMater` | `CHAR(30)` | — | — |
| `pNombre1` | `CHAR(30)` | — | — |
| `pNombre2` | `CHAR(30)` | — | — |
| `pRepLegal` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INTEGER` | L10 |
| `cCodRet` | `CHAR(6)` | L11 |
| `cMensaje` | `CHAR(50)` | L12 |
| `cFolioSolToken` | `CHAR(12)` | L13 |
| `cMancomunado` | `SMALLINT` | L14 |
| `cApellidoPater` | `CHAR(30)` | L15 |
| `cApellidoMater` | `CHAR(30)` | L16 |
| `cNombre1` | `CHAR(30)` | L17 |
| `cNombre2` | `CHAR(30)` | L18 |
| `sStatus` | `SMALLINT` | L19 |
| `cFolioSolTokenAnt` | `CHAR(12)` | L20 |
| `cCodigoIdent` | `CHAR(3)` | L21 |
| `cId_usuario` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L70 |
| `bei_servicio` | `bdibei` | no | UPDATE | L75 |
| `bei_datos_usuario` | `bdibei` | no | UPDATE | L82 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_generafoliosolicitudtoken` | `bdinteg` | ⚠️ sí | L60 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_mod_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?istrador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `esp` | MODIF | especial | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_mod_`, `?istrador`, `?m` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_mod_senet_altaservicioempresanet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_mod_senet_altaservicioempresanet.sql` |
| **LOC (1er CREATE)** | 147 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta servicio y empresa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_random` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_mod_senet_altaservicioempresanet(
  pNumCte                      CHAR(20)
  pUsuario                     CHAR(8)
  pNoTokens                    smallint
) RETURNING CHAR(5) AS cCodRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pNoTokens` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INTEGER` | L7 |
| `cCodRet` | `CHAR(5)` | L8 |
| `sSecuencia` | `SMALLINT` | L9 |
| `cSolicitud` | `CHAR(10)` | L10 |
| `cFolioSucursal` | `CHAR(16)` | L11 |
| `cRandon1` | `CHAR(6)` | L12 |
| `cRandon2` | `CHAR(2)` | L13 |
| `cRepLegal` | `CHAR(104)` | L14 |
| `vTotal_admin` | `SMALLINT` | L15 |
| `vNoTokens_oper` | `SMALLINT` | L16 |
| `dMonto` | `DECIMAL(12,2)` | L17 |
| `cEstatus` | `SMALLINT` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L64 |
| `bei_contratacion` | `bdibei` | no | SELECT | L71 |
| `bei_contratacion` | `bdibei` | no | UPDATE | L75 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L82 |
| `si_direcciones` | `bdinteg` | ⚠️ sí | SELECT | L89 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L97 |
| `sysshmvals` | `sysmaster` | ⚠️ sí | SELECT | L108 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L116 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L120 |
| `sw_regbitacora_empresanet` | `bdicnweb` | ⚠️ sí | INSERT | L133 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_random` | `bdicheq` | ⚠️ sí | L111 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L55 | CÓDIGO_RETORNO | `LET cCodRet = '00002';` |  |
| L67 | FÓRMULA | `LET vNoTokens_oper = pNoTokens - vTotal_admin;` |  |
| L86 | VALIDACIÓN_NULL | `IF sSecuencia IS NULL THEN` |  |
| L99 | VALIDACIÓN_NULL | `IF cSolicitud IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_mod_senet_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alta` | ACCION | da de alta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `empresa` | ENTIDAD | empresa (entidad bancaria) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_mod_senet_`, `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_modifica_estatus_user_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_modifica_estatus_user_bei.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "modifica estatus y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `modifica` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Modifica Status Usuario |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_modifica_estatus_user_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pIdStatus                    INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdStatus` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | UPDATE | L48 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `modifica` | ACCION | modifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estatus` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `?_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_montos_manc_aut_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_montos_manc_aut_bei.sql` |
| **LOC (1er CREATE)** | 100 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización monto y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS MONTOS PENDIENTES POR AUTORIZAR |
| FECHA | 08/01/2015 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_montos_manc_aut_bei(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L15 |
| `sql_err` | `INTEGER` | L16 |
| `iTotalReg` | `INTEGER` | L17 |
| `sTipoOperacion` | `CHAR(2)` | L18 |
| `iIdMancomunidad` | `INTEGER` | L19 |
| `sUserNom` | `CHAR(50)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L56 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `?s_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_montos_manc_aut_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_montos_manc_aut_bei2.sql` |
| **LOC (1er CREATE)** | 112 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización monto y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | se clona y modifica el spl para mostrar el nombre de la persona en lugar de usuario |
| FECHA | NA |
| DESCRIPCION | OBTIENE LOS DATOS DE LOS MONTOS PENDIENTES POR AUTORIZAR |
| FECHA | 08/01/2015 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_montos_manc_aut_bei2(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L15 |
| `sql_err` | `INTEGER` | L16 |
| `iTotalReg` | `INTEGER` | L17 |
| `sTipoOperacion` | `CHAR(2)` | L18 |
| `iIdMancomunidad` | `INTEGER` | L19 |
| `sUserNom` | `CHAR(50)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L67 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `?s_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_manc_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obt_beneficiarios_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obt_beneficiarios_bei.sql` |
| **LOC (1er CREATE)** | 216 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene beneficiarios y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consulta Beneficiarios por filtro |
| FECHA | 10/02/2015 |
| MODIFICACION | Se ajusta para que no tome encuenta aquellos registrso que tienen menos de 30 min. |
| FECHA | 12/Noviembre/2015 |
| FECHA | 17-Noviembre-2015 |

### Firma

```sql
CREATE PROCEDURE sp_obt_beneficiarios_bei(
  pRegIni                      integer
  pNumCliente                  char(9)
  pTipoConsulta                integer
  pAlias                       CHAR(20)
  pNombre                      CHAR(50)
  pApellidoPaterno             CHAR(50)
  pApellidoMaterno             CHAR(50)
) RETURNING char(5),char(20),char(50),char(50),char(50),char(50),char(100), char(50),decimal(16,2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRegIni` | `integer` | — | — |
| `pNumCliente` | `char(9)` | — | — |
| `pTipoConsulta` | `integer` | — | — |
| `pAlias` | `CHAR(20)` | — | — |
| `pNombre` | `CHAR(50)` | — | — |
| `pApellidoPaterno` | `CHAR(50)` | — | — |
| `pApellidoMaterno` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L11 |
| `cod_ret` | `char(5)` | L12 |
| `iTotalReg` | `integer` | L13 |
| `sAlias` | `char(20)` | L14 |
| `sPrimerNombre` | `char(50)` | L15 |
| `sSegundoNombre` | `char(50)` | L16 |
| `sApellidoPaterno` | `char(50)` | L17 |
| `sApellidoMaterno` | `char(50)` | L18 |
| `sDomicilio` | `char(100)` | L19 |
| `sTelefono` | `char(50)` | L20 |
| `sMontoMaximo` | `decimal(16,2)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_beneficiariosfrec_bpi` | `bdiprog` | ⚠️ sí | SELECT | L80 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `beneficiarios` | ENTIDAD | beneficiarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_obt_datosempresa_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obt_datosempresa_bei.sql` |
| **LOC (1er CREATE)** | 159 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene datos, empresa y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtiene los datos del cliente |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza query para que consulta las personas es_fisica=N en lugar de tipo de persona 02 |
| MODIFICACION | BanCoppel - Internet - Berenice Noriega Guevara |
| FECHA | 10 Julio 2015 |
| MODIFICACION | Para identificar a los usuarios que tuvieron problemas en el paso definición de usuario |
| FECHA | 29-Septiembre-2015 |

### Firma

```sql
CREATE PROCEDURE sp_obt_datosempresa_bei(
  pRfc                         char(15)
  pNumCte                      char(9)
  pCuenta                      char(20)
  pIdentAdmin                  char(30)
) RETURNING char(5), char(13), char(41), char(2),INTEGER,DATETIME YEAR TO SECOND,char(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pRfc` | `char(15)` | — | — |
| `pNumCte` | `char(9)` | — | — |
| `pCuenta` | `char(20)` | — | — |
| `pIdentAdmin` | `char(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L29 |
| `cCod_ret` | `char(5)` | L30 |
| `cRFC` | `char(13)` | L31 |
| `cNombre` | `char(41)` | L32 |
| `cIdStatus` | `char(2)` | L33 |
| `cIdUsuario` | `INTEGER` | L34 |
| `sIdStatusToken` | `INTEGER` | L35 |
| `cNsToken` | `char(10)` | L36 |
| `iUsuario` | `VARCHAR(50);	DEFINE vUsuario VARCHAR(50)` | L37 |
| `cFecBloqueoTemp` | `DATETIME YEAR TO SECOND` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L63 |
| `bei_servicio` | `bdibei` | no | SELECT | L69 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L85 |
| `bei_usuario` | `bdibei` | no | SELECT | L95 |
| `bei_token` | `bdibei` | no | SELECT | L105 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L127 |
| `bei_servicio` | `bdibei` | no | UPDATE | L137 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L99 | VALIDACIÓN_NULL | `IF cFecBloqueoTemp IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `empresa` | ENTIDAD | empresa (entidad bancaria) | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_obt_numcte_status_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obt_numcte_status_bei.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene número de cliente, estatus y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | REGRESA EL ESTATUS USUARIO, ESTATUS TOKEN, NUMERO CLIENTE Y SI TIENE UN BLOQUEO. |
| MODIFICACION | AHORA RECIBE pIdUsuario EN LUGAR DEL NUMERO DE IDENTIFICACIÓN Y LO UTILIZA PARA LA |
| FECHA | 22-ENERO-2015 |
| MODIFICACION | SOLSER |

### Firma

```sql
CREATE PROCEDURE sp_obt_numcte_status_bei(
  pNumCte                      CHAR(20)
  pIdUsuario                   CHAR(30)
) RETURNING CHAR(5), CHAR(20), SMALLINT, INTEGER, INTEGER,DATETIME YEAR to SECOND
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | `numcte`=número de cliente | 🔵 CONVENCIÓN |
| `pIdUsuario` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `CHAR(5)` | L8 |
| `sql_err` | `INTEGER` | L9 |
| `sId_status` | `SMALLINT` | L10 |
| `iId_status_token` | `INTEGER` | L11 |
| `cNum_cte` | `CHAR (20)` | L12 |
| `cNsToken` | `CHAR (50)` | L13 |
| `iIdusuario` | `Integer` | L14 |
| `iFecBloqueo` | `DATETIME YEAR to SECOND` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L50 |
| `bei_token` | `bdibei` | no | SELECT | L68 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | VALIDACIÓN_NULL | `IF cNum_cte IS NULL THEN` |  |
| L81 | FÓRMULA | `LET iId_status_token = -1; --En caso de que el VAlor de Token regrese Cero, se cambia el valor a -1 ` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `numcte` | ENTIDAD | número de cliente | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_obt_reporte_manifiesto_pm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obt_reporte_manifiesto_pm.sql` |
| **LOC (1er CREATE)** | 73 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene reporte" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Obtiene los datos para generar el reporte de manifiesto en persona morales. |
| FECHA | 25-05-2015 |

### Firma

```sql
CREATE PROCEDURE sp_obt_reporte_manifiesto_pm(
  pFechaIni                    char(10)
  pFechaFin                    char(10)
  pRegistros                   smallint
) RETURNING char(5), char(30), char(10), char(9), char(60), char(4), char(150)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaIni` | `char(10)` | — | — |
| `pFechaFin` | `char(10)` | — | — |
| `pRegistros` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L10 |
| `iSql_err` | `integer` | L11 |
| `vNumGuia` | `char(30)` | L13 |
| `vCodRast` | `char(10)` | L14 |
| `vDestinatario` | `char(9)` | L15 |
| `vRazonSocial` | `char(60)` | L16 |
| `vDestino` | `char(4)` | L17 |
| `vRefToken` | `char(10)` | L18 |
| `vRefTokenAux` | `char(150)` | L19 |
| `vSolicTkn` | `char(10)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_envios` | `bdibei` | no | SELECT | L53 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L58 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_manifiesto_pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_manifiesto_pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obt_solicitud_admtoken_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obt_solicitud_admtoken_bei.sql` |
| **LOC (1er CREATE)** | 245 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene solicitud, AdmToken — módulo de administración de tokens de autenticación para personas morales y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Obtiene los datos de la consulta de solicitudes del AdmToken personas morales |
| FECHA | 2013-08-26. |

### Firma

```sql
CREATE PROCEDURE sp_obt_solicitud_admtoken_bei(
  pSolicitud                   char(10)
  pCliente                     char(9)
  pStatus                      char(5)
  pSucursal                    char(4)
  pFecha                       char(20)
  pRegistros                   smallint
  pTipoConsulta                char(1)
) RETURNING char(5), char(10), char(9), char(5), char(5), char(4), char(20), char(8), integer, char(26), char(26), char(26), char(26),char(60), char(200)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSolicitud` | `char(10)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pCliente` | `char(9)` | — | — |
| `pStatus` | `char(5)` | — | — |
| `pSucursal` | `char(4)` | — | — |
| `pFecha` | `char(20)` | — | — |
| `pRegistros` | `smallint` | — | — |
| `pTipoConsulta` | `char(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L12 |
| `sql_err` | `integer` | L13 |
| `vSolicitud` | `char(10)` | L14 |
| `vCliente` | `char(9)` | L15 |
| `vStatus` | `char(5)` | L16 |
| `vUnidades` | `char(5)` | L17 |
| `vSucursal` | `char(4)` | L18 |
| `vFecha` | `char(50)` | L19 |
| `vUsrSolicita` | `char(8)` | L20 |
| `vNombre1` | `char(26)` | L21 |
| `vNombre2` | `char(26)` | L22 |
| `vApaterno` | `char(26)` | L23 |
| `vAmaterno` | `char(26)` | L24 |
| `vSecuencia` | `integer` | L25 |
| `vComentarios` | `char(200)` | L26 |
| `vRazonSocial` | `char(60)` | L27 |
| `vTipoPersona` | `char (2)` | L28 |
| `iLongitud` | `integer` | L29 |
| `vFechaVieja` | `date` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L85 |
| `25` | `bdibei` | no | SELECT | L105 |
| `49` | `bdibei` | no | SELECT | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L52 | FÓRMULA | `LET vFechaVieja = CURRENT - INTERVAL(62) DAY TO DAY;` |  |
| L107 | FÓRMULA | `LET vApaterno = SUBSTRING(vRazonSocial FROM 49 FOR (iLongitud - 48));` |  |
| L144 | FÓRMULA | `LET vApaterno = SUBSTRING(vRazonSocial FROM 49 FOR (iLongitud - 48));` |  |
| L190 | FÓRMULA | `LET vApaterno = SUBSTRING(vRazonSocial FROM 49 FOR (iLongitud - 48));` |  |
| L227 | FÓRMULA | `LET vApaterno = SUBSTRING(vRazonSocial FROM 49 FOR (iLongitud - 48));` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admtoken` | ENTIDAD | AdmToken — módulo de administración de tokens de autenticaci | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_obt_ult_envio_admtoken_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obt_ult_envio_admtoken_bei.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene AdmToken — módulo de administración de tokens de autenticación para personas morales y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Obtiene los datos de la consulta del último envio del AdmToken |
| FECHA | 2013-07-24 |

### Firma

```sql
CREATE PROCEDURE sp_obt_ult_envio_admtoken_bei(
  pSolicitud                   char(10)
) RETURNING char(5), char(10), char(20), char(30), char(200)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSolicitud` | `char(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L13 |
| `sql_err` | `integer` | L14 |
| `vNumEnvio` | `char(10)` | L15 |
| `vFecha` | `char(20)` | L16 |
| `vNumGuia` | `char(30)` | L17 |
| `vComentarios` | `char(200)` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_envios` | `bdibei` | no | SELECT | L43 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `?_ult_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admtoken` | ENTIDAD | AdmToken — módulo de administración de tokens de autenticaci | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ult_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtener_cons_mov`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtener_cons_mov.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene movimiento" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtener la Consulta de Movimientos de la cuenta del cliente |
| FECHA | 14/Julio/2016 |

### Firma

```sql
CREATE PROCEDURE sp_obtener_cons_mov(
  pEmpresa                     CHAR(3)
  pNum_cliente                 CHAR(9)
) RETURNING CHAR(5) AS cod_ret, CHAR(25) AS folio, CHAR(30) AS f_solicitud_arch, CHAR(10) AS f_inicial,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNum_cliente` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vFolio` | `CHAR(25)` | L12 |
| `vFsolicitud` | `CHAR(30)` | L13 |
| `vFinicial` | `CHAR(10)` | L14 |
| `vFfinal` | `CHAR(10)` | L15 |
| `vStatus` | `CHAR(5)` | L16 |
| `sql_err` | `INTEGER` | L17 |
| `cod_ret` | `CHAR(5)` | L18 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_obtener_cons_mov_det`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtener_cons_mov_det.sql` |
| **LOC (1er CREATE)** | 55 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene movimiento y detalle" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtener el Detalle de Movimientos de la cuenta del cliente |
| FECHA | 14/Julio/2016 |

### Firma

```sql
CREATE PROCEDURE sp_obtener_cons_mov_det(
  pFolio                       CHAR(25)
) RETURNING CHAR(5) AS cod_ret, CHAR(25) AS folio, CHAR(20) AS cuenta, CHAR(10) AS f_inicial,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFolio` | `CHAR(25)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vFolio` | `CHAR(25)` | L12 |
| `vCuenta` | `CHAR(20)` | L13 |
| `vFinicial` | `CHAR(10)` | L14 |
| `vFfinal` | `CHAR(10)` | L15 |
| `vTotalReg` | `CHAR(20)` | L16 |
| `vNombre` | `CHAR(30)` | L17 |
| `sql_err` | `INTEGER` | L18 |
| `cod_ret` | `CHAR(5)` | L19 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `mov` | ENTIDAD | movimiento | 🔵 CONVENCIÓN | nombre_sp |
| `det` | ENTIDAD | detalle | 🟡 INFERIDO | nombre_sp |

---

## `sp_obtener_id_admin_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtener_id_admin_bei.sql` |
| **LOC (1er CREATE)** | 42 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene identificador, Administrador — rol de usuario con privilegios administrativos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtiene el id de identificacion del administrador por usuario y numero de cliente |
| FECHA | 10/10/2013 |

### Firma

```sql
CREATE PROCEDURE sp_obtener_id_admin_bei(
  pIdUsuario                   INTEGER
  pNumCte                      CHAR(9)
) RETURNING CHAR(5),CHAR(30)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | `id`=identificador (de) | ✅ CÓDIGO |
| `pNumCte` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L4 |
| `cod_ret` | `CHAR (5)` | L5 |
| `id_admin` | `CHAR(30)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L30 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `id` | ENTIDAD | identificador (de) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_obtener_num_serie_token_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtener_num_serie_token_bei.sql` |
| **LOC (1er CREATE)** | 41 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene número, token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtiene el numero de serie del token asignado a un cliente |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_obtener_num_serie_token_bei(
  pIdUsuario                   INTEGER
  pNumCte                      char(9)
) RETURNING char(5), char(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCte` | `char(9)` | `num`=número (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L5 |
| `cod_ret` | `char (5)` | L6 |
| `vNumSerie` | `char(9)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L32 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?_serie_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_serie_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtener_numcte_status_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtener_numcte_status_bei.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene número de cliente, estatus y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | REGRESA EL ESTATUS USUARIO, ESTATUS TOKEN, NUMERO CLIENTE Y SI TIENE UN BLOQUEO. |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_obtener_numcte_status_bei(
  pNumCte                      CHAR(20)
  pIdUsuario                   CHAR(30)
  pIdentificacionAdmin         CHAR(30)
) RETURNING CHAR(5), CHAR(20), SMALLINT, INTEGER, INTEGER,DATETIME YEAR to SECOND
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | `numcte`=número de cliente | 🔵 CONVENCIÓN |
| `pIdUsuario` | `CHAR(30)` | — | — |
| `pIdentificacionAdmin` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `CHAR(5)` | L9 |
| `sql_err` | `INTEGER` | L10 |
| `sId_status` | `SMALLINT` | L11 |
| `iId_status_token` | `INTEGER` | L12 |
| `cNum_cte` | `CHAR (20)` | L13 |
| `cNsToken` | `CHAR (50)` | L14 |
| `iIdusuario` | `Integer` | L15 |
| `iFecBloqueo` | `DATETIME YEAR to SECOND` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L49 |
| `bei_token` | `bdibei` | no | SELECT | L90 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L81 | VALIDACIÓN_NULL | `IF cNum_cte IS NULL THEN` |  |
| L103 | FÓRMULA | `LET iId_status_token = -1; --En caso de que el VAlor de Token regrese Cero, se cambia el valor a -1 ` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `numcte` | ENTIDAD | número de cliente | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_obtenerdatoscontacto_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtenerdatoscontacto_bei.sql` |
| **LOC (1er CREATE)** | 33 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene datos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenerdatoscontacto_bei(
  pIdUser                      Integer
) RETURNING CHAR (5), CHAR(80), CHAR(15), INT
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUser` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L5 |
| `cCod_ret` | `CHAR (5)` | L6 |
| `cEmail` | `CHAR(80)` | L7 |
| `cCel` | `CHAR(15)` | L8 |
| `iCiaCel` | `INT` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | SELECT | L28 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?o_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenerdatoscontacto_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtenerdatoscontacto_bei_historico.sql` |
| **LOC (1er CREATE)** | 35 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene datos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenerdatoscontacto_bei_historico(
  pIdUser                      INTEGER
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5), CHAR(80), CHAR(15), INT
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUser` | `INTEGER` | — | — |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INT` | L5 |
| `cCod_ret` | `CHAR (5)` | L6 |
| `cEmail` | `CHAR(80)` | L7 |
| `cCel` | `CHAR(15)` | L8 |
| `iCiaCel` | `INT` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario_historico` | `bdibei` | no | SELECT | L30 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?o_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?o_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obteneremailusuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obteneremailusuario_bei.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene correo electrónico, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtiene el correo del usuario |
| FECHA | 29/08/2013 |
| FECHA | 29/08/2013 |

### Firma

```sql
CREATE PROCEDURE sp_obteneremailusuario_bei(
  pNumCliente                  VARCHAR(9)
  pUsuario                     INTEGER
) RETURNING CHAR (5), CHAR(100)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `VARCHAR(9)` | — | — |
| `pUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L4 |
| `cCod_ret` | `CHAR (5)` | L5 |
| `vEmail` | `VARCHAR(100)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L32 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L35 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mail` | ENTIDAD | correo electrónico | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenerfechablotemporal_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtenerfechablotemporal_bei.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene fecha, lote y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 9 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtiene bloqueo temporal del usuario |
| FECHA | 04/08/2011 |

### Firma

```sql
CREATE PROCEDURE sp_obtenerfechablotemporal_bei(
  pIdUsuario                   INTEGER
  pTipoBloq                    VARCHAR(5)
) RETURNING CHAR (5), DATETIME YEAR TO SECOND, INT
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pTipoBloq` | `VARCHAR(5)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INT` | L5 |
| `cCod_ret` | `CHAR (5)` | L6 |
| `dFechaBloq` | `DATETIME YEAR TO SECOND` | L7 |
| `iTipoBloq` | `INT` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | SELECT | L38 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `fecha` | ENTIDAD | fecha | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?b` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `lote` | ENTIDAD | lote (proceso batch) | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?al_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?b`, `?m`, `?al_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obteneridusuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obteneridusuario_bei.sql` |
| **LOC (1er CREATE)** | 32 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene identificador, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | CONSULTA EL ID_USUARIO APARTIR DEL USUARIO |
| FECHA | 05/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_obteneridusuario_bei(
  pUsuario                     CHAR(50)
) RETURNING CHAR (5) as vCod_ret, CHAR(11) as vIdUsuario
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L11 |
| `vCod_ret` | `CHAR (5)` | L12 |
| `vIdUsuario` | `VARCHAR(11)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L29 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_obtenermenuoperador_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtenermenuoperador_bei.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtenermenuoperador_bei(
  pIdusario                    INTEGER
  pRegInicial                  INTEGER
) RETURNING CHAR(5), INTEGER, INTEGER, INTEGER, CHAR(100), CHAR(150), SMALLINT, CHAR(50), CHAR(150), CHAR(150), SMALLINT, SMALLINT
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdusario` | `INTEGER` | — | — |
| `pRegInicial` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `id_menu` | `INTEGER` | L6 |
| `id_menu_padre` | `INTEGER` | L7 |
| `nombre` | `CHAR(100)` | L8 |
| `factory` | `CHAR(150)` | L10 |
| `nivel` | `SMALLINT` | L11 |
| `id_factory` | `CHAR(50)` | L12 |
| `contenedor` | `CHAR(150)` | L13 |
| `parametros` | `CHAR(150)` | L14 |
| `tipo` | `SMALLINT` | L15 |
| `TotalReg` | `INTEGER` | L16 |
| `TipoUsuario` | `SMALLINT` | L17 |
| `activo` | `SMALLINT` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L48 |
| `bei_operaciones` | `bdibei` | no | SELECT | L55 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?menu` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?rador_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?menu`, `?rador_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenernuevaspreguntas`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtenernuevaspreguntas.sql` |
| **LOC (1er CREATE)** | 99 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE UN JUEGO DE PREGUNTAS NUEVAS PARA UN USUARIO- MODULO OLVIDO CONTRASE??A |
| FECHA | 05/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_obtenernuevaspreguntas(
  pid_usuario                  INTEGER
) RETURNING CHAR (5) as cCod_ret, INT as iId_pregunta, CHAR(80) as vDesc_pregunta, CHAR(100) as vLista_resp
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pid_usuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_err` | `int` | L13 |
| `cCod_ret` | `CHAR (5)` | L14 |
| `iId_pregunta` | `INT` | L15 |
| `vDesc_pregunta` | `VARCHAR(80)` | L16 |
| `iIdRegistro` | `INTEGER` | L17 |
| `iOrdAle` | `INTEGER` | L18 |
| `iOrdAleAux` | `INTEGER` | L19 |
| `iIdUsuario` | `INTEGER` | L20 |
| `iNum_pregunta` | `INTEGER` | L21 |
| `vLista_resp` | `VARCHAR(100)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L46 |
| `bei_cat_preguntas_auxpm` | `bdibei` | no | SELECT | L49 |
| `bei_cat_preguntas_auxpm` | `bdibei` | no | DELETE | L49 |
| `bei_cat_preguntaspm` | `bdibei` | no | SELECT | L52 |
| `bei_preguntas_his` | `bdibei` | no | SELECT | L58 |
| `bei_preguntas_his` | `bdibei` | no | DELETE | L58 |
| `bei_cat_preguntas_auxpm` | `bdibei` | no | INSERT | L76 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_random` | `bdibpi` | ⚠️ sí | L74 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?nueva` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?untas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?nueva`, `?untas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenerpreguntas_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtenerpreguntas_bei.sql` |
| **LOC (1er CREATE)** | 51 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | CONSULTA DE PREGUNTA EN EL MODULO DE ACTIVACION DE USUARIO |
| FECHA | 05/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_obtenerpreguntas_bei(
) RETURNING CHAR (5) as cCod_ret, INT as iId_pregunta, CHAR(70) as vDesc_pregunta, CHAR(100) as vLista_resp
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L10 |
| `cCod_ret` | `CHAR (5)` | L11 |
| `iId_pregunta` | `INT` | L12 |
| `vDesc_pregunta` | `VARCHAR(70)` | L13 |
| `iIdRegistro` | `INTEGER` | L14 |
| `iOrdAle` | `INTEGER` | L15 |
| `iOrdAleAux` | `INTEGER` | L16 |
| `vLista_resp` | `VARCHAR(100)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_cat_preguntaspm` | `bdibei` | no | SELECT | L41 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?untas_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `?untas_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtenerpreguntasusuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtenerpreguntasusuario_bei.sql` |
| **LOC (1er CREATE)** | 58 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LAS PREGUNTAS VIGENTES DEL USUARIO- MODULO OLVIDO CONTRASE |
| FECHA | 05/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_obtenerpreguntasusuario_bei(
  pid_usuario                  INTEGER
  pLimite                      INT
) RETURNING CHAR (5) as cCod_ret, INT as iId_pregunta, CHAR(70) as vDesc_pregunta, CHAR(100) as vLista_resp
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pid_usuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pLimite` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L13 |
| `cCod_ret` | `CHAR (5)` | L14 |
| `iId_pregunta` | `INT` | L15 |
| `vDesc_pregunta` | `VARCHAR(70)` | L16 |
| `iIdRegistro` | `INTEGER` | L17 |
| `iOrdAle` | `INTEGER` | L18 |
| `iOrdAleAux` | `INTEGER` | L19 |
| `iIdUsuario` | `CHAR(8)` | L20 |
| `vLista_resp` | `VARCHAR(100)` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L40 |
| `bei_respuestas` | `bdibei` | no | SELECT | L47 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `?untas` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `?untas` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtiene_guiastoken_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtiene_guiastoken_bei.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |

### Firma

```sql
CREATE PROCEDURE sp_obtiene_guiastoken_bei(
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
  pFechaAtencion               char(10)
  pNumeroGuia                  char(22)
  pRegistros                   smallint
) RETURNING CHAR(5),CHAR(10),CHAR(9), CHAR(1), CHAR(10),CHAR(22)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumSolicitud` | `char(10)` | — | — |
| `pNumCliente` | `char(9)` | — | — |
| `pFechaAtencion` | `char(10)` | — | — |
| `pNumeroGuia` | `char(22)` | — | — |
| `pRegistros` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vSolicitud` | `CHAR(10)` | L13 |
| `sql_err` | `integer` | L14 |
| `vCliente` | `CHAR(9)` | L15 |
| `vFechaAtencion` | `CHAR(10)` | L16 |
| `vNumGuia` | `CHAR(22)` | L17 |
| `cod_ret` | `CHAR(5)` | L18 |
| `vSec_domicilio` | `char(1)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L48 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_guias` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_guias` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtiene_guiastoken_logify_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtiene_guiastoken_logify_bei.sql` |
| **LOC (1er CREATE)** | 88 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2018-05-04. |

### Firma

```sql
CREATE PROCEDURE sp_obtiene_guiastoken_logify_bei(
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
  pFechaAtencion               char(10)
  pNumeroGuia                  char(30)
  pRegistros                   smallint
) RETURNING CHAR(5),CHAR(10),CHAR(9), CHAR(1), CHAR(10),CHAR(30)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumSolicitud` | `char(10)` | — | — |
| `pNumCliente` | `char(9)` | — | — |
| `pFechaAtencion` | `char(10)` | — | — |
| `pNumeroGuia` | `char(30)` | — | — |
| `pRegistros` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vSolicitud` | `CHAR(10)` | L13 |
| `sql_err` | `integer` | L14 |
| `vCliente` | `CHAR(9)` | L15 |
| `vFechaAtencion` | `CHAR(10)` | L16 |
| `vNumGuia` | `CHAR(30)` | L17 |
| `cod_ret` | `CHAR(5)` | L18 |
| `vSec_domicilio` | `char(1)` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L48 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_guias` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?_logify_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_guias`, `?_logify_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtiene_inventariosol_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtiene_inventariosol_bei.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene inv — inversión, Tarjeta, OS — Originación de Solicitudes y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=3 · SINTÉTICO=3 / 9 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |
| MODIFICACION | Fecha Descripci |
| FECHA | 2017-05-05. |

### Firma

```sql
CREATE PROCEDURE sp_obtiene_inventariosol_bei(
) RETURNING CHAR(5),CHAR(100)
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L19 |
| `sql_err` | `INTEGER` | L20 |
| `vRecibidasM` | `INTEGER` | L21 |
| `vEnProcesoM` | `INTEGER` | L22 |
| `vAtendidasM` | `INTEGER` | L23 |
| `vDevueltasM` | `INTEGER` | L24 |
| `vReactivadasM` | `INTEGER` | L25 |
| `vRechazadasM` | `INTEGER` | L26 |
| `vRecibidasF` | `INTEGER` | L27 |
| `vEnProcesoF` | `INTEGER` | L28 |
| `vAtendidasF` | `INTEGER` | L29 |
| `vDevueltasF` | `INTEGER` | L30 |
| `vReactivadasF` | `INTEGER` | L31 |
| `vRechazadasF` | `INTEGER` | L32 |
| `vTramaRet` | `CHAR(36)` | L33 |
| `vEnviadaM` | `INTEGER` | L34 |
| `vEnviadaF` | `INTEGER` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L73 |
| `bpi_tokensolicitud` | `bdibpi` | ⚠️ sí | SELECT | L103 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `inv` | ENTIDAD | inv — inversión (abreviación — calsdoinvcrec, cierrechqinvcr | 🟡 INFERIDO | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `?ol_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?en`, `?i`, `?ol_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtiene_tokenasociados_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtiene_tokenasociados_bei.sql` |
| **LOC (1er CREATE)** | 53 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene token, Sistema Operativo Central, OS — Originación de Solicitudes y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |

### Firma

```sql
CREATE PROCEDURE sp_obtiene_tokenasociados_bei(
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
) RETURNING char(5),char(9)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumSolicitud` | `char(10)` | — | — |
| `pNumCliente` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L14 |
| `sql_err` | `integer` | L15 |
| `vNstoken` | `char(9)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L39 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?iad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a`, `?iad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtiene_wsestafetalabel_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtiene_wsestafetalabel_bei.sql` |
| **LOC (1er CREATE)** | 378 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| MODIFICACION | Cesar Adrian Mendoza Gonzalez |
| ACTIVIDAD | Se modifica para retornar los token con topes maximo. |
| FECHA | 04-06-2015 |
| MODIFICACION | Manuel Ramos Figueroa |
| ACTIVIDAD | Se modifica para retornar la vigencia original de la gu (campo "vEffectiveDate") en caso de una reimpresi |
| FECHA | 13-08-2015 |
| MODIFICACION | Gabriela Aguilar Mendoza |
| ACTIVIDAD | Se modifica para ampliar a 14 dias la vigencia original de la gu (campo "vEffectiveDate"). |
| FECHA | 28-02-2017 |

### Firma

```sql
CREATE PROCEDURE sp_obtiene_wsestafetalabel_bei(
  pCliente                     char(20)
  pSolicitud                   char(10)
  pSecuencia                   integer
) RETURNING CHAR(5), --CodRet
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pCliente` | `char(20)` | — | — |
| `pSolicitud` | `char(10)` | — | — |
| `pSecuencia` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L52 |
| `sql_err` | `INTEGER` | L53 |
| `vTramaRet` | `CHAR(36)` | L54 |
| `vSecDomicilio` | `smallint` | L55 |
| `vEffectiveDate` | `CHAR(8)` | L56 |
| `vCellPhone` | `CHAR(13)` | L57 |
| `vContactName` | `CHAR(30)` | L58 |
| `vCorporateName` | `CHAR(50)` | L59 |
| `vCustomerNumber` | `CHAR(10)` | L60 |
| `vPhoneNumber` | `CHAR(13)` | L61 |
| `vValid` | `CHAR(1)` | L62 |
| `vDestinationCountryId` | `CHAR(3)` | L63 |
| `vNumbersOfLabels` | `CHAR(2)` | L64 |
| `vDestinationaddress1` | `CHAR(6)` | L65 |
| `vDestinationaddress2` | `int` | L66 |
| `vDestinationManzana` | `CHAR(6)` | L67 |
| `vDestinationOtros` | `CHAR(6)` | L68 |
| `vDestinationAndador` | `CHAR(6)` | L69 |
| `vDestinationEtapa` | `CHAR(6)` | L70 |
| `vDestinationLote` | `CHAR(6)` | L71 |
| `vDestinationEdificio` | `CHAR(6)` | L72 |
| `vDestinationEntrada` | `CHAR(6)` | L73 |
| `vDestinationObservaciones` | `CHAR(80)` | L74 |
| `vTipoDir` | `char(1)` | L75 |
| `vContent` | `CHAR(75)` | L76 |
| *…21 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L185 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L189 |
| `systabnames` | `sysmaster` | ⚠️ sí | SELECT | L252 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L121 | FÓRMULA | `LET vFecha = current::date + 14;` |  |
| L230 | FÓRMULA | `LET vNumbersOfLabels = 1; --Min 1 Max 70` |  |
| L269 | FÓRMULA | `LET vContador = 1; --Contador para el manejo de las comas entre tokens series` |  |
| L271 | FÓRMULA | `LET vCount = LENGTH(vContent)+LENGTH(vSeriesContentToken);` |  |
| L275 | FÓRMULA | `LET vMaxLine=vMaxLine+vMaxLine;` |  |
| L286 | FÓRMULA | `LET vBandera = vBandera + 1;` |  |
| L311 | FÓRMULA | `LET vCount = LENGTH(vContent)+LENGTH(vSeriesContentToken);` |  |
| L313 | FÓRMULA | `LET vMaxLine=vMaxLine+vMaxLine;` |  |
| L321 | FÓRMULA | `LET vContador = 1; --Contador para el manejo de las comas entre tokens series` |  |
| L323 | FÓRMULA | `LET vContador = 0; --Contador para el manejo de las comas entre tokens series` |  |
| L324 | FÓRMULA | `LET vConjunto = vConjunto + 1;` |  |
| L334 | FÓRMULA | `LET vContador = 1; --Contador para el manejo de las comas entre tokens series` |  |
| L345 | FÓRMULA | `LET vContador = 0; --Contador para el manejo de las comas entre tokens series` |  |
| L371 | FÓRMULA | `LET vDestinationaddress2=LENGTH(vDestinationManzana)+LENGTH(vDestinationOtros)+LENGTH(vDestinationAn` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `?_wsestafetalabel_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_wsestafetalabel_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtieneoperacionesdeldia_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtieneoperacionesdeldia_bei.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene operaciones y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (del día)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| AUTOR | Mauricio Leon Ibarra |
| MODIFICACION | Consulta las operaciones del dia de usuarios de BEI |
| FECHA | 25/10/2011 |
| AUTOR | Jose Ruben Lopez Hernandez |
| MODIFICACION | Se agrego el parametro v_sDestinoSpei |
| FECHA | 12/04/2013 |
| MODIFICACION | Se agregan los id's para dispersiones de enes de pago 3003 y 3004 |
| MODIFICACION | Se agregan validaciones para omitir las consultas de reimpresi de comprobantes |

### Firma

```sql
CREATE PROCEDURE sp_obtieneoperacionesdeldia_bei(
  pNumCte                      CHAR(20)
  p_sUsuario                   CHAR(20)
  p_dFecha                     date
  pDesde                       INTEGER
  pHasta                       INTEGER
) RETURNING CHAR(6), CHAR(10), CHAR(50), CHAR(12), CHAR(12), CHAR(16), CHAR(10), CHAR(40),CHAR(40)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `p_sUsuario` | `CHAR(20)` | — | — |
| `p_dFecha` | `date` | — | — |
| `pDesde` | `INTEGER` | — | — |
| `pHasta` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_sCodRet` | `CHAR(6)` | L22 |
| `v_sMensajeRet` | `CHAR(60)` | L23 |
| `intcodret` | `INTEGER` | L24 |
| `v_sFecha` | `CHAR(10)` | L25 |
| `v_sTransaccion` | `CHAR(50)` | L26 |
| `v_sOrigen` | `CHAR(12)` | L27 |
| `v_sDestino` | `CHAR(12)` | L28 |
| `v_sImporte` | `CHAR(16)` | L29 |
| `v_sFAplicacion` | `CHAR(10)` | L30 |
| `v_sFolio` | `CHAR(40)` | L31 |
| `v_sDestinoSpei` | `CHAR(40)` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_mensajes` | `bdiprog` | ⚠️ sí | SELECT | L63 |
| `bei_bitacora` | `bdibei` | no | SELECT | L70 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obtiene` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?del` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `dia` | MODIF | del día | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?del` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtsec_archivo_disp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtsec_archivo_disp_bei.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene archivo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtsec_archivo_disp_bei(
  pParam                       char(11)
  pCteEmpresa                  char(9)
  pRegistros                   smallint
) RETURNING char(5), CHAR(17)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pParam` | `char(11)` | — | — |
| `pCteEmpresa` | `char(9)` | — | — |
| `pRegistros` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L10 |
| `sql_err` | `INTEGER` | L11 |
| `vValor` | `INTEGER` | L12 |
| `vNombreArchivo` | `CHAR(17)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bpi_dispersarchivo` | `bdibpi` | ⚠️ sí | SELECT | L40 |
| `bei_archivos` | `bdibei` | no | SELECT | L46 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET vCodRet='00000';` |  |
| L58 | CÓDIGO_RETORNO | `LET vCodRet='00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `?sec_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | 🔵 CONVENCIÓN | nombre_sp |
| `?_di` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sec_`, `?_di` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_obtsec_archivo_dispodp_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_obtsec_archivo_dispodp_bei.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene archivo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_obtsec_archivo_dispodp_bei(
  pParam                       char(9)
  pCteEmpresa                  char(9)
  pRegistros                   smallint
) RETURNING char(5), CHAR(17)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pParam` | `char(9)` | — | — |
| `pCteEmpresa` | `char(9)` | — | — |
| `pRegistros` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `CHAR(5)` | L9 |
| `sql_err` | `INTEGER` | L10 |
| `vValor` | `INTEGER` | L11 |
| `vNombreArchivo` | `CHAR(17)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L36 |
| `bei_dispersiones_odp` | `bdibei` | no | SELECT | L43 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET vCodRet='00000';` |  |
| L55 | CÓDIGO_RETORNO | `LET vCodRet='00001';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `?sec_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `archivo` | ENTIDAD | archivo | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_di` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?odp_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?sec_`, `?_di`, `?odp_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_opers_por_cte_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_opers_por_cte_bei.sql` |
| **LOC (1er CREATE)** | 122 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "operación cliente y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 6 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LA CANTIDAD DE USUARIOS QUE PUEDEN SER CREADOS POR CUENTA |
| FECHA | 04/03/2014 |
| MODIFICACION | Se modifica para que no tome en cuenta los administradores con estatus cancelado 99 |
| FECHA | 03-Nov- 2014 |
| MODIFICACION | Berenice Noriega Guevara - BanCoppel |

### Firma

```sql
CREATE PROCEDURE sp_opers_por_cte_bei(
  pNumCliente                  char(9)
) RETURNING char(5),   INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `iCountUsuario` | `INTEGER` | L8 |
| `iCountManco` | `INTEGER` | L9 |
| `iTotalTokensEmpresa` | `Integer` | L11 |
| `iTokensolicitud` | `Integer` | L12 |
| `iTkn_nseries` | `Integer` | L13 |
| `iCantAdmons` | `Integer` | L14 |
| `iTotalOpersPuedeCrear` | `Integer` | L15 |
| `iOpersCreados` | `Integer` | L16 |
| `iTotalOpersDisp` | `Integer` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_contratacion` | `bdibei` | no | SELECT | L58 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L66 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L71 |
| `bei_servicio` | `bdibei` | no | SELECT | L85 |
| `bei_usuario` | `bdibei` | no | SELECT | L97 |
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L104 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L79 | FÓRMULA | `LET iTotalTokensEmpresa  = iTokensolicitud - iTkn_nseries;` |  |
| L91 | FÓRMULA | `LET iTotalOpersPuedeCrear = iTotalTokensEmpresa - iCantAdmons;` |  |
| L111 | FÓRMULA | `LET iTotalOpersDisp = iTotalOpersPuedeCrear - (iOpersCreados + iCountManco);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?rs_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rs_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_ordenpago_cons_detalle`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_ordenpago_cons_detalle.sql` |
| **LOC (1er CREATE)** | 248 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta orden de pago y detalle" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | "informix".sac_enviosdineroya/bdisac:sac_enviosdineroyahis |
| FECHA | bdisac:sac_enviosdineroya/bdisac:sac_enviosdineroyahis |

### Firma

```sql
CREATE PROCEDURE sp_ordenpago_cons_detalle(
  pEmpresa                     CHAR(3)
  pNum_Cte                     CHAR(9)
  pno_control                  CHAR(20)
  pArchivo                     CHAR(12)
  pTipoDisp                    CHAR(4)
  pFecha                       date
  pRegistro                    SMALLINT
) RETURNING CHAR(5) as codigo, --CODIGO
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNum_Cte` | `CHAR(9)` | — | — |
| `pno_control` | `CHAR(20)` | — | — |
| `pArchivo` | `CHAR(12)` | — | — |
| `pTipoDisp` | `CHAR(4)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pFecha` | `date` | — | — |
| `pRegistro` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L33 |
| `cod_ret` | `CHAR(5)` | L34 |
| `varchivo` | `CHAR(20)` | L35 |
| `valias` | `CHAR(20)` | L36 |
| `vnombre_completo` | `CHAR(104)` | L37 |
| `vclave_envio` | `CHAR(12)` | L38 |
| `vnum_referencia` | `CHAR(16)` | L39 |
| `Vcuenta_origen` | `CHAR(20)` | L40 |
| `vconcepto` | `CHAR(30)` | L41 |
| `vimporte_envio` | `DECIMAL(16,2)` | L42 |
| `vcomision` | `DECIMAL(16,2)` | L43 |
| `viva` | `DECIMAL(16,2)` | L44 |
| `vimporte_total` | `DECIMAL(16,2)` | L45 |
| `vsuc_origen` | `CHAR(4)` | L46 |
| `vfecha_envio` | `DATE` | L47 |
| `vhora_envio` | `DATETIME HOUR TO FRACTION(3)` | L48 |
| `vfecha_pago` | `DATE` | L49 |
| `vhora_pago` | `DATETIME HOUR TO FRACTION(3)` | L50 |
| `vsuc_cance` | `CHAR(4)` | L51 |
| `vfecha_cance` | `DATE` | L52 |
| `vhora_cance` | `DATETIME HOUR TO FRACTION(3)` | L53 |
| `vfecha_bloq` | `DATE` | L54 |
| `vhoy` | `DATETIME YEAR to DAY` | L55 |
| `vfechaStatus` | `DATE` | L56 |
| `vhoraStatus` | `DATETIME HOUR TO FRACTION(3)` | L57 |
| *…3 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sac_param` | `bdisac` | ⚠️ sí | SELECT | L128 |
| `bei_dispersiones_odp` | `bdibei` | no | SELECT | L145 |
| `sac_estatus` | `bdisac` | ⚠️ sí | SELECT | L186 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L118 | VALIDACIÓN_NULL | `If (NVL(pNum_Cte,0) == 0) OR (pNum_Cte is null) Then` |  |
| L122 | VALIDACIÓN_NULL | `If (NVL(pno_control,0) == 0) OR (pno_control is null) Then` |  |
| L133 | VALIDACIÓN_NULL | `IF (NVL(pArchivo,'') == '') OR (pArchivo is null)   THEN --No se recibe numero de archivo, es indivi` |  |
| L174 | FÓRMULA | `LET vfechaStatus=vfecha_envio + VdiasBloqueo; --para saber cuando se bloqueo se le suman los dias a ` |  |
| L230 | FÓRMULA | `LET vfechaStatus=vfecha_envio + VdiasBloqueo; --para saber cuando se bloqueo se le suman los dias a ` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ordenpago` | ENTIDAD | orden de pago | 🔵 CONVENCIÓN | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_perfilamiento_operadores`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_perfilamiento_operadores.sql` |
| **LOC (1er CREATE)** | 183 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "operación perfil de usuario" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_guarda_oper_bei` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | SP temporal para ejecutar un perfilamiento del modulo de cancelaciones de empresanet para aquellas |
| FECHA | 17/03/2021 |

### Firma

```sql
CREATE PROCEDURE sp_perfilamiento_operadores(
) RETURNING CHAR(5) AS codigo, CHAR(300) AS mensaje
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSqlErr` | `INTEGER` | L19 |
| `iIsamErr` | `INTEGER` | L20 |
| `iDescErr` | `CHAR(300)` | L21 |
| `cCodRet` | `CHAR(5)` | L22 |
| `cCodRet2` | `CHAR(5)` | L23 |
| `cMensaje` | `CHAR(300)` | L24 |
| `cMensajeLog` | `CHAR(300)` | L25 |
| `iTransaccion` | `INTEGER` | L26 |
| `cCodSPGuardarOper` | `CHAR(5)` | L27 |
| `iClaveSPGuardarOper` | `INTEGER` | L28 |
| `cClaveOperativa` | `CHAR(10)` | L31 |
| `iClaveMenuOperativa` | `INTEGER` | L32 |
| `iClavePerfil` | `INTEGER` | L33 |
| `cNumCuenta` | `CHAR(16)` | L34 |
| `dMontoMin` | `DECIMAL(16,2)` | L35 |
| `dMontoMax` | `DECIMAL(16,2)` | L36 |
| `cMancomunidad` | `CHAR(5)` | L37 |
| `iClavePerfilAdmin` | `INTEGER` | L38 |
| `iClaveRelMenuOperCancelacion` | `INTEGER` | L41 |
| `cEstatusMancomunidad` | `CHAR(1)` | L42 |
| `iBanderaExisteCancelacion` | `INTEGER` | L43 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operaciones` | `bdibei` | no | SELECT | L105 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_guarda_oper_bei` | `bdibei` | no | L142 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L48 | CÓDIGO_RETORNO | `LET cCodRet         = '00000';` |  |
| L78 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L158 | FÓRMULA | `LET cMensajeLog = cMensajeLog \|\| ' - ERROR INSERT';` |  |
| L169 | CÓDIGO_RETORNO | `LET cCodRet = '00002';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `perfil` | ENTIDAD | perfil de usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?amiento_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?radores` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?amiento_`, `?radores` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_consulta_programacion_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_pp_consulta_programacion_manco_bei.sql` |
| **LOC (1er CREATE)** | 244 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta PP — Pago Programado, ro — Rol Operativo, dirección MAC, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=4 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consulta_programacion_manco_bei(
  p_snum_cte                   CHAR(20)
  p_sid_operacion              INTEGER
) RETURNING CHAR(5),      --Código Retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_snum_cte` | `CHAR(20)` | — | — |
| `p_sid_operacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_sCodRet` | `CHAR(5)` | L54 |
| `v_sMensajeRet` | `CHAR(250)` | L55 |
| `v_sid_operacion` | `INTEGER` | L56 |
| `v_sidusuariosol` | `INTEGER` | L57 |
| `v_snumcte` | `CHAR(20)` | L58 |
| `v_sdescripcion` | `CHAR(30)` | L59 |
| `v_scvepago` | `CHAR(2)` | L60 |
| `v_scvectaori` | `CHAR(2)` | L61 |
| `v_snumctaori` | `CHAR(20)` | L62 |
| `v_scvectadest` | `CHAR(2)` | L63 |
| `v_snumctadest` | `CHAR(20)` | L64 |
| `v_sbancodest` | `CHAR(3)` | L65 |
| `v_sref1` | `CHAR(40)` | L66 |
| `v_sref2` | `CHAR(20)` | L67 |
| `v_sconvenio` | `CHAR(5)` | L68 |
| `v_simporte` | `MONEY(16,2)` | L69 |
| `v_srefcobranza` | `CHAR(40)` | L70 |
| `v_simporteiva` | `MONEY(16,2)` | L71 |
| `v_stipospei` | `INTEGER` | L72 |
| `v_sconcepto` | `CHAR(60)` | L73 |
| `v_sfechainicio` | `DATE` | L74 |
| `v_scvefinal` | `CHAR(2)` | L75 |
| `v_snumrepeteciones` | `INTEGER` | L76 |
| `v_sfechafin` | `DATE` | L77 |
| `v_scveprograma` | `CHAR(2)` | L78 |
| *…24 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L170 |
| `bei_pp_progmanco` | `bdibei` | no | SELECT | L174 |
| `pp_mensajes` | `bdiprog` | ⚠️ sí | SELECT | L218 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?gra` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `mac` | ENTIDAD | dirección MAC | 🔵 CONVENCIÓN | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_p`, `ro`, `?gra`, `?ion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_consultarcveprog_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_pp_consultarcveprog_bei.sql` |
| **LOC (1er CREATE)** | 372 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consultar PP — Pago Programado, clave, ro — Rol Operativo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultarcveprog_bei(
  p_empresa                    Char(3)
  p_snum_cte                   Char(20)
  p_usuario                    INTEGER
  pcve_pagoprog                CHAR(10)
  p_sestado                    CHAR(2)
  pDesde                       INTEGER
  pHasta                       INTEGER
) RETURNING CHAR(5),    --Codigo Retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_empresa` | `Char(3)` | — | — |
| `p_snum_cte` | `Char(20)` | — | — |
| `p_usuario` | `INTEGER` | — | — |
| `pcve_pagoprog` | `CHAR(10)` | `cve`=clave (cve) · `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO / 🔵 CONVENCIÓN |
| `p_sestado` | `CHAR(2)` | — | — |
| `pDesde` | `INTEGER` | — | — |
| `pHasta` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_sCodRet` | `CHAR(5)` | L34 |
| `v_sMensajeRet` | `CHAR(250)` | L35 |
| `sCve_PagoProg` | `CHAR(10)` | L37 |
| `aCve_Estado` | `CHAR(2)` | L39 |
| `sDescripcion_Estado` | `CHAR(30)` | L40 |
| `sConcepto_Pago` | `CHAR(20)` | L42 |
| `sFecha_Programacion` | `DATE` | L43 |
| `sFrecuencia` | `CHAR(30)` | L44 |
| `sMonto` | `MONEY(16,2)` | L45 |
| `sFecha_Inicio` | `DATE` | L46 |
| `sCanal_Programacion` | `CHAR(40)` | L47 |
| `sCuenta_Destino` | `CHAR(20)` | L48 |
| `sBeneficiario` | `CHAR(60)` | L49 |
| `sBanco_Descrip` | `CHAR(40)` | L50 |
| `sCuenta_Origen` | `CHAR(20)` | L51 |
| `sTipo_Operacion` | `CHAR(70)` | L52 |
| `sCve_Notifica_Emi` | `CHAR(2)` | L53 |
| `sCve_Notifica` | `CHAR(2)` | L54 |
| `sTipo_Cta_Origen` | `CHAR(40)` | L55 |
| `sNotifica_Cte` | `CHAR(30)` | L56 |
| `sNotifica_Recep` | `CHAR(30)` | L57 |
| `sCveOperacion` | `CHAR(2)` | L58 |
| `sFecha_Cancelacion` | `DATE` | L59 |
| `v_dfecha` | `DATE` | L60 |
| `iContador` | `INT` | L61 |
| *…10 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L131 |
| `pp_mensajes` | `bdiprog` | ⚠️ sí | SELECT | L138 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L184 |
| `pp_pagoprog` | `bdiprog` | ⚠️ sí | SELECT | L191 |
| `bei_pp_usuprog` | `bdibei` | no | SELECT | L193 |
| `pp_pagospend` | `bdiprog` | ⚠️ sí | SELECT | L278 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L298 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L305 |
| `sd_tarjeta` | `bdicred` | ⚠️ sí | SELECT | L315 |
| `pp_tpnotifica` | `bdiprog` | ⚠️ sí | SELECT | L329 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `monthadd` | `bdicred` | ⚠️ sí | L284 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L74 | CÓDIGO_RETORNO | `LET v_sCodRet = '00001';` |  |
| L341 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `cve` | ENTIDAD | clave (cve) | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_consultarprog_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_pp_consultarprog_bei.sql` |
| **LOC (1er CREATE)** | 133 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consultar PP — Pago Programado, ro — Rol Operativo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | tomado como base el spl bdiprog:sp_consultarprogramaciongeneral_bpi |
| FECHA | 17/06/2015 |
| DESCRIPCION | se agrega el siguiente filtro "and a.banco_destino = d.cve_banco" al siguiente inner |

### Firma

```sql
CREATE PROCEDURE sp_pp_consultarprog_bei(
  p_snum_cte                   CHAR(20)
  pDesde                       INTEGER
  pHasta                       INTEGER
  p_usuario                    INTEGER
) RETURNING CHAR(5),    --Código Retorno
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_snum_cte` | `CHAR(20)` | — | — |
| `pDesde` | `INTEGER` | — | — |
| `pHasta` | `INTEGER` | — | — |
| `p_usuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_sCodRet` | `CHAR(5)` | L16 |
| `v_sMensajeRet` | `CHAR(250)` | L17 |
| `sCve_PagoProg` | `CHAR(10)` | L19 |
| `aCve_Estado` | `CHAR(2)` | L20 |
| `sConcepto_Pago` | `CHAR(20)` | L21 |
| `sFecha_Programacion` | `DATE` | L22 |
| `sFrecuencia` | `CHAR(30)` | L23 |
| `sMonto` | `MONEY(16,2)` | L24 |
| `sFecha_Inicio` | `DATE` | L25 |
| `iContador` | `INT` | L27 |
| `vCountProgramaciones` | `INT` | L28 |
| `v_dfecha` | `DATE` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L75 |
| `pp_mensajes` | `bdiprog` | ⚠️ sí | SELECT | L79 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L84 |
| `pp_pagoprog` | `bdiprog` | ⚠️ sí | SELECT | L91 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L119 | FÓRMULA | `LET iContador = iContador + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_opermancoprog`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_pp_opermancoprog.sql` |
| **LOC (1er CREATE)** | 828 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "operación PP — Pago Programado, Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_registra_correos` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=4 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 24/Junio/2015 |
| FECHA | pendiente |
| FECHA | 25/04/2013 |

### Firma

```sql
CREATE PROCEDURE sp_pp_opermancoprog(
  p_idoperManco                INTEGER
  pidUsuarioSol                INTEGER
  pNumCte                      CHAR(20)
  pDescripcion                 CHAR(30)
  pCvePago                     CHAR(2)
  pCveCtaOri                   CHAR(2)
  pNumCtaOri                   CHAR(20)
  pCveCtaDest                  CHAR(2)
  pNumCtaDest                  CHAR(20)
  pBancoDest                   CHAR(3)
  pRef1                        CHAR(40)
  pRef2                        CHAR(20)
  pConvenio                    CHAR(5)
  pImporte                     Money(16,2)
  pRefCobranza                 CHAR(40)
  pImporteIva                  MONEY(16,2)
  pTipoSPEI                    INTEGER
  pConcepto                    CHAR(60)
  pFechaInicio                 DATE
  pCveFinal                    CHAR(2)
  pNumRepeteciones             INTEGER
  pFechaFin                    DATE
  pCvePrograma                 CHAR(2)
  pTipoDiaria                  CHAR(2)
  pCadaXDia                    INTEGER
  pCadaXSemana                 INTEGER
  pDiasSemana                  CHAR(7)
  pTipoMensual                 CHAR(2)
  pDiaXMes                     INTEGER
  pCadaXMeses                  INTEGER
  pCveOcurre                   CHAR(2)
  pCveDia                      CHAR(2)
  pCveCanal                    CHAR(2)
  pCveNotifica                 CHAR(2)
  pBenEmail                    CHAR(40)
  pBenCveCompania              CHAR(2)
  pBenCelular                  CHAR(10)
  pCveNotificaEmi              CHAR(2)
  pEmiEmail                    CHAR(40)
  pEmiCveCompania              CHAR(2)
  pEmiCelular                  CHAR(10)
  pMensaje                     CHAR(100)
  pCveEstado                   CHAR(2)
  pUserInsert                  CHAR(8)
) RETURNING CHAR(5),CHAR(250)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `p_idoperManco` | `INTEGER` | `ope`=operación · `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | 🔵 CONVENCIÓN / 🟡 INFERIDO |
| `pidUsuarioSol` | `INTEGER` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pDescripcion` | `CHAR(30)` | — | — |
| `pCvePago` | `CHAR(2)` | — | — |
| `pCveCtaOri` | `CHAR(2)` | — | — |
| `pNumCtaOri` | `CHAR(20)` | — | — |
| `pCveCtaDest` | `CHAR(2)` | — | — |
| `pNumCtaDest` | `CHAR(20)` | — | — |
| `pBancoDest` | `CHAR(3)` | — | — |
| `pRef1` | `CHAR(40)` | — | — |
| `pRef2` | `CHAR(20)` | — | — |
| `pConvenio` | `CHAR(5)` | — | — |
| `pImporte` | `Money(16,2)` | — | — |
| `pRefCobranza` | `CHAR(40)` | — | — |
| `pImporteIva` | `MONEY(16,2)` | — | — |
| `pTipoSPEI` | `INTEGER` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pConcepto` | `CHAR(60)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pCveFinal` | `CHAR(2)` | — | — |
| `pNumRepeteciones` | `INTEGER` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pCvePrograma` | `CHAR(2)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pTipoDiaria` | `CHAR(2)` | — | — |
| `pCadaXDia` | `INTEGER` | — | — |
| `pCadaXSemana` | `INTEGER` | — | — |
| `pDiasSemana` | `CHAR(7)` | — | — |
| `pTipoMensual` | `CHAR(2)` | — | — |
| `pDiaXMes` | `INTEGER` | — | — |
| `pCadaXMeses` | `INTEGER` | — | — |
| `pCveOcurre` | `CHAR(2)` | — | — |
| `pCveDia` | `CHAR(2)` | — | — |
| `pCveCanal` | `CHAR(2)` | — | — |
| `pCveNotifica` | `CHAR(2)` | — | — |
| `pBenEmail` | `CHAR(40)` | — | — |
| `pBenCveCompania` | `CHAR(2)` | — | — |
| `pBenCelular` | `CHAR(10)` | — | — |
| `pCveNotificaEmi` | `CHAR(2)` | — | — |
| `pEmiEmail` | `CHAR(40)` | — | — |
| `pEmiCveCompania` | `CHAR(2)` | — | — |
| `pEmiCelular` | `CHAR(10)` | — | — |
| `pMensaje` | `CHAR(100)` | — | — |
| `pCveEstado` | `CHAR(2)` | — | — |
| `pUserInsert` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L23 |
| `vcCodRet` | `CHAR(6)` | L24 |
| `vcCodRet1` | `CHAR(5)` | L25 |
| `vcMensaje` | `CHAR(250)` | L26 |
| `vdFechaHoy` | `DATE` | L27 |
| `vdMesInicio` | `CHAR(2)` | L28 |
| `vdMesFin` | `CHAR(2)` | L29 |
| `vdAnoInicio` | `CHAR(4)` | L30 |
| `vdAnoFin` | `CHAR(4)` | L31 |
| `vcLongDesc` | `CHAR(30)` | L32 |
| `v_sProducto` | `CHAR(5)` | L33 |
| `vcEsNumerico` | `CHAR(1)` | L34 |
| `viDiasLimite` | `INTEGER` | L35 |
| `vdfechaActual` | `DATE` | L36 |
| `viDiasDiferencia` | `INTEGER` | L37 |
| `vdFechaMaximaPermitida` | `DATE` | L38 |
| `vdFechaEstimada` | `DATE` | L39 |
| `vdFechaEstimadaM` | `DATE` | L40 |
| `vsDiaMes` | `CHAR(2)` | L41 |
| `vsciclo` | `INTEGER` | L42 |
| `viPasoPrimerMes` | `CHAR(1)` | L43 |
| `vcCodFechas` | `CHAR(5)` | L44 |
| `vdFechaDisponible1` | `DATE` | L45 |
| `vdFechaMovil2` | `DATE` | L46 |
| `vFechaCaducidad` | `DATE` | L47 |
| *…2 más…* | | |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_mensajes` | `bdiprog` | ⚠️ sí | SELECT | L80 |
| `pp_tppago` | `bdiprog` | ⚠️ sí | SELECT | L84 |
| `pp_pagoprog` | `bdiprog` | ⚠️ sí | SELECT | L102 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L115 |
| `pp_tpcuenta` | `bdiprog` | ⚠️ sí | SELECT | L128 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L158 |
| `pp_producperm` | `bdiprog` | ⚠️ sí | SELECT | L170 |
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L220 |
| `pp_tpfinal` | `bdiprog` | ⚠️ sí | SELECT | L262 |
| `pp_tpprograma` | `bdiprog` | ⚠️ sí | SELECT | L278 |
| `pp_tpcanal` | `bdiprog` | ⚠️ sí | SELECT | L289 |
| `pp_tpnotifica` | `bdiprog` | ⚠️ sí | SELECT | L310 |
| `pp_companias` | `bdiprog` | ⚠️ sí | SELECT | L342 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L480 |
| `pp_tpdiaria` | `bdiprog` | ⚠️ sí | SELECT | L503 |
| `pp_tpmensual` | `bdiprog` | ⚠️ sí | SELECT | L573 |
| `pp_tpocurre` | `bdiprog` | ⚠️ sí | SELECT | L615 |
| `pp_diassemana` | `bdiprog` | ⚠️ sí | SELECT | L632 |
| `pp_parametros` | `bdiprog` | ⚠️ sí | SELECT | L684 |
| `bei_pp_progmanco` | `bdibei` | no | INSERT | L819 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_registra_correos` | `bdinteg` | ⚠️ sí | L808 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L59 | CÓDIGO_RETORNO | `LET vcCodRet = '00000';` |  |
| L692 | FÓRMULA | `LET vdFechaMaximaPermitida = vdfechaActual + viDiasLimite;` |  |
| L695 | FÓRMULA | `LET viDiasDiferencia = pFechaFin - vdfechaActual;` |  |
| L728 | FÓRMULA | `LET vdFechaEstimadaM = month (vdFechaEstimadaM) \|\| '/01/' \|\| year(vdFechaEstimadaM);` |  |
| L732 | FÓRMULA | `LET vsciclo = vsciclo + 1;` |  |
| L738 | FÓRMULA | `LET vdFechaMovil2 = month (vdFechaEstimadaM) \|\| '/01/' \|\| year(vdFechaEstimadaM);` |  |
| L739 | FÓRMULA | `LET vdFechaEstimadaM  = vdFechaMovil2 + pCadaXMeses UNITS MONTH;` |  |
| L743 | FÓRMULA | `LET vdFechaEstimadaM = vdFechaDisponible1 -1;` |  |
| L749 | FÓRMULA | `LET vdFechaEstimadaM  = vdFechaEstimadaM + pCadaXMeses UNITS MONTH;` |  |
| L763 | FÓRMULA | `LET vdFechaEstimada = pFechaInicio + ( ( pNumRepeteciones -1) * pCadaXDia);` |  |
| L781 | FÓRMULA | `LET vdFechaMaximaPermitida = vdfechaActual + viDiasLimite;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?p`, `ro`, `?g` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_regusuarioprog`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_pp_regusuarioprog.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "registro PP — Pago Programado, usuario y ro — Rol Operativo" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=3 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Registra en la tabla "" la relacion de que usuario realiza la programación. |
| FECHA | 17/06/2015 |

### Firma

```sql
CREATE PROCEDURE sp_pp_regusuarioprog(
  pnum_cte                     CHAR(20)
  pid_usuario                  INTEGER
  pcuenta_origen               CHAR(20)
  pdescripcion                 CHAR(20)
  pfolio                       CHAR(40)
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pnum_cte` | `CHAR(20)` | — | — |
| `pid_usuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pcuenta_origen` | `CHAR(20)` | — | — |
| `pdescripcion` | `CHAR(20)` | — | — |
| `pfolio` | `CHAR(40)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L12 |
| `sql_err` | `INTEGER` | L13 |
| `sIdOper` | `INTEGER` | L14 |
| `mens_ret` | `CHAR(60)` | L15 |
| `vcve_pagoprog` | `CHAR(10)` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L77 |
| `pp_pagoprog` | `bdiprog` | ⚠️ sí | SELECT | L79 |
| `bei_pp_usuprog` | `bdibei` | no | INSERT | L88 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `reg` | ACCION | registro | 🟡 INFERIDO | nombre_sp |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?p`, `ro`, `?g` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_pp_valida_prog_manco`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_pp_valida_prog_manco.sql` |
| **LOC (1er CREATE)** | 789 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida PP — Pago Programado, ro — Rol Operativo y Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_pp_valida_prog_manco(
  pNumCte                      CHAR(20)
  pDescripcion                 CHAR(30)
  pCvePago                     CHAR(2)
  pCveCtaOri                   CHAR(2)
  pNumCtaOri                   CHAR(20)
  pCveCtaDest                  CHAR(2)
  pNumCtaDest                  CHAR(20)
  pBancoDest                   CHAR(3)
  pImporte                     Money(16,2)
  pImporteIva                  MONEY(16,2)
  pTipoSPEI                    INTEGER
  pConcepto                    CHAR(60)
  pFechaInicio                 DATE
  pCveFinal                    CHAR(2)
  pNumRepeteciones             INTEGER
  pFechaFin                    DATE
  pCvePrograma                 CHAR(2)
  pTipoDiaria                  CHAR(2)
  pCadaXDia                    INTEGER
  pCadaXSemana                 INTEGER
  pDiasSemana                  CHAR(7)
  pTipoMensual                 CHAR(2)
  pDiaXMes                     INTEGER
  pCadaXMeses                  INTEGER
  pCveOcurre                   CHAR(2)
  pCveDia                      CHAR(2)
  pCveCanal                    CHAR(2)
  pCveNotifica                 CHAR(2)
  pBenEmail                    CHAR(40)
  pBenCveCompania              CHAR(2)
  pBenCelular                  CHAR(10)
  pCveNotificaEmi              CHAR(2)
  pEmiEmail                    CHAR(40)
  pEmiCveCompania              CHAR(2)
  pEmiCelular                  CHAR(10)
  pMensaje                     CHAR(100)
  pUserInsert                  CHAR(8)
) RETURNING CHAR(5),CHAR(250)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pDescripcion` | `CHAR(30)` | — | — |
| `pCvePago` | `CHAR(2)` | — | — |
| `pCveCtaOri` | `CHAR(2)` | — | — |
| `pNumCtaOri` | `CHAR(20)` | — | — |
| `pCveCtaDest` | `CHAR(2)` | — | — |
| `pNumCtaDest` | `CHAR(20)` | — | — |
| `pBancoDest` | `CHAR(3)` | — | — |
| `pImporte` | `Money(16,2)` | — | — |
| `pImporteIva` | `MONEY(16,2)` | — | — |
| `pTipoSPEI` | `INTEGER` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pConcepto` | `CHAR(60)` | — | — |
| `pFechaInicio` | `DATE` | — | — |
| `pCveFinal` | `CHAR(2)` | — | — |
| `pNumRepeteciones` | `INTEGER` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pCvePrograma` | `CHAR(2)` | `ro`=ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO |
| `pTipoDiaria` | `CHAR(2)` | — | — |
| `pCadaXDia` | `INTEGER` | — | — |
| `pCadaXSemana` | `INTEGER` | — | — |
| `pDiasSemana` | `CHAR(7)` | — | — |
| `pTipoMensual` | `CHAR(2)` | — | — |
| `pDiaXMes` | `INTEGER` | — | — |
| `pCadaXMeses` | `INTEGER` | — | — |
| `pCveOcurre` | `CHAR(2)` | — | — |
| `pCveDia` | `CHAR(2)` | — | — |
| `pCveCanal` | `CHAR(2)` | — | — |
| `pCveNotifica` | `CHAR(2)` | — | — |
| `pBenEmail` | `CHAR(40)` | — | — |
| `pBenCveCompania` | `CHAR(2)` | — | — |
| `pBenCelular` | `CHAR(10)` | — | — |
| `pCveNotificaEmi` | `CHAR(2)` | — | — |
| `pEmiEmail` | `CHAR(40)` | — | — |
| `pEmiCveCompania` | `CHAR(2)` | — | — |
| `pEmiCelular` | `CHAR(10)` | — | — |
| `pMensaje` | `CHAR(100)` | — | — |
| `pUserInsert` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L9 |
| `vcCodRet` | `CHAR(5)` | L10 |
| `vcMensaje` | `CHAR(250)` | L11 |
| `vcLongDesc` | `CHAR(30)` | L12 |
| `v_sProducto` | `CHAR(5)` | L13 |
| `vcEsNumerico` | `CHAR(1)` | L14 |
| `vdfechaActual` | `DATE` | L15 |
| `viDiasLimite` | `INTEGER` | L16 |
| `vdFechaMaximaPermitida` | `DATE` | L17 |
| `viDiasDiferencia` | `INTEGER` | L18 |
| `vsDiaMes` | `CHAR(2)` | L19 |
| `vsciclo` | `INTEGER` | L20 |
| `vdFechaEstimadaM` | `DATE` | L21 |
| `viPasoPrimerMes` | `CHAR(1)` | L22 |
| `vcCodFechas` | `CHAR(5)` | L23 |
| `vdFechaDisponible1` | `DATE` | L24 |
| `vdFechaMovil2` | `DATE` | L25 |
| `vdFechaEstimada` | `DATE` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `pp_mensajes` | `bdiprog` | ⚠️ sí | SELECT | L58 |
| `pp_tppago` | `bdiprog` | ⚠️ sí | SELECT | L62 |
| `pp_pagoprog` | `bdiprog` | ⚠️ sí | SELECT | L82 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L95 |
| `pp_tpcuenta` | `bdiprog` | ⚠️ sí | SELECT | L108 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L137 |
| `pp_producperm` | `bdiprog` | ⚠️ sí | SELECT | L147 |
| `si_bancos` | `bdinteg` | ⚠️ sí | SELECT | L200 |
| `pp_tpfinal` | `bdiprog` | ⚠️ sí | SELECT | L246 |
| `pp_tpprograma` | `bdiprog` | ⚠️ sí | SELECT | L263 |
| `pp_tpcanal` | `bdiprog` | ⚠️ sí | SELECT | L274 |
| `pp_tpnotifica` | `bdiprog` | ⚠️ sí | SELECT | L296 |
| `pp_companias` | `bdiprog` | ⚠️ sí | SELECT | L328 |
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L467 |
| `pp_tpdiaria` | `bdiprog` | ⚠️ sí | SELECT | L491 |
| `pp_tpmensual` | `bdiprog` | ⚠️ sí | SELECT | L561 |
| `pp_tpocurre` | `bdiprog` | ⚠️ sí | SELECT | L603 |
| `pp_diassemana` | `bdiprog` | ⚠️ sí | SELECT | L620 |
| `pp_parametros` | `bdiprog` | ⚠️ sí | SELECT | L672 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L33 | CÓDIGO_RETORNO | `LET vcCodRet = '00000';` |  |
| L680 | FÓRMULA | `LET vdFechaMaximaPermitida = vdfechaActual + viDiasLimite;` |  |
| L683 | FÓRMULA | `LET viDiasDiferencia = pFechaFin - vdfechaActual;` |  |
| L716 | FÓRMULA | `LET vdFechaEstimadaM = month (vdFechaEstimadaM) \|\| '/01/' \|\| year(vdFechaEstimadaM);` |  |
| L720 | FÓRMULA | `LET vsciclo = vsciclo + 1;` |  |
| L726 | FÓRMULA | `LET vdFechaMovil2 = month (vdFechaEstimadaM) \|\| '/01/' \|\| year(vdFechaEstimadaM);` |  |
| L727 | FÓRMULA | `LET vdFechaEstimadaM  = vdFechaMovil2 + pCadaXMeses UNITS MONTH;` |  |
| L731 | FÓRMULA | `LET vdFechaEstimadaM = vdFechaDisponible1 -1;` |  |
| L737 | FÓRMULA | `LET vdFechaEstimadaM  = vdFechaEstimadaM + pCadaXMeses UNITS MONTH;` |  |
| L751 | FÓRMULA | `LET vdFechaEstimada = pFechaInicio + ( ( pNumRepeteciones -1) * pCadaXDia);` |  |
| L769 | FÓRMULA | `LET vdFechaMaximaPermitida = vdfechaActual + viDiasLimite;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `pp` | ENTIDAD | PP — Pago Programado / domiciliación (apercred1_pp, generaed | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_p` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?g_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_p`, `ro`, `?g_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_registra_envio_logify_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_registra_envio_logify_bei.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "registra BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `registra` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2018-05-04. |

### Firma

```sql
CREATE PROCEDURE sp_registra_envio_logify_bei(
  pNumGuia                     char(30)
  pCodRast                     char(10)
  pNumSolicitud                char(10)
  pNumCte                      char(9)
  pNumEnvio                    char(3)
  pStatus                      char(3)
  pComentario                  char(200)
  pFechaEnvio                  char(30)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumGuia` | `char(30)` | — | — |
| `pCodRast` | `char(10)` | — | — |
| `pNumSolicitud` | `char(10)` | — | — |
| `pNumCte` | `char(9)` | — | — |
| `pNumEnvio` | `char(3)` | `envio`=envía | ✅ CÓDIGO |
| `pStatus` | `char(3)` | — | — |
| `pComentario` | `char(200)` | — | — |
| `pFechaEnvio` | `char(30)` | `envio`=envía | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `CodRet` | `CHAR(5)` | L9 |
| `sqlErr` | `INTEGER` | L10 |
| `vNumSolicitud` | `CHAR(10)` | L11 |
| `vNumCte` | `CHAR(9)` | L12 |
| `vEstado` | `CHAR(4)` | L13 |
| `vSiglas` | `CHAR(3)` | L14 |
| `vSecDomicilio` | `INTEGER` | L15 |
| `vTipoDir` | `CHAR(1)` | L16 |
| `dFechaReg` | `DATETIME YEAR TO SECOND` | L17 |
| `dFechaEnv` | `DATETIME YEAR TO SECOND` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_direcciones` | `bdinteg` | ⚠️ sí | SELECT | L51 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L59 |
| `si_estados` | `bdinteg` | ⚠️ sí | SELECT | L71 |
| `bei_envios` | `bdibei` | no | SELECT | L75 |
| `bei_envios` | `bdibei` | no | UPDATE | L78 |
| `bei_envios` | `bdibei` | no | INSERT | L88 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L20 | CÓDIGO_RETORNO | `LET codRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `envio` | ACCION | envía | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_logify_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_logify_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_registraoperacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_registraoperacion.sql` |
| **LOC (1er CREATE)** | 102 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "registra" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `registra` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Gearda la operacion en la tabla bei_autorizaoperacion |

### Firma

```sql
CREATE PROCEDURE sp_registraoperacion(
  pNumCte                      CHAR(20)
  pIdUsuario                   INTEGER
  pIdOper                      CHAR(4)
  pFoliOperacion               CHAR(20)
  pStatus                      CHAR(1)
  pNumCtaOri                   CHAR(20)
  pProdCtaOri                  CHAR(4)
  pCveCtaDest                  CHAR(2)
  pNumCtaDest                  CHAR(20)
  pBancoDest                   CHAR(3)
  pBeneficiario                CHAR(100)
  pCveNotifica                 CHAR(2)
  pBenEmail                    CHAR(40)
  pBenCveCompania              CHAR(2)
  pBenCelular                  CHAR(10)
  pImporte                     Money(16,2)
  pImporteComis                MONEY(16,2)
  pRefOri                      CHAR(40)
  pRefDes                      CHAR(40)
  pRefNumSpei                  CHAR(8)
  pNomArchivo                  CHAR (20)
  pFechaOper                   DATE
  pFechaAplica                 DATE
) RETURNING CHAR(5),CHAR(100)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIdOper` | `CHAR(4)` | — | — |
| `pFoliOperacion` | `CHAR(20)` | `operacion`=operación | ✅ CÓDIGO |
| `pStatus` | `CHAR(1)` | — | — |
| `pNumCtaOri` | `CHAR(20)` | — | — |
| `pProdCtaOri` | `CHAR(4)` | — | — |
| `pCveCtaDest` | `CHAR(2)` | — | — |
| `pNumCtaDest` | `CHAR(20)` | — | — |
| `pBancoDest` | `CHAR(3)` | — | — |
| `pBeneficiario` | `CHAR(100)` | — | — |
| `pCveNotifica` | `CHAR(2)` | — | — |
| `pBenEmail` | `CHAR(40)` | — | — |
| `pBenCveCompania` | `CHAR(2)` | — | — |
| `pBenCelular` | `CHAR(10)` | — | — |
| `pImporte` | `Money(16,2)` | — | — |
| `pImporteComis` | `MONEY(16,2)` | — | — |
| `pRefOri` | `CHAR(40)` | — | — |
| `pRefDes` | `CHAR(40)` | — | — |
| `pRefNumSpei` | `CHAR(8)` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pNomArchivo` | `CHAR (20)` | — | — |
| `pFechaOper` | `DATE` | — | — |
| `pFechaAplica` | `DATE` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L8 |
| `vcCodRet` | `CHAR(6)` | L9 |
| `vdFechaHoy` | `DATE` | L13 |
| `v_sProducto` | `CHAR(5)` | L14 |
| `vcEsNumerico` | `CHAR(1)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L50 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L60 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L77 |
| `bpi_pprod` | `bdibpi` | ⚠️ sí | SELECT | L83 |
| `bei_autorizaoperacion` | `bdibei` | no | INSERT | L90 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET vcCodRet = '00000';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `operacion` | ACCION | operación | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_registrarblotemporal_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_registrarblotemporal_bei.sql` |
| **LOC (1er CREATE)** | 43 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "registra lote y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: UPDATE |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 05/08/2011 |

### Firma

```sql
CREATE PROCEDURE sp_registrarblotemporal_bei(
  pFechaBloq                   DATETIME YEAR TO SECOND
  pIdUsuario                   VARCHAR(9)
  pTipoBloq                    VARCHAR(5)
  pTipoBloqTemp                INT
) RETURNING CHAR (5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFechaBloq` | `DATETIME YEAR TO SECOND` | — | — |
| `pIdUsuario` | `VARCHAR(9)` | — | — |
| `pTipoBloq` | `VARCHAR(5)` | — | — |
| `pTipoBloqTemp` | `INT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L13 |
| `cCod_ret` | `CHAR (5)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | UPDATE | L30 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `registra` | ACCION | registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?rb` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `lote` | ENTIDAD | lote (proceso batch) | 🔵 CONVENCIÓN | nombre_sp |
| `?m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `por` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?al_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rb`, `?m`, `?al_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reset_avatar_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_reset_avatar_bei.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "avatar y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: DELETE, SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Realiza eliminacion del registro del Avatar de un usuario |
| FECHA | 27/10/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_reset_avatar_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_avatar` | `bdibei` | no | SELECT | L40 |
| `bei_avatar` | `bdibei` | no | DELETE | L40 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_reset_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `avatar` | ENTIDAD | avatar (foto de perfil del usuario en app) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_reset_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reset_usuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_reset_usuario_bei.sql` |
| **LOC (1er CREATE)** | 107 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_reset_avatar_bei` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Realiza Reset Usuario |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza para que borre el registro de Avatar. |
| MODIFICACION | Berenice Noriega Guevara - BanCoppel |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_reset_usuario_bei(
  pIdUsuario                   INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `vnum_cliente` | `char(9)` | L7 |
| `sPass` | `CHAR(50)` | L11 |
| `sFpass` | `DATE` | L12 |
| `sPass1` | `CHAR(50)` | L13 |
| `sFpass1` | `DATE` | L14 |
| `sPass2` | `CHAR(50)` | L15 |
| `sFpass2` | `DATE` | L16 |
| `sPass3` | `CHAR(50)` | L17 |
| `sFpass3` | `DATE` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_reset_avatar_bei` | `bdibei` | no | L66 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_reset_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_reset_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_reversa_solicitudes_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_reversa_solicitudes_bei.sql` |
| **LOC (1er CREATE)** | 121 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "Reversión — anula/revierte una operación solicitudes y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 2 llamada(s): `sp_set_statustoken_admtoken`, `sp_set_solicitudstatus_admtoken_bei` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |
| FECHA | 2015-06-12. |

### Firma

```sql
CREATE PROCEDURE sp_reversa_solicitudes_bei(
) RETURNING CHAR(5),DATE
```

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L18 |
| `sql_err` | `INTEGER` | L19 |
| `vSolicitud` | `CHAR(10)` | L20 |
| `vStatusSolOr` | `SMALLINT` | L21 |
| `vFechaSol` | `DATE` | L22 |
| `vNumSerieTknA` | `CHAR(10)` | L23 |
| `vUserAtendio` | `CHAR(8)` | L24 |
| `vCod_Ret_Token` | `CHAR(5)` | L25 |
| `vNumCte` | `CHAR(9)` | L26 |
| `vCod_Ret_Sol` | `CHAR(5)` | L27 |
| `vCanToken` | `INTEGER` | L28 |
| `votraFecha` | `DATE` | L29 |
| `vFechaAtencion` | `date` | L30 |
| `vHoraAtencion` | `DATETIME HOUR TO SECOND` | L31 |
| `vFechaHoraAtencion` | `DATETIME YEAR TO FRACTION` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L65 |
| `bei_stasolicitud` | `bdibei` | no | SELECT | L76 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L84 |
| `bei_tokensolicitud` | `bdibei` | no | DELETE | L105 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_set_statustoken_admtoken` | `bdibpi` | ⚠️ sí | L97 |
| `sp_set_solicitudstatus_admtoken_bei` | `bdibei` | no | L110 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L68 | FÓRMULA | `LET vFechaHoraAtencion = ( YEAR(vFechaAtencion) \|\| '-' \|\| MONTH(vFechaAtencion) \|\| '-' \|\| DA` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitudes` | ENTIDAD | solicitudes (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_reversa_tokenasociados_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_reversa_tokenasociados_bei.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "Reversión — anula/revierte una operación token, Sistema Operativo Central, OS — Originación de Solicitudes y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_set_statustoken_admtoken` |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |

### Firma

```sql
CREATE PROCEDURE sp_reversa_tokenasociados_bei(
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
  pUserAtendio                 char(9)
  pCanal                       char(2)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumSolicitud` | `char(10)` | — | — |
| `pNumCliente` | `char(9)` | — | — |
| `pUserAtendio` | `char(9)` | — | — |
| `pCanal` | `char(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L11 |
| `sql_err` | `integer` | L12 |
| `vNstoken` | `char(9)` | L13 |
| `vStatusT` | `char(3)` | L14 |
| `cod_ret_stausTkn` | `char(5)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_set_statustoken_admtoken` | `bdibpi` | ⚠️ sí | L50 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?iad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a`, `?iad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_scvalidatransfctaspropias_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_scvalidatransfctaspropias_bei.sql` |
| **LOC (1er CREATE)** | 97 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida [polisemia] Transferencia, fc — Fuentes Combinadas, ro — Rol Operativo y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=5 / 10 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Pago Tarjeta Credito Bancoppel |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_scvalidatransfctaspropias_bei(
  pEmpresa                     char(3)
  pUsuario                     char(50)
  pCtaOrigen                   char(20)
  pCtaDestino                  char(20)
  pMonto                       money(14,2)
) RETURNING char(5), char(20), char(20)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char(3)` | — | — |
| `pUsuario` | `char(50)` | — | — |
| `pCtaOrigen` | `char(20)` | — | — |
| `pCtaDestino` | `char(20)` | — | — |
| `pMonto` | `money(14,2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vcodret` | `char(5)` | L16 |
| `vUsuStatus` | `smallint` | L17 |
| `vSdoReal` | `money(14,2)` | L18 |
| `vNumTarjOrigen` | `char(20)` | L19 |
| `vNumTarjDestino` | `char(20)` | L20 |
| `sql_err` | `integer` | L21 |
| `cCodRet` | `CHAR(5)` | L23 |
| `cMensajeRet` | `CHAR(50)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L41 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cons_sdodisp_x_tpcalculo` | `bdicheq` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L78 | VALIDACIÓN_NULL | `if 	vNumTarjDestino is null then` |  |
| L82 | VALIDACIÓN_NULL | `if 	vNumTarjOrigen is null then` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_sc` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `trans` | ENTIDAD | [polisemia] Transferencia (bditransfer, bditrans: transferen | 🔵 CONVENCIÓN | nombre_sp |
| `fc` | ENTIDAD | fc — Fuentes Combinadas (subsistema sp_fc_*; biométricos — b | 🔴 SINTÉTICO | nombre_sp |
| `?ta` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `ro` | ENTIDAD | ro — Rol Operativo (subsistema sp_sw_ro_* — bdicnweb) | 🔴 SINTÉTICO | nombre_sp |
| `?pias_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_sc`, `fc`, `?ta`, `ro`, `?pias_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_senet_actualizaestatusctepm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_senet_actualizaestatusctepm.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza estatus y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_senet_actualizaestatusctepm(
  pEmpresa                     CHAR(3)
  pNumCte                      CHAR(20)
  pEstatus                     CHAR(3)
  pFolioToken                  CHAR(12)
  pUsuario                     CHAR(8)
  pIP                          CHAR(15)
  pAdminTipo                   CHAR(30)
  pIdAdmin                     CHAR(30)
  pNombreuno                   CHAR(30)
  pNombredos                   CHAR(30)
  pApellidopaterno             CHAR(30)
  poApellidomaterno            CHAR(30)
) RETURNING CHAR(6) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pNumCte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |
| `pEstatus` | `CHAR(3)` | `estatus`=estatus | ✅ CÓDIGO |
| `pFolioToken` | `CHAR(12)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pIP` | `CHAR(15)` | — | — |
| `pAdminTipo` | `CHAR(30)` | — | — |
| `pIdAdmin` | `CHAR(30)` | — | — |
| `pNombreuno` | `CHAR(30)` | — | — |
| `pNombredos` | `CHAR(30)` | — | — |
| `pApellidopaterno` | `CHAR(30)` | — | — |
| `poApellidomaterno` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(6)` | L14 |
| `iSql_Err` | `INTEGER` | L15 |
| `cMensaje` | `CHAR(100)` | L16 |
| `sEstatusAnterior` | `SMALLINT` | L17 |
| `sEstatus` | `SMALLINT` | L18 |
| `cNumCte` | `CHAR(20)` | L19 |
| `cFolioToken` | `CHAR(12)` | L20 |
| `dtFechaHoy` | `DATE` | L21 |
| `pUsuarioAutoriza` | `CHAR(30)` | L22 |
| `pNumIdentOficial` | `CHAR(30)` | L23 |
| `pCodIdentif` | `CHAR(30)` | L24 |
| `pNumAdmin` | `CHAR(5)` | L25 |
| `pIdServicio` | `CHAR(10)` | L26 |
| `pRepLegal` | `CHAR(60)` | L27 |
| `cServicioId` | `CHAR(30)` | L28 |
| `pUsuarioAut` | `CHAR(30)` | L29 |
| `pFolioContrato` | `CHAR(30)` | L30 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L75 |
| `bei_servicio` | `bdibei` | no | INSERT | L86 |
| `bei_contratacion` | `bdibei` | no | INSERT | L96 |
| `bei_contratacion` | `bdibei` | no | SELECT | L97 |
| `si_cambiostctepm` | `bdinteg` | ⚠️ sí | INSERT | L99 |
| `bei_servicio` | `bdibei` | no | UPDATE | L122 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L66 | VALIDACIÓN_NULL | `IF (pEmpresa = '' OR pEmpresa IS NULL) OR (pNumCte = '' OR pNumCte IS NULL)   THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_senet_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_senet_`, `?pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_senet_altaservicioempresanet`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_senet_altaservicioempresanet.sql` |
| **LOC (1er CREATE)** | 223 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "da de alta servicio y empresa" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_random` |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| MODIFICACION | Se modifica para que reste uno al campo oper_no_token |
| FECHA | 07/11/2013 |
| MODIFICACION | Se modifica para agregar un insert al final para el registro de la conciliación |
| FECHA | 26/08/2015 |

### Firma

```sql
CREATE PROCEDURE sp_senet_altaservicioempresanet(
  pNumCte                      CHAR(20)
  pEstatus                     CHAR(2)
  pUsuario                     CHAR(8)
  pNoTokens                    smallint
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS cCodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pEstatus` | `CHAR(2)` | — | — |
| `pUsuario` | `CHAR(8)` | — | — |
| `pNoTokens` | `smallint` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iSql_Err` | `INTEGER` | L22 |
| `cCodRet` | `CHAR(5)` | L23 |
| `cMensaje` | `CHAR(100)` | L24 |
| `sSecuencia` | `SMALLINT` | L25 |
| `cSolicitud` | `CHAR(10)` | L26 |
| `cFolioSucursal` | `CHAR(16)` | L27 |
| `cRandon1` | `CHAR(6)` | L28 |
| `cRandon2` | `CHAR(2)` | L29 |
| `cRepLegal` | `CHAR(104)` | L30 |
| `cEmpresa` | `CHAR(3)` | L31 |
| `vTotal_admin` | `SMALLINT` | L32 |
| `vNoTokens_oper` | `SMALLINT` | L33 |
| `dMonto` | `DECIMAL(12,2)` | L34 |
| `cTipoPersona` | `CHAR(2)` | L35 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `sc_fechas` | `bdicheq` | ⚠️ sí | SELECT | L92 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L100 |
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L105 |
| `statistics` | `bdibei` | no | UPDATE | L111 |
| `tmp_si_ctepm` | `bdibei` | no | SELECT | L113 |
| `bei_servicio` | `bdibei` | no | SELECT | L119 |
| `bei_contratacion` | `bdibei` | no | INSERT | L123 |
| `bei_contratacion` | `bdibei` | no | SELECT | L124 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L131 |
| `si_direcciones` | `bdinteg` | ⚠️ sí | SELECT | L138 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L146 |
| `sysshmvals` | `sysmaster` | ⚠️ sí | SELECT | L157 |
| `bei_token` | `bdibei` | no | INSERT | L184 |
| `bei_solicitudtoken` | `bdibei` | no | INSERT | L189 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L193 |
| `bdibpi` | `bdibei` | no | INSERT | L206 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_random` | `bdicheq` | ⚠️ sí | L160 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00001';` |  |
| L77 | CÓDIGO_RETORNO | `LET cCodRet = '00002';` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L121 | FÓRMULA | `LET vNoTokens_oper = pNoTokens - vTotal_admin;` |  |
| L135 | VALIDACIÓN_NULL | `IF sSecuencia IS NULL THEN` |  |
| L148 | VALIDACIÓN_NULL | `IF cSolicitud IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_senet_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `alta` | ACCION | da de alta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `empresa` | ENTIDAD | empresa (entidad bancaria) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?net` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_senet_`, `?net` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_senet_consultaestatusctepm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_senet_consultaestatusctepm.sql` |
| **LOC (1er CREATE)** | 116 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta estatus y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_senet_consultaestatusctepm(
  pNumCte                      CHAR(20)
) RETURNING CHAR(6) 		AS CodRet,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | `cte`=cliente | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(6)` | L10 |
| `iSqlErr` | `INTEGER` | L11 |
| `iIsamErr` | `INTEGER` | L12 |
| `cDescErr` | `CHAR(100)` | L13 |
| `cMensaje` | `CHAR(100)` | L14 |
| `cEstatus` | `CHAR(3)` | L15 |
| `cNumCte` | `CHAR(20)` | L16 |
| `cRazonsocial` | `CHAR(60)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L52 |
| `bei_contratacion` | `bdibei` | no | SELECT | L66 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_senet_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `?pm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_senet_`, `?pm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_session`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_session.sql` |
| **LOC (1er CREATE)** | 277 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT, UPDATE, DELETE, SELECT |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=3 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | GUARDA INFO DE SESSION y VALIDA |
| FECHA | 24/09/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_session(
  pIdUsuario                   INTEGER
  pNumCliente                  CHAR(9)
  pIp                          VARCHAR(20)
  pNavegador                   VARCHAR(200)
  pTipoSession                 SMALLINT
  pCode                        VARCHAR(200)
  pCodeNew                     VARCHAR(200)
  pTokenVirtualNew             VARCHAR(20)
  pMin                         INTEGER
  pDel                         INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIp` | `VARCHAR(20)` | — | — |
| `pNavegador` | `VARCHAR(200)` | — | — |
| `pTipoSession` | `SMALLINT` | `ss`=ss — subsistema / canal de monitoreo (abreviación — envia_monitorsol_*_ss_* — bdisolic) | 🔴 SINTÉTICO |
| `pCode` | `VARCHAR(200)` | — | — |
| `pCodeNew` | `VARCHAR(200)` | — | — |
| `pTokenVirtualNew` | `VARCHAR(20)` | — | — |
| `pMin` | `INTEGER` | — | — |
| `pDel` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L16 |
| `sql_err` | `integer` | L17 |
| `sIdUsuario` | `INTEGER` | L19 |
| `sNumCliente` | `CHAR(9)` | L20 |
| `sIp` | `VARCHAR(20)` | L21 |
| `sNavegador` | `VARCHAR(200)` | L22 |
| `sTipoSession` | `SMALLINT` | L23 |
| `sCode1` | `VARCHAR(200)` | L24 |
| `sTokenVirtual1` | `VARCHAR(20)` | L25 |
| `sCode2` | `VARCHAR(200)` | L26 |
| `sTokenVirtual2` | `VARCHAR(20)` | L27 |
| `sFecha` | `DATETIME YEAR to SECOND` | L28 |
| `sFechaReset` | `DATETIME YEAR to SECOND` | L30 |
| `sFecha_Mod` | `DATETIME YEAR to SECOND` | L31 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_session` | `bdibei` | no | SELECT | L73 |
| `bei_session` | `bdibei` | no | DELETE | L73 |
| `bei_session` | `bdibei` | no | INSERT | L129 |
| `bei_session` | `bdibei` | no | UPDATE | L261 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L168 | FÓRMULA | `LET sFecha =CURRENT - pMin units minute;` |  |
| L257 | FÓRMULA | `LET sFechaReset =CURRENT - 30 units SECOND;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ion` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_se`, `ss`, `?ion` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_session_temp`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_session_temp.sql` |
| **LOC (1er CREATE)** | 137 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema (temporal)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=3 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | GUARDA INFO DE SESSION TEMPORAL |
| FECHA | 28/08/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_session_temp(
  pIp                          VARCHAR(20)
  pNavegador                   VARCHAR(200)
  pNumIntentosMax              SMALLINT
  pMinBloq                     INTEGER
  pExitoso                     SMALLINT
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIp` | `VARCHAR(20)` | — | — |
| `pNavegador` | `VARCHAR(200)` | — | — |
| `pNumIntentosMax` | `SMALLINT` | — | — |
| `pMinBloq` | `INTEGER` | — | — |
| `pExitoso` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sIp` | `VARCHAR(20)` | L8 |
| `sNumIntento` | `SMALLINT` | L9 |
| `sFBloqueoTemp` | `DATETIME YEAR to SECOND` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_session_temp` | `bdibei` | no | SELECT | L55 |
| `bei_session_temp` | `bdibei` | no | INSERT | L61 |
| `bei_session_temp` | `bdibei` | no | UPDATE | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L100 | FÓRMULA | `LET sFBloqueoTemp =CURRENT+ pMinBloq units minute;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `temp` | MODIF | temporal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_se`, `ss`, `?ion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_session_temp2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_session_temp2.sql` |
| **LOC (1er CREATE)** | 137 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema (temporal)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=4 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | GUARDA INFO DE SESSION TEMPORAL |
| FECHA | 28/08/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_session_temp2(
  pIp                          VARCHAR(20)
  pNavegador                   VARCHAR(200)
  pNumIntentosMax              SMALLINT
  pMinBloq                     INTEGER
  pExitoso                     SMALLINT
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIp` | `VARCHAR(20)` | — | — |
| `pNavegador` | `VARCHAR(200)` | — | — |
| `pNumIntentosMax` | `SMALLINT` | — | — |
| `pMinBloq` | `INTEGER` | — | — |
| `pExitoso` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sIp` | `VARCHAR(20)` | L8 |
| `sNumIntento` | `SMALLINT` | L9 |
| `sFBloqueoTemp` | `DATETIME YEAR to SECOND` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_session_temp` | `bdibei` | no | SELECT | L55 |
| `bei_session_temp` | `bdibei` | no | INSERT | L61 |
| `bei_session_temp` | `bdibei` | no | UPDATE | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L100 | FÓRMULA | `LET sFBloqueoTemp =CURRENT+ pMinBloq units minute;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `temp` | MODIF | temporal | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_se`, `ss`, `?ion_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_set_devolucion_comentarios_admtoken_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_set_devolucion_comentarios_admtoken_bei.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "devuelve Comisión bancaria — cobro de comisión sobre cuenta, Tarjeta, OS — Originación de Solicitudes, AdmToken — módulo de administración de tokens de autenticación para personas morales y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 10 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Inserta comentarios en la devolucion de envio del AdmToken |
| FECHA | 24/07/2013 |

### Firma

```sql
CREATE PROCEDURE sp_set_devolucion_comentarios_admtoken_bei(
  pNumSolicitud                char(10)
  pNumGuia                     char(30)
  pNumCliente                  char(9)
  pComentarios                 char(200)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumSolicitud` | `char(10)` | — | — |
| `pNumGuia` | `char(30)` | — | — |
| `pNumCliente` | `char(9)` | — | — |
| `pComentarios` | `char(200)` | `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) · `tar`=Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, mover_his_tar, obtener_cta_con_num_tar) | 🔵 CONVENCIÓN / 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L14 |
| `cod_ret` | `char(5)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_envios` | `bdibei` | no | SELECT | L34 |
| `bei_envios` | `bdibei` | no | UPDATE | L36 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_set_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `devolucion` | ACCION | devuelve | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `admtoken` | ENTIDAD | AdmToken — módulo de administración de tokens de autenticaci | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_set_`, `?en`, `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_set_estatus_tokenasociados_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_set_estatus_tokenasociados_bei.sql` |
| **LOC (1er CREATE)** | 65 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "estatus, token, Sistema Operativo Central, OS — Originación de Solicitudes y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — SP delega a 1 llamada(s): `sp_set_statustoken_admtoken` |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=3 / 9 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 2013-08-14. |

### Firma

```sql
CREATE PROCEDURE sp_set_estatus_tokenasociados_bei(
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
  pStatusNuevo                 char(3)
  pUserAtendio                 char(9)
  pCanal                       char(2)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumSolicitud` | `char(10)` | — | — |
| `pNumCliente` | `char(9)` | — | — |
| `pStatusNuevo` | `char(3)` | — | — |
| `pUserAtendio` | `char(9)` | — | — |
| `pCanal` | `char(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L11 |
| `sql_err` | `integer` | L12 |
| `vNstoken` | `char(9)` | L13 |
| `vStatusT` | `char(3)` | L14 |
| `cod_ret_stausTkn` | `char(5)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_set_statustoken_admtoken` | `bdibpi` | ⚠️ sí | L48 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_set_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `estatus` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?iad` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_set_`, `?a`, `?iad` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_set_solicitudcomentarios_admtoken_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_set_solicitudcomentarios_admtoken_bei.sql` |
| **LOC (1er CREATE)** | 40 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "solicitud, Comisión bancaria — cobro de comisión sobre cuenta, Tarjeta, OS — Originación de Solicitudes, AdmToken — módulo de administración de tokens de autenticación para personas morales y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=2 · SINTÉTICO=3 / 10 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Inserta comentarios de la solicitud del AdmToken |
| FECHA | 24/07/2013 |

### Firma

```sql
CREATE PROCEDURE sp_set_solicitudcomentarios_admtoken_bei(
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
  pComentarios                 char(200)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumSolicitud` | `char(10)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pNumCliente` | `char(9)` | — | — |
| `pComentarios` | `char(200)` | `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) · `tar`=Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, mover_his_tar, obtener_cta_con_num_tar) | 🔵 CONVENCIÓN / 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L12 |
| `cod_ret` | `char(5)` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L31 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L33 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_set_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?i` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `admtoken` | ENTIDAD | AdmToken — módulo de administración de tokens de autenticaci | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_set_`, `?en`, `?i` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_set_solicitudstatus_admtoken_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_set_solicitudstatus_admtoken_bei.sql` |
| **LOC (1er CREATE)** | 60 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "solicitud, estatus, AdmToken — módulo de administración de tokens de autenticación para personas morales y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 7 tabla(s) con operaciones: DELETE, INSERT, UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Actualiza el estatus de la solicitud del AdmToken personas morales |
| FECHA | 24-07-2013 |

### Firma

```sql
CREATE PROCEDURE sp_set_solicitudstatus_admtoken_bei(
  pNumSolicitud                char(10)
  pNumCliente                  char(9)
  pUsrAtiende                  char(8)
  pStatusViejo                 char(3)
  pStatusNuevo                 char(3)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumSolicitud` | `char(10)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pNumCliente` | `char(9)` | — | — |
| `pUsrAtiende` | `char(8)` | — | — |
| `pStatusViejo` | `char(3)` | `status`=estatus | 🔵 CONVENCIÓN |
| `pStatusNuevo` | `char(3)` | `status`=estatus | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L11 |
| `cod_ret` | `char(5)` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L30 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L32 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L34 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L37 |
| `bei_tokensolicitud` | `bdibei` | no | DELETE | L39 |
| `bei_tokensolicitud` | `bdibei` | no | UPDATE | L41 |
| `bei_envios` | `bdibei` | no | SELECT | L47 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_set_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `admtoken` | ENTIDAD | AdmToken — módulo de administración de tokens de autenticaci | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_set_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_actualiza_status`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_actualiza_status.sql` |
| **LOC (1er CREATE)** | 222 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza SOE — Soporte Operativo EmpresaNet; confirmado por SME y estatus" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_actualiza_status(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pIdStatus                    SMALLINT
  pIdUsuario                   INTEGER
  pIpUsuario                   CHAR(15)
  pSucCambio                   CHAR(4)
) RETURNING CHAR(5) as codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pIdStatus` | `SMALLINT` | `status`=estatus | 🔵 CONVENCIÓN |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIpUsuario` | `CHAR(15)` | — | — |
| `pSucCambio` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iIdStatus` | `SMALLINT` | L6 |
| `iIdentAdmin` | `CHAR(30)` | L7 |
| `cAscii` | `CHAR(1)` | L105 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L41 |
| `bei_servicio` | `bdibei` | no | SELECT | L47 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_soe_actualiza_status_bpi` | `bdibpi` | ⚠️ sí | L86 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_soe_actualiza_status_tkndinamico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_actualiza_status_tkndinamico.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "actualiza SOE — Soporte Operativo EmpresaNet; confirmado por SME y estatus" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_actualiza_status_tkndinamico(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pStatusToken                 INTEGER
  pNsToken                     CHAR(10)
  pIdUsuario                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pStatusToken` | `INTEGER` | `status`=estatus | ✅ CÓDIGO |
| `pNsToken` | `CHAR(10)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iStatus` | `INTEGER` | L7 |
| `iExiste` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L10 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L45 | CÓDIGO_RETORNO | `LET cCodRet = '00053';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `actualiza` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_tkndinamico` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_tkndinamico` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_actualiza_status_token`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_actualiza_status_token.sql` |
| **LOC (1er CREATE)** | 89 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "actualiza SOE — Soporte Operativo EmpresaNet; confirmado por SME, estatus y token" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_actualiza_status_token(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(9)
  pEstatus                     CHAR(4)
  pNumSerieToken               VARCHAR(10)
  pCIdUsuario                  INTEGER
) RETURNING CHAR(5) AS v_cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pEstatus` | `CHAR(4)` | `status`=estatus | 🔵 CONVENCIÓN |
| `pNumSerieToken` | `VARCHAR(10)` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pCIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iExiste` | `SMALLINT` | L8 |
| `v_cod_ret` | `CHAR(5)` | L9 |
| `vMensajeErr` | `VARCHAR(50)` | L10 |
| `iSamErr` | `INTEGER` | L11 |
| `iSqlErr` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L52 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `status` | ENTIDAD | estatus | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_soe_autentica_cliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_autentica_cliente.sql` |
| **LOC (1er CREATE)** | 54 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "autorización SOE — Soporte Operativo EmpresaNet; confirmado por SME y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_autentica_cliente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pCodEmail                    CHAR(8)
  pAutenticar                  CHAR(1)
) RETURNING CHAR(5) AS codret, INTEGER AS regs_afectados
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pCodEmail` | `CHAR(8)` | — | — |
| `pAutenticar` | `CHAR(1)` | `aut`=autorización | 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iNoRegs` | `SMALLINT` | L6 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L8 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L23 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00049';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `?entica_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cliente` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?entica_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_autenticacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_autenticacion.sql` |
| **LOC (1er CREATE)** | 156 |
| **Callgraph** | ✅ fan_in=0 / fan_out=3 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "SOE — Soporte Operativo EmpresaNet; confirmado por SME y autenticación" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 3 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_autenticacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRfc                         CHAR(13)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRfc` | `CHAR(13)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L22 |
| `iSqlErr` | `INTEGER` | L23 |
| `cNumCte` | `CHAR(20)` | L24 |
| `cEmpresa` | `CHAR(3)` | L25 |
| `iExiste` | `INTEGER` | L26 |
| `cRazon_Social` | `CHAR(40)` | L27 |
| `cNombre1` | `CHAR(50)` | L28 |
| `cNombre2` | `CHAR(50)` | L29 |
| `cApPaterno` | `CHAR(50)` | L30 |
| `cApMaterno` | `CHAR(50)` | L31 |
| `cIdentificacion` | `CHAR(30)` | L32 |
| `cFolioActivacion` | `CHAR(12)` | L33 |
| `cTipoUsuario` | `CHAR(1)` | L34 |
| `iIdStatus` | `SMALLINT` | L35 |
| `iIdUsuario` | `INTEGER` | L36 |
| `cCorreo` | `CHAR(50)` | L37 |
| `cNsToken` | `CHAR(10)` | L38 |
| `cNumSucursal` | `CHAR(4)` | L39 |
| `cMensaje` | `CHAR(50)` | L40 |
| `iNoRegs` | `SMALLINT` | L41 |
| `iValidacion1` | `SMALLINT` | L42 |
| `iValidacion2` | `SMALLINT` | L43 |
| `iIdTipoUsuario` | `SMALLINT` | L44 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_cliente` | `bdinteg` | ⚠️ sí | SELECT | L98 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_soe_autenticacion_bpi` | `bdibpi` | ⚠️ sí | L142 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L114 | VALIDACIÓN_NULL | `IF cNumCte IS NULL OR cRazon_Social IS NULL OR cEmpresa IS NULL` |  |
| L116 | CÓDIGO_RETORNO | `LET cCodRet = '00177';` |  |
| L133 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L141 | CÓDIGO_RETORNO | `LET cCodRet = '00035';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `autenticacion` | ENTIDAD | autenticación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_soe_baja_admin`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_baja_admin.sql` |
| **LOC (1er CREATE)** | 157 |
| **Callgraph** | ✅ fan_in=0 / fan_out=7 |
| **Deps concatenadas** | 17 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "SOE — Soporte Operativo EmpresaNet; confirmado por SME y Administrador — rol de usuario con privilegios administrativos (de baja)" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_baja_admin(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pNumIdentif                  CHAR(30)
  pNumSerTkn                   CHAR(9)
  pIpUsuario                   CHAR(15)
  pSucursal                    CHAR(4)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumIdentif` | `CHAR(30)` | — | — |
| `pNumSerTkn` | `CHAR(9)` | — | — |
| `pIpUsuario` | `CHAR(15)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cCodRetSp` | `CHAR(5)` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iIdUsuario` | `INTEGER` | L9 |
| `cNsToken` | `CHAR(9)` | L10 |
| `sIdStatus` | `SMALLINT` | L11 |
| `cMensajeRetorno` | `CHAR(50)` | L12 |
| `iIdStatusActualUsuario` | `SMALLINT` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L52 |
| `bei_usuario` | `bdibei` | no | SELECT | L62 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L76 |
| `bei_token` | `bdibei` | no | SELECT | L81 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L41 |
| `sp_soe_obtenertoken` | `bdibei` | no | L83 |
| `sp_soe_set_solicitudstatus` | `bdibei` | no | L100 |
| `sp_soe_cancelartoken` | `bdibei` | no | L114 |
| `sp_soe_set_statustoken` | `bdibei` | no | L128 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L88 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_obtenertoken';` |  |
| L105 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_set_solicitudstatus';` |  |
| L119 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_cancelartoken';` |  |
| L133 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_set_statustoken';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `baja` | MODIF | de baja | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_soe_cancelartoken`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_cancelartoken.sql` |
| **LOC (1er CREATE)** | 110 |
| **Callgraph** | ✅ fan_in=5 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela SOE — Soporte Operativo EmpresaNet; confirmado por SME y token" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| MODIFICACION | Se agrega el update a tablas del token tkn_nseries e inserts a tablas de cambio de estatus de token tkn_status_token |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 23 Octubre 2023 |

### Firma

```sql
CREATE PROCEDURE sp_soe_cancelartoken(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumSerieToken               CHAR(10)
  pIdStatusTokenActual         SMALLINT
  pIdNuevoStatusToken          SMALLINT
  pCanal                       CHAR(2)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSerieToken` | `CHAR(10)` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pIdStatusTokenActual` | `SMALLINT` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pIdNuevoStatusToken` | `SMALLINT` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pCanal` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L10 |
| `cCodRetSp` | `CHAR(5)` | L11 |
| `iCodRetSp` | `INTEGER` | L12 |
| `cMesnajeRetorno` | `CHAR(50)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `iIdUsuario` | `INTEGER` | L15 |
| `cSucRegistro` | `CHAR(4)` | L16 |
| `cFolioToken` | `CHAR(25)` | L17 |
| `dFechaStatus` | `DATETIME YEAR TO FRACTION(3)` | L18 |
| `dFechaRegistro` | `DATE` | L19 |
| `iExisteRegistro` | `SMALLINT` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L60 |
| `bei_tokenhis` | `bdibei` | no | INSERT | L79 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | UPDATE | L83 |
| `tkn_status_token` | `bdibpi` | ⚠️ sí | INSERT | L86 |
| `bei_token` | `bdibei` | no | DELETE | L95 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L22 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L44 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pNumSerieToken = '' OR pIdStatusTokenActu` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_cancelaserviciobei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_cancelaserviciobei.sql` |
| **LOC (1er CREATE)** | 287 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 16 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela SOE — Soporte Operativo EmpresaNet; confirmado por SME, servicio y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_cancelaserviciobei(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pIdStatusNuevo               SMALLINT
  pIpUsuario                   CHAR(15)
  pSucursal                    CHAR(4)
  pIdStatusTokenNuevo          SMALLINT
  pCanal                       CHAR(2)
  pIdStatusProceso             SMALLINT
  pDescripcionEstatus          CHAR(50)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pIdStatusNuevo` | `SMALLINT` | — | — |
| `pIpUsuario` | `CHAR(15)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pIdStatusTokenNuevo` | `SMALLINT` | — | — |
| `pCanal` | `CHAR(2)` | — | — |
| `pIdStatusProceso` | `SMALLINT` | `sp`=stored procedure | 🔵 CONVENCIÓN |
| `pDescripcionEstatus` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `iIdUsuario` | `INTEGER` | L12 |
| `iIdStatusActualUsuario` | `SMALLINT` | L13 |
| `cSolicitud` | `CHAR(10)` | L14 |
| `iIdStatusSolicitud` | `SMALLINT` | L15 |
| `cNumSerieToken` | `CHAR(10)` | L16 |
| `iIdStatusToken` | `SMALLINT` | L17 |
| `cMensajeRetorno` | `CHAR(80)` | L18 |
| `iRegUsuarios` | `INTEGER` | L19 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_contratacion` | `bdibei` | no | SELECT | L58 |
| `bei_usuario` | `bdibei` | no | SELECT | L106 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L116 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L134 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L149 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L236 |
| `bei_tkn_envios` | `bdibei` | no | SELECT | L240 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L52 |
| `sp_soe_obtstatus_cancserv` | `bdibei` | no | L72 |
| `sp_soe_set_solicitudstatus` | `bdibei` | no | L153 |
| `sp_soe_cancelartoken` | `bdibei` | no | L178 |
| `sp_soe_set_statustoken` | `bdibei` | no | L205 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L21 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L47 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L77 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_obtstatus_cancserv';` |  |
| L92 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_obtstatus_cancserv';` |  |
| L158 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_set_solicitudstatus';` |  |
| L167 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_obtstatus_cancserv';` |  |
| L183 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_cancelartoken';` |  |
| L192 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_obtstatus_cancserv';` |  |
| L202 | FÓRMULA | `LET iRegUsuarios = iRegUsuarios + 1;` |  |
| L210 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_set_statustoken';` |  |
| L219 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_obtstatus_cancserv';` |  |
| L268 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_obtstatus_cancserv';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cancela` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `servicio` | ENTIDAD | servicio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_soe_cancelatokenporactivar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_cancelatokenporactivar.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 15 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "cancela SOE — Soporte Operativo EmpresaNet; confirmado por SME y token" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_cancelatokenporactivar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNumSerieToken               CHAR(10)
  pIdNuevoStatusToken          SMALLINT
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNumSerieToken` | `CHAR(10)` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pIdNuevoStatusToken` | `SMALLINT` | `token`=token (autenticación) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `iExisteRegistro` | `SMALLINT` | L6 |
| `cNumSerieToken` | `CHAR(10)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L31 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pNumSerieToken = '' OR pIdNuevoStatusToke` |  |
| L26 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `cancela` | ACCION | cancela | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `por` | MODIF | por (criterio) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `activar` | ACCION | activar | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_soe_cargarreversarcuentatoken`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_cargarreversarcuentatoken.sql` |
| **LOC (1er CREATE)** | 328 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga SOE — Soporte Operativo EmpresaNet; confirmado por SME, cuenta y token" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_cargarreversarcuentatoken(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pSistema                     CHAR(1)
  pNumCte                      CHAR(20)
  pSucursal                    CHAR(4)
  pSecDomicilio                SMALLINT
  pFolioSuc                    CHAR (16)
  pCuenta                      CHAR(12)
  pMonto                       DECIMAL(14,2)
  pCantToken                   SMALLINT
  pTransac                     CHAR(4)
  pTransacIva                  CHAR(4)
  pStatusToken                 SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pSistema` | `CHAR(1)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pSecDomicilio` | `SMALLINT` | — | — |
| `pFolioSuc` | `CHAR (16)` | — | — |
| `pCuenta` | `CHAR(12)` | `cuenta`=cuenta | ✅ CÓDIGO |
| `pMonto` | `DECIMAL(14,2)` | — | — |
| `pCantToken` | `SMALLINT` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pTransac` | `CHAR(4)` | — | — |
| `pTransacIva` | `CHAR(4)` | — | — |
| `pStatusToken` | `SMALLINT` | `token`=token (autenticación) | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `pTransac` | `CHAR(4)` | L14 |
| `pTransacIva` | `CHAR(4)` | L15 |
| `cMensajeErr` | `CHAR(255)` | L17 |
| `cDivisa` | `CHAR(2)` | L18 |
| `cNumTarjeta` | `CHAR(20)` | L19 |
| `cTrans` | `CHAR(4)` | L20 |
| `dFecha` | `DATE` | L21 |
| `mSaldo` | `MONEY(14,2)` | L22 |
| `iTransaccion` | `INTEGER` | L23 |
| `iCargo` | `INTEGER` | L24 |
| `cNumSolicitud` | `CHAR(10)` | L25 |
| `mIva` | `MONEY(16,2)` | L26 |
| `cFolio` | `CHAR(12)` | L27 |
| `iSolicitud` | `INTEGER` | L28 |
| `cSolicitud` | `CHAR(10)` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L155 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L295 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L302 |
| `bei_solicitudtoken` | `bdibei` | no | INSERT | L304 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L91 |
| `reversion` | `bdicheq` | ⚠️ sí | L98 |
| `reversion` | `bdicred` | ⚠️ sí | L112 |
| `sp_cons_tar_divisa` | `bdibpi` | ⚠️ sí | L136 |
| `cargo_ref` | `bdicheq` | ⚠️ sí | L159 |
| `cargoref_tc_ofi` | `bdicred` | ⚠️ sí | L251 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L80 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L103 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP reversion';` |  |
| L117 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP reversion';` |  |
| L148 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cons_tar_divisa';` |  |
| L156 | FÓRMULA | `LET pMonto = pMonto / (1+mIva); --Se quita el IVA al monto` | 🔴 MONEY/aritmética financiera |
| L171 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP cargo_ref';` |  |
| L198 | FÓRMULA | `LET mIva = pMonto * mIva;` | 🔴 MONEY/aritmética financiera |
| L211 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP cargo_ref';` |  |
| L240 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP reversion';` |  |
| L263 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP cargoref_tc_ofi';` |  |
| L297 | FÓRMULA | `LET iSolicitud = iSolicitud + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cuenta` | ENTIDAD | cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_cargarreversarcuentatokenreenvio`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_cargarreversarcuentatokenreenvio.sql` |
| **LOC (1er CREATE)** | 377 |
| **Callgraph** | ✅ fan_in=0 / fan_out=6 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "carga SOE — Soporte Operativo EmpresaNet; confirmado por SME, cuenta y token" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=3 / 10 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_cargarreversarcuentatokenreenvio(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pTipo                        CHAR(1)
  pSistema                     CHAR(5)
  pNumCte                      CHAR(20)
  pSucursal                    CHAR(4)
  pSecDomicilio                SMALLINT
  pFolioSuc                    CHAR (16)
  pCuenta                      CHAR(12)
  pMonto                       DECIMAL(14,2)
  pCantToken                   SMALLINT
  pTransac                     CHAR(4)
  pTransacIva                  CHAR(4)
  pStatusToken                 SMALLINT
  pIdsUsuarios                 CHAR(255)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pTipo` | `CHAR(1)` | — | — |
| `pSistema` | `CHAR(5)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pSecDomicilio` | `SMALLINT` | — | — |
| `pFolioSuc` | `CHAR (16)` | — | — |
| `pCuenta` | `CHAR(12)` | `cuenta`=cuenta | 🔵 CONVENCIÓN |
| `pMonto` | `DECIMAL(14,2)` | — | — |
| `pCantToken` | `SMALLINT` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pTransac` | `CHAR(4)` | — | — |
| `pTransacIva` | `CHAR(4)` | — | — |
| `pStatusToken` | `SMALLINT` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pIdsUsuarios` | `CHAR(255)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cCodRetSp` | `CHAR(5)` | L10 |
| `iCodRetSp` | `INTEGER` | L11 |
| `cEmpresa` | `CHAR(3)` | L12 |
| `pTransac` | `CHAR(4)` | L14 |
| `pTransacIva` | `CHAR(4)` | L15 |
| `cMensajeErr` | `CHAR(255)` | L17 |
| `cDivisa` | `CHAR(2)` | L18 |
| `cNumTarjeta` | `CHAR(20)` | L19 |
| `cTrans` | `CHAR(4)` | L20 |
| `dFecha` | `DATE` | L21 |
| `mSaldo` | `MONEY(14,2)` | L22 |
| `iTransaccion` | `INTEGER` | L23 |
| `iCargo` | `INTEGER` | L24 |
| `cNumSolicitud` | `CHAR(10)` | L25 |
| `mIva` | `MONEY(16,2)` | L26 |
| `cFolio` | `CHAR(12)` | L27 |
| `iSolicitud` | `INTEGER` | L28 |
| `cSolicitud` | `CHAR(10)` | L29 |
| `iIdUsuario` | `INTEGER` | L30 |
| `iIdTipoUsuario` | `SMALLINT` | L31 |
| `iIdStatusUsuario` | `SMALLINT` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L163 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L291 |
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L298 |
| `bei_solicitudtoken` | `bdibei` | no | INSERT | L300 |
| `bei_usuario` | `bdibei` | no | SELECT | L310 |
| `bei_usuario` | `bdibei` | no | UPDATE | L317 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L322 |
| `bei_servicio` | `bdibei` | no | SELECT | L325 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L97 |
| `reversion` | `bdicheq` | ⚠️ sí | L104 |
| `reversion` | `bdicred` | ⚠️ sí | L120 |
| `sp_cons_tar_divisa` | `bdibpi` | ⚠️ sí | L144 |
| `cargo_ref` | `bdicheq` | ⚠️ sí | L167 |
| `cargoref_tc_ofi` | `bdicred` | ⚠️ sí | L245 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L86 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L109 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP reversion';` |  |
| L125 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP reversion';` |  |
| L156 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_cons_tar_divisa';` |  |
| L164 | FÓRMULA | `LET pMonto = pMonto / (1+mIva); --Se quita el IVA al monto` | 🔴 MONEY/aritmética financiera |
| L179 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP cargo_ref';` |  |
| L199 | FÓRMULA | `LET mIva = pMonto * mIva;` | 🔴 MONEY/aritmética financiera |
| L212 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP cargo_ref';` |  |
| L232 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP reversion';` |  |
| L257 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP cargoref_tc_ofi';` |  |
| L293 | FÓRMULA | `LET iSolicitud = iSolicitud + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `carga` | ACCION | carga / ingresa | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `reversa` | ACCION | Reversión — anula/revierte una operación (bdibei:sp_reversa_ | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cuenta` | ENTIDAD | cuenta | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?r`, `?re` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_constknacancelar`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_constknacancelar.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 14 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SOE — Soporte Operativo EmpresaNet; confirmado por SME" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_constknacancelar(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pNumSerTkn                   CHAR(9)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumSerTkn` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cIdTipoUsuario` | `CHAR(1)` | L9 |
| `cDescripcion` | `CHAR(20)` | L10 |
| `cEsReplegal` | `CHAR(1)` | L11 |
| `iIdUsuario` | `INTEGER` | L12 |
| `iIdStatusNSToken` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L44 |
| `bei_usuario` | `bdibei` | no | SELECT | L56 |
| `bei_token` | `bdibei` | no | SELECT | L58 |
| `bei_servicio` | `bdibei` | no | SELECT | L73 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L39 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L15 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L34 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L64 | VALIDACIÓN_NULL | `IF cIdTipoUsuario IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?tkna` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cancela` | ACCION | cancela | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tkna`, `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_consulta_solicitud_numsol`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_consulta_solicitud_numsol.sql` |
| **LOC (1er CREATE)** | 123 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 13 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SOE — Soporte Operativo EmpresaNet; confirmado por SME, solicitud y número de solicitud" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_consulta_solicitud_numsol(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(20)
  pNumSolicitud                CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(20)` | — | — |
| `pNumSolicitud` | `CHAR(10)` | `solicitud`=solicitud · `numsol`=número de solicitud | 🔵 CONVENCIÓN / ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L13 |
| `iSqlErr` | `INTEGER` | L14 |
| `cEmpresa` | `CHAR(3)` | L15 |
| `cSolicitud` | `CHAR(10)` | L17 |
| `dFechaSolicitud` | `DATE` | L18 |
| `iIdStatus` | `INTEGER` | L19 |
| `cNumGuia` | `CHAR(30)` | L20 |
| `iNumEnvio` | `SMALLINT` | L21 |
| `iCostoIVA` | `DECIMAL(18,2)` | L22 |
| `iConsto` | `INTEGER` | L23 |
| `cNumSerieToken` | `CHAR(9)` | L24 |
| `cDomicilio` | `CHAR(1)` | L25 |
| `cComentario` | `CHAR(200)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L81 |
| `tkn_parametros` | `bdibpi` | ⚠️ sí | SELECT | L100 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L101 | FÓRMULA | `LET iCostoIVA = iConsto * 1.16;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `numsol` | ENTIDAD | número de solicitud | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_soe_consultacatoperaciones`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_consultacatoperaciones.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SOE — Soporte Operativo EmpresaNet; confirmado por SME, catálogo y operaciones" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_consultacatoperaciones(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCatOperacion` | `INTEGER` | L8 |
| `cDescripcion` | `CHAR(50)` | L9 |
| `iNoRegistros` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_cat_operaciones` | `bdibei` | no | SELECT | L43 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L46 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '01001';` |  |
| L53 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cat` | ENTIDAD | catálogo | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `operaciones` | ENTIDAD | operaciones (plural) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_soe_consultacomentario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_consultacomentario.sql` |
| **LOC (1er CREATE)** | 106 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SOE — Soporte Operativo EmpresaNet; confirmado por SME, Comisión bancaria — cobro de comisión sobre cuenta y Tarjeta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_consultacomentario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pReferencia                  CHAR(30)
  pIdOperacion                 CHAR(4)
  pRegistros                   INTEGER
  pRecuperacion                INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pReferencia` | `CHAR(30)` | — | — |
| `pIdOperacion` | `CHAR(4)` | — | — |
| `pRegistros` | `INTEGER` | — | — |
| `pRecuperacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L12 |
| `iSqlErr` | `INTEGER` | L13 |
| `cMensaje` | `VARCHAR(255)` | L14 |
| `iExiste` | `SMALLINT` | L15 |
| `cFolio` | `CHAR(60)` | L16 |
| `cEmpleado` | `CHAR(8)` | L17 |
| `cFecRegistro` | `DATETIME YEAR TO SECOND` | L18 |
| `cIdOperacion` | `CHAR(4)` | L19 |
| `cNumCliente` | `CHAR(9)` | L20 |
| `cComentario` | `CHAR(500)` | L21 |
| `iNoRegs` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `soe_comentarios` | `bdibei` | no | SELECT | L63 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L48 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L75 | FÓRMULA | `LET iNoRegs = iNoRegs + 1;` |  |
| L82 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |
| L92 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?io` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?en`, `?io` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_consultanombreusuario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_consultanombreusuario.sql` |
| **LOC (1er CREATE)** | 74 |
| **Callgraph** | ✅ fan_in=14 / fan_out=2 |
| **Deps concatenadas** | 12 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SOE — Soporte Operativo EmpresaNet; confirmado por SME, nombre y usuario" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_consultanombreusuario(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pNumSerTkn                   CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumSerTkn` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `cNombreCompleto` | `CHAR(150)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | SELECT | L40 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L29 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L24 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `nombre` | ENTIDAD | nombre | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sp_soe_consultarcontador`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_consultarcontador.sql` |
| **LOC (1er CREATE)** | 67 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Propósito inferido** | "consultar SOE — Soporte Operativo EmpresaNet; confirmado por SME" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_consultarcontador(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS v_cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_cod_ret` | `CHAR(5)` | L7 |
| `vMensajeErr` | `VARCHAR(50)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `iSamErr` | `INTEGER` | L10 |
| `iContinuo` | `INTEGER` | L11 |
| `iCont` | `INTEGER` | L12 |
| `i` | `INTEGER` | L13 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `soe_parametros` | `bdibei` | no | SELECT | L50 |
| `soe_parametros` | `bdibei` | no | UPDATE | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L57 | FÓRMULA | `LET iCont=iContinuo + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `consultar` | ACCION | consultar | 🔵 CONVENCIÓN | nombre_sp |
| `cont` | PREFIJO | familia contabilidad | 🔵 CONVENCIÓN | nombre_sp |
| `?ador` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ador` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_consultatipoidentifpm`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_consultatipoidentifpm.sql` |
| **LOC (1er CREATE)** | 61 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 11 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SOE — Soporte Operativo EmpresaNet; confirmado por SME y identificador (tipo de)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_consultatipoidentifpm(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `cCodigo` | `CHAR(2)` | L9 |
| `cDescripcion` | `CHAR(50)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_tipoidentif` | `bdinteg` | ⚠️ sí | SELECT | L45 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L34 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L29 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `tipo` | MODIF | tipo de | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?entifpm` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?entifpm` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_consultatokenareponer`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_consultatokenareponer.sql` |
| **LOC (1er CREATE)** | 353 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "consulta SOE — Soporte Operativo EmpresaNet; confirmado por SME y token" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| MODIFICACION | Se modifican las consultas que buscan los token no asociados a un usuario en la tabla historica bei_tokenhis obteniendo los tokens mas recientes de los usuarios del cliente |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 16 Octubre 2023 |
| MODIFICACION | Se agrega validacion para saber si un usuario tiene/tuvo token asignado, si no, para su descarte |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 02 Junio 2025 |

### Firma

```sql
CREATE PROCEDURE sp_soe_consultatokenareponer(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pTipoBusqueda                CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pTipoBusqueda` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L21 |
| `iSqlErr` | `INTEGER` | L22 |
| `dCosto` | `DECIMAL(18,2)` | L23 |
| `sIdUsuario` | `SMALLINT` | L24 |
| `cNsToken` | `CHAR(10)` | L25 |
| `iIdStatusToken` | `INTEGER` | L26 |
| `cSolicitud` | `CHAR(10)` | L27 |
| `dFechaSolicitud` | `DATE` | L28 |
| `cNombre` | `CHAR(150)` | L29 |
| `dFechaMaxima` | `DATE` | L30 |
| `dIva` | `DECIMAL(18,2)` | L31 |
| `cUsarWs` | `CHAR(1)` | L32 |
| `iProductos` | `INTEGER` | L33 |
| `iTipoUsuario` | `INTEGER` | L34 |
| `cTokenServicioAdmin` | `CHAR(10)` | L35 |
| `sCountTotalRegsTkn` | `SMALLINT` | L36 |
| `cNsTokenDuplicado` | `CHAR(10)` | L37 |
| `iIdStatusTokenDuplicado` | `INTEGER` | L38 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L83 |
| `tkn_parametros` | `bdibpi` | ⚠️ sí | SELECT | L96 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L99 |
| `bei_usuario` | `bdibei` | no | SELECT | L107 |
| `bei_token` | `bdibei` | no | SELECT | L112 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L121 |
| `bei_tokenhis` | `bdibei` | no | SELECT | L128 |
| `bei_servicio` | `bdibei` | no | SELECT | L146 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L221 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L78 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L68 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00005';` |  |
| L100 | FÓRMULA | `LET dCosto = dCosto + (dCosto * dIva);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rep` | ACCION | reporte | 🔵 CONVENCIÓN | nombre_sp |
| `?oner` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a`, `?oner` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_folios`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_folios.sql` |
| **LOC (1er CREATE)** | 98 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 5 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "SOE — Soporte Operativo EmpresaNet; confirmado por SME y folio" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_folios(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pParametro                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pParametro` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `cMensajeError` | `CHAR(50)` | L7 |
| `cFolio` | `CHAR(7)` | L8 |
| `iSqlErr` | `INTEGER` | L9 |
| `cValor` | `CHAR(50)` | L10 |
| `iPosGuion` | `SMALLINT` | L11 |
| `i` | `SMALLINT` | L12 |
| `cC` | `CHAR(1)` | L13 |
| `iContinuo` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `soe_parametros` | `bdibei` | no | SELECT | L55 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L62 | FÓRMULA | `LET iPosGuion = i + 1;` |  |
| L73 | FÓRMULA | `LET iContinuo = iContinuo + 1;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `folio` | ENTIDAD | folio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_generacodigoautenticacion`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_generacodigoautenticacion.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 4 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "genera SOE — Soporte Operativo EmpresaNet; confirmado por SME, código y autenticación" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `INSERT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_generacodigoautenticacion(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pRfc                         CHAR(13)
  pNoIdentificacion            CHAR(30)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pRfc` | `CHAR(13)` | — | — |
| `pNoIdentificacion` | `CHAR(30)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `integer` | L6 |
| `cCodEmail` | `CHAR(8)` | L7 |
| `iExiste` | `INTEGER` | L8 |
| `iCodEncontrado` | `SMALLINT` | L9 |
| `iCodGen` | `INTEGER` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `soe_codigo_email` | `bdibei` | no | SELECT | L46 |
| `soe_codigo_email` | `bdibei` | no | INSERT | L50 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `getrandomcode` | `bdibei` | no | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `genera` | ACCION | genera / produce | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `codigo` | ENTIDAD | código | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `autenticacion` | ENTIDAD | autenticación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_soe_generarreporteactividades`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_generarreporteactividades.sql` |
| **LOC (1er CREATE)** | 82 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 10 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "generar SOE — Soporte Operativo EmpresaNet; confirmado por SME, reporte y identificador" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_generarreporteactividades(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pFechaInic                   DATE
  pFechaFin                    DATE
  pCatOperacion                CHAR(500)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNumCte` | `CHAR(9)` | — | — |
| `pFechaInic` | `DATE` | — | — |
| `pFechaFin` | `DATE` | — | — |
| `pCatOperacion` | `CHAR(500)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `dFechOperacion` | `DATE` | L11 |
| `cDescripOper` | `CHAR(50)` | L12 |
| `iIdUsuario` | `INTEGER` | L13 |
| `cNombre` | `CHAR(150)` | L14 |
| `iNoRegistros` | `INTEGER` | L15 |
| `cIdOperacion` | `CHAR (4)` | L16 |
| `cSecTransaccion` | `CHAR(16)` | L17 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_bitacora` | `bdibei` | no | SELECT | L57 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L19 | CÓDIGO_RETORNO | `LET cCodRet         = '00000';` |  |
| L40 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L65 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L70 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `generar` | ACCION | generar (infinitivo — sp_generarbalanza*) | 🟡 INFERIDO | nombre_sp |
| `reporte` | ENTIDAD | reporte | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `?iv` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?ades` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?iv`, `?ades` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_insertacomentario`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_insertacomentario.sql` |
| **LOC (1er CREATE)** | 114 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "inserta SOE — Soporte Operativo EmpresaNet; confirmado por SME, Comisión bancaria — cobro de comisión sobre cuenta y Tarjeta" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_insertacomentario(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pcFolio                      char(10)
  pcNumEmpleado                char(8)
  pcIdOperacion                char(4)
  pcNumCte                     char(9)
  pcReferencia                 varchar(30)
  pcComentario                 char(500)
) RETURNING CHAR(5) AS cCodRetChar,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pcFolio` | `char(10)` | — | — |
| `pcNumEmpleado` | `char(8)` | — | — |
| `pcIdOperacion` | `char(4)` | — | — |
| `pcNumCte` | `char(9)` | — | — |
| `pcReferencia` | `varchar(30)` | — | — |
| `pcComentario` | `char(500)` | `com`=Comisión bancaria — cobro de comisión sobre cuenta (bdicheq:sp_cobra_com, sp_com_manejo_cta_cobro_*; también en OXXO) · `tar`=Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, mover_his_tar, obtener_cta_con_num_tar) | 🔵 CONVENCIÓN / 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `v_cod_ret` | `CHAR(5)` | L7 |
| `vMensajeErr` | `VARCHAR(50)` | L8 |
| `iExiste` | `SMALLINT` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iSamErr` | `INTEGER` | L11 |
| `vcmes` | `CHAR(2)` | L13 |
| `vcanio` | `CHAR(2)` | L14 |
| `cFolioMax` | `CHAR(60)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_fechas` | `bdinteg` | ⚠️ sí | SELECT | L62 |
| `soe_comentarios` | `bdibei` | no | SELECT | L94 |
| `soe_comentarios` | `bdibei` | no | INSERT | L102 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `inserta` | ACCION | inserta / registra | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `com` | ENTIDAD | Comisión bancaria — cobro de comisión sobre cuenta (bdicheq: | 🔵 CONVENCIÓN | nombre_sp |
| `?en` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `tar` | ENTIDAD | Tarjeta (abreviación — bdicheq/bdicred: cons_cta_o_tar, move | 🟡 INFERIDO | nombre_sp |
| `?io` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?en`, `?io` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_nuevo_admin`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_nuevo_admin.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 9 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "SOE — Soporte Operativo EmpresaNet; confirmado por SME y Administrador — rol de usuario con privilegios administrativos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_nuevo_admin(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pNumIdentif                  CHAR(30)
  pNumSerTkn                   CHAR(9)
  pNombre1                     CHAR(50)
  pNombre2                     CHAR(50)
  pApePat                      CHAR(50)
  pApeMat                      CHAR(50)
  pCodIdentif                  CHAR(2)
  pNumIdentifBaja              CHAR(30)
  pEsReplegal                  CHAR(1)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumIdentif` | `CHAR(30)` | — | — |
| `pNumSerTkn` | `CHAR(9)` | — | — |
| `pNombre1` | `CHAR(50)` | — | — |
| `pNombre2` | `CHAR(50)` | — | — |
| `pApePat` | `CHAR(50)` | — | — |
| `pApeMat` | `CHAR(50)` | — | — |
| `pCodIdentif` | `CHAR(2)` | — | — |
| `pNumIdentifBaja` | `CHAR(30)` | — | — |
| `pEsReplegal` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L5 |
| `iSqlErr` | `INTEGER` | L6 |
| `iIdUsuario` | `INTEGER` | L7 |
| `iIdStatus` | `INTEGER` | L8 |
| `cNumSerieToken` | `CHAR(9)` | L9 |
| `iFolioContrato` | `INTEGER` | L10 |
| `cFolioActiva` | `CHAR(12)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L47 |
| `bei_usuario` | `bdibei` | no | SELECT | L50 |
| `bei_servicio` | `bdibei` | no | SELECT | L53 |
| `bei_servicio` | `bdibei` | no | INSERT | L59 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L38 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L33 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_nuevo_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_nuevo_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_obt_direcciones_cliente`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_obt_direcciones_cliente.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene SOE — Soporte Operativo EmpresaNet; confirmado por SME, direcciones y cliente" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_obt_direcciones_cliente(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pTipoDir                     CHAR(1)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pTipoDir` | `CHAR(1)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L14 |
| `iSqlErr` | `INTEGER` | L15 |
| `cCalle` | `CHAR(40)` | L16 |
| `cNumInt` | `CHAR(10)` | L17 |
| `cNumExt` | `CHAR(10)` | L18 |
| `cColonia` | `CHAR(60)` | L19 |
| `cMunDel` | `CHAR(100)` | L20 |
| `cEstado` | `CHAR(30)` | L21 |
| `cCp` | `CHAR(5)` | L22 |
| `cObservaciones` | `CHAR(80)` | L23 |
| `cDescTipoDir` | `CHAR(20)` | L24 |
| `Cmensaje` | `CHAR (100)` | L25 |
| `cSecuencia` | `CHAR (1)` | L26 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_direcciones_actual` | `bdinteg` | ⚠️ sí | SELECT | L65 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L54 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `direcciones` | ENTIDAD | direcciones | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cliente` | ENTIDAD | cliente | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_soe_obtener_solicitud_token`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_obtener_solicitud_token.sql` |
| **LOC (1er CREATE)** | 75 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 8 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene SOE — Soporte Operativo EmpresaNet; confirmado por SME, solicitud y token" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_obtener_solicitud_token(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pSolicitud                   CHAR(10)
  pIdStatusSolicitud           SMALLINT
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pSolicitud` | `CHAR(10)` | `solicitud`=solicitud | ✅ CÓDIGO |
| `pIdStatusSolicitud` | `SMALLINT` | `solicitud`=solicitud | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L7 |
| `iSqlErr` | `INTEGER` | L8 |
| `cNoSolicitud` | `CHAR(10)` | L9 |
| `dFechaSolicitud` | `DATETIME YEAR TO SECOND` | L10 |
| `iUnidades` | `SMALLINT` | L11 |
| `iNoRegistros` | `INTEGER` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L44 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L14 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pNumCliente = '' OR pIdStatusSolicitud IS NULL THEN` |  |
| L32 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L51 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L57 | CÓDIGO_RETORNO | `LET cCodRet = '00421';` |  |
| L59 | CÓDIGO_RETORNO | `LET cCodRet = '00422';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_soe_obtenercuentasparatoken`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_obtenercuentasparatoken.sql` |
| **LOC (1er CREATE)** | 96 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene SOE — Soporte Operativo EmpresaNet; confirmado por SME, cuentas y token" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_obtenercuentasparatoken(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pSucursal                    CHAR(4)
  pCostoToken                  MONEY(18,2)
  pCveOperacion                CHAR(12)
  pRegistros                   INTEGER
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pSucursal` | `CHAR(4)` | — | — |
| `pCostoToken` | `MONEY(18,2)` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pCveOperacion` | `CHAR(12)` | — | — |
| `pRegistros` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cEmpresa` | `CHAR(3)` | L8 |
| `mIva` | `MONEY(18, 2)` | L9 |
| `mCosto` | `MONEY(18,2)` | L10 |
| `cCuenta` | `CHAR(20)` | L11 |
| `cNombreProducto` | `CHAR(40)` | L12 |
| `iNoRegistros` | `INTEGER` | L13 |
| `iRegistros` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `si_sucursales` | `bdinteg` | ⚠️ sí | SELECT | L52 |
| `sc_maechq` | `bdicheq` | ⚠️ sí | SELECT | L60 |
| `si_bpipprod` | `bdinteg` | ⚠️ sí | SELECT | L64 |
| `sd_maecred` | `bdicred` | ⚠️ sí | SELECT | L68 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L42 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L16 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L36 | VALIDACIÓN_NULL | `IF pUsuario = '' OR pIdFuncion = '' OR pSucursal = '' OR pCostoToken IS NULL OR pNumCliente = '' OR ` |  |
| L37 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | FÓRMULA | `LET mIva = pCostoToken * mIva;` |  |
| L56 | FÓRMULA | `LET mCosto = pCostoToken + mIva;` |  |
| L79 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L84 | CÓDIGO_RETORNO | `LET cCodRet = '00017';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `cuentas` | ENTIDAD | cuentas (plural) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?para` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?para` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_obtenerdatosgenreasigroladmin`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_obtenerdatosgenreasigroladmin.sql` |
| **LOC (1er CREATE)** | 77 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Deps concatenadas** | 7 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene SOE — Soporte Operativo EmpresaNet; confirmado por SME, datos, rol y Administrador — rol de usuario con privilegios administrativos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=4 · INFERIDO=1 · SINTÉTICO=1 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_obtenerdatosgenreasigroladmin(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pNumSerTkn                   CHAR(9)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumSerTkn` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cCodRetSp` | `CHAR(5)` | L8 |
| `iCodRetSp` | `INTEGER` | L9 |
| `iIdUsuario` | `INTEGER` | L10 |
| `cNombreCompleto` | `CHAR(100)` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L44 |
| `bei_usuario` | `bdibei` | no | SELECT | L46 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L47 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |
| `sp_soe_consultanombreusuario` | `bdibei` | no | L50 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L13 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L55 | EXCEPCIÓN | `RAISE EXCEPTION iCodRetSp, 0, 'ERROR EN LA EJECUCION DEL SP sp_soe_consultanombreusuario';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `gen` | ACCION | genera / general | 🟡 INFERIDO | nombre_sp |
| `?reasig` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `rol` | ENTIDAD | rol / perfil | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?reasig` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_obtenertoken`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_obtenertoken.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ✅ fan_in=4 / fan_out=1 |
| **Deps concatenadas** | 6 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene SOE — Soporte Operativo EmpresaNet; confirmado por SME y token" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_obtenertoken(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pNumSerTkn                   CHAR(9)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumSerTkn` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `cNsToken` | `CHAR(9)` | L9 |
| `cIdStatus` | `SMALLINT` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L42 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L35 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L30 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_soe_obtparametro`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_obtparametro.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "obtiene SOE — Soporte Operativo EmpresaNet; confirmado por SME y parámetro" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_obtparametro(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pParametro                   CHAR(20)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pParametro` | `CHAR(20)` | `parametro`=parámetro | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `cMensajeError` | `CHAR(50)` | L8 |
| `cValor` | `CHAR(50)` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `soe_parametros` | `bdibei` | no | SELECT | L45 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L11 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L28 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L48 | VALIDACIÓN_NULL | `IF cValor IS NULL OR TRIM(cValor) = '' THEN` |  |
| L49 | CÓDIGO_RETORNO | `LET cCodRet = '00190';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `obt` | ACCION | obtiene | 🟡 INFERIDO | nombre_sp |
| `parametro` | ENTIDAD | parámetro | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_soe_reenviotoken`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_reenviotoken.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "envía SOE — Soporte Operativo EmpresaNet; confirmado por SME y token" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo · `envía` → `INSERT` encontrado en el cuerpo · `envía` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_reenviotoken(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumSolicitud                CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumSolicitud` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `cNumCte` | `CHAR(9)` | L6 |
| `cNsToken` | `CHAR(9)` | L7 |
| `cFolioSucursal` | `CHAR(16)` | L8 |
| `cSucursal` | `CHAR(5)` | L9 |
| `dMonto` | `DECIMAL(12,2)` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L40 |
| `bei_solicitudtoken` | `bdibei` | no | UPDATE | L45 |
| `bei_stasolicitud` | `bdibei` | no | INSERT | L48 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L51 |
| `bdibpi` | `bdibei` | no | INSERT | L62 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L36 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L12 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L31 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_re` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `envio` | ACCION | envía | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_re` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_registra_evento`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_registra_evento.sql` |
| **LOC (1er CREATE)** | 69 |
| **Callgraph** | ✅ fan_in=0 / fan_out=2 |
| **Propósito inferido** | "registra SOE — Soporte Operativo EmpresaNet; confirmado por SME y evento/notificación" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `notifica` → `EXECUTE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=0 · CONVENCIÓN=4 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_registra_evento(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCliente                  CHAR(20)
  pNoIdentificacion            CHAR(30)
  pEmailAlterno                CHAR(50)
  pCodEmail                    CHAR(8)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCliente` | `CHAR(20)` | — | — |
| `pNoIdentificacion` | `CHAR(30)` | — | — |
| `pEmailAlterno` | `CHAR(50)` | — | — |
| `pCodEmail` | `CHAR(8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `dFechaMail` | `DATETIME YEAR TO SECOND` | L6 |
| `eMailAlterno` | `CHAR(50)` | L7 |
| `vnumgenerico` | `CHAR(20)` | L8 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | 🔵 CONVENCIÓN | nombre_sp |
| `registra` | ACCION | registra | 🔵 CONVENCIÓN | nombre_sp |
| `evento` | ENTIDAD | evento/notificación | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_soe_set_statustoken`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_set_statustoken.sql` |
| **LOC (1er CREATE)** | 81 |
| **Callgraph** | ✅ fan_in=5 / fan_out=1 |
| **Deps concatenadas** | 1 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "SOE — Soporte Operativo EmpresaNet; confirmado por SME, estatus y token" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `confirma` → `SELECT` encontrado en el cuerpo · `confirma` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_set_statustoken(
  pIdUsuario                   CHAR(8)
  pIdFuncion                   CHAR(10)
  pNsToken                     CHAR(9)
  pEstatusViejo                SMALLINT
  pEstatusNuevo                SMALLINT
  pUsuAtendido                 CHAR(8)
  pCanal                       CHAR(2)
) RETURNING CHAR(5) AS v_cod_ret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNsToken` | `CHAR(9)` | `token`=token (autenticación) | ✅ CÓDIGO |
| `pEstatusViejo` | `SMALLINT` | `status`=estatus | ✅ CÓDIGO |
| `pEstatusNuevo` | `SMALLINT` | `status`=estatus | ✅ CÓDIGO |
| `pUsuAtendido` | `CHAR(8)` | — | — |
| `pCanal` | `CHAR(2)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `iExiste` | `SMALLINT` | L7 |
| `v_cod_ret` | `CHAR(5)` | L8 |
| `vMensajeErr` | `VARCHAR(50)` | L9 |
| `iSqlErr` | `INTEGER` | L10 |
| `iSamErr` | `INTEGER` | L11 |
| `iIdStatus` | `SMALLINT` | L12 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L45 |
| `tkn_status_token` | `bdibpi` | ⚠️ sí | INSERT | L67 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?_set_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_set_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_soe_validasolicitud`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_validasolicitud.sql` |
| **LOC (1er CREATE)** | 64 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 3 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida SOE — Soporte Operativo EmpresaNet; confirmado por SME y solicitud" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_validasolicitud(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
  pNumSerTkn                   CHAR(10)
) RETURNING CHAR(5) AS codret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |
| `pNumSerTkn` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L4 |
| `iSqlErr` | `INTEGER` | L5 |
| `sIdStatus` | `SMALLINT` | L6 |
| `iNoRegistros` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L39 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L30 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L9 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L25 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L42 | FÓRMULA | `LET iNoRegistros = iNoRegistros + 1;` |  |
| L46 | CÓDIGO_RETORNO | `LET cCodRet = '00413';			RETURN cCodRet;` |  |
| L50 | CÓDIGO_RETORNO | `LET cCodRet = '00414';		ELIF sIdStatus > 120 THEN` |  |
| L51 | CÓDIGO_RETORNO | `LET cCodRet = '00415';		ELIF sIdStatus = 0 THEN` |  |
| L52 | CÓDIGO_RETORNO | `LET cCodRet = '00416';		ELIF sIdStatus = 120 THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `solicitud` | ENTIDAD | solicitud | ✅ CÓDIGO | nombre_sp, texto_cuerpo |

---

## `sp_soe_validatokensasignados`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_soe_validatokensasignados.sql` |
| **LOC (1er CREATE)** | 94 |
| **Callgraph** | ✅ fan_in=0 / fan_out=1 |
| **Deps concatenadas** | 2 SPs adicionales en el archivo (NO analizados aquí) |
| **Propósito inferido** | "valida SOE — Soporte Operativo EmpresaNet; confirmado por SME, token y OS — Originación de Solicitudes" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo · `confirma` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 8 términos |

### Firma

```sql
CREATE PROCEDURE sp_soe_validatokensasignados(
  pUsuario                     CHAR(8)
  pIdFuncion                   CHAR(10)
  pNumCte                      CHAR(9)
) RETURNING CHAR(5) AS codret,
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUsuario` | `CHAR(8)` | — | — |
| `pIdFuncion` | `CHAR(10)` | — | — |
| `pNumCte` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCodRet` | `CHAR(5)` | L6 |
| `iSqlErr` | `INTEGER` | L7 |
| `iCodRetSp` | `INTEGER` | L8 |
| `iNumTokens` | `INTEGER` | L9 |
| `iNumTknsAsignados` | `INTEGER` | L10 |
| `iNumTknsNuevos` | `INTEGER` | L11 |
| `iTotalTokens` | `INTEGER` | L12 |
| `dCosto` | `DECIMAL(18,2)` | L13 |
| `dIva` | `DECIMAL(18,2)` | L14 |
| `dCostoIva` | `DECIMAL(18,2)` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L53 |
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L54 |
| `tkn_parametros` | `bdibpi` | ⚠️ sí | SELECT | L67 |
| `si_param` | `bdinteg` | ⚠️ sí | SELECT | L70 |

### Llamadas (EXECUTE PROCEDURE / CALL)

| SP llamado | Base | Cross-DB | Línea |
|------------|------|----------|-------|
| `sp_cnsif_confirmaejecutivo` | `bdinteg` | ⚠️ sí | L47 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L17 | CÓDIGO_RETORNO | `LET cCodRet = '00000';` |  |
| L42 | CÓDIGO_RETORNO | `LET cCodRet = '00003';` |  |
| L63 | FÓRMULA | `LET iNumTokens = iNumTknsAsignados + iNumTknsNuevos;` |  |
| L68 | FÓRMULA | `LET iTotalTokens = 10 - iNumTokens;` |  |
| L71 | FÓRMULA | `LET dCostoIva = (dCosto * dIva) +  dCosto;` |  |
| L73 | CÓDIGO_RETORNO | `LET cCodRet = '00190';` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `soe` | ENTIDAD | SOE — Soporte Operativo EmpresaNet; confirmado por SME (Jorg | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?s` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `asigna` | ACCION | asigna | 🔵 CONVENCIÓN | nombre_sp |
| `?d` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s`, `?d` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_token_avatar_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_token_avatar_bei.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "token, avatar y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ⚠️ PARCIAL — Tokens confirmados en el vocab pero DML no correlaciona con el propósito |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza la información del avatar |
| FECHA | 22/09/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_token_avatar_bei(
  pIdUsuario                   INTEGER
  pNumCliente                  CHAR(9)
  pTokenVirtual                VARCHAR(20)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pTokenVirtual` | `VARCHAR(20)` | `token`=token (autenticación) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `avatar` | ENTIDAD | avatar (foto de perfil del usuario en app) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | 🔵 CONVENCIÓN | nombre_sp |

---

## `sp_token_manc_aut_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_token_manc_aut_bei.sql` |
| **LOC (1er CREATE)** | 129 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza para que retorne el nombre de usuario (sUserNomAdmin) con una longitud |
| MODIFICACION | Jos el Hernez Gonzz |
| FECHA | 05-Diciembre-2016 |

### Firma

```sql
CREATE PROCEDURE sp_token_manc_aut_bei(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING char(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L17 |
| `sql_err` | `INTEGER` | L18 |
| `iTotalReg` | `INTEGER` | L19 |
| `sNsToken` | `VARCHAR(10)` | L20 |
| `sIdStatusToken` | `INTEGER` | L21 |
| `sUserNomAdmin` | `CHAR(250)` | L22 |
| `sIdUserAdmin` | `INTEGER` | L23 |
| `sIdMancomunidad` | `INTEGER` | L24 |
| `sUserNom` | `CHAR(50)` | L26 |
| `sIdUser` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L78 |
| `bei_usuario` | `bdibei` | no | SELECT | L111 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_token_manc_aut_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_token_manc_aut_bei2.sql` |
| **LOC (1er CREATE)** | 134 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | se clona y modifica el spl para mostrar el nombre de la persona en lugar de usuario |
| FECHA | NA |
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_token_manc_aut_bei2(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING char(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L17 |
| `sql_err` | `INTEGER` | L18 |
| `iTotalReg` | `INTEGER` | L19 |
| `sNsToken` | `VARCHAR(10)` | L20 |
| `sIdStatusToken` | `INTEGER` | L21 |
| `sUserNomAdmin` | `CHAR(50)` | L22 |
| `sIdUserAdmin` | `INTEGER` | L23 |
| `sIdMancomunidad` | `INTEGER` | L24 |
| `sUserNom` | `CHAR(50)` | L26 |
| `sIdUser` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L82 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L116 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_manc_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_user_manc_aut_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_user_manc_aut_bei.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza para que retorne el nombre de usuario (sUserNom) con una longitud de 250 |
| MODIFICACION | Jos el Hernez Gonzz |

### Firma

```sql
CREATE PROCEDURE sp_user_manc_aut_bei(
  pNumCliente                  CHAR(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
  pIdTipoMov                   SMALLINT
) RETURNING CHAR(5),   INTEGER,   INTEGER,    CHAR(9),    CHAR(250),     CHAR(150),     CHAR(15),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |
| `pIdTipoMov` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L11 |
| `sql_err` | `INTEGER` | L12 |
| `iTotalReg` | `INTEGER` | L14 |
| `sIdUsuario` | `INTEGER` | L16 |
| `sNumCliente` | `CHAR (9)` | L17 |
| `sIdAdminManco` | `INTEGER` | L18 |
| `sIdStatus` | `SMALLINT` | L20 |
| `sUserNom` | `CHAR(250)` | L21 |
| `sIdTipoUser` | `SMALLINT` | L22 |
| `sNombre` | `CHAR(150)` | L23 |
| `sTelCelular` | `CHAR(15)` | L24 |
| `sCiaCel` | `SMALLINT` | L25 |
| `sEmail` | `CHAR(50)` | L26 |
| `sUserNomAdmin` | `CHAR(50)` | L28 |
| `sIdUserAdmin` | `INTEGER` | L29 |
| `sIdUser` | `INTEGER` | L30 |
| `sTipoMov` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L82 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_user_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_user_manc_aut_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_user_manc_aut_bei2.sql` |
| **LOC (1er CREATE)** | 141 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza para que retorne el nombre de usuario (sUserNom) con una longitud de 250 |
| MODIFICACION | Jos el Hernez Gonzz |

### Firma

```sql
CREATE PROCEDURE sp_user_manc_aut_bei2(
  pNumCliente                  CHAR(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
  pIdTipoMov                   SMALLINT
) RETURNING CHAR(5),   INTEGER,   INTEGER,    CHAR(9),    CHAR(250),     CHAR(150),     CHAR(15),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |
| `pIdTipoMov` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L11 |
| `sql_err` | `INTEGER` | L12 |
| `iTotalReg` | `INTEGER` | L14 |
| `sIdUsuario` | `INTEGER` | L16 |
| `sNumCliente` | `CHAR (9)` | L17 |
| `sIdAdminManco` | `INTEGER` | L18 |
| `sIdStatus` | `SMALLINT` | L20 |
| `sUserNom` | `CHAR(250)` | L21 |
| `sIdTipoUser` | `SMALLINT` | L22 |
| `sNombre` | `CHAR(150)` | L23 |
| `sTelCelular` | `CHAR(15)` | L24 |
| `sCiaCel` | `SMALLINT` | L25 |
| `sEmail` | `CHAR(50)` | L26 |
| `sUserNomAdmin` | `CHAR(50)` | L28 |
| `sIdUserAdmin` | `INTEGER` | L29 |
| `sIdUser` | `INTEGER` | L30 |
| `sTipoMov` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L82 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_user_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_manc_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_user_manc_aut_bei3`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_user_manc_aut_bei3.sql` |
| **LOC (1er CREATE)** | 165 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | se clona y modifica el spl para mostrar el nombre de la persona en lugar de usuario |
| FECHA | NA |

### Firma

```sql
CREATE PROCEDURE sp_user_manc_aut_bei3(
  pNumCliente                  CHAR(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
  pIdTipoMov                   SMALLINT
) RETURNING CHAR(5),   INTEGER,   INTEGER,    CHAR(9),    CHAR(50),     CHAR(150),     CHAR(15),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |
| `pIdTipoMov` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L11 |
| `sql_err` | `INTEGER` | L12 |
| `iTotalReg` | `INTEGER` | L14 |
| `sIdUsuario` | `INTEGER` | L16 |
| `sNumCliente` | `CHAR (9)` | L17 |
| `sIdAdminManco` | `INTEGER` | L18 |
| `sIdStatus` | `SMALLINT` | L20 |
| `sUserNom` | `CHAR(250)` | L21 |
| `sIdTipoUser` | `SMALLINT` | L22 |
| `sNombre` | `CHAR(150)` | L23 |
| `sTelCelular` | `CHAR(15)` | L24 |
| `sCiaCel` | `SMALLINT` | L25 |
| `sEmail` | `CHAR(50)` | L26 |
| `sUserNomAdmin` | `CHAR(50)` | L28 |
| `sIdUserAdmin` | `INTEGER` | L29 |
| `sIdUser` | `INTEGER` | L30 |
| `sTipoMov` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L92 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L134 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_user_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?3` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_manc_`, `?3` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_user_per_cte_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_user_per_cte_bei.sql` |
| **LOC (1er CREATE)** | 79 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "cliente y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LA CANTIDAD DE USUARIOS QUE PUEDEN SER CREADOS POR CUENTA |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_user_per_cte_bei(
  pNumCliente                  char(9)
) RETURNING char(5),   INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `iTotalUsuario` | `INTEGER` | L8 |
| `iCountUsuario` | `INTEGER` | L9 |
| `iCountManco` | `INTEGER` | L10 |
| `iTotal` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_contratacion` | `bdibei` | no | SELECT | L45 |
| `bei_usuario` | `bdibei` | no | SELECT | L53 |
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L59 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L64 | FÓRMULA | `LET iTotal=iCountUsuario+iCountManco;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_user_per_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `cte` | ENTIDAD | cliente | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_per_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_val_pass_exis_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_val_pass_exis_bei.sql` |
| **LOC (1er CREATE)** | 85 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=4 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza usuario Bei |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_val_pass_exis_bei(
  pIdUsuario                   INTEGER
  pPass                        CHAR(50)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pPass` | `CHAR(50)` | `ss`=ss — subsistema / canal de monitoreo (abreviación — envia_monitorsol_*_ss_* — bdisolic) | 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sPass` | `CHAR(50)` | L8 |
| `sFpass` | `DATE` | L9 |
| `sPass1` | `CHAR(50)` | L10 |
| `sFpass1` | `DATE` | L11 |
| `sPass2` | `CHAR(50)` | L12 |
| `sFpass2` | `DATE` | L13 |
| `sPass3` | `CHAR(50)` | L14 |
| `sFpass3` | `DATE` | L15 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L51 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `?_val_pa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?is_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_val_pa`, `ss`, `?_e`, `?is_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_foliocar_numserie_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_valida_foliocar_numserie_bei.sql` |
| **LOC (1er CREATE)** | 56 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida folio, número y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| FECHA | 05/08/2011 |
| ACTIVIDAD | Valida que el folio de la caratula y el numero de serie concuerden con el cliente |
| DESCRIPCION | Modificación para consultar el folio en la tabla |
| FECHA | 04 de Septiembre 2013 |
| MODIFICACION | Para consultar el folio de activación en la tabla bei_servio |
| FECHA | 01 de Octubre 2013 |

### Firma

```sql
CREATE PROCEDURE sp_valida_foliocar_numserie_bei(
  pNumCte                      char(9)
  pFolio                       char(25)
  pNumSerie                    char(10)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `char(9)` | `num`=número (de) | 🔵 CONVENCIÓN |
| `pFolio` | `char(25)` | `folio`=folio | ✅ CÓDIGO |
| `pNumSerie` | `char(10)` | `num`=número (de) | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L4 |
| `cCod_ret` | `char (5)` | L5 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_tokensolicitud` | `bdibei` | no | SELECT | L40 |
| `tkn_nseries` | `bdibpi` | ⚠️ sí | SELECT | L44 |
| `bei_servicio` | `bdibei` | no | SELECT | L48 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `folio` | ENTIDAD | folio | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?car_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `num` | ENTIDAD | número (de) | 🔵 CONVENCIÓN | nombre_sp |
| `?serie_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?car_`, `?serie_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_manco_admin_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_valida_manco_admin_bei.sql` |
| **LOC (1er CREATE)** | 52 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos, Administrador — rol de usuario con privilegios administrativos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Valida la Mancomunidad de Usuarios Administradores |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_valida_manco_admin_bei(
  pIdUsuario                   INTEGER
  pNumCliente                  CHAR(9)
) RETURNING char(5), smallint
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sStatusMan` | `smallint` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L38 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sp_valida_montos_definidos_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_valida_montos_definidos_bei.sql` |
| **LOC (1er CREATE)** | 150 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida monto, identificador, OS — Originación de Solicitudes y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=1 · SINTÉTICO=1 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Valida los montos |
| FECHA | 22-ENERO-2015 |
| MODIFICACION | Se ajusta para el manejo correcto de operacion 03 y 08 |
| MODIFICACION | SOLSER |
| FECHA | 30-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sp_valida_montos_definidos_bei(
  pNumCte                      CHAR(9)
  pIdRest                      CHAR(4)
  pNum_cta                     CHAR(20)
  pMontoCtaPropias             DECIMAL(16,2)
  pMontoMismoBanco             DECIMAL(16,2)
  pMontoSpei                   DECIMAL(16,2)
) RETURNING char(5),char(2)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(9)` | — | — |
| `pIdRest` | `CHAR(4)` | `id`=identificador (de) | 🔵 CONVENCIÓN |
| `pNum_cta` | `CHAR(20)` | — | — |
| `pMontoCtaPropias` | `DECIMAL(16,2)` | `monto`=monto | 🔵 CONVENCIÓN |
| `pMontoMismoBanco` | `DECIMAL(16,2)` | `monto`=monto | 🔵 CONVENCIÓN |
| `pMontoSpei` | `DECIMAL(16,2)` | `sp`=stored procedure · `monto`=monto | 🔵 CONVENCIÓN |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vCodRet` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sIdOper` | `char(20)` | L7 |
| `dMontoMaximo` | `decimal(16,2)` | L8 |
| `dRegistros` | `Integer` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_cat_operaciones` | `bdibei` | no | SELECT | L67 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `?s_defin` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `id` | ENTIDAD | identificador (de) | 🔵 CONVENCIÓN | nombre_sp |
| `os` | ENTIDAD | OS — Originación de Solicitudes / subsistema de ofertas (sp_ | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_defin` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_opermanco_desbloqueousr`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_valida_opermanco_desbloqueousr.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=1 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Valida si existe un registro en la bitacora admin para la operacion: Operacion Mancomunada |
| FECHA | 12/07/2018 |
| FECHA | 2018-Agosto |

### Firma

```sql
CREATE PROCEDURE sp_valida_opermanco_desbloqueousr(
  pIdOperacion                 CHAR(4)
  pSubtipoOperacion            CHAR(2)
  pIdMancomunidad              CHAR(10)
  pIdOperacionManco            CHAR(4)
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdOperacion` | `CHAR(4)` | `ope`=operación | 🟡 INFERIDO |
| `pSubtipoOperacion` | `CHAR(2)` | `ope`=operación | 🟡 INFERIDO |
| `pIdMancomunidad` | `CHAR(10)` | `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | 🔵 CONVENCIÓN |
| `pIdOperacionManco` | `CHAR(4)` | `ope`=operación · `manco`=Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos (bdibei:sp_*_manco_bei; DESCRIPCION: 'Actualiza Status Mancomunidad') | 🔵 CONVENCIÓN / 🟡 INFERIDO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L19 |
| `cod_ret` | `CHAR(5)` | L20 |
| `vtotal_registros` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_bitacora_admin` | `bdibei` | no | SELECT | L49 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L37 | VALIDACIÓN_NULL | `IF (pIdOperacion IS NULL OR pSubtipoOperacion IS NULL OR pIdMancomunidad IS NULL OR pIdOperacionManc` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `ope` | ACCION | operación | 🟡 INFERIDO | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | 🔵 CONVENCIÓN | nombre_sp |
| `desbloqueo` | ACCION | desbloquea cuenta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?usr` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r`, `?usr` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_token_activacion_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_valida_token_activacion_bei.sql` |
| **LOC (1er CREATE)** | 120 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que valida si un cliente tiene tokens para activacion desde renovacion de tokens |
| FECHA | 29/08/2018 |

### Firma

```sql
CREATE PROCEDURE sp_valida_token_activacion_bei(
  pSucursal                    CHAR(4)
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pPaginacion                  INTEGER
) RETURNING CHAR(5), CHAR(9), INTEGER, CHAR(10), CHAR(150), INTEGER, CHAR(1), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pPaginacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L13 |
| `cod_ret` | `CHAR(5)` | L14 |
| `vid_tipo_usuario` | `SMALLINT` | L15 |
| `vnum_reg_xpag` | `INTEGER` | L16 |
| `snum_cte` | `CHAR(9)` | L18 |
| `sid_usuario` | `INTEGER` | L19 |
| `sns_token` | `CHAR(10)` | L20 |
| `snombre` | `CHAR(150)` | L21 |
| `sid_status_token` | `INTEGER` | L22 |
| `sid_token_vencido` | `CHAR(1)` | L23 |
| `stotal_registros` | `INTEGER` | L24 |
| `vnum_tokens` | `INTEGER` | L26 |
| `vnum_solicitud` | `CHAR(10)` | L27 |
| `vtkns_preact` | `INTEGER` | L28 |
| `sid_tipo_usuario` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L78 |
| `bei_solicitudtoken` | `bdibei` | no | SELECT | L89 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L34 | FÓRMULA | `LET pPaginacion = pPaginacion * vnum_reg_xpag;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `activa` | ACCION | activa | 🔵 CONVENCIÓN | nombre_sp |
| `?cion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?cion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_token_asociado_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_valida_token_asociado_bei.sql` |
| **LOC (1er CREATE)** | 63 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida token, Sistema Operativo Central y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Cambia Estatus Token |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sp_valida_token_asociado_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING char(5),SMALLINT
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `INTEGER` | L6 |
| `sNsToken` | `CHAR(10)` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_token` | `bdibei` | no | SELECT | L47 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `?_a` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `soc` | ENTIDAD | Sistema Operativo Central (SOC) — confirmado SME | 🔵 CONVENCIÓN | nombre_sp |
| `?iado_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_a`, `?iado_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_valida_token_expira_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_valida_token_expira_bei.sql` |
| **LOC (1er CREATE)** | 108 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (sufijo Exportar — SP genera/exporta archivo de salida)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo · `genera` → `SELECT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Procedimiento que valida si un cliente o usuario tiene tokens proximos a vencer o vencidos |
| FECHA | 31/07/2018 |

### Firma

```sql
CREATE PROCEDURE sp_valida_token_expira_bei(
  pSucursal                    CHAR(4)
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pPaginacion                  INTEGER
) RETURNING CHAR(5), CHAR(10), CHAR(150), CHAR(1), CHAR(1), INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pSucursal` | `CHAR(4)` | — | — |
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pPaginacion` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L13 |
| `cod_ret` | `CHAR(5)` | L14 |
| `vid_tipo_usuario` | `SMALLINT` | L15 |
| `vns_token` | `VARCHAR(10)` | L16 |
| `vnombre` | `CHAR(150)` | L17 |
| `vid_status_solicitud` | `CHAR(1)` | L18 |
| `vid_token_vencido` | `CHAR(1)` | L19 |
| `vnum_tokens` | `INTEGER` | L20 |
| `vnum_reg_xpag` | `INTEGER` | L21 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L62 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L31 | FÓRMULA | `LET pPaginacion = pPaginacion * vnum_reg_xpag;` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `token` | ENTIDAD | token (autenticación) | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `exp` | MODIF | sufijo Exportar — SP genera/exporta archivo de salida (sp_*_ | 🟡 INFERIDO | nombre_sp |
| `?ira_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?ira_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validalineacapgdf_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_validalineacapgdf_bei.sql` |
| **LOC (1er CREATE)** | 48 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida línea, Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200, gdf — código geográfico y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=1 / 6 términos |

### Firma

```sql
CREATE PROCEDURE sp_validalineacapgdf_bei(
  pLineaCap                    CHAR(20)
  pTipoServicio                CHAR(4)
  pIdCatOperacion              CHAR(4)
) RETURNING char(5), integer
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pLineaCap` | `CHAR(20)` | `linea`=línea (de crédito) · `cap`=Captación — cuentas de ahorro/depósito; evidencia: sp_cap_genrepcancelacioncuentascaptacion, nCtaCap, recalculagat1200 (GAT = Ganancia Anual Total regulado por Banxico) | 🔵 CONVENCIÓN / ✅ CÓDIGO |
| `pTipoServicio` | `CHAR(4)` | — | — |
| `pIdCatOperacion` | `CHAR(4)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `INTEGER` | L5 |
| `sCount` | `INTEGER` | L7 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_operacionesmancomunadasoperador` | `bdibei` | no | SELECT | L39 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `linea` | ENTIDAD | línea (de crédito) | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `cap` | ENTIDAD | Captación — cuentas de ahorro/depósito; evidencia: sp_cap_ge | 🔵 CONVENCIÓN | nombre_sp |
| `gdf` | ENTIDAD | gdf — código geográfico / Gobierno CDMX (abreviación — bdisa | 🔴 SINTÉTICO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `gdf` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validapass_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_validapass_bei.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | VObtiene informacion del cliente de BEI |
| FECHA | 08/07/2013 |
| MODIFICACION | Se cambia para que no consulte los telefonos en la tabla si_direcciones_actual sino |
| MODIFICACION | Berenice Noriega |
| FECHA | 23/Junio/2014 |

### Firma

```sql
CREATE PROCEDURE sp_validapass_bei(
  pNumCte                      CHAR(20)
  pNombreUsuario               CHAR(50)
  pEmpresa                     CHAR(3)
) RETURNING CHAR(5),CHAR(50),CHAR(50), CHAR(50),CHAR(50), CHAR(50), CHAR(26), CHAR(13), CHAR(13), DATE, DATE
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `CHAR(20)` | — | — |
| `pNombreUsuario` | `CHAR(50)` | — | — |
| `pEmpresa` | `CHAR(3)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `CHAR(5)` | L8 |
| `sql_err` | `INTEGER` | L9 |
| `cNombre` | `CHAR(26)` | L11 |
| `cIdusuario` | `INTEGER` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L57 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L71 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L78 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?pa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pa`, `ss` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validarpassword_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_validarpassword_bei.sql` |
| **LOC (1er CREATE)** | 35 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida ss — subsistema, ordenante y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=3 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| ACTIVIDAD | Permite validar si la contrase del usuario es correcta |

### Firma

```sql
CREATE PROCEDURE sp_validarpassword_bei(
  pEmpresa                     char(3)
  pUsuario                     char(50)
  pPass                        char(50)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char(3)` | — | — |
| `pUsuario` | `char(50)` | — | — |
| `pPass` | `char(50)` | `ss`=ss — subsistema / canal de monitoreo (abreviación — envia_monitorsol_*_ss_* — bdisolic) | 🔴 SINTÉTICO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L8 |
| `sql_err` | `integer` | L9 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L28 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?rpa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?w` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ord` | ENTIDAD | ordenante / orden (SPEI) | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?rpa`, `ss`, `?w` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validarrespuesta_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_validarrespuesta_bei.sql` |
| **LOC (1er CREATE)** | 46 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida respuesta y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[conf]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=0 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | VALIDA CADA RESPUESTA DEL USURIO- MODULO OLVIDO CONTRASE |
| FECHA | 05/06/2013 |

### Firma

```sql
CREATE PROCEDURE sp_validarrespuesta_bei(
  pid_usuario                  INTEGER
  pIdPregunta                  INT
  pRespuesta                   CHAR(80)
) RETURNING CHAR (5) AS cCod_ret, CHAR(1) AS cResCor
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pid_usuario` | `INTEGER` | — | — |
| `pIdPregunta` | `INT` | — | — |
| `pRespuesta` | `CHAR(80)` | `sp`=stored procedure · `respuesta`=respuesta | 🔵 CONVENCIÓN / ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L17 |
| `cCod_ret` | `CHAR (5)` | L18 |
| `vIdUsuario` | `VARCHAR(11)` | L19 |
| `cResCor` | `CHAR(1)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L37 |
| `bei_respuestas` | `bdibei` | no | SELECT | L38 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `respuesta` | ENTIDAD | respuesta | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_validatknadmin_renovaciontkn`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_validatknadmin_renovaciontkn.sql` |
| **LOC (1er CREATE)** | 72 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida Administrador — rol de usuario con privilegios administrativos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Se valida si el administrador debe renovar su token |
| FECHA | 31/08/2018 |

### Firma

```sql
CREATE PROCEDURE sp_validatknadmin_renovaciontkn(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
) RETURNING CHAR(5), CHAR(10)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L13 |
| `cod_ret` | `CHAR(5)` | L14 |
| `vExisteTkn` | `INTEGER` | L15 |
| `vSolicitud` | `CHAR(10)` | L16 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `valida` | ACCION | valida | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?tkn` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | 🔵 CONVENCIÓN | nombre_sp |
| `?_renovaciontkn` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?tkn`, `?_renovaciontkn` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificaexiste_manco_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_verificaexiste_manco_bei.sql` |
| **LOC (1er CREATE)** | 84 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "verifica Mancomunidad — cuenta u operación con múltiples titulares autorizados; requiere autorización de todos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=3 · INFERIDO=0 · SINTÉTICO=2 / 7 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Busca si la operacion a realiza requiere mancomunidad. |

### Firma

```sql
CREATE PROCEDURE sp_verificaexiste_manco_bei(
  pNumCliente                  CHAR(9)
  pTipoOper                    SMALLINT
  pTipoMov                     SMALLINT
  pIdUsuario                   INTEGER
  pNSToken                     CHAR(10)
) RETURNING CHAR(5),char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pTipoOper` | `SMALLINT` | — | — |
| `pTipoMov` | `SMALLINT` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pNSToken` | `CHAR(10)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L4 |
| `cCod_ret` | `CHAR (5)` | L5 |
| `vExist` | `CHAR(5)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L53 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | 🔵 CONVENCIÓN | nombre_sp |
| `?e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?iste_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `manco` | ENTIDAD | Mancomunidad — cuenta u operación con múltiples titulares au | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?e`, `?iste_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sp_verificauser_exist_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sp_verificauser_exist_bei.sql` |
| **LOC (1er CREATE)** | 66 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "verifica BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=2 · INFERIDO=0 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Verifica si Nombre de Usuario ya Existe en la base de Datos |
| FECHA | 24/05/2013 |
| MODIFICACION | Ahora tambien tomara encuenta los usuarios que esten pendientes de crear en la tabla |
| FECHA | 30 JUNIO 2015 |

### Firma

```sql
CREATE PROCEDURE sp_verificauser_exist_bei(
  pUserNom                     CHAR(50)
) RETURNING char(5), smallint
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUserNom` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sExiste` | `smallint` | L8 |
| `sExisteManco` | `smallint` | L9 |
| `sTotal` | `smallint` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L49 |
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L56 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L61 | FÓRMULA | `LET sTotal=NVL(sExiste,0)+NVL(sExisteManco,0);` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sp` | PREFIJO | stored procedure | 🔵 CONVENCIÓN | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?user_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ist_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?user_e`, `?ist_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_act_statususuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_act_statususuario_bei.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza estatus, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza Status de usuario por Nombre de Usuario |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sps_act_statususuario_bei(
  idUsuario                    integer
  pStatus                      integer
  pIp                          char (15)
  pSuc                         char (4)
  pUsuCambio                   char (8)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `idUsuario` | `integer` | `usuario`=usuario | ✅ CÓDIGO |
| `pStatus` | `integer` | `status`=estatus | ✅ CÓDIGO |
| `pIp` | `char (15)` | — | — |
| `pSuc` | `char (4)` | — | — |
| `pUsuCambio` | `char (8)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `char(5)` | L18 |
| `sql_err` | `integer` | L19 |
| `iIdUsuario` | `integer` | L20 |
| `iStatus` | `integer` | L21 |
| `iEmpresa` | `char(3)` | L22 |
| `iNumCliente` | `char(20)` | L23 |
| `iIdentAdmin` | `char(30)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L55 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L62 |
| `bei_servicio` | `bdibei` | no | SELECT | L71 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `act` | ACCION | actualiza | 🔵 CONVENCIÓN | nombre_sp |
| `status` | ENTIDAD | estatus | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sps_activausuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_activausuario_bei.sql` |
| **LOC (1er CREATE)** | 101 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "activa usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 3 tabla(s) con operaciones: INSERT, SELECT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=0 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Activa y Registra el usuario y registra el cambio de status |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sps_activausuario_bei(
  pNumCte                      char(20)
  pStatus                      integer
  pIp                          char (15)
  pSuc                         char (4)
  pUsuCambio                   char (8)
  pIdentAdmin                  char(30)
  pIdUsuario                   INTEGER
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCte` | `char(20)` | — | — |
| `pStatus` | `integer` | — | — |
| `pIp` | `char (15)` | — | — |
| `pSuc` | `char (4)` | — | — |
| `pUsuCambio` | `char (8)` | — | — |
| `pIdentAdmin` | `char(30)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `integer` | L19 |
| `cCod_ret` | `char(5)` | L20 |
| `iCont` | `smallint` | L21 |
| `iStatus` | `INTEGER` | L22 |
| `sIdUsuario` | `INTEGER` | L23 |
| `sIdentAdmin` | `char(30)` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L49 |
| `bei_usuario` | `bdibei` | no | SELECT | L74 |
| `bei_cambiostusuario` | `bdibei` | no | INSERT | L78 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `activa` | ACCION | activa | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sps_actualiza_usuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_actualiza_usuario_bei.sql` |
| **LOC (1er CREATE)** | 116 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=0 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza usuario Bei |
| FECHA | 24/05/2013 |
| MODIFICACION | Se actualiza para que al recibir el dato del PASS tambien borre el registro de Avatar |
| MODIFICACION | Berenice Noriega Guevara |
| FECHA | 09-Enero-2015 |
| FECHA | 22-ENERO-2015 |
| MODIFICACION | Se actualiza para que al recibir el dato del PASS tambien borre el registro de Avatar |
| MODIFICACION | Berenice Noriega Guevara |
| FECHA | 29-Enero-2015 |

### Firma

```sql
CREATE PROCEDURE sps_actualiza_usuario_bei(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pPass                        CHAR(50)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            Integer
  pUsuario                     CHAR(50)
) RETURNING char(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pPass` | `CHAR(50)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `Integer` | `usuario`=usuario | ✅ CÓDIGO |
| `pUsuario` | `CHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L11 |
| `cod_ret2` | `CHAR(5)` | L12 |
| `sql_err` | `INTEGER` | L13 |
| `vid_tipo_usuario` | `SMALLINT` | L14 |
| `vid_status` | `SMALLINT` | L15 |
| `sPass` | `CHAR(50)` | L17 |
| `sFpass` | `DATE` | L18 |
| `sPass1` | `CHAR(50)` | L19 |
| `sFpass1` | `DATE` | L20 |
| `sPass2` | `CHAR(50)` | L21 |
| `sFpass2` | `DATE` | L22 |
| `sPass3` | `CHAR(50)` | L23 |
| `sFpass3` | `DATE` | L24 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | UPDATE | L86 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

---

## `sps_actualiza_usuario_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_actualiza_usuario_bei_historico.sql` |
| **LOC (1er CREATE)** | 90 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "actualiza usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `actualiza` → `UPDATE` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=0 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Actualiza usuario Bei |
| FECHA | 05/06/2018 |

### Firma

```sql
CREATE PROCEDURE sps_actualiza_usuario_bei_historico(
  pNumCliente                  CHAR(9)
  pIdUsuario                   INTEGER
  pPass                        CHAR(50)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            INTEGER
  pUsuario                     CHAR(50)
  pIdBitacoraAdmin             INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pPass` | `CHAR(50)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pUsuario` | `CHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdBitacoraAdmin` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L14 |
| `sql_err` | `INTEGER` | L15 |
| `sIdPerfil` | `INTEGER` | L16 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario_historico` | `bdibei` | no | INSERT | L41 |
| `bei_usuario` | `bdibei` | no | SELECT | L53 |
| `informix` | `bdibei` | no | INSERT | L56 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L64 |
| `bei_usuario_perfil` | `bdibei` | no | SELECT | L69 |
| `bei_perfil_historico` | `bdibei` | no | INSERT | L72 |
| `bei_perfil` | `bdibei` | no | SELECT | L77 |
| `bei_usuario_perfil_historico` | `bdibei` | no | INSERT | L80 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `actualiza` | ACCION | actualiza | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sps_cons_nom_user_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_cons_nom_user_bei.sql` |
| **LOC (1er CREATE)** | 35 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta nómina y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS NOMBRE DE LA PERSONA , PARA VALIDACION INICIAL EMPRESANET |
| FECHA | 26/04/2013 |

### Firma

```sql
CREATE PROCEDURE sps_cons_nom_user_bei(
  pIdUsuario                   Integer
) RETURNING char(5), char (150)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sNombre` | `CHAR(150)` | L6 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_datos_usuario` | `bdibei` | no | SELECT | L29 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `nom` | ENTIDAD | nómina | 🟡 INFERIDO | nombre_sp |
| `?_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_cons_user_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_cons_user_bei.sql` |
| **LOC (1er CREATE)** | 45 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE DATOS USUARIO POR NOMBRE DE USUARIO , PARA VALIDACION INICIAL EMPRESANET |
| FECHA | 28/08/2014 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sps_cons_user_bei(
  pIdUsuario                   Integer
) RETURNING char(5),Integer,CHAR(9)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sNumCliente` | `CHAR(9)` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L36 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `cons` | ACCION | consulta | 🔵 CONVENCIÓN | nombre_sp |
| `?_user_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_consulta_datos_detalle_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_consulta_datos_detalle_bei.sql` |
| **LOC (1er CREATE)** | 86 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta datos, detalle y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=4 · CONVENCIÓN=0 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Consultar los datos para presentar el detalle de un operacion con mancomunidad |
| FECHA | 01/04/2014 |

### Firma

```sql
CREATE PROCEDURE sps_consulta_datos_detalle_bei(
  pIdMancomunidad              Integer
) RETURNING char(5), Integer, integer,integer,CHAR(150),CHAR(15),CHAR(50),CHAR(50), CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sIdUsuario` | `INTEGER` | L8 |
| `sIdUsuarioSolicita` | `INTEGER` | L9 |
| `sIdUsuarioAfectado` | `INTEGER` | L10 |
| `sIdStatusToken` | `CHAR(50)` | L12 |
| `sTipoMov` | `INTEGER` | L13 |
| `sSolicita` | `CHAR(50)` | L14 |
| `sUsuario` | `CHAR(50)` | L15 |
| `sNombre` | `CHAR(150)` | L16 |
| `sTelCelular` | `CHAR(15)` | L17 |
| `sEmail` | `CHAR(50)` | L18 |
| `sToken` | `CHAR(50)` | L19 |
| `sTokenAux` | `CHAR(50)` | L20 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L67 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L74 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sps_consulta_usuario_detalle_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_consulta_usuario_detalle_bei.sql` |
| **LOC (1er CREATE)** | 71 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuario, detalle y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Verifica si Nombre de Usuario ya Existe en la base de Datos |
| FECHA | 21/02/2014 |

### Firma

```sql
CREATE PROCEDURE sps_consulta_usuario_detalle_bei(
  pIdMancomunidad              Integer
) RETURNING char(5), Integer, Integer,CHAR(250),CHAR(250),CHAR(150),CHAR(15),SMALLINT,CHAR(50),INTEGER,INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdMancomunidad` | `Integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sUsuario` | `CHAR(250)` | L9 |
| `sPass` | `CHAR(250)` | L10 |
| `sNombre` | `CHAR(150)` | L11 |
| `sTelCelular` | `CHAR(15)` | L12 |
| `sCiaCel` | `SMALLINT` | L13 |
| `sEmail` | `CHAR(50)` | L14 |
| `sIdPerfil` | `INTEGER` | L15 |
| `sIdAdminManco` | `INTEGER` | L16 |
| `sTipoMov` | `INTEGER` | L17 |
| `sIdUsuario` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L64 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `detalle` | ENTIDAD | detalle | 🔵 CONVENCIÓN | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sps_consulta_usuarios_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_consulta_usuarios_bei.sql` |
| **LOC (1er CREATE)** | 260 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta usuarios y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=0 · INFERIDO=1 · SINTÉTICO=0 / 4 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR EXISTENTES |
| FECHA | 24/05/2013 |
| MODIFICACION | CONSULTA EL BLOQUEO TEMPORAL EN LA TABLA DE AVATAR |
| FECHA | 22-ENERO-2015 |
| MODIFICACION | SOLSER |

### Firma

```sql
CREATE PROCEDURE sps_consulta_usuarios_bei(
  pNumCliente                  char(9)
  pNoReg                       integer
  pRegIni                      integer
  pIdUserNom                   integer
  pNombrem                     CHAR(150)
  pIdStatus                    smallint
  pIdTipoUser                  smallint
) RETURNING char(5),   integer ,char (9) ,integer   , char(50),smallint, char(150),smallint,  CHAR(15),smallint, CHAR(50)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |
| `pIdUserNom` | `integer` | — | — |
| `pNombrem` | `CHAR(150)` | — | — |
| `pIdStatus` | `smallint` | — | — |
| `pIdTipoUser` | `smallint` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L12 |
| `sql_err` | `integer` | L13 |
| `iTotalReg` | `integer` | L15 |
| `sIdUsuario` | `integer` | L17 |
| `sNumCliente` | `char (9)` | L18 |
| `sUserNom` | `CHAR(50)` | L19 |
| `sNombre` | `CHAR(150)` | L20 |
| `sTelCelular` | `CHAR(15)` | L21 |
| `sCiaCel` | `smallint` | L22 |
| `sEmail` | `CHAR(50)` | L23 |
| `sIdStatus` | `smallint` | L24 |
| `sIdTipoUser` | `smallint` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L69 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `consulta` | ACCION | consulta / lee | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `usuarios` | ENTIDAD | usuarios | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sps_crea_usuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_crea_usuario_bei.sql` |
| **LOC (1er CREATE)** | 113 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "crédito, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: INSERT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=0 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Crea usuario Bei |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sps_crea_usuario_bei(
  pNumCliente                  CHAR(9)
  pIdStatus                    SMALLINT
  pUsuario                     CHAR(50)
  pTipoUsuario                 SMALLINT
  pNombrePerfil                CHAR(50)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            Integer
) RETURNING char(5),INTEGER,INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdStatus` | `SMALLINT` | — | — |
| `pUsuario` | `CHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |
| `pTipoUsuario` | `SMALLINT` | `usuario`=usuario | ✅ CÓDIGO |
| `pNombrePerfil` | `CHAR(50)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `Integer` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L7 |
| `sql_err` | `INTEGER` | L8 |
| `sIdUsuario` | `INTEGER` | L10 |
| `sIdPerfil` | `INTEGER` | L11 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_perfil` | `bdibei` | no | INSERT | L90 |
| `bei_usuario` | `bdibei` | no | INSERT | L96 |
| `bei_datos_usuario` | `bdibei` | no | INSERT | L102 |
| `bei_usuario_perfil` | `bdibei` | no | INSERT | L106 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `?a_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_crea_usuario_bei_historico`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_crea_usuario_bei_historico.sql` |
| **LOC (1er CREATE)** | 92 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "crédito, usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad (histórico)" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 8 tabla(s) con operaciones: SELECT, INSERT |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=0 · INFERIDO=2 · SINTÉTICO=1 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Crea usuario Bei |
| FECHA | 05/06/2018 |

### Firma

```sql
CREATE PROCEDURE sps_crea_usuario_bei_historico(
  pNumCliente                  CHAR(9)
  pIdStatus                    SMALLINT
  pUsuario                     CHAR(50)
  pTipoUsuario                 SMALLINT
  pNombrePerfil                CHAR(50)
  pNombre                      CHAR(150)
  pTelCel                      CHAR(15)
  pCiaCel                      SMALLINT
  pMail                        CHAR(50)
  pIdUsuarioLogeado            INTEGER
  pidBitacoraAdmin             INTEGER
  pIdPerfil                    INTEGER
  pIdUsuario                   INTEGER
) RETURNING CHAR(5)
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdStatus` | `SMALLINT` | — | — |
| `pUsuario` | `CHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |
| `pTipoUsuario` | `SMALLINT` | `usuario`=usuario | ✅ CÓDIGO |
| `pNombrePerfil` | `CHAR(50)` | — | — |
| `pNombre` | `CHAR(150)` | — | — |
| `pTelCel` | `CHAR(15)` | — | — |
| `pCiaCel` | `SMALLINT` | — | — |
| `pMail` | `CHAR(50)` | — | — |
| `pIdUsuarioLogeado` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |
| `pidBitacoraAdmin` | `INTEGER` | — | — |
| `pIdPerfil` | `INTEGER` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L17 |
| `sql_err` | `INTEGER` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_perfil_historico` | `bdibei` | no | INSERT | L44 |
| `bei_perfil` | `bdibei` | no | SELECT | L49 |
| `bei_usuario_historico` | `bdibei` | no | INSERT | L53 |
| `bei_usuario` | `bdibei` | no | SELECT | L65 |
| `bei_datos_usuario_historico` | `bdibei` | no | INSERT | L69 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L77 |
| `bei_usuario_perfil_historico` | `bdibei` | no | INSERT | L81 |
| `bei_usuario_perfil` | `bdibei` | no | SELECT | L84 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `cre` | ENTIDAD | crédito | 🟡 INFERIDO | nombre_sp |
| `?a_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `historico` | MODIF | histórico | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?a_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_destoken_manc_aut_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_destoken_manc_aut_bei.sql` |
| **LOC (1er CREATE)** | 111 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sps_destoken_manc_aut_bei(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L18 |
| `sql_err` | `INTEGER` | L19 |
| `iTotalReg` | `INTEGER` | L20 |
| `sNsToken` | `VARCHAR(10)` | L21 |
| `sIdStatusToken` | `INTEGER` | L22 |
| `sUserNomAdmin` | `CHAR(50)` | L23 |
| `sUserNom` | `CHAR(50)` | L24 |
| `sIdUser` | `INTEGER` | L25 |
| `sIdUserAdmin` | `INTEGER` | L26 |
| `sIdMancomunidad` | `INTEGER` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L75 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `?_des` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_des`, `?_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_ini_session_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_ini_session_bei.sql` |
| **LOC (1er CREATE)** | 158 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=0 · INFERIDO=1 · SINTÉTICO=3 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS PARA INICIAR LA SESION EN LA BANCA EMPRESARIAL |
| FECHA | 26/04/2013 |
| MODIFICACION | Se inicializa la variabe de fechabloqueo cuando ele status es 95 o 10. |
| MODIFICACION | Berenice Noriega - BanCoppel. |
| FECHA | 23/Junio/2014 |

### Firma

```sql
CREATE PROCEDURE sps_ini_session_bei(
  pEmpresa                     char(3)
  pIdUsuario                   Integer
  pIp                          char(15)
) RETURNING char(5), char (20), char(50), smallint, integer, char(19),DATETIME YEAR TO SECOND, integer,char(3), char(20),char(50),smallint,DATETIME YEAR TO SECOND
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `char(3)` | — | — |
| `pIdUsuario` | `Integer` | — | — |
| `pIp` | `char(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L4 |
| `sql_err` | `integer` | L5 |
| `sNumCliente` | `char (20)` | L7 |
| `iIdStatus` | `smallint` | L8 |
| `fecPrimAcceso` | `date` | L9 |
| `fecUltAcceso` | `char(19)` | L10 |
| `iIdStatusToken` | `integer` | L11 |
| `sNombre` | `CHAR(150)` | L12 |
| `vFecha` | `DATETIME YEAR TO SECOND` | L13 |
| `vIdEmpresa` | `char(3)` | L14 |
| `nomEmpresa` | `char(50)` | L15 |
| `sCuenta` | `CHAR(20)` | L16 |
| `sIdTipoUsuario` | `smallint` | L17 |
| `sFBloqueoTemp` | `DATETIME YEAR TO SECOND` | L18 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L64 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L79 |
| `bei_usuario` | `bdibei` | no | UPDATE | L95 |
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L98 | VALIDACIÓN_NULL | `IF fecPrimAcceso IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `?_ini_se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ini_se`, `ss`, `?ion_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_ini_session_bei_duplicado`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_ini_session_bei_duplicado.sql` |
| **LOC (1er CREATE)** | 209 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=0 · INFERIDO=1 · SINTÉTICO=4 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS PARA INICIAR LA SESION EN LA BANCA EMPRESARIAL |
| FECHA | 26/04/2013 |
| MODIFICACION | Se inicializa la variabe de fechabloqueo cuando el status es 95 o 10. |
| MODIFICACION | Berenice Noriega - BanCoppel. |
| FECHA | 23/Junio/2014 |
| MODIFICACION | Se optimiza |
| MODIFICACION | BanCoppel. |
| FECHA | 02/Octubre/2018 |
| DESCRIPCION | Optimizaci spl |
| FECHA | 13/02/2019 |

### Firma

```sql
CREATE PROCEDURE sps_ini_session_bei_duplicado(
  pEmpresa                     CHAR(3)
  pIdUsuario                   INTEGER
  pIp                          CHAR(15)
) RETURNING CHAR(5), CHAR (20), CHAR(50), SMALLINT, INTEGER, CHAR(19),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIp` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L7 |
| `sql_err` | `INTEGER` | L8 |
| `sNumCliente` | `CHAR(20)` | L10 |
| `iIdStatus` | `SMALLINT` | L11 |
| `fecPrimAcceso` | `DATE` | L12 |
| `fecUltAcceso` | `CHAR(19)` | L13 |
| `iIdStatusToken` | `INTEGER` | L14 |
| `sNombre` | `CHAR(150)` | L15 |
| `vFecha` | `DATETIME YEAR TO SECOND` | L16 |
| `vIdEmpresa` | `CHAR(3)` | L17 |
| `nomEmpresa` | `CHAR(50)` | L18 |
| `sCuenta` | `CHAR(20)` | L19 |
| `sIdTipoUsuario` | `SMALLINT` | L20 |
| `sFBloqueoTemp` | `DATETIME YEAR TO SECOND` | L21 |
| `token` | `VARCHAR(10)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L84 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L139 |
| `bei_usuario` | `bdibei` | no | UPDATE | L159 |
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L172 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L96 | VALIDACIÓN_NULL | `IF  sIdTipoUsuario  = 0 OR sIdTipoUsuario IS NULL THEN` |  |
| L105 | VALIDACIÓN_NULL | `IF fecUltAcceso IS NULL  THEN` |  |
| L111 | VALIDACIÓN_NULL | `IF iIdStatusToken = 0 OR iIdStatusToken IS NULL THEN` |  |
| L112 | FÓRMULA | `LET iIdStatusToken = -1;` |  |
| L115 | VALIDACIÓN_NULL | `IF sNombre = '' OR  sNombre IS NULL THEN` |  |
| L123 | VALIDACIÓN_NULL | `IF vFecha = '' OR vFecha IS NULL  THEN` |  |
| L127 | VALIDACIÓN_NULL | `IF sFBloqueoTemp = '' OR  sFBloqueoTemp IS NULL THEN` |  |
| L145 | VALIDACIÓN_NULL | `IF sFBloqueoTemp = '' OR sFBloqueoTemp IS NULL THEN` |  |
| L162 | VALIDACIÓN_NULL | `IF fecPrimAcceso IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `?_ini_se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_duplicado` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ini_se`, `ss`, `?ion_`, `?_duplicado` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_ini_session_bei_duplicado_mx2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_ini_session_bei_duplicado_mx2.sql` |
| **LOC (1er CREATE)** | 209 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 4 tabla(s) con operaciones: UPDATE, SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=5 / 8 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS PARA INICIAR LA SESION EN LA BANCA EMPRESARIAL |
| FECHA | 26/04/2013 |
| MODIFICACION | Se inicializa la variabe de fechabloqueo cuando el status es 95 o 10. |
| MODIFICACION | Berenice Noriega - BanCoppel. |
| FECHA | 23/Junio/2014 |
| MODIFICACION | Se optimiza |
| MODIFICACION | BanCoppel. |
| FECHA | 02/Octubre/2018 |
| DESCRIPCION | Optimizaci spl |
| FECHA | 13/02/2019 |

### Firma

```sql
CREATE PROCEDURE sps_ini_session_bei_duplicado_mx2(
  pEmpresa                     CHAR(3)
  pIdUsuario                   INTEGER
  pIp                          CHAR(15)
) RETURNING CHAR(5), CHAR (20), CHAR(50), SMALLINT, INTEGER, CHAR(19),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pIdUsuario` | `INTEGER` | — | — |
| `pIp` | `CHAR(15)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L7 |
| `sql_err` | `INTEGER` | L8 |
| `sNumCliente` | `CHAR(20)` | L10 |
| `iIdStatus` | `SMALLINT` | L11 |
| `fecPrimAcceso` | `DATE` | L12 |
| `fecUltAcceso` | `CHAR(19)` | L13 |
| `iIdStatusToken` | `INTEGER` | L14 |
| `sNombre` | `CHAR(150)` | L15 |
| `vFecha` | `DATETIME YEAR TO SECOND` | L16 |
| `vIdEmpresa` | `CHAR(3)` | L17 |
| `nomEmpresa` | `CHAR(50)` | L18 |
| `sCuenta` | `CHAR(20)` | L19 |
| `sIdTipoUsuario` | `SMALLINT` | L20 |
| `sFBloqueoTemp` | `DATETIME YEAR TO SECOND` | L21 |
| `token` | `VARCHAR(10)` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L84 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L139 |
| `bei_usuario` | `bdibei` | no | UPDATE | L159 |
| `sc_nominaempresas` | `bdicheq` | ⚠️ sí | SELECT | L172 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L96 | VALIDACIÓN_NULL | `IF  sIdTipoUsuario  = 0 OR sIdTipoUsuario IS NULL THEN` |  |
| L105 | VALIDACIÓN_NULL | `IF fecUltAcceso IS NULL  THEN` |  |
| L111 | VALIDACIÓN_NULL | `IF iIdStatusToken = 0 OR iIdStatusToken IS NULL THEN` |  |
| L112 | FÓRMULA | `LET iIdStatusToken = -1;` |  |
| L115 | VALIDACIÓN_NULL | `IF sNombre = '' OR  sNombre IS NULL THEN` |  |
| L123 | VALIDACIÓN_NULL | `IF vFecha = '' OR vFecha IS NULL  THEN` |  |
| L127 | VALIDACIÓN_NULL | `IF sFBloqueoTemp = '' OR  sFBloqueoTemp IS NULL THEN` |  |
| L145 | VALIDACIÓN_NULL | `IF sFBloqueoTemp = '' OR sFBloqueoTemp IS NULL THEN` |  |
| L162 | VALIDACIÓN_NULL | `IF fecPrimAcceso IS NULL THEN` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `?_ini_se` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `?ion_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?_duplicado_m` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_ini_se`, `ss`, `?ion_`, `?_duplicado_m`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_insertarusuario_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_insertarusuario_bei.sql` |
| **LOC (1er CREATE)** | 138 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "inserta usuario y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `inserta` → `INSERT` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Registra numero de cliente y el usuario |
| FECHA | 24/05/2013 |
| MODIFICACION | Permite sobreescribir los nombres de usuario que ya tienen registro y no son reset. |
| FECHA | 29-Septiembre-2015 |

### Firma

```sql
CREATE PROCEDURE sps_insertarusuario_bei(
  pNumCliente                  VARCHAR(9)
  pUsuario                     VARCHAR(50)
  pIdentAdmin                  CHAR(30)
  pIdUsuario                   INTEGER
) RETURNING CHAR (5),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `VARCHAR(9)` | — | — |
| `pUsuario` | `VARCHAR(50)` | `usuario`=usuario | ✅ CÓDIGO |
| `pIdentAdmin` | `CHAR(30)` | — | — |
| `pIdUsuario` | `INTEGER` | `usuario`=usuario | ✅ CÓDIGO |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `int` | L20 |
| `cCod_ret` | `CHAR (5)` | L21 |
| `vNumCliente` | `VARCHAR(9)` | L22 |
| `vUsuario` | `VARCHAR(50)` | L23 |
| `iUsuario` | `VARCHAR(50)` | L24 |
| `sIdUsuario` | `INTEGER` | L25 |
| `sIdStatus` | `SMALLINT` | L26 |
| `sNombre` | `CHAR(250)` | L27 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L66 |
| `bei_usuario` | `bdibei` | no | INSERT | L84 |
| `bei_datos_usuario` | `bdibei` | no | INSERT | L94 |
| `bei_usuario` | `bdibei` | no | SELECT | L101 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L107 |

### Reglas extraídas del código

| Línea | Tipo | Código | Riesgo equivalencia |
|-------|------|--------|---------------------|
| L98 | FÓRMULA | `LET iUsuario=iUsuario\|\|sIdUsuario; --RESETUSUARIO + idusuario` |  |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `inserta` | ACCION | inserta / registra | 🔵 CONVENCIÓN | nombre_sp |
| `?r` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `usuario` | ENTIDAD | usuario | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?r` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_montos_manc_aut_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_montos_manc_aut_bei.sql` |
| **LOC (1er CREATE)** | 103 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización monto y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS MONTOS PENDIENTES POR AUTORIZAR |
| FECHA | 08/01/2015 |
| FECHA | 22-ENERO-2015 |

### Firma

```sql
CREATE PROCEDURE sps_montos_manc_aut_bei(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING CHAR(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L16 |
| `sql_err` | `INTEGER` | L17 |
| `iTotalReg` | `INTEGER` | L18 |
| `sTipoOperacion` | `CHAR(2)` | L19 |
| `iIdMancomunidad` | `INTEGER` | L20 |
| `sUserNom` | `CHAR(50)` | L21 |
| `sIdUser` | `INTEGER` | L22 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L59 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `monto` | ENTIDAD | monto | 🔵 CONVENCIÓN | nombre_sp |
| `?s_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?s_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_obtener_datos_admin_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_obtener_datos_admin_bei.sql` |
| **LOC (1er CREATE)** | 47 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "obtiene datos, Administrador — rol de usuario con privilegios administrativos y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `obtiene` → `SELECT` encontrado en el cuerpo · `obtiene` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=3 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=0 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Obtiene el id de identificacion del administrador por usuario y numero de cliente |
| FECHA | 10/10/2013 |

### Firma

```sql
CREATE PROCEDURE sps_obtener_datos_admin_bei(
  pIdUsuario                   INTEGER
) RETURNING CHAR(5),CHAR(30),CHAR(9),INTEGER
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pIdUsuario` | `INTEGER` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `sql_err` | `INTEGER` | L4 |
| `cod_ret` | `CHAR (5)` | L5 |
| `id_admin` | `CHAR(30)` | L6 |
| `num_cliente` | `CHAR(9)` | L7 |
| `id_usuario` | `INTEGER` | L8 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_servicio` | `bdibei` | no | SELECT | L34 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `obtener` | ACCION | obtiene / recupera | 🔵 CONVENCIÓN | nombre_sp |
| `datos` | ENTIDAD | datos | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `admin` | ENTIDAD | Administrador — rol de usuario con privilegios administrativ | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

---

## `sps_token_manc_aut_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_token_manc_aut_bei.sql` |
| **LOC (1er CREATE)** | 125 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización token y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 2 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=2 · SINTÉTICO=1 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sps_token_manc_aut_bei(
  pNumCliente                  char(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
) RETURNING char(5),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `char(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L19 |
| `sql_err` | `INTEGER` | L20 |
| `iTotalReg` | `INTEGER` | L21 |
| `sNsToken` | `VARCHAR(10)` | L22 |
| `sIdStatusToken` | `INTEGER` | L23 |
| `sUserNomAdmin` | `CHAR(50)` | L24 |
| `sIdUserAdmin` | `INTEGER` | L25 |
| `sIdMancomunidad` | `INTEGER` | L26 |
| `sUserNom` | `CHAR(50)` | L28 |
| `sIdUser` | `INTEGER` | L29 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L74 |
| `bei_usuario` | `bdibei` | no | SELECT | L107 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `token` | ENTIDAD | token (autenticación) | 🔵 CONVENCIÓN | nombre_sp |
| `?_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_manc_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_user_manc_aut_bei2`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_user_manc_aut_bei2.sql` |
| **LOC (1er CREATE)** | 135 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "autorización BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=0 · INFERIDO=2 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | OBTIENE LOS DATOS DE LOS USUARIOS OPERADOR PENDIENTES DE AUTORIZAR MODIFICACION |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sps_user_manc_aut_bei2(
  pNumCliente                  CHAR(9)
  pIdUser                      integer
  pNoReg                       integer
  pRegIni                      integer
  pIdTipoMov                   SMALLINT
) RETURNING CHAR(5),   INTEGER,   INTEGER,    CHAR(9),    CHAR(50),     CHAR(150),     CHAR(15),
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pNumCliente` | `CHAR(9)` | — | — |
| `pIdUser` | `integer` | — | — |
| `pNoReg` | `integer` | — | — |
| `pRegIni` | `integer` | — | — |
| `pIdTipoMov` | `SMALLINT` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `CHAR(5)` | L11 |
| `sql_err` | `INTEGER` | L12 |
| `iTotalReg` | `INTEGER` | L14 |
| `sIdUsuario` | `INTEGER` | L16 |
| `sNumCliente` | `CHAR (9)` | L17 |
| `sIdAdminManco` | `INTEGER` | L18 |
| `sIdStatus` | `SMALLINT` | L20 |
| `sUserNom` | `CHAR(50)` | L21 |
| `sIdTipoUser` | `SMALLINT` | L22 |
| `sNombre` | `CHAR(150)` | L23 |
| `sTelCelular` | `CHAR(15)` | L24 |
| `sCiaCel` | `SMALLINT` | L25 |
| `sEmail` | `CHAR(50)` | L26 |
| `sUserNomAdmin` | `CHAR(50)` | L28 |
| `sIdUserAdmin` | `INTEGER` | L29 |
| `sIdUser` | `INTEGER` | L30 |
| `sTipoMov` | `INTEGER` | L32 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L76 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `?_user_manc_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `aut` | ACCION | autorización | 🟡 INFERIDO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |
| `?2` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?_user_manc_`, `?2` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_validapass_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_validapass_bei.sql` |
| **LOC (1er CREATE)** | 93 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "valida ss — subsistema y BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `valida` → `IF` encontrado en el cuerpo · `valida` → `RETURN` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=1 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 5 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | VObtiene informacion del cliente de BEI |
| FECHA | 08/07/2013 |
| MODIFICACION | Se cambia para que no consulte los telefonos en la tabla si_direcciones_actual sino |
| MODIFICACION | Berenice Noriega |
| FECHA | 23/Junio/2014 |

### Firma

```sql
CREATE PROCEDURE sps_validapass_bei(
  pEmpresa                     CHAR(3)
  pIdUsuario                   CHAR(20)
) RETURNING CHAR(5),CHAR(50),CHAR(5), CHAR(26), CHAR(13), CHAR(13), DATE, DATE
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pEmpresa` | `CHAR(3)` | — | — |
| `pIdUsuario` | `CHAR(20)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cCod_ret` | `CHAR(5)` | L7 |
| `sql_err` | `INTEGER` | L8 |
| `cNombre` | `CHAR(26)` | L10 |
| `cIdusuario` | `INTEGER` | L13 |
| `eNumCliente` | `char(10)` | L14 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_usuario` | `bdibei` | no | SELECT | L58 |
| `si_ctepm` | `bdinteg` | ⚠️ sí | SELECT | L78 |
| `bei_datos_usuario` | `bdibei` | no | SELECT | L85 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `valida` | ACCION | valida | 🔵 CONVENCIÓN | nombre_sp |
| `?pa` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `ss` | ENTIDAD | ss — subsistema / canal de monitoreo (abreviación — envia_mo | 🔴 SINTÉTICO | nombre_sp |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?pa`, `ss` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `sps_verificauser_exist_bei`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_sps_verificauser_exist_bei.sql` |
| **LOC (1er CREATE)** | 57 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "verifica BEI — Banca En Internet; canal digital principal de BanCoppel; base de datos bdibei con 279+ SPs de operaciones, autenticación, transferencias y mancomunidad" `[partial]` |
| **Propósito verificado** | ⚠️ PARCIAL — Accede a 1 tabla(s) con operaciones: SELECT |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=1 · INFERIDO=1 · SINTÉTICO=2 / 6 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Verifica si Nombre de Usuario ya Existe en la base de Datos |
| FECHA | 24/05/2013 |

### Firma

```sql
CREATE PROCEDURE sps_verificauser_exist_bei(
  pUserNom                     CHAR(50)
) RETURNING char(5), smallint
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pUserNom` | `CHAR(50)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `cod_ret` | `char(5)` | L5 |
| `sql_err` | `integer` | L6 |
| `sExiste` | `smallint` | L8 |
| `sExisteManco` | `smallint` | L9 |
| `sTotal` | `smallint` | L10 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_admin_manco_temp` | `bdibei` | no | SELECT | L47 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `sps` | PREFIJO | sps — prefijo alternativo de SP en bdibei (posiblemente 'sto | 🟡 INFERIDO | nombre_sp |
| `verifica` | ACCION | verifica | ✅ CÓDIGO | nombre_sp, texto_cuerpo |
| `?user_e` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `x` | MODIF | por (criterio) | 🔵 CONVENCIÓN | nombre_sp |
| `?ist_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `bei` | ENTIDAD | BEI — Banca En Internet; canal digital principal de BanCoppe | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?user_e`, `?ist_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---

## `trace_movimientos_cons`

| Campo | Valor |
|-------|-------|
| **Dominio** | D14 · `bdibei` · BEI |
| **Archivo fuente** | `bdibei_trace_movimientos_cons.sql` |
| **LOC (1er CREATE)** | 68 |
| **Callgraph** | ❌ SP aislado (no conectado en el grafo principal) |
| **Propósito inferido** | "consulta movimientos" `[partial]` |
| **Propósito verificado** | ✅ VERIFICADO — `consulta` → `SELECT` encontrado en el cuerpo · `consulta` → `FROM` encontrado en el cuerpo |
| **Evidencia vocab** | CÓDIGO=2 · CONVENCIÓN=0 · INFERIDO=0 · SINTÉTICO=1 / 3 términos |

### Historia del SP

| Tipo | Valor |
|------|-------|
| DESCRIPCION | Generar trazado de movimientos en archivo |
| FECHA | 13/Julio/2016 |
| FECHA | 29/Julio/2016 |
| FECHA | 04/Abril/2017 |
| MODIFICACION | Se modifica el ORDER de los queries de movimientos a solo fecha y hora, debido a un descuadre del ID SERIAL |
| MODIFICACION | Marco Tinajero - BanCoppel - Internet. |
| FECHA | 08 Diciembre 2022 |
| MODIFICACION | A solicitud de usuario, se reversa el cambio del 8 de Diciembre porque no resolv el problema de ordenamiento de raiz |
| MODIFICACION | Armando Barrientos - BanCoppel - Internet. |
| FECHA | 14 Diciembre 2022 |

### Firma

```sql
CREATE PROCEDURE trace_movimientos_cons(
  pFolio                       CHAR(30)
  pRuta                        CHAR(200)
) RETURNING CHAR(5) AS cod_ret
```

### Parámetros

| Nombre | Tipo | Vocab encontrado | Evidencia |
|--------|------|-----------------|-----------|
| `pFolio` | `CHAR(30)` | — | — |
| `pRuta` | `CHAR(200)` | — | — |

### Variables (DEFINE)

| Variable | Tipo | Línea |
|----------|------|-------|
| `vFile` | `CHAR(300)` | L19 |
| `vSql` | `CHAR(300)` | L20 |
| `vCommand` | `CHAR(900)` | L21 |
| `sql_err` | `INTEGER` | L23 |
| `vRegistos` | `INTEGER` | L24 |
| `cod_ret` | `CHAR(5)` | L25 |

### Tablas accedidas

| Tabla | Base de datos | Cross-DB | Operación | Línea |
|-------|--------------|----------|-----------|-------|
| `bei_movimientos_cons` | `bdibei` | no | SELECT | L49 |

### Vocabulario verificado contra el código

| Token | Categoría | Significado | Evidencia | Encontrado en |
|-------|-----------|-------------|-----------|---------------|
| `?trace_` | DESCONOCIDO | (desconocido — no en vocab) | 🔴 SINTÉTICO |  |
| `movimientos` | ENTIDAD | movimientos | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo, texto_cuerpo |
| `cons` | ACCION | consulta | ✅ CÓDIGO | nombre_sp, identificador_en_cuerpo |

> ⚠️ **Elementos sin grounding (SINTÉTICO):**
> Los tokens `?trace_` aparecen en el nombre del SP pero no tienen evidencia en el vocab ni en el cuerpo del código. Requieren validación SME antes de usarlos como base para decisions de diseño.

---
