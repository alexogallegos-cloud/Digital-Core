# === Terraform ===
# Provider plugins / modules cache (629 MB — jamás versionar)
**/.terraform/
# Estado e info de lock (ARNs, posibles datos sensibles)
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
crash.log
crash.*.log
# Variables (pueden contener secretos); el lock.hcl SÍ se versiona
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# (se conserva .terraform.lock.hcl)
!*.tfvars.example

# === Secretos / credenciales ===
.env
*.env
*.pem
*.key
*.pfx
id_rsa*
credentials
.aws/

# === Python (site/_build.py) ===
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# === OS / OneDrive ===
.DS_Store
Thumbs.db
desktop.ini
~$*

# === Editor ===
.vscode/
.idea/

# === Claude Code ===
.claude/worktrees/

# === Dev server (generado automáticamente) ===
_catalog.html

# === Archivos fuente grandes (BanCoppel / clientes) ===
# Logs de transacciones y errores — pueden ser cientos de MB
**/source/logs/
# ZIPs de entregables originales
**/source/*.zip
**/source/**/*.zip